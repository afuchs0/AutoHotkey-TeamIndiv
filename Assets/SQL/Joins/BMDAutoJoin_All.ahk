
getJoinTextAll(joinKuerzel)
{
searchResult := ""

;==========================================================================
;Hier hinzufügen falls als erster Join vorgeschlagen werden sollen!!!!!!!!!
;FALLS NICHT ZU BEGINN BITTE AM ENDE DES FILES HINZUFÜGEN!!!!!!!!!!!!!!!!!!
;==========================================================================

if joinKuerzel = DKZARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_DKZID = DKZ_DKZID" "~" 
if joinKuerzel = ARLDKZ
searchResult := searchResult "LEFT JOIN BUERO.DKZ_DOKUMENTZU ON DKZ_DKZID = ARL_DKZID" "~" 
if joinKuerzel = DKZDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_ARCHIVNR = DKZ_ARCHIVNR AND DOK_DOKUMENTENNR = DKZ_DOKUMENTENNR" "~" 
if joinKuerzel = DOKDKZ
searchResult := searchResult "LEFT JOIN BUERO.DKZ_DOKUMENTZU ON DKZ_ARCHIVNR = DOK_ARCHIVNR AND DKZ_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"

;==========================================================================
;ENDE ==
;==========================================================================

;Automatically generated: 10.07.2019 14:26:52!!!!!!!!!
if joinKuerzel = FIRINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_INR = FIR_FIRMENNR" "~"
if joinKuerzel = INSFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = INS_INR" "~"
if joinKuerzel = AAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AAZ_ERSTELLUSER" "~"
if joinKuerzel = BMUAAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.AAZ_AUFARBEITARTIKELZU ON AAZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABG_ERSTELLUSER" "~"
if joinKuerzel = BMUABG
searchResult := searchResult "LEFT JOIN HRS.ABG_ABRECHNUNG ON ABG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABM_ERSTELLUSER" "~"
if joinKuerzel = BMUABM
searchResult := searchResult "LEFT JOIN HRS.ABM_ABWESENHEIT ON ABM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABO_ERSTELLUSER" "~"
if joinKuerzel = BMUABO
searchResult := searchResult "LEFT JOIN BIL.ABO_ANBUBUCHUNGKONTEN ON ABO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AETBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AET_ERSTELLUSER" "~"
if joinKuerzel = BMUAET
searchResult := searchResult "LEFT JOIN BIL.AET_ANLAGEGUT_ERFASS_TMP ON AET_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AGIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AGI_ERSTELLUSER" "~"
if joinKuerzel = BMUAGI
searchResult := searchResult "LEFT JOIN WWS.AGI_AGPER_INFOZU ON AGI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AGMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AGM_ERSTELLUSER" "~"
if joinKuerzel = BMUAGM
searchResult := searchResult "LEFT JOIN PRODUKTION.AGM_AG_PERSON_ZU ON AGM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AGPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AGP_ERSTELLUSER" "~"
if joinKuerzel = BMUAGP
searchResult := searchResult "LEFT JOIN WWS.AGP_ARTIKELGRUPPENPOS ON AGP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AJTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AJT_ERSTELLUSER" "~"
if joinKuerzel = BMUAJT
searchResult := searchResult "LEFT JOIN BIL.AJT_ANBUJOURNAL_TMP ON AJT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AKKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AKK_ERSTELLUSER" "~"
if joinKuerzel = BMUAKK
searchResult := searchResult "LEFT JOIN BUERO.AKK_AKTIONSTATUSKOPF ON AKK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AKNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AKN_ERSTELLUSER" "~"
if joinKuerzel = BMUAKN
searchResult := searchResult "LEFT JOIN BIL.AKN_ANBU_KONSOL_AUSSCHLUSS ON AKN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AKRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AKR_ERSTELLUSER" "~"
if joinKuerzel = BMUAKR
searchResult := searchResult "LEFT JOIN BUERO.AKR_AVKOSTENARTENZU ON AKR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AMDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AMD_ERSTELLUSER" "~"
if joinKuerzel = BMUAMD
searchResult := searchResult "LEFT JOIN KORE.AMD_MODELLDETAIL ON AMD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AMUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AMU_ERSTELLUSER" "~"
if joinKuerzel = BMUAMU
searchResult := searchResult "LEFT JOIN BIL.AMU_ANLAGEGUT_MDE_ZU ON AMU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ANDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AND_ERSTELLUSER" "~"
if joinKuerzel = BMUAND
searchResult := searchResult "LEFT JOIN BIL.AND_ANBUNUTZUNGSDAUER ON AND_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ANHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ANH_ERSTELLUSER" "~"
if joinKuerzel = BMUANH
searchResult := searchResult "LEFT JOIN BUERO.ANH_ANGEHOERIGER ON ANH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ANSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ANS_ERSTELLUSER" "~"
if joinKuerzel = BMUANS
searchResult := searchResult "LEFT JOIN BIL.ANS_ANLAGENSCHEMA ON ANS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = APGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = APG_ERSTELLUSER" "~"
if joinKuerzel = BMUAPG
searchResult := searchResult "LEFT JOIN WWS.APG_ARAPRODUKTGRUPPEN ON APG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = APKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = APK_ERSTELLUSER" "~"
if joinKuerzel = BMUAPK
searchResult := searchResult "LEFT JOIN BMD.APK_AKTIONSPROTOKOLL ON APK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = APZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = APZ_ERSTELLUSER" "~"
if joinKuerzel = BMUAPZ
searchResult := searchResult "LEFT JOIN WWS.APZ_ARAPACKSTOFFTABZU ON APZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AUZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AUZ_ERSTELLUSER" "~"
if joinKuerzel = BMUAUZ
searchResult := searchResult "LEFT JOIN BIL.AUZ_AUSWERTUNGZUSATZ ON AUZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AWZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AWZ_ERSTELLUSER" "~"
if joinKuerzel = BMUAWZ
searchResult := searchResult "LEFT JOIN ZLE.AWZ_AUSWERTUNGZU ON AWZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AZDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AZD_ERSTELLUSER" "~"
if joinKuerzel = BMUAZD
searchResult := searchResult "LEFT JOIN BUERO.AZD_ARBEITSZEITMODELLDET ON AZD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BAK_ERSTELLUSER" "~"
if joinKuerzel = BMUBAK
searchResult := searchResult "LEFT JOIN BUERO.BAK_BANK ON BAK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BAUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BAU_ERSTELLUSER" "~"
if joinKuerzel = BMUBAU
searchResult := searchResult "LEFT JOIN BUERO.BAU_BAUM ON BAU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PERADR
searchResult := searchResult "LEFT JOIN BUERO.ADR_ADRESSE ON ADR_ADRESSLFDNR = PER_ADRESSLFDNR" "~"
if joinKuerzel = ADRPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = AALBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AAL_ERSTELLUSER" "~"
if joinKuerzel = BMUAAL
searchResult := searchResult "LEFT JOIN BIL.AAL_AUSDRUCKLAENDER ON AAL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABN_ERSTELLUSER" "~"
if joinKuerzel = BMUABN
searchResult := searchResult "LEFT JOIN HRS.ABN_ABWESENHEIT_TAG ON ABN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABV_ERSTELLUSER" "~"
if joinKuerzel = BMUABV
searchResult := searchResult "LEFT JOIN HRS.ABV_ABREVERGLEICH ON ABV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ACABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ACA_ERSTELLUSER" "~"
if joinKuerzel = BMUACA
searchResult := searchResult "LEFT JOIN FIBU.ACA_TESTFALL_AUSWERTUNG ON ACA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ACZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ACZ_ERSTELLUSER" "~"
if joinKuerzel = BMUACZ
searchResult := searchResult "LEFT JOIN BIL.ACZ_ANLAGEGUTSACHKONTOZU ON ACZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ADABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ADA_ERSTELLUSER" "~"
if joinKuerzel = BMUADA
searchResult := searchResult "LEFT JOIN BUERO.ADA_ADRESSART ON ADA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ADBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ADB_ERSTELLUSER" "~"
if joinKuerzel = BMUADB
searchResult := searchResult "LEFT JOIN WWS.ADB_ABGABENDETAILBON ON ADB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ADCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ADC_ERSTELLUSER" "~"
if joinKuerzel = BMUADC
searchResult := searchResult "LEFT JOIN WWS.ADC_ABGABENDETAILRECH ON ADC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ADDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ADD_ERSTELLUSER" "~"
if joinKuerzel = BMUADD
searchResult := searchResult "LEFT JOIN BUERO.ADD_AUSDRUCKMODELLDETAIL ON ADD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ADEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ADE_ERSTELLUSER" "~"
if joinKuerzel = BMUADE
searchResult := searchResult "LEFT JOIN WWS.ADE_ABGABENDETAILERFPOS ON ADE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ADPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ADP_ERSTELLUSER" "~"
if joinKuerzel = BMUADP
searchResult := searchResult "LEFT JOIN BIL.ADP_AUSDRUCKGESTALTUNG ON ADP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AEZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AEZ_ERSTELLUSER" "~"
if joinKuerzel = BMUAEZ
searchResult := searchResult "LEFT JOIN BUERO.AEZ_AUFGABENTICKETEMAILZU ON AEZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AFBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AFB_ERSTELLUSER" "~"
if joinKuerzel = BMUAFB
searchResult := searchResult "LEFT JOIN FIBU.AFB_ANBUFIB ON AFB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AFIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AFI_ERSTELLUSER" "~"
if joinKuerzel = BMUAFI
searchResult := searchResult "LEFT JOIN BMD.AFI_ABFRAGEFILTER ON AFI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AFPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AFP_ERSTELLUSER" "~"
if joinKuerzel = BMUAFP
searchResult := searchResult "LEFT JOIN ZLE.AFP_EXTFILTERPOS ON AFP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AINBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AIN_ERSTELLUSER" "~"
if joinKuerzel = BMUAIN
searchResult := searchResult "LEFT JOIN BUERO.AIN_AUSSCHREIBUNGINSERTZU ON AIN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AKABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AKA_ERSTELLUSER" "~"
if joinKuerzel = BMUAKA
searchResult := searchResult "LEFT JOIN BUERO.AKA_AVKOSTENARTEN ON AKA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AKSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AKS_ERSTELLUSER" "~"
if joinKuerzel = BMUAKS
searchResult := searchResult "LEFT JOIN BUERO.AKS_AKTIONSTATUS ON AKS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ALEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ALE_ERSTELLUSER" "~"
if joinKuerzel = BMUALE
searchResult := searchResult "LEFT JOIN BUERO.ALE_ADRESSENLISTE ON ALE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = APIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = API_ERSTELLUSER" "~"
if joinKuerzel = BMUAPI
searchResult := searchResult "LEFT JOIN BMDINTERN.API_ASPINFO ON API_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ARKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ARK_ERSTELLUSER" "~"
if joinKuerzel = BMUARK
searchResult := searchResult "LEFT JOIN WWS.ARK_ARTIKELKLASSE ON ARK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ARLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ARL_ERSTELLUSER" "~"
if joinKuerzel = BMUARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ARPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ARP_ERSTELLUSER" "~"
if joinKuerzel = BMUARP
searchResult := searchResult "LEFT JOIN BUERO.ARP_ARBEITSZEITGESETZ_PAU ON ARP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ASJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ASJ_ERSTELLUSER" "~"
if joinKuerzel = BMUASJ
searchResult := searchResult "LEFT JOIN BMD.ASJ_ASYNCHRONEJOBS ON ASJ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ASOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ASO_ERSTELLUSER" "~"
if joinKuerzel = BMUASO
searchResult := searchResult "LEFT JOIN ZLE.ASO_SORTIERPOS ON ASO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AAMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AAM_ERSTELLUSER" "~"
if joinKuerzel = BMUAAM
searchResult := searchResult "LEFT JOIN BIL.AAM_AUSDRUCKMASTER ON AAM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AARBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AAR_ERSTELLUSER" "~"
if joinKuerzel = BMUAAR
searchResult := searchResult "LEFT JOIN WWS.AAR_ALTERNATIVARTIKEL ON AAR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABU_ERSTELLUSER" "~"
if joinKuerzel = BMUABU
searchResult := searchResult "LEFT JOIN BIL.ABU_ANLAGENBUCHHALTUNG ON ABU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ACSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ACS_ERSTELLUSER" "~"
if joinKuerzel = BMUACS
searchResult := searchResult "LEFT JOIN WWS.ACS_TEST ON ACS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ADRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ADR_ERSTELLUSER" "~"
if joinKuerzel = BMUADR
searchResult := searchResult "LEFT JOIN BUERO.ADR_ADRESSE ON ADR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ADWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ADW_ERSTELLUSER" "~"
if joinKuerzel = BMUADW
searchResult := searchResult "LEFT JOIN WWS.ADW_ABGABENDETAILWAP ON ADW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AITBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AIT_ERSTELLUSER" "~"
if joinKuerzel = BMUAIT
searchResult := searchResult "LEFT JOIN BIL.AIT_ANBU_IDS_TMP ON AIT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ALGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ALG_ERSTELLUSER" "~"
if joinKuerzel = BMUALG
searchResult := searchResult "LEFT JOIN BIL.ALG_ANLAGENGRUPPE ON ALG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = APCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = APC_ERSTELLUSER" "~"
if joinKuerzel = BMUAPC
searchResult := searchResult "LEFT JOIN BIL.APC_ANBUPRODUKTIONSCODES ON APC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ARRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ARR_ERSTELLUSER" "~"
if joinKuerzel = BMUARR
searchResult := searchResult "LEFT JOIN PRODUKTION.ARR_PROD_ARTIKEL_REPORT ON ARR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ASBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ASB_ERSTELLUSER" "~"
if joinKuerzel = BMUASB
searchResult := searchResult "LEFT JOIN ZLE.ASB_AUSWERT_SUB ON ASB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AUCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AUC_ERSTELLUSER" "~"
if joinKuerzel = BMUAUC
searchResult := searchResult "LEFT JOIN BMD.AUC_AUSWERTUNGSCENTER ON AUC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AVGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AVG_ERSTELLUSER" "~"
if joinKuerzel = BMUAVG
searchResult := searchResult "LEFT JOIN WWS.AVG_AUFTRAGSVORERFGRUPP ON AVG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AVTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AVT_ERSTELLUSER" "~"
if joinKuerzel = BMUAVT
searchResult := searchResult "LEFT JOIN BUERO.AVT_AVIMPORTTABELLE ON AVT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AZKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AZK_ERSTELLUSER" "~"
if joinKuerzel = BMUAZK
searchResult := searchResult "LEFT JOIN BIL.AZK_ANBUZUSCHUSSKENNZEICH ON AZK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BALBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BAL_ERSTELLUSER" "~"
if joinKuerzel = BMUBAL
searchResult := searchResult "LEFT JOIN PRODUKTION.BAL_ARBEITSPLATZLAGERZU ON BAL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BATBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BAT_ERSTELLUSER" "~"
if joinKuerzel = BMUBAT
searchResult := searchResult "LEFT JOIN BMD.BAT_BMDATTACHMENT ON BAT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BEFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BEF_ERSTELLUSER" "~"
if joinKuerzel = BMUBEF
searchResult := searchResult "LEFT JOIN BUERO.BEF_BEFRAGUNG ON BEF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BEMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BEM_ERSTELLUSER" "~"
if joinKuerzel = BMUBEM
searchResult := searchResult "LEFT JOIN BMD.BEM_BMDEMAIL ON BEM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BEWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BEW_ERSTELLUSER" "~"
if joinKuerzel = BMUBEW
searchResult := searchResult "LEFT JOIN BUERO.BEW_BEWERTUNGSMODELL ON BEW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BEXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BEX_ERSTELLUSER" "~"
if joinKuerzel = BMUBEX
searchResult := searchResult "LEFT JOIN FIBU.BEX_BUDGETEXT ON BEX_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BFIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BFI_ERSTELLUSER" "~"
if joinKuerzel = BMUBFI
searchResult := searchResult "LEFT JOIN HRS.BFI_BUDGETFIRMA ON BFI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BFTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BFT_ERSTELLUSER" "~"
if joinKuerzel = BMUBFT
searchResult := searchResult "LEFT JOIN BUERO.BFT_BERUFSTITEL ON BFT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BGSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BGS_ERSTELLUSER" "~"
if joinKuerzel = BMUBGS
searchResult := searchResult "LEFT JOIN HRS.BGS_BEITRAGSGRUPPE_SEQ ON BGS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BKEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BKE_ERSTELLUSER" "~"
if joinKuerzel = BMUBKE
searchResult := searchResult "LEFT JOIN FIBU.BKE_BANKRVKOREERF ON BKE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BLBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BLB_ERSTELLUSER" "~"
if joinKuerzel = BMUBLB
searchResult := searchResult "LEFT JOIN BMD.BLB_BLOB ON BLB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DIPPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = DIP_FIRMENNR AND PER_PERSONENID = DIP_EXTPERSID" "~"
if joinKuerzel = PERDIP
searchResult := searchResult "LEFT JOIN BUERO.DIP_DIVERSE_PERSON ON DIP_FIRMENNR = PER_FIRMENNR AND DIP_EXTPERSID = PER_PERSONENID" "~"
if joinKuerzel = ABDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABD_ERSTELLUSER" "~"
if joinKuerzel = BMUABD
searchResult := searchResult "LEFT JOIN HRS.ABD_ABRECHNUNGSDETAIL ON ABD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ACEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ACE_ERSTELLUSER" "~"
if joinKuerzel = BMUACE
searchResult := searchResult "LEFT JOIN FIBU.ACE_EXTTESTFALL ON ACE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ADOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ADO_ERSTELLUSER" "~"
if joinKuerzel = BMUADO
searchResult := searchResult "LEFT JOIN BUERO.ADO_AUSDRUCKMODELL ON ADO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ADZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ADZ_ERSTELLUSER" "~"
if joinKuerzel = BMUADZ
searchResult := searchResult "LEFT JOIN WWS.ADZ_ARTIKELDIENSTLNRZU ON ADZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AENBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AEN_ERSTELLUSER" "~"
if joinKuerzel = BMUAEN
searchResult := searchResult "LEFT JOIN BUERO.AEN_AENDERUNGCRM ON AEN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AFKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AFK_ERSTELLUSER" "~"
if joinKuerzel = BMUAFK
searchResult := searchResult "LEFT JOIN ZLE.AFK_EXTFILTERKOPF ON AFK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AGABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AGA_ERSTELLUSER" "~"
if joinKuerzel = BMUAGA
searchResult := searchResult "LEFT JOIN BUERO.AGA_ABGABENART ON AGA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AGLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AGL_ERSTELLUSER" "~"
if joinKuerzel = BMUAGL
searchResult := searchResult "LEFT JOIN PRODUKTION.AGL_ARBEITSGANGZU ON AGL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AI1BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AI1_ERSTELLUSER" "~"
if joinKuerzel = BMUAI1
searchResult := searchResult "LEFT JOIN WWS.AI1_VA_ARTIKELIDENT ON AI1_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AIDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AID_ERSTELLUSER" "~"
if joinKuerzel = BMUAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AIZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AIZ_ERSTELLUSER" "~"
if joinKuerzel = BMUAIZ
searchResult := searchResult "LEFT JOIN PRODUKTION.AIZ_AGPER_INFOZU ON AIZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AKTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AKT_ERSTELLUSER" "~"
if joinKuerzel = BMUAKT
searchResult := searchResult "LEFT JOIN WWS.AKT_AKTIONSSTAMM ON AKT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AKUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AKU_ERSTELLUSER" "~"
if joinKuerzel = BMUAKU
searchResult := searchResult "LEFT JOIN ZLE.AKU_ARTIKELKUNDEZULEA ON AKU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ALIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ALI_ERSTELLUSER" "~"
if joinKuerzel = BMUALI
searchResult := searchResult "LEFT JOIN BMDDEFAULT.ALI_ATTRGRUPPELIZENZZU ON ALI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ALKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ALK_ERSTELLUSER" "~"
if joinKuerzel = BMUALK
searchResult := searchResult "LEFT JOIN PRODUKTION.ALK_ARBEITSPLAN_KOSTEN ON ALK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ALSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ALS_ERSTELLUSER" "~"
if joinKuerzel = BMUALS
searchResult := searchResult "LEFT JOIN WWS.ALS_ALLERGEN ON ALS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ALTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ALT_ERSTELLUSER" "~"
if joinKuerzel = BMUALT
searchResult := searchResult "LEFT JOIN ZLE.ALT_ARCHIVLEISTUNGEN ON ALT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ALZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ALZ_ERSTELLUSER" "~"
if joinKuerzel = BMUALZ
searchResult := searchResult "LEFT JOIN WWS.ALZ_ALLERGENDATEN ON ALZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AMGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AMG_ERSTELLUSER" "~"
if joinKuerzel = BMUAMG
searchResult := searchResult "LEFT JOIN BUERO.AMG_GKKABMELDEGRUND ON AMG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ANOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ANO_ERSTELLUSER" "~"
if joinKuerzel = BMUANO
searchResult := searchResult "LEFT JOIN WWS.ANO_ARTIKELNORM ON ANO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ANTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ANT_ERSTELLUSER" "~"
if joinKuerzel = BMUANT
searchResult := searchResult "LEFT JOIN BUERO.ANT_ANTWORT ON ANT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AROBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ARO_ERSTELLUSER" "~"
if joinKuerzel = BMUARO
searchResult := searchResult "LEFT JOIN WWS.ARO_APS_KOSTEN ON ARO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ASKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ASK_ERSTELLUSER" "~"
if joinKuerzel = BMUASK
searchResult := searchResult "LEFT JOIN ZLE.ASK_SORTIERKOPF ON ASK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ASUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ASU_ERSTELLUSER" "~"
if joinKuerzel = BMUASU
searchResult := searchResult "LEFT JOIN BIL.ASU_ANLAGENSUMMEN ON ASU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ASWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ASW_ERSTELLUSER" "~"
if joinKuerzel = BMUASW
searchResult := searchResult "LEFT JOIN BUERO.ASW_AUSWEIS ON ASW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ARLVWH
searchResult := searchResult "LEFT JOIN WWS.VWH_VIEW_WARENHAUPTGRUPPE ON VWH_FIRMENNR = ARL_FIRMENNR AND VWH_WARENHAUPTGRNR = ARL_WARENHAUPTGRNR" "~"
if joinKuerzel = VWHARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = VWH_FIRMENNR AND ARL_WARENHAUPTGRNR = VWH_WARENHAUPTGRNR" "~"
if joinKuerzel = AACBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AAC_ERSTELLUSER" "~"
if joinKuerzel = BMUAAC
searchResult := searchResult "LEFT JOIN BIL.AAC_ANBU_ATTRSEL_CHECK ON AAC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABJ_ERSTELLUSER" "~"
if joinKuerzel = BMUABJ
searchResult := searchResult "LEFT JOIN BIL.ABJ_ANBUBEWRESJOURNAL ON ABJ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABQBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABQ_ERSTELLUSER" "~"
if joinKuerzel = BMUABQ
searchResult := searchResult "LEFT JOIN HRS.ABQ_ARBEITSSTAETTE_SEQ ON ABQ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABS_ERSTELLUSER" "~"
if joinKuerzel = BMUABS
searchResult := searchResult "LEFT JOIN HRS.ABS_ARBEITSSTAETTE ON ABS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AFSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AFS_ERSTELLUSER" "~"
if joinKuerzel = BMUAFS
searchResult := searchResult "LEFT JOIN BUERO.AFS_AUFGABENSTATUS ON AFS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AFZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AFZ_ERSTELLUSER" "~"
if joinKuerzel = BMUAFZ
searchResult := searchResult "LEFT JOIN BMD.AFZ_ABFRAGEFELDERZU ON AFZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AGTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AGT_ERSTELLUSER" "~"
if joinKuerzel = BMUAGT
searchResult := searchResult "LEFT JOIN BIL.AGT_ANLAGEGUT_TMP ON AGT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AIVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AIV_ERSTELLUSER" "~"
if joinKuerzel = BMUAIV
searchResult := searchResult "LEFT JOIN WWS.AIV_INTERVALL_ARBEITSPLAN ON AIV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ALOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ALO_ERSTELLUSER" "~"
if joinKuerzel = BMUALO
searchResult := searchResult "LEFT JOIN HRS.ALO_ARTIKELLOHNARTZU ON ALO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AMOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AMO_ERSTELLUSER" "~"
if joinKuerzel = BMUAMO
searchResult := searchResult "LEFT JOIN KORE.AMO_AUSWERTUNGMODELL ON AMO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AOZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AOZ_ERSTELLUSER" "~"
if joinKuerzel = BMUAOZ
searchResult := searchResult "LEFT JOIN WWS.AOZ_ARTIKELDOKUMENTZU ON AOZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = APSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = APS_ERSTELLUSER" "~"
if joinKuerzel = BMUAPS
searchResult := searchResult "LEFT JOIN WWS.APS_ARBEITSPLAN_STAMM ON APS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AQBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AQB_ERSTELLUSER" "~"
if joinKuerzel = BMUAQB
searchResult := searchResult "LEFT JOIN WWS.AQB_ARAQUOTENBLATT ON AQB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ARBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ARB_ERSTELLUSER" "~"
if joinKuerzel = BMUARB
searchResult := searchResult "LEFT JOIN WWS.ARB_ARTIKELBEREICHSWERTE ON ARB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ARGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ARG_ERSTELLUSER" "~"
if joinKuerzel = BMUARG
searchResult := searchResult "LEFT JOIN BUERO.ARG_ARBEITSRUHEZEITGESETZ ON ARG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ARSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ARS_ERSTELLUSER" "~"
if joinKuerzel = BMUARS
searchResult := searchResult "LEFT JOIN WWS.ARS_ARTIKELSTATISTIK ON ARS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ARZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ARZ_ERSTELLUSER" "~"
if joinKuerzel = BMUARZ
searchResult := searchResult "LEFT JOIN BIL.ARZ_ANLAGEGUTGRUPPEZU ON ARZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ASMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ASM_ERSTELLUSER" "~"
if joinKuerzel = BMUASM
searchResult := searchResult "LEFT JOIN BUERO.ASM_AUSBILDUNGSSTAMMMERKM ON ASM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ATFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ATF_ERSTELLUSER" "~"
if joinKuerzel = BMUATF
searchResult := searchResult "LEFT JOIN BUERO.ATF_ABTEILUNGFIRMENZU ON ATF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AVBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AVB_ERSTELLUSER" "~"
if joinKuerzel = BMUAVB
searchResult := searchResult "LEFT JOIN BUERO.AVB_AVBUDGET ON AVB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AVHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AVH_ERSTELLUSER" "~"
if joinKuerzel = BMUAVH
searchResult := searchResult "LEFT JOIN BUERO.AVH_AVIMPORTHEADER ON AVH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AWCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AWC_ERSTELLUSER" "~"
if joinKuerzel = BMUAWC
searchResult := searchResult "LEFT JOIN ZLE.AWC_SPALTENAUSWAHL ON AWC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AZGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AZG_ERSTELLUSER" "~"
if joinKuerzel = BMUAZG
searchResult := searchResult "LEFT JOIN BUERO.AZG_ABZUG ON AZG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AZMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AZM_ERSTELLUSER" "~"
if joinKuerzel = BMUAZM
searchResult := searchResult "LEFT JOIN BUERO.AZM_ARBEITSZEITMODELL ON AZM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BCGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BCG_ERSTELLUSER" "~"
if joinKuerzel = BMUBCG
searchResult := searchResult "LEFT JOIN BUERO.BCG_BMDCOMUSERGRUPPENZU ON BCG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ARLVLA
searchResult := searchResult "LEFT JOIN WWS.VLA_VIEW_HAUPTLIEFERARTIKEL ON VLA_ARL_FIRMENNR = ARL_FIRMENNR AND VLA_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = VLAARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = VLA_ARL_FIRMENNR AND ARL_ARTIKELNR = VLA_ARL_ARTIKELNR" "~"
if joinKuerzel = AAPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AAP_ERSTELLUSER" "~"
if joinKuerzel = BMUAAP
searchResult := searchResult "LEFT JOIN WWS.AAP_ARAPOSITION ON AAP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABK_ERSTELLUSER" "~"
if joinKuerzel = BMUABK
searchResult := searchResult "LEFT JOIN BUERO.ABK_ARCHIVBAUMKOPF ON ABK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABL_ERSTELLUSER" "~"
if joinKuerzel = BMUABL
searchResult := searchResult "LEFT JOIN BUERO.ABL_AVBUDGETDETAIL ON ABL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABR_ERSTELLUSER" "~"
if joinKuerzel = BMUABR
searchResult := searchResult "LEFT JOIN BIL.ABR_ANBUBEWRES ON ABR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ACTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ACT_ERSTELLUSER" "~"
if joinKuerzel = BMUACT
searchResult := searchResult "LEFT JOIN BIL.ACT_ANLAGEGUT_BERECH_TMP ON ACT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AFTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AFT_ERSTELLUSER" "~"
if joinKuerzel = BMUAFT
searchResult := searchResult "LEFT JOIN ZLE.AFT_AUFBEREITUNG ON AFT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AGRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AGR_ERSTELLUSER" "~"
if joinKuerzel = BMUAGR
searchResult := searchResult "LEFT JOIN BUERO.AGR_ABRECHNUNGSGRUPPE ON AGR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AGUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AGU_ERSTELLUSER" "~"
if joinKuerzel = BMUAGU
searchResult := searchResult "LEFT JOIN BIL.AGU_ANLAGEGUT ON AGU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AKVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AKV_ERSTELLUSER" "~"
if joinKuerzel = BMUAKV
searchResult := searchResult "LEFT JOIN FIBU.AKV_AUTOKONTONR_VERGABE ON AKV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ALNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ALN_ERSTELLUSER" "~"
if joinKuerzel = BMUALN
searchResult := searchResult "LEFT JOIN PRODUKTION.ALN_ALIBINUMMERN ON ALN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ANBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ANB_ERSTELLUSER" "~"
if joinKuerzel = BMUANB
searchResult := searchResult "LEFT JOIN BIL.ANB_ANBUBUCHBELEGZU ON ANB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ANZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ANZ_ERSTELLUSER" "~"
if joinKuerzel = BMUANZ
searchResult := searchResult "LEFT JOIN BIL.ANZ_ANLAGEGUTKOSTZU ON ANZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = APLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = APL_ERSTELLUSER" "~"
if joinKuerzel = BMUAPL
searchResult := searchResult "LEFT JOIN WWS.APL_ARBEITSPLANLINIEZU ON APL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = APRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = APR_ERSTELLUSER" "~"
if joinKuerzel = BMUAPR
searchResult := searchResult "LEFT JOIN BUERO.APR_ARCHIVPROTOKOLL ON APR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ARTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ART_ERSTELLUSER" "~"
if joinKuerzel = BMUART
searchResult := searchResult "LEFT JOIN BIL.ART_ANBUBEWRES_TMP ON ART_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ARUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ARU_ERSTELLUSER" "~"
if joinKuerzel = BMUARU
searchResult := searchResult "LEFT JOIN ZLE.ARU_ARTIKELUMLAGE ON ARU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ASFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ASF_ERSTELLUSER" "~"
if joinKuerzel = BMUASF
searchResult := searchResult "LEFT JOIN BMD.ASF_AUTOSTARTFUNKTION ON ASF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ASHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ASH_ERSTELLUSER" "~"
if joinKuerzel = BMUASH
searchResult := searchResult "LEFT JOIN BUERO.ASH_AUSWEISHIST ON ASH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ASRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ASR_ERSTELLUSER" "~"
if joinKuerzel = BMUASR
searchResult := searchResult "LEFT JOIN BUERO.ASR_AUSSCHREIBUNG ON ASR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ASTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AST_ERSTELLUSER" "~"
if joinKuerzel = BMUAST
searchResult := searchResult "LEFT JOIN WWS.AST_ARTIKELSTATISTIK ON AST_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ASZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ASZ_ERSTELLUSER" "~"
if joinKuerzel = BMUASZ
searchResult := searchResult "LEFT JOIN BUERO.ASZ_AUSSCHRFILIALEZU ON ASZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ATLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ATL_ERSTELLUSER" "~"
if joinKuerzel = BMUATL
searchResult := searchResult "LEFT JOIN BMD.ATL_ATTRLOCK ON ATL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AUWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AUW_ERSTELLUSER" "~"
if joinKuerzel = BMUAUW
searchResult := searchResult "LEFT JOIN BUERO.AUW_AUSWAHL ON AUW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AVSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AVS_ERSTELLUSER" "~"
if joinKuerzel = BMUAVS
searchResult := searchResult "LEFT JOIN BUERO.AVS_AVSTATUS ON AVS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AWEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AWE_ERSTELLUSER" "~"
if joinKuerzel = BMUAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AUGPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_PERSONENID = AUG_PERSONENID AND PER_FIRMENNR = AUG_PER_FIRMENNR" "~"
if joinKuerzel = PERAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_PERSONENID = PER_PERSONENID AND AUG_PER_FIRMENNR = PER_FIRMENNR" "~"
if joinKuerzel = MITPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = MIT_FIRMENNR AND PER_PERSONENID = MIT_MITARBEITERID" "~"
if joinKuerzel = PERMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = PER_FIRMENNR AND MIT_MITARBEITERID = PER_PERSONENID" "~"
if joinKuerzel = AASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AAS_ERSTELLUSER" "~"
if joinKuerzel = BMUAAS
searchResult := searchResult "LEFT JOIN BUERO.AAS_ANTWORTAUSWAHLSET ON AAS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ACFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ACF_ERSTELLUSER" "~"
if joinKuerzel = BMUACF
searchResult := searchResult "LEFT JOIN FIBU.ACF_TESTFALL ON ACF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ADKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ADK_ERSTELLUSER" "~"
if joinKuerzel = BMUADK
searchResult := searchResult "LEFT JOIN BUERO.ADK_ADRESSENLISTEKOPF ON ADK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ADLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ADL_ERSTELLUSER" "~"
if joinKuerzel = BMUADL
searchResult := searchResult "LEFT JOIN BUERO.ADL_ADLCODE ON ADL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AEEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AEE_ERSTELLUSER" "~"
if joinKuerzel = BMUAEE
searchResult := searchResult "LEFT JOIN BUERO.AEE_AUSDRUCKEMAILEMPF ON AEE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AGSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AGS_ERSTELLUSER" "~"
if joinKuerzel = BMUAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AKOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AKO_ERSTELLUSER" "~"
if joinKuerzel = BMUAKO
searchResult := searchResult "LEFT JOIN BMD.AKO_AGGREGATKOPF ON AKO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AKZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AKZ_ERSTELLUSER" "~"
if joinKuerzel = BMUAKZ
searchResult := searchResult "LEFT JOIN WWS.AKZ_ARTIKELKUNDENZU ON AKZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AMPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AMP_ERSTELLUSER" "~"
if joinKuerzel = BMUAMP
searchResult := searchResult "LEFT JOIN BUERO.AMP_AUTOMAILARCHIVPARAM ON AMP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = APOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = APO_ERSTELLUSER" "~"
if joinKuerzel = BMUAPO
searchResult := searchResult "LEFT JOIN BMD.APO_AGGREGATPOSITION ON APO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AUDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AUD_ERSTELLUSER" "~"
if joinKuerzel = BMUAUD
searchResult := searchResult "LEFT JOIN BUERO.AUD_AUFGABEDOKUMENTZU ON AUD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AUEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AUE_ERSTELLUSER" "~"
if joinKuerzel = BMUAUE
searchResult := searchResult "LEFT JOIN ZLE.AUE_AUSWERTTYPEINST ON AUE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AUGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AUG_ERSTELLUSER" "~"
if joinKuerzel = BMUAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AULBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AUL_ERSTELLUSER" "~"
if joinKuerzel = BMUAUL
searchResult := searchResult "LEFT JOIN WWS.AUL_ARTIKELUMSTELLLISTE ON AUL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AXZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AXZ_ERSTELLUSER" "~"
if joinKuerzel = BMUAXZ
searchResult := searchResult "LEFT JOIN BMDENTW.AXZ_ATTREXTZU ON AXZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AZBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AZB_ERSTELLUSER" "~"
if joinKuerzel = BMUAZB
searchResult := searchResult "LEFT JOIN BUERO.AZB_ARBEITSZEITBEZ ON AZB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AZNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AZN_ERSTELLUSER" "~"
if joinKuerzel = BMUAZN
searchResult := searchResult "LEFT JOIN BUERO.AZN_AZNACHRICHTEN ON AZN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AZUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AZU_ERSTELLUSER" "~"
if joinKuerzel = BMUAZU
searchResult := searchResult "LEFT JOIN WWS.AZU_STATISTIKARTIKELZU ON AZU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AZZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AZZ_ERSTELLUSER" "~"
if joinKuerzel = BMUAZZ
searchResult := searchResult "LEFT JOIN FIBU.AZZ_AUSDRUCKZUSATZ ON AZZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BAIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BAI_ERSTELLUSER" "~"
if joinKuerzel = BMUBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BAZ_ERSTELLUSER" "~"
if joinKuerzel = BMUBAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BDZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BDZ_ERSTELLUSER" "~"
if joinKuerzel = BMUBDZ
searchResult := searchResult "LEFT JOIN BUERO.BDZ_BEWERBUNGDOKUMENTZU ON BDZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BEKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BEK_ERSTELLUSER" "~"
if joinKuerzel = BMUBEK
searchResult := searchResult "LEFT JOIN BUERO.BEK_BERECHTKATEGORIE ON BEK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BIPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BIP_ERSTELLUSER" "~"
if joinKuerzel = BMUBIP
searchResult := searchResult "LEFT JOIN FIBU.BIP_BUDGETINVESTPLAN ON BIP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KLIPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = KLI_FIRMENNR AND PER_PERSONENID = KLI_KLID" "~"
if joinKuerzel = PERKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = PER_FIRMENNR AND KLI_KLID = PER_PERSONENID" "~"
if joinKuerzel = BAZARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = BAZ_ARL_FIRMENNR AND ARL_ARTIKELNR = BAZ_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLBAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_ARL_FIRMENNR = ARL_FIRMENNR AND BAZ_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = AAABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AAA_ERSTELLUSER" "~"
if joinKuerzel = BMUAAA
searchResult := searchResult "LEFT JOIN BUERO.AAA_AUSDRUCKABLAUFART ON AAA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AAD_ERSTELLUSER" "~"
if joinKuerzel = BMUAAD
searchResult := searchResult "LEFT JOIN BIL.AAD_AUSDRUCKDETAIL ON AAD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AAEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AAE_ERSTELLUSER" "~"
if joinKuerzel = BMUAAE
searchResult := searchResult "LEFT JOIN WWS.AAE_ANGEBOTSAUSW_ERFQ_TMP ON AAE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AAK_ERSTELLUSER" "~"
if joinKuerzel = BMUAAK
searchResult := searchResult "LEFT JOIN WWS.AAK_ARAKOPF ON AAK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABF_ERSTELLUSER" "~"
if joinKuerzel = BMUABF
searchResult := searchResult "LEFT JOIN BMD.ABF_ABFRAGE ON ABF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABW_ERSTELLUSER" "~"
if joinKuerzel = BMUABW
searchResult := searchResult "LEFT JOIN BUERO.ABW_BEWERBERAUFGABEN ON ABW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABZ_ERSTELLUSER" "~"
if joinKuerzel = BMUABZ
searchResult := searchResult "LEFT JOIN BUERO.ABZ_ARCHIVBAUMKOPFZU ON ABZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AGBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AGB_ERSTELLUSER" "~"
if joinKuerzel = BMUAGB
searchResult := searchResult "LEFT JOIN WWS.AGB_ARBEITSGANGBAZZU ON AGB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AGNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AGN_ERSTELLUSER" "~"
if joinKuerzel = BMUAGN
searchResult := searchResult "LEFT JOIN WWS.AGN_AG_PERSON_ZU ON AGN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AGZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AGZ_ERSTELLUSER" "~"
if joinKuerzel = BMUAGZ
searchResult := searchResult "LEFT JOIN WWS.AGZ_ARBEITSGANGZU ON AGZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AJOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AJO_ERSTELLUSER" "~"
if joinKuerzel = BMUAJO
searchResult := searchResult "LEFT JOIN BIL.AJO_ANBUJOURNAL ON AJO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AKLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AKL_ERSTELLUSER" "~"
if joinKuerzel = BMUAKL
searchResult := searchResult "LEFT JOIN WWS.AKL_ARTIKELKLASSIFIKATION ON AKL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AMZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AMZ_ERSTELLUSER" "~"
if joinKuerzel = BMUAMZ
searchResult := searchResult "LEFT JOIN BUERO.AMZ_AUSDRUCKMODELLZU ON AMZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ANRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ANR_ERSTELLUSER" "~"
if joinKuerzel = BMUANR
searchResult := searchResult "LEFT JOIN BUERO.ANR_ANREDE ON ANR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = APABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = APA_ERSTELLUSER" "~"
if joinKuerzel = BMUAPA
searchResult := searchResult "LEFT JOIN PRODUKTION.APA_ARBEITSPLAN ON APA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = APPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = APP_ERSTELLUSER" "~"
if joinKuerzel = BMUAPP
searchResult := searchResult "LEFT JOIN BMDINTERN.APP_ASPPWD ON APP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ARMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ARM_ERSTELLUSER" "~"
if joinKuerzel = BMUARM
searchResult := searchResult "LEFT JOIN BUERO.ARM_ARBEITSZEITGESETZ_MAX ON ARM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ASCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ASC_ERSTELLUSER" "~"
if joinKuerzel = BMUASC
searchResult := searchResult "LEFT JOIN ZLE.ASC_ARBEITSSCHEIN ON ASC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ASPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ASP_ERSTELLUSER" "~"
if joinKuerzel = BMUASP
searchResult := searchResult "LEFT JOIN BIL.ASP_SPALTENKOPF ON ASP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AUBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AUB_ERSTELLUSER" "~"
if joinKuerzel = BMUAUB
searchResult := searchResult "LEFT JOIN BIL.AUB_KIND ON AUB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AVDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AVD_ERSTELLUSER" "~"
if joinKuerzel = BMUAVD
searchResult := searchResult "LEFT JOIN BUERO.AVD_AUSBVERWDOKUMENTZU ON AVD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AVEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AVE_ERSTELLUSER" "~"
if joinKuerzel = BMUAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AWKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AWK_ERSTELLUSER" "~"
if joinKuerzel = BMUAWK
searchResult := searchResult "LEFT JOIN BIL.AWK_AUSWERTUNGKOPF ON AWK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AZRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AZR_ERSTELLUSER" "~"
if joinKuerzel = BMUAZR
searchResult := searchResult "LEFT JOIN BUERO.AZR_ABRECHNUNGSZEITRAUM ON AZR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BEGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BEG_ERSTELLUSER" "~"
if joinKuerzel = BMUBEG
searchResult := searchResult "LEFT JOIN BMD.BEG_BERECHTIGUNG ON BEG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BERBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BER_ERSTELLUSER" "~"
if joinKuerzel = BMUBER
searchResult := searchResult "LEFT JOIN BUERO.BER_BERUF ON BER_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BESBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BES_ERSTELLUSER" "~"
if joinKuerzel = BMUBES
searchResult := searchResult "LEFT JOIN BUERO.BES_BEWERBUNGSTATUS ON BES_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BGDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BGD_ERSTELLUSER" "~"
if joinKuerzel = BMUBGD
searchResult := searchResult "LEFT JOIN HRS.BGD_BUDGETGLDNGBAUMDATA ON BGD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BGRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BGR_ERSTELLUSER" "~"
if joinKuerzel = BMUBGR
searchResult := searchResult "LEFT JOIN BMD.BGR_BMDGRUPPENRECHTE ON BGR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BKUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BKU_ERSTELLUSER" "~"
if joinKuerzel = BMUBKU
searchResult := searchResult "LEFT JOIN BUERO.BKU_BEFRAGUNGKRITERIUMZU ON BKU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BLGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BLG_ERSTELLUSER" "~"
if joinKuerzel = BMUBLG
searchResult := searchResult "LEFT JOIN BUERO.BLG_BEZARTLISTGEN ON BLG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BOIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BOI_ERSTELLUSER" "~"
if joinKuerzel = BMUBOI
searchResult := searchResult "LEFT JOIN WWSINDIV.BOI_BONUSTAB_IFA ON BOI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BPBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BPB_ERSTELLUSER" "~"
if joinKuerzel = BMUBPB
searchResult := searchResult "LEFT JOIN WWS.BPB_LIEFBESTAET ON BPB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BPVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BPV_ERSTELLUSER" "~"
if joinKuerzel = BMUBPV
searchResult := searchResult "LEFT JOIN BIL.BPV_ANBU_BERECHPARAMS ON BPV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BRKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BRK_ERSTELLUSER" "~"
if joinKuerzel = BMUBRK
searchResult := searchResult "LEFT JOIN HRS.BRK_BEARBEITERKZ ON BRK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BRSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BRS_ERSTELLUSER" "~"
if joinKuerzel = BMUBRS
searchResult := searchResult "LEFT JOIN FIBU.BRS_BANKRUECKAUFTEILSATZ ON BRS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BSEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BSE_ERSTELLUSER" "~"
if joinKuerzel = BMUBSE
searchResult := searchResult "LEFT JOIN BMD.BSE_BMDSESSION ON BSE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BSNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BSN_ERSTELLUSER" "~"
if joinKuerzel = BMUBSN
searchResult := searchResult "LEFT JOIN FIBU.BSN_BILANZSTEUERKONTEN ON BSN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BTK_ERSTELLUSER" "~"
if joinKuerzel = BMUBTK
searchResult := searchResult "LEFT JOIN FIBU.BTK_BUCHUNGSTEXTKUERZEL ON BTK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BTS_ERSTELLUSER" "~"
if joinKuerzel = BMUBTS
searchResult := searchResult "LEFT JOIN BUERO.BTS_BTERMSATZART ON BTS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BTT_ERSTELLUSER" "~"
if joinKuerzel = BMUBTT
searchResult := searchResult "LEFT JOIN HRS.BTT_BRUTTOERF_TMP ON BTT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BUEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BUE_ERSTELLUSER" "~"
if joinKuerzel = BMUBUE
searchResult := searchResult "LEFT JOIN HRS.BUE_BUAK_URLAUBSEINREICHG ON BUE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BUMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BUM_ERSTELLUSER" "~"
if joinKuerzel = BMUBUM
searchResult := searchResult "LEFT JOIN BIL.BUM_BILUMBUCHUNG ON BUM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BUVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BUV_ERSTELLUSER" "~"
if joinKuerzel = BMUBUV
searchResult := searchResult "LEFT JOIN FIBU.BUV_BUCHUNGSVARIANTE ON BUV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BWIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BWI_ERSTELLUSER" "~"
if joinKuerzel = BMUBWI
searchResult := searchResult "LEFT JOIN BUERO.BWI_BEWERBUNGSINFO ON BWI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BWPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BWP_ERSTELLUSER" "~"
if joinKuerzel = BMUBWP
searchResult := searchResult "LEFT JOIN BUERO.BWP_BENUTZERWERTEPARAM ON BWP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CAWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CAW_ERSTELLUSER" "~"
if joinKuerzel = BMUCAW
searchResult := searchResult "LEFT JOIN BUERO.CAW_CRMABWESENHEIT ON CAW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CBRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CBR_ERSTELLUSER" "~"
if joinKuerzel = BMUCBR
searchResult := searchResult "LEFT JOIN BUERO.CBR_CRMABWBENACHREGEL ON CBR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CMDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CMD_ERSTELLUSER" "~"
if joinKuerzel = BMUCMD
searchResult := searchResult "LEFT JOIN BMDDEFAULT.CMD_CMDLINEPARAM ON CMD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DALBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DAL_ERSTELLUSER" "~"
if joinKuerzel = BMUDAL
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DAL_DATTRLOCK ON DAL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ATABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ATA_ERSTELLUSER" "~"
if joinKuerzel = BMUATA
searchResult := searchResult "LEFT JOIN WWS.ATA_ARTIKELTEXTARCHIV ON ATA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ATEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ATE_ERSTELLUSER" "~"
if joinKuerzel = BMUATE
searchResult := searchResult "LEFT JOIN BUERO.ATE_ABTEILUNG ON ATE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ATZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ATZ_ERSTELLUSER" "~"
if joinKuerzel = BMUATZ
searchResult := searchResult "LEFT JOIN BMD.ATZ_ABFRAGETABELLENZU ON ATZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AVLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AVL_ERSTELLUSER" "~"
if joinKuerzel = BMUAVL
searchResult := searchResult "LEFT JOIN WWS.AVL_AUFTRAGSVORERFLIEFDAT ON AVL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AVRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AVR_ERSTELLUSER" "~"
if joinKuerzel = BMUAVR
searchResult := searchResult "LEFT JOIN BUERO.AVR_AVRUEZAHLUNGSTABELLE ON AVR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AVWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AVW_ERSTELLUSER" "~"
if joinKuerzel = BMUAVW
searchResult := searchResult "LEFT JOIN BUERO.AVW_AVRUEZAHLUNGSWERTE ON AVW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AWRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AWR_ERSTELLUSER" "~"
if joinKuerzel = BMUAWR
searchResult := searchResult "LEFT JOIN BUERO.AWR_AUSWERTUNGSKAT ON AWR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BCUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BCU_ERSTELLUSER" "~"
if joinKuerzel = BMUBCU
searchResult := searchResult "LEFT JOIN BUERO.BCU_BMDCOMUSER ON BCU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BINBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BIN_ERSTELLUSER" "~"
if joinKuerzel = BMUBIN
searchResult := searchResult "LEFT JOIN BUERO.BIN_BEWERBERINSERATZU ON BIN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BKTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BKT_ERSTELLUSER" "~"
if joinKuerzel = BMUBKT
searchResult := searchResult "LEFT JOIN BUERO.BKT_AUSBILDUNGSKOSTEN_TMP ON BKT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BLCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BLC_ERSTELLUSER" "~"
if joinKuerzel = BMUBLC
searchResult := searchResult "LEFT JOIN BMD.BLC_BMDLOCK ON BLC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BMABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BMA_ERSTELLUSER" "~"
if joinKuerzel = BMUBMA
searchResult := searchResult "LEFT JOIN BMD.BMA_BMDMAILACCOUNT ON BMA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BMFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BMF_ERSTELLUSER" "~"
if joinKuerzel = BMUBMF
searchResult := searchResult "LEFT JOIN BMD.BMF_BMDFORMATKATALOG ON BMF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BMHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BMH_ERSTELLUSER" "~"
if joinKuerzel = BMUBMH
searchResult := searchResult "LEFT JOIN BMD.BMH_BMDUSER_VALUEHISTORY ON BMH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BMUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BMU_ERSTELLUSER" "~"
if joinKuerzel = BMUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BPKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BPK_ERSTELLUSER" "~"
if joinKuerzel = BMUBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BRHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BRH_ERSTELLUSER" "~"
if joinKuerzel = BMUBRH
searchResult := searchResult "LEFT JOIN FIBU.BRH_BANKRVKOPF ON BRH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BSTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BST_ERSTELLUSER" "~"
if joinKuerzel = BMUBST
searchResult := searchResult "LEFT JOIN BUERO.BST_SONDERTAGE ON BST_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BTE_ERSTELLUSER" "~"
if joinKuerzel = BMUBTE
searchResult := searchResult "LEFT JOIN BMDENTW.BTE_BMDTABFELDBESCHEXT ON BTE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BWBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BWB_ERSTELLUSER" "~"
if joinKuerzel = BMUBWB
searchResult := searchResult "LEFT JOIN BUERO.BWB_BEWERBUNG ON BWB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BWRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BWR_ERSTELLUSER" "~"
if joinKuerzel = BMUBWR
searchResult := searchResult "LEFT JOIN BIL.BWR_BEWERTRESERVESUMMEN ON BWR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BWTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BWT_ERSTELLUSER" "~"
if joinKuerzel = BMUBWT
searchResult := searchResult "LEFT JOIN BUERO.BWT_BENUTZERWERT ON BWT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CCEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CCE_ERSTELLUSER" "~"
if joinKuerzel = BMUCCE
searchResult := searchResult "LEFT JOIN BIL.CCE_CBCENTITY ON CCE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CLDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CLD_ERSTELLUSER" "~"
if joinKuerzel = BMUCLD
searchResult := searchResult "LEFT JOIN BMD.CLD_CLOADACCOUNT ON CLD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DBKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DBK_ERSTELLUSER" "~"
if joinKuerzel = BMUDBK
searchResult := searchResult "LEFT JOIN BMD.DBK_DRUCKBAUSTEINKOPF ON DBK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DBSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DBS_ERSTELLUSER" "~"
if joinKuerzel = BMUDBS
searchResult := searchResult "LEFT JOIN BUERO.DBS_DATENBESTAND ON DBS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BMGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BMG_ERSTELLUSER" "~"
if joinKuerzel = BMUBMG
searchResult := searchResult "LEFT JOIN BMD.BMG_BMDGRUPPE ON BMG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BMMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BMM_ERSTELLUSER" "~"
if joinKuerzel = BMUBMM
searchResult := searchResult "LEFT JOIN HRS.BMM_BUAK_MONMELDUNG ON BMM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BONBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BON_ERSTELLUSER" "~"
if joinKuerzel = BMUBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BOZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BOZ_ERSTELLUSER" "~"
if joinKuerzel = BMUBOZ
searchResult := searchResult "LEFT JOIN WWS.BOZ_BONZAHLUNG ON BOZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BPPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BPP_ERSTELLUSER" "~"
if joinKuerzel = BMUBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BUKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BUK_ERSTELLUSER" "~"
if joinKuerzel = BMUBUK
searchResult := searchResult "LEFT JOIN FIBU.BUK_BUCHUNGSKREIS ON BUK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BUPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BUP_ERSTELLUSER" "~"
if joinKuerzel = BMUBUP
searchResult := searchResult "LEFT JOIN FIBU.BUP_BUERFIMPORTPARAMETER ON BUP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BVRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BVR_ERSTELLUSER" "~"
if joinKuerzel = BMUBVR
searchResult := searchResult "LEFT JOIN FIBU.BVR_BANKRVREGELN ON BVR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BVZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BVZ_ERSTELLUSER" "~"
if joinKuerzel = BMUBVZ
searchResult := searchResult "LEFT JOIN FIBU.BVZ_BUCHUNGSVAR_ARTZU ON BVZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BWHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BWH_ERSTELLUSER" "~"
if joinKuerzel = BMUBWH
searchResult := searchResult "LEFT JOIN BUERO.BWH_BEWERBERHISTORIE ON BWH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BWUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BWU_ERSTELLUSER" "~"
if joinKuerzel = BMUBWU
searchResult := searchResult "LEFT JOIN BUERO.BWU_BEWERTUNGUSERFIELD ON BWU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BZBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BZB_ERSTELLUSER" "~"
if joinKuerzel = BMUBZB
searchResult := searchResult "LEFT JOIN PRODUKTION.BZB_BAZZEITBUCHUNG ON BZB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BZVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BZV_ERSTELLUSER" "~"
if joinKuerzel = BMUBZV
searchResult := searchResult "LEFT JOIN HRS.BZV_BUAK_ZUSCHLAGSVERRECHN ON BZV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CMZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CMZ_ERSTELLUSER" "~"
if joinKuerzel = BMUCMZ
searchResult := searchResult "LEFT JOIN BMD.CMZ_CMDZEILEN ON CMZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CSSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CSS_ERSTELLUSER" "~"
if joinKuerzel = BMUCSS
searchResult := searchResult "LEFT JOIN BMDDEFAULT.CSS_CASCSTYLESHEET ON CSS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DAGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DAG_ERSTELLUSER" "~"
if joinKuerzel = BMUDAG
searchResult := searchResult "LEFT JOIN BUERO.DAG_DOKANLAGEGUTZU ON DAG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DAS_ERSTELLUSER" "~"
if joinKuerzel = BMUDAS
searchResult := searchResult "LEFT JOIN BUERO.DAS_DOCSARCHIVSTAMM ON DAS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DBLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DBL_ERSTELLUSER" "~"
if joinKuerzel = BMUDBL
searchResult := searchResult "LEFT JOIN BUERO.DBL_DBSLOHNDATEI ON DBL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DBNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DBN_ERSTELLUSER" "~"
if joinKuerzel = BMUDBN
searchResult := searchResult "LEFT JOIN BMD.DBN_DBCLONE ON DBN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DBXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DBX_ERSTELLUSER" "~"
if joinKuerzel = BMUDBX
searchResult := searchResult "LEFT JOIN BUERO.DBX_FINONDATABOX ON DBX_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DCABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DCA_ERSTELLUSER" "~"
if joinKuerzel = BMUDCA
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DCA_WAAUSDRUCKE ON DCA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DCVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DCV_ERSTELLUSER" "~"
if joinKuerzel = BMUDCV
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DCV_WAPARAMDEFVALUES ON DCV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DCZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DCZ_ERSTELLUSER" "~"
if joinKuerzel = BMUDCZ
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DCZ_WAAUSDRUCK_PARAM_ZU ON DCZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DFEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DFE_ERSTELLUSER" "~"
if joinKuerzel = BMUDFE
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFE_DFELDBESCHPOS ON DFE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DFZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DFZ_ERSTELLUSER" "~"
if joinKuerzel = BMUDFZ
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFZ_DFELDGRUPPENZU ON DFZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DGWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DGW_ERSTELLUSER" "~"
if joinKuerzel = BMUDGW
searchResult := searchResult "LEFT JOIN HRS.DGW_DBGESETZWERTE ON DGW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BFKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BFK_ERSTELLUSER" "~"
if joinKuerzel = BMUBFK
searchResult := searchResult "LEFT JOIN WWS.BFK_BEFOERDKAT ON BFK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BIMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BIM_ERSTELLUSER" "~"
if joinKuerzel = BMUBIM
searchResult := searchResult "LEFT JOIN FIBU.BIM_BUERFIMPORTMODELL ON BIM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BKRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BKR_ERSTELLUSER" "~"
if joinKuerzel = BMUBKR
searchResult := searchResult "LEFT JOIN FIBU.BKR_BANKEINZUGSKREIS ON BKR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BKZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BKZ_ERSTELLUSER" "~"
if joinKuerzel = BMUBKZ
searchResult := searchResult "LEFT JOIN BIL.BKZ_BILANZKENNZAHL ON BKZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BMIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BMI_ERSTELLUSER" "~"
if joinKuerzel = BMUBMI
searchResult := searchResult "LEFT JOIN HRS.BMI_BUDGETMITARBEITER ON BMI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BMLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BML_ERSTELLUSER" "~"
if joinKuerzel = BMUBML
searchResult := searchResult "LEFT JOIN FIBU.BML_BANKRVMODEL ON BML_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BOPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BOP_ERSTELLUSER" "~"
if joinKuerzel = BMUBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BRGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BRG_ERSTELLUSER" "~"
if joinKuerzel = BMUBRG
searchResult := searchResult "LEFT JOIN BUERO.BRG_BERUFSGRUPPEN ON BRG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BRUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BRU_ERSTELLUSER" "~"
if joinKuerzel = BMUBRU
searchResult := searchResult "LEFT JOIN HRS.BRU_BETRIEBSRATSUMLAGE ON BRU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BSABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BSA_ERSTELLUSER" "~"
if joinKuerzel = BMUBSA
searchResult := searchResult "LEFT JOIN BMD.BSA_BMDSESSIONARCHIV ON BSA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BSDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BSD_ERSTELLUSER" "~"
if joinKuerzel = BMUBSD
searchResult := searchResult "LEFT JOIN WWS.BSD_BONUSERMDETAIL ON BSD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BSIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BSI_ERSTELLUSER" "~"
if joinKuerzel = BMUBSI
searchResult := searchResult "LEFT JOIN WWSINDIV.BSI_BONUSSTATISTIK ON BSI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BTW_ERSTELLUSER" "~"
if joinKuerzel = BMUBTW
searchResult := searchResult "LEFT JOIN BIL.BTW_BESCHEIDWERT ON BTW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BTZ_ERSTELLUSER" "~"
if joinKuerzel = BMUBTZ
searchResult := searchResult "LEFT JOIN BUERO.BTZ_BTERMINALZUORD ON BTZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BVGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BVG_ERSTELLUSER" "~"
if joinKuerzel = BMUBVG
searchResult := searchResult "LEFT JOIN BIL.BVG_BRANCHENVERGLEICH ON BVG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BVOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BVO_ERSTELLUSER" "~"
if joinKuerzel = BMUBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BZSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BZS_ERSTELLUSER" "~"
if joinKuerzel = BMUBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BZTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BZT_ERSTELLUSER" "~"
if joinKuerzel = BMUBZT
searchResult := searchResult "LEFT JOIN HRS.BZT_BGZUTARIFGRUPPE ON BZT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CAD_ERSTELLUSER" "~"
if joinKuerzel = BMUCAD
searchResult := searchResult "LEFT JOIN BMDDEFAULT.CAD_CACHEDEFINITION ON CAD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DBTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DBT_ERSTELLUSER" "~"
if joinKuerzel = BMUDBT
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DBT_DBMDTABFELDBESCH ON DBT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DIDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DID_ERSTELLUSER" "~"
if joinKuerzel = BMUDID
searchResult := searchResult "LEFT JOIN DATANORM.DID_DIM_TEXT_DETAIL ON DID_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DIIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DII_ERSTELLUSER" "~"
if joinKuerzel = BMUDII
searchResult := searchResult "LEFT JOIN BMD.DII_DIAGRAMMINSTANZ ON DII_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DIZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DIZ_ERSTELLUSER" "~"
if joinKuerzel = BMUDIZ
searchResult := searchResult "LEFT JOIN BUERO.DIZ_DOCSINSTZU ON DIZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DKPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DKP_ERSTELLUSER" "~"
if joinKuerzel = BMUDKP
searchResult := searchResult "LEFT JOIN FIBU.DKP_DOKUMENTENPOOL ON DKP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DKVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DKV_ERSTELLUSER" "~"
if joinKuerzel = BMUDKV
searchResult := searchResult "LEFT JOIN BUERO.DKV_DOKUMENTVERSION ON DKV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DPOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DPO_ERSTELLUSER" "~"
if joinKuerzel = BMUDPO
searchResult := searchResult "LEFT JOIN BMD.DPO_DOCUMENTPOOL ON DPO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BKJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BKJ_ERSTELLUSER" "~"
if joinKuerzel = BMUBKJ
searchResult := searchResult "LEFT JOIN WWS.BKJ_BANKOMATJOURNAL ON BKJ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BKSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BKS_ERSTELLUSER" "~"
if joinKuerzel = BMUBKS
searchResult := searchResult "LEFT JOIN FIBU.BKS_BUCHUNGSKONTROLLSUMME ON BKS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BPOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BPO_ERSTELLUSER" "~"
if joinKuerzel = BMUBPO
searchResult := searchResult "LEFT JOIN BUERO.BPO_BESPRECHOBJEKTE ON BPO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BRVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BRV_ERSTELLUSER" "~"
if joinKuerzel = BMUBRV
searchResult := searchResult "LEFT JOIN FIBU.BRV_BANKRUECKVERBUCHUNG ON BRV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BSKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BSK_ERSTELLUSER" "~"
if joinKuerzel = BMUBSK
searchResult := searchResult "LEFT JOIN WWS.BSK_BONUSERMKOPF ON BSK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BSZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BSZ_ERSTELLUSER" "~"
if joinKuerzel = BMUBSZ
searchResult := searchResult "LEFT JOIN BUERO.BSZ_BESPRECHUNGZU ON BSZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BTF_ERSTELLUSER" "~"
if joinKuerzel = BMUBTF
searchResult := searchResult "LEFT JOIN BMD.BTF_BMDTABFELDBESCH ON BTF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BTR_ERSTELLUSER" "~"
if joinKuerzel = BMUBTR
searchResult := searchResult "LEFT JOIN BUERO.BTR_BETREFF ON BTR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BVEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BVE_ERSTELLUSER" "~"
if joinKuerzel = BMUBVE
searchResult := searchResult "LEFT JOIN WWS.BVE_EINZELTEILLAGERINFO ON BVE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CABBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CAB_ERSTELLUSER" "~"
if joinKuerzel = BMUCAB
searchResult := searchResult "LEFT JOIN BUERO.CAB_CRMABWBERECHTZU ON CAB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CHGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CHG_ERSTELLUSER" "~"
if joinKuerzel = BMUCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CPRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CPR_ERSTELLUSER" "~"
if joinKuerzel = BMUCPR
searchResult := searchResult "LEFT JOIN BUERO.CPR_CRMPROTOKOLL ON CPR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CPZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CPZ_ERSTELLUSER" "~"
if joinKuerzel = BMUCPZ
searchResult := searchResult "LEFT JOIN BMDDEFAULT.CPZ_CMDLINEPARAMZU ON CPZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DAK_ERSTELLUSER" "~"
if joinKuerzel = BMUDAK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DAK_DATTRIBUTKATEGORIE ON DAK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DBCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DBC_ERSTELLUSER" "~"
if joinKuerzel = BMUDBC
searchResult := searchResult "LEFT JOIN BMD.DBC_DBCHANGE ON DBC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DBEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DBE_ERSTELLUSER" "~"
if joinKuerzel = BMUDBE
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DBE_DBERECHTIGUNG ON DBE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DBZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DBZ_ERSTELLUSER" "~"
if joinKuerzel = BMUDBZ
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DBZ_DBMDUSERGRUPPENZU ON DBZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DCDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DCD_ERSTELLUSER" "~"
if joinKuerzel = BMUDCD
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DCD_WAPARAMDEF ON DCD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DNZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DNZ_ERSTELLUSER" "~"
if joinKuerzel = BMUDNZ
searchResult := searchResult "LEFT JOIN HRS.DNZ_DIVNICHTLEISTUNGSZEIT ON DNZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DODBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DOD_ERSTELLUSER" "~"
if joinKuerzel = BMUDOD
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DOD_OCRTEMPLATEDETAIL ON DOD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DOHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DOH_ERSTELLUSER" "~"
if joinKuerzel = BMUDOH
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DOH_OCRTEMPLATEHEADER ON DOH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DOKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DOK_ERSTELLUSER" "~"
if joinKuerzel = BMUDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DPFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DPF_ERSTELLUSER" "~"
if joinKuerzel = BMUDPF
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DPF_DFUNKTIONSPARAMZU ON DPF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DPRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DPR_ERSTELLUSER" "~"
if joinKuerzel = BMUDPR
searchResult := searchResult "LEFT JOIN BUERO.DPR_DOKUMENTPROJEKTZU ON DPR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DQUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DQU_ERSTELLUSER" "~"
if joinKuerzel = BMUDQU
searchResult := searchResult "LEFT JOIN BMD.DQU_DATENQUELLE ON DQU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DREBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DRE_ERSTELLUSER" "~"
if joinKuerzel = BMUDRE
searchResult := searchResult "LEFT JOIN HRS.DRE_ERWDRUCKEREINST ON DRE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AWSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AWS_ERSTELLUSER" "~"
if joinKuerzel = BMUAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AXTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AXT_ERSTELLUSER" "~"
if joinKuerzel = BMUAXT
searchResult := searchResult "LEFT JOIN BMDENTW.AXT_ATTREXT ON AXT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BAPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BAP_ERSTELLUSER" "~"
if joinKuerzel = BMUBAP
searchResult := searchResult "LEFT JOIN FIBU.BAP_BUDGETARTIKELPLAN ON BAP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BEZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BEZ_ERSTELLUSER" "~"
if joinKuerzel = BMUBEZ
searchResult := searchResult "LEFT JOIN FIBU.BEZ_BANKEINZUG ON BEZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BGUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BGU_ERSTELLUSER" "~"
if joinKuerzel = BMUBGU
searchResult := searchResult "LEFT JOIN HRS.BGU_BEITRAGSGRUPPEIMPZU ON BGU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BGZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BGZ_ERSTELLUSER" "~"
if joinKuerzel = BMUBGZ
searchResult := searchResult "LEFT JOIN HRS.BGZ_BNZEILE ON BGZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BLTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BLT_ERSTELLUSER" "~"
if joinKuerzel = BMUBLT
searchResult := searchResult "LEFT JOIN BIL.BLT_BILANZLANGTEXT ON BLT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BMPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BMP_ERSTELLUSER" "~"
if joinKuerzel = BMUBMP
searchResult := searchResult "LEFT JOIN BMD.BMP_BMDMAILPROTOCOL ON BMP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BPABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BPA_ERSTELLUSER" "~"
if joinKuerzel = BMUBPA
searchResult := searchResult "LEFT JOIN FIBU.BPA_BUDGETPLANBILAUFTLG ON BPA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BRCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BRC_ERSTELLUSER" "~"
if joinKuerzel = BMUBRC
searchResult := searchResult "LEFT JOIN BUERO.BRC_BEREICH ON BRC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BRPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BRP_ERSTELLUSER" "~"
if joinKuerzel = BMUBRP
searchResult := searchResult "LEFT JOIN HRS.BRP_BRUTTOERFPROTOKOLL ON BRP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BRZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BRZ_ERSTELLUSER" "~"
if joinKuerzel = BMUBRZ
searchResult := searchResult "LEFT JOIN BUERO.BRZ_BUEROBERECHTZU ON BRZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BSBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BSB_ERSTELLUSER" "~"
if joinKuerzel = BMUBSB
searchResult := searchResult "LEFT JOIN WWS.BSB_BONUSERMPERSONENZU ON BSB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BTD_ERSTELLUSER" "~"
if joinKuerzel = BMUBTD
searchResult := searchResult "LEFT JOIN BUERO.BTD_BTERMSATZARTDETAIL ON BTD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BTH_ERSTELLUSER" "~"
if joinKuerzel = BMUBTH
searchResult := searchResult "LEFT JOIN BUERO.BTH_BTERMHERSTELLER ON BTH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BTM_ERSTELLUSER" "~"
if joinKuerzel = BMUBTM
searchResult := searchResult "LEFT JOIN BUERO.BTM_BTERMINAL ON BTM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BTP_ERSTELLUSER" "~"
if joinKuerzel = BMUBTP
searchResult := searchResult "LEFT JOIN FIBU.BTP_BUDGETTILGUNGSPLAN ON BTP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BUCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BUC_ERSTELLUSER" "~"
if joinKuerzel = BMUBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BUDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BUD_ERSTELLUSER" "~"
if joinKuerzel = BMUBUD
searchResult := searchResult "LEFT JOIN FIBU.BUD_BUDGET ON BUD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BWEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BWE_ERSTELLUSER" "~"
if joinKuerzel = BMUBWE
searchResult := searchResult "LEFT JOIN FIBU.BWE_BUDGETWARENEINKPLAN ON BWE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BWFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BWF_ERSTELLUSER" "~"
if joinKuerzel = BMUBWF
searchResult := searchResult "LEFT JOIN BUERO.BWF_BEWERTUNGFORMEL ON BWF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BWVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BWV_ERSTELLUSER" "~"
if joinKuerzel = BMUBWV
searchResult := searchResult "LEFT JOIN BUERO.BWV_BEWERTUNGVERSION ON BWV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BWXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BWX_ERSTELLUSER" "~"
if joinKuerzel = BMUBWX
searchResult := searchResult "LEFT JOIN BMDWORK.BWX_BMDWORK ON BWX_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CCABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CCA_ERSTELLUSER" "~"
if joinKuerzel = BMUCCA
searchResult := searchResult "LEFT JOIN BIL.CCA_CBCADDINFO ON CCA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CHVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CHV_ERSTELLUSER" "~"
if joinKuerzel = BMUCHV
searchResult := searchResult "LEFT JOIN PRODUKTION.CHV_CHARGENVERFOLGUNG ON CHV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DBDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DBD_ERSTELLUSER" "~"
if joinKuerzel = BMUDBD
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DBD_KEYBREAKDOWN_PD ON DBD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AUSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AUS_ERSTELLUSER" "~"
if joinKuerzel = BMUAUS
searchResult := searchResult "LEFT JOIN BIL.AUS_AUSWERTUNG ON AUS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AUTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AUT_ERSTELLUSER" "~"
if joinKuerzel = BMUAUT
searchResult := searchResult "LEFT JOIN ZLE.AUT_AUSWERTTYP ON AUT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AVIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AVI_ERSTELLUSER" "~"
if joinKuerzel = BMUAVI
searchResult := searchResult "LEFT JOIN HRS.AVI_AVISOMELDUNG ON AVI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AVKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AVK_ERSTELLUSER" "~"
if joinKuerzel = BMUAVK
searchResult := searchResult "LEFT JOIN BUERO.AVK_AVKONTINGENTE ON AVK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AVZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AVZ_ERSTELLUSER" "~"
if joinKuerzel = BMUAVZ
searchResult := searchResult "LEFT JOIN FIBU.AVZ_ANBUFIBZU ON AVZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AWDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AWD_ERSTELLUSER" "~"
if joinKuerzel = BMUAWD
searchResult := searchResult "LEFT JOIN BUERO.AWD_ANTWORTDETAIL ON AWD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AZVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AZV_ERSTELLUSER" "~"
if joinKuerzel = BMUAZV
searchResult := searchResult "LEFT JOIN BUERO.AZV_ARBEITSZEITVERSION ON AZV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BARBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BAR_ERSTELLUSER" "~"
if joinKuerzel = BMUBAR
searchResult := searchResult "LEFT JOIN HRS.BAR_BARAUSLAGEN ON BAR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BBKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BBK_ERSTELLUSER" "~"
if joinKuerzel = BMUBBK
searchResult := searchResult "LEFT JOIN HRS.BBK_BUAK_BETRIEBSKZ ON BBK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BCTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BCT_ERSTELLUSER" "~"
if joinKuerzel = BMUBCT
searchResult := searchResult "LEFT JOIN BUERO.BCT_BMDCOMTEMPLATES ON BCT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BDLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BDL_ERSTELLUSER" "~"
if joinKuerzel = BMUBDL
searchResult := searchResult "LEFT JOIN BUERO.BDL_BUERODELETE ON BDL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BEIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BEI_ERSTELLUSER" "~"
if joinKuerzel = BMUBEI
searchResult := searchResult "LEFT JOIN BUERO.BEI_BEIFAHRER ON BEI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BETBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BET_ERSTELLUSER" "~"
if joinKuerzel = BMUBET
searchResult := searchResult "LEFT JOIN BUERO.BET_BETRIEBSMITTEL ON BET_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BFZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BFZ_ERSTELLUSER" "~"
if joinKuerzel = BMUBFZ
searchResult := searchResult "LEFT JOIN BIL.BFZ_XMLJABBETFIRZU ON BFZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BLFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BLF_ERSTELLUSER" "~"
if joinKuerzel = BMUBLF
searchResult := searchResult "LEFT JOIN BUERO.BLF_BLOCKLOCHFUELLER ON BLF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BLZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BLZ_ERSTELLUSER" "~"
if joinKuerzel = BMUBLZ
searchResult := searchResult "LEFT JOIN BMDENTW.BLZ_PACKAGESZU ON BLZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BMOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BMO_ERSTELLUSER" "~"
if joinKuerzel = BMUBMO
searchResult := searchResult "LEFT JOIN FIBU.BMO_BUDGETMODELL ON BMO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BMRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BMR_ERSTELLUSER" "~"
if joinKuerzel = BMUBMR
searchResult := searchResult "LEFT JOIN BMD.BMR_BMDROLLE ON BMR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BPZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BPZ_ERSTELLUSER" "~"
if joinKuerzel = BMUBPZ
searchResult := searchResult "LEFT JOIN WWS.BPZ_BESTELLPOSZUORDNUNG ON BPZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BRABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BRA_ERSTELLUSER" "~"
if joinKuerzel = BMUBRA
searchResult := searchResult "LEFT JOIN BUERO.BRA_BRANCHE ON BRA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BREBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BRE_ERSTELLUSER" "~"
if joinKuerzel = BMUBRE
searchResult := searchResult "LEFT JOIN HRS.BRE_BRUTTOERFASSUNG ON BRE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BRMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BRM_ERSTELLUSER" "~"
if joinKuerzel = BMUBRM
searchResult := searchResult "LEFT JOIN BUERO.BRM_BUEROMAKROS ON BRM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BSPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BSP_ERSTELLUSER" "~"
if joinKuerzel = BMUBSP
searchResult := searchResult "LEFT JOIN WWS.BSP_BONUSERMPOS ON BSP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BUABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BUA_ERSTELLUSER" "~"
if joinKuerzel = BMUBUA
searchResult := searchResult "LEFT JOIN FIBU.BUA_BUCHUNGSART ON BUA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BUSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BUS_ERSTELLUSER" "~"
if joinKuerzel = BMUBUS
searchResult := searchResult "LEFT JOIN FIBU.BUS_BUCHUNGSSYMBOL ON BUS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BVLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BVL_ERSTELLUSER" "~"
if joinKuerzel = BMUBVL
searchResult := searchResult "LEFT JOIN WWS.BVL_LIEFERANTENZUORDNUNG ON BVL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BWMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BWM_ERSTELLUSER" "~"
if joinKuerzel = BMUBWM
searchResult := searchResult "LEFT JOIN BUERO.BWM_BEWERTUNGMONAT ON BWM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BAS_ERSTELLUSER" "~"
if joinKuerzel = BMUBAS
searchResult := searchResult "LEFT JOIN FIBU.BAS_BUCAUFTEILUNGSSATZ ON BAS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BBABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BBA_ERSTELLUSER" "~"
if joinKuerzel = BMUBBA
searchResult := searchResult "LEFT JOIN BIL.BBA_BERICHTSART ON BBA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BCIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BCI_ERSTELLUSER" "~"
if joinKuerzel = BMUBCI
searchResult := searchResult "LEFT JOIN BMDENTW.BCI_BMDCONSTINTERN ON BCI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BFOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BFO_ERSTELLUSER" "~"
if joinKuerzel = BMUBFO
searchResult := searchResult "LEFT JOIN FIBU.BFO_BANKFIBUOPZU ON BFO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BGPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BGP_ERSTELLUSER" "~"
if joinKuerzel = BMUBGP
searchResult := searchResult "LEFT JOIN HRS.BGP_BEITRAGSGRUPPE ON BGP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BIZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BIZ_ERSTELLUSER" "~"
if joinKuerzel = BMUBIZ
searchResult := searchResult "LEFT JOIN FIBU.BIZ_BUDGETINVESTZAHLUNG ON BIZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BKMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BKM_ERSTELLUSER" "~"
if joinKuerzel = BMUBKM
searchResult := searchResult "LEFT JOIN BUERO.BKM_ZEITBETRKALMAZUORD ON BKM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BLDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BLD_ERSTELLUSER" "~"
if joinKuerzel = BMUBLD
searchResult := searchResult "LEFT JOIN BUERO.BLD_BUNDESLAND ON BLD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BLSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BLS_ERSTELLUSER" "~"
if joinKuerzel = BMUBLS
searchResult := searchResult "LEFT JOIN BMD.BLS_BLOB_STAMM ON BLS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BMKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BMK_ERSTELLUSER" "~"
if joinKuerzel = BMUBMK
searchResult := searchResult "LEFT JOIN BMD.BMK_BMDEMAILKATEGORIE ON BMK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BOABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BOA_ERSTELLUSER" "~"
if joinKuerzel = BMUBOA
searchResult := searchResult "LEFT JOIN WWS.BOA_BONUSAUSSCHLUSS ON BOA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BORBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BOR_ERSTELLUSER" "~"
if joinKuerzel = BMUBOR
searchResult := searchResult "LEFT JOIN WWS.BOR_BONRABATT ON BOR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BPDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BPD_ERSTELLUSER" "~"
if joinKuerzel = BMUBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BPLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BPL_ERSTELLUSER" "~"
if joinKuerzel = BMUBPL
searchResult := searchResult "LEFT JOIN BMDENTW.BPL_PACKAGES ON BPL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BPTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BPT_ERSTELLUSER" "~"
if joinKuerzel = BMUBPT
searchResult := searchResult "LEFT JOIN BUERO.BPT_BESPRECHPROTOKOLL ON BPT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BRBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BRB_ERSTELLUSER" "~"
if joinKuerzel = BMUBRB
searchResult := searchResult "LEFT JOIN BUERO.BRB_BUEROBERECHT ON BRB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BRTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BRT_ERSTELLUSER" "~"
if joinKuerzel = BMUBRT
searchResult := searchResult "LEFT JOIN BUERO.BRT_BERICHT ON BRT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BSGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BSG_ERSTELLUSER" "~"
if joinKuerzel = BMUBSG
searchResult := searchResult "LEFT JOIN BUERO.BSG_BESPRECHUNG ON BSG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BSUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BSU_ERSTELLUSER" "~"
if joinKuerzel = BMUBSU
searchResult := searchResult "LEFT JOIN FIBU.BSU_BANKEINZUGSSUMME ON BSU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BTA_ERSTELLUSER" "~"
if joinKuerzel = BMUBTA
searchResult := searchResult "LEFT JOIN FIBU.BTA_BUDGETTILGPLAN_AEND ON BTA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BTG_ERSTELLUSER" "~"
if joinKuerzel = BMUBTG
searchResult := searchResult "LEFT JOIN BUERO.BTG_BEWERTUNGTAG ON BTG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BURBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BUR_ERSTELLUSER" "~"
if joinKuerzel = BMUBUR
searchResult := searchResult "LEFT JOIN BUERO.BUR_BMDCOMUPLOADTREE ON BUR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BUZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BUZ_ERSTELLUSER" "~"
if joinKuerzel = BMUBUZ
searchResult := searchResult "LEFT JOIN BMD.BUZ_BMDUSERGRUPPENZU ON BUZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BWKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BWK_ERSTELLUSER" "~"
if joinKuerzel = BMUBWK
searchResult := searchResult "LEFT JOIN BUERO.BWK_BENUTZERWERTKATEGORIE ON BWK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BZPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BZP_ERSTELLUSER" "~"
if joinKuerzel = BMUBZP
searchResult := searchResult "LEFT JOIN BMD.BZP_BLOBZUGRIFFSPROTOKOLL ON BZP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BZUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BZU_ERSTELLUSER" "~"
if joinKuerzel = BMUBZU
searchResult := searchResult "LEFT JOIN FIBU.BZU_BUARTKONSZU ON BZU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DDSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DDS_ERSTELLUSER" "~"
if joinKuerzel = BMUDDS
searchResult := searchResult "LEFT JOIN HRS.DDS_DBSDIVSTAMM ON DDS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DHZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DHZ_ERSTELLUSER" "~"
if joinKuerzel = BMUDHZ
searchResult := searchResult "LEFT JOIN DATANORM.DHZ_DATANORM_WHGZU ON DHZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DIHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DIH_ERSTELLUSER" "~"
if joinKuerzel = BMUDIH
searchResult := searchResult "LEFT JOIN BMD.DIH_DIAGRAMMINSTANZKOPF ON DIH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DIKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DIK_ERSTELLUSER" "~"
if joinKuerzel = BMUDIK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DIK_INSTITUTIONENKEYS_PD ON DIK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DIPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DIP_ERSTELLUSER" "~"
if joinKuerzel = BMUDIP
searchResult := searchResult "LEFT JOIN BUERO.DIP_DIVERSE_PERSON ON DIP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DISBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DIS_ERSTELLUSER" "~"
if joinKuerzel = BMUDIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DOFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DOF_ERSTELLUSER" "~"
if joinKuerzel = BMUDOF
searchResult := searchResult "LEFT JOIN FIBU.DOF_FIBUDOKUMENT ON DOF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DRFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DRF_ERSTELLUSER" "~"
if joinKuerzel = BMUDRF
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DRF_DREPORTFORMATVORLAGE ON DRF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DTZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DTZ_ERSTELLUSER" "~"
if joinKuerzel = BMUDTZ
searchResult := searchResult "LEFT JOIN DATANORM.DTZ_DATANORM_TEXTZU ON DTZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DWHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DWH_ERSTELLUSER" "~"
if joinKuerzel = BMUDWH
searchResult := searchResult "LEFT JOIN DATANORM.DWH_WAREN_HAUPT_GRUPPE ON DWH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EAFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EAF_ERSTELLUSER" "~"
if joinKuerzel = BMUEAF
searchResult := searchResult "LEFT JOIN HRS.EAF_ELDADETAIL_AFB ON EAF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EANBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EAN_ERSTELLUSER" "~"
if joinKuerzel = BMUEAN
searchResult := searchResult "LEFT JOIN WWS.EAN_EANNUMMERN ON EAN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EFHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EFH_ERSTELLUSER" "~"
if joinKuerzel = BMUEFH
searchResult := searchResult "LEFT JOIN HRS.EFH_ELDADETAIL_FH ON EFH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EGMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EGM_ERSTELLUSER" "~"
if joinKuerzel = BMUEGM
searchResult := searchResult "LEFT JOIN HRS.EGM_ELDADETAIL_GM ON EGM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EMABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EMA_ERSTELLUSER" "~"
if joinKuerzel = BMUEMA
searchResult := searchResult "LEFT JOIN BUERO.EMA_EMAIL ON EMA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EPSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EPS_ERSTELLUSER" "~"
if joinKuerzel = BMUEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ERDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ERD_ERSTELLUSER" "~"
if joinKuerzel = BMUERD
searchResult := searchResult "LEFT JOIN WWS.ERD_ERFASSRABATTDETAIL ON ERD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ERGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ERG_ERSTELLUSER" "~"
if joinKuerzel = BMUERG
searchResult := searchResult "LEFT JOIN WWS.ERG_ERFASSUNGSGRUPPEN ON ERG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ERNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ERN_ERSTELLUSER" "~"
if joinKuerzel = BMUERN
searchResult := searchResult "LEFT JOIN WWS.ERN_EXTARLKEYNAMES ON ERN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ERPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ERP_ERSTELLUSER" "~"
if joinKuerzel = BMUERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ESEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ESE_ERSTELLUSER" "~"
if joinKuerzel = BMUESE
searchResult := searchResult "LEFT JOIN HRS.ESE_ELDADETAIL_SE ON ESE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ETDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ETD_ERSTELLUSER" "~"
if joinKuerzel = BMUETD
searchResult := searchResult "LEFT JOIN BMDDEFAULT.ETD_ETIKETTENFORMAT ON ETD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EVQBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EVQ_ERSTELLUSER" "~"
if joinKuerzel = BMUEVQ
searchResult := searchResult "LEFT JOIN HRS.EVQ_ELDADETAIL_VSNR ON EVQ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EVTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EVT_ERSTELLUSER" "~"
if joinKuerzel = BMUEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FAD_ERSTELLUSER" "~"
if joinKuerzel = BMUFAD
searchResult := searchResult "LEFT JOIN FIBU.FAD_AUSDRUCKDETAIL ON FAD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FDPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FDP_ERSTELLUSER" "~"
if joinKuerzel = BMUFDP
searchResult := searchResult "LEFT JOIN BMD.FDP_FELDWERTEPOS ON FDP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DCEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DCE_ERSTELLUSER" "~"
if joinKuerzel = BMUDCE
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DCE_WABAUMELEMENTE ON DCE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DFRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DFR_ERSTELLUSER" "~"
if joinKuerzel = BMUDFR
searchResult := searchResult "LEFT JOIN WWS.DFR_DOKUMENTFREIGABE ON DFR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DIABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DIA_ERSTELLUSER" "~"
if joinKuerzel = BMUDIA
searchResult := searchResult "LEFT JOIN BMD.DIA_DIAGRAMM ON DIA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DIMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DIM_ERSTELLUSER" "~"
if joinKuerzel = BMUDIM
searchResult := searchResult "LEFT JOIN DATANORM.DIM_DIM_TEXT ON DIM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DKWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DKW_ERSTELLUSER" "~"
if joinKuerzel = BMUDKW
searchResult := searchResult "LEFT JOIN BUERO.DKW_DOKKATWORKFLOWZU ON DKW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DLGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DLG_ERSTELLUSER" "~"
if joinKuerzel = BMUDLG
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DLG_DLIZENZGRUPPE ON DLG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DOZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DOZ_ERSTELLUSER" "~"
if joinKuerzel = BMUDOZ
searchResult := searchResult "LEFT JOIN BMD.DOZ_DATAOWNERZU ON DOZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DRGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DRG_ERSTELLUSER" "~"
if joinKuerzel = BMUDRG
searchResult := searchResult "LEFT JOIN HRS.DRG_DIENSTREISEGEN ON DRG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DTDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DTD_ERSTELLUSER" "~"
if joinKuerzel = BMUDTD
searchResult := searchResult "LEFT JOIN DATANORM.DTD_TEXT_DETAIL ON DTD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DTUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DTU_ERSTELLUSER" "~"
if joinKuerzel = BMUDTU
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DTU_DTABUEBERS ON DTU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DVWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DVW_ERSTELLUSER" "~"
if joinKuerzel = BMUDVW
searchResult := searchResult "LEFT JOIN BUERO.DVW_DOKUMENTENVERWEIS ON DVW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DZUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DZU_ERSTELLUSER" "~"
if joinKuerzel = BMUDZU
searchResult := searchResult "LEFT JOIN HRS.DZU_DOKZU_HRS ON DZU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EAEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EAE_ERSTELLUSER" "~"
if joinKuerzel = BMUEAE
searchResult := searchResult "LEFT JOIN HRS.EAE_ELDADETAIL_AEK ON EAE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EAPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EAP_ERSTELLUSER" "~"
if joinKuerzel = BMUEAP
searchResult := searchResult "LEFT JOIN WWS.EAP_ERLOESABGRENZUNGSPOS ON EAP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EARBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EAR_ERSTELLUSER" "~"
if joinKuerzel = BMUEAR
searchResult := searchResult "LEFT JOIN WWS.EAR_EXTARTIKELWERTE ON EAR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EAS_ERSTELLUSER" "~"
if joinKuerzel = BMUEAS
searchResult := searchResult "LEFT JOIN FIBU.EAS_ERKONTOAUFTEILSATZ ON EAS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EBVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EBV_ERSTELLUSER" "~"
if joinKuerzel = BMUEBV
searchResult := searchResult "LEFT JOIN HRS.EBV_ELDADETAIL_BV ON EBV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EDABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EDA_ERSTELLUSER" "~"
if joinKuerzel = BMUEDA
searchResult := searchResult "LEFT JOIN HRS.EDA_ELDADETAIL_ADR ON EDA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EDKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EDK_ERSTELLUSER" "~"
if joinKuerzel = BMUEDK
searchResult := searchResult "LEFT JOIN HRS.EDK_ERWDIVKOERPER ON EDK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EFABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EFA_ERSTELLUSER" "~"
if joinKuerzel = BMUEFA
searchResult := searchResult "LEFT JOIN WWS.EFA_ERFASSUNGSART ON EFA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EKRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EKR_ERSTELLUSER" "~"
if joinKuerzel = BMUEKR
searchResult := searchResult "LEFT JOIN WWS.EKR_ERFASSKOPFRECHNUNG ON EKR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ELZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ELZ_ERSTELLUSER" "~"
if joinKuerzel = BMUELZ
searchResult := searchResult "LEFT JOIN HRS.ELZ_ELDADETAIL_LZM ON ELZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EM4BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EM4_ERSTELLUSER" "~"
if joinKuerzel = BMUEM4
searchResult := searchResult "LEFT JOIN HRS.EM4_ELDADETAIL_VB ON EM4_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EM5BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EM5_ERSTELLUSER" "~"
if joinKuerzel = BMUEM5
searchResult := searchResult "LEFT JOIN HRS.EM5_ELDADETAIL_VP ON EM5_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EMTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EMT_ERSTELLUSER" "~"
if joinKuerzel = BMUEMT
searchResult := searchResult "LEFT JOIN BUERO.EMT_EMAILART ON EMT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EROBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ERO_ERSTELLUSER" "~"
if joinKuerzel = BMUERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DRKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DRK_ERSTELLUSER" "~"
if joinKuerzel = BMUDRK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DRK_DTABREPLGRUPPE ON DRK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DRUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DRU_ERSTELLUSER" "~"
if joinKuerzel = BMUDRU
searchResult := searchResult "LEFT JOIN BMD.DRU_DRUCKER ON DRU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DRZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DRZ_ERSTELLUSER" "~"
if joinKuerzel = BMUDRZ
searchResult := searchResult "LEFT JOIN BMD.DRZ_DRUCKERBENUTZERWERT ON DRZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DSQBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DSQ_ERSTELLUSER" "~"
if joinKuerzel = BMUDSQ
searchResult := searchResult "LEFT JOIN HRS.DSQ_DIVSTAMM_SEQ ON DSQ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DTMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DTM_ERSTELLUSER" "~"
if joinKuerzel = BMUDTM
searchResult := searchResult "LEFT JOIN DATANORM.DTM_TEXT ON DTM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DUFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DUF_ERSTELLUSER" "~"
if joinKuerzel = BMUDUF
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DUF_DUSERLINKFIELD ON DUF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DWZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DWZ_ERSTELLUSER" "~"
if joinKuerzel = BMUDWZ
searchResult := searchResult "LEFT JOIN BUERO.DWZ_DOKUMENTWORKFLOWZU ON DWZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EAVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EAV_ERSTELLUSER" "~"
if joinKuerzel = BMUEAV
searchResult := searchResult "LEFT JOIN HRS.EAV_ELDADETAIL_AV ON EAV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EAWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EAW_ERSTELLUSER" "~"
if joinKuerzel = BMUEAW
searchResult := searchResult "LEFT JOIN HRS.EAW_ELDADETAIL_AEW ON EAW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EBNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EBN_ERSTELLUSER" "~"
if joinKuerzel = BMUEBN
searchResult := searchResult "LEFT JOIN HRS.EBN_ELDADETAIL_BN ON EBN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EEFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EEF_ERSTELLUSER" "~"
if joinKuerzel = BMUEEF
searchResult := searchResult "LEFT JOIN HRS.EEF_ELDADETAIL_EFZ ON EEF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EFKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EFK_ERSTELLUSER" "~"
if joinKuerzel = BMUEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EGZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EGZ_ERSTELLUSER" "~"
if joinKuerzel = BMUEGZ
searchResult := searchResult "LEFT JOIN HRS.EGZ_ERGAENZUNG ON EGZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EHPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EHP_ERSTELLUSER" "~"
if joinKuerzel = BMUEHP
searchResult := searchResult "LEFT JOIN WWS.EHP_EINHEITENTABPOS ON EHP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EKLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EKL_ERSTELLUSER" "~"
if joinKuerzel = BMUEKL
searchResult := searchResult "LEFT JOIN WWS.EKL_ERFASSKOPFLFS ON EKL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EKZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EKZ_ERSTELLUSER" "~"
if joinKuerzel = BMUEKZ
searchResult := searchResult "LEFT JOIN WWS.EKZ_ERLOESAUFWANDKZ ON EKZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ELIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ELI_ERSTELLUSER" "~"
if joinKuerzel = BMUELI
searchResult := searchResult "LEFT JOIN HRS.ELI_ELDADETAIL_LZI ON ELI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EM2BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EM2_ERSTELLUSER" "~"
if joinKuerzel = BMUEM2
searchResult := searchResult "LEFT JOIN HRS.EM2_ELDADETAIL_MBGM ON EM2_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EQIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EQI_ERSTELLUSER" "~"
if joinKuerzel = BMUEQI
searchResult := searchResult "LEFT JOIN WWS.EQI_ERFASSPOSQUELLINFO ON EQI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ERMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ERM_ERSTELLUSER" "~"
if joinKuerzel = BMUERM
searchResult := searchResult "LEFT JOIN FIBU.ERM_ERMITARBEITERZU ON ERM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ERWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ERW_ERSTELLUSER" "~"
if joinKuerzel = BMUERW
searchResult := searchResult "LEFT JOIN FIBU.ERW_WORKFLOW ON ERW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EVRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EVR_ERSTELLUSER" "~"
if joinKuerzel = BMUEVR
searchResult := searchResult "LEFT JOIN HRS.EVR_ELDADETAIL_VMR ON EVR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EZTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EZT_ERSTELLUSER" "~"
if joinKuerzel = BMUEZT
searchResult := searchResult "LEFT JOIN HRS.EZT_ELDAZENTRALTABELLE ON EZT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FBPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FBP_ERSTELLUSER" "~"
if joinKuerzel = BMUFBP
searchResult := searchResult "LEFT JOIN BMD.FBP_FELDBESCHPOS ON FBP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FRBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FRB_ERSTELLUSER" "~"
if joinKuerzel = BMUFRB
searchResult := searchResult "LEFT JOIN WWS.FRB_FARBE ON FRB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FSKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FSK_ERSTELLUSER" "~"
if joinKuerzel = BMUFSK
searchResult := searchResult "LEFT JOIN FIBU.FSK_SORTIERKOPF ON FSK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DRTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DRT_ERSTELLUSER" "~"
if joinKuerzel = BMUDRT
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DRT_DTABREPLGTABELLEZU ON DRT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DSDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DSD_ERSTELLUSER" "~"
if joinKuerzel = BMUDSD
searchResult := searchResult "LEFT JOIN DATANORM.DSD_SET_DETAIL ON DSD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DSSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DSS_ERSTELLUSER" "~"
if joinKuerzel = BMUDSS
searchResult := searchResult "LEFT JOIN BUERO.DSS_DSGVO_STANDARD ON DSS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DZABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DZA_ERSTELLUSER" "~"
if joinKuerzel = BMUDZA
searchResult := searchResult "LEFT JOIN HRS.DZA_ANRECHNUNG ON DZA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EAK_ERSTELLUSER" "~"
if joinKuerzel = BMUEAK
searchResult := searchResult "LEFT JOIN WWS.EAK_ERLOESABGRENZUNGSKOPF ON EAK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EAOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EAO_ERSTELLUSER" "~"
if joinKuerzel = BMUEAO
searchResult := searchResult "LEFT JOIN BMD.EAO_EXTARCHIVOP ON EAO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EHTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EHT_ERSTELLUSER" "~"
if joinKuerzel = BMUEHT
searchResult := searchResult "LEFT JOIN WWS.EHT_EINHEITENTAB ON EHT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EPFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EPF_ERSTELLUSER" "~"
if joinKuerzel = BMUEPF
searchResult := searchResult "LEFT JOIN WWS.EPF_EPSTKL_REF ON EPF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ETKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ETK_ERSTELLUSER" "~"
if joinKuerzel = BMUETK
searchResult := searchResult "LEFT JOIN WWS.ETK_ETIKETTENKLASSE ON ETK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EVGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EVG_ERSTELLUSER" "~"
if joinKuerzel = BMUEVG
searchResult := searchResult "LEFT JOIN BUERO.EVG_EVENTTAGE ON EVG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EXPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EXP_ERSTELLUSER" "~"
if joinKuerzel = BMUEXP
searchResult := searchResult "LEFT JOIN BUERO.EXP_EXTERNPERSONKEY ON EXP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EXTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EXT_ERSTELLUSER" "~"
if joinKuerzel = BMUEXT
searchResult := searchResult "LEFT JOIN FIBU.EXT_EXTJOURNAL ON EXT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FAVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FAV_ERSTELLUSER" "~"
if joinKuerzel = BMUFAV
searchResult := searchResult "LEFT JOIN BUERO.FAV_FAVORITEN ON FAV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FBABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FBA_ERSTELLUSER" "~"
if joinKuerzel = BMUFBA
searchResult := searchResult "LEFT JOIN BIL.FBA_XMLJABFBANHANG ON FBA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FBNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FBN_ERSTELLUSER" "~"
if joinKuerzel = BMUFBN
searchResult := searchResult "LEFT JOIN BUERO.FBN_FRAGEBOGEN ON FBN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FDOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FDO_ERSTELLUSER" "~"
if joinKuerzel = BMUFDO
searchResult := searchResult "LEFT JOIN FIBU.FDO_FIBUDOKUMENTZU ON FDO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FEDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FED_ERSTELLUSER" "~"
if joinKuerzel = BMUFED
searchResult := searchResult "LEFT JOIN WWS.FED_FERTEP_AUFTDETAIL ON FED_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FESBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FES_ERSTELLUSER" "~"
if joinKuerzel = BMUFES
searchResult := searchResult "LEFT JOIN BUERO.FES_FRISTERLEDIGTSTATUS ON FES_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FGEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FGE_ERSTELLUSER" "~"
if joinKuerzel = BMUFGE
searchResult := searchResult "LEFT JOIN BUERO.FGE_FRAGE ON FGE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FGMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FGM_ERSTELLUSER" "~"
if joinKuerzel = BMUFGM
searchResult := searchResult "LEFT JOIN BUERO.FGM_FIRMENGEMEINDEZU ON FGM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FIFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FIF_ERSTELLUSER" "~"
if joinKuerzel = BMUFIF
searchResult := searchResult "LEFT JOIN FIBU.FIF_FILIALEFIBU ON FIF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FIRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FIR_ERSTELLUSER" "~"
if joinKuerzel = BMUFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FIZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FIZ_ERSTELLUSER" "~"
if joinKuerzel = BMUFIZ
searchResult := searchResult "LEFT JOIN FIBU.FIZ_FILIALEKONSZU ON FIZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FOTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FOT_ERSTELLUSER" "~"
if joinKuerzel = BMUFOT
searchResult := searchResult "LEFT JOIN BUERO.FOT_FOTO ON FOT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FPKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FPK_ERSTELLUSER" "~"
if joinKuerzel = BMUFPK
searchResult := searchResult "LEFT JOIN WWSINDIV.FPK_PROZENTSATZKONTEN ON FPK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FTGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FTG_ERSTELLUSER" "~"
if joinKuerzel = BMUFTG
searchResult := searchResult "LEFT JOIN BUERO.FTG_FEIERTAG ON FTG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CAIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CAI_ERSTELLUSER" "~"
if joinKuerzel = BMUCAI
searchResult := searchResult "LEFT JOIN BMD.CAI_CACHEINVALIDIERUNG ON CAI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CKLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CKL_ERSTELLUSER" "~"
if joinKuerzel = BMUCKL
searchResult := searchResult "LEFT JOIN BUERO.CKL_CHECKLISTE ON CKL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CLVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CLV_ERSTELLUSER" "~"
if joinKuerzel = BMUCLV
searchResult := searchResult "LEFT JOIN BUERO.CLV_CHECKLISTVORLAGE ON CLV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CTZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CTZ_ERSTELLUSER" "~"
if joinKuerzel = BMUCTZ
searchResult := searchResult "LEFT JOIN BMDDEFAULT.CTZ_CACHETABLEZU ON CTZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CWFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CWF_ERSTELLUSER" "~"
if joinKuerzel = BMUCWF
searchResult := searchResult "LEFT JOIN BUERO.CWF_CRMWORKFLOW ON CWF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DACBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DAC_ERSTELLUSER" "~"
if joinKuerzel = BMUDAC
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DAC_DATTRIBUTECLONE ON DAC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DAZ_ERSTELLUSER" "~"
if joinKuerzel = BMUDAZ
searchResult := searchResult "LEFT JOIN DATANORM.DAZ_DATANORM_ARTIKELZU ON DAZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DBGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DBG_ERSTELLUSER" "~"
if joinKuerzel = BMUDBG
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DBG_DBMDGRUPPE ON DBG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DBPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DBP_ERSTELLUSER" "~"
if joinKuerzel = BMUDBP
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DBP_DBERECHTPAKET ON DBP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DCPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DCP_ERSTELLUSER" "~"
if joinKuerzel = BMUDCP
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DCP_CONSTRAINTS_PD ON DCP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DEZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DEZ_ERSTELLUSER" "~"
if joinKuerzel = BMUDEZ
searchResult := searchResult "LEFT JOIN DATANORM.DEZ_DATANORM_ERSTELLERZU ON DEZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DFLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DFL_ERSTELLUSER" "~"
if joinKuerzel = BMUDFL
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFL_DFUNKBERECHT_LINK ON DFL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DOUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DOU_ERSTELLUSER" "~"
if joinKuerzel = BMUDOU
searchResult := searchResult "LEFT JOIN BMD.DOU_DATAOWNERUSERZU_TMP ON DOU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DPABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DPA_ERSTELLUSER" "~"
if joinKuerzel = BMUDPA
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DPA_DPAKET ON DPA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DPDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DPD_ERSTELLUSER" "~"
if joinKuerzel = BMUDPD
searchResult := searchResult "LEFT JOIN BMD.DPD_DOCUMENTPOOLDETAIL ON DPD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DRBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DRB_ERSTELLUSER" "~"
if joinKuerzel = BMUDRB
searchResult := searchResult "LEFT JOIN BMD.DRB_DRUCKBAUSTEINE ON DRB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DSPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DSP_ERSTELLUSER" "~"
if joinKuerzel = BMUDSP
searchResult := searchResult "LEFT JOIN BUERO.DSP_DSGVO_PERSONALIEN ON DSP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DTABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DTA_ERSTELLUSER" "~"
if joinKuerzel = BMUDTA
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DTA_DBMDTABELLE ON DTA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DUKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DUK_ERSTELLUSER" "~"
if joinKuerzel = BMUDUK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DUK_DUSERLINK ON DUK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DVKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DVK_ERSTELLUSER" "~"
if joinKuerzel = BMUDVK
searchResult := searchResult "LEFT JOIN BUERO.DVK_DSGVO_VERZEICHNISKATZU ON DVK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DVLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DVL_ERSTELLUSER" "~"
if joinKuerzel = BMUDVL
searchResult := searchResult "LEFT JOIN BUERO.DVL_DOKUMENTVERLAUF ON DVL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EAZ_ERSTELLUSER" "~"
if joinKuerzel = BMUEAZ
searchResult := searchResult "LEFT JOIN BUERO.EAZ_EVENTAUSBZU ON EAZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EBGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EBG_ERSTELLUSER" "~"
if joinKuerzel = BMUEBG
searchResult := searchResult "LEFT JOIN HRS.EBG_ELDADETAIL_BGN ON EBG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EBPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EBP_ERSTELLUSER" "~"
if joinKuerzel = BMUEBP
searchResult := searchResult "LEFT JOIN FIBU.EBP_BESTELLPOSITION ON EBP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EEZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EEZ_ERSTELLUSER" "~"
if joinKuerzel = BMUEEZ
searchResult := searchResult "LEFT JOIN WWS.EEZ_EDIEINHEITENZU ON EEZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EFZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EFZ_ERSTELLUSER" "~"
if joinKuerzel = BMUEFZ
searchResult := searchResult "LEFT JOIN HRS.EFZ_ENTGELTFORTZAHLUNG ON EFZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DBIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DBI_ERSTELLUSER" "~"
if joinKuerzel = BMUDBI
searchResult := searchResult "LEFT JOIN BMD.DBI_DBCLONEITEM ON DBI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DCFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DCF_ERSTELLUSER" "~"
if joinKuerzel = BMUDCF
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DCF_WABAUMELEMAUSDRUCKZU ON DCF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DEIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DEI_ERSTELLUSER" "~"
if joinKuerzel = BMUDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DELBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DEL_ERSTELLUSER" "~"
if joinKuerzel = BMUDEL
searchResult := searchResult "LEFT JOIN BUERO.DEL_DELETE ON DEL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DFFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DFF_ERSTELLUSER" "~"
if joinKuerzel = BMUDFF
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFF_DFELDBESCHFORMAT ON DFF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DFGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DFG_ERSTELLUSER" "~"
if joinKuerzel = BMUDFG
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFG_DFELDBESCHGRUPPE ON DFG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DLFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DLF_ERSTELLUSER" "~"
if joinKuerzel = BMUDLF
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DLF_LINKFIELD ON DLF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DNABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DNA_ERSTELLUSER" "~"
if joinKuerzel = BMUDNA
searchResult := searchResult "LEFT JOIN DATANORM.DNA_ARTIKEL ON DNA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DNEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DNE_ERSTELLUSER" "~"
if joinKuerzel = BMUDNE
searchResult := searchResult "LEFT JOIN DATANORM.DNE_ERSTELLER ON DNE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DNIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DNI_ERSTELLUSER" "~"
if joinKuerzel = BMUDNI
searchResult := searchResult "LEFT JOIN DATANORM.DNI_DATEI_INFO ON DNI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DNLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DNL_ERSTELLUSER" "~"
if joinKuerzel = BMUDNL
searchResult := searchResult "LEFT JOIN HRS.DNL_DNLISTE ON DNL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DNVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DNV_ERSTELLUSER" "~"
if joinKuerzel = BMUDNV
searchResult := searchResult "LEFT JOIN HRS.DNV_DNVORSCHLAG ON DNV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DOGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DOG_ERSTELLUSER" "~"
if joinKuerzel = BMUDOG
searchResult := searchResult "LEFT JOIN BMD.DOG_DATAOWNERGRUPPE ON DOG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DPKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DPK_ERSTELLUSER" "~"
if joinKuerzel = BMUDPK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DPK_PERSONENKEYS_PD ON DPK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DPMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DPM_ERSTELLUSER" "~"
if joinKuerzel = BMUDPM
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DPM_DFUNKTIONSPARAM ON DPM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DTHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DTH_ERSTELLUSER" "~"
if joinKuerzel = BMUDTH
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DTH_DEFTREEHEAD ON DTH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DUZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DUZ_ERSTELLUSER" "~"
if joinKuerzel = BMUDUZ
searchResult := searchResult "LEFT JOIN DATANORM.DUZ_DATANORM_WUGZU ON DUZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = E18BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = E18_ERSTELLUSER" "~"
if joinKuerzel = BMUE18
searchResult := searchResult "LEFT JOIN HRS.E18_ELDADETAIL_E18 ON E18_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EEPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EEP_ERSTELLUSER" "~"
if joinKuerzel = BMUEEP
searchResult := searchResult "LEFT JOIN WWS.EEP_EDIEINHEITENPOS ON EEP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EGSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EGS_ERSTELLUSER" "~"
if joinKuerzel = BMUEGS
searchResult := searchResult "LEFT JOIN BUERO.EGS_ERFASSUNGSGRUND ON EGS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EKOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EKO_ERSTELLUSER" "~"
if joinKuerzel = BMUEKO
searchResult := searchResult "LEFT JOIN HRS.EKO_ELDAKONTROLLE ON EKO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ERKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ERK_ERSTELLUSER" "~"
if joinKuerzel = BMUERK
searchResult := searchResult "LEFT JOIN FIBU.ERK_KONTO ON ERK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ESMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ESM_ERSTELLUSER" "~"
if joinKuerzel = BMUESM
searchResult := searchResult "LEFT JOIN HRS.ESM_ELDADETAIL_SM ON ESM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ETFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ETF_ERSTELLUSER" "~"
if joinKuerzel = BMUETF
searchResult := searchResult "LEFT JOIN BMD.ETF_ETIKETTENFORMAT ON ETF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EVKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EVK_ERSTELLUSER" "~"
if joinKuerzel = BMUEVK
searchResult := searchResult "LEFT JOIN BUERO.EVK_EVENTKATEGORIEZU ON EVK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EVLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EVL_ERSTELLUSER" "~"
if joinKuerzel = BMUEVL
searchResult := searchResult "LEFT JOIN BUERO.EVL_EVENTLIEFERANTZU ON EVL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CCCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CCC_ERSTELLUSER" "~"
if joinKuerzel = BMUCCC
searchResult := searchResult "LEFT JOIN BIL.CCC_CBCCONSTENTITY ON CCC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CDCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CDC_ERSTELLUSER" "~"
if joinKuerzel = BMUCDC
searchResult := searchResult "LEFT JOIN BMD.CDC_COMPASSDATACACHE ON CDC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CILBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CIL_ERSTELLUSER" "~"
if joinKuerzel = BMUCIL
searchResult := searchResult "LEFT JOIN BMDINTERN.CIL_CLIENTSINFOLOGIN ON CIL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CLABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CLA_ERSTELLUSER" "~"
if joinKuerzel = BMUCLA
searchResult := searchResult "LEFT JOIN BUERO.CLA_CLAUSWAHLFELD ON CLA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CLWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CLW_ERSTELLUSER" "~"
if joinKuerzel = BMUCLW
searchResult := searchResult "LEFT JOIN BUERO.CLW_CLAUSWAHLFELDWERT ON CLW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CSZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CSZ_ERSTELLUSER" "~"
if joinKuerzel = BMUCSZ
searchResult := searchResult "LEFT JOIN BMD.CSZ_CAI_SESSIONZU ON CSZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DAMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DAM_ERSTELLUSER" "~"
if joinKuerzel = BMUDAM
searchResult := searchResult "LEFT JOIN DATANORM.DAM_DATANORMARTIKELMATCH ON DAM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DAPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DAP_ERSTELLUSER" "~"
if joinKuerzel = BMUDAP
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DAP_WAAUSDRUCKPARAMETER ON DAP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DCCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DCC_ERSTELLUSER" "~"
if joinKuerzel = BMUDCC
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DCC_CHECK_COLUMNS ON DCC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DDCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DDC_ERSTELLUSER" "~"
if joinKuerzel = BMUDDC
searchResult := searchResult "LEFT JOIN BUERO.DDC_DELETEDDOCS ON DDC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DFBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DFB_ERSTELLUSER" "~"
if joinKuerzel = BMUDFB
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFB_DFELDBESCHKOPF ON DFB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DFKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DFK_ERSTELLUSER" "~"
if joinKuerzel = BMUDFK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFK_DBMDFORMATKATALOG ON DFK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DFPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DFP_ERSTELLUSER" "~"
if joinKuerzel = BMUDFP
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFP_DFUNKTIONPAKETZU ON DFP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DFUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DFU_ERSTELLUSER" "~"
if joinKuerzel = BMUDFU
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFU_DFUNKTION ON DFU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DKTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DKT_ERSTELLUSER" "~"
if joinKuerzel = BMUDKT
searchResult := searchResult "LEFT JOIN BUERO.DKT_DOCSKATEGORIE ON DKT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DKZERB
searchResult := searchResult "LEFT JOIN FIBU.ERB_BUCHUNG ON ERB_DKZID = DKZ_DKZID" "~"
if joinKuerzel = DKZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DKZ_ERSTELLUSER" "~"
if joinKuerzel = BMUDKZ
searchResult := searchResult "LEFT JOIN BUERO.DKZ_DOKUMENTZU ON DKZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DTPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DTP_ERSTELLUSER" "~"
if joinKuerzel = BMUDTP
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DTP_DEFTREEPOS ON DTP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DWUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DWU_ERSTELLUSER" "~"
if joinKuerzel = BMUDWU
searchResult := searchResult "LEFT JOIN DATANORM.DWU_WAREN_UNTER_GRUPPE ON DWU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EAD_ERSTELLUSER" "~"
if joinKuerzel = BMUEAD
searchResult := searchResult "LEFT JOIN HRS.EAD_ELDADETAIL_AD ON EAD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EDNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EDN_ERSTELLUSER" "~"
if joinKuerzel = BMUEDN
searchResult := searchResult "LEFT JOIN BMD.EDN_EDINACHRICHT ON EDN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EEMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EEM_ERSTELLUSER" "~"
if joinKuerzel = BMUEEM
searchResult := searchResult "LEFT JOIN WWS.EEM_EDIEINHEITENMAPPING ON EEM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EFPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EFP_ERSTELLUSER" "~"
if joinKuerzel = BMUEFP
searchResult := searchResult "LEFT JOIN HRS.EFP_ENTGELTFORTZAHLPROZ ON EFP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EKKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EKK_ERSTELLUSER" "~"
if joinKuerzel = BMUEKK
searchResult := searchResult "LEFT JOIN WWS.EKK_EXTERNE_KUNDENKARTEI ON EKK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EMIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EMI_ERSTELLUSER" "~"
if joinKuerzel = BMUEMI
searchResult := searchResult "LEFT JOIN HRS.EMI_ELDADETAIL_MA ON EMI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ERBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ERB_ERSTELLUSER" "~"
if joinKuerzel = BMUERB
searchResult := searchResult "LEFT JOIN FIBU.ERB_BUCHUNG ON ERB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ERXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ERX_ERSTELLUSER" "~"
if joinKuerzel = BMUERX
searchResult := searchResult "LEFT JOIN HRS.ERX_ERFASSUNGSZUSATZ ON ERX_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FEZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FEZ_ERSTELLUSER" "~"
if joinKuerzel = BMUFEZ
searchResult := searchResult "LEFT JOIN BMD.FEZ_FELDGRUPPENZU ON FEZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FFZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FFZ_ERSTELLUSER" "~"
if joinKuerzel = BMUFFZ
searchResult := searchResult "LEFT JOIN BUERO.FFZ_FRAGEBOGENFRAGEZU ON FFZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FGTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FGT_ERSTELLUSER" "~"
if joinKuerzel = BMUFGT
searchResult := searchResult "LEFT JOIN WWS.FGT_FREMDGUTSCHEINTYP ON FGT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FIGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FIG_ERSTELLUSER" "~"
if joinKuerzel = BMUFIG
searchResult := searchResult "LEFT JOIN BUERO.FIG_FIRMENGRUPPENZU ON FIG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FINBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FIN_ERSTELLUSER" "~"
if joinKuerzel = BMUFIN
searchResult := searchResult "LEFT JOIN BUERO.FIN_FINANZAMT ON FIN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FOXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FOX_ERSTELLUSER" "~"
if joinKuerzel = BMUFOX
searchResult := searchResult "LEFT JOIN BMD.FOX_FORMELTEXT ON FOX_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FSZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FSZ_ERSTELLUSER" "~"
if joinKuerzel = BMUFSZ
searchResult := searchResult "LEFT JOIN BUERO.FSZ_FREIEANTWORTSETZU ON FSZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GBTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GBT_ERSTELLUSER" "~"
if joinKuerzel = BMUGBT
searchResult := searchResult "LEFT JOIN BUERO.GBT_GEBURTSTAG ON GBT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GKOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GKO_ERSTELLUSER" "~"
if joinKuerzel = BMUGKO
searchResult := searchResult "LEFT JOIN FIBU.GKO_GESELLSCHAFTERKONTEN ON GKO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GOHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GOH_ERSTELLUSER" "~"
if joinKuerzel = BMUGOH
searchResult := searchResult "LEFT JOIN ZLE.GOH_GRUPPENORDNER_HNPOS ON GOH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GOPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GOP_ERSTELLUSER" "~"
if joinKuerzel = BMUGOP
searchResult := searchResult "LEFT JOIN BUERO.GOP_GRUPPENORDNERPERSZU ON GOP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GRSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GRS_ERSTELLUSER" "~"
if joinKuerzel = BMUGRS
searchResult := searchResult "LEFT JOIN BUERO.GRS_GELDRISIKO ON GRS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GSABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GSA_ERSTELLUSER" "~"
if joinKuerzel = BMUGSA
searchResult := searchResult "LEFT JOIN WWS.GSA_GUTSCHEINAKTION ON GSA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GTPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GTP_ERSTELLUSER" "~"
if joinKuerzel = BMUGTP
searchResult := searchResult "LEFT JOIN WWS.GTP_GROESSENTABPOS ON GTP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GWUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GWU_ERSTELLUSER" "~"
if joinKuerzel = BMUGWU
searchResult := searchResult "LEFT JOIN HRS.GWU_GWUNTERGRUPPE ON GWU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GZKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GZK_ERSTELLUSER" "~"
if joinKuerzel = BMUGZK
searchResult := searchResult "LEFT JOIN WWS.GZK_GELDZAEHLKOPF ON GZK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HPTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HPT_ERSTELLUSER" "~"
if joinKuerzel = BMUHPT
searchResult := searchResult "LEFT JOIN ZLE.HPT_HONORARPOSTEXTE ON HPT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HTKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HTK_ERSTELLUSER" "~"
if joinKuerzel = BMUHTK
searchResult := searchResult "LEFT JOIN BUERO.HTK_HILFSTAB_KONTAKTKONTR ON HTK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HVPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HVP_ERSTELLUSER" "~"
if joinKuerzel = BMUHVP
searchResult := searchResult "LEFT JOIN HRS.HVP_VERRECHUNGSPOSITION ON HVP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ICDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ICD_ERSTELLUSER" "~"
if joinKuerzel = BMUICD
searchResult := searchResult "LEFT JOIN BUERO.ICD_ICCODES ON ICD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IDPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IDP_ERSTELLUSER" "~"
if joinKuerzel = BMUIDP
searchResult := searchResult "LEFT JOIN PRODUKTION.IDP_INFO_DELAY_BREAKDOWN ON IDP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IFRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IFR_ERSTELLUSER" "~"
if joinKuerzel = BMUIFR
searchResult := searchResult "LEFT JOIN HRS.IFR_IMPORTFELDREFERENZ ON IFR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IFZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IFZ_ERSTELLUSER" "~"
if joinKuerzel = BMUIFZ
searchResult := searchResult "LEFT JOIN HRS.IFZ_IMPORTFELDREFERENZZU ON IFZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IMDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IMD_ERSTELLUSER" "~"
if joinKuerzel = BMUIMD
searchResult := searchResult "LEFT JOIN BUERO.IMD_INSERATMEDIUMDETAIL ON IMD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IMSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IMS_ERSTELLUSER" "~"
if joinKuerzel = BMUIMS
searchResult := searchResult "LEFT JOIN HRS.IMS_IMPORTSCHNITTSTELLEN ON IMS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IMZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IMZ_ERSTELLUSER" "~"
if joinKuerzel = BMUIMZ
searchResult := searchResult "LEFT JOIN BUERO.IMZ_INSTANDHMASSNZU ON IMZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ERTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ERT_ERSTELLUSER" "~"
if joinKuerzel = BMUERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EVVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EVV_ERSTELLUSER" "~"
if joinKuerzel = BMUEVV
searchResult := searchResult "LEFT JOIN BUERO.EVV_EVENTVORTRAGZU ON EVV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EVZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EVZ_ERSTELLUSER" "~"
if joinKuerzel = BMUEVZ
searchResult := searchResult "LEFT JOIN BUERO.EVZ_TLNTAGZU ON EVZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EWLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EWL_ERSTELLUSER" "~"
if joinKuerzel = BMUEWL
searchResult := searchResult "LEFT JOIN BUERO.EWL_EVENTWARTELISTE ON EWL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FAMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FAM_ERSTELLUSER" "~"
if joinKuerzel = BMUFAM
searchResult := searchResult "LEFT JOIN FIBU.FAM_AUSDRUCKMASTER ON FAM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FBKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FBK_ERSTELLUSER" "~"
if joinKuerzel = BMUFBK
searchResult := searchResult "LEFT JOIN BMD.FBK_FELDBESCHKOPF ON FBK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FDHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FDH_ERSTELLUSER" "~"
if joinKuerzel = BMUFDH
searchResult := searchResult "LEFT JOIN FIBU.FDH_FIBUDASHBOARDKOPF ON FDH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FFOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FFO_ERSTELLUSER" "~"
if joinKuerzel = BMUFFO
searchResult := searchResult "LEFT JOIN BMD.FFO_FUNKFIRMENRECHTE_OPT ON FFO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FGKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FGK_ERSTELLUSER" "~"
if joinKuerzel = BMUFGK
searchResult := searchResult "LEFT JOIN BUERO.FGK_FIRMENGKKZU ON FGK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FIXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FIX_ERSTELLUSER" "~"
if joinKuerzel = BMUFIX
searchResult := searchResult "LEFT JOIN FIBU.FIX_FIXKONTEN ON FIX_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FRDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FRD_ERSTELLUSER" "~"
if joinKuerzel = BMUFRD
searchResult := searchResult "LEFT JOIN BUERO.FRD_FRISTDOKUMENTZU ON FRD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FVWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FVW_ERSTELLUSER" "~"
if joinKuerzel = BMUFVW
searchResult := searchResult "LEFT JOIN BMD.FVW_FELDVORGABEWERTE ON FVW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FWEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FWE_ERSTELLUSER" "~"
if joinKuerzel = BMUFWE
searchResult := searchResult "LEFT JOIN HRS.FWE_FIXWERTE ON FWE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GEABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GEA_ERSTELLUSER" "~"
if joinKuerzel = BMUGEA
searchResult := searchResult "LEFT JOIN BUERO.GEA_GESCHAEFTSART ON GEA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GEGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GEG_ERSTELLUSER" "~"
if joinKuerzel = BMUGEG
searchResult := searchResult "LEFT JOIN ZLE.GEG_GEGENSTANDSWERTE ON GEG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GKKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GKK_ERSTELLUSER" "~"
if joinKuerzel = BMUGKK
searchResult := searchResult "LEFT JOIN BUERO.GKK_KRANKENKASSA ON GKK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GLVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GLV_ERSTELLUSER" "~"
if joinKuerzel = BMUGLV
searchResult := searchResult "LEFT JOIN BIL.GLV_GLIEDERUNGSVERWEIS ON GLV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GOIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GOI_ERSTELLUSER" "~"
if joinKuerzel = BMUGOI
searchResult := searchResult "LEFT JOIN BUERO.GOI_GRUPPENORDNERINSTZU ON GOI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GSPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GSP_ERSTELLUSER" "~"
if joinKuerzel = BMUGSP
searchResult := searchResult "LEFT JOIN WWS.GSP_GUTSCHEINPOSITION ON GSP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GZMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GZM_ERSTELLUSER" "~"
if joinKuerzel = BMUGZM
searchResult := searchResult "LEFT JOIN HRS.GZM_GLAEUBIGERZU_MONAT ON GZM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GZUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GZU_ERSTELLUSER" "~"
if joinKuerzel = BMUGZU
searchResult := searchResult "LEFT JOIN WWS.GZU_GEFAHRENGUTZU ON GZU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HFTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HFT_ERSTELLUSER" "~"
if joinKuerzel = BMUHFT
searchResult := searchResult "LEFT JOIN BUERO.HFT_HILFSTABELLE ON HFT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HISBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HIS_ERSTELLUSER" "~"
if joinKuerzel = BMUHIS
searchResult := searchResult "LEFT JOIN HRS.HIS_HISTORIE ON HIS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HLBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HLB_ERSTELLUSER" "~"
if joinKuerzel = BMUHLB
searchResult := searchResult "LEFT JOIN BMDENTW.HLB_HILFEBAUSTEIN ON HLB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HZLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HZL_ERSTELLUSER" "~"
if joinKuerzel = BMUHZL
searchResult := searchResult "LEFT JOIN BUERO.HZL_HAUPTZIEL ON HZL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IAPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IAP_ERSTELLUSER" "~"
if joinKuerzel = BMUIAP
searchResult := searchResult "LEFT JOIN WWS.IAP_INVABWTPOS ON IAP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FZUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FZU_ERSTELLUSER" "~"
if joinKuerzel = BMUFZU
searchResult := searchResult "LEFT JOIN FIBU.FZU_FIBUFIBUZU ON FZU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GAS_ERSTELLUSER" "~"
if joinKuerzel = BMUGAS
searchResult := searchResult "LEFT JOIN BMD.GAS_GLOBAL_ACTIONSTACK ON GAS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GAWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GAW_ERSTELLUSER" "~"
if joinKuerzel = BMUGAW
searchResult := searchResult "LEFT JOIN ZLE.GAW_GEBUEHRARTIKELWERT ON GAW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GEKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GEK_ERSTELLUSER" "~"
if joinKuerzel = BMUGEK
searchResult := searchResult "LEFT JOIN WWS.GEK_GEFAHRENGUTKOPF ON GEK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GFZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GFZ_ERSTELLUSER" "~"
if joinKuerzel = BMUGFZ
searchResult := searchResult "LEFT JOIN BIL.GFZ_XMLJABGESFIRZU ON GFZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GKLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GKL_ERSTELLUSER" "~"
if joinKuerzel = BMUGKL
searchResult := searchResult "LEFT JOIN BIL.GKL_GROESSENKLASSEN ON GKL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GLZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GLZ_ERSTELLUSER" "~"
if joinKuerzel = BMUGLZ
searchResult := searchResult "LEFT JOIN BIL.GLZ_GLIEDERUNGSZAHL ON GLZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GROBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GRO_ERSTELLUSER" "~"
if joinKuerzel = BMUGRO
searchResult := searchResult "LEFT JOIN WWS.GRO_GROESSE ON GRO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GRTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GRT_ERSTELLUSER" "~"
if joinKuerzel = BMUGRT
searchResult := searchResult "LEFT JOIN WWS.GRT_GROESSENTAB ON GRT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GVTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GVT_ERSTELLUSER" "~"
if joinKuerzel = BMUGVT
searchResult := searchResult "LEFT JOIN HRS.GVT_GEMVERTEILUNG ON GVT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GZJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GZJ_ERSTELLUSER" "~"
if joinKuerzel = BMUGZJ
searchResult := searchResult "LEFT JOIN HRS.GZJ_GLAEUBIGERZU_JAHR ON GZJ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HAK_ERSTELLUSER" "~"
if joinKuerzel = BMUHAK
searchResult := searchResult "LEFT JOIN HRS.HAK_AKONTO ON HAK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IAK_ERSTELLUSER" "~"
if joinKuerzel = BMUIAK
searchResult := searchResult "LEFT JOIN WWS.IAK_INVABWTKOPF ON IAK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IAMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IAM_ERSTELLUSER" "~"
if joinKuerzel = BMUIAM
searchResult := searchResult "LEFT JOIN BUERO.IAM_INSTANZAKTIONMITARB ON IAM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IEPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IEP_ERSTELLUSER" "~"
if joinKuerzel = BMUIEP
searchResult := searchResult "LEFT JOIN BMD.IEP_IMPEXP_PROTOKOLL ON IEP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IFABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IFA_ERSTELLUSER" "~"
if joinKuerzel = BMUIFA
searchResult := searchResult "LEFT JOIN WWSINDIV.IFA_FAKTURA ON IFA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IKZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IKZ_ERSTELLUSER" "~"
if joinKuerzel = BMUIKZ
searchResult := searchResult "LEFT JOIN BUERO.IKZ_INSTITUTIONKONTAKTZU ON IKZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IOGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IOG_ERSTELLUSER" "~"
if joinKuerzel = BMUIOG
searchResult := searchResult "LEFT JOIN BUERO.IOG_INSTANDHOBJEKTGRUPPE ON IOG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IPMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IPM_ERSTELLUSER" "~"
if joinKuerzel = BMUIPM
searchResult := searchResult "LEFT JOIN HRS.IPM_INDIVPROGM ON IPM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ITVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ITV_ERSTELLUSER" "~"
if joinKuerzel = BMUITV
searchResult := searchResult "LEFT JOIN BMD.ITV_INTERVALL ON ITV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IVKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IVK_ERSTELLUSER" "~"
if joinKuerzel = BMUIVK
searchResult := searchResult "LEFT JOIN WWS.IVK_INVENTURKOPF ON IVK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IVZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IVZ_ERSTELLUSER" "~"
if joinKuerzel = BMUIVZ
searchResult := searchResult "LEFT JOIN BUERO.IVZ_INSOLVENZ ON IVZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KAZ_ERSTELLUSER" "~"
if joinKuerzel = BMUKAZ
searchResult := searchResult "LEFT JOIN BUERO.KAZ_KATEGORIEZU ON KAZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KBZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KBZ_ERSTELLUSER" "~"
if joinKuerzel = BMUKBZ
searchResult := searchResult "LEFT JOIN KORE.KBZ_KOSTENBAUMZU ON KBZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KGVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KGV_ERSTELLUSER" "~"
if joinKuerzel = BMUKGV
searchResult := searchResult "LEFT JOIN FIBU.KGV_KONTENGUVVERBUCHUNG ON KGV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KKVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KKV_ERSTELLUSER" "~"
if joinKuerzel = BMUKKV
searchResult := searchResult "LEFT JOIN KORE.KKV_KSTKONTOVARIATOR ON KKV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FZEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FZE_ERSTELLUSER" "~"
if joinKuerzel = BMUFZE
searchResult := searchResult "LEFT JOIN FIBU.FZE_ZAHLSCHLUESSELEINGABE ON FZE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GAD_ERSTELLUSER" "~"
if joinKuerzel = BMUGAD
searchResult := searchResult "LEFT JOIN BUERO.GAD_GHOSTADRESSE ON GAD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GAZ_ERSTELLUSER" "~"
if joinKuerzel = BMUGAZ
searchResult := searchResult "LEFT JOIN BUERO.GAZ_GESELLANGEHOERIGERZU ON GAZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GBKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GBK_ERSTELLUSER" "~"
if joinKuerzel = BMUGBK
searchResult := searchResult "LEFT JOIN HRS.GBK_GEBIETEKV ON GBK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GEBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GEB_ERSTELLUSER" "~"
if joinKuerzel = BMUGEB
searchResult := searchResult "LEFT JOIN ZLE.GEB_GEBUEHRENTABELLEN ON GEB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GZGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GZG_ERSTELLUSER" "~"
if joinKuerzel = BMUGZG
searchResult := searchResult "LEFT JOIN WWS.GZG_GELDZAEHLUNG ON GZG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HAD_ERSTELLUSER" "~"
if joinKuerzel = BMUHAD
searchResult := searchResult "LEFT JOIN HRS.HAD_AUSDRUCKDETAIL ON HAD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HINBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HIN_ERSTELLUSER" "~"
if joinKuerzel = BMUHIN
searchResult := searchResult "LEFT JOIN HRS.HIN_HINWEIS ON HIN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HTBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HTB_ERSTELLUSER" "~"
if joinKuerzel = BMUHTB
searchResult := searchResult "LEFT JOIN BMDENTW.HTB_HILFETEXTBAUSTEIN ON HTB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HVBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HVB_ERSTELLUSER" "~"
if joinKuerzel = BMUHVB
searchResult := searchResult "LEFT JOIN HRS.HVB_VERRECHNUNGSBASIS ON HVB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IFTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IFT_ERSTELLUSER" "~"
if joinKuerzel = BMUIFT
searchResult := searchResult "LEFT JOIN BUERO.IFT_INFOTAB ON IFT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IPDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IPD_ERSTELLUSER" "~"
if joinKuerzel = BMUIPD
searchResult := searchResult "LEFT JOIN HRS.IPD_INDIVPROGD ON IPD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IVGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IVG_ERSTELLUSER" "~"
if joinKuerzel = BMUIVG
searchResult := searchResult "LEFT JOIN BMD.IVG_INTERVALLGRUPPE ON IVG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KBEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KBE_ERSTELLUSER" "~"
if joinKuerzel = BMUKBE
searchResult := searchResult "LEFT JOIN WWS.KBE_KASSEBUCHEINZELNW ON KBE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KFGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KFG_ERSTELLUSER" "~"
if joinKuerzel = BMUKFG
searchResult := searchResult "LEFT JOIN WWS.KFG_KTOFINDBETRGRENZEN ON KFG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KGDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KGD_ERSTELLUSER" "~"
if joinKuerzel = BMUKGD
searchResult := searchResult "LEFT JOIN WWS.KGD_KASSENGELDLADE ON KGD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KIMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KIM_ERSTELLUSER" "~"
if joinKuerzel = BMUKIM
searchResult := searchResult "LEFT JOIN FIBU.KIM_KONTENCSVIMPORTMODELL ON KIM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KOCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KOC_ERSTELLUSER" "~"
if joinKuerzel = BMUKOC
searchResult := searchResult "LEFT JOIN PRODUKTION.KOC_KOMBI_CHARGE ON KOC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KPKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KPK_ERSTELLUSER" "~"
if joinKuerzel = BMUKPK
searchResult := searchResult "LEFT JOIN KORE.KPK_PLANKENNZAHLEN ON KPK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KRABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KRA_ERSTELLUSER" "~"
if joinKuerzel = BMUKRA
searchResult := searchResult "LEFT JOIN WWS.KRA_KASSENRABATT ON KRA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KSEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KSE_ERSTELLUSER" "~"
if joinKuerzel = BMUKSE
searchResult := searchResult "LEFT JOIN KORE.KSE_KORESCHLUESSELEINGABE ON KSE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KSKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KSK_ERSTELLUSER" "~"
if joinKuerzel = BMUKSK
searchResult := searchResult "LEFT JOIN KORE.KSK_KORESCHLUESSELKOPF ON KSK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KSUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KSU_ERSTELLUSER" "~"
if joinKuerzel = BMUKSU
searchResult := searchResult "LEFT JOIN WWS.KSU_KASSENSUMMENKOPF ON KSU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KTABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KTA_ERSTELLUSER" "~"
if joinKuerzel = BMUKTA
searchResult := searchResult "LEFT JOIN WWS.KTA_KOSTENTYPENAUFTLG ON KTA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KUKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KUK_ERSTELLUSER" "~"
if joinKuerzel = BMUKUK
searchResult := searchResult "LEFT JOIN WWS.KUK_KUNDENKARTEI ON KUK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KZKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KZK_ERSTELLUSER" "~"
if joinKuerzel = BMUKZK
searchResult := searchResult "LEFT JOIN KORE.KZK_ZUSCHLAGKOPF ON KZK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EKJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EKJ_ERSTELLUSER" "~"
if joinKuerzel = BMUEKJ
searchResult := searchResult "LEFT JOIN PRODUKTION.EKJ_ERP_KOREJOURNAL ON EKJ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ELVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ELV_ERSTELLUSER" "~"
if joinKuerzel = BMUELV
searchResult := searchResult "LEFT JOIN WWS.ELV_ERFASSPOSLIEFERVOR ON ELV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EMBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EMB_ERSTELLUSER" "~"
if joinKuerzel = BMUEMB
searchResult := searchResult "LEFT JOIN HRS.EMB_ELDADETAIL_MBGM_P ON EMB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ERABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ERA_ERSTELLUSER" "~"
if joinKuerzel = BMUERA
searchResult := searchResult "LEFT JOIN WWS.ERA_ERFASSRABATT ON ERA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ES2BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ES2_ERSTELLUSER" "~"
if joinKuerzel = BMUES2
searchResult := searchResult "LEFT JOIN HRS.ES2_ELDADETAIL_SM2 ON ES2_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ETLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ETL_ERSTELLUSER" "~"
if joinKuerzel = BMUETL
searchResult := searchResult "LEFT JOIN BUERO.ETL_ENTGANGENE_TELEFONATE ON ETL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ETZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ETZ_ERSTELLUSER" "~"
if joinKuerzel = BMUETZ
searchResult := searchResult "LEFT JOIN BUERO.ETZ_EVENTTERMINZU ON ETZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EVABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EVA_ERSTELLUSER" "~"
if joinKuerzel = BMUEVA
searchResult := searchResult "LEFT JOIN BUERO.EVA_EVENTTEAM ON EVA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EVIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EVI_ERSTELLUSER" "~"
if joinKuerzel = BMUEVI
searchResult := searchResult "LEFT JOIN BUERO.EVI_EVIDENZ ON EVI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EVMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EVM_ERSTELLUSER" "~"
if joinKuerzel = BMUEVM
searchResult := searchResult "LEFT JOIN HRS.EVM_ELDADETAIL_VM ON EVM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FAPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FAP_ERSTELLUSER" "~"
if joinKuerzel = BMUFAP
searchResult := searchResult "LEFT JOIN BUERO.FAP_FAPERSZU ON FAP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FEHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FEH_ERSTELLUSER" "~"
if joinKuerzel = BMUFEH
searchResult := searchResult "LEFT JOIN WWS.FEH_FERTEP_HISTORIE ON FEH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FGABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FGA_ERSTELLUSER" "~"
if joinKuerzel = BMUFGA
searchResult := searchResult "LEFT JOIN BUERO.FGA_FRAGEBOGENAUSWAHL ON FGA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FIWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FIW_ERSTELLUSER" "~"
if joinKuerzel = BMUFIW
searchResult := searchResult "LEFT JOIN BUERO.FIW_FIRMENWERTE ON FIW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FKKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FKK_ERSTELLUSER" "~"
if joinKuerzel = BMUFKK
searchResult := searchResult "LEFT JOIN ZLE.FKK_FAKTURAKZKUNDENZU ON FKK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FORBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FOR_ERSTELLUSER" "~"
if joinKuerzel = BMUFOR
searchResult := searchResult "LEFT JOIN BMD.FOR_FORMEL ON FOR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FPOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FPO_ERSTELLUSER" "~"
if joinKuerzel = BMUFPO
searchResult := searchResult "LEFT JOIN BUERO.FPO_FAVPROJEKTZU ON FPO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FPRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FPR_ERSTELLUSER" "~"
if joinKuerzel = BMUFPR
searchResult := searchResult "LEFT JOIN FIBU.FPR_FIBUPROZENTSATZ ON FPR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FRABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FRA_ERSTELLUSER" "~"
if joinKuerzel = BMUFRA
searchResult := searchResult "LEFT JOIN BUERO.FRA_FRISTART ON FRA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FRTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FRT_ERSTELLUSER" "~"
if joinKuerzel = BMUFRT
searchResult := searchResult "LEFT JOIN ZLE.FRT_FAKTURATYP ON FRT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FTPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FTP_ERSTELLUSER" "~"
if joinKuerzel = BMUFTP
searchResult := searchResult "LEFT JOIN WWS.FTP_FARBENTABPOS ON FTP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FWFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FWF_ERSTELLUSER" "~"
if joinKuerzel = BMUFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FZPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FZP_ERSTELLUSER" "~"
if joinKuerzel = BMUFZP
searchResult := searchResult "LEFT JOIN FIBU.FZP_FINANZPLAN ON FZP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GEFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GEF_ERSTELLUSER" "~"
if joinKuerzel = BMUGEF
searchResult := searchResult "LEFT JOIN WWS.GEF_GEFAHRENGUTSTAMM ON GEF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GIDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GID_ERSTELLUSER" "~"
if joinKuerzel = BMUGID
searchResult := searchResult "LEFT JOIN BUERO.GID_GELDIDENTIFIKATION ON GID_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GLUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GLU_ERSTELLUSER" "~"
if joinKuerzel = BMUGLU
searchResult := searchResult "LEFT JOIN WWS.GLU_GELDLADENZUORDNUNG ON GLU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FATBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FAT_ERSTELLUSER" "~"
if joinKuerzel = BMUFAT
searchResult := searchResult "LEFT JOIN WWS.FAT_FARBENTAB ON FAT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FAZ_ERSTELLUSER" "~"
if joinKuerzel = BMUFAZ
searchResult := searchResult "LEFT JOIN BUERO.FAZ_FRAGEANTWORTZU ON FAZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FDKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FDK_ERSTELLUSER" "~"
if joinKuerzel = BMUFDK
searchResult := searchResult "LEFT JOIN BMD.FDK_FELDWERTEKOPF ON FDK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FGBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FGB_ERSTELLUSER" "~"
if joinKuerzel = BMUFGB
searchResult := searchResult "LEFT JOIN BUERO.FGB_FRAGEBOGEN ON FGB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FGRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FGR_ERSTELLUSER" "~"
if joinKuerzel = BMUFGR
searchResult := searchResult "LEFT JOIN BUERO.FGR_FIRMENGERICHTZU ON FGR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FIPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FIP_ERSTELLUSER" "~"
if joinKuerzel = BMUFIP
searchResult := searchResult "LEFT JOIN BMD.FIP_FILTERPANEL ON FIP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FKZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FKZ_ERSTELLUSER" "~"
if joinKuerzel = BMUFKZ
searchResult := searchResult "LEFT JOIN FIBU.FKZ_FREIESKZ ON FKZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FPZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FPZ_ERSTELLUSER" "~"
if joinKuerzel = BMUFPZ
searchResult := searchResult "LEFT JOIN HRS.FPZ_FIRMENPENSIONSKASSEZU ON FPZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FRIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FRI_ERSTELLUSER" "~"
if joinKuerzel = BMUFRI
searchResult := searchResult "LEFT JOIN BUERO.FRI_FRISTVERLAENGERUNGEN ON FRI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GAUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GAU_ERSTELLUSER" "~"
if joinKuerzel = BMUGAU
searchResult := searchResult "LEFT JOIN BIL.GAU_GEWINNAUFTEILUNG ON GAU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GEPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GEP_ERSTELLUSER" "~"
if joinKuerzel = BMUGEP
searchResult := searchResult "LEFT JOIN WWS.GEP_GEFAHRENGUTPOS ON GEP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GNDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GND_ERSTELLUSER" "~"
if joinKuerzel = BMUGND
searchResult := searchResult "LEFT JOIN BIL.GND_GAU_DATA ON GND_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GPOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GPO_ERSTELLUSER" "~"
if joinKuerzel = BMUGPO
searchResult := searchResult "LEFT JOIN BUERO.GPO_GRUPPENORDNER ON GPO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GWIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GWI_ERSTELLUSER" "~"
if joinKuerzel = BMUGWI
searchResult := searchResult "LEFT JOIN BUERO.GWI_GELDWIEREG ON GWI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GWZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GWZ_ERSTELLUSER" "~"
if joinKuerzel = BMUGWZ
searchResult := searchResult "LEFT JOIN HRS.GWZ_GWBANKZU ON GWZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HAMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HAM_ERSTELLUSER" "~"
if joinKuerzel = BMUHAM
searchResult := searchResult "LEFT JOIN HRS.HAM_AUSDRUCKMASTER ON HAM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HKPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HKP_ERSTELLUSER" "~"
if joinKuerzel = BMUHKP
searchResult := searchResult "LEFT JOIN BMDENTW.HKP_HILFEKAPITEL ON HKP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HKTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HKT_ERSTELLUSER" "~"
if joinKuerzel = BMUHKT
searchResult := searchResult "LEFT JOIN BMDENTW.HKT_HILFEKAPITELTBZU ON HKT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HLFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HLF_ERSTELLUSER" "~"
if joinKuerzel = BMUHLF
searchResult := searchResult "LEFT JOIN BMDENTW.HLF_HILFEFAVORITEN ON HLF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HLLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HLL_ERSTELLUSER" "~"
if joinKuerzel = BMUHLL
searchResult := searchResult "LEFT JOIN BMDENTW.HLL_HILFEDOKUMENT ON HLL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HSABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HSA_ERSTELLUSER" "~"
if joinKuerzel = BMUHSA
searchResult := searchResult "LEFT JOIN HRS.HSA_SORTIERART ON HSA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HSOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HSO_ERSTELLUSER" "~"
if joinKuerzel = BMUHSO
searchResult := searchResult "LEFT JOIN HRS.HSO_SORTIERPOS ON HSO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IMMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IMM_ERSTELLUSER" "~"
if joinKuerzel = BMUIMM
searchResult := searchResult "LEFT JOIN BUERO.IMM_INSERATMEDIUMMASTER ON IMM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IPRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IPR_ERSTELLUSER" "~"
if joinKuerzel = BMUIPR
searchResult := searchResult "LEFT JOIN WWS.IPR_IMPORTREFERENZ_55 ON IPR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IPZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IPZ_ERSTELLUSER" "~"
if joinKuerzel = BMUIPZ
searchResult := searchResult "LEFT JOIN BUERO.IPZ_INTERNPROJEKTZU ON IPZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ITBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ITB_ERSTELLUSER" "~"
if joinKuerzel = BMUITB
searchResult := searchResult "LEFT JOIN BUERO.ITB_INFOTABBEWERTUNG ON ITB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ERZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ERZ_ERSTELLUSER" "~"
if joinKuerzel = BMUERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ESDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ESD_ERSTELLUSER" "~"
if joinKuerzel = BMUESD
searchResult := searchResult "LEFT JOIN WWS.ESD_ERFASSUNGSERVICEDATEN ON ESD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ESTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EST_ERSTELLUSER" "~"
if joinKuerzel = BMUEST
searchResult := searchResult "LEFT JOIN BMD.EST_EDISCHEMATYP ON EST_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EVNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EVN_ERSTELLUSER" "~"
if joinKuerzel = BMUEVN
searchResult := searchResult "LEFT JOIN BUERO.EVN_TEILNEHMER ON EVN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EVSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EVS_ERSTELLUSER" "~"
if joinKuerzel = BMUEVS
searchResult := searchResult "LEFT JOIN BUERO.EVS_EVENTLNSTATUS ON EVS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FAK_ERSTELLUSER" "~"
if joinKuerzel = BMUFAK
searchResult := searchResult "LEFT JOIN HRS.FAK_FAKTORTABELLE ON FAK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FARBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FAR_ERSTELLUSER" "~"
if joinKuerzel = BMUFAR
searchResult := searchResult "LEFT JOIN BMD.FAR_FARBCODE ON FAR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FBRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FBR_ERSTELLUSER" "~"
if joinKuerzel = BMUFBR
searchResult := searchResult "LEFT JOIN BUERO.FBR_FIRMENBRANCHEZU ON FBR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FFBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FFB_ERSTELLUSER" "~"
if joinKuerzel = BMUFFB
searchResult := searchResult "LEFT JOIN BMD.FFB_FUNKFELDBESCHRECHTE ON FFB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FFRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FFR_ERSTELLUSER" "~"
if joinKuerzel = BMUFFR
searchResult := searchResult "LEFT JOIN BMD.FFR_FUNKFIRMENRECHTE ON FFR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FKTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FKT_ERSTELLUSER" "~"
if joinKuerzel = BMUFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FMJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FMJ_ERSTELLUSER" "~"
if joinKuerzel = BMUFMJ
searchResult := searchResult "LEFT JOIN WWS.FMJ_FISKALMODULJOURNAL ON FMJ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FNSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FNS_ERSTELLUSER" "~"
if joinKuerzel = BMUFNS
searchResult := searchResult "LEFT JOIN BIL.FNS_FINANZINSTRUMENTE ON FNS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FRXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FRX_ERSTELLUSER" "~"
if joinKuerzel = BMUFRX
searchResult := searchResult "LEFT JOIN BMDENTW.FRX_FRAMES ON FRX_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FSOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FSO_ERSTELLUSER" "~"
if joinKuerzel = BMUFSO
searchResult := searchResult "LEFT JOIN FIBU.FSO_SORTIERPOS ON FSO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FSPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FSP_ERSTELLUSER" "~"
if joinKuerzel = BMUFSP
searchResult := searchResult "LEFT JOIN FIBU.FSP_SPALTENKOPF ON FSP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FZKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FZK_ERSTELLUSER" "~"
if joinKuerzel = BMUFZK
searchResult := searchResult "LEFT JOIN FIBU.FZK_ZAHLSCHLUESSELKOPF ON FZK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GERBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GER_ERSTELLUSER" "~"
if joinKuerzel = BMUGER
searchResult := searchResult "LEFT JOIN BUERO.GER_GERICHT ON GER_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GKVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GKV_ERSTELLUSER" "~"
if joinKuerzel = BMUGKV
searchResult := searchResult "LEFT JOIN HRS.GKV_GEBSTUFEKV ON GKV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GLSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GLS_ERSTELLUSER" "~"
if joinKuerzel = BMUGLS
searchResult := searchResult "LEFT JOIN BIL.GLS_GLIEDERUNGSSCHEMA ON GLS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GLTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GLT_ERSTELLUSER" "~"
if joinKuerzel = BMUGLT
searchResult := searchResult "LEFT JOIN BIL.GLT_GLIEDERUNGSTEXT ON GLT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GMGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GMG_ERSTELLUSER" "~"
if joinKuerzel = BMUGMG
searchResult := searchResult "LEFT JOIN BIL.GMG_GRUPPENMITGLIEDER ON GMG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GSKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GSK_ERSTELLUSER" "~"
if joinKuerzel = BMUGSK
searchResult := searchResult "LEFT JOIN WWS.GSK_GUTSCHEINKOPF ON GSK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GSTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GST_ERSTELLUSER" "~"
if joinKuerzel = BMUGST
searchResult := searchResult "LEFT JOIN BUERO.GST_GEBUEHRENSTUFE ON GST_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GWBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GWB_ERSTELLUSER" "~"
if joinKuerzel = BMUGWB
searchResult := searchResult "LEFT JOIN HRS.GWB_GWBERECHNUNG ON GWB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GWPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GWP_ERSTELLUSER" "~"
if joinKuerzel = BMUGWP
searchResult := searchResult "LEFT JOIN BMD.GWP_GROUPWISEPOSTOFFICE ON GWP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = INLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = INL_ERSTELLUSER" "~"
if joinKuerzel = BMUINL
searchResult := searchResult "LEFT JOIN ZLE.INL_INTERNLEISTUNG ON INL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ISZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ISZ_ERSTELLUSER" "~"
if joinKuerzel = BMUISZ
searchResult := searchResult "LEFT JOIN BUERO.ISZ_INFOTABSTELLEZU ON ISZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IWVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IWV_ERSTELLUSER" "~"
if joinKuerzel = BMUIWV
searchResult := searchResult "LEFT JOIN BUERO.IWV_INTRASTATWV ON IWV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = JAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = JAK_ERSTELLUSER" "~"
if joinKuerzel = BMUJAK
searchResult := searchResult "LEFT JOIN BIL.JAK_JABKOPF ON JAK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KAS_ERSTELLUSER" "~"
if joinKuerzel = BMUKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KDZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KDZ_ERSTELLUSER" "~"
if joinKuerzel = BMUKDZ
searchResult := searchResult "LEFT JOIN BUERO.KDZ_KOREDOKUMENTZU ON KDZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KIZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KIZ_ERSTELLUSER" "~"
if joinKuerzel = BMUKIZ
searchResult := searchResult "LEFT JOIN BUERO.KIZ_KUND_INFOTABPOSZU ON KIZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KJOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KJO_ERSTELLUSER" "~"
if joinKuerzel = BMUKJO
searchResult := searchResult "LEFT JOIN BUERO.KJO_KONTAKTJOURNAL ON KJO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KKSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KKS_ERSTELLUSER" "~"
if joinKuerzel = BMUKKS
searchResult := searchResult "LEFT JOIN BUERO.KKS_KONSOLIDIERUNGSKREIS ON KKS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KKZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KKZ_ERSTELLUSER" "~"
if joinKuerzel = BMUKKZ
searchResult := searchResult "LEFT JOIN BUERO.KKZ_KREDITKARTENKUNDENZU ON KKZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KOZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KOZ_ERSTELLUSER" "~"
if joinKuerzel = BMUKOZ
searchResult := searchResult "LEFT JOIN PRODUKTION.KOZ_KOMMISSIONIERUNG_ZU ON KOZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KPPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KPP_ERSTELLUSER" "~"
if joinKuerzel = BMUKPP
searchResult := searchResult "LEFT JOIN BUERO.KPP_KAMPAGNENPHASE ON KPP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KRIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KRI_ERSTELLUSER" "~"
if joinKuerzel = BMUKRI
searchResult := searchResult "LEFT JOIN BUERO.KRI_KRITERIUM ON KRI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KSDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KSD_ERSTELLUSER" "~"
if joinKuerzel = BMUKSD
searchResult := searchResult "LEFT JOIN WWS.KSD_KASSENSUMMENDETAIL ON KSD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KSZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KSZ_ERSTELLUSER" "~"
if joinKuerzel = BMUKSZ
searchResult := searchResult "LEFT JOIN HRS.KSZ_BBKALKSZ ON KSZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KTKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KTK_ERSTELLUSER" "~"
if joinKuerzel = BMUKTK
searchResult := searchResult "LEFT JOIN FIBU.KTK_KONTENKREIS ON KTK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KVSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KVS_ERSTELLUSER" "~"
if joinKuerzel = BMUKVS
searchResult := searchResult "LEFT JOIN HRS.KVS_KSERVICEZU ON KVS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KZEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KZE_ERSTELLUSER" "~"
if joinKuerzel = BMUKZE
searchResult := searchResult "LEFT JOIN KORE.KZE_ZUSCHLAGEINGABE ON KZE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KZUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KZU_ERSTELLUSER" "~"
if joinKuerzel = BMUKZU
searchResult := searchResult "LEFT JOIN KORE.KZU_KOREKOREZU ON KZU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAD_ERSTELLUSER" "~"
if joinKuerzel = BMULAD
searchResult := searchResult "LEFT JOIN WWS.LAD_LAGERARTIKELDATEN ON LAD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LANBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAN_ERSTELLUSER" "~"
if joinKuerzel = BMULAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LBKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LBK_ERSTELLUSER" "~"
if joinKuerzel = BMULBK
searchResult := searchResult "LEFT JOIN WWS.LBK_LAGERBUCHKENNZ ON LBK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LEBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LEB_ERSTELLUSER" "~"
if joinKuerzel = BMULEB
searchResult := searchResult "LEFT JOIN BUERO.LEB_LEBENSLAUF ON LEB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LFIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LFI_ERSTELLUSER" "~"
if joinKuerzel = BMULFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LFZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LFZ_ERSTELLUSER" "~"
if joinKuerzel = BMULFZ
searchResult := searchResult "LEFT JOIN BMDDEFAULT.LFZ_DLIZG_NTC_ZU ON LFZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LITBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LIT_ERSTELLUSER" "~"
if joinKuerzel = BMULIT
searchResult := searchResult "LEFT JOIN HRS.LIT_LOHNINSTITUTION ON LIT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IMNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IMN_ERSTELLUSER" "~"
if joinKuerzel = BMUIMN
searchResult := searchResult "LEFT JOIN BUERO.IMN_INSTANDHMASSN ON IMN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = INPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = INP_ERSTELLUSER" "~"
if joinKuerzel = BMUINP
searchResult := searchResult "LEFT JOIN BMD.INP_INTPARAM ON INP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = INTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = INT_ERSTELLUSER" "~"
if joinKuerzel = BMUINT
searchResult := searchResult "LEFT JOIN BUERO.INT_INTRASTAT ON INT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IVPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IVP_ERSTELLUSER" "~"
if joinKuerzel = BMUIVP
searchResult := searchResult "LEFT JOIN WWS.IVP_INVENTURPOSITION ON IVP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KAPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KAP_ERSTELLUSER" "~"
if joinKuerzel = BMUKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KFIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KFI_ERSTELLUSER" "~"
if joinKuerzel = BMUKFI
searchResult := searchResult "LEFT JOIN BUERO.KFI_KORETYPENFINDUNG ON KFI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KGBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KGB_ERSTELLUSER" "~"
if joinKuerzel = BMUKGB
searchResult := searchResult "LEFT JOIN PRODUKTION.KGB_KATZU_PPSKAPA ON KGB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KGLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KGL_ERSTELLUSER" "~"
if joinKuerzel = BMUKGL
searchResult := searchResult "LEFT JOIN KORE.KGL_KOREGLIEDERUNG ON KGL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KGSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KGS_ERSTELLUSER" "~"
if joinKuerzel = BMUKGS
searchResult := searchResult "LEFT JOIN WWS.KGS_KATZU_ARBEITSGANG ON KGS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KKABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KKA_ERSTELLUSER" "~"
if joinKuerzel = BMUKKA
searchResult := searchResult "LEFT JOIN BUERO.KKA_KREDITKARTENART ON KKA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KMPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KMP_ERSTELLUSER" "~"
if joinKuerzel = BMUKMP
searchResult := searchResult "LEFT JOIN KORE.KMP_PLANMENGEN ON KMP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KOWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KOW_ERSTELLUSER" "~"
if joinKuerzel = BMUKOW
searchResult := searchResult "LEFT JOIN HRS.KOW_KOLLEKTIVVERTRAGWERTE ON KOW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KPRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KPR_ERSTELLUSER" "~"
if joinKuerzel = BMUKPR
searchResult := searchResult "LEFT JOIN KORE.KPR_PLANPREISE ON KPR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KSMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KSM_ERSTELLUSER" "~"
if joinKuerzel = BMUKSM
searchResult := searchResult "LEFT JOIN WWS.KSM_KASSENSUMMEN ON KSM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KUDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KUD_ERSTELLUSER" "~"
if joinKuerzel = BMUKUD
searchResult := searchResult "LEFT JOIN BMD.KUD_KURSDATEI ON KUD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KUGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KUG_ERSTELLUSER" "~"
if joinKuerzel = BMUKUG
searchResult := searchResult "LEFT JOIN BUERO.KUG_KUNDENGRUPPE ON KUG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KURBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KUR_ERSTELLUSER" "~"
if joinKuerzel = BMUKUR
searchResult := searchResult "LEFT JOIN KORE.KUR_UMLAGEREIHENFOLGE ON KUR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KVGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KVG_ERSTELLUSER" "~"
if joinKuerzel = BMUKVG
searchResult := searchResult "LEFT JOIN KORE.KVG_KORESTAMMVERTEILUNG ON KVG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KZABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KZA_ERSTELLUSER" "~"
if joinKuerzel = BMUKZA
searchResult := searchResult "LEFT JOIN WWS.KZA_KASSENZAHLUNGSARTENZU ON KZA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LAGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAG_ERSTELLUSER" "~"
if joinKuerzel = BMULAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAS_ERSTELLUSER" "~"
if joinKuerzel = BMULAS
searchResult := searchResult "LEFT JOIN BIL.LAS_LATENTE_STEUERN ON LAS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LDDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LDD_ERSTELLUSER" "~"
if joinKuerzel = BMULDD
searchResult := searchResult "LEFT JOIN BMD.LDD_LIZDATEIDOWNLOAD ON LDD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LGBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LGB_ERSTELLUSER" "~"
if joinKuerzel = BMULGB
searchResult := searchResult "LEFT JOIN HRS.LGB_LGVBERECHNUNGEN ON LGB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LGFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LGF_ERSTELLUSER" "~"
if joinKuerzel = BMULGF
searchResult := searchResult "LEFT JOIN HRS.LGF_LGVMAKROS ON LGF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LISBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LIS_ERSTELLUSER" "~"
if joinKuerzel = BMULIS
searchResult := searchResult "LEFT JOIN HRS.LIS_LOHNIMPORTSPERRE ON LIS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LIZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LIZ_ERSTELLUSER" "~"
if joinKuerzel = BMULIZ
searchResult := searchResult "LEFT JOIN BMD.LIZ_LIZENZ ON LIZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LAQBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAQ_ERSTELLUSER" "~"
if joinKuerzel = BMULAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAZ_ERSTELLUSER" "~"
if joinKuerzel = BMULAZ
searchResult := searchResult "LEFT JOIN WWS.LAZ_LIEFARTIKEL_ZUSATZ ON LAZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LBDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LBD_ERSTELLUSER" "~"
if joinKuerzel = BMULBD
searchResult := searchResult "LEFT JOIN WWS.LBD_LIEFBEWDATEN ON LBD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LBZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LBZ_ERSTELLUSER" "~"
if joinKuerzel = BMULBZ
searchResult := searchResult "LEFT JOIN BIL.LBZ_LANGFRISTERST_BELEGZU ON LBZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LGDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LGD_ERSTELLUSER" "~"
if joinKuerzel = BMULGD
searchResult := searchResult "LEFT JOIN DATANORM.LGD_LEISTUNG_DETAIL ON LGD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LMWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LMW_ERSTELLUSER" "~"
if joinKuerzel = BMULMW
searchResult := searchResult "LEFT JOIN WWS.LMW_LAGERMONATSWERTE ON LMW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LPEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LPE_ERSTELLUSER" "~"
if joinKuerzel = BMULPE
searchResult := searchResult "LEFT JOIN HRS.LPE_PERSON ON LPE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LPRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LPR_ERSTELLUSER" "~"
if joinKuerzel = BMULPR
searchResult := searchResult "LEFT JOIN ZLE.LPR_LEAPERSON ON LPR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LPZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LPZ_ERSTELLUSER" "~"
if joinKuerzel = BMULPZ
searchResult := searchResult "LEFT JOIN BUERO.LPZ_LIZENZKEYPERSONZU ON LPZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LRSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LRS_ERSTELLUSER" "~"
if joinKuerzel = BMULRS
searchResult := searchResult "LEFT JOIN BIL.LRS_LANGFRISTIGE_RST ON LRS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LTSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LTS_ERSTELLUSER" "~"
if joinKuerzel = BMULTS
searchResult := searchResult "LEFT JOIN ZLE.LTS_LEISTTAUSCHSATZ ON LTS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LWPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LWP_ERSTELLUSER" "~"
if joinKuerzel = BMULWP
searchResult := searchResult "LEFT JOIN FIBU.LWP_LANDWIRT_PRODUKTE ON LWP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MAGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MAG_ERSTELLUSER" "~"
if joinKuerzel = BMUMAG
searchResult := searchResult "LEFT JOIN BUERO.MAG_MITARBGESPR ON MAG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MDABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MDA_ERSTELLUSER" "~"
if joinKuerzel = BMUMDA
searchResult := searchResult "LEFT JOIN WWS.MDA_MDE_ARTEN ON MDA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MGZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MGZ_ERSTELLUSER" "~"
if joinKuerzel = BMUMGZ
searchResult := searchResult "LEFT JOIN BUERO.MGZ_MERKMALSGRUPPEZU ON MGZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MRVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MRV_ERSTELLUSER" "~"
if joinKuerzel = BMUMRV
searchResult := searchResult "LEFT JOIN ZLE.MRV_MONATSRECHNUNGVORLAGE ON MRV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MVABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MVA_ERSTELLUSER" "~"
if joinKuerzel = BMUMVA
searchResult := searchResult "LEFT JOIN FIBU.MVA_MAHNVZINSENAB ON MVA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MWTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MWT_ERSTELLUSER" "~"
if joinKuerzel = BMUMWT
searchResult := searchResult "LEFT JOIN BUERO.MWT_MEDIENWERTE ON MWT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NOGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NOG_ERSTELLUSER" "~"
if joinKuerzel = BMUNOG
searchResult := searchResult "LEFT JOIN BUERO.NOG_NOTIZGRUPPE ON NOG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NUIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NUI_ERSTELLUSER" "~"
if joinKuerzel = BMUNUI
searchResult := searchResult "LEFT JOIN BMD.NUI_NTCSUPDINSTALLATION ON NUI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OBEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OBE_ERSTELLUSER" "~"
if joinKuerzel = BMUOBE
searchResult := searchResult "LEFT JOIN LOHN.OBE_OF_BERUF ON OBE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OBGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OBG_ERSTELLUSER" "~"
if joinKuerzel = BMUOBG
searchResult := searchResult "LEFT JOIN LOHN.OBG_OF_BEITRAGSGRUPPE ON OBG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OBLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OBL_ERSTELLUSER" "~"
if joinKuerzel = BMUOBL
searchResult := searchResult "LEFT JOIN BUERO.OBL_ONLBEWLEBENSLAUF ON OBL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OBPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OBP_ERSTELLUSER" "~"
if joinKuerzel = BMUOBP
searchResult := searchResult "LEFT JOIN BUERO.OBP_ONLBEWPERSSTAMM ON OBP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ODABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ODA_ERSTELLUSER" "~"
if joinKuerzel = BMUODA
searchResult := searchResult "LEFT JOIN BMD.ODA_ONLINEDATEI ON ODA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OFKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OFK_ERSTELLUSER" "~"
if joinKuerzel = BMUOFK
searchResult := searchResult "LEFT JOIN LOHN.OFK_OF_FKREIS ON OFK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KLIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KLI_ERSTELLUSER" "~"
if joinKuerzel = BMUKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KMGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KMG_ERSTELLUSER" "~"
if joinKuerzel = BMUKMG
searchResult := searchResult "LEFT JOIN KORE.KMG_KOREMENGE ON KMG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KPMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KPM_ERSTELLUSER" "~"
if joinKuerzel = BMUKPM
searchResult := searchResult "LEFT JOIN KORE.KPM_PLANMODELL ON KPM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KRDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KRD_ERSTELLUSER" "~"
if joinKuerzel = BMUKRD
searchResult := searchResult "LEFT JOIN HRS.KRD_KONTENREFERENZDETAIL ON KRD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KTGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KTG_ERSTELLUSER" "~"
if joinKuerzel = BMUKTG
searchResult := searchResult "LEFT JOIN FIBU.KTG_KONTENGRENZEN ON KTG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KUVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KUV_ERSTELLUSER" "~"
if joinKuerzel = BMUKUV
searchResult := searchResult "LEFT JOIN KORE.KUV_UMLAGEVARIANTE ON KUV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LAJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAJ_ERSTELLUSER" "~"
if joinKuerzel = BMULAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LATBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAT_ERSTELLUSER" "~"
if joinKuerzel = BMULAT
searchResult := searchResult "LEFT JOIN WWS.LAT_LIEFERANTENARTIKEL ON LAT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LAYBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAY_ERSTELLUSER" "~"
if joinKuerzel = BMULAY
searchResult := searchResult "LEFT JOIN BMD.LAY_LAYOUT ON LAY_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LEIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LEI_ERSTELLUSER" "~"
if joinKuerzel = BMULEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LGZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LGZ_ERSTELLUSER" "~"
if joinKuerzel = BMULGZ
searchResult := searchResult "LEFT JOIN HRS.LGZ_LOHNARTGRUPPEZU ON LGZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LKMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LKM_ERSTELLUSER" "~"
if joinKuerzel = BMULKM
searchResult := searchResult "LEFT JOIN WWS.LKM_LAGERKONTOMONATSWERTE ON LKM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LRKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LRK_ERSTELLUSER" "~"
if joinKuerzel = BMULRK
searchResult := searchResult "LEFT JOIN BIL.LRK_LANGFRISTIGE_RSTKONTEN ON LRK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LSGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LSG_ERSTELLUSER" "~"
if joinKuerzel = BMULSG
searchResult := searchResult "LEFT JOIN WWS.LSG_LIEFERSCHEINGRUPPEN ON LSG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LTKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LTK_ERSTELLUSER" "~"
if joinKuerzel = BMULTK
searchResult := searchResult "LEFT JOIN ZLE.LTK_LEISTTAUSCHKOPF ON LTK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LTXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LTX_ERSTELLUSER" "~"
if joinKuerzel = BMULTX
searchResult := searchResult "LEFT JOIN BUERO.LTX_LANGTEXT ON LTX_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LVZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LVZ_ERSTELLUSER" "~"
if joinKuerzel = BMULVZ
searchResult := searchResult "LEFT JOIN BMDINTERN.LVZ_LIZENZVKPZU ON LVZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LXDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LXD_ERSTELLUSER" "~"
if joinKuerzel = BMULXD
searchResult := searchResult "LEFT JOIN BUERO.LXD_LOHNEXPPROTDETAIL ON LXD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MABBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MAB_ERSTELLUSER" "~"
if joinKuerzel = BMUMAB
searchResult := searchResult "LEFT JOIN BUERO.MAB_MITARBAUSBILDUNG ON MAB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MEVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MEV_ERSTELLUSER" "~"
if joinKuerzel = BMUMEV
searchResult := searchResult "LEFT JOIN BUERO.MEV_MEDIENVERLEIH ON MEV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MIKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MIK_ERSTELLUSER" "~"
if joinKuerzel = BMUMIK
searchResult := searchResult "LEFT JOIN BUERO.MIK_MITARBEITERKARRIERE ON MIK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MUZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MUZ_ERSTELLUSER" "~"
if joinKuerzel = BMUMUZ
searchResult := searchResult "LEFT JOIN BUERO.MUZ_MITARBUSERZU ON MUZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NSCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NSC_ERSTELLUSER" "~"
if joinKuerzel = BMUNSC
searchResult := searchResult "LEFT JOIN BMD.NSC_NETSCANNER ON NSC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OAK_ERSTELLUSER" "~"
if joinKuerzel = BMUOAK
searchResult := searchResult "LEFT JOIN LOHN.OAK_OF_AKREIS ON OAK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OBDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OBD_ERSTELLUSER" "~"
if joinKuerzel = BMUOBD
searchResult := searchResult "LEFT JOIN BUERO.OBD_ONLBEWDOKUMENTE ON OBD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OCTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OCT_ERSTELLUSER" "~"
if joinKuerzel = BMUOCT
searchResult := searchResult "LEFT JOIN BMD.OCT_OCR_SUCHTEXT ON OCT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GNHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GNH_ERSTELLUSER" "~"
if joinKuerzel = BMUGNH
searchResult := searchResult "LEFT JOIN BIL.GNH_GAU_HEADER ON GNH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GOEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GOE_ERSTELLUSER" "~"
if joinKuerzel = BMUGOE
searchResult := searchResult "LEFT JOIN BUERO.GOE_GRUPPENORDNEREVENTZU ON GOE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GRMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GRM_ERSTELLUSER" "~"
if joinKuerzel = BMUGRM
searchResult := searchResult "LEFT JOIN DATANORM.GRM_GRAFIK ON GRM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GSFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GSF_ERSTELLUSER" "~"
if joinKuerzel = BMUGSF
searchResult := searchResult "LEFT JOIN WWS.GSF_GUTSCHEINFILIALDATEN ON GSF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GVMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GVM_ERSTELLUSER" "~"
if joinKuerzel = BMUGVM
searchResult := searchResult "LEFT JOIN BUERO.GVM_GHOSTVOLLMACHT ON GVM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HFVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HFV_ERSTELLUSER" "~"
if joinKuerzel = BMUHFV
searchResult := searchResult "LEFT JOIN BMD.HFV_HILFEFAVORITEN ON HFV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HLDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HLD_ERSTELLUSER" "~"
if joinKuerzel = BMUHLD
searchResult := searchResult "LEFT JOIN BMDENTW.HLD_HILFEDOKUMENT ON HLD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HLZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HLZ_ERSTELLUSER" "~"
if joinKuerzel = BMUHLZ
searchResult := searchResult "LEFT JOIN BMDENTW.HLZ_HILFEDOKBAUSTEINZU ON HLZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IFCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IFC_ERSTELLUSER" "~"
if joinKuerzel = BMUIFC
searchResult := searchResult "LEFT JOIN BMDENTW.IFC_INTERNERFARBCODE ON IFC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IMOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IMO_ERSTELLUSER" "~"
if joinKuerzel = BMUIMO
searchResult := searchResult "LEFT JOIN BUERO.IMO_IMMOBILIE ON IMO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = INZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = INZ_ERSTELLUSER" "~"
if joinKuerzel = BMUINZ
searchResult := searchResult "LEFT JOIN BUERO.INZ_INSOLVENZPERSONZU ON INZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ITMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ITM_ERSTELLUSER" "~"
if joinKuerzel = BMUITM
searchResult := searchResult "LEFT JOIN BUERO.ITM_INTERNMAIL ON ITM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KAEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KAE_ERSTELLUSER" "~"
if joinKuerzel = BMUKAE
searchResult := searchResult "LEFT JOIN WWS.KAE_KASSENEINAUSGABEN ON KAE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KAJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KAJ_ERSTELLUSER" "~"
if joinKuerzel = BMUKAJ
searchResult := searchResult "LEFT JOIN WWS.KAJ_KASSENJOURNAL ON KAJ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KARBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KAR_ERSTELLUSER" "~"
if joinKuerzel = BMUKAR
searchResult := searchResult "LEFT JOIN WWS.KAR_KUNDENARTIKEL ON KAR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KAUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KAU_ERSTELLUSER" "~"
if joinKuerzel = BMUKAU
searchResult := searchResult "LEFT JOIN BIL.KAU_KONTOAUFGLIEDERUNG ON KAU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KEEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KEE_ERSTELLUSER" "~"
if joinKuerzel = BMUKEE
searchResult := searchResult "LEFT JOIN KORE.KEE_ERFASSEINGABE ON KEE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KOFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KOF_ERSTELLUSER" "~"
if joinKuerzel = BMUKOF
searchResult := searchResult "LEFT JOIN PRODUKTION.KOF_KOKFILTER ON KOF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KOMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KOM_ERSTELLUSER" "~"
if joinKuerzel = BMUKOM
searchResult := searchResult "LEFT JOIN PRODUKTION.KOM_KOMBI ON KOM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KPABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KPA_ERSTELLUSER" "~"
if joinKuerzel = BMUKPA
searchResult := searchResult "LEFT JOIN WWS.KPA_KOSTENPERIODENAUFTLG ON KPA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KSLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KSL_ERSTELLUSER" "~"
if joinKuerzel = BMUKSL
searchResult := searchResult "LEFT JOIN BUERO.KSL_KONTAKTSYNCEINST ON KSL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KSTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KST_ERSTELLUSER" "~"
if joinKuerzel = BMUKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KVNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KVN_ERSTELLUSER" "~"
if joinKuerzel = BMUKVN
searchResult := searchResult "LEFT JOIN HRS.KVN_KVNAME ON KVN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAK_ERSTELLUSER" "~"
if joinKuerzel = BMULAK
searchResult := searchResult "LEFT JOIN HRS.LAK_LOHNARTENKOPPELUNG ON LAK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LAPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAP_ERSTELLUSER" "~"
if joinKuerzel = BMULAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LEDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LED_ERSTELLUSER" "~"
if joinKuerzel = BMULED
searchResult := searchResult "LEFT JOIN BUERO.LED_LEAD ON LED_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GWQBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GWQ_ERSTELLUSER" "~"
if joinKuerzel = BMUGWQ
searchResult := searchResult "LEFT JOIN HRS.GWQ_GWBANKZU_SEQ ON GWQ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HAZ_ERSTELLUSER" "~"
if joinKuerzel = BMUHAZ
searchResult := searchResult "LEFT JOIN WWS.HAZ_HAUPTLIEFARTIKELZU ON HAZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HSKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HSK_ERSTELLUSER" "~"
if joinKuerzel = BMUHSK
searchResult := searchResult "LEFT JOIN HRS.HSK_SORTIERKOPF ON HSK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HSPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HSP_ERSTELLUSER" "~"
if joinKuerzel = BMUHSP
searchResult := searchResult "LEFT JOIN HRS.HSP_SPALTENKOPF ON HSP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = INRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = INR_ERSTELLUSER" "~"
if joinKuerzel = BMUINR
searchResult := searchResult "LEFT JOIN BUERO.INR_INSERATE ON INR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KAIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KAI_ERSTELLUSER" "~"
if joinKuerzel = BMUKAI
searchResult := searchResult "LEFT JOIN WWS.KAI_KNDARTIKELINFO ON KAI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KAK_ERSTELLUSER" "~"
if joinKuerzel = BMUKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KDPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KDP_ERSTELLUSER" "~"
if joinKuerzel = BMUKDP
searchResult := searchResult "LEFT JOIN KORE.KDP_PLANWERTE ON KDP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KFFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KFF_ERSTELLUSER" "~"
if joinKuerzel = BMUKFF
searchResult := searchResult "LEFT JOIN BUERO.KFF_KORETYPFIND_FELDERZU ON KFF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KODBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KOD_ERSTELLUSER" "~"
if joinKuerzel = BMUKOD
searchResult := searchResult "LEFT JOIN BUERO.KOD_KONDITIONEN ON KOD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KOPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KOP_ERSTELLUSER" "~"
if joinKuerzel = BMUKOP
searchResult := searchResult "LEFT JOIN PRODUKTION.KOP_KOMMISSIONIERUNG_POS ON KOP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KSBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KSB_ERSTELLUSER" "~"
if joinKuerzel = BMUKSB
searchResult := searchResult "LEFT JOIN HRS.KSB_KRANKENSTANDSBESCH ON KSB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KSVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KSV_ERSTELLUSER" "~"
if joinKuerzel = BMUKSV
searchResult := searchResult "LEFT JOIN HRS.KSV_KOSTENVERTEILUNG ON KSV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KVEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KVE_ERSTELLUSER" "~"
if joinKuerzel = BMUKVE
searchResult := searchResult "LEFT JOIN FIBU.KVE_KOREVORERFASSUNG ON KVE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LABBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAB_ERSTELLUSER" "~"
if joinKuerzel = BMULAB
searchResult := searchResult "LEFT JOIN BIL.LAB_LATENTESTEUERN_BELEGZU ON LAB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LAWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAW_ERSTELLUSER" "~"
if joinKuerzel = BMULAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LGMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LGM_ERSTELLUSER" "~"
if joinKuerzel = BMULGM
searchResult := searchResult "LEFT JOIN DATANORM.LGM_LEISTUNG ON LGM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LIBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LIB_ERSTELLUSER" "~"
if joinKuerzel = BMULIB
searchResult := searchResult "LEFT JOIN WWS.LIB_LIEFERBEDINGUNG ON LIB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LKVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LKV_ERSTELLUSER" "~"
if joinKuerzel = BMULKV
searchResult := searchResult "LEFT JOIN HRS.LKV_LOHNKOSTENVERTEILUNG ON LKV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LNGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LNG_ERSTELLUSER" "~"
if joinKuerzel = BMULNG
searchResult := searchResult "LEFT JOIN BMDENTW.LNG_LANGUAGE ON LNG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LSKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LSK_ERSTELLUSER" "~"
if joinKuerzel = BMULSK
searchResult := searchResult "LEFT JOIN BIL.LSK_LATENTESTEUERN_KONTEN ON LSK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LUABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LUA_ERSTELLUSER" "~"
if joinKuerzel = BMULUA
searchResult := searchResult "LEFT JOIN PRODUKTION.LUA_LAGER_UMS_ANGEFORDERT ON LUA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LUPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LUP_ERSTELLUSER" "~"
if joinKuerzel = BMULUP
searchResult := searchResult "LEFT JOIN PRODUKTION.LUP_LAGER_UMS_POS ON LUP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LVEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LVE_ERSTELLUSER" "~"
if joinKuerzel = BMULVE
searchResult := searchResult "LEFT JOIN PRODUKTION.LVE_LEISTUNGSVORERFASSUNG ON LVE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LZUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LZU_ERSTELLUSER" "~"
if joinKuerzel = BMULZU
searchResult := searchResult "LEFT JOIN HRS.LZU_LOHNZUSTAND ON LZU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MAK_ERSTELLUSER" "~"
if joinKuerzel = BMUMAK
searchResult := searchResult "LEFT JOIN BMD.MAK_MAKROFUNKTIONSZU ON MAK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IZJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IZJ_ERSTELLUSER" "~"
if joinKuerzel = BMUIZJ
searchResult := searchResult "LEFT JOIN BUERO.IZJ_ZAEHLERJOURNAL ON IZJ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = JABBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = JAB_ERSTELLUSER" "~"
if joinKuerzel = BMUJAB
searchResult := searchResult "LEFT JOIN BIL.JAB_JABINFO ON JAB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KATBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KAT_ERSTELLUSER" "~"
if joinKuerzel = BMUKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KEKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KEK_ERSTELLUSER" "~"
if joinKuerzel = BMUKEK
searchResult := searchResult "LEFT JOIN KORE.KEK_ERFASSKOPF ON KEK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KKKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KKK_ERSTELLUSER" "~"
if joinKuerzel = BMUKKK
searchResult := searchResult "LEFT JOIN WWS.KKK_KREDITKARTENKONTENZU ON KKK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KMDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KMD_ERSTELLUSER" "~"
if joinKuerzel = BMUKMD
searchResult := searchResult "LEFT JOIN HRS.KMD_KOMMSTDETAIL ON KMD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KPOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KPO_ERSTELLUSER" "~"
if joinKuerzel = BMUKPO
searchResult := searchResult "LEFT JOIN WWS.KPO_KASSENPROVISION ON KPO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KRMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KRM_ERSTELLUSER" "~"
if joinKuerzel = BMUKRM
searchResult := searchResult "LEFT JOIN HRS.KRM_KONTENREFERENZTAB ON KRM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KSCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KSC_ERSTELLUSER" "~"
if joinKuerzel = BMUKSC
searchResult := searchResult "LEFT JOIN KORE.KSC_KORESCHLUESSEL ON KSC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KSWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KSW_ERSTELLUSER" "~"
if joinKuerzel = BMUKSW
searchResult := searchResult "LEFT JOIN KORE.KSW_KORESTAMMWERTE ON KSW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KUSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KUS_ERSTELLUSER" "~"
if joinKuerzel = BMUKUS
searchResult := searchResult "LEFT JOIN BMD.KUS_KURS ON KUS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KVXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KVX_ERSTELLUSER" "~"
if joinKuerzel = BMUKVX
searchResult := searchResult "LEFT JOIN HRS.KVX_KVTEXT ON KVX_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KZPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KZP_ERSTELLUSER" "~"
if joinKuerzel = BMUKZP
searchResult := searchResult "LEFT JOIN FIBU.KZP_ZAHLUNGSPLAN ON KZP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LAABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAA_ERSTELLUSER" "~"
if joinKuerzel = BMULAA
searchResult := searchResult "LEFT JOIN WWS.LAA_LAGERART ON LAA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LDOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LDO_ERSTELLUSER" "~"
if joinKuerzel = BMULDO
searchResult := searchResult "LEFT JOIN BUERO.LDO_LEBENSLAUFDOKUMENTZU ON LDO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LENBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LEN_ERSTELLUSER" "~"
if joinKuerzel = BMULEN
searchResult := searchResult "LEFT JOIN BUERO.LEN_LOHNEINSTELLUNGEN ON LEN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LFKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LFK_ERSTELLUSER" "~"
if joinKuerzel = BMULFK
searchResult := searchResult "LEFT JOIN ZLE.LFK_LEISTFIBKOREZU ON LFK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LINBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LIN_ERSTELLUSER" "~"
if joinKuerzel = BMULIN
searchResult := searchResult "LEFT JOIN PRODUKTION.LIN_PROD_LINIE ON LIN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LNSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LNS_ERSTELLUSER" "~"
if joinKuerzel = BMULNS
searchResult := searchResult "LEFT JOIN HRS.LNS_LOHNSCHABLONENDETAILS ON LNS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LOPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LOP_ERSTELLUSER" "~"
if joinKuerzel = BMULOP
searchResult := searchResult "LEFT JOIN PRODUKTION.LOP_LOGIKTABELLE_POS ON LOP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LPFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LPF_ERSTELLUSER" "~"
if joinKuerzel = BMULPF
searchResult := searchResult "LEFT JOIN BMDENTW.LPF_LIZENZPAKETFUNKZU ON LPF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LPMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LPM_ERSTELLUSER" "~"
if joinKuerzel = BMULPM
searchResult := searchResult "LEFT JOIN WWS.LPM_LAGERPLATZMONATSWERTE ON LPM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LSDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LSD_ERSTELLUSER" "~"
if joinKuerzel = BMULSD
searchResult := searchResult "LEFT JOIN HRS.LSD_LOHNSTAPEL_DETAIL ON LSD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LVPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LVP_ERSTELLUSER" "~"
if joinKuerzel = BMULVP
searchResult := searchResult "LEFT JOIN HRS.LVP_LVPERSSTAMM ON LVP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LVTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LVT_ERSTELLUSER" "~"
if joinKuerzel = BMULVT
searchResult := searchResult "LEFT JOIN HRS.LVT_LOHNKOSTENVERTEILG_TMP ON LVT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LXMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LXM_ERSTELLUSER" "~"
if joinKuerzel = BMULXM
searchResult := searchResult "LEFT JOIN BUERO.LXM_LOHNEXPPROTOKOLL ON LXM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OFMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OFM_ERSTELLUSER" "~"
if joinKuerzel = BMUOFM
searchResult := searchResult "LEFT JOIN LOHN.OFM_OF_FAMILIE ON OFM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OLABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OLA_ERSTELLUSER" "~"
if joinKuerzel = BMUOLA
searchResult := searchResult "LEFT JOIN LOHN.OLA_OF_LOHNART ON OLA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ORTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ORT_ERSTELLUSER" "~"
if joinKuerzel = BMUORT
searchResult := searchResult "LEFT JOIN BUERO.ORT_ORT ON ORT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OSZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OSZ_ERSTELLUSER" "~"
if joinKuerzel = BMUOSZ
searchResult := searchResult "LEFT JOIN LOHN.OSZ_OF_SWBKZ ON OSZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OUFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OUF_ERSTELLUSER" "~"
if joinKuerzel = BMUOUF
searchResult := searchResult "LEFT JOIN BUERO.OUF_ONLINEUMFRAGE ON OUF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OVEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OVE_ERSTELLUSER" "~"
if joinKuerzel = BMUOVE
searchResult := searchResult "LEFT JOIN LOHN.OVE_OF_VERANTWORTLICHER ON OVE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PAPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAP_ERSTELLUSER" "~"
if joinKuerzel = BMUPAP
searchResult := searchResult "LEFT JOIN WWS.PAP_PACKSTOFFTABPOS ON PAP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PCLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PCL_ERSTELLUSER" "~"
if joinKuerzel = BMUPCL
searchResult := searchResult "LEFT JOIN BUERO.PCL_PERSONCLOUDZU ON PCL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PCZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PCZ_ERSTELLUSER" "~"
if joinKuerzel = BMUPCZ
searchResult := searchResult "LEFT JOIN BUERO.PCZ_PERSONKZCOMPASSZU ON PCZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PDSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PDS_ERSTELLUSER" "~"
if joinKuerzel = BMUPDS
searchResult := searchResult "LEFT JOIN BIL.PDS_PERSKORST_DNSTAMM ON PDS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PEZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PEZ_ERSTELLUSER" "~"
if joinKuerzel = BMUPEZ
searchResult := searchResult "LEFT JOIN BUERO.PEZ_PERSONENZU ON PEZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PFRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PFR_ERSTELLUSER" "~"
if joinKuerzel = BMUPFR
searchResult := searchResult "LEFT JOIN WWS.PFR_PICFARBE ON PFR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PII_ERSTELLUSER" "~"
if joinKuerzel = BMUPII
searchResult := searchResult "LEFT JOIN WWS.PII_PICKUNDENGRUPPE ON PII_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIW_ERSTELLUSER" "~"
if joinKuerzel = BMUPIW
searchResult := searchResult "LEFT JOIN WWS.PIW_PICWARENGRUPPE ON PIW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIX_ERSTELLUSER" "~"
if joinKuerzel = BMUPIX
searchResult := searchResult "LEFT JOIN BIL.PIX_PERSRSTIMP_FELDSPERRE ON PIX_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKA_ERSTELLUSER" "~"
if joinKuerzel = BMUPKA
searchResult := searchResult "LEFT JOIN BUERO.PKA_PROJEKTART ON PKA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKC_ERSTELLUSER" "~"
if joinKuerzel = BMUPKC
searchResult := searchResult "LEFT JOIN HRS.PKC_PENSKASSA_CALC ON PKC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKH_ERSTELLUSER" "~"
if joinKuerzel = BMUPKH
searchResult := searchResult "LEFT JOIN BIL.PKH_PERSKORSTHEADER ON PKH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKK_ERSTELLUSER" "~"
if joinKuerzel = BMUPKK
searchResult := searchResult "LEFT JOIN BIL.PKK_PERSKORST_PARAMKOPF ON PKK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PLGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PLG_ERSTELLUSER" "~"
if joinKuerzel = BMUPLG
searchResult := searchResult "LEFT JOIN BUERO.PLG_PLANGRUPPE ON PLG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PLPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PLP_ERSTELLUSER" "~"
if joinKuerzel = BMUPLP
searchResult := searchResult "LEFT JOIN PRODUKTION.PLP_PLANUNGSPERIODE ON PLP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PMZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PMZ_ERSTELLUSER" "~"
if joinKuerzel = BMUPMZ
searchResult := searchResult "LEFT JOIN BUERO.PMZ_PAUSENTAGESMODELLZU ON PMZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = POABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = POA_ERSTELLUSER" "~"
if joinKuerzel = BMUPOA
searchResult := searchResult "LEFT JOIN BUERO.POA_POSTART ON POA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PRDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PRD_ERSTELLUSER" "~"
if joinKuerzel = BMUPRD
searchResult := searchResult "LEFT JOIN ZLE.PRD_PERIODE ON PRD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PSTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PST_ERSTELLUSER" "~"
if joinKuerzel = BMUPST
searchResult := searchResult "LEFT JOIN PRODUKTION.PST_STAPEL ON PST_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PSYBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PSY_ERSTELLUSER" "~"
if joinKuerzel = BMUPSY
searchResult := searchResult "LEFT JOIN WWS.PSY_PICSYNCFELDZU ON PSY_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LLDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LLD_ERSTELLUSER" "~"
if joinKuerzel = BMULLD
searchResult := searchResult "LEFT JOIN ZLE.LLD_LEALOHNDATEN ON LLD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LOZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LOZ_ERSTELLUSER" "~"
if joinKuerzel = BMULOZ
searchResult := searchResult "LEFT JOIN HRS.LOZ_LOHNERFASSUNGZLE ON LOZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LPOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LPO_ERSTELLUSER" "~"
if joinKuerzel = BMULPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LUKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LUK_ERSTELLUSER" "~"
if joinKuerzel = BMULUK
searchResult := searchResult "LEFT JOIN PRODUKTION.LUK_LAGER_UMS_KOPF ON LUK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MAS_ERSTELLUSER" "~"
if joinKuerzel = BMUMAS
searchResult := searchResult "LEFT JOIN FIBU.MAS_MAHN_STAMM ON MAS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MGBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MGB_ERSTELLUSER" "~"
if joinKuerzel = BMUMGB
searchResult := searchResult "LEFT JOIN FIBU.MGB_MAHNGEBUEHR ON MGB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MLTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MLT_ERSTELLUSER" "~"
if joinKuerzel = BMUMLT
searchResult := searchResult "LEFT JOIN ZLE.MLT_MITARBLEISTTAUSCH ON MLT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MOKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MOK_ERSTELLUSER" "~"
if joinKuerzel = BMUMOK
searchResult := searchResult "LEFT JOIN WWS.MOK_MODELLKOPF ON MOK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MPDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MPD_ERSTELLUSER" "~"
if joinKuerzel = BMUMPD
searchResult := searchResult "LEFT JOIN WWS.MPD_MODELLPOSVARDETAIL ON MPD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NUPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NUP_ERSTELLUSER" "~"
if joinKuerzel = BMUNUP
searchResult := searchResult "LEFT JOIN BMD.NUP_NTCSUPDATE ON NUP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OBABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OBA_ERSTELLUSER" "~"
if joinKuerzel = BMUOBA
searchResult := searchResult "LEFT JOIN BUERO.OBA_ONLBEWAUSSCHREIBUNGZU ON OBA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OBRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OBR_ERSTELLUSER" "~"
if joinKuerzel = BMUOBR
searchResult := searchResult "LEFT JOIN LOHN.OBR_OF_BETRIEBSRAT ON OBR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OFIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OFI_ERSTELLUSER" "~"
if joinKuerzel = BMUOFI
searchResult := searchResult "LEFT JOIN LOHN.OFI_OF_FIXWERTE ON OFI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OPZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OPZ_ERSTELLUSER" "~"
if joinKuerzel = BMUOPZ
searchResult := searchResult "LEFT JOIN FIBU.OPZ_OPZUORDNUNG ON OPZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OTDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OTD_ERSTELLUSER" "~"
if joinKuerzel = BMUOTD
searchResult := searchResult "LEFT JOIN BMD.OTD_OCRTEMPLATEDETAIL ON OTD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OUZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OUZ_ERSTELLUSER" "~"
if joinKuerzel = BMUOUZ
searchResult := searchResult "LEFT JOIN BUERO.OUZ_ONLINEKONFUMFRAGEZU ON OUZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OVGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OVG_ERSTELLUSER" "~"
if joinKuerzel = BMUOVG
searchResult := searchResult "LEFT JOIN LOHN.OVG_OF_VERWENDUNGSGRUPPE ON OVG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PABBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAB_ERSTELLUSER" "~"
if joinKuerzel = BMUPAB
searchResult := searchResult "LEFT JOIN WWS.PAB_PROVARTBAUM ON PAB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PAEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAE_ERSTELLUSER" "~"
if joinKuerzel = BMUPAE
searchResult := searchResult "LEFT JOIN BMDINTERN.PAE_PRODUKTANFRAGE ON PAE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAK_ERSTELLUSER" "~"
if joinKuerzel = BMUPAK
searchResult := searchResult "LEFT JOIN BIL.PAK_PERSRST_ALTERSV_KOPF ON PAK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PALBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAL_ERSTELLUSER" "~"
if joinKuerzel = BMUPAL
searchResult := searchResult "LEFT JOIN ZLE.PAL_PAUSCHALE ON PAL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAZ_ERSTELLUSER" "~"
if joinKuerzel = BMUPAZ
searchResult := searchResult "LEFT JOIN BMDDEFAULT.PAZ_PARAMGRUPPEPAKETZU ON PAZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PDWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PDW_ERSTELLUSER" "~"
if joinKuerzel = BMUPDW
searchResult := searchResult "LEFT JOIN BMDDEFAULT.PDW_PARAMWERT ON PDW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PEKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PEK_ERSTELLUSER" "~"
if joinKuerzel = BMUPEK
searchResult := searchResult "LEFT JOIN FIBU.PEK_PERSONENKONTO ON PEK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PERBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PER_ERSTELLUSER" "~"
if joinKuerzel = BMUPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PGSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PGS_ERSTELLUSER" "~"
if joinKuerzel = BMUPGS
searchResult := searchResult "LEFT JOIN BIL.PGS_PERSKORST_GUTSTUNDEN ON PGS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OFABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OFA_ERSTELLUSER" "~"
if joinKuerzel = BMUOFA
searchResult := searchResult "LEFT JOIN LOHN.OFA_OF_FINANZAMT ON OFA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OFRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OFR_ERSTELLUSER" "~"
if joinKuerzel = BMUOFR
searchResult := searchResult "LEFT JOIN BUERO.OFR_FRAGEBOGENREGEL ON OFR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OGEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OGE_ERSTELLUSER" "~"
if joinKuerzel = BMUOGE
searchResult := searchResult "LEFT JOIN LOHN.OGE_OF_GEMEINDE ON OGE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OKSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OKS_ERSTELLUSER" "~"
if joinKuerzel = BMUOKS
searchResult := searchResult "LEFT JOIN LOHN.OKS_OF_KOSTENSTELLE ON OKS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OKZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OKZ_ERSTELLUSER" "~"
if joinKuerzel = BMUOKZ
searchResult := searchResult "LEFT JOIN LOHN.OKZ_OF_KFZVERWALTUNG ON OKZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ORLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ORL_ERSTELLUSER" "~"
if joinKuerzel = BMUORL
searchResult := searchResult "LEFT JOIN LOHN.ORL_OF_REISELEISTUNG_TMP ON ORL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OTEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OTE_ERSTELLUSER" "~"
if joinKuerzel = BMUOTE
searchResult := searchResult "LEFT JOIN LOHN.OTE_OF_TEXTE ON OTE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OTRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OTR_ERSTELLUSER" "~"
if joinKuerzel = BMUOTR
searchResult := searchResult "LEFT JOIN LOHN.OTR_OF_TRANSFER ON OTR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OUWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OUW_ERSTELLUSER" "~"
if joinKuerzel = BMUOUW
searchResult := searchResult "LEFT JOIN BMD.OUW_OUTLOOKBARWINDOW ON OUW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PAABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAA_ERSTELLUSER" "~"
if joinKuerzel = BMUPAA
searchResult := searchResult "LEFT JOIN BMDDEFAULT.PAA_PARAMAUSPRAEGUNG ON PAA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAD_ERSTELLUSER" "~"
if joinKuerzel = BMUPAD
searchResult := searchResult "LEFT JOIN BMDDEFAULT.PAD_PARAMETERDEF ON PAD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PARBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAR_ERSTELLUSER" "~"
if joinKuerzel = BMUPAR
searchResult := searchResult "LEFT JOIN BMDDEFAULT.PAR_PARAM ON PAR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PAWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAW_ERSTELLUSER" "~"
if joinKuerzel = BMUPAW
searchResult := searchResult "LEFT JOIN BMD.PAW_PARAMWERT ON PAW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PBABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PBA_ERSTELLUSER" "~"
if joinKuerzel = BMUPBA
searchResult := searchResult "LEFT JOIN PRODUKTION.PBA_PLAN_BAS_ALT ON PBA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PCPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PCP_ERSTELLUSER" "~"
if joinKuerzel = BMUPCP
searchResult := searchResult "LEFT JOIN PRODUKTION.PCP_PPS_PRODUKT ON PCP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PCWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PCW_ERSTELLUSER" "~"
if joinKuerzel = BMUPCW
searchResult := searchResult "LEFT JOIN PRODUKTION.PCW_PPSKAPAWERT ON PCW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PCXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PCX_ERSTELLUSER" "~"
if joinKuerzel = BMUPCX
searchResult := searchResult "LEFT JOIN PRODUKTION.PCX_PPS_KAPA_EXT ON PCX_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PFZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PFZ_ERSTELLUSER" "~"
if joinKuerzel = BMUPFZ
searchResult := searchResult "LEFT JOIN BUERO.PFZ_PERSONFIRMENZU ON PFZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PGRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PGR_ERSTELLUSER" "~"
if joinKuerzel = BMUPGR
searchResult := searchResult "LEFT JOIN BUERO.PGR_PERSONGERICHTZU ON PGR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIB_ERSTELLUSER" "~"
if joinKuerzel = BMUPIB
searchResult := searchResult "LEFT JOIN WWS.PIB_PICRABATTSTAFFEL ON PIB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PICBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIC_ERSTELLUSER" "~"
if joinKuerzel = BMUPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PID_ERSTELLUSER" "~"
if joinKuerzel = BMUPID
searchResult := searchResult "LEFT JOIN WWS.PID_PICPREISDETAIL ON PID_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIE_ERSTELLUSER" "~"
if joinKuerzel = BMUPIE
searchResult := searchResult "LEFT JOIN WWS.PIE_PICEINHEIT ON PIE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PISBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIS_ERSTELLUSER" "~"
if joinKuerzel = BMUPIS
searchResult := searchResult "LEFT JOIN WWS.PIS_PICPREISSTAFFEL ON PIS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKB_ERSTELLUSER" "~"
if joinKuerzel = BMUPKB
searchResult := searchResult "LEFT JOIN BIL.PKB_PERSKORSTBUCHBELEGZU ON PKB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKG_ERSTELLUSER" "~"
if joinKuerzel = BMUPKG
searchResult := searchResult "LEFT JOIN BMDDEFAULT.PKG_PROTOKOLLGRUPPE ON PKG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LMABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LMA_ERSTELLUSER" "~"
if joinKuerzel = BMULMA
searchResult := searchResult "LEFT JOIN WWS.LMA_LAGERMONATSABSCHLUSS ON LMA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LMBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LMB_ERSTELLUSER" "~"
if joinKuerzel = BMULMB
searchResult := searchResult "LEFT JOIN WWS.LMB_LAGERMINMAXBESTAND ON LMB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LSEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LSE_ERSTELLUSER" "~"
if joinKuerzel = BMULSE
searchResult := searchResult "LEFT JOIN HRS.LSE_LOHNSCHABLONE ON LSE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LUEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LUE_ERSTELLUSER" "~"
if joinKuerzel = BMULUE
searchResult := searchResult "LEFT JOIN PRODUKTION.LUE_LAGER_UMS_EMPFANGEN ON LUE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LZSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LZS_ERSTELLUSER" "~"
if joinKuerzel = BMULZS
searchResult := searchResult "LEFT JOIN HRS.LZS_LOHNZETTEL_SV ON LZS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MFLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MFL_ERSTELLUSER" "~"
if joinKuerzel = BMUMFL
searchResult := searchResult "LEFT JOIN BMDINTERN.MFL_MFLIZENZEN ON MFL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MISBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MIS_ERSTELLUSER" "~"
if joinKuerzel = BMUMIS
searchResult := searchResult "LEFT JOIN BUERO.MIS_MITARBSTELLEZU ON MIS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MIZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MIZ_ERSTELLUSER" "~"
if joinKuerzel = BMUMIZ
searchResult := searchResult "LEFT JOIN BUERO.MIZ_MEDIENINFOZU ON MIZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MKRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MKR_ERSTELLUSER" "~"
if joinKuerzel = BMUMKR
searchResult := searchResult "LEFT JOIN FIBU.MKR_MAHNKREIS ON MKR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MLZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MLZ_ERSTELLUSER" "~"
if joinKuerzel = BMUMLZ
searchResult := searchResult "LEFT JOIN BUERO.MLZ_VORSKASSELEITZAHLZU ON MLZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MOPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MOP_ERSTELLUSER" "~"
if joinKuerzel = BMUMOP
searchResult := searchResult "LEFT JOIN WWS.MOP_MODELLPOSVAR ON MOP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MSTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MST_ERSTELLUSER" "~"
if joinKuerzel = BMUMST
searchResult := searchResult "LEFT JOIN BUERO.MST_MITARBSTELLVERTRETER ON MST_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MVKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MVK_ERSTELLUSER" "~"
if joinKuerzel = BMUMVK
searchResult := searchResult "LEFT JOIN BUERO.MVK_MITARBVORSORGEKASSE ON MVK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MWRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MWR_ERSTELLUSER" "~"
if joinKuerzel = BMUMWR
searchResult := searchResult "LEFT JOIN BIL.MWR_MWRDATEN ON MWR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NEBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NEB_ERSTELLUSER" "~"
if joinKuerzel = BMUNEB
searchResult := searchResult "LEFT JOIN BIL.NEB_NEBENWERTE ON NEB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NFZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NFZ_ERSTELLUSER" "~"
if joinKuerzel = BMUNFZ
searchResult := searchResult "LEFT JOIN BIL.NFZ_ANBUFIBUZU ON NFZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OBBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OBB_ERSTELLUSER" "~"
if joinKuerzel = BMUOBB
searchResult := searchResult "LEFT JOIN BUERO.OBB_ONLBEWBEWERBUNG ON OBB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OTABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OTA_ERSTELLUSER" "~"
if joinKuerzel = BMUOTA
searchResult := searchResult "LEFT JOIN LOHN.OTA_OF_TAETIGKEIT ON OTA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PATBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAT_ERSTELLUSER" "~"
if joinKuerzel = BMUPAT
searchResult := searchResult "LEFT JOIN WWS.PAT_PACKSTOFFTAB ON PAT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PBOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PBO_ERSTELLUSER" "~"
if joinKuerzel = BMUPBO
searchResult := searchResult "LEFT JOIN BIL.PBO_PERSKORST_KONTEN ON PBO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PBSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PBS_ERSTELLUSER" "~"
if joinKuerzel = BMUPBS
searchResult := searchResult "LEFT JOIN PRODUKTION.PBS_BEFEHL ON PBS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PDGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PDG_ERSTELLUSER" "~"
if joinKuerzel = BMUPDG
searchResult := searchResult "LEFT JOIN WWS.PDG_PICDYNAMICGROUP ON PDG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PFABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PFA_ERSTELLUSER" "~"
if joinKuerzel = BMUPFA
searchResult := searchResult "LEFT JOIN BUERO.PFA_PERSONFINANZAMTZU ON PFA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PFFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PFF_ERSTELLUSER" "~"
if joinKuerzel = BMUPFF
searchResult := searchResult "LEFT JOIN BUERO.PFF_PAGEFGBFRAGEZU ON PFF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PGIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PGI_ERSTELLUSER" "~"
if joinKuerzel = BMUPGI
searchResult := searchResult "LEFT JOIN WWS.PGI_PICARTIKELGRPITEM ON PGI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIJ_ERSTELLUSER" "~"
if joinKuerzel = BMUPIJ
searchResult := searchResult "LEFT JOIN WWS.PIJ_PICKUNDENGRZU ON PIJ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MAVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MAV_ERSTELLUSER" "~"
if joinKuerzel = BMUMAV
searchResult := searchResult "LEFT JOIN FIBU.MAV_MAHNVZINSEN ON MAV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MBOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MBO_ERSTELLUSER" "~"
if joinKuerzel = BMUMBO
searchResult := searchResult "LEFT JOIN BMD.MBO_MAILBOX ON MBO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MDEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MDE_ERSTELLUSER" "~"
if joinKuerzel = BMUMDE
searchResult := searchResult "LEFT JOIN WWS.MDE_MOBILEDATENERFASSUNG ON MDE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MIVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MIV_ERSTELLUSER" "~"
if joinKuerzel = BMUMIV
searchResult := searchResult "LEFT JOIN HRS.MIV_MITARBVORSCHLAG ON MIV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MVLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MVL_ERSTELLUSER" "~"
if joinKuerzel = BMUMVL
searchResult := searchResult "LEFT JOIN BUERO.MVL_MITARBEITERVORLAGE ON MVL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NEGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NEG_ERSTELLUSER" "~"
if joinKuerzel = BMUNEG
searchResult := searchResult "LEFT JOIN BIL.NEG_NICHTENTGEWINNE ON NEG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NUKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NUK_ERSTELLUSER" "~"
if joinKuerzel = BMUNUK
searchResult := searchResult "LEFT JOIN BMD.NUK_NTCSUPDPRODUKT ON NUK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NVSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NVS_ERSTELLUSER" "~"
if joinKuerzel = BMUNVS
searchResult := searchResult "LEFT JOIN BIL.NVS_NACHVERSTEU_PAR11A ON NVS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OAS_ERSTELLUSER" "~"
if joinKuerzel = BMUOAS
searchResult := searchResult "LEFT JOIN LOHN.OAS_OF_ARBEITSZEITSTUNDEN ON OAS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OGWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OGW_ERSTELLUSER" "~"
if joinKuerzel = BMUOGW
searchResult := searchResult "LEFT JOIN LOHN.OGW_OF_GEWERKSCHAFT ON OGW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OKTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OKT_ERSTELLUSER" "~"
if joinKuerzel = BMUOKT
searchResult := searchResult "LEFT JOIN LOHN.OKT_OF_KOSTENTRAEGER ON OKT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ONZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ONZ_ERSTELLUSER" "~"
if joinKuerzel = BMUONZ
searchResult := searchResult "LEFT JOIN HRS.ONZ_ONLINEBEW_STELLE_ZU ON ONZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OPKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OPK_ERSTELLUSER" "~"
if joinKuerzel = BMUOPK
searchResult := searchResult "LEFT JOIN LOHN.OPK_OF_PROZKO ON OPK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OTHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OTH_ERSTELLUSER" "~"
if joinKuerzel = BMUOTH
searchResult := searchResult "LEFT JOIN BMD.OTH_OCRTEMPLATEHEADER ON OTH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OUNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OUN_ERSTELLUSER" "~"
if joinKuerzel = BMUOUN
searchResult := searchResult "LEFT JOIN BMD.OUN_OUTLOOKBARTABNODES ON OUN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OUPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OUP_ERSTELLUSER" "~"
if joinKuerzel = BMUOUP
searchResult := searchResult "LEFT JOIN BUERO.OUP_ONLUMFRAGEPAGE ON OUP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PAIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAI_ERSTELLUSER" "~"
if joinKuerzel = BMUPAI
searchResult := searchResult "LEFT JOIN BMDINTERN.PAI_PRODUKTANFINTERESSENT ON PAI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PBWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PBW_ERSTELLUSER" "~"
if joinKuerzel = BMUPBW
searchResult := searchResult "LEFT JOIN PRODUKTION.PBW_PLAN_WERT ON PBW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PGABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PGA_ERSTELLUSER" "~"
if joinKuerzel = BMUPGA
searchResult := searchResult "LEFT JOIN WWS.PGA_PICARTIKELGRPZU ON PGA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PGMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PGM_ERSTELLUSER" "~"
if joinKuerzel = BMUPGM
searchResult := searchResult "LEFT JOIN BUERO.PGM_PERSONGEMEINDEZU ON PGM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PGPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PGP_ERSTELLUSER" "~"
if joinKuerzel = BMUPGP
searchResult := searchResult "LEFT JOIN WWS.PGP_PICARTIKELGRP ON PGP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKD_ERSTELLUSER" "~"
if joinKuerzel = BMUPKD
searchResult := searchResult "LEFT JOIN PRODUKTION.PKD_KM_DETAIL ON PKD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKM_ERSTELLUSER" "~"
if joinKuerzel = BMUPKM
searchResult := searchResult "LEFT JOIN PRODUKTION.PKM_KM_MASTER ON PKM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PLSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PLS_ERSTELLUSER" "~"
if joinKuerzel = BMUPLS
searchResult := searchResult "LEFT JOIN WWS.PLS_PICALLERGEN ON PLS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PMDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PMD_ERSTELLUSER" "~"
if joinKuerzel = BMUPMD
searchResult := searchResult "LEFT JOIN BUERO.PMD_PAUSENMODELL ON PMD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PMPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PMP_ERSTELLUSER" "~"
if joinKuerzel = BMUPMP
searchResult := searchResult "LEFT JOIN HRS.PMP_PERSSTAMMPROTOKOLL ON PMP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PUIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PUI_ERSTELLUSER" "~"
if joinKuerzel = BMUPUI
searchResult := searchResult "LEFT JOIN WWS.PUI_PICSTKL ON PUI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PVABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PVA_ERSTELLUSER" "~"
if joinKuerzel = BMUPVA
searchResult := searchResult "LEFT JOIN BUERO.PVA_PERSONVORERFASSAKTION ON PVA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PVFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PVF_ERSTELLUSER" "~"
if joinKuerzel = BMUPVF
searchResult := searchResult "LEFT JOIN BUERO.PVF_PERSONENVORERFASSUNG ON PVF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PVWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PVW_ERSTELLUSER" "~"
if joinKuerzel = BMUPVW
searchResult := searchResult "LEFT JOIN BUERO.PVW_PERSONVORERFASSWOCHSTD ON PVW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PWEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PWE_ERSTELLUSER" "~"
if joinKuerzel = BMUPWE
searchResult := searchResult "LEFT JOIN WWS.PWE_PWKPREISTABZU ON PWE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PZGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PZG_ERSTELLUSER" "~"
if joinKuerzel = BMUPZG
searchResult := searchResult "LEFT JOIN WWS.PZG_PICGROESSENZU ON PZG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PZUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PZU_ERSTELLUSER" "~"
if joinKuerzel = BMUPZU
searchResult := searchResult "LEFT JOIN WWS.PZU_PREISRABATTZUGRIFF ON PZU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RATBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RAT_ERSTELLUSER" "~"
if joinKuerzel = BMURAT
searchResult := searchResult "LEFT JOIN WWS.RAT_RABATTTAB ON RAT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RLPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RLP_ERSTELLUSER" "~"
if joinKuerzel = BMURLP
searchResult := searchResult "LEFT JOIN WWS.RLP_RECHNUNGSPLANPOS ON RLP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RNABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RNA_ERSTELLUSER" "~"
if joinKuerzel = BMURNA
searchResult := searchResult "LEFT JOIN WWS.RNA_RECHNUNGANZAHLUNG ON RNA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RSPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RSP_ERSTELLUSER" "~"
if joinKuerzel = BMURSP
searchResult := searchResult "LEFT JOIN BIL.RSP_RSTSPALTENKOPF ON RSP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RTZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RTZ_ERSTELLUSER" "~"
if joinKuerzel = BMURTZ
searchResult := searchResult "LEFT JOIN ZLE.RTZ_RECHTEXTZU ON RTZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RVCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RVC_ERSTELLUSER" "~"
if joinKuerzel = BMURVC
searchResult := searchResult "LEFT JOIN FIBU.RVC_RVCODE ON RVC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RVDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RVD_ERSTELLUSER" "~"
if joinKuerzel = BMURVD
searchResult := searchResult "LEFT JOIN ZLE.RVD_RECHVORLAGEDETAIL ON RVD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RWZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RWZ_ERSTELLUSER" "~"
if joinKuerzel = BMURWZ
searchResult := searchResult "LEFT JOIN BUERO.RWZ_ZEITRUNDWOCHENMDZUORD ON RWZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SAPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SAP_ERSTELLUSER" "~"
if joinKuerzel = BMUSAP
searchResult := searchResult "LEFT JOIN BUERO.SAP_SAMMELPROJEKT ON SAP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SARBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SAR_ERSTELLUSER" "~"
if joinKuerzel = BMUSAR
searchResult := searchResult "LEFT JOIN HRS.SAR_SCHWERARBEIT ON SAR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SBVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SBV_ERSTELLUSER" "~"
if joinKuerzel = BMUSBV
searchResult := searchResult "LEFT JOIN BUERO.SBV_STELLENBESCHVORLAGE ON SBV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCB_ERSTELLUSER" "~"
if joinKuerzel = BMUSCB
searchResult := searchResult "LEFT JOIN BMDENTW.SCB_SCRPERSZU ON SCB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCD_ERSTELLUSER" "~"
if joinKuerzel = BMUSCD
searchResult := searchResult "LEFT JOIN BMDENTW.SCD_SCRDEPSCR ON SCD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCL_ERSTELLUSER" "~"
if joinKuerzel = BMUSCL
searchResult := searchResult "LEFT JOIN PRODUKTION.SCL_SCKENNZAHLDET ON SCL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCN_ERSTELLUSER" "~"
if joinKuerzel = BMUSCN
searchResult := searchResult "LEFT JOIN BMDENTW.SCN_SCRNOTIZ ON SCN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCP_ERSTELLUSER" "~"
if joinKuerzel = BMUSCP
searchResult := searchResult "LEFT JOIN BMDENTW.SCP_SCRPROT ON SCP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCX_ERSTELLUSER" "~"
if joinKuerzel = BMUSCX
searchResult := searchResult "LEFT JOIN KORE.SCX_SUMMENCHECKEXPORT ON SCX_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SDTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SDT_ERSTELLUSER" "~"
if joinKuerzel = BMUSDT
searchResult := searchResult "LEFT JOIN BMDENTW.SDT_SCRDOCTEXTZU ON SDT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SEBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SEB_ERSTELLUSER" "~"
if joinKuerzel = BMUSEB
searchResult := searchResult "LEFT JOIN BIL.SEB_STEUERERKLBAUM ON SEB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LEHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LEH_ERSTELLUSER" "~"
if joinKuerzel = BMULEH
searchResult := searchResult "LEFT JOIN HRS.LEH_LOHNEINSTELLUNG_HIST ON LEH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LHPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LHP_ERSTELLUSER" "~"
if joinKuerzel = BMULHP
searchResult := searchResult "LEFT JOIN BMDINTERN.LHP_LIZENZHAUPTPAKETZU ON LHP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LKPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LKP_ERSTELLUSER" "~"
if joinKuerzel = BMULKP
searchResult := searchResult "LEFT JOIN BMDINTERN.LKP_LIZENZKUNDENVKPZU ON LKP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LMSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LMS_ERSTELLUSER" "~"
if joinKuerzel = BMULMS
searchResult := searchResult "LEFT JOIN WWS.LMS_LIEFERMAHNUNG ON LMS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LOABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LOA_ERSTELLUSER" "~"
if joinKuerzel = BMULOA
searchResult := searchResult "LEFT JOIN HRS.LOA_LOHNART ON LOA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LOKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LOK_ERSTELLUSER" "~"
if joinKuerzel = BMULOK
searchResult := searchResult "LEFT JOIN PRODUKTION.LOK_LOGIKTABELLE_KOPF ON LOK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LZTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LZT_ERSTELLUSER" "~"
if joinKuerzel = BMULZT
searchResult := searchResult "LEFT JOIN BMD.LZT_LIZENZTEST ON LZT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LZZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LZZ_ERSTELLUSER" "~"
if joinKuerzel = BMULZZ
searchResult := searchResult "LEFT JOIN BMD.LZZ_AUTOLIZENZZU ON LZZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MAHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MAH_ERSTELLUSER" "~"
if joinKuerzel = BMUMAH
searchResult := searchResult "LEFT JOIN FIBU.MAH_MAHNUNG ON MAH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MKOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MKO_ERSTELLUSER" "~"
if joinKuerzel = BMUMKO
searchResult := searchResult "LEFT JOIN BIL.MKO_MINDESTKOEST ON MKO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MMZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MMZ_ERSTELLUSER" "~"
if joinKuerzel = BMUMMZ
searchResult := searchResult "LEFT JOIN BUERO.MMZ_MAGBETRIEBSMITTELZU ON MMZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MRDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MRD_ERSTELLUSER" "~"
if joinKuerzel = BMUMRD
searchResult := searchResult "LEFT JOIN ZLE.MRD_MONATSRECHNUNGDETAIL ON MRD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MSGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MSG_ERSTELLUSER" "~"
if joinKuerzel = BMUMSG
searchResult := searchResult "LEFT JOIN BMDENTW.MSG_MESSAGE ON MSG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MWKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MWK_ERSTELLUSER" "~"
if joinKuerzel = BMUMWK
searchResult := searchResult "LEFT JOIN BIL.MWK_MWRKOPF ON MWK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NLZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NLZ_ERSTELLUSER" "~"
if joinKuerzel = BMUNLZ
searchResult := searchResult "LEFT JOIN HRS.NLZ_NICHTLEISTUNGSZEITEN ON NLZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NOTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NOT_ERSTELLUSER" "~"
if joinKuerzel = BMUNOT
searchResult := searchResult "LEFT JOIN BUERO.NOT_NOTIZ ON NOT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NTCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NTC_ERSTELLUSER" "~"
if joinKuerzel = BMUNTC
searchResult := searchResult "LEFT JOIN BMDENTW.NTC_NTCSARTIKEL ON NTC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NUDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NUD_ERSTELLUSER" "~"
if joinKuerzel = BMUNUD
searchResult := searchResult "LEFT JOIN BMD.NUD_NTCSUPDATEONLINEDATEI ON NUD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NUYBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NUY_ERSTELLUSER" "~"
if joinKuerzel = BMUNUY
searchResult := searchResult "LEFT JOIN BMD.NUY_NTCSUPDTYP ON NUY_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OBTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OBT_ERSTELLUSER" "~"
if joinKuerzel = BMUOBT
searchResult := searchResult "LEFT JOIN LOHN.OBT_OF_BLOBTEXT ON OBT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ONSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ONS_ERSTELLUSER" "~"
if joinKuerzel = BMUONS
searchResult := searchResult "LEFT JOIN HRS.ONS_ONLINEBEW_STAMMDATEN ON ONS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PANBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAN_ERSTELLUSER" "~"
if joinKuerzel = BMUPAN
searchResult := searchResult "LEFT JOIN WWS.PAN_PROVARTBAUMKNOTEN ON PAN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PDZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PDZ_ERSTELLUSER" "~"
if joinKuerzel = BMUPDZ
searchResult := searchResult "LEFT JOIN BUERO.PDZ_PROJEKTWERTDOKZU ON PDZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PFEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PFE_ERSTELLUSER" "~"
if joinKuerzel = BMUPFE
searchResult := searchResult "LEFT JOIN BMD.PFE_PANELFILTERELEM ON PFE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PGOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PGO_ERSTELLUSER" "~"
if joinKuerzel = BMUPGO
searchResult := searchResult "LEFT JOIN WWS.PGO_PICGROESSE ON PGO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIO_ERSTELLUSER" "~"
if joinKuerzel = BMUPIO
searchResult := searchResult "LEFT JOIN WWS.PIO_PICARTIKELDOKZU ON PIO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PGWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PGW_ERSTELLUSER" "~"
if joinKuerzel = BMUPGW
searchResult := searchResult "LEFT JOIN BUERO.PGW_PERSONGESELLWERTE ON PGW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PGZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PGZ_ERSTELLUSER" "~"
if joinKuerzel = BMUPGZ
searchResult := searchResult "LEFT JOIN BUERO.PGZ_PLANGRUPPETMOZU ON PGZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIA_ERSTELLUSER" "~"
if joinKuerzel = BMUPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIG_ERSTELLUSER" "~"
if joinKuerzel = BMUPIG
searchResult := searchResult "LEFT JOIN WWS.PIG_PICPREISGRUPPE ON PIG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIH_ERSTELLUSER" "~"
if joinKuerzel = BMUPIH
searchResult := searchResult "LEFT JOIN WWS.PIH_PICSPRACHEZU ON PIH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIK_ERSTELLUSER" "~"
if joinKuerzel = BMUPIK
searchResult := searchResult "LEFT JOIN WWS.PIK_PICKATEGORIEZU ON PIK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PILBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIL_ERSTELLUSER" "~"
if joinKuerzel = BMUPIL
searchResult := searchResult "LEFT JOIN WWS.PIL_PICKUNDE ON PIL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIM_ERSTELLUSER" "~"
if joinKuerzel = BMUPIM
searchResult := searchResult "LEFT JOIN BUERO.PIM_PERSONIMPORT ON PIM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PLDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PLD_ERSTELLUSER" "~"
if joinKuerzel = BMUPLD
searchResult := searchResult "LEFT JOIN WWS.PLD_PICALLERGENDATEN ON PLD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PLTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PLT_ERSTELLUSER" "~"
if joinKuerzel = BMUPLT
searchResult := searchResult "LEFT JOIN BUERO.PLT_PIPELINESTUFE ON PLT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PNKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PNK_ERSTELLUSER" "~"
if joinKuerzel = BMUPNK
searchResult := searchResult "LEFT JOIN BUERO.PNK_PERSONNUMMERNKREIS ON PNK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PNZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PNZ_ERSTELLUSER" "~"
if joinKuerzel = BMUPNZ
searchResult := searchResult "LEFT JOIN BUERO.PNZ_PERSONENZUSATZ ON PNZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = POZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = POZ_ERSTELLUSER" "~"
if joinKuerzel = BMUPOZ
searchResult := searchResult "LEFT JOIN BUERO.POZ_PISWORKFLOWZU ON POZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PPZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PPZ_ERSTELLUSER" "~"
if joinKuerzel = BMUPPZ
searchResult := searchResult "LEFT JOIN BUERO.PPZ_PLANGRUPPEPROZESSZU ON PPZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PRWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PRW_ERSTELLUSER" "~"
if joinKuerzel = BMUPRW
searchResult := searchResult "LEFT JOIN BUERO.PRW_PROJEKTWERT ON PRW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PUCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PUC_ERSTELLUSER" "~"
if joinKuerzel = BMUPUC
searchResult := searchResult "LEFT JOIN PRODUKTION.PUC_PPSUSERSTATICOMBI ON PUC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PWZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PWZ_ERSTELLUSER" "~"
if joinKuerzel = BMUPWZ
searchResult := searchResult "LEFT JOIN WWS.PWZ_PWKKUNDELIEFERANTZU ON PWZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PZFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PZF_ERSTELLUSER" "~"
if joinKuerzel = BMUPZF
searchResult := searchResult "LEFT JOIN WWS.PZF_PICFARBENZU ON PZF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PZKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PZK_ERSTELLUSER" "~"
if joinKuerzel = BMUPZK
searchResult := searchResult "LEFT JOIN WWSINDIV.PZK_PROZKTOZU ON PZK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = QMABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = QMA_ERSTELLUSER" "~"
if joinKuerzel = BMUQMA
searchResult := searchResult "LEFT JOIN PRODUKTION.QMA_QM_ARTIKEL ON QMA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = QMKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = QMK_ERSTELLUSER" "~"
if joinKuerzel = BMUQMK
searchResult := searchResult "LEFT JOIN PRODUKTION.QMK_QM_KOPF ON QMK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = QSTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = QST_ERSTELLUSER" "~"
if joinKuerzel = BMUQST
searchResult := searchResult "LEFT JOIN PRODUKTION.QST_QM_STAMM ON QST_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = QUSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = QUS_ERSTELLUSER" "~"
if joinKuerzel = BMUQUS
searchResult := searchResult "LEFT JOIN BUERO.QUS_QUOTENSUMME ON QUS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = QUVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = QUV_ERSTELLUSER" "~"
if joinKuerzel = BMUQUV
searchResult := searchResult "LEFT JOIN BUERO.QUV_QUOTENVERLAENGERUNG ON QUV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RAABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RAA_ERSTELLUSER" "~"
if joinKuerzel = BMURAA
searchResult := searchResult "LEFT JOIN WWS.RAA_RAHMENABRUFABFRAGE ON RAA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RDMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RDM_ERSTELLUSER" "~"
if joinKuerzel = BMURDM
searchResult := searchResult "LEFT JOIN BIL.RDM_RSTDRUCKMASTER ON RDM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKL_ERSTELLUSER" "~"
if joinKuerzel = BMUPKL
searchResult := searchResult "LEFT JOIN BUERO.PKL_PROJEKTPERSZU ON PKL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKYBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKY_ERSTELLUSER" "~"
if joinKuerzel = BMUPKY
searchResult := searchResult "LEFT JOIN BIL.PKY_PKH_PKK_ZU ON PKY_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PLCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PLC_ERSTELLUSER" "~"
if joinKuerzel = BMUPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PLIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PLI_ERSTELLUSER" "~"
if joinKuerzel = BMUPLI
searchResult := searchResult "LEFT JOIN BUERO.PLI_PIPELINE ON PLI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PLZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PLZ_ERSTELLUSER" "~"
if joinKuerzel = BMUPLZ
searchResult := searchResult "LEFT JOIN BUERO.PLZ_PLANGRUPPEZU ON PLZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PRKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PRK_ERSTELLUSER" "~"
if joinKuerzel = BMUPRK
searchResult := searchResult "LEFT JOIN BUERO.PRK_PROJEKTKREIS ON PRK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PSEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PSE_ERSTELLUSER" "~"
if joinKuerzel = BMUPSE
searchResult := searchResult "LEFT JOIN BIL.PSE_PERSRST_STERBET_WERT ON PSE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PSSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PSS_ERSTELLUSER" "~"
if joinKuerzel = BMUPSS
searchResult := searchResult "LEFT JOIN PRODUKTION.PSS_STAPEL_SCHRITT ON PSS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PTRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PTR_ERSTELLUSER" "~"
if joinKuerzel = BMUPTR
searchResult := searchResult "LEFT JOIN PRODUKTION.PTR_TRANSAKTION ON PTR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PWKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PWK_ERSTELLUSER" "~"
if joinKuerzel = BMUPWK
searchResult := searchResult "LEFT JOIN WWS.PWK_PREISRABATTWARTKOPF ON PWK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PWRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PWR_ERSTELLUSER" "~"
if joinKuerzel = BMUPWR
searchResult := searchResult "LEFT JOIN WWS.PWR_RABATTWARTUNGPOS ON PWR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = QAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = QAZ_ERSTELLUSER" "~"
if joinKuerzel = BMUQAZ
searchResult := searchResult "LEFT JOIN BUERO.QAZ_QUOTENAUSWERTUNGZU ON QAZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RAD_ERSTELLUSER" "~"
if joinKuerzel = BMURAD
searchResult := searchResult "LEFT JOIN WWS.RAD_RABATTBETRAGDETAIL ON RAD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RAPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RAP_ERSTELLUSER" "~"
if joinKuerzel = BMURAP
searchResult := searchResult "LEFT JOIN WWS.RAP_RABATTTABPOS ON RAP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = REGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = REG_ERSTELLUSER" "~"
if joinKuerzel = BMUREG
searchResult := searchResult "LEFT JOIN BUERO.REG_REGION ON REG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = REMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = REM_ERSTELLUSER" "~"
if joinKuerzel = BMUREM
searchResult := searchResult "LEFT JOIN BUERO.REM_REMINDER ON REM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RFMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RFM_ERSTELLUSER" "~"
if joinKuerzel = BMURFM
searchResult := searchResult "LEFT JOIN BUERO.RFM_RECHTSFORM ON RFM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RMDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RMD_ERSTELLUSER" "~"
if joinKuerzel = BMURMD
searchResult := searchResult "LEFT JOIN BUERO.RMD_RUNDUNGSMODELLDET ON RMD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RPTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RPT_ERSTELLUSER" "~"
if joinKuerzel = BMURPT
searchResult := searchResult "LEFT JOIN BMD.RPT_REPORT ON RPT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RPVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RPV_ERSTELLUSER" "~"
if joinKuerzel = BMURPV
searchResult := searchResult "LEFT JOIN BMD.RPV_REPORTVORSCHLAG ON RPV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RSKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RSK_ERSTELLUSER" "~"
if joinKuerzel = BMURSK
searchResult := searchResult "LEFT JOIN WWS.RSK_REGISTRIERKASSE ON RSK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SAK_ERSTELLUSER" "~"
if joinKuerzel = BMUSAK
searchResult := searchResult "LEFT JOIN FIBU.SAK_SACHKONTO ON SAK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SBABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SBA_ERSTELLUSER" "~"
if joinKuerzel = BMUSBA
searchResult := searchResult "LEFT JOIN HRS.SBA_SCHNITTBASIS ON SBA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SBFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SBF_ERSTELLUSER" "~"
if joinKuerzel = BMUSBF
searchResult := searchResult "LEFT JOIN BMD.SBF_STDBRIEF ON SBF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCK_ERSTELLUSER" "~"
if joinKuerzel = BMUSCK
searchResult := searchResult "LEFT JOIN PRODUKTION.SCK_SCKENNZAHL ON SCK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SKLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SKL_ERSTELLUSER" "~"
if joinKuerzel = BMUSKL
searchResult := searchResult "LEFT JOIN WWS.SKL_STUECKELUNGSLISTE ON SKL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PITBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIT_ERSTELLUSER" "~"
if joinKuerzel = BMUPIT
searchResult := searchResult "LEFT JOIN BMD.PIT_POSTIT ON PIT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIZ_ERSTELLUSER" "~"
if joinKuerzel = BMUPIZ
searchResult := searchResult "LEFT JOIN WWS.PIZ_PICARTIKELZU ON PIZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKW_ERSTELLUSER" "~"
if joinKuerzel = BMUPKW
searchResult := searchResult "LEFT JOIN BIL.PKW_PERSKORST_PARAMWERT ON PKW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PLOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PLO_ERSTELLUSER" "~"
if joinKuerzel = BMUPLO
searchResult := searchResult "LEFT JOIN PRODUKTION.PLO_PPS_LOS ON PLO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PMABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PMA_ERSTELLUSER" "~"
if joinKuerzel = BMUPMA
searchResult := searchResult "LEFT JOIN BUERO.PMA_PROJEKTMITARBZU ON PMA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PROBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PRO_ERSTELLUSER" "~"
if joinKuerzel = BMUPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PRPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PRP_ERSTELLUSER" "~"
if joinKuerzel = BMUPRP
searchResult := searchResult "LEFT JOIN WWS.PRP_PROVISIONTABPOS ON PRP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PSGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PSG_ERSTELLUSER" "~"
if joinKuerzel = BMUPSG
searchResult := searchResult "LEFT JOIN WWS.PSG_PREISGRUPPE ON PSG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PSJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PSJ_ERSTELLUSER" "~"
if joinKuerzel = BMUPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PSMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PSM_ERSTELLUSER" "~"
if joinKuerzel = BMUPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PSOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PSO_ERSTELLUSER" "~"
if joinKuerzel = BMUPSO
searchResult := searchResult "LEFT JOIN BUERO.PSO_PERSONSOZIALZU ON PSO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PTPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PTP_ERSTELLUSER" "~"
if joinKuerzel = BMUPTP
searchResult := searchResult "LEFT JOIN WWS.PTP_PREISTABPOS ON PTP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PVGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PVG_ERSTELLUSER" "~"
if joinKuerzel = BMUPVG
searchResult := searchResult "LEFT JOIN BUERO.PVG_PERSONVORERFASSANGEH ON PVG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PVYBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PVY_ERSTELLUSER" "~"
if joinKuerzel = BMUPVY
searchResult := searchResult "LEFT JOIN BMD.PVY_PACKAGEVISIBILITY ON PVY_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PWBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PWB_ERSTELLUSER" "~"
if joinKuerzel = BMUPWB
searchResult := searchResult "LEFT JOIN WWS.PWB_PWKRABATTTABZU ON PWB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PWPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PWP_ERSTELLUSER" "~"
if joinKuerzel = BMUPWP
searchResult := searchResult "LEFT JOIN WWS.PWP_PREISWARTUNGPOS ON PWP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RAS_ERSTELLUSER" "~"
if joinKuerzel = BMURAS
searchResult := searchResult "LEFT JOIN WWS.RAS_RABATTTABPOSSTAFFEL ON RAS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RAZ_ERSTELLUSER" "~"
if joinKuerzel = BMURAZ
searchResult := searchResult "LEFT JOIN BUERO.RAZ_RATENZAHLUNG ON RAZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = REIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = REI_ERSTELLUSER" "~"
if joinKuerzel = BMUREI
searchResult := searchResult "LEFT JOIN HRS.REI_REISESTAMM ON REI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = REOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = REO_ERSTELLUSER" "~"
if joinKuerzel = BMUREO
searchResult := searchResult "LEFT JOIN BUERO.REO_RELEVANTEOBJEKTE ON REO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = REZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = REZ_ERSTELLUSER" "~"
if joinKuerzel = BMUREZ
searchResult := searchResult "LEFT JOIN ZLE.REZ_REGELWERKZU ON REZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RMOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RMO_ERSTELLUSER" "~"
if joinKuerzel = BMURMO
searchResult := searchResult "LEFT JOIN BUERO.RMO_RUNDUNGSMODELL ON RMO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RNGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RNG_ERSTELLUSER" "~"
if joinKuerzel = BMURNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RVKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RVK_ERSTELLUSER" "~"
if joinKuerzel = BMURVK
searchResult := searchResult "LEFT JOIN ZLE.RVK_RECHVORLAGEKOPF ON RVK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SABBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SAB_ERSTELLUSER" "~"
if joinKuerzel = BMUSAB
searchResult := searchResult "LEFT JOIN BUERO.SAB_STELLENAUSBILDUNG ON SAB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SBGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SBG_ERSTELLUSER" "~"
if joinKuerzel = BMUSBG
searchResult := searchResult "LEFT JOIN BMD.SBG_STDBRIEFGRUPPE ON SBG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MAD_ERSTELLUSER" "~"
if joinKuerzel = BMUMAD
searchResult := searchResult "LEFT JOIN HRS.MAD_MASSENDATENERF ON MAD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MBMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MBM_ERSTELLUSER" "~"
if joinKuerzel = BMUMBM
searchResult := searchResult "LEFT JOIN HRS.MBM_MBGM ON MBM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MDDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MDD_ERSTELLUSER" "~"
if joinKuerzel = BMUMDD
searchResult := searchResult "LEFT JOIN BUERO.MDD_MEDIENDOKUMENTZU ON MDD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MEDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MED_ERSTELLUSER" "~"
if joinKuerzel = BMUMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MHTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MHT_ERSTELLUSER" "~"
if joinKuerzel = BMUMHT
searchResult := searchResult "LEFT JOIN BUERO.MHT_MITARBHISTORIE ON MHT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MLSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MLS_ERSTELLUSER" "~"
if joinKuerzel = BMUMLS
searchResult := searchResult "LEFT JOIN ZLE.MLS_MITARBLEISTTAUSCHSATZ ON MLS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MSUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MSU_ERSTELLUSER" "~"
if joinKuerzel = BMUMSU
searchResult := searchResult "LEFT JOIN FIBU.MSU_MAHNSUMME ON MSU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MVPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MVP_ERSTELLUSER" "~"
if joinKuerzel = BMUMVP
searchResult := searchResult "LEFT JOIN BUERO.MVP_MITARBEITERVORLAGEPOS ON MVP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NGZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NGZ_ERSTELLUSER" "~"
if joinKuerzel = BMUNGZ
searchResult := searchResult "LEFT JOIN BUERO.NGZ_NOTIZGRUPPEZU ON NGZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NORBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NOR_ERSTELLUSER" "~"
if joinKuerzel = BMUNOR
searchResult := searchResult "LEFT JOIN WWS.NOR_NORM ON NOR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NUOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NUO_ERSTELLUSER" "~"
if joinKuerzel = BMUNUO
searchResult := searchResult "LEFT JOIN BMD.NUO_NTCSUPDPRIORITAET ON NUO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OAUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OAU_ERSTELLUSER" "~"
if joinKuerzel = BMUOAU
searchResult := searchResult "LEFT JOIN LOHN.OAU_OF_AUFTRAG ON OAU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OBQBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OBQ_ERSTELLUSER" "~"
if joinKuerzel = BMUOBQ
searchResult := searchResult "LEFT JOIN BUERO.OBQ_ONLBEWQUALIFIKATION ON OBQ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ODTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ODT_ERSTELLUSER" "~"
if joinKuerzel = BMUODT
searchResult := searchResult "LEFT JOIN BMD.ODT_ONLINEDATEITRANSFER ON ODT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OGKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OGK_ERSTELLUSER" "~"
if joinKuerzel = BMUOGK
searchResult := searchResult "LEFT JOIN LOHN.OGK_OF_GKK ON OGK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OKVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OKV_ERSTELLUSER" "~"
if joinKuerzel = BMUOKV
searchResult := searchResult "LEFT JOIN LOHN.OKV_OF_KOLLEKTIV ON OKV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OLEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OLE_ERSTELLUSER" "~"
if joinKuerzel = BMUOLE
searchResult := searchResult "LEFT JOIN LOHN.OLE_OF_LEISTUNG ON OLE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ONMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ONM_ERSTELLUSER" "~"
if joinKuerzel = BMUONM
searchResult := searchResult "LEFT JOIN HRS.ONM_ONLINEBEW_MERKMALE ON ONM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OPEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OPE_ERSTELLUSER" "~"
if joinKuerzel = BMUOPE
searchResult := searchResult "LEFT JOIN LOHN.OPE_OF_PERSON ON OPE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OROBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ORO_ERSTELLUSER" "~"
if joinKuerzel = BMUORO
searchResult := searchResult "LEFT JOIN LOHN.ORO_OF_REGORT ON ORO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ORSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ORS_ERSTELLUSER" "~"
if joinKuerzel = BMUORS
searchResult := searchResult "LEFT JOIN LOHN.ORS_OF_REISESTAMM ON ORS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OSTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OST_ERSTELLUSER" "~"
if joinKuerzel = BMUOST
searchResult := searchResult "LEFT JOIN LOHN.OST_OF_STAAT ON OST_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OTIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OTI_ERSTELLUSER" "~"
if joinKuerzel = BMUOTI
searchResult := searchResult "LEFT JOIN LOHN.OTI_OF_TITEL ON OTI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAS_ERSTELLUSER" "~"
if joinKuerzel = BMUPAS
searchResult := searchResult "LEFT JOIN WWS.PAS_PACKSTOFFPREIS ON PAS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PAUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAU_ERSTELLUSER" "~"
if joinKuerzel = BMUPAU
searchResult := searchResult "LEFT JOIN BMDDEFAULT.PAU_PARAMUNTERGRUPPE ON PAU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PAVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAV_ERSTELLUSER" "~"
if joinKuerzel = BMUPAV
searchResult := searchResult "LEFT JOIN WWS.PAV_PICALTERNATIVARTIKEL ON PAV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PMXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PMX_ERSTELLUSER" "~"
if joinKuerzel = BMUPMX
searchResult := searchResult "LEFT JOIN KORE.PMX_PLANMENGENEXT ON PMX_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = POSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = POS_ERSTELLUSER" "~"
if joinKuerzel = BMUPOS
searchResult := searchResult "LEFT JOIN BUERO.POS_POST ON POS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PRLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PRL_ERSTELLUSER" "~"
if joinKuerzel = BMUPRL
searchResult := searchResult "LEFT JOIN WWS.PRL_PREISGRUNDLISTE ON PRL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PRZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PRZ_ERSTELLUSER" "~"
if joinKuerzel = BMUPRZ
searchResult := searchResult "LEFT JOIN PRODUKTION.PRZ_PROZESSBAZZU ON PRZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PTZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PTZ_ERSTELLUSER" "~"
if joinKuerzel = BMUPTZ
searchResult := searchResult "LEFT JOIN WWS.PTZ_PREISTABZU ON PTZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PUPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PUP_ERSTELLUSER" "~"
if joinKuerzel = BMUPUP
searchResult := searchResult "LEFT JOIN BUERO.PUP_PERSONENUPDATE ON PUP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PVZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PVZ_ERSTELLUSER" "~"
if joinKuerzel = BMUPVZ
searchResult := searchResult "LEFT JOIN WWS.PVZ_PROVISIONTABZU ON PVZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PWABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PWA_ERSTELLUSER" "~"
if joinKuerzel = BMUPWA
searchResult := searchResult "LEFT JOIN WWS.PWA_PWKARTIKELZU ON PWA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PZEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PZE_ERSTELLUSER" "~"
if joinKuerzel = BMUPZE
searchResult := searchResult "LEFT JOIN HRS.PZE_PERKZGLGZEILE ON PZE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = QAWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = QAW_ERSTELLUSER" "~"
if joinKuerzel = BMUQAW
searchResult := searchResult "LEFT JOIN BUERO.QAW_QUOTENAUSWERTUNG ON QAW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = REFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = REF_ERSTELLUSER" "~"
if joinKuerzel = BMUREF
searchResult := searchResult "LEFT JOIN BUERO.REF_REFERENZ ON REF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = REHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = REH_ERSTELLUSER" "~"
if joinKuerzel = BMUREH
searchResult := searchResult "LEFT JOIN PRODUKTION.REH_REIHE ON REH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RELBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = REL_ERSTELLUSER" "~"
if joinKuerzel = BMUREL
searchResult := searchResult "LEFT JOIN BUERO.REL_RELIGION ON REL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RIABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RIA_ERSTELLUSER" "~"
if joinKuerzel = BMURIA
searchResult := searchResult "LEFT JOIN PRODUKTION.RIA_RISIKOANALYSE ON RIA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RKABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RKA_ERSTELLUSER" "~"
if joinKuerzel = BMURKA
searchResult := searchResult "LEFT JOIN BMD.RKA_REPLIKATIONSAENDERUNG ON RKA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RKGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RKG_ERSTELLUSER" "~"
if joinKuerzel = BMURKG
searchResult := searchResult "LEFT JOIN BMD.RKG_TABREPLGRUPPE ON RKG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RPZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RPZ_ERSTELLUSER" "~"
if joinKuerzel = BMURPZ
searchResult := searchResult "LEFT JOIN ZLE.RPZ_RECHPOSZU ON RPZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RRDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RRD_ERSTELLUSER" "~"
if joinKuerzel = BMURRD
searchResult := searchResult "LEFT JOIN WWS.RRD_RECHNUNGSRABATTDETAIL ON RRD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RSBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RSB_ERSTELLUSER" "~"
if joinKuerzel = BMURSB
searchResult := searchResult "LEFT JOIN BUERO.RSB_RESSOURCENBEDARF ON RSB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RTIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RTI_ERSTELLUSER" "~"
if joinKuerzel = BMURTI
searchResult := searchResult "LEFT JOIN BMD.RTI_RTFINDEX ON RTI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RUPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RUP_ERSTELLUSER" "~"
if joinKuerzel = BMURUP
searchResult := searchResult "LEFT JOIN WWS.RUP_RUNDUNGPOSITION ON RUP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SALBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SAL_ERSTELLUSER" "~"
if joinKuerzel = BMUSAL
searchResult := searchResult "LEFT JOIN FIBU.SAL_SALDEN ON SAL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SAWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SAW_ERSTELLUSER" "~"
if joinKuerzel = BMUSAW
searchResult := searchResult "LEFT JOIN WWSINDIV.SAW_STATISTIKWERTE ON SAW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SBBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SBB_ERSTELLUSER" "~"
if joinKuerzel = BMUSBB
searchResult := searchResult "LEFT JOIN HRS.SBB_SOLLBESETZUNG ON SBB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SBKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SBK_ERSTELLUSER" "~"
if joinKuerzel = BMUSBK
searchResult := searchResult "LEFT JOIN WWS.SBK_SAISONBESCHAFFKOPF ON SBK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCA_ERSTELLUSER" "~"
if joinKuerzel = BMUSCA
searchResult := searchResult "LEFT JOIN PRODUKTION.SCA_SCKRITERIUM ON SCA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIP_ERSTELLUSER" "~"
if joinKuerzel = BMUPIP
searchResult := searchResult "LEFT JOIN BUERO.PIP_PERSINFOPROTOKOLL ON PIP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKS_ERSTELLUSER" "~"
if joinKuerzel = BMUPKS
searchResult := searchResult "LEFT JOIN PRODUKTION.PKS_SOLL_KAPAZITAET ON PKS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PRNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PRN_ERSTELLUSER" "~"
if joinKuerzel = BMUPRN
searchResult := searchResult "LEFT JOIN WWS.PRN_EXTPIAKEYNAMES ON PRN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PSABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PSA_ERSTELLUSER" "~"
if joinKuerzel = BMUPSA
searchResult := searchResult "LEFT JOIN HRS.PSA_PERSONALSTAMM_ABRSTAT ON PSA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PSWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PSW_ERSTELLUSER" "~"
if joinKuerzel = BMUPSW
searchResult := searchResult "LEFT JOIN BUERO.PSW_PERSONENWERTE ON PSW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PVKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PVK_ERSTELLUSER" "~"
if joinKuerzel = BMUPVK
searchResult := searchResult "LEFT JOIN WWS.PVK_PROVISIONTAB ON PVK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PWFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PWF_ERSTELLUSER" "~"
if joinKuerzel = BMUPWF
searchResult := searchResult "LEFT JOIN WWS.PWF_PWKFIRMENZU ON PWF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PWOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PWO_ERSTELLUSER" "~"
if joinKuerzel = BMUPWO
searchResult := searchResult "LEFT JOIN BUERO.PWO_PASSWORT ON PWO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PWTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PWT_ERSTELLUSER" "~"
if joinKuerzel = BMUPWT
searchResult := searchResult "LEFT JOIN BIL.PWT_PERSRST_ALTERSV_WERT ON PWT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PWXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PWX_ERSTELLUSER" "~"
if joinKuerzel = BMUPWX
searchResult := searchResult "LEFT JOIN KORE.PWX_PLANWERTEEXT ON PWX_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = QDABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = QDA_ERSTELLUSER" "~"
if joinKuerzel = BMUQDA
searchResult := searchResult "LEFT JOIN PRODUKTION.QDA_QM_DATA ON QDA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = QLKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = QLK_ERSTELLUSER" "~"
if joinKuerzel = BMUQLK
searchResult := searchResult "LEFT JOIN BMD.QLK_LINK ON QLK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = QUFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = QUF_ERSTELLUSER" "~"
if joinKuerzel = BMUQUF
searchResult := searchResult "LEFT JOIN BUERO.QUF_QUOTENFRIST ON QUF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = QUOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = QUO_ERSTELLUSER" "~"
if joinKuerzel = BMUQUO
searchResult := searchResult "LEFT JOIN BUERO.QUO_QUOTE ON QUO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RAEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RAE_ERSTELLUSER" "~"
if joinKuerzel = BMURAE
searchResult := searchResult "LEFT JOIN FIBU.RAE_RATE ON RAE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RCLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RCL_ERSTELLUSER" "~"
if joinKuerzel = BMURCL
searchResult := searchResult "LEFT JOIN PRODUKTION.RCL_REPEATING_CHECKLIST ON RCL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RERBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RER_ERSTELLUSER" "~"
if joinKuerzel = BMURER
searchResult := searchResult "LEFT JOIN WWS.RER_RESERVIERUNGSREFERENZ ON RER_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = REWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = REW_ERSTELLUSER" "~"
if joinKuerzel = BMUREW
searchResult := searchResult "LEFT JOIN ZLE.REW_REGELWERK ON REW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RIHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RIH_ERSTELLUSER" "~"
if joinKuerzel = BMURIH
searchResult := searchResult "LEFT JOIN PRODUKTION.RIH_RISIKOANALYSE_HEAD ON RIH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RLKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RLK_ERSTELLUSER" "~"
if joinKuerzel = BMURLK
searchResult := searchResult "LEFT JOIN WWS.RLK_RECHNUNGSPLANKOPF ON RLK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RPMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RPM_ERSTELLUSER" "~"
if joinKuerzel = BMURPM
searchResult := searchResult "LEFT JOIN PRODUKTION.RPM_RETRO_PRODUKT_MATERIAL ON RPM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RSOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RSO_ERSTELLUSER" "~"
if joinKuerzel = BMURSO
searchResult := searchResult "LEFT JOIN HRS.RSO_REPORTSORTIERUNG ON RSO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RZGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RZG_ERSTELLUSER" "~"
if joinKuerzel = BMURZG
searchResult := searchResult "LEFT JOIN BUERO.RZG_RUHEZEITGESETZ ON RZG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RZSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RZS_ERSTELLUSER" "~"
if joinKuerzel = BMURZS
searchResult := searchResult "LEFT JOIN BUERO.RZS_RUECKZAHLUNGSSTAMM ON RZS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SA2BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SA2_ERSTELLUSER" "~"
if joinKuerzel = BMUSA2
searchResult := searchResult "LEFT JOIN ONVIS.SA2_SAURER_BDE ON SA2_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SA4BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SA4_ERSTELLUSER" "~"
if joinKuerzel = BMUSA4
searchResult := searchResult "LEFT JOIN ONVIS.SA4_SAURER_BDE ON SA4_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SEKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SEK_ERSTELLUSER" "~"
if joinKuerzel = BMUSEK
searchResult := searchResult "LEFT JOIN BUERO.SEK_SEKRETARIAT ON SEK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SERBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SER_ERSTELLUSER" "~"
if joinKuerzel = BMUSER
searchResult := searchResult "LEFT JOIN BMD.SER_SQLEXECREQUEST ON SER_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SLABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SLA_ERSTELLUSER" "~"
if joinKuerzel = BMUSLA
searchResult := searchResult "LEFT JOIN WWS.SLA_STKLISTVARAUSWAHL ON SLA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SMZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SMZ_ERSTELLUSER" "~"
if joinKuerzel = BMUSMZ
searchResult := searchResult "LEFT JOIN ZLE.SMZ_SMARTINTERFACEZU ON SMZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SN1BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SN1_ERSTELLUSER" "~"
if joinKuerzel = BMUSN1
searchResult := searchResult "LEFT JOIN WWS.SN1_VA_SNR_SERIENNUMMERN ON SN1_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SN2BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SN2_ERSTELLUSER" "~"
if joinKuerzel = BMUSN2
searchResult := searchResult "LEFT JOIN WWS.SN2_VA_SNR_SERIENNUMMERN ON SN2_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPW_ERSTELLUSER" "~"
if joinKuerzel = BMUSPW
searchResult := searchResult "LEFT JOIN BIL.SPW_SPIEGELWERTE ON SPW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SRABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SRA_ERSTELLUSER" "~"
if joinKuerzel = BMUSRA
searchResult := searchResult "LEFT JOIN WWS.SRA_STEUERART ON SRA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SSZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SSZ_ERSTELLUSER" "~"
if joinKuerzel = BMUSSZ
searchResult := searchResult "LEFT JOIN ZLE.SSZ_SORTIERKOPFSMIZU ON SSZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STE_ERSTELLUSER" "~"
if joinKuerzel = BMUSTE
searchResult := searchResult "LEFT JOIN BUERO.STE_STEUERERKLAERUNG ON STE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TAD_ERSTELLUSER" "~"
if joinKuerzel = BMUTAD
searchResult := searchResult "LEFT JOIN HRS.TAD_ELDADETAIL_AD_TMP ON TAD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TBABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TBA_ERSTELLUSER" "~"
if joinKuerzel = BMUTBA
searchResult := searchResult "LEFT JOIN BUERO.TBA_TAGESMDBUCHARTAUFB ON TBA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TBEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TBE_ERSTELLUSER" "~"
if joinKuerzel = BMUTBE
searchResult := searchResult "LEFT JOIN BMD.TBE_TEMPBERECHTIGUNG ON TBE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TBGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TBG_ERSTELLUSER" "~"
if joinKuerzel = BMUTBG
searchResult := searchResult "LEFT JOIN HRS.TBG_ELDADETAIL_BGN_TMP ON TBG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TBZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TBZ_ERSTELLUSER" "~"
if joinKuerzel = BMUTBZ
searchResult := searchResult "LEFT JOIN BUERO.TBZ_TERMINALBUCHARTZUORD ON TBZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TCNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TCN_ERSTELLUSER" "~"
if joinKuerzel = BMUTCN
searchResult := searchResult "LEFT JOIN BMD.TCN_TCVERSIONEN ON TCN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TEHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TEH_ERSTELLUSER" "~"
if joinKuerzel = BMUTEH
searchResult := searchResult "LEFT JOIN BUERO.TEH_TELEKOMHARDWARE ON TEH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TLABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TLA_ERSTELLUSER" "~"
if joinKuerzel = BMUTLA
searchResult := searchResult "LEFT JOIN PRODUKTION.TLA_BT_LOGISTIK_ANGEFORDERT ON TLA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TLNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TLN_ERSTELLUSER" "~"
if joinKuerzel = BMUTLN
searchResult := searchResult "LEFT JOIN BUERO.TLN_TEILNEHMER ON TLN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TOOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TOO_ERSTELLUSER" "~"
if joinKuerzel = BMUTOO
searchResult := searchResult "LEFT JOIN WWS.TOO_TOUROBJEKT ON TOO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TPRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TPR_ERSTELLUSER" "~"
if joinKuerzel = BMUTPR
searchResult := searchResult "LEFT JOIN BUERO.TPR_TELEFONPROTOKOLL ON TPR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TRPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TRP_ERSTELLUSER" "~"
if joinKuerzel = BMUTRP
searchResult := searchResult "LEFT JOIN WWS.TRP_TRANSPORTPOSITION ON TRP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TSDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TSD_ERSTELLUSER" "~"
if joinKuerzel = BMUTSD
searchResult := searchResult "LEFT JOIN WWS.TSD_TOUCHSCREENDETAIL ON TSD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UDPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UDP_ERSTELLUSER" "~"
if joinKuerzel = BMUUDP
searchResult := searchResult "LEFT JOIN KORE.UDP_UMLAGEPLANWERTE ON UDP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ULEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ULE_ERSTELLUSER" "~"
if joinKuerzel = BMUULE
searchResult := searchResult "LEFT JOIN BMDINTERN.ULE_UPDATELEBENSLAUF ON ULE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ULFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ULF_ERSTELLUSER" "~"
if joinKuerzel = BMUULF
searchResult := searchResult "LEFT JOIN BMD.ULF_USERLINKFIELD ON ULF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SKOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SKO_ERSTELLUSER" "~"
if joinKuerzel = BMUSKO
searchResult := searchResult "LEFT JOIN FIBU.SKO_STEUERKONTEN ON SKO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SNVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SNV_ERSTELLUSER" "~"
if joinKuerzel = BMUSNV
searchResult := searchResult "LEFT JOIN HRS.SNV_SCHNITTVORSCHLAG ON SNV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SOZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SOZ_ERSTELLUSER" "~"
if joinKuerzel = BMUSOZ
searchResult := searchResult "LEFT JOIN BUERO.SOZ_SOZIAL ON SOZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPS_ERSTELLUSER" "~"
if joinKuerzel = BMUSPS
searchResult := searchResult "LEFT JOIN FIBU.SPS_SPENDENSUMME ON SPS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPV_ERSTELLUSER" "~"
if joinKuerzel = BMUSPV
searchResult := searchResult "LEFT JOIN WWS.SPV_PROVISIONSTAFFEL ON SPV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SREBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SRE_ERSTELLUSER" "~"
if joinKuerzel = BMUSRE
searchResult := searchResult "LEFT JOIN BMD.SRE_STANDARDREPORT ON SRE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SSKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SSK_ERSTELLUSER" "~"
if joinKuerzel = BMUSSK
searchResult := searchResult "LEFT JOIN PRODUKTION.SSK_SAMMELSCHEIN_KOPF ON SSK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STA_ERSTELLUSER" "~"
if joinKuerzel = BMUSTA
searchResult := searchResult "LEFT JOIN BUERO.STA_STELLENAUSBILDUNGEN ON STA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STL_ERSTELLUSER" "~"
if joinKuerzel = BMUSTL
searchResult := searchResult "LEFT JOIN BUERO.STL_STELLE ON STL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SWSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SWS_ERSTELLUSER" "~"
if joinKuerzel = BMUSWS
searchResult := searchResult "LEFT JOIN HRS.SWS_SW_STUNDEN ON SWS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TAK_ERSTELLUSER" "~"
if joinKuerzel = BMUTAK
searchResult := searchResult "LEFT JOIN WWS.TAK_ARTIKEL_KAUFAUSW_TMP ON TAK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TCVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TCV_ERSTELLUSER" "~"
if joinKuerzel = BMUTCV
searchResult := searchResult "LEFT JOIN BMD.TCV_TCVERSIONCASE ON TCV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TGRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TGR_ERSTELLUSER" "~"
if joinKuerzel = BMUTGR
searchResult := searchResult "LEFT JOIN HRS.TGR_TARIFGRUPPE ON TGR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TLTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TLT_ERSTELLUSER" "~"
if joinKuerzel = BMUTLT
searchResult := searchResult "LEFT JOIN PRODUKTION.TLT_BT_LOGISTIK_TRANSPORT ON TLT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TMDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TMD_ERSTELLUSER" "~"
if joinKuerzel = BMUTMD
searchResult := searchResult "LEFT JOIN HRS.TMD_TERMINSTAMMDATEN ON TMD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TRZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TRZ_ERSTELLUSER" "~"
if joinKuerzel = BMUTRZ
searchResult := searchResult "LEFT JOIN BUERO.TRZ_TERMINRESSOURCENZU ON TRZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TSSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TSS_ERSTELLUSER" "~"
if joinKuerzel = BMUTSS
searchResult := searchResult "LEFT JOIN ZLE.TSS_TAGESABSOLLSTUNDEN ON TSS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TTBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TTB_ERSTELLUSER" "~"
if joinKuerzel = BMUTTB
searchResult := searchResult "LEFT JOIN BIL.TTB_TANTIEMENBERECHNUNG ON TTB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UMPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UMP_ERSTELLUSER" "~"
if joinKuerzel = BMUUMP
searchResult := searchResult "LEFT JOIN KORE.UMP_UMLAGEPLANMENGEN ON UMP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UTPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UTP_ERSTELLUSER" "~"
if joinKuerzel = BMUUTP
searchResult := searchResult "LEFT JOIN BMD.UTP_USERTREEPOS ON UTP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UVSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UVS_ERSTELLUSER" "~"
if joinKuerzel = BMUUVS
searchResult := searchResult "LEFT JOIN FIBU.UVS_MCUSTEUERCODEZU ON UVS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UVWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UVW_ERSTELLUSER" "~"
if joinKuerzel = BMUUVW
searchResult := searchResult "LEFT JOIN FIBU.UVW_UVAWERT ON UVW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VLJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VLJ_ERSTELLUSER" "~"
if joinKuerzel = BMUVLJ
searchResult := searchResult "LEFT JOIN WWS.VLJ_VERLEIHJOURNAL ON VLJ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VMRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VMR_ERSTELLUSER" "~"
if joinKuerzel = BMUVMR
searchResult := searchResult "LEFT JOIN WWS.VMR_VERTABRMAXRABATTE ON VMR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VPIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VPI_ERSTELLUSER" "~"
if joinKuerzel = BMUVPI
searchResult := searchResult "LEFT JOIN BUERO.VPI_VPINDEX ON VPI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VRLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VRL_ERSTELLUSER" "~"
if joinKuerzel = BMUVRL
searchResult := searchResult "LEFT JOIN WWS.VRL_VA_PWLIEFARTIKEL ON VRL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RDRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RDR_ERSTELLUSER" "~"
if joinKuerzel = BMURDR
searchResult := searchResult "LEFT JOIN ZLE.RDR_RECHDRUCKSESSION ON RDR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RECBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = REC_ERSTELLUSER" "~"
if joinKuerzel = BMUREC
searchResult := searchResult "LEFT JOIN BMD.REC_RECORDINTERVALL ON REC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = REDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RED_ERSTELLUSER" "~"
if joinKuerzel = BMURED
searchResult := searchResult "LEFT JOIN HRS.RED_REISEDETAIL ON RED_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RETBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RET_ERSTELLUSER" "~"
if joinKuerzel = BMURET
searchResult := searchResult "LEFT JOIN BIL.RET_BEWRES_ERFASS_TMP ON RET_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RGMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RGM_ERSTELLUSER" "~"
if joinKuerzel = BMURGM
searchResult := searchResult "LEFT JOIN DATANORM.RGM_RABATT_GRUPPE ON RGM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RKEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RKE_ERSTELLUSER" "~"
if joinKuerzel = BMURKE
searchResult := searchResult "LEFT JOIN BMD.RKE_TABREPLEINTRAG ON RKE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RNPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RNP_ERSTELLUSER" "~"
if joinKuerzel = BMURNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RNRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RNR_ERSTELLUSER" "~"
if joinKuerzel = BMURNR
searchResult := searchResult "LEFT JOIN WWS.RNR_RECHNUNGSRABATT ON RNR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RPKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RPK_ERSTELLUSER" "~"
if joinKuerzel = BMURPK
searchResult := searchResult "LEFT JOIN WWS.RPK_RECHNUNGSPOS_KUM_TMP ON RPK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RSEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RSE_ERSTELLUSER" "~"
if joinKuerzel = BMURSE
searchResult := searchResult "LEFT JOIN WWS.RSE_RESERVIERUNGSSYSEXT ON RSE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RSIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RSI_ERSTELLUSER" "~"
if joinKuerzel = BMURSI
searchResult := searchResult "LEFT JOIN WWS.RSI_REGKASIGNATUREH ON RSI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RUKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RUK_ERSTELLUSER" "~"
if joinKuerzel = BMURUK
searchResult := searchResult "LEFT JOIN WWS.RUK_RUNDUNGKOPF ON RUK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RZIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RZI_ERSTELLUSER" "~"
if joinKuerzel = BMURZI
searchResult := searchResult "LEFT JOIN HRS.RZI_REISEZIELE ON RZI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SAZ_ERSTELLUSER" "~"
if joinKuerzel = BMUSAZ
searchResult := searchResult "LEFT JOIN BUERO.SAZ_SACHKONTOARCHIVZU ON SAZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SBPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SBP_ERSTELLUSER" "~"
if joinKuerzel = BMUSBP
searchResult := searchResult "LEFT JOIN WWS.SBP_SAISONBESCHAFFPOS ON SBP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCE_ERSTELLUSER" "~"
if joinKuerzel = BMUSCE
searchResult := searchResult "LEFT JOIN PRODUKTION.SCE_SCORECARD ON SCE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCG_ERSTELLUSER" "~"
if joinKuerzel = BMUSCG
searchResult := searchResult "LEFT JOIN PRODUKTION.SCG_SCORECARDKRITZU ON SCG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SFKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SFK_ERSTELLUSER" "~"
if joinKuerzel = BMUSFK
searchResult := searchResult "LEFT JOIN BUERO.SFK_SYNCFELDKOPF ON SFK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SIRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SIR_ERSTELLUSER" "~"
if joinKuerzel = BMUSIR
searchResult := searchResult "LEFT JOIN HRS.SIR_STANDARDIMPORTREFERENZ ON SIR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPK_ERSTELLUSER" "~"
if joinKuerzel = BMUSPK
searchResult := searchResult "LEFT JOIN BMD.SPK_STAPELKOPF ON SPK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPR_ERSTELLUSER" "~"
if joinKuerzel = BMUSPR
searchResult := searchResult "LEFT JOIN BUERO.SPR_SPRACHE ON SPR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SSCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SSC_ERSTELLUSER" "~"
if joinKuerzel = BMUSSC
searchResult := searchResult "LEFT JOIN FIBU.SSC_STEUERSCHLUESSEL ON SSC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SSPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SSP_ERSTELLUSER" "~"
if joinKuerzel = BMUSSP
searchResult := searchResult "LEFT JOIN PRODUKTION.SSP_SAMMELSCHEIN_POS ON SSP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STS_ERSTELLUSER" "~"
if joinKuerzel = BMUSTS
searchResult := searchResult "LEFT JOIN BIL.STS_STEUERSTAMMDAT ON STS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STV_ERSTELLUSER" "~"
if joinKuerzel = BMUSTV
searchResult := searchResult "LEFT JOIN BUERO.STV_STATVERFAHREN ON STV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STZ_ERSTELLUSER" "~"
if joinKuerzel = BMUSTZ
searchResult := searchResult "LEFT JOIN WWS.STZ_STEUERTEXTZUORDNUNG ON STZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SBMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SBM_ERSTELLUSER" "~"
if joinKuerzel = BMUSBM
searchResult := searchResult "LEFT JOIN BUERO.SBM_SBSPERSONALMITARBZU ON SBM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCQBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCQ_ERSTELLUSER" "~"
if joinKuerzel = BMUSCQ
searchResult := searchResult "LEFT JOIN PRODUKTION.SCQ_SCKZGRP ON SCQ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCR_ERSTELLUSER" "~"
if joinKuerzel = BMUSCR
searchResult := searchResult "LEFT JOIN BMDENTW.SCR_SCRREGELWERK ON SCR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCT_ERSTELLUSER" "~"
if joinKuerzel = BMUSCT
searchResult := searchResult "LEFT JOIN BMDENTW.SCT_SCRINTRAY ON SCT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SESBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SES_ERSTELLUSER" "~"
if joinKuerzel = BMUSES
searchResult := searchResult "LEFT JOIN BIL.SES_STEUERERKLSETTINGS ON SES_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SFZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SFZ_ERSTELLUSER" "~"
if joinKuerzel = BMUSFZ
searchResult := searchResult "LEFT JOIN BUERO.SFZ_SYNCFELDZUORDNUNG ON SFZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SKEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SKE_ERSTELLUSER" "~"
if joinKuerzel = BMUSKE
searchResult := searchResult "LEFT JOIN BIL.SKE_STEUERKUNDENZUERW ON SKE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SRTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SRT_ERSTELLUSER" "~"
if joinKuerzel = BMUSRT
searchResult := searchResult "LEFT JOIN WWS.SRT_STEUERTAB ON SRT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STB_ERSTELLUSER" "~"
if joinKuerzel = BMUSTB
searchResult := searchResult "LEFT JOIN PRODUKTION.STB_STAKE_HEAD ON STB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STF_ERSTELLUSER" "~"
if joinKuerzel = BMUSTF
searchResult := searchResult "LEFT JOIN FIBU.STF_FIBUSTEUERCODEZU ON STF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TALBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TAL_ERSTELLUSER" "~"
if joinKuerzel = BMUTAL
searchResult := searchResult "LEFT JOIN WWS.TAL_ARTIKELZUORDNUNG_TMP ON TAL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TCZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TCZ_ERSTELLUSER" "~"
if joinKuerzel = BMUTCZ
searchResult := searchResult "LEFT JOIN BMD.TCZ_TCVERSIONCASEALZU ON TCZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TEABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TEA_ERSTELLUSER" "~"
if joinKuerzel = BMUTEA
searchResult := searchResult "LEFT JOIN BUERO.TEA_TELEFONART ON TEA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TELBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TEL_ERSTELLUSER" "~"
if joinKuerzel = BMUTEL
searchResult := searchResult "LEFT JOIN BUERO.TEL_TELEFON ON TEL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TFPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TFP_ERSTELLUSER" "~"
if joinKuerzel = BMUTFP
searchResult := searchResult "LEFT JOIN FIBU.TFP_FIBUPERSON_TMP ON TFP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TFTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TFT_ERSTELLUSER" "~"
if joinKuerzel = BMUTFT
searchResult := searchResult "LEFT JOIN WWS.TFT_TOURFEIERTAG ON TFT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TIPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TIP_ERSTELLUSER" "~"
if joinKuerzel = BMUTIP
searchResult := searchResult "LEFT JOIN BUERO.TIP_TERMINALIMPPROTOKOLL ON TIP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TIZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TIZ_ERSTELLUSER" "~"
if joinKuerzel = BMUTIZ
searchResult := searchResult "LEFT JOIN BMDENTW.TIZ_TICKETPEPAUFGABEZU ON TIZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TMSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TMS_ERSTELLUSER" "~"
if joinKuerzel = BMUTMS
searchResult := searchResult "LEFT JOIN BUERO.TMS_TMSICHERUNG ON TMS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TPDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TPD_ERSTELLUSER" "~"
if joinKuerzel = BMUTPD
searchResult := searchResult "LEFT JOIN BUERO.TPD_TELPROTDOKUMENTZU ON TPD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TPTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TPT_ERSTELLUSER" "~"
if joinKuerzel = BMUTPT
searchResult := searchResult "LEFT JOIN HRS.TPT_TARIFPROZENT ON TPT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TSWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TSW_ERSTELLUSER" "~"
if joinKuerzel = BMUTSW
searchResult := searchResult "LEFT JOIN WWS.TSW_STATISTIKWERTE_TMP ON TSW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TWLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TWL_ERSTELLUSER" "~"
if joinKuerzel = BMUTWL
searchResult := searchResult "LEFT JOIN WWS.TWL_WEINSATZLAJ_TMP ON TWL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UANBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UAN_ERSTELLUSER" "~"
if joinKuerzel = BMUUAN
searchResult := searchResult "LEFT JOIN BMDINTERN.UAN_UPDATEANFORDERUNGSTXT ON UAN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UPKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UPK_ERSTELLUSER" "~"
if joinKuerzel = BMUUPK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.UPK_UPH_KUNDE ON UPK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = USPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = USP_ERSTELLUSER" "~"
if joinKuerzel = BMUUSP
searchResult := searchResult "LEFT JOIN BMDINTERN.USP_UPDATESPERRE ON USP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PBDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PBD_ERSTELLUSER" "~"
if joinKuerzel = BMUPBD
searchResult := searchResult "LEFT JOIN BIL.PBD_PERSKORST_BERECHDET ON PBD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PBMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PBM_ERSTELLUSER" "~"
if joinKuerzel = BMUPBM
searchResult := searchResult "LEFT JOIN PRODUKTION.PBM_BEFEHL ON PBM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PCKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PCK_ERSTELLUSER" "~"
if joinKuerzel = BMUPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PFTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PFT_ERSTELLUSER" "~"
if joinKuerzel = BMUPFT
searchResult := searchResult "LEFT JOIN HRS.PFT_PFAENDUNG_TILGUNG ON PFT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIR_ERSTELLUSER" "~"
if joinKuerzel = BMUPIR
searchResult := searchResult "LEFT JOIN WWS.PIR_PICRABATTDETAIL ON PIR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKR_ERSTELLUSER" "~"
if joinKuerzel = BMUPKR
searchResult := searchResult "LEFT JOIN BIL.PKR_PERSKOSTEN_RST ON PKR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKX_ERSTELLUSER" "~"
if joinKuerzel = BMUPKX
searchResult := searchResult "LEFT JOIN FIBU.PKX_BUDGETPERSKOSTPARAMS ON PKX_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKZ_ERSTELLUSER" "~"
if joinKuerzel = BMUPKZ
searchResult := searchResult "LEFT JOIN BUERO.PKZ_PERSONKONTAKTZU ON PKZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PPABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PPA_ERSTELLUSER" "~"
if joinKuerzel = BMUPPA
searchResult := searchResult "LEFT JOIN PRODUKTION.PPA_PSPPARAM ON PPA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PPFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PPF_ERSTELLUSER" "~"
if joinKuerzel = BMUPPF
searchResult := searchResult "LEFT JOIN BUERO.PPF_PERSONENPRUEFUNG ON PPF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PPPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PPP_ERSTELLUSER" "~"
if joinKuerzel = BMUPPP
searchResult := searchResult "LEFT JOIN PRODUKTION.PPP_KAPA_POOL ON PPP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PRSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PRS_ERSTELLUSER" "~"
if joinKuerzel = BMUPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PRTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PRT_ERSTELLUSER" "~"
if joinKuerzel = BMUPRT
searchResult := searchResult "LEFT JOIN BUERO.PRT_PROJEKTTAETIGKEIT ON PRT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PSPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PSP_ERSTELLUSER" "~"
if joinKuerzel = BMUPSP
searchResult := searchResult "LEFT JOIN PRODUKTION.PSP_STAPEL_SCHRITT_PARAM ON PSP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PSRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PSR_ERSTELLUSER" "~"
if joinKuerzel = BMUPSR
searchResult := searchResult "LEFT JOIN HRS.PSR_PROGRAMMSPERRE ON PSR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PSZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PSZ_ERSTELLUSER" "~"
if joinKuerzel = BMUPSZ
searchResult := searchResult "LEFT JOIN BUERO.PSZ_PERSONENKZ ON PSZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PTSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PTS_ERSTELLUSER" "~"
if joinKuerzel = BMUPTS
searchResult := searchResult "LEFT JOIN WWS.PTS_PREISTABPOSSTAFFEL ON PTS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PTXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PTX_ERSTELLUSER" "~"
if joinKuerzel = BMUPTX
searchResult := searchResult "LEFT JOIN BMD.PTX_PERFORMANCETESTTAB ON PTX_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = QLFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = QLF_ERSTELLUSER" "~"
if joinKuerzel = BMUQLF
searchResult := searchResult "LEFT JOIN BMD.QLF_LINKFIELD ON QLF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RDDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RDD_ERSTELLUSER" "~"
if joinKuerzel = BMURDD
searchResult := searchResult "LEFT JOIN BIL.RDD_RSTDRUCKDETAIL ON RDD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = REABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = REA_ERSTELLUSER" "~"
if joinKuerzel = BMUREA
searchResult := searchResult "LEFT JOIN HRS.REA_REISEAUFTEILUNG ON REA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RGTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RGT_ERSTELLUSER" "~"
if joinKuerzel = BMURGT
searchResult := searchResult "LEFT JOIN BMD.RGT_TABREPLGTABELLEZU ON RGT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RKSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RKS_ERSTELLUSER" "~"
if joinKuerzel = BMURKS
searchResult := searchResult "LEFT JOIN BMD.RKS_REPLIKATIONSSTANDORT ON RKS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RSDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RSD_ERSTELLUSER" "~"
if joinKuerzel = BMURSD
searchResult := searchResult "LEFT JOIN WWS.RSD_REGKASPROTOKOLL ON RSD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RZUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RZU_ERSTELLUSER" "~"
if joinKuerzel = BMURZU
searchResult := searchResult "LEFT JOIN BUERO.RZU_RESSOURCENPAKETZU ON RZU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RZWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RZW_ERSTELLUSER" "~"
if joinKuerzel = BMURZW
searchResult := searchResult "LEFT JOIN HRS.RZW_REISEZWECK ON RZW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCI_ERSTELLUSER" "~"
if joinKuerzel = BMUSCI
searchResult := searchResult "LEFT JOIN PRODUKTION.SCI_SCINSTANZDET ON SCI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCV_ERSTELLUSER" "~"
if joinKuerzel = BMUSCV
searchResult := searchResult "LEFT JOIN BMDENTW.SCV_SCRREVISION ON SCV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCYBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCY_ERSTELLUSER" "~"
if joinKuerzel = BMUSCY
searchResult := searchResult "LEFT JOIN BMDENTW.SCY_SCRDEPENDENCY ON SCY_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SFRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SFR_ERSTELLUSER" "~"
if joinKuerzel = BMUSFR
searchResult := searchResult "LEFT JOIN BUERO.SFR_SKILLFINDERREGEL ON SFR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SFTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SFT_ERSTELLUSER" "~"
if joinKuerzel = BMUSFT
searchResult := searchResult "LEFT JOIN BUERO.SFT_SKILLFINDER_TMP ON SFT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SLBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SLB_ERSTELLUSER" "~"
if joinKuerzel = BMUSLB
searchResult := searchResult "LEFT JOIN BIL.SLB_SALDENBESTAETIGUNG ON SLB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SLZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SLZ_ERSTELLUSER" "~"
if joinKuerzel = BMUSLZ
searchResult := searchResult "LEFT JOIN BIL.SLZ_STEUERLOHNZETTEL ON SLZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPC_ERSTELLUSER" "~"
if joinKuerzel = BMUSPC
searchResult := searchResult "LEFT JOIN BMD.SPC_SPELLCHECKERDICT ON SPC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPG_ERSTELLUSER" "~"
if joinKuerzel = BMUSPG
searchResult := searchResult "LEFT JOIN BMDENTW.SPG_PGMS ON SPG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPN_ERSTELLUSER" "~"
if joinKuerzel = BMUSPN
searchResult := searchResult "LEFT JOIN FIBU.SPN_SPENDE ON SPN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SRWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SRW_ERSTELLUSER" "~"
if joinKuerzel = BMUSRW
searchResult := searchResult "LEFT JOIN WWS.SRW_STEUERARTWERT ON SRW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SSUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SSU_ERSTELLUSER" "~"
if joinKuerzel = BMUSSU
searchResult := searchResult "LEFT JOIN BIL.SSU_STEUERSACHKONTOZU ON SSU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STH_ERSTELLUSER" "~"
if joinKuerzel = BMUSTH
searchResult := searchResult "LEFT JOIN PRODUKTION.STH_STAKE ON STH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STK_ERSTELLUSER" "~"
if joinKuerzel = BMUSTK
searchResult := searchResult "LEFT JOIN ZLE.STK_STAFFELKOPF ON STK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STO_ERSTELLUSER" "~"
if joinKuerzel = BMUSTO
searchResult := searchResult "LEFT JOIN PRODUKTION.STO_STAKE_TYPORDER ON STO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STP_ERSTELLUSER" "~"
if joinKuerzel = BMUSTP
searchResult := searchResult "LEFT JOIN ZLE.STP_STAFFELPOS ON STP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SZUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SZU_ERSTELLUSER" "~"
if joinKuerzel = BMUSZU
searchResult := searchResult "LEFT JOIN HRS.SZU_SCHNITTLAZU ON SZU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TAEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TAE_ERSTELLUSER" "~"
if joinKuerzel = BMUTAE
searchResult := searchResult "LEFT JOIN WWS.TAE_AUFTSCHNELLERF_TMP ON TAE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TAGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TAG_ERSTELLUSER" "~"
if joinKuerzel = BMUTAG
searchResult := searchResult "LEFT JOIN ZLE.TAG_TAGESABSCHLUSS ON TAG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TAVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TAV_ERSTELLUSER" "~"
if joinKuerzel = BMUTAV
searchResult := searchResult "LEFT JOIN BUERO.TAV_VERTEILUNG ON TAV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TAZ_ERSTELLUSER" "~"
if joinKuerzel = BMUTAZ
searchResult := searchResult "LEFT JOIN BMDENTW.TAZ_TICKETAUFGABEZU ON TAZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TBTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TBT_ERSTELLUSER" "~"
if joinKuerzel = BMUTBT
searchResult := searchResult "LEFT JOIN BMDENTW.TBT_TICKETKATEGORIEZU ON TBT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = THVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = THV_ERSTELLUSER" "~"
if joinKuerzel = BMUTHV
searchResult := searchResult "LEFT JOIN HRS.THV_TARIFGR_HAEUFIGVERW ON THV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TITBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TIT_ERSTELLUSER" "~"
if joinKuerzel = BMUTIT
searchResult := searchResult "LEFT JOIN BUERO.TIT_TITEL ON TIT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TKTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TKT_ERSTELLUSER" "~"
if joinKuerzel = BMUTKT
searchResult := searchResult "LEFT JOIN BMDENTW.TKT_TICKETBEREICHZU ON TKT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TLZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TLZ_ERSTELLUSER" "~"
if joinKuerzel = BMUTLZ
searchResult := searchResult "LEFT JOIN HRS.TLZ_ELDADETAIL_ELZ_TMP ON TLZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UMABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UMA_ERSTELLUSER" "~"
if joinKuerzel = BMUUMA
searchResult := searchResult "LEFT JOIN BMD.UMA_USERMATCHCODE ON UMA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UPNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UPN_ERSTELLUSER" "~"
if joinKuerzel = BMUUPN
searchResult := searchResult "LEFT JOIN BMDDEFAULT.UPN_UPH_KUNDE_NLS ON UPN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VAK_ERSTELLUSER" "~"
if joinKuerzel = BMUVAK
searchResult := searchResult "LEFT JOIN BUERO.VAK_VERANSTALTUNGSKOPF ON VAK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VERBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VER_ERSTELLUSER" "~"
if joinKuerzel = BMUVER
searchResult := searchResult "LEFT JOIN BUERO.VER_VERSANDART ON VER_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VLTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VLT_ERSTELLUSER" "~"
if joinKuerzel = BMUVLT
searchResult := searchResult "LEFT JOIN WWS.VLT_VA_LIEFERSCHEIN ON VLT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VMZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VMZ_ERSTELLUSER" "~"
if joinKuerzel = BMUVMZ
searchResult := searchResult "LEFT JOIN WWS.VMZ_VERSMGR_LFS_ZUORD ON VMZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VOBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VOB_ERSTELLUSER" "~"
if joinKuerzel = BMUVOB
searchResult := searchResult "LEFT JOIN PRODUKTION.VOB_VORGANGSBENACHR ON VOB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VPBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VPB_ERSTELLUSER" "~"
if joinKuerzel = BMUVPB
searchResult := searchResult "LEFT JOIN WWS.VPB_VPERMITTLUNG_ERKZUORD ON VPB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VPWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VPW_ERSTELLUSER" "~"
if joinKuerzel = BMUVPW
searchResult := searchResult "LEFT JOIN BUERO.VPW_VPIWERTE ON VPW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VRIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VRI_ERSTELLUSER" "~"
if joinKuerzel = BMUVRI
searchResult := searchResult "LEFT JOIN BUERO.VRI_VERSIONINFO ON VRI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VZWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VZW_ERSTELLUSER" "~"
if joinKuerzel = BMUVZW
searchResult := searchResult "LEFT JOIN BUERO.VZW_VERKEHRSZWEIG ON VZW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WAABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAA_ERSTELLUSER" "~"
if joinKuerzel = BMUWAA
searchResult := searchResult "LEFT JOIN BIL.WAA_WIPAKTENART ON WAA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WAGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAG_ERSTELLUSER" "~"
if joinKuerzel = BMUWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WAOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAO_ERSTELLUSER" "~"
if joinKuerzel = BMUWAO
searchResult := searchResult "LEFT JOIN WWS.WAO_WARENEINGWBK ON WAO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WARBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAR_ERSTELLUSER" "~"
if joinKuerzel = BMUWAR
searchResult := searchResult "LEFT JOIN WWS.WAR_WARENEINGANGRABATTE ON WAR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WBGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WBG_ERSTELLUSER" "~"
if joinKuerzel = BMUWBG
searchResult := searchResult "LEFT JOIN WWS.WBG_WARENBESCHKOSTENGRP ON WBG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WCVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WCV_ERSTELLUSER" "~"
if joinKuerzel = BMUWCV
searchResult := searchResult "LEFT JOIN BMD.WCV_WAPARAMDEFVALUES ON WCV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WDRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WDR_ERSTELLUSER" "~"
if joinKuerzel = BMUWDR
searchResult := searchResult "LEFT JOIN WWS.WDR_WWSDATENREIHENFOLGE ON WDR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEJ_ERSTELLUSER" "~"
if joinKuerzel = BMUWEJ
searchResult := searchResult "LEFT JOIN WWS.WEJ_WARENEINSATZLAJ ON WEJ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEK_ERSTELLUSER" "~"
if joinKuerzel = BMUWEK
searchResult := searchResult "LEFT JOIN PRODUKTION.WEK_WERKZEUG ON WEK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WETBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WET_ERSTELLUSER" "~"
if joinKuerzel = BMUWET
searchResult := searchResult "LEFT JOIN WWS.WET_WARENEINSATZARTZU ON WET_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WFABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WFA_ERSTELLUSER" "~"
if joinKuerzel = BMUWFA
searchResult := searchResult "LEFT JOIN WEB.WFA_WEBFARBEN ON WFA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WFIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WFI_ERSTELLUSER" "~"
if joinKuerzel = BMUWFI
searchResult := searchResult "LEFT JOIN WEB.WFI_WEBFIRMA ON WFI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WGRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WGR_ERSTELLUSER" "~"
if joinKuerzel = BMUWGR
searchResult := searchResult "LEFT JOIN WWS.WGR_WWSBERICHTGRUPPIERUNG ON WGR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WIBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WIB_ERSTELLUSER" "~"
if joinKuerzel = BMUWIB
searchResult := searchResult "LEFT JOIN BIL.WIB_WIBERICHTSART ON WIB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WIFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WIF_ERSTELLUSER" "~"
if joinKuerzel = BMUWIF
searchResult := searchResult "LEFT JOIN BMDENTW.WIF_WII_DFU_ZU ON WIF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SAGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SAG_ERSTELLUSER" "~"
if joinKuerzel = BMUSAG
searchResult := searchResult "LEFT JOIN WWS.SAG_STATISTIKARTIKELGRP ON SAG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SBSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SBS_ERSTELLUSER" "~"
if joinKuerzel = BMUSBS
searchResult := searchResult "LEFT JOIN BUERO.SBS_STELLENBESCHREIBUNG ON SBS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCJ_ERSTELLUSER" "~"
if joinKuerzel = BMUSCJ
searchResult := searchResult "LEFT JOIN BMDENTW.SCJ_SCRPROJZU ON SCJ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SEQBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SEQ_ERSTELLUSER" "~"
if joinKuerzel = BMUSEQ
searchResult := searchResult "LEFT JOIN BMD.SEQ_SEQUENZ ON SEQ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SFEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SFE_ERSTELLUSER" "~"
if joinKuerzel = BMUSFE
searchResult := searchResult "LEFT JOIN BUERO.SFE_SKILLFINDERELEMENT ON SFE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SIDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SID_ERSTELLUSER" "~"
if joinKuerzel = BMUSID
searchResult := searchResult "LEFT JOIN HRS.SID_HRSSEQ ON SID_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SLPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SLP_ERSTELLUSER" "~"
if joinKuerzel = BMUSLP
searchResult := searchResult "LEFT JOIN WWS.SLP_STUECKLISTENPOSITION ON SLP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPE_ERSTELLUSER" "~"
if joinKuerzel = BMUSPE
searchResult := searchResult "LEFT JOIN HRS.SPE_SPESENARTEN ON SPE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPU_ERSTELLUSER" "~"
if joinKuerzel = BMUSPU
searchResult := searchResult "LEFT JOIN BMDENTW.SPU_SCRPGMZU ON SPU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SRPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SRP_ERSTELLUSER" "~"
if joinKuerzel = BMUSRP
searchResult := searchResult "LEFT JOIN WWS.SRP_STEUERTABPOS ON SRP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STC_ERSTELLUSER" "~"
if joinKuerzel = BMUSTC
searchResult := searchResult "LEFT JOIN FIBU.STC_STEUERCODE ON STC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SUDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SUD_ERSTELLUSER" "~"
if joinKuerzel = BMUSUD
searchResult := searchResult "LEFT JOIN HRS.SUD_SUM_KOE_DIFF ON SUD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SYKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SYK_ERSTELLUSER" "~"
if joinKuerzel = BMUSYK
searchResult := searchResult "LEFT JOIN BMD.SYK_BMDSYSTEMKOMPONENTE ON SYK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SYSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SYS_ERSTELLUSER" "~"
if joinKuerzel = BMUSYS
searchResult := searchResult "LEFT JOIN BMD.SYS_BMDSYSTEM ON SYS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TCKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TCK_ERSTELLUSER" "~"
if joinKuerzel = BMUTCK
searchResult := searchResult "LEFT JOIN BMD.TCK_TCKONFIGURATIONEN ON TCK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TPZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TPZ_ERSTELLUSER" "~"
if joinKuerzel = BMUTPZ
searchResult := searchResult "LEFT JOIN ZLE.TPZ_TAETIGKEITPAUSCHALEZU ON TPZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TSRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TSR_ERSTELLUSER" "~"
if joinKuerzel = BMUTSR
searchResult := searchResult "LEFT JOIN BUERO.TSR_SERIENTERMIN ON TSR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TWZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TWZ_ERSTELLUSER" "~"
if joinKuerzel = BMUTWZ
searchResult := searchResult "LEFT JOIN WWS.TWZ_WEINSKOPFZU_TMP ON TWZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UATBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UAT_ERSTELLUSER" "~"
if joinKuerzel = BMUUAT
searchResult := searchResult "LEFT JOIN BUERO.UAT_UMFRAGEANTWTEILNZU ON UAT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UMFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UMF_ERSTELLUSER" "~"
if joinKuerzel = BMUUMF
searchResult := searchResult "LEFT JOIN BUERO.UMF_UMFRAGE ON UMF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VFDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VFD_ERSTELLUSER" "~"
if joinKuerzel = BMUVFD
searchResult := searchResult "LEFT JOIN PRODUKTION.VFD_VERFAHRENSANWEISUNG_POS ON VFD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VJLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VJL_ERSTELLUSER" "~"
if joinKuerzel = BMUVJL
searchResult := searchResult "LEFT JOIN WWS.VJL_VA_LAGERJOURNAL ON VJL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VPEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VPE_ERSTELLUSER" "~"
if joinKuerzel = BMUVPE
searchResult := searchResult "LEFT JOIN WWS.VPE_VPI_ERHOEHUNG ON VPE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VPUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VPU_ERSTELLUSER" "~"
if joinKuerzel = BMUVPU
searchResult := searchResult "LEFT JOIN BMD.VPU_VPFADUSER ON VPU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VRSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VRS_ERSTELLUSER" "~"
if joinKuerzel = BMUVRS
searchResult := searchResult "LEFT JOIN WWS.VRS_VERTABRRECHPOSSTORNO ON VRS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WATBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAT_ERSTELLUSER" "~"
if joinKuerzel = BMUWAT
searchResult := searchResult "LEFT JOIN BUERO.WAT_WEBANMELDUNG ON WAT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VSTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VST_ERSTELLUSER" "~"
if joinKuerzel = BMUVST
searchResult := searchResult "LEFT JOIN WWS.VST_VERKAUFSSTATISTIK ON VST_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VSZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VSZ_ERSTELLUSER" "~"
if joinKuerzel = BMUVSZ
searchResult := searchResult "LEFT JOIN BUERO.VSZ_AVSTAMMVORAUSSETZZU ON VSZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VWVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VWV_ERSTELLUSER" "~"
if joinKuerzel = BMUVWV
searchResult := searchResult "LEFT JOIN FIBU.VWV_VSTERST_WERTEVORRAT ON VWV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WABBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAB_ERSTELLUSER" "~"
if joinKuerzel = BMUWAB
searchResult := searchResult "LEFT JOIN WWS.WAB_WARENEINGANGBUCH ON WAB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WAEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAE_ERSTELLUSER" "~"
if joinKuerzel = BMUWAE
searchResult := searchResult "LEFT JOIN BMD.WAE_WAEHRUNG ON WAE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAK_ERSTELLUSER" "~"
if joinKuerzel = BMUWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WBXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WBX_ERSTELLUSER" "~"
if joinKuerzel = BMUWBX
searchResult := searchResult "LEFT JOIN WEB.WBX_WEBTEXTE ON WBX_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WCDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WCD_ERSTELLUSER" "~"
if joinKuerzel = BMUWCD
searchResult := searchResult "LEFT JOIN BMD.WCD_WAPARAMDEF ON WCD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEG_ERSTELLUSER" "~"
if joinKuerzel = BMUWEG
searchResult := searchResult "LEFT JOIN WWS.WEG_WWSERKONTROLLEGRP ON WEG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEZ_ERSTELLUSER" "~"
if joinKuerzel = BMUWEZ
searchResult := searchResult "LEFT JOIN PRODUKTION.WEZ_WERKZEUGBAZZU ON WEZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WFNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WFN_ERSTELLUSER" "~"
if joinKuerzel = BMUWFN
searchResult := searchResult "LEFT JOIN BUERO.WFN_WORKFLOWACTION ON WFN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WFZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WFZ_ERSTELLUSER" "~"
if joinKuerzel = BMUWFZ
searchResult := searchResult "LEFT JOIN BUERO.WFZ_WORKFLOWZU ON WFZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WGDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WGD_ERSTELLUSER" "~"
if joinKuerzel = BMUWGD
searchResult := searchResult "LEFT JOIN FIBU.WGD_WGBDETAIL ON WGD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WKABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WKA_ERSTELLUSER" "~"
if joinKuerzel = BMUWKA
searchResult := searchResult "LEFT JOIN WWS.WKA_WARENAUSGKOSTENARTEN ON WKA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WKLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WKL_ERSTELLUSER" "~"
if joinKuerzel = BMUWKL
searchResult := searchResult "LEFT JOIN WWS.WKL_WARENAUSGKOSTENLIEF ON WKL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WKRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WKR_ERSTELLUSER" "~"
if joinKuerzel = BMUWKR
searchResult := searchResult "LEFT JOIN WWS.WKR_WARENAUSGKOSTENRECH ON WKR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WQUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WQU_ERSTELLUSER" "~"
if joinKuerzel = BMUWQU
searchResult := searchResult "LEFT JOIN BMD.WQU_WQI_BMU_ZU ON WQU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZBABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZBA_ERSTELLUSER" "~"
if joinKuerzel = BMUZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZBUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZBU_ERSTELLUSER" "~"
if joinKuerzel = BMUZBU
searchResult := searchResult "LEFT JOIN BUERO.ZBU_ZEITBUCHUNG ON ZBU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZEDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZED_ERSTELLUSER" "~"
if joinKuerzel = BMUZED
searchResult := searchResult "LEFT JOIN BMD.ZED_ZELLENDEF ON ZED_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZEKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZEK_ERSTELLUSER" "~"
if joinKuerzel = BMUZEK
searchResult := searchResult "LEFT JOIN WWS.ZEK_ZEITEINHEITENKOPF ON ZEK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZFIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZFI_ERSTELLUSER" "~"
if joinKuerzel = BMUZFI
searchResult := searchResult "LEFT JOIN BUERO.ZFI_ZEITFEHLERIGNORE ON ZFI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZKKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZKK_ERSTELLUSER" "~"
if joinKuerzel = BMUZKK
searchResult := searchResult "LEFT JOIN KORE.ZKK_KSTKONTOZU ON ZKK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZMBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZMB_ERSTELLUSER" "~"
if joinKuerzel = BMUZMB
searchResult := searchResult "LEFT JOIN BUERO.ZMB_ZEITMINDESTBESETZUNG ON ZMB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZNGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZNG_ERSTELLUSER" "~"
if joinKuerzel = BMUZNG
searchResult := searchResult "LEFT JOIN BUERO.ZNG_ZEITNACHRICHTGESENDET ON ZNG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZSWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZSW_ERSTELLUSER" "~"
if joinKuerzel = BMUZSW
searchResult := searchResult "LEFT JOIN BUERO.ZSW_ZEITSUPERWE ON ZSW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VFABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VFA_ERSTELLUSER" "~"
if joinKuerzel = BMUVFA
searchResult := searchResult "LEFT JOIN PRODUKTION.VFA_VERFAHRENSANWEISUNG_KOPF ON VFA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VOPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VOP_ERSTELLUSER" "~"
if joinKuerzel = BMUVOP
searchResult := searchResult "LEFT JOIN PRODUKTION.VOP_VOB_PERSON_ZU ON VOP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VVVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VVV_ERSTELLUSER" "~"
if joinKuerzel = BMUVVV
searchResult := searchResult "LEFT JOIN BIL.VVV_STEUERVERLUSTVV ON VVV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VWZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VWZ_ERSTELLUSER" "~"
if joinKuerzel = BMUVWZ
searchResult := searchResult "LEFT JOIN BUERO.VWZ_VORGANGWORKFLOWZU ON VWZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VZZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VZZ_ERSTELLUSER" "~"
if joinKuerzel = BMUVZZ
searchResult := searchResult "LEFT JOIN HRS.VZZ_VORZEITZU ON VZZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAZ_ERSTELLUSER" "~"
if joinKuerzel = BMUWAZ
searchResult := searchResult "LEFT JOIN WWS.WAZ_WARENZUORDNUNG ON WAZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WBPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WBP_ERSTELLUSER" "~"
if joinKuerzel = BMUWBP
searchResult := searchResult "LEFT JOIN WEB.WBP_WEBWARENKORBPOS ON WBP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WCPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WCP_ERSTELLUSER" "~"
if joinKuerzel = BMUWCP
searchResult := searchResult "LEFT JOIN BMD.WCP_WAAUSDRUCKPARAMETER ON WCP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WDABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WDA_ERSTELLUSER" "~"
if joinKuerzel = BMUWDA
searchResult := searchResult "LEFT JOIN BUERO.WDA_WFDAGRAMMZU ON WDA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WDIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WDI_ERSTELLUSER" "~"
if joinKuerzel = BMUWDI
searchResult := searchResult "LEFT JOIN BUERO.WDI_WFDIAGRAMMINSTANZZU ON WDI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEE_ERSTELLUSER" "~"
if joinKuerzel = BMUWEE
searchResult := searchResult "LEFT JOIN WWS.WEE_ERKONT_BESTELL_ZUORD ON WEE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WELBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEL_ERSTELLUSER" "~"
if joinKuerzel = BMUWEL
searchResult := searchResult "LEFT JOIN WWS.WEL_WARENEINSATZLAGERZU ON WEL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WENBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEN_ERSTELLUSER" "~"
if joinKuerzel = BMUWEN
searchResult := searchResult "LEFT JOIN WWS.WEN_WARENEINSATZRNP ON WEN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WESBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WES_ERSTELLUSER" "~"
if joinKuerzel = BMUWES
searchResult := searchResult "LEFT JOIN WWS.WES_WWSERKONTROLLEWBK ON WES_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WFKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WFK_ERSTELLUSER" "~"
if joinKuerzel = BMUWFK
searchResult := searchResult "LEFT JOIN WWS.WFK_FOLGEARTIKELKAT ON WFK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WGIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WGI_ERSTELLUSER" "~"
if joinKuerzel = BMUWGI
searchResult := searchResult "LEFT JOIN WWS.WGI_WWSBERICHTGRUPPINFO ON WGI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WQIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WQI_ERSTELLUSER" "~"
if joinKuerzel = BMUWQI
searchResult := searchResult "LEFT JOIN BMDDEFAULT.WQI_WEITERE_INFO_ITEM ON WQI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WSKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WSK_ERSTELLUSER" "~"
if joinKuerzel = BMUWSK
searchResult := searchResult "LEFT JOIN WWS.WSK_WWSKORESTATISTIK ON WSK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WTEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WTE_ERSTELLUSER" "~"
if joinKuerzel = BMUWTE
searchResult := searchResult "LEFT JOIN BUERO.WTE_WEBTEILNEHMER ON WTE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WZZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WZZ_ERSTELLUSER" "~"
if joinKuerzel = BMUWZZ
searchResult := searchResult "LEFT JOIN WWS.WZZ_WARENEINSATZERZZU ON WZZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = XCNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = XCN_ERSTELLUSER" "~"
if joinKuerzel = BMUXCN
searchResult := searchResult "LEFT JOIN BIL.XCN_EXTCHECKLIST_NOTES ON XCN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = YBUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = YBU_ERSTELLUSER" "~"
if joinKuerzel = BMUYBU
searchResult := searchResult "LEFT JOIN FIBU.YBU_AUTOBUCHUNG ON YBU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZAMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZAM_ERSTELLUSER" "~"
if joinKuerzel = BMUZAM
searchResult := searchResult "LEFT JOIN DATANORM.ZAM_ZU_ABSCHLAG ON ZAM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZBFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZBF_ERSTELLUSER" "~"
if joinKuerzel = BMUZBF
searchResult := searchResult "LEFT JOIN BUERO.ZBF_ZEITBAFUNKZUORD ON ZBF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZBGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZBG_ERSTELLUSER" "~"
if joinKuerzel = BMUZBG
searchResult := searchResult "LEFT JOIN BUERO.ZBG_ZEITBABERECHTGRUPPE ON ZBG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZCLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZCL_ERSTELLUSER" "~"
if joinKuerzel = BMUZCL
searchResult := searchResult "LEFT JOIN BUERO.ZCL_ZEITCHANGELOG ON ZCL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SYDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SYD_ERSTELLUSER" "~"
if joinKuerzel = BMUSYD
searchResult := searchResult "LEFT JOIN BMD.SYD_SYSTEMDRUCKER ON SYD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SZZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SZZ_ERSTELLUSER" "~"
if joinKuerzel = BMUSZZ
searchResult := searchResult "LEFT JOIN PRODUKTION.SZZ_STAKE_ZU ON SZZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TAPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TAP_ERSTELLUSER" "~"
if joinKuerzel = BMUTAP
searchResult := searchResult "LEFT JOIN ZLE.TAP_TAGESABPROTOKOLL ON TAP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TARBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TAR_ERSTELLUSER" "~"
if joinKuerzel = BMUTAR
searchResult := searchResult "LEFT JOIN BUERO.TAR_TERMINARTIKELZU ON TAR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TAS_ERSTELLUSER" "~"
if joinKuerzel = BMUTAS
searchResult := searchResult "LEFT JOIN BMD.TAS_ACTIONSTACK_TMP ON TAS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TBNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TBN_ERSTELLUSER" "~"
if joinKuerzel = BMUTBN
searchResult := searchResult "LEFT JOIN BUERO.TBN_TERMINBENACHRICHTIGUNG ON TBN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TBRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TBR_ERSTELLUSER" "~"
if joinKuerzel = BMUTBR
searchResult := searchResult "LEFT JOIN HRS.TBR_BESCHAEFTIGTENGRUPPE ON TBR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TCABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TCA_ERSTELLUSER" "~"
if joinKuerzel = BMUTCA
searchResult := searchResult "LEFT JOIN BMD.TCA_TCACTIONLIST ON TCA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TFKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TFK_ERSTELLUSER" "~"
if joinKuerzel = BMUTFK
searchResult := searchResult "LEFT JOIN BMD.TFK_FELDBESCHKOPF_TMP ON TFK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TKZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TKZ_ERSTELLUSER" "~"
if joinKuerzel = BMUTKZ
searchResult := searchResult "LEFT JOIN BMDENTW.TKZ_TICKETKONTAKTZU ON TKZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TLEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TLE_ERSTELLUSER" "~"
if joinKuerzel = BMUTLE
searchResult := searchResult "LEFT JOIN PRODUKTION.TLE_BT_LOGISTIK_EMPFANGEN ON TLE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TLJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TLJ_ERSTELLUSER" "~"
if joinKuerzel = BMUTLJ
searchResult := searchResult "LEFT JOIN WWS.TLJ_LAGERJOURNAL_TMP ON TLJ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TLKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TLK_ERSTELLUSER" "~"
if joinKuerzel = BMUTLK
searchResult := searchResult "LEFT JOIN PRODUKTION.TLK_BT_LOGISTIK_KOPF ON TLK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TOZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TOZ_ERSTELLUSER" "~"
if joinKuerzel = BMUTOZ
searchResult := searchResult "LEFT JOIN WWS.TOZ_TOURZUORDNUNG ON TOZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TRJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TRJ_ERSTELLUSER" "~"
if joinKuerzel = BMUTRJ
searchResult := searchResult "LEFT JOIN WWS.TRJ_TRANSPORTEINH_JOURNAL ON TRJ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TRSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TRS_ERSTELLUSER" "~"
if joinKuerzel = BMUTRS
searchResult := searchResult "LEFT JOIN BUERO.TRS_TEILSCHLUSSRECHSCHEMA ON TRS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TRXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TRX_ERSTELLUSER" "~"
if joinKuerzel = BMUTRX
searchResult := searchResult "LEFT JOIN WWS.TRX_TEILSCHLUSSRECHSCHEMA ON TRX_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TSABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TSA_ERSTELLUSER" "~"
if joinKuerzel = BMUTSA
searchResult := searchResult "LEFT JOIN FIBU.TSA_TEILSCHLUSSRECHABLAUF ON TSA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TSKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TSK_ERSTELLUSER" "~"
if joinKuerzel = BMUTSK
searchResult := searchResult "LEFT JOIN WWS.TSK_TOUCHSCREENKOPF ON TSK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TWPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TWP_ERSTELLUSER" "~"
if joinKuerzel = BMUTWP
searchResult := searchResult "LEFT JOIN WWS.TWP_WARENEINGANGPOS_TMP ON TWP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UESBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UES_ERSTELLUSER" "~"
if joinKuerzel = BMUUES
searchResult := searchResult "LEFT JOIN BMD.UES_UEBERSETZUNG ON UES_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UEVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UEV_ERSTELLUSER" "~"
if joinKuerzel = BMUUEV
searchResult := searchResult "LEFT JOIN BIL.UEV_STEUERUEBERGVERLUSTE ON UEV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UVFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UVF_ERSTELLUSER" "~"
if joinKuerzel = BMUUVF
searchResult := searchResult "LEFT JOIN FIBU.UVF_UVAFELDZU ON UVF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UZLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UZL_ERSTELLUSER" "~"
if joinKuerzel = BMUUZL
searchResult := searchResult "LEFT JOIN BUERO.UZL_UNTERZIEL ON UZL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VCZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VCZ_ERSTELLUSER" "~"
if joinKuerzel = BMUVCZ
searchResult := searchResult "LEFT JOIN BIL.VCZ_VIEW_ANLAGEGUTSACHKONTOZU ON VCZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VJDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VJD_ERSTELLUSER" "~"
if joinKuerzel = BMUVJD
searchResult := searchResult "LEFT JOIN WWS.VJD_VERLEIHJOURNALDETAIL ON VJD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SAD_ERSTELLUSER" "~"
if joinKuerzel = BMUSAD
searchResult := searchResult "LEFT JOIN FIBU.SAD_SALDEN ON SAD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SAIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SAI_ERSTELLUSER" "~"
if joinKuerzel = BMUSAI
searchResult := searchResult "LEFT JOIN WWS.SAI_SAISON ON SAI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCC_ERSTELLUSER" "~"
if joinKuerzel = BMUSCC
searchResult := searchResult "LEFT JOIN BMDENTW.SCC_SCRREVISIONFILE ON SCC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCH_ERSTELLUSER" "~"
if joinKuerzel = BMUSCH
searchResult := searchResult "LEFT JOIN BMD.SCH_SCHEDULE ON SCH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCO_ERSTELLUSER" "~"
if joinKuerzel = BMUSCO
searchResult := searchResult "LEFT JOIN PRODUKTION.SCO_SCINSTANZ ON SCO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SFFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SFF_ERSTELLUSER" "~"
if joinKuerzel = BMUSFF
searchResult := searchResult "LEFT JOIN BUERO.SFF_SKILLFINDERFILTER ON SFF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SKZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SKZ_ERSTELLUSER" "~"
if joinKuerzel = BMUSKZ
searchResult := searchResult "LEFT JOIN BUERO.SKZ_STEUERKUNDENZU ON SKZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SLRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SLR_ERSTELLUSER" "~"
if joinKuerzel = BMUSLR
searchResult := searchResult "LEFT JOIN BUERO.SLR_SKILLREGELELEMENT ON SLR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPF_ERSTELLUSER" "~"
if joinKuerzel = BMUSPF
searchResult := searchResult "LEFT JOIN BMDENTW.SPF_SCRPROJFILEZU ON SPF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPL_ERSTELLUSER" "~"
if joinKuerzel = BMUSPL
searchResult := searchResult "LEFT JOIN BMD.SPL_STEMPEL ON SPL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPQBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPQ_ERSTELLUSER" "~"
if joinKuerzel = BMUSPQ
searchResult := searchResult "LEFT JOIN HRS.SPQ_SPESENARTEN_SEQ ON SPQ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPZ_ERSTELLUSER" "~"
if joinKuerzel = BMUSPZ
searchResult := searchResult "LEFT JOIN BMD.SPZ_SESSIONPAKETZU ON SPZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SRMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SRM_ERSTELLUSER" "~"
if joinKuerzel = BMUSRM
searchResult := searchResult "LEFT JOIN WWS.SRM_SAMMELRECHNUNGSMODELL ON SRM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STG_ERSTELLUSER" "~"
if joinKuerzel = BMUSTG
searchResult := searchResult "LEFT JOIN FIBU.STG_STEUERGRUPPE ON STG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STM_ERSTELLUSER" "~"
if joinKuerzel = BMUSTM
searchResult := searchResult "LEFT JOIN BUERO.STM_STELLENMERKMALE ON STM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STT_ERSTELLUSER" "~"
if joinKuerzel = BMUSTT
searchResult := searchResult "LEFT JOIN PRODUKTION.STT_STAKE_TYP ON STT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STW_ERSTELLUSER" "~"
if joinKuerzel = BMUSTW
searchResult := searchResult "LEFT JOIN BIL.STW_STEUERWERT ON STW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SUKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SUK_ERSTELLUSER" "~"
if joinKuerzel = BMUSUK
searchResult := searchResult "LEFT JOIN HRS.SUK_SUM_KOE ON SUK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SYNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SYN_ERSTELLUSER" "~"
if joinKuerzel = BMUSYN
searchResult := searchResult "LEFT JOIN BMD.SYN_SYNCDATEN ON SYN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = T18BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = T18_ERSTELLUSER" "~"
if joinKuerzel = BMUT18
searchResult := searchResult "LEFT JOIN HRS.T18_ELDADETAIL_E18_TMP ON T18_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TBUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TBU_ERSTELLUSER" "~"
if joinKuerzel = BMUTBU
searchResult := searchResult "LEFT JOIN FIBU.TBU_BUCHUNG_TMP ON TBU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TDKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TDK_ERSTELLUSER" "~"
if joinKuerzel = BMUTDK
searchResult := searchResult "LEFT JOIN WWS.TDK_WWSDATENKONTR_TMP ON TDK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TEDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TED_ERSTELLUSER" "~"
if joinKuerzel = BMUTED
searchResult := searchResult "LEFT JOIN BUERO.TED_TERMINDOKUMENTZU ON TED_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TEZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TEZ_ERSTELLUSER" "~"
if joinKuerzel = BMUTEZ
searchResult := searchResult "LEFT JOIN WWS.TEZ_ERFASSKOPFZU_TMP ON TEZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TLPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TLP_ERSTELLUSER" "~"
if joinKuerzel = BMUTLP
searchResult := searchResult "LEFT JOIN PRODUKTION.TLP_BT_LOGISTIK_POSITION ON TLP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TLUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TLU_ERSTELLUSER" "~"
if joinKuerzel = BMUTLU
searchResult := searchResult "LEFT JOIN PRODUKTION.TLU_BT_LOGISTIK_ZU ON TLU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TPEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TPE_ERSTELLUSER" "~"
if joinKuerzel = BMUTPE
searchResult := searchResult "LEFT JOIN BUERO.TPE_TEMPPERSON_TMP ON TPE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TTGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TTG_ERSTELLUSER" "~"
if joinKuerzel = BMUTTG
searchResult := searchResult "LEFT JOIN HRS.TTG_TARIFGR_ERGAENZUNG_ZU ON TTG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ULKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ULK_ERSTELLUSER" "~"
if joinKuerzel = BMUULK
searchResult := searchResult "LEFT JOIN BMD.ULK_USERLINK ON ULK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VAS_ERSTELLUSER" "~"
if joinKuerzel = BMUVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VATBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VAT_ERSTELLUSER" "~"
if joinKuerzel = BMUVAT
searchResult := searchResult "LEFT JOIN BUERO.VAT_TEILNAHME ON VAT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VDZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VDZ_ERSTELLUSER" "~"
if joinKuerzel = BMUVDZ
searchResult := searchResult "LEFT JOIN BUERO.VDZ_VSTAMMDOKZU ON VDZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VETBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VET_ERSTELLUSER" "~"
if joinKuerzel = BMUVET
searchResult := searchResult "LEFT JOIN BUERO.VET_VERANSTALTUNGSTAG ON VET_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VEUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VEU_ERSTELLUSER" "~"
if joinKuerzel = BMUVEU
searchResult := searchResult "LEFT JOIN FIBU.VEU_VORERFASSPROZAUFTEIL ON VEU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VKPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VKP_ERSTELLUSER" "~"
if joinKuerzel = BMUVKP
searchResult := searchResult "LEFT JOIN FIBU.VKP_VSTKPROJEKT ON VKP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VSKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VSK_ERSTELLUSER" "~"
if joinKuerzel = BMUVSK
searchResult := searchResult "LEFT JOIN FIBU.VSK_VSTPAKET ON VSK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VWABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VWA_ERSTELLUSER" "~"
if joinKuerzel = BMUVWA
searchResult := searchResult "LEFT JOIN BUERO.VWA_VORSCHLAGSWERTEARCHIV ON VWA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VZEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VZE_ERSTELLUSER" "~"
if joinKuerzel = BMUVZE
searchResult := searchResult "LEFT JOIN BUERO.VZE_VORDIENSTZEITEN ON VZE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WAFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAF_ERSTELLUSER" "~"
if joinKuerzel = BMUWAF
searchResult := searchResult "LEFT JOIN WWS.WAF_WWSADRZUFIRMENBEZ ON WAF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WAPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAP_ERSTELLUSER" "~"
if joinKuerzel = BMUWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAS_ERSTELLUSER" "~"
if joinKuerzel = BMUWAS
searchResult := searchResult "LEFT JOIN WWS.WAS_WARENEINSATZARL ON WAS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WBUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WBU_ERSTELLUSER" "~"
if joinKuerzel = BMUWBU
searchResult := searchResult "LEFT JOIN WEB.WBU_WEBKUNDE ON WBU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WCWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WCW_ERSTELLUSER" "~"
if joinKuerzel = BMUWCW
searchResult := searchResult "LEFT JOIN BMD.WCW_WAAUSDRUCKPARAMWERT ON WCW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEB_ERSTELLUSER" "~"
if joinKuerzel = BMUWEB
searchResult := searchResult "LEFT JOIN WWS.WEB_WARENEINSATZBUCH ON WEB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WERBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WER_ERSTELLUSER" "~"
if joinKuerzel = BMUWER
searchResult := searchResult "LEFT JOIN WWS.WER_WWSERKONTROLLEKOPF ON WER_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEU_ERSTELLUSER" "~"
if joinKuerzel = BMUWEU
searchResult := searchResult "LEFT JOIN WWS.WEU_WWSERKONTROLLEZU ON WEU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WFEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WFE_ERSTELLUSER" "~"
if joinKuerzel = BMUWFE
searchResult := searchResult "LEFT JOIN WWS.WFE_FERTEP_AUFTDETAIL ON WFE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WFGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WFG_ERSTELLUSER" "~"
if joinKuerzel = BMUWFG
searchResult := searchResult "LEFT JOIN WWS.WFG_FOLGEARTIKELGRUPPE ON WFG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WMDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WMD_ERSTELLUSER" "~"
if joinKuerzel = BMUWMD
searchResult := searchResult "LEFT JOIN BUERO.WMD_WOCHENMODELL ON WMD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WOLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WOL_ERSTELLUSER" "~"
if joinKuerzel = BMUWOL
searchResult := searchResult "LEFT JOIN ZLE.WOL_WOCHENLEISTUNG ON WOL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WRKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WRK_ERSTELLUSER" "~"
if joinKuerzel = BMUWRK
searchResult := searchResult "LEFT JOIN WWS.WRK_WWSANKAUFSRNGKOPF ON WRK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WVDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WVD_ERSTELLUSER" "~"
if joinKuerzel = BMUWVD
searchResult := searchResult "LEFT JOIN WWS.WVD_WEINSATZVERLEIHDAT ON WVD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WIPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WIP_ERSTELLUSER" "~"
if joinKuerzel = BMUWIP
searchResult := searchResult "LEFT JOIN BIL.WIP_WIPRUEFUNG ON WIP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WIZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WIZ_ERSTELLUSER" "~"
if joinKuerzel = BMUWIZ
searchResult := searchResult "LEFT JOIN WWS.WIZ_WEINSATZKOPFZU ON WIZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WPTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WPT_ERSTELLUSER" "~"
if joinKuerzel = BMUWPT
searchResult := searchResult "LEFT JOIN WWS.WPT_WWSPERSON_TMP ON WPT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = YKEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = YKE_ERSTELLUSER" "~"
if joinKuerzel = BMUYKE
searchResult := searchResult "LEFT JOIN FIBU.YKE_AUTOBUCHKOREERF ON YKE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZAD_ERSTELLUSER" "~"
if joinKuerzel = BMUZAD
searchResult := searchResult "LEFT JOIN DATANORM.ZAD_ZU_ABSCHLAG_DETAIL ON ZAD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZBKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZBK_ERSTELLUSER" "~"
if joinKuerzel = BMUZBK
searchResult := searchResult "LEFT JOIN BUERO.ZBK_ZEITBETRIEBSKALENDER ON ZBK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZBMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZBM_ERSTELLUSER" "~"
if joinKuerzel = BMUZBM
searchResult := searchResult "LEFT JOIN FIBU.ZBM_ZINSBERECHNUNG_SUMME ON ZBM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZBRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZBR_ERSTELLUSER" "~"
if joinKuerzel = BMUZBR
searchResult := searchResult "LEFT JOIN FIBU.ZBR_ZINSBERECHNUNG ON ZBR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZBSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZBS_ERSTELLUSER" "~"
if joinKuerzel = BMUZBS
searchResult := searchResult "LEFT JOIN FIBU.ZBS_ZINSBERECHNUNG_STAMM ON ZBS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZITBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZIT_ERSTELLUSER" "~"
if joinKuerzel = BMUZIT
searchResult := searchResult "LEFT JOIN BUERO.ZIT_ZUSATZINFOTYP ON ZIT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZKRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZKR_ERSTELLUSER" "~"
if joinKuerzel = BMUZKR
searchResult := searchResult "LEFT JOIN FIBU.ZKR_ZAHLUNGSKREIS ON ZKR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZLABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZLA_ERSTELLUSER" "~"
if joinKuerzel = BMUZLA
searchResult := searchResult "LEFT JOIN ZLE.ZLA_ZEILAB ON ZLA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZLZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZLZ_ERSTELLUSER" "~"
if joinKuerzel = BMUZLZ
searchResult := searchResult "LEFT JOIN BUERO.ZLZ_ZIELZUORDNUNG ON ZLZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZMABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZMA_ERSTELLUSER" "~"
if joinKuerzel = BMUZMA
searchResult := searchResult "LEFT JOIN BUERO.ZMA_ZEITBABERECHTMAZUORD ON ZMA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZSTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZST_ERSTELLUSER" "~"
if joinKuerzel = BMUZST
searchResult := searchResult "LEFT JOIN BUERO.ZST_ZEITSALDO_TMP ON ZST_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZXABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZXA_ERSTELLUSER" "~"
if joinKuerzel = BMUZXA
searchResult := searchResult "LEFT JOIN BUERO.ZXA_ZEITAENDERUNGSANTRAG ON ZXA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VATKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = VAT_TN_PER_FIRMENNR AND KLI_KLID = VAT_TN_PER_PERSONENID" "~"
if joinKuerzel = KLIVAT
searchResult := searchResult "LEFT JOIN BUERO.VAT_TEILNAHME ON VAT_TN_PER_FIRMENNR = KLI_FIRMENNR AND VAT_TN_PER_PERSONENID = KLI_KLID" "~"
if joinKuerzel = BOIIFA
searchResult := searchResult "LEFT JOIN WWSINDIV.IFA_FAKTURA ON IFA_FIRMENNR = BOI_FIRMENNR AND IFA_FA_RECHNR = BOI_FA_RECHNR" "~"
if joinKuerzel = IFABOI
searchResult := searchResult "LEFT JOIN WWSINDIV.BOI_BONUSTAB_IFA ON BOI_FIRMENNR = IFA_FIRMENNR AND BOI_FA_RECHNR = IFA_FA_RECHNR" "~"
if joinKuerzel = ASPAAM
searchResult := searchResult "LEFT JOIN BIL.AAM_AUSDRUCKMASTER ON AAM_ANBUAUSDRMASTERNR = ASP_AAM_ANBUAUSDRMASTERNR" "~"
if joinKuerzel = AAMASP
searchResult := searchResult "LEFT JOIN BIL.ASP_SPALTENKOPF ON ASP_AAM_ANBUAUSDRMASTERNR = AAM_ANBUAUSDRMASTERNR" "~"
if joinKuerzel = AWDAAS
searchResult := searchResult "LEFT JOIN BUERO.AAS_ANTWORTAUSWAHLSET ON AAS_AASID = AWD_AAS_AASID" "~"
if joinKuerzel = AASAWD
searchResult := searchResult "LEFT JOIN BUERO.AWD_ANTWORTDETAIL ON AWD_AAS_AASID = AAS_AASID" "~"
if joinKuerzel = ABJABJ
searchResult := searchResult "LEFT JOIN BIL.ABJ_ANBUBEWRESJOURNAL ON ABJ_BRJOURNALID = ABJ_UMBUCH_BRJOURNALID AND ABJ_BEWRESID = ABJ_UMBUCH_BEWRESID" "~"
if joinKuerzel = ABJABJ
searchResult := searchResult "LEFT JOIN BIL.ABJ_ANBUBEWRESJOURNAL ON ABJ_UMBUCH_BRJOURNALID = ABJ_BRJOURNALID AND ABJ_UMBUCH_BEWRESID = ABJ_BEWRESID" "~"
if joinKuerzel = ABLABL
searchResult := searchResult "LEFT JOIN BUERO.ABL_AVBUDGETDETAIL ON ABL_ABLID = ABL_PARENT" "~"
if joinKuerzel = ABLABL
searchResult := searchResult "LEFT JOIN BUERO.ABL_AVBUDGETDETAIL ON ABL_PARENT = ABL_ABLID" "~"
if joinKuerzel = AJOABR
searchResult := searchResult "LEFT JOIN BIL.ABR_ANBUBEWRES ON ABR_BEWRESID = AJO_BEWRESID" "~"
if joinKuerzel = ABRAJO
searchResult := searchResult "LEFT JOIN BIL.AJO_ANBUJOURNAL ON AJO_BEWRESID = ABR_BEWRESID" "~"
if joinKuerzel = AVZABR
searchResult := searchResult "LEFT JOIN ABR_ANBUBEWRES.ABR_ANBUBEWRES ON ABR_BEWRESID = AVZ_BEWRESID" "~"
if joinKuerzel = ABRAVZ
searchResult := searchResult "LEFT JOIN FIBU.AVZ_ANBUFIBZU ON AVZ_BEWRESID = ABR_BEWRESID" "~"
if joinKuerzel = AGUABU
searchResult := searchResult "LEFT JOIN BIL.ABU_ANLAGENBUCHHALTUNG ON ABU_FIRMENNR = AGU_FIRMENNR AND ABU_ANBUNR = AGU_ANBUNR" "~"
if joinKuerzel = ABUAGU
searchResult := searchResult "LEFT JOIN BIL.AGU_ANLAGEGUT ON AGU_FIRMENNR = ABU_FIRMENNR AND AGU_ANBUNR = ABU_ANBUNR" "~"
if joinKuerzel = WBDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WBD_ERSTELLUSER" "~"
if joinKuerzel = BMUWBD
searchResult := searchResult "LEFT JOIN WWS.WBD_WARENBESCHKOSTENDET ON WBD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WBFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WBF_ERSTELLUSER" "~"
if joinKuerzel = BMUWBF
searchResult := searchResult "LEFT JOIN WWS.WBF_WWSBERICHTFILTER ON WBF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WBWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WBW_ERSTELLUSER" "~"
if joinKuerzel = BMUWBW
searchResult := searchResult "LEFT JOIN WWS.WBW_WWSBUDGETWERTE ON WBW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WFPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WFP_ERSTELLUSER" "~"
if joinKuerzel = BMUWFP
searchResult := searchResult "LEFT JOIN WEB.WFP_WEBFARBENPOS ON WFP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WICBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WIC_ERSTELLUSER" "~"
if joinKuerzel = BMUWIC
searchResult := searchResult "LEFT JOIN BIL.WIC_WIPCHECKLIST ON WIC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WIKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WIK_ERSTELLUSER" "~"
if joinKuerzel = BMUWIK
searchResult := searchResult "LEFT JOIN BIL.WIK_WIVARIABLENKATEGORIE ON WIK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WKZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WKZ_ERSTELLUSER" "~"
if joinKuerzel = BMUWKZ
searchResult := searchResult "LEFT JOIN WWS.WKZ_WFKKUNDELIEFERANTZU ON WKZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WPDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WPD_ERSTELLUSER" "~"
if joinKuerzel = BMUWPD
searchResult := searchResult "LEFT JOIN BIL.WPD_WPDIFFERENZEN ON WPD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WQCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WQC_ERSTELLUSER" "~"
if joinKuerzel = BMUWQC
searchResult := searchResult "LEFT JOIN BMD.WQC_WQI_CUSTOMER ON WQC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WQFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WQF_ERSTELLUSER" "~"
if joinKuerzel = BMUWQF
searchResult := searchResult "LEFT JOIN BMDDEFAULT.WQF_WQI_DFU_ZU ON WQF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WQLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WQL_ERSTELLUSER" "~"
if joinKuerzel = BMUWQL
searchResult := searchResult "LEFT JOIN BMDDEFAULT.WQL_WEITERE_INFO_LANG ON WQL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WSTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WST_ERSTELLUSER" "~"
if joinKuerzel = BMUWST
searchResult := searchResult "LEFT JOIN WWS.WST_WWSSTATISTIK_TMP ON WST_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WURBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WUR_ERSTELLUSER" "~"
if joinKuerzel = BMUWUR
searchResult := searchResult "LEFT JOIN WWS.WUR_WWSURL ON WUR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = XFBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = XFB_ERSTELLUSER" "~"
if joinKuerzel = BMUXFB
searchResult := searchResult "LEFT JOIN BMDENTW.XFB_GELOESCHTE_FB_DATEN ON XFB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZABBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZAB_ERSTELLUSER" "~"
if joinKuerzel = BMUZAB
searchResult := searchResult "LEFT JOIN BUERO.ZAB_AUSWEISBUCHARTZU ON ZAB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZAK_ERSTELLUSER" "~"
if joinKuerzel = BMUZAK
searchResult := searchResult "LEFT JOIN BUERO.ZAK_ZEITAUSWERTMAKRO ON ZAK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZATBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZAT_ERSTELLUSER" "~"
if joinKuerzel = BMUZAT
searchResult := searchResult "LEFT JOIN BUERO.ZAT_ZEITAUSWERTTYP ON ZAT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZOEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZOE_ERSTELLUSER" "~"
if joinKuerzel = BMUZOE
searchResult := searchResult "LEFT JOIN FIBU.ZOE_ZOLLBELEGERFASSUNG ON ZOE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZSUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZSU_ERSTELLUSER" "~"
if joinKuerzel = BMUZSU
searchResult := searchResult "LEFT JOIN FIBU.ZSU_ZAHLSUMME ON ZSU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZUUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZUU_ERSTELLUSER" "~"
if joinKuerzel = BMUZUU
searchResult := searchResult "LEFT JOIN BUERO.ZUU_ZEITBEZARTUMBUCHUSER ON ZUU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZVZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZVZ_ERSTELLUSER" "~"
if joinKuerzel = BMUZVZ
searchResult := searchResult "LEFT JOIN BUERO.ZVZ_TBVERWENDUNGSZWECK ON ZVZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZWRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZWR_ERSTELLUSER" "~"
if joinKuerzel = BMUZWR
searchResult := searchResult "LEFT JOIN BUERO.ZWR_ZAHLART_WAEHRUNGRUND ON ZWR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZXFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZXF_ERSTELLUSER" "~"
if joinKuerzel = BMUZXF
searchResult := searchResult "LEFT JOIN BUERO.ZXF_ZEITAENDERUNGFELD ON ZXF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZZTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZZT_ERSTELLUSER" "~"
if joinKuerzel = BMUZZT
searchResult := searchResult "LEFT JOIN BUERO.ZZT_ZEITZUORDNUNG ON ZZT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZZUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZZU_ERSTELLUSER" "~"
if joinKuerzel = BMUZZU
searchResult := searchResult "LEFT JOIN FIBU.ZZU_ZINSBERECHNUNG_ZLG_ZU ON ZZU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AAPAAK
searchResult := searchResult "LEFT JOIN WWS.AAK_ARAKOPF ON AAK_ARAKOPFID = AAP_ARAKOPFID" "~"
if joinKuerzel = AAKAAP
searchResult := searchResult "LEFT JOIN WWS.AAP_ARAPOSITION ON AAP_ARAKOPFID = AAK_ARAKOPFID" "~"
if joinKuerzel = ZTZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZTZ_ERSTELLUSER" "~"
if joinKuerzel = BMUZTZ
searchResult := searchResult "LEFT JOIN BMD.ZTZ_ZEITZONEN ON ZTZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZUBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZUB_ERSTELLUSER" "~"
if joinKuerzel = BMUZUB
searchResult := searchResult "LEFT JOIN BUERO.ZUB_ZEITUMBUCHBEWERTMDZU ON ZUB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZVDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZVD_ERSTELLUSER" "~"
if joinKuerzel = BMUZVD
searchResult := searchResult "LEFT JOIN BUERO.ZVD_ZEITPLANUNGVERSIONDAT ON ZVD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VSILAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_FIRMENNR = VSI_FIRMENNR AND LAJ_LAGERJOURNALLFDNR = VSI_LAGERJOURNALLFDNR" "~"
if joinKuerzel = LAJVSI
searchResult := searchResult "LEFT JOIN WWS.VSI_VA_SERIENNR_ABGANG ON VSI_FIRMENNR = LAJ_FIRMENNR AND VSI_LAGERJOURNALLFDNR = LAJ_LAGERJOURNALLFDNR" "~"
if joinKuerzel = PMDZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_FIRMENNR = PMD_FIRMENNR AND ZBA_ZBUCHARTNR = PMD_ZBUCHARTNR" "~"
if joinKuerzel = ZBAPMD
searchResult := searchResult "LEFT JOIN BUERO.PMD_PAUSENMODELL ON PMD_FIRMENNR = ZBA_FIRMENNR AND PMD_ZBUCHARTNR = ZBA_ZBUCHARTNR" "~"
if joinKuerzel = ADOAAA
searchResult := searchResult "LEFT JOIN BUERO.AAA_AUSDRUCKABLAUFART ON AAA_ABLAUFART = ADO_AUSDRUCKABLAUFART" "~"
if joinKuerzel = AAAADO
searchResult := searchResult "LEFT JOIN BUERO.ADO_AUSDRUCKMODELL ON ADO_AUSDRUCKABLAUFART = AAA_ABLAUFART" "~"
if joinKuerzel = AADAAM
searchResult := searchResult "LEFT JOIN BIL.AAM_AUSDRUCKMASTER ON AAM_ANBUAUSDRMASTERNR = AAD_AAM_ANBUAUSDRMASTERNR" "~"
if joinKuerzel = AAMAAD
searchResult := searchResult "LEFT JOIN BIL.AAD_AUSDRUCKDETAIL ON AAD_AAM_ANBUAUSDRMASTERNR = AAM_ANBUAUSDRMASTERNR" "~"
if joinKuerzel = SATAAS
searchResult := searchResult "LEFT JOIN BUERO.AAS_ANTWORTAUSWAHLSET ON AAS_AASID = SAT_AAS_AASID" "~"
if joinKuerzel = AASSAT
searchResult := searchResult "LEFT JOIN BUERO.SAT_SETANTWORT ON SAT_AAS_AASID = AAS_AASID" "~"
if joinKuerzel = ANBABJ
searchResult := searchResult "LEFT JOIN BIL.ABJ_ANBUBEWRESJOURNAL ON ABJ_BRJOURNALID = ANB_ABJ_BRJOURNALID AND ABJ_BEWRESID = ANB_ABJ_BEWRESID" "~"
if joinKuerzel = ABJANB
searchResult := searchResult "LEFT JOIN BIL.ANB_ANBUBUCHBELEGZU ON ANB_ABJ_BRJOURNALID = ABJ_BRJOURNALID AND ANB_ABJ_BEWRESID = ABJ_BEWRESID" "~"
if joinKuerzel = IABABU
searchResult := searchResult "LEFT JOIN BIL.ABU_ANLAGENBUCHHALTUNG ON ABU_FIRMENNR = IAB_FIRMENNR AND ABU_ANBUNR = IAB_ANBUNR" "~"
if joinKuerzel = ABUIAB
searchResult := searchResult "LEFT JOIN BIL.IAB_INVESTITIONSABZUG ON IAB_FIRMENNR = ABU_FIRMENNR AND IAB_ANBUNR = ABU_ANBUNR" "~"
if joinKuerzel = EFKADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = EFK_AUFTRGEB_ADRESSLFDNR" "~"
if joinKuerzel = ADREFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_AUFTRGEB_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = PTZADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = PTZ_ADRESSLFDNR" "~"
if joinKuerzel = ADRPTZ
searchResult := searchResult "LEFT JOIN WWS.PTZ_PREISTABZU ON PTZ_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = WADADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = WAD_ADRESSLFDNR" "~"
if joinKuerzel = ADRWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = WSSADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = WSS_ADRESSLFDNR" "~"
if joinKuerzel = ADRWSS
searchResult := searchResult "LEFT JOIN WWS.WSS_WWSSTATISTIK ON WSS_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = ZBRADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = ZBR_ADRESSLFDNR" "~"
if joinKuerzel = ADRZBR
searchResult := searchResult "LEFT JOIN FIBU.ZBR_ZINSBERECHNUNG ON ZBR_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = KAKADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = KAK_ZRECHEMPF_ADRESSLFDNR" "~"
if joinKuerzel = ADRKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_ZRECHEMPF_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = VSWADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = VSW_LIEF_ADRESSLFDNR" "~"
if joinKuerzel = ADRVSW
searchResult := searchResult "LEFT JOIN WWS.VSW_VERTRETERSTATISTIK ON VSW_LIEF_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = BPKADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = BPK_ZRECHEMPF_ADRESSLFDNR" "~"
if joinKuerzel = ADRBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_ZRECHEMPF_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = WSKADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = WSK_AUFTRGEB_ADRESSLFDNR" "~"
if joinKuerzel = ADRWSK
searchResult := searchResult "LEFT JOIN WWS.WSK_WWSKORESTATISTIK ON WSK_AUFTRGEB_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = AUGAFS
searchResult := searchResult "LEFT JOIN BUERO.AFS_AUFGABENSTATUS ON AFS_AUFGABENSTATUSNR = AUG_AUFGABENSTATUSNR" "~"
if joinKuerzel = AFSAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_AUFGABENSTATUSNR = AFS_AUFGABENSTATUSNR" "~"
if joinKuerzel = DVHAGR
searchResult := searchResult "LEFT JOIN BUERO.AGR_ABRECHNUNGSGRUPPE ON AGR_FIRMENNR = DVH_AGR_FIRMENNR AND AGR_ABRECHNUNGSGRPNR = DVH_ABRECHNUNGSGRPNR" "~"
if joinKuerzel = AGRDVH
searchResult := searchResult "LEFT JOIN BUERO.DVH_DIENSTVERHAELTNIS ON DVH_AGR_FIRMENNR = AGR_FIRMENNR AND DVH_ABRECHNUNGSGRPNR = AGR_ABRECHNUNGSGRPNR" "~"
if joinKuerzel = AGBAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_AG_ID = AGB_AG_ID" "~"
if joinKuerzel = AGSAGB
searchResult := searchResult "LEFT JOIN WWS.AGB_ARBEITSGANGBAZZU ON AGB_AG_ID = AGS_AG_ID" "~"
if joinKuerzel = AAZAID
searchResult := searchResult "LEFT JOIN AID_ARTIKELIDENTIFIKATION.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = AAZ_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDAAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.AAZ_AUFARBEITARTIKELZU ON AAZ_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = VPFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VPF_ERSTELLUSER" "~"
if joinKuerzel = BMUVPF
searchResult := searchResult "LEFT JOIN BMD.VPF_VPFAD ON VPF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VSRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VSR_ERSTELLUSER" "~"
if joinKuerzel = BMUVSR
searchResult := searchResult "LEFT JOIN FIBU.VSR_VSTERSTATTUNG ON VSR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VSWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VSW_ERSTELLUSER" "~"
if joinKuerzel = BMUVSW
searchResult := searchResult "LEFT JOIN WWS.VSW_VERTRETERSTATISTIK ON VSW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VTMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VTM_ERSTELLUSER" "~"
if joinKuerzel = BMUVTM
searchResult := searchResult "LEFT JOIN BUERO.VTM_VA_TAGESMODELL ON VTM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VTZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VTZ_ERSTELLUSER" "~"
if joinKuerzel = BMUVTZ
searchResult := searchResult "LEFT JOIN BUERO.VTZ_VERANSTALTUNGTERMINZU ON VTZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VVZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VVZ_ERSTELLUSER" "~"
if joinKuerzel = BMUVVZ
searchResult := searchResult "LEFT JOIN BUERO.VVZ_VTAGVORTRAGENDERZU ON VVZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VZABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VZA_ERSTELLUSER" "~"
if joinKuerzel = BMUVZA
searchResult := searchResult "LEFT JOIN HRS.VZA_VORZEITART ON VZA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WAMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAM_ERSTELLUSER" "~"
if joinKuerzel = BMUWAM
searchResult := searchResult "LEFT JOIN BUERO.WAM_WEBANMELDUNG ON WAM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WBKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WBK_ERSTELLUSER" "~"
if joinKuerzel = BMUWBK
searchResult := searchResult "LEFT JOIN WEB.WBK_WEBWARENKORBKOPF ON WBK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WBOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WBO_ERSTELLUSER" "~"
if joinKuerzel = BMUWBO
searchResult := searchResult "LEFT JOIN WEB.WBO_WEBOUTLOOKBAR ON WBO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WBSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WBS_ERSTELLUSER" "~"
if joinKuerzel = BMUWBS
searchResult := searchResult "LEFT JOIN WWS.WBS_WWSBERICHT_SNAPSHOT ON WBS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WCABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WCA_ERSTELLUSER" "~"
if joinKuerzel = BMUWCA
searchResult := searchResult "LEFT JOIN BMD.WCA_WAAUSDRUCKE ON WCA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WCFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WCF_ERSTELLUSER" "~"
if joinKuerzel = BMUWCF
searchResult := searchResult "LEFT JOIN BMD.WCF_WABAUMELEMAUSDRUCKZU ON WCF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WCZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WCZ_ERSTELLUSER" "~"
if joinKuerzel = BMUWCZ
searchResult := searchResult "LEFT JOIN BMD.WCZ_WAAUSDRUCK_PARAM_ZU ON WCZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEO_ERSTELLUSER" "~"
if joinKuerzel = BMUWEO
searchResult := searchResult "LEFT JOIN WWS.WEO_WARENEINSATZLPO ON WEO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEW_ERSTELLUSER" "~"
if joinKuerzel = BMUWEW
searchResult := searchResult "LEFT JOIN WWS.WEW_WARENEINSATZWGZU ON WEW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WGZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WGZ_ERSTELLUSER" "~"
if joinKuerzel = BMUWGZ
searchResult := searchResult "LEFT JOIN WWS.WGZ_WFK_WKG_ZU ON WGZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WIDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WID_ERSTELLUSER" "~"
if joinKuerzel = BMUWID
searchResult := searchResult "LEFT JOIN BIL.WID_WIPDOKUMENTE ON WID_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WIIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WII_ERSTELLUSER" "~"
if joinKuerzel = BMUWII
searchResult := searchResult "LEFT JOIN BMDENTW.WII_WEITERE_INFO_ITEM ON WII_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WKGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WKG_ERSTELLUSER" "~"
if joinKuerzel = BMUWKG
searchResult := searchResult "LEFT JOIN WWS.WKG_WWSKUNDENGRUPPE ON WKG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WMZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WMZ_ERSTELLUSER" "~"
if joinKuerzel = BMUWMZ
searchResult := searchResult "LEFT JOIN BUERO.WMZ_WFAMITARBZU ON WMZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WRPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WRP_ERSTELLUSER" "~"
if joinKuerzel = BMUWRP
searchResult := searchResult "LEFT JOIN WWS.WRP_WWSANKAUFSRNGPOS ON WRP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WTBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WTB_ERSTELLUSER" "~"
if joinKuerzel = BMUWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WWKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WWK_ERSTELLUSER" "~"
if joinKuerzel = BMUWWK
searchResult := searchResult "LEFT JOIN WWS.WWK_WWSBERICHTKOPF ON WWK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZBBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZBB_ERSTELLUSER" "~"
if joinKuerzel = BMUZBB
searchResult := searchResult "LEFT JOIN BUERO.ZBB_ZEITBETRKALBUCHART ON ZBB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZBOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZBO_ERSTELLUSER" "~"
if joinKuerzel = BMUZBO
searchResult := searchResult "LEFT JOIN BUERO.ZBO_ZEITBABERECHTZUORD ON ZBO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TMLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TML_ERSTELLUSER" "~"
if joinKuerzel = BMUTML
searchResult := searchResult "LEFT JOIN HRS.TML_TERMINISIERTELOHNART ON TML_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TMZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TMZ_ERSTELLUSER" "~"
if joinKuerzel = BMUTMZ
searchResult := searchResult "LEFT JOIN BUERO.TMZ_TAGESMDPRUEFZUORD ON TMZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TPJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TPJ_ERSTELLUSER" "~"
if joinKuerzel = BMUTPJ
searchResult := searchResult "LEFT JOIN BUERO.TPJ_TEMPPROJEKT_TMP ON TPJ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TREBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TRE_ERSTELLUSER" "~"
if joinKuerzel = BMUTRE
searchResult := searchResult "LEFT JOIN WWS.TRE_TRANSPORTEINHEIT ON TRE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TRTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TRT_ERSTELLUSER" "~"
if joinKuerzel = BMUTRT
searchResult := searchResult "LEFT JOIN WWS.TRT_TRANSPORTTYP ON TRT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TZMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TZM_ERSTELLUSER" "~"
if joinKuerzel = BMUTZM
searchResult := searchResult "LEFT JOIN BUERO.TZM_TERMINALZUORDMASTER ON TZM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UDBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UDB_ERSTELLUSER" "~"
if joinKuerzel = BMUUDB
searchResult := searchResult "LEFT JOIN BIL.UDB_ANGUNTERBILANZ ON UDB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UDIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UDI_ERSTELLUSER" "~"
if joinKuerzel = BMUUDI
searchResult := searchResult "LEFT JOIN KORE.UDI_UMLAGEISTWERTE ON UDI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UIDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UID_ERSTELLUSER" "~"
if joinKuerzel = BMUUID
searchResult := searchResult "LEFT JOIN BUERO.UID_UIDNUMMER ON UID_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UMIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UMI_ERSTELLUSER" "~"
if joinKuerzel = BMUUMI
searchResult := searchResult "LEFT JOIN KORE.UMI_UMLAGEISTMENGE ON UMI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UMSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UMS_ERSTELLUSER" "~"
if joinKuerzel = BMUUMS
searchResult := searchResult "LEFT JOIN WWS.UMS_UMSATZZEILE ON UMS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UPHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UPH_ERSTELLUSER" "~"
if joinKuerzel = BMUUPH
searchResult := searchResult "LEFT JOIN BMDENTW.UPH_UPDATEHINWEIS ON UPH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UTHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UTH_ERSTELLUSER" "~"
if joinKuerzel = BMUUTH
searchResult := searchResult "LEFT JOIN BMD.UTH_USERTREEHEAD ON UTH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UTUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UTU_ERSTELLUSER" "~"
if joinKuerzel = BMUUTU
searchResult := searchResult "LEFT JOIN BMD.UTU_UTABUEBERS ON UTU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VAHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VAH_ERSTELLUSER" "~"
if joinKuerzel = BMUVAH
searchResult := searchResult "LEFT JOIN BUERO.VAH_VERANSTABHAENGIG ON VAH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VAZ_ERSTELLUSER" "~"
if joinKuerzel = BMUVAZ
searchResult := searchResult "LEFT JOIN WWS.VAZ_VERTABRZAHLUNGEN ON VAZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VCPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VCP_ERSTELLUSER" "~"
if joinKuerzel = BMUVCP
searchResult := searchResult "LEFT JOIN PRODUKTION.VCP_VA_CHG_PLC_CHARGEN ON VCP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VFRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VFR_ERSTELLUSER" "~"
if joinKuerzel = BMUVFR
searchResult := searchResult "LEFT JOIN WWS.VFR_VERLEIHFREISTELLUNG ON VFR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VKDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VKD_ERSTELLUSER" "~"
if joinKuerzel = BMUVKD
searchResult := searchResult "LEFT JOIN BUERO.VKD_VKOPFDOKZU ON VKD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VMKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VMK_ERSTELLUSER" "~"
if joinKuerzel = BMUVMK
searchResult := searchResult "LEFT JOIN WWS.VMK_VERSMGR_KOPF ON VMK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VPDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VPD_ERSTELLUSER" "~"
if joinKuerzel = BMUVPD
searchResult := searchResult "LEFT JOIN WWS.VPD_VPI_ERHOEHUNGDETAIL ON VPD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VPGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VPG_ERSTELLUSER" "~"
if joinKuerzel = BMUVPG
searchResult := searchResult "LEFT JOIN WWS.VPG_VPERMITTLUNG_EFKZUORD ON VPG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VPKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VPK_ERSTELLUSER" "~"
if joinKuerzel = BMUVPK
searchResult := searchResult "LEFT JOIN WWS.VPK_VPERMITTLUNG_KUGZUORD ON VPK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VRGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VRG_ERSTELLUSER" "~"
if joinKuerzel = BMUVRG
searchResult := searchResult "LEFT JOIN BIL.VRG_VERLUSTANRECHNUNG ON VRG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VTXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VTX_ERSTELLUSER" "~"
if joinKuerzel = BMUVTX
searchResult := searchResult "LEFT JOIN BUERO.VTX_VA_UESLANGTEXT ON VTX_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WBABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WBA_ERSTELLUSER" "~"
if joinKuerzel = BMUWBA
searchResult := searchResult "LEFT JOIN WEB.WBA_WEBARTIKEL ON WBA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZIBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZIB_ERSTELLUSER" "~"
if joinKuerzel = BMUZIB
searchResult := searchResult "LEFT JOIN FIBU.ZIB_KONTENZINSENBERECHNG ON ZIB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZLBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZLB_ERSTELLUSER" "~"
if joinKuerzel = BMUZLB
searchResult := searchResult "LEFT JOIN BUERO.ZLB_ZBEZARTLOHNBLOCKSORT ON ZLB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZLSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZLS_ERSTELLUSER" "~"
if joinKuerzel = BMUZLS
searchResult := searchResult "LEFT JOIN ZLE.ZLS_ZEILAB_SICHERUNG ON ZLS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZPLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZPL_ERSTELLUSER" "~"
if joinKuerzel = BMUZPL
searchResult := searchResult "LEFT JOIN BUERO.ZPL_ZEITPLANUNG ON ZPL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZRBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZRB_ERSTELLUSER" "~"
if joinKuerzel = BMUZRB
searchResult := searchResult "LEFT JOIN BUERO.ZRB_ZEITRUNDUNGBEDINGUNG ON ZRB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZSABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZSA_ERSTELLUSER" "~"
if joinKuerzel = BMUZSA
searchResult := searchResult "LEFT JOIN BUERO.ZSA_ZEITSALDEN ON ZSA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZSDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZSD_ERSTELLUSER" "~"
if joinKuerzel = BMUZSD
searchResult := searchResult "LEFT JOIN ZLE.ZSD_ZEITSCHEIBEDETAIL ON ZSD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZUZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZUZ_ERSTELLUSER" "~"
if joinKuerzel = BMUZUZ
searchResult := searchResult "LEFT JOIN BUERO.ZUZ_ZEITUMBUCHZUORD ON ZUZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BPDERA
searchResult := searchResult "LEFT JOIN WWS.ERA_ERFASSRABATT ON ERA_FIRMENNR = BPD_FIRMENNR AND ERA_BPD_ERFASSUNGSKZ = BPD_ERFASSUNGSKZ AND ERA_BPD_BESTPRODERFASSNR = BPD_BESTPRODERFASSUNGSNR AND ERA_BPD_BESTPOSLFDNR = BPD_BESTPOSLFDNR AND ERA_BPD_ERFASSPOSDETLFDNR = BPD_ERFASSPOSDETAILLFDNR" "~"
if joinKuerzel = ERABPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_FIRMENNR = ERA_FIRMENNR AND BPD_ERFASSUNGSKZ = ERA_BPD_ERFASSUNGSKZ AND BPD_BESTPRODERFASSUNGSNR = ERA_BPD_BESTPRODERFASSNR AND BPD_BESTPOSLFDNR = ERA_BPD_BESTPOSLFDNR AND BPD_ERFASSPOSDETAILLFDNR = ERA_BPD_ERFASSPOSDETLFDNR" "~"
if joinKuerzel = AZRAGR
searchResult := searchResult "LEFT JOIN BUERO.AGR_ABRECHNUNGSGRUPPE ON AGR_FIRMENNR = AZR_FIRMENNR AND AGR_ABRECHNUNGSGRPNR = AZR_ABRECHNUNGSGRPNR" "~"
if joinKuerzel = AGRAZR
searchResult := searchResult "LEFT JOIN BUERO.AZR_ABRECHNUNGSZEITRAUM ON AZR_FIRMENNR = AGR_FIRMENNR AND AZR_ABRECHNUNGSGRPNR = AGR_ABRECHNUNGSGRPNR" "~"
if joinKuerzel = GUDGUT
searchResult := searchResult "LEFT JOIN WWSINDIV.GUT_GUTHABEN_IFA ON GUT_FIRMENNR = GUD_FIRMENNR AND GUT_LIE_KLID = GUD_LIE_KLID" "~"
if joinKuerzel = GUTGUD
searchResult := searchResult "LEFT JOIN WWSINDIV.GUD_GUTHABEN_IFA_DETAIL ON GUD_FIRMENNR = GUT_FIRMENNR AND GUD_LIE_KLID = GUT_LIE_KLID" "~"
if joinKuerzel = ABZABK
searchResult := searchResult "LEFT JOIN BUERO.ABK_ARCHIVBAUMKOPF ON ABK_FIRMENNR = ABZ_FIRMENNR AND ABK_ABKOPFLFDNR = ABZ_ABKOPFLFDNR" "~"
if joinKuerzel = ABKABZ
searchResult := searchResult "LEFT JOIN BUERO.ABZ_ARCHIVBAUMKOPFZU ON ABZ_FIRMENNR = ABK_FIRMENNR AND ABZ_ABKOPFLFDNR = ABK_ABKOPFLFDNR" "~"
if joinKuerzel = ABNABM
searchResult := searchResult "LEFT JOIN HRS.ABM_ABWESENHEIT ON ABM_ABMID = ABN_ABMID" "~"
if joinKuerzel = ABMABN
searchResult := searchResult "LEFT JOIN HRS.ABN_ABWESENHEIT_TAG ON ABN_ABMID = ABM_ABMID" "~"
if joinKuerzel = ABJABR
searchResult := searchResult "LEFT JOIN BIL.ABR_ANBUBEWRES ON ABR_BEWRESID = ABJ_BEWRESID" "~"
if joinKuerzel = ABRABJ
searchResult := searchResult "LEFT JOIN BIL.ABJ_ANBUBEWRESJOURNAL ON ABJ_BEWRESID = ABR_BEWRESID" "~"
if joinKuerzel = ANSABU
searchResult := searchResult "LEFT JOIN BIL.ABU_ANLAGENBUCHHALTUNG ON ABU_FIRMENNR = ANS_FIRMENNR AND ABU_ANBUNR = ANS_ANBUNR" "~"
if joinKuerzel = ABUANS
searchResult := searchResult "LEFT JOIN BIL.ANS_ANLAGENSCHEMA ON ANS_FIRMENNR = ABU_FIRMENNR AND ANS_ANBUNR = ABU_ANBUNR" "~"
if joinKuerzel = ADAADA
searchResult := searchResult "LEFT JOIN BUERO.ADA_ADRESSART ON ADA_ADRESSARTNR = ADA_FOLGE_ADRESSARTNR" "~"
if joinKuerzel = ADAADA
searchResult := searchResult "LEFT JOIN BUERO.ADA_ADRESSART ON ADA_FOLGE_ADRESSARTNR = ADA_ADRESSARTNR" "~"
if joinKuerzel = ALEADK
searchResult := searchResult "LEFT JOIN BUERO.ADK_ADRESSENLISTEKOPF ON ADK_FIRMENNR = ALE_FIRMENNR AND ADK_ADRLISTENNR = ALE_ADRLISTENNR" "~"
if joinKuerzel = ADKALE
searchResult := searchResult "LEFT JOIN BUERO.ALE_ADRESSENLISTE ON ALE_FIRMENNR = ADK_FIRMENNR AND ALE_ADRLISTENNR = ADK_ADRLISTENNR" "~"
if joinKuerzel = KZAADO
searchResult := searchResult "LEFT JOIN ADO_AUSDRUCKMODELL.ADO_AUSDRUCKMODELL ON ADO_FIRMENNR = KZA_ADO_FIRMENNR AND ADO_AUSDRUCKMODELLNR = KZA_ADO_AUSDRUCKMODELLNR" "~"
if joinKuerzel = ADOKZA
searchResult := searchResult "LEFT JOIN WWS.KZA_KASSENZAHLUNGSARTENZU ON KZA_ADO_FIRMENNR = ADO_FIRMENNR AND KZA_ADO_AUSDRUCKMODELLNR = ADO_AUSDRUCKMODELLNR" "~"
if joinKuerzel = AENADR
searchResult := searchResult "LEFT JOIN BUERO.ADR_ADRESSE ON ADR_ADRESSLFDNR = AEN_ADRESSLFDNR" "~"
if joinKuerzel = ADRAEN
searchResult := searchResult "LEFT JOIN BUERO.AEN_AENDERUNGCRM ON AEN_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = ALEADR
searchResult := searchResult "LEFT JOIN BUERO.ADR_ADRESSE ON ADR_ADRESSLFDNR = ALE_ADRESSLFDNR" "~"
if joinKuerzel = ADRALE
searchResult := searchResult "LEFT JOIN BUERO.ALE_ADRESSENLISTE ON ALE_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = AVEADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = AVE_LIEFEREMPF_ADRESSLFDNR" "~"
if joinKuerzel = ADRAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_LIEFEREMPF_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = FWFADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = FWF_BETRIEB_ADRESSLFDNR" "~"
if joinKuerzel = ADRFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_BETRIEB_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = XSPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = XSP_ERSTELLUSER" "~"
if joinKuerzel = BMUXSP
searchResult := searchResult "LEFT JOIN BUERO.XSP_XML_STEUERERKL_PAKET ON XSP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = YASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = YAS_ERSTELLUSER" "~"
if joinKuerzel = BMUYAS
searchResult := searchResult "LEFT JOIN FIBU.YAS_AUTOBUCHAUFTEILSATZ ON YAS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = YBMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = YBM_ERSTELLUSER" "~"
if joinKuerzel = BMUYBM
searchResult := searchResult "LEFT JOIN FIBU.YBM_AUTOBUCHMODELLE ON YBM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZBPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZBP_ERSTELLUSER" "~"
if joinKuerzel = BMUZBP
searchResult := searchResult "LEFT JOIN BUERO.ZBP_ZEITBUCHPROTOKOLL ON ZBP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZDEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZDE_ERSTELLUSER" "~"
if joinKuerzel = BMUZDE
searchResult := searchResult "LEFT JOIN FIBU.ZDE_ZIB_DETAIL ON ZDE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZINBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZIN_ERSTELLUSER" "~"
if joinKuerzel = BMUZIN
searchResult := searchResult "LEFT JOIN FIBU.ZIN_ZAHLUNGSINFORMATION ON ZIN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZIWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZIW_ERSTELLUSER" "~"
if joinKuerzel = BMUZIW
searchResult := searchResult "LEFT JOIN BUERO.ZIW_ZUSATZINFOWERT ON ZIW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZIZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZIZ_ERSTELLUSER" "~"
if joinKuerzel = BMUZIZ
searchResult := searchResult "LEFT JOIN BUERO.ZIZ_ZUSATZINFOZUORD ON ZIZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZLGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZLG_ERSTELLUSER" "~"
if joinKuerzel = BMUZLG
searchResult := searchResult "LEFT JOIN ZLE.ZLG_ZAHLUNGEN ON ZLG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZPTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZPT_ERSTELLUSER" "~"
if joinKuerzel = BMUZPT
searchResult := searchResult "LEFT JOIN BUERO.ZPT_ZEITPLANTEXT ON ZPT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZSKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZSK_ERSTELLUSER" "~"
if joinKuerzel = BMUZSK
searchResult := searchResult "LEFT JOIN ZLE.ZSK_ZEITSCHEIBEKOPF ON ZSK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZTVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZTV_ERSTELLUSER" "~"
if joinKuerzel = BMUZTV
searchResult := searchResult "LEFT JOIN BUERO.ZTV_ZEITPLANTEXTVERSDAT ON ZTV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZUHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZUH_ERSTELLUSER" "~"
if joinKuerzel = BMUZUH
searchResult := searchResult "LEFT JOIN BUERO.ZUH_ZEITUMBUCHHIST ON ZUH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZVMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZVM_ERSTELLUSER" "~"
if joinKuerzel = BMUZVM
searchResult := searchResult "LEFT JOIN BUERO.ZVM_ZEITPLANUNGVERSION ON ZVM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZWPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZWP_ERSTELLUSER" "~"
if joinKuerzel = BMUZWP
searchResult := searchResult "LEFT JOIN WWS.ZWP_ZUORDNUNGWVPOS ON ZWP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BPDERA
searchResult := searchResult "LEFT JOIN WWS.ERA_ERFASSRABATT ON ERA_BPD_ERFASSUNGSKZ = BPD_ERFASSUNGSKZ AND ERA_BPD_BESTPRODERFASSNR = BPD_BESTPRODERFASSUNGSNR AND ERA_BPD_BESTPOSLFDNR = BPD_BESTPOSLFDNR AND ERA_BPD_ERFASSPOSDETLFDNR = BPD_ERFASSPOSDETAILLFDNR AND ERA_FIRMENNR = BPD_FIRMENNR" "~"
if joinKuerzel = ERABPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_ERFASSUNGSKZ = ERA_BPD_ERFASSUNGSKZ AND BPD_BESTPRODERFASSUNGSNR = ERA_BPD_BESTPRODERFASSNR AND BPD_BESTPOSLFDNR = ERA_BPD_BESTPOSLFDNR AND BPD_ERFASSPOSDETAILLFDNR = ERA_BPD_ERFASSPOSDETLFDNR AND BPD_FIRMENNR = ERA_FIRMENNR" "~"
if joinKuerzel = AALAAD
searchResult := searchResult "LEFT JOIN BIL.AAD_AUSDRUCKDETAIL ON AAD_ANBUAUSDRDETAILID = AAL_AUSDRUCKID" "~"
if joinKuerzel = AADAAL
searchResult := searchResult "LEFT JOIN BIL.AAL_AUSDRUCKLAENDER ON AAL_AUSDRUCKID = AAD_ANBUAUSDRDETAILID" "~"
if joinKuerzel = IABABR
searchResult := searchResult "LEFT JOIN BIL.ABR_ANBUBEWRES ON ABR_BEWRESID = IAB_ABR_BEWRESID" "~"
if joinKuerzel = ABRIAB
searchResult := searchResult "LEFT JOIN BIL.IAB_INVESTITIONSABZUG ON IAB_ABR_BEWRESID = ABR_BEWRESID" "~"
if joinKuerzel = PVFABS
searchResult := searchResult "LEFT JOIN ABS_ARBEITSSTAETTE.ABS_ARBEITSSTAETTE ON ABS_ABSID = PVF_ABSID AND ABS_LOHNJAHR = PVF_LOHNJAHR AND ABS_FIRMENNR = PVF_ABS_FIRMENNR" "~"
if joinKuerzel = ABSPVF
searchResult := searchResult "LEFT JOIN BUERO.PVF_PERSONENVORERFASSUNG ON PVF_ABSID = ABS_ABSID AND PVF_LOHNJAHR = ABS_LOHNJAHR AND PVF_ABS_FIRMENNR = ABS_FIRMENNR" "~"
if joinKuerzel = BPVABU
searchResult := searchResult "LEFT JOIN BIL.ABU_ANLAGENBUCHHALTUNG ON ABU_FIRMENNR = BPV_FIRMENNR AND ABU_ANBUNR = BPV_ANBUNR" "~"
if joinKuerzel = ABUBPV
searchResult := searchResult "LEFT JOIN BIL.BPV_ANBU_BERECHPARAMS ON BPV_FIRMENNR = ABU_FIRMENNR AND BPV_ANBUNR = ABU_ANBUNR" "~"
if joinKuerzel = ADRADA
searchResult := searchResult "LEFT JOIN BUERO.ADA_ADRESSART ON ADA_ADRESSARTNR = ADR_ADRESSARTNR" "~"
if joinKuerzel = ADAADR
searchResult := searchResult "LEFT JOIN BUERO.ADR_ADRESSE ON ADR_ADRESSARTNR = ADA_ADRESSARTNR" "~"
if joinKuerzel = DFRADD
searchResult := searchResult "LEFT JOIN ADD_AUSDRUCKMODELLDETAIL.ADD_AUSDRUCKMODELLDETAIL ON ADD_FIRMENNR = DFR_ADD_FIRMENNR AND ADD_AUSDRUCKMODELLNR = DFR_ADD_AUSDRUCKMODELLNR AND ADD_AUSDRUCKMODELLFDNR = DFR_ADD_AUSDRUCKMODELLFDNR" "~"
if joinKuerzel = ADDDFR
searchResult := searchResult "LEFT JOIN WWS.DFR_DOKUMENTFREIGABE ON DFR_ADD_FIRMENNR = ADD_FIRMENNR AND DFR_ADD_AUSDRUCKMODELLNR = ADD_AUSDRUCKMODELLNR AND DFR_ADD_AUSDRUCKMODELLFDNR = ADD_AUSDRUCKMODELLFDNR" "~"
if joinKuerzel = BPKADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = BPK_LIEFERANT_ADRESSLFDNR" "~"
if joinKuerzel = ADRBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_LIEFERANT_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = AKNABU
searchResult := searchResult "LEFT JOIN BIL.ABU_ANLAGENBUCHHALTUNG ON ABU_FIRMENNR = AKN_FIRMENNR AND ABU_ANBUNR = AKN_ANBUNR" "~"
if joinKuerzel = ABUAKN
searchResult := searchResult "LEFT JOIN BIL.AKN_ANBU_KONSOL_AUSSCHLUSS ON AKN_FIRMENNR = ABU_FIRMENNR AND AKN_ANBUNR = ABU_ANBUNR" "~"
if joinKuerzel = ACAACF
searchResult := searchResult "LEFT JOIN FIBU.ACF_TESTFALL ON ACF_FIRMENNR = ACA_FIRMENNR AND ACF_FIBUNR = ACA_FIBUNR AND ACF_TESTFALLNR = ACA_TESTFALLNR" "~"
if joinKuerzel = ACFACA
searchResult := searchResult "LEFT JOIN FIBU.ACA_TESTFALL_AUSWERTUNG ON ACA_FIRMENNR = ACF_FIRMENNR AND ACA_FIBUNR = ACF_FIBUNR AND ACA_TESTFALLNR = ACF_TESTFALLNR" "~"
if joinKuerzel = ACEACF
searchResult := searchResult "LEFT JOIN FIBU.ACF_TESTFALL ON ACF_FIRMENNR = ACE_FIRMENNR AND ACF_FIBUNR = ACE_FIBUNR AND ACF_TESTFALLNR = ACE_TESTFALLNR" "~"
if joinKuerzel = ACFACE
searchResult := searchResult "LEFT JOIN FIBU.ACE_EXTTESTFALL ON ACE_FIRMENNR = ACF_FIRMENNR AND ACE_FIBUNR = ACF_FIBUNR AND ACE_TESTFALLNR = ACF_TESTFALLNR" "~"
if joinKuerzel = HNPADA
searchResult := searchResult "LEFT JOIN ADA_ADRESSART.ADA_ADRESSART ON ADA_ADRESSARTNR = HNP_ADRESSARTNR" "~"
if joinKuerzel = ADAHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_ADRESSARTNR = ADA_ADRESSARTNR" "~"
if joinKuerzel = AEEADD
searchResult := searchResult "LEFT JOIN BUERO.ADD_AUSDRUCKMODELLDETAIL ON ADD_FIRMENNR = AEE_FIRMENNR AND ADD_AUSDRUCKMODELLNR = AEE_AUSDRUCKMODELLNR AND ADD_AUSDRUCKMODELLFDNR = AEE_AUSDRUCKMODELLFDNR" "~"
if joinKuerzel = ADDAEE
searchResult := searchResult "LEFT JOIN BUERO.AEE_AUSDRUCKEMAILEMPF ON AEE_FIRMENNR = ADD_FIRMENNR AND AEE_AUSDRUCKMODELLNR = ADD_AUSDRUCKMODELLNR AND AEE_AUSDRUCKMODELLFDNR = ADD_AUSDRUCKMODELLFDNR" "~"
if joinKuerzel = AEEADO
searchResult := searchResult "LEFT JOIN BUERO.ADO_AUSDRUCKMODELL ON ADO_FIRMENNR = AEE_FIRMENNR AND ADO_AUSDRUCKMODELLNR = AEE_AUSDRUCKMODELLNR" "~"
if joinKuerzel = ADOAEE
searchResult := searchResult "LEFT JOIN BUERO.AEE_AUSDRUCKEMAILEMPF ON AEE_FIRMENNR = ADO_FIRMENNR AND AEE_AUSDRUCKMODELLNR = ADO_AUSDRUCKMODELLNR" "~"
if joinKuerzel = MAHADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = MAH_ADRESSLFDNR" "~"
if joinKuerzel = ADRMAH
searchResult := searchResult "LEFT JOIN FIBU.MAH_MAHNUNG ON MAH_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = ZVKADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = ZVK_ADRESSLFDNR" "~"
if joinKuerzel = ADRZVK
searchResult := searchResult "LEFT JOIN FIBU.ZVK_ZAHLUNGSVORSCHLAG ON ZVK_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = WSKADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = WSK_LIEF_ADRESSLFDNR" "~"
if joinKuerzel = ADRWSK
searchResult := searchResult "LEFT JOIN WWS.WSK_WWSKORESTATISTIK ON WSK_LIEF_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = AWEAFK
searchResult := searchResult "LEFT JOIN ZLE.AFK_EXTFILTERKOPF ON AFK_FIRMENNR = AWE_AFK_FIRMENNR AND AFK_FILTERKOPFNR = AWE_AFK_FILTERKOPFNR" "~"
if joinKuerzel = AFKAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_AFK_FIRMENNR = AFK_FIRMENNR AND AWE_AFK_FILTERKOPFNR = AFK_FILTERKOPFNR" "~"
if joinKuerzel = TERAGM
searchResult := searchResult "LEFT JOIN AGM_AG_PERSONZU.AGM_AG_PERSONZU ON AGM_PPSKAPAID = TER_PPSKAPAID AND AGM_AGPERSONZULFDNR = TER_AGPERSONZULFDNR" "~"
if joinKuerzel = AGMTER
searchResult := searchResult "LEFT JOIN BUERO.TER_TERMIN ON TER_PPSKAPAID = AGM_PPSKAPAID AND TER_AGPERSONZULFDNR = AGM_AGPERSONZULFDNR" "~"
if joinKuerzel = AGIAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_AG_ID = AGI_AG_ID" "~"
if joinKuerzel = AGSAGI
searchResult := searchResult "LEFT JOIN WWS.AGI_AGPER_INFOZU ON AGI_AG_ID = AGS_AG_ID" "~"
if joinKuerzel = AGNAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_AG_ID = AGN_AG_ID" "~"
if joinKuerzel = AGSAGN
searchResult := searchResult "LEFT JOIN WWS.AGN_AG_PERSON_ZU ON AGN_AG_ID = AGS_AG_ID" "~"
if joinKuerzel = AGZAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_AG_ID = AGZ_AG_ID" "~"
if joinKuerzel = AGSAGZ
searchResult := searchResult "LEFT JOIN WWS.AGZ_ARBEITSGANGZU ON AGZ_AG_ID = AGS_AG_ID" "~"
if joinKuerzel = RSBAGS
searchResult := searchResult "LEFT JOIN AGS_ARBEITSGANG.AGS_ARBEITSGANG ON AGS_AG_ID = RSB_AGS_AGSTAMMID" "~"
if joinKuerzel = AGSRSB
searchResult := searchResult "LEFT JOIN BUERO.RSB_RESSOURCENBEDARF ON RSB_AGS_AGSTAMMID = AGS_AG_ID" "~"
if joinKuerzel = AGZAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_AG_ID = AGZ_AG_PARENT_ID" "~"
if joinKuerzel = AGSAGZ
searchResult := searchResult "LEFT JOIN WWS.AGZ_ARBEITSGANGZU ON AGZ_AG_PARENT_ID = AGS_AG_ID" "~"
if joinKuerzel = KOCAID
searchResult := searchResult "LEFT JOIN AID_ARTIKELIDENTIFIKATION.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = KOC_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDKOC
searchResult := searchResult "LEFT JOIN PRODUKTION.KOC_KOMBI_CHARGE ON KOC_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = SNRAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = SNR_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = TLPAID
searchResult := searchResult "LEFT JOIN AID_ARTIKELIDENTIFIKATION.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = TLP_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDTLP
searchResult := searchResult "LEFT JOIN PRODUKTION.TLP_BT_LOGISTIK_POSITION ON TLP_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = VSTAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = VST_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDVST
searchResult := searchResult "LEFT JOIN WWS.VST_VERKAUFSSTATISTIK ON VST_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = WAPAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = WAP_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = WSKAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = WSK_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDWSK
searchResult := searchResult "LEFT JOIN WWS.WSK_WWSKORESTATISTIK ON WSK_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = AZKABU
searchResult := searchResult "LEFT JOIN BIL.ABU_ANLAGENBUCHHALTUNG ON ABU_FIRMENNR = AZK_FIRMENNR AND ABU_ANBUNR = AZK_ANBUNR" "~"
if joinKuerzel = ABUAZK
searchResult := searchResult "LEFT JOIN BIL.AZK_ANBUZUSCHUSSKENNZEICH ON AZK_FIRMENNR = ABU_FIRMENNR AND AZK_ANBUNR = ABU_ANBUNR" "~"
if joinKuerzel = NFZABU
searchResult := searchResult "LEFT JOIN BIL.ABU_ANLAGENBUCHHALTUNG ON ABU_FIRMENNR = NFZ_FIRMENNR AND ABU_ANBUNR = NFZ_ANBUNR" "~"
if joinKuerzel = ABUNFZ
searchResult := searchResult "LEFT JOIN BIL.NFZ_ANBUFIBUZU ON NFZ_FIRMENNR = ABU_FIRMENNR AND NFZ_ANBUNR = ABU_ANBUNR" "~"
if joinKuerzel = AMZADO
searchResult := searchResult "LEFT JOIN BUERO.ADO_AUSDRUCKMODELL ON ADO_FIRMENNR = AMZ_FIRMENNR AND ADO_AUSDRUCKMODELLNR = AMZ_AUSDRUCKMODELLNR" "~"
if joinKuerzel = ADOAMZ
searchResult := searchResult "LEFT JOIN BUERO.AMZ_AUSDRUCKMODELLZU ON AMZ_FIRMENNR = ADO_FIRMENNR AND AMZ_AUSDRUCKMODELLNR = ADO_AUSDRUCKMODELLNR" "~"
if joinKuerzel = RNGADO
searchResult := searchResult "LEFT JOIN ADO_AUSDRUCKMODELL.ADO_AUSDRUCKMODELL ON ADO_FIRMENNR = RNG_AUSDRUCKMODELLFIRMENNR AND ADO_AUSDRUCKMODELLNR = RNG_AUSDRUCKMODELLNR" "~"
if joinKuerzel = ADORNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_AUSDRUCKMODELLFIRMENNR = ADO_FIRMENNR AND RNG_AUSDRUCKMODELLNR = ADO_AUSDRUCKMODELLNR" "~"
if joinKuerzel = BONADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = BON_KUNDEN_ADRESSLFDNR" "~"
if joinKuerzel = ADRBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_KUNDEN_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = MSUADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = MSU_ADRESSLFDNR" "~"
if joinKuerzel = ADRMSU
searchResult := searchResult "LEFT JOIN FIBU.MSU_MAHNSUMME ON MSU_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = WAMADR
searchResult := searchResult "LEFT JOIN BUERO.ADR_ADRESSE ON ADR_ADRESSLFDNR = WAM_ADRESSLFDNR" "~"
if joinKuerzel = ADRWAM
searchResult := searchResult "LEFT JOIN BUERO.WAM_WEBANMELDUNG ON WAM_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = EFKADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = EFK_LIEFEMPF_ADRESSLFDNR" "~"
if joinKuerzel = ADREFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_LIEFEMPF_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = BPKADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = BPK_RECHEMPF_ADRESSLFDNR" "~"
if joinKuerzel = ADRBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_RECHEMPF_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = WSSADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = WSS_RECHEMPF_ADRESSLFDNR" "~"
if joinKuerzel = ADRWSS
searchResult := searchResult "LEFT JOIN WWS.WSS_WWSSTATISTIK ON WSS_RECHEMPF_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = RSBAGM
searchResult := searchResult "LEFT JOIN AGM_AG_PERSONZU.AGM_AG_PERSONZU ON AGM_PPSKAPAID = RSB_PCK_PPSKAPAID AND AGM_AGPERSONZULFDNR = RSB_AGM_AGPERSONZULFDNR" "~"
if joinKuerzel = AGMRSB
searchResult := searchResult "LEFT JOIN BUERO.RSB_RESSOURCENBEDARF ON RSB_PCK_PPSKAPAID = AGM_PPSKAPAID AND RSB_AGM_AGPERSONZULFDNR = AGM_AGPERSONZULFDNR" "~"
if joinKuerzel = APSAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_AG_ID = APS_AG_START_ID" "~"
if joinKuerzel = AGSAPS
searchResult := searchResult "LEFT JOIN WWS.APS_ARBEITSPLAN_STAMM ON APS_AG_START_ID = AGS_AG_ID" "~"
if joinKuerzel = KGSAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_AG_ID = KGS_AGS_AG_ID" "~"
if joinKuerzel = AGSKGS
searchResult := searchResult "LEFT JOIN WWS.KGS_KATZU_ARBEITSGANG ON KGS_AGS_AG_ID = AGS_AG_ID" "~"
if joinKuerzel = DAGAGU
searchResult := searchResult "LEFT JOIN AGU_ANLAGEGUT.AGU_ANLAGEGUT ON AGU_ANLAGEGUTID = DAG_AGU_ANLAGEGUTID" "~"
if joinKuerzel = AGUDAG
searchResult := searchResult "LEFT JOIN BUERO.DAG_DOKANLAGEGUTZU ON DAG_AGU_ANLAGEGUTID = AGU_ANLAGEGUTID" "~"
if joinKuerzel = ASTAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = AST_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDAST
searchResult := searchResult "LEFT JOIN WWS.AST_ARTIKELSTATISTIK ON AST_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = BPDAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = BPD_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = EKKAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = EKK_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDEKK
searchResult := searchResult "LEFT JOIN WWS.EKK_EXTERNE_KUNDENKARTEI ON EKK_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = ELVAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = ELV_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDELV
searchResult := searchResult "LEFT JOIN WWS.ELV_ERFASSPOSLIEFERVOR ON ELV_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = EPVAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = EPV_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDEPV
searchResult := searchResult "LEFT JOIN WWS.EPV_ERFASSPOSRECHVOR ON EPV_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = SLKAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = SLK_AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDSLK
searchResult := searchResult "LEFT JOIN WWS.SLK_STUECKLISTENKOPF ON SLK_AID_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = SLPAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = SLP_AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDSLP
searchResult := searchResult "LEFT JOIN WWS.SLP_STUECKLISTENPOSITION ON SLP_AID_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = TRJAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = TRJ_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDTRJ
searchResult := searchResult "LEFT JOIN WWS.TRJ_TRANSPORTEINH_JOURNAL ON TRJ_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = AVLAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = AVL_AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDAVL
searchResult := searchResult "LEFT JOIN WWS.AVL_AUFTRAGSVORERFLIEFDAT ON AVL_AID_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = KOPAID
searchResult := searchResult "LEFT JOIN AID_ARTIKELIDENTIFIKATION.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = KOP_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDKOP
searchResult := searchResult "LEFT JOIN PRODUKTION.KOP_KOMMISSIONIERUNG_POS ON KOP_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = LPOAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = LPO_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = LAJAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = LAJ_UMB_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_UMB_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = OBAAIN
searchResult := searchResult "LEFT JOIN BUERO.AIN_AUSSCHREIBUNGINSERTZU ON AIN_AINZUID = OBA_AINZUID" "~"
if joinKuerzel = AINOBA
searchResult := searchResult "LEFT JOIN BUERO.OBA_ONLBEWAUSSCHREIBUNGZU ON OBA_AINZUID = AIN_AINZUID" "~"
if joinKuerzel = OBBAIN
searchResult := searchResult "LEFT JOIN BUERO.AIN_AUSSCHREIBUNGINSERTZU ON AIN_AINZUID = OBB_AIN_AINZUID" "~"
if joinKuerzel = AINOBB
searchResult := searchResult "LEFT JOIN BUERO.OBB_ONLBEWBEWERBUNG ON OBB_AIN_AINZUID = AIN_AINZUID" "~"
if joinKuerzel = AWEAKO
searchResult := searchResult "LEFT JOIN AKO_AGGREGATKOPF.AKO_AGGREGATKOPF ON AKO_AGGKOPFID = AWE_AKO_AGGKOPFID" "~"
if joinKuerzel = AKOAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_AKO_AGGKOPFID = AKO_AGGKOPFID" "~"
if joinKuerzel = EROAKT
searchResult := searchResult "LEFT JOIN WWS.AKT_AKTIONSSTAMM ON AKT_FIRMENNR = ERO_AKT_FIRMENNR AND AKT_AKTIONSSTAMMNR = ERO_AKTIONSSTAMMNR" "~"
if joinKuerzel = AKTERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_AKT_FIRMENNR = AKT_FIRMENNR AND ERO_AKTIONSSTAMMNR = AKT_AKTIONSSTAMMNR" "~"
if joinKuerzel = EROAKT
searchResult := searchResult "LEFT JOIN WWS.AKT_AKTIONSSTAMM ON AKT_FIRMENNR = ERO_AKT_FIRMENNR_RABATT AND AKT_AKTIONSSTAMMNR = ERO_AKTIONSSTAMMNR_RABATT" "~"
if joinKuerzel = AKTERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_AKT_FIRMENNR_RABATT = AKT_FIRMENNR AND ERO_AKTIONSSTAMMNR_RABATT = AKT_AKTIONSSTAMMNR" "~"
if joinKuerzel = FAZALE
searchResult := searchResult "LEFT JOIN BUERO.ALE_ADRESSENLISTE ON ALE_FIRMENNR = FAZ_FIRMENNR AND ALE_ADRLISTENNR = FAZ_ADRLISTENNR AND ALE_ADRLISTENLFDNR = FAZ_ADRLISTENLFDNR" "~"
if joinKuerzel = ALEFAZ
searchResult := searchResult "LEFT JOIN BUERO.FAZ_FRAGEANTWORTZU ON FAZ_FIRMENNR = ALE_FIRMENNR AND FAZ_ADRLISTENNR = ALE_ADRLISTENNR AND FAZ_ADRLISTENLFDNR = ALE_ADRLISTENLFDNR" "~"
if joinKuerzel = EPSALN
searchResult := searchResult "LEFT JOIN ALN_ALIBINUMMERN.ALN_ALIBINUMMERN ON ALN_ALNID = EPS_ALNID" "~"
if joinKuerzel = ALNEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_ALNID = ALN_ALNID" "~"
if joinKuerzel = PCPALN
searchResult := searchResult "LEFT JOIN PRODUKTION.ALN_ALIBINUMMERN ON ALN_ALNID = PCP_ALNID" "~"
if joinKuerzel = ALNPCP
searchResult := searchResult "LEFT JOIN PRODUKTION.PCP_PPS_PRODUKT ON PCP_ALNID = ALN_ALNID" "~"
if joinKuerzel = ALSALS
searchResult := searchResult "LEFT JOIN WWS.ALS_ALLERGEN ON ALS_ALLERGENNR = ALS_DAVON_ALLERGENNR AND ALS_ALSMODUS = ALS_DAVON_ALSMODUS AND ALS_FIRMENNR = ALS_DAVON_FIRMENNR" "~"
if joinKuerzel = ALSALS
searchResult := searchResult "LEFT JOIN WWS.ALS_ALLERGEN ON ALS_DAVON_ALLERGENNR = ALS_ALLERGENNR AND ALS_DAVON_ALSMODUS = ALS_ALSMODUS AND ALS_DAVON_FIRMENNR = ALS_FIRMENNR" "~"
if joinKuerzel = ADRANR
searchResult := searchResult "LEFT JOIN BUERO.ANR_ANREDE ON ANR_ANREDENR = ADR_GESCHAEFTLICH_ANREDENR" "~"
if joinKuerzel = ANRADR
searchResult := searchResult "LEFT JOIN BUERO.ADR_ADRESSE ON ADR_GESCHAEFTLICH_ANREDENR = ANR_ANREDENR" "~"
if joinKuerzel = ADRANR
searchResult := searchResult "LEFT JOIN BUERO.ANR_ANREDE ON ANR_ANREDENR = ADR_EHEPAAR_ANREDENR" "~"
if joinKuerzel = ANRADR
searchResult := searchResult "LEFT JOIN BUERO.ADR_ADRESSE ON ADR_EHEPAAR_ANREDENR = ANR_ANREDENR" "~"
if joinKuerzel = ARZANS
searchResult := searchResult "LEFT JOIN BIL.ANS_ANLAGENSCHEMA ON ANS_ANLSCHEMAID = ARZ_ANLSCHEMAID" "~"
if joinKuerzel = ANSARZ
searchResult := searchResult "LEFT JOIN BIL.ARZ_ANLAGEGUTGRUPPEZU ON ARZ_ANLSCHEMAID = ANS_ANLSCHEMAID" "~"
if joinKuerzel = ALKAPA
searchResult := searchResult "LEFT JOIN PRODUKTION.APA_ARBEITSPLAN ON APA_AP_ID = ALK_AP_ID" "~"
if joinKuerzel = APAALK
searchResult := searchResult "LEFT JOIN PRODUKTION.ALK_ARBEITSPLAN_KOSTEN ON ALK_AP_ID = APA_AP_ID" "~"
if joinKuerzel = ERPAPA
searchResult := searchResult "LEFT JOIN APA_ARBEITSPLAN.APA_ARBEITSPLAN ON APA_AP_ID = ERP_AP_ID" "~"
if joinKuerzel = APAERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_AP_ID = APA_AP_ID" "~"
if joinKuerzel = AROAPS
searchResult := searchResult "LEFT JOIN WWS.APS_ARBEITSPLAN_STAMM ON APS_APSTAMM_ID = ARO_APSTAMM_ID" "~"
if joinKuerzel = APSARO
searchResult := searchResult "LEFT JOIN WWS.ARO_APS_KOSTEN ON ARO_APSTAMM_ID = APS_APSTAMM_ID" "~"
if joinKuerzel = APSAPS
searchResult := searchResult "LEFT JOIN WWS.APS_ARBEITSPLAN_STAMM ON APS_APSTAMM_ID = APS_APS_VORLAGE_AP_ID" "~"
if joinKuerzel = APSAPS
searchResult := searchResult "LEFT JOIN WWS.APS_ARBEITSPLAN_STAMM ON APS_APS_VORLAGE_AP_ID = APS_APSTAMM_ID" "~"
if joinKuerzel = PSWARG
searchResult := searchResult "LEFT JOIN BUERO.ARG_ARBEITSRUHEZEITGESETZ ON ARG_FIRMENNR = PSW_ARG_FIRMENNR AND ARG_ARGNR = PSW_ARGNR" "~"
if joinKuerzel = ARGPSW
searchResult := searchResult "LEFT JOIN BUERO.PSW_PERSONENWERTE ON PSW_ARG_FIRMENNR = ARG_FIRMENNR AND PSW_ARGNR = ARG_ARGNR" "~"
if joinKuerzel = ARLARK
searchResult := searchResult "LEFT JOIN WWS.ARK_ARTIKELKLASSE ON ARK_FIRMENNR = ARL_FIRMENNR AND ARK_ARTIKELKLASSE = ARL_ARTIKELKLASSE" "~"
if joinKuerzel = ARKARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = ARK_FIRMENNR AND ARL_ARTIKELKLASSE = ARK_ARTIKELKLASSE" "~"
if joinKuerzel = ZBZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZBZ_ERSTELLUSER" "~"
if joinKuerzel = BMUZBZ
searchResult := searchResult "LEFT JOIN BUERO.ZBZ_ZEITBEZUGSART ON ZBZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZEPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZEP_ERSTELLUSER" "~"
if joinKuerzel = BMUZEP
searchResult := searchResult "LEFT JOIN WWS.ZEP_ZEITEINHEITENPOS ON ZEP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZMDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZMD_ERSTELLUSER" "~"
if joinKuerzel = BMUZMD
searchResult := searchResult "LEFT JOIN BUERO.ZMD_ZEITMINDESTBESETZDET ON ZMD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZNSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZNS_ERSTELLUSER" "~"
if joinKuerzel = BMUZNS
searchResult := searchResult "LEFT JOIN BUERO.ZNS_ZEITNACHRICHTSYSTEM ON ZNS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZPDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZPD_ERSTELLUSER" "~"
if joinKuerzel = BMUZPD
searchResult := searchResult "LEFT JOIN BUERO.ZPD_ZEITPROFILDETAIL ON ZPD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZPKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZPK_ERSTELLUSER" "~"
if joinKuerzel = BMUZPK
searchResult := searchResult "LEFT JOIN BUERO.ZPK_ZEITPROFILKOPF ON ZPK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZRTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZRT_ERSTELLUSER" "~"
if joinKuerzel = BMUZRT
searchResult := searchResult "LEFT JOIN BUERO.ZRT_ZAHLUNGSART ON ZRT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZUSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZUS_ERSTELLUSER" "~"
if joinKuerzel = BMUZUS
searchResult := searchResult "LEFT JOIN BUERO.ZUS_ZUSTAENDIGKEIT ON ZUS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZUWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZUW_ERSTELLUSER" "~"
if joinKuerzel = BMUZUW
searchResult := searchResult "LEFT JOIN BUERO.ZUW_ZEITUMSATZWERTE ON ZUW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZVBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZVB_ERSTELLUSER" "~"
if joinKuerzel = BMUZVB
searchResult := searchResult "LEFT JOIN BUERO.ZVB_TBBEZEICHNUNGSZWECK ON ZVB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZVTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZVT_ERSTELLUSER" "~"
if joinKuerzel = BMUZVT
searchResult := searchResult "LEFT JOIN BUERO.ZVT_TBDATEIEN ON ZVT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZBAFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = ZBA_FIRMENNR" "~"
if joinKuerzel = FIRZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = OAUOKL
searchResult := searchResult "LEFT JOIN LOHN.OKL_OF_KLIENT ON OKL_KLIENTENNR = OAU_KLIENTENNR" "~"
if joinKuerzel = OKLOAU
searchResult := searchResult "LEFT JOIN LOHN.OAU_OF_AUFTRAG ON OAU_KLIENTENNR = OKL_KLIENTENNR" "~"
if joinKuerzel = PMZPMD
searchResult := searchResult "LEFT JOIN BUERO.PMD_PAUSENMODELL ON PMD_FIRMENNR = PMZ_FIRMENNR AND PMD_PAUSENMODELLNR = PMZ_PAUSENMODELLNR" "~"
if joinKuerzel = PMDPMZ
searchResult := searchResult "LEFT JOIN BUERO.PMZ_PAUSENTAGESMODELLZU ON PMZ_FIRMENNR = PMD_FIRMENNR AND PMZ_PAUSENMODELLNR = PMD_PAUSENMODELLNR" "~"
if joinKuerzel = ZBARMO
searchResult := searchResult "LEFT JOIN BUERO.RMO_RUNDUNGSMODELL ON RMO_FIRMENNR = ZBA_FIRMENNR AND RMO_RMODELLNR = ZBA_RMODELLNR" "~"
if joinKuerzel = RMOZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_FIRMENNR = RMO_FIRMENNR AND ZBA_RMODELLNR = RMO_RMODELLNR" "~"
if joinKuerzel = AMZAAA
searchResult := searchResult "LEFT JOIN BUERO.AAA_AUSDRUCKABLAUFART ON AAA_ABLAUFART = AMZ_AUSDRUCKABLAUFART" "~"
if joinKuerzel = AAAAMZ
searchResult := searchResult "LEFT JOIN BUERO.AMZ_AUSDRUCKMODELLZU ON AMZ_AUSDRUCKABLAUFART = AAA_ABLAUFART" "~"
if joinKuerzel = RVKADA
searchResult := searchResult "LEFT JOIN ADA_ADRESSART.ADA_ADRESSART ON ADA_ADRESSARTNR = RVK_ADRESSARTNR" "~"
if joinKuerzel = ADARVK
searchResult := searchResult "LEFT JOIN ZLE.RVK_RECHVORLAGEKOPF ON RVK_ADRESSARTNR = ADA_ADRESSARTNR" "~"
if joinKuerzel = UMFADK
searchResult := searchResult "LEFT JOIN BUERO.ADK_ADRESSENLISTEKOPF ON ADK_FIRMENNR = UMF_ADK_FIRMENNR AND ADK_ADRLISTENNR = UMF_ADRLISTENNR" "~"
if joinKuerzel = ADKUMF
searchResult := searchResult "LEFT JOIN BUERO.UMF_UMFRAGE ON UMF_ADK_FIRMENNR = ADK_FIRMENNR AND UMF_ADRLISTENNR = ADK_ADRLISTENNR" "~"
if joinKuerzel = EVNADR
searchResult := searchResult "LEFT JOIN BUERO.ADR_ADRESSE ON ADR_ADRESSLFDNR = EVN_ADRESSLFDNR" "~"
if joinKuerzel = ADREVN
searchResult := searchResult "LEFT JOIN BUERO.EVN_TEILNEHMER ON EVN_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = IMOADR
searchResult := searchResult "LEFT JOIN BUERO.ADR_ADRESSE ON ADR_ADRESSLFDNR = IMO_ADRESSLFDNR" "~"
if joinKuerzel = ADRIMO
searchResult := searchResult "LEFT JOIN BUERO.IMO_IMMOBILIE ON IMO_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = AVEADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = AVE_AUFTRGEB_ADRESSLFDNR" "~"
if joinKuerzel = ADRAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_AUFTRGEB_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = WSSADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = WSS_LIEF_ADRESSLFDNR" "~"
if joinKuerzel = ADRWSS
searchResult := searchResult "LEFT JOIN WWS.WSS_WWSSTATISTIK ON WSS_LIEF_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = WADADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = WAD_ZRECHEMPF_ADRESSLFDNR" "~"
if joinKuerzel = ADRWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_ZRECHEMPF_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = RIAAGS
searchResult := searchResult "LEFT JOIN AGS_ARBEITSGANG.AGS_ARBEITSGANG ON AGS_AG_ID = RIA_ARBEITSGANG" "~"
if joinKuerzel = AGSRIA
searchResult := searchResult "LEFT JOIN PRODUKTION.RIA_RISIKOANALYSE ON RIA_ARBEITSGANG = AGS_AG_ID" "~"
if joinKuerzel = LAGADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = LAG_ADRESSLFDNR" "~"
if joinKuerzel = ADRLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = RNGADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = RNG_ZRECHEMPF_ADRESSLFDNR" "~"
if joinKuerzel = ADRRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_ZRECHEMPF_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = VATADR
searchResult := searchResult "LEFT JOIN BUERO.ADR_ADRESSE ON ADR_ADRESSLFDNR = VAT_ADRESSLFDNR" "~"
if joinKuerzel = ADRVAT
searchResult := searchResult "LEFT JOIN BUERO.VAT_TEILNAHME ON VAT_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = WAFADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = WAF_ADRESSLFDNR" "~"
if joinKuerzel = ADRWAF
searchResult := searchResult "LEFT JOIN WWS.WAF_WWSADRZUFIRMENBEZ ON WAF_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = WBWADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = WBW_ADRESSLFDNR" "~"
if joinKuerzel = ADRWBW
searchResult := searchResult "LEFT JOIN WWS.WBW_WWSBUDGETWERTE ON WBW_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = AVEADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = AVE_RECHEMPF_ADRESSLFDNR" "~"
if joinKuerzel = ADRAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_RECHEMPF_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = KAKADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = KAK_LIEFEREMPF_ADRESSLFDNR" "~"
if joinKuerzel = ADRKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_LIEFEREMPF_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = RNGADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = RNG_RECHEMPF_ADRESSLFDNR" "~"
if joinKuerzel = ADRRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_RECHEMPF_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = WADADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = WAD_LIEFEREMPF_ADRESSLFDNR" "~"
if joinKuerzel = ADRWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_LIEFEREMPF_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = WSSADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = WSS_AUFTRGEB_ADRESSLFDNR" "~"
if joinKuerzel = ADRWSS
searchResult := searchResult "LEFT JOIN WWS.WSS_WWSSTATISTIK ON WSS_AUFTRGEB_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = AVEADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = AVE_ZRECHEMPF_ADRESSLFDNR" "~"
if joinKuerzel = ADRAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ZRECHEMPF_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = EFKADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = EFK_ZRECHEMPF_ADRESSLFDNR" "~"
if joinKuerzel = ADREFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_ZRECHEMPF_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = VSWADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = VSW_AUFTRGEB_ADRESSLFDNR" "~"
if joinKuerzel = ADRVSW
searchResult := searchResult "LEFT JOIN WWS.VSW_VERTRETERSTATISTIK ON VSW_AUFTRGEB_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = KUZAGA
searchResult := searchResult "LEFT JOIN BUERO.AGA_ABGABENART ON AGA_ABGABENARTID = KUZ_ABGABENARTID" "~"
if joinKuerzel = AGAKUZ
searchResult := searchResult "LEFT JOIN BUERO.KUZ_KUNDENZAHLUNGEN ON KUZ_ABGABENARTID = AGA_ABGABENARTID" "~"
if joinKuerzel = AGIAGN
searchResult := searchResult "LEFT JOIN WWS.AGN_AG_PERSON_ZU ON AGN_AG_ID = AGI_AG_ID AND AGN_AGPERSONZULFDNR = AGI_AGPERSONZULFDNR" "~"
if joinKuerzel = AGNAGI
searchResult := searchResult "LEFT JOIN WWS.AGI_AGPER_INFOZU ON AGI_AG_ID = AGN_AG_ID AND AGI_AGPERSONZULFDNR = AGN_AGPERSONZULFDNR" "~"
if joinKuerzel = MITAGR
searchResult := searchResult "LEFT JOIN BUERO.AGR_ABRECHNUNGSGRUPPE ON AGR_FIRMENNR = MIT_AGR_FIRMENNR AND AGR_ABRECHNUNGSGRPNR = MIT_ABRECHNUNGSGRPNR" "~"
if joinKuerzel = AGRMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_AGR_FIRMENNR = AGR_FIRMENNR AND MIT_ABRECHNUNGSGRPNR = AGR_ABRECHNUNGSGRPNR" "~"
if joinKuerzel = AGSAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_AG_ID = AGS_AGS_VORLAGE_AG_ID" "~"
if joinKuerzel = AGSAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_AGS_VORLAGE_AG_ID = AGS_AG_ID" "~"
if joinKuerzel = ANZAGU
searchResult := searchResult "LEFT JOIN BIL.AGU_ANLAGEGUT ON AGU_ANLAGEGUTID = ANZ_ANLAGEGUTID" "~"
if joinKuerzel = AGUANZ
searchResult := searchResult "LEFT JOIN BIL.ANZ_ANLAGEGUTKOSTZU ON ANZ_ANLAGEGUTID = AGU_ANLAGEGUTID" "~"
if joinKuerzel = AVEAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = AVE_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = BVOAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = BVO_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = EROAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = ERO_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = KOMAID
searchResult := searchResult "LEFT JOIN AID_ARTIKELIDENTIFIKATION.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = KOM_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDKOM
searchResult := searchResult "LEFT JOIN PRODUKTION.KOM_KOMBI ON KOM_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = PCPAID
searchResult := searchResult "LEFT JOIN AID_ARTIKELIDENTIFIKATION.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = PCP_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDPCP
searchResult := searchResult "LEFT JOIN PRODUKTION.PCP_PPS_PRODUKT ON PCP_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = WEDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WED_ERSTELLUSER" "~"
if joinKuerzel = BMUWED
searchResult := searchResult "LEFT JOIN WWS.WED_WARENEING_POSDETAIL ON WED_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEI_ERSTELLUSER" "~"
if joinKuerzel = BMUWEI
searchResult := searchResult "LEFT JOIN WWS.WEI_WARENEINSATZKOPF ON WEI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WFDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WFD_ERSTELLUSER" "~"
if joinKuerzel = BMUWFD
searchResult := searchResult "LEFT JOIN WWS.WFD_FOLGEARTIKELDETAIL ON WFD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WFFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WFF_ERSTELLUSER" "~"
if joinKuerzel = BMUWFF
searchResult := searchResult "LEFT JOIN BUERO.WFF_WORKFLOWFUNC ON WFF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WFHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WFH_ERSTELLUSER" "~"
if joinKuerzel = BMUWFH
searchResult := searchResult "LEFT JOIN WWS.WFH_FERTEP_HISTORIE ON WFH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WGBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WGB_ERSTELLUSER" "~"
if joinKuerzel = BMUWGB
searchResult := searchResult "LEFT JOIN FIBU.WGB_WARENEINGANGSBUCH ON WGB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WIVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WIV_ERSTELLUSER" "~"
if joinKuerzel = BMUWIV
searchResult := searchResult "LEFT JOIN BIL.WIV_WIVARIABLEN ON WIV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WKEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WKE_ERSTELLUSER" "~"
if joinKuerzel = BMUWKE
searchResult := searchResult "LEFT JOIN WWS.WKE_WARENAUSGKOSTENERF ON WKE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WKPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WKP_ERSTELLUSER" "~"
if joinKuerzel = BMUWKP
searchResult := searchResult "LEFT JOIN WWS.WKP_WWSKONTENTABPOS ON WKP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WKTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WKT_ERSTELLUSER" "~"
if joinKuerzel = BMUWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WLZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WLZ_ERSTELLUSER" "~"
if joinKuerzel = BMUWLZ
searchResult := searchResult "LEFT JOIN WWS.WLZ_WESLAJ_ZUORDNUNG ON WLZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WOHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WOH_ERSTELLUSER" "~"
if joinKuerzel = BMUWOH
searchResult := searchResult "LEFT JOIN BUERO.WOH_WOHNUNG ON WOH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WPABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WPA_ERSTELLUSER" "~"
if joinKuerzel = BMUWPA
searchResult := searchResult "LEFT JOIN WEB.WPA_WEBPARAMS ON WPA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WPZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WPZ_ERSTELLUSER" "~"
if joinKuerzel = BMUWPZ
searchResult := searchResult "LEFT JOIN WWS.WPZ_WESLPO_ZUORDNUNG ON WPZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WSNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WSN_ERSTELLUSER" "~"
if joinKuerzel = BMUWSN
searchResult := searchResult "LEFT JOIN WWS.WSN_WWSSEQUENZNUMMERN ON WSN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WVTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WVT_ERSTELLUSER" "~"
if joinKuerzel = BMUWVT
searchResult := searchResult "LEFT JOIN WWS.WVT_WARTUNGSVERTRAG ON WVT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WWBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WWB_ERSTELLUSER" "~"
if joinKuerzel = BMUWWB
searchResult := searchResult "LEFT JOIN WWS.WWB_WWSBERICHTE ON WWB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZAS_ERSTELLUSER" "~"
if joinKuerzel = BMUZAS
searchResult := searchResult "LEFT JOIN BUERO.ZAS_ZAHLSCHEIN ON ZAS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZAWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZAW_ERSTELLUSER" "~"
if joinKuerzel = BMUZAW
searchResult := searchResult "LEFT JOIN BUERO.ZAW_ZEITAUSWERTUNG ON ZAW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZDKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZDK_ERSTELLUSER" "~"
if joinKuerzel = BMUZDK
searchResult := searchResult "LEFT JOIN BMD.ZDK_ZEILENDEFKOPF ON ZDK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZESBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZES_ERSTELLUSER" "~"
if joinKuerzel = BMUZES
searchResult := searchResult "LEFT JOIN FIBU.ZES_ZESSIONSTEXT ON ZES_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZIABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZIA_ERSTELLUSER" "~"
if joinKuerzel = BMUZIA
searchResult := searchResult "LEFT JOIN FIBU.ZIA_KONTENZINSAB ON ZIA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZIDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZID_ERSTELLUSER" "~"
if joinKuerzel = BMUZID
searchResult := searchResult "LEFT JOIN BMD.ZID_ZEILENDEF ON ZID_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZSGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZSG_ERSTELLUSER" "~"
if joinKuerzel = BMUZSG
searchResult := searchResult "LEFT JOIN BUERO.ZSG_ZEITSALDENGRENZWERT ON ZSG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZTGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZTG_ERSTELLUSER" "~"
if joinKuerzel = BMUZTG
searchResult := searchResult "LEFT JOIN HRS.ZTG_ZAHLTAG ON ZTG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZTSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZTS_ERSTELLUSER" "~"
if joinKuerzel = BMUZTS
searchResult := searchResult "LEFT JOIN BUERO.ZTS_ZEITTAGESSUMMEN ON ZTS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ERZADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = ERZ_LIEFEREMPF_ADRESSLFDNR" "~"
if joinKuerzel = ADRERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_LIEFEREMPF_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = LFSADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = LFS_LIEFEMPF_ADRESSLFDNR" "~"
if joinKuerzel = ADRLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_LIEFEMPF_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = VSTADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = VST_ADRESSLFDNR" "~"
if joinKuerzel = ADRVST
searchResult := searchResult "LEFT JOIN WWS.VST_VERKAUFSSTATISTIK ON VST_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = YBUADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = YBU_ADR_ADRESSLFDNR" "~"
if joinKuerzel = ADRYBU
searchResult := searchResult "LEFT JOIN FIBU.YBU_AUTOBUCHUNG ON YBU_ADR_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = BPKADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = BPK_LIEFEREMPF_ADRESSLFDNR" "~"
if joinKuerzel = ADRBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_LIEFEREMPF_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = EFKADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = EFK_RECHEMPF_ADRESSLFDNR" "~"
if joinKuerzel = ADREFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_RECHEMPF_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = WADADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = WAD_RECHEMPF_ADRESSLFDNR" "~"
if joinKuerzel = ADRWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_RECHEMPF_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = VSTADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = VST_AUFTRGEB_ADRESSLFDNR" "~"
if joinKuerzel = ADRVST
searchResult := searchResult "LEFT JOIN WWS.VST_VERKAUFSSTATISTIK ON VST_AUFTRGEB_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = WADADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = WAD_RECHLEG_ADRESSLFDNR" "~"
if joinKuerzel = ADRWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_RECHLEG_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = AWEAFS
searchResult := searchResult "LEFT JOIN AFS_AUFGABENSTATUS.AFS_AUFGABENSTATUS ON AFS_AUFGABENSTATUSNR = AWE_AUG_STATUS" "~"
if joinKuerzel = AFSAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_AUG_STATUS = AFS_AUFGABENSTATUSNR" "~"
if joinKuerzel = TSRAGM
searchResult := searchResult "LEFT JOIN AGM_AG_PERSONZU.AGM_AG_PERSONZU ON AGM_PPSKAPAID = TSR_PPSKAPAID AND AGM_AGPERSONZULFDNR = TSR_AGPERSONZULFDNR" "~"
if joinKuerzel = AGMTSR
searchResult := searchResult "LEFT JOIN BUERO.TSR_SERIENTERMIN ON TSR_PPSKAPAID = AGM_PPSKAPAID AND TSR_AGPERSONZULFDNR = AGM_AGPERSONZULFDNR" "~"
if joinKuerzel = ACTAGU
searchResult := searchResult "LEFT JOIN BIL.AGU_ANLAGEGUT ON AGU_ANLAGEGUTID = ACT_ANLAGEGUTID" "~"
if joinKuerzel = AGUACT
searchResult := searchResult "LEFT JOIN BIL.ACT_ANLAGEGUT_BERECH_TMP ON ACT_ANLAGEGUTID = AGU_ANLAGEGUTID" "~"
if joinKuerzel = ARZAGU
searchResult := searchResult "LEFT JOIN BIL.AGU_ANLAGEGUT ON AGU_ANLAGEGUTID = ARZ_ANLAGEGUTID" "~"
if joinKuerzel = AGUARZ
searchResult := searchResult "LEFT JOIN BIL.ARZ_ANLAGEGUTGRUPPEZU ON ARZ_ANLAGEGUTID = AGU_ANLAGEGUTID" "~"
if joinKuerzel = AVZAGU
searchResult := searchResult "LEFT JOIN AGU_ANLAGEGUT.AGU_ANLAGEGUT ON AGU_ANLAGEGUTID = AVZ_ANLAGEGUTID" "~"
if joinKuerzel = AGUAVZ
searchResult := searchResult "LEFT JOIN FIBU.AVZ_ANBUFIBZU ON AVZ_ANLAGEGUTID = AGU_ANLAGEGUTID" "~"
if joinKuerzel = MEDAGU
searchResult := searchResult "LEFT JOIN AGU_ANLAGEGUT.AGU_ANLAGEGUT ON AGU_ANLAGEGUTID = MED_ANLAGEGUTID" "~"
if joinKuerzel = AGUMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_ANLAGEGUTID = AGU_ANLAGEGUTID" "~"
if joinKuerzel = BOPAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = BOP_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = EPSAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = EPS_AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_AID_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = IVPAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = IVP_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDIVP
searchResult := searchResult "LEFT JOIN WWS.IVP_INVENTURPOSITION ON IVP_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = KAPAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = KAP_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = LKOAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = LKO_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDLKO
searchResult := searchResult "LEFT JOIN WWS.LKO_LAGERKONTO ON LKO_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = RNPAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = RNP_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = TLAAID
searchResult := searchResult "LEFT JOIN AID_ARTIKELIDENTIFIKATION.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = TLA_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDTLA
searchResult := searchResult "LEFT JOIN PRODUKTION.TLA_BT_LOGISTIK_ANGEFORDERT ON TLA_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = TLEAID
searchResult := searchResult "LEFT JOIN AID_ARTIKELIDENTIFIKATION.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = TLE_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDTLE
searchResult := searchResult "LEFT JOIN PRODUKTION.TLE_BT_LOGISTIK_EMPFANGEN ON TLE_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = AKSAKK
searchResult := searchResult "LEFT JOIN BUERO.AKK_AKTIONSTATUSKOPF ON AKK_AKKID = AKS_AKKID" "~"
if joinKuerzel = AKKAKS
searchResult := searchResult "LEFT JOIN BUERO.AKS_AKTIONSTATUS ON AKS_AKKID = AKK_AKKID" "~"
if joinKuerzel = WSKAKT
searchResult := searchResult "LEFT JOIN WWS.AKT_AKTIONSSTAMM ON AKT_FIRMENNR = WSK_AKT_FIRMENNR AND AKT_AKTIONSSTAMMNR = WSK_AKTIONSSTAMMNR" "~"
if joinKuerzel = AKTWSK
searchResult := searchResult "LEFT JOIN WWS.WSK_WWSKORESTATISTIK ON WSK_AKT_FIRMENNR = AKT_FIRMENNR AND WSK_AKTIONSSTAMMNR = AKT_AKTIONSSTAMMNR" "~"
if joinKuerzel = KOPALN
searchResult := searchResult "LEFT JOIN PRODUKTION.ALN_ALIBINUMMERN ON ALN_ALNID = KOP_ALNID" "~"
if joinKuerzel = ALNKOP
searchResult := searchResult "LEFT JOIN PRODUKTION.KOP_KOMMISSIONIERUNG_POS ON KOP_ALNID = ALN_ALNID" "~"
if joinKuerzel = LMSAMZ
searchResult := searchResult "LEFT JOIN AMZ_AUSDRUCKMODELLZU.AMZ_AUSDRUCKMODELLZU ON AMZ_FIRMENNR = LMS_AUSDRUCKMODELLFIRMENNR AND AMZ_AUSDRUCKMODELLZULFDNR = LMS_AUSDRUCKMODELLNR" "~"
if joinKuerzel = AMZLMS
searchResult := searchResult "LEFT JOIN WWS.LMS_LIEFERMAHNUNG ON LMS_AUSDRUCKMODELLFIRMENNR = AMZ_FIRMENNR AND LMS_AUSDRUCKMODELLNR = AMZ_AUSDRUCKMODELLZULFDNR" "~"
if joinKuerzel = ADRANR
searchResult := searchResult "LEFT JOIN BUERO.ANR_ANREDE ON ANR_ANREDENR = ADR_ANREDENR" "~"
if joinKuerzel = ANRADR
searchResult := searchResult "LEFT JOIN BUERO.ADR_ADRESSE ON ADR_ANREDENR = ANR_ANREDENR" "~"
if joinKuerzel = ALGANS
searchResult := searchResult "LEFT JOIN BIL.ANS_ANLAGENSCHEMA ON ANS_ANLSCHEMAID = ALG_ANLSCHEMAID" "~"
if joinKuerzel = ANSALG
searchResult := searchResult "LEFT JOIN BIL.ALG_ANLAGENGRUPPE ON ALG_ANLSCHEMAID = ANS_ANLSCHEMAID" "~"
if joinKuerzel = PBAAPA
searchResult := searchResult "LEFT JOIN PRODUKTION.APA_ARBEITSPLAN ON APA_AP_ID = PBA_AP_ID" "~"
if joinKuerzel = APAPBA
searchResult := searchResult "LEFT JOIN PRODUKTION.PBA_PLAN_BAS_ALT ON PBA_AP_ID = APA_AP_ID" "~"
if joinKuerzel = AQBAPG
searchResult := searchResult "LEFT JOIN WWS.APG_ARAPRODUKTGRUPPEN ON APG_FIRMENNR = AQB_APG_FIRMENNR AND APG_PRODUKTGRPNR = AQB_APG_PRODUKTGRPNR" "~"
if joinKuerzel = APGAQB
searchResult := searchResult "LEFT JOIN WWS.AQB_ARAQUOTENBLATT ON AQB_APG_FIRMENNR = APG_FIRMENNR AND AQB_APG_PRODUKTGRPNR = APG_PRODUKTGRPNR" "~"
if joinKuerzel = AAZARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = AAZ_ARL_FIRMENNR AND ARL_ARTIKELNR = AAZ_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLAAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.AAZ_AUFARBEITARTIKELZU ON AAZ_ARL_FIRMENNR = ARL_FIRMENNR AND AAZ_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = AOZARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = AOZ_ARL_FIRMENNR AND ARL_ARTIKELNR = AOZ_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLAOZ
searchResult := searchResult "LEFT JOIN WWS.AOZ_ARTIKELDOKUMENTZU ON AOZ_ARL_FIRMENNR = ARL_FIRMENNR AND AOZ_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = ATAARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = ATA_FIRMENNR AND ARL_ARTIKELNR = ATA_ARTIKELNR" "~"
if joinKuerzel = ARLATA
searchResult := searchResult "LEFT JOIN WWS.ATA_ARTIKELTEXTARCHIV ON ATA_FIRMENNR = ARL_FIRMENNR AND ATA_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = AULARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = AUL_FIRMENNR AND ARL_ARTIKELNR = AUL_VON_ARTIKELNR" "~"
if joinKuerzel = ARLAUL
searchResult := searchResult "LEFT JOIN WWS.AUL_ARTIKELUMSTELLLISTE ON AUL_FIRMENNR = ARL_FIRMENNR AND AUL_VON_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = EARARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = EAR_FIRMENNR AND ARL_ARTIKELNR = EAR_ARTIKELNR" "~"
if joinKuerzel = ARLEAR
searchResult := searchResult "LEFT JOIN WWS.EAR_EXTARTIKELWERTE ON EAR_FIRMENNR = ARL_FIRMENNR AND EAR_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = ESDARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = ESD_ARL_FIRMENNR AND ARL_ARTIKELNR = ESD_ARTIKELNR" "~"
if joinKuerzel = ARLESD
searchResult := searchResult "LEFT JOIN WWS.ESD_ERFASSUNGSERVICEDATEN ON ESD_ARL_FIRMENNR = ARL_FIRMENNR AND ESD_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = EVTARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = EVT_ARL_FIRMENNR AND ARL_ARTIKELNR = EVT_ARTIKELNR" "~"
if joinKuerzel = ARLEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_ARL_FIRMENNR = ARL_FIRMENNR AND EVT_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = GSFARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = GSF_ARL_FIRMENNR AND ARL_ARTIKELNR = GSF_ARTIKELNR" "~"
if joinKuerzel = ARLGSF
searchResult := searchResult "LEFT JOIN WWS.GSF_GUTSCHEINFILIALDATEN ON GSF_ARL_FIRMENNR = ARL_FIRMENNR AND GSF_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = KARARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = KAR_ARL_FIRMENNR AND ARL_ARTIKELNR = KAR_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLKAR
searchResult := searchResult "LEFT JOIN WWS.KAR_KUNDENARTIKEL ON KAR_ARL_FIRMENNR = ARL_FIRMENNR AND KAR_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = KOSARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = KOS_ARL_FIRMENNR AND ARL_ARTIKELNR = KOS_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLKOS
searchResult := searchResult "LEFT JOIN ZLE.KOS_KOSTENFAKTOR ON KOS_ARL_FIRMENNR = ARL_FIRMENNR AND KOS_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = PALARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = PAL_ARL_FIRMENNR AND ARL_ARTIKELNR = PAL_ARL_ARTIKELNR2" "~"
if joinKuerzel = ARLPAL
searchResult := searchResult "LEFT JOIN ZLE.PAL_PAUSCHALE ON PAL_ARL_FIRMENNR = ARL_FIRMENNR AND PAL_ARL_ARTIKELNR2 = ARL_ARTIKELNR" "~"
if joinKuerzel = PCPARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = PCP_ARL_FIRMENNR AND ARL_ARTIKELNR = PCP_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLPCP
searchResult := searchResult "LEFT JOIN PRODUKTION.PCP_PPS_PRODUKT ON PCP_ARL_FIRMENNR = ARL_FIRMENNR AND PCP_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = TARARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = TAR_ARL_FIRMENNR AND ARL_ARTIKELNR = TAR_ARTIKELNR" "~"
if joinKuerzel = ARLTAR
searchResult := searchResult "LEFT JOIN BUERO.TAR_TERMINARTIKELZU ON TAR_ARL_FIRMENNR = ARL_FIRMENNR AND TAR_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = WBWAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = WBW_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDWBW
searchResult := searchResult "LEFT JOIN WWS.WBW_WWSBUDGETWERTE ON WBW_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = AAZAID
searchResult := searchResult "LEFT JOIN AID_ARTIKELIDENTIFIKATION.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = AAZ_PROD_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDAAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.AAZ_AUFARBEITARTIKELZU ON AAZ_PROD_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = WFNAKK
searchResult := searchResult "LEFT JOIN BUERO.AKK_AKTIONSTATUSKOPF ON AKK_AKKID = WFN_AKKID" "~"
if joinKuerzel = AKKWFN
searchResult := searchResult "LEFT JOIN BUERO.WFN_WORKFLOWACTION ON WFN_AKKID = AKK_AKKID" "~"
if joinKuerzel = PTZAKT
searchResult := searchResult "LEFT JOIN WWS.AKT_AKTIONSSTAMM ON AKT_FIRMENNR = PTZ_AKT_FIRMENNR AND AKT_AKTIONSSTAMMNR = PTZ_AKT_AKTIONSSTAMMNR" "~"
if joinKuerzel = AKTPTZ
searchResult := searchResult "LEFT JOIN WWS.PTZ_PREISTABZU ON PTZ_AKT_FIRMENNR = AKT_FIRMENNR AND PTZ_AKT_AKTIONSSTAMMNR = AKT_AKTIONSSTAMMNR" "~"
if joinKuerzel = VSWAKT
searchResult := searchResult "LEFT JOIN WWS.AKT_AKTIONSSTAMM ON AKT_FIRMENNR = VSW_AKT_FIRMENNR AND AKT_AKTIONSSTAMMNR = VSW_AKTIONSSTAMMNR" "~"
if joinKuerzel = AKTVSW
searchResult := searchResult "LEFT JOIN WWS.VSW_VERTRETERSTATISTIK ON VSW_AKT_FIRMENNR = AKT_FIRMENNR AND VSW_AKTIONSSTAMMNR = AKT_AKTIONSSTAMMNR" "~"
if joinKuerzel = BPKAPA
searchResult := searchResult "LEFT JOIN APA_ARBEITSPLAN.APA_ARBEITSPLAN ON APA_AP_ID = BPK_AP_ID" "~"
if joinKuerzel = APABPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_AP_ID = APA_AP_ID" "~"
if joinKuerzel = AGLAPA
searchResult := searchResult "LEFT JOIN PRODUKTION.APA_ARBEITSPLAN ON APA_AP_ID = AGL_AP_PARENT_ID" "~"
if joinKuerzel = APAAGL
searchResult := searchResult "LEFT JOIN PRODUKTION.AGL_ARBEITSGANGZU ON AGL_AP_PARENT_ID = APA_AP_ID" "~"
if joinKuerzel = APZAPG
searchResult := searchResult "LEFT JOIN WWS.APG_ARAPRODUKTGRUPPEN ON APG_FIRMENNR = APZ_FIRMENNR AND APG_PRODUKTGRPNR = APZ_PRODUKTGRPNR" "~"
if joinKuerzel = APGAPZ
searchResult := searchResult "LEFT JOIN WWS.APZ_ARAPACKSTOFFTABZU ON APZ_FIRMENNR = APG_FIRMENNR AND APZ_PRODUKTGRPNR = APG_PRODUKTGRPNR" "~"
if joinKuerzel = AGSAPS
searchResult := searchResult "LEFT JOIN WWS.APS_ARBEITSPLAN_STAMM ON APS_APSTAMM_ID = AGS_APSTAMM_ID" "~"
if joinKuerzel = APSAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_APSTAMM_ID = APS_APSTAMM_ID" "~"
if joinKuerzel = AIVAPS
searchResult := searchResult "LEFT JOIN WWS.APS_ARBEITSPLAN_STAMM ON APS_APSTAMM_ID = AIV_APS_APSTAMM_ID" "~"
if joinKuerzel = APSAIV
searchResult := searchResult "LEFT JOIN WWS.AIV_INTERVALL_ARBEITSPLAN ON AIV_APS_APSTAMM_ID = APS_APSTAMM_ID" "~"
if joinKuerzel = KOFARK
searchResult := searchResult "LEFT JOIN ARK_ARTIKELKLASSE.ARK_ARTIKELKLASSE ON ARK_FIRMENNR = KOF_ARK_FIRMENNR AND ARK_ARTIKELKLASSE = KOF_ARTIKELKLASSE" "~"
if joinKuerzel = ARKKOF
searchResult := searchResult "LEFT JOIN PRODUKTION.KOF_KOKFILTER ON KOF_ARK_FIRMENNR = ARK_FIRMENNR AND KOF_ARTIKELKLASSE = ARK_ARTIKELKLASSE" "~"
if joinKuerzel = AFTARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = AFT_ARL_FIRMENNR AND ARL_ARTIKELNR = AFT_ARTIKELNR" "~"
if joinKuerzel = ARLAFT
searchResult := searchResult "LEFT JOIN ZLE.AFT_AUFBEREITUNG ON AFT_ARL_FIRMENNR = ARL_FIRMENNR AND AFT_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = DAZARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = DAZ_FIRMENNR AND ARL_ARTIKELNR = DAZ_ARTIKELNR" "~"
if joinKuerzel = ARLDAZ
searchResult := searchResult "LEFT JOIN DATANORM.DAZ_DATANORM_ARTIKELZU ON DAZ_FIRMENNR = ARL_FIRMENNR AND DAZ_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = LATARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = LAT_ARL_FIRMENNR AND ARL_ARTIKELNR = LAT_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLLAT
searchResult := searchResult "LEFT JOIN WWS.LAT_LIEFERANTENARTIKEL ON LAT_ARL_FIRMENNR = ARL_FIRMENNR AND LAT_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = LLDARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = LLD_ARL_FIRMENNR AND ARL_ARTIKELNR = LLD_ARTIKELNR" "~"
if joinKuerzel = ARLLLD
searchResult := searchResult "LEFT JOIN ZLE.LLD_LEALOHNDATEN ON LLD_ARL_FIRMENNR = ARL_FIRMENNR AND LLD_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = LMBARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = LMB_ARL_FIRMENNR AND ARL_ARTIKELNR = LMB_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLLMB
searchResult := searchResult "LEFT JOIN WWS.LMB_LAGERMINMAXBESTAND ON LMB_ARL_FIRMENNR = ARL_FIRMENNR AND LMB_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = LUPARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = LUP_ARL_FIRMENNR AND ARL_ARTIKELNR = LUP_ART_NR" "~"
if joinKuerzel = ARLLUP
searchResult := searchResult "LEFT JOIN PRODUKTION.LUP_LAGER_UMS_POS ON LUP_ARL_FIRMENNR = ARL_FIRMENNR AND LUP_ART_NR = ARL_ARTIKELNR" "~"
if joinKuerzel = MDEARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = MDE_HAUPTFIRMENNR AND ARL_ARTIKELNR = MDE_ARTIKELNR" "~"
if joinKuerzel = ARLMDE
searchResult := searchResult "LEFT JOIN WWS.MDE_MOBILEDATENERFASSUNG ON MDE_HAUPTFIRMENNR = ARL_FIRMENNR AND MDE_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = RAPARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = RAP_ARL_FIRMENNR AND ARL_ARTIKELNR = RAP_ARTIKELNR" "~"
if joinKuerzel = ARLRAP
searchResult := searchResult "LEFT JOIN WWS.RAP_RABATTTABPOS ON RAP_ARL_FIRMENNR = ARL_FIRMENNR AND RAP_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = REWARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = REW_ARL_FIRMENNR AND ARL_ARTIKELNR = REW_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLREW
searchResult := searchResult "LEFT JOIN ZLE.REW_REGELWERK ON REW_ARL_FIRMENNR = ARL_FIRMENNR AND REW_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = REZARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = REZ_ARL_FIRMENNR AND ARL_ARTIKELNR = REZ_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLREZ
searchResult := searchResult "LEFT JOIN ZLE.REZ_REGELWERKZU ON REZ_ARL_FIRMENNR = ARL_FIRMENNR AND REZ_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = APSAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_AG_ID = APS_AG_END_ID" "~"
if joinKuerzel = AGSAPS
searchResult := searchResult "LEFT JOIN WWS.APS_ARBEITSPLAN_STAMM ON APS_AG_END_ID = AGS_AG_ID" "~"
if joinKuerzel = ACZAGU
searchResult := searchResult "LEFT JOIN BIL.AGU_ANLAGEGUT ON AGU_ANLAGEGUTID = ACZ_ANLAGEGUTID" "~"
if joinKuerzel = AGUACZ
searchResult := searchResult "LEFT JOIN BIL.ACZ_ANLAGEGUTSACHKONTOZU ON ACZ_ANLAGEGUTID = AGU_ANLAGEGUTID" "~"
if joinKuerzel = AIDAI1
searchResult := searchResult "LEFT JOIN WWS.AI1_VA_ARTIKELIDENT ON AI1_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = AI1AID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = AI1_ARTIKELIDLFDNR" "~"
if joinKuerzel = ACSAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = ACS_AID_ID" "~"
if joinKuerzel = AIDACS
searchResult := searchResult "LEFT JOIN WWS.ACS_TEST ON ACS_AID_ID = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = LAJAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = LAJ_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = LUPAID
searchResult := searchResult "LEFT JOIN AID_ARTIKELIDENTIFIKATION.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = LUP_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDLUP
searchResult := searchResult "LEFT JOIN PRODUKTION.LUP_LAGER_UMS_POS ON LUP_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIVAIV
searchResult := searchResult "LEFT JOIN WWS.AIV_INTERVALL_ARBEITSPLAN ON AIV_AIV_ID = AIV_VORLAGE_ID" "~"
if joinKuerzel = AIVAIV
searchResult := searchResult "LEFT JOIN WWS.AIV_INTERVALL_ARBEITSPLAN ON AIV_VORLAGE_ID = AIV_AIV_ID" "~"
if joinKuerzel = VSTAKT
searchResult := searchResult "LEFT JOIN WWS.AKT_AKTIONSSTAMM ON AKT_FIRMENNR = VST_AKT_FIRMENNR AND AKT_AKTIONSSTAMMNR = VST_AKTIONSSTAMMNR" "~"
if joinKuerzel = AKTVST
searchResult := searchResult "LEFT JOIN WWS.VST_VERKAUFSSTATISTIK ON VST_AKT_FIRMENNR = AKT_FIRMENNR AND VST_AKTIONSSTAMMNR = AKT_AKTIONSSTAMMNR" "~"
if joinKuerzel = ELVALN
searchResult := searchResult "LEFT JOIN ALN_ALIBINUMMERN.ALN_ALIBINUMMERN ON ALN_ALNID = ELV_ALNID" "~"
if joinKuerzel = ALNELV
searchResult := searchResult "LEFT JOIN WWS.ELV_ERFASSPOSLIEFERVOR ON ELV_ALNID = ALN_ALNID" "~"
if joinKuerzel = ALZALS
searchResult := searchResult "LEFT JOIN WWS.ALS_ALLERGEN ON ALS_ALLERGENNR = ALZ_ALLERGENNR AND ALS_ALSMODUS = ALZ_ALSMODUS AND ALS_FIRMENNR = ALZ_ALS_FIRMENNR" "~"
if joinKuerzel = ALSALZ
searchResult := searchResult "LEFT JOIN WWS.ALZ_ALLERGENDATEN ON ALZ_ALLERGENNR = ALS_ALLERGENNR AND ALZ_ALSMODUS = ALS_ALSMODUS AND ALZ_ALS_FIRMENNR = ALS_FIRMENNR" "~"
if joinKuerzel = AWDANT
searchResult := searchResult "LEFT JOIN BUERO.ANT_ANTWORT ON ANT_ANTWORTID = AWD_ANTWORTID" "~"
if joinKuerzel = ANTAWD
searchResult := searchResult "LEFT JOIN BUERO.AWD_ANTWORTDETAIL ON AWD_ANTWORTID = ANT_ANTWORTID" "~"
if joinKuerzel = AGLAPA
searchResult := searchResult "LEFT JOIN PRODUKTION.APA_ARBEITSPLAN ON APA_AP_ID = AGL_AP_ID" "~"
if joinKuerzel = APAAGL
searchResult := searchResult "LEFT JOIN PRODUKTION.AGL_ARBEITSGANGZU ON AGL_AP_ID = APA_AP_ID" "~"
if joinKuerzel = BPPAPA
searchResult := searchResult "LEFT JOIN APA_ARBEITSPLAN.APA_ARBEITSPLAN ON APA_AP_ID = BPP_AP_ID" "~"
if joinKuerzel = APABPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_AP_ID = APA_AP_ID" "~"
if joinKuerzel = PROAPA
searchResult := searchResult "LEFT JOIN APA_ARBEITSPLAN.APA_ARBEITSPLAN ON APA_AP_ID = PRO_APA_AP_ID" "~"
if joinKuerzel = APAPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_APA_AP_ID = APA_AP_ID" "~"
if joinKuerzel = AAPAPG
searchResult := searchResult "LEFT JOIN WWS.APG_ARAPRODUKTGRUPPEN ON APG_FIRMENNR = AAP_APG_FIRMENNR AND APG_PRODUKTGRPNR = AAP_APG_PRODGRPNR" "~"
if joinKuerzel = APGAAP
searchResult := searchResult "LEFT JOIN WWS.AAP_ARAPOSITION ON AAP_APG_FIRMENNR = APG_FIRMENNR AND AAP_APG_PRODGRPNR = APG_PRODUKTGRPNR" "~"
if joinKuerzel = SLKAPS
searchResult := searchResult "LEFT JOIN WWS.APS_ARBEITSPLAN_STAMM ON APS_APSTAMM_ID = SLK_APSTAMM_ID" "~"
if joinKuerzel = APSSLK
searchResult := searchResult "LEFT JOIN WWS.SLK_STUECKLISTENKOPF ON SLK_APSTAMM_ID = APS_APSTAMM_ID" "~"
if joinKuerzel = AMZARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = AMZ_ARL_FIRMENNR AND ARL_ARTIKELNR = AMZ_ARTIKELNR" "~"
if joinKuerzel = ARLAMZ
searchResult := searchResult "LEFT JOIN BUERO.AMZ_AUSDRUCKMODELLZU ON AMZ_ARL_FIRMENNR = ARL_FIRMENNR AND AMZ_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = ANOARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = ANO_ARL_FIRMENNR AND ARL_ARTIKELNR = ANO_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLANO
searchResult := searchResult "LEFT JOIN WWS.ANO_ARTIKELNORM ON ANO_ARL_FIRMENNR = ARL_FIRMENNR AND ANO_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = ARRARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = ARR_ARL_FIRMENNR AND ARL_ARTIKELNR = ARR_ART_NR" "~"
if joinKuerzel = ARLARR
searchResult := searchResult "LEFT JOIN PRODUKTION.ARR_PROD_ARTIKEL_REPORT ON ARR_ARL_FIRMENNR = ARL_FIRMENNR AND ARR_ART_NR = ARL_ARTIKELNR" "~"
if joinKuerzel = ASTARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = AST_HAUPT_FIRMENNR AND ARL_ARTIKELNR = AST_ARTIKELNR" "~"
if joinKuerzel = ARLAST
searchResult := searchResult "LEFT JOIN WWS.AST_ARTIKELSTATISTIK ON AST_HAUPT_FIRMENNR = ARL_FIRMENNR AND AST_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = EKKARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = EKK_ARL_FIRMENNR AND ARL_ARTIKELNR = EKK_ARTIKELNR" "~"
if joinKuerzel = ARLEKK
searchResult := searchResult "LEFT JOIN WWS.EKK_EXTERNE_KUNDENKARTEI ON EKK_ARL_FIRMENNR = ARL_FIRMENNR AND EKK_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = EPSARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = EPS_ARL_FIRMENNR AND ARL_ARTIKELNR = EPS_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_ARL_FIRMENNR = ARL_FIRMENNR AND EPS_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = ARUARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = ARU_ARL_FIRMENNR AND ARL_ARTIKELNR = ARU_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLARU
searchResult := searchResult "LEFT JOIN ZLE.ARU_ARTIKELUMLAGE ON ARU_ARL_FIRMENNR = ARL_FIRMENNR AND ARU_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = AVEARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = AVE_ARL_FIRMENNR AND ARL_ARTIKELNR = AVE_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ARL_FIRMENNR = ARL_FIRMENNR AND AVE_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = AWSARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = AWS_ARL_FIRMENNR AND ARL_ARTIKELNR = AWS_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_ARL_FIRMENNR = ARL_FIRMENNR AND AWS_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = AZUARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = AZU_FIRMENNR AND ARL_ARTIKELNR = AZU_ARTIKELNR" "~"
if joinKuerzel = ARLAZU
searchResult := searchResult "LEFT JOIN WWS.AZU_STATISTIKARTIKELZU ON AZU_FIRMENNR = ARL_FIRMENNR AND AZU_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = BPPARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = BPP_ARL_FIRMENNR AND ARL_ARTIKELNR = BPP_ARTIKELNR" "~"
if joinKuerzel = ARLBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_ARL_FIRMENNR = ARL_FIRMENNR AND BPP_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = BVOARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = BVO_HAUPTFIRMENNR AND ARL_ARTIKELNR = BVO_ARTIKELNR" "~"
if joinKuerzel = ARLBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_HAUPTFIRMENNR = ARL_FIRMENNR AND BVO_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = EDVARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = EDV_HAUPTFIRMENNR AND ARL_ARTIKELNR = EDV_ARTIKELNR" "~"
if joinKuerzel = ARLEDV
searchResult := searchResult "LEFT JOIN WWS.EDV_ERFASSRABATTDETAILVOR ON EDV_HAUPTFIRMENNR = ARL_FIRMENNR AND EDV_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = EPVARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = EPV_ARL_FIRMENNR AND ARL_ARTIKELNR = EPV_ARTIKELNR" "~"
if joinKuerzel = ARLEPV
searchResult := searchResult "LEFT JOIN WWS.EPV_ERFASSPOSRECHVOR ON EPV_ARL_FIRMENNR = ARL_FIRMENNR AND EPV_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = ERDARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = ERD_HAUPTFIRMENNR AND ARL_ARTIKELNR = ERD_ARTIKELNR" "~"
if joinKuerzel = ARLERD
searchResult := searchResult "LEFT JOIN WWS.ERD_ERFASSRABATTDETAIL ON ERD_HAUPTFIRMENNR = ARL_FIRMENNR AND ERD_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = ERPARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = ERP_ARL_FIRMENNR AND ARL_ARTIKELNR = ERP_ARTIKELNR" "~"
if joinKuerzel = ARLERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_ARL_FIRMENNR = ARL_FIRMENNR AND ERP_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = EVNARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = EVN_ARL_FIRMENNR AND ARL_ARTIKELNR = EVN_ARTIKELNR" "~"
if joinKuerzel = ARLEVN
searchResult := searchResult "LEFT JOIN BUERO.EVN_TEILNEHMER ON EVN_ARL_FIRMENNR = ARL_FIRMENNR AND EVN_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = GEGARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = GEG_ARL_FIRMENNR AND ARL_ARTIKELNR = GEG_ARTIKELNR" "~"
if joinKuerzel = ARLGEG
searchResult := searchResult "LEFT JOIN ZLE.GEG_GEGENSTANDSWERTE ON GEG_ARL_FIRMENNR = ARL_FIRMENNR AND GEG_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = HPTARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = HPT_ARL_FIRMENNR AND ARL_ARTIKELNR = HPT_ARTIKELNR" "~"
if joinKuerzel = ARLHPT
searchResult := searchResult "LEFT JOIN ZLE.HPT_HONORARPOSTEXTE ON HPT_ARL_FIRMENNR = ARL_FIRMENNR AND HPT_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = IAPARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = IAP_FIRMENNR AND ARL_ARTIKELNR = IAP_ARTIKELNR" "~"
if joinKuerzel = ARLIAP
searchResult := searchResult "LEFT JOIN WWS.IAP_INVABWTPOS ON IAP_FIRMENNR = ARL_FIRMENNR AND IAP_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = KAPARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = KAP_ARL_FIRMENNR AND ARL_ARTIKELNR = KAP_ARTIKELNR" "~"
if joinKuerzel = ARLKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_ARL_FIRMENNR = ARL_FIRMENNR AND KAP_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = LAJARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = LAJ_ARTIKELFIRMENNR AND ARL_ARTIKELNR = LAJ_ARTIKELNR" "~"
if joinKuerzel = ARLLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_ARTIKELFIRMENNR = ARL_FIRMENNR AND LAJ_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = RVDARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = RVD_ARL_FIRMENNR AND ARL_ARTIKELNR = RVD_ARTIKELNR" "~"
if joinKuerzel = ARLRVD
searchResult := searchResult "LEFT JOIN ZLE.RVD_RECHVORLAGEDETAIL ON RVD_ARL_FIRMENNR = ARL_FIRMENNR AND RVD_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = SLPARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = SLP_ARL_FIRMENNR AND ARL_ARTIKELNR = SLP_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLSLP
searchResult := searchResult "LEFT JOIN WWS.SLP_STUECKLISTENPOSITION ON SLP_ARL_FIRMENNR = ARL_FIRMENNR AND SLP_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = VAKARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = VAK_ARL_FIRMENNR AND ARL_ARTIKELNR = VAK_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLVAK
searchResult := searchResult "LEFT JOIN BUERO.VAK_VERANSTALTUNGSKOPF ON VAK_ARL_FIRMENNR = ARL_FIRMENNR AND VAK_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = VATARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = VAT_ARL_FIRMENNR AND ARL_ARTIKELNR = VAT_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLVAT
searchResult := searchResult "LEFT JOIN BUERO.VAT_TEILNAHME ON VAT_ARL_FIRMENNR = ARL_FIRMENNR AND VAT_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = VJSARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = VJS_HAUPTFIRMENNR AND ARL_ARTIKELNR = VJS_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLVJS
searchResult := searchResult "LEFT JOIN WWS.VJS_VA_VERLEIHJOURNALSUM ON VJS_HAUPTFIRMENNR = ARL_FIRMENNR AND VJS_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = ZTUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZTU_ERSTELLUSER" "~"
if joinKuerzel = BMUZTU
searchResult := searchResult "LEFT JOIN BMD.ZTU_ZEITUMSTELLUNG ON ZTU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZVKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZVK_ERSTELLUSER" "~"
if joinKuerzel = BMUZVK
searchResult := searchResult "LEFT JOIN FIBU.ZVK_ZAHLUNGSVORSCHLAG ON ZVK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZXDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZXD_ERSTELLUSER" "~"
if joinKuerzel = BMUZXD
searchResult := searchResult "LEFT JOIN BUERO.ZXD_ZEITAENDERUNG ON ZXD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PMDFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = PMD_FIRMENNR" "~"
if joinKuerzel = FIRPMD
searchResult := searchResult "LEFT JOIN BUERO.PMD_PAUSENMODELL ON PMD_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SNRVSI
searchResult := searchResult "LEFT JOIN WWS.VSI_VA_SERIENNR_ABGANG ON VSI_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = VSISNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_SERNRID = VSI_SERNRID" "~"
if joinKuerzel = PSMABS
searchResult := searchResult "LEFT JOIN HRS.ABS_ARBEITSSTAETTE ON ABS_ABSID = PSM_ABS_HRSSEQID AND ABS_LOHNJAHR = PSM_PSMJAHR AND ABS_FIRMENNR = PSM_DVFIRMENNR" "~"
if joinKuerzel = ABSPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_ABS_HRSSEQID = ABS_ABSID AND PSM_PSMJAHR = ABS_LOHNJAHR AND PSM_DVFIRMENNR = ABS_FIRMENNR" "~"
if joinKuerzel = ANBABU
searchResult := searchResult "LEFT JOIN BIL.ABU_ANLAGENBUCHHALTUNG ON ABU_FIRMENNR = ANB_FIRMENNR AND ABU_ANBUNR = ANB_ANBUNR" "~"
if joinKuerzel = ABUANB
searchResult := searchResult "LEFT JOIN BIL.ANB_ANBUBUCHBELEGZU ON ANB_FIRMENNR = ABU_FIRMENNR AND ANB_ANBUNR = ABU_ANBUNR" "~"
if joinKuerzel = MKRADO
searchResult := searchResult "LEFT JOIN ADO_AUSDRUCKMODELL.ADO_AUSDRUCKMODELL ON ADO_FIRMENNR = MKR_ADO_FIRMENNR AND ADO_AUSDRUCKMODELLNR = MKR_ADO_AUSDRUCKMODELLNR" "~"
if joinKuerzel = ADOMKR
searchResult := searchResult "LEFT JOIN FIBU.MKR_MAHNKREIS ON MKR_ADO_FIRMENNR = ADO_FIRMENNR AND MKR_ADO_AUSDRUCKMODELLNR = ADO_AUSDRUCKMODELLNR" "~"
if joinKuerzel = BUCADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = BUC_ADR_ADRESSLFDNR" "~"
if joinKuerzel = ADRBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_ADR_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = INSADR
searchResult := searchResult "LEFT JOIN BUERO.ADR_ADRESSE ON ADR_ADRESSLFDNR = INS_ADRESSLFDNR" "~"
if joinKuerzel = ADRINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = KAKADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = KAK_KUNDEN_ADRESSLFDNR" "~"
if joinKuerzel = ADRKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_KUNDEN_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = VSWADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = VSW_ADR_ADRESSLFDNR2" "~"
if joinKuerzel = ADRVSW
searchResult := searchResult "LEFT JOIN WWS.VSW_VERTRETERSTATISTIK ON VSW_ADR_ADRESSLFDNR2 = ADR_ADRESSLFDNR" "~"
if joinKuerzel = WSKADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = WSK_ADR_ADRESSLFDNR" "~"
if joinKuerzel = ADRWSK
searchResult := searchResult "LEFT JOIN WWS.WSK_WWSKORESTATISTIK ON WSK_ADR_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = BONADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = BON_ZRECHEMPF_ADRESSLFDNR" "~"
if joinKuerzel = ADRBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_ZRECHEMPF_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = VSTADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = VST_LIEF_ADRESSLFDNR" "~"
if joinKuerzel = ADRVST
searchResult := searchResult "LEFT JOIN WWS.VST_VERKAUFSSTATISTIK ON VST_LIEF_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = AVZAFB
searchResult := searchResult "LEFT JOIN FIBU.AFB_ANBUFIB ON AFB_ANBUFIBID = AVZ_ANBUFIBID" "~"
if joinKuerzel = AFBAVZ
searchResult := searchResult "LEFT JOIN FIBU.AVZ_ANBUFIBZU ON AVZ_ANBUFIBID = AFB_ANBUFIBID" "~"
if joinKuerzel = WFNAFS
searchResult := searchResult "LEFT JOIN BUERO.AFS_AUFGABENSTATUS ON AFS_AUFGABENSTATUSNR = WFN_AUFGABENSTATUSNR" "~"
if joinKuerzel = AFSWFN
searchResult := searchResult "LEFT JOIN BUERO.WFN_WORKFLOWACTION ON WFN_AUFGABENSTATUSNR = AFS_AUFGABENSTATUSNR" "~"
if joinKuerzel = PCKAGS
searchResult := searchResult "LEFT JOIN AGS_ARBEITSGANG.AGS_ARBEITSGANG ON AGS_AG_ID = PCK_AG_VORLAGE_ID" "~"
if joinKuerzel = AGSPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_AG_VORLAGE_ID = AGS_AG_ID" "~"
if joinKuerzel = VOBAGS
searchResult := searchResult "LEFT JOIN AGS_ARBEITSGANG.AGS_ARBEITSGANG ON AGS_AG_ID = VOB_AGS_STAMMID" "~"
if joinKuerzel = AGSVOB
searchResult := searchResult "LEFT JOIN PRODUKTION.VOB_VORGANGSBENACHR ON VOB_AGS_STAMMID = AGS_AG_ID" "~"
if joinKuerzel = AETAGU
searchResult := searchResult "LEFT JOIN BIL.AGU_ANLAGEGUT ON AGU_ANLAGEGUTID = AET_ANLAGEGUTID" "~"
if joinKuerzel = AGUAET
searchResult := searchResult "LEFT JOIN BIL.AET_ANLAGEGUT_ERFASS_TMP ON AET_ANLAGEGUTID = AGU_ANLAGEGUTID" "~"
if joinKuerzel = AJOAGU
searchResult := searchResult "LEFT JOIN BIL.AGU_ANLAGEGUT ON AGU_ANLAGEGUTID = AJO_ANLAGEGUTID" "~"
if joinKuerzel = AGUAJO
searchResult := searchResult "LEFT JOIN BIL.AJO_ANBUJOURNAL ON AJO_ANLAGEGUTID = AGU_ANLAGEGUTID" "~"
if joinKuerzel = AMUAGU
searchResult := searchResult "LEFT JOIN BIL.AGU_ANLAGEGUT ON AGU_ANLAGEGUTID = AMU_ANLAGEGUTID" "~"
if joinKuerzel = AGUAMU
searchResult := searchResult "LEFT JOIN BIL.AMU_ANLAGEGUT_MDE_ZU ON AMU_ANLAGEGUTID = AGU_ANLAGEGUTID" "~"
if joinKuerzel = BINAIN
searchResult := searchResult "LEFT JOIN BUERO.AIN_AUSSCHREIBUNGINSERTZU ON AIN_AINZUID = BIN_AIN_AINZUID" "~"
if joinKuerzel = AINBIN
searchResult := searchResult "LEFT JOIN BUERO.BIN_BEWERBERINSERATZU ON BIN_AIN_AINZUID = AIN_AINZUID" "~"
if joinKuerzel = ANBAJO
searchResult := searchResult "LEFT JOIN BIL.AJO_ANBUJOURNAL ON AJO_ANBUJOURNALID = ANB_AJO_ANBUJOURNALID AND AJO_ANLAGEGUTID = ANB_AJO_ANLAGEGUTID" "~"
if joinKuerzel = AJOANB
searchResult := searchResult "LEFT JOIN BIL.ANB_ANBUBUCHBELEGZU ON ANB_AJO_ANBUJOURNALID = AJO_ANBUJOURNALID AND ANB_AJO_ANLAGEGUTID = AJO_ANLAGEGUTID" "~"
if joinKuerzel = TLTAID
searchResult := searchResult "LEFT JOIN AID_ARTIKELIDENTIFIKATION.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = TLT_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDTLT
searchResult := searchResult "LEFT JOIN PRODUKTION.TLT_BT_LOGISTIK_TRANSPORT ON TLT_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = TRPAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = TRP_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDTRP
searchResult := searchResult "LEFT JOIN WWS.TRP_TRANSPORTPOSITION ON TRP_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = WRPAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = WRP_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDWRP
searchResult := searchResult "LEFT JOIN WWS.WRP_WWSANKAUFSRNGPOS ON WRP_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = AKRAKA
searchResult := searchResult "LEFT JOIN BUERO.AKA_AVKOSTENARTEN ON AKA_FIRMENNR = AKR_AKA_FIRMENNR AND AKA_KOSTENARTNR = AKR_AKA_KOSTENARTNR" "~"
if joinKuerzel = AKAAKR
searchResult := searchResult "LEFT JOIN BUERO.AKR_AVKOSTENARTENZU ON AKR_AKA_FIRMENNR = AKA_FIRMENNR AND AKR_AKA_KOSTENARTNR = AKA_KOSTENARTNR" "~"
if joinKuerzel = BOPAKT
searchResult := searchResult "LEFT JOIN WWS.AKT_AKTIONSSTAMM ON AKT_FIRMENNR = BOP_AKT_FIRMENNR AND AKT_AKTIONSSTAMMNR = BOP_AKT_AKTIONSSTAMMNR" "~"
if joinKuerzel = AKTBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_AKT_FIRMENNR = AKT_FIRMENNR AND BOP_AKT_AKTIONSSTAMMNR = AKT_AKTIONSSTAMMNR" "~"
if joinKuerzel = MITAMG
searchResult := searchResult "LEFT JOIN BUERO.AMG_GKKABMELDEGRUND ON AMG_ABMELDEGRUNDNR = MIT_ABMELDEGRUNDNR" "~"
if joinKuerzel = AMGMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_ABMELDEGRUNDNR = AMG_ABMELDEGRUNDNR" "~"
if joinKuerzel = ADRANR
searchResult := searchResult "LEFT JOIN BUERO.ANR_ANREDE ON ANR_ANREDENR = ADR_PERSOENLICH_ANREDENR" "~"
if joinKuerzel = ANRADR
searchResult := searchResult "LEFT JOIN BUERO.ADR_ADRESSE ON ADR_PERSOENLICH_ANREDENR = ANR_ANREDENR" "~"
if joinKuerzel = INSANR
searchResult := searchResult "LEFT JOIN BUERO.ANR_ANREDE ON ANR_ANREDENR = INS_ANREDENR" "~"
if joinKuerzel = ANRINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_ANREDENR = ANR_ANREDENR" "~"
if joinKuerzel = APAAPA
searchResult := searchResult "LEFT JOIN PRODUKTION.APA_ARBEITSPLAN ON APA_AP_ID = APA_AP_PARENT_ID" "~"
if joinKuerzel = APAAPA
searchResult := searchResult "LEFT JOIN PRODUKTION.APA_ARBEITSPLAN ON APA_AP_PARENT_ID = APA_AP_ID" "~"
if joinKuerzel = PCKAPA
searchResult := searchResult "LEFT JOIN PRODUKTION.APA_ARBEITSPLAN ON APA_AP_ID = PCK_AP_CHILD_ID" "~"
if joinKuerzel = APAPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_AP_CHILD_ID = APA_AP_ID" "~"
if joinKuerzel = AGUAPC
searchResult := searchResult "LEFT JOIN BIL.APC_ANBUPRODUKTIONSCODES ON APC_PRODUKTIONSCODENR = AGU_PRODUKTIONSCODENR" "~"
if joinKuerzel = APCAGU
searchResult := searchResult "LEFT JOIN BIL.AGU_ANLAGEGUT ON AGU_PRODUKTIONSCODENR = APC_PRODUKTIONSCODENR" "~"
if joinKuerzel = AGZAPS
searchResult := searchResult "LEFT JOIN WWS.APS_ARBEITSPLAN_STAMM ON APS_APSTAMM_ID = AGZ_APSTAMM_ID" "~"
if joinKuerzel = APSAGZ
searchResult := searchResult "LEFT JOIN WWS.AGZ_ARBEITSGANGZU ON AGZ_APSTAMM_ID = APS_APSTAMM_ID" "~"
if joinKuerzel = AGSAPS
searchResult := searchResult "LEFT JOIN WWS.APS_ARBEITSPLAN_STAMM ON APS_APSTAMM_ID = AGS_APSTAMM_CHILD_ID" "~"
if joinKuerzel = APSAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_APSTAMM_CHILD_ID = APS_APSTAMM_ID" "~"
if joinKuerzel = AARARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = AAR_ARL_FIRMENNR_HAUPT AND ARL_ARTIKELNR = AAR_ARL_ARTIKELNR_HAUPT" "~"
if joinKuerzel = ARLAAR
searchResult := searchResult "LEFT JOIN WWS.AAR_ALTERNATIVARTIKEL ON AAR_ARL_FIRMENNR_HAUPT = ARL_FIRMENNR AND AAR_ARL_ARTIKELNR_HAUPT = ARL_ARTIKELNR" "~"
if joinKuerzel = AKUARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = AKU_ARL_FIRMENNR AND ARL_ARTIKELNR = AKU_ARTIKELNR" "~"
if joinKuerzel = ARLAKU
searchResult := searchResult "LEFT JOIN ZLE.AKU_ARTIKELKUNDEZULEA ON AKU_ARL_FIRMENNR = ARL_FIRMENNR AND AKU_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = ALOARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = ALO_FIRMENNR AND ARL_ARTIKELNR = ALO_ARTIKELNR" "~"
if joinKuerzel = ARLALO
searchResult := searchResult "LEFT JOIN HRS.ALO_ARTIKELLOHNARTZU ON ALO_FIRMENNR = ARL_FIRMENNR AND ALO_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = EANARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = EAN_FIRMENNR AND ARL_ARTIKELNR = EAN_ARTIKELNR" "~"
if joinKuerzel = ARLEAN
searchResult := searchResult "LEFT JOIN WWS.EAN_EANNUMMERN ON EAN_FIRMENNR = ARL_FIRMENNR AND EAN_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = HBTARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = HBT_FIRMENNR_PRODUKT AND ARL_ARTIKELNR = HBT_ARTIKELNR_PRODUKT" "~"
if joinKuerzel = ARLHBT
searchResult := searchResult "LEFT JOIN WWS.HBT_HAUPTBESTANDTEIL ON HBT_FIRMENNR_PRODUKT = ARL_FIRMENNR AND HBT_ARTIKELNR_PRODUKT = ARL_ARTIKELNR" "~"
if joinKuerzel = KOMARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = KOM_ARL_FIRMENNR AND ARL_ARTIKELNR = KOM_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLKOM
searchResult := searchResult "LEFT JOIN PRODUKTION.KOM_KOMBI ON KOM_ARL_FIRMENNR = ARL_FIRMENNR AND KOM_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = LAWARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = LAW_ARTIKEL_FIRMENNR AND ARL_ARTIKELNR = LAW_ARTIKELNR" "~"
if joinKuerzel = ARLLAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_ARTIKEL_FIRMENNR = ARL_FIRMENNR AND LAW_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = LPOARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = LPO_ARL_FIRMENNR AND ARL_ARTIKELNR = LPO_ARTIKELNR" "~"
if joinKuerzel = ARLLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_ARL_FIRMENNR = ARL_FIRMENNR AND LPO_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = LVZARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = LVZ_FIRMENNR AND ARL_ARTIKELNR = LVZ_ARTIKELNR" "~"
if joinKuerzel = ARLLVZ
searchResult := searchResult "LEFT JOIN BMDINTERN.LVZ_LIZENZVKPZU ON LVZ_FIRMENNR = ARL_FIRMENNR AND LVZ_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = PLCAID
searchResult := searchResult "LEFT JOIN AID_ARTIKELIDENTIFIKATION.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = PLC_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = AJOAJO
searchResult := searchResult "LEFT JOIN BIL.AJO_ANBUJOURNAL ON AJO_ANBUJOURNALID = AJO_UMBUCH_ANBUJOURNALID AND AJO_ANLAGEGUTID = AJO_UMBUCH_ANLAGEGUTID" "~"
if joinKuerzel = AJOAJO
searchResult := searchResult "LEFT JOIN BIL.AJO_ANBUJOURNAL ON AJO_UMBUCH_ANBUJOURNALID = AJO_ANBUJOURNALID AND AJO_UMBUCH_ANLAGEGUTID = AJO_ANLAGEGUTID" "~"
if joinKuerzel = APOAKO
searchResult := searchResult "LEFT JOIN BMD.AKO_AGGREGATKOPF ON AKO_AGGKOPFID = APO_AGGKOPFID" "~"
if joinKuerzel = AKOAPO
searchResult := searchResult "LEFT JOIN BMD.APO_AGGREGATPOSITION ON APO_AGGKOPFID = AKO_AGGKOPFID" "~"
if joinKuerzel = AKRAKR
searchResult := searchResult "LEFT JOIN BUERO.AKR_AVKOSTENARTENZU ON AKR_AKRID = AKR_VERDECKT_AKRID" "~"
if joinKuerzel = AKRAKR
searchResult := searchResult "LEFT JOIN BUERO.AKR_AVKOSTENARTENZU ON AKR_VERDECKT_AKRID = AKR_AKRID" "~"
if joinKuerzel = ASRAKS
searchResult := searchResult "LEFT JOIN BUERO.AKS_AKTIONSTATUS ON AKS_AKKID = ASR_AKS_AKKID AND AKS_AKSNR = ASR_AKS_AKSNR" "~"
if joinKuerzel = AKSASR
searchResult := searchResult "LEFT JOIN BUERO.ASR_AUSSCHREIBUNG ON ASR_AKS_AKKID = AKS_AKKID AND ASR_AKS_AKSNR = AKS_AKSNR" "~"
if joinKuerzel = ASTAKT
searchResult := searchResult "LEFT JOIN WWS.AKT_AKTIONSSTAMM ON AKT_FIRMENNR = AST_AKT_FIRMENNR AND AKT_AKTIONSSTAMMNR = AST_AKTIONSSTAMMNR" "~"
if joinKuerzel = AKTAST
searchResult := searchResult "LEFT JOIN WWS.AST_ARTIKELSTATISTIK ON AST_AKT_FIRMENNR = AKT_FIRMENNR AND AST_AKTIONSSTAMMNR = AKT_AKTIONSSTAMMNR" "~"
if joinKuerzel = KRAAKT
searchResult := searchResult "LEFT JOIN WWS.AKT_AKTIONSSTAMM ON AKT_FIRMENNR = KRA_AKT_FIRMENNR AND AKT_AKTIONSSTAMMNR = KRA_AKT_AKTIONSSTAMMNR" "~"
if joinKuerzel = AKTKRA
searchResult := searchResult "LEFT JOIN WWS.KRA_KASSENRABATT ON KRA_AKT_FIRMENNR = AKT_FIRMENNR AND KRA_AKT_AKTIONSSTAMMNR = AKT_AKTIONSSTAMMNR" "~"
if joinKuerzel = RNPAKT
searchResult := searchResult "LEFT JOIN WWS.AKT_AKTIONSSTAMM ON AKT_FIRMENNR = RNP_AKT_FIRMENNR AND AKT_AKTIONSSTAMMNR = RNP_AKTIONSSTAMMNR" "~"
if joinKuerzel = AKTRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_AKT_FIRMENNR = AKT_FIRMENNR AND RNP_AKTIONSSTAMMNR = AKT_AKTIONSSTAMMNR" "~"
if joinKuerzel = UATALE
searchResult := searchResult "LEFT JOIN BUERO.ALE_ADRESSENLISTE ON ALE_FIRMENNR = UAT_ALE_FIRMENNR AND ALE_ADRLISTENNR = UAT_ALE_ADRLISTENNR AND ALE_ADRLISTENLFDNR = UAT_ALE_ADRLISTENLFDNR" "~"
if joinKuerzel = ALEUAT
searchResult := searchResult "LEFT JOIN BUERO.UAT_UMFRAGEANTWTEILNZU ON UAT_ALE_FIRMENNR = ALE_FIRMENNR AND UAT_ALE_ADRLISTENNR = ALE_ADRLISTENNR AND UAT_ALE_ADRLISTENLFDNR = ALE_ADRLISTENLFDNR" "~"
if joinKuerzel = ARZALG
searchResult := searchResult "LEFT JOIN BIL.ALG_ANLAGENGRUPPE ON ALG_ANLGRUPPENID = ARZ_ANLGRUPPENID" "~"
if joinKuerzel = ALGARZ
searchResult := searchResult "LEFT JOIN BIL.ARZ_ANLAGEGUTGRUPPEZU ON ARZ_ANLGRUPPENID = ALG_ANLGRUPPENID" "~"
if joinKuerzel = LPOALN
searchResult := searchResult "LEFT JOIN ALN_ALIBINUMMERN.ALN_ALIBINUMMERN ON ALN_ALNID = LPO_ALNID" "~"
if joinKuerzel = ALNLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_ALNID = ALN_ALNID" "~"
if joinKuerzel = AMDAMO
searchResult := searchResult "LEFT JOIN KORE.AMO_AUSWERTUNGMODELL ON AMO_FIRMENNR = AMD_FIRMENNR AND AMO_KORENR = AMD_KORENR AND AMO_AUSWMODELLNR = AMD_AUSWMODELLNR" "~"
if joinKuerzel = AMOAMD
searchResult := searchResult "LEFT JOIN KORE.AMD_MODELLDETAIL ON AMD_FIRMENNR = AMO_FIRMENNR AND AMD_KORENR = AMO_KORENR AND AMD_AUSWMODELLNR = AMO_AUSWMODELLNR" "~"
if joinKuerzel = PLCAPA
searchResult := searchResult "LEFT JOIN PRODUKTION.APA_ARBEITSPLAN ON APA_AP_ID = PLC_AP_ID" "~"
if joinKuerzel = APAPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_AP_ID = APA_AP_ID" "~"
if joinKuerzel = APAAPS
searchResult := searchResult "LEFT JOIN APS_ARBEITSPLAN_STAMM.APS_ARBEITSPLAN_STAMM ON APS_APSTAMM_ID = APA_AP_VORLAGE_ID" "~"
if joinKuerzel = APSAPA
searchResult := searchResult "LEFT JOIN PRODUKTION.APA_ARBEITSPLAN ON APA_AP_VORLAGE_ID = APS_APSTAMM_ID" "~"
if joinKuerzel = ARPARG
searchResult := searchResult "LEFT JOIN BUERO.ARG_ARBEITSRUHEZEITGESETZ ON ARG_FIRMENNR = ARP_FIRMENNR AND ARG_ARGNR = ARP_ARGNR" "~"
if joinKuerzel = ARGARP
searchResult := searchResult "LEFT JOIN BUERO.ARP_ARBEITSZEITGESETZ_PAU ON ARP_FIRMENNR = ARG_FIRMENNR AND ARP_ARGNR = ARG_ARGNR" "~"
if joinKuerzel = RAGARG
searchResult := searchResult "LEFT JOIN BUERO.ARG_ARBEITSRUHEZEITGESETZ ON ARG_FIRMENNR = RAG_FIRMENNR AND ARG_ARGNR = RAG_ARGNR" "~"
if joinKuerzel = ARGRAG
searchResult := searchResult "LEFT JOIN BUERO.RAG_RUHEARBEITSZEITGESETZ ON RAG_FIRMENNR = ARG_FIRMENNR AND RAG_ARGNR = ARG_ARGNR" "~"
if joinKuerzel = ADZARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = ADZ_ARL_FIRMENNR AND ARL_ARTIKELNR = ADZ_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLADZ
searchResult := searchResult "LEFT JOIN WWS.ADZ_ARTIKELDIENSTLNRZU ON ADZ_ARL_FIRMENNR = ARL_FIRMENNR AND ADZ_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = AKLARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = AKL_HAUPTFIRMENNR AND ARL_ARTIKELNR = AKL_ARTIKELNR" "~"
if joinKuerzel = ARLAKL
searchResult := searchResult "LEFT JOIN WWS.AKL_ARTIKELKLASSIFIKATION ON AKL_HAUPTFIRMENNR = ARL_FIRMENNR AND AKL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = ARBARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = ARB_FIRMENNR AND ARL_ARTIKELNR = ARB_ARTIKELNR" "~"
if joinKuerzel = ARLARB
searchResult := searchResult "LEFT JOIN WWS.ARB_ARTIKELBEREICHSWERTE ON ARB_FIRMENNR = ARL_FIRMENNR AND ARB_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = BOPARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = BOP_ARL_FIRMENNR AND ARL_ARTIKELNR = BOP_ARTIKELNR" "~"
if joinKuerzel = ARLBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_ARL_FIRMENNR = ARL_FIRMENNR AND BOP_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = BPBARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = BPB_ARL_FIRMENNR AND ARL_ARTIKELNR = BPB_ARTIKELNR" "~"
if joinKuerzel = ARLBPB
searchResult := searchResult "LEFT JOIN WWS.BPB_LIEFBESTAET ON BPB_ARL_FIRMENNR = ARL_FIRMENNR AND BPB_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = TSWARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = TSW_ARL_FIRMENNR AND ARL_ARTIKELNR = TSW_ARTIKELNR" "~"
if joinKuerzel = ARLTSW
searchResult := searchResult "LEFT JOIN WWS.TSW_STATISTIKWERTE_TMP ON TSW_ARL_FIRMENNR = ARL_FIRMENNR AND TSW_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = UBEARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = UBE_ARL_FIRMENNR AND ARL_ARTIKELNR = UBE_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLUBE
searchResult := searchResult "LEFT JOIN BMDINTERN.UBE_UPDATEBESTELLUNG ON UBE_ARL_FIRMENNR = ARL_FIRMENNR AND UBE_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = WENARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = WEN_ARL_FIRMENNR AND ARL_ARTIKELNR = WEN_ARTIKELNR" "~"
if joinKuerzel = ARLWEN
searchResult := searchResult "LEFT JOIN WWS.WEN_WARENEINSATZRNP ON WEN_ARL_FIRMENNR = ARL_FIRMENNR AND WEN_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = WEPARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = WEP_ARL_FIRMENNR AND ARL_ARTIKELNR = WEP_ARTIKELNR" "~"
if joinKuerzel = ARLWEP
searchResult := searchResult "LEFT JOIN WWS.WEP_WWSERKONTROLLEPOS ON WEP_ARL_FIRMENNR = ARL_FIRMENNR AND WEP_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = LHPARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = LHP_UP_ARL_FIRMENNR AND ARL_ARTIKELNR = LHP_ARTIKELNR" "~"
if joinKuerzel = ARLLHP
searchResult := searchResult "LEFT JOIN BMDINTERN.LHP_LIZENZHAUPTPAKETZU ON LHP_UP_ARL_FIRMENNR = ARL_FIRMENNR AND LHP_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = ARLARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = ARL_FIRMENNR AND ARL_ARTIKELNR = ARL_VERLEIH_GM_ARTIKELNR" "~"
if joinKuerzel = ARLARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = ARL_FIRMENNR AND ARL_VERLEIH_GM_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = PALARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = PAL_ZUSATZ_ARL_FIRMENNR AND ARL_ARTIKELNR = PAL_ARTIKELNR" "~"
if joinKuerzel = ARLPAL
searchResult := searchResult "LEFT JOIN ZLE.PAL_PAUSCHALE ON PAL_ZUSATZ_ARL_FIRMENNR = ARL_FIRMENNR AND PAL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = PGPARS
searchResult := searchResult "LEFT JOIN WWS.ARS_ARTIKELSTATISTIK ON ARS_FIRMENNR = PGP_ARS_FIRMENNR AND ARS_STATISTIKLFDNR = PGP_ARS_STATISTIKLFDNR" "~"
if joinKuerzel = ARSPGP
searchResult := searchResult "LEFT JOIN WWS.PGP_PICARTIKELGRP ON PGP_ARS_FIRMENNR = ARS_FIRMENNR AND PGP_ARS_STATISTIKLFDNR = ARS_STATISTIKLFDNR" "~"
if joinKuerzel = EFKASJ
searchResult := searchResult "LEFT JOIN BMD.ASJ_ASYNCHRONEJOBS ON ASJ_JOB_ID = EFK_ASYNC_JOBID" "~"
if joinKuerzel = ASJEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_ASYNC_JOBID = ASJ_JOB_ID" "~"
if joinKuerzel = POZASR
searchResult := searchResult "LEFT JOIN BUERO.ASR_AUSSCHREIBUNG ON ASR_FIRMENNR = POZ_ASR_FIRMENNR AND ASR_AUSSCHREIBUNGSNR = POZ_ASR_AUSSCHREIBUNGSNR" "~"
if joinKuerzel = ASRPOZ
searchResult := searchResult "LEFT JOIN BUERO.POZ_PISWORKFLOWZU ON POZ_ASR_FIRMENNR = ASR_FIRMENNR AND POZ_ASR_AUSSCHREIBUNGSNR = ASR_AUSSCHREIBUNGSNR" "~"
if joinKuerzel = BSGATE
searchResult := searchResult "LEFT JOIN BUERO.ATE_ABTEILUNG ON ATE_FIRMENNR = BSG_ATE_FIRMENNR AND ATE_ABTEILUNGSNR = BSG_ATE_ABTEILUNGSNR" "~"
if joinKuerzel = ATEBSG
searchResult := searchResult "LEFT JOIN BUERO.BSG_BESPRECHUNG ON BSG_ATE_FIRMENNR = ATE_FIRMENNR AND BSG_ATE_ABTEILUNGSNR = ATE_ABTEILUNGSNR" "~"
if joinKuerzel = SAPATE
searchResult := searchResult "LEFT JOIN BUERO.ATE_ABTEILUNG ON ATE_FIRMENNR = SAP_ATE_FIRMENNR AND ATE_ABTEILUNGSNR = SAP_ABTEILUNGSNR" "~"
if joinKuerzel = ATESAP
searchResult := searchResult "LEFT JOIN BUERO.SAP_SAMMELPROJEKT ON SAP_ATE_FIRMENNR = ATE_FIRMENNR AND SAP_ABTEILUNGSNR = ATE_ABTEILUNGSNR" "~"
if joinKuerzel = AWSATE
searchResult := searchResult "LEFT JOIN ATE_ABTEILUNG.ATE_ABTEILUNG ON ATE_FIRMENNR = AWS_ATE_MIT_FIRMENNR AND ATE_ABTEILUNGSNR = AWS_ATE_MIT_ABTEILUNGSNR" "~"
if joinKuerzel = ATEAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_ATE_MIT_FIRMENNR = ATE_FIRMENNR AND AWS_ATE_MIT_ABTEILUNGSNR = ATE_ABTEILUNGSNR" "~"
if joinKuerzel = MAGAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_AUFGABENNR = MAG_AUFGABENNR" "~"
if joinKuerzel = AUGMAG
searchResult := searchResult "LEFT JOIN BUERO.MAG_MITARBGESPR ON MAG_AUFGABENNR = AUG_AUFGABENNR" "~"
if joinKuerzel = FGAAUW
searchResult := searchResult "LEFT JOIN BUERO.AUW_AUSWAHL ON AUW_KRITERIENNR = FGA_KRITERIENNR AND AUW_AUSWAHLNR = FGA_AUSWAHLNR" "~"
if joinKuerzel = AUWFGA
searchResult := searchResult "LEFT JOIN BUERO.FGA_FRAGEBOGENAUSWAHL ON FGA_KRITERIENNR = AUW_KRITERIENNR AND FGA_AUSWAHLNR = AUW_AUSWAHLNR" "~"
if joinKuerzel = OFRAWD
searchResult := searchResult "LEFT JOIN BUERO.AWD_ANTWORTDETAIL ON AWD_ANTWORTID = OFR_ANTWORTID AND AWD_ANTWDETAILLFDNR = OFR_ANTWDETAILLFDNR" "~"
if joinKuerzel = AWDOFR
searchResult := searchResult "LEFT JOIN BUERO.OFR_FRAGEBOGENREGEL ON OFR_ANTWORTID = AWD_ANTWORTID AND OFR_ANTWDETAILLFDNR = AWD_ANTWDETAILLFDNR" "~"
if joinKuerzel = MITAZB
searchResult := searchResult "LEFT JOIN BUERO.AZB_ARBEITSZEITBEZ ON AZB_FIRMENNR = MIT_AZB_FIRMENNR AND AZB_AMODELLNR = MIT_AMODELLNR" "~"
if joinKuerzel = AZBMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_AZB_FIRMENNR = AZB_FIRMENNR AND MIT_AMODELLNR = AZB_AMODELLNR" "~"
if joinKuerzel = ABOAZK
searchResult := searchResult "LEFT JOIN BIL.AZK_ANBUZUSCHUSSKENNZEICH ON AZK_ANBUZKID = ABO_ANBUZKID" "~"
if joinKuerzel = AZKABO
searchResult := searchResult "LEFT JOIN BIL.ABO_ANBUBUCHUNGKONTEN ON ABO_ANBUZKID = AZK_ANBUZKID" "~"
if joinKuerzel = ABRAZK
searchResult := searchResult "LEFT JOIN BIL.AZK_ANBUZUSCHUSSKENNZEICH ON AZK_ANBUZKID = ABR_ANBUZKID" "~"
if joinKuerzel = AZKABR
searchResult := searchResult "LEFT JOIN BIL.ABR_ANBUBEWRES ON ABR_ANBUZKID = AZK_ANBUZKID" "~"
if joinKuerzel = BRVBAI
searchResult := searchResult "LEFT JOIN BAI_BANKINSTITUTIONSZU.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = BRV_BAI_ZAHLUNGSBANKFIRMA" "~"
if joinKuerzel = BAIBRV
searchResult := searchResult "LEFT JOIN FIBU.BRV_BANKRUECKVERBUCHUNG ON BRV_BAI_ZAHLUNGSBANKFIRMA = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = RNGBAI
searchResult := searchResult "LEFT JOIN BAI_BANKINSTITUTIONSZU.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = RNG_BANKINSTLFDNR" "~"
if joinKuerzel = BAIRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = GSKARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = GSK_ARL_FIRMENNR AND ARL_ARTIKELNR = GSK_ARTIKELNR" "~"
if joinKuerzel = ARLGSK
searchResult := searchResult "LEFT JOIN WWS.GSK_GUTSCHEINKOPF ON GSK_ARL_FIRMENNR = ARL_FIRMENNR AND GSK_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = IMZARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = IMZ_IMNARLFIRMENNR AND ARL_ARTIKELNR = IMZ_ARTIKELNR" "~"
if joinKuerzel = ARLIMZ
searchResult := searchResult "LEFT JOIN BUERO.IMZ_INSTANDHMASSNZU ON IMZ_IMNARLFIRMENNR = ARL_FIRMENNR AND IMZ_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = IVPARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = IVP_FIRMENNR AND ARL_ARTIKELNR = IVP_ARTIKELNR" "~"
if joinKuerzel = ARLIVP
searchResult := searchResult "LEFT JOIN WWS.IVP_INVENTURPOSITION ON IVP_FIRMENNR = ARL_FIRMENNR AND IVP_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = KATARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = KAT_ARL_FIRMENNR AND ARL_ARTIKELNR = KAT_ARTIKELNR" "~"
if joinKuerzel = ARLKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_ARL_FIRMENNR = ARL_FIRMENNR AND KAT_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = LVEARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = LVE_ARL_FIRMENNR AND ARL_ARTIKELNR = LVE_ARTIKELNR" "~"
if joinKuerzel = ARLLVE
searchResult := searchResult "LEFT JOIN PRODUKTION.LVE_LEISTUNGSVORERFASSUNG ON LVE_ARL_FIRMENNR = ARL_FIRMENNR AND LVE_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = PLCARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = PLC_ARL_FIRMENNR AND ARL_ARTIKELNR = PLC_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_ARL_FIRMENNR = ARL_FIRMENNR AND PLC_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = PWAARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = PWA_FIRMENNR AND ARL_ARTIKELNR = PWA_ARTIKELNR" "~"
if joinKuerzel = ARLPWA
searchResult := searchResult "LEFT JOIN WWS.PWA_PWKARTIKELZU ON PWA_FIRMENNR = ARL_FIRMENNR AND PWA_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = RRDARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = RRD_HAUPTFIRMENNR AND ARL_ARTIKELNR = RRD_ARTIKELNR" "~"
if joinKuerzel = ARLRRD
searchResult := searchResult "LEFT JOIN WWS.RRD_RECHNUNGSRABATTDETAIL ON RRD_HAUPTFIRMENNR = ARL_FIRMENNR AND RRD_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = SLKARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = SLK_ARL_FIRMENNR AND ARL_ARTIKELNR = SLK_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLSLK
searchResult := searchResult "LEFT JOIN WWS.SLK_STUECKLISTENKOPF ON SLK_ARL_FIRMENNR = ARL_FIRMENNR AND SLK_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = TBAARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = TBA_ARL_FIRMENNR AND ARL_ARTIKELNR = TBA_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLTBA
searchResult := searchResult "LEFT JOIN BUERO.TBA_TAGESMDBUCHARTAUFB ON TBA_ARL_FIRMENNR = ARL_FIRMENNR AND TBA_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = VLJARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = VLJ_HAUPTFIRMENNR AND ARL_ARTIKELNR = VLJ_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLVLJ
searchResult := searchResult "LEFT JOIN WWS.VLJ_VERLEIHJOURNAL ON VLJ_HAUPTFIRMENNR = ARL_FIRMENNR AND VLJ_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = REWARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = REW_FINAL_ARL_FIRMENNR AND ARL_ARTIKELNR = REW_FINAL_ARTIKELNR" "~"
if joinKuerzel = ARLREW
searchResult := searchResult "LEFT JOIN ZLE.REW_REGELWERK ON REW_FINAL_ARL_FIRMENNR = ARL_FIRMENNR AND REW_FINAL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = SNRARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = SNR_VALID_ARL_FIRMENNR AND ARL_ARTIKELNR = SNR_VALID_ARTIKELNR" "~"
if joinKuerzel = ARLSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_VALID_ARL_FIRMENNR = ARL_FIRMENNR AND SNR_VALID_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = ZAWASK
searchResult := searchResult "LEFT JOIN ASK_SORTIERKOPF.ASK_SORTIERKOPF ON ASK_FIRMENNR = ZAW_ASK_FIRMENNR AND ASK_SORTKOPFNR = ZAW_ASK_SORTKOPFNR" "~"
if joinKuerzel = ASKZAW
searchResult := searchResult "LEFT JOIN BUERO.ZAW_ZEITAUSWERTUNG ON ZAW_ASK_FIRMENNR = ASK_FIRMENNR AND ZAW_ASK_SORTKOPFNR = ASK_SORTKOPFNR" "~"
if joinKuerzel = INRASR
searchResult := searchResult "LEFT JOIN BUERO.ASR_AUSSCHREIBUNG ON ASR_FIRMENNR = INR_ASR_FIRMENNR AND ASR_AUSSCHREIBUNGSNR = INR_ASR_AUSSCHREIBUNGSNR" "~"
if joinKuerzel = ASRINR
searchResult := searchResult "LEFT JOIN BUERO.INR_INSERATE ON INR_ASR_FIRMENNR = ASR_FIRMENNR AND INR_ASR_AUSSCHREIBUNGSNR = ASR_AUSSCHREIBUNGSNR" "~"
if joinKuerzel = ASWPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = ASW_POSTITNR" "~"
if joinKuerzel = PITASW
searchResult := searchResult "LEFT JOIN BUERO.ASW_AUSWEIS ON ASW_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = ZBUASW
searchResult := searchResult "LEFT JOIN BUERO.ASW_AUSWEIS ON ASW_FIRMENNR = ZBU_ASW_FIRMENNR AND ASW_AUSWEISNR = ZBU_AUSWEISNR" "~"
if joinKuerzel = ASWZBU
searchResult := searchResult "LEFT JOIN BUERO.ZBU_ZEITBUCHUNG ON ZBU_ASW_FIRMENNR = ASW_FIRMENNR AND ZBU_AUSWEISNR = ASW_AUSWEISNR" "~"
if joinKuerzel = HFTATE
searchResult := searchResult "LEFT JOIN BUERO.ATE_ABTEILUNG ON ATE_FIRMENNR = HFT_ATE_FIRMENNR AND ATE_ABTEILUNGSNR = HFT_ABTEILUNGSNR" "~"
if joinKuerzel = ATEHFT
searchResult := searchResult "LEFT JOIN BUERO.HFT_HILFSTABELLE ON HFT_ATE_FIRMENNR = ATE_FIRMENNR AND HFT_ABTEILUNGSNR = ATE_ABTEILUNGSNR" "~"
if joinKuerzel = PSWATE
searchResult := searchResult "LEFT JOIN BUERO.ATE_ABTEILUNG ON ATE_FIRMENNR = PSW_ATE_FIRMENNR AND ATE_ABTEILUNGSNR = PSW_ABTEILUNGSNR" "~"
if joinKuerzel = ATEPSW
searchResult := searchResult "LEFT JOIN BUERO.PSW_PERSONENWERTE ON PSW_ATE_FIRMENNR = ATE_FIRMENNR AND PSW_ABTEILUNGSNR = ATE_ABTEILUNGSNR" "~"
if joinKuerzel = PVFATE
searchResult := searchResult "LEFT JOIN BUERO.ATE_ABTEILUNG ON ATE_FIRMENNR = PVF_ATE_FIRMENNR AND ATE_ABTEILUNGSNR = PVF_ABTEILUNGSNR" "~"
if joinKuerzel = ATEPVF
searchResult := searchResult "LEFT JOIN BUERO.PVF_PERSONENVORERFASSUNG ON PVF_ATE_FIRMENNR = ATE_FIRMENNR AND PVF_ABTEILUNGSNR = ATE_ABTEILUNGSNR" "~"
if joinKuerzel = AUEAUT
searchResult := searchResult "LEFT JOIN ZLE.AUT_AUSWERTTYP ON AUT_AUSWERTTYPNR = AUE_AUSWERTTYPNR" "~"
if joinKuerzel = AUTAUE
searchResult := searchResult "LEFT JOIN ZLE.AUE_AUSWERTTYPEINST ON AUE_AUSWERTTYPNR = AUT_AUSWERTTYPNR" "~"
if joinKuerzel = TRJARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = TRJ_ARTIKELFIRMENNR AND ARL_ARTIKELNR = TRJ_ARTIKELNR" "~"
if joinKuerzel = ARLTRJ
searchResult := searchResult "LEFT JOIN WWS.TRJ_TRANSPORTEINH_JOURNAL ON TRJ_ARTIKELFIRMENNR = ARL_FIRMENNR AND TRJ_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = TRPARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = TRP_ARL_FIRMENNR AND ARL_ARTIKELNR = TRP_ARTIKELNR" "~"
if joinKuerzel = ARLTRP
searchResult := searchResult "LEFT JOIN WWS.TRP_TRANSPORTPOSITION ON TRP_ARL_FIRMENNR = ARL_FIRMENNR AND TRP_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = VASARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = VAS_ARL_FIRMENNR AND ARL_ARTIKELNR = VAS_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_ARL_FIRMENNR = ARL_FIRMENNR AND VAS_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = VSTARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = VST_HAUPT_FIRMENNR AND ARL_ARTIKELNR = VST_ARTIKELNR" "~"
if joinKuerzel = ARLVST
searchResult := searchResult "LEFT JOIN WWS.VST_VERKAUFSSTATISTIK ON VST_HAUPT_FIRMENNR = ARL_FIRMENNR AND VST_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = WBWARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = WBW_HAUPTFIRMENNR AND ARL_ARTIKELNR = WBW_ARTIKELNR" "~"
if joinKuerzel = ARLWBW
searchResult := searchResult "LEFT JOIN WWS.WBW_WWSBUDGETWERTE ON WBW_HAUPTFIRMENNR = ARL_FIRMENNR AND WBW_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = WEOARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = WEO_ARL_FIRMENNR AND ARL_ARTIKELNR = WEO_ARTIKELNR" "~"
if joinKuerzel = ARLWEO
searchResult := searchResult "LEFT JOIN WWS.WEO_WARENEINSATZLPO ON WEO_ARL_FIRMENNR = ARL_FIRMENNR AND WEO_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = WEUARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = WEU_ARL_FIRMENNR AND ARL_ARTIKELNR = WEU_ARTIKELNR" "~"
if joinKuerzel = ARLWEU
searchResult := searchResult "LEFT JOIN WWS.WEU_WWSERKONTROLLEZU ON WEU_ARL_FIRMENNR = ARL_FIRMENNR AND WEU_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = ZBAARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = ZBA_ARL_FIRMENNR AND ARL_ARTIKELNR = ZBA_ARTIKELNR" "~"
if joinKuerzel = ARLZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_ARL_FIRMENNR = ARL_FIRMENNR AND ZBA_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = ZSDARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = ZSD_ARL_FIRMENNR AND ARL_ARTIKELNR = ZSD_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLZSD
searchResult := searchResult "LEFT JOIN ZLE.ZSD_ZEITSCHEIBEDETAIL ON ZSD_ARL_FIRMENNR = ARL_FIRMENNR AND ZSD_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = AAZARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = AAZ_PROD_ARL_FIRMENNR AND ARL_ARTIKELNR = AAZ_PROD_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLAAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.AAZ_AUFARBEITARTIKELZU ON AAZ_PROD_ARL_FIRMENNR = ARL_FIRMENNR AND AAZ_PROD_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = LEIARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = LEI_UEBERSTD_ARL_FIRMENNR AND ARL_ARTIKELNR = LEI_UEBERSTD_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_UEBERSTD_ARL_FIRMENNR = ARL_FIRMENNR AND LEI_UEBERSTD_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = PALARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = PAL_SOCKEL_ARL_FIRMENNR AND ARL_ARTIKELNR = PAL_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLPAL
searchResult := searchResult "LEFT JOIN ZLE.PAL_PAUSCHALE ON PAL_SOCKEL_ARL_FIRMENNR = ARL_FIRMENNR AND PAL_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = ASOASB
searchResult := searchResult "LEFT JOIN ZLE.ASB_AUSWERT_SUB ON ASB_AUSWERTSUBID = ASO_ASB_SUM_AUSWERTSUBID" "~"
if joinKuerzel = ASBASO
searchResult := searchResult "LEFT JOIN ZLE.ASO_SORTIERPOS ON ASO_ASB_SUM_AUSWERTSUBID = ASB_AUSWERTSUBID" "~"
if joinKuerzel = AADASP
searchResult := searchResult "LEFT JOIN BIL.ASP_SPALTENKOPF ON ASP_ANBUSPALTENKOPFID = AAD_ASP_ANBUSPALTENKOPFID" "~"
if joinKuerzel = ASPAAD
searchResult := searchResult "LEFT JOIN BIL.AAD_AUSDRUCKDETAIL ON AAD_ASP_ANBUSPALTENKOPFID = ASP_ANBUSPALTENKOPFID" "~"
if joinKuerzel = ONZASR
searchResult := searchResult "LEFT JOIN ASR_AUSSCHREIBUNG.ASR_AUSSCHREIBUNG ON ASR_FIRMENNR = ONZ_FIRMENNR AND ASR_AUSSCHREIBUNGSNR = ONZ_AUSSCHREIBUNGSNR" "~"
if joinKuerzel = ASRONZ
searchResult := searchResult "LEFT JOIN HRS.ONZ_ONLINEBEW_STELLE_ZU ON ONZ_FIRMENNR = ASR_FIRMENNR AND ONZ_AUSSCHREIBUNGSNR = ASR_AUSSCHREIBUNGSNR" "~"
if joinKuerzel = LEIATE
searchResult := searchResult "LEFT JOIN ATE_ABTEILUNG.ATE_ABTEILUNG ON ATE_FIRMENNR = LEI_ATE_MIT_FIRMENNR AND ATE_ABTEILUNGSNR = LEI_ATE_MIT_ABTEILUNGSNR" "~"
if joinKuerzel = ATELEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_ATE_MIT_FIRMENNR = ATE_FIRMENNR AND LEI_ATE_MIT_ABTEILUNGSNR = ATE_ABTEILUNGSNR" "~"
if joinKuerzel = AEZAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_AUFGABENNR = AEZ_AUFGABENNR" "~"
if joinKuerzel = AUGAEZ
searchResult := searchResult "LEFT JOIN BUERO.AEZ_AUFGABENTICKETEMAILZU ON AEZ_AUFGABENNR = AUG_AUFGABENNR" "~"
if joinKuerzel = BPOAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_AUFGABENNR = BPO_AUFGABENNR" "~"
if joinKuerzel = AUGBPO
searchResult := searchResult "LEFT JOIN BUERO.BPO_BESPRECHOBJEKTE ON BPO_AUFGABENNR = AUG_AUFGABENNR" "~"
if joinKuerzel = ASKAUT
searchResult := searchResult "LEFT JOIN ZLE.AUT_AUSWERTTYP ON AUT_AUSWERTTYPNR = ASK_AUSWERTTYPNR" "~"
if joinKuerzel = AUTASK
searchResult := searchResult "LEFT JOIN ZLE.ASK_SORTIERKOPF ON ASK_AUSWERTTYPNR = AUT_AUSWERTTYPNR" "~"
if joinKuerzel = BPZAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_VORERFID = BPZ_VORERFID" "~"
if joinKuerzel = AVEBPZ
searchResult := searchResult "LEFT JOIN WWS.BPZ_BESTELLPOSZUORDNUNG ON BPZ_VORERFID = AVE_VORERFID" "~"
if joinKuerzel = AZVAZB
searchResult := searchResult "LEFT JOIN BUERO.AZB_ARBEITSZEITBEZ ON AZB_FIRMENNR = AZV_FIRMENNR AND AZB_AMODELLNR = AZV_AMODELLNR" "~"
if joinKuerzel = AZBAZV
searchResult := searchResult "LEFT JOIN BUERO.AZV_ARBEITSZEITVERSION ON AZV_FIRMENNR = AZB_FIRMENNR AND AZV_AMODELLNR = AZB_AMODELLNR" "~"
if joinKuerzel = BORAKT
searchResult := searchResult "LEFT JOIN WWS.AKT_AKTIONSSTAMM ON AKT_FIRMENNR = BOR_AKT_FIRMENNR AND AKT_AKTIONSSTAMMNR = BOR_AKT_AKTIONSSTAMMNR" "~"
if joinKuerzel = AKTBOR
searchResult := searchResult "LEFT JOIN WWS.BOR_BONRABATT ON BOR_AKT_FIRMENNR = AKT_FIRMENNR AND BOR_AKT_AKTIONSSTAMMNR = AKT_AKTIONSSTAMMNR" "~"
if joinKuerzel = KAPAKT
searchResult := searchResult "LEFT JOIN WWS.AKT_AKTIONSSTAMM ON AKT_FIRMENNR = KAP_AKT_FIRMENNR AND AKT_AKTIONSSTAMMNR = KAP_AKT_AKTIONSSTAMMNR" "~"
if joinKuerzel = AKTKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_AKT_FIRMENNR = AKT_FIRMENNR AND KAP_AKT_AKTIONSSTAMMNR = AKT_AKTIONSSTAMMNR" "~"
if joinKuerzel = WSSAKT
searchResult := searchResult "LEFT JOIN WWS.AKT_AKTIONSSTAMM ON AKT_FIRMENNR = WSS_AKT_FIRMENNR AND AKT_AKTIONSSTAMMNR = WSS_AKTIONSSTAMMNR" "~"
if joinKuerzel = AKTWSS
searchResult := searchResult "LEFT JOIN WWS.WSS_WWSSTATISTIK ON WSS_AKT_FIRMENNR = AKT_FIRMENNR AND WSS_AKTIONSSTAMMNR = AKT_AKTIONSSTAMMNR" "~"
if joinKuerzel = RNPAKT
searchResult := searchResult "LEFT JOIN WWS.AKT_AKTIONSSTAMM ON AKT_FIRMENNR = RNP_AKT_FIRMENNR_RABATT AND AKT_AKTIONSSTAMMNR = RNP_AKTIONSSTAMMNR_RABATT" "~"
if joinKuerzel = AKTRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_AKT_FIRMENNR_RABATT = AKT_FIRMENNR AND RNP_AKTIONSSTAMMNR_RABATT = AKT_AKTIONSSTAMMNR" "~"
if joinKuerzel = ALGALG
searchResult := searchResult "LEFT JOIN BIL.ALG_ANLAGENGRUPPE ON ALG_ANLGRUPPENID = ALG_HAUPTANLGRUPPENID" "~"
if joinKuerzel = ALGALG
searchResult := searchResult "LEFT JOIN BIL.ALG_ANLAGENGRUPPE ON ALG_HAUPTANLGRUPPENID = ALG_ANLGRUPPENID" "~"
if joinKuerzel = WAPALN
searchResult := searchResult "LEFT JOIN ALN_ALIBINUMMERN.ALN_ALIBINUMMERN ON ALN_ALNID = WAP_ALNID" "~"
if joinKuerzel = ALNWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_ALNID = ALN_ALNID" "~"
if joinKuerzel = PVFAMG
searchResult := searchResult "LEFT JOIN BUERO.AMG_GKKABMELDEGRUND ON AMG_ABMELDEGRUNDNR = PVF_ABMELDEGRUNDNR" "~"
if joinKuerzel = AMGPVF
searchResult := searchResult "LEFT JOIN BUERO.PVF_PERSONENVORERFASSUNG ON PVF_ABMELDEGRUNDNR = AMG_ABMELDEGRUNDNR" "~"
if joinKuerzel = EPSAPA
searchResult := searchResult "LEFT JOIN APA_ARBEITSPLAN.APA_ARBEITSPLAN ON APA_AP_ID = EPS_AP_ID" "~"
if joinKuerzel = APAEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_AP_ID = APA_AP_ID" "~"
if joinKuerzel = WADAPG
searchResult := searchResult "LEFT JOIN WWS.APG_ARAPRODUKTGRUPPEN ON APG_FIRMENNR = WAD_APG_FIRMENNR AND APG_PRODUKTGRPNR = WAD_APG_PRODUKTGRPNR" "~"
if joinKuerzel = APGWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_APG_FIRMENNR = APG_FIRMENNR AND WAD_APG_PRODUKTGRPNR = APG_PRODUKTGRPNR" "~"
if joinKuerzel = APSAPS
searchResult := searchResult "LEFT JOIN WWS.APS_ARBEITSPLAN_STAMM ON APS_APSTAMM_ID = APS_APSTAMM_PARENT_ID" "~"
if joinKuerzel = APSAPS
searchResult := searchResult "LEFT JOIN WWS.APS_ARBEITSPLAN_STAMM ON APS_APSTAMM_PARENT_ID = APS_APSTAMM_ID" "~"
if joinKuerzel = AGZAPS
searchResult := searchResult "LEFT JOIN WWS.APS_ARBEITSPLAN_STAMM ON APS_APSTAMM_ID = AGZ_AP_PARENT_ID" "~"
if joinKuerzel = APSAGZ
searchResult := searchResult "LEFT JOIN WWS.AGZ_ARBEITSGANGZU ON AGZ_AP_PARENT_ID = APS_APSTAMM_ID" "~"
if joinKuerzel = RZGARG
searchResult := searchResult "LEFT JOIN BUERO.ARG_ARBEITSRUHEZEITGESETZ ON ARG_FIRMENNR = RZG_FIRMENNR AND ARG_ARGNR = RZG_ARGNR" "~"
if joinKuerzel = ARGRZG
searchResult := searchResult "LEFT JOIN BUERO.RZG_RUHEZEITGESETZ ON RZG_FIRMENNR = ARG_FIRMENNR AND RZG_ARGNR = ARG_ARGNR" "~"
if joinKuerzel = AKZARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = AKZ_ARL_FIRMENNR AND ARL_ARTIKELNR = AKZ_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLAKZ
searchResult := searchResult "LEFT JOIN WWS.AKZ_ARTIKELKUNDENZU ON AKZ_ARL_FIRMENNR = ARL_FIRMENNR AND AKZ_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = ARLARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = ARL_FIRMENNR AND ARL_ARTIKELNR = ARL_FOLGE_ARTIKELNR" "~"
if joinKuerzel = ARLARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = ARL_FIRMENNR AND ARL_FOLGE_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = AULARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = AUL_FIRMENNR AND ARL_ARTIKELNR = AUL_AUF_ARTIKELNR" "~"
if joinKuerzel = ARLAUL
searchResult := searchResult "LEFT JOIN WWS.AUL_ARTIKELUMSTELLLISTE ON AUL_FIRMENNR = ARL_FIRMENNR AND AUL_AUF_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = EEPARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = EEP_ARL_FIRMENNR AND ARL_ARTIKELNR = EEP_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLEEP
searchResult := searchResult "LEFT JOIN WWS.EEP_EDIEINHEITENPOS ON EEP_ARL_FIRMENNR = ARL_FIRMENNR AND EEP_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = EHTARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = EHT_ARL_FIRMENNR AND ARL_ARTIKELNR = EHT_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLEHT
searchResult := searchResult "LEFT JOIN WWS.EHT_EINHEITENTAB ON EHT_ARL_FIRMENNR = ARL_FIRMENNR AND EHT_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = ELVARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = ELV_ARL_FIRMENNR AND ARL_ARTIKELNR = ELV_ARTIKELNR" "~"
if joinKuerzel = ARLELV
searchResult := searchResult "LEFT JOIN WWS.ELV_ERFASSPOSLIEFERVOR ON ELV_ARL_FIRMENNR = ARL_FIRMENNR AND ELV_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = IVKARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = IVK_FIRMENNR AND ARL_ARTIKELNR = IVK_ARTIKELNR" "~"
if joinKuerzel = ARLIVK
searchResult := searchResult "LEFT JOIN WWS.IVK_INVENTURKOPF ON IVK_FIRMENNR = ARL_FIRMENNR AND IVK_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = KAIARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = KAI_ARL_FIRMENNR AND ARL_ARTIKELNR = KAI_ARTIKELNR" "~"
if joinKuerzel = ARLKAI
searchResult := searchResult "LEFT JOIN WWS.KAI_KNDARTIKELINFO ON KAI_ARL_FIRMENNR = ARL_FIRMENNR AND KAI_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = KMAARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = KMA_ARL_FIRMENNR AND ARL_ARTIKELNR = KMA_ARTIKELNR" "~"
if joinKuerzel = ARLKMA
searchResult := searchResult "LEFT JOIN WWS.KMA_KUNDENMITARBBEZUG ON KMA_ARL_FIRMENNR = ARL_FIRMENNR AND KMA_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = LADARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = LAD_ARL_FIRMENNR AND ARL_ARTIKELNR = LAD_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLLAD
searchResult := searchResult "LEFT JOIN WWS.LAD_LAGERARTIKELDATEN ON LAD_ARL_FIRMENNR = ARL_FIRMENNR AND LAD_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = PIAARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = PIA_ARL_FIRMENNR AND ARL_ARTIKELNR = PIA_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_ARL_FIRMENNR = ARL_FIRMENNR AND PIA_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = RATARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = RAT_ARL_FIRMENNR AND ARL_ARTIKELNR = RAT_ARTIKELNR" "~"
if joinKuerzel = ARLRAT
searchResult := searchResult "LEFT JOIN WWS.RAT_RABATTTAB ON RAT_ARL_FIRMENNR = ARL_FIRMENNR AND RAT_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = RTZARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = RTZ_ARL_FIRMENNR AND ARL_ARTIKELNR = RTZ_ARTIKELNR" "~"
if joinKuerzel = ARLRTZ
searchResult := searchResult "LEFT JOIN ZLE.RTZ_RECHTEXTZU ON RTZ_ARL_FIRMENNR = ARL_FIRMENNR AND RTZ_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = SNRARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = SNR_VALID_ARL_FIRMENNR AND ARL_ARTIKELNR = SNR_ARTIKELNR" "~"
if joinKuerzel = ARLSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_VALID_ARL_FIRMENNR = ARL_FIRMENNR AND SNR_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = WAPARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = WAP_ARL_FIRMENNR AND ARL_ARTIKELNR = WAP_ARTIKELNR" "~"
if joinKuerzel = ARLWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_ARL_FIRMENNR = ARL_FIRMENNR AND WAP_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = WEJARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = WEJ_ARL_FIRMENNR AND ARL_ARTIKELNR = WEJ_ARTIKELNR" "~"
if joinKuerzel = ARLWEJ
searchResult := searchResult "LEFT JOIN WWS.WEJ_WARENEINSATZLAJ ON WEJ_ARL_FIRMENNR = ARL_FIRMENNR AND WEJ_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = CHGARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = CHG_VALID_ARL_FIRMENNR AND ARL_ARTIKELNR = CHG_VALID_ARTIKELNR" "~"
if joinKuerzel = ARLCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_VALID_ARL_FIRMENNR = ARL_FIRMENNR AND CHG_VALID_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = HBTARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = HBT_FIRMENNR_EINZELTEIL AND ARL_ARTIKELNR = HBT_ARTIKELNR_EINZELTEIL" "~"
if joinKuerzel = ARLHBT
searchResult := searchResult "LEFT JOIN WWS.HBT_HAUPTBESTANDTEIL ON HBT_FIRMENNR_EINZELTEIL = ARL_FIRMENNR AND HBT_ARTIKELNR_EINZELTEIL = ARL_ARTIKELNR" "~"
if joinKuerzel = VFAARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = VFA_ARL_FIRMENNR AND ARL_ARTIKELNR = VFA_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLVFA
searchResult := searchResult "LEFT JOIN PRODUKTION.VFA_VERFAHRENSANWEISUNG_KOPF ON VFA_ARL_FIRMENNR = ARL_FIRMENNR AND VFA_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = VFDARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = VFD_ARL_FIRMENNR AND ARL_ARTIKELNR = VFD_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLVFD
searchResult := searchResult "LEFT JOIN PRODUKTION.VFD_VERFAHRENSANWEISUNG_POS ON VFD_ARL_FIRMENNR = ARL_FIRMENNR AND VFD_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = ARLARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = ARL_FIRMENNR AND ARL_ARTIKELNR = ARL_VERRECHN_ARTIKELNR" "~"
if joinKuerzel = ARLARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = ARL_FIRMENNR AND ARL_VERRECHN_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = ASOASK
searchResult := searchResult "LEFT JOIN ZLE.ASK_SORTIERKOPF ON ASK_FIRMENNR = ASO_FIRMENNR AND ASK_SORTKOPFNR = ASO_SORTKOPFNR" "~"
if joinKuerzel = ASKASO
searchResult := searchResult "LEFT JOIN ZLE.ASO_SORTIERPOS ON ASO_FIRMENNR = ASK_FIRMENNR AND ASO_SORTKOPFNR = ASK_SORTKOPFNR" "~"
if joinKuerzel = AINASR
searchResult := searchResult "LEFT JOIN BUERO.ASR_AUSSCHREIBUNG ON ASR_FIRMENNR = AIN_ASR_FIRMENNR AND ASR_AUSSCHREIBUNGSNR = AIN_ASR_AUSSCHREIBUNGSNR" "~"
if joinKuerzel = ASRAIN
searchResult := searchResult "LEFT JOIN BUERO.AIN_AUSSCHREIBUNGINSERTZU ON AIN_ASR_FIRMENNR = ASR_FIRMENNR AND AIN_ASR_AUSSCHREIBUNGSNR = ASR_AUSSCHREIBUNGSNR" "~"
if joinKuerzel = IMDASR
searchResult := searchResult "LEFT JOIN BUERO.ASR_AUSSCHREIBUNG ON ASR_FIRMENNR = IMD_ASR_FIRMENNR AND ASR_AUSSCHREIBUNGSNR = IMD_ASR_AUSSCHREIBUNGSNR" "~"
if joinKuerzel = ASRIMD
searchResult := searchResult "LEFT JOIN BUERO.IMD_INSERATMEDIUMDETAIL ON IMD_ASR_FIRMENNR = ASR_FIRMENNR AND IMD_ASR_AUSSCHREIBUNGSNR = ASR_AUSSCHREIBUNGSNR" "~"
if joinKuerzel = ATFATE
searchResult := searchResult "LEFT JOIN BUERO.ATE_ABTEILUNG ON ATE_FIRMENNR = ATF_ATE_FIRMENNR AND ATE_ABTEILUNGSNR = ATF_ABTEILUNGSNR" "~"
if joinKuerzel = ATEATF
searchResult := searchResult "LEFT JOIN BUERO.ATF_ABTEILUNGFIRMENZU ON ATF_ATE_FIRMENNR = ATE_FIRMENNR AND ATF_ABTEILUNGSNR = ATE_ABTEILUNGSNR" "~"
if joinKuerzel = PROATE
searchResult := searchResult "LEFT JOIN BUERO.ATE_ABTEILUNG ON ATE_FIRMENNR = PRO_ATE_FIRMENNR AND ATE_ABTEILUNGSNR = PRO_ABTEILUNGSNR" "~"
if joinKuerzel = ATEPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_ATE_FIRMENNR = ATE_FIRMENNR AND PRO_ABTEILUNGSNR = ATE_ABTEILUNGSNR" "~"
if joinKuerzel = VETATE
searchResult := searchResult "LEFT JOIN BUERO.ATE_ABTEILUNG ON ATE_FIRMENNR = VET_ATE_FIRMENNR AND ATE_ABTEILUNGSNR = VET_ABTEILUNGSNR" "~"
if joinKuerzel = ATEVET
searchResult := searchResult "LEFT JOIN BUERO.VET_VERANSTALTUNGSTAG ON VET_ATE_FIRMENNR = ATE_FIRMENNR AND VET_ABTEILUNGSNR = ATE_ABTEILUNGSNR" "~"
if joinKuerzel = MABAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_AUFGABENNR = MAB_AUFGABENNR" "~"
if joinKuerzel = AUGMAB
searchResult := searchResult "LEFT JOIN BUERO.MAB_MITARBAUSBILDUNG ON MAB_AUFGABENNR = AUG_AUFGABENNR" "~"
if joinKuerzel = VATAVR
searchResult := searchResult "LEFT JOIN BUERO.AVR_AVRUEZAHLUNGSTABELLE ON AVR_AVRID = VAT_AVRID" "~"
if joinKuerzel = AVRVAT
searchResult := searchResult "LEFT JOIN BUERO.VAT_TEILNAHME ON VAT_AVRID = AVR_AVRID" "~"
if joinKuerzel = VATAVS
searchResult := searchResult "LEFT JOIN BUERO.AVS_AVSTATUS ON AVS_FIRMENNR = VAT_AVS_FIRMENNR AND AVS_STATUSTYP = VAT_AVS_TYP AND AVS_STATUSNR = VAT_AVS_NR" "~"
if joinKuerzel = AVSVAT
searchResult := searchResult "LEFT JOIN BUERO.VAT_TEILNAHME ON VAT_AVS_FIRMENNR = AVS_FIRMENNR AND VAT_AVS_TYP = AVS_STATUSTYP AND VAT_AVS_NR = AVS_STATUSNR" "~"
if joinKuerzel = AWEAWC
searchResult := searchResult "LEFT JOIN ZLE.AWC_SPALTENAUSWAHL ON AWC_FIRMENNR = AWE_AWC_FIRMENNR AND AWC_SPALTENAUSWAHLNR = AWE_SPALTENAUSWAHLNR" "~"
if joinKuerzel = AWCAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_AWC_FIRMENNR = AWC_FIRMENNR AND AWE_SPALTENAUSWAHLNR = AWC_SPALTENAUSWAHLNR" "~"
if joinKuerzel = WASARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = WAS_WAG_FIRMENNR AND ARL_ARTIKELNR = WAS_ARTIKELNR" "~"
if joinKuerzel = ARLWAS
searchResult := searchResult "LEFT JOIN WWS.WAS_WARENEINSATZARL ON WAS_WAG_FIRMENNR = ARL_FIRMENNR AND WAS_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = WBDARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = WBD_ARL_FIRMENNR AND ARL_ARTIKELNR = WBD_ARTIKELNR" "~"
if joinKuerzel = ARLWBD
searchResult := searchResult "LEFT JOIN WWS.WBD_WARENBESCHKOSTENDET ON WBD_ARL_FIRMENNR = ARL_FIRMENNR AND WBD_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = WTEARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = WTE_ARL_FIRMENNR AND ARL_ARTIKELNR = WTE_ARTIKELNR" "~"
if joinKuerzel = ARLWTE
searchResult := searchResult "LEFT JOIN BUERO.WTE_WEBTEILNEHMER ON WTE_ARL_FIRMENNR = ARL_FIRMENNR AND WTE_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = ASOASB
searchResult := searchResult "LEFT JOIN ZLE.ASB_AUSWERT_SUB ON ASB_AUSWERTSUBID = ASO_ASB_AUSWERTSUBID" "~"
if joinKuerzel = ASBASO
searchResult := searchResult "LEFT JOIN ZLE.ASO_SORTIERPOS ON ASO_ASB_AUSWERTSUBID = ASB_AUSWERTSUBID" "~"
if joinKuerzel = AWEASK
searchResult := searchResult "LEFT JOIN ZLE.ASK_SORTIERKOPF ON ASK_FIRMENNR = AWE_ASK_FIRMENNR AND ASK_SORTKOPFNR = AWE_ASK_SORTKOPFNR" "~"
if joinKuerzel = ASKAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_ASK_FIRMENNR = ASK_FIRMENNR AND AWE_ASK_SORTKOPFNR = ASK_SORTKOPFNR" "~"
if joinKuerzel = AWSASR
searchResult := searchResult "LEFT JOIN ASR_AUSSCHREIBUNG.ASR_AUSSCHREIBUNG ON ASR_FIRMENNR = AWS_ASR_FIRMENNR AND ASR_AUSSCHREIBUNGSNR = AWS_ASR_AUSSCHREIBUNGSNR" "~"
if joinKuerzel = ASRAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_ASR_FIRMENNR = ASR_FIRMENNR AND AWS_ASR_AUSSCHREIBUNGSNR = ASR_AUSSCHREIBUNGSNR" "~"
if joinKuerzel = ISZASR
searchResult := searchResult "LEFT JOIN BUERO.ASR_AUSSCHREIBUNG ON ASR_FIRMENNR = ISZ_FIRMENNR AND ASR_AUSSCHREIBUNGSNR = ISZ_STELLENNR" "~"
if joinKuerzel = ASRISZ
searchResult := searchResult "LEFT JOIN BUERO.ISZ_INFOTABSTELLEZU ON ISZ_FIRMENNR = ASR_FIRMENNR AND ISZ_STELLENNR = ASR_AUSSCHREIBUNGSNR" "~"
if joinKuerzel = OBAASR
searchResult := searchResult "LEFT JOIN BUERO.ASR_AUSSCHREIBUNG ON ASR_FIRMENNR = OBA_ASR_FIRMENNR AND ASR_AUSSCHREIBUNGSNR = OBA_ASR_AUSSCHREIBUNGSNR" "~"
if joinKuerzel = ASROBA
searchResult := searchResult "LEFT JOIN BUERO.OBA_ONLBEWAUSSCHREIBUNGZU ON OBA_ASR_FIRMENNR = ASR_FIRMENNR AND OBA_ASR_AUSSCHREIBUNGSNR = ASR_AUSSCHREIBUNGSNR" "~"
if joinKuerzel = PIPASR
searchResult := searchResult "LEFT JOIN BUERO.ASR_AUSSCHREIBUNG ON ASR_FIRMENNR = PIP_ASR_FIRMENNR AND ASR_AUSSCHREIBUNGSNR = PIP_ASR_AUSSCHREIBUNGSNR" "~"
if joinKuerzel = ASRPIP
searchResult := searchResult "LEFT JOIN BUERO.PIP_PERSINFOPROTOKOLL ON PIP_ASR_FIRMENNR = ASR_FIRMENNR AND PIP_ASR_AUSSCHREIBUNGSNR = ASR_AUSSCHREIBUNGSNR" "~"
if joinKuerzel = ASTAST
searchResult := searchResult "LEFT JOIN WWS.AST_ARTIKELSTATISTIK ON AST_ASTATISTIKID = AST_SET_ASTATISTIKID" "~"
if joinKuerzel = ASTAST
searchResult := searchResult "LEFT JOIN WWS.AST_ARTIKELSTATISTIK ON AST_SET_ASTATISTIKID = AST_ASTATISTIKID" "~"
if joinKuerzel = BTZASW
searchResult := searchResult "LEFT JOIN BUERO.ASW_AUSWEIS ON ASW_FIRMENNR = BTZ_ASW_FIRMENNR AND ASW_AUSWEISNR = BTZ_ASW_AUSWEISNR" "~"
if joinKuerzel = ASWBTZ
searchResult := searchResult "LEFT JOIN BUERO.BTZ_BTERMINALZUORD ON BTZ_ASW_FIRMENNR = ASW_FIRMENNR AND BTZ_ASW_AUSWEISNR = ASW_AUSWEISNR" "~"
if joinKuerzel = PLGATE
searchResult := searchResult "LEFT JOIN BUERO.ATE_ABTEILUNG ON ATE_FIRMENNR = PLG_ATE_FIRMENNR AND ATE_ABTEILUNGSNR = PLG_ABTEILUNGSNR" "~"
if joinKuerzel = ATEPLG
searchResult := searchResult "LEFT JOIN BUERO.PLG_PLANGRUPPE ON PLG_ATE_FIRMENNR = ATE_FIRMENNR AND PLG_ABTEILUNGSNR = ATE_ABTEILUNGSNR" "~"
if joinKuerzel = BETAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_AUFGABENNR = BET_AUG_AUFGABENNR" "~"
if joinKuerzel = AUGBET
searchResult := searchResult "LEFT JOIN BUERO.BET_BETRIEBSMITTEL ON BET_AUG_AUFGABENNR = AUG_AUFGABENNR" "~"
if joinKuerzel = DRGAUG
searchResult := searchResult "LEFT JOIN AUG_AUFGABE.AUG_AUFGABE ON AUG_AUFGABENNR = DRG_AUFGABENNR" "~"
if joinKuerzel = AUGDRG
searchResult := searchResult "LEFT JOIN HRS.DRG_DIENSTREISEGEN ON DRG_AUFGABENNR = AUG_AUFGABENNR" "~"
if joinKuerzel = SYNAUG
searchResult := searchResult "LEFT JOIN AUG_AUFGABE.AUG_AUFGABE ON AUG_AUFGABENNR = SYN_AUFGABENNR" "~"
if joinKuerzel = AUGSYN
searchResult := searchResult "LEFT JOIN BMD.SYN_SYNCDATEN ON SYN_AUFGABENNR = AUG_AUFGABENNR" "~"
if joinKuerzel = AFKAUT
searchResult := searchResult "LEFT JOIN ZLE.AUT_AUSWERTTYP ON AUT_AUSWERTTYPNR = AFK_AUSWERTTYPNR" "~"
if joinKuerzel = AUTAFK
searchResult := searchResult "LEFT JOIN ZLE.AFK_EXTFILTERKOPF ON AFK_AUSWERTTYPNR = AUT_AUSWERTTYPNR" "~"
if joinKuerzel = ASBAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_FIRMENNR = ASB_AWE_FIRMENNR AND AWE_AUSWERTTYPNR = ASB_AWE_AUSWERTTYPNR AND AWE_AUSWERTNR = ASB_AWE_AUSWERTNR" "~"
if joinKuerzel = AWEASB
searchResult := searchResult "LEFT JOIN ZLE.ASB_AUSWERT_SUB ON ASB_AWE_FIRMENNR = AWE_FIRMENNR AND ASB_AWE_AUSWERTTYPNR = AWE_AUSWERTTYPNR AND ASB_AWE_AUSWERTNR = AWE_AUSWERTNR" "~"
if joinKuerzel = SMIAWR
searchResult := searchResult "LEFT JOIN AWR_AUSWERTUNGSKAT.AWR_AUSWERTUNGSKAT ON AWR_AWRKATID = SMI_AWR_AWRKATID" "~"
if joinKuerzel = AWRSMI
searchResult := searchResult "LEFT JOIN ZLE.SMI_SMARTINTERFACE ON SMI_AWR_AWRKATID = AWR_AWRKATID" "~"
if joinKuerzel = PSWAZB
searchResult := searchResult "LEFT JOIN BUERO.AZB_ARBEITSZEITBEZ ON AZB_FIRMENNR = PSW_AZB_FIRMENNR AND AZB_AMODELLNR = PSW_AMODELLNR" "~"
if joinKuerzel = AZBPSW
searchResult := searchResult "LEFT JOIN BUERO.PSW_PERSONENWERTE ON PSW_AZB_FIRMENNR = AZB_FIRMENNR AND PSW_AMODELLNR = AZB_AMODELLNR" "~"
if joinKuerzel = AZMAZV
searchResult := searchResult "LEFT JOIN BUERO.AZV_ARBEITSZEITVERSION ON AZV_FIRMENNR = AZM_FIRMENNR AND AZV_AMODELLNR = AZM_AMODELLNR AND AZV_AMODELLVERSION = AZM_AMODELLVERSION" "~"
if joinKuerzel = AZVAZM
searchResult := searchResult "LEFT JOIN BUERO.AZM_ARBEITSZEITMODELL ON AZM_FIRMENNR = AZV_FIRMENNR AND AZM_AMODELLNR = AZV_AMODELLNR AND AZM_AMODELLVERSION = AZV_AMODELLVERSION" "~"
if joinKuerzel = ZKRBAI
searchResult := searchResult "LEFT JOIN BAI_BANKINSTITUTIONSZU.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = ZKR_BAI_ZAHLBANKNR" "~"
if joinKuerzel = BAIZKR
searchResult := searchResult "LEFT JOIN FIBU.ZKR_ZAHLUNGSKREIS ON ZKR_BAI_ZAHLBANKNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = RVKBAI
searchResult := searchResult "LEFT JOIN BAI_BANKINSTITUTIONSZU.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = RVK_BANKINSTLFDNR" "~"
if joinKuerzel = BAIRVK
searchResult := searchResult "LEFT JOIN ZLE.RVK_RECHVORLAGEKOPF ON RVK_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = ZVDBAI
searchResult := searchResult "LEFT JOIN BAI_BANKINSTITUTIONSZU.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = ZVD_BAI_ERST_BANKINSTLFDNR" "~"
if joinKuerzel = BAIZVD
searchResult := searchResult "LEFT JOIN ZX_GELOESCHT.ZVD_ZAHLUNGSDETAILS ON ZVD_BAI_ERST_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = ZVKBAI
searchResult := searchResult "LEFT JOIN BAI_BANKINSTITUTIONSZU.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = ZVK_BAI_ZAHLUNGSBANKPERSON" "~"
if joinKuerzel = BAIZVK
searchResult := searchResult "LEFT JOIN FIBU.ZVK_ZAHLUNGSVORSCHLAG ON ZVK_BAI_ZAHLUNGSBANKPERSON = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = FIWBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = FIW_GEM_BANKINSTLFDNR" "~"
if joinKuerzel = BAIFIW
searchResult := searchResult "LEFT JOIN BUERO.FIW_FIRMENWERTE ON FIW_GEM_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = FIWBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = FIW_GER_EMPF_BANKINSTLFDNR" "~"
if joinKuerzel = BAIFIW
searchResult := searchResult "LEFT JOIN BUERO.FIW_FIRMENWERTE ON FIW_GER_EMPF_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = BALBAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_FIRMENNR = BAL_BAZ_FIRMENNR AND BAZ_BAZ = BAL_BAZ_BAZ" "~"
if joinKuerzel = BAZBAL
searchResult := searchResult "LEFT JOIN PRODUKTION.BAL_ARBEITSPLATZLAGERZU ON BAL_BAZ_FIRMENNR = BAZ_FIRMENNR AND BAL_BAZ_BAZ = BAZ_BAZ" "~"
if joinKuerzel = BAZBAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_FIRMENNR = BAZ_REFBAZ_FIRMENNR AND BAZ_BAZ = BAZ_REF_BAZ" "~"
if joinKuerzel = BAZBAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_REFBAZ_FIRMENNR = BAZ_FIRMENNR AND BAZ_REF_BAZ = BAZ_BAZ" "~"
if joinKuerzel = EPSBAZ
searchResult := searchResult "LEFT JOIN BAZ_BAZ.BAZ_BAZ ON BAZ_FIRMENNR = EPS_BAZ_FIRMENNR AND BAZ_BAZ = EPS_BAZ" "~"
if joinKuerzel = BAZEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_BAZ_FIRMENNR = BAZ_FIRMENNR AND EPS_BAZ = BAZ_BAZ" "~"
if joinKuerzel = LOKBAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_FIRMENNR = LOK_BAZ_FIRMENNR AND BAZ_BAZ = LOK_BAZ" "~"
if joinKuerzel = BAZLOK
searchResult := searchResult "LEFT JOIN PRODUKTION.LOK_LOGIKTABELLE_KOPF ON LOK_BAZ_FIRMENNR = BAZ_FIRMENNR AND LOK_BAZ = BAZ_BAZ" "~"
if joinKuerzel = SLPBAZ
searchResult := searchResult "LEFT JOIN BAZ_BAZ.BAZ_BAZ ON BAZ_FIRMENNR = SLP_BAZ_FIRMENNR AND BAZ_BAZ = SLP_BAZ" "~"
if joinKuerzel = BAZSLP
searchResult := searchResult "LEFT JOIN WWS.SLP_STUECKLISTENPOSITION ON SLP_BAZ_FIRMENNR = BAZ_FIRMENNR AND SLP_BAZ = BAZ_BAZ" "~"
if joinKuerzel = PCKBAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_FIRMENNR = PCK_ORIG_BAZ_FIRMENNR AND BAZ_BAZ = PCK_ORIG_BAZ" "~"
if joinKuerzel = BAZPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_ORIG_BAZ_FIRMENNR = BAZ_FIRMENNR AND PCK_ORIG_BAZ = BAZ_BAZ" "~"
if joinKuerzel = VFABAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_FIRMENNR = VFA_BAZ_FIRMENNR AND BAZ_BAZ = VFA_BAZ" "~"
if joinKuerzel = BAZVFA
searchResult := searchResult "LEFT JOIN PRODUKTION.VFA_VERFAHRENSANWEISUNG_KOPF ON VFA_BAZ_FIRMENNR = BAZ_FIRMENNR AND VFA_BAZ = BAZ_BAZ" "~"
if joinKuerzel = MITBCU
searchResult := searchResult "LEFT JOIN BUERO.BCU_BMDCOMUSER ON BCU_BMDCOMUSERID = MIT_BMDCOMUSERID" "~"
if joinKuerzel = BCUMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_BMDCOMUSERID = BCU_BMDCOMUSERID" "~"
if joinKuerzel = BKUBEF
searchResult := searchResult "LEFT JOIN BUERO.BEF_BEFRAGUNG ON BEF_BEFRAGUNGSNR = BKU_BEFRAGUNGSNR" "~"
if joinKuerzel = BEFBKU
searchResult := searchResult "LEFT JOIN BUERO.BKU_BEFRAGUNGKRITERIUMZU ON BKU_BEFRAGUNGSNR = BEF_BEFRAGUNGSNR" "~"
if joinKuerzel = DVHBEW
searchResult := searchResult "LEFT JOIN BUERO.BEW_BEWERTUNGSMODELL ON BEW_FIRMENNR = DVH_BEW_FIRMENNR AND BEW_BEWMODELLNR = DVH_BEWMODELLNR" "~"
if joinKuerzel = BEWDVH
searchResult := searchResult "LEFT JOIN BUERO.DVH_DIENSTVERHAELTNIS ON DVH_BEW_FIRMENNR = BEW_FIRMENNR AND DVH_BEWMODELLNR = BEW_BEWMODELLNR" "~"
if joinKuerzel = PSWBEW
searchResult := searchResult "LEFT JOIN BUERO.BEW_BEWERTUNGSMODELL ON BEW_FIRMENNR = PSW_BEW_FIRMENNR AND BEW_BEWMODELLNR = PSW_BEWMODELLNR" "~"
if joinKuerzel = BEWPSW
searchResult := searchResult "LEFT JOIN BUERO.PSW_PERSONENWERTE ON PSW_BEW_FIRMENNR = BEW_FIRMENNR AND PSW_BEWMODELLNR = BEW_BEWMODELLNR" "~"
if joinKuerzel = BGDBFI
searchResult := searchResult "LEFT JOIN HRS.BFI_BUDGETFIRMA ON BFI_BFIFIRMENNR = BGD_BFI_FIRMENNR" "~"
if joinKuerzel = BFIBGD
searchResult := searchResult "LEFT JOIN HRS.BGD_BUDGETGLDNGBAUMDATA ON BGD_BFI_FIRMENNR = BFI_BFIFIRMENNR" "~"
if joinKuerzel = BGPBGS
searchResult := searchResult "LEFT JOIN HRS.BGS_BEITRAGSGRUPPE_SEQ ON BGS_BEITRAGSGRUPPEID = BGP_SID_LEHRLING4" "~"
if joinKuerzel = BGSBGP
searchResult := searchResult "LEFT JOIN HRS.BGP_BEITRAGSGRUPPE ON BGP_SID_LEHRLING4 = BGS_BEITRAGSGRUPPEID" "~"
if joinKuerzel = BGPBGS
searchResult := searchResult "LEFT JOIN HRS.BGS_BEITRAGSGRUPPE_SEQ ON BGS_BEITRAGSGRUPPEID = BGP_SID_LEHRLING1_HA" "~"
if joinKuerzel = BGSBGP
searchResult := searchResult "LEFT JOIN HRS.BGP_BEITRAGSGRUPPE ON BGP_SID_LEHRLING1_HA = BGS_BEITRAGSGRUPPEID" "~"
if joinKuerzel = BGPBGS
searchResult := searchResult "LEFT JOIN HRS.BGS_BEITRAGSGRUPPE_SEQ ON BGS_BEITRAGSGRUPPEID = BGP_VOLLGERING_BGPID" "~"
if joinKuerzel = BGSBGP
searchResult := searchResult "LEFT JOIN HRS.BGP_BEITRAGSGRUPPE ON BGP_VOLLGERING_BGPID = BGS_BEITRAGSGRUPPEID" "~"
if joinKuerzel = KMPBKZ
searchResult := searchResult "LEFT JOIN BKZ_BILANZKENNZAHL.BKZ_BILANZKENNZAHL ON BKZ_FIRMENNR = KMP_BKZ_FIRMENNR AND BKZ_ISTEIGENE = KMP_BKZ_ISTEIGENE AND BKZ_KZNR = KMP_BKZ_KZNR AND BKZ_KZPAKETNR = KMP_BKZ_KZPAKETNR" "~"
if joinKuerzel = BKZKMP
searchResult := searchResult "LEFT JOIN KORE.KMP_PLANMENGEN ON KMP_BKZ_FIRMENNR = BKZ_FIRMENNR AND KMP_BKZ_ISTEIGENE = BKZ_ISTEIGENE AND KMP_BKZ_KZNR = BKZ_KZNR AND KMP_BKZ_KZPAKETNR = BKZ_KZPAKETNR" "~"
if joinKuerzel = SFEAVS
searchResult := searchResult "LEFT JOIN BUERO.AVS_AVSTATUS ON AVS_FIRMENNR = SFE_AVS_FIRMENNR AND AVS_STATUSTYP = SFE_AVS_STATUSTYP AND AVS_STATUSNR = SFE_AVS_STATUSNR" "~"
if joinKuerzel = AVSSFE
searchResult := searchResult "LEFT JOIN BUERO.SFE_SKILLFINDERELEMENT ON SFE_AVS_FIRMENNR = AVS_FIRMENNR AND SFE_AVS_STATUSTYP = AVS_STATUSTYP AND SFE_AVS_STATUSNR = AVS_STATUSNR" "~"
if joinKuerzel = AWSAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_FIRMENNR = AWS_FIRMENNR AND AWE_AUSWERTTYPNR = AWS_AUSWERTTYPNR AND AWE_AUSWERTNR = AWS_AUSWERTNR" "~"
if joinKuerzel = AWEAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_FIRMENNR = AWE_FIRMENNR AND AWS_AUSWERTTYPNR = AWE_AUSWERTTYPNR AND AWS_AUSWERTNR = AWE_AUSWERTNR" "~"
if joinKuerzel = BAZAZB
searchResult := searchResult "LEFT JOIN AZB_ARBEITSZEITBEZ.AZB_ARBEITSZEITBEZ ON AZB_FIRMENNR = BAZ_AZB_FIRMENNR AND AZB_AMODELLNR = BAZ_AMODELLNR" "~"
if joinKuerzel = AZBBAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_AZB_FIRMENNR = AZB_FIRMENNR AND BAZ_AMODELLNR = AZB_AMODELLNR" "~"
if joinKuerzel = PLPAZB
searchResult := searchResult "LEFT JOIN AZB_ARBEITSZEITBEZ.AZB_ARBEITSZEITBEZ ON AZB_FIRMENNR = PLP_AZB_FIRMENNR AND AZB_AMODELLNR = PLP_AZB_AMODELLNR" "~"
if joinKuerzel = AZBPLP
searchResult := searchResult "LEFT JOIN PRODUKTION.PLP_PLANUNGSPERIODE ON PLP_AZB_FIRMENNR = AZB_FIRMENNR AND PLP_AZB_AMODELLNR = AZB_AMODELLNR" "~"
if joinKuerzel = BUCBAI
searchResult := searchResult "LEFT JOIN BAI_BANKINSTITUTIONSZU.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = BUC_FIRMA_BAI_LFDNR" "~"
if joinKuerzel = BAIBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_FIRMA_BAI_LFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = FGKBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = FGK_EMPF_BANKINSTLFDNR" "~"
if joinKuerzel = BAIFGK
searchResult := searchResult "LEFT JOIN BUERO.FGK_FIRMENGKKZU ON FGK_EMPF_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = ZASBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = ZAS_BANKINSTLFDNR" "~"
if joinKuerzel = BAIZAS
searchResult := searchResult "LEFT JOIN BUERO.ZAS_ZAHLSCHEIN ON ZAS_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = BUCBAI
searchResult := searchResult "LEFT JOIN BAI_BANKINSTITUTIONSZU.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = BUC_PERSON_BAI_LFDNR" "~"
if joinKuerzel = BAIBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_PERSON_BAI_LFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = ERBBAI
searchResult := searchResult "LEFT JOIN BAI_BANKINSTITUTIONSZU.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = ERB_EIGENE_BANKINSTLFDNR" "~"
if joinKuerzel = BAIERB
searchResult := searchResult "LEFT JOIN FIBU.ERB_BUCHUNG ON ERB_EIGENE_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = FGMBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = FGM_SENDER_BANKINSTLFDNR" "~"
if joinKuerzel = BAIFGM
searchResult := searchResult "LEFT JOIN BUERO.FGM_FIRMENGEMEINDEZU ON FGM_SENDER_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = FGRBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = FGR_EMPF_BANKINSTLFDNR" "~"
if joinKuerzel = BAIFGR
searchResult := searchResult "LEFT JOIN BUERO.FGR_FIRMENGERICHTZU ON FGR_EMPF_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = BAIBAK
searchResult := searchResult "LEFT JOIN BUERO.BAK_BANK ON BAK_LANDNR = BAI_BAK_LANDNR AND BAK_BANKLEITZAHL = BAI_BANKLEITZAHL AND BAK_BANKNR = BAI_BANKNR" "~"
if joinKuerzel = BAKBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_BAK_LANDNR = BAK_LANDNR AND BAI_BANKLEITZAHL = BAK_BANKLEITZAHL AND BAI_BANKNR = BAK_BANKNR" "~"
if joinKuerzel = KUZBAK
searchResult := searchResult "LEFT JOIN BUERO.BAK_BANK ON BAK_LANDNR = KUZ_BAK_LANDNR AND BAK_BANKLEITZAHL = KUZ_BANKLEITZAHL AND BAK_BANKNR = KUZ_BANKNR" "~"
if joinKuerzel = BAKKUZ
searchResult := searchResult "LEFT JOIN BUERO.KUZ_KUNDENZAHLUNGEN ON KUZ_BAK_LANDNR = BAK_LANDNR AND KUZ_BANKLEITZAHL = BAK_BANKLEITZAHL AND KUZ_BANKNR = BAK_BANKNR" "~"
if joinKuerzel = BATBEM
searchResult := searchResult "LEFT JOIN BMD.BEM_BMDEMAIL ON BEM_BEMID = BAT_BEMID" "~"
if joinKuerzel = BEMBAT
searchResult := searchResult "LEFT JOIN BMD.BAT_BMDATTACHMENT ON BAT_BEMID = BEM_BEMID" "~"
if joinKuerzel = STTBER
searchResult := searchResult "LEFT JOIN BER_BERUF.BER_BERUF ON BER_BERUFSNR = STT_BER_BERUFSNR" "~"
if joinKuerzel = BERSTT
searchResult := searchResult "LEFT JOIN PRODUKTION.STT_STAKE_TYP ON STT_BER_BERUFSNR = BER_BERUFSNR" "~"
if joinKuerzel = BWIBES
searchResult := searchResult "LEFT JOIN BUERO.BES_BEWERBUNGSTATUS ON BES_BEWSTATUSNR = BWI_BES_BEWSTATUSNR" "~"
if joinKuerzel = BESBWI
searchResult := searchResult "LEFT JOIN BUERO.BWI_BEWERBUNGSINFO ON BWI_BES_BEWSTATUSNR = BES_BEWSTATUSNR" "~"
if joinKuerzel = GEPBFK
searchResult := searchResult "LEFT JOIN WWS.BFK_BEFOERDKAT ON BFK_BEFOERDERGKAT = GEP_BEFOERDERGKAT" "~"
if joinKuerzel = BFKGEP
searchResult := searchResult "LEFT JOIN WWS.GEP_GEFAHRENGUTPOS ON GEP_BEFOERDERGKAT = BFK_BEFOERDERGKAT" "~"
if joinKuerzel = BGDBGD
searchResult := searchResult "LEFT JOIN HRS.BGD_BUDGETGLDNGBAUMDATA ON BGD_BGDID = BGD_PARENTDATENID" "~"
if joinKuerzel = BGDBGD
searchResult := searchResult "LEFT JOIN HRS.BGD_BUDGETGLDNGBAUMDATA ON BGD_PARENTDATENID = BGD_BGDID" "~"
if joinKuerzel = BGPBGS
searchResult := searchResult "LEFT JOIN HRS.BGS_BEITRAGSGRUPPE_SEQ ON BGS_BEITRAGSGRUPPEID = BGP_BONUS_BGPID" "~"
if joinKuerzel = BGSBGP
searchResult := searchResult "LEFT JOIN HRS.BGP_BEITRAGSGRUPPE ON BGP_BONUS_BGPID = BGS_BEITRAGSGRUPPEID" "~"
if joinKuerzel = PSMBGS
searchResult := searchResult "LEFT JOIN HRS.BGS_BEITRAGSGRUPPE_SEQ ON BGS_BEITRAGSGRUPPEID = PSM_BGS_VG_WECHSEL_ID" "~"
if joinKuerzel = BGSPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_BGS_VG_WECHSEL_ID = BGS_BEITRAGSGRUPPEID" "~"
if joinKuerzel = BGPBGS
searchResult := searchResult "LEFT JOIN HRS.BGS_BEITRAGSGRUPPE_SEQ ON BGS_BEITRAGSGRUPPEID = BGP_LEHRLING_BGPID" "~"
if joinKuerzel = BGSBGP
searchResult := searchResult "LEFT JOIN HRS.BGP_BEITRAGSGRUPPE ON BGP_LEHRLING_BGPID = BGS_BEITRAGSGRUPPEID" "~"
if joinKuerzel = BSDARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = BSD_ARL_FIRMENNR AND ARL_ARTIKELNR = BSD_ARTIKELNR" "~"
if joinKuerzel = ARLBSD
searchResult := searchResult "LEFT JOIN WWS.BSD_BONUSERMDETAIL ON BSD_ARL_FIRMENNR = ARL_FIRMENNR AND BSD_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = CHGARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = CHG_VALID_ARL_FIRMENNR AND ARL_ARTIKELNR = CHG_ARTIKELNR" "~"
if joinKuerzel = ARLCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_VALID_ARL_FIRMENNR = ARL_FIRMENNR AND CHG_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = DISARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = DIS_ARL_FIRMENNR AND ARL_ARTIKELNR = DIS_ARTIKELNR" "~"
if joinKuerzel = ARLDIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_ARL_FIRMENNR = ARL_FIRMENNR AND DIS_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = GAWARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = GAW_ARL_FIRMENNR AND ARL_ARTIKELNR = GAW_ARTIKELNR" "~"
if joinKuerzel = ARLGAW
searchResult := searchResult "LEFT JOIN ZLE.GAW_GEBUEHRARTIKELWERT ON GAW_ARL_FIRMENNR = ARL_FIRMENNR AND GAW_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = GZUARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = GZU_ARL_FIRMENNR AND ARL_ARTIKELNR = GZU_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLGZU
searchResult := searchResult "LEFT JOIN WWS.GZU_GEFAHRENGUTZU ON GZU_ARL_FIRMENNR = ARL_FIRMENNR AND GZU_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = HAZARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = HAZ_ARL_FIRMENNR AND ARL_ARTIKELNR = HAZ_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLHAZ
searchResult := searchResult "LEFT JOIN WWS.HAZ_HAUPTLIEFARTIKELZU ON HAZ_ARL_FIRMENNR = ARL_FIRMENNR AND HAZ_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = KOCARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = KOC_ARL_FIRMENNR AND ARL_ARTIKELNR = KOC_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLKOC
searchResult := searchResult "LEFT JOIN PRODUKTION.KOC_KOMBI_CHARGE ON KOC_ARL_FIRMENNR = ARL_FIRMENNR AND KOC_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = LEIARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = LEI_ARL_FIRMENNR AND ARL_ARTIKELNR = LEI_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_ARL_FIRMENNR = ARL_FIRMENNR AND LEI_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = LKOARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = LKO_ARTIKELIDFIRMENNR AND ARL_ARTIKELNR = LKO_ARTIKELNR" "~"
if joinKuerzel = ARLLKO
searchResult := searchResult "LEFT JOIN WWS.LKO_LAGERKONTO ON LKO_ARTIKELIDFIRMENNR = ARL_FIRMENNR AND LKO_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = MRDARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = MRD_ARL_FIRMENNR AND ARL_ARTIKELNR = MRD_ARTIKELNR" "~"
if joinKuerzel = ARLMRD
searchResult := searchResult "LEFT JOIN ZLE.MRD_MONATSRECHNUNGDETAIL ON MRD_ARL_FIRMENNR = ARL_FIRMENNR AND MRD_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = PIZARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = PIZ_ARL_FIRMENNR AND ARL_ARTIKELNR = PIZ_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLPIZ
searchResult := searchResult "LEFT JOIN WWS.PIZ_PICARTIKELZU ON PIZ_ARL_FIRMENNR = ARL_FIRMENNR AND PIZ_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = PRPARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = PRP_ARL_FIRMENNR AND ARL_ARTIKELNR = PRP_ARTIKELNR" "~"
if joinKuerzel = ARLPRP
searchResult := searchResult "LEFT JOIN WWS.PRP_PROVISIONTABPOS ON PRP_ARL_FIRMENNR = ARL_FIRMENNR AND PRP_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = QMAARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = QMA_ARL_FIRMENNR AND ARL_ARTIKELNR = QMA_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLQMA
searchResult := searchResult "LEFT JOIN PRODUKTION.QMA_QM_ARTIKEL ON QMA_ARL_FIRMENNR = ARL_FIRMENNR AND QMA_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = RADARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = RAD_ARL_FIRMENNR AND ARL_ARTIKELNR = RAD_ARTIKELNR" "~"
if joinKuerzel = ARLRAD
searchResult := searchResult "LEFT JOIN WWS.RAD_RABATTBETRAGDETAIL ON RAD_ARL_FIRMENNR = ARL_FIRMENNR AND RAD_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = TPZARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = TPZ_ARL_FIRMENNR AND ARL_ARTIKELNR = TPZ_ARTIKELNR" "~"
if joinKuerzel = ARLTPZ
searchResult := searchResult "LEFT JOIN ZLE.TPZ_TAETIGKEITPAUSCHALEZU ON TPZ_ARL_FIRMENNR = ARL_FIRMENNR AND TPZ_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = WSKARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = WSK_HAUPTFIRMENNR AND ARL_ARTIKELNR = WSK_ARTIKELNR" "~"
if joinKuerzel = ARLWSK
searchResult := searchResult "LEFT JOIN WWS.WSK_WWSKORESTATISTIK ON WSK_HAUPTFIRMENNR = ARL_FIRMENNR AND WSK_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = ARLARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = ARL_FIRMENNR AND ARL_ARTIKELNR = ARL_KOMBI_ARTIKELNR" "~"
if joinKuerzel = ARLARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = ARL_FIRMENNR AND ARL_KOMBI_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = AGPARS
searchResult := searchResult "LEFT JOIN WWS.ARS_ARTIKELSTATISTIK ON ARS_FIRMENNR = AGP_FIRMENNR AND ARS_STATISTIKLFDNR = AGP_STATISTIKLFDNR" "~"
if joinKuerzel = ARSAGP
searchResult := searchResult "LEFT JOIN WWS.AGP_ARTIKELGRUPPENPOS ON AGP_FIRMENNR = ARS_FIRMENNR AND AGP_STATISTIKLFDNR = ARS_STATISTIKLFDNR" "~"
if joinKuerzel = LEIASC
searchResult := searchResult "LEFT JOIN ZLE.ASC_ARBEITSSCHEIN ON ASC_ASCID = LEI_ASC_ASCID" "~"
if joinKuerzel = ASCLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_ASC_ASCID = ASC_ASCID" "~"
if joinKuerzel = BWBASR
searchResult := searchResult "LEFT JOIN BUERO.ASR_AUSSCHREIBUNG ON ASR_FIRMENNR = BWB_ASR_FIRMENNR AND ASR_AUSSCHREIBUNGSNR = BWB_ASR_AUSSCHREIBUNGSNR" "~"
if joinKuerzel = ASRBWB
searchResult := searchResult "LEFT JOIN BUERO.BWB_BEWERBUNG ON BWB_ASR_FIRMENNR = ASR_FIRMENNR AND BWB_ASR_AUSSCHREIBUNGSNR = ASR_AUSSCHREIBUNGSNR" "~"
if joinKuerzel = ZABASW
searchResult := searchResult "LEFT JOIN BUERO.ASW_AUSWEIS ON ASW_FIRMENNR = ZAB_ASW_FIRMENNR AND ASW_AUSWEISNR = ZAB_ASW_AUSWEISNR" "~"
if joinKuerzel = ASWZAB
searchResult := searchResult "LEFT JOIN BUERO.ZAB_AUSWEISBUCHARTZU ON ZAB_ASW_FIRMENNR = ASW_FIRMENNR AND ZAB_ASW_AUSWEISNR = ASW_AUSWEISNR" "~"
if joinKuerzel = DVHAZB
searchResult := searchResult "LEFT JOIN BUERO.AZB_ARBEITSZEITBEZ ON AZB_FIRMENNR = DVH_AZB_FIRMENNR AND AZB_AMODELLNR = DVH_AMODELLNR" "~"
if joinKuerzel = AZBDVH
searchResult := searchResult "LEFT JOIN BUERO.DVH_DIENSTVERHAELTNIS ON DVH_AZB_FIRMENNR = AZB_FIRMENNR AND DVH_AMODELLNR = AZB_AMODELLNR" "~"
if joinKuerzel = AZDAZM
searchResult := searchResult "LEFT JOIN BUERO.AZM_ARBEITSZEITMODELL ON AZM_FIRMENNR = AZD_FIRMENNR AND AZM_AMODELLNR = AZD_AMODELLNR AND AZM_AMODELLVERSION = AZD_AMODELLVERSION AND AZM_AMODELLTAG = AZD_AMODELLTAG" "~"
if joinKuerzel = AZMAZD
searchResult := searchResult "LEFT JOIN BUERO.AZD_ARBEITSZEITMODELLDET ON AZD_FIRMENNR = AZM_FIRMENNR AND AZD_AMODELLNR = AZM_AMODELLNR AND AZD_AMODELLVERSION = AZM_AMODELLVERSION AND AZD_AMODELLTAG = AZM_AMODELLTAG" "~"
if joinKuerzel = BKRBAI
searchResult := searchResult "LEFT JOIN BAI_BANKINSTITUTIONSZU.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = BKR_BAI_BANKINSTLFDNR" "~"
if joinKuerzel = BAIBKR
searchResult := searchResult "LEFT JOIN FIBU.BKR_BANKEINZUGSKREIS ON BKR_BAI_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = FGMBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = FGM_EMPF_BANKINSTLFDNR" "~"
if joinKuerzel = BAIFGM
searchResult := searchResult "LEFT JOIN BUERO.FGM_FIRMENGEMEINDEZU ON FGM_EMPF_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = PROBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = PRO_FIRMA_BANKINSTLFDNR" "~"
if joinKuerzel = BAIPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMA_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = ZSUBAI
searchResult := searchResult "LEFT JOIN BAI_BANKINSTITUTIONSZU.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = ZSU_BAI_ZAHLUNGSBANKFIRMA" "~"
if joinKuerzel = BAIZSU
searchResult := searchResult "LEFT JOIN FIBU.ZSU_ZAHLSUMME ON ZSU_BAI_ZAHLUNGSBANKFIRMA = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = FFABAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = FFA_SENDER_BANKINSTLFDNR" "~"
if joinKuerzel = BAIFFA
searchResult := searchResult "LEFT JOIN BUERO.FFA_FIRMENFINANZAMTZU ON FFA_SENDER_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = ZVKBAI
searchResult := searchResult "LEFT JOIN BAI_BANKINSTITUTIONSZU.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = ZVK_BAI_ZAHLUNGSBANKFIRMA" "~"
if joinKuerzel = BAIZVK
searchResult := searchResult "LEFT JOIN FIBU.ZVK_ZAHLUNGSVORSCHLAG ON ZVK_BAI_ZAHLUNGSBANKFIRMA = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = AGSBAZ
searchResult := searchResult "LEFT JOIN BAZ_BAZ.BAZ_BAZ ON BAZ_FIRMENNR = AGS_BAZ_FIRMENNR AND BAZ_BAZ = AGS_BAZ" "~"
if joinKuerzel = BAZAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_BAZ_FIRMENNR = BAZ_FIRMENNR AND AGS_BAZ = BAZ_BAZ" "~"
if joinKuerzel = PTZBAZ
searchResult := searchResult "LEFT JOIN BAZ_BAZ.BAZ_BAZ ON BAZ_FIRMENNR = PTZ_BAZ_FIRMENNR AND BAZ_BAZ = PTZ_BAZ_BAZ" "~"
if joinKuerzel = BAZPTZ
searchResult := searchResult "LEFT JOIN WWS.PTZ_PREISTABZU ON PTZ_BAZ_FIRMENNR = BAZ_FIRMENNR AND PTZ_BAZ_BAZ = BAZ_BAZ" "~"
if joinKuerzel = BURBCU
searchResult := searchResult "LEFT JOIN BUERO.BCU_BMDCOMUSER ON BCU_BMDCOMUSERID = BUR_BMDCOMUSERID" "~"
if joinKuerzel = BCUBUR
searchResult := searchResult "LEFT JOIN BUERO.BUR_BMDCOMUPLOADTREE ON BUR_BMDCOMUSERID = BCU_BMDCOMUSERID" "~"
if joinKuerzel = WCUBCU
searchResult := searchResult "LEFT JOIN BCU_BMDCOMUSER.BCU_BMDCOMUSER ON BCU_BMDCOMUSERID = WCU_BMDCOMUSERID" "~"
if joinKuerzel = BCUWCU
searchResult := searchResult "LEFT JOIN BMD.WCU_WAAUSDRUCKUSERZU ON WCU_BMDCOMUSERID = BCU_BMDCOMUSERID" "~"
if joinKuerzel = WCWBCU
searchResult := searchResult "LEFT JOIN BCU_BMDCOMUSER.BCU_BMDCOMUSER ON BCU_BMDCOMUSERID = WCW_BMDCOMUSERID" "~"
if joinKuerzel = BCUWCW
searchResult := searchResult "LEFT JOIN BMD.WCW_WAAUSDRUCKPARAMWERT ON WCW_BMDCOMUSERID = BCU_BMDCOMUSERID" "~"
if joinKuerzel = AWSBES
searchResult := searchResult "LEFT JOIN BES_BEWERBUNGSTATUS.BES_BEWERBUNGSTATUS ON BES_BEWSTATUSNR = AWS_BES_BEWSTATUSNR" "~"
if joinKuerzel = BESAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_BES_BEWSTATUSNR = BES_BEWSTATUSNR" "~"
if joinKuerzel = ZSGBEW
searchResult := searchResult "LEFT JOIN BUERO.BEW_BEWERTUNGSMODELL ON BEW_FIRMENNR = ZSG_BEW_FIRMENNR AND BEW_BEWMODELLNR = ZSG_BEWMODELLNR" "~"
if joinKuerzel = BEWZSG
searchResult := searchResult "LEFT JOIN BUERO.ZSG_ZEITSALDENGRENZWERT ON ZSG_BEW_FIRMENNR = BEW_FIRMENNR AND ZSG_BEWMODELLNR = BEW_BEWMODELLNR" "~"
if joinKuerzel = ZUBBEW
searchResult := searchResult "LEFT JOIN BUERO.BEW_BEWERTUNGSMODELL ON BEW_FIRMENNR = ZUB_BEW_FIRMENNR AND BEW_BEWMODELLNR = ZUB_BEWMODELLNR" "~"
if joinKuerzel = BEWZUB
searchResult := searchResult "LEFT JOIN BUERO.ZUB_ZEITUMBUCHBEWERTMDZU ON ZUB_BEW_FIRMENNR = BEW_FIRMENNR AND ZUB_BEWMODELLNR = BEW_BEWMODELLNR" "~"
if joinKuerzel = ZUZBEW
searchResult := searchResult "LEFT JOIN BUERO.BEW_BEWERTUNGSMODELL ON BEW_FIRMENNR = ZUZ_BEW_FIRMENNR AND BEW_BEWMODELLNR = ZUZ_BEWMODELLNR" "~"
if joinKuerzel = BEWZUZ
searchResult := searchResult "LEFT JOIN BUERO.ZUZ_ZEITUMBUCHZUORD ON ZUZ_BEW_FIRMENNR = BEW_FIRMENNR AND ZUZ_BEWMODELLNR = BEW_BEWMODELLNR" "~"
if joinKuerzel = BGPBGS
searchResult := searchResult "LEFT JOIN HRS.BGS_BEITRAGSGRUPPE_SEQ ON BGS_BEITRAGSGRUPPEID = BGP_SID_LEHRLING3" "~"
if joinKuerzel = BGSBGP
searchResult := searchResult "LEFT JOIN HRS.BGP_BEITRAGSGRUPPE ON BGP_SID_LEHRLING3 = BGS_BEITRAGSGRUPPEID" "~"
if joinKuerzel = BGPBGS
searchResult := searchResult "LEFT JOIN HRS.BGS_BEITRAGSGRUPPE_SEQ ON BGS_BEITRAGSGRUPPEID = BGP_LEHRLING_PV_BGPID" "~"
if joinKuerzel = BGSBGP
searchResult := searchResult "LEFT JOIN HRS.BGP_BEITRAGSGRUPPE ON BGP_LEHRLING_PV_BGPID = BGS_BEITRAGSGRUPPEID" "~"
if joinKuerzel = GEGBKZ
searchResult := searchResult "LEFT JOIN BKZ_BILANZKENNZAHL.BKZ_BILANZKENNZAHL ON BKZ_FIRMENNR = GEG_BKZ_FIRMENNR AND BKZ_ISTEIGENE = GEG_BKZ_ISTEIGENE AND BKZ_KZNR = GEG_BKZ_KZNR AND BKZ_KZPAKETNR = GEG_BKZ_KZPAKETNR" "~"
if joinKuerzel = BKZGEG
searchResult := searchResult "LEFT JOIN ZLE.GEG_GEGENSTANDSWERTE ON GEG_BKZ_FIRMENNR = BKZ_FIRMENNR AND GEG_BKZ_ISTEIGENE = BKZ_ISTEIGENE AND GEG_BKZ_KZNR = BKZ_KZNR AND GEG_BKZ_KZPAKETNR = BKZ_KZPAKETNR" "~"
if joinKuerzel = MEDARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = MED_ARL_FIRMENNR AND ARL_ARTIKELNR = MED_ARTIKELNR" "~"
if joinKuerzel = ARLMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_ARL_FIRMENNR = ARL_FIRMENNR AND MED_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = PRSARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = PRS_ARL_FIRMENNR AND ARL_ARTIKELNR = PRS_ARL_TAETIGKEIT" "~"
if joinKuerzel = ARLPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_ARL_FIRMENNR = ARL_FIRMENNR AND PRS_ARL_TAETIGKEIT = ARL_ARTIKELNR" "~"
if joinKuerzel = RNPARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = RNP_ARL_FIRMENNR AND ARL_ARTIKELNR = RNP_ARTIKELNR" "~"
if joinKuerzel = ARLRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_ARL_FIRMENNR = ARL_FIRMENNR AND RNP_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = WFDARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = WFD_ARL_FIRMENNR AND ARL_ARTIKELNR = WFD_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLWFD
searchResult := searchResult "LEFT JOIN WWS.WFD_FOLGEARTIKELDETAIL ON WFD_ARL_FIRMENNR = ARL_FIRMENNR AND WFD_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = ZBUARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = ZBU_TAETIGKEIT_FIRMENNR AND ARL_ARTIKELNR = ZBU_TAETIGKEIT_ARTIKELNR" "~"
if joinKuerzel = ARLZBU
searchResult := searchResult "LEFT JOIN BUERO.ZBU_ZEITBUCHUNG ON ZBU_TAETIGKEIT_FIRMENNR = ARL_FIRMENNR AND ZBU_TAETIGKEIT_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = AARARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = AAR_ARL_FIRMENNR AND ARL_ARTIKELNR = AAR_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLAAR
searchResult := searchResult "LEFT JOIN WWS.AAR_ALTERNATIVARTIKEL ON AAR_ARL_FIRMENNR = ARL_FIRMENNR AND AAR_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = AVEARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = AVE_ARL_FIRMENNR AND ARL_ARTIKELNR = AVE_ERPESD_ARTIKELNR" "~"
if joinKuerzel = ARLAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ARL_FIRMENNR = ARL_FIRMENNR AND AVE_ERPESD_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = AVEARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = AVE_ARL_FIRMENNR AND ARL_ARTIKELNR = AVE_EFKESD_ARTIKELNR" "~"
if joinKuerzel = ARLAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ARL_FIRMENNR = ARL_FIRMENNR AND AVE_EFKESD_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = TSKARS
searchResult := searchResult "LEFT JOIN WWS.ARS_ARTIKELSTATISTIK ON ARS_FIRMENNR = TSK_ARS_FIRMENNR AND ARS_STATISTIKLFDNR = TSK_ARS_STATISTIKLFDNR" "~"
if joinKuerzel = ARSTSK
searchResult := searchResult "LEFT JOIN WWS.TSK_TOUCHSCREENKOPF ON TSK_ARS_FIRMENNR = ARS_FIRMENNR AND TSK_ARS_STATISTIKLFDNR = ARS_STATISTIKLFDNR" "~"
if joinKuerzel = UMSATE
searchResult := searchResult "LEFT JOIN ATE_ABTEILUNG.ATE_ABTEILUNG ON ATE_FIRMENNR = UMS_ATE_FIRMENNR AND ATE_ABTEILUNGSNR = UMS_ABTEILUNGSNR" "~"
if joinKuerzel = ATEUMS
searchResult := searchResult "LEFT JOIN WWS.UMS_UMSATZZEILE ON UMS_ATE_FIRMENNR = ATE_FIRMENNR AND UMS_ABTEILUNGSNR = ATE_ABTEILUNGSNR" "~"
if joinKuerzel = AWEAUT
searchResult := searchResult "LEFT JOIN ZLE.AUT_AUSWERTTYP ON AUT_AUSWERTTYPNR = AWE_AUSWERTTYPNR" "~"
if joinKuerzel = AUTAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_AUSWERTTYPNR = AUT_AUSWERTTYPNR" "~"
if joinKuerzel = ABLAVB
searchResult := searchResult "LEFT JOIN BUERO.AVB_AVBUDGET ON AVB_FIRMENNR = ABL_FIRMENNR AND AVB_KORENR = ABL_KORENR AND AVB_BUDGETNR = ABL_BUDGETNR" "~"
if joinKuerzel = AVBABL
searchResult := searchResult "LEFT JOIN BUERO.ABL_AVBUDGETDETAIL ON ABL_FIRMENNR = AVB_FIRMENNR AND ABL_KORENR = AVB_KORENR AND ABL_BUDGETNR = AVB_BUDGETNR" "~"
if joinKuerzel = AWZAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_FIRMENNR = AWZ_AWE_FIRMENNR AND AWE_AUSWERTTYPNR = AWZ_AWE_AUSWERTTYPNR AND AWE_AUSWERTNR = AWZ_AWE_AUSWERTNR" "~"
if joinKuerzel = AWEAWZ
searchResult := searchResult "LEFT JOIN ZLE.AWZ_AUSWERTUNGZU ON AWZ_AWE_FIRMENNR = AWE_FIRMENNR AND AWZ_AWE_AUSWERTTYPNR = AWE_AUSWERTTYPNR AND AWZ_AWE_AUSWERTNR = AWE_AUSWERTNR" "~"
if joinKuerzel = PKDAZB
searchResult := searchResult "LEFT JOIN AZB_ARBEITSZEITBEZ.AZB_ARBEITSZEITBEZ ON AZB_FIRMENNR = PKD_AZB_FIRMENNR AND AZB_AMODELLNR = PKD_AZB_AMODELLNR" "~"
if joinKuerzel = AZBPKD
searchResult := searchResult "LEFT JOIN PRODUKTION.PKD_KM_DETAIL ON PKD_AZB_FIRMENNR = AZB_FIRMENNR AND PKD_AZB_AMODELLNR = AZB_AMODELLNR" "~"
if joinKuerzel = EFKBAI
searchResult := searchResult "LEFT JOIN BAI_BANKINSTITUTIONSZU.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = EFK_KUNDE_BANKINSTLFDNR" "~"
if joinKuerzel = BAIEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_KUNDE_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = MITBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = MIT_LOHN_BANKINSTLFDNR" "~"
if joinKuerzel = BAIMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_LOHN_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = FIWBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = FIW_FIN_EMPF_BANKINSTLFDNR" "~"
if joinKuerzel = BAIFIW
searchResult := searchResult "LEFT JOIN BUERO.FIW_FIRMENWERTE ON FIW_FIN_EMPF_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = HNPBAI
searchResult := searchResult "LEFT JOIN BAI_BANKINSTITUTIONSZU.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = HNP_KUNDE_BANKINSTLFDNR" "~"
if joinKuerzel = BAIHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_KUNDE_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = YBUBAI
searchResult := searchResult "LEFT JOIN BAI_BANKINSTITUTIONSZU.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = YBU_PERSON_BAI_LFDNR" "~"
if joinKuerzel = BAIYBU
searchResult := searchResult "LEFT JOIN FIBU.YBU_AUTOBUCHUNG ON YBU_PERSON_BAI_LFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = FIWBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = FIW_GEM_EMPF_BANKINSTLFDNR" "~"
if joinKuerzel = BAIFIW
searchResult := searchResult "LEFT JOIN BUERO.FIW_FIRMENWERTE ON FIW_GEM_EMPF_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = EFKBAI
searchResult := searchResult "LEFT JOIN BAI_BANKINSTITUTIONSZU.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = EFK_EIGENE_BANKINSTLFDNR" "~"
if joinKuerzel = BAIEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_EIGENE_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = RNGBAI
searchResult := searchResult "LEFT JOIN BAI_BANKINSTITUTIONSZU.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = RNG_KUNDE_BANKINSTLFDNR" "~"
if joinKuerzel = BAIRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_KUNDE_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = FIWBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = FIW_GKK_BANKINSTLFDNR" "~"
if joinKuerzel = BAIFIW
searchResult := searchResult "LEFT JOIN BUERO.FIW_FIRMENWERTE ON FIW_GKK_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = FIWBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = FIW_GKK_EMPF_BANKINSTLFDNR" "~"
if joinKuerzel = BAIFIW
searchResult := searchResult "LEFT JOIN BUERO.FIW_FIRMENWERTE ON FIW_GKK_EMPF_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = BARBAR
searchResult := searchResult "LEFT JOIN HRS.BAR_BARAUSLAGEN ON BAR_REISENUMMER = BAR_REISENUMMER AND BAR_BARLFDNR = BAR_SAMMELSPESENNR" "~"
if joinKuerzel = BARBAR
searchResult := searchResult "LEFT JOIN HRS.BAR_BARAUSLAGEN ON BAR_REISENUMMER = BAR_REISENUMMER AND BAR_SAMMELSPESENNR = BAR_BARLFDNR" "~"
if joinKuerzel = GPOBAU
searchResult := searchResult "LEFT JOIN BUERO.BAU_BAUM ON BAU_BAUMID = GPO_BAUMID" "~"
if joinKuerzel = BAUGPO
searchResult := searchResult "LEFT JOIN BUERO.GPO_GRUPPENORDNER ON GPO_BAUMID = BAU_BAUMID" "~"
if joinKuerzel = PKSBAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_FIRMENNR = PKS_BAZ_FIRMENNR AND BAZ_BAZ = PKS_BAZ" "~"
if joinKuerzel = BAZPKS
searchResult := searchResult "LEFT JOIN PRODUKTION.PKS_SOLL_KAPAZITAET ON PKS_BAZ_FIRMENNR = BAZ_FIRMENNR AND PKS_BAZ = BAZ_BAZ" "~"
if joinKuerzel = MITBES
searchResult := searchResult "LEFT JOIN BUERO.BES_BEWERBUNGSTATUS ON BES_BEWSTATUSNR = MIT_BEWSTATUSNR" "~"
if joinKuerzel = BESMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_BEWSTATUSNR = BES_BEWSTATUSNR" "~"
if joinKuerzel = VATBES
searchResult := searchResult "LEFT JOIN BUERO.BES_BEWERBUNGSTATUS ON BES_BEWSTATUSNR = VAT_AVTEILNAHMESTATUSNR" "~"
if joinKuerzel = BESVAT
searchResult := searchResult "LEFT JOIN BUERO.VAT_TEILNAHME ON VAT_AVTEILNAHMESTATUSNR = BES_BEWSTATUSNR" "~"
if joinKuerzel = MITBEW
searchResult := searchResult "LEFT JOIN BUERO.BEW_BEWERTUNGSMODELL ON BEW_FIRMENNR = MIT_BEW_FIRMENNR AND BEW_BEWMODELLNR = MIT_BEWMODELLNR" "~"
if joinKuerzel = BEWMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_BEW_FIRMENNR = BEW_FIRMENNR AND MIT_BEWMODELLNR = BEW_BEWMODELLNR" "~"
if joinKuerzel = ZBKBEW
searchResult := searchResult "LEFT JOIN BUERO.BEW_BEWERTUNGSMODELL ON BEW_FIRMENNR = ZBK_BEW_FIRMENNR AND BEW_BEWMODELLNR = ZBK_BEWMODELLNR" "~"
if joinKuerzel = BEWZBK
searchResult := searchResult "LEFT JOIN BUERO.ZBK_ZEITBETRIEBSKALENDER ON ZBK_BEW_FIRMENNR = BEW_FIRMENNR AND ZBK_BEWMODELLNR = BEW_BEWMODELLNR" "~"
if joinKuerzel = BGPBGS
searchResult := searchResult "LEFT JOIN HRS.BGS_BEITRAGSGRUPPE_SEQ ON BGS_BEITRAGSGRUPPEID = BGP_SID_LEHRLING2_HA" "~"
if joinKuerzel = BGSBGP
searchResult := searchResult "LEFT JOIN HRS.BGP_BEITRAGSGRUPPE ON BGP_SID_LEHRLING2_HA = BGS_BEITRAGSGRUPPEID" "~"
if joinKuerzel = BGPBGS
searchResult := searchResult "LEFT JOIN HRS.BGS_BEITRAGSGRUPPE_SEQ ON BGS_BEITRAGSGRUPPEID = BGP_SID_LEHRLING3_HA" "~"
if joinKuerzel = BGSBGP
searchResult := searchResult "LEFT JOIN HRS.BGP_BEITRAGSGRUPPE ON BGP_SID_LEHRLING3_HA = BGS_BEITRAGSGRUPPEID" "~"
if joinKuerzel = BGPBGS
searchResult := searchResult "LEFT JOIN HRS.BGS_BEITRAGSGRUPPE_SEQ ON BGS_BEITRAGSGRUPPEID = BGP_UEBER60_BGPID" "~"
if joinKuerzel = BGSBGP
searchResult := searchResult "LEFT JOIN HRS.BGP_BEITRAGSGRUPPE ON BGP_UEBER60_BGPID = BGS_BEITRAGSGRUPPEID" "~"
if joinKuerzel = BGPBGS
searchResult := searchResult "LEFT JOIN HRS.BGS_BEITRAGSGRUPPE_SEQ ON BGS_BEITRAGSGRUPPEID = BGP_WECHSELBN_BGPID" "~"
if joinKuerzel = BGSBGP
searchResult := searchResult "LEFT JOIN HRS.BGP_BEITRAGSGRUPPE ON BGP_WECHSELBN_BGPID = BGS_BEITRAGSGRUPPEID" "~"
if joinKuerzel = BVGBKZ
searchResult := searchResult "LEFT JOIN BIL.BKZ_BILANZKENNZAHL ON BKZ_FIRMENNR = BVG_FIRMENNR AND BKZ_KZNR = BVG_KZNR AND BKZ_KZPAKETNR = BVG_KZPAKETNR AND BKZ_ISTEIGENE = BVG_ISTEIGENE" "~"
if joinKuerzel = BKZBVG
searchResult := searchResult "LEFT JOIN BIL.BVG_BRANCHENVERGLEICH ON BVG_FIRMENNR = BKZ_FIRMENNR AND BVG_KZNR = BKZ_KZNR AND BVG_KZPAKETNR = BKZ_KZPAKETNR AND BVG_ISTEIGENE = BKZ_ISTEIGENE" "~"
if joinKuerzel = AKZBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = AKZ_BLOBLFDNR" "~"
if joinKuerzel = BLSAKZ
searchResult := searchResult "LEFT JOIN WWS.AKZ_ARTIKELKUNDENZU ON AKZ_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = BGPBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = BGP_BLOBLFDNR" "~"
if joinKuerzel = BLSBGP
searchResult := searchResult "LEFT JOIN HRS.BGP_BEITRAGSGRUPPE ON BGP_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = BRTBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = BRT_BLOBLFDNR" "~"
if joinKuerzel = BLSBRT
searchResult := searchResult "LEFT JOIN BUERO.BRT_BERICHT ON BRT_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = ERGBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = ERG_LEISTUNGSTEXT_BLOBNR" "~"
if joinKuerzel = BLSERG
searchResult := searchResult "LEFT JOIN WWS.ERG_ERFASSUNGSGRUPPEN ON ERG_LEISTUNGSTEXT_BLOBNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = GOHBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = GOH_BLOBLFDNR" "~"
if joinKuerzel = BLSGOH
searchResult := searchResult "LEFT JOIN ZLE.GOH_GRUPPENORDNER_HNPOS ON GOH_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = MIVBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = MIV_BLOBLFDNR" "~"
if joinKuerzel = BLSMIV
searchResult := searchResult "LEFT JOIN HRS.MIV_MITARBVORSCHLAG ON MIV_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = ZAWAWC
searchResult := searchResult "LEFT JOIN AWC_SPALTENAUSWAHL.AWC_SPALTENAUSWAHL ON AWC_FIRMENNR = ZAW_AWC_FIRMENNR AND AWC_SPALTENAUSWAHLNR = ZAW_AWC_SPALTENAUSWAHLNR" "~"
if joinKuerzel = AWCZAW
searchResult := searchResult "LEFT JOIN BUERO.ZAW_ZEITAUSWERTUNG ON ZAW_AWC_FIRMENNR = AWC_FIRMENNR AND ZAW_AWC_SPALTENAUSWAHLNR = AWC_SPALTENAUSWAHLNR" "~"
if joinKuerzel = SMIAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_FIRMENNR = SMI_AWE_FIRMENNR AND AWE_AUSWERTTYPNR = SMI_AUSWERTTYPNR AND AWE_AUSWERTNR = SMI_AUSWERTNR" "~"
if joinKuerzel = AWESMI
searchResult := searchResult "LEFT JOIN ZLE.SMI_SMARTINTERFACE ON SMI_AWE_FIRMENNR = AWE_FIRMENNR AND SMI_AUSWERTTYPNR = AWE_AUSWERTTYPNR AND SMI_AUSWERTNR = AWE_AUSWERTNR" "~"
if joinKuerzel = ALEBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = ALE_BANKINSTLFDNR" "~"
if joinKuerzel = BAIALE
searchResult := searchResult "LEFT JOIN BUERO.ALE_ADRESSENLISTE ON ALE_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = FFABAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = FFA_EMPF_BANKINSTLFDNR" "~"
if joinKuerzel = BAIFFA
searchResult := searchResult "LEFT JOIN BUERO.FFA_FIRMENFINANZAMTZU ON FFA_EMPF_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = FIWBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = FIW_FIN_BANKINSTLFDNR" "~"
if joinKuerzel = BAIFIW
searchResult := searchResult "LEFT JOIN BUERO.FIW_FIRMENWERTE ON FIW_FIN_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = GZJBAI
searchResult := searchResult "LEFT JOIN BAI_BANKINSTITUTIONSZU.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = GZJ_EMPFBANK" "~"
if joinKuerzel = BAIGZJ
searchResult := searchResult "LEFT JOIN HRS.GZJ_GLAEUBIGERZU_JAHR ON GZJ_EMPFBANK = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = MAHBAI
searchResult := searchResult "LEFT JOIN BAI_BANKINSTITUTIONSZU.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = MAH_FIRMA_BANKINSTLFDNR" "~"
if joinKuerzel = BAIMAH
searchResult := searchResult "LEFT JOIN FIBU.MAH_MAHNUNG ON MAH_FIRMA_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = SAPBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = SAP_FIRMA_BANKINSTLFDNR" "~"
if joinKuerzel = BAISAP
searchResult := searchResult "LEFT JOIN BUERO.SAP_SAMMELPROJEKT ON SAP_FIRMA_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = UEBBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = UEB_BANKINSTLFDNR" "~"
if joinKuerzel = BAIUEB
searchResult := searchResult "LEFT JOIN BUERO.UEB_UEBERWEISUNGSART ON UEB_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = PERBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = PER_FIRMA_BANKINSTLFDNR" "~"
if joinKuerzel = BAIPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMA_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = IDPBAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_FIRMENNR = IDP_BAZ_FIRMENNR AND BAZ_BAZ = IDP_BAZ" "~"
if joinKuerzel = BAZIDP
searchResult := searchResult "LEFT JOIN PRODUKTION.IDP_INFO_DELAY_BREAKDOWN ON IDP_BAZ_FIRMENNR = BAZ_FIRMENNR AND IDP_BAZ = BAZ_BAZ" "~"
if joinKuerzel = PCPBAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_FIRMENNR = PCP_BAZ_FIRMENNR AND BAZ_BAZ = PCP_LETZTES_BAZ" "~"
if joinKuerzel = BAZPCP
searchResult := searchResult "LEFT JOIN PRODUKTION.PCP_PPS_PRODUKT ON PCP_BAZ_FIRMENNR = BAZ_FIRMENNR AND PCP_LETZTES_BAZ = BAZ_BAZ" "~"
if joinKuerzel = WEZBAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_FIRMENNR = WEZ_BAZ_FIRMENNR AND BAZ_BAZ = WEZ_BAZ" "~"
if joinKuerzel = BAZWEZ
searchResult := searchResult "LEFT JOIN PRODUKTION.WEZ_WERKZEUGBAZZU ON WEZ_BAZ_FIRMENNR = BAZ_FIRMENNR AND WEZ_BAZ = BAZ_BAZ" "~"
if joinKuerzel = PSMBBK
searchResult := searchResult "LEFT JOIN HRS.BBK_BUAK_BETRIEBSKZ ON BBK_FIRMENNR = PSM_DVFIRMENNR AND BBK_LOHNJAHR = PSM_PSMJAHR AND BBK_BBKNR = PSM_BBK_BBKNR" "~"
if joinKuerzel = BBKPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_DVFIRMENNR = BBK_FIRMENNR AND PSM_PSMJAHR = BBK_LOHNJAHR AND PSM_BBK_BBKNR = BBK_BBKNR" "~"
if joinKuerzel = BWBBES
searchResult := searchResult "LEFT JOIN BUERO.BES_BEWERBUNGSTATUS ON BES_BEWSTATUSNR = BWB_BEWSTATUSNR" "~"
if joinKuerzel = BESBWB
searchResult := searchResult "LEFT JOIN BUERO.BWB_BEWERBUNG ON BWB_BEWSTATUSNR = BES_BEWSTATUSNR" "~"
if joinKuerzel = GEFBFK
searchResult := searchResult "LEFT JOIN WWS.BFK_BEFOERDKAT ON BFK_BEFOERDERGKAT = GEF_BEFOERDERGKAT" "~"
if joinKuerzel = BFKGEF
searchResult := searchResult "LEFT JOIN WWS.GEF_GEFAHRENGUTSTAMM ON GEF_BEFOERDERGKAT = BFK_BEFOERDERGKAT" "~"
if joinKuerzel = BGPBGS
searchResult := searchResult "LEFT JOIN HRS.BGS_BEITRAGSGRUPPE_SEQ ON BGS_BEITRAGSGRUPPEID = BGP_BEITRAGSGRUPPEID" "~"
if joinKuerzel = BGSBGP
searchResult := searchResult "LEFT JOIN HRS.BGP_BEITRAGSGRUPPE ON BGP_BEITRAGSGRUPPEID = BGS_BEITRAGSGRUPPEID" "~"
if joinKuerzel = PSMBGS
searchResult := searchResult "LEFT JOIN HRS.BGS_BEITRAGSGRUPPE_SEQ ON BGS_BEITRAGSGRUPPEID = PSM_BGS_BEITRAGSGRUPPEID" "~"
if joinKuerzel = BGSPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_BGS_BEITRAGSGRUPPEID = BGS_BEITRAGSGRUPPEID" "~"
if joinKuerzel = BGPBGS
searchResult := searchResult "LEFT JOIN HRS.BGS_BEITRAGSGRUPPE_SEQ ON BGS_BEITRAGSGRUPPEID = BGP_WECHSELPAUSCH_BGPID" "~"
if joinKuerzel = BGSBGP
searchResult := searchResult "LEFT JOIN HRS.BGP_BEITRAGSGRUPPE ON BGP_WECHSELPAUSCH_BGPID = BGS_BEITRAGSGRUPPEID" "~"
if joinKuerzel = FTGBLD
searchResult := searchResult "LEFT JOIN BUERO.BLD_BUNDESLAND ON BLD_LANDNR = FTG_BLD_LANDNR AND BLD_BUNDESLANDLFDNR = FTG_BUNDESLANDLFDNR" "~"
if joinKuerzel = BLDFTG
searchResult := searchResult "LEFT JOIN BUERO.FTG_FEIERTAG ON FTG_BLD_LANDNR = BLD_LANDNR AND FTG_BUNDESLANDLFDNR = BLD_BUNDESLANDLFDNR" "~"
if joinKuerzel = ABFBLS
searchResult := searchResult "LEFT JOIN BMD.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = ABF_BLS_BLOBLFDNR" "~"
if joinKuerzel = BLSABF
searchResult := searchResult "LEFT JOIN BMD.ABF_ABFRAGE ON ABF_BLS_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = ABRBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = ABR_CHRONIK" "~"
if joinKuerzel = BLSABR
searchResult := searchResult "LEFT JOIN BIL.ABR_ANBUBEWRES ON ABR_CHRONIK = BLS_BLOBLFDNR" "~"
if joinKuerzel = BSUBKR
searchResult := searchResult "LEFT JOIN FIBU.BKR_BANKEINZUGSKREIS ON BKR_FIRMENNR = BSU_FIRMENNR AND BKR_FIBUNR = BSU_FIBUNR AND BKR_EINZUGSKREISLFDNR = BSU_EINZUGSKREISLFDNR" "~"
if joinKuerzel = BKRBSU
searchResult := searchResult "LEFT JOIN FIBU.BSU_BANKEINZUGSSUMME ON BSU_FIRMENNR = BKR_FIRMENNR AND BSU_FIBUNR = BKR_FIBUNR AND BSU_EINZUGSKREISLFDNR = BKR_EINZUGSKREISLFDNR" "~"
if joinKuerzel = KPKBKZ
searchResult := searchResult "LEFT JOIN BKZ_BILANZKENNZAHL.BKZ_BILANZKENNZAHL ON BKZ_FIRMENNR = KPK_BKZ_FIRMENNR AND BKZ_ISTEIGENE = KPK_BKZ_ISTEIGENE AND BKZ_KZNR = KPK_BKZ_KZNR AND BKZ_KZPAKETNR = KPK_BKZ_KZPAKETNR" "~"
if joinKuerzel = BKZKPK
searchResult := searchResult "LEFT JOIN KORE.KPK_PLANKENNZAHLEN ON KPK_BKZ_FIRMENNR = BKZ_FIRMENNR AND KPK_BKZ_ISTEIGENE = BKZ_ISTEIGENE AND KPK_BKZ_KZNR = BKZ_KZNR AND KPK_BKZ_KZPAKETNR = BKZ_KZPAKETNR" "~"
if joinKuerzel = AGSBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = AGS_BLS_BLOBLFDNR" "~"
if joinKuerzel = BLSAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_BLS_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = AUGBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = AUG_CHECKLISTLFDNR" "~"
if joinKuerzel = BLSAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_CHECKLISTLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = BPTBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = BPT_BLOBLFDNR" "~"
if joinKuerzel = BLSBPT
searchResult := searchResult "LEFT JOIN BUERO.BPT_BESPRECHPROTOKOLL ON BPT_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = DIABLS
searchResult := searchResult "LEFT JOIN BMD.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = DIA_BLOBLFDNR" "~"
if joinKuerzel = BLSDIA
searchResult := searchResult "LEFT JOIN BMD.DIA_DIAGRAMM ON DIA_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = FIRBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = FIR_BRFKOPF_BLOBLFDNR" "~"
if joinKuerzel = BLSFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_BRFKOPF_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = KOWBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = KOW_BLS_BLOBLFDNR3" "~"
if joinKuerzel = BLSKOW
searchResult := searchResult "LEFT JOIN HRS.KOW_KOLLEKTIVVERTRAGWERTE ON KOW_BLS_BLOBLFDNR3 = BLS_BLOBLFDNR" "~"
if joinKuerzel = PIABLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = PIA_PICTURE_BLOBLFDNR" "~"
if joinKuerzel = BLSPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_PICTURE_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = PRTBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = PRT_BLOBLFDNR" "~"
if joinKuerzel = BLSPRT
searchResult := searchResult "LEFT JOIN BUERO.PRT_PROJEKTTAETIGKEIT ON PRT_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = PZEBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = PZE_FILTERBLOBLFDNR" "~"
if joinKuerzel = BLSPZE
searchResult := searchResult "LEFT JOIN HRS.PZE_PERKZGLGZEILE ON PZE_FILTERBLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = RIABLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = RIA_CHRONIK" "~"
if joinKuerzel = BLSRIA
searchResult := searchResult "LEFT JOIN PRODUKTION.RIA_RISIKOANALYSE ON RIA_CHRONIK = BLS_BLOBLFDNR" "~"
if joinKuerzel = GOHBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = GOH_LEISTTEXT_BLOBLFDNR" "~"
if joinKuerzel = BLSGOH
searchResult := searchResult "LEFT JOIN ZLE.GOH_GRUPPENORDNER_HNPOS ON GOH_LEISTTEXT_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = PBABLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = PBA_BLOBLFDNR" "~"
if joinKuerzel = BLSPBA
searchResult := searchResult "LEFT JOIN PRODUKTION.PBA_PLAN_BAS_ALT ON PBA_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = TCTBLS
searchResult := searchResult "LEFT JOIN BMD.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = TCT_NOTIZBLOBLFDNR" "~"
if joinKuerzel = BLSTCT
searchResult := searchResult "LEFT JOIN BMD.TCT_TCTESTCASE ON TCT_NOTIZBLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = OUFBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = OUF_BILD_TOPMD" "~"
if joinKuerzel = BLSOUF
searchResult := searchResult "LEFT JOIN BUERO.OUF_ONLINEUMFRAGE ON OUF_BILD_TOPMD = BLS_BLOBLFDNR" "~"
if joinKuerzel = PERBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = PER_TEXT2_CHRONIK" "~"
if joinKuerzel = BLSPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_TEXT2_CHRONIK = BLS_BLOBLFDNR" "~"
if joinKuerzel = PIABLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = PIA_PRODTEXT_BLOBLFDNR" "~"
if joinKuerzel = BLSPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_PRODTEXT_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = RNGBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = RNG_BLOBNR_RNGINFO" "~"
if joinKuerzel = BLSRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_BLOBNR_RNGINFO = BLS_BLOBLFDNR" "~"
if joinKuerzel = DRSBLS
searchResult := searchResult "LEFT JOIN BMD.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = DRS_MAILBETREFF_MBLOBNR" "~"
if joinKuerzel = BLSDRS
searchResult := searchResult "LEFT JOIN BMD.DRS_DRUCKEREINSTELLUNG ON DRS_MAILBETREFF_MBLOBNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = OUFBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = OUF_BILD_TOPRT" "~"
if joinKuerzel = BLSOUF
searchResult := searchResult "LEFT JOIN BUERO.OUF_ONLINEUMFRAGE ON OUF_BILD_TOPRT = BLS_BLOBLFDNR" "~"
if joinKuerzel = FIRBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = FIR_KOPFWORD_BLOBLFDNR" "~"
if joinKuerzel = BLSFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_KOPFWORD_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = KSLBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = KSL_DIVPER_BLOBLFDNR" "~"
if joinKuerzel = BLSKSL
searchResult := searchResult "LEFT JOIN BUERO.KSL_KONTAKTSYNCEINST ON KSL_DIVPER_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = DVHATE
searchResult := searchResult "LEFT JOIN BUERO.ATE_ABTEILUNG ON ATE_FIRMENNR = DVH_ATE_FIRMENNR AND ATE_ABTEILUNGSNR = DVH_ABTEILUNGSNR" "~"
if joinKuerzel = ATEDVH
searchResult := searchResult "LEFT JOIN BUERO.DVH_DIENSTVERHAELTNIS ON DVH_ATE_FIRMENNR = ATE_FIRMENNR AND DVH_ABTEILUNGSNR = ATE_ABTEILUNGSNR" "~"
if joinKuerzel = PERATE
searchResult := searchResult "LEFT JOIN BUERO.ATE_ABTEILUNG ON ATE_FIRMENNR = PER_ATE_FIRMENNR AND ATE_ABTEILUNGSNR = PER_ABTEILUNGSNR" "~"
if joinKuerzel = ATEPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_ATE_FIRMENNR = ATE_FIRMENNR AND PER_ABTEILUNGSNR = ATE_ABTEILUNGSNR" "~"
if joinKuerzel = ATEPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = ATE_MIT_FIRMENNR AND PER_PERSONENID = ATE_MITARBEITERID" "~"
if joinKuerzel = AWEATE
searchResult := searchResult "LEFT JOIN ATE_ABTEILUNG.ATE_ABTEILUNG ON ATE_FIRMENNR = AWE_ATE_KLI_FIRMENNR AND ATE_ABTEILUNGSNR = AWE_ATE_KLI_ABTEILUNGSNR" "~"
if joinKuerzel = ATEAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_ATE_KLI_FIRMENNR = ATE_FIRMENNR AND AWE_ATE_KLI_ABTEILUNGSNR = ATE_ABTEILUNGSNR" "~"
if joinKuerzel = TAZAUG
searchResult := searchResult "LEFT JOIN AUG_AUFGABE.AUG_AUFGABE ON AUG_AUFGABENNR = TAZ_AUG_AUFGABENNR" "~"
if joinKuerzel = AUGTAZ
searchResult := searchResult "LEFT JOIN BMDENTW.TAZ_TICKETAUFGABEZU ON TAZ_AUG_AUFGABENNR = AUG_AUFGABENNR" "~"
if joinKuerzel = AWCAUT
searchResult := searchResult "LEFT JOIN ZLE.AUT_AUSWERTTYP ON AUT_AUSWERTTYPNR = AWC_AUSWERTTYPNR" "~"
if joinKuerzel = AUTAWC
searchResult := searchResult "LEFT JOIN ZLE.AWC_SPALTENAUSWAHL ON AWC_AUSWERTTYPNR = AUT_AUSWERTTYPNR" "~"
if joinKuerzel = AWRAUT
searchResult := searchResult "LEFT JOIN AUT_AUSWERTTYP.AUT_AUSWERTTYP ON AUT_AUSWERTTYPNR = AWR_AUSWERTTYPNR" "~"
if joinKuerzel = AUTAWR
searchResult := searchResult "LEFT JOIN BUERO.AWR_AUSWERTUNGSKAT ON AWR_AUSWERTTYPNR = AUT_AUSWERTTYPNR" "~"
if joinKuerzel = AVLAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_VORERFID = AVL_VORERFID" "~"
if joinKuerzel = AVEAVL
searchResult := searchResult "LEFT JOIN WWS.AVL_AUFTRAGSVORERFLIEFDAT ON AVL_VORERFID = AVE_VORERFID" "~"
if joinKuerzel = AVWAVR
searchResult := searchResult "LEFT JOIN BUERO.AVR_AVRUEZAHLUNGSTABELLE ON AVR_AVRID = AVW_AVRID" "~"
if joinKuerzel = AVRAVW
searchResult := searchResult "LEFT JOIN BUERO.AVW_AVRUEZAHLUNGSWERTE ON AVW_AVRID = AVR_AVRID" "~"
if joinKuerzel = VAKAVR
searchResult := searchResult "LEFT JOIN BUERO.AVR_AVRUEZAHLUNGSTABELLE ON AVR_AVRID = VAK_AVRID" "~"
if joinKuerzel = AVRVAK
searchResult := searchResult "LEFT JOIN BUERO.VAK_VERANSTALTUNGSKOPF ON VAK_AVRID = AVR_AVRID" "~"
if joinKuerzel = FAZAWD
searchResult := searchResult "LEFT JOIN BUERO.AWD_ANTWORTDETAIL ON AWD_ANTWORTID = FAZ_ANTWORTID AND AWD_ANTWDETAILLFDNR = FAZ_ANTWDETAILLFDNR" "~"
if joinKuerzel = AWDFAZ
searchResult := searchResult "LEFT JOIN BUERO.FAZ_FRAGEANTWORTZU ON FAZ_ANTWORTID = AWD_ANTWORTID AND FAZ_ANTWDETAILLFDNR = AWD_ANTWDETAILLFDNR" "~"
if joinKuerzel = UMFAWE
searchResult := searchResult "LEFT JOIN AWE_AUSWERTUNG.AWE_AUSWERTUNG ON AWE_FIRMENNR = UMF_AWE_FIRMENNR AND AWE_AUSWERTTYPNR = UMF_AWE_AUSWERTTYPNR AND AWE_AUSWERTNR = UMF_AWE_AUSWERTNR" "~"
if joinKuerzel = AWEUMF
searchResult := searchResult "LEFT JOIN BUERO.UMF_UMFRAGE ON UMF_AWE_FIRMENNR = AWE_FIRMENNR AND UMF_AWE_AUSWERTTYPNR = AWE_AUSWERTTYPNR AND UMF_AWE_AUSWERTNR = AWE_AUSWERTNR" "~"
if joinKuerzel = AXZAXT
searchResult := searchResult "LEFT JOIN BMDENTW.AXT_ATTREXT ON AXT_AXTID = AXZ_AXTID" "~"
if joinKuerzel = AXTAXZ
searchResult := searchResult "LEFT JOIN BMDENTW.AXZ_ATTREXTZU ON AXZ_AXTID = AXT_AXTID" "~"
if joinKuerzel = BEZBAI
searchResult := searchResult "LEFT JOIN BAI_BANKINSTITUTIONSZU.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = BEZ_BAI_EINZUGSBANK_PERSON" "~"
if joinKuerzel = BAIBEZ
searchResult := searchResult "LEFT JOIN FIBU.BEZ_BANKEINZUG ON BEZ_BAI_EINZUGSBANK_PERSON = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = FGRBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = FGR_SENDER_BANKINSTLFDNR" "~"
if joinKuerzel = BAIFGR
searchResult := searchResult "LEFT JOIN BUERO.FGR_FIRMENGERICHTZU ON FGR_SENDER_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = HNPBAI
searchResult := searchResult "LEFT JOIN BAI_BANKINSTITUTIONSZU.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = HNP_FIRMA_BANKINSTLFDNR" "~"
if joinKuerzel = BAIHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_FIRMA_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = BEZBAI
searchResult := searchResult "LEFT JOIN BAI_BANKINSTITUTIONSZU.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = BEZ_BAI_EINZUGSBANK_FIRMA" "~"
if joinKuerzel = BAIBEZ
searchResult := searchResult "LEFT JOIN FIBU.BEZ_BANKEINZUG ON BEZ_BAI_EINZUGSBANK_FIRMA = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = FGKBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = FGK_SENDER_BANKINSTLFDNR" "~"
if joinKuerzel = BAIFGK
searchResult := searchResult "LEFT JOIN BUERO.FGK_FIRMENGKKZU ON FGK_SENDER_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = ZVDBAI
searchResult := searchResult "LEFT JOIN BAI_BANKINSTITUTIONSZU.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = ZVD_BAI_EMPF_BANKINSTLFDNR" "~"
if joinKuerzel = BAIZVD
searchResult := searchResult "LEFT JOIN ZX_GELOESCHT.ZVD_ZAHLUNGSDETAILS ON ZVD_BAI_EMPF_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = AGBBAZ
searchResult := searchResult "LEFT JOIN BAZ_BAZ.BAZ_BAZ ON BAZ_FIRMENNR = AGB_BAZ_FIRMENNR AND BAZ_BAZ = AGB_BAZ" "~"
if joinKuerzel = BAZAGB
searchResult := searchResult "LEFT JOIN WWS.AGB_ARBEITSGANGBAZZU ON AGB_BAZ_FIRMENNR = BAZ_FIRMENNR AND AGB_BAZ = BAZ_BAZ" "~"
if joinKuerzel = BZBBAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_FIRMENNR = BZB_BAZ_FIRMENNR AND BAZ_BAZ = BZB_BAZ" "~"
if joinKuerzel = BAZBZB
searchResult := searchResult "LEFT JOIN PRODUKTION.BZB_BAZZEITBUCHUNG ON BZB_BAZ_FIRMENNR = BAZ_FIRMENNR AND BZB_BAZ = BAZ_BAZ" "~"
if joinKuerzel = PCKBAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_FIRMENNR = PCK_BAZ_FIRMENNR AND BAZ_BAZ = PCK_BAZ" "~"
if joinKuerzel = BAZPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_BAZ_FIRMENNR = BAZ_FIRMENNR AND PCK_BAZ = BAZ_BAZ" "~"
if joinKuerzel = BCGBCU
searchResult := searchResult "LEFT JOIN BUERO.BCU_BMDCOMUSER ON BCU_BMDCOMUSERID = BCG_BMDCOMUSERID" "~"
if joinKuerzel = BCUBCG
searchResult := searchResult "LEFT JOIN BUERO.BCG_BMDCOMUSERGRUPPENZU ON BCG_BMDCOMUSERID = BCU_BMDCOMUSERID" "~"
if joinKuerzel = PKMBLD
searchResult := searchResult "LEFT JOIN BLD_BUNDESLAND.BLD_BUNDESLAND ON BLD_LANDNR = PKM_LAN_LANDNR AND BLD_BUNDESLANDLFDNR = PKM_BLD_BUNDESLANDLFDNR" "~"
if joinKuerzel = BLDPKM
searchResult := searchResult "LEFT JOIN PRODUKTION.PKM_KM_MASTER ON PKM_LAN_LANDNR = BLD_LANDNR AND PKM_BLD_BUNDESLANDLFDNR = BLD_BUNDESLANDLFDNR" "~"
if joinKuerzel = AMPBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = AMP_MAIL_BLOBLFDNR" "~"
if joinKuerzel = BLSAMP
searchResult := searchResult "LEFT JOIN BUERO.AMP_AUTOMAILARCHIVPARAM ON AMP_MAIL_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = BCTBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = BCT_BLOBLFDNR" "~"
if joinKuerzel = BLSBCT
searchResult := searchResult "LEFT JOIN BUERO.BCT_BMDCOMTEMPLATES ON BCT_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = BPDBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = BPD_BLOBNR_PREISINFO" "~"
if joinKuerzel = BLSBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_BLOBNR_PREISINFO = BLS_BLOBLFDNR" "~"
if joinKuerzel = BRMBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = BRM_BLOBLFDNR" "~"
if joinKuerzel = BLSBRM
searchResult := searchResult "LEFT JOIN BUERO.BRM_BUEROMAKROS ON BRM_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = DKPBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = DKP_BLOBLFDNR" "~"
if joinKuerzel = BLSDKP
searchResult := searchResult "LEFT JOIN FIBU.DKP_DOKUMENTENPOOL ON DKP_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = DKVBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = DKV_BLOBLFDNR" "~"
if joinKuerzel = BLSDKV
searchResult := searchResult "LEFT JOIN BUERO.DKV_DOKUMENTVERSION ON DKV_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = EROBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = ERO_BLOBNR_PREISINFO" "~"
if joinKuerzel = BLSERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_BLOBNR_PREISINFO = BLS_BLOBLFDNR" "~"
if joinKuerzel = ERTBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = ERT_BLOBLFDNR" "~"
if joinKuerzel = BLSERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = KVXBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = KVX_BLOBLFDNR" "~"
if joinKuerzel = BLSKVX
searchResult := searchResult "LEFT JOIN HRS.KVX_KVTEXT ON KVX_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = MZZBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = MZZ_BLOBLFDNR" "~"
if joinKuerzel = BLSMZZ
searchResult := searchResult "LEFT JOIN BUERO.MZZ_MITARBZIELZU ON MZZ_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = OBTBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = OBT_BLOBLFDNR" "~"
if joinKuerzel = BLSOBT
searchResult := searchResult "LEFT JOIN LOHN.OBT_OF_BLOBTEXT ON OBT_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = ORSBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = ORS_BLOBLFDNR" "~"
if joinKuerzel = BLSORS
searchResult := searchResult "LEFT JOIN LOHN.ORS_OF_REISESTAMM ON ORS_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = PRWBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = PRW_BLOBLFDNR" "~"
if joinKuerzel = BLSPRW
searchResult := searchResult "LEFT JOIN BUERO.PRW_PROJEKTWERT ON PRW_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = UVWBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = UVW_BLOBLFDNR" "~"
if joinKuerzel = BLSUVW
searchResult := searchResult "LEFT JOIN FIBU.UVW_UVAWERT ON UVW_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = WICBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = WIC_BLOBLFDNR" "~"
if joinKuerzel = BLSWIC
searchResult := searchResult "LEFT JOIN BIL.WIC_WIPCHECKLIST ON WIC_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = WTBBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = WTB_BLOBLFDNR" "~"
if joinKuerzel = BLSWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = AGSBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = AGS_CHECKLISTE_BLOBLFDNR" "~"
if joinKuerzel = BLSAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_CHECKLISTE_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = DRSBLS
searchResult := searchResult "LEFT JOIN BMD.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = DRS_MAILSTAMPSET_BLOBLFDNR" "~"
if joinKuerzel = BLSDRS
searchResult := searchResult "LEFT JOIN BMD.DRS_DRUCKEREINSTELLUNG ON DRS_MAILSTAMPSET_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = FIRBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = FIR_BRFFUSS_BLOBLFDNR" "~"
if joinKuerzel = BLSFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_BRFFUSS_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = KTOBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = KTO_BILANZ_CHRONIK" "~"
if joinKuerzel = BLSKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_BILANZ_CHRONIK = BLS_BLOBLFDNR" "~"
if joinKuerzel = PCKBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = PCK_CHECKLISTE_BLOBLFDNR" "~"
if joinKuerzel = BLSPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_CHECKLISTE_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = ZASBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = ZAS_VERWTEXT_BLOBLFDNR_HOC" "~"
if joinKuerzel = BLSZAS
searchResult := searchResult "LEFT JOIN BUERO.ZAS_ZAHLSCHEIN ON ZAS_VERWTEXT_BLOBLFDNR_HOC = BLS_BLOBLFDNR" "~"
if joinKuerzel = BDLBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = BDL_HTML_BLOBLFDNR" "~"
if joinKuerzel = BLSBDL
searchResult := searchResult "LEFT JOIN BUERO.BDL_BUERODELETE ON BDL_HTML_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = FIRBAK
searchResult := searchResult "LEFT JOIN BUERO.BAK_BANK ON BAK_LANDNR = FIR_KURS_LANDNR AND BAK_BANKLEITZAHL = FIR_KURS_BANKLEITZAHL AND BAK_BANKNR = FIR_KURS_BANKNR" "~"
if joinKuerzel = BAKFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_KURS_LANDNR = BAK_LANDNR AND FIR_KURS_BANKLEITZAHL = BAK_BANKLEITZAHL AND FIR_KURS_BANKNR = BAK_BANKNR" "~"
if joinKuerzel = SLKBAZ
searchResult := searchResult "LEFT JOIN BAZ_BAZ.BAZ_BAZ ON BAZ_FIRMENNR = SLK_BAZ_FIRMENNR AND BAZ_BAZ = SLK_BAZ" "~"
if joinKuerzel = BAZSLK
searchResult := searchResult "LEFT JOIN WWS.SLK_STUECKLISTENKOPF ON SLK_BAZ_FIRMENNR = BAZ_FIRMENNR AND SLK_BAZ = BAZ_BAZ" "~"
if joinKuerzel = BRTBBA
searchResult := searchResult "LEFT JOIN BBA_BERICHTSART.BBA_BERICHTSART ON BBA_BANR = BRT_BANR" "~"
if joinKuerzel = BBABRT
searchResult := searchResult "LEFT JOIN BUERO.BRT_BERICHT ON BRT_BANR = BBA_BANR" "~"
if joinKuerzel = PSJBGS
searchResult := searchResult "LEFT JOIN HRS.BGS_BEITRAGSGRUPPE_SEQ ON BGS_BEITRAGSGRUPPEID = PSJ_BGS_LEHRLING_ID" "~"
if joinKuerzel = BGSPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_BGS_LEHRLING_ID = BGS_BEITRAGSGRUPPEID" "~"
if joinKuerzel = BGPBGS
searchResult := searchResult "LEFT JOIN HRS.BGS_BEITRAGSGRUPPE_SEQ ON BGS_BEITRAGSGRUPPEID = BGP_SID_LEHRLING4_HA" "~"
if joinKuerzel = BGSBGP
searchResult := searchResult "LEFT JOIN HRS.BGP_BEITRAGSGRUPPE ON BGP_SID_LEHRLING4_HA = BGS_BEITRAGSGRUPPEID" "~"
if joinKuerzel = BGPBGS
searchResult := searchResult "LEFT JOIN HRS.BGS_BEITRAGSGRUPPE_SEQ ON BGS_BEITRAGSGRUPPEID = BGP_UEBER57_BGPID" "~"
if joinKuerzel = BGSBGP
searchResult := searchResult "LEFT JOIN HRS.BGP_BEITRAGSGRUPPE ON BGP_UEBER57_BGPID = BGS_BEITRAGSGRUPPEID" "~"
if joinKuerzel = BGPBGS
searchResult := searchResult "LEFT JOIN HRS.BGS_BEITRAGSGRUPPE_SEQ ON BGS_BEITRAGSGRUPPEID = BGP_ALTERSPENSION_BGPID" "~"
if joinKuerzel = BGSBGP
searchResult := searchResult "LEFT JOIN HRS.BGP_BEITRAGSGRUPPE ON BGP_ALTERSPENSION_BGPID = BGS_BEITRAGSGRUPPEID" "~"
if joinKuerzel = BGPBGS
searchResult := searchResult "LEFT JOIN HRS.BGS_BEITRAGSGRUPPE_SEQ ON BGS_BEITRAGSGRUPPEID = BGP_FRUEHPENS_BGPID" "~"
if joinKuerzel = BGSBGP
searchResult := searchResult "LEFT JOIN HRS.BGP_BEITRAGSGRUPPE ON BGP_FRUEHPENS_BGPID = BGS_BEITRAGSGRUPPEID" "~"
if joinKuerzel = KDPBKZ
searchResult := searchResult "LEFT JOIN BKZ_BILANZKENNZAHL.BKZ_BILANZKENNZAHL ON BKZ_FIRMENNR = KDP_BKZ_FIRMENNR AND BKZ_ISTEIGENE = KDP_BKZ_ISTEIGENE AND BKZ_KZNR = KDP_BKZ_KZNR AND BKZ_KZPAKETNR = KDP_BKZ_KZPAKETNR" "~"
if joinKuerzel = BKZKDP
searchResult := searchResult "LEFT JOIN KORE.KDP_PLANWERTE ON KDP_BKZ_FIRMENNR = BKZ_FIRMENNR AND KDP_BKZ_ISTEIGENE = BKZ_ISTEIGENE AND KDP_BKZ_KZNR = BKZ_KZNR AND KDP_BKZ_KZPAKETNR = BKZ_KZPAKETNR" "~"
if joinKuerzel = AGUBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = AGU_CHRONIK" "~"
if joinKuerzel = BLSAGU
searchResult := searchResult "LEFT JOIN BIL.AGU_ANLAGEGUT ON AGU_CHRONIK = BLS_BLOBLFDNR" "~"
if joinKuerzel = AWEBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = AWE_FILTERBLOBLFDNR" "~"
if joinKuerzel = BLSAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_FILTERBLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = DOKBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = DOK_BLOBLFDNR" "~"
if joinKuerzel = BLSDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = DPDBLS
searchResult := searchResult "LEFT JOIN BMD.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = DPD_BLOBLFDNR" "~"
if joinKuerzel = BLSDPD
searchResult := searchResult "LEFT JOIN BMD.DPD_DOCUMENTPOOLDETAIL ON DPD_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = FOTBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = FOT_BLOBLFDNR" "~"
if joinKuerzel = BLSFOT
searchResult := searchResult "LEFT JOIN BUERO.FOT_FOTO ON FOT_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = HPTBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = HPT_BLOBLFDNR" "~"
if joinKuerzel = BLSHPT
searchResult := searchResult "LEFT JOIN ZLE.HPT_HONORARPOSTEXTE ON HPT_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = KTOBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = KTO_CHRONIK" "~"
if joinKuerzel = BLSKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_CHRONIK = BLS_BLOBLFDNR" "~"
if joinKuerzel = OLEBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = OLE_BLOBLFDNR" "~"
if joinKuerzel = BLSOLE
searchResult := searchResult "LEFT JOIN LOHN.OLE_OF_LEISTUNG ON OLE_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = OUPBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = OUP_HTMLFILE_BLOBLFDNR" "~"
if joinKuerzel = BLSOUP
searchResult := searchResult "LEFT JOIN BUERO.OUP_ONLUMFRAGEPAGE ON OUP_HTMLFILE_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = PIOBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = PIO_BLOBLFDNR" "~"
if joinKuerzel = BLSPIO
searchResult := searchResult "LEFT JOIN WWS.PIO_PICARTIKELDOKZU ON PIO_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = RCLBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = RCL_BLOBLFDNR" "~"
if joinKuerzel = BLSRCL
searchResult := searchResult "LEFT JOIN PRODUKTION.RCL_REPEATING_CHECKLIST ON RCL_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = RIHBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = RIH_RTF_TEXT" "~"
if joinKuerzel = BLSRIH
searchResult := searchResult "LEFT JOIN PRODUKTION.RIH_RISIKOANALYSE_HEAD ON RIH_RTF_TEXT = BLS_BLOBLFDNR" "~"
if joinKuerzel = RTIBLS
searchResult := searchResult "LEFT JOIN BMD.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = RTI_BLOBLFDNR" "~"
if joinKuerzel = BLSRTI
searchResult := searchResult "LEFT JOIN BMD.RTI_RTFINDEX ON RTI_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = SBVBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = SBV_VORLAGENBLOBLFDNR" "~"
if joinKuerzel = BLSSBV
searchResult := searchResult "LEFT JOIN BUERO.SBV_STELLENBESCHVORLAGE ON SBV_VORLAGENBLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = ASRBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = ASR_BLOBLFDNR" "~"
if joinKuerzel = BLSASR
searchResult := searchResult "LEFT JOIN BUERO.ASR_AUSSCHREIBUNG ON ASR_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = BKJBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = BKJ_BLS_BLOBNR_BELEG" "~"
if joinKuerzel = BLSBKJ
searchResult := searchResult "LEFT JOIN WWS.BKJ_BANKOMATJOURNAL ON BKJ_BLS_BLOBNR_BELEG = BLS_BLOBLFDNR" "~"
if joinKuerzel = CKLBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = CKL_BLOBLFDNR" "~"
if joinKuerzel = BLSCKL
searchResult := searchResult "LEFT JOIN BUERO.CKL_CHECKLISTE ON CKL_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = DRSBLS
searchResult := searchResult "LEFT JOIN BMD.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = DRS_MAILBODY_BLOBLFDNR" "~"
if joinKuerzel = BLSDRS
searchResult := searchResult "LEFT JOIN BMD.DRS_DRUCKEREINSTELLUNG ON DRS_MAILBODY_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = FMJBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = FMJ_BLOBNR_RESPONSE" "~"
if joinKuerzel = BLSFMJ
searchResult := searchResult "LEFT JOIN WWS.FMJ_FISKALMODULJOURNAL ON FMJ_BLOBNR_RESPONSE = BLS_BLOBLFDNR" "~"
if joinKuerzel = TERBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = TER_CHECKLISTLFDNR" "~"
if joinKuerzel = BLSTER
searchResult := searchResult "LEFT JOIN BUERO.TER_TERMIN ON TER_CHECKLISTLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = TSRBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = TSR_CHECKLISTLFDNR" "~"
if joinKuerzel = BLSTSR
searchResult := searchResult "LEFT JOIN BUERO.TSR_SERIENTERMIN ON TSR_CHECKLISTLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = AUGBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = AUG_BLOBLFDNR" "~"
if joinKuerzel = BLSAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = OUFBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = OUF_BILD_TOPLT" "~"
if joinKuerzel = BLSOUF
searchResult := searchResult "LEFT JOIN BUERO.OUF_ONLINEUMFRAGE ON OUF_BILD_TOPLT = BLS_BLOBLFDNR" "~"
if joinKuerzel = DRSBLS
searchResult := searchResult "LEFT JOIN BMD.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = DRS_ARCHIVBETREFF_MBLOBNR" "~"
if joinKuerzel = BLSDRS
searchResult := searchResult "LEFT JOIN BMD.DRS_DRUCKEREINSTELLUNG ON DRS_ARCHIVBETREFF_MBLOBNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = KSLBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = KSL_LIEFERANT_BLOBLFDNR" "~"
if joinKuerzel = BLSKSL
searchResult := searchResult "LEFT JOIN BUERO.KSL_KONTAKTSYNCEINST ON KSL_LIEFERANT_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = KSLBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = KSL_KON_BLOBLFDNR" "~"
if joinKuerzel = BLSKSL
searchResult := searchResult "LEFT JOIN BUERO.KSL_KONTAKTSYNCEINST ON KSL_KON_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = FFOBMG
searchResult := searchResult "LEFT JOIN BMD.BMG_BMDGRUPPE ON BMG_BMDGRUPPENID = FFO_BMDGRUPPENID" "~"
if joinKuerzel = BMGFFO
searchResult := searchResult "LEFT JOIN BMD.FFO_FUNKFIRMENRECHTE_OPT ON FFO_BMDGRUPPENID = BMG_BMDGRUPPENID" "~"
if joinKuerzel = FFRBMG
searchResult := searchResult "LEFT JOIN BMD.BMG_BMDGRUPPE ON BMG_BMDGRUPPENID = FFR_BMDGRUPPENID" "~"
if joinKuerzel = BMGFFR
searchResult := searchResult "LEFT JOIN BMD.FFR_FUNKFIRMENRECHTE ON FFR_BMDGRUPPENID = BMG_BMDGRUPPENID" "~"
if joinKuerzel = AAKBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = AAK_VERBUCHUNGSUSERID" "~"
if joinKuerzel = BMUAAK
searchResult := searchResult "LEFT JOIN WWS.AAK_ARAKOPF ON AAK_VERBUCHUNGSUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = BCUBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = BCU_BMDUSERID" "~"
if joinKuerzel = BMUBCU
searchResult := searchResult "LEFT JOIN BUERO.BCU_BMDCOMUSER ON BCU_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = MITBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = MIT_BMDUSERID" "~"
if joinKuerzel = BMUMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = PUCBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = PUC_BMDUSERID" "~"
if joinKuerzel = BMUPUC
searchResult := searchResult "LEFT JOIN PRODUKTION.PUC_PPSUSERSTATICOMBI ON PUC_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = PVFBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = PVF_UEBERNOMMENVON" "~"
if joinKuerzel = BMUPVF
searchResult := searchResult "LEFT JOIN BUERO.PVF_PERSONENVORERFASSUNG ON PVF_UEBERNOMMENVON = BMU_BMDUSERID" "~"
if joinKuerzel = SCRBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = SCR_VERANTWUSERID" "~"
if joinKuerzel = BMUSCR
searchResult := searchResult "LEFT JOIN BMDENTW.SCR_SCRREGELWERK ON SCR_VERANTWUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = UBEBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = UBE_GETSF_USERID" "~"
if joinKuerzel = BMUUBE
searchResult := searchResult "LEFT JOIN BMDINTERN.UBE_UPDATEBESTELLUNG ON UBE_GETSF_USERID = BMU_BMDUSERID" "~"
if joinKuerzel = LEIBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = LEI_TAGESAB_USERID" "~"
if joinKuerzel = BMULEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_TAGESAB_USERID = BMU_BMDUSERID" "~"
if joinKuerzel = KEPBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_BONKOPFID = KEP_BON_BONKOPFID" "~"
if joinKuerzel = BONKEP
searchResult := searchResult "LEFT JOIN WWS.KEP_KASSENERFASSUNGSPROT ON KEP_BON_BONKOPFID = BON_BONKOPFID" "~"
if joinKuerzel = RSDBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_BONKOPFID = RSD_BON_BONKOPFID" "~"
if joinKuerzel = BONRSD
searchResult := searchResult "LEFT JOIN WWS.RSD_REGKASPROTOKOLL ON RSD_BON_BONKOPFID = BON_BONKOPFID" "~"
if joinKuerzel = PBABLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = PBA_CHRONIK" "~"
if joinKuerzel = BLSPBA
searchResult := searchResult "LEFT JOIN PRODUKTION.PBA_PLAN_BAS_ALT ON PBA_CHRONIK = BLS_BLOBLFDNR" "~"
if joinKuerzel = PERBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = PER_CHRONIK" "~"
if joinKuerzel = BLSPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_CHRONIK = BLS_BLOBLFDNR" "~"
if joinKuerzel = PROBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = PRO_CHRONIK" "~"
if joinKuerzel = BLSPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_CHRONIK = BLS_BLOBLFDNR" "~"
if joinKuerzel = STHBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = STH_CHRONIK" "~"
if joinKuerzel = BLSSTH
searchResult := searchResult "LEFT JOIN PRODUKTION.STH_STAKE ON STH_CHRONIK = BLS_BLOBLFDNR" "~"
if joinKuerzel = UVABLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = UVA_BLOBLFDNR" "~"
if joinKuerzel = BLSUVA
searchResult := searchResult "LEFT JOIN FIBU.UVA_UVAMODELL ON UVA_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = UZLBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = UZL_CHECKLISTLFDNR" "~"
if joinKuerzel = BLSUZL
searchResult := searchResult "LEFT JOIN BUERO.UZL_UNTERZIEL ON UZL_CHECKLISTLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = XSEBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = XSE_BILANZ_ID" "~"
if joinKuerzel = BLSXSE
searchResult := searchResult "LEFT JOIN BUERO.XSE_XML_STEUERERKL ON XSE_BILANZ_ID = BLS_BLOBLFDNR" "~"
if joinKuerzel = BURBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = BUR_METADATEI_BLOBLFDNR" "~"
if joinKuerzel = BLSBUR
searchResult := searchResult "LEFT JOIN BUERO.BUR_BMDCOMUPLOADTREE ON BUR_METADATEI_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = KSLBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = KSL_KUNDE_BLOBLFDNR" "~"
if joinKuerzel = BLSKSL
searchResult := searchResult "LEFT JOIN BUERO.KSL_KONTAKTSYNCEINST ON KSL_KUNDE_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = RIABLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = RIA_CHECKLISTID" "~"
if joinKuerzel = BLSRIA
searchResult := searchResult "LEFT JOIN PRODUKTION.RIA_RISIKOANALYSE ON RIA_CHECKLISTID = BLS_BLOBLFDNR" "~"
if joinKuerzel = OUFBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = OUF_BILD_RT" "~"
if joinKuerzel = BLSOUF
searchResult := searchResult "LEFT JOIN BUERO.OUF_ONLINEUMFRAGE ON OUF_BILD_RT = BLS_BLOBLFDNR" "~"
if joinKuerzel = FIRBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = FIR_FUSSWORDFLG_BLOBLFDNR" "~"
if joinKuerzel = BLSFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FUSSWORDFLG_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = GLSBMF
searchResult := searchResult "LEFT JOIN BMF_BMDFORMATKATALOG.BMF_BMDFORMATKATALOG ON BMF_KATALOGNR = GLS_KATALOGNR" "~"
if joinKuerzel = BMFGLS
searchResult := searchResult "LEFT JOIN BIL.GLS_GLIEDERUNGSSCHEMA ON GLS_KATALOGNR = BMF_KATALOGNR" "~"
if joinKuerzel = RGZBMG
searchResult := searchResult "LEFT JOIN BMD.BMG_BMDGRUPPE ON BMG_BMDGRUPPENID = RGZ_BMDGRUPPENID" "~"
if joinKuerzel = BMGRGZ
searchResult := searchResult "LEFT JOIN BMD.RGZ_ROLLEGRUPPEZU ON RGZ_BMDGRUPPENID = BMG_BMDGRUPPENID" "~"
if joinKuerzel = KZPBMO
searchResult := searchResult "LEFT JOIN FIBU.BMO_BUDGETMODELL ON BMO_FIRMENNR = KZP_FIRMENNR AND BMO_FIBUNR = KZP_FIBUNR AND BMO_BUDGETMODELLNR = KZP_BUDGETMODELLNR" "~"
if joinKuerzel = BMOKZP
searchResult := searchResult "LEFT JOIN FIBU.KZP_ZAHLUNGSPLAN ON KZP_FIRMENNR = BMO_FIRMENNR AND KZP_FIBUNR = BMO_FIBUNR AND KZP_BUDGETMODELLNR = BMO_BUDGETMODELLNR" "~"
if joinKuerzel = ALEBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = ALE_UMFDATENTRANSUSER" "~"
if joinKuerzel = BMUALE
searchResult := searchResult "LEFT JOIN BUERO.ALE_ADRESSENLISTE ON ALE_UMFDATENTRANSUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BMHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BMH_BMDUSERID" "~"
if joinKuerzel = BMUBMH
searchResult := searchResult "LEFT JOIN BMD.BMH_BMDUSER_VALUEHISTORY ON BMH_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = DELBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = DEL_LOESCHUSER" "~"
if joinKuerzel = BMUDEL
searchResult := searchResult "LEFT JOIN BUERO.DEL_DELETE ON DEL_LOESCHUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FIRBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = FIR_BMDUSERID" "~"
if joinKuerzel = BMUFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = FWFBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = FWF_BILBU_SPERREBIS_USER" "~"
if joinKuerzel = BMUFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_BILBU_SPERREBIS_USER = BMU_BMDUSERID" "~"
if joinKuerzel = KLIBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = KLI_GEPRUEFT_VON" "~"
if joinKuerzel = BMUKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_GEPRUEFT_VON = BMU_BMDUSERID" "~"
if joinKuerzel = SCHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCH_BMDUSERID_EXECUTE" "~"
if joinKuerzel = BMUSCH
searchResult := searchResult "LEFT JOIN BMD.SCH_SCHEDULE ON SCH_BMDUSERID_EXECUTE = BMU_BMDUSERID" "~"
if joinKuerzel = VPEBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = VPE_DRUCKUSERID" "~"
if joinKuerzel = BMUVPE
searchResult := searchResult "LEFT JOIN WWS.VPE_VPI_ERHOEHUNG ON VPE_DRUCKUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = ZLABMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = ZLA_FIBU_USERID" "~"
if joinKuerzel = BMUZLA
searchResult := searchResult "LEFT JOIN ZLE.ZLA_ZEILAB ON ZLA_FIBU_USERID = BMU_BMDUSERID" "~"
if joinKuerzel = ORTBLD
searchResult := searchResult "LEFT JOIN BUERO.BLD_BUNDESLAND ON BLD_LANDNR = ORT_BLD_LANDNR AND BLD_BUNDESLANDLFDNR = ORT_BUNDESLANDLFDNR" "~"
if joinKuerzel = BLDORT
searchResult := searchResult "LEFT JOIN BUERO.ORT_ORT ON ORT_BLD_LANDNR = BLD_LANDNR AND ORT_BUNDESLANDLFDNR = BLD_BUNDESLANDLFDNR" "~"
if joinKuerzel = BDLBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = BDL_BLS_BLOBLFDNR2" "~"
if joinKuerzel = BLSBDL
searchResult := searchResult "LEFT JOIN BUERO.BDL_BUERODELETE ON BDL_BLS_BLOBLFDNR2 = BLS_BLOBLFDNR" "~"
if joinKuerzel = BOPBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = BOP_BLS_BLOBNR_PREISINFO" "~"
if joinKuerzel = BLSBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_BLS_BLOBNR_PREISINFO = BLS_BLOBLFDNR" "~"
if joinKuerzel = BPOBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = BPO_BLOBLFDNR" "~"
if joinKuerzel = BLSBPO
searchResult := searchResult "LEFT JOIN BUERO.BPO_BESPRECHOBJEKTE ON BPO_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = CLVBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = CLV_BLOBLFDNR" "~"
if joinKuerzel = BLSCLV
searchResult := searchResult "LEFT JOIN BUERO.CLV_CHECKLISTVORLAGE ON CLV_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = DBXBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = DBX_BLOBLFDNR" "~"
if joinKuerzel = BLSDBX
searchResult := searchResult "LEFT JOIN BUERO.DBX_FINONDATABOX ON DBX_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = EFKBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = EFK_LETZTE_ANZ_BLOBLFDNR" "~"
if joinKuerzel = BLSEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_LETZTE_ANZ_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = FBABLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = FBA_BLOBLFDNR" "~"
if joinKuerzel = BLSFBA
searchResult := searchResult "LEFT JOIN BIL.FBA_XMLJABFBANHANG ON FBA_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = FSTBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = FST_BLOBLFDNR" "~"
if joinKuerzel = BLSFST
searchResult := searchResult "LEFT JOIN BUERO.FST_FRIST ON FST_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = KGSBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = KGS_BLOBLFDNR" "~"
if joinKuerzel = BLSKGS
searchResult := searchResult "LEFT JOIN WWS.KGS_KATZU_ARBEITSGANG ON KGS_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = PCKBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = PCK_BLS_BLOBLFDNR" "~"
if joinKuerzel = BLSPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_BLS_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = PVABLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = PVA_MITTEILUNG_BLOBLFDNR" "~"
if joinKuerzel = BLSPVA
searchResult := searchResult "LEFT JOIN BUERO.PVA_PERSONVORERFASSAKTION ON PVA_MITTEILUNG_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = RGTBLS
searchResult := searchResult "LEFT JOIN BMD.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = RGT_FILTER_BLOBLFDNR" "~"
if joinKuerzel = BLSRGT
searchResult := searchResult "LEFT JOIN BMD.RGT_TABREPLGTABELLEZU ON RGT_FILTER_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = RNGBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = RNG_RTFDOK_BLOBLFDNR" "~"
if joinKuerzel = BLSRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_RTFDOK_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = SBSBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = SBS_BLOBLFDNR" "~"
if joinKuerzel = BLSSBS
searchResult := searchResult "LEFT JOIN BUERO.SBS_STELLENBESCHREIBUNG ON SBS_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = SCEBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = SCE_RTF_TEXT" "~"
if joinKuerzel = BLSSCE
searchResult := searchResult "LEFT JOIN PRODUKTION.SCE_SCORECARD ON SCE_RTF_TEXT = BLS_BLOBLFDNR" "~"
if joinKuerzel = SCFBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = SCF_BLOBLFDNR" "~"
if joinKuerzel = BLSSCF
searchResult := searchResult "LEFT JOIN BMDENTW.SCF_SCRFILE ON SCF_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = SCNBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = SCN_BLOBLFDNR" "~"
if joinKuerzel = BLSSCN
searchResult := searchResult "LEFT JOIN BMDENTW.SCN_SCRNOTIZ ON SCN_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = WADBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = WAD_CHRONIK" "~"
if joinKuerzel = BLSWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_CHRONIK = BLS_BLOBLFDNR" "~"
if joinKuerzel = AMPBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = AMP_ANHANG_BLOBLFDNR" "~"
if joinKuerzel = BLSAMP
searchResult := searchResult "LEFT JOIN BUERO.AMP_AUTOMAILARCHIVPARAM ON AMP_ANHANG_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = PERBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = PER_TEXT1_CHRONIK" "~"
if joinKuerzel = BLSPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_TEXT1_CHRONIK = BLS_BLOBLFDNR" "~"
if joinKuerzel = PIABLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = PIA_BESCHR_BLOBLFDNR" "~"
if joinKuerzel = BLSPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_BESCHR_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = PROBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = PRO_CHECKLISTLFDNR" "~"
if joinKuerzel = BLSPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_CHECKLISTLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = RNGBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = RNG_VERSP_MELDUNG_BLOBLFDNR" "~"
if joinKuerzel = BLSRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_VERSP_MELDUNG_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = BCGBMG
searchResult := searchResult "LEFT JOIN BMG_BMDGRUPPE.BMG_BMDGRUPPE ON BMG_BMDGRUPPENID = BCG_BMDGRUPPENID" "~"
if joinKuerzel = BMGBCG
searchResult := searchResult "LEFT JOIN BUERO.BCG_BMDCOMUSERGRUPPENZU ON BCG_BMDGRUPPENID = BMG_BMDGRUPPENID" "~"
if joinKuerzel = FFBBMG
searchResult := searchResult "LEFT JOIN BMD.BMG_BMDGRUPPE ON BMG_BMDGRUPPENID = FFB_BMDGRUPPENID" "~"
if joinKuerzel = BMGFFB
searchResult := searchResult "LEFT JOIN BMD.FFB_FUNKFELDBESCHRECHTE ON FFB_BMDGRUPPENID = BMG_BMDGRUPPENID" "~"
if joinKuerzel = FURBMG
searchResult := searchResult "LEFT JOIN BMD.BMG_BMDGRUPPE ON BMG_BMDGRUPPENID = FUR_BMDGRUPPENID" "~"
if joinKuerzel = BMGFUR
searchResult := searchResult "LEFT JOIN BMD.FUR_FUNKRECHTE ON FUR_BMDGRUPPENID = BMG_BMDGRUPPENID" "~"
if joinKuerzel = DBEBMG
searchResult := searchResult "LEFT JOIN BMG_BMDGRUPPE.BMG_BMDGRUPPE ON BMG_BMDGRUPPENID = DBE_BMDGRUPPENID" "~"
if joinKuerzel = BMGDBE
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DBE_DBERECHTIGUNG ON DBE_BMDGRUPPENID = BMG_BMDGRUPPENID" "~"
if joinKuerzel = BUDBMO
searchResult := searchResult "LEFT JOIN FIBU.BMO_BUDGETMODELL ON BMO_FIRMENNR = BUD_FIRMENNR AND BMO_FIBUNR = BUD_FIBUNR AND BMO_BUDGETMODELLNR = BUD_BUDGETMODELLNR" "~"
if joinKuerzel = BMOBUD
searchResult := searchResult "LEFT JOIN FIBU.BUD_BUDGET ON BUD_FIRMENNR = BMO_FIRMENNR AND BUD_FIBUNR = BMO_FIBUNR AND BUD_BUDGETMODELLNR = BMO_BUDGETMODELLNR" "~"
if joinKuerzel = BRHBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = BRH_BMDUSERID" "~"
if joinKuerzel = BMUBRH
searchResult := searchResult "LEFT JOIN FIBU.BRH_BANKRVKOPF ON BRH_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = DFBBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = DFB_BMDUSERID" "~"
if joinKuerzel = BMUDFB
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFB_DFELDBESCHKOPF ON DFB_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = OPEBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = OPE_BMDUSERID" "~"
if joinKuerzel = BMUOPE
searchResult := searchResult "LEFT JOIN LOHN.OPE_OF_PERSON ON OPE_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = PLCBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = PLC_AV_FREIGABE_USER" "~"
if joinKuerzel = BMUPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_AV_FREIGABE_USER = BMU_BMDUSERID" "~"
if joinKuerzel = PVGBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = PVG_UEBERNOMMENVON" "~"
if joinKuerzel = BMUPVG
searchResult := searchResult "LEFT JOIN BUERO.PVG_PERSONVORERFASSANGEH ON PVG_UEBERNOMMENVON = BMU_BMDUSERID" "~"
if joinKuerzel = BUKBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = BUK_JOURNALISIERUNGSUSER" "~"
if joinKuerzel = BMUBUK
searchResult := searchResult "LEFT JOIN FIBU.BUK_BUCHUNGSKREIS ON BUK_JOURNALISIERUNGSUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCRBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = SCR_BESITZERUSERID" "~"
if joinKuerzel = BMUSCR
searchResult := searchResult "LEFT JOIN BMDENTW.SCR_SCRREGELWERK ON SCR_BESITZERUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = SCHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCH_BMDUSERID_EMAIL_ERROR" "~"
if joinKuerzel = BMUSCH
searchResult := searchResult "LEFT JOIN BMD.SCH_SCHEDULE ON SCH_BMDUSERID_EMAIL_ERROR = BMU_BMDUSERID" "~"
if joinKuerzel = VPEBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = VPE_VERARBEITETUSERID" "~"
if joinKuerzel = BMUVPE
searchResult := searchResult "LEFT JOIN WWS.VPE_VPI_ERHOEHUNG ON VPE_VERARBEITETUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = BSPBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_BONKOPFID = BSP_BONKOPFID" "~"
if joinKuerzel = BONBSP
searchResult := searchResult "LEFT JOIN WWS.BSP_BONUSERMPOS ON BSP_BONKOPFID = BON_BONKOPFID" "~"
if joinKuerzel = FMJBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_BONKOPFID = FMJ_BONKOPFID" "~"
if joinKuerzel = BONFMJ
searchResult := searchResult "LEFT JOIN WWS.FMJ_FISKALMODULJOURNAL ON FMJ_BONKOPFID = BON_BONKOPFID" "~"
if joinKuerzel = KBEBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_BONKOPFID = KBE_BON_BONKOPFID" "~"
if joinKuerzel = BONKBE
searchResult := searchResult "LEFT JOIN WWS.KBE_KASSEBUCHEINZELNW ON KBE_BON_BONKOPFID = BON_BONKOPFID" "~"
if joinKuerzel = VATBON
searchResult := searchResult "LEFT JOIN BON_BONKOPF.BON_BONKOPF ON BON_BONKOPFID = VAT_BONKOPFID" "~"
if joinKuerzel = BONVAT
searchResult := searchResult "LEFT JOIN BUERO.VAT_TEILNAHME ON VAT_BONKOPFID = BON_BONKOPFID" "~"
if joinKuerzel = ADBBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_BONPOSID = ADB_BOP_BONPOSID" "~"
if joinKuerzel = BOPADB
searchResult := searchResult "LEFT JOIN WWS.ADB_ABGABENDETAILBON ON ADB_BOP_BONPOSID = BOP_BONPOSID" "~"
if joinKuerzel = BORBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_BONPOSID = BOR_BONPOSID" "~"
if joinKuerzel = BOPBOR
searchResult := searchResult "LEFT JOIN WWS.BOR_BONRABATT ON BOR_BONPOSID = BOP_BONPOSID" "~"
if joinKuerzel = VEPBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_BONPOSID = VEP_BOP_BONPOSID" "~"
if joinKuerzel = BOPVEP
searchResult := searchResult "LEFT JOIN WWS.VEP_VERTRETERABRECHPOS ON VEP_BOP_BONPOSID = BOP_BONPOSID" "~"
if joinKuerzel = BKJBOZ
searchResult := searchResult "LEFT JOIN WWS.BOZ_BONZAHLUNG ON BOZ_BONZAHLUNGSID = BKJ_BOZ_BONZAHLUNGSID" "~"
if joinKuerzel = BOZBKJ
searchResult := searchResult "LEFT JOIN WWS.BKJ_BANKOMATJOURNAL ON BKJ_BOZ_BONZAHLUNGSID = BOZ_BONZAHLUNGSID" "~"
if joinKuerzel = ADEBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_FIRMENNR = ADE_FIRMENNR AND BPD_ERFASSUNGSKZ = ADE_BPD_ERFASSUNGSKZ AND BPD_BESTPRODERFASSUNGSNR = ADE_BPD_BESTPRODERFASSNR AND BPD_BESTPOSLFDNR = ADE_BPD_BESTPOSLFDNR AND BPD_ERFASSPOSDETAILLFDNR = ADE_BPD_ERFASSPOSDETLFDNR" "~"
if joinKuerzel = BPDADE
searchResult := searchResult "LEFT JOIN WWS.ADE_ABGABENDETAILERFPOS ON ADE_FIRMENNR = BPD_FIRMENNR AND ADE_BPD_ERFASSUNGSKZ = BPD_ERFASSUNGSKZ AND ADE_BPD_BESTPRODERFASSNR = BPD_BESTPRODERFASSUNGSNR AND ADE_BPD_BESTPOSLFDNR = BPD_BESTPOSLFDNR AND ADE_BPD_ERFASSPOSDETLFDNR = BPD_ERFASSPOSDETAILLFDNR" "~"
if joinKuerzel = FIRBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = FIR_FOLGEKOPF_BLOBLFDNR" "~"
if joinKuerzel = BLSFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FOLGEKOPF_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = RIABLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = RIA_RTF_TEXT_MASSNAHME" "~"
if joinKuerzel = BLSRIA
searchResult := searchResult "LEFT JOIN PRODUKTION.RIA_RISIKOANALYSE ON RIA_RTF_TEXT_MASSNAHME = BLS_BLOBLFDNR" "~"
if joinKuerzel = FIRBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = FIR_FOLGEFUSS_BLOBLFDNR" "~"
if joinKuerzel = BLSFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FOLGEFUSS_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = KOWBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = KOW_KVUPDATE_BLOBLFDNR" "~"
if joinKuerzel = BLSKOW
searchResult := searchResult "LEFT JOIN HRS.KOW_KOLLEKTIVVERTRAGWERTE ON KOW_KVUPDATE_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = KSLBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = KSL_MGLLIEFERANT_BLOBLFDNR" "~"
if joinKuerzel = BLSKSL
searchResult := searchResult "LEFT JOIN BUERO.KSL_KONTAKTSYNCEINST ON KSL_MGLLIEFERANT_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = BRHBML
searchResult := searchResult "LEFT JOIN FIBU.BML_BANKRVMODEL ON BML_BMLID = BRH_BMLID" "~"
if joinKuerzel = BMLBRH
searchResult := searchResult "LEFT JOIN FIBU.BRH_BANKRVKOPF ON BRH_BMLID = BML_BMLID" "~"
if joinKuerzel = FZPBMO
searchResult := searchResult "LEFT JOIN FIBU.BMO_BUDGETMODELL ON BMO_FIRMENNR = FZP_FIRMENNR AND BMO_FIBUNR = FZP_FIBUNR AND BMO_BUDGETMODELLNR = FZP_BUDGETMODELLNR" "~"
if joinKuerzel = BMOFZP
searchResult := searchResult "LEFT JOIN FIBU.FZP_FINANZPLAN ON FZP_FIRMENNR = BMO_FIRMENNR AND FZP_FIBUNR = BMO_FIBUNR AND FZP_BUDGETMODELLNR = BMO_BUDGETMODELLNR" "~"
if joinKuerzel = DOZBMR
searchResult := searchResult "LEFT JOIN BMD.BMR_BMDROLLE ON BMR_BMDROLLENID = DOZ_BMDROLLENID" "~"
if joinKuerzel = BMRDOZ
searchResult := searchResult "LEFT JOIN BMD.DOZ_DATAOWNERZU ON DOZ_BMDROLLENID = BMR_BMDROLLENID" "~"
if joinKuerzel = BSKBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = BSK_VERBUCHUNGSUSERID" "~"
if joinKuerzel = BMUBSK
searchResult := searchResult "LEFT JOIN WWS.BSK_BONUSERMKOPF ON BSK_VERBUCHUNGSUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = DBXBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = DBX_GELOESCHT_VON" "~"
if joinKuerzel = BMUDBX
searchResult := searchResult "LEFT JOIN BUERO.DBX_FINONDATABOX ON DBX_GELOESCHT_VON = BMU_BMDUSERID" "~"
if joinKuerzel = DVLBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = DVL_USERID" "~"
if joinKuerzel = BMUDVL
searchResult := searchResult "LEFT JOIN BUERO.DVL_DOKUMENTVERLAUF ON DVL_USERID = BMU_BMDUSERID" "~"
if joinKuerzel = HLFBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = HLF_USERID" "~"
if joinKuerzel = BMUHLF
searchResult := searchResult "LEFT JOIN BMDENTW.HLF_HILFEFAVORITEN ON HLF_USERID = BMU_BMDUSERID" "~"
if joinKuerzel = OUWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OUW_BMDUSERID" "~"
if joinKuerzel = BMUOUW
searchResult := searchResult "LEFT JOIN BMD.OUW_OUTLOOKBARWINDOW ON OUW_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = PPFBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = PPF_PRUEFUNG_USERID" "~"
if joinKuerzel = BMUPPF
searchResult := searchResult "LEFT JOIN BUERO.PPF_PERSONENPRUEFUNG ON PPF_PRUEFUNG_USERID = BMU_BMDUSERID" "~"
if joinKuerzel = PVYBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PVY_BMDUSERID" "~"
if joinKuerzel = BMUPVY
searchResult := searchResult "LEFT JOIN BMD.PVY_PACKAGEVISIBILITY ON PVY_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = RUZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RUZ_BMDUSERID" "~"
if joinKuerzel = BMURUZ
searchResult := searchResult "LEFT JOIN BMD.RUZ_ROLLEUSERZU ON RUZ_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = SCTBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = SCT_BMDUSERID" "~"
if joinKuerzel = BMUSCT
searchResult := searchResult "LEFT JOIN BMDENTW.SCT_SCRINTRAY ON SCT_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = UTHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UTH_USERID" "~"
if joinKuerzel = BMUUTH
searchResult := searchResult "LEFT JOIN BMD.UTH_USERTREEHEAD ON UTH_USERID = BMU_BMDUSERID" "~"
if joinKuerzel = FIRBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = FIR_BETRIEBSPRUEFER_USERID" "~"
if joinKuerzel = BMUFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_BETRIEBSPRUEFER_USERID = BMU_BMDUSERID" "~"
if joinKuerzel = FWFBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = FWF_BH_SPERRE_USER" "~"
if joinKuerzel = BMUFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_BH_SPERRE_USER = BMU_BMDUSERID" "~"
if joinKuerzel = UBEBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = UBE_SETSF_USERID" "~"
if joinKuerzel = BMUUBE
searchResult := searchResult "LEFT JOIN BMDINTERN.UBE_UPDATEBESTELLUNG ON UBE_SETSF_USERID = BMU_BMDUSERID" "~"
if joinKuerzel = BORBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_BONKOPFID = BOR_BONKOPFID" "~"
if joinKuerzel = BONBOR
searchResult := searchResult "LEFT JOIN WWS.BOR_BONRABATT ON BOR_BONKOPFID = BON_BONKOPFID" "~"
if joinKuerzel = ERPBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_BONPOSID = ERP_BONPOSID" "~"
if joinKuerzel = BOPERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_BONPOSID = BOP_BONPOSID" "~"
if joinKuerzel = KPOBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_BONPOSID = KPO_BONPOSID" "~"
if joinKuerzel = BOPKPO
searchResult := searchResult "LEFT JOIN WWS.KPO_KASSENPROVISION ON KPO_BONPOSID = BOP_BONPOSID" "~"
if joinKuerzel = PIPBES
searchResult := searchResult "LEFT JOIN BUERO.BES_BEWERBUNGSTATUS ON BES_BEWSTATUSNR = PIP_BES_BEWSTATUSNR" "~"
if joinKuerzel = BESPIP
searchResult := searchResult "LEFT JOIN BUERO.PIP_PERSINFOPROTOKOLL ON PIP_BES_BEWSTATUSNR = BES_BEWSTATUSNR" "~"
if joinKuerzel = BGPBGS
searchResult := searchResult "LEFT JOIN HRS.BGS_BEITRAGSGRUPPE_SEQ ON BGS_BEITRAGSGRUPPEID = BGP_SID_LEHRLING1" "~"
if joinKuerzel = BGSBGP
searchResult := searchResult "LEFT JOIN HRS.BGP_BEITRAGSGRUPPE ON BGP_SID_LEHRLING1 = BGS_BEITRAGSGRUPPEID" "~"
if joinKuerzel = BGPBGS
searchResult := searchResult "LEFT JOIN HRS.BGS_BEITRAGSGRUPPE_SEQ ON BGS_BEITRAGSGRUPPEID = BGP_SID_LEHRLING2" "~"
if joinKuerzel = BGSBGP
searchResult := searchResult "LEFT JOIN HRS.BGP_BEITRAGSGRUPPE ON BGP_SID_LEHRLING2 = BGS_BEITRAGSGRUPPEID" "~"
if joinKuerzel = ADRBLD
searchResult := searchResult "LEFT JOIN BUERO.BLD_BUNDESLAND ON BLD_LANDNR = ADR_BLD_LANDNR AND BLD_BUNDESLANDLFDNR = ADR_BUNDESLANDLFDNR" "~"
if joinKuerzel = BLDADR
searchResult := searchResult "LEFT JOIN BUERO.ADR_ADRESSE ON ADR_BLD_LANDNR = BLD_LANDNR AND ADR_BUNDESLANDLFDNR = BLD_BUNDESLANDLFDNR" "~"
if joinKuerzel = AFBBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = AFB_CHRONIK" "~"
if joinKuerzel = BLSAFB
searchResult := searchResult "LEFT JOIN FIBU.AFB_ANBUFIB ON AFB_CHRONIK = BLS_BLOBLFDNR" "~"
if joinKuerzel = AUCBLS
searchResult := searchResult "LEFT JOIN BMD.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = AUC_BLOBLFDNR" "~"
if joinKuerzel = BLSAUC
searchResult := searchResult "LEFT JOIN BMD.AUC_AUSWERTUNGSCENTER ON AUC_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = DFRBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = DFR_BLOBLFDNR" "~"
if joinKuerzel = BLSDFR
searchResult := searchResult "LEFT JOIN WWS.DFR_DOKUMENTFREIGABE ON DFR_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = OUFBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = OUF_BLOBLFDNR" "~"
if joinKuerzel = BLSOUF
searchResult := searchResult "LEFT JOIN BUERO.OUF_ONLINEUMFRAGE ON OUF_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = REIBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = REI_BLOBLFDNR" "~"
if joinKuerzel = BLSREI
searchResult := searchResult "LEFT JOIN HRS.REI_REISESTAMM ON REI_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = SLKBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = SLK_CHRONIK" "~"
if joinKuerzel = BLSSLK
searchResult := searchResult "LEFT JOIN WWS.SLK_STUECKLISTENKOPF ON SLK_CHRONIK = BLS_BLOBLFDNR" "~"
if joinKuerzel = STWBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = STW_BLOBLFDNR" "~"
if joinKuerzel = BLSSTW
searchResult := searchResult "LEFT JOIN BIL.STW_STEUERWERT ON STW_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = TCVBLS
searchResult := searchResult "LEFT JOIN BMD.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = TCV_BEFEHLBLOBLFDNR" "~"
if joinKuerzel = BLSTCV
searchResult := searchResult "LEFT JOIN BMD.TCV_TCVERSIONCASE ON TCV_BEFEHLBLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = UPHBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = UPH_BLOBLFDNR" "~"
if joinKuerzel = BLSUPH
searchResult := searchResult "LEFT JOIN BMDENTW.UPH_UPDATEHINWEIS ON UPH_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = ZASBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = ZAS_VERWTEXT_BLOBLFDNR" "~"
if joinKuerzel = BLSZAS
searchResult := searchResult "LEFT JOIN BUERO.ZAS_ZAHLSCHEIN ON ZAS_VERWTEXT_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = BKJBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = BKJ_BLS_BLOBNR_KNDBELEG" "~"
if joinKuerzel = BLSBKJ
searchResult := searchResult "LEFT JOIN WWS.BKJ_BANKOMATJOURNAL ON BKJ_BLS_BLOBNR_KNDBELEG = BLS_BLOBLFDNR" "~"
if joinKuerzel = DPDBLS
searchResult := searchResult "LEFT JOIN BMD.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = DPD_METABLOBLFDNR" "~"
if joinKuerzel = BLSDPD
searchResult := searchResult "LEFT JOIN BMD.DPD_DOCUMENTPOOLDETAIL ON DPD_METABLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = KOWBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = KOW_SERVICE_BLOBLFDNR" "~"
if joinKuerzel = BLSKOW
searchResult := searchResult "LEFT JOIN HRS.KOW_KOLLEKTIVVERTRAGWERTE ON KOW_SERVICE_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = TSRBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = TSR_BLOBLFDNR" "~"
if joinKuerzel = BLSTSR
searchResult := searchResult "LEFT JOIN BUERO.TSR_SERIENTERMIN ON TSR_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = UZLBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = UZL_BLOBLFDNR" "~"
if joinKuerzel = BLSUZL
searchResult := searchResult "LEFT JOIN BUERO.UZL_UNTERZIEL ON UZL_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = RIABLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = RIA_RTF_TEXT" "~"
if joinKuerzel = BLSRIA
searchResult := searchResult "LEFT JOIN PRODUKTION.RIA_RISIKOANALYSE ON RIA_RTF_TEXT = BLS_BLOBLFDNR" "~"
if joinKuerzel = OUFBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = OUF_BILD_BOTTOM" "~"
if joinKuerzel = BLSOUF
searchResult := searchResult "LEFT JOIN BUERO.OUF_ONLINEUMFRAGE ON OUF_BILD_BOTTOM = BLS_BLOBLFDNR" "~"
if joinKuerzel = FIRBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = FIR_BRIEF_BLOBLFDNR" "~"
if joinKuerzel = BLSFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_BRIEF_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = REMBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = REM_BMDUSERID" "~"
if joinKuerzel = BMUREM
searchResult := searchResult "LEFT JOIN BUERO.REM_REMINDER ON REM_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = RPFBMF
searchResult := searchResult "LEFT JOIN BMD.BMF_BMDFORMATKATALOG ON BMF_KATALOGNR = RPF_KATALOGNR" "~"
if joinKuerzel = BMFRPF
searchResult := searchResult "LEFT JOIN BMD.RPF_REPORTFORMATVORLAGE ON RPF_KATALOGNR = BMF_KATALOGNR" "~"
if joinKuerzel = VEPBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_BONKOPFID = VEP_BON_BONKOPFID" "~"
if joinKuerzel = BONVEP
searchResult := searchResult "LEFT JOIN WWS.VEP_VERTRETERABRECHPOS ON VEP_BON_BONKOPFID = BON_BONKOPFID" "~"
if joinKuerzel = KAIBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_BONPOSID = KAI_BONPOSID" "~"
if joinKuerzel = BOPKAI
searchResult := searchResult "LEFT JOIN WWS.KAI_KNDARTIKELINFO ON KAI_BONPOSID = BOP_BONPOSID" "~"
if joinKuerzel = RNPBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_BONPOSID = RNP_BONPOSID" "~"
if joinKuerzel = BOPRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_BONPOSID = BOP_BONPOSID" "~"
if joinKuerzel = GSPBOZ
searchResult := searchResult "LEFT JOIN WWS.BOZ_BONZAHLUNG ON BOZ_BONZAHLUNGSID = GSP_BONZAHLUNGSID" "~"
if joinKuerzel = BOZGSP
searchResult := searchResult "LEFT JOIN WWS.GSP_GUTSCHEINPOSITION ON GSP_BONZAHLUNGSID = BOZ_BONZAHLUNGSID" "~"
if joinKuerzel = KOZBPD
searchResult := searchResult "LEFT JOIN BPD_BESTPOSDETAIL.BPD_BESTPOSDETAIL ON BPD_FIRMENNR = KOZ_ERFASS_FIRMENNR AND BPD_ERFASSUNGSKZ = KOZ_ERFASSUNGSKZ AND BPD_BESTPRODERFASSUNGSNR = KOZ_BESTELLNR AND BPD_BESTPOSLFDNR = KOZ_BESTELLPOSNR AND BPD_ERFASSPOSDETAILLFDNR = KOZ_BESTELLPOSDETAIL" "~"
if joinKuerzel = BPDKOZ
searchResult := searchResult "LEFT JOIN PRODUKTION.KOZ_KOMMISSIONIERUNG_ZU ON KOZ_ERFASS_FIRMENNR = BPD_FIRMENNR AND KOZ_ERFASSUNGSKZ = BPD_ERFASSUNGSKZ AND KOZ_BESTELLNR = BPD_BESTPRODERFASSUNGSNR AND KOZ_BESTELLPOSNR = BPD_BESTPOSLFDNR AND KOZ_BESTELLPOSDETAIL = BPD_ERFASSPOSDETAILLFDNR" "~"
if joinKuerzel = BPDBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_BESTPOSDETAILID = BPD_ANF_BESTPOSDETAILID" "~"
if joinKuerzel = BPDBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_ANF_BESTPOSDETAILID = BPD_BESTPOSDETAILID" "~"
if joinKuerzel = BPPBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_FIRMENNR = BPP_FIRMENNR AND BPK_ERFASSUNGSKZ = BPP_ERFASSUNGSKZ AND BPK_BESTPRODERFASSUNGSNR = BPP_BESTPRODERFASSUNGSNR" "~"
if joinKuerzel = BPKBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_FIRMENNR = BPK_FIRMENNR AND BPP_ERFASSUNGSKZ = BPK_ERFASSUNGSKZ AND BPP_BESTPRODERFASSUNGSNR = BPK_BESTPRODERFASSUNGSNR" "~"
if joinKuerzel = EROBPZ
searchResult := searchResult "LEFT JOIN WWS.BPZ_BESTELLPOSZUORDNUNG ON BPZ_ERFASSPOSORIGID = ERO_ERFASSPOSORIGID" "~"
if joinKuerzel = BPZERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_ERFASSPOSORIGID = BPZ_ERFASSPOSORIGID" "~"
if joinKuerzel = TKTBRC
searchResult := searchResult "LEFT JOIN BRC_BEREICH.BRC_BEREICH ON BRC_BEREICHKZ = TKT_BEREICHKZ AND BRC_BEREICHLFDNR = TKT_BEREICHLFDNR" "~"
if joinKuerzel = BRCTKT
searchResult := searchResult "LEFT JOIN BMDENTW.TKT_TICKETBEREICHZU ON TKT_BEREICHKZ = BRC_BEREICHKZ AND TKT_BEREICHLFDNR = BRC_BEREICHLFDNR" "~"
if joinKuerzel = PSWBRG
searchResult := searchResult "LEFT JOIN BUERO.BRG_BERUFSGRUPPEN ON BRG_FIRMENNR = PSW_BRG_FIRMENNR AND BRG_BRGGRUPPENNR = PSW_BRGGRUPPENNR" "~"
if joinKuerzel = BRGPSW
searchResult := searchResult "LEFT JOIN BUERO.PSW_PERSONENWERTE ON PSW_BRG_FIRMENNR = BRG_FIRMENNR AND PSW_BRGGRUPPENNR = BRG_BRGGRUPPENNR" "~"
if joinKuerzel = BRSBRH
searchResult := searchResult "LEFT JOIN FIBU.BRH_BANKRVKOPF ON BRH_FIRMENNR = BRS_FIRMENNR AND BRH_FIBUNR = BRS_FIBUNR AND BRH_BRHID = BRS_BRV_BRHID" "~"
if joinKuerzel = BRHBRS
searchResult := searchResult "LEFT JOIN FIBU.BRS_BANKRUECKAUFTEILSATZ ON BRS_FIRMENNR = BRH_FIRMENNR AND BRS_FIBUNR = BRH_FIBUNR AND BRS_BRV_BRHID = BRH_BRHID" "~"
if joinKuerzel = LEHBRK
searchResult := searchResult "LEFT JOIN HRS.BRK_BEARBEITERKZ ON BRK_BEARBKZID = LEH_BEARBKZID AND BRK_DATENBESTANDNR = LEH_DBS" "~"
if joinKuerzel = BRKLEH
searchResult := searchResult "LEFT JOIN HRS.LEH_LOHNEINSTELLUNG_HIST ON LEH_BEARBKZID = BRK_BEARBKZID AND LEH_DBS = BRK_DATENBESTANDNR" "~"
if joinKuerzel = CAWBRM
searchResult := searchResult "LEFT JOIN BUERO.BRM_BUEROMAKROS ON BRM_BUEROMAKROID = CAW_BUEROMAKROID_FRI" "~"
if joinKuerzel = BRMCAW
searchResult := searchResult "LEFT JOIN BUERO.CAW_CRMABWESENHEIT ON CAW_BUEROMAKROID_FRI = BRM_BUEROMAKROID" "~"
if joinKuerzel = WFNBRM
searchResult := searchResult "LEFT JOIN BUERO.BRM_BUEROMAKROS ON BRM_BUEROMAKROID = WFN_BEGINN_BUEROMAKROID" "~"
if joinKuerzel = BRMWFN
searchResult := searchResult "LEFT JOIN BUERO.WFN_WORKFLOWACTION ON WFN_BEGINN_BUEROMAKROID = BRM_BUEROMAKROID" "~"
if joinKuerzel = WFNBRM
searchResult := searchResult "LEFT JOIN BUERO.BRM_BUEROMAKROS ON BRM_BUEROMAKROID = WFN_ERLEDIGT_BUEROMAKROID" "~"
if joinKuerzel = BRMWFN
searchResult := searchResult "LEFT JOIN BUERO.WFN_WORKFLOWACTION ON WFN_ERLEDIGT_BUEROMAKROID = BRM_BUEROMAKROID" "~"
if joinKuerzel = KFFBTF
searchResult := searchResult "LEFT JOIN BTF_BMDTABFELDBESCH.BTF_BMDTABFELDBESCH ON BTF_TABELLENNAME = KFF_TABELLENNAME_QUELLE AND BTF_FELDNAME = KFF_FELDNAME_QUELLE" "~"
if joinKuerzel = BTFKFF
searchResult := searchResult "LEFT JOIN BUERO.KFF_KORETYPFIND_FELDERZU ON KFF_TABELLENNAME_QUELLE = BTF_TABELLENNAME AND KFF_FELDNAME_QUELLE = BTF_FELDNAME" "~"
if joinKuerzel = TBZBTM
searchResult := searchResult "LEFT JOIN BUERO.BTM_BTERMINAL ON BTM_FIRMENNR = TBZ_BTM_FIRMENNR AND BTM_TERMINALNR = TBZ_TERMINALNR" "~"
if joinKuerzel = BTMTBZ
searchResult := searchResult "LEFT JOIN BUERO.TBZ_TERMINALBUCHARTZUORD ON TBZ_BTM_FIRMENNR = BTM_FIRMENNR AND TBZ_TERMINALNR = BTM_TERMINALNR" "~"
if joinKuerzel = TZMBTM
searchResult := searchResult "LEFT JOIN BUERO.BTM_BTERMINAL ON BTM_FIRMENNR = TZM_FIRMENNR_SUB AND BTM_TERMINALNR = TZM_TERMINALNR_SUB" "~"
if joinKuerzel = BTMTZM
searchResult := searchResult "LEFT JOIN BUERO.TZM_TERMINALZUORDMASTER ON TZM_FIRMENNR_SUB = BTM_FIRMENNR AND TZM_TERMINALNR_SUB = BTM_TERMINALNR" "~"
if joinKuerzel = BTDBTS
searchResult := searchResult "LEFT JOIN BUERO.BTS_BTERMSATZART ON BTS_BTH_FIRMENNR = BTD_BTH_FIRMENNR AND BTS_TERMINALHERSTELLER = BTD_TERMINALHERSTELLER AND BTS_FIRMENNR = BTD_FIRMENNR AND BTS_BUCHCODE = BTD_BUCHCODE" "~"
if joinKuerzel = BTSBTD
searchResult := searchResult "LEFT JOIN BUERO.BTD_BTERMSATZARTDETAIL ON BTD_BTH_FIRMENNR = BTS_BTH_FIRMENNR AND BTD_TERMINALHERSTELLER = BTS_TERMINALHERSTELLER AND BTD_FIRMENNR = BTS_FIRMENNR AND BTD_BUCHCODE = BTS_BUCHCODE" "~"
if joinKuerzel = KSKBUA
searchResult := searchResult "LEFT JOIN BUA_BUCHUNGSART.BUA_BUCHUNGSART ON BUA_FIRMENNR = KSK_FIRMENNR AND BUA_FIBUNR = KSK_AUFKONTO_FIBUNR AND BUA_BUART = KSK_BUART" "~"
if joinKuerzel = BUAKSK
searchResult := searchResult "LEFT JOIN KORE.KSK_KORESCHLUESSELKOPF ON KSK_FIRMENNR = BUA_FIRMENNR AND KSK_AUFKONTO_FIBUNR = BUA_FIBUNR AND KSK_BUART = BUA_BUART" "~"
if joinKuerzel = DBXBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = DBX_KONTROLLIERT_VON" "~"
if joinKuerzel = BMUDBX
searchResult := searchResult "LEFT JOIN BUERO.DBX_FINONDATABOX ON DBX_KONTROLLIERT_VON = BMU_BMDUSERID" "~"
if joinKuerzel = SCHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCH_BMDUSERID_SMS_ERROR" "~"
if joinKuerzel = BMUSCH
searchResult := searchResult "LEFT JOIN BMD.SCH_SCHEDULE ON SCH_BMDUSERID_SMS_ERROR = BMU_BMDUSERID" "~"
if joinKuerzel = BOZBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_BONKOPFID = BOZ_BON_BONKOPFID" "~"
if joinKuerzel = BONBOZ
searchResult := searchResult "LEFT JOIN WWS.BOZ_BONZAHLUNG ON BOZ_BON_BONKOPFID = BON_BONKOPFID" "~"
if joinKuerzel = KAEBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_BONKOPFID = KAE_BONKOPFID" "~"
if joinKuerzel = BONKAE
searchResult := searchResult "LEFT JOIN WWS.KAE_KASSENEINAUSGABEN ON KAE_BONKOPFID = BON_BONKOPFID" "~"
if joinKuerzel = KPOBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_BONKOPFID = KPO_BONKOPFID" "~"
if joinKuerzel = BONKPO
searchResult := searchResult "LEFT JOIN WWS.KPO_KASSENPROVISION ON KPO_BONKOPFID = BON_BONKOPFID" "~"
if joinKuerzel = GSPBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_BONPOSID = GSP_BONPOSID" "~"
if joinKuerzel = BOPGSP
searchResult := searchResult "LEFT JOIN WWS.GSP_GUTSCHEINPOSITION ON GSP_BONPOSID = BOP_BONPOSID" "~"
if joinKuerzel = KEPBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_BONPOSID = KEP_BOP_BONPOSID" "~"
if joinKuerzel = BOPKEP
searchResult := searchResult "LEFT JOIN WWS.KEP_KASSENERFASSUNGSPROT ON KEP_BOP_BONPOSID = BOP_BONPOSID" "~"
if joinKuerzel = BPZBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_BESTPOSDETAILID = BPZ_BESTPOSDETAILID" "~"
if joinKuerzel = BPDBPZ
searchResult := searchResult "LEFT JOIN WWS.BPZ_BESTELLPOSZUORDNUNG ON BPZ_BESTPOSDETAILID = BPD_BESTPOSDETAILID" "~"
if joinKuerzel = ERABPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_FIRMENNR = ERA_FIRMENNR AND BPD_ERFASSUNGSKZ = ERA_BPD_ERFASSUNGSKZ AND BPD_BESTPRODERFASSUNGSNR = ERA_BPD_BESTPRODERFASSNR AND BPD_BESTPOSLFDNR = ERA_BPD_BESTPOSLFDNR AND BPD_ERFASSPOSDETAILLFDNR = ERA_BPD_ERFASSPOSDETLFDNR" "~"
if joinKuerzel = BPDERA
searchResult := searchResult "LEFT JOIN WWS.ERA_ERFASSRABATT ON ERA_FIRMENNR = BPD_FIRMENNR AND ERA_BPD_ERFASSUNGSKZ = BPD_ERFASSUNGSKZ AND ERA_BPD_BESTPRODERFASSNR = BPD_BESTPRODERFASSUNGSNR AND ERA_BPD_BESTPOSLFDNR = BPD_BESTPOSLFDNR AND ERA_BPD_ERFASSPOSDETLFDNR = BPD_ERFASSPOSDETAILLFDNR" "~"
if joinKuerzel = WABBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_FIRMENNR = WAB_BPD_FIRMENNR AND BPD_ERFASSUNGSKZ = WAB_BPD_ERFASSUNGSKZ AND BPD_BESTPRODERFASSUNGSNR = WAB_BPD_BESTPRODERFASSNR AND BPD_BESTPOSLFDNR = WAB_BPD_BESTPOSLFDNR AND BPD_ERFASSPOSDETAILLFDNR = WAB_BPD_ERFASSPOSDETLFDNR" "~"
if joinKuerzel = BPDWAB
searchResult := searchResult "LEFT JOIN WWS.WAB_WARENEINGANGBUCH ON WAB_BPD_FIRMENNR = BPD_FIRMENNR AND WAB_BPD_ERFASSUNGSKZ = BPD_ERFASSUNGSKZ AND WAB_BPD_BESTPRODERFASSNR = BPD_BESTPRODERFASSUNGSNR AND WAB_BPD_BESTPOSLFDNR = BPD_BESTPOSLFDNR AND WAB_BPD_ERFASSPOSDETLFDNR = BPD_ERFASSPOSDETAILLFDNR" "~"
if joinKuerzel = DISBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_FIRMENNR = DIS_FIRMENNR AND BPK_ERFASSUNGSKZ = DIS_BPD_ERFASSUNGSKZ AND BPK_BESTPRODERFASSUNGSNR = DIS_BPD_BESTPRODERFASSNR" "~"
if joinKuerzel = BPKDIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_FIRMENNR = BPK_FIRMENNR AND DIS_BPD_ERFASSUNGSKZ = BPK_ERFASSUNGSKZ AND DIS_BPD_BESTPRODERFASSNR = BPK_BESTPRODERFASSUNGSNR" "~"
if joinKuerzel = BPKBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_FIRMENNR = BPK_AUFBESTPR_FIRMENNR AND BPK_ERFASSUNGSKZ = BPK_AUFBESTPR_ERFASSUNGSKZ AND BPK_BESTPRODERFASSUNGSNR = BPK_AUFBESTPR_ERFASSUNGSNR" "~"
if joinKuerzel = BPKBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_AUFBESTPR_FIRMENNR = BPK_FIRMENNR AND BPK_AUFBESTPR_ERFASSUNGSKZ = BPK_ERFASSUNGSKZ AND BPK_AUFBESTPR_ERFASSUNGSNR = BPK_BESTPRODERFASSUNGSNR" "~"
if joinKuerzel = BPDBPZ
searchResult := searchResult "LEFT JOIN WWS.BPZ_BESTELLPOSZUORDNUNG ON BPZ_BESTPOSDETAILID = BPD_BESTPOSDETAILID" "~"
if joinKuerzel = BPZBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_BESTPOSDETAILID = BPZ_BESTPOSDETAILID" "~"
if joinKuerzel = FBRBRA
searchResult := searchResult "LEFT JOIN BUERO.BRA_BRANCHE ON BRA_LANDNR = FBR_LANDNR AND BRA_BRANCHENNR = FBR_BRANCHENNR" "~"
if joinKuerzel = BRAFBR
searchResult := searchResult "LEFT JOIN BUERO.FBR_FIRMENBRANCHEZU ON FBR_LANDNR = BRA_LANDNR AND FBR_BRANCHENNR = BRA_BRANCHENNR" "~"
if joinKuerzel = BRVBRH
searchResult := searchResult "LEFT JOIN FIBU.BRH_BANKRVKOPF ON BRH_FIRMENNR = BRV_BRH_FIRMENNR AND BRH_FIBUNR = BRV_BRH_FIBUNR AND BRH_BRHID = BRV_BRHID" "~"
if joinKuerzel = BRHBRV
searchResult := searchResult "LEFT JOIN FIBU.BRV_BANKRUECKVERBUCHUNG ON BRV_BRH_FIRMENNR = BRH_FIRMENNR AND BRV_BRH_FIBUNR = BRH_FIBUNR AND BRV_BRHID = BRH_BRHID" "~"
if joinKuerzel = EKOBRK
searchResult := searchResult "LEFT JOIN HRS.BRK_BEARBEITERKZ ON BRK_BEARBKZID = EKO_BEARBKZID AND BRK_DATENBESTANDNR = EKO_DATENBESTANDNR" "~"
if joinKuerzel = BRKEKO
searchResult := searchResult "LEFT JOIN HRS.EKO_ELDAKONTROLLE ON EKO_BEARBKZID = BRK_BEARBKZID AND EKO_DATENBESTANDNR = BRK_DATENBESTANDNR" "~"
if joinKuerzel = LENBRK
searchResult := searchResult "LEFT JOIN BRK_BEARBEITERKZ.BRK_BEARBEITERKZ ON BRK_BEARBKZID = LEN_BRK_BEARBKZID AND BRK_DATENBESTANDNR = LEN_DBS_DATENBESTANDNR" "~"
if joinKuerzel = BRKLEN
searchResult := searchResult "LEFT JOIN BUERO.LEN_LOHNEINSTELLUNGEN ON LEN_BRK_BEARBKZID = BRK_BEARBKZID AND LEN_DBS_DATENBESTANDNR = BRK_DATENBESTANDNR" "~"
if joinKuerzel = BRVBRV
searchResult := searchResult "LEFT JOIN FIBU.BRV_BANKRUECKVERBUCHUNG ON BRV_ZAHLUNGSID = BRV_REFERENZID" "~"
if joinKuerzel = BRVBRV
searchResult := searchResult "LEFT JOIN FIBU.BRV_BANKRUECKVERBUCHUNG ON BRV_REFERENZID = BRV_ZAHLUNGSID" "~"
if joinKuerzel = BGPBGS
searchResult := searchResult "LEFT JOIN HRS.BGS_BEITRAGSGRUPPE_SEQ ON BGS_BEITRAGSGRUPPEID = BGP_LEHRLING_PVKV_BGPID" "~"
if joinKuerzel = BGSBGP
searchResult := searchResult "LEFT JOIN HRS.BGP_BEITRAGSGRUPPE ON BGP_LEHRLING_PVKV_BGPID = BGS_BEITRAGSGRUPPEID" "~"
if joinKuerzel = BSPBSK
searchResult := searchResult "LEFT JOIN WWS.BSK_BONUSERMKOPF ON BSK_BONUSERMKOPFID = BSP_BONUSERMKOPFID" "~"
if joinKuerzel = BSKBSP
searchResult := searchResult "LEFT JOIN WWS.BSP_BONUSERMPOS ON BSP_BONUSERMKOPFID = BSK_BONUSERMKOPFID" "~"
if joinKuerzel = FIRBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = FIR_KOPFWORDFLG_BLOBLFDNR" "~"
if joinKuerzel = BLSFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_KOPFWORDFLG_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = FIRBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = FIR_FUSSWORD_BLOBLFDNR" "~"
if joinKuerzel = BLSFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FUSSWORD_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = RUZBMR
searchResult := searchResult "LEFT JOIN BMD.BMR_BMDROLLE ON BMR_BMDROLLENID = RUZ_BMDROLLENID" "~"
if joinKuerzel = BMRRUZ
searchResult := searchResult "LEFT JOIN BMD.RUZ_ROLLEUSERZU ON RUZ_BMDROLLENID = BMR_BMDROLLENID" "~"
if joinKuerzel = AMUBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = AMU_KONTROLLE_VON" "~"
if joinKuerzel = BMUAMU
searchResult := searchResult "LEFT JOIN BIL.AMU_ANLAGEGUT_MDE_ZU ON AMU_KONTROLLE_VON = BMU_BMDUSERID" "~"
if joinKuerzel = AULBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = AUL_UMSTELLUNGSUSER" "~"
if joinKuerzel = BMUAUL
searchResult := searchResult "LEFT JOIN WWS.AUL_ARTIKELUMSTELLLISTE ON AUL_UMSTELLUNGSUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FIPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FIP_BMDUSERID" "~"
if joinKuerzel = BMUFIP
searchResult := searchResult "LEFT JOIN BMD.FIP_FILTERPANEL ON FIP_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = HLBBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = HLB_LOCKUSERID" "~"
if joinKuerzel = BMUHLB
searchResult := searchResult "LEFT JOIN BMDENTW.HLB_HILFEBAUSTEIN ON HLB_LOCKUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = LAYBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAY_BMDUSERID" "~"
if joinKuerzel = BMULAY
searchResult := searchResult "LEFT JOIN BMD.LAY_LAYOUT ON LAY_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = PBABMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = PBA_PLAYBACKUSER" "~"
if joinKuerzel = BMUPBA
searchResult := searchResult "LEFT JOIN PRODUKTION.PBA_PLAN_BAS_ALT ON PBA_PLAYBACKUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PDWBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = PDW_BMDUSERID" "~"
if joinKuerzel = BMUPDW
searchResult := searchResult "LEFT JOIN BMDDEFAULT.PDW_PARAMWERT ON PDW_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = SPCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPC_BMDUSERID" "~"
if joinKuerzel = BMUSPC
searchResult := searchResult "LEFT JOIN BMD.SPC_SPELLCHECKERDICT ON SPC_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = WQUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WQU_USERID" "~"
if joinKuerzel = BMUWQU
searchResult := searchResult "LEFT JOIN BMD.WQU_WQI_BMU_ZU ON WQU_USERID = BMU_BMDUSERID" "~"
if joinKuerzel = ABWBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = ABW_BMDUSERID" "~"
if joinKuerzel = BMUABW
searchResult := searchResult "LEFT JOIN BUERO.ABW_BEWERBERAUFGABEN ON ABW_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = SCHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCH_BMDUSERID_EMAIL_OK" "~"
if joinKuerzel = BMUSCH
searchResult := searchResult "LEFT JOIN BMD.SCH_SCHEDULE ON SCH_BMDUSERID_EMAIL_OK = BMU_BMDUSERID" "~"
if joinKuerzel = FWFBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = FWF_JAB_GESPERRT_USER" "~"
if joinKuerzel = BMUFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_JAB_GESPERRT_USER = BMU_BMDUSERID" "~"
if joinKuerzel = DISBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_BONPOSID = DIS_BOP_BONPOSID" "~"
if joinKuerzel = BOPDIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_BOP_BONPOSID = BOP_BONPOSID" "~"
if joinKuerzel = WAPBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_FIRMENNR = WAP_BPD_FIRMENNR AND BPD_ERFASSUNGSKZ = WAP_BPD_ERFASSUNGSKZ AND BPD_BESTPRODERFASSUNGSNR = WAP_BPD_BESTPRODERFASSUNGSNR AND BPD_BESTPOSLFDNR = WAP_BPD_BESTPOSLFDNR AND BPD_ERFASSPOSDETAILLFDNR = WAP_BPD_ERFASSPOSDETAILLFDNR" "~"
if joinKuerzel = BPDWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_BPD_FIRMENNR = BPD_FIRMENNR AND WAP_BPD_ERFASSUNGSKZ = BPD_ERFASSUNGSKZ AND WAP_BPD_BESTPRODERFASSUNGSNR = BPD_BESTPRODERFASSUNGSNR AND WAP_BPD_BESTPOSLFDNR = BPD_BESTPOSLFDNR AND WAP_BPD_ERFASSPOSDETAILLFDNR = BPD_ERFASSPOSDETAILLFDNR" "~"
if joinKuerzel = EDZBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_FIRMENNR = EDZ_FIRMENNR AND BPK_ERFASSUNGSKZ = EDZ_BEST_ERFASSUNGSKZ AND BPK_BESTPRODERFASSUNGSNR = EDZ_BEST_BESTPRODERFASSNR" "~"
if joinKuerzel = BPKEDZ
searchResult := searchResult "LEFT JOIN WWS.EDZ_ERFASSDOKUMENTZU ON EDZ_FIRMENNR = BPK_FIRMENNR AND EDZ_BEST_ERFASSUNGSKZ = BPK_ERFASSUNGSKZ AND EDZ_BEST_BESTPRODERFASSNR = BPK_BESTPRODERFASSUNGSNR" "~"
if joinKuerzel = BLZBPL
searchResult := searchResult "LEFT JOIN BMDENTW.BPL_PACKAGES ON BPL_BPLID = BLZ_PACKAGEID" "~"
if joinKuerzel = BPLBLZ
searchResult := searchResult "LEFT JOIN BMDENTW.BLZ_PACKAGESZU ON BLZ_PACKAGEID = BPL_BPLID" "~"
if joinKuerzel = EDZBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_FIRMENNR = EDZ_FIRMENNR AND BPP_ERFASSUNGSKZ = EDZ_BEST_ERFASSUNGSKZ AND BPP_BESTPRODERFASSUNGSNR = EDZ_BEST_BESTPRODERFASSNR AND BPP_BESTPOSLFDNR = EDZ_BEST_BESTPOSLFDNR" "~"
if joinKuerzel = BPPEDZ
searchResult := searchResult "LEFT JOIN WWS.EDZ_ERFASSDOKUMENTZU ON EDZ_FIRMENNR = BPP_FIRMENNR AND EDZ_BEST_ERFASSUNGSKZ = BPP_ERFASSUNGSKZ AND EDZ_BEST_BESTPRODERFASSNR = BPP_BESTPRODERFASSUNGSNR AND EDZ_BEST_BESTPOSLFDNR = BPP_BESTPOSLFDNR" "~"
if joinKuerzel = PBRBRA
searchResult := searchResult "LEFT JOIN BUERO.BRA_BRANCHE ON BRA_LANDNR = PBR_LANDNR AND BRA_BRANCHENNR = PBR_BRANCHENNR" "~"
if joinKuerzel = BRAPBR
searchResult := searchResult "LEFT JOIN BUERO.PBR_PERSONBRANCHEZU ON PBR_LANDNR = BRA_LANDNR AND PBR_BRANCHENNR = BRA_BRANCHENNR" "~"
if joinKuerzel = LFIBRK
searchResult := searchResult "LEFT JOIN HRS.BRK_BEARBEITERKZ ON BRK_BEARBKZID = LFI_BEARBKZID AND BRK_DATENBESTANDNR = LFI_BRK_DATENBESTANDNR" "~"
if joinKuerzel = BRKLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_BEARBKZID = BRK_BEARBKZID AND LFI_BRK_DATENBESTANDNR = BRK_DATENBESTANDNR" "~"
if joinKuerzel = PLCBPD
searchResult := searchResult "LEFT JOIN BPD_BESTPOSDETAIL.BPD_BESTPOSDETAIL ON BPD_FIRMENNR = PLC_ERO_FIRMENNR AND BPD_ERFASSUNGSKZ = PLC_ERO_ERFASSUNGSKZ AND BPD_BESTPRODERFASSUNGSNR = PLC_BPD_BESTPRODERFNR AND BPD_BESTPOSLFDNR = PLC_BPD_BESTPOSLFDNR AND BPD_ERFASSPOSDETAILLFDNR = PLC_BPD_ERFPOSDETAILLFDNR" "~"
if joinKuerzel = BPDPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_ERO_FIRMENNR = BPD_FIRMENNR AND PLC_ERO_ERFASSUNGSKZ = BPD_ERFASSUNGSKZ AND PLC_BPD_BESTPRODERFNR = BPD_BESTPRODERFASSUNGSNR AND PLC_BPD_BESTPOSLFDNR = BPD_BESTPOSLFDNR AND PLC_BPD_ERFPOSDETAILLFDNR = BPD_ERFASSPOSDETAILLFDNR" "~"
if joinKuerzel = WRPBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_FIRMENNR = WRP_FIRMENNR AND BPD_ERFASSUNGSKZ = WRP_ERFASSUNGSKZ AND BPD_BESTPRODERFASSUNGSNR = WRP_BESTPRODERFASSUNGSNR AND BPD_BESTPOSLFDNR = WRP_BESTPOSLFDNR AND BPD_ERFASSPOSDETAILLFDNR = WRP_ERFASSPOSDETAILLFDNR" "~"
if joinKuerzel = BPDWRP
searchResult := searchResult "LEFT JOIN WWS.WRP_WWSANKAUFSRNGPOS ON WRP_FIRMENNR = BPD_FIRMENNR AND WRP_ERFASSUNGSKZ = BPD_ERFASSUNGSKZ AND WRP_BESTPRODERFASSUNGSNR = BPD_BESTPRODERFASSUNGSNR AND WRP_BESTPOSLFDNR = BPD_BESTPOSLFDNR AND WRP_ERFASSPOSDETAILLFDNR = BPD_ERFASSPOSDETAILLFDNR" "~"
if joinKuerzel = BPKBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_FIRMENNR = BPK_VONANFR_FIRMENNR AND BPK_ERFASSUNGSKZ = BPK_VONANFR_ERFASSUNGSKZ AND BPK_BESTPRODERFASSUNGSNR = BPK_VONANFR_ERFASSUNGSNR" "~"
if joinKuerzel = BPKBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_VONANFR_FIRMENNR = BPK_FIRMENNR AND BPK_VONANFR_ERFASSUNGSKZ = BPK_ERFASSUNGSKZ AND BPK_VONANFR_ERFASSUNGSNR = BPK_BESTPRODERFASSUNGSNR" "~"
if joinKuerzel = KOZBPK
searchResult := searchResult "LEFT JOIN BPK_BESTPRODKOPF.BPK_BESTPRODKOPF ON BPK_FIRMENNR = KOZ_ERFASS_FIRMENNR AND BPK_ERFASSUNGSKZ = KOZ_ERFASSUNGSKZ AND BPK_BESTPRODERFASSUNGSNR = KOZ_BESTELLNR" "~"
if joinKuerzel = BPKKOZ
searchResult := searchResult "LEFT JOIN PRODUKTION.KOZ_KOMMISSIONIERUNG_ZU ON KOZ_ERFASS_FIRMENNR = BPK_FIRMENNR AND KOZ_ERFASSUNGSKZ = BPK_ERFASSUNGSKZ AND KOZ_BESTELLNR = BPK_BESTPRODERFASSUNGSNR" "~"
if joinKuerzel = GKLBRA
searchResult := searchResult "LEFT JOIN BRA_BRANCHE.BRA_BRANCHE ON BRA_LANDNR = GKL_LANDNR AND BRA_BRANCHENNR = GKL_BRANCHENNR" "~"
if joinKuerzel = BRAGKL
searchResult := searchResult "LEFT JOIN BIL.GKL_GROESSENKLASSEN ON GKL_LANDNR = BRA_LANDNR AND GKL_BRANCHENNR = BRA_BRANCHENNR" "~"
if joinKuerzel = PERBRA
searchResult := searchResult "LEFT JOIN BUERO.BRA_BRANCHE ON BRA_LANDNR = PER_BRA_LANDNR AND BRA_BRANCHENNR = PER_BRANCHENNR" "~"
if joinKuerzel = BRAPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_BRA_LANDNR = BRA_LANDNR AND PER_BRANCHENNR = BRA_BRANCHENNR" "~"
if joinKuerzel = PERBRA
searchResult := searchResult "LEFT JOIN BUERO.BRA_BRANCHE ON BRA_LANDNR = PER_INDIV_BRA_LANDNR AND BRA_BRANCHENNR = PER_INDIV_BRANCHENNR" "~"
if joinKuerzel = BRAPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_INDIV_BRA_LANDNR = BRA_LANDNR AND PER_INDIV_BRANCHENNR = BRA_BRANCHENNR" "~"
if joinKuerzel = TPRBRC
searchResult := searchResult "LEFT JOIN BUERO.BRC_BEREICH ON BRC_BEREICHKZ = TPR_TAET_BEREICHKZ AND BRC_BEREICHLFDNR = TPR_TAET_BEREICHLFDNR" "~"
if joinKuerzel = BRCTPR
searchResult := searchResult "LEFT JOIN BUERO.TPR_TELEFONPROTOKOLL ON TPR_TAET_BEREICHKZ = BRC_BEREICHKZ AND TPR_TAET_BEREICHLFDNR = BRC_BEREICHLFDNR" "~"
if joinKuerzel = STTBRG
searchResult := searchResult "LEFT JOIN BRG_BERUFSGRUPPEN.BRG_BERUFSGRUPPEN ON BRG_FIRMENNR = STT_BRG_FIRMENNR AND BRG_BRGGRUPPENNR = STT_BRG_BRGGRUPPENNR" "~"
if joinKuerzel = BRGSTT
searchResult := searchResult "LEFT JOIN PRODUKTION.STT_STAKE_TYP ON STT_BRG_FIRMENNR = BRG_FIRMENNR AND STT_BRG_BRGGRUPPENNR = BRG_BRGGRUPPENNR" "~"
if joinKuerzel = SERBSE
searchResult := searchResult "LEFT JOIN BMD.BSE_BMDSESSION ON BSE_BMDSESSIONID = SER_BMDSESSIONID" "~"
if joinKuerzel = BSESER
searchResult := searchResult "LEFT JOIN BMD.SER_SQLEXECREQUEST ON SER_BMDSESSIONID = BSE_BMDSESSIONID" "~"
if joinKuerzel = SPZBSE
searchResult := searchResult "LEFT JOIN BMD.BSE_BMDSESSION ON BSE_BMDSESSIONID = SPZ_BMDSESSIONID" "~"
if joinKuerzel = BSESPZ
searchResult := searchResult "LEFT JOIN BMD.SPZ_SESSIONPAKETZU ON SPZ_BMDSESSIONID = BSE_BMDSESSIONID" "~"
if joinKuerzel = BPOBSG
searchResult := searchResult "LEFT JOIN BUERO.BSG_BESPRECHUNG ON BSG_BSGID = BPO_BSGID" "~"
if joinKuerzel = BSGBPO
searchResult := searchResult "LEFT JOIN BUERO.BPO_BESPRECHOBJEKTE ON BPO_BSGID = BSG_BSGID" "~"
if joinKuerzel = BSZBSG
searchResult := searchResult "LEFT JOIN BUERO.BSG_BESPRECHUNG ON BSG_BSGID = BSZ_AUFBSGID" "~"
if joinKuerzel = BSGBSZ
searchResult := searchResult "LEFT JOIN BUERO.BSZ_BESPRECHUNGZU ON BSZ_AUFBSGID = BSG_BSGID" "~"
if joinKuerzel = DSPBTF
searchResult := searchResult "LEFT JOIN BTF_BMDTABFELDBESCH.BTF_BMDTABFELDBESCH ON BTF_BMDCONSTNAME = DSP_BMDCONSTNAME" "~"
if joinKuerzel = BTFDSP
searchResult := searchResult "LEFT JOIN BUERO.DSP_DSGVO_PERSONALIEN ON DSP_BMDCONSTNAME = BTF_BMDCONSTNAME" "~"
if joinKuerzel = KFFBTF
searchResult := searchResult "LEFT JOIN BTF_BMDTABFELDBESCH.BTF_BMDTABFELDBESCH ON BTF_TABELLENNAME = KFF_TABELLENNAME_KOSTAMM AND BTF_FELDNAME = KFF_FELDNAME_KOSTAMM" "~"
if joinKuerzel = BTFKFF
searchResult := searchResult "LEFT JOIN BUERO.KFF_KORETYPFIND_FELDERZU ON KFF_TABELLENNAME_KOSTAMM = BTF_TABELLENNAME AND KFF_FELDNAME_KOSTAMM = BTF_FELDNAME" "~"
if joinKuerzel = BUCBUA
searchResult := searchResult "LEFT JOIN FIBU.BUA_BUCHUNGSART ON BUA_FIRMENNR = BUC_FIRMENNR AND BUA_FIBUNR = BUC_FIBUNR AND BUA_BUART = BUC_BUA_BUART" "~"
if joinKuerzel = BUABUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_FIRMENNR = BUA_FIRMENNR AND BUC_FIBUNR = BUA_FIBUNR AND BUC_BUA_BUART = BUA_BUART" "~"
if joinKuerzel = BVZBUA
searchResult := searchResult "LEFT JOIN FIBU.BUA_BUCHUNGSART ON BUA_FIRMENNR = BVZ_FIRMENNR AND BUA_FIBUNR = BVZ_FIBUNR AND BUA_BUART = BVZ_BUART" "~"
if joinKuerzel = BUABVZ
searchResult := searchResult "LEFT JOIN FIBU.BVZ_BUCHUNGSVAR_ARTZU ON BVZ_FIRMENNR = BUA_FIRMENNR AND BVZ_FIBUNR = BUA_FIBUNR AND BVZ_BUART = BUA_BUART" "~"
if joinKuerzel = PKPBUA
searchResult := searchResult "LEFT JOIN FIBU.BUA_BUCHUNGSART ON BUA_FIRMENNR = PKP_FIRMENNR AND BUA_FIBUNR = PKP_FIBUNR AND BUA_BUART = PKP_BUART" "~"
if joinKuerzel = BUAPKP
searchResult := searchResult "LEFT JOIN FIBU.PKP_BUDGETPERSKOSTPLAN ON PKP_FIRMENNR = BUA_FIRMENNR AND PKP_FIBUNR = BUA_FIBUNR AND PKP_BUART = BUA_BUART" "~"
if joinKuerzel = UDIBUA
searchResult := searchResult "LEFT JOIN BUA_BUCHUNGSART.BUA_BUCHUNGSART ON BUA_FIRMENNR = UDI_FIRMENNR AND BUA_FIBUNR = UDI_FIBUNR AND BUA_BUART = UDI_BUART" "~"
if joinKuerzel = BUAUDI
searchResult := searchResult "LEFT JOIN KORE.UDI_UMLAGEISTWERTE ON UDI_FIRMENNR = BUA_FIRMENNR AND UDI_FIBUNR = BUA_FIBUNR AND UDI_BUART = BUA_BUART" "~"
if joinKuerzel = LAJBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_BONPOSID = LAJ_BONPOSID" "~"
if joinKuerzel = BOPLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_BONPOSID = BOP_BONPOSID" "~"
if joinKuerzel = KAEBOZ
searchResult := searchResult "LEFT JOIN WWS.BOZ_BONZAHLUNG ON BOZ_BONZAHLUNGSID = KAE_BOZ_BONZAHLUNGSID" "~"
if joinKuerzel = BOZKAE
searchResult := searchResult "LEFT JOIN WWS.KAE_KASSENEINAUSGABEN ON KAE_BOZ_BONZAHLUNGSID = BOZ_BONZAHLUNGSID" "~"
if joinKuerzel = BPBBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_FIRMENNR = BPB_FIRMENNR AND BPD_ERFASSUNGSKZ = BPB_ERFASSUNGSKZ AND BPD_BESTPRODERFASSUNGSNR = BPB_BESTPRODERFASSUNGSNR AND BPD_BESTPOSLFDNR = BPB_BESTPOSLFDNR AND BPD_ERFASSPOSDETAILLFDNR = BPB_ERFASSPOSDETAILLFDNR" "~"
if joinKuerzel = BPDBPB
searchResult := searchResult "LEFT JOIN WWS.BPB_LIEFBESTAET ON BPB_FIRMENNR = BPD_FIRMENNR AND BPB_ERFASSUNGSKZ = BPD_ERFASSUNGSKZ AND BPB_BESTPRODERFASSUNGSNR = BPD_BESTPRODERFASSUNGSNR AND BPB_BESTPOSLFDNR = BPD_BESTPOSLFDNR AND BPB_ERFASSPOSDETAILLFDNR = BPD_ERFASSPOSDETAILLFDNR" "~"
if joinKuerzel = EPSBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_FIRMENNR = EPS_ERO_FIRMENNR AND BPD_ERFASSUNGSKZ = EPS_ERO_ERFASSUNGSKZ AND BPD_BESTPRODERFASSUNGSNR = EPS_BPD_BESTPRODERFNR AND BPD_BESTPOSLFDNR = EPS_BPD_BESTPOSLFDNR AND BPD_ERFASSPOSDETAILLFDNR = EPS_BPD_ERFPOSDETAILLFDNR" "~"
if joinKuerzel = BPDEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_ERO_FIRMENNR = BPD_FIRMENNR AND EPS_ERO_ERFASSUNGSKZ = BPD_ERFASSUNGSKZ AND EPS_BPD_BESTPRODERFNR = BPD_BESTPRODERFASSUNGSNR AND EPS_BPD_BESTPOSLFDNR = BPD_BESTPOSLFDNR AND EPS_BPD_ERFPOSDETAILLFDNR = BPD_ERFASSPOSDETAILLFDNR" "~"
if joinKuerzel = AUGBPK
searchResult := searchResult "LEFT JOIN BPK_BESTPRODKOPF.BPK_BESTPRODKOPF ON BPK_FIRMENNR = AUG_BPK_FIRMENNR AND BPK_ERFASSUNGSKZ = AUG_BPK_ERFASSUNGSKZ AND BPK_BESTPRODERFASSUNGSNR = AUG_BPK_ERFASSUNGSNR" "~"
if joinKuerzel = BPKAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_BPK_FIRMENNR = BPK_FIRMENNR AND AUG_BPK_ERFASSUNGSKZ = BPK_ERFASSUNGSKZ AND AUG_BPK_ERFASSUNGSNR = BPK_BESTPRODERFASSUNGSNR" "~"
if joinKuerzel = BRABRA
searchResult := searchResult "LEFT JOIN BUERO.BRA_BRANCHE ON BRA_LANDNR = BRA_INDIV_LANDNR AND BRA_BRANCHENNR = BRA_INDIV_BRANCHENNR" "~"
if joinKuerzel = BRABRA
searchResult := searchResult "LEFT JOIN BUERO.BRA_BRANCHE ON BRA_INDIV_LANDNR = BRA_LANDNR AND BRA_INDIV_BRANCHENNR = BRA_BRANCHENNR" "~"
if joinKuerzel = MITBRC
searchResult := searchResult "LEFT JOIN BUERO.BRC_BEREICH ON BRC_BEREICHKZ = MIT_BEREICHKZ AND BRC_BEREICHLFDNR = MIT_BEREICHLFDNR" "~"
if joinKuerzel = BRCMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_BEREICHKZ = BRC_BEREICHKZ AND MIT_BEREICHLFDNR = BRC_BEREICHLFDNR" "~"
if joinKuerzel = AGMBRG
searchResult := searchResult "LEFT JOIN BRG_BERUFSGRUPPEN.BRG_BERUFSGRUPPEN ON BRG_FIRMENNR = AGM_BRG_FIRMENNR AND BRG_BRGGRUPPENNR = AGM_BRGGRUPPENNR" "~"
if joinKuerzel = BRGAGM
searchResult := searchResult "LEFT JOIN PRODUKTION.AGM_AG_PERSON_ZU ON AGM_BRG_FIRMENNR = BRG_FIRMENNR AND AGM_BRGGRUPPENNR = BRG_BRGGRUPPENNR" "~"
if joinKuerzel = RAPBRG
searchResult := searchResult "LEFT JOIN BRG_BERUFSGRUPPEN.BRG_BERUFSGRUPPEN ON BRG_FIRMENNR = RAP_BRG_FIRMENNR AND BRG_BRGGRUPPENNR = RAP_BRG_BRGGRUPPENNR" "~"
if joinKuerzel = BRGRAP
searchResult := searchResult "LEFT JOIN WWS.RAP_RABATTTABPOS ON RAP_BRG_FIRMENNR = BRG_FIRMENNR AND RAP_BRG_BRGGRUPPENNR = BRG_BRGGRUPPENNR" "~"
if joinKuerzel = STHBRG
searchResult := searchResult "LEFT JOIN BRG_BERUFSGRUPPEN.BRG_BERUFSGRUPPEN ON BRG_FIRMENNR = STH_BRG_FIRMENNR AND BRG_BRGGRUPPENNR = STH_BRGGRUPPENNR" "~"
if joinKuerzel = BRGSTH
searchResult := searchResult "LEFT JOIN PRODUKTION.STH_STAKE ON STH_BRG_FIRMENNR = BRG_FIRMENNR AND STH_BRGGRUPPENNR = BRG_BRGGRUPPENNR" "~"
if joinKuerzel = WFNBRM
searchResult := searchResult "LEFT JOIN BUERO.BRM_BUEROMAKROS ON BRM_BUEROMAKROID = WFN_BEFORE_BUEROMAKROID" "~"
if joinKuerzel = BRMWFN
searchResult := searchResult "LEFT JOIN BUERO.WFN_WORKFLOWACTION ON WFN_BEFORE_BUEROMAKROID = BRM_BUEROMAKROID" "~"
if joinKuerzel = WFNBRM
searchResult := searchResult "LEFT JOIN BUERO.BRM_BUEROMAKROS ON BRM_BUEROMAKROID = WFN_ZUSTBEREICH_BMAKROID" "~"
if joinKuerzel = BRMWFN
searchResult := searchResult "LEFT JOIN BUERO.WFN_WORKFLOWACTION ON WFN_ZUSTBEREICH_BMAKROID = BRM_BUEROMAKROID" "~"
if joinKuerzel = BRSBRV
searchResult := searchResult "LEFT JOIN FIBU.BRV_BANKRUECKVERBUCHUNG ON BRV_ZAHLUNGSID = BRS_BRV_ZAHLUNGSID" "~"
if joinKuerzel = BRVBRS
searchResult := searchResult "LEFT JOIN FIBU.BRS_BANKRUECKAUFTEILSATZ ON BRS_BRV_ZAHLUNGSID = BRV_ZAHLUNGSID" "~"
if joinKuerzel = RNPBSD
searchResult := searchResult "LEFT JOIN WWS.BSD_BONUSERMDETAIL ON BSD_BONUSERMDETID = RNP_BONUSERMDETID" "~"
if joinKuerzel = BSDRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_BONUSERMDETID = BSD_BONUSERMDETID" "~"
if joinKuerzel = BSZBSG
searchResult := searchResult "LEFT JOIN BUERO.BSG_BESPRECHUNG ON BSG_BSGID = BSZ_VONBSGID" "~"
if joinKuerzel = BSGBSZ
searchResult := searchResult "LEFT JOIN BUERO.BSZ_BESPRECHUNGZU ON BSZ_VONBSGID = BSG_BSGID" "~"
if joinKuerzel = BGPBGS
searchResult := searchResult "LEFT JOIN HRS.BGS_BEITRAGSGRUPPE_SEQ ON BGS_BEITRAGSGRUPPEID = BGP_LEHRLING_PVKVAV_BGPID" "~"
if joinKuerzel = BGSBGP
searchResult := searchResult "LEFT JOIN HRS.BGP_BEITRAGSGRUPPE ON BGP_LEHRLING_PVKVAV_BGPID = BGS_BEITRAGSGRUPPEID" "~"
if joinKuerzel = BSBBSK
searchResult := searchResult "LEFT JOIN WWS.BSK_BONUSERMKOPF ON BSK_BONUSERMKOPFID = BSB_BONUSERMKOPFID" "~"
if joinKuerzel = BSKBSB
searchResult := searchResult "LEFT JOIN WWS.BSB_BONUSERMPERSONENZU ON BSB_BONUSERMKOPFID = BSK_BONUSERMKOPFID" "~"
if joinKuerzel = DFEBTF
searchResult := searchResult "LEFT JOIN BTF_BMDTABFELDBESCH.BTF_BMDTABFELDBESCH ON BTF_TABELLENNAME = DFE_TABELLENNAME AND BTF_FELDNAME = DFE_FELDNAME" "~"
if joinKuerzel = BTFDFE
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFE_DFELDBESCHPOS ON DFE_TABELLENNAME = BTF_TABELLENNAME AND DFE_FELDNAME = BTF_FELDNAME" "~"
if joinKuerzel = TZMBTM
searchResult := searchResult "LEFT JOIN BUERO.BTM_BTERMINAL ON BTM_FIRMENNR = TZM_FIRMENNR_MASTER AND BTM_TERMINALNR = TZM_TERMINALNR_MASTER" "~"
if joinKuerzel = BTMTZM
searchResult := searchResult "LEFT JOIN BUERO.TZM_TERMINALZUORDMASTER ON TZM_FIRMENNR_MASTER = BTM_FIRMENNR AND TZM_TERMINALNR_MASTER = BTM_TERMINALNR" "~"
if joinKuerzel = DOZBMG
searchResult := searchResult "LEFT JOIN BMD.BMG_BMDGRUPPE ON BMG_BMDGRUPPENID = DOZ_BMDGRUPPENID" "~"
if joinKuerzel = BMGDOZ
searchResult := searchResult "LEFT JOIN BMD.DOZ_DATAOWNERZU ON DOZ_BMDGRUPPENID = BMG_BMDGRUPPENID" "~"
if joinKuerzel = RGZBMR
searchResult := searchResult "LEFT JOIN BMD.BMR_BMDROLLE ON BMR_BMDROLLENID = RGZ_BMDROLLENID" "~"
if joinKuerzel = BMRRGZ
searchResult := searchResult "LEFT JOIN BMD.RGZ_ROLLEGRUPPEZU ON RGZ_BMDROLLENID = BMR_BMDROLLENID" "~"
if joinKuerzel = BEMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BEM_SENDUSERID" "~"
if joinKuerzel = BMUBEM
searchResult := searchResult "LEFT JOIN BMD.BEM_BMDEMAIL ON BEM_SENDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = HLDBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = HLD_LOCKUSERID" "~"
if joinKuerzel = BMUHLD
searchResult := searchResult "LEFT JOIN BMDENTW.HLD_HILFEDOKUMENT ON HLD_LOCKUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = LEHBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = LEH_BMDUSERID" "~"
if joinKuerzel = BMULEH
searchResult := searchResult "LEFT JOIN HRS.LEH_LOHNEINSTELLUNG_HIST ON LEH_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = LSPBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = LSP_USERID" "~"
if joinKuerzel = BMULSP
searchResult := searchResult "LEFT JOIN HRS.LSP_LOHNSTAPEL ON LSP_USERID = BMU_BMDUSERID" "~"
if joinKuerzel = OVEBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = OVE_BMDUSERID" "~"
if joinKuerzel = BMUOVE
searchResult := searchResult "LEFT JOIN LOHN.OVE_OF_VERANTWORTLICHER ON OVE_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = SCMBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = SCM_VERANTW_BMDUSERID" "~"
if joinKuerzel = BMUSCM
searchResult := searchResult "LEFT JOIN BMDENTW.SCM_SCRMAIN ON SCM_VERANTW_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = WERBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = WER_ERSPERREUSER" "~"
if joinKuerzel = BMUWER
searchResult := searchResult "LEFT JOIN WWS.WER_WWSERKONTROLLEKOPF ON WER_ERSPERREUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZUZBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = ZUZ_BMDUSERID" "~"
if joinKuerzel = BMUZUZ
searchResult := searchResult "LEFT JOIN BUERO.ZUZ_ZEITUMBUCHZUORD ON ZUZ_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = SCMBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = SCM_BESITZER_BMDUSERID" "~"
if joinKuerzel = BMUSCM
searchResult := searchResult "LEFT JOIN BMDENTW.SCM_SCRMAIN ON SCM_BESITZER_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = VPEBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = VPE_ERMITTLUNGSUSERID" "~"
if joinKuerzel = BMUVPE
searchResult := searchResult "LEFT JOIN WWS.VPE_VPI_ERHOEHUNG ON VPE_ERMITTLUNGSUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = SCHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCH_BMDUSERID_SMS_OK" "~"
if joinKuerzel = BMUSCH
searchResult := searchResult "LEFT JOIN BMD.SCH_SCHEDULE ON SCH_BMDUSERID_SMS_OK = BMU_BMDUSERID" "~"
if joinKuerzel = GSPBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_BONKOPFID = GSP_BON_BONKOPFID" "~"
if joinKuerzel = BONGSP
searchResult := searchResult "LEFT JOIN WWS.GSP_GUTSCHEINPOSITION ON GSP_BON_BONKOPFID = BON_BONKOPFID" "~"
if joinKuerzel = LPOBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_BONPOSID = LPO_BONPOSID" "~"
if joinKuerzel = BOPLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_BONPOSID = BOP_BONPOSID" "~"
if joinKuerzel = KBEBOZ
searchResult := searchResult "LEFT JOIN WWS.BOZ_BONZAHLUNG ON BOZ_BONZAHLUNGSID = KBE_BOZ_BONZAHLUNGSID" "~"
if joinKuerzel = BOZKBE
searchResult := searchResult "LEFT JOIN WWS.KBE_KASSEBUCHEINZELNW ON KBE_BOZ_BONZAHLUNGSID = BOZ_BONZAHLUNGSID" "~"
if joinKuerzel = WEEBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_FIRMENNR = WEE_FIRMENNR AND BPK_ERFASSUNGSKZ = WEE_ERFASSUNGSKZ AND BPK_BESTPRODERFASSUNGSNR = WEE_BESTPRODERFASSUNGSNR" "~"
if joinKuerzel = BPKWEE
searchResult := searchResult "LEFT JOIN WWS.WEE_ERKONT_BESTELL_ZUORD ON WEE_FIRMENNR = BPK_FIRMENNR AND WEE_ERFASSUNGSKZ = BPK_ERFASSUNGSKZ AND WEE_BESTPRODERFASSUNGSNR = BPK_BESTPRODERFASSUNGSNR" "~"
if joinKuerzel = DISBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_FIRMENNR = DIS_FIRMENNR AND BPP_ERFASSUNGSKZ = DIS_BPD_ERFASSUNGSKZ AND BPP_BESTPRODERFASSUNGSNR = DIS_BPD_BESTPRODERFASSNR AND BPP_BESTPOSLFDNR = DIS_BPD_BESTPOSLFDNR" "~"
if joinKuerzel = BPPDIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_FIRMENNR = BPP_FIRMENNR AND DIS_BPD_ERFASSUNGSKZ = BPP_ERFASSUNGSKZ AND DIS_BPD_BESTPRODERFASSNR = BPP_BESTPRODERFASSUNGSNR AND DIS_BPD_BESTPOSLFDNR = BPP_BESTPOSLFDNR" "~"
if joinKuerzel = LEIBRG
searchResult := searchResult "LEFT JOIN BRG_BERUFSGRUPPEN.BRG_BERUFSGRUPPEN ON BRG_FIRMENNR = LEI_BRG_FIRMENNR AND BRG_BRGGRUPPENNR = LEI_BRGGRUPPENNR" "~"
if joinKuerzel = BRGLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_BRG_FIRMENNR = BRG_FIRMENNR AND LEI_BRGGRUPPENNR = BRG_BRGGRUPPENNR" "~"
if joinKuerzel = CAWBRM
searchResult := searchResult "LEFT JOIN BUERO.BRM_BUEROMAKROS ON BRM_BUEROMAKROID = CAW_BUEROMAKROID_TER" "~"
if joinKuerzel = BRMCAW
searchResult := searchResult "LEFT JOIN BUERO.CAW_CRMABWESENHEIT ON CAW_BUEROMAKROID_TER = BRM_BUEROMAKROID" "~"
if joinKuerzel = WFNBRM
searchResult := searchResult "LEFT JOIN BUERO.BRM_BUEROMAKROS ON BRM_BUEROMAKROID = WFN_AFTER_BUEROMAKROID" "~"
if joinKuerzel = BRMWFN
searchResult := searchResult "LEFT JOIN BUERO.WFN_WORKFLOWACTION ON WFN_AFTER_BUEROMAKROID = BRM_BUEROMAKROID" "~"
if joinKuerzel = WFNBRM
searchResult := searchResult "LEFT JOIN BUERO.BRM_BUEROMAKROS ON BRM_BUEROMAKROID = WFN_FUNKTION_BUEROMAKROID" "~"
if joinKuerzel = BRMWFN
searchResult := searchResult "LEFT JOIN BUERO.WFN_WORKFLOWACTION ON WFN_FUNKTION_BUEROMAKROID = BRM_BUEROMAKROID" "~"
if joinKuerzel = WFNBRM
searchResult := searchResult "LEFT JOIN BUERO.BRM_BUEROMAKROS ON BRM_BUEROMAKROID = WFN_AUGERZ_BUEROMAKROID" "~"
if joinKuerzel = BRMWFN
searchResult := searchResult "LEFT JOIN BUERO.WFN_WORKFLOWACTION ON WFN_AUGERZ_BUEROMAKROID = BRM_BUEROMAKROID" "~"
if joinKuerzel = SLBBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = SLB_CHRONIK" "~"
if joinKuerzel = BLSSLB
searchResult := searchResult "LEFT JOIN BIL.SLB_SALDENBESTAETIGUNG ON SLB_CHRONIK = BLS_BLOBLFDNR" "~"
if joinKuerzel = SZZBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = SZZ_CHRONIK" "~"
if joinKuerzel = BLSSZZ
searchResult := searchResult "LEFT JOIN PRODUKTION.SZZ_STAKE_ZU ON SZZ_CHRONIK = BLS_BLOBLFDNR" "~"
if joinKuerzel = TCTBLS
searchResult := searchResult "LEFT JOIN BMD.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = TCT_DREHBUCHBLOBLFDNR" "~"
if joinKuerzel = BLSTCT
searchResult := searchResult "LEFT JOIN BMD.TCT_TCTESTCASE ON TCT_DREHBUCHBLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = BDLBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = BDL_DATABLOBLFDNR" "~"
if joinKuerzel = BLSBDL
searchResult := searchResult "LEFT JOIN BUERO.BDL_BUERODELETE ON BDL_DATABLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = KOWBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = KOW_KVINFO_BLOBLFDNR" "~"
if joinKuerzel = BLSKOW
searchResult := searchResult "LEFT JOIN HRS.KOW_KOLLEKTIVVERTRAGWERTE ON KOW_KVINFO_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = KSLBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = KSL_INTERESSENT_BLOBLFDNR" "~"
if joinKuerzel = BLSKSL
searchResult := searchResult "LEFT JOIN BUERO.KSL_KONTAKTSYNCEINST ON KSL_INTERESSENT_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = DRSBMA
searchResult := searchResult "LEFT JOIN BMD.BMA_BMDMAILACCOUNT ON BMA_ACCOUNTLFDNR = DRS_MAILACCOUNTLFDNR" "~"
if joinKuerzel = BMADRS
searchResult := searchResult "LEFT JOIN BMD.DRS_DRUCKEREINSTELLUNG ON DRS_MAILACCOUNTLFDNR = BMA_ACCOUNTLFDNR" "~"
if joinKuerzel = BGDBMI
searchResult := searchResult "LEFT JOIN HRS.BMI_BUDGETMITARBEITER ON BMI_BMIID = BGD_BUDGETMITARBID" "~"
if joinKuerzel = BMIBGD
searchResult := searchResult "LEFT JOIN HRS.BGD_BUDGETGLDNGBAUMDATA ON BGD_BUDGETMITARBID = BMI_BMIID" "~"
if joinKuerzel = BPABMO
searchResult := searchResult "LEFT JOIN FIBU.BMO_BUDGETMODELL ON BMO_FIRMENNR = BPA_FIRMENNR AND BMO_FIBUNR = BPA_FIBUNR AND BMO_BUDGETMODELLNR = BPA_BUDGETMODELLNR" "~"
if joinKuerzel = BMOBPA
searchResult := searchResult "LEFT JOIN FIBU.BPA_BUDGETPLANBILAUFTLG ON BPA_FIRMENNR = BMO_FIRMENNR AND BPA_FIBUNR = BMO_FIBUNR AND BPA_BUDGETMODELLNR = BMO_BUDGETMODELLNR" "~"
if joinKuerzel = AUCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AUC_BMDUSERID" "~"
if joinKuerzel = BMUAUC
searchResult := searchResult "LEFT JOIN BMD.AUC_AUSWERTUNGSCENTER ON AUC_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = BUKBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = BUK_BEARBEITERUSER" "~"
if joinKuerzel = BMUBUK
searchResult := searchResult "LEFT JOIN FIBU.BUK_BUCHUNGSKREIS ON BUK_BEARBEITERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ITMBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = ITM_BMDUSERID" "~"
if joinKuerzel = BMUITM
searchResult := searchResult "LEFT JOIN BUERO.ITM_INTERNMAIL ON ITM_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = LEIBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = LEI_AUTORISIERUNG_USERID" "~"
if joinKuerzel = BMULEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_AUTORISIERUNG_USERID = BMU_BMDUSERID" "~"
if joinKuerzel = MUZBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = MUZ_BMDUSERID" "~"
if joinKuerzel = BMUMUZ
searchResult := searchResult "LEFT JOIN BUERO.MUZ_MITARBUSERZU ON MUZ_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = OUNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OUN_BMDUSERID" "~"
if joinKuerzel = BMUOUN
searchResult := searchResult "LEFT JOIN BMD.OUN_OUTLOOKBARTABNODES ON OUN_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = QMKBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = QMK_SONDERFREIGABE_USERID" "~"
if joinKuerzel = BMUQMK
searchResult := searchResult "LEFT JOIN PRODUKTION.QMK_QM_KOPF ON QMK_SONDERFREIGABE_USERID = BMU_BMDUSERID" "~"
if joinKuerzel = TAVBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = TAV_GENEHM_BMDUSERID" "~"
if joinKuerzel = BMUTAV
searchResult := searchResult "LEFT JOIN BUERO.TAV_VERTEILUNG ON TAV_GENEHM_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = WBFBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = WBF_BMDUSERID" "~"
if joinKuerzel = BMUWBF
searchResult := searchResult "LEFT JOIN WWS.WBF_WWSBERICHTFILTER ON WBF_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = ZUUBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = ZUU_BMDUSERID" "~"
if joinKuerzel = BMUZUU
searchResult := searchResult "LEFT JOIN BUERO.ZUU_ZEITBEZARTUMBUCHUSER ON ZUU_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = KAJBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_BONKOPFID = KAJ_BONKOPFID" "~"
if joinKuerzel = BONKAJ
searchResult := searchResult "LEFT JOIN WWS.KAJ_KASSENJOURNAL ON KAJ_BONKOPFID = BON_BONKOPFID" "~"
if joinKuerzel = BOPBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_BONPOSID = BOP_HAUPT_BONPOSID" "~"
if joinKuerzel = BOPBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_HAUPT_BONPOSID = BOP_BONPOSID" "~"
if joinKuerzel = KBEBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_BONPOSID = KBE_BOP_BONPOSID" "~"
if joinKuerzel = BOPKBE
searchResult := searchResult "LEFT JOIN WWS.KBE_KASSEBUCHEINZELNW ON KBE_BOP_BONPOSID = BOP_BONPOSID" "~"
if joinKuerzel = BOPBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_BONPOSID = BOP_FOLGE_BONPOSID" "~"
if joinKuerzel = BOPBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_FOLGE_BONPOSID = BOP_BONPOSID" "~"
if joinKuerzel = KEPBOZ
searchResult := searchResult "LEFT JOIN WWS.BOZ_BONZAHLUNG ON BOZ_BONZAHLUNGSID = KEP_BOZ_BONZAHLUNGSID" "~"
if joinKuerzel = BOZKEP
searchResult := searchResult "LEFT JOIN WWS.KEP_KASSENERFASSUNGSPROT ON KEP_BOZ_BONZAHLUNGSID = BOZ_BONZAHLUNGSID" "~"
if joinKuerzel = OPZBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_BUCHUNGID = OPZ_BUCHUNGID_RNG AND BUC_FIRMENNR = OPZ_FIRMENNR AND BUC_FIBUNR = OPZ_FIBUNR" "~"
if joinKuerzel = BUCOPZ
searchResult := searchResult "LEFT JOIN FIBU.OPZ_OPZUORDNUNG ON OPZ_BUCHUNGID_RNG = BUC_BUCHUNGID AND OPZ_FIRMENNR = BUC_FIRMENNR AND OPZ_FIBUNR = BUC_FIBUNR" "~"
if joinKuerzel = BUCBUK
searchResult := searchResult "LEFT JOIN FIBU.BUK_BUCHUNGSKREIS ON BUK_FIRMENNR = BUC_FIRMENNR AND BUK_FIBUNR = BUC_FIBUNR AND BUK_BUCHKREISNR = BUC_BUCHKREISNR" "~"
if joinKuerzel = BUKBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_FIRMENNR = BUK_FIRMENNR AND BUC_FIBUNR = BUK_FIBUNR AND BUC_BUCHKREISNR = BUK_BUCHKREISNR" "~"
if joinKuerzel = FZUBUV
searchResult := searchResult "LEFT JOIN FIBU.BUV_BUCHUNGSVARIANTE ON BUV_FIRMENNR = FZU_ZUGEORDNETE_FIRMENNR AND BUV_FIBUNR = FZU_ZUGEORDNETE_FIBUNR AND BUV_BUVARIANTE = FZU_BUVARIANTE" "~"
if joinKuerzel = BUVFZU
searchResult := searchResult "LEFT JOIN FIBU.FZU_FIBUFIBUZU ON FZU_ZUGEORDNETE_FIRMENNR = BUV_FIRMENNR AND FZU_ZUGEORDNETE_FIBUNR = BUV_FIBUNR AND FZU_BUVARIANTE = BUV_BUVARIANTE" "~"
if joinKuerzel = BTGBWV
searchResult := searchResult "LEFT JOIN BUERO.BWV_BEWERTUNGVERSION ON BWV_FIRMENNR = BTG_BWV_FIRMENNR AND BWV_BWVERSIONNR = BTG_BWVERSIONNR" "~"
if joinKuerzel = BWVBTG
searchResult := searchResult "LEFT JOIN BUERO.BTG_BEWERTUNGTAG ON BTG_BWV_FIRMENNR = BWV_FIRMENNR AND BTG_BWVERSIONNR = BWV_BWVERSIONNR" "~"
if joinKuerzel = GSMBZB
searchResult := searchResult "LEFT JOIN PRODUKTION.BZB_BAZZEITBUCHUNG ON BZB_ZEITBUCHUNG_ID = GSM_ZEITBUCHUNG_ID" "~"
if joinKuerzel = BZBGSM
searchResult := searchResult "LEFT JOIN PRODUKTION.GSM_GUTSCHLECHTMENGE ON GSM_ZEITBUCHUNG_ID = BZB_ZEITBUCHUNG_ID" "~"
if joinKuerzel = SUKBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = SUK_BNZEILEID" "~"
if joinKuerzel = BZSSUK
searchResult := searchResult "LEFT JOIN HRS.SUK_SUM_KOE ON SUK_BNZEILEID = BZS_BNZEILEID" "~"
if joinKuerzel = DEIBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = DEI_NB" "~"
if joinKuerzel = BZSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_NB = BZS_BNZEILEID" "~"
if joinKuerzel = LKOCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_CHARGEIDNR = LKO_CHARGEIDNR" "~"
if joinKuerzel = CHGLKO
searchResult := searchResult "LEFT JOIN WWS.LKO_LAGERKONTO ON LKO_CHARGEIDNR = CHG_CHARGEIDNR" "~"
if joinKuerzel = SNRCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_CHARGEIDNR = SNR_CHARGEIDNR" "~"
if joinKuerzel = CHGSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_CHARGEIDNR = CHG_CHARGEIDNR" "~"
if joinKuerzel = TLECHG
searchResult := searchResult "LEFT JOIN CHG_CHARGE.CHG_CHARGE ON CHG_CHARGEIDNR = TLE_CHARGEIDNR" "~"
if joinKuerzel = CHGTLE
searchResult := searchResult "LEFT JOIN PRODUKTION.TLE_BT_LOGISTIK_EMPFANGEN ON TLE_CHARGEIDNR = CHG_CHARGEIDNR" "~"
if joinKuerzel = TLTCHG
searchResult := searchResult "LEFT JOIN CHG_CHARGE.CHG_CHARGE ON CHG_CHARGEIDNR = TLT_CHARGEIDNR" "~"
if joinKuerzel = CHGTLT
searchResult := searchResult "LEFT JOIN PRODUKTION.TLT_BT_LOGISTIK_TRANSPORT ON TLT_CHARGEIDNR = CHG_CHARGEIDNR" "~"
if joinKuerzel = BPKCKQ
searchResult := searchResult "LEFT JOIN CKQ_CHECKLISTEID.CKQ_CHECKLISTEID ON CKQ_CKLID = BPK_CKLID" "~"
if joinKuerzel = CKQBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_CKLID = CKQ_CKLID" "~"
if joinKuerzel = EFKCKQ
searchResult := searchResult "LEFT JOIN CKQ_CHECKLISTEID.CKQ_CHECKLISTEID ON CKQ_CKLID = EFK_CKLID" "~"
if joinKuerzel = CKQEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_CKLID = CKQ_CKLID" "~"
if joinKuerzel = PCLCLD
searchResult := searchResult "LEFT JOIN CLD_CLOADACCOUNT.CLD_CLOADACCOUNT ON CLD_CLOUDID = PCL_CLOUDID" "~"
if joinKuerzel = CLDPCL
searchResult := searchResult "LEFT JOIN BUERO.PCL_PERSONCLOUDZU ON PCL_CLOUDID = CLD_CLOUDID" "~"
if joinKuerzel = DBTDAK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DAK_DATTRIBUTKATEGORIE ON DAK_ATTRKATLFDNR = DBT_UNTER_ATTRKATLFDNR" "~"
if joinKuerzel = DAKDBT
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DBT_DBMDTABFELDBESCH ON DBT_UNTER_ATTRKATLFDNR = DAK_ATTRKATLFDNR" "~"
if joinKuerzel = PICDAS
searchResult := searchResult "LEFT JOIN DAS_DOCSARCHIVSTAMM.DAS_DOCSARCHIVSTAMM ON DAS_ARCHIVNR = PIC_ARCHIVNR" "~"
if joinKuerzel = DASPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_ARCHIVNR = DAS_ARCHIVNR" "~"
if joinKuerzel = DNZDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = DNZ_DBSNR" "~"
if joinKuerzel = DBSDNZ
searchResult := searchResult "LEFT JOIN HRS.DNZ_DIVNICHTLEISTUNGSZEIT ON DNZ_DBSNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = EGZDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = EGZ_DATENBESTAND" "~"
if joinKuerzel = DBSEGZ
searchResult := searchResult "LEFT JOIN HRS.EGZ_ERGAENZUNG ON EGZ_DATENBESTAND = DBS_DATENBESTANDNR" "~"
if joinKuerzel = FIRDBS
searchResult := searchResult "LEFT JOIN BUERO.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = FIR_DATENBESTANDNR" "~"
if joinKuerzel = DBSFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = KRMDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = KRM_DATENBESTANDNR" "~"
if joinKuerzel = DBSKRM
searchResult := searchResult "LEFT JOIN HRS.KRM_KONTENREFERENZTAB ON KRM_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = LGRDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = LGR_DBSNR" "~"
if joinKuerzel = DBSLGR
searchResult := searchResult "LEFT JOIN HRS.LGR_LOHNARTGRUPPE ON LGR_DBSNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = TBRDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = TBR_DATENBESTAND" "~"
if joinKuerzel = DBSTBR
searchResult := searchResult "LEFT JOIN HRS.TBR_BESCHAEFTIGTENGRUPPE ON TBR_DATENBESTAND = DBS_DATENBESTANDNR" "~"
if joinKuerzel = TPTDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = TPT_DATENBESTAND" "~"
if joinKuerzel = DBSTPT
searchResult := searchResult "LEFT JOIN HRS.TPT_TARIFPROZENT ON TPT_DATENBESTAND = DBS_DATENBESTANDNR" "~"
if joinKuerzel = BSDBSP
searchResult := searchResult "LEFT JOIN WWS.BSP_BONUSERMPOS ON BSP_BONUSERMPOSID = BSD_BONUSERMPOSID" "~"
if joinKuerzel = BSPBSD
searchResult := searchResult "LEFT JOIN WWS.BSD_BONUSERMDETAIL ON BSD_BONUSERMPOSID = BSP_BONUSERMPOSID" "~"
if joinKuerzel = BTSBTH
searchResult := searchResult "LEFT JOIN BUERO.BTH_BTERMHERSTELLER ON BTH_FIRMENNR = BTS_BTH_FIRMENNR AND BTH_TERMINALHERSTELLER = BTS_TERMINALHERSTELLER" "~"
if joinKuerzel = BTHBTS
searchResult := searchResult "LEFT JOIN BUERO.BTS_BTERMSATZART ON BTS_BTH_FIRMENNR = BTH_FIRMENNR AND BTS_TERMINALHERSTELLER = BTH_TERMINALHERSTELLER" "~"
if joinKuerzel = ANBBUA
searchResult := searchResult "LEFT JOIN BUA_BUCHUNGSART.BUA_BUCHUNGSART ON BUA_FIRMENNR = ANB_FIBUFIRMENNR AND BUA_FIBUNR = ANB_FIBUNR AND BUA_BUART = ANB_BUART" "~"
if joinKuerzel = BUAANB
searchResult := searchResult "LEFT JOIN BIL.ANB_ANBUBUCHBELEGZU ON ANB_FIBUFIRMENNR = BUA_FIRMENNR AND ANB_FIBUNR = BUA_FIBUNR AND ANB_BUART = BUA_BUART" "~"
if joinKuerzel = BRVBUA
searchResult := searchResult "LEFT JOIN FIBU.BUA_BUCHUNGSART ON BUA_FIRMENNR = BRV_BRH_FIRMENNR AND BUA_FIBUNR = BRV_BRH_FIBUNR AND BUA_BUART = BRV_BUART" "~"
if joinKuerzel = BUABRV
searchResult := searchResult "LEFT JOIN FIBU.BRV_BANKRUECKVERBUCHUNG ON BRV_BRH_FIRMENNR = BUA_FIRMENNR AND BRV_BRH_FIBUNR = BUA_FIBUNR AND BRV_BUART = BUA_BUART" "~"
if joinKuerzel = BUDBUA
searchResult := searchResult "LEFT JOIN FIBU.BUA_BUCHUNGSART ON BUA_FIRMENNR = BUD_FIRMENNR AND BUA_FIBUNR = BUD_FIBUNR AND BUA_BUART = BUD_BUART" "~"
if joinKuerzel = BUABUD
searchResult := searchResult "LEFT JOIN FIBU.BUD_BUDGET ON BUD_FIRMENNR = BUA_FIRMENNR AND BUD_FIBUNR = BUA_FIBUNR AND BUD_BUART = BUA_BUART" "~"
if joinKuerzel = BZUBUA
searchResult := searchResult "LEFT JOIN FIBU.BUA_BUCHUNGSART ON BUA_FIRMENNR = BZU_ZUGEORDNETE_FIRMENNR AND BUA_FIBUNR = BZU_ZUGEORDNETE_FIBUNR AND BUA_BUART = BZU_ZUGEORDNETE_BUART" "~"
if joinKuerzel = BUABZU
searchResult := searchResult "LEFT JOIN FIBU.BZU_BUARTKONSZU ON BZU_ZUGEORDNETE_FIRMENNR = BUA_FIRMENNR AND BZU_ZUGEORDNETE_FIBUNR = BUA_FIBUNR AND BZU_ZUGEORDNETE_BUART = BUA_BUART" "~"
if joinKuerzel = SADBUA
searchResult := searchResult "LEFT JOIN FIBU.BUA_BUCHUNGSART ON BUA_FIRMENNR = SAD_FIRMENNR AND BUA_FIBUNR = SAD_FIBUNR AND BUA_BUART = SAD_BUART" "~"
if joinKuerzel = BUASAD
searchResult := searchResult "LEFT JOIN FIBU.SAD_SALDEN ON SAD_FIRMENNR = BUA_FIRMENNR AND SAD_FIBUNR = BUA_FIBUNR AND SAD_BUART = BUA_BUART" "~"
if joinKuerzel = UMPBUA
searchResult := searchResult "LEFT JOIN BUA_BUCHUNGSART.BUA_BUCHUNGSART ON BUA_FIRMENNR = UMP_FIRMENNR AND BUA_FIBUNR = UMP_FIBUNR AND BUA_BUART = UMP_BUART" "~"
if joinKuerzel = BUAUMP
searchResult := searchResult "LEFT JOIN KORE.UMP_UMLAGEPLANMENGEN ON UMP_FIRMENNR = BUA_FIRMENNR AND UMP_FIBUNR = BUA_FIBUNR AND UMP_BUART = BUA_BUART" "~"
if joinKuerzel = ASDBUC
searchResult := searchResult "LEFT JOIN BUC_BUCHUNG.BUC_BUCHUNG ON BUC_BUCHUNGID = ASD_BID_BUCHUNGID3 AND BUC_FIRMENNR = ASD_FWF_FIRMENNR2 AND BUC_FIBUNR = ASD_BUC_FIBUNR" "~"
if joinKuerzel = BUCASD
searchResult := searchResult "LEFT JOIN ZX_GELOESCHT.ASD_ABSTATTUNGSDETAIL ON ASD_BID_BUCHUNGID3 = BUC_BUCHUNGID AND ASD_FWF_FIRMENNR2 = BUC_FIRMENNR AND ASD_BUC_FIBUNR = BUC_FIBUNR" "~"
if joinKuerzel = KOEBUC
searchResult := searchResult "LEFT JOIN BUC_BUCHUNG.BUC_BUCHUNG ON BUC_BUCHUNGID = KOE_BUCHUNGID AND BUC_FIRMENNR = KOE_FWF_FIRMENNR2 AND BUC_FIBUNR = KOE_BUC_FIBUNR" "~"
if joinKuerzel = BUCKOE
searchResult := searchResult "LEFT JOIN ZX_GELOESCHT.KOE_KOSTENERFASSUNG ON KOE_BUCHUNGID = BUC_BUCHUNGID AND KOE_FWF_FIRMENNR2 = BUC_FIRMENNR AND KOE_BUC_FIBUNR = BUC_FIBUNR" "~"
if joinKuerzel = ASDBUC
searchResult := searchResult "LEFT JOIN BUC_BUCHUNG.BUC_BUCHUNG ON BUC_BUCHUNGID = ASD_BUC_BID_BUCHUNGID3 AND BUC_FIRMENNR = ASD_BUC_FWF_FIRMENNR2 AND BUC_FIBUNR = ASD_BUC_FIBUNR2" "~"
if joinKuerzel = BUCASD
searchResult := searchResult "LEFT JOIN ZX_GELOESCHT.ASD_ABSTATTUNGSDETAIL ON ASD_BUC_BID_BUCHUNGID3 = BUC_BUCHUNGID AND ASD_BUC_FWF_FIRMENNR2 = BUC_FIRMENNR AND ASD_BUC_FIBUNR2 = BUC_FIBUNR" "~"
if joinKuerzel = POZBWB
searchResult := searchResult "LEFT JOIN BUERO.BWB_BEWERBUNG ON BWB_BEWERBID = POZ_BWB_BEWERBID" "~"
if joinKuerzel = BWBPOZ
searchResult := searchResult "LEFT JOIN BUERO.POZ_PISWORKFLOWZU ON POZ_BWB_BEWERBID = BWB_BEWERBID" "~"
if joinKuerzel = BWTBWP
searchResult := searchResult "LEFT JOIN BUERO.BWP_BENUTZERWERTEPARAM ON BWP_BWERTPAKETNR = BWT_BWERTPAKETNR AND BWP_BWERTKATEGORIENR = BWT_BWERTKATEGORIENR AND BWP_PARAMLFDNR = BWT_PARAMLFDNR" "~"
if joinKuerzel = BWPBWT
searchResult := searchResult "LEFT JOIN BUERO.BWT_BENUTZERWERT ON BWT_BWERTPAKETNR = BWP_BWERTPAKETNR AND BWT_BWERTKATEGORIENR = BWP_BWERTKATEGORIENR AND BWT_PARAMLFDNR = BWP_PARAMLFDNR" "~"
if joinKuerzel = BGPBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = BGP_BNZEILEID" "~"
if joinKuerzel = BZSBGP
searchResult := searchResult "LEFT JOIN HRS.BGP_BEITRAGSGRUPPE ON BGP_BNZEILEID = BZS_BNZEILEID" "~"
if joinKuerzel = DEIBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = DEI_VB_KV" "~"
if joinKuerzel = BZSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_VB_KV = BZS_BNZEILEID" "~"
if joinKuerzel = DEIBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = DEI_VB_AL2" "~"
if joinKuerzel = BZSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_VB_AL2 = BZS_BNZEILEID" "~"
if joinKuerzel = DEIBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = DEI_VN_DGA" "~"
if joinKuerzel = BZSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_VN_DGA = BZS_BNZEILEID" "~"
if joinKuerzel = DEIBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = DEI_VB_AL10" "~"
if joinKuerzel = BZSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_VB_AL10 = BZS_BNZEILEID" "~"
if joinKuerzel = DEIBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = DEI_A13" "~"
if joinKuerzel = BZSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_A13 = BZS_BNZEILEID" "~"
if joinKuerzel = CSZCAI
searchResult := searchResult "LEFT JOIN BMD.CAI_CACHEINVALIDIERUNG ON CAI_CADID = CSZ_CADID AND CAI_KEY = CSZ_KEY" "~"
if joinKuerzel = CAICSZ
searchResult := searchResult "LEFT JOIN BMD.CSZ_CAI_SESSIONZU ON CSZ_CADID = CAI_CADID AND CSZ_KEY = CAI_KEY" "~"
if joinKuerzel = CCACCE
searchResult := searchResult "LEFT JOIN BIL.CCE_CBCENTITY ON CCE_CCEID = CCA_CCEID" "~"
if joinKuerzel = CCECCA
searchResult := searchResult "LEFT JOIN BIL.CCA_CBCADDINFO ON CCA_CCEID = CCE_CCEID" "~"
if joinKuerzel = BKEBRV
searchResult := searchResult "LEFT JOIN FIBU.BRV_BANKRUECKVERBUCHUNG ON BRV_ZAHLUNGSID = BKE_BRV_ZAHLUNGSID" "~"
if joinKuerzel = BRVBKE
searchResult := searchResult "LEFT JOIN FIBU.BKE_BANKRVKOREERF ON BKE_BRV_ZAHLUNGSID = BRV_ZAHLUNGSID" "~"
if joinKuerzel = BOPBSD
searchResult := searchResult "LEFT JOIN WWS.BSD_BONUSERMDETAIL ON BSD_BONUSERMDETID = BOP_BONUSERMDETID_PLAN" "~"
if joinKuerzel = BSDBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_BONUSERMDETID_PLAN = BSD_BONUSERMDETID" "~"
if joinKuerzel = BLCBSE
searchResult := searchResult "LEFT JOIN BMD.BSE_BMDSESSION ON BSE_BMDSESSIONID = BLC_BMDSESSIONID" "~"
if joinKuerzel = BSEBLC
searchResult := searchResult "LEFT JOIN BMD.BLC_BMDLOCK ON BLC_BMDSESSIONID = BSE_BMDSESSIONID" "~"
if joinKuerzel = CSZBSE
searchResult := searchResult "LEFT JOIN BMD.BSE_BMDSESSION ON BSE_BMDSESSIONID = CSZ_BMDSESSIONID" "~"
if joinKuerzel = BSECSZ
searchResult := searchResult "LEFT JOIN BMD.CSZ_CAI_SESSIONZU ON CSZ_BMDSESSIONID = BSE_BMDSESSIONID" "~"
if joinKuerzel = BPTBSG
searchResult := searchResult "LEFT JOIN BUERO.BSG_BESPRECHUNG ON BSG_BSGID = BPT_BSGID" "~"
if joinKuerzel = BSGBPT
searchResult := searchResult "LEFT JOIN BUERO.BPT_BESPRECHPROTOKOLL ON BPT_BSGID = BSG_BSGID" "~"
if joinKuerzel = TERBSG
searchResult := searchResult "LEFT JOIN BUERO.BSG_BESPRECHUNG ON BSG_BSGID = TER_BSGID" "~"
if joinKuerzel = BSGTER
searchResult := searchResult "LEFT JOIN BUERO.TER_TERMIN ON TER_BSGID = BSG_BSGID" "~"
if joinKuerzel = BSDBSK
searchResult := searchResult "LEFT JOIN WWS.BSK_BONUSERMKOPF ON BSK_BONUSERMKOPFID = BSD_BONUSERMKOPFID" "~"
if joinKuerzel = BSKBSD
searchResult := searchResult "LEFT JOIN WWS.BSD_BONUSERMDETAIL ON BSD_BONUSERMKOPFID = BSK_BONUSERMKOPFID" "~"
if joinKuerzel = BPABUA
searchResult := searchResult "LEFT JOIN FIBU.BUA_BUCHUNGSART ON BUA_FIRMENNR = BPA_FIRMENNR AND BUA_FIBUNR = BPA_FIBUNR AND BUA_BUART = BPA_BUART" "~"
if joinKuerzel = BUABPA
searchResult := searchResult "LEFT JOIN FIBU.BPA_BUDGETPLANBILAUFTLG ON BPA_FIRMENNR = BUA_FIRMENNR AND BPA_FIBUNR = BUA_FIBUNR AND BPA_BUART = BUA_BUART" "~"
if joinKuerzel = SAKBUA
searchResult := searchResult "LEFT JOIN FIBU.BUA_BUCHUNGSART ON BUA_FIRMENNR = SAK_FIRMENNR AND BUA_FIBUNR = SAK_FIBUNR AND BUA_BUART = SAK_ABGRENZUNG_BUART" "~"
if joinKuerzel = BUASAK
searchResult := searchResult "LEFT JOIN FIBU.SAK_SACHKONTO ON SAK_FIRMENNR = BUA_FIRMENNR AND SAK_FIBUNR = BUA_FIBUNR AND SAK_ABGRENZUNG_BUART = BUA_BUART" "~"
if joinKuerzel = KZEBUA
searchResult := searchResult "LEFT JOIN BUA_BUCHUNGSART.BUA_BUCHUNGSART ON BUA_FIRMENNR = KZE_FWK_FIRMENNR AND BUA_FIBUNR = KZE_FIBUNR AND BUA_BUART = KZE_ZIEL_BUART" "~"
if joinKuerzel = BUAKZE
searchResult := searchResult "LEFT JOIN KORE.KZE_ZUSCHLAGEINGABE ON KZE_FWK_FIRMENNR = BUA_FIRMENNR AND KZE_FIBUNR = BUA_FIBUNR AND KZE_ZIEL_BUART = BUA_BUART" "~"
if joinKuerzel = AFBBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_BUCHUNGID = AFB_BUCHUNGID AND BUC_FIRMENNR = AFB_FIRMENNR AND BUC_FIBUNR = AFB_FIBUNR" "~"
if joinKuerzel = BUCAFB
searchResult := searchResult "LEFT JOIN FIBU.AFB_ANBUFIB ON AFB_BUCHUNGID = BUC_BUCHUNGID AND AFB_FIRMENNR = BUC_FIRMENNR AND AFB_FIBUNR = BUC_FIBUNR" "~"
if joinKuerzel = LEBBUC
searchResult := searchResult "LEFT JOIN BUC_BUCHUNG.BUC_BUCHUNG ON BUC_BUCHUNGID = LEB_BUC_BUCHUNGID AND BUC_FIRMENNR = LEB_BUC_FIRMENNR AND BUC_FIBUNR = LEB_BUC_FIBUNR" "~"
if joinKuerzel = BUCLEB
searchResult := searchResult "LEFT JOIN BUERO.LEB_LEBENSLAUF ON LEB_BUC_BUCHUNGID = BUC_BUCHUNGID AND LEB_BUC_FIRMENNR = BUC_FIRMENNR AND LEB_BUC_FIBUNR = BUC_FIBUNR" "~"
if joinKuerzel = ERABVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_BESTELLVORSCHLAGID = ERA_BESTELLVORSCHLAGID" "~"
if joinKuerzel = BVOERA
searchResult := searchResult "LEFT JOIN WWS.ERA_ERFASSRABATT ON ERA_BESTELLVORSCHLAGID = BVO_BESTELLVORSCHLAGID" "~"
if joinKuerzel = BGZBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = BGZ_BNZEILEID" "~"
if joinKuerzel = BZSBGZ
searchResult := searchResult "LEFT JOIN HRS.BGZ_BNZEILE ON BGZ_BNZEILEID = BZS_BNZEILEID" "~"
if joinKuerzel = DEIBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = DEI_ISEG" "~"
if joinKuerzel = BZSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_ISEG = BZS_BNZEILEID" "~"
if joinKuerzel = DEIBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = DEI_VB_PV" "~"
if joinKuerzel = BZSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_VB_PV = BZS_BNZEILEID" "~"
if joinKuerzel = DEIBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = DEI_VB_AL33" "~"
if joinKuerzel = BZSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_VB_AL33 = BZS_BNZEILEID" "~"
if joinKuerzel = DEIBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = DEI_NEUFOEGN69" "~"
if joinKuerzel = BZSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_NEUFOEGN69 = BZS_BNZEILEID" "~"
if joinKuerzel = DEIBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = DEI_KU" "~"
if joinKuerzel = BZSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_KU = BZS_BNZEILEID" "~"
if joinKuerzel = ZUSZBE
searchResult := searchResult "LEFT JOIN BUERO.ZBE_ZUSTAENDIGKEITBEREICH ON ZBE_ZUSTBEREICHLFDNR = ZUS_ZUSTBEREICHLFDNR" "~"
if joinKuerzel = ZBEZUS
searchResult := searchResult "LEFT JOIN BUERO.ZUS_ZUSTAENDIGKEIT ON ZUS_ZUSTBEREICHLFDNR = ZBE_ZUSTBEREICHLFDNR" "~"
if joinKuerzel = ELVCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_CHARGEIDNR = ELV_CHARGEIDNR" "~"
if joinKuerzel = CHGELV
searchResult := searchResult "LEFT JOIN WWS.ELV_ERFASSPOSLIEFERVOR ON ELV_CHARGEIDNR = CHG_CHARGEIDNR" "~"
if joinKuerzel = KAPCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_CHARGEIDNR = KAP_CHG_CHARGEIDNR" "~"
if joinKuerzel = CHGKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_CHG_CHARGEIDNR = CHG_CHARGEIDNR" "~"
if joinKuerzel = LUACHG
searchResult := searchResult "LEFT JOIN CHG_CHARGE.CHG_CHARGE ON CHG_CHARGEIDNR = LUA_CHARGEIDNR" "~"
if joinKuerzel = CHGLUA
searchResult := searchResult "LEFT JOIN PRODUKTION.LUA_LAGER_UMS_ANGEFORDERT ON LUA_CHARGEIDNR = CHG_CHARGEIDNR" "~"
if joinKuerzel = BUSBUA
searchResult := searchResult "LEFT JOIN FIBU.BUA_BUCHUNGSART ON BUA_FIRMENNR = BUS_FIRMENNR AND BUA_FIBUNR = BUS_FIBUNR AND BUA_BUART = BUS_BUART" "~"
if joinKuerzel = BUABUS
searchResult := searchResult "LEFT JOIN FIBU.BUS_BUCHUNGSSYMBOL ON BUS_FIRMENNR = BUA_FIRMENNR AND BUS_FIBUNR = BUA_FIBUNR AND BUS_BUART = BUA_BUART" "~"
if joinKuerzel = FWFBUA
searchResult := searchResult "LEFT JOIN FIBU.BUA_BUCHUNGSART ON BUA_FIRMENNR = FWF_KORE_FIRMENNR AND BUA_FIBUNR = FWF_KORE_FIBUNR AND BUA_BUART = FWF_KORE_BUART" "~"
if joinKuerzel = BUAFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_KORE_FIRMENNR = BUA_FIRMENNR AND FWF_KORE_FIBUNR = BUA_FIBUNR AND FWF_KORE_BUART = BUA_BUART" "~"
if joinKuerzel = KDIBUA
searchResult := searchResult "LEFT JOIN BUA_BUCHUNGSART.BUA_BUCHUNGSART ON BUA_FIRMENNR = KDI_FIRMENNR AND BUA_FIBUNR = KDI_FIBUNR AND BUA_BUART = KDI_BUART" "~"
if joinKuerzel = BUAKDI
searchResult := searchResult "LEFT JOIN KORE.KDI_ISTWERTE ON KDI_FIRMENNR = BUA_FIRMENNR AND KDI_FIBUNR = BUA_FIBUNR AND KDI_BUART = BUA_BUART" "~"
if joinKuerzel = KZEBUA
searchResult := searchResult "LEFT JOIN BUA_BUCHUNGSART.BUA_BUCHUNGSART ON BUA_FIRMENNR = KZE_FWK_FIRMENNR AND BUA_FIBUNR = KZE_FIBUNR AND BUA_BUART = KZE_BUART" "~"
if joinKuerzel = BUAKZE
searchResult := searchResult "LEFT JOIN KORE.KZE_ZUSCHLAGEINGABE ON KZE_FWK_FIRMENNR = BUA_FIRMENNR AND KZE_FIBUNR = BUA_FIBUNR AND KZE_BUART = BUA_BUART" "~"
if joinKuerzel = PMXBUA
searchResult := searchResult "LEFT JOIN BUA_BUCHUNGSART.BUA_BUCHUNGSART ON BUA_FIRMENNR = PMX_FIRMENNR AND BUA_FIBUNR = PMX_FIBUNR AND BUA_BUART = PMX_BUART" "~"
if joinKuerzel = BUAPMX
searchResult := searchResult "LEFT JOIN KORE.PMX_PLANMENGENEXT ON PMX_FIRMENNR = BUA_FIRMENNR AND PMX_FIBUNR = BUA_FIBUNR AND PMX_BUART = BUA_BUART" "~"
if joinKuerzel = VEBBUA
searchResult := searchResult "LEFT JOIN FIBU.BUA_BUCHUNGSART ON BUA_FIRMENNR = VEB_FIRMENNR AND BUA_FIBUNR = VEB_FIBUNR AND BUA_BUART = VEB_BUA_BUART" "~"
if joinKuerzel = BUAVEB
searchResult := searchResult "LEFT JOIN FIBU.VEB_VORERFASSBUCHUNG ON VEB_FIRMENNR = BUA_FIRMENNR AND VEB_FIBUNR = BUA_FIBUNR AND VEB_BUA_BUART = BUA_BUART" "~"
if joinKuerzel = ZVDBUC
searchResult := searchResult "LEFT JOIN BUC_BUCHUNG.BUC_BUCHUNG ON BUC_BUCHUNGID = ZVD_BUC_BUCHUNGID AND BUC_FIRMENNR = ZVD_BUC_FIRMENNR AND BUC_FIBUNR = ZVD_BUC_FIBUNR" "~"
if joinKuerzel = BUCZVD
searchResult := searchResult "LEFT JOIN ZX_GELOESCHT.ZVD_ZAHLUNGSDETAILS ON ZVD_BUC_BUCHUNGID = BUC_BUCHUNGID AND ZVD_BUC_FIRMENNR = BUC_FIRMENNR AND ZVD_BUC_FIBUNR = BUC_FIBUNR" "~"
if joinKuerzel = AFBBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_BUCHUNGID = AFB_BUCHUNGID_SKONTO AND BUC_FIRMENNR = AFB_FIRMENNR_SKONTO AND BUC_FIBUNR = AFB_FIBUNR_SKONTO" "~"
if joinKuerzel = BUCAFB
searchResult := searchResult "LEFT JOIN FIBU.AFB_ANBUFIB ON AFB_BUCHUNGID_SKONTO = BUC_BUCHUNGID AND AFB_FIRMENNR_SKONTO = BUC_FIRMENNR AND AFB_FIBUNR_SKONTO = BUC_FIBUNR" "~"
if joinKuerzel = BUSBUV
searchResult := searchResult "LEFT JOIN FIBU.BUV_BUCHUNGSVARIANTE ON BUV_FIRMENNR = BUS_FIRMENNR AND BUV_FIBUNR = BUS_FIBUNR AND BUV_BUVARIANTE = BUS_BUVARIANTE" "~"
if joinKuerzel = BUVBUS
searchResult := searchResult "LEFT JOIN FIBU.BUS_BUCHUNGSSYMBOL ON BUS_FIRMENNR = BUV_FIRMENNR AND BUS_FIBUNR = BUV_FIBUNR AND BUS_BUVARIANTE = BUV_BUVARIANTE" "~"
if joinKuerzel = AWKBUV
searchResult := searchResult "LEFT JOIN BUV_BUCHUNGSVARIANTE.BUV_BUCHUNGSVARIANTE ON BUV_FIRMENNR = AWK_FIRMENNR AND BUV_FIBUNR = AWK_FIBUNR AND BUV_BUVARIANTE = AWK_BUV_BUVARIANTE_VOR" "~"
if joinKuerzel = BUVAWK
searchResult := searchResult "LEFT JOIN BIL.AWK_AUSWERTUNGKOPF ON AWK_FIRMENNR = BUV_FIRMENNR AND AWK_FIBUNR = BUV_FIBUNR AND AWK_BUV_BUVARIANTE_VOR = BUV_BUVARIANTE" "~"
if joinKuerzel = BWMBEW
searchResult := searchResult "LEFT JOIN BUERO.BEW_BEWERTUNGSMODELL ON BEW_FIRMENNR = BWM_BEW_FIRMENNR AND BEW_BEWMODELLNR = BWM_BEWMODELLNR" "~"
if joinKuerzel = BEWBWM
searchResult := searchResult "LEFT JOIN BUERO.BWM_BEWERTUNGMONAT ON BWM_BEW_FIRMENNR = BEW_FIRMENNR AND BWM_BEWMODELLNR = BEW_BEWMODELLNR" "~"
if joinKuerzel = BWPBWK
searchResult := searchResult "LEFT JOIN BUERO.BWK_BENUTZERWERTKATEGORIE ON BWK_BWERTPAKETNR = BWP_BWERTPAKETNR AND BWK_BWERTKATEGORIENR = BWP_BWERTKATEGORIENR" "~"
if joinKuerzel = BWKBWP
searchResult := searchResult "LEFT JOIN BUERO.BWP_BENUTZERWERTEPARAM ON BWP_BWERTPAKETNR = BWK_BWERTPAKETNR AND BWP_BWERTKATEGORIENR = BWK_BWERTKATEGORIENR" "~"
if joinKuerzel = DEIBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = DEI_VB_AL20" "~"
if joinKuerzel = BZSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_VB_AL20 = BZS_BNZEILEID" "~"
if joinKuerzel = CAICAD
searchResult := searchResult "LEFT JOIN CAD_CACHEDEFINITION.CAD_CACHEDEFINITION ON CAD_CADID = CAI_CADID" "~"
if joinKuerzel = CADCAI
searchResult := searchResult "LEFT JOIN BMD.CAI_CACHEINVALIDIERUNG ON CAI_CADID = CAD_CADID" "~"
if joinKuerzel = BVOCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_CHARGEIDNR = BVO_CHG_CHARGEIDNR" "~"
if joinKuerzel = CHGBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_CHG_CHARGEIDNR = CHG_CHARGEIDNR" "~"
if joinKuerzel = TLACHG
searchResult := searchResult "LEFT JOIN CHG_CHARGE.CHG_CHARGE ON CHG_CHARGEIDNR = TLA_CHARGEIDNR" "~"
if joinKuerzel = CHGTLA
searchResult := searchResult "LEFT JOIN PRODUKTION.TLA_BT_LOGISTIK_ANGEFORDERT ON TLA_CHARGEIDNR = CHG_CHARGEIDNR" "~"
if joinKuerzel = ARLDAZ
searchResult := searchResult "LEFT JOIN DAZ_DATANORM_ARTIKELZU.DAZ_DATANORM_ARTIKELZU ON DAZ_DAZID = ARL_DAZ_DAZID" "~"
if joinKuerzel = DAZARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_DAZ_DAZID = DAZ_DAZID" "~"
if joinKuerzel = LNGDBP
searchResult := searchResult "LEFT JOIN DBP_DBERECHTPAKET.DBP_DBERECHTPAKET ON DBP_BERECHTPAKETNR = LNG_BERECHTPAKETNR" "~"
if joinKuerzel = DBPLNG
searchResult := searchResult "LEFT JOIN BMDENTW.LNG_LANGUAGE ON LNG_BERECHTPAKETNR = DBP_BERECHTPAKETNR" "~"
if joinKuerzel = BEGDBP
searchResult := searchResult "LEFT JOIN DBP_DBERECHTPAKET.DBP_DBERECHTPAKET ON DBP_BERECHTPAKETNR = BEG_BERECHTPAKETNR" "~"
if joinKuerzel = DBPBEG
searchResult := searchResult "LEFT JOIN BMD.BEG_BERECHTIGUNG ON BEG_BERECHTPAKETNR = DBP_BERECHTPAKETNR" "~"
if joinKuerzel = DGWDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = DGW_DBSNR" "~"
if joinKuerzel = DBSDGW
searchResult := searchResult "LEFT JOIN HRS.DGW_DBGESETZWERTE ON DGW_DBSNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = BFOBRV
searchResult := searchResult "LEFT JOIN FIBU.BRV_BANKRUECKVERBUCHUNG ON BRV_ZAHLUNGSID = BFO_BRV_ZAHLUNGSID" "~"
if joinKuerzel = BRVBFO
searchResult := searchResult "LEFT JOIN FIBU.BFO_BANKFIBUOPZU ON BFO_BRV_ZAHLUNGSID = BRV_ZAHLUNGSID" "~"
if joinKuerzel = BOPBSD
searchResult := searchResult "LEFT JOIN WWS.BSD_BONUSERMDETAIL ON BSD_BONUSERMDETID = BOP_BONUSERMDETID" "~"
if joinKuerzel = BSDBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_BONUSERMDETID = BSD_BONUSERMDETID" "~"
if joinKuerzel = KJOBSG
searchResult := searchResult "LEFT JOIN BUERO.BSG_BESPRECHUNG ON BSG_BSGID = KJO_BSGID" "~"
if joinKuerzel = BSGKJO
searchResult := searchResult "LEFT JOIN BUERO.KJO_KONTAKTJOURNAL ON KJO_BSGID = BSG_BSGID" "~"
if joinKuerzel = BOABSK
searchResult := searchResult "LEFT JOIN WWS.BSK_BONUSERMKOPF ON BSK_BONUSERMKOPFID = BOA_BONUSERMKOPFID" "~"
if joinKuerzel = BSKBOA
searchResult := searchResult "LEFT JOIN WWS.BOA_BONUSAUSSCHLUSS ON BOA_BONUSERMKOPFID = BSK_BONUSERMKOPFID" "~"
if joinKuerzel = BAPBUA
searchResult := searchResult "LEFT JOIN FIBU.BUA_BUCHUNGSART ON BUA_FIRMENNR = BAP_FIRMENNR AND BUA_FIBUNR = BAP_FIBUNR AND BUA_BUART = BAP_BUART" "~"
if joinKuerzel = BUABAP
searchResult := searchResult "LEFT JOIN FIBU.BAP_BUDGETARTIKELPLAN ON BAP_FIRMENNR = BUA_FIRMENNR AND BAP_FIBUNR = BUA_FIBUNR AND BAP_BUART = BUA_BUART" "~"
if joinKuerzel = BWEBUA
searchResult := searchResult "LEFT JOIN FIBU.BUA_BUCHUNGSART ON BUA_FIRMENNR = BWE_FIRMENNR AND BUA_FIBUNR = BWE_FIBUNR AND BUA_BUART = BWE_BUART" "~"
if joinKuerzel = BUABWE
searchResult := searchResult "LEFT JOIN FIBU.BWE_BUDGETWARENEINKPLAN ON BWE_FIRMENNR = BUA_FIRMENNR AND BWE_FIBUNR = BUA_FIBUNR AND BWE_BUART = BUA_BUART" "~"
if joinKuerzel = KMIBUA
searchResult := searchResult "LEFT JOIN BUA_BUCHUNGSART.BUA_BUCHUNGSART ON BUA_FIRMENNR = KMI_FIRMENNR AND BUA_FIBUNR = KMI_FIBUNR AND BUA_BUART = KMI_BUART" "~"
if joinKuerzel = BUAKMI
searchResult := searchResult "LEFT JOIN KORE.KMI_ISTMENGEN ON KMI_FIRMENNR = BUA_FIRMENNR AND KMI_FIBUNR = BUA_FIBUNR AND KMI_BUART = BUA_BUART" "~"
if joinKuerzel = YBUBUA
searchResult := searchResult "LEFT JOIN FIBU.BUA_BUCHUNGSART ON BUA_FIRMENNR = YBU_FIRMENNR AND BUA_FIBUNR = YBU_FIBUNR AND BUA_BUART = YBU_BUA_BUART" "~"
if joinKuerzel = BUAYBU
searchResult := searchResult "LEFT JOIN FIBU.YBU_AUTOBUCHUNG ON YBU_FIRMENNR = BUA_FIRMENNR AND YBU_FIBUNR = BUA_FIBUNR AND YBU_BUA_BUART = BUA_BUART" "~"
if joinKuerzel = KEKBUC
searchResult := searchResult "LEFT JOIN BUC_BUCHUNG.BUC_BUCHUNG ON BUC_BUCHUNGID = KEK_KOPF_BUCHUNGID AND BUC_FIRMENNR = KEK_KOPF_FIBU_FIRMENNR AND BUC_FIBUNR = KEK_KOPF_FIBUNR" "~"
if joinKuerzel = BUCKEK
searchResult := searchResult "LEFT JOIN KORE.KEK_ERFASSKOPF ON KEK_KOPF_BUCHUNGID = BUC_BUCHUNGID AND KEK_KOPF_FIBU_FIRMENNR = BUC_FIRMENNR AND KEK_KOPF_FIBUNR = BUC_FIBUNR" "~"
if joinKuerzel = AFBBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_BUCHUNGID = AFB_BUCHUNGID_VST_KUERZUNG AND BUC_FIRMENNR = AFB_FIRMENNR_VST_KUERZUNG AND BUC_FIBUNR = AFB_FIBUNR_VST_KUERZUNG" "~"
if joinKuerzel = BUCAFB
searchResult := searchResult "LEFT JOIN FIBU.AFB_ANBUFIB ON AFB_BUCHUNGID_VST_KUERZUNG = BUC_BUCHUNGID AND AFB_FIRMENNR_VST_KUERZUNG = BUC_FIRMENNR AND AFB_FIBUNR_VST_KUERZUNG = BUC_FIBUNR" "~"
if joinKuerzel = VEBBUR
searchResult := searchResult "LEFT JOIN BUR_BMDCOMUPLOADTREE.BUR_BMDCOMUPLOADTREE ON BUR_UPLOADID = VEB_BUR_UPLOADID" "~"
if joinKuerzel = BURVEB
searchResult := searchResult "LEFT JOIN FIBU.VEB_VORERFASSBUCHUNG ON VEB_BUR_UPLOADID = BUR_UPLOADID" "~"
if joinKuerzel = AWKBUV
searchResult := searchResult "LEFT JOIN BUV_BUCHUNGSVARIANTE.BUV_BUCHUNGSVARIANTE ON BUV_FIRMENNR = AWK_FIRMENNR AND BUV_FIBUNR = AWK_FIBUNR AND BUV_BUVARIANTE = AWK_BUV_BUVARIANTE" "~"
if joinKuerzel = BUVAWK
searchResult := searchResult "LEFT JOIN BIL.AWK_AUSWERTUNGKOPF ON AWK_FIRMENNR = BUV_FIRMENNR AND AWK_FIBUNR = BUV_FIBUNR AND AWK_BUV_BUVARIANTE = BUV_BUVARIANTE" "~"
if joinKuerzel = BVLBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_BESTELLVORSCHLAGID = BVL_BESTELLVORSCHLAGID" "~"
if joinKuerzel = BVOBVL
searchResult := searchResult "LEFT JOIN WWS.BVL_LIEFERANTENZUORDNUNG ON BVL_BESTELLVORSCHLAGID = BVO_BESTELLVORSCHLAGID" "~"
if joinKuerzel = BWMBWV
searchResult := searchResult "LEFT JOIN BUERO.BWV_BEWERTUNGVERSION ON BWV_FIRMENNR = BWM_BWV_FIRMENNR AND BWV_BWVERSIONNR = BWM_BWVERSIONNR" "~"
if joinKuerzel = BWVBWM
searchResult := searchResult "LEFT JOIN BUERO.BWM_BEWERTUNGMONAT ON BWM_BWV_FIRMENNR = BWV_FIRMENNR AND BWM_BWVERSIONNR = BWV_BWVERSIONNR" "~"
if joinKuerzel = DEIBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = DEI_N25A" "~"
if joinKuerzel = BZSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_N25A = BZS_BNZEILEID" "~"
if joinKuerzel = DEIBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = DEI_VB_PA" "~"
if joinKuerzel = BZSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_VB_PA = BZS_BNZEILEID" "~"
if joinKuerzel = DEIBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = DEI_N25C" "~"
if joinKuerzel = BZSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_N25C = BZS_BNZEILEID" "~"
if joinKuerzel = CTZCAD
searchResult := searchResult "LEFT JOIN BMDDEFAULT.CAD_CACHEDEFINITION ON CAD_CADID = CTZ_CADID" "~"
if joinKuerzel = CADCTZ
searchResult := searchResult "LEFT JOIN BMDDEFAULT.CTZ_CACHETABLEZU ON CTZ_CADID = CAD_CADID" "~"
if joinKuerzel = TERCAW
searchResult := searchResult "LEFT JOIN BUERO.CAW_CRMABWESENHEIT ON CAW_CAWID = TER_CAW_CAWID" "~"
if joinKuerzel = CAWTER
searchResult := searchResult "LEFT JOIN BUERO.TER_TERMIN ON TER_CAW_CAWID = CAW_CAWID" "~"
if joinKuerzel = BPDCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_CHARGEIDNR = BPD_CHARGEIDNR" "~"
if joinKuerzel = CHGBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_CHARGEIDNR = CHG_CHARGEIDNR" "~"
if joinKuerzel = IVPCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_CHARGEIDNR = IVP_CHARGEIDNR" "~"
if joinKuerzel = CHGIVP
searchResult := searchResult "LEFT JOIN WWS.IVP_INVENTURPOSITION ON IVP_CHARGEIDNR = CHG_CHARGEIDNR" "~"
if joinKuerzel = LAJCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_CHARGEIDNR = LAJ_CHARGEIDNR" "~"
if joinKuerzel = CHGLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_CHARGEIDNR = CHG_CHARGEIDNR" "~"
if joinKuerzel = UMIBUA
searchResult := searchResult "LEFT JOIN BUA_BUCHUNGSART.BUA_BUCHUNGSART ON BUA_FIRMENNR = UMI_FIRMENNR AND BUA_FIBUNR = UMI_FIBUNR AND BUA_BUART = UMI_BUART" "~"
if joinKuerzel = BUAUMI
searchResult := searchResult "LEFT JOIN KORE.UMI_UMLAGEISTMENGE ON UMI_FIRMENNR = BUA_FIRMENNR AND UMI_FIBUNR = BUA_FIBUNR AND UMI_BUART = BUA_BUART" "~"
if joinKuerzel = BUABUA
searchResult := searchResult "LEFT JOIN FIBU.BUA_BUCHUNGSART ON BUA_FIRMENNR = BUA_FIRMENNR AND BUA_FIBUNR = BUA_FIBUNR AND BUA_BUART = BUA_STORNOLFD_BUART" "~"
if joinKuerzel = BUABUA
searchResult := searchResult "LEFT JOIN FIBU.BUA_BUCHUNGSART ON BUA_FIRMENNR = BUA_FIRMENNR AND BUA_FIBUNR = BUA_FIBUNR AND BUA_STORNOLFD_BUART = BUA_BUART" "~"
if joinKuerzel = ALDBUC
searchResult := searchResult "LEFT JOIN BUC_BUCHUNG.BUC_BUCHUNG ON BUC_BUCHUNGID = ALD_BUCHUNGID AND BUC_FIRMENNR = ALD_BUC_FIRMENNR AND BUC_FIBUNR = ALD_BUC_FIBUNR" "~"
if joinKuerzel = BUCALD
searchResult := searchResult "LEFT JOIN ZX_GELOESCHT.ALD_ANLAGENDETAIL ON ALD_BUCHUNGID = BUC_BUCHUNGID AND ALD_BUC_FIRMENNR = BUC_FIRMENNR AND ALD_BUC_FIBUNR = BUC_FIBUNR" "~"
if joinKuerzel = BKSBUK
searchResult := searchResult "LEFT JOIN FIBU.BUK_BUCHUNGSKREIS ON BUK_FIRMENNR = BKS_FIRMENNR AND BUK_FIBUNR = BKS_FIBUNR AND BUK_BUCHKREISNR = BKS_BUCHKREISNR" "~"
if joinKuerzel = BUKBKS
searchResult := searchResult "LEFT JOIN FIBU.BKS_BUCHUNGSKONTROLLSUMME ON BKS_FIRMENNR = BUK_FIRMENNR AND BKS_FIBUNR = BUK_FIBUNR AND BKS_BUCHKREISNR = BUK_BUCHKREISNR" "~"
if joinKuerzel = BVZBUV
searchResult := searchResult "LEFT JOIN FIBU.BUV_BUCHUNGSVARIANTE ON BUV_FIRMENNR = BVZ_FIRMENNR AND BUV_FIBUNR = BVZ_FIBUNR AND BUV_BUVARIANTE = BVZ_BUVARIANTE" "~"
if joinKuerzel = BUVBVZ
searchResult := searchResult "LEFT JOIN FIBU.BVZ_BUCHUNGSVAR_ARTZU ON BVZ_FIRMENNR = BUV_FIRMENNR AND BVZ_FIBUNR = BUV_FIBUNR AND BVZ_BUVARIANTE = BUV_BUVARIANTE" "~"
if joinKuerzel = BWIBWB
searchResult := searchResult "LEFT JOIN BUERO.BWB_BEWERBUNG ON BWB_BEWERBID = BWI_BWB_BEWERBID" "~"
if joinKuerzel = BWBBWI
searchResult := searchResult "LEFT JOIN BUERO.BWI_BEWERBUNGSINFO ON BWI_BWB_BEWERBID = BWB_BEWERBID" "~"
if joinKuerzel = PIPBWB
searchResult := searchResult "LEFT JOIN BUERO.BWB_BEWERBUNG ON BWB_BEWERBID = PIP_BWB_BEWERBID" "~"
if joinKuerzel = BWBPIP
searchResult := searchResult "LEFT JOIN BUERO.PIP_PERSINFOPROTOKOLL ON PIP_BWB_BEWERBID = BWB_BEWERBID" "~"
if joinKuerzel = BTGBWF
searchResult := searchResult "LEFT JOIN BUERO.BWF_BEWERTUNGFORMEL ON BWF_BWFORMELID = BTG_BWFORMELID" "~"
if joinKuerzel = BWFBTG
searchResult := searchResult "LEFT JOIN BUERO.BTG_BEWERTUNGTAG ON BTG_BWFORMELID = BWF_BWFORMELID" "~"
if joinKuerzel = DEIBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = DEI_N25B" "~"
if joinKuerzel = BZSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_N25B = BZS_BNZEILEID" "~"
if joinKuerzel = DEIBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = DEI_WF" "~"
if joinKuerzel = BZSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_WF = BZS_BNZEILEID" "~"
if joinKuerzel = DEIBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = DEI_VB_UV" "~"
if joinKuerzel = BZSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_VB_UV = BZS_BNZEILEID" "~"
if joinKuerzel = DEIBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = DEI_VB_AL30" "~"
if joinKuerzel = BZSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_VB_AL30 = BZS_BNZEILEID" "~"
if joinKuerzel = DEIBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = DEI_VB_AL00" "~"
if joinKuerzel = BZSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_VB_AL00 = BZS_BNZEILEID" "~"
if joinKuerzel = DEIBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = DEI_NEUFOEGN63" "~"
if joinKuerzel = BZSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_NEUFOEGN63 = BZS_BNZEILEID" "~"
if joinKuerzel = DEIBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = DEI_LG" "~"
if joinKuerzel = BZSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_LG = BZS_BNZEILEID" "~"
if joinKuerzel = EROCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_CHARGEIDNR = ERO_CHARGEIDNR" "~"
if joinKuerzel = CHGERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_CHARGEIDNR = CHG_CHARGEIDNR" "~"
if joinKuerzel = LUECHG
searchResult := searchResult "LEFT JOIN CHG_CHARGE.CHG_CHARGE ON CHG_CHARGEIDNR = LUE_CHARGEIDNR" "~"
if joinKuerzel = CHGLUE
searchResult := searchResult "LEFT JOIN PRODUKTION.LUE_LAGER_UMS_EMPFANGEN ON LUE_CHARGEIDNR = CHG_CHARGEIDNR" "~"
if joinKuerzel = WAPCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_CHARGEIDNR = WAP_CHARGEIDNR" "~"
if joinKuerzel = CHGWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_CHARGEIDNR = CHG_CHARGEIDNR" "~"
if joinKuerzel = CWFCLV
searchResult := searchResult "LEFT JOIN BUERO.CLV_CHECKLISTVORLAGE ON CLV_CLVNR = CWF_CLVNR" "~"
if joinKuerzel = CLVCWF
searchResult := searchResult "LEFT JOIN BUERO.CWF_CRMWORKFLOW ON CWF_CLVNR = CLV_CLVNR" "~"
if joinKuerzel = DKZDAK
searchResult := searchResult "LEFT JOIN DAK_DATTRIBUTKATEGORIE.DAK_DATTRIBUTKATEGORIE ON DAK_ATTRKATLFDNR = DKZ_ATTRKATLFDNR" "~"
if joinKuerzel = DAKDKZ
searchResult := searchResult "LEFT JOIN BUERO.DKZ_DOKUMENTZU ON DKZ_ATTRKATLFDNR = DAK_ATTRKATLFDNR" "~"
if joinKuerzel = DEIDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = DEI_DATENBESTANDNR" "~"
if joinKuerzel = DBSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = FTGDBS
searchResult := searchResult "LEFT JOIN BUERO.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = FTG_DATENBESTANDNR" "~"
if joinKuerzel = DBSFTG
searchResult := searchResult "LEFT JOIN BUERO.FTG_FEIERTAG ON FTG_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = HVBDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = HVB_DBS_DATENBESTANDNR" "~"
if joinKuerzel = DBSHVB
searchResult := searchResult "LEFT JOIN HRS.HVB_VERRECHNUNGSBASIS ON HVB_DBS_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = BVGBRA
searchResult := searchResult "LEFT JOIN BRA_BRANCHE.BRA_BRANCHE ON BRA_LANDNR = BVG_LANDNR AND BRA_BRANCHENNR = BVG_BRANCHENNR" "~"
if joinKuerzel = BRABVG
searchResult := searchResult "LEFT JOIN BIL.BVG_BRANCHENVERGLEICH ON BVG_LANDNR = BRA_LANDNR AND BVG_BRANCHENNR = BRA_BRANCHENNR" "~"
if joinKuerzel = FWFBRA
searchResult := searchResult "LEFT JOIN BRA_BRANCHE.BRA_BRANCHE ON BRA_LANDNR = FWF_LANDNR AND BRA_BRANCHENNR = FWF_BRANCHENNR" "~"
if joinKuerzel = BRAFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_LANDNR = BRA_LANDNR AND FWF_BRANCHENNR = BRA_BRANCHENNR" "~"
if joinKuerzel = BRZBRB
searchResult := searchResult "LEFT JOIN BUERO.BRB_BUEROBERECHT ON BRB_BUEROBERECHTNR = BRZ_BUEROBERECHTNR" "~"
if joinKuerzel = BRBBRZ
searchResult := searchResult "LEFT JOIN BUERO.BRZ_BUEROBERECHTZU ON BRZ_BUEROBERECHTNR = BRB_BUEROBERECHTNR" "~"
if joinKuerzel = TPRBRC
searchResult := searchResult "LEFT JOIN BUERO.BRC_BEREICH ON BRC_BEREICHKZ = TPR_BEREICHKZ AND BRC_BEREICHLFDNR = TPR_BEREICHLFDNR" "~"
if joinKuerzel = BRCTPR
searchResult := searchResult "LEFT JOIN BUERO.TPR_TELEFONPROTOKOLL ON TPR_BEREICHKZ = BRC_BEREICHKZ AND TPR_BEREICHLFDNR = BRC_BEREICHLFDNR" "~"
if joinKuerzel = AGNBRG
searchResult := searchResult "LEFT JOIN BRG_BERUFSGRUPPEN.BRG_BERUFSGRUPPEN ON BRG_FIRMENNR = AGN_BRG_FIRMENNR AND BRG_BRGGRUPPENNR = AGN_BRGGRUPPENNR" "~"
if joinKuerzel = BRGAGN
searchResult := searchResult "LEFT JOIN WWS.AGN_AG_PERSON_ZU ON AGN_BRG_FIRMENNR = BRG_FIRMENNR AND AGN_BRGGRUPPENNR = BRG_BRGGRUPPENNR" "~"
if joinKuerzel = KMZBRK
searchResult := searchResult "LEFT JOIN HRS.BRK_BEARBEITERKZ ON BRK_BEARBKZID = KMZ_BEARBKZID AND BRK_DATENBESTANDNR = KMZ_DATENBESTANDNR" "~"
if joinKuerzel = BRKKMZ
searchResult := searchResult "LEFT JOIN HRS.KMZ_KOMMSTZENTRALE ON KMZ_BEARBKZID = BRK_BEARBKZID AND KMZ_DATENBESTANDNR = BRK_DATENBESTANDNR" "~"
if joinKuerzel = CAWBRM
searchResult := searchResult "LEFT JOIN BUERO.BRM_BUEROMAKROS ON BRM_BUEROMAKROID = CAW_BUEROMAKROID_AUG" "~"
if joinKuerzel = BRMCAW
searchResult := searchResult "LEFT JOIN BUERO.CAW_CRMABWESENHEIT ON CAW_BUEROMAKROID_AUG = BRM_BUEROMAKROID" "~"
if joinKuerzel = KAPBSD
searchResult := searchResult "LEFT JOIN WWS.BSD_BONUSERMDETAIL ON BSD_BONUSERMDETID = KAP_BONUSERMDETID" "~"
if joinKuerzel = BSDKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_BONUSERMDETID = BSD_BONUSERMDETID" "~"
if joinKuerzel = RNPBSD
searchResult := searchResult "LEFT JOIN WWS.BSD_BONUSERMDETAIL ON BSD_BONUSERMDETID = RNP_BONUSERMDETID_PLAN" "~"
if joinKuerzel = BSDRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_BONUSERMDETID_PLAN = BSD_BONUSERMDETID" "~"
if joinKuerzel = MAGBSG
searchResult := searchResult "LEFT JOIN BUERO.BSG_BESPRECHUNG ON BSG_BSGID = MAG_BSGID" "~"
if joinKuerzel = BSGMAG
searchResult := searchResult "LEFT JOIN BUERO.MAG_MITARBGESPR ON MAG_BSGID = BSG_BSGID" "~"
if joinKuerzel = TLNBSG
searchResult := searchResult "LEFT JOIN BUERO.BSG_BESPRECHUNG ON BSG_BSGID = TLN_BSGID" "~"
if joinKuerzel = BSGTLN
searchResult := searchResult "LEFT JOIN BUERO.TLN_TEILNEHMER ON TLN_BSGID = BSG_BSGID" "~"
if joinKuerzel = TRZBSG
searchResult := searchResult "LEFT JOIN BUERO.BSG_BESPRECHUNG ON BSG_BSGID = TRZ_BSGID" "~"
if joinKuerzel = BSGTRZ
searchResult := searchResult "LEFT JOIN BUERO.TRZ_TERMINRESSOURCENZU ON TRZ_BSGID = BSG_BSGID" "~"
if joinKuerzel = AGPBTF
searchResult := searchResult "LEFT JOIN BTF_BMDTABFELDBESCH.BTF_BMDTABFELDBESCH ON BTF_BMDCONSTNAME = AGP_BMDCONSTNAME" "~"
if joinKuerzel = BTFAGP
searchResult := searchResult "LEFT JOIN WWS.AGP_ARTIKELGRUPPENPOS ON AGP_BMDCONSTNAME = BTF_BMDCONSTNAME" "~"
if joinKuerzel = TABBTF
searchResult := searchResult "LEFT JOIN BTF_BMDTABFELDBESCH.BTF_BMDTABFELDBESCH ON BTF_TABELLENNAME = TABELLENNAME AND BTF_FELDNAME = FELDNAME" "~"
if joinKuerzel = BTFTAB
searchResult := searchResult "LEFT JOIN BMDENTW.TABFELDHILFE ON TABELLENNAME = BTF_TABELLENNAME AND FELDNAME = BTF_FELDNAME" "~"
if joinKuerzel = ZBUBTM
searchResult := searchResult "LEFT JOIN BUERO.BTM_BTERMINAL ON BTM_FIRMENNR = ZBU_BTM_FIRMENNR AND BTM_TERMINALNR = ZBU_TERMINALNR" "~"
if joinKuerzel = BTMZBU
searchResult := searchResult "LEFT JOIN BUERO.ZBU_ZEITBUCHUNG ON ZBU_BTM_FIRMENNR = BTM_FIRMENNR AND ZBU_TERMINALNR = BTM_TERMINALNR" "~"
if joinKuerzel = ERBBUA
searchResult := searchResult "LEFT JOIN FIBU.BUA_BUCHUNGSART ON BUA_FIRMENNR = ERB_FIRMENNR AND BUA_FIBUNR = ERB_FIBUNR AND BUA_BUART = ERB_BUART" "~"
if joinKuerzel = BUAERB
searchResult := searchResult "LEFT JOIN FIBU.ERB_BUCHUNG ON ERB_FIRMENNR = BUA_FIRMENNR AND ERB_FIBUNR = BUA_FIBUNR AND ERB_BUART = BUA_BUART" "~"
if joinKuerzel = KMPBUA
searchResult := searchResult "LEFT JOIN BUA_BUCHUNGSART.BUA_BUCHUNGSART ON BUA_FIRMENNR = KMP_FIRMENNR AND BUA_FIBUNR = KMP_FIBUNR AND BUA_BUART = KMP_BUART" "~"
if joinKuerzel = BUAKMP
searchResult := searchResult "LEFT JOIN KORE.KMP_PLANMENGEN ON KMP_FIRMENNR = BUA_FIRMENNR AND KMP_FIBUNR = BUA_FIBUNR AND KMP_BUART = BUA_BUART" "~"
if joinKuerzel = UDPBUA
searchResult := searchResult "LEFT JOIN BUA_BUCHUNGSART.BUA_BUCHUNGSART ON BUA_FIRMENNR = UDP_FIRMENNR AND BUA_FIBUNR = UDP_FIBUNR AND BUA_BUART = UDP_BUART" "~"
if joinKuerzel = BUAUDP
searchResult := searchResult "LEFT JOIN KORE.UDP_UMLAGEPLANWERTE ON UDP_FIRMENNR = BUA_FIRMENNR AND UDP_FIBUNR = BUA_FIBUNR AND UDP_BUART = BUA_BUART" "~"
if joinKuerzel = RNGBUC
searchResult := searchResult "LEFT JOIN BUC_BUCHUNG.BUC_BUCHUNG ON BUC_BUCHUNGID = RNG_FIBU_BUCHUNGID AND BUC_FIRMENNR = RNG_FIBU_FIRMENNR AND BUC_FIBUNR = RNG_FIBUNR" "~"
if joinKuerzel = BUCRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIBU_BUCHUNGID = BUC_BUCHUNGID AND RNG_FIBU_FIRMENNR = BUC_FIRMENNR AND RNG_FIBUNR = BUC_FIBUNR" "~"
if joinKuerzel = OPZBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_BUCHUNGID = OPZ_BUCHUNGID_ZLG AND BUC_FIRMENNR = OPZ_FIRMENNR AND BUC_FIBUNR = OPZ_FIBUNR" "~"
if joinKuerzel = BUCOPZ
searchResult := searchResult "LEFT JOIN FIBU.OPZ_OPZUORDNUNG ON OPZ_BUCHUNGID_ZLG = BUC_BUCHUNGID AND OPZ_FIRMENNR = BUC_FIRMENNR AND OPZ_FIBUNR = BUC_FIBUNR" "~"
if joinKuerzel = GLSBUV
searchResult := searchResult "LEFT JOIN BUV_BUCHUNGSVARIANTE.BUV_BUCHUNGSVARIANTE ON BUV_FIRMENNR = GLS_FIRMENNR AND BUV_FIBUNR = GLS_FIBUNR AND BUV_BUVARIANTE = GLS_BUVARIANTE_VOR" "~"
if joinKuerzel = BUVGLS
searchResult := searchResult "LEFT JOIN BIL.GLS_GLIEDERUNGSSCHEMA ON GLS_FIRMENNR = BUV_FIRMENNR AND GLS_FIBUNR = BUV_FIBUNR AND GLS_BUVARIANTE_VOR = BUV_BUVARIANTE" "~"
if joinKuerzel = KOCCHG
searchResult := searchResult "LEFT JOIN CHG_CHARGE.CHG_CHARGE ON CHG_CHARGEIDNR = KOC_CHARGEIDNR" "~"
if joinKuerzel = CHGKOC
searchResult := searchResult "LEFT JOIN PRODUKTION.KOC_KOMBI_CHARGE ON KOC_CHARGEIDNR = CHG_CHARGEIDNR" "~"
if joinKuerzel = WFNCLV
searchResult := searchResult "LEFT JOIN BUERO.CLV_CHECKLISTVORLAGE ON CLV_CLVNR = WFN_CLVNR" "~"
if joinKuerzel = CLVWFN
searchResult := searchResult "LEFT JOIN BUERO.WFN_WORKFLOWACTION ON WFN_CLVNR = CLV_CLVNR" "~"
if joinKuerzel = SBSCWF
searchResult := searchResult "LEFT JOIN BUERO.CWF_CRMWORKFLOW ON CWF_CWFID = SBS_ASR_CWF_CWFID" "~"
if joinKuerzel = CWFSBS
searchResult := searchResult "LEFT JOIN BUERO.SBS_STELLENBESCHREIBUNG ON SBS_ASR_CWF_CWFID = CWF_CWFID" "~"
if joinKuerzel = SBFDBP
searchResult := searchResult "LEFT JOIN DBP_DBERECHTPAKET.DBP_DBERECHTPAKET ON DBP_BERECHTPAKETNR = SBF_BERECHTPAKETNR" "~"
if joinKuerzel = DBPSBF
searchResult := searchResult "LEFT JOIN BMD.SBF_STDBRIEF ON SBF_BERECHTPAKETNR = DBP_BERECHTPAKETNR" "~"
if joinKuerzel = SEQDBP
searchResult := searchResult "LEFT JOIN DBP_DBERECHTPAKET.DBP_DBERECHTPAKET ON DBP_BERECHTPAKETNR = SEQ_FILTERPAKETNR" "~"
if joinKuerzel = DBPSEQ
searchResult := searchResult "LEFT JOIN BMD.SEQ_SEQUENZ ON SEQ_FILTERPAKETNR = DBP_BERECHTPAKETNR" "~"
if joinKuerzel = AWEDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = AWE_DATENBESTANDNR" "~"
if joinKuerzel = DBSAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = BGUDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = BGU_DATENBESTANDNR" "~"
if joinKuerzel = DBSBGU
searchResult := searchResult "LEFT JOIN HRS.BGU_BEITRAGSGRUPPEIMPZU ON BGU_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = BRKDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = BRK_DATENBESTANDNR" "~"
if joinKuerzel = DBSBRK
searchResult := searchResult "LEFT JOIN HRS.BRK_BEARBEITERKZ ON BRK_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = GKVDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = GKV_DATENBESTANDNR" "~"
if joinKuerzel = DBSGKV
searchResult := searchResult "LEFT JOIN HRS.GKV_GEBSTUFEKV ON GKV_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = HVPDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = HVP_DBS_DATENBESTANDNR" "~"
if joinKuerzel = DBSHVP
searchResult := searchResult "LEFT JOIN HRS.HVP_VERRECHUNGSPOSITION ON HVP_DBS_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = KSZDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = KSZ_DBSNR" "~"
if joinKuerzel = DBSKSZ
searchResult := searchResult "LEFT JOIN HRS.KSZ_BBKALKSZ ON KSZ_DBSNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = KVNDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = KVN_DATENBESTANDNR" "~"
if joinKuerzel = DBSKVN
searchResult := searchResult "LEFT JOIN HRS.KVN_KVNAME ON KVN_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = LGFDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = LGF_DBS" "~"
if joinKuerzel = DBSLGF
searchResult := searchResult "LEFT JOIN HRS.LGF_LGVMAKROS ON LGF_DBS = DBS_DATENBESTANDNR" "~"
if joinKuerzel = SIRDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = SIR_DATENBESTANDNR" "~"
if joinKuerzel = DBSSIR
searchResult := searchResult "LEFT JOIN HRS.SIR_STANDARDIMPORTREFERENZ ON SIR_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = THVDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = THV_DATENBESTANDNR" "~"
if joinKuerzel = DBSTHV
searchResult := searchResult "LEFT JOIN HRS.THV_TARIFGR_HAEUFIGVERW ON THV_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = FIPDFG
searchResult := searchResult "LEFT JOIN DFG_DFELDBESCHGRUPPE.DFG_DFELDBESCHGRUPPE ON DFG_HAUPTGRUPPE = FIP_HAUPTGRUPPE AND DFG_UNTERGRUPPE = FIP_UNTERGRUPPE" "~"
if joinKuerzel = DFGFIP
searchResult := searchResult "LEFT JOIN BMD.FIP_FILTERPANEL ON FIP_HAUPTGRUPPE = DFG_HAUPTGRUPPE AND FIP_UNTERGRUPPE = DFG_UNTERGRUPPE" "~"
if joinKuerzel = BRBDFU
searchResult := searchResult "LEFT JOIN DFU_DFUNKTION.DFU_DFUNKTION ON DFU_FUNKTIONSNR = BRB_FUNKTIONSNR" "~"
if joinKuerzel = DFUBRB
searchResult := searchResult "LEFT JOIN BUERO.BRB_BUEROBERECHT ON BRB_FUNKTIONSNR = DFU_FUNKTIONSNR" "~"
if joinKuerzel = FNLDFU
searchResult := searchResult "LEFT JOIN DFU_DFUNKTION.DFU_DFUNKTION ON DFU_BMDCONSTID = FNL_BMDCONSTID" "~"
if joinKuerzel = DFUFNL
searchResult := searchResult "LEFT JOIN BMD.FNL_DFUNKTION_NLS_TMP ON FNL_BMDCONSTID = DFU_BMDCONSTID" "~"
if joinKuerzel = FURDFU
searchResult := searchResult "LEFT JOIN DFU_DFUNKTION.DFU_DFUNKTION ON DFU_FUNKTIONSNR = FUR_FUNKTIONSNR" "~"
if joinKuerzel = DFUFUR
searchResult := searchResult "LEFT JOIN BMD.FUR_FUNKRECHTE ON FUR_FUNKTIONSNR = DFU_FUNKTIONSNR" "~"
if joinKuerzel = HLDDFU
searchResult := searchResult "LEFT JOIN DFU_DFUNKTION.DFU_DFUNKTION ON DFU_FUNKTIONSNR = HLD_FUNKTIONSNR" "~"
if joinKuerzel = DFUHLD
searchResult := searchResult "LEFT JOIN BMDENTW.HLD_HILFEDOKUMENT ON HLD_FUNKTIONSNR = DFU_FUNKTIONSNR" "~"
if joinKuerzel = WWMDFU
searchResult := searchResult "LEFT JOIN DFU_DFUNKTION.DFU_DFUNKTION ON DFU_FUNKTIONSNR = WWM_FUNKTIONSNR" "~"
if joinKuerzel = DFUWWM
searchResult := searchResult "LEFT JOIN WWS.WWM_WWSMAKROZUORD ON WWM_FUNKTIONSNR = DFU_FUNKTIONSNR" "~"
if joinKuerzel = DIHDIA
searchResult := searchResult "LEFT JOIN BMD.DIA_DIAGRAMM ON DIA_DIAGRAMMNR = DIH_DIAGRAMMNR" "~"
if joinKuerzel = DIADIH
searchResult := searchResult "LEFT JOIN BMD.DIH_DIAGRAMMINSTANZKOPF ON DIH_DIAGRAMMNR = DIA_DIAGRAMMNR" "~"
if joinKuerzel = HRWDIA
searchResult := searchResult "LEFT JOIN DIA_DIAGRAMM.DIA_DIAGRAMM ON DIA_DIAGRAMMNR = HRW_DIAGRAMMNR" "~"
if joinKuerzel = DIAHRW
searchResult := searchResult "LEFT JOIN HRS.HRW_WORKFLOW ON HRW_DIAGRAMMNR = DIA_DIAGRAMMNR" "~"
if joinKuerzel = AXZDBT
searchResult := searchResult "LEFT JOIN DBT_DBMDTABFELDBESCH.DBT_DBMDTABFELDBESCH ON DBT_BMDCONSTNAME = AXZ_BMDCONSTNAME" "~"
if joinKuerzel = DBTAXZ
searchResult := searchResult "LEFT JOIN BMDENTW.AXZ_ATTREXTZU ON AXZ_BMDCONSTNAME = DBT_BMDCONSTNAME" "~"
if joinKuerzel = QLFDBT
searchResult := searchResult "LEFT JOIN DBT_DBMDTABFELDBESCH.DBT_DBMDTABFELDBESCH ON DBT_BMDCONSTNAME = QLF_VONFELDMCA" "~"
if joinKuerzel = DBTQLF
searchResult := searchResult "LEFT JOIN BMD.QLF_LINKFIELD ON QLF_VONFELDMCA = DBT_BMDCONSTNAME" "~"
if joinKuerzel = FBKDFG
searchResult := searchResult "LEFT JOIN DFG_DFELDBESCHGRUPPE.DFG_DFELDBESCHGRUPPE ON DFG_HAUPTGRUPPE = FBK_HAUPTGRUPPE AND DFG_UNTERGRUPPE = FBK_UNTERGRUPPE" "~"
if joinKuerzel = DFGFBK
searchResult := searchResult "LEFT JOIN BMD.FBK_FELDBESCHKOPF ON FBK_HAUPTGRUPPE = DFG_HAUPTGRUPPE AND FBK_UNTERGRUPPE = DFG_UNTERGRUPPE" "~"
if joinKuerzel = PFIDFG
searchResult := searchResult "LEFT JOIN DFG_DFELDBESCHGRUPPE.DFG_DFELDBESCHGRUPPE ON DFG_HAUPTGRUPPE = PFI_FBESCH_HAUPTGRUPPE AND DFG_UNTERGRUPPE = PFI_FBESCH_UNTERGRUPPE" "~"
if joinKuerzel = DFGPFI
searchResult := searchResult "LEFT JOIN BMD.PFI_PANELFILTER ON PFI_FBESCH_HAUPTGRUPPE = DFG_HAUPTGRUPPE AND PFI_FBESCH_UNTERGRUPPE = DFG_UNTERGRUPPE" "~"
if joinKuerzel = FFBDFU
searchResult := searchResult "LEFT JOIN DFU_DFUNKTION.DFU_DFUNKTION ON DFU_FUNKTIONSNR = FFB_FUNKTIONSNR" "~"
if joinKuerzel = DFUFFB
searchResult := searchResult "LEFT JOIN BMD.FFB_FUNKFELDBESCHRECHTE ON FFB_FUNKTIONSNR = DFU_FUNKTIONSNR" "~"
if joinKuerzel = GASDFU
searchResult := searchResult "LEFT JOIN DFU_DFUNKTION.DFU_DFUNKTION ON DFU_BMDCONSTID = GAS_BMDCONSTID" "~"
if joinKuerzel = DFUGAS
searchResult := searchResult "LEFT JOIN BMD.GAS_GLOBAL_ACTIONSTACK ON GAS_BMDCONSTID = DFU_BMDCONSTID" "~"
if joinKuerzel = MAKDFU
searchResult := searchResult "LEFT JOIN DFU_DFUNKTION.DFU_DFUNKTION ON DFU_FUNKTIONSNR = MAK_FUNKTIONSNR" "~"
if joinKuerzel = DFUMAK
searchResult := searchResult "LEFT JOIN BMD.MAK_MAKROFUNKTIONSZU ON MAK_FUNKTIONSNR = DFU_FUNKTIONSNR" "~"
if joinKuerzel = OUEDIA
searchResult := searchResult "LEFT JOIN BMD.DIA_DIAGRAMM ON DIA_DIAGRAMMNR = OUE_DIAGRAMMNR" "~"
if joinKuerzel = DIAOUE
searchResult := searchResult "LEFT JOIN BMD.OUE_OUTLOOKBARELEM ON OUE_DIAGRAMMNR = DIA_DIAGRAMMNR" "~"
if joinKuerzel = LAEDIP
searchResult := searchResult "LEFT JOIN DIP_DIVERSE.DIP_DIVERSE ON DIP_FIRMENNR = LAE_DIP_FIRMENNR AND DIP_EXTPERSID = LAE_DIP_EXTPERSID" "~"
if joinKuerzel = DIPLAE
searchResult := searchResult "LEFT JOIN HRS.LAE_LOHNARTEINSTELLUNG ON LAE_DIP_FIRMENNR = DIP_FIRMENNR AND LAE_DIP_EXTPERSID = DIP_EXTPERSID" "~"
if joinKuerzel = PTZDIP
searchResult := searchResult "LEFT JOIN DIP_DIVERSE.DIP_DIVERSE ON DIP_FIRMENNR = PTZ_SPEDITION_FIRMENNR AND DIP_EXTPERSID = PTZ_SPEDITION_EXTPERSID" "~"
if joinKuerzel = DIPPTZ
searchResult := searchResult "LEFT JOIN WWS.PTZ_PREISTABZU ON PTZ_SPEDITION_FIRMENNR = DIP_FIRMENNR AND PTZ_SPEDITION_EXTPERSID = DIP_EXTPERSID" "~"
if joinKuerzel = BPKDIP
searchResult := searchResult "LEFT JOIN DIP_DIVERSE.DIP_DIVERSE ON DIP_FIRMENNR = BPK_AUSLIEF_FIRMENNR AND DIP_EXTPERSID = BPK_AUSLIEF_EXTPERSID" "~"
if joinKuerzel = DIPBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_AUSLIEF_FIRMENNR = DIP_FIRMENNR AND BPK_AUSLIEF_EXTPERSID = DIP_EXTPERSID" "~"
if joinKuerzel = DASDKT
searchResult := searchResult "LEFT JOIN BUERO.DKT_DOCSKATEGORIE ON DKT_ARCHIVNR = DAS_DKT_ARCHIVNR AND DKT_DOCSKATEGORIEKZ = DAS_DKT_DOCSKATEGORIEKZ AND DKT_DOCSKATEGORIELFDNR = DAS_DKT_DOCSKATEGORIELFDNR" "~"
if joinKuerzel = DKTDAS
searchResult := searchResult "LEFT JOIN BUERO.DAS_DOCSARCHIVSTAMM ON DAS_DKT_ARCHIVNR = DKT_ARCHIVNR AND DAS_DKT_DOCSKATEGORIEKZ = DKT_DOCSKATEGORIEKZ AND DAS_DKT_DOCSKATEGORIELFDNR = DKT_DOCSKATEGORIELFDNR" "~"
if joinKuerzel = DOKDKT
searchResult := searchResult "LEFT JOIN BUERO.DKT_DOCSKATEGORIE ON DKT_ARCHIVNR = DOK_DKT_ARCHIVNR1 AND DKT_DOCSKATEGORIEKZ = DOK_DKT_DOCSKATEGORIEKZ1 AND DKT_DOCSKATEGORIELFDNR = DOK_DKT_DOCSKATEGORIELFDNR1" "~"
if joinKuerzel = DKTDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_DKT_ARCHIVNR1 = DKT_ARCHIVNR AND DOK_DKT_DOCSKATEGORIEKZ1 = DKT_DOCSKATEGORIEKZ AND DOK_DKT_DOCSKATEGORIELFDNR1 = DKT_DOCSKATEGORIELFDNR" "~"
if joinKuerzel = PINDKT
searchResult := searchResult "LEFT JOIN DKT_DOCSKATEGORIE.DKT_DOCSKATEGORIE ON DKT_ARCHIVNR = PIN_ARCHIVNR AND DKT_DOCSKATEGORIEKZ = PIN_DOCSKATEGORIEKZ AND DKT_DOCSKATEGORIELFDNR = PIN_DOCSKATEGORIELFDNR" "~"
if joinKuerzel = DKTPIN
searchResult := searchResult "LEFT JOIN WWS.PIN_PICANBINDEARTZU ON PIN_ARCHIVNR = DKT_ARCHIVNR AND PIN_DOCSKATEGORIEKZ = DKT_DOCSKATEGORIEKZ AND PIN_DOCSKATEGORIELFDNR = DKT_DOCSKATEGORIELFDNR" "~"
if joinKuerzel = DOKDKT
searchResult := searchResult "LEFT JOIN BUERO.DKT_DOCSKATEGORIE ON DKT_ARCHIVNR = DOK_DKT_ARCHIVNR AND DKT_DOCSKATEGORIEKZ = DOK_DKT_DOCSKATEGORIEKZ AND DKT_DOCSKATEGORIELFDNR = DOK_DKT_DOCSKATEGORIELFDNR" "~"
if joinKuerzel = DKTDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_DKT_ARCHIVNR = DKT_ARCHIVNR AND DOK_DKT_DOCSKATEGORIEKZ = DKT_DOCSKATEGORIEKZ AND DOK_DKT_DOCSKATEGORIELFDNR = DKT_DOCSKATEGORIELFDNR" "~"
if joinKuerzel = UAZDLK
searchResult := searchResult "LEFT JOIN DLK_LINK.DLK_LINK ON DLK_LINKNR = UAZ_LINKNR" "~"
if joinKuerzel = DLKUAZ
searchResult := searchResult "LEFT JOIN BMDENTW.UAZ_USERLINKAUSLIEFERNZU ON UAZ_LINKNR = DLK_LINKNR" "~"
if joinKuerzel = VEBDLN
searchResult := searchResult "LEFT JOIN DLN_DIENSTLEISTUNGSNUMMER.DLN_DIENSTLEISTUNGSNUMMER ON DLN_LANDNR = VEB_LANDNR AND DLN_DLCODE = VEB_ADL_DLCODE" "~"
if joinKuerzel = DLNVEB
searchResult := searchResult "LEFT JOIN FIBU.VEB_VORERFASSBUCHUNG ON VEB_LANDNR = DLN_LANDNR AND VEB_ADL_DLCODE = DLN_DLCODE" "~"
if joinKuerzel = DAZDNA
searchResult := searchResult "LEFT JOIN DATANORM.DNA_ARTIKEL ON DNA_ERST_NR = DAZ_ERST_NR AND DNA_ART_NR = DAZ_ART_NR" "~"
if joinKuerzel = DNADAZ
searchResult := searchResult "LEFT JOIN DATANORM.DAZ_DATANORM_ARTIKELZU ON DAZ_ERST_NR = DNA_ERST_NR AND DAZ_ART_NR = DNA_ART_NR" "~"
if joinKuerzel = ZAMDNA
searchResult := searchResult "LEFT JOIN DATANORM.DNA_ARTIKEL ON DNA_ERST_NR = ZAM_ERST_NR AND DNA_ART_NR = ZAM_ART_NR" "~"
if joinKuerzel = DNAZAM
searchResult := searchResult "LEFT JOIN DATANORM.ZAM_ZU_ABSCHLAG ON ZAM_ERST_NR = DNA_ERST_NR AND ZAM_ART_NR = DNA_ART_NR" "~"
if joinKuerzel = PCPCHG
searchResult := searchResult "LEFT JOIN CHG_CHARGE.CHG_CHARGE ON CHG_CHARGEIDNR = PCP_CHARGEIDNR" "~"
if joinKuerzel = CHGPCP
searchResult := searchResult "LEFT JOIN PRODUKTION.PCP_PPS_PRODUKT ON PCP_CHARGEIDNR = CHG_CHARGEIDNR" "~"
if joinKuerzel = TLPCHG
searchResult := searchResult "LEFT JOIN CHG_CHARGE.CHG_CHARGE ON CHG_CHARGEIDNR = TLP_CHARGEIDNR" "~"
if joinKuerzel = CHGTLP
searchResult := searchResult "LEFT JOIN PRODUKTION.TLP_BT_LOGISTIK_POSITION ON TLP_CHARGEIDNR = CHG_CHARGEIDNR" "~"
if joinKuerzel = TRJCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_CHARGEIDNR = TRJ_CHARGEIDNR" "~"
if joinKuerzel = CHGTRJ
searchResult := searchResult "LEFT JOIN WWS.TRJ_TRANSPORTEINH_JOURNAL ON TRJ_CHARGEIDNR = CHG_CHARGEIDNR" "~"
if joinKuerzel = FRACWF
searchResult := searchResult "LEFT JOIN BUERO.CWF_CRMWORKFLOW ON CWF_CWFID = FRA_CWFID" "~"
if joinKuerzel = CWFFRA
searchResult := searchResult "LEFT JOIN BUERO.FRA_FRISTART ON FRA_CWFID = CWF_CWFID" "~"
if joinKuerzel = DAKDAK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DAK_DATTRIBUTKATEGORIE ON DAK_ATTRKATLFDNR = DAK_HAUPTKAT_ATTRKATLFDNR" "~"
if joinKuerzel = DAKDAK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DAK_DATTRIBUTKATEGORIE ON DAK_HAUPTKAT_ATTRKATLFDNR = DAK_ATTRKATLFDNR" "~"
if joinKuerzel = BTFDAK
searchResult := searchResult "LEFT JOIN DAK_DATTRIBUTKATEGORIE.DAK_DATTRIBUTKATEGORIE ON DAK_ATTRKATLFDNR = BTF_UNTER_ATTRKATLFDNR" "~"
if joinKuerzel = DAKBTF
searchResult := searchResult "LEFT JOIN BMD.BTF_BMDTABFELDBESCH ON BTF_UNTER_ATTRKATLFDNR = DAK_ATTRKATLFDNR" "~"
if joinKuerzel = DBTDAK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DAK_DATTRIBUTKATEGORIE ON DAK_ATTRKATLFDNR = DBT_ATTRKATLFDNR" "~"
if joinKuerzel = DAKDBT
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DBT_DBMDTABFELDBESCH ON DBT_ATTRKATLFDNR = DAK_ATTRKATLFDNR" "~"
if joinKuerzel = BRMDBS
searchResult := searchResult "LEFT JOIN BUERO.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = BRM_DATENBESTANDNR" "~"
if joinKuerzel = DBSBRM
searchResult := searchResult "LEFT JOIN BUERO.BRM_BUEROMAKROS ON BRM_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = DZADBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = DZA_DBSNR" "~"
if joinKuerzel = DBSDZA
searchResult := searchResult "LEFT JOIN HRS.DZA_ANRECHNUNG ON DZA_DBSNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = FWEDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = FWE_DATENBESTANDNR" "~"
if joinKuerzel = DBSFWE
searchResult := searchResult "LEFT JOIN HRS.FWE_FIXWERTE ON FWE_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = IFRDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = IFR_DBSNR" "~"
if joinKuerzel = DBSIFR
searchResult := searchResult "LEFT JOIN HRS.IFR_IMPORTFELDREFERENZ ON IFR_DBSNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = KVSDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = KVS_DATENBESTANDNR" "~"
if joinKuerzel = DBSKVS
searchResult := searchResult "LEFT JOIN HRS.KVS_KSERVICEZU ON KVS_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = LGBDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = LGB_DBSNR" "~"
if joinKuerzel = DBSLGB
searchResult := searchResult "LEFT JOIN HRS.LGB_LGVBERECHNUNGEN ON LGB_DBSNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = LSEDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = LSE_DBSNR" "~"
if joinKuerzel = DBSLSE
searchResult := searchResult "LEFT JOIN HRS.LSE_LOHNSCHABLONE ON LSE_DBSNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = PFEDBT
searchResult := searchResult "LEFT JOIN DBT_DBMDTABFELDBESCH.DBT_DBMDTABFELDBESCH ON DBT_BMDCONSTNAME = PFE_BMDCONSTNAME" "~"
if joinKuerzel = DBTPFE
searchResult := searchResult "LEFT JOIN BMD.PFE_PANELFILTERELEM ON PFE_BMDCONSTNAME = DBT_BMDCONSTNAME" "~"
if joinKuerzel = QLFDBT
searchResult := searchResult "LEFT JOIN DBT_DBMDTABFELDBESCH.DBT_DBMDTABFELDBESCH ON DBT_BMDCONSTNAME = QLF_NACHFELDMCA" "~"
if joinKuerzel = DBTQLF
searchResult := searchResult "LEFT JOIN BMD.QLF_LINKFIELD ON QLF_NACHFELDMCA = DBT_BMDCONSTNAME" "~"
if joinKuerzel = DCVDCD
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DCD_WAPARAMDEF ON DCD_WAPARAMDEFNR = DCV_WAPARAMDEFNR" "~"
if joinKuerzel = DCDDCV
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DCV_WAPARAMDEFVALUES ON DCV_WAPARAMDEFNR = DCD_WAPARAMDEFNR" "~"
if joinKuerzel = DCEDCE
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DCE_WABAUMELEMENTE ON DCE_WABAUMELEMNR = DCE_WABAUMELEMNR_PARENT" "~"
if joinKuerzel = DCEDCE
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DCE_WABAUMELEMENTE ON DCE_WABAUMELEMNR_PARENT = DCE_WABAUMELEMNR" "~"
if joinKuerzel = CWFDIA
searchResult := searchResult "LEFT JOIN DIA_DIAGRAMM.DIA_DIAGRAMM ON DIA_DIAGRAMMNR = CWF_DIAGRAMMNR" "~"
if joinKuerzel = DIACWF
searchResult := searchResult "LEFT JOIN BUERO.CWF_CRMWORKFLOW ON CWF_DIAGRAMMNR = DIA_DIAGRAMMNR" "~"
if joinKuerzel = ALEDIB
searchResult := searchResult "LEFT JOIN DIB_DIAGRAMMINSTANZBASIS.DIB_DIAGRAMMINSTANZBASIS ON DIB_DIAGRAMMNR = ALE_DIAGRAMMNR AND DIB_DIAGRAMMINSTANZNR = ALE_DIAGRAMMINSTANZNR" "~"
if joinKuerzel = DIBALE
searchResult := searchResult "LEFT JOIN BUERO.ALE_ADRESSENLISTE ON ALE_DIAGRAMMNR = DIB_DIAGRAMMNR AND ALE_DIAGRAMMINSTANZNR = DIB_DIAGRAMMINSTANZNR" "~"
if joinKuerzel = ERBDIB
searchResult := searchResult "LEFT JOIN DIB_DIAGRAMMINSTANZBASIS.DIB_DIAGRAMMINSTANZBASIS ON DIB_DIAGRAMMNR = ERB_DIAGRAMMNR AND DIB_DIAGRAMMINSTANZNR = ERB_DIAGRAMMINSTANZNR" "~"
if joinKuerzel = DIBERB
searchResult := searchResult "LEFT JOIN FIBU.ERB_BUCHUNG ON ERB_DIAGRAMMNR = DIB_DIAGRAMMNR AND ERB_DIAGRAMMINSTANZNR = DIB_DIAGRAMMINSTANZNR" "~"
if joinKuerzel = ZXADIB
searchResult := searchResult "LEFT JOIN DIB_DIAGRAMMINSTANZBASIS.DIB_DIAGRAMMINSTANZBASIS ON DIB_DIAGRAMMNR = ZXA_DIAGRAMMNR AND DIB_DIAGRAMMINSTANZNR = ZXA_DIAGRAMMINSTANZNR" "~"
if joinKuerzel = DIBZXA
searchResult := searchResult "LEFT JOIN BUERO.ZXA_ZEITAENDERUNGSANTRAG ON ZXA_DIAGRAMMNR = DIB_DIAGRAMMNR AND ZXA_DIAGRAMMINSTANZNR = DIB_DIAGRAMMINSTANZNR" "~"
if joinKuerzel = LPOCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_CHARGEIDNR = LPO_CHARGEIDNR" "~"
if joinKuerzel = CHGLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_CHARGEIDNR = CHG_CHARGEIDNR" "~"
if joinKuerzel = CLWCLA
searchResult := searchResult "LEFT JOIN BUERO.CLA_CLAUSWAHLFELD ON CLA_FIRMENNR = CLW_FIRMENNR AND CLA_PROJEKTNR = CLW_PROJEKTNR AND CLA_PROJEKTTYP = CLW_PROJEKTTYP AND CLA_CLFELDNR = CLW_CLFELDNR" "~"
if joinKuerzel = CLACLW
searchResult := searchResult "LEFT JOIN BUERO.CLW_CLAUSWAHLFELDWERT ON CLW_FIRMENNR = CLA_FIRMENNR AND CLW_PROJEKTNR = CLA_PROJEKTNR AND CLW_PROJEKTTYP = CLA_PROJEKTTYP AND CLW_CLFELDNR = CLA_CLFELDNR" "~"
if joinKuerzel = CKLCLV
searchResult := searchResult "LEFT JOIN BUERO.CLV_CHECKLISTVORLAGE ON CLV_CLVNR = CKL_CLVNR" "~"
if joinKuerzel = CLVCKL
searchResult := searchResult "LEFT JOIN BUERO.CKL_CHECKLISTE ON CKL_CLVNR = CLV_CLVNR" "~"
if joinKuerzel = DSVCLV
searchResult := searchResult "LEFT JOIN BUERO.CLV_CHECKLISTVORLAGE ON CLV_CLVNR = DSV_CLVNR" "~"
if joinKuerzel = CLVDSV
searchResult := searchResult "LEFT JOIN BUERO.DSV_DSGVO_VERZEICHNIS ON DSV_CLVNR = CLV_CLVNR" "~"
if joinKuerzel = ERZCWF
searchResult := searchResult "LEFT JOIN CWF_CRMWORKFLOW.CWF_CRMWORKFLOW ON CWF_CWFID = ERZ_CWFID" "~"
if joinKuerzel = CWFERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_CWFID = CWF_CWFID" "~"
if joinKuerzel = MEDCWF
searchResult := searchResult "LEFT JOIN BUERO.CWF_CRMWORKFLOW ON CWF_CWFID = MED_CWFID" "~"
if joinKuerzel = CWFMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_CWFID = CWF_CWFID" "~"
if joinKuerzel = PROCWF
searchResult := searchResult "LEFT JOIN BUERO.CWF_CRMWORKFLOW ON CWF_CWFID = PRO_CWFID" "~"
if joinKuerzel = CWFPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_CWFID = CWF_CWFID" "~"
if joinKuerzel = DCZDAP
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DAP_WAAUSDRUCKPARAMETER ON DAP_WAPARAMNR = DCZ_WAPARAMNR" "~"
if joinKuerzel = DAPDCZ
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DCZ_WAAUSDRUCK_PARAM_ZU ON DCZ_WAPARAMNR = DAP_WAPARAMNR" "~"
if joinKuerzel = DKTDAS
searchResult := searchResult "LEFT JOIN BUERO.DAS_DOCSARCHIVSTAMM ON DAS_ARCHIVNR = DKT_ARCHIVNR" "~"
if joinKuerzel = DASDKT
searchResult := searchResult "LEFT JOIN BUERO.DKT_DOCSKATEGORIE ON DKT_ARCHIVNR = DAS_ARCHIVNR" "~"
if joinKuerzel = DOKDAS
searchResult := searchResult "LEFT JOIN BUERO.DAS_DOCSARCHIVSTAMM ON DAS_ARCHIVNR = DOK_ARCHIVNR" "~"
if joinKuerzel = DASDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_ARCHIVNR = DAS_ARCHIVNR" "~"
if joinKuerzel = VWADAS
searchResult := searchResult "LEFT JOIN BUERO.DAS_DOCSARCHIVSTAMM ON DAS_ARCHIVNR = VWA_ARCHIVNR" "~"
if joinKuerzel = DASVWA
searchResult := searchResult "LEFT JOIN BUERO.VWA_VORSCHLAGSWERTEARCHIV ON VWA_ARCHIVNR = DAS_ARCHIVNR" "~"
if joinKuerzel = DBIDBN
searchResult := searchResult "LEFT JOIN BMD.DBN_DBCLONE ON DBN_CLONEID = DBI_CLONEID" "~"
if joinKuerzel = DBNDBI
searchResult := searchResult "LEFT JOIN BMD.DBI_DBCLONEITEM ON DBI_CLONEID = DBN_CLONEID" "~"
if joinKuerzel = AWRDBS
searchResult := searchResult "LEFT JOIN BUERO.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = AWR_DATENBESTANDNR" "~"
if joinKuerzel = DBSAWR
searchResult := searchResult "LEFT JOIN BUERO.AWR_AUSWERTUNGSKAT ON AWR_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = GBKDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = GBK_DATENBESTANDNR" "~"
if joinKuerzel = DBSGBK
searchResult := searchResult "LEFT JOIN HRS.GBK_GEBIETEKV ON GBK_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = KMZDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = KMZ_DATENBESTANDNR" "~"
if joinKuerzel = DBSKMZ
searchResult := searchResult "LEFT JOIN HRS.KMZ_KOMMSTZENTRALE ON KMZ_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = KVXDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = KVX_DBSNR" "~"
if joinKuerzel = DBSKVX
searchResult := searchResult "LEFT JOIN HRS.KVX_KVTEXT ON KVX_DBSNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = LEHDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = LEH_DBS" "~"
if joinKuerzel = DBSLEH
searchResult := searchResult "LEFT JOIN HRS.LEH_LOHNEINSTELLUNG_HIST ON LEH_DBS = DBS_DATENBESTANDNR" "~"
if joinKuerzel = LENDBS
searchResult := searchResult "LEFT JOIN BUERO.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = LEN_DBS_DATENBESTANDNR" "~"
if joinKuerzel = DBSLEN
searchResult := searchResult "LEFT JOIN BUERO.LEN_LOHNEINSTELLUNGEN ON LEN_DBS_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = VWADBS
searchResult := searchResult "LEFT JOIN BUERO.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = VWA_DATENBESTANDNR" "~"
if joinKuerzel = DBSVWA
searchResult := searchResult "LEFT JOIN BUERO.VWA_VORSCHLAGSWERTEARCHIV ON VWA_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = CMDDBT
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DBT_DBMDTABFELDBESCH ON DBT_BMDCONSTNAME = CMD_BMDCONSTNAME" "~"
if joinKuerzel = DBTCMD
searchResult := searchResult "LEFT JOIN BMDDEFAULT.CMD_CMDLINEPARAM ON CMD_BMDCONSTNAME = DBT_BMDCONSTNAME" "~"
if joinKuerzel = CPZDBT
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DBT_DBMDTABFELDBESCH ON DBT_BMDCONSTNAME = CPZ_BMDCONSTNAME" "~"
if joinKuerzel = DBTCPZ
searchResult := searchResult "LEFT JOIN BMDDEFAULT.CPZ_CMDLINEPARAMZU ON CPZ_BMDCONSTNAME = DBT_BMDCONSTNAME" "~"
if joinKuerzel = NTCDBT
searchResult := searchResult "LEFT JOIN DBT_DBMDTABFELDBESCH.DBT_DBMDTABFELDBESCH ON DBT_BMDCONSTNAME = NTC_CONSTNAME" "~"
if joinKuerzel = DBTNTC
searchResult := searchResult "LEFT JOIN BMDENTW.NTC_NTCSARTIKEL ON NTC_CONSTNAME = DBT_BMDCONSTNAME" "~"
if joinKuerzel = ALIDFG
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFG_DFELDBESCHGRUPPE ON DFG_HAUPTGRUPPE = ALI_HAUPTGRUPPE AND DFG_UNTERGRUPPE = ALI_UNTERGRUPPE" "~"
if joinKuerzel = DFGALI
searchResult := searchResult "LEFT JOIN BMDDEFAULT.ALI_ATTRGRUPPELIZENZZU ON ALI_HAUPTGRUPPE = DFG_HAUPTGRUPPE AND ALI_UNTERGRUPPE = DFG_UNTERGRUPPE" "~"
if joinKuerzel = KOWDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = KOW_DATENBESTANDNR" "~"
if joinKuerzel = DBSKOW
searchResult := searchResult "LEFT JOIN HRS.KOW_KOLLEKTIVVERTRAGWERTE ON KOW_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = LAEDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = LAE_DBSNR" "~"
if joinKuerzel = DBSLAE
searchResult := searchResult "LEFT JOIN HRS.LAE_LOHNARTEINSTELLUNG ON LAE_DBSNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = LSPDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = LSP_DBSNR" "~"
if joinKuerzel = DBSLSP
searchResult := searchResult "LEFT JOIN HRS.LSP_LOHNSTAPEL ON LSP_DBSNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = PKKDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = PKK_DATENBESTANDNR" "~"
if joinKuerzel = DBSPKK
searchResult := searchResult "LEFT JOIN BIL.PKK_PERSKORST_PARAMKOPF ON PKK_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = RSODBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = RSO_DBSNR" "~"
if joinKuerzel = DBSRSO
searchResult := searchResult "LEFT JOIN HRS.RSO_REPORTSORTIERUNG ON RSO_DBSNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = LISDBT
searchResult := searchResult "LEFT JOIN DBT_DBMDTABFELDBESCH.DBT_DBMDTABFELDBESCH ON DBT_BMDCONSTNAME = LIS_BMDCONSTNAME" "~"
if joinKuerzel = DBTLIS
searchResult := searchResult "LEFT JOIN HRS.LIS_LOHNIMPORTSPERRE ON LIS_BMDCONSTNAME = DBT_BMDCONSTNAME" "~"
if joinKuerzel = FFBDFG
searchResult := searchResult "LEFT JOIN DFG_DFELDBESCHGRUPPE.DFG_DFELDBESCHGRUPPE ON DFG_HAUPTGRUPPE = FFB_DFG_HAUPTGRUPPE AND DFG_UNTERGRUPPE = FFB_DFG_UNTERGRUPPE" "~"
if joinKuerzel = DFGFFB
searchResult := searchResult "LEFT JOIN BMD.FFB_FUNKFELDBESCHRECHTE ON FFB_DFG_HAUPTGRUPPE = DFG_HAUPTGRUPPE AND FFB_DFG_UNTERGRUPPE = DFG_UNTERGRUPPE" "~"
if joinKuerzel = TFKDFG
searchResult := searchResult "LEFT JOIN DFG_DFELDBESCHGRUPPE.DFG_DFELDBESCHGRUPPE ON DFG_HAUPTGRUPPE = TFK_HAUPTGRUPPE AND DFG_UNTERGRUPPE = TFK_UNTERGRUPPE" "~"
if joinKuerzel = DFGTFK
searchResult := searchResult "LEFT JOIN BMD.TFK_FELDBESCHKOPF_TMP ON TFK_HAUPTGRUPPE = DFG_HAUPTGRUPPE AND TFK_UNTERGRUPPE = DFG_UNTERGRUPPE" "~"
if joinKuerzel = ABFDFU
searchResult := searchResult "LEFT JOIN DFU_DFUNKTION.DFU_DFUNKTION ON DFU_FUNKTIONSNR = ABF_KONTEXTFUNKTIONSNR" "~"
if joinKuerzel = DFUABF
searchResult := searchResult "LEFT JOIN BMD.ABF_ABFRAGE ON ABF_KONTEXTFUNKTIONSNR = DFU_FUNKTIONSNR" "~"
if joinKuerzel = AUGDFU
searchResult := searchResult "LEFT JOIN DFU_DFUNKTION.DFU_DFUNKTION ON DFU_FUNKTIONSNR = AUG_EXEC_MCS" "~"
if joinKuerzel = DFUAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_EXEC_MCS = DFU_FUNKTIONSNR" "~"
if joinKuerzel = DSVDFU
searchResult := searchResult "LEFT JOIN DFU_DFUNKTION.DFU_DFUNKTION ON DFU_FUNKTIONSNR = DSV_FUNKTIONSNR" "~"
if joinKuerzel = DFUDSV
searchResult := searchResult "LEFT JOIN BUERO.DSV_DSGVO_VERZEICHNIS ON DSV_FUNKTIONSNR = DFU_FUNKTIONSNR" "~"
if joinKuerzel = IEPDFU
searchResult := searchResult "LEFT JOIN DFU_DFUNKTION.DFU_DFUNKTION ON DFU_FUNKTIONSNR = IEP_FUNKTIONSNR" "~"
if joinKuerzel = DFUIEP
searchResult := searchResult "LEFT JOIN BMD.IEP_IMPEXP_PROTOKOLL ON IEP_FUNKTIONSNR = DFU_FUNKTIONSNR" "~"
if joinKuerzel = DFUDFU
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFU_DFUNKTION ON DFU_FUNKTIONSNR = DFU_ABHAENGIGVON" "~"
if joinKuerzel = DFUDFU
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFU_DFUNKTION ON DFU_ABHAENGIGVON = DFU_FUNKTIONSNR" "~"
if joinKuerzel = GASDFU
searchResult := searchResult "LEFT JOIN DFU_DFUNKTION.DFU_DFUNKTION ON DFU_BMDCONSTID = GAS_BMDCONSTID_FENSTER" "~"
if joinKuerzel = DFUGAS
searchResult := searchResult "LEFT JOIN BMD.GAS_GLOBAL_ACTIONSTACK ON GAS_BMDCONSTID_FENSTER = DFU_BMDCONSTID" "~"
if joinKuerzel = AUGDIB
searchResult := searchResult "LEFT JOIN DIB_DIAGRAMMINSTANZBASIS.DIB_DIAGRAMMINSTANZBASIS ON DIB_DIAGRAMMNR = AUG_DIAGRAMMNR AND DIB_DIAGRAMMINSTANZNR = AUG_DIAGRAMMINSTANZNR" "~"
if joinKuerzel = DIBAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_DIAGRAMMNR = DIB_DIAGRAMMNR AND AUG_DIAGRAMMINSTANZNR = DIB_DIAGRAMMINSTANZNR" "~"
if joinKuerzel = TSRDIB
searchResult := searchResult "LEFT JOIN DIB_DIAGRAMMINSTANZBASIS.DIB_DIAGRAMMINSTANZBASIS ON DIB_DIAGRAMMNR = TSR_DIAGRAMMNR AND DIB_DIAGRAMMINSTANZNR = TSR_DIAGRAMMINSTANZNR" "~"
if joinKuerzel = DIBTSR
searchResult := searchResult "LEFT JOIN BUERO.TSR_SERIENTERMIN ON TSR_DIAGRAMMNR = DIB_DIAGRAMMNR AND TSR_DIAGRAMMINSTANZNR = DIB_DIAGRAMMINSTANZNR" "~"
if joinKuerzel = WDIDII
searchResult := searchResult "LEFT JOIN DII_DIAGRAMMINSTANZ.DII_DIAGRAMMINSTANZ ON DII_DIAGRAMMINSTANZNR = WDI_DII_INSTANZNR_MAIN AND DII_DIAGRAMMNR = WDI_DII_DIAGRAMMNR_MAIN AND DII_AKTIONSNR = WDI_DII_AKTIONSNR_MAIN" "~"
if joinKuerzel = DIIWDI
searchResult := searchResult "LEFT JOIN BUERO.WDI_WFDIAGRAMMINSTANZZU ON WDI_DII_INSTANZNR_MAIN = DII_DIAGRAMMINSTANZNR AND WDI_DII_DIAGRAMMNR_MAIN = DII_DIAGRAMMNR AND WDI_DII_AKTIONSNR_MAIN = DII_AKTIONSNR" "~"
if joinKuerzel = SUKDIP
searchResult := searchResult "LEFT JOIN DIP_DIVERSE.DIP_DIVERSE ON DIP_FIRMENNR = SUK_DIP_FIRMENNR AND DIP_EXTPERSID = SUK_BETRIEBSRAT_PERSID" "~"
if joinKuerzel = DIPSUK
searchResult := searchResult "LEFT JOIN HRS.SUK_SUM_KOE ON SUK_DIP_FIRMENNR = DIP_FIRMENNR AND SUK_BETRIEBSRAT_PERSID = DIP_EXTPERSID" "~"
if joinKuerzel = DKTDKT
searchResult := searchResult "LEFT JOIN BUERO.DKT_DOCSKATEGORIE ON DKT_ARCHIVNR = DKT_M_ARCHIVNR AND DKT_DOCSKATEGORIEKZ = DKT_M_DOCSKATEGORIEKZ AND DKT_DOCSKATEGORIELFDNR = DKT_M_DOCSKATEGORIELFDNR" "~"
if joinKuerzel = DKTDKT
searchResult := searchResult "LEFT JOIN BUERO.DKT_DOCSKATEGORIE ON DKT_M_ARCHIVNR = DKT_ARCHIVNR AND DKT_M_DOCSKATEGORIEKZ = DKT_DOCSKATEGORIEKZ AND DKT_M_DOCSKATEGORIELFDNR = DKT_DOCSKATEGORIELFDNR" "~"
if joinKuerzel = DKWDKT
searchResult := searchResult "LEFT JOIN BUERO.DKT_DOCSKATEGORIE ON DKT_ARCHIVNR = DKW_ARCHIVNR AND DKT_DOCSKATEGORIEKZ = DKW_DOCSKATKZ AND DKT_DOCSKATEGORIELFDNR = DKW_DOCSKATLFDNR" "~"
if joinKuerzel = DKTDKW
searchResult := searchResult "LEFT JOIN BUERO.DKW_DOKKATWORKFLOWZU ON DKW_ARCHIVNR = DKT_ARCHIVNR AND DKW_DOCSKATKZ = DKT_DOCSKATEGORIEKZ AND DKW_DOCSKATLFDNR = DKT_DOCSKATEGORIELFDNR" "~"
if joinKuerzel = IMSDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = IMS_DBSNR" "~"
if joinKuerzel = DBSIMS
searchResult := searchResult "LEFT JOIN HRS.IMS_IMPORTSCHNITTSTELLEN ON IMS_DBSNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = IPMDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = IPM_DBSNR" "~"
if joinKuerzel = DBSIPM
searchResult := searchResult "LEFT JOIN HRS.IPM_INDIVPROGD ON IPM_DBSNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = LARDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = LAR_DBSNR" "~"
if joinKuerzel = DBSLAR
searchResult := searchResult "LEFT JOIN HRS.LAR_LOHNARTENREGEL ON LAR_DBSNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = DCZDCA
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DCA_WAAUSDRUCKE ON DCA_WAAUSDRUCKNR = DCZ_WAAUSDRUCKNR AND DCA_ABVERSIONSNR = DCZ_ABVERSIONSNR" "~"
if joinKuerzel = DCADCZ
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DCZ_WAAUSDRUCK_PARAM_ZU ON DCZ_WAAUSDRUCKNR = DCA_WAAUSDRUCKNR AND DCZ_ABVERSIONSNR = DCA_ABVERSIONSNR" "~"
if joinKuerzel = DCFDCE
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DCE_WABAUMELEMENTE ON DCE_WABAUMELEMNR = DCF_WABAUMELEMNR" "~"
if joinKuerzel = DCEDCF
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DCF_WABAUMELEMAUSDRUCKZU ON DCF_WABAUMELEMNR = DCE_WABAUMELEMNR" "~"
if joinKuerzel = RNGDFR
searchResult := searchResult "LEFT JOIN WWS.DFR_DOKUMENTFREIGABE ON DFR_DFRID = RNG_DFRID AND DFR_LFDNR = RNG_DFR_LFDNR" "~"
if joinKuerzel = DFRRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_DFRID = DFR_DFRID AND RNG_DFR_LFDNR = DFR_LFDNR" "~"
if joinKuerzel = WFNDIA
searchResult := searchResult "LEFT JOIN DIA_DIAGRAMM.DIA_DIAGRAMM ON DIA_DIAGRAMMNR = WFN_DIAGRAMMNR" "~"
if joinKuerzel = DIAWFN
searchResult := searchResult "LEFT JOIN BUERO.WFN_WORKFLOWACTION ON WFN_DIAGRAMMNR = DIA_DIAGRAMMNR" "~"
if joinKuerzel = WFZDIH
searchResult := searchResult "LEFT JOIN DIH_DIAGRAMMINSTANZKOPF.DIH_DIAGRAMMINSTANZKOPF ON DIH_DIAGRAMMNR = WFZ_DIAGRAMMNR AND DIH_INSTANZNR = WFZ_INSTANZNR" "~"
if joinKuerzel = DIHWFZ
searchResult := searchResult "LEFT JOIN BUERO.WFZ_WORKFLOWZU ON WFZ_DIAGRAMMNR = DIH_DIAGRAMMNR AND WFZ_INSTANZNR = DIH_INSTANZNR" "~"
if joinKuerzel = KOMDKQ
searchResult := searchResult "LEFT JOIN DKQ_DOKUMENTZUID.DKQ_DOKUMENTZUID ON DKQ_DKZID = KOM_DKZID" "~"
if joinKuerzel = DKQKOM
searchResult := searchResult "LEFT JOIN PRODUKTION.KOM_KOMBI ON KOM_DKZID = DKQ_DKZID" "~"
if joinKuerzel = VFDDKQ
searchResult := searchResult "LEFT JOIN DKQ_DOKUMENTZUID.DKQ_DOKUMENTZUID ON DKQ_DKZID = VFD_DKZID" "~"
if joinKuerzel = DKQVFD
searchResult := searchResult "LEFT JOIN PRODUKTION.VFD_VERFAHRENSANWEISUNG_POS ON VFD_DKZID = DKQ_DKZID" "~"
if joinKuerzel = DLFDLK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DLK_LINK ON DLK_LINKNR = DLF_LINKNR" "~"
if joinKuerzel = DLKDLF
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DLF_LINKFIELD ON DLF_LINKNR = DLK_LINKNR" "~"
if joinKuerzel = GRMDNE
searchResult := searchResult "LEFT JOIN DATANORM.DNE_ERSTELLER ON DNE_ERST_NR = GRM_ERST_NR" "~"
if joinKuerzel = DNEGRM
searchResult := searchResult "LEFT JOIN DATANORM.GRM_GRAFIK ON GRM_ERST_NR = DNE_ERST_NR" "~"
if joinKuerzel = DWHDNE
searchResult := searchResult "LEFT JOIN DATANORM.DNE_ERSTELLER ON DNE_ERST_NR = DWH_ERST_NR" "~"
if joinKuerzel = DNEDWH
searchResult := searchResult "LEFT JOIN DATANORM.DWH_WAREN_HAUPT_GRUPPE ON DWH_ERST_NR = DNE_ERST_NR" "~"
if joinKuerzel = DODDOH
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DOH_OCRTEMPLATEHEADER ON DOH_OCRID = DOD_OCRID" "~"
if joinKuerzel = DOHDOD
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DOD_OCRTEMPLATEDETAIL ON DOD_OCRID = DOH_OCRID" "~"
if joinKuerzel = BPKDOK
searchResult := searchResult "LEFT JOIN DOK_DOKUMENT.DOK_DOKUMENT ON DOK_ARCHIVNR = BPK_DOK_ARCHIVNR AND DOK_DOKUMENTENNR = BPK_DOK_DOKUMENTENNR" "~"
if joinKuerzel = DOKBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_DOK_ARCHIVNR = DOK_ARCHIVNR AND BPK_DOK_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = EFKDOK
searchResult := searchResult "LEFT JOIN DOK_DOKUMENT.DOK_DOKUMENT ON DOK_ARCHIVNR = EFK_DOK_ARCHIVNR AND DOK_DOKUMENTENNR = EFK_DOK_DOKUMENTENNR" "~"
if joinKuerzel = DOKEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_DOK_ARCHIVNR = DOK_ARCHIVNR AND EFK_DOK_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = VEBDOK
searchResult := searchResult "LEFT JOIN DOK_DOKUMENT.DOK_DOKUMENT ON DOK_ARCHIVNR = VEB_ARCHIVNR AND DOK_DOKUMENTENNR = VEB_DOKUMENTENNR" "~"
if joinKuerzel = DOKVEB
searchResult := searchResult "LEFT JOIN FIBU.VEB_VORERFASSBUCHUNG ON VEB_ARCHIVNR = DOK_ARCHIVNR AND VEB_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = VKDDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_ARCHIVNR = VKD_DOK_ARCHIVNR AND DOK_DOKUMENTENNR = VKD_DOK_DOKUMENTENNR" "~"
if joinKuerzel = DOKVKD
searchResult := searchResult "LEFT JOIN BUERO.VKD_VKOPFDOKZU ON VKD_DOK_ARCHIVNR = DOK_ARCHIVNR AND VKD_DOK_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = ZVKDOK
searchResult := searchResult "LEFT JOIN DOK_DOKUMENT.DOK_DOKUMENT ON DOK_ARCHIVNR = ZVK_ZAHLUNG_ARCHIVNR AND DOK_DOKUMENTENNR = ZVK_ZAHLUNG_DOKID" "~"
if joinKuerzel = DOKZVK
searchResult := searchResult "LEFT JOIN FIBU.ZVK_ZAHLUNGSVORSCHLAG ON ZVK_ZAHLUNG_ARCHIVNR = DOK_ARCHIVNR AND ZVK_ZAHLUNG_DOKID = DOK_DOKUMENTENNR" "~"
if joinKuerzel = DVWDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_ARCHIVNR = DVW_ARCHIVNR1 AND DOK_DOKUMENTENNR = DVW_DOKUMENTENNR1" "~"
if joinKuerzel = DOKDVW
searchResult := searchResult "LEFT JOIN BUERO.DVW_DOKUMENTENVERWEIS ON DVW_ARCHIVNR1 = DOK_ARCHIVNR AND DVW_DOKUMENTENNR1 = DOK_DOKUMENTENNR" "~"
if joinKuerzel = BLSDPA
searchResult := searchResult "LEFT JOIN DPA_DPAKET.DPA_DPAKET ON DPA_PAKETNR = BLS_PAKETNR" "~"
if joinKuerzel = DPABLS
searchResult := searchResult "LEFT JOIN BMD.BLS_BLOB_STAMM ON BLS_PAKETNR = DPA_PAKETNR" "~"
if joinKuerzel = PVYDPA
searchResult := searchResult "LEFT JOIN DPA_DPAKET.DPA_DPAKET ON DPA_PAKETNR = PVY_PAKETNR" "~"
if joinKuerzel = DPAPVY
searchResult := searchResult "LEFT JOIN BMD.PVY_PACKAGEVISIBILITY ON PVY_PAKETNR = DPA_PAKETNR" "~"
if joinKuerzel = BWMBWF
searchResult := searchResult "LEFT JOIN BUERO.BWF_BEWERTUNGFORMEL ON BWF_BWFORMELID = BWM_BWFORMELID" "~"
if joinKuerzel = BWFBWM
searchResult := searchResult "LEFT JOIN BUERO.BWM_BEWERTUNGMONAT ON BWM_BWFORMELID = BWF_BWFORMELID" "~"
if joinKuerzel = DEIBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = DEI_SW" "~"
if joinKuerzel = BZSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_SW = BZS_BNZEILEID" "~"
if joinKuerzel = DEIBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = DEI_VB_AL" "~"
if joinKuerzel = BZSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_VB_AL = BZS_BNZEILEID" "~"
if joinKuerzel = DEIBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = DEI_VB_AL1" "~"
if joinKuerzel = BZSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_VB_AL1 = BZS_BNZEILEID" "~"
if joinKuerzel = DEIBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = DEI_SONST_SV" "~"
if joinKuerzel = BZSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_SONST_SV = BZS_BNZEILEID" "~"
if joinKuerzel = DEIBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = DEI_N25H" "~"
if joinKuerzel = BZSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_N25H = BZS_BNZEILEID" "~"
if joinKuerzel = DEIBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = DEI_NEUFOEGN44" "~"
if joinKuerzel = BZSDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_NEUFOEGN44 = BZS_BNZEILEID" "~"
if joinKuerzel = AVECHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_CHARGEIDNR = AVE_CHG_CHARGEIDNR" "~"
if joinKuerzel = CHGAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_CHG_CHARGEIDNR = CHG_CHARGEIDNR" "~"
if joinKuerzel = BOPCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_CHARGEIDNR = BOP_CHG_CHARGEIDNR" "~"
if joinKuerzel = CHGBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_CHG_CHARGEIDNR = CHG_CHARGEIDNR" "~"
if joinKuerzel = KOPCHG
searchResult := searchResult "LEFT JOIN CHG_CHARGE.CHG_CHARGE ON CHG_CHARGEIDNR = KOP_CHARGEIDNR" "~"
if joinKuerzel = CHGKOP
searchResult := searchResult "LEFT JOIN PRODUKTION.KOP_KOMMISSIONIERUNG_POS ON KOP_CHARGEIDNR = CHG_CHARGEIDNR" "~"
if joinKuerzel = TRPCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_CHARGEIDNR = TRP_CHARGEIDNR" "~"
if joinKuerzel = CHGTRP
searchResult := searchResult "LEFT JOIN WWS.TRP_TRANSPORTPOSITION ON TRP_CHARGEIDNR = CHG_CHARGEIDNR" "~"
if joinKuerzel = LAJCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_CHARGEIDNR = LAJ_UMB_CHARGEIDNR" "~"
if joinKuerzel = CHGLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_UMB_CHARGEIDNR = CHG_CHARGEIDNR" "~"
if joinKuerzel = DKWCWF
searchResult := searchResult "LEFT JOIN BUERO.CWF_CRMWORKFLOW ON CWF_CWFID = DKW_CWFID" "~"
if joinKuerzel = CWFDKW
searchResult := searchResult "LEFT JOIN BUERO.DKW_DOKKATWORKFLOWZU ON DKW_CWFID = CWF_CWFID" "~"
if joinKuerzel = VASCWF
searchResult := searchResult "LEFT JOIN BUERO.CWF_CRMWORKFLOW ON CWF_CWFID = VAS_CWF_CWFID_PBEDARF" "~"
if joinKuerzel = CWFVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_CWF_CWFID_PBEDARF = CWF_CWFID" "~"
if joinKuerzel = BTFDAK
searchResult := searchResult "LEFT JOIN DAK_DATTRIBUTKATEGORIE.DAK_DATTRIBUTKATEGORIE ON DAK_ATTRKATLFDNR = BTF_ATTRKATLFDNR" "~"
if joinKuerzel = DAKBTF
searchResult := searchResult "LEFT JOIN BMD.BTF_BMDTABFELDBESCH ON BTF_ATTRKATLFDNR = DAK_ATTRKATLFDNR" "~"
if joinKuerzel = DALDBG
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DBG_DBMDGRUPPE ON DBG_BMDGRUPPENID = DAL_BMDGRUPPENID" "~"
if joinKuerzel = DBGDAL
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DAL_DATTRLOCK ON DAL_BMDGRUPPENID = DBG_BMDGRUPPENID" "~"
if joinKuerzel = ABFDBP
searchResult := searchResult "LEFT JOIN DBP_DBERECHTPAKET.DBP_DBERECHTPAKET ON DBP_BERECHTPAKETNR = ABF_BERECHTPAKETNR" "~"
if joinKuerzel = DBPABF
searchResult := searchResult "LEFT JOIN BMD.ABF_ABFRAGE ON ABF_BERECHTPAKETNR = DBP_BERECHTPAKETNR" "~"
if joinKuerzel = LENDBP
searchResult := searchResult "LEFT JOIN DBP_DBERECHTPAKET.DBP_DBERECHTPAKET ON DBP_BERECHTPAKETNR = LEN_BERECHTPAKETNR" "~"
if joinKuerzel = DBPLEN
searchResult := searchResult "LEFT JOIN BUERO.LEN_LOHNEINSTELLUNGEN ON LEN_BERECHTPAKETNR = DBP_BERECHTPAKETNR" "~"
if joinKuerzel = BGPDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = BGP_DBS_DATENBESTANDNR" "~"
if joinKuerzel = DBSBGP
searchResult := searchResult "LEFT JOIN HRS.BGP_BEITRAGSGRUPPE ON BGP_DBS_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = HINDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = HIN_DBSNR" "~"
if joinKuerzel = DBSHIN
searchResult := searchResult "LEFT JOIN HRS.HIN_HINWEIS ON HIN_DBSNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = PGLDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = PGL_DATENBESTANDNR" "~"
if joinKuerzel = DBSPGL
searchResult := searchResult "LEFT JOIN HRS.PGL_PERKZGLIEDERUNG ON PGL_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = SNIDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = SNI_DBSNR" "~"
if joinKuerzel = DBSSNI
searchResult := searchResult "LEFT JOIN HRS.SNI_SCHNITTE ON SNI_DBSNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = ZLADBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = ZLA_QUELLNR" "~"
if joinKuerzel = DBSZLA
searchResult := searchResult "LEFT JOIN ZLE.ZLA_ZEILAB ON ZLA_QUELLNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = ZTGDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = ZTG_DBSNR" "~"
if joinKuerzel = DBSZTG
searchResult := searchResult "LEFT JOIN HRS.ZTG_ZAHLTAG ON ZTG_DBSNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = FDODOF
searchResult := searchResult "LEFT JOIN FIBU.DOF_FIBUDOKUMENT ON DOF_FIRMENNR = FDO_FIRMENNR AND DOF_FIBUNR = FDO_FIBUNR AND DOF_FIBUDOKID = FDO_FIBUDOKID" "~"
if joinKuerzel = DOFFDO
searchResult := searchResult "LEFT JOIN FIBU.FDO_FIBUDOKUMENTZU ON FDO_FIRMENNR = DOF_FIRMENNR AND FDO_FIBUNR = DOF_FIBUNR AND FDO_FIBUDOKID = DOF_FIBUDOKID" "~"
if joinKuerzel = DKVDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_ARCHIVNR = DKV_ARCHIVNR AND DOK_DOKUMENTENNR = DKV_DOKUMENTENNR" "~"
if joinKuerzel = DOKDKV
searchResult := searchResult "LEFT JOIN BUERO.DKV_DOKUMENTVERSION ON DKV_ARCHIVNR = DOK_ARCHIVNR AND DKV_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = HFTDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_ARCHIVNR = HFT_DOK_ARCHIVNR AND DOK_DOKUMENTENNR = HFT_DOK_DOKUMENTENNR" "~"
if joinKuerzel = DOKHFT
searchResult := searchResult "LEFT JOIN BUERO.HFT_HILFSTABELLE ON HFT_DOK_ARCHIVNR = DOK_ARCHIVNR AND HFT_DOK_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = KDZDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_ARCHIVNR = KDZ_DOK_ARCHIVNR AND DOK_DOKUMENTENNR = KDZ_DOK_DOKUMENTENNR" "~"
if joinKuerzel = DOKKDZ
searchResult := searchResult "LEFT JOIN BUERO.KDZ_KOREDOKUMENTZU ON KDZ_DOK_ARCHIVNR = DOK_ARCHIVNR AND KDZ_DOK_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = LDODOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_ARCHIVNR = LDO_ARCHIVNR AND DOK_DOKUMENTENNR = LDO_DOKUMENTENNR" "~"
if joinKuerzel = DOKLDO
searchResult := searchResult "LEFT JOIN BUERO.LDO_LEBENSLAUFDOKUMENTZU ON LDO_ARCHIVNR = DOK_ARCHIVNR AND LDO_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = SAZDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_ARCHIVNR = SAZ_DOK_ARCHIVNR AND DOK_DOKUMENTENNR = SAZ_DOK_DOKUMENTENNR" "~"
if joinKuerzel = DOKSAZ
searchResult := searchResult "LEFT JOIN BUERO.SAZ_SACHKONTOARCHIVZU ON SAZ_DOK_ARCHIVNR = DOK_ARCHIVNR AND SAZ_DOK_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = TEDDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_ARCHIVNR = TED_ARCHIVNR AND DOK_DOKUMENTENNR = TED_DOKUMENTENNR" "~"
if joinKuerzel = DOKTED
searchResult := searchResult "LEFT JOIN BUERO.TED_TERMINDOKUMENTZU ON TED_ARCHIVNR = DOK_ARCHIVNR AND TED_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = BAIDRS
searchResult := searchResult "LEFT JOIN DRS_DRUCKEREINSTELLUNG.DRS_DRUCKEREINSTELLUNG ON DRS_DREINSTELLUNGSID = BAI_SI_DREINSTELLUNGSID" "~"
if joinKuerzel = DRSBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_SI_DREINSTELLUNGSID = DRS_DREINSTELLUNGSID" "~"
if joinKuerzel = ZRTDRS
searchResult := searchResult "LEFT JOIN DRS_DRUCKEREINSTELLUNG.DRS_DRUCKEREINSTELLUNG ON DRS_DREINSTELLUNGSID = ZRT_DREINSTELLUNGSID" "~"
if joinKuerzel = DRSZRT
searchResult := searchResult "LEFT JOIN BUERO.ZRT_ZAHLUNGSART ON ZRT_DREINSTELLUNGSID = DRS_DREINSTELLUNGSID" "~"
if joinKuerzel = DELDSV
searchResult := searchResult "LEFT JOIN BUERO.DSV_DSGVO_VERZEICHNIS ON DSV_FIRMENNR = DEL_DSV_FIRMENNR AND DSV_EINTRAGNR = DEL_EINTRAGNR" "~"
if joinKuerzel = DSVDEL
searchResult := searchResult "LEFT JOIN BUERO.DEL_DELETE ON DEL_DSV_FIRMENNR = DSV_FIRMENNR AND DEL_EINTRAGNR = DSV_EINTRAGNR" "~"
if joinKuerzel = ABFDTA
searchResult := searchResult "LEFT JOIN DTA_DBMDTABELLE.DTA_DBMDTABELLE ON DTA_TABELLENNAME = ABF_DOGTABELLENNAME" "~"
if joinKuerzel = DTAABF
searchResult := searchResult "LEFT JOIN BMD.ABF_ABFRAGE ON ABF_DOGTABELLENNAME = DTA_TABELLENNAME" "~"
if joinKuerzel = BTFDTA
searchResult := searchResult "LEFT JOIN DTA_DBMDTABELLE.DTA_DBMDTABELLE ON DTA_TABELLENNAME = BTF_TABELLENNAME" "~"
if joinKuerzel = DTABTF
searchResult := searchResult "LEFT JOIN BMD.BTF_BMDTABFELDBESCH ON BTF_TABELLENNAME = DTA_TABELLENNAME" "~"
if joinKuerzel = DRTDTA
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DTA_DBMDTABELLE ON DTA_DTAID = DRT_DTAID" "~"
if joinKuerzel = DTADRT
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DRT_DTABREPLGTABELLEZU ON DRT_DTAID = DTA_DTAID" "~"
if joinKuerzel = DTPDTH
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DTH_DEFTREEHEAD ON DTH_BAUMID = DTP_BAUMID" "~"
if joinKuerzel = DTHDTP
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DTP_DEFTREEPOS ON DTP_BAUMID = DTH_BAUMID" "~"
if joinKuerzel = BZVDVH
searchResult := searchResult "LEFT JOIN DVH_DIENSTVERHAELTNIS.DVH_DIENSTVERHAELTNIS ON DVH_MIT_FIRMENNR = BZV_FIRMENNR AND DVH_MITARBEITERID = BZV_MITARBEITERID AND DVH_DIENSTVLFDNR = BZV_DIENSTVLFDNR" "~"
if joinKuerzel = DVHBZV
searchResult := searchResult "LEFT JOIN HRS.BZV_BUAK_ZUSCHLAGSVERRECHN ON BZV_FIRMENNR = DVH_MIT_FIRMENNR AND BZV_MITARBEITERID = DVH_MITARBEITERID AND BZV_DIENSTVLFDNR = DVH_DIENSTVLFDNR" "~"
if joinKuerzel = HAKDVH
searchResult := searchResult "LEFT JOIN DVH_DIENSTVERHAELTNIS.DVH_DIENSTVERHAELTNIS ON DVH_MIT_FIRMENNR = HAK_MIT_FIRMENNR AND DVH_MITARBEITERID = HAK_MITARBEITERID AND DVH_DIENSTVLFDNR = HAK_DIENSTVLFDNR" "~"
if joinKuerzel = DVHHAK
searchResult := searchResult "LEFT JOIN HRS.HAK_AKONTO ON HAK_MIT_FIRMENNR = DVH_MIT_FIRMENNR AND HAK_MITARBEITERID = DVH_MITARBEITERID AND HAK_DIENSTVLFDNR = DVH_DIENSTVLFDNR" "~"
if joinKuerzel = SWSDVH
searchResult := searchResult "LEFT JOIN DVH_DIENSTVERHAELTNIS.DVH_DIENSTVERHAELTNIS ON DVH_MIT_FIRMENNR = SWS_FIRMENNR AND DVH_MITARBEITERID = SWS_MITARBEITERID AND DVH_DIENSTVLFDNR = SWS_DIENSTVLFDNR" "~"
if joinKuerzel = DVHSWS
searchResult := searchResult "LEFT JOIN HRS.SWS_SW_STUNDEN ON SWS_FIRMENNR = DVH_MIT_FIRMENNR AND SWS_MITARBEITERID = DVH_MITARBEITERID AND SWS_DIENSTVLFDNR = DVH_DIENSTVLFDNR" "~"
if joinKuerzel = DUZDWU
searchResult := searchResult "LEFT JOIN DATANORM.DWU_WAREN_UNTER_GRUPPE ON DWU_WH_GRP = DUZ_WH_GRP AND DWU_WU_GRP = DUZ_WU_GRP AND DWU_ERST_NR = DUZ_ERST_NR" "~"
if joinKuerzel = DWUDUZ
searchResult := searchResult "LEFT JOIN DATANORM.DUZ_DATANORM_WUGZU ON DUZ_WH_GRP = DWU_WH_GRP AND DUZ_WU_GRP = DWU_WU_GRP AND DUZ_ERST_NR = DWU_ERST_NR" "~"
if joinKuerzel = EAPEAK
searchResult := searchResult "LEFT JOIN WWS.EAK_ERLOESABGRENZUNGSKOPF ON EAK_ERLOESABGKOPFID = EAP_ERLOESABGKOPFID" "~"
if joinKuerzel = EAKEAP
searchResult := searchResult "LEFT JOIN WWS.EAP_ERLOESABGRENZUNGSPOS ON EAP_ERLOESABGKOPFID = EAK_ERLOESABGKOPFID" "~"
if joinKuerzel = WEGEBP
searchResult := searchResult "LEFT JOIN EBP_BESTELLPOSITION.EBP_BESTELLPOSITION ON EBP_FIRMENNR = WEG_EBP_FIRMENNR AND EBP_FIBUNR = WEG_EBP_FIBUNR AND EBP_ERFASSNR = WEG_EBP_ERFASSNR AND EBP_EBPNR = WEG_EBP_EBPNR" "~"
if joinKuerzel = EBPWEG
searchResult := searchResult "LEFT JOIN WWS.WEG_WWSERKONTROLLEGRP ON WEG_EBP_FIRMENNR = EBP_FIRMENNR AND WEG_EBP_FIBUNR = EBP_FIBUNR AND WEG_EBP_ERFASSNR = EBP_ERFASSNR AND WEG_EBP_EBPNR = EBP_EBPNR" "~"
if joinKuerzel = DTUDFG
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFG_DFELDBESCHGRUPPE ON DFG_HAUPTGRUPPE = DTU_HAUPTGRUPPE AND DFG_UNTERGRUPPE = DTU_UNTERGRUPPE" "~"
if joinKuerzel = DFGDTU
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DTU_DTABUEBERS ON DTU_HAUPTGRUPPE = DFG_HAUPTGRUPPE AND DTU_UNTERGRUPPE = DFG_UNTERGRUPPE" "~"
if joinKuerzel = FEZDFG
searchResult := searchResult "LEFT JOIN DFG_DFELDBESCHGRUPPE.DFG_DFELDBESCHGRUPPE ON DFG_HAUPTGRUPPE = FEZ_HAUPTGRUPPE AND DFG_UNTERGRUPPE = FEZ_UNTERGRUPPE" "~"
if joinKuerzel = DFGFEZ
searchResult := searchResult "LEFT JOIN BMD.FEZ_FELDGRUPPENZU ON FEZ_HAUPTGRUPPE = DFG_HAUPTGRUPPE AND FEZ_UNTERGRUPPE = DFG_UNTERGRUPPE" "~"
if joinKuerzel = UAZDFG
searchResult := searchResult "LEFT JOIN DFG_DFELDBESCHGRUPPE.DFG_DFELDBESCHGRUPPE ON DFG_HAUPTGRUPPE = UAZ_HAUPTGRUPPE AND DFG_UNTERGRUPPE = UAZ_UNTERGRUPPE" "~"
if joinKuerzel = DFGUAZ
searchResult := searchResult "LEFT JOIN BMDENTW.UAZ_USERLINKAUSLIEFERNZU ON UAZ_HAUPTGRUPPE = DFG_HAUPTGRUPPE AND UAZ_UNTERGRUPPE = DFG_UNTERGRUPPE" "~"
if joinKuerzel = CABDFU
searchResult := searchResult "LEFT JOIN DFU_DFUNKTION.DFU_DFUNKTION ON DFU_FUNKTIONSNR = CAB_FUNKTIONSNR" "~"
if joinKuerzel = DFUCAB
searchResult := searchResult "LEFT JOIN BUERO.CAB_CRMABWBERECHTZU ON CAB_FUNKTIONSNR = DFU_FUNKTIONSNR" "~"
if joinKuerzel = DIXDFU
searchResult := searchResult "LEFT JOIN DFU_DFUNKTION.DFU_DFUNKTION ON DFU_FUNKTIONSNR = DIX_FUNKTIONSNR" "~"
if joinKuerzel = DFUDIX
searchResult := searchResult "LEFT JOIN BMD.DIX_DIAGRAMMAKTION ON DIX_FUNKTIONSNR = DFU_FUNKTIONSNR" "~"
if joinKuerzel = FFRDFU
searchResult := searchResult "LEFT JOIN DFU_DFUNKTION.DFU_DFUNKTION ON DFU_FUNKTIONSNR = FFR_FUNKTIONSNR" "~"
if joinKuerzel = DFUFFR
searchResult := searchResult "LEFT JOIN BMD.FFR_FUNKFIRMENRECHTE ON FFR_FUNKTIONSNR = DFU_FUNKTIONSNR" "~"
if joinKuerzel = LSDDFU
searchResult := searchResult "LEFT JOIN DFU_DFUNKTION.DFU_DFUNKTION ON DFU_BMDCONSTID = LSD_PROGRAM" "~"
if joinKuerzel = DFULSD
searchResult := searchResult "LEFT JOIN HRS.LSD_LOHNSTAPEL_DETAIL ON LSD_PROGRAM = DFU_BMDCONSTID" "~"
if joinKuerzel = TASDFU
searchResult := searchResult "LEFT JOIN DFU_DFUNKTION.DFU_DFUNKTION ON DFU_BMDCONSTID = TAS_BMDCONSTID" "~"
if joinKuerzel = DFUTAS
searchResult := searchResult "LEFT JOIN BMD.TAS_ACTIONSTACK1_TMP ON TAS_BMDCONSTID = DFU_BMDCONSTID" "~"
if joinKuerzel = UMADFU
searchResult := searchResult "LEFT JOIN DFU_DFUNKTION.DFU_DFUNKTION ON DFU_FUNKTIONSNR = UMA_FUNKTIONSNR" "~"
if joinKuerzel = DFUUMA
searchResult := searchResult "LEFT JOIN BMD.UMA_USERMATCHCODE ON UMA_FUNKTIONSNR = DFU_FUNKTIONSNR" "~"
if joinKuerzel = WIFDFU
searchResult := searchResult "LEFT JOIN DFU_DFUNKTION.DFU_DFUNKTION ON DFU_FUNKTIONSNR = WIF_FUNKTIONSNR" "~"
if joinKuerzel = DFUWIF
searchResult := searchResult "LEFT JOIN BMDENTW.WIF_WII_DFU_ZU ON WIF_FUNKTIONSNR = DFU_FUNKTIONSNR" "~"
if joinKuerzel = DIXDIA
searchResult := searchResult "LEFT JOIN BMD.DIA_DIAGRAMM ON DIA_DIAGRAMMNR = DIX_DIAGRAMMNR" "~"
if joinKuerzel = DIADIX
searchResult := searchResult "LEFT JOIN BMD.DIX_DIAGRAMMAKTION ON DIX_DIAGRAMMNR = DIA_DIAGRAMMNR" "~"
if joinKuerzel = ERWDIA
searchResult := searchResult "LEFT JOIN DIA_DIAGRAMM.DIA_DIAGRAMM ON DIA_DIAGRAMMNR = ERW_DIAGRAMMNR" "~"
if joinKuerzel = DIAERW
searchResult := searchResult "LEFT JOIN FIBU.ERW_WORKFLOW ON ERW_DIAGRAMMNR = DIA_DIAGRAMMNR" "~"
if joinKuerzel = BSKDIB
searchResult := searchResult "LEFT JOIN DIB_DIAGRAMMINSTANZBASIS.DIB_DIAGRAMMINSTANZBASIS ON DIB_DIAGRAMMNR = BSK_DIAGRAMMNR AND DIB_DIAGRAMMINSTANZNR = BSK_DIAGRAMMINSTANZNR" "~"
if joinKuerzel = DIBBSK
searchResult := searchResult "LEFT JOIN WWS.BSK_BONUSERMKOPF ON BSK_DIAGRAMMNR = DIB_DIAGRAMMNR AND BSK_DIAGRAMMINSTANZNR = DIB_DIAGRAMMINSTANZNR" "~"
if joinKuerzel = REIDIB
searchResult := searchResult "LEFT JOIN DIB_DIAGRAMMINSTANZBASIS.DIB_DIAGRAMMINSTANZBASIS ON DIB_DIAGRAMMNR = REI_DIAGRAMMNR AND DIB_DIAGRAMMINSTANZNR = REI_DIAGRAMMINSTANZNR" "~"
if joinKuerzel = DIBREI
searchResult := searchResult "LEFT JOIN HRS.REI_REISESTAMM ON REI_DIAGRAMMNR = DIB_DIAGRAMMNR AND REI_DIAGRAMMINSTANZNR = DIB_DIAGRAMMINSTANZNR" "~"
if joinKuerzel = TERDIB
searchResult := searchResult "LEFT JOIN DIB_DIAGRAMMINSTANZBASIS.DIB_DIAGRAMMINSTANZBASIS ON DIB_DIAGRAMMNR = TER_DIAGRAMMNR AND DIB_DIAGRAMMINSTANZNR = TER_DIAGRAMMINSTANZNR" "~"
if joinKuerzel = DIBTER
searchResult := searchResult "LEFT JOIN BUERO.TER_TERMIN ON TER_DIAGRAMMNR = DIB_DIAGRAMMNR AND TER_DIAGRAMMINSTANZNR = DIB_DIAGRAMMINSTANZNR" "~"
if joinKuerzel = POZDII
searchResult := searchResult "LEFT JOIN DII_DIAGRAMMINSTANZ.DII_DIAGRAMMINSTANZ ON DII_DIAGRAMMINSTANZNR = POZ_DII_DIAGRAMMINSTANZNR AND DII_DIAGRAMMNR = POZ_DII_DIAGRAMMNR AND DII_AKTIONSNR = POZ_DII_AKTIONSNR" "~"
if joinKuerzel = DIIPOZ
searchResult := searchResult "LEFT JOIN BUERO.POZ_PISWORKFLOWZU ON POZ_DII_DIAGRAMMINSTANZNR = DII_DIAGRAMMINSTANZNR AND POZ_DII_DIAGRAMMNR = DII_DIAGRAMMNR AND POZ_DII_AKTIONSNR = DII_AKTIONSNR" "~"
if joinKuerzel = AUGDIX
searchResult := searchResult "LEFT JOIN DIX_DIAGRAMMAKTION.DIX_DIAGRAMMAKTION ON DIX_DIAGRAMMNR = AUG_DIAGRAMMNR AND DIX_AKTIONSNR = AUG_AKTIONSNR" "~"
if joinKuerzel = DIXAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_DIAGRAMMNR = DIX_DIAGRAMMNR AND AUG_AKTIONSNR = DIX_AKTIONSNR" "~"
if joinKuerzel = ADZDLN
searchResult := searchResult "LEFT JOIN DLN_DIENSTLEISTUNGSNUMMER.DLN_DIENSTLEISTUNGSNUMMER ON DLN_LANDNR = ADZ_DLN_LANDNR AND DLN_DLCODE = ADZ_DLN_DLCODE" "~"
if joinKuerzel = DLNADZ
searchResult := searchResult "LEFT JOIN WWS.ADZ_ARTIKELDIENSTLNRZU ON ADZ_DLN_LANDNR = DLN_LANDNR AND ADZ_DLN_DLCODE = DLN_DLCODE" "~"
if joinKuerzel = DTMDNE
searchResult := searchResult "LEFT JOIN DATANORM.DNE_ERSTELLER ON DNE_ERST_NR = DTM_ERST_NR" "~"
if joinKuerzel = DNEDTM
searchResult := searchResult "LEFT JOIN DATANORM.DTM_TEXT ON DTM_ERST_NR = DNE_ERST_NR" "~"
if joinKuerzel = PP_DOK
searchResult := searchResult "LEFT JOIN DOK_DOKUMENT.DOK_DOKUMENT ON DOK_ARCHIVNR = ARCHIVNR AND DOK_DOKUMENTENNR = DOKUMENTENNR" "~"
if joinKuerzel = DOKPP_
searchResult := searchResult "LEFT JOIN PRODUKTION.PP_ARTIKEL ON ARCHIVNR = DOK_ARCHIVNR AND DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = ERBDOK
searchResult := searchResult "LEFT JOIN DOK_DOKUMENT.DOK_DOKUMENT ON DOK_ARCHIVNR = ERB_DOK_ARCHIVNR AND DOK_DOKUMENTENNR = ERB_DOK_DOKUMENTENNR" "~"
if joinKuerzel = DOKERB
searchResult := searchResult "LEFT JOIN FIBU.ERB_BUCHUNG ON ERB_DOK_ARCHIVNR = DOK_ARCHIVNR AND ERB_DOK_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = WDADIA
searchResult := searchResult "LEFT JOIN DIA_DIAGRAMM.DIA_DIAGRAMM ON DIA_DIAGRAMMNR = WDA_DIA_DIAGRAMMNR_MAIN" "~"
if joinKuerzel = DIAWDA
searchResult := searchResult "LEFT JOIN BUERO.WDA_WFDAGRAMMZU ON WDA_DIA_DIAGRAMMNR_MAIN = DIA_DIAGRAMMNR" "~"
if joinKuerzel = EDKDIP
searchResult := searchResult "LEFT JOIN DIP_DIVERSE.DIP_DIVERSE ON DIP_FIRMENNR = EDK_DIP_FIRMENNR AND DIP_EXTPERSID = EDK_PERSONENID" "~"
if joinKuerzel = DIPEDK
searchResult := searchResult "LEFT JOIN HRS.EDK_ERWDIVKOERPER ON EDK_DIP_FIRMENNR = DIP_FIRMENNR AND EDK_PERSONENID = DIP_EXTPERSID" "~"
if joinKuerzel = ERZDIP
searchResult := searchResult "LEFT JOIN DIP_DIVERSE.DIP_DIVERSE ON DIP_FIRMENNR = ERZ_SPEDITION_FIRMENNR AND DIP_EXTPERSID = ERZ_SPEDITION_EXTPERSID" "~"
if joinKuerzel = DIPERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_SPEDITION_FIRMENNR = DIP_FIRMENNR AND ERZ_SPEDITION_EXTPERSID = DIP_EXTPERSID" "~"
if joinKuerzel = PCKDKQ
searchResult := searchResult "LEFT JOIN DKQ_DOKUMENTZUID.DKQ_DOKUMENTZUID ON DKQ_DKZID = PCK_DKZID" "~"
if joinKuerzel = DKQPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_DKZID = DKQ_DKZID" "~"
if joinKuerzel = RIADKQ
searchResult := searchResult "LEFT JOIN DKQ_DOKUMENTZUID.DKQ_DOKUMENTZUID ON DKQ_DKZID = RIA_DKZID" "~"
if joinKuerzel = DKQRIA
searchResult := searchResult "LEFT JOIN PRODUKTION.RIA_RISIKOANALYSE ON RIA_DKZID = DKQ_DKZID" "~"
if joinKuerzel = ADDDKT
searchResult := searchResult "LEFT JOIN BUERO.DKT_DOCSKATEGORIE ON DKT_ARCHIVNR = ADD_ARCHIVNR AND DKT_DOCSKATEGORIEKZ = ADD_DOCSKATEGORIEKZ AND DKT_DOCSKATEGORIELFDNR = ADD_DOCSKATEGORIELFDNR" "~"
if joinKuerzel = DKTADD
searchResult := searchResult "LEFT JOIN BUERO.ADD_AUSDRUCKMODELLDETAIL ON ADD_ARCHIVNR = DKT_ARCHIVNR AND ADD_DOCSKATEGORIEKZ = DKT_DOCSKATEGORIEKZ AND ADD_DOCSKATEGORIELFDNR = DKT_DOCSKATEGORIELFDNR" "~"
if joinKuerzel = FBPDLG
searchResult := searchResult "LEFT JOIN DLG_DLIZENZGRUPPE.DLG_DLIZENZGRUPPE ON DLG_DLGID = FBP_DLGID" "~"
if joinKuerzel = DLGFBP
searchResult := searchResult "LEFT JOIN BMD.FBP_FELDBESCHPOS ON FBP_DLGID = DLG_DLGID" "~"
if joinKuerzel = DWUDNE
searchResult := searchResult "LEFT JOIN DATANORM.DNE_ERSTELLER ON DNE_ERST_NR = DWU_ERST_NR" "~"
if joinKuerzel = DNEDWU
searchResult := searchResult "LEFT JOIN DATANORM.DWU_WAREN_UNTER_GRUPPE ON DWU_ERST_NR = DNE_ERST_NR" "~"
if joinKuerzel = LGMDNA
searchResult := searchResult "LEFT JOIN DATANORM.DNA_ARTIKEL ON DNA_ERST_NR = LGM_ERST_NR AND DNA_ART_NR = LGM_ART_NR" "~"
if joinKuerzel = DNALGM
searchResult := searchResult "LEFT JOIN DATANORM.LGM_LEISTUNG ON LGM_ERST_NR = DNA_ERST_NR AND LGM_ART_NR = DNA_ART_NR" "~"
if joinKuerzel = ABMDNZ
searchResult := searchResult "LEFT JOIN HRS.DNZ_DIVNICHTLEISTUNGSZEIT ON DNZ_DIVNLZID = ABM_DIVNLZID" "~"
if joinKuerzel = DNZABM
searchResult := searchResult "LEFT JOIN HRS.ABM_ABWESENHEIT ON ABM_DIVNLZID = DNZ_DIVNLZID" "~"
if joinKuerzel = TBEDOG
searchResult := searchResult "LEFT JOIN BMD.DOG_DATAOWNERGRUPPE ON DOG_DATAOWNERGRUPPENNR = TBE_DATAOWNERGRUPPENR" "~"
if joinKuerzel = DOGTBE
searchResult := searchResult "LEFT JOIN BMD.TBE_TEMPBERECHTIGUNG ON TBE_DATAOWNERGRUPPENR = DOG_DATAOWNERGRUPPENNR" "~"
if joinKuerzel = DOVDOG
searchResult := searchResult "LEFT JOIN BMD.DOG_DATAOWNERGRUPPE ON DOG_DATAOWNERGRUPPENNR = DOV_SHOW_VOR_DOGRUPPENNR" "~"
if joinKuerzel = DOGDOV
searchResult := searchResult "LEFT JOIN BMD.DOV_DOGVORSCHLAG ON DOV_SHOW_VOR_DOGRUPPENNR = DOG_DATAOWNERGRUPPENNR" "~"
if joinKuerzel = AVDDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_ARCHIVNR = AVD_ARCHIVNR AND DOK_DOKUMENTENNR = AVD_DOKUMENTENNR" "~"
if joinKuerzel = DOKAVD
searchResult := searchResult "LEFT JOIN BUERO.AVD_AUSBVERWDOKUMENTZU ON AVD_ARCHIVNR = DOK_ARCHIVNR AND AVD_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = DAGDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_ARCHIVNR = DAG_DOK_ARCHIVNR AND DOK_DOKUMENTENNR = DAG_DOK_DOKUMENTENNR" "~"
if joinKuerzel = DOKDAG
searchResult := searchResult "LEFT JOIN BUERO.DAG_DOKANLAGEGUTZU ON DAG_DOK_ARCHIVNR = DOK_ARCHIVNR AND DAG_DOK_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = MAGDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_ARCHIVNR = MAG_DOK_ARCHIVNR AND DOK_DOKUMENTENNR = MAG_DOKUMENTENNR" "~"
if joinKuerzel = DOKMAG
searchResult := searchResult "LEFT JOIN BUERO.MAG_MITARBGESPR ON MAG_DOK_ARCHIVNR = DOK_ARCHIVNR AND MAG_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = RNGDOK
searchResult := searchResult "LEFT JOIN DOK_DOKUMENT.DOK_DOKUMENT ON DOK_ARCHIVNR = RNG_DOK_ARCHIVNR AND DOK_DOKUMENTENNR = RNG_DOK_DOKUMENTENNR" "~"
if joinKuerzel = DOKRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_DOK_ARCHIVNR = DOK_ARCHIVNR AND RNG_DOK_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = TPDDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_ARCHIVNR = TPD_ARCHIVNR AND DOK_DOKUMENTENNR = TPD_DOKUMENTENNR" "~"
if joinKuerzel = DOKTPD
searchResult := searchResult "LEFT JOIN BUERO.TPD_TELPROTDOKUMENTZU ON TPD_ARCHIVNR = DOK_ARCHIVNR AND TPD_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = WAKDOK
searchResult := searchResult "LEFT JOIN DOK_DOKUMENT.DOK_DOKUMENT ON DOK_ARCHIVNR = WAK_DOK_ARCHIVNR AND DOK_DOKUMENTENNR = WAK_DOK_DOKUMENTENNR" "~"
if joinKuerzel = DOKWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_DOK_ARCHIVNR = DOK_ARCHIVNR AND WAK_DOK_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = DFRDRS
searchResult := searchResult "LEFT JOIN DRS_DRUCKEREINSTELLUNG.DRS_DRUCKEREINSTELLUNG ON DRS_DREINSTELLUNGSID = DFR_DREINSTELLUNGSID" "~"
if joinKuerzel = DRSDFR
searchResult := searchResult "LEFT JOIN WWS.DFR_DOKUMENTFREIGABE ON DFR_DREINSTELLUNGSID = DRS_DREINSTELLUNGSID" "~"
if joinKuerzel = KASDRS
searchResult := searchResult "LEFT JOIN DRS_DRUCKEREINSTELLUNG.DRS_DRUCKEREINSTELLUNG ON DRS_DREINSTELLUNGSID = KAS_BELEG_DRSID" "~"
if joinKuerzel = DRSKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_BELEG_DRSID = DRS_DREINSTELLUNGSID" "~"
if joinKuerzel = LSPDRS
searchResult := searchResult "LEFT JOIN DRS_DRUCKEREINSTELLUNG.DRS_DRUCKEREINSTELLUNG ON DRS_DREINSTELLUNGSID = LSP_DRUCK_SETT" "~"
if joinKuerzel = DRSLSP
searchResult := searchResult "LEFT JOIN HRS.LSP_LOHNSTAPEL ON LSP_DRUCK_SETT = DRS_DREINSTELLUNGSID" "~"
if joinKuerzel = DWZDIH
searchResult := searchResult "LEFT JOIN DIH_DIAGRAMMINSTANZKOPF.DIH_DIAGRAMMINSTANZKOPF ON DIH_DIAGRAMMNR = DWZ_WFDIAGRAMMNR AND DIH_INSTANZNR = DWZ_WFINSTANZNR" "~"
if joinKuerzel = DIHDWZ
searchResult := searchResult "LEFT JOIN BUERO.DWZ_DOKUMENTWORKFLOWZU ON DWZ_WFDIAGRAMMNR = DIH_DIAGRAMMNR AND DWZ_WFINSTANZNR = DIH_INSTANZNR" "~"
if joinKuerzel = DIDDIM
searchResult := searchResult "LEFT JOIN DATANORM.DIM_DIM_TEXT ON DIM_ERST_NR = DID_ERST_NR AND DIM_ART_NR = DID_ART_NR" "~"
if joinKuerzel = DIMDID
searchResult := searchResult "LEFT JOIN DATANORM.DID_DIM_TEXT_DETAIL ON DID_ERST_NR = DIM_ERST_NR AND DID_ART_NR = DIM_ART_NR" "~"
if joinKuerzel = EFKDIP
searchResult := searchResult "LEFT JOIN DIP_DIVERSE.DIP_DIVERSE ON DIP_FIRMENNR = EFK_SPEDITION_FIRMENNR AND DIP_EXTPERSID = EFK_SPEDITION_EXTPERSID" "~"
if joinKuerzel = DIPEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_SPEDITION_FIRMENNR = DIP_FIRMENNR AND EFK_SPEDITION_EXTPERSID = DIP_EXTPERSID" "~"
if joinKuerzel = SUDDIP
searchResult := searchResult "LEFT JOIN DIP_DIVERSE.DIP_DIVERSE ON DIP_FIRMENNR = SUD_FIRMENNR AND DIP_EXTPERSID = SUD_BETRIEBSRAT_PERSID" "~"
if joinKuerzel = DIPSUD
searchResult := searchResult "LEFT JOIN HRS.SUD_SUM_KOE_DIFF ON SUD_FIRMENNR = DIP_FIRMENNR AND SUD_BETRIEBSRAT_PERSID = DIP_EXTPERSID" "~"
if joinKuerzel = TREDIP
searchResult := searchResult "LEFT JOIN DIP_DIVERSE.DIP_DIVERSE ON DIP_FIRMENNR = TRE_SPEDITION_FIRMENNR AND DIP_EXTPERSID = TRE_SPEDITION_EXTPERSID" "~"
if joinKuerzel = DIPTRE
searchResult := searchResult "LEFT JOIN WWS.TRE_TRANSPORTEINHEIT ON TRE_SPEDITION_FIRMENNR = DIP_FIRMENNR AND TRE_SPEDITION_EXTPERSID = DIP_EXTPERSID" "~"
if joinKuerzel = LFSDIP
searchResult := searchResult "LEFT JOIN DIP_DIVERSE.DIP_DIVERSE ON DIP_FIRMENNR = LFS_FRAECHTER_FIRMENNR AND DIP_EXTPERSID = LFS_FRAECHTER_EXTPERSID" "~"
if joinKuerzel = DIPLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_FRAECHTER_FIRMENNR = DIP_FIRMENNR AND LFS_FRAECHTER_EXTPERSID = DIP_EXTPERSID" "~"
if joinKuerzel = WFNDIX
searchResult := searchResult "LEFT JOIN DIX_DIAGRAMMAKTION.DIX_DIAGRAMMAKTION ON DIX_DIAGRAMMNR = WFN_DIX_DIAGRAMMNR AND DIX_AKTIONSNR = WFN_AKTIONSNR" "~"
if joinKuerzel = DIXWFN
searchResult := searchResult "LEFT JOIN BUERO.WFN_WORKFLOWACTION ON WFN_DIX_DIAGRAMMNR = DIX_DIAGRAMMNR AND WFN_AKTIONSNR = DIX_AKTIONSNR" "~"
if joinKuerzel = APADKQ
searchResult := searchResult "LEFT JOIN DKQ_DOKUMENTZUID.DKQ_DOKUMENTZUID ON DKQ_DKZID = APA_DKZID" "~"
if joinKuerzel = DKQAPA
searchResult := searchResult "LEFT JOIN PRODUKTION.APA_ARBEITSPLAN ON APA_DKZID = DKQ_DKZID" "~"
if joinKuerzel = DKADKT
searchResult := searchResult "LEFT JOIN BUERO.DKT_DOCSKATEGORIE ON DKT_ARCHIVNR = DKA_DKT_ARCHIVNR AND DKT_DOCSKATEGORIEKZ = DKA_DKT_DOCSKATEGORIEKZ AND DKT_DOCSKATEGORIELFDNR = DKA_DKT_DOCSKATEGORIELFDNR" "~"
if joinKuerzel = DKTDKA
searchResult := searchResult "LEFT JOIN BUERO.DKA_DOCSKATEGORIEZU ON DKA_DKT_ARCHIVNR = DKT_ARCHIVNR AND DKA_DKT_DOCSKATEGORIEKZ = DKT_DOCSKATEGORIEKZ AND DKA_DKT_DOCSKATEGORIELFDNR = DKT_DOCSKATEGORIELFDNR" "~"
if joinKuerzel = RGMDNE
searchResult := searchResult "LEFT JOIN DATANORM.DNE_ERSTELLER ON DNE_ERST_NR = RGM_ERST_NR" "~"
if joinKuerzel = DNERGM
searchResult := searchResult "LEFT JOIN DATANORM.RGM_RABATT_GRUPPE ON RGM_ERST_NR = DNE_ERST_NR" "~"
if joinKuerzel = AOZDOK
searchResult := searchResult "LEFT JOIN DOK_DOKUMENT.DOK_DOKUMENT ON DOK_ARCHIVNR = AOZ_DOK_ARCHIVNR AND DOK_DOKUMENTENNR = AOZ_DOK_DOKUMENTENNR" "~"
if joinKuerzel = DOKAOZ
searchResult := searchResult "LEFT JOIN WWS.AOZ_ARTIKELDOKUMENTZU ON AOZ_DOK_ARCHIVNR = DOK_ARCHIVNR AND AOZ_DOK_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = DVWDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_ARCHIVNR = DVW_ARCHIVNR2 AND DOK_DOKUMENTENNR = DVW_DOKUMENTENNR2" "~"
if joinKuerzel = DOKDVW
searchResult := searchResult "LEFT JOIN BUERO.DVW_DOKUMENTENVERWEIS ON DVW_ARCHIVNR2 = DOK_ARCHIVNR AND DVW_DOKUMENTENNR2 = DOK_DOKUMENTENNR" "~"
if joinKuerzel = DZUDOK
searchResult := searchResult "LEFT JOIN DOK_DOKUMENT.DOK_DOKUMENT ON DOK_ARCHIVNR = DZU_ARCHIVNR AND DOK_DOKUMENTENNR = DZU_DOKUMENTENNR" "~"
if joinKuerzel = DOKDZU
searchResult := searchResult "LEFT JOIN HRS.DZU_DOKZU_HRS ON DZU_ARCHIVNR = DOK_ARCHIVNR AND DZU_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = FORDPA
searchResult := searchResult "LEFT JOIN DPA_DPAKET.DPA_DPAKET ON DPA_PAKETNR = FOR_PAKETNR" "~"
if joinKuerzel = DPAFOR
searchResult := searchResult "LEFT JOIN BMD.FOR_FORMEL ON FOR_PAKETNR = DPA_PAKETNR" "~"
if joinKuerzel = DPDDPO
searchResult := searchResult "LEFT JOIN BMD.DPO_DOCUMENTPOOL ON DPO_DPOID = DPD_DPOID" "~"
if joinKuerzel = DPODPD
searchResult := searchResult "LEFT JOIN BMD.DPD_DOCUMENTPOOLDETAIL ON DPD_DPOID = DPO_DPOID" "~"
if joinKuerzel = PSMDST
searchResult := searchResult "LEFT JOIN HRS.DST_DIVSTAMM ON DST_DSTID = PSM_DSTID_ABRKR AND DST_LOHNJAHR = PSM_PSMJAHR AND DST_FIRMENNR = PSM_DVFIRMENNR" "~"
if joinKuerzel = DSTPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_DSTID_ABRKR = DST_DSTID AND PSM_PSMJAHR = DST_LOHNJAHR AND PSM_DVFIRMENNR = DST_FIRMENNR" "~"
if joinKuerzel = RKEDTA
searchResult := searchResult "LEFT JOIN DTA_DBMDTABELLE.DTA_DBMDTABELLE ON DTA_DTAID = RKE_DTAID" "~"
if joinKuerzel = DTARKE
searchResult := searchResult "LEFT JOIN BMD.RKE_TABREPLEINTRAG ON RKE_DTAID = DTA_DTAID" "~"
if joinKuerzel = DTDDTM
searchResult := searchResult "LEFT JOIN DATANORM.DTM_TEXT ON DTM_TB_NR = DTD_TB_NR AND DTM_ERST_NR = DTD_ERST_NR" "~"
if joinKuerzel = DTMDTD
searchResult := searchResult "LEFT JOIN DATANORM.DTD_TEXT_DETAIL ON DTD_TB_NR = DTM_TB_NR AND DTD_ERST_NR = DTM_ERST_NR" "~"
if joinKuerzel = PSJDVH
searchResult := searchResult "LEFT JOIN DVH_DIENSTVERHAELTNIS.DVH_DIENSTVERHAELTNIS ON DVH_MIT_FIRMENNR = PSJ_FIRMENNR AND DVH_MITARBEITERID = PSJ_MITARBEITERID AND DVH_DIENSTVLFDNR = PSJ_DIENSTVLFDNR" "~"
if joinKuerzel = DVHPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = DVH_MIT_FIRMENNR AND PSJ_MITARBEITERID = DVH_MITARBEITERID AND PSJ_DIENSTVLFDNR = DVH_DIENSTVLFDNR" "~"
if joinKuerzel = SARDVH
searchResult := searchResult "LEFT JOIN DVH_DIENSTVERHAELTNIS.DVH_DIENSTVERHAELTNIS ON DVH_MIT_FIRMENNR = SAR_FIRMENNR AND DVH_MITARBEITERID = SAR_MITARBEITERID AND DVH_DIENSTVLFDNR = SAR_DIENSTVLFDNR" "~"
if joinKuerzel = DVHSAR
searchResult := searchResult "LEFT JOIN HRS.SAR_SCHWERARBEIT ON SAR_FIRMENNR = DVH_MIT_FIRMENNR AND SAR_MITARBEITERID = DVH_MITARBEITERID AND SAR_DIENSTVLFDNR = DVH_DIENSTVLFDNR" "~"
if joinKuerzel = DRSDKT
searchResult := searchResult "LEFT JOIN DKT_DOCSKATEGORIE.DKT_DOCSKATEGORIE ON DKT_ARCHIVNR = DRS_LEISTVZ_ARCHIVNR AND DKT_DOCSKATEGORIEKZ = DRS_LEISTVZ_DOCSKATKZ AND DKT_DOCSKATEGORIELFDNR = DRS_LEISTVZ_DOCSKATLFDNR" "~"
if joinKuerzel = DKTDRS
searchResult := searchResult "LEFT JOIN BMD.DRS_DRUCKEREINSTELLUNG ON DRS_LEISTVZ_ARCHIVNR = DKT_ARCHIVNR AND DRS_LEISTVZ_DOCSKATKZ = DKT_DOCSKATEGORIEKZ AND DRS_LEISTVZ_DOCSKATLFDNR = DKT_DOCSKATEGORIELFDNR" "~"
if joinKuerzel = PICDKT
searchResult := searchResult "LEFT JOIN DKT_DOCSKATEGORIE.DKT_DOCSKATEGORIE ON DKT_ARCHIVNR = PIC_ARCHIVNR AND DKT_DOCSKATEGORIEKZ = PIC_DOCSKATEGORIEKZ AND DKT_DOCSKATEGORIELFDNR = PIC_DOCSKATEGORIELFDNR" "~"
if joinKuerzel = DKTPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_ARCHIVNR = DKT_ARCHIVNR AND PIC_DOCSKATEGORIEKZ = DKT_DOCSKATEGORIEKZ AND PIC_DOCSKATEGORIELFDNR = DKT_DOCSKATEGORIELFDNR" "~"
if joinKuerzel = PIKDKT
searchResult := searchResult "LEFT JOIN DKT_DOCSKATEGORIE.DKT_DOCSKATEGORIE ON DKT_ARCHIVNR = PIK_ARCHIVNR AND DKT_DOCSKATEGORIEKZ = PIK_DOCSKATEGORIEKZ AND DKT_DOCSKATEGORIELFDNR = PIK_DOCSKATEGORIELFDNR" "~"
if joinKuerzel = DKTPIK
searchResult := searchResult "LEFT JOIN WWS.PIK_PICKATEGORIEZU ON PIK_ARCHIVNR = DKT_ARCHIVNR AND PIK_DOCSKATEGORIEKZ = DKT_DOCSKATEGORIEKZ AND PIK_DOCSKATEGORIELFDNR = DKT_DOCSKATEGORIELFDNR" "~"
if joinKuerzel = ULKDLK
searchResult := searchResult "LEFT JOIN DLK_LINK.DLK_LINK ON DLK_LINKNR = ULK_LINKNR" "~"
if joinKuerzel = DLKULK
searchResult := searchResult "LEFT JOIN BMD.ULK_USERLINK ON ULK_LINKNR = DLK_LINKNR" "~"
if joinKuerzel = DSMDNE
searchResult := searchResult "LEFT JOIN DATANORM.DNE_ERSTELLER ON DNE_ERST_NR = DSM_ERST_NR" "~"
if joinKuerzel = DNEDSM
searchResult := searchResult "LEFT JOIN DATANORM.DSM_SET ON DSM_ERST_NR = DNE_ERST_NR" "~"
if joinKuerzel = DIMDNA
searchResult := searchResult "LEFT JOIN DATANORM.DNA_ARTIKEL ON DNA_ERST_NR = DIM_ERST_NR AND DNA_ART_NR = DIM_ART_NR" "~"
if joinKuerzel = DNADIM
searchResult := searchResult "LEFT JOIN DATANORM.DIM_DIM_TEXT ON DIM_ERST_NR = DNA_ERST_NR AND DIM_ART_NR = DNA_ART_NR" "~"
if joinKuerzel = BRBDOG
searchResult := searchResult "LEFT JOIN DOG_DATAOWNERGRUPPE.DOG_DATAOWNERGRUPPE ON DOG_DATAOWNERGRUPPENNR = BRB_DATAOWNERGRUPPENNR" "~"
if joinKuerzel = DOGBRB
searchResult := searchResult "LEFT JOIN BUERO.BRB_BUEROBERECHT ON BRB_DATAOWNERGRUPPENNR = DOG_DATAOWNERGRUPPENNR" "~"
if joinKuerzel = AUDDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_ARCHIVNR = AUD_ARCHIVNR AND DOK_DOKUMENTENNR = AUD_DOKUMENTENNR" "~"
if joinKuerzel = DOKAUD
searchResult := searchResult "LEFT JOIN BUERO.AUD_AUFGABEDOKUMENTZU ON AUD_ARCHIVNR = DOK_ARCHIVNR AND AUD_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = DFRDOK
searchResult := searchResult "LEFT JOIN DOK_DOKUMENT.DOK_DOKUMENT ON DOK_ARCHIVNR = DFR_ARCHIVNR AND DOK_DOKUMENTENNR = DFR_DOKUMENTENNR" "~"
if joinKuerzel = DOKDFR
searchResult := searchResult "LEFT JOIN WWS.DFR_DOKUMENTFREIGABE ON DFR_ARCHIVNR = DOK_ARCHIVNR AND DFR_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = DPZDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_ARCHIVNR = DPZ_ARCHIVNR AND DOK_DOKUMENTENNR = DPZ_DOKUMENTENNR" "~"
if joinKuerzel = DOKDPZ
searchResult := searchResult "LEFT JOIN BUERO.DPZ_DOCSPERSZU ON DPZ_ARCHIVNR = DOK_ARCHIVNR AND DPZ_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = MDDDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_ARCHIVNR = MDD_ARCHIVNR AND DOK_DOKUMENTENNR = MDD_DOKUMENTENNR" "~"
if joinKuerzel = DOKMDD
searchResult := searchResult "LEFT JOIN BUERO.MDD_MEDIENDOKUMENTZU ON MDD_ARCHIVNR = DOK_ARCHIVNR AND MDD_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = OBBDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_ARCHIVNR = OBB_DOK_ARCHIVNR AND DOK_DOKUMENTENNR = OBB_DOK_DOKUMENTENNR" "~"
if joinKuerzel = DOKOBB
searchResult := searchResult "LEFT JOIN BUERO.OBB_ONLBEWBEWERBUNG ON OBB_DOK_ARCHIVNR = DOK_ARCHIVNR AND OBB_DOK_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = DPDDPD
searchResult := searchResult "LEFT JOIN BMD.DPD_DOCUMENTPOOLDETAIL ON DPD_DPOID = DPD_DPOID AND DPD_DPDID = DPD_QUELLDPDID" "~"
if joinKuerzel = DPDDPD
searchResult := searchResult "LEFT JOIN BMD.DPD_DOCUMENTPOOLDETAIL ON DPD_DPOID = DPD_DPOID AND DPD_QUELLDPDID = DPD_DPDID" "~"
if joinKuerzel = JABDRB
searchResult := searchResult "LEFT JOIN DRB.DRB ON FIRMENNR = JAB_KOPF_FIRMENNR AND BSTKZ = JAB_KOPF_BSTKZ AND DR_FIRMENUNTERNR = JAB_KOPF_DR_FIRMENUNTERNR AND BSTNR = JAB_KOPF_BSTNR" "~"
if joinKuerzel = DRBJAB
searchResult := searchResult "LEFT JOIN BIL.JAB_JABINFO ON JAB_KOPF_FIRMENNR = FIRMENNR AND JAB_KOPF_BSTKZ = BSTKZ AND JAB_KOPF_DR_FIRMENUNTERNR = DR_FIRMENUNTERNR AND JAB_KOPF_BSTNR = BSTNR" "~"
if joinKuerzel = ADDDRS
searchResult := searchResult "LEFT JOIN DRS_DRUCKEREINSTELLUNG.DRS_DRUCKEREINSTELLUNG ON DRS_DREINSTELLUNGSID = ADD_DREINSTELLUNGSID" "~"
if joinKuerzel = DRSADD
searchResult := searchResult "LEFT JOIN BUERO.ADD_AUSDRUCKMODELLDETAIL ON ADD_DREINSTELLUNGSID = DRS_DREINSTELLUNGSID" "~"
if joinKuerzel = KASDRS
searchResult := searchResult "LEFT JOIN DRS_DRUCKEREINSTELLUNG.DRS_DRUCKEREINSTELLUNG ON DRS_DREINSTELLUNGSID = KAS_ASCHEIN_DRSID" "~"
if joinKuerzel = DRSKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_ASCHEIN_DRSID = DRS_DREINSTELLUNGSID" "~"
if joinKuerzel = KASDRS
searchResult := searchResult "LEFT JOIN DRS_DRUCKEREINSTELLUNG.DRS_DRUCKEREINSTELLUNG ON DRS_DREINSTELLUNGSID = KAS_GUTSCHRIFT_DRSID" "~"
if joinKuerzel = DRSKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_GUTSCHRIFT_DRSID = DRS_DREINSTELLUNGSID" "~"
if joinKuerzel = HINDVH
searchResult := searchResult "LEFT JOIN DVH_DIENSTVERHAELTNIS.DVH_DIENSTVERHAELTNIS ON DVH_MIT_FIRMENNR = HIN_DVH_MIT_FIRMENNR AND DVH_MITARBEITERID = HIN_DVH_MITARBEITERID AND DVH_DIENSTVLFDNR = HIN_DVH_DIENSTVLFDNR" "~"
if joinKuerzel = DVHHIN
searchResult := searchResult "LEFT JOIN HRS.HIN_HINWEIS ON HIN_DVH_MIT_FIRMENNR = DVH_MIT_FIRMENNR AND HIN_DVH_MITARBEITERID = DVH_MITARBEITERID AND HIN_DVH_DIENSTVLFDNR = DVH_DIENSTVLFDNR" "~"
if joinKuerzel = LVTDVH
searchResult := searchResult "LEFT JOIN DVH_DIENSTVERHAELTNIS.DVH_DIENSTVERHAELTNIS ON DVH_MIT_FIRMENNR = LVT_DVFIRMENNR AND DVH_MITARBEITERID = LVT_MITARBEITERID AND DVH_DIENSTVLFDNR = LVT_DIENSTVLFDNR" "~"
if joinKuerzel = DVHLVT
searchResult := searchResult "LEFT JOIN HRS.LVT_LOHNKOSTENVERTEILG_TMP ON LVT_DVFIRMENNR = DVH_MIT_FIRMENNR AND LVT_MITARBEITERID = DVH_MITARBEITERID AND LVT_DIENSTVLFDNR = DVH_DIENSTVLFDNR" "~"
if joinKuerzel = DKPDPD
searchResult := searchResult "LEFT JOIN DPD_DOCUMENTPOOLDETAIL.DPD_DOCUMENTPOOLDETAIL ON DPD_DPOID = DKP_DPOID AND DPD_DPDID = DKP_DPDID" "~"
if joinKuerzel = DPDDKP
searchResult := searchResult "LEFT JOIN FIBU.DKP_DOKUMENTENPOOL ON DKP_DPOID = DPD_DPOID AND DKP_DPDID = DPD_DPDID" "~"
if joinKuerzel = DPFDPM
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DPM_DFUNKTIONSPARAM ON DPM_FUNKPARAMNR = DPF_FUNKPARAMNR" "~"
if joinKuerzel = DPMDPF
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DPF_DFUNKTIONSPARAMZU ON DPF_FUNKPARAMNR = DPM_FUNKPARAMNR" "~"
if joinKuerzel = AWEDRS
searchResult := searchResult "LEFT JOIN DRS_DRUCKEREINSTELLUNG.DRS_DRUCKEREINSTELLUNG ON DRS_DREINSTELLUNGSID = AWE_DRS_DREINSTELLUNGSID" "~"
if joinKuerzel = DRSAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_DRS_DREINSTELLUNGSID = DRS_DREINSTELLUNGSID" "~"
if joinKuerzel = ZASDRS
searchResult := searchResult "LEFT JOIN DRS_DRUCKEREINSTELLUNG.DRS_DRUCKEREINSTELLUNG ON DRS_DREINSTELLUNGSID = ZAS_DRUCKEINSTELLUNGSID" "~"
if joinKuerzel = DRSZAS
searchResult := searchResult "LEFT JOIN BUERO.ZAS_ZAHLSCHEIN ON ZAS_DRUCKEINSTELLUNGSID = DRS_DREINSTELLUNGSID" "~"
if joinKuerzel = LSPDRS
searchResult := searchResult "LEFT JOIN DRS_DRUCKEREINSTELLUNG.DRS_DRUCKEREINSTELLUNG ON DRS_DREINSTELLUNGSID = LSP_ARCHIV_SETT" "~"
if joinKuerzel = DRSLSP
searchResult := searchResult "LEFT JOIN HRS.LSP_LOHNSTAPEL ON LSP_ARCHIV_SETT = DRS_DREINSTELLUNGSID" "~"
if joinKuerzel = DRZDRU
searchResult := searchResult "LEFT JOIN BMD.DRU_DRUCKER ON DRU_DRUCKERID = DRZ_DRUCKERID" "~"
if joinKuerzel = DRUDRZ
searchResult := searchResult "LEFT JOIN BMD.DRZ_DRUCKERBENUTZERWERT ON DRZ_DRUCKERID = DRU_DRUCKERID" "~"
if joinKuerzel = DSTDSQ
searchResult := searchResult "LEFT JOIN HRS.DSQ_DIVSTAMM_SEQ ON DSQ_DSTID = DST_DSTID" "~"
if joinKuerzel = DSQDST
searchResult := searchResult "LEFT JOIN HRS.DST_DIVSTAMM ON DST_DSTID = DSQ_DSTID" "~"
if joinKuerzel = ATZDTA
searchResult := searchResult "LEFT JOIN DTA_DBMDTABELLE.DTA_DBMDTABELLE ON DTA_TABELLENNAME = ATZ_TABELLENNAME" "~"
if joinKuerzel = DTAATZ
searchResult := searchResult "LEFT JOIN BMD.ATZ_ABFRAGETABELLENZU ON ATZ_TABELLENNAME = DTA_TABELLENNAME" "~"
if joinKuerzel = RGTDTA
searchResult := searchResult "LEFT JOIN DTA_DBMDTABELLE.DTA_DBMDTABELLE ON DTA_DTAID = RGT_DTAID" "~"
if joinKuerzel = DTARGT
searchResult := searchResult "LEFT JOIN BMD.RGT_TABREPLGTABELLEZU ON RGT_DTAID = DTA_DTAID" "~"
if joinKuerzel = DUFDUK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DUK_DUSERLINK ON DUK_LINKGUID = DUF_LINKGUID" "~"
if joinKuerzel = DUKDUF
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DUF_DUSERLINKFIELD ON DUF_LINKGUID = DUK_LINKGUID" "~"
if joinKuerzel = ALEDVH
searchResult := searchResult "LEFT JOIN BUERO.DVH_DIENSTVERHAELTNIS ON DVH_MIT_FIRMENNR = ALE_DVH_FIRMENNR AND DVH_MITARBEITERID = ALE_DVH_MITARBEITERID AND DVH_DIENSTVLFDNR = ALE_DIENSTVLFDNR" "~"
if joinKuerzel = DVHALE
searchResult := searchResult "LEFT JOIN BUERO.ALE_ADRESSENLISTE ON ALE_DVH_FIRMENNR = DVH_MIT_FIRMENNR AND ALE_DVH_MITARBEITERID = DVH_MITARBEITERID AND ALE_DIENSTVLFDNR = DVH_DIENSTVLFDNR" "~"
if joinKuerzel = BUEDVH
searchResult := searchResult "LEFT JOIN DVH_DIENSTVERHAELTNIS.DVH_DIENSTVERHAELTNIS ON DVH_MIT_FIRMENNR = BUE_FIRMENNR AND DVH_MITARBEITERID = BUE_MITARBEITERID AND DVH_DIENSTVLFDNR = BUE_DIENSTVLFDNR" "~"
if joinKuerzel = DVHBUE
searchResult := searchResult "LEFT JOIN HRS.BUE_BUAK_URLAUBSEINREICHG ON BUE_FIRMENNR = DVH_MIT_FIRMENNR AND BUE_MITARBEITERID = DVH_MITARBEITERID AND BUE_DIENSTVLFDNR = DVH_DIENSTVLFDNR" "~"
if joinKuerzel = TMDDVH
searchResult := searchResult "LEFT JOIN DVH_DIENSTVERHAELTNIS.DVH_DIENSTVERHAELTNIS ON DVH_MIT_FIRMENNR = TMD_FIRMENNR AND DVH_MITARBEITERID = TMD_MITARBEITERID AND DVH_DIENSTVLFDNR = TMD_DIENSTVLFDNR" "~"
if joinKuerzel = DVHTMD
searchResult := searchResult "LEFT JOIN HRS.TMD_TERMINSTAMMDATEN ON TMD_FIRMENNR = DVH_MIT_FIRMENNR AND TMD_MITARBEITERID = DVH_MITARBEITERID AND TMD_DIENSTVLFDNR = DVH_DIENSTVLFDNR" "~"
if joinKuerzel = TMLDVH
searchResult := searchResult "LEFT JOIN DVH_DIENSTVERHAELTNIS.DVH_DIENSTVERHAELTNIS ON DVH_MIT_FIRMENNR = TML_FIRMENNR AND DVH_MITARBEITERID = TML_MITARBEITERID AND DVH_DIENSTVLFDNR = TML_DIENSTVLFDNR" "~"
if joinKuerzel = DVHTML
searchResult := searchResult "LEFT JOIN HRS.TML_TERMINISIERTELOHNART ON TML_FIRMENNR = DVH_MIT_FIRMENNR AND TML_MITARBEITERID = DVH_MITARBEITERID AND TML_DIENSTVLFDNR = DVH_DIENSTVLFDNR" "~"
if joinKuerzel = EB2EBN
searchResult := searchResult "LEFT JOIN HRS.EBN_ELDADETAIL_BN ON EBN_EBNID = EB2_EBNID" "~"
if joinKuerzel = EBNEB2
searchResult := searchResult "LEFT JOIN HRS.EB2_ELDADETAIL_BN_BEGR ON EB2_EBNID = EBN_EBNID" "~"
if joinKuerzel = PICEEM
searchResult := searchResult "LEFT JOIN WWS.EEM_EDIEINHEITENMAPPING ON EEM_FIRMENNR = PIC_EEM_FIRMENNR AND EEM_EEMTABNR = PIC_EEMTABNR" "~"
if joinKuerzel = EEMPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_EEM_FIRMENNR = EEM_FIRMENNR AND PIC_EEMTABNR = EEM_EEMTABNR" "~"
if joinKuerzel = AUGEFK
searchResult := searchResult "LEFT JOIN EFK_ERFASSKOPFWWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = AUG_EFK_FIRMENNR AND EFK_ERFASSUNGSKZ = AUG_EFK_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = AUG_EFK_ERFASSUNGSNR" "~"
if joinKuerzel = EFKAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_EFK_FIRMENNR = EFK_FIRMENNR AND AUG_EFK_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND AUG_EFK_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = BSPEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = BSP_EFK_FIRMENNR AND EFK_ERFASSUNGSKZ = BSP_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = BSP_ERFASSUNGSNR" "~"
if joinKuerzel = EFKBSP
searchResult := searchResult "LEFT JOIN WWS.BSP_BONUSERMPOS ON BSP_EFK_FIRMENNR = EFK_FIRMENNR AND BSP_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND BSP_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = ERPEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = ERP_FIRMENNR AND EFK_ERFASSUNGSKZ = ERP_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = ERP_ERFASSUNGSNR" "~"
if joinKuerzel = EFKERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_FIRMENNR = EFK_FIRMENNR AND ERP_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND ERP_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = RNGEDN
searchResult := searchResult "LEFT JOIN EDN_EDINACHRICHT.EDN_EDINACHRICHT ON EDN_EDNID = RNG_EDNID_FINANZ" "~"
if joinKuerzel = EDNRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_EDNID_FINANZ = EDN_EDNID" "~"
if joinKuerzel = IDPEGS
searchResult := searchResult "LEFT JOIN EGS_ERFASSUNGSGRUND.EGS_ERFASSUNGSGRUND ON EGS_GRUND_ID = IDP_GRUND_ID" "~"
if joinKuerzel = EGSIDP
searchResult := searchResult "LEFT JOIN PRODUKTION.IDP_INFO_DELAY_BREAKDOWN ON IDP_GRUND_ID = EGS_GRUND_ID" "~"
if joinKuerzel = TTGEGZ
searchResult := searchResult "LEFT JOIN HRS.EGZ_ERGAENZUNG ON EGZ_DATENBESTAND = TTG_DATENBESTAND AND EGZ_LOHNJAHR = TTG_LOHNJAHR AND EGZ_ERGAENZUNGNR = TTG_EGZ_NR" "~"
if joinKuerzel = EGZTTG
searchResult := searchResult "LEFT JOIN HRS.TTG_TARIFGR_ERGAENZUNG_ZU ON TTG_DATENBESTAND = EGZ_DATENBESTAND AND TTG_LOHNJAHR = EGZ_LOHNJAHR AND TTG_EGZ_NR = EGZ_ERGAENZUNGNR" "~"
if joinKuerzel = PAPEHT
searchResult := searchResult "LEFT JOIN WWS.EHT_EINHEITENTAB ON EHT_FIRMENNR = PAP_FIRMENNR AND EHT_EINHEITENTABNR = PAP_EINHEITENTABNR" "~"
if joinKuerzel = EHTPAP
searchResult := searchResult "LEFT JOIN WWS.PAP_PACKSTOFFTABPOS ON PAP_FIRMENNR = EHT_FIRMENNR AND PAP_EINHEITENTABNR = EHT_EINHEITENTABNR" "~"
if joinKuerzel = AAPEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = AAP_EINHEITKBEZ" "~"
if joinKuerzel = EINAAP
searchResult := searchResult "LEFT JOIN WWS.AAP_ARAPOSITION ON AAP_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = DISEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = DIS_EINHEITKBEZ" "~"
if joinKuerzel = EINDIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = EQIEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = EQI_EINHEITKBEZ" "~"
if joinKuerzel = EINEQI
searchResult := searchResult "LEFT JOIN WWS.EQI_ERFASSPOSQUELLINFO ON EQI_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = GEPEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = GEP_EINHEITKBEZ" "~"
if joinKuerzel = EINGEP
searchResult := searchResult "LEFT JOIN WWS.GEP_GEFAHRENGUTPOS ON GEP_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = LUEEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = LUE_QTY_EINH" "~"
if joinKuerzel = EINLUE
searchResult := searchResult "LEFT JOIN PRODUKTION.LUE_LAGER_UMS_EMPFANGEN ON LUE_QTY_EINH = EIN_EINHEITKBEZ" "~"
if joinKuerzel = LUPEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = LUP_QTY_EINH" "~"
if joinKuerzel = EINLUP
searchResult := searchResult "LEFT JOIN PRODUKTION.LUP_LAGER_UMS_POS ON LUP_QTY_EINH = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PLOEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = PLO_QTY_EINH" "~"
if joinKuerzel = EINPLO
searchResult := searchResult "LEFT JOIN PRODUKTION.PLO_PPS_LOS ON PLO_QTY_EINH = EIN_EINHEITKBEZ" "~"
if joinKuerzel = WAOEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = WAO_EINHEITKBEZ" "~"
if joinKuerzel = EINWAO
searchResult := searchResult "LEFT JOIN WWS.WAO_WARENEINGWBK ON WAO_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = WAZEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = WAZ_EINHEITKBEZ" "~"
if joinKuerzel = EINWAZ
searchResult := searchResult "LEFT JOIN WWS.WAZ_WARENZUORDNUNG ON WAZ_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = BPPEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = BPP_GEWICHTS_EINHEITKBEZ" "~"
if joinKuerzel = EINBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_GEWICHTS_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = ELVEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = ELV_LIEFER_ZLM_EINHEITKBEZ" "~"
if joinKuerzel = EINELV
searchResult := searchResult "LEFT JOIN WWS.ELV_ERFASSPOSLIEFERVOR ON ELV_LIEFER_ZLM_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PIAEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = PIA_VERKAUFSEINHEITKBEZ" "~"
if joinKuerzel = EINPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_VERKAUFSEINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PIEEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = PIE_UNTEREINHEITKBEZ" "~"
if joinKuerzel = EINPIE
searchResult := searchResult "LEFT JOIN WWS.PIE_PICEINHEIT ON PIE_UNTEREINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = LATEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = LAT_EINKPREISEINHEITKBEZ" "~"
if joinKuerzel = EINLAT
searchResult := searchResult "LEFT JOIN WWS.LAT_LIEFERANTENARTIKEL ON LAT_EINKPREISEINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PIAEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = PIA_EINKPREISEINHEITKBEZ" "~"
if joinKuerzel = EINPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_EINKPREISEINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = AVEEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = AVE_EFKESD_ZAEHLER_EH_1" "~"
if joinKuerzel = EINAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_EFKESD_ZAEHLER_EH_1 = EIN_EINHEITKBEZ" "~"
if joinKuerzel = BPKEKZ
searchResult := searchResult "LEFT JOIN WWS.EKZ_ERLOESAUFWANDKZ ON EKZ_FIRMENNR = BPK_ERLOESAUFWAND_FIRMENNR AND EKZ_ERLOESAUFWANDKZ = BPK_ERLOESAUFWANDKZ" "~"
if joinKuerzel = EKZBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_ERLOESAUFWAND_FIRMENNR = EKZ_FIRMENNR AND BPK_ERLOESAUFWANDKZ = EKZ_ERLOESAUFWANDKZ" "~"
if joinKuerzel = GEPEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_EPSTKLISTID = GEP_AUFTRAGS_EPSTKLISTID" "~"
if joinKuerzel = EPSGEP
searchResult := searchResult "LEFT JOIN WWS.GEP_GEFAHRENGUTPOS ON GEP_AUFTRAGS_EPSTKLISTID = EPS_EPSTKLISTID" "~"
if joinKuerzel = VEPEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_EPSTKLISTID = VEP_EPS_EPSTKLISTID" "~"
if joinKuerzel = EPSVEP
searchResult := searchResult "LEFT JOIN WWS.VEP_VERTRETERABRECHPOS ON VEP_EPS_EPSTKLISTID = EPS_EPSTKLISTID" "~"
if joinKuerzel = ULFDBT
searchResult := searchResult "LEFT JOIN DBT_DBMDTABFELDBESCH.DBT_DBMDTABFELDBESCH ON DBT_BMDCONSTNAME = ULF_BMDCONSTNAME" "~"
if joinKuerzel = DBTULF
searchResult := searchResult "LEFT JOIN BMD.ULF_USERLINKFIELD ON ULF_BMDCONSTNAME = DBT_BMDCONSTNAME" "~"
if joinKuerzel = DCFDCA
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DCA_WAAUSDRUCKE ON DCA_WAAUSDRUCKNR = DCF_WAAUSDRUCKNR AND DCA_ABVERSIONSNR = DCF_ABVERSIONSNR" "~"
if joinKuerzel = DCADCF
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DCF_WABAUMELEMAUSDRUCKZU ON DCF_WAAUSDRUCKNR = DCA_WAAUSDRUCKNR AND DCF_ABVERSIONSNR = DCA_ABVERSIONSNR" "~"
if joinKuerzel = DAPDCD
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DCD_WAPARAMDEF ON DCD_WAPARAMDEFNR = DAP_WAPARAMDEFNR" "~"
if joinKuerzel = DCDDAP
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DAP_WAAUSDRUCKPARAMETER ON DAP_WAPARAMDEFNR = DCD_WAPARAMDEFNR" "~"
if joinKuerzel = ABFDFG
searchResult := searchResult "LEFT JOIN DFG_DFELDBESCHGRUPPE.DFG_DFELDBESCHGRUPPE ON DFG_HAUPTGRUPPE = ABF_HAUPTGRUPPE AND DFG_UNTERGRUPPE = ABF_UNTERGRUPPE" "~"
if joinKuerzel = DFGABF
searchResult := searchResult "LEFT JOIN BMD.ABF_ABFRAGE ON ABF_HAUPTGRUPPE = DFG_HAUPTGRUPPE AND ABF_UNTERGRUPPE = DFG_UNTERGRUPPE" "~"
if joinKuerzel = RSPDFG
searchResult := searchResult "LEFT JOIN DFG_DFELDBESCHGRUPPE.DFG_DFELDBESCHGRUPPE ON DFG_HAUPTGRUPPE = RSP_HAUPTGRUPPE AND DFG_UNTERGRUPPE = RSP_UNTERGRUPPE" "~"
if joinKuerzel = DFGRSP
searchResult := searchResult "LEFT JOIN BIL.RSP_RSTSPALTENKOPF ON RSP_HAUPTGRUPPE = DFG_HAUPTGRUPPE AND RSP_UNTERGRUPPE = DFG_UNTERGRUPPE" "~"
if joinKuerzel = FUNDFU
searchResult := searchResult "LEFT JOIN DFU_DFUNKTION.DFU_DFUNKTION ON DFU_FUNKTIONSNR = FUNKTIONSNR" "~"
if joinKuerzel = DFUFUN
searchResult := searchResult "LEFT JOIN BMDENTW.FUNKTIONSHILFE ON FUNKTIONSNR = DFU_FUNKTIONSNR" "~"
if joinKuerzel = SPTDFU
searchResult := searchResult "LEFT JOIN DFU_DFUNKTION.DFU_DFUNKTION ON DFU_FUNKTIONSNR = SPT_CONSTNAME" "~"
if joinKuerzel = DFUSPT
searchResult := searchResult "LEFT JOIN BMD.SPT_STAPELSCHRITT ON SPT_CONSTNAME = DFU_FUNKTIONSNR" "~"
if joinKuerzel = TASDFU
searchResult := searchResult "LEFT JOIN DFU_DFUNKTION.DFU_DFUNKTION ON DFU_BMDCONSTID = TAS_BMDCONSTID_FENSTER" "~"
if joinKuerzel = DFUTAS
searchResult := searchResult "LEFT JOIN BMD.TAS_ACTIONSTACK1_TMP ON TAS_BMDCONSTID_FENSTER = DFU_BMDCONSTID" "~"
if joinKuerzel = DIIDIA
searchResult := searchResult "LEFT JOIN BMD.DIA_DIAGRAMM ON DIA_DIAGRAMMNR = DII_DIAGRAMMNR" "~"
if joinKuerzel = DIADII
searchResult := searchResult "LEFT JOIN BMD.DII_DIAGRAMMINSTANZ ON DII_DIAGRAMMNR = DIA_DIAGRAMMNR" "~"
if joinKuerzel = DIADIG
searchResult := searchResult "LEFT JOIN BMD.DIG_DIAGRAMMGRUPPE ON DIG_DIAGRAMMGRPNR = DIA_DIAGRAMMGRPNR" "~"
if joinKuerzel = DIGDIA
searchResult := searchResult "LEFT JOIN BMD.DIA_DIAGRAMM ON DIA_DIAGRAMMGRPNR = DIG_DIAGRAMMGRPNR" "~"
if joinKuerzel = IAMDII
searchResult := searchResult "LEFT JOIN DII_DIAGRAMMINSTANZ.DII_DIAGRAMMINSTANZ ON DII_DIAGRAMMINSTANZNR = IAM_DIAGRAMMINSTANZNR AND DII_DIAGRAMMNR = IAM_DIAGRAMMNR AND DII_AKTIONSNR = IAM_AKTIONSNR" "~"
if joinKuerzel = DIIIAM
searchResult := searchResult "LEFT JOIN BUERO.IAM_INSTANZAKTIONMITARB ON IAM_DIAGRAMMINSTANZNR = DII_DIAGRAMMINSTANZNR AND IAM_DIAGRAMMNR = DII_DIAGRAMMNR AND IAM_AKTIONSNR = DII_AKTIONSNR" "~"
if joinKuerzel = BPKDIP
searchResult := searchResult "LEFT JOIN DIP_DIVERSE.DIP_DIVERSE ON DIP_FIRMENNR = BPK_DIVPER_FIRMENNR AND DIP_EXTPERSID = BPK_DIVPER_PERSONENID" "~"
if joinKuerzel = DIPBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_DIVPER_FIRMENNR = DIP_FIRMENNR AND BPK_DIVPER_PERSONENID = DIP_EXTPERSID" "~"
if joinKuerzel = LFSDIP
searchResult := searchResult "LEFT JOIN DIP_DIVERSE.DIP_DIVERSE ON DIP_FIRMENNR = LFS_SPEDITION_FIRMENNR AND DIP_EXTPERSID = LFS_SPEDITION_EXTPERSID" "~"
if joinKuerzel = DIPLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_SPEDITION_FIRMENNR = DIP_FIRMENNR AND LFS_SPEDITION_EXTPERSID = DIP_EXTPERSID" "~"
if joinKuerzel = RIHDKQ
searchResult := searchResult "LEFT JOIN DKQ_DOKUMENTZUID.DKQ_DOKUMENTZUID ON DKQ_DKZID = RIH_DKZID" "~"
if joinKuerzel = DKQRIH
searchResult := searchResult "LEFT JOIN PRODUKTION.RIH_RISIKOANALYSE_HEAD ON RIH_DKZID = DKQ_DKZID" "~"
if joinKuerzel = VFADKQ
searchResult := searchResult "LEFT JOIN DKQ_DOKUMENTZUID.DKQ_DOKUMENTZUID ON DKQ_DKZID = VFA_DKZID" "~"
if joinKuerzel = DKQVFA
searchResult := searchResult "LEFT JOIN PRODUKTION.VFA_VERFAHRENSANWEISUNG_KOPF ON VFA_DKZID = DKQ_DKZID" "~"
if joinKuerzel = VWADKT
searchResult := searchResult "LEFT JOIN BUERO.DKT_DOCSKATEGORIE ON DKT_ARCHIVNR = VWA_DKT_ARCHIVNR AND DKT_DOCSKATEGORIEKZ = VWA_DKT_DOCSKATEGORIEKZ AND DKT_DOCSKATEGORIELFDNR = VWA_DKT_DOCSKATEGORIELFDNR" "~"
if joinKuerzel = DKTVWA
searchResult := searchResult "LEFT JOIN BUERO.VWA_VORSCHLAGSWERTEARCHIV ON VWA_DKT_ARCHIVNR = DKT_ARCHIVNR AND VWA_DKT_DOCSKATEGORIEKZ = DKT_DOCSKATEGORIEKZ AND VWA_DKT_DOCSKATEGORIELFDNR = DKT_DOCSKATEGORIELFDNR" "~"
if joinKuerzel = DKTDKW
searchResult := searchResult "LEFT JOIN BUERO.DKW_DOKKATWORKFLOWZU ON DKW_DKWID = DKT_DKW_WORKFLOWZUID" "~"
if joinKuerzel = DKWDKT
searchResult := searchResult "LEFT JOIN BUERO.DKT_DOCSKATEGORIE ON DKT_DKW_WORKFLOWZUID = DKW_DKWID" "~"
if joinKuerzel = DNADNE
searchResult := searchResult "LEFT JOIN DATANORM.DNE_ERSTELLER ON DNE_ERST_NR = DNA_ERST_NR" "~"
if joinKuerzel = DNEDNA
searchResult := searchResult "LEFT JOIN DATANORM.DNA_ARTIKEL ON DNA_ERST_NR = DNE_ERST_NR" "~"
if joinKuerzel = OSLDNE
searchResult := searchResult "LEFT JOIN DATANORM.DNE_ERSTELLER ON DNE_ERST_NR = OSL_ERST_NR" "~"
if joinKuerzel = DNEOSL
searchResult := searchResult "LEFT JOIN DATANORM.OSL_SET_ON_LBSL ON OSL_ERST_NR = DNE_ERST_NR" "~"
if joinKuerzel = DTZDNE
searchResult := searchResult "LEFT JOIN DATANORM.DNE_ERSTELLER ON DNE_ERST_NR = DTZ_ERST_NR" "~"
if joinKuerzel = DNEDTZ
searchResult := searchResult "LEFT JOIN DATANORM.DTZ_DATANORM_TEXTZU ON DTZ_ERST_NR = DNE_ERST_NR" "~"
if joinKuerzel = DIZDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_ARCHIVNR = DIZ_ARCHIVNR AND DOK_DOKUMENTENNR = DIZ_DOKUMENTENNR" "~"
if joinKuerzel = DOKDIZ
searchResult := searchResult "LEFT JOIN BUERO.DIZ_DOCSINSTZU ON DIZ_ARCHIVNR = DOK_ARCHIVNR AND DIZ_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = FDODOK
searchResult := searchResult "LEFT JOIN DOK_DOKUMENT.DOK_DOKUMENT ON DOK_ARCHIVNR = FDO_ARCHIVNR AND DOK_DOKUMENTENNR = FDO_DOKUMENTENNR" "~"
if joinKuerzel = DOKFDO
searchResult := searchResult "LEFT JOIN FIBU.FDO_FIBUDOKUMENTZU ON FDO_ARCHIVNR = DOK_ARCHIVNR AND FDO_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = LSPDRS
searchResult := searchResult "LEFT JOIN DRS_DRUCKEREINSTELLUNG.DRS_DRUCKEREINSTELLUNG ON DRS_DREINSTELLUNGSID = LSP_MAIL_SETT" "~"
if joinKuerzel = DRSLSP
searchResult := searchResult "LEFT JOIN HRS.LSP_LOHNSTAPEL ON LSP_MAIL_SETT = DRS_DREINSTELLUNGSID" "~"
if joinKuerzel = DSVDSK
searchResult := searchResult "LEFT JOIN BUERO.DSK_DSGVO_DATENKATEGORIE ON DSK_DSKID = DSV_DSKID" "~"
if joinKuerzel = DSKDSV
searchResult := searchResult "LEFT JOIN BUERO.DSV_DSGVO_VERZEICHNIS ON DSV_DSKID = DSK_DSKID" "~"
if joinKuerzel = DSKDSS
searchResult := searchResult "LEFT JOIN BUERO.DSS_DSGVO_STANDARD ON DSS_DSSID = DSK_DSSID" "~"
if joinKuerzel = DSSDSK
searchResult := searchResult "LEFT JOIN BUERO.DSK_DSGVO_DATENKATEGORIE ON DSK_DSSID = DSS_DSSID" "~"
if joinKuerzel = KSZDST
searchResult := searchResult "LEFT JOIN HRS.DST_DIVSTAMM ON DST_DSTID = KSZ_DSTID AND DST_LOHNJAHR = KSZ_JAHR AND DST_FIRMENNR = KSZ_DVFIRMENNR" "~"
if joinKuerzel = DSTKSZ
searchResult := searchResult "LEFT JOIN HRS.KSZ_BBKALKSZ ON KSZ_DSTID = DST_DSTID AND KSZ_JAHR = DST_LOHNJAHR AND KSZ_DVFIRMENNR = DST_FIRMENNR" "~"
if joinKuerzel = DSPDSV
searchResult := searchResult "LEFT JOIN BUERO.DSV_DSGVO_VERZEICHNIS ON DSV_FIRMENNR = DSP_DSV_FIRMENNR AND DSV_EINTRAGNR = DSP_EINTRAGNR" "~"
if joinKuerzel = DSVDSP
searchResult := searchResult "LEFT JOIN BUERO.DSP_DSGVO_PERSONALIEN ON DSP_DSV_FIRMENNR = DSV_FIRMENNR AND DSP_EINTRAGNR = DSV_EINTRAGNR" "~"
if joinKuerzel = DTUDTA
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DTA_DBMDTABELLE ON DTA_TABELLENNAME = DTU_TABELLENNAME" "~"
if joinKuerzel = DTADTU
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DTU_DTABUEBERS ON DTU_TABELLENNAME = DTA_TABELLENNAME" "~"
if joinKuerzel = QLKDTA
searchResult := searchResult "LEFT JOIN DTA_DBMDTABELLE.DTA_DBMDTABELLE ON DTA_TABELLENNAME = QLK_VON_TABELLENNAME" "~"
if joinKuerzel = DTAQLK
searchResult := searchResult "LEFT JOIN BMD.QLK_LINK ON QLK_VON_TABELLENNAME = DTA_TABELLENNAME" "~"
if joinKuerzel = RGTDTA
searchResult := searchResult "LEFT JOIN DTA_DBMDTABELLE.DTA_DBMDTABELLE ON DTA_DTAID = RGT_HAUPTTABELLE_DTAID" "~"
if joinKuerzel = DTARGT
searchResult := searchResult "LEFT JOIN BMD.RGT_TABREPLGTABELLEZU ON RGT_HAUPTTABELLE_DTAID = DTA_DTAID" "~"
if joinKuerzel = BMIDVH
searchResult := searchResult "LEFT JOIN DVH_DIENSTVERHAELTNIS.DVH_DIENSTVERHAELTNIS ON DVH_MIT_FIRMENNR = BMI_MIT_FIRMENNR AND DVH_MITARBEITERID = BMI_MITARBEITERID AND DVH_DIENSTVLFDNR = BMI_DIENSTVLFDNR" "~"
if joinKuerzel = DVHBMI
searchResult := searchResult "LEFT JOIN HRS.BMI_BUDGETMITARBEITER ON BMI_MIT_FIRMENNR = DVH_MIT_FIRMENNR AND BMI_MITARBEITERID = DVH_MITARBEITERID AND BMI_DIENSTVLFDNR = DVH_DIENSTVLFDNR" "~"
if joinKuerzel = HISDVH
searchResult := searchResult "LEFT JOIN DVH_DIENSTVERHAELTNIS.DVH_DIENSTVERHAELTNIS ON DVH_MIT_FIRMENNR = HIS_FIRMENNR AND DVH_MITARBEITERID = HIS_MITARBEITERID AND DVH_DIENSTVLFDNR = HIS_DIENSTVLFDNR" "~"
if joinKuerzel = DVHHIS
searchResult := searchResult "LEFT JOIN HRS.HIS_HISTORIE ON HIS_FIRMENNR = DVH_MIT_FIRMENNR AND HIS_MITARBEITERID = DVH_MITARBEITERID AND HIS_DIENSTVLFDNR = DVH_DIENSTVLFDNR" "~"
if joinKuerzel = INLDVH
searchResult := searchResult "LEFT JOIN DVH_DIENSTVERHAELTNIS.DVH_DIENSTVERHAELTNIS ON DVH_MIT_FIRMENNR = INL_DVH_MIT_FIRMENNR AND DVH_MITARBEITERID = INL_DVH_MITARBEITERID AND DVH_DIENSTVLFDNR = INL_DIENSTVLFDNR" "~"
if joinKuerzel = DVHINL
searchResult := searchResult "LEFT JOIN ZLE.INL_INTERNLEISTUNG ON INL_DVH_MIT_FIRMENNR = DVH_MIT_FIRMENNR AND INL_DVH_MITARBEITERID = DVH_MITARBEITERID AND INL_DIENSTVLFDNR = DVH_DIENSTVLFDNR" "~"
if joinKuerzel = TMSDVH
searchResult := searchResult "LEFT JOIN BUERO.DVH_DIENSTVERHAELTNIS ON DVH_MIT_FIRMENNR = TMS_DVH_FIRMENNR AND DVH_MITARBEITERID = TMS_DVH_MITARBEITERID AND DVH_DIENSTVLFDNR = TMS_DVH_DIENSTVLFDNR" "~"
if joinKuerzel = DVHTMS
searchResult := searchResult "LEFT JOIN BUERO.TMS_TMSICHERUNG ON TMS_DVH_FIRMENNR = DVH_MIT_FIRMENNR AND TMS_DVH_MITARBEITERID = DVH_MITARBEITERID AND TMS_DVH_DIENSTVLFDNR = DVH_DIENSTVLFDNR" "~"
if joinKuerzel = WEUEBP
searchResult := searchResult "LEFT JOIN EBP_BESTELLPOSITION.EBP_BESTELLPOSITION ON EBP_FIRMENNR = WEU_EBP_FIRMENNR AND EBP_FIBUNR = WEU_EBP_FIBUNR AND EBP_ERFASSNR = WEU_EBP_ERFASSNR AND EBP_EBPNR = WEU_EBP_EBPNR" "~"
if joinKuerzel = EBPWEU
searchResult := searchResult "LEFT JOIN WWS.WEU_WWSERKONTROLLEZU ON WEU_EBP_FIRMENNR = EBP_FIRMENNR AND WEU_EBP_FIBUNR = EBP_FIBUNR AND WEU_EBP_ERFASSNR = EBP_ERFASSNR AND WEU_EBP_EBPNR = EBP_EBPNR" "~"
if joinKuerzel = EEPEEM
searchResult := searchResult "LEFT JOIN WWS.EEM_EDIEINHEITENMAPPING ON EEM_FIRMENNR = EEP_FIRMENNR AND EEM_EEMTABNR = EEP_EEMTABNR" "~"
if joinKuerzel = EEMEEP
searchResult := searchResult "LEFT JOIN WWS.EEP_EDIEINHEITENPOS ON EEP_FIRMENNR = EEM_FIRMENNR AND EEP_EEMTABNR = EEM_EEMTABNR" "~"
if joinKuerzel = EKLEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = EKL_FIRMENNR AND EFK_ERFASSUNGSKZ = EKL_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = EKL_ERFASSUNGSNR" "~"
if joinKuerzel = EFKEKL
searchResult := searchResult "LEFT JOIN WWS.EKL_ERFASSKOPFLFS ON EKL_FIRMENNR = EFK_FIRMENNR AND EKL_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND EKL_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = REAEFK
searchResult := searchResult "LEFT JOIN EFK_ERFASSKOPFWWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = REA_EFK_FIRMENNR AND EFK_ERFASSUNGSKZ = REA_EFK_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = REA_EFK_ERFASSUNGSNR" "~"
if joinKuerzel = EFKREA
searchResult := searchResult "LEFT JOIN HRS.REA_REISEAUFTEILUNG ON REA_EFK_FIRMENNR = EFK_FIRMENNR AND REA_EFK_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND REA_EFK_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = VPGEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = VPG_FIRMENNR AND EFK_ERFASSUNGSKZ = VPG_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = VPG_ERFASSUNGSNR" "~"
if joinKuerzel = EFKVPG
searchResult := searchResult "LEFT JOIN WWS.VPG_VPERMITTLUNG_EFKZUORD ON VPG_FIRMENNR = EFK_FIRMENNR AND VPG_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND VPG_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = EFKEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = EFK_VON_ANGEB_FIRMENNR AND EFK_ERFASSUNGSKZ = EFK_VON_ANGEB_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = EFK_VON_ANGEB_ERFASSUNGSNR" "~"
if joinKuerzel = EFKEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_VON_ANGEB_FIRMENNR = EFK_FIRMENNR AND EFK_VON_ANGEB_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND EFK_VON_ANGEB_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = DTPDTP
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DTP_DEFTREEPOS ON DTP_BAUMID = DTP_BAUMID AND DTP_OBJPOSID = DTP_PARENTID" "~"
if joinKuerzel = DTPDTP
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DTP_DEFTREEPOS ON DTP_BAUMID = DTP_BAUMID AND DTP_PARENTID = DTP_OBJPOSID" "~"
if joinKuerzel = UTUDTU
searchResult := searchResult "LEFT JOIN DTU_DTABUEBERS.DTU_DTABUEBERS ON DTU_HAUPTGRUPPE = UTU_HAUPTGRUPPE AND DTU_UNTERGRUPPE = UTU_UNTERGRUPPE AND DTU_TABELLENNAME = UTU_TABELLENNAME" "~"
if joinKuerzel = DTUUTU
searchResult := searchResult "LEFT JOIN BMD.UTU_UTABUEBERS ON UTU_HAUPTGRUPPE = DTU_HAUPTGRUPPE AND UTU_UNTERGRUPPE = DTU_UNTERGRUPPE AND UTU_TABELLENNAME = DTU_TABELLENNAME" "~"
if joinKuerzel = EB3EBN
searchResult := searchResult "LEFT JOIN HRS.EBN_ELDADETAIL_BN ON EBN_EBNID = EB3_EBNID" "~"
if joinKuerzel = EBNEB3
searchResult := searchResult "LEFT JOIN HRS.EB3_ELDADETAIL_BN_NEBEN ON EB3_EBNID = EBN_EBNID" "~"
if joinKuerzel = EBSEBS
searchResult := searchResult "LEFT JOIN BIL.EBS_EBILANZSTRUKTUR ON EBS_EBILNR = EBS_PARENT_EBILNR AND EBS_ABJAHRWJBEGINN = EBS_PARENT_ABJAHRWJBEGINN" "~"
if joinKuerzel = EBSEBS
searchResult := searchResult "LEFT JOIN BIL.EBS_EBILANZSTRUKTUR ON EBS_PARENT_EBILNR = EBS_EBILNR AND EBS_PARENT_ABJAHRWJBEGINN = EBS_ABJAHRWJBEGINN" "~"
if joinKuerzel = BPKEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = BPK_KDAUFTR_FIRMENNR AND EFK_ERFASSUNGSKZ = BPK_KDAUFTR_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = BPK_KDAUFTR_ERFASSUNGSNR" "~"
if joinKuerzel = EFKBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_KDAUFTR_FIRMENNR = EFK_FIRMENNR AND BPK_KDAUFTR_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND BPK_KDAUFTR_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = KOZEFK
searchResult := searchResult "LEFT JOIN EFK_ERFASSKOPFWWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = KOZ_ERFASS_FIRMENNR AND EFK_ERFASSUNGSKZ = KOZ_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = KOZ_ERFASSUNGSNR" "~"
if joinKuerzel = EFKKOZ
searchResult := searchResult "LEFT JOIN PRODUKTION.KOZ_KOMMISSIONIERUNG_ZU ON KOZ_ERFASS_FIRMENNR = EFK_FIRMENNR AND KOZ_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND KOZ_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = REIEFK
searchResult := searchResult "LEFT JOIN EFK_ERFASSKOPFWWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = REI_EFK_FIRMENNR AND EFK_ERFASSUNGSKZ = REI_EFK_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = REI_EFK_ERFASSUNGSNR" "~"
if joinKuerzel = EFKREI
searchResult := searchResult "LEFT JOIN HRS.REI_REISESTAMM ON REI_EFK_FIRMENNR = EFK_FIRMENNR AND REI_EFK_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND REI_EFK_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = BPKEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = BPK_EFK_FIRMENNR AND EFK_ERFASSUNGSKZ = BPK_EFK_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = BPK_EFK_ERFASSUNGSNR" "~"
if joinKuerzel = EFKBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_EFK_FIRMENNR = EFK_FIRMENNR AND BPK_EFK_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND BPK_EFK_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = EFPEFZ
searchResult := searchResult "LEFT JOIN HRS.EFZ_ENTGELTFORTZAHLUNG ON EFZ_DBSNR = EFP_DBSNR AND EFZ_EFANR = EFP_EFANR" "~"
if joinKuerzel = EFZEFP
searchResult := searchResult "LEFT JOIN HRS.EFP_ENTGELTFORTZAHLPROZ ON EFP_DBSNR = EFZ_DBSNR AND EFP_EFANR = EFZ_EFANR" "~"
if joinKuerzel = AVEEGS
searchResult := searchResult "LEFT JOIN EGS_ERFASSUNGSGRUND.EGS_ERFASSUNGSGRUND ON EGS_GRUND_ID = AVE_GRUND_ID" "~"
if joinKuerzel = EGSAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_GRUND_ID = EGS_GRUND_ID" "~"
if joinKuerzel = BPKEGS
searchResult := searchResult "LEFT JOIN EGS_ERFASSUNGSGRUND.EGS_ERFASSUNGSGRUND ON EGS_GRUND_ID = BPK_ABLEHNUNGS_GRUND_ID" "~"
if joinKuerzel = EGSBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_ABLEHNUNGS_GRUND_ID = EGS_GRUND_ID" "~"
if joinKuerzel = BPPEGS
searchResult := searchResult "LEFT JOIN EGS_ERFASSUNGSGRUND.EGS_ERFASSUNGSGRUND ON EGS_GRUND_ID = BPP_FERTIGMELD_GRUND_ID" "~"
if joinKuerzel = EGSBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_FERTIGMELD_GRUND_ID = EGS_GRUND_ID" "~"
if joinKuerzel = AGSEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = AGS_SOLL_BM_EINHEITKBEZ" "~"
if joinKuerzel = EINAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_SOLL_BM_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = BSPEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = BSP_EINHEITKBEZ" "~"
if joinKuerzel = EINBSP
searchResult := searchResult "LEFT JOIN WWS.BSP_BONUSERMPOS ON BSP_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = CHGEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = CHG_GRUNDEINHEITKBEZ" "~"
if joinKuerzel = EINCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_GRUNDEINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = LFSEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = LFS_GEWICHT_EINHEITKBEZ" "~"
if joinKuerzel = EINLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_GEWICHT_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = LINEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = LIN_PQTY_A_UNIT" "~"
if joinKuerzel = EINLIN
searchResult := searchResult "LEFT JOIN PRODUKTION.LIN_PROD_LINIE ON LIN_PQTY_A_UNIT = EIN_EINHEITKBEZ" "~"
if joinKuerzel = LUAEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = LUA_QTY_EINH" "~"
if joinKuerzel = EINLUA
searchResult := searchResult "LEFT JOIN PRODUKTION.LUA_LAGER_UMS_ANGEFORDERT ON LUA_QTY_EINH = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PBSEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = PBS_EINHEITKBEZ" "~"
if joinKuerzel = EINPBS
searchResult := searchResult "LEFT JOIN PRODUKTION.PBS_BEFEHL ON PBS_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PCKEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = PCK_SOLL_BM_EINHEITKBEZ" "~"
if joinKuerzel = EINPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_SOLL_BM_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PIREIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = PIR_EINHEITKBEZ" "~"
if joinKuerzel = EINPIR
searchResult := searchResult "LEFT JOIN WWS.PIR_PICRABATTDETAIL ON PIR_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = EKREFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = EKR_FIRMENNR AND EFK_ERFASSUNGSKZ = EKR_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = EKR_ERFASSUNGSNR" "~"
if joinKuerzel = EFKEKR
searchResult := searchResult "LEFT JOIN WWS.EKR_ERFASSKOPFRECHNUNG ON EKR_FIRMENNR = EFK_FIRMENNR AND EKR_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND EKR_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = KAKEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = KAK_EFK_FIRMENNR AND EFK_ERFASSUNGSKZ = KAK_EFK_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = KAK_EFK_ERFASSUNGSNR" "~"
if joinKuerzel = EFKKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_EFK_FIRMENNR = EFK_FIRMENNR AND KAK_EFK_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND KAK_EFK_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = PCKEFK
searchResult := searchResult "LEFT JOIN EFK_ERFASSKOPFWWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = PCK_EFK_FIRMENNR AND EFK_ERFASSUNGSKZ = PCK_EFK_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = PCK_EFK_ERFASSUNGSNR" "~"
if joinKuerzel = EFKPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_EFK_FIRMENNR = EFK_FIRMENNR AND PCK_EFK_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND PCK_EFK_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = LFIEFZ
searchResult := searchResult "LEFT JOIN HRS.EFZ_ENTGELTFORTZAHLUNG ON EFZ_DBSNR = LFI_BRK_DATENBESTANDNR AND EFZ_EFANR = LFI_EFZNR" "~"
if joinKuerzel = EFZLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_BRK_DATENBESTANDNR = EFZ_DBSNR AND LFI_EFZNR = EFZ_EFANR" "~"
if joinKuerzel = AGSEGS
searchResult := searchResult "LEFT JOIN EGS_ERFASSUNGSGRUND.EGS_ERFASSUNGSGRUND ON EGS_GRUND_ID = AGS_SPERR_GRUND_ID" "~"
if joinKuerzel = EGSAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_SPERR_GRUND_ID = EGS_GRUND_ID" "~"
if joinKuerzel = KAPEGS
searchResult := searchResult "LEFT JOIN EGS_ERFASSUNGSGRUND.EGS_ERFASSUNGSGRUND ON EGS_GRUND_ID = KAP_VKPREISGRUND_ID" "~"
if joinKuerzel = EGSKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_VKPREISGRUND_ID = EGS_GRUND_ID" "~"
if joinKuerzel = WKPEGS
searchResult := searchResult "LEFT JOIN EGS_ERFASSUNGSGRUND.EGS_ERFASSUNGSGRUND ON EGS_GRUND_ID = WKP_EGS_PREISGRUND_ID" "~"
if joinKuerzel = EGSWKP
searchResult := searchResult "LEFT JOIN WWS.WKP_WWSKONTENTABPOS ON WKP_EGS_PREISGRUND_ID = EGS_GRUND_ID" "~"
if joinKuerzel = ARLEHT
searchResult := searchResult "LEFT JOIN WWS.EHT_EINHEITENTAB ON EHT_FIRMENNR = ARL_FIRMENNR AND EHT_EINHEITENTABNR = ARL_EINHEITENTABNR" "~"
if joinKuerzel = EHTARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = EHT_FIRMENNR AND ARL_EINHEITENTABNR = EHT_EINHEITENTABNR" "~"
if joinKuerzel = BPPEHT
searchResult := searchResult "LEFT JOIN WWS.EHT_EINHEITENTAB ON EHT_FIRMENNR = BPP_ARL_FIRMENNR AND EHT_EINHEITENTABNR = BPP_EINHEITENTABNR" "~"
if joinKuerzel = EHTBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_ARL_FIRMENNR = EHT_FIRMENNR AND BPP_EINHEITENTABNR = EHT_EINHEITENTABNR" "~"
if joinKuerzel = BVOEHT
searchResult := searchResult "LEFT JOIN WWS.EHT_EINHEITENTAB ON EHT_FIRMENNR = BVO_HAUPTFIRMENNR AND EHT_EINHEITENTABNR = BVO_EINHEITENTABNR" "~"
if joinKuerzel = EHTBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_HAUPTFIRMENNR = EHT_FIRMENNR AND BVO_EINHEITENTABNR = EHT_EINHEITENTABNR" "~"
if joinKuerzel = SLKEHT
searchResult := searchResult "LEFT JOIN WWS.EHT_EINHEITENTAB ON EHT_FIRMENNR = SLK_EHT_FIRMENNR AND EHT_EINHEITENTABNR = SLK_EINHEITENTABNR" "~"
if joinKuerzel = EHTSLK
searchResult := searchResult "LEFT JOIN WWS.SLK_STUECKLISTENKOPF ON SLK_EHT_FIRMENNR = EHT_FIRMENNR AND SLK_EINHEITENTABNR = EHT_EINHEITENTABNR" "~"
if joinKuerzel = ADCEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = ADC_EINHEITKBEZ" "~"
if joinKuerzel = EINADC
searchResult := searchResult "LEFT JOIN WWS.ADC_ABGABENDETAILRECH ON ADC_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = AWEEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = AWE_EINHEITKBEZ" "~"
if joinKuerzel = EINAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PEAEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = PEA_EINHEITKBEZ" "~"
if joinKuerzel = EINPEA
searchResult := searchResult "LEFT JOIN WWS.PEA_PICEANNUMMERN ON PEA_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PIAEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = PIA_GRUNDEINHEITKBEZ" "~"
if joinKuerzel = EINPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_GRUNDEINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PIDEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = PID_EINHEITKBEZ" "~"
if joinKuerzel = EINPID
searchResult := searchResult "LEFT JOIN WWS.PID_PICPREISDETAIL ON PID_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PIEEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = PIE_EINHEITKBEZ" "~"
if joinKuerzel = EINPIE
searchResult := searchResult "LEFT JOIN WWS.PIE_PICEINHEIT ON PIE_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PLCEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = PLC_QTY_EINH" "~"
if joinKuerzel = EINPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_QTY_EINH = EIN_EINHEITKBEZ" "~"
if joinKuerzel = RNREIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = RNR_EINHEITKBEZ" "~"
if joinKuerzel = EINRNR
searchResult := searchResult "LEFT JOIN WWS.RNR_RECHNUNGSRABATT ON RNR_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = TLTEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = TLT_QTY_EINHEIT" "~"
if joinKuerzel = EINTLT
searchResult := searchResult "LEFT JOIN PRODUKTION.TLT_BT_LOGISTIK_TRANSPORT ON TLT_QTY_EINHEIT = EIN_EINHEITKBEZ" "~"
if joinKuerzel = TRPEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = TRP_EINHEITKBEZ" "~"
if joinKuerzel = EINTRP
searchResult := searchResult "LEFT JOIN WWS.TRP_TRANSPORTPOSITION ON TRP_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PCKEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = PCK_ALT_DAUER_EINHEITKBEZ" "~"
if joinKuerzel = EINPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_ALT_DAUER_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PKSDVH
searchResult := searchResult "LEFT JOIN DVH_DIENSTVERHAELTNIS.DVH_DIENSTVERHAELTNIS ON DVH_MIT_FIRMENNR = PKS_PER_FIRMENNR AND DVH_MITARBEITERID = PKS_PER_PERSONENID AND DVH_DIENSTVLFDNR = PKS_DVH_DIENSTVLFDNR" "~"
if joinKuerzel = DVHPKS
searchResult := searchResult "LEFT JOIN PRODUKTION.PKS_SOLL_KAPAZITAET ON PKS_PER_FIRMENNR = DVH_MIT_FIRMENNR AND PKS_PER_PERSONENID = DVH_MITARBEITERID AND PKS_DVH_DIENSTVLFDNR = DVH_DIENSTVLFDNR" "~"
if joinKuerzel = VZZDVH
searchResult := searchResult "LEFT JOIN DVH_DIENSTVERHAELTNIS.DVH_DIENSTVERHAELTNIS ON DVH_MIT_FIRMENNR = VZZ_FIRMENNR AND DVH_MITARBEITERID = VZZ_MITARBEITERID AND DVH_DIENSTVLFDNR = VZZ_DIENSTVLFDNR" "~"
if joinKuerzel = DVHVZZ
searchResult := searchResult "LEFT JOIN HRS.VZZ_VORZEITZU ON VZZ_FIRMENNR = DVH_MIT_FIRMENNR AND VZZ_MITARBEITERID = DVH_MITARBEITERID AND VZZ_DIENSTVLFDNR = DVH_DIENSTVLFDNR" "~"
if joinKuerzel = EEZEEM
searchResult := searchResult "LEFT JOIN WWS.EEM_EDIEINHEITENMAPPING ON EEM_FIRMENNR = EEZ_EEM_FIRMENNR AND EEM_EEMTABNR = EEZ_EEM_EEMTABNR" "~"
if joinKuerzel = EEMEEZ
searchResult := searchResult "LEFT JOIN WWS.EEZ_EDIEINHEITENZU ON EEZ_EEM_FIRMENNR = EEM_FIRMENNR AND EEZ_EEM_EEMTABNR = EEM_EEMTABNR" "~"
if joinKuerzel = KAEEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = KAE_EFK_FIRMENNR AND EFK_ERFASSUNGSKZ = KAE_EFK_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = KAE_EFK_ERFASSUNGSNR" "~"
if joinKuerzel = EFKKAE
searchResult := searchResult "LEFT JOIN WWS.KAE_KASSENEINAUSGABEN ON KAE_EFK_FIRMENNR = EFK_FIRMENNR AND KAE_EFK_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND KAE_EFK_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = EHPEHT
searchResult := searchResult "LEFT JOIN WWS.EHT_EINHEITENTAB ON EHT_FIRMENNR = EHP_FIRMENNR AND EHT_EINHEITENTABNR = EHP_EINHEITENTABNR" "~"
if joinKuerzel = EHTEHP
searchResult := searchResult "LEFT JOIN WWS.EHP_EINHEITENTABPOS ON EHP_FIRMENNR = EHT_FIRMENNR AND EHP_EINHEITENTABNR = EHT_EINHEITENTABNR" "~"
if joinKuerzel = WAGEHT
searchResult := searchResult "LEFT JOIN WWS.EHT_EINHEITENTAB ON EHT_FIRMENNR = WAG_FIRMENNR AND EHT_EINHEITENTABNR = WAG_EINHEITENTABNR" "~"
if joinKuerzel = EHTWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = EHT_FIRMENNR AND WAG_EINHEITENTABNR = EHT_EINHEITENTABNR" "~"
if joinKuerzel = ADWEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = ADW_EINHEITKBEZ" "~"
if joinKuerzel = EINADW
searchResult := searchResult "LEFT JOIN WWS.ADW_ABGABENDETAILWAP ON ADW_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = AGBEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = AGB_SOLL_BM_EINHEITKBEZ" "~"
if joinKuerzel = EINAGB
searchResult := searchResult "LEFT JOIN WWS.AGB_ARBEITSGANGBAZZU ON AGB_SOLL_BM_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = ALSEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = ALS_NWEINHEIT" "~"
if joinKuerzel = EINALS
searchResult := searchResult "LEFT JOIN WWS.ALS_ALLERGEN ON ALS_NWEINHEIT = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PAPEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = PAP_EINHEITKBEZ" "~"
if joinKuerzel = EINPAP
searchResult := searchResult "LEFT JOIN WWS.PAP_PACKSTOFFTABPOS ON PAP_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PTPEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = PTP_EINHEITKBEZ" "~"
if joinKuerzel = EINPTP
searchResult := searchResult "LEFT JOIN WWS.PTP_PREISTABPOS ON PTP_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = VLJEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = VLJ_GRUNDEINHKBEZ" "~"
if joinKuerzel = EINVLJ
searchResult := searchResult "LEFT JOIN WWS.VLJ_VERLEIHJOURNAL ON VLJ_GRUNDEINHKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = WBDEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = WBD_EINHEITKBEZ" "~"
if joinKuerzel = EINWBD
searchResult := searchResult "LEFT JOIN WWS.WBD_WARENBESCHKOSTENDET ON WBD_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = WENEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = WEN_EINHEITKBEZ" "~"
if joinKuerzel = EINWEN
searchResult := searchResult "LEFT JOIN WWS.WEN_WARENEINSATZRNP ON WEN_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = WEPEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = WEP_EINHEITKBEZ" "~"
if joinKuerzel = EINWEP
searchResult := searchResult "LEFT JOIN WWS.WEP_WWSERKONTROLLEPOS ON WEP_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = KOPEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = KOP_EINHEITKBEZ_ZE" "~"
if joinKuerzel = EINKOP
searchResult := searchResult "LEFT JOIN PRODUKTION.KOP_KOMMISSIONIERUNG_POS ON KOP_EINHEITKBEZ_ZE = EIN_EINHEITKBEZ" "~"
if joinKuerzel = RSBEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = RSB_ZAEHLER_EH_1" "~"
if joinKuerzel = EINRSB
searchResult := searchResult "LEFT JOIN BUERO.RSB_RESSOURCENBEDARF ON RSB_ZAEHLER_EH_1 = EIN_EINHEITKBEZ" "~"
if joinKuerzel = WRPEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = WRP_GEWICHTS_EINHEITKBEZ" "~"
if joinKuerzel = EINWRP
searchResult := searchResult "LEFT JOIN WWS.WRP_WWSANKAUFSRNGPOS ON WRP_GEWICHTS_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PLCEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = PLC_PQTY_M_UNIT" "~"
if joinKuerzel = EINPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_PQTY_M_UNIT = EIN_EINHEITKBEZ" "~"
if joinKuerzel = RNPEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = RNP_GEWICHTS_EINHEITKBEZ" "~"
if joinKuerzel = EINRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_GEWICHTS_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = ARLEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = ARL_VERPACKUNGSEINHEITKBEZ" "~"
if joinKuerzel = EINARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_VERPACKUNGSEINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = BONEKZ
searchResult := searchResult "LEFT JOIN WWS.EKZ_ERLOESAUFWANDKZ ON EKZ_FIRMENNR = BON_ERLOESAUFWAND_FIRMENNR AND EKZ_ERLOESAUFWANDKZ = BON_ERLOESAUFWANDKZ" "~"
if joinKuerzel = EKZBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_ERLOESAUFWAND_FIRMENNR = EKZ_FIRMENNR AND BON_ERLOESAUFWANDKZ = EKZ_ERLOESAUFWANDKZ" "~"
if joinKuerzel = DKADOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_ARCHIVNR = DKA_ARCHIVNR AND DOK_DOKUMENTENNR = DKA_DOKUMENTENNR" "~"
if joinKuerzel = DOKDKA
searchResult := searchResult "LEFT JOIN BUERO.DKA_DOCSKATEGORIEZU ON DKA_ARCHIVNR = DOK_ARCHIVNR AND DKA_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = DPRDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_ARCHIVNR = DPR_DOK_ARCHIVNR AND DOK_DOKUMENTENNR = DPR_DOK_DOKUMENTENNR" "~"
if joinKuerzel = DOKDPR
searchResult := searchResult "LEFT JOIN BUERO.DPR_DOKUMENTPROJEKTZU ON DPR_DOK_ARCHIVNR = DOK_ARCHIVNR AND DPR_DOK_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = DVLDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_ARCHIVNR = DVL_DOK_ARCHIVNR AND DOK_DOKUMENTENNR = DVL_DOK_DOKUMENTENNR" "~"
if joinKuerzel = DOKDVL
searchResult := searchResult "LEFT JOIN BUERO.DVL_DOKUMENTVERLAUF ON DVL_DOK_ARCHIVNR = DOK_ARCHIVNR AND DVL_DOK_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = VDZDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_ARCHIVNR = VDZ_DOK_ARCHIVNR AND DOK_DOKUMENTENNR = VDZ_DOK_DOKUMENTENNR" "~"
if joinKuerzel = DOKVDZ
searchResult := searchResult "LEFT JOIN BUERO.VDZ_VSTAMMDOKZU ON VDZ_DOK_ARCHIVNR = DOK_ARCHIVNR AND VDZ_DOK_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = WWBDRS
searchResult := searchResult "LEFT JOIN DRS_DRUCKEREINSTELLUNG.DRS_DRUCKEREINSTELLUNG ON DRS_DREINSTELLUNGSID = WWB_DREINSTELLUNGSID" "~"
if joinKuerzel = DRSWWB
searchResult := searchResult "LEFT JOIN WWS.WWB_WWSBERICHTE ON WWB_DREINSTELLUNGSID = DRS_DREINSTELLUNGSID" "~"
if joinKuerzel = KASDRS
searchResult := searchResult "LEFT JOIN DRS_DRUCKEREINSTELLUNG.DRS_DRUCKEREINSTELLUNG ON DRS_DREINSTELLUNGSID = KAS_BERICHTE_DRSID" "~"
if joinKuerzel = DRSKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_BERICHTE_DRSID = DRS_DREINSTELLUNGSID" "~"
if joinKuerzel = DSVDSK
searchResult := searchResult "LEFT JOIN BUERO.DSK_DSGVO_DATENKATEGORIE ON DSK_DSKID = DSV_VERARBEITER_DSKID" "~"
if joinKuerzel = DSKDSV
searchResult := searchResult "LEFT JOIN BUERO.DSV_DSGVO_VERZEICHNIS ON DSV_VERARBEITER_DSKID = DSK_DSKID" "~"
if joinKuerzel = EKODST
searchResult := searchResult "LEFT JOIN HRS.DST_DIVSTAMM ON DST_DSTID = EKO_ABRKREIS AND DST_LOHNJAHR = EKO_JAHR AND DST_FIRMENNR = EKO_FIRMENNR" "~"
if joinKuerzel = DSTEKO
searchResult := searchResult "LEFT JOIN HRS.EKO_ELDAKONTROLLE ON EKO_ABRKREIS = DST_DSTID AND EKO_JAHR = DST_LOHNJAHR AND EKO_FIRMENNR = DST_FIRMENNR" "~"
if joinKuerzel = DRTDTA
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DTA_DBMDTABELLE ON DTA_DTAID = DRT_HAUPTTABELLE_DTAID" "~"
if joinKuerzel = DTADRT
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DRT_DTABREPLGTABELLEZU ON DRT_HAUPTTABELLE_DTAID = DTA_DTAID" "~"
if joinKuerzel = BMMDVH
searchResult := searchResult "LEFT JOIN DVH_DIENSTVERHAELTNIS.DVH_DIENSTVERHAELTNIS ON DVH_MIT_FIRMENNR = BMM_FIRMENNR AND DVH_MITARBEITERID = BMM_MITARBEITERID AND DVH_DIENSTVLFDNR = BMM_DIENSTVLFDNR" "~"
if joinKuerzel = DVHBMM
searchResult := searchResult "LEFT JOIN HRS.BMM_BUAK_MONMELDUNG ON BMM_FIRMENNR = DVH_MIT_FIRMENNR AND BMM_MITARBEITERID = DVH_MITARBEITERID AND BMM_DIENSTVLFDNR = DVH_DIENSTVLFDNR" "~"
if joinKuerzel = LKVDVH
searchResult := searchResult "LEFT JOIN DVH_DIENSTVERHAELTNIS.DVH_DIENSTVERHAELTNIS ON DVH_MIT_FIRMENNR = LKV_DVH_MIT_FIRMENNR AND DVH_MITARBEITERID = LKV_DVH_MITARBEITERID AND DVH_DIENSTVLFDNR = LKV_DVH_DIENSTVLFDNR" "~"
if joinKuerzel = DVHLKV
searchResult := searchResult "LEFT JOIN HRS.LKV_LOHNKOSTENVERTEILUNG ON LKV_DVH_MIT_FIRMENNR = DVH_MIT_FIRMENNR AND LKV_DVH_MITARBEITERID = DVH_MITARBEITERID AND LKV_DVH_DIENSTVLFDNR = DVH_DIENSTVLFDNR" "~"
if joinKuerzel = PDSDVH
searchResult := searchResult "LEFT JOIN DVH_DIENSTVERHAELTNIS.DVH_DIENSTVERHAELTNIS ON DVH_MIT_FIRMENNR = PDS_DVH_FIRMENNR AND DVH_MITARBEITERID = PDS_DVH_MITARBEITERID AND DVH_DIENSTVLFDNR = PDS_DVH_DIENSTVLFDNR" "~"
if joinKuerzel = DVHPDS
searchResult := searchResult "LEFT JOIN BIL.PDS_PERSKORST_DNSTAMM ON PDS_DVH_FIRMENNR = DVH_MIT_FIRMENNR AND PDS_DVH_MITARBEITERID = DVH_MITARBEITERID AND PDS_DVH_DIENSTVLFDNR = DVH_DIENSTVLFDNR" "~"
if joinKuerzel = PTPDVH
searchResult := searchResult "LEFT JOIN DVH_DIENSTVERHAELTNIS.DVH_DIENSTVERHAELTNIS ON DVH_MIT_FIRMENNR = PTP_DVH_MIT_FIRMENNR AND DVH_MITARBEITERID = PTP_DVH_MITARBEITERID AND DVH_DIENSTVLFDNR = PTP_DVH_DIENSTVLFDNR" "~"
if joinKuerzel = DVHPTP
searchResult := searchResult "LEFT JOIN WWS.PTP_PREISTABPOS ON PTP_DVH_MIT_FIRMENNR = DVH_MIT_FIRMENNR AND PTP_DVH_MITARBEITERID = DVH_MITARBEITERID AND PTP_DVH_DIENSTVLFDNR = DVH_DIENSTVLFDNR" "~"
if joinKuerzel = DHZDWH
searchResult := searchResult "LEFT JOIN DATANORM.DWH_WAREN_HAUPT_GRUPPE ON DWH_WH_GRP = DHZ_WH_GRP AND DWH_ERST_NR = DHZ_ERST_NR" "~"
if joinKuerzel = DWHDHZ
searchResult := searchResult "LEFT JOIN DATANORM.DHZ_DATANORM_WHGZU ON DHZ_WH_GRP = DWH_WH_GRP AND DHZ_ERST_NR = DWH_ERST_NR" "~"
if joinKuerzel = WAKEDN
searchResult := searchResult "LEFT JOIN EDN_EDINACHRICHT.EDN_EDINACHRICHT ON EDN_EDNID = WAK_EDNID" "~"
if joinKuerzel = EDNWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_EDNID = EDN_EDNID" "~"
if joinKuerzel = AFTEFK
searchResult := searchResult "LEFT JOIN EFK_ERFASSKOPFWWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = AFT_EFK_FIRMENNR AND EFK_ERFASSUNGSKZ = AFT_EFK_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = AFT_EFK_ERFASSUNGSNR" "~"
if joinKuerzel = EFKAFT
searchResult := searchResult "LEFT JOIN ZLE.AFT_AUFBEREITUNG ON AFT_EFK_FIRMENNR = EFK_FIRMENNR AND AFT_EFK_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND AFT_EFK_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = AGSEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = AGS_EFK_FIRMENNR AND EFK_ERFASSUNGSKZ = AGS_EFK_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = AGS_EFK_ERFASSUNGSNR" "~"
if joinKuerzel = EFKAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_EFK_FIRMENNR = EFK_FIRMENNR AND AGS_EFK_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND AGS_EFK_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = EDZEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = EDZ_FIRMENNR AND EFK_ERFASSUNGSKZ = EDZ_AUFT_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = EDZ_AUFT_ERFASSUNGSNR" "~"
if joinKuerzel = EFKEDZ
searchResult := searchResult "LEFT JOIN WWS.EDZ_ERFASSDOKUMENTZU ON EDZ_FIRMENNR = EFK_FIRMENNR AND EDZ_AUFT_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND EDZ_AUFT_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = ERDERA
searchResult := searchResult "LEFT JOIN WWS.ERA_ERFASSRABATT ON ERA_FIRMENNR = ERD_FIRMENNR AND ERA_ERFASSRABATTNR = ERD_ERFASSRABATTNR" "~"
if joinKuerzel = ERAERD
searchResult := searchResult "LEFT JOIN WWS.ERD_ERFASSRABATTDETAIL ON ERD_FIRMENNR = ERA_FIRMENNR AND ERD_ERFASSRABATTNR = ERA_ERFASSRABATTNR" "~"
if joinKuerzel = ERKERB
searchResult := searchResult "LEFT JOIN FIBU.ERB_BUCHUNG ON ERB_FIRMENNR = ERK_FIRMENNR AND ERB_FIBUNR = ERK_FIBUNR AND ERB_ERFASSNR = ERK_ERFASSNR" "~"
if joinKuerzel = ERBERK
searchResult := searchResult "LEFT JOIN FIBU.ERK_KONTO ON ERK_FIRMENNR = ERB_FIRMENNR AND ERK_FIBUNR = ERB_FIBUNR AND ERK_ERFASSNR = ERB_ERFASSNR" "~"
if joinKuerzel = BPPERG
searchResult := searchResult "LEFT JOIN WWS.ERG_ERFASSUNGSGRUPPEN ON ERG_ERGID = BPP_ERGID AND ERG_FIRMENNR = BPP_FIRMENNR AND ERG_ERFASSUNGSKZ = BPP_ERFASSUNGSKZ AND ERG_ERFASSUNGSNR = BPP_BESTPRODERFASSUNGSNR AND ERG_ERFGRUPPE_EBENE1 = BPP_ERFGRUPPE_EBENE1 AND ERG_ERFGRUPPE_EBENE2 = BPP_ERFGRUPPE_EBENE2 AND ERG_ERFGRUPPE_EBENE3 = BPP_ERFGRUPPE_EBENE3" "~"
if joinKuerzel = ERGBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_ERGID = ERG_ERGID AND BPP_FIRMENNR = ERG_FIRMENNR AND BPP_ERFASSUNGSKZ = ERG_ERFASSUNGSKZ AND BPP_BESTPRODERFASSUNGSNR = ERG_ERFASSUNGSNR AND BPP_ERFGRUPPE_EBENE1 = ERG_ERFGRUPPE_EBENE1 AND BPP_ERFGRUPPE_EBENE2 = ERG_ERFGRUPPE_EBENE2 AND BPP_ERFGRUPPE_EBENE3 = ERG_ERFGRUPPE_EBENE3" "~"
if joinKuerzel = ELVERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_FIRMENNR = ELV_FIRMENNR AND ERO_ERFASSUNGSKZ = ELV_ERFASSUNGSKZ AND ERO_ERFASSUNGSNR = ELV_ERFASSUNGSNR AND ERO_ERFASSPOSLFDNR = ELV_ERFASSPOSLFDNR AND ERO_ERFASSPOSORIGLFDNR = ELV_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = EROELV
searchResult := searchResult "LEFT JOIN WWS.ELV_ERFASSPOSLIEFERVOR ON ELV_FIRMENNR = ERO_FIRMENNR AND ELV_ERFASSUNGSKZ = ERO_ERFASSUNGSKZ AND ELV_ERFASSUNGSNR = ERO_ERFASSUNGSNR AND ELV_ERFASSPOSLFDNR = ERO_ERFASSPOSLFDNR AND ELV_ERFASSPOSORIGLFDNR = ERO_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = LPOERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_FIRMENNR = LPO_FIRMENNR AND ERO_ERFASSUNGSKZ = LPO_ERFASSUNGSKZ AND ERO_ERFASSUNGSNR = LPO_ERFASSUNGSNR AND ERO_ERFASSPOSLFDNR = LPO_ERFASSPOSLFDNR AND ERO_ERFASSPOSORIGLFDNR = LPO_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = EROLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_FIRMENNR = ERO_FIRMENNR AND LPO_ERFASSUNGSKZ = ERO_ERFASSUNGSKZ AND LPO_ERFASSUNGSNR = ERO_ERFASSUNGSNR AND LPO_ERFASSPOSLFDNR = ERO_ERFASSPOSLFDNR AND LPO_ERFASSPOSORIGLFDNR = ERO_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = RNPERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_FIRMENNR = RNP_FIRMENNR AND ERO_ERFASSUNGSKZ = RNP_ERFASSUNGSKZ AND ERO_ERFASSUNGSNR = RNP_ERFASSUNGSNR AND ERO_ERFASSPOSLFDNR = RNP_ERFASSPOSLFDNR AND ERO_ERFASSPOSORIGLFDNR = RNP_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = ERORNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = ERO_FIRMENNR AND RNP_ERFASSUNGSKZ = ERO_ERFASSUNGSKZ AND RNP_ERFASSUNGSNR = ERO_ERFASSUNGSNR AND RNP_ERFASSPOSLFDNR = ERO_ERFASSPOSLFDNR AND RNP_ERFASSPOSORIGLFDNR = ERO_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = VPDERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_FIRMENNR = VPD_ERO_FIRMENNR AND ERO_ERFASSUNGSKZ = VPD_EFO_ERFASSUNGSKZ AND ERO_ERFASSUNGSNR = VPD_ERO_ERFASSUNGSNR AND ERO_ERFASSPOSLFDNR = VPD_ERO_ERFASSPOSLFDNR AND ERO_ERFASSPOSORIGLFDNR = VPD_ERO_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = EROVPD
searchResult := searchResult "LEFT JOIN WWS.VPD_VPI_ERHOEHUNGDETAIL ON VPD_ERO_FIRMENNR = ERO_FIRMENNR AND VPD_EFO_ERFASSUNGSKZ = ERO_ERFASSUNGSKZ AND VPD_ERO_ERFASSUNGSNR = ERO_ERFASSUNGSNR AND VPD_ERO_ERFASSPOSLFDNR = ERO_ERFASSPOSLFDNR AND VPD_ERO_ERFASSPOSORIGLFDNR = ERO_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = WAPERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_ERFASSPOSORIGID = WAP_ERFASSPOSORIGID" "~"
if joinKuerzel = EROWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_ERFASSPOSORIGID = ERO_ERFASSPOSORIGID" "~"
if joinKuerzel = VPPERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_FIRMENNR = VPP_WWSERF_FIRMENNR AND ERP_ERFASSUNGSKZ = VPP_WWSERF_KZ AND ERP_ERFASSUNGSNR = VPP_WWSERF_NR AND ERP_ERFASSPOSLFDNR = VPP_WWSERF_POSNR" "~"
if joinKuerzel = ERPVPP
searchResult := searchResult "LEFT JOIN PRODUKTION.VPP_VIEW_PLC_PPS_CHARGE ON VPP_WWSERF_FIRMENNR = ERP_FIRMENNR AND VPP_WWSERF_KZ = ERP_ERFASSUNGSKZ AND VPP_WWSERF_NR = ERP_ERFASSUNGSNR AND VPP_WWSERF_POSNR = ERP_ERFASSPOSLFDNR" "~"
if joinKuerzel = AVEERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_FIRMENNR = AVE_REFERP_FIRMENNR AND ERP_ERFASSUNGSKZ = AVE_REFERP_ERFASSUNGSKZ AND ERP_ERFASSUNGSNR = AVE_REFERP_ERFASSUNGSNR AND ERP_ERFASSPOSLFDNR = AVE_REFERP_ERFASSPOSLFDNR" "~"
if joinKuerzel = ERPAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_REFERP_FIRMENNR = ERP_FIRMENNR AND AVE_REFERP_ERFASSUNGSKZ = ERP_ERFASSUNGSKZ AND AVE_REFERP_ERFASSUNGSNR = ERP_ERFASSUNGSNR AND AVE_REFERP_ERFASSPOSLFDNR = ERP_ERFASSPOSLFDNR" "~"
if joinKuerzel = AVEERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = AVE_ERT_KBESC_FIRMENNR AND ERT_ERFASSTEXTNR = AVE_ERT_KBESC_ERFASSTEXTNR" "~"
if joinKuerzel = ERTAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ERT_KBESC_FIRMENNR = ERT_FIRMENNR AND AVE_ERT_KBESC_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = EFKERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = EFK_FIRMENNR AND ERT_ERFASSTEXTNR = EFK_STEUER_ERFASSTEXTNR" "~"
if joinKuerzel = ERTEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = ERT_FIRMENNR AND EFK_STEUER_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = BVOERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = BVO_ERT_FIRMENNR AND ERT_ERFASSTEXTNR = BVO_NACH_ERFASSTEXTNR" "~"
if joinKuerzel = ERTBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_ERT_FIRMENNR = ERT_FIRMENNR AND BVO_NACH_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = ERPERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = ERP_FIRMENNR AND ERT_ERFASSTEXTNR = ERP_BEZ_ERFASSTEXTNR" "~"
if joinKuerzel = ERTERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_FIRMENNR = ERT_FIRMENNR AND ERP_BEZ_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = KAPERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = KAP_ERT_KBEZ_FIRMENNR AND ERT_ERFASSTEXTNR = KAP_ERT_KBEZ_ERFASSTEXTNR" "~"
if joinKuerzel = ERTKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_ERT_KBEZ_FIRMENNR = ERT_FIRMENNR AND KAP_ERT_KBEZ_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = RNPEGS
searchResult := searchResult "LEFT JOIN EGS_ERFASSUNGSGRUND.EGS_ERFASSUNGSGRUND ON EGS_GRUND_ID = RNP_VKPREISGRUND_ID" "~"
if joinKuerzel = EGSRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_VKPREISGRUND_ID = EGS_GRUND_ID" "~"
if joinKuerzel = EHTEHT
searchResult := searchResult "LEFT JOIN WWS.EHT_EINHEITENTAB ON EHT_FIRMENNR = EHT_FIRMENNR AND EHT_EINHEITENTABNR = EHT_VORL_EINHEITENTABNR" "~"
if joinKuerzel = EHTEHT
searchResult := searchResult "LEFT JOIN WWS.EHT_EINHEITENTAB ON EHT_FIRMENNR = EHT_FIRMENNR AND EHT_VORL_EINHEITENTABNR = EHT_EINHEITENTABNR" "~"
if joinKuerzel = ERPEHT
searchResult := searchResult "LEFT JOIN WWS.EHT_EINHEITENTAB ON EHT_FIRMENNR = ERP_ARL_FIRMENNR AND EHT_EINHEITENTABNR = ERP_EINHEITENTABNR" "~"
if joinKuerzel = EHTERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_ARL_FIRMENNR = EHT_FIRMENNR AND ERP_EINHEITENTABNR = EHT_EINHEITENTABNR" "~"
if joinKuerzel = KAPEHT
searchResult := searchResult "LEFT JOIN WWS.EHT_EINHEITENTAB ON EHT_FIRMENNR = KAP_ARL_FIRMENNR AND EHT_EINHEITENTABNR = KAP_EHT_EINHEITENTABNR" "~"
if joinKuerzel = EHTKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_ARL_FIRMENNR = EHT_FIRMENNR AND KAP_EHT_EINHEITENTABNR = EHT_EINHEITENTABNR" "~"
if joinKuerzel = ADEEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = ADE_EINHEITKBEZ" "~"
if joinKuerzel = EINADE
searchResult := searchResult "LEFT JOIN WWS.ADE_ABGABENDETAILERFPOS ON ADE_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = EKJEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = EKJ_EIN_EINHEITKBEZ" "~"
if joinKuerzel = EINEKJ
searchResult := searchResult "LEFT JOIN PRODUKTION.EKJ_ERP_KOREJOURNAL ON EKJ_EIN_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PJOEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = PJO_EINHEITKBEZ" "~"
if joinKuerzel = EINPJO
searchResult := searchResult "LEFT JOIN WWS.PJO_PREISJOURNAL ON PJO_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PUIEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = PUI_EINHEITKBEZ" "~"
if joinKuerzel = EINPUI
searchResult := searchResult "LEFT JOIN WWS.PUI_PICSTKL ON PUI_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = RNGEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = RNG_GEWICHT_EINHEITKBEZ" "~"
if joinKuerzel = EINRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_GEWICHT_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = TLEEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = TLE_QTY_EINHEIT" "~"
if joinKuerzel = EINTLE
searchResult := searchResult "LEFT JOIN PRODUKTION.TLE_BT_LOGISTIK_EMPFANGEN ON TLE_QTY_EINHEIT = EIN_EINHEITKBEZ" "~"
if joinKuerzel = TRJEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = TRJ_EINHEITKBEZ" "~"
if joinKuerzel = EINTRJ
searchResult := searchResult "LEFT JOIN WWS.TRJ_TRANSPORTEINH_JOURNAL ON TRJ_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = WABEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = WAB_EINHEITKBEZ" "~"
if joinKuerzel = EINWAB
searchResult := searchResult "LEFT JOIN WWS.WAB_WARENEINGANGBUCH ON WAB_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PIDEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = PID_STAFFELMG_EINHEITKBEZ" "~"
if joinKuerzel = EINPID
searchResult := searchResult "LEFT JOIN WWS.PID_PICPREISDETAIL ON PID_STAFFELMG_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = WAPEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = WAP_LIEFER_EINHEITKBEZ" "~"
if joinKuerzel = EINWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_LIEFER_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PIAEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = PIA_VERKPREISEINHEITKBEZ" "~"
if joinKuerzel = EINPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_VERKPREISEINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = AVEEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = AVE_ERPESD_ZAEHLER_EH_3" "~"
if joinKuerzel = EINAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ERPESD_ZAEHLER_EH_3 = EIN_EINHEITKBEZ" "~"
if joinKuerzel = AVEEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = AVE_EFKESD_ZAEHLER_EH_2" "~"
if joinKuerzel = EINAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_EFKESD_ZAEHLER_EH_2 = EIN_EINHEITKBEZ" "~"
if joinKuerzel = AVEEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = AVE_EFKESD_ZAEHLER_EH_3" "~"
if joinKuerzel = EINAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_EFKESD_ZAEHLER_EH_3 = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PIAEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = PIA_GRUNDPREISEINHEITKBEZ" "~"
if joinKuerzel = EINPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_GRUNDPREISEINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = AVEEKZ
searchResult := searchResult "LEFT JOIN WWS.EKZ_ERLOESAUFWANDKZ ON EKZ_FIRMENNR = AVE_EKZ_FIRMENNR AND EKZ_ERLOESAUFWANDKZ = AVE_EKZ_ERLOESAUFWANDKZ" "~"
if joinKuerzel = EKZAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_EKZ_FIRMENNR = EKZ_FIRMENNR AND AVE_EKZ_ERLOESAUFWANDKZ = EKZ_ERLOESAUFWANDKZ" "~"
if joinKuerzel = WADEKZ
searchResult := searchResult "LEFT JOIN WWS.EKZ_ERLOESAUFWANDKZ ON EKZ_FIRMENNR = WAD_HAUPTFIRMENNR AND EKZ_ERLOESAUFWANDKZ = WAD_ERLOESAUFWANDKZ" "~"
if joinKuerzel = EKZWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_HAUPTFIRMENNR = EKZ_FIRMENNR AND WAD_ERLOESAUFWANDKZ = EKZ_ERLOESAUFWANDKZ" "~"
if joinKuerzel = WAPEKZ
searchResult := searchResult "LEFT JOIN WWS.EKZ_ERLOESAUFWANDKZ ON EKZ_FIRMENNR = WAP_ERLOESAUFWAND_FIRMENNR AND EKZ_ERLOESAUFWANDKZ = WAP_ERLOESAUFWANDKZ" "~"
if joinKuerzel = EKZWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_ERLOESAUFWAND_FIRMENNR = EKZ_FIRMENNR AND WAP_ERLOESAUFWANDKZ = EKZ_ERLOESAUFWANDKZ" "~"
if joinKuerzel = PKZEMA
searchResult := searchResult "LEFT JOIN BUERO.EMA_EMAIL ON EMA_EMAILLFDNR = PKZ_EMAILLFDNR" "~"
if joinKuerzel = EMAPKZ
searchResult := searchResult "LEFT JOIN BUERO.PKZ_PERSONKONTAKTZU ON PKZ_EMAILLFDNR = EMA_EMAILLFDNR" "~"
if joinKuerzel = PLSEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = PLS_NWEINHEIT" "~"
if joinKuerzel = EINPLS
searchResult := searchResult "LEFT JOIN WWS.PLS_PICALLERGEN ON PLS_NWEINHEIT = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PWPEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = PWP_EP_EINHEITKBEZ" "~"
if joinKuerzel = EINPWP
searchResult := searchResult "LEFT JOIN WWS.PWP_PREISWARTUNGPOS ON PWP_EP_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = VFAEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = VFA_BEZUGSMENGE_EINHEITKBEZ" "~"
if joinKuerzel = EINVFA
searchResult := searchResult "LEFT JOIN PRODUKTION.VFA_VERFAHRENSANWEISUNG_KOPF ON VFA_BEZUGSMENGE_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = WBTEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = WBT_EINHEITKBEZ" "~"
if joinKuerzel = EINWBT
searchResult := searchResult "LEFT JOIN WWS.WBT_WWSBUDGETKOPF ON WBT_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = WEOEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = WEO_EINHEITKBEZ" "~"
if joinKuerzel = EINWEO
searchResult := searchResult "LEFT JOIN WWS.WEO_WARENEINSATZLPO ON WEO_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = DISEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = DIS_GE_EINHEITKBEZ" "~"
if joinKuerzel = EINDIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_GE_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PCPEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = PCP_QTY_EINH_2" "~"
if joinKuerzel = EINPCP
searchResult := searchResult "LEFT JOIN PRODUKTION.PCP_PPS_PRODUKT ON PCP_QTY_EINH_2 = EIN_EINHEITKBEZ" "~"
if joinKuerzel = TRTEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = TRT_EINHEITKBEZ_LBH" "~"
if joinKuerzel = EINTRT
searchResult := searchResult "LEFT JOIN WWS.TRT_TRANSPORTTYP ON TRT_EINHEITKBEZ_LBH = EIN_EINHEITKBEZ" "~"
if joinKuerzel = RSBEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = RSB_ZAEHLER_EH_2" "~"
if joinKuerzel = EINRSB
searchResult := searchResult "LEFT JOIN BUERO.RSB_RESSOURCENBEDARF ON RSB_ZAEHLER_EH_2 = EIN_EINHEITKBEZ" "~"
if joinKuerzel = RSBEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = RSB_ZAEHLER_EH_3" "~"
if joinKuerzel = EINRSB
searchResult := searchResult "LEFT JOIN BUERO.RSB_RESSOURCENBEDARF ON RSB_ZAEHLER_EH_3 = EIN_EINHEITKBEZ" "~"
if joinKuerzel = ARLEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = ARL_EINHEITKBEZ_LBH" "~"
if joinKuerzel = EINARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_EINHEITKBEZ_LBH = EIN_EINHEITKBEZ" "~"
if joinKuerzel = EFKEKP
searchResult := searchResult "LEFT JOIN PRODUKTION.EKP_VIEW_ERFASSKOPFPPS ON EKP_FIRMENNR = EFK_FIRMENNR AND EKP_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND EKP_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = EKPEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = EKP_FIRMENNR AND EFK_ERFASSUNGSKZ = EKP_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = EKP_ERFASSUNGSNR" "~"
if joinKuerzel = WEOEKZ
searchResult := searchResult "LEFT JOIN WWS.EKZ_ERLOESAUFWANDKZ ON EKZ_FIRMENNR = WEO_EKZ_FIRMENNR AND EKZ_ERLOESAUFWANDKZ = WEO_ERLOESAUFWANDKZ" "~"
if joinKuerzel = EKZWEO
searchResult := searchResult "LEFT JOIN WWS.WEO_WARENEINSATZLPO ON WEO_EKZ_FIRMENNR = EKZ_FIRMENNR AND WEO_ERLOESAUFWANDKZ = EKZ_ERLOESAUFWANDKZ" "~"
if joinKuerzel = WENEKZ
searchResult := searchResult "LEFT JOIN WWS.EKZ_ERLOESAUFWANDKZ ON EKZ_FIRMENNR = WEN_EKZ_EING_FIRMENNR AND EKZ_ERLOESAUFWANDKZ = WEN_EING_ERLOESAUFWANDKZ" "~"
if joinKuerzel = EKZWEN
searchResult := searchResult "LEFT JOIN WWS.WEN_WARENEINSATZRNP ON WEN_EKZ_EING_FIRMENNR = EKZ_FIRMENNR AND WEN_EING_ERLOESAUFWANDKZ = EKZ_ERLOESAUFWANDKZ" "~"
if joinKuerzel = DISELV
searchResult := searchResult "LEFT JOIN WWS.ELV_ERFASSPOSLIEFERVOR ON ELV_FIRMENNR = DIS_FIRMENNR AND ELV_ERFASSUNGSKZ = DIS_ERO_ERFASSUNGSKZ AND ELV_ERFASSUNGSNR = DIS_ERO_ERFASSUNGSNR AND ELV_ERFASSPOSLFDNR = DIS_ERO_ERFASSPOSLFDNR AND ELV_ERFASSPOSORIGLFDNR = DIS_ERO_ERFASSPOSORIGLFDNR AND ELV_ERFASSPOSLVLFDNR = DIS_ELV_ERFASSPOSLVLFDNR" "~"
if joinKuerzel = ELVDIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_FIRMENNR = ELV_FIRMENNR AND DIS_ERO_ERFASSUNGSKZ = ELV_ERFASSUNGSKZ AND DIS_ERO_ERFASSUNGSNR = ELV_ERFASSUNGSNR AND DIS_ERO_ERFASSPOSLFDNR = ELV_ERFASSPOSLFDNR AND DIS_ERO_ERFASSPOSORIGLFDNR = ELV_ERFASSPOSORIGLFDNR AND DIS_ELV_ERFASSPOSLVLFDNR = ELV_ERFASSPOSLVLFDNR" "~"
if joinKuerzel = TRPELV
searchResult := searchResult "LEFT JOIN WWS.ELV_ERFASSPOSLIEFERVOR ON ELV_FIRMENNR = TRP_LIEFER_FIRMENNR AND ELV_ERFASSUNGSKZ = TRP_ELV_ERFASSUNGSKZ AND ELV_ERFASSUNGSNR = TRP_ELV_ERFASSUNGSNR AND ELV_ERFASSPOSLFDNR = TRP_ELV_ERFASSPOSLFDNR AND ELV_ERFASSPOSORIGLFDNR = TRP_ELV_ERFASSPOSORIGLFDNR AND ELV_ERFASSPOSLVLFDNR = TRP_ELV_ERFASSPOSLVLFDNR" "~"
if joinKuerzel = ELVTRP
searchResult := searchResult "LEFT JOIN WWS.TRP_TRANSPORTPOSITION ON TRP_LIEFER_FIRMENNR = ELV_FIRMENNR AND TRP_ELV_ERFASSUNGSKZ = ELV_ERFASSUNGSKZ AND TRP_ELV_ERFASSUNGSNR = ELV_ERFASSUNGSNR AND TRP_ELV_ERFASSPOSLFDNR = ELV_ERFASSPOSLFDNR AND TRP_ELV_ERFASSPOSORIGLFDNR = ELV_ERFASSPOSORIGLFDNR AND TRP_ELV_ERFASSPOSLVLFDNR = ELV_ERFASSPOSLVLFDNR" "~"
if joinKuerzel = EM4EM3
searchResult := searchResult "LEFT JOIN HRS.EM3_ELDADETAIL_TG ON EM3_EM3ID = EM4_EM3ID" "~"
if joinKuerzel = EM3EM4
searchResult := searchResult "LEFT JOIN HRS.EM4_ELDADETAIL_VB ON EM4_EM3ID = EM3_EM3ID" "~"
if joinKuerzel = APAEPS
searchResult := searchResult "LEFT JOIN EPS_ERFASSPOS_STUECKLISTE.EPS_ERFASSPOS_STUECKLISTE ON EPS_EPSTKLISTID = APA_EPSTKLISTID" "~"
if joinKuerzel = EPSAPA
searchResult := searchResult "LEFT JOIN PRODUKTION.APA_ARBEITSPLAN ON APA_EPSTKLISTID = EPS_EPSTKLISTID" "~"
if joinKuerzel = BOPEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_EPSTKLISTID = BOP_EPS_EPSTKLISTID" "~"
if joinKuerzel = EPSBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_EPS_EPSTKLISTID = EPS_EPSTKLISTID" "~"
if joinKuerzel = EARERN
searchResult := searchResult "LEFT JOIN WWS.ERN_EXTARLKEYNAMES ON ERN_ERNLFDNR = EAR_ERNLFDNR" "~"
if joinKuerzel = ERNEAR
searchResult := searchResult "LEFT JOIN WWS.EAR_EXTARTIKELWERTE ON EAR_ERNLFDNR = ERN_ERNLFDNR" "~"
if joinKuerzel = LEIEFK
searchResult := searchResult "LEFT JOIN EFK_ERFASSKOPFWWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = LEI_AUFTR_FIRMENNR AND EFK_ERFASSUNGSKZ = LEI_AUFTR_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = LEI_AUFTR_ERFASSUNGSNR" "~"
if joinKuerzel = EFKLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_AUFTR_FIRMENNR = EFK_FIRMENNR AND LEI_AUFTR_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND LEI_AUFTR_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = RNPEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = RNP_FIRMENNR AND EFK_ERFASSUNGSKZ = RNP_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = RNP_ERFASSUNGSNR" "~"
if joinKuerzel = EFKRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = EFK_FIRMENNR AND RNP_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND RNP_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = EFKEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = EFK_ORGANGEB_FIRMENNR AND EFK_ERFASSUNGSKZ = EFK_ORGANGEB_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = EFK_ORGANGEB_ERFASSUNGSNR" "~"
if joinKuerzel = EFKEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_ORGANGEB_FIRMENNR = EFK_FIRMENNR AND EFK_ORGANGEB_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND EFK_ORGANGEB_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = EFWEFP
searchResult := searchResult "LEFT JOIN HRS.EFP_ENTGELTFORTZAHLPROZ ON EFP_DBSNR = EFW_DATENBESTANDNR AND EFP_EFANR = EFW_EFANR AND EFP_GUELTIGAB = EFW_GUELTIGAB AND EFP_EFPGESETZ = EFW_EFPGESETZ AND EFP_EFPART = EFW_EFPART" "~"
if joinKuerzel = EFPEFW
searchResult := searchResult "LEFT JOIN HRS.EFW_ENTGELTFORTZ_WERTE ON EFW_DATENBESTANDNR = EFP_DBSNR AND EFW_EFANR = EFP_EFANR AND EFW_GUELTIGAB = EFP_GUELTIGAB AND EFW_EFPGESETZ = EFP_EFPGESETZ AND EFW_EFPART = EFP_EFPART" "~"
if joinKuerzel = BOPEGS
searchResult := searchResult "LEFT JOIN EGS_ERFASSUNGSGRUND.EGS_ERFASSUNGSGRUND ON EGS_GRUND_ID = BOP_VKPREISGRUND_ID" "~"
if joinKuerzel = EGSBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_VKPREISGRUND_ID = EGS_GRUND_ID" "~"
if joinKuerzel = EFKEGS
searchResult := searchResult "LEFT JOIN EGS_ERFASSUNGSGRUND.EGS_ERFASSUNGSGRUND ON EGS_GRUND_ID = EFK_ABLEHNUNGS_GRUND_ID" "~"
if joinKuerzel = EGSEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_ABLEHNUNGS_GRUND_ID = EGS_GRUND_ID" "~"
if joinKuerzel = PCKEGS
searchResult := searchResult "LEFT JOIN EGS_ERFASSUNGSGRUND.EGS_ERFASSUNGSGRUND ON EGS_GRUND_ID = PCK_SPERR_GRUND_ID" "~"
if joinKuerzel = EGSPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_SPERR_GRUND_ID = EGS_GRUND_ID" "~"
if joinKuerzel = KAREHP
searchResult := searchResult "LEFT JOIN WWS.EHP_EINHEITENTABPOS ON EHP_FIRMENNR = KAR_EHP_FIRMENNR AND EHP_EINHEITENTABNR = KAR_EHP_EINHEITENTABNR AND EHP_EINHEITKBEZ = KAR_EHP_EINHEITKBEZ" "~"
if joinKuerzel = EHPKAR
searchResult := searchResult "LEFT JOIN WWS.KAR_KUNDENARTIKEL ON KAR_EHP_FIRMENNR = EHP_FIRMENNR AND KAR_EHP_EINHEITENTABNR = EHP_EINHEITENTABNR AND KAR_EHP_EINHEITKBEZ = EHP_EINHEITKBEZ" "~"
if joinKuerzel = LAJEHT
searchResult := searchResult "LEFT JOIN WWS.EHT_EINHEITENTAB ON EHT_FIRMENNR = LAJ_ARTIKELFIRMENNR AND EHT_EINHEITENTABNR = LAJ_EINHEITENTABNR" "~"
if joinKuerzel = EHTLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_ARTIKELFIRMENNR = EHT_FIRMENNR AND LAJ_EINHEITENTABNR = EHT_EINHEITENTABNR" "~"
if joinKuerzel = ADBEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = ADB_EINHEITKBEZ" "~"
if joinKuerzel = EINADB
searchResult := searchResult "LEFT JOIN WWS.ADB_ABGABENDETAILBON ON ADB_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = ALNEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = ALN_EINHEITKBEZ" "~"
if joinKuerzel = EINALN
searchResult := searchResult "LEFT JOIN PRODUKTION.ALN_ALIBINUMMERN ON ALN_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = BVOEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = BVO_EINHEITKBEZ" "~"
if joinKuerzel = EINBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = EKKEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = EKK_EINHEITKBEZ" "~"
if joinKuerzel = EINEKK
searchResult := searchResult "LEFT JOIN WWS.EKK_EXTERNE_KUNDENKARTEI ON EKK_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = ERAEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = ERA_EINHEITKBEZ" "~"
if joinKuerzel = EINERA
searchResult := searchResult "LEFT JOIN WWS.ERA_ERFASSRABATT ON ERA_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = IMZEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = IMZ_Z_EINHEITKBEZ" "~"
if joinKuerzel = EINIMZ
searchResult := searchResult "LEFT JOIN BUERO.IMZ_INSTANDHMASSNZU ON IMZ_Z_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PCPEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = PCP_QTY_EINH" "~"
if joinKuerzel = EINPCP
searchResult := searchResult "LEFT JOIN PRODUKTION.PCP_PPS_PRODUKT ON PCP_QTY_EINH = EIN_EINHEITKBEZ" "~"
if joinKuerzel = RPMEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = RPM_PCP_EINHEITKBEZ" "~"
if joinKuerzel = EINRPM
searchResult := searchResult "LEFT JOIN PRODUKTION.RPM_RETRO_PRODUKT_MATERIAL ON RPM_PCP_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = RSBEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = RSB_BEZUGS_EINHEIT" "~"
if joinKuerzel = EINRSB
searchResult := searchResult "LEFT JOIN BUERO.RSB_RESSOURCENBEDARF ON RSB_BEZUGS_EINHEIT = EIN_EINHEITKBEZ" "~"
if joinKuerzel = TREEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = TRE_EINHEITKBEZ_LBH" "~"
if joinKuerzel = EINTRE
searchResult := searchResult "LEFT JOIN WWS.TRE_TRANSPORTEINHEIT ON TRE_EINHEITKBEZ_LBH = EIN_EINHEITKBEZ" "~"
if joinKuerzel = VFDEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = VFD_PROZESSEINHEIT" "~"
if joinKuerzel = EINVFD
searchResult := searchResult "LEFT JOIN PRODUKTION.VFD_VERFAHRENSANWEISUNG_POS ON VFD_PROZESSEINHEIT = EIN_EINHEITKBEZ" "~"
if joinKuerzel = WEJEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = WEJ_EINHEITKBEZ" "~"
if joinKuerzel = EINWEJ
searchResult := searchResult "LEFT JOIN WWS.WEJ_WARENEINSATZLAJ ON WEJ_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = WAFEKZ
searchResult := searchResult "LEFT JOIN WWS.EKZ_ERLOESAUFWANDKZ ON EKZ_FIRMENNR = WAF_EKZ_FIRMENNR AND EKZ_ERLOESAUFWANDKZ = WAF_ERLOESAUFWANDKZ" "~"
if joinKuerzel = EKZWAF
searchResult := searchResult "LEFT JOIN WWS.WAF_WWSADRZUFIRMENBEZ ON WAF_EKZ_FIRMENNR = EKZ_FIRMENNR AND WAF_ERLOESAUFWANDKZ = EKZ_ERLOESAUFWANDKZ" "~"
if joinKuerzel = WEOEKZ
searchResult := searchResult "LEFT JOIN WWS.EKZ_ERLOESAUFWANDKZ ON EKZ_FIRMENNR = WEO_EKZ_EING_FIRMENNR AND EKZ_ERLOESAUFWANDKZ = WEO_EING_ERLOESAUFWANDKZ" "~"
if joinKuerzel = EKZWEO
searchResult := searchResult "LEFT JOIN WWS.WEO_WARENEINSATZLPO ON WEO_EKZ_EING_FIRMENNR = EKZ_FIRMENNR AND WEO_EING_ERLOESAUFWANDKZ = EKZ_ERLOESAUFWANDKZ" "~"
if joinKuerzel = WKPEKZ
searchResult := searchResult "LEFT JOIN WWS.EKZ_ERLOESAUFWANDKZ ON EKZ_FIRMENNR = WKP_FIRMENNR AND EKZ_ERLOESAUFWANDKZ = WKP_ERLOESKZ" "~"
if joinKuerzel = EKZWKP
searchResult := searchResult "LEFT JOIN WWS.WKP_WWSKONTENTABPOS ON WKP_FIRMENNR = EKZ_FIRMENNR AND WKP_ERLOESKZ = EKZ_ERLOESAUFWANDKZ" "~"
if joinKuerzel = ALEEMA
searchResult := searchResult "LEFT JOIN BUERO.EMA_EMAIL ON EMA_EMAILLFDNR = ALE_EMAILLFDNR" "~"
if joinKuerzel = EMAALE
searchResult := searchResult "LEFT JOIN BUERO.ALE_ADRESSENLISTE ON ALE_EMAILLFDNR = EMA_EMAILLFDNR" "~"
if joinKuerzel = ERPEPP
searchResult := searchResult "LEFT JOIN PRODUKTION.EPP_VIEW_ERFASSPOSPPS ON EPP_AP_ID = ERP_AP_ID" "~"
if joinKuerzel = EPPERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_AP_ID = EPP_AP_ID" "~"
if joinKuerzel = AAPEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_EPSTKLISTID = AAP_AUFTRAGS_EPSTKLISTID" "~"
if joinKuerzel = EPSAAP
searchResult := searchResult "LEFT JOIN WWS.AAP_ARAPOSITION ON AAP_AUFTRAGS_EPSTKLISTID = EPS_EPSTKLISTID" "~"
if joinKuerzel = KPOEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_EPSTKLISTID = KPO_EPSTKLISTID" "~"
if joinKuerzel = EPSKPO
searchResult := searchResult "LEFT JOIN WWS.KPO_KASSENPROVISION ON KPO_EPSTKLISTID = EPS_EPSTKLISTID" "~"
if joinKuerzel = RPREPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_EPSTKLISTID = RPR_EPSTKLISTID" "~"
if joinKuerzel = EPSRPR
searchResult := searchResult "LEFT JOIN WWS.RPR_RECHNUNGSPROVISION ON RPR_EPSTKLISTID = EPS_EPSTKLISTID" "~"
if joinKuerzel = WAZEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_EPSTKLISTID = WAZ_EPSTKLISTID" "~"
if joinKuerzel = EPSWAZ
searchResult := searchResult "LEFT JOIN WWS.WAZ_WARENZUORDNUNG ON WAZ_EPSTKLISTID = EPS_EPSTKLISTID" "~"
if joinKuerzel = GEPEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_EPSTKLISTID = GEP_PRODUKTIONS_EPSTKLISTID" "~"
if joinKuerzel = EPSGEP
searchResult := searchResult "LEFT JOIN WWS.GEP_GEFAHRENGUTPOS ON GEP_PRODUKTIONS_EPSTKLISTID = EPS_EPSTKLISTID" "~"
if joinKuerzel = EPVEPV
searchResult := searchResult "LEFT JOIN WWS.EPV_ERFASSPOSRECHVOR ON EPV_FIRMENNR = EPV_FIRMENNR AND EPV_ERFASSUNGSKZ = EPV_ERFASSUNGSKZ AND EPV_ERFASSUNGSNR = EPV_ERFASSUNGSNR AND EPV_ERFASSPOSLFDNR = EPV_H_ERFASSPOSLFDNR AND EPV_ERFASSPOSORIGLFDNR = EPV_H_ERFASSPOSORIGLFDNR AND EPV_ERFASSPOSRVLFDNR = EPV_H_ERFASSPOSRVLFDNR" "~"
if joinKuerzel = EPVEPV
searchResult := searchResult "LEFT JOIN WWS.EPV_ERFASSPOSRECHVOR ON EPV_FIRMENNR = EPV_FIRMENNR AND EPV_ERFASSUNGSKZ = EPV_ERFASSUNGSKZ AND EPV_ERFASSUNGSNR = EPV_ERFASSUNGSNR AND EPV_H_ERFASSPOSLFDNR = EPV_ERFASSPOSLFDNR AND EPV_H_ERFASSPOSORIGLFDNR = EPV_ERFASSPOSORIGLFDNR AND EPV_H_ERFASSPOSRVLFDNR = EPV_ERFASSPOSRVLFDNR" "~"
if joinKuerzel = EBPERB
searchResult := searchResult "LEFT JOIN FIBU.ERB_BUCHUNG ON ERB_FIRMENNR = EBP_FIRMENNR AND ERB_FIBUNR = EBP_FIBUNR AND ERB_ERFASSNR = EBP_ERFASSNR" "~"
if joinKuerzel = ERBEBP
searchResult := searchResult "LEFT JOIN FIBU.EBP_BESTELLPOSITION ON EBP_FIRMENNR = ERB_FIRMENNR AND EBP_FIBUNR = ERB_FIBUNR AND EBP_ERFASSNR = ERB_ERFASSNR" "~"
if joinKuerzel = RLPERB
searchResult := searchResult "LEFT JOIN ERB_BUCHUNG.ERB_BUCHUNG ON ERB_FIRMENNR = RLP_ERB_FIRMENNR AND ERB_FIBUNR = RLP_ERB_FIBUNR AND ERB_ERFASSNR = RLP_ERB_ERFASSNR" "~"
if joinKuerzel = ERBRLP
searchResult := searchResult "LEFT JOIN WWS.RLP_RECHNUNGSPLANPOS ON RLP_ERB_FIRMENNR = ERB_FIRMENNR AND RLP_ERB_FIBUNR = ERB_FIBUNR AND RLP_ERB_ERFASSNR = ERB_ERFASSNR" "~"
if joinKuerzel = PRNERN
searchResult := searchResult "LEFT JOIN WWS.ERN_EXTARLKEYNAMES ON ERN_ERNLFDNR = PRN_ERNLFDNR" "~"
if joinKuerzel = ERNPRN
searchResult := searchResult "LEFT JOIN WWS.PRN_EXTPIAKEYNAMES ON PRN_ERNLFDNR = ERN_ERNLFDNR" "~"
if joinKuerzel = BPDERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_ERFASSPOSORIGID = BPD_ERO_ERFASSPOSORIGID" "~"
if joinKuerzel = EROBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_ERO_ERFASSPOSORIGID = ERO_ERFASSPOSORIGID" "~"
if joinKuerzel = BVOERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_ERFASSPOSORIGID = BVO_ERO_ERFASSPOSORIGID" "~"
if joinKuerzel = EROBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_ERO_ERFASSPOSORIGID = ERO_ERFASSPOSORIGID" "~"
if joinKuerzel = DISERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_FIRMENNR = DIS_FIRMENNR AND ERO_ERFASSUNGSKZ = DIS_ERO_ERFASSUNGSKZ AND ERO_ERFASSUNGSNR = DIS_ERO_ERFASSUNGSNR AND ERO_ERFASSPOSLFDNR = DIS_ERO_ERFASSPOSLFDNR AND ERO_ERFASSPOSORIGLFDNR = DIS_ERO_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = ERODIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_FIRMENNR = ERO_FIRMENNR AND DIS_ERO_ERFASSUNGSKZ = ERO_ERFASSUNGSKZ AND DIS_ERO_ERFASSUNGSNR = ERO_ERFASSUNGSNR AND DIS_ERO_ERFASSPOSLFDNR = ERO_ERFASSPOSLFDNR AND DIS_ERO_ERFASSPOSORIGLFDNR = ERO_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = ERAERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_FIRMENNR = ERA_FIRMENNR AND ERO_ERFASSUNGSKZ = ERA_ERFASSUNGSKZ AND ERO_ERFASSUNGSNR = ERA_ERFASSUNGSNR AND ERO_ERFASSPOSLFDNR = ERA_ERFASSPOSLFDNR AND ERO_ERFASSPOSORIGLFDNR = ERA_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = EROERA
searchResult := searchResult "LEFT JOIN WWS.ERA_ERFASSRABATT ON ERA_FIRMENNR = ERO_FIRMENNR AND ERA_ERFASSUNGSKZ = ERO_ERFASSUNGSKZ AND ERA_ERFASSUNGSNR = ERO_ERFASSUNGSNR AND ERA_ERFASSPOSLFDNR = ERO_ERFASSPOSLFDNR AND ERA_ERFASSPOSORIGLFDNR = ERO_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = EROERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_FIRMENNR = ERO_RA_FIRMENNR AND ERO_ERFASSUNGSKZ = ERO_RA_ERFASSUNGSKZ AND ERO_ERFASSUNGSNR = ERO_RA_ERFASSUNGSNR AND ERO_ERFASSPOSLFDNR = ERO_RA_ERFASSPOSLFDNR AND ERO_ERFASSPOSORIGLFDNR = ERO_RA_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = EROERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_RA_FIRMENNR = ERO_FIRMENNR AND ERO_RA_ERFASSUNGSKZ = ERO_ERFASSUNGSKZ AND ERO_RA_ERFASSUNGSNR = ERO_ERFASSUNGSNR AND ERO_RA_ERFASSPOSLFDNR = ERO_ERFASSPOSLFDNR AND ERO_RA_ERFASSPOSORIGLFDNR = ERO_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = RLPEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = RLP_ANZ_FIRMENNR AND EFK_ERFASSUNGSKZ = RLP_ANZ_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = RLP_ANZ_ERFASSUNGSNR" "~"
if joinKuerzel = EFKRLP
searchResult := searchResult "LEFT JOIN WWS.RLP_RECHNUNGSPLANPOS ON RLP_ANZ_FIRMENNR = EFK_FIRMENNR AND RLP_ANZ_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND RLP_ANZ_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = VEPEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = VEP_EFK_FIRMENNR_GUT AND EFK_ERFASSUNGSKZ = VEP_EFK_ERFASSUNGSKZ_GUT AND EFK_ERFASSUNGSNR = VEP_EFK_ERFASSUNGSNR_GUT" "~"
if joinKuerzel = EFKVEP
searchResult := searchResult "LEFT JOIN WWS.VEP_VERTRETERABRECHPOS ON VEP_EFK_FIRMENNR_GUT = EFK_FIRMENNR AND VEP_EFK_ERFASSUNGSKZ_GUT = EFK_ERFASSUNGSKZ AND VEP_EFK_ERFASSUNGSNR_GUT = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = VPPEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = VPP_WWSERF_FIRMENNR AND EFK_ERFASSUNGSKZ = VPP_WWSERF_KZ AND EFK_ERFASSUNGSNR = VPP_WWSERF_NR" "~"
if joinKuerzel = EFKVPP
searchResult := searchResult "LEFT JOIN PRODUKTION.VPP_VIEW_PLC_PPS_CHARGE ON VPP_WWSERF_FIRMENNR = EFK_FIRMENNR AND VPP_WWSERF_KZ = EFK_ERFASSUNGSKZ AND VPP_WWSERF_NR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = EFKEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = EFK_AUF_AUFTR_FIRMENNR AND EFK_ERFASSUNGSKZ = EFK_AUF_AUFTR_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = EFK_AUF_AUFTR_ERFASSUNGSNR" "~"
if joinKuerzel = EFKEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_AUF_AUFTR_FIRMENNR = EFK_FIRMENNR AND EFK_AUF_AUFTR_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND EFK_AUF_AUFTR_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = ERPEGS
searchResult := searchResult "LEFT JOIN EGS_ERFASSUNGSGRUND.EGS_ERFASSUNGSGRUND ON EGS_GRUND_ID = ERP_VKPREISGRUND_ID" "~"
if joinKuerzel = EGSERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_VKPREISGRUND_ID = EGS_GRUND_ID" "~"
if joinKuerzel = LATEHT
searchResult := searchResult "LEFT JOIN WWS.EHT_EINHEITENTAB ON EHT_FIRMENNR = LAT_ARL_FIRMENNR AND EHT_EINHEITENTABNR = LAT_EHT_EINHEITENTABNR" "~"
if joinKuerzel = EHTLAT
searchResult := searchResult "LEFT JOIN WWS.LAT_LIEFERANTENARTIKEL ON LAT_ARL_FIRMENNR = EHT_FIRMENNR AND LAT_EHT_EINHEITENTABNR = EHT_EINHEITENTABNR" "~"
if joinKuerzel = AMZEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = AMZ_EINHEITKBEZ" "~"
if joinKuerzel = EINAMZ
searchResult := searchResult "LEFT JOIN BUERO.AMZ_AUSDRUCKMODELLZU ON AMZ_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = AVLEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = AVL_EIN_EINHEITKBEZ" "~"
if joinKuerzel = EINAVL
searchResult := searchResult "LEFT JOIN WWS.AVL_AUFTRAGSVORERFLIEFDAT ON AVL_EIN_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = EEPEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = EEP_BMDEINHEITKBEZ" "~"
if joinKuerzel = EINEEP
searchResult := searchResult "LEFT JOIN WWS.EEP_EDIEINHEITENPOS ON EEP_BMDEINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PCWEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = PCW_EINHEITKBEZ" "~"
if joinKuerzel = EINPCW
searchResult := searchResult "LEFT JOIN PRODUKTION.PCW_PPSKAPAWERT ON PCW_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = SCKEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = SCK_EIN_EINHEITKBEZ" "~"
if joinKuerzel = EINSCK
searchResult := searchResult "LEFT JOIN PRODUKTION.SCK_SCKENNZAHL ON SCK_EIN_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = TLPEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = TLP_QTY_EINHEIT" "~"
if joinKuerzel = EINTLP
searchResult := searchResult "LEFT JOIN PRODUKTION.TLP_BT_LOGISTIK_POSITION ON TLP_QTY_EINHEIT = EIN_EINHEITKBEZ" "~"
if joinKuerzel = TLUEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = TLU_QTY_EINHEIT" "~"
if joinKuerzel = EINTLU
searchResult := searchResult "LEFT JOIN PRODUKTION.TLU_BT_LOGISTIK_ZU ON TLU_QTY_EINHEIT = EIN_EINHEITKBEZ" "~"
if joinKuerzel = WRPEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = WRP_EINHEITKBEZ" "~"
if joinKuerzel = EINWRP
searchResult := searchResult "LEFT JOIN WWS.WRP_WWSANKAUFSRNGPOS ON WRP_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = QMKEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = QMK_QTY_EINH_2" "~"
if joinKuerzel = EINQMK
searchResult := searchResult "LEFT JOIN PRODUKTION.QMK_QM_KOPF ON QMK_QTY_EINH_2 = EIN_EINHEITKBEZ" "~"
if joinKuerzel = SLKEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = SLK_LAENGENEINHEITKBEZ" "~"
if joinKuerzel = EINSLK
searchResult := searchResult "LEFT JOIN WWS.SLK_STUECKLISTENKOPF ON SLK_LAENGENEINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = SLPEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = SLP_LAENGENEINHEITKBEZ" "~"
if joinKuerzel = EINSLP
searchResult := searchResult "LEFT JOIN WWS.SLP_STUECKLISTENPOSITION ON SLP_LAENGENEINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = BPPEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = BPP_EINHEITKBEZ_LBH" "~"
if joinKuerzel = EINBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_EINHEITKBEZ_LBH = EIN_EINHEITKBEZ" "~"
if joinKuerzel = AVEEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = AVE_ERPESD_ZAEHLER_EH_1" "~"
if joinKuerzel = EINAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ERPESD_ZAEHLER_EH_1 = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PIAEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = PIA_LAENGENEINHEITKBEZ" "~"
if joinKuerzel = EINPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_LAENGENEINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = EFKEKZ
searchResult := searchResult "LEFT JOIN WWS.EKZ_ERLOESAUFWANDKZ ON EKZ_FIRMENNR = EFK_ERLOESAUFWAND_FIRMENNR AND EKZ_ERLOESAUFWANDKZ = EFK_ERLOESAUFWANDKZ" "~"
if joinKuerzel = EKZEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_ERLOESAUFWAND_FIRMENNR = EKZ_FIRMENNR AND EFK_ERLOESAUFWANDKZ = EKZ_ERLOESAUFWANDKZ" "~"
if joinKuerzel = RNGERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = RNG_FIRMENNR AND ERT_ERFASSTEXTNR = RNG_STEUER_ERFASSTEXTNR_S1" "~"
if joinKuerzel = ERTRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = ERT_FIRMENNR AND RNG_STEUER_ERFASSTEXTNR_S1 = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = EFKERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = EFK_FIRMENNR AND ERT_ERFASSTEXTNR = EFK_KOPF_RECH_ERFASSTEXTNR" "~"
if joinKuerzel = ERTEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = ERT_FIRMENNR AND EFK_KOPF_RECH_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = BPKERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_FIRMENNR = BPK_FIRMENNR AND ERZ_ERFASSUNGSKZ = BPK_ERFASSUNGSKZ" "~"
if joinKuerzel = ERZBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_FIRMENNR = ERZ_FIRMENNR AND BPK_ERFASSUNGSKZ = ERZ_ERFASSUNGSKZ" "~"
if joinKuerzel = KAEERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_FIRMENNR = KAE_ERZ_FIRMENNR AND ERZ_ERFASSUNGSKZ = KAE_ERZ_ERFASSUNGSKZ" "~"
if joinKuerzel = ERZKAE
searchResult := searchResult "LEFT JOIN WWS.KAE_KASSENEINAUSGABEN ON KAE_ERZ_FIRMENNR = ERZ_FIRMENNR AND KAE_ERZ_ERFASSUNGSKZ = ERZ_ERFASSUNGSKZ" "~"
if joinKuerzel = KAKERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_FIRMENNR = KAK_FIRMENNR AND ERZ_ERFASSUNGSKZ = KAK_ERFASSUNGSKZ" "~"
if joinKuerzel = ERZKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_FIRMENNR = ERZ_FIRMENNR AND KAK_ERFASSUNGSKZ = ERZ_ERFASSUNGSKZ" "~"
if joinKuerzel = KASERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_FIRMENNR = KAS_ERZ_FIRMENNR AND ERZ_ERFASSUNGSKZ = KAS_ERZ_ERFASSUNGSKZ_WZA" "~"
if joinKuerzel = ERZKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_ERZ_FIRMENNR = ERZ_FIRMENNR AND KAS_ERZ_ERFASSUNGSKZ_WZA = ERZ_ERFASSUNGSKZ" "~"
if joinKuerzel = ES2ESM
searchResult := searchResult "LEFT JOIN HRS.ESM_ELDADETAIL_SM ON ESM_ESMID = ES2_ESMID" "~"
if joinKuerzel = ESMES2
searchResult := searchResult "LEFT JOIN HRS.ES2_ELDADETAIL_SM2 ON ES2_ESMID = ESM_ESMID" "~"
if joinKuerzel = KJOEVN
searchResult := searchResult "LEFT JOIN BUERO.EVN_TEILNEHMER ON EVN_TEILNEHMERID = KJO_TEILNEHMERID" "~"
if joinKuerzel = EVNKJO
searchResult := searchResult "LEFT JOIN BUERO.KJO_KONTAKTJOURNAL ON KJO_TEILNEHMERID = EVN_TEILNEHMERID" "~"
if joinKuerzel = EVNEVS
searchResult := searchResult "LEFT JOIN BUERO.EVS_EVENTLNSTATUS ON EVS_STATUSID = EVN_ZUSAGE_STATUSID" "~"
if joinKuerzel = EVSEVN
searchResult := searchResult "LEFT JOIN BUERO.EVN_TEILNEHMER ON EVN_ZUSAGE_STATUSID = EVS_STATUSID" "~"
if joinKuerzel = MABEVS
searchResult := searchResult "LEFT JOIN BUERO.EVS_EVENTLNSTATUS ON EVS_STATUSID = MAB_EVS_TEILNAHME_STATUSID" "~"
if joinKuerzel = EVSMAB
searchResult := searchResult "LEFT JOIN BUERO.MAB_MITARBAUSBILDUNG ON MAB_EVS_TEILNAHME_STATUSID = EVS_STATUSID" "~"
if joinKuerzel = EVGEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_EVENTID = EVG_EVENTID" "~"
if joinKuerzel = EVTEVG
searchResult := searchResult "LEFT JOIN BUERO.EVG_EVENTTAGE ON EVG_EVENTID = EVT_EVENTID" "~"
if joinKuerzel = EVNEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_EVENTID = EVN_EVENTID" "~"
if joinKuerzel = EVTEVN
searchResult := searchResult "LEFT JOIN BUERO.EVN_TEILNEHMER ON EVN_EVENTID = EVT_EVENTID" "~"
if joinKuerzel = WTEEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_EVENTID = WTE_EVENTID" "~"
if joinKuerzel = EVTWTE
searchResult := searchResult "LEFT JOIN BUERO.WTE_WEBTEILNEHMER ON WTE_EVENTID = EVT_EVENTID" "~"
if joinKuerzel = EAEEZT
searchResult := searchResult "LEFT JOIN HRS.EZT_ELDAZENTRALTABELLE ON EZT_EZTID = EAE_EZTID" "~"
if joinKuerzel = EZTEAE
searchResult := searchResult "LEFT JOIN HRS.EAE_ELDADETAIL_AEK ON EAE_EZTID = EZT_EZTID" "~"
if joinKuerzel = EBNEZT
searchResult := searchResult "LEFT JOIN HRS.EZT_ELDAZENTRALTABELLE ON EZT_EZTID = EBN_EZTID" "~"
if joinKuerzel = EZTEBN
searchResult := searchResult "LEFT JOIN HRS.EBN_ELDADETAIL_BN ON EBN_EZTID = EZT_EZTID" "~"
if joinKuerzel = EVQEZT
searchResult := searchResult "LEFT JOIN HRS.EZT_ELDAZENTRALTABELLE ON EZT_EZTID = EVQ_EZTID" "~"
if joinKuerzel = EZTEVQ
searchResult := searchResult "LEFT JOIN HRS.EVQ_ELDADETAIL_VSNR ON EVQ_EZTID = EZT_EZTID" "~"
if joinKuerzel = AZZFAD
searchResult := searchResult "LEFT JOIN FIBU.FAD_AUSDRUCKDETAIL ON FAD_FIBUAUSDRDETAILID = AZZ_FIBUAUSDRDETAILID" "~"
if joinKuerzel = FADAZZ
searchResult := searchResult "LEFT JOIN FIBU.AZZ_AUSDRUCKZUSATZ ON AZZ_FIBUAUSDRDETAILID = FAD_FIBUAUSDRDETAILID" "~"
if joinKuerzel = VATFAK
searchResult := searchResult "LEFT JOIN FAK_FAKTORTABELLE.FAK_FAKTORTABELLE ON FAK_FAKID = VAT_RZTABELLE_FAKID" "~"
if joinKuerzel = FAKVAT
searchResult := searchResult "LEFT JOIN BUERO.VAT_TEILNAHME ON VAT_RZTABELLE_FAKID = FAK_FAKID" "~"
if joinKuerzel = FSPFAM
searchResult := searchResult "LEFT JOIN FIBU.FAM_AUSDRUCKMASTER ON FAM_FIBUAUSDRMASTERNR = FSP_FIBUAUSDRMASTERNR" "~"
if joinKuerzel = FAMFSP
searchResult := searchResult "LEFT JOIN FIBU.FSP_SPALTENKOPF ON FSP_FIBUAUSDRMASTERNR = FAM_FIBUAUSDRMASTERNR" "~"
if joinKuerzel = AMZFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = AMZ_FARBCODETABNR AND FAR_FARBCODENR = AMZ_FARBCODENR" "~"
if joinKuerzel = FARAMZ
searchResult := searchResult "LEFT JOIN BUERO.AMZ_AUSDRUCKMODELLZU ON AMZ_FARBCODETABNR = FAR_FARBCODETABNR AND AMZ_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = ARLFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = ARL_FARBCODETABNR AND FAR_FARBCODENR = ARL_FARBCODENR" "~"
if joinKuerzel = FARARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FARBCODETABNR = FAR_FARBCODETABNR AND ARL_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = AUGFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = AUG_FARBCODETABNR AND FAR_FARBCODENR = AUG_FARBCODENR" "~"
if joinKuerzel = FARAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_FARBCODETABNR = FAR_FARBCODETABNR AND AUG_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = BMRFAR
searchResult := searchResult "LEFT JOIN BMD.FAR_FARBCODE ON FAR_FARBCODETABNR = BMR_FARBCODETABNR AND FAR_FARBCODENR = BMR_FARBCODENR" "~"
if joinKuerzel = FARBMR
searchResult := searchResult "LEFT JOIN BMD.BMR_BMDROLLE ON BMR_FARBCODETABNR = FAR_FARBCODETABNR AND BMR_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = PLCEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = PLC_PQTY_A_UNIT" "~"
if joinKuerzel = EINPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_PQTY_A_UNIT = EIN_EINHEITKBEZ" "~"
if joinKuerzel = WEPEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = WEP_RECH_EINHEITKBEZ" "~"
if joinKuerzel = EINWEP
searchResult := searchResult "LEFT JOIN WWS.WEP_WWSERKONTROLLEPOS ON WEP_RECH_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = RNPEKZ
searchResult := searchResult "LEFT JOIN WWS.EKZ_ERLOESAUFWANDKZ ON EKZ_FIRMENNR = RNP_ARL_FIRMENNR AND EKZ_ERLOESAUFWANDKZ = RNP_ERLOESAUFWANDKZ" "~"
if joinKuerzel = EKZRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_ARL_FIRMENNR = EKZ_FIRMENNR AND RNP_ERLOESAUFWANDKZ = EKZ_ERLOESAUFWANDKZ" "~"
if joinKuerzel = WENEKZ
searchResult := searchResult "LEFT JOIN WWS.EKZ_ERLOESAUFWANDKZ ON EKZ_FIRMENNR = WEN_EKZ_FIRMENNR AND EKZ_ERLOESAUFWANDKZ = WEN_ERLOESAUFWANDKZ" "~"
if joinKuerzel = EKZWEN
searchResult := searchResult "LEFT JOIN WWS.WEN_WARENEINSATZRNP ON WEN_EKZ_FIRMENNR = EKZ_FIRMENNR AND WEN_ERLOESAUFWANDKZ = EKZ_ERLOESAUFWANDKZ" "~"
if joinKuerzel = EM5EM4
searchResult := searchResult "LEFT JOIN HRS.EM4_ELDADETAIL_VB ON EM4_EM4ID = EM5_EM4ID" "~"
if joinKuerzel = EM4EM5
searchResult := searchResult "LEFT JOIN HRS.EM5_ELDADETAIL_VP ON EM5_EM4ID = EM4_EM4ID" "~"
if joinKuerzel = AEZEMA
searchResult := searchResult "LEFT JOIN BUERO.EMA_EMAIL ON EMA_EMAILLFDNR = AEZ_EMAILLFDNR" "~"
if joinKuerzel = EMAAEZ
searchResult := searchResult "LEFT JOIN BUERO.AEZ_AUFGABENTICKETEMAILZU ON AEZ_EMAILLFDNR = EMA_EMAILLFDNR" "~"
if joinKuerzel = EM2EMB
searchResult := searchResult "LEFT JOIN HRS.EMB_ELDADETAIL_MBGM_P ON EMB_EMBID = EM2_EMBID" "~"
if joinKuerzel = EMBEM2
searchResult := searchResult "LEFT JOIN HRS.EM2_ELDADETAIL_MBGM ON EM2_EMBID = EMB_EMBID" "~"
if joinKuerzel = EPFEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_EPSTKLISTID = EPF_EPSTKLISTID" "~"
if joinKuerzel = EPSEPF
searchResult := searchResult "LEFT JOIN WWS.EPF_EPSTKL_REF ON EPF_EPSTKLISTID = EPS_EPSTKLISTID" "~"
if joinKuerzel = KOPEPS
searchResult := searchResult "LEFT JOIN EPS_ERFASSPOS_STUECKLISTE.EPS_ERFASSPOS_STUECKLISTE ON EPS_EPSTKLISTID = KOP_EPSTKLISTID" "~"
if joinKuerzel = EPSKOP
searchResult := searchResult "LEFT JOIN PRODUKTION.KOP_KOMMISSIONIERUNG_POS ON KOP_EPSTKLISTID = EPS_EPSTKLISTID" "~"
if joinKuerzel = LAJEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_EPSTKLISTID = LAJ_EPSTKLISTID" "~"
if joinKuerzel = EPSLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_EPSTKLISTID = EPS_EPSTKLISTID" "~"
if joinKuerzel = TRPEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_EPSTKLISTID = TRP_EPS_EPSTKLISTID" "~"
if joinKuerzel = EPSTRP
searchResult := searchResult "LEFT JOIN WWS.TRP_TRANSPORTPOSITION ON TRP_EPS_EPSTKLISTID = EPS_EPSTKLISTID" "~"
if joinKuerzel = AAPEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_EPSTKLISTID = AAP_PRODUKTIONS_EPSTKLISTID" "~"
if joinKuerzel = EPSAAP
searchResult := searchResult "LEFT JOIN WWS.AAP_ARAPOSITION ON AAP_PRODUKTIONS_EPSTKLISTID = EPS_EPSTKLISTID" "~"
if joinKuerzel = ERPERG
searchResult := searchResult "LEFT JOIN WWS.ERG_ERFASSUNGSGRUPPEN ON ERG_ERGID = ERP_ERGID" "~"
if joinKuerzel = ERGERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_ERGID = ERG_ERGID" "~"
if joinKuerzel = EPSERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_FIRMENNR = EPS_ERO_FIRMENNR AND ERO_ERFASSUNGSKZ = EPS_ERO_ERFASSUNGSKZ AND ERO_ERFASSUNGSNR = EPS_ERO_ERFASSUNGSNR AND ERO_ERFASSPOSLFDNR = EPS_ERO_ERFASSPOSLFDNR AND ERO_ERFASSPOSORIGLFDNR = EPS_ERO_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = EROEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_ERO_FIRMENNR = ERO_FIRMENNR AND EPS_ERO_ERFASSUNGSKZ = ERO_ERFASSUNGSKZ AND EPS_ERO_ERFASSUNGSNR = ERO_ERFASSUNGSNR AND EPS_ERO_ERFASSPOSLFDNR = ERO_ERFASSPOSLFDNR AND EPS_ERO_ERFASSPOSORIGLFDNR = ERO_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = PLCERO
searchResult := searchResult "LEFT JOIN ERO_ERFASSPOSORIG.ERO_ERFASSPOSORIG ON ERO_FIRMENNR = PLC_ERO_FIRMENNR AND ERO_ERFASSUNGSKZ = PLC_ERO_ERFASSUNGSKZ AND ERO_ERFASSUNGSNR = PLC_ERO_ERFASSUNGSNR AND ERO_ERFASSPOSLFDNR = PLC_ERO_ERFASSPOSLFDNR AND ERO_ERFASSPOSORIGLFDNR = PLC_ERO_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = EROPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_ERO_FIRMENNR = ERO_FIRMENNR AND PLC_ERO_ERFASSUNGSKZ = ERO_ERFASSUNGSKZ AND PLC_ERO_ERFASSUNGSNR = ERO_ERFASSUNGSNR AND PLC_ERO_ERFASSPOSLFDNR = ERO_ERFASSPOSLFDNR AND PLC_ERO_ERFASSPOSORIGLFDNR = ERO_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = WKEERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_FIRMENNR = WKE_FIRMENNR AND ERO_ERFASSUNGSKZ = WKE_ERFASSUNGSKZ AND ERO_ERFASSUNGSNR = WKE_ERFASSUNGSNR AND ERO_ERFASSPOSLFDNR = WKE_ERFASSPOSLFDNR AND ERO_ERFASSPOSORIGLFDNR = WKE_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = EROWKE
searchResult := searchResult "LEFT JOIN WWS.WKE_WARENAUSGKOSTENERF ON WKE_FIRMENNR = ERO_FIRMENNR AND WKE_ERFASSUNGSKZ = ERO_ERFASSUNGSKZ AND WKE_ERFASSUNGSNR = ERO_ERFASSUNGSNR AND WKE_ERFASSPOSLFDNR = ERO_ERFASSPOSLFDNR AND WKE_ERFASSPOSORIGLFDNR = ERO_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = EDZERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_FIRMENNR = EDZ_FIRMENNR AND ERP_ERFASSUNGSKZ = EDZ_AUFT_ERFASSUNGSKZ AND ERP_ERFASSUNGSNR = EDZ_AUFT_ERFASSUNGSNR AND ERP_ERFASSPOSLFDNR = EDZ_AUFT_ERFASSPOSLFDNR" "~"
if joinKuerzel = ERPEDZ
searchResult := searchResult "LEFT JOIN WWS.EDZ_ERFASSDOKUMENTZU ON EDZ_FIRMENNR = ERP_FIRMENNR AND EDZ_AUFT_ERFASSUNGSKZ = ERP_ERFASSUNGSKZ AND EDZ_AUFT_ERFASSUNGSNR = ERP_ERFASSUNGSNR AND EDZ_AUFT_ERFASSPOSLFDNR = ERP_ERFASSPOSLFDNR" "~"
if joinKuerzel = EPRERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_FIRMENNR = EPR_FIRMENNR AND ERP_ERFASSUNGSKZ = EPR_ERFASSUNGSKZ AND ERP_ERFASSUNGSNR = EPR_ERFASSUNGSNR AND ERP_ERFASSPOSLFDNR = EPR_ERFASSPOSLFDNR" "~"
if joinKuerzel = ERPEPR
searchResult := searchResult "LEFT JOIN WWS.EPR_ERFASSPROVISION ON EPR_FIRMENNR = ERP_FIRMENNR AND EPR_ERFASSUNGSKZ = ERP_ERFASSUNGSKZ AND EPR_ERFASSUNGSNR = ERP_ERFASSUNGSNR AND EPR_ERFASSPOSLFDNR = ERP_ERFASSPOSLFDNR" "~"
if joinKuerzel = EROERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_FIRMENNR = ERO_FIRMENNR AND ERP_ERFASSUNGSKZ = ERO_ERFASSUNGSKZ AND ERP_ERFASSUNGSNR = ERO_ERFASSUNGSNR AND ERP_ERFASSPOSLFDNR = ERO_ERFASSPOSLFDNR" "~"
if joinKuerzel = ERPERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_FIRMENNR = ERP_FIRMENNR AND ERO_ERFASSUNGSKZ = ERP_ERFASSUNGSKZ AND ERO_ERFASSUNGSNR = ERP_ERFASSUNGSNR AND ERO_ERFASSPOSLFDNR = ERP_ERFASSPOSLFDNR" "~"
if joinKuerzel = VOBEMT
searchResult := searchResult "LEFT JOIN EMT_EMAILART.EMT_EMAILART ON EMT_EMAILARTNR = VOB_EMT_EMAILARTNR" "~"
if joinKuerzel = EMTVOB
searchResult := searchResult "LEFT JOIN PRODUKTION.VOB_VORGANGSBENACHR ON VOB_EMT_EMAILARTNR = EMT_EMAILARTNR" "~"
if joinKuerzel = BVOEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_EPSTKLISTID = BVO_EPS_EPSTKLISTID" "~"
if joinKuerzel = EPSBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_EPS_EPSTKLISTID = EPS_EPSTKLISTID" "~"
if joinKuerzel = DISEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_EPSTKLISTID = DIS_EPSTKLISTID" "~"
if joinKuerzel = EPSDIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_EPSTKLISTID = EPS_EPSTKLISTID" "~"
if joinKuerzel = WAPEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_EPSTKLISTID = WAP_EPSTKLISTID" "~"
if joinKuerzel = EPSWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_EPSTKLISTID = EPS_EPSTKLISTID" "~"
if joinKuerzel = AVEERB
searchResult := searchResult "LEFT JOIN ERB_BUCHUNG.ERB_BUCHUNG ON ERB_FIRMENNR = AVE_ERB_FIRMENNR AND ERB_FIBUNR = AVE_ERB_FIBUNR AND ERB_ERFASSNR = AVE_ERB_ERFASSNR" "~"
if joinKuerzel = ERBAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ERB_FIRMENNR = ERB_FIRMENNR AND AVE_ERB_FIBUNR = ERB_FIBUNR AND AVE_ERB_ERFASSNR = ERB_ERFASSNR" "~"
if joinKuerzel = AZGERB
searchResult := searchResult "LEFT JOIN ERB_BUCHUNG.ERB_BUCHUNG ON ERB_FIRMENNR = AZG_ERB_FIRMENNR AND ERB_FIBUNR = AZG_FIBUNR AND ERB_ERFASSNR = AZG_ERFASSNR" "~"
if joinKuerzel = ERBAZG
searchResult := searchResult "LEFT JOIN BUERO.AZG_ABZUG ON AZG_ERB_FIRMENNR = ERB_FIRMENNR AND AZG_FIBUNR = ERB_FIBUNR AND AZG_ERFASSNR = ERB_ERFASSNR" "~"
if joinKuerzel = AIVERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_FIRMENNR = AIV_ERO_FIRMENNR AND ERO_ERFASSUNGSKZ = AIV_ERO_ERFASSUNGSKZ AND ERO_ERFASSUNGSNR = AIV_ERO_ERFASSUNGSNR AND ERO_ERFASSPOSLFDNR = AIV_ERO_ERFASSPOSLFDNR AND ERO_ERFASSPOSORIGLFDNR = AIV_ERO_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = EROAIV
searchResult := searchResult "LEFT JOIN WWS.AIV_INTERVALL_ARBEITSPLAN ON AIV_ERO_FIRMENNR = ERO_FIRMENNR AND AIV_ERO_ERFASSUNGSKZ = ERO_ERFASSUNGSKZ AND AIV_ERO_ERFASSUNGSNR = ERO_ERFASSUNGSNR AND AIV_ERO_ERFASSPOSLFDNR = ERO_ERFASSPOSLFDNR AND AIV_ERO_ERFASSPOSORIGLFDNR = ERO_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = AVEERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_FIRMENNR = AVE_AUFTRAG_FIRMENNR AND ERO_ERFASSUNGSKZ = AVE_AUFTRAG_ERFASSUNGSKZ AND ERO_ERFASSUNGSNR = AVE_AUFTRAG_ERFASSUNGSNR AND ERO_ERFASSPOSLFDNR = AVE_AUFTRAG_ERFASSPOSLFDN AND ERO_ERFASSPOSORIGLFDNR = AVE_AUFTRAG_ERO_ORIGLFDNR" "~"
if joinKuerzel = EROAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_AUFTRAG_FIRMENNR = ERO_FIRMENNR AND AVE_AUFTRAG_ERFASSUNGSKZ = ERO_ERFASSUNGSKZ AND AVE_AUFTRAG_ERFASSUNGSNR = ERO_ERFASSUNGSNR AND AVE_AUFTRAG_ERFASSPOSLFDN = ERO_ERFASSPOSLFDNR AND AVE_AUFTRAG_ERO_ORIGLFDNR = ERO_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = BONERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = BON_FUSSTEXT_FIRMENNR AND ERT_ERFASSTEXTNR = BON_FUSS_ERFASSTEXTNR" "~"
if joinKuerzel = ERTBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_FUSSTEXT_FIRMENNR = ERT_FIRMENNR AND BON_FUSS_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = RNGERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = RNG_FIRMENNR AND ERT_ERFASSTEXTNR = RNG_STEUER_ERFASSTEXTNR" "~"
if joinKuerzel = ERTRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = ERT_FIRMENNR AND RNG_STEUER_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = ERPERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = ERP_FIRMENNR AND ERT_ERFASSTEXTNR = ERP_BESCH_ERFASSTEXTNR" "~"
if joinKuerzel = ERTERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_FIRMENNR = ERT_FIRMENNR AND ERP_BESCH_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = AVEERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = AVE_ERFGRP_ET_FIRMENNR AND ERT_ERFASSTEXTNR = AVE_ERFGRP_ERFASSTEXTNR" "~"
if joinKuerzel = ERTAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ERFGRP_ET_FIRMENNR = ERT_FIRMENNR AND AVE_ERFGRP_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = AVEERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = AVE_ERFGRP_ET_FIRMENNR AND ERT_ERFASSTEXTNR = AVE_ERFGRP_ERFASSTEXTNR1" "~"
if joinKuerzel = ERTAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ERFGRP_ET_FIRMENNR = ERT_FIRMENNR AND AVE_ERFGRP_ERFASSTEXTNR1 = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = AVEERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = AVE_ERFGRP_ET_FIRMENNR AND ERT_ERFASSTEXTNR = AVE_ERFGRP_ERFASSTEXTNR2" "~"
if joinKuerzel = ERTAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ERFGRP_ET_FIRMENNR = ERT_FIRMENNR AND AVE_ERFGRP_ERFASSTEXTNR2 = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = EVVEVG
searchResult := searchResult "LEFT JOIN BUERO.EVG_EVENTTAGE ON EVG_EVGTAGID = EVV_TAGID" "~"
if joinKuerzel = EVGEVV
searchResult := searchResult "LEFT JOIN BUERO.EVV_EVENTVORTRAGZU ON EVV_TAGID = EVG_EVGTAGID" "~"
if joinKuerzel = EVZEVN
searchResult := searchResult "LEFT JOIN BUERO.EVN_TEILNEHMER ON EVN_TEILNEHMERID = EVZ_TEILNEHMERID" "~"
if joinKuerzel = EVNEVZ
searchResult := searchResult "LEFT JOIN BUERO.EVZ_TLNTAGZU ON EVZ_TEILNEHMERID = EVN_TEILNEHMERID" "~"
if joinKuerzel = EVLEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_EVENTID = EVL_EVENTID" "~"
if joinKuerzel = EVTEVL
searchResult := searchResult "LEFT JOIN BUERO.EVL_EVENTLIEFERANTZU ON EVL_EVENTID = EVT_EVENTID" "~"
if joinKuerzel = EVTEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_EVENTID = EVT_VORLAGE_EVENTID" "~"
if joinKuerzel = EVTEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_VORLAGE_EVENTID = EVT_EVENTID" "~"
if joinKuerzel = EVVEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_EVENTID = EVV_EVENTID" "~"
if joinKuerzel = EVTEVV
searchResult := searchResult "LEFT JOIN BUERO.EVV_EVENTVORTRAGZU ON EVV_EVENTID = EVT_EVENTID" "~"
if joinKuerzel = WAMEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_EVENTID = WAM_EVENTID" "~"
if joinKuerzel = EVTWAM
searchResult := searchResult "LEFT JOIN BUERO.WAM_WEBANMELDUNG ON WAM_EVENTID = EVT_EVENTID" "~"
if joinKuerzel = BPZERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_ERFASSPOSORIGID = BPZ_ERFASSPOSORIGID" "~"
if joinKuerzel = EROBPZ
searchResult := searchResult "LEFT JOIN WWS.BPZ_BESTELLPOSZUORDNUNG ON BPZ_ERFASSPOSORIGID = ERO_ERFASSPOSORIGID" "~"
if joinKuerzel = EQIERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_ERFASSPOSORIGID = EQI_ERFASSPOSORIGID" "~"
if joinKuerzel = EROEQI
searchResult := searchResult "LEFT JOIN WWS.EQI_ERFASSPOSQUELLINFO ON EQI_ERFASSPOSORIGID = ERO_ERFASSPOSORIGID" "~"
if joinKuerzel = EROERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_ERFASSPOSORIGID = ERO_ANG_ERFASSPOSORIGID" "~"
if joinKuerzel = EROERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_ANG_ERFASSPOSORIGID = ERO_ERFASSPOSORIGID" "~"
if joinKuerzel = AVEERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_FIRMENNR = AVE_ERP_FIRMENNR AND ERP_ERFASSUNGSKZ = AVE_ERP_ERFASSUNGSKZ AND ERP_ERFASSUNGSNR = AVE_ERP_ERFASSUNGSNR AND ERP_ERFASSPOSLFDNR = AVE_ERP_ERFASSPOSLFDNR" "~"
if joinKuerzel = ERPAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ERP_FIRMENNR = ERP_FIRMENNR AND AVE_ERP_ERFASSUNGSKZ = ERP_ERFASSUNGSKZ AND AVE_ERP_ERFASSUNGSNR = ERP_ERFASSUNGSNR AND AVE_ERP_ERFASSPOSLFDNR = ERP_ERFASSPOSLFDNR" "~"
if joinKuerzel = AVEERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_FIRMENNR = AVE_AUFTRAG_FIRMENNR AND ERP_ERFASSUNGSKZ = AVE_AUFTRAG_ERFASSUNGSKZ AND ERP_ERFASSUNGSNR = AVE_AUFTRAG_ERFASSUNGSNR AND ERP_ERFASSPOSLFDNR = AVE_AUFTRAG_ERFASSPOSLFDN" "~"
if joinKuerzel = ERPAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_AUFTRAG_FIRMENNR = ERP_FIRMENNR AND AVE_AUFTRAG_ERFASSUNGSKZ = ERP_ERFASSUNGSKZ AND AVE_AUFTRAG_ERFASSUNGSNR = ERP_ERFASSUNGSNR AND AVE_AUFTRAG_ERFASSPOSLFDN = ERP_ERFASSPOSLFDNR" "~"
if joinKuerzel = BVOERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = BVO_ERT_FIRMENNR AND ERT_ERFASSTEXTNR = BVO_BESCH_ERFASSTEXTNR" "~"
if joinKuerzel = ERTBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_ERT_FIRMENNR = ERT_FIRMENNR AND BVO_BESCH_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = EFKERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = EFK_FIRMENNR AND ERT_ERFASSTEXTNR = EFK_KOPF_ERFASSTEXTNR" "~"
if joinKuerzel = ERTEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = ERT_FIRMENNR AND EFK_KOPF_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = EQIERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = EQI_FIRMENNR AND ERT_ERFASSTEXTNR = EQI_BESCH_ERFASSTEXTNR" "~"
if joinKuerzel = ERTEQI
searchResult := searchResult "LEFT JOIN WWS.EQI_ERFASSPOSQUELLINFO ON EQI_FIRMENNR = ERT_FIRMENNR AND EQI_BESCH_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = KAKERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = KAK_KOPFTEXT_FIRMENNR AND ERT_ERFASSTEXTNR = KAK_KOPF_ERFASSTEXTNR" "~"
if joinKuerzel = ERTKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_KOPFTEXT_FIRMENNR = ERT_FIRMENNR AND KAK_KOPF_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = AVEERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = AVE_FIRMENNR AND ERT_ERFASSTEXTNR = AVE_KOPF_ERFASSTEXTNR" "~"
if joinKuerzel = ERTAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_FIRMENNR = ERT_FIRMENNR AND AVE_KOPF_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = BOPERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = BOP_ERT_BEZ_FIRMENNR AND ERT_ERFASSTEXTNR = BOP_ERT_BEZ_ERFASSTEXTNR" "~"
if joinKuerzel = ERTBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_ERT_BEZ_FIRMENNR = ERT_FIRMENNR AND BOP_ERT_BEZ_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = KAPERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = KAP_ERT_BEZ_FIRMENNR AND ERT_ERFASSTEXTNR = KAP_ERT_BEZ_ERFASSTEXTNR" "~"
if joinKuerzel = ERTKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_ERT_BEZ_FIRMENNR = ERT_FIRMENNR AND KAP_ERT_BEZ_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = LFSERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = LFS_FIRMENNR AND ERT_ERFASSTEXTNR = LFS_EMAIL_ERFASSTEXTNR" "~"
if joinKuerzel = ERTLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_FIRMENNR = ERT_FIRMENNR AND LFS_EMAIL_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = BOPERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = BOP_ERT_BESCH_FIRMENNR AND ERT_ERFASSTEXTNR = BOP_ERT_BESCH_ERFASSTEXTNR" "~"
if joinKuerzel = ERTBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_ERT_BESCH_FIRMENNR = ERT_FIRMENNR AND BOP_ERT_BESCH_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = AVEERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = AVE_FIRMENNR AND ERT_ERFASSTEXTNR = AVE_ERP_NACH_ERFASSTEXTNR" "~"
if joinKuerzel = ERTAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_FIRMENNR = ERT_FIRMENNR AND AVE_ERP_NACH_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = BONERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_FIRMENNR = BON_FIRMENNR AND ERZ_ERFASSUNGSKZ = BON_ERFASSUNGSKZ" "~"
if joinKuerzel = ERZBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_FIRMENNR = ERZ_FIRMENNR AND BON_ERFASSUNGSKZ = ERZ_ERFASSUNGSKZ" "~"
if joinKuerzel = KOZERZ
searchResult := searchResult "LEFT JOIN ERZ_ERFASSKENNZ.ERZ_ERFASSKENNZ ON ERZ_FIRMENNR = KOZ_ERFASS_FIRMENNR AND ERZ_ERFASSUNGSKZ = KOZ_ERFASSUNGSKZ" "~"
if joinKuerzel = ERZKOZ
searchResult := searchResult "LEFT JOIN PRODUKTION.KOZ_KOMMISSIONIERUNG_ZU ON KOZ_ERFASS_FIRMENNR = ERZ_FIRMENNR AND KOZ_ERFASSUNGSKZ = ERZ_ERFASSUNGSKZ" "~"
if joinKuerzel = EVNEVG
searchResult := searchResult "LEFT JOIN BUERO.EVG_EVENTTAGE ON EVG_EVGTAGID = EVN_EVGTAGID" "~"
if joinKuerzel = EVGEVN
searchResult := searchResult "LEFT JOIN BUERO.EVN_TEILNEHMER ON EVN_EVGTAGID = EVG_EVGTAGID" "~"
if joinKuerzel = VATEVN
searchResult := searchResult "LEFT JOIN BUERO.EVN_TEILNEHMER ON EVN_TEILNEHMERID = VAT_EVN_TEILNEHMERID" "~"
if joinKuerzel = EVNVAT
searchResult := searchResult "LEFT JOIN BUERO.VAT_TEILNAHME ON VAT_EVN_TEILNEHMERID = EVN_TEILNEHMERID" "~"
if joinKuerzel = AGSEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = AGS_ALT_DAUER_EINHEITKBEZ" "~"
if joinKuerzel = EINAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_ALT_DAUER_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = CHGEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = CHG_GEBINDEEINHEITKBEZ" "~"
if joinKuerzel = EINCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_GEBINDEEINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = ERPEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = ERP_GEWICHTS_EINHEITKBEZ" "~"
if joinKuerzel = EINERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_GEWICHTS_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = LINEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = LIN_PQTY_V_UNIT" "~"
if joinKuerzel = EINLIN
searchResult := searchResult "LEFT JOIN PRODUKTION.LIN_PROD_LINIE ON LIN_PQTY_V_UNIT = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PTPEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = PTP_KUNDE_EINHEITKBEZ" "~"
if joinKuerzel = EINPTP
searchResult := searchResult "LEFT JOIN WWS.PTP_PREISTABPOS ON PTP_KUNDE_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = VFAEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = VFA_EINHEITKBEZ" "~"
if joinKuerzel = EINVFA
searchResult := searchResult "LEFT JOIN PRODUKTION.VFA_VERFAHRENSANWEISUNG_KOPF ON VFA_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = AVEEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = AVE_ERPESD_ZAEHLER_EH_2" "~"
if joinKuerzel = EINAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ERPESD_ZAEHLER_EH_2 = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PLCEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = PLC_PQTY_V_UNIT" "~"
if joinKuerzel = EINPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_PQTY_V_UNIT = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PTPEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = PTP_STAFFELMG_EINHEITKBEZ" "~"
if joinKuerzel = EINPTP
searchResult := searchResult "LEFT JOIN WWS.PTP_PREISTABPOS ON PTP_STAFFELMG_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = ARLEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = ARL_EINKPREISEINHEITKBEZ" "~"
if joinKuerzel = EINARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_EINKPREISEINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = WRPEKZ
searchResult := searchResult "LEFT JOIN WWS.EKZ_ERLOESAUFWANDKZ ON EKZ_FIRMENNR = WRP_HAUPTFIRMENNR AND EKZ_ERLOESAUFWANDKZ = WRP_ERLOESAUFWANDKZ" "~"
if joinKuerzel = EKZWRP
searchResult := searchResult "LEFT JOIN WWS.WRP_WWSANKAUFSRNGPOS ON WRP_HAUPTFIRMENNR = EKZ_FIRMENNR AND WRP_ERLOESAUFWANDKZ = EKZ_ERLOESAUFWANDKZ" "~"
if joinKuerzel = KOPELV
searchResult := searchResult "LEFT JOIN ELV_ERFASSPOSLIEFERVOR.ELV_ERFASSPOSLIEFERVOR ON ELV_FIRMENNR = KOP_ELV_FIRMENNR AND ELV_ERFASSUNGSKZ = KOP_ELV_ERFASSUNGSKZ AND ELV_ERFASSUNGSNR = KOP_ELV_ERFASSUNGSNR AND ELV_ERFASSPOSLFDNR = KOP_ELV_ERFASSPOSLFDNR AND ELV_ERFASSPOSORIGLFDNR = KOP_ELV_ERFASSPOSORIGLFDNR AND ELV_ERFASSPOSLVLFDNR = KOP_ELV_ERFASSPOSLVLFDNR" "~"
if joinKuerzel = ELVKOP
searchResult := searchResult "LEFT JOIN PRODUKTION.KOP_KOMMISSIONIERUNG_POS ON KOP_ELV_FIRMENNR = ELV_FIRMENNR AND KOP_ELV_ERFASSUNGSKZ = ELV_ERFASSUNGSKZ AND KOP_ELV_ERFASSUNGSNR = ELV_ERFASSUNGSNR AND KOP_ELV_ERFASSPOSLFDNR = ELV_ERFASSPOSLFDNR AND KOP_ELV_ERFASSPOSORIGLFDNR = ELV_ERFASSPOSORIGLFDNR AND KOP_ELV_ERFASSPOSLVLFDNR = ELV_ERFASSPOSLVLFDNR" "~"
if joinKuerzel = EDNENT
searchResult := searchResult "LEFT JOIN BMD.ENT_EDINACHRICHTTYP ON ENT_ENTID = EDN_ENTID" "~"
if joinKuerzel = ENTEDN
searchResult := searchResult "LEFT JOIN BMD.EDN_EDINACHRICHT ON EDN_ENTID = ENT_ENTID" "~"
if joinKuerzel = EPVERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_FIRMENNR = EPV_FIRMENNR AND ERO_ERFASSUNGSKZ = EPV_ERFASSUNGSKZ AND ERO_ERFASSUNGSNR = EPV_ERFASSUNGSNR AND ERO_ERFASSPOSLFDNR = EPV_ERFASSPOSLFDNR AND ERO_ERFASSPOSORIGLFDNR = EPV_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = EROEPV
searchResult := searchResult "LEFT JOIN WWS.EPV_ERFASSPOSRECHVOR ON EPV_FIRMENNR = ERO_FIRMENNR AND EPV_ERFASSUNGSKZ = ERO_ERFASSUNGSKZ AND EPV_ERFASSUNGSNR = ERO_ERFASSUNGSNR AND EPV_ERFASSPOSLFDNR = ERO_ERFASSPOSLFDNR AND EPV_ERFASSPOSORIGLFDNR = ERO_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = EPREPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_EPSTKLISTID = EPR_EPSTKLISTID" "~"
if joinKuerzel = EPSEPR
searchResult := searchResult "LEFT JOIN WWS.EPR_ERFASSPROVISION ON EPR_EPSTKLISTID = EPS_EPSTKLISTID" "~"
if joinKuerzel = EPSEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_EPSTKLISTID = EPS_UEBERG_EPSTKLISTID" "~"
if joinKuerzel = EPSEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_UEBERG_EPSTKLISTID = EPS_EPSTKLISTID" "~"
if joinKuerzel = WERERB
searchResult := searchResult "LEFT JOIN ERB_BUCHUNG.ERB_BUCHUNG ON ERB_FIRMENNR = WER_FIRMENNR AND ERB_FIBUNR = WER_FIBUNR AND ERB_ERFASSNR = WER_ERFASSNR" "~"
if joinKuerzel = ERBWER
searchResult := searchResult "LEFT JOIN WWS.WER_WWSERKONTROLLEKOPF ON WER_FIRMENNR = ERB_FIRMENNR AND WER_FIBUNR = ERB_FIBUNR AND WER_ERFASSNR = ERB_ERFASSNR" "~"
if joinKuerzel = EASERK
searchResult := searchResult "LEFT JOIN FIBU.ERK_KONTO ON ERK_FIRMENNR = EAS_FIRMENNR AND ERK_FIBUNR = EAS_FIBUNR AND ERK_ERFASSNR = EAS_ERFASSNR AND ERK_ERFASSUNTERNR = EAS_ERFASSUNTERNR" "~"
if joinKuerzel = ERKEAS
searchResult := searchResult "LEFT JOIN FIBU.EAS_ERKONTOAUFTEILSATZ ON EAS_FIRMENNR = ERK_FIRMENNR AND EAS_FIBUNR = ERK_FIBUNR AND EAS_ERFASSNR = ERK_ERFASSNR AND EAS_ERFASSUNTERNR = ERK_ERFASSUNTERNR" "~"
if joinKuerzel = BOPERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_FIRMENNR = BOP_ERP_FIRMENNR AND ERP_ERFASSUNGSKZ = BOP_ERP_ERFASSUNGSKZ AND ERP_ERFASSUNGSNR = BOP_ERP_ERFASSUNGSNR AND ERP_ERFASSPOSLFDNR = BOP_ERP_ERFASSPOSLFDNR" "~"
if joinKuerzel = ERPBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_ERP_FIRMENNR = ERP_FIRMENNR AND BOP_ERP_ERFASSUNGSKZ = ERP_ERFASSUNGSKZ AND BOP_ERP_ERFASSUNGSNR = ERP_ERFASSUNGSNR AND BOP_ERP_ERFASSPOSLFDNR = ERP_ERFASSPOSLFDNR" "~"
if joinKuerzel = AVEERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = AVE_ERT_BESC_FIRMENNR AND ERT_ERFASSTEXTNR = AVE_ERT_BESC_ERFASSTEXTNR" "~"
if joinKuerzel = ERTAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ERT_BESC_FIRMENNR = ERT_FIRMENNR AND AVE_ERT_BESC_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = KOZERO
searchResult := searchResult "LEFT JOIN ERO_ERFASSPOSORIG.ERO_ERFASSPOSORIG ON ERO_FIRMENNR = KOZ_ERFASS_FIRMENNR AND ERO_ERFASSUNGSKZ = KOZ_ERFASSUNGSKZ AND ERO_ERFASSUNGSNR = KOZ_ERFASSUNGSNR AND ERO_ERFASSPOSLFDNR = KOZ_ERFASSPOSNR AND ERO_ERFASSPOSORIGLFDNR = KOZ_ERFASSPOSDETAIL" "~"
if joinKuerzel = EROKOZ
searchResult := searchResult "LEFT JOIN PRODUKTION.KOZ_KOMMISSIONIERUNG_ZU ON KOZ_ERFASS_FIRMENNR = ERO_FIRMENNR AND KOZ_ERFASSUNGSKZ = ERO_ERFASSUNGSKZ AND KOZ_ERFASSUNGSNR = ERO_ERFASSUNGSNR AND KOZ_ERFASSPOSNR = ERO_ERFASSPOSLFDNR AND KOZ_ERFASSPOSDETAIL = ERO_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = WAZERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_ERFASSPOSORIGID = WAZ_ERO_ERFASSPOSORIGID" "~"
if joinKuerzel = EROWAZ
searchResult := searchResult "LEFT JOIN WWS.WAZ_WARENZUORDNUNG ON WAZ_ERO_ERFASSPOSORIGID = ERO_ERFASSPOSORIGID" "~"
if joinKuerzel = VJDERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_FIRMENNR = VJD_FIRMENNR AND ERP_ERFASSUNGSKZ = VJD_VERR_ERFKZ AND ERP_ERFASSUNGSNR = VJD_VERR_ERFNR AND ERP_ERFASSPOSLFDNR = VJD_VERR_ERFPOSLFDNR" "~"
if joinKuerzel = ERPVJD
searchResult := searchResult "LEFT JOIN WWS.VJD_VERLEIHJOURNALDETAIL ON VJD_FIRMENNR = ERP_FIRMENNR AND VJD_VERR_ERFKZ = ERP_ERFASSUNGSKZ AND VJD_VERR_ERFNR = ERP_ERFASSUNGSNR AND VJD_VERR_ERFPOSLFDNR = ERP_ERFASSPOSLFDNR" "~"
if joinKuerzel = BONERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = BON_KOPFTEXT_FIRMENNR AND ERT_ERFASSTEXTNR = BON_KOPF_ERFASSTEXTNR" "~"
if joinKuerzel = ERTBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_KOPFTEXT_FIRMENNR = ERT_FIRMENNR AND BON_KOPF_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = ERGERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = ERG_FIRMENNR AND ERT_ERFASSTEXTNR = ERG_ERFASSTEXTNR" "~"
if joinKuerzel = ERTERG
searchResult := searchResult "LEFT JOIN WWS.ERG_ERFASSUNGSGRUPPEN ON ERG_FIRMENNR = ERT_FIRMENNR AND ERG_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = KAKERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = KAK_FUSSTEXT_FIRMENNR AND ERT_ERFASSTEXTNR = KAK_FUSS_ERFASSTEXTNR" "~"
if joinKuerzel = ERTKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_FUSSTEXT_FIRMENNR = ERT_FIRMENNR AND KAK_FUSS_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = BOPERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = BOP_ERT_KBEZ_FIRMENNR AND ERT_ERFASSTEXTNR = BOP_ERT_KBEZ_ERFASSTEXTNR" "~"
if joinKuerzel = ERTBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_ERT_KBEZ_FIRMENNR = ERT_FIRMENNR AND BOP_ERT_KBEZ_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = BPPERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = BPP_FIRMENNR AND ERT_ERFASSTEXTNR = BPP_VOR_ERFASSTEXTNR" "~"
if joinKuerzel = ERTBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_FIRMENNR = ERT_FIRMENNR AND BPP_VOR_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = EFKERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = EFK_FIRMENNR AND ERT_ERFASSTEXTNR = EFK_FUSS_RECH_ERFASSTEXTNR" "~"
if joinKuerzel = ERTEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = ERT_FIRMENNR AND EFK_FUSS_RECH_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = RNGERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = RNG_FIRMENNR AND ERT_ERFASSTEXTNR = RNG_STEUER_ERFASSTEXTNR_S3" "~"
if joinKuerzel = ERTRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = ERT_FIRMENNR AND RNG_STEUER_ERFASSTEXTNR_S3 = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = ERGERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_FIRMENNR = ERG_FIRMENNR AND ERZ_ERFASSUNGSKZ = ERG_ERFASSUNGSKZ" "~"
if joinKuerzel = ERZERG
searchResult := searchResult "LEFT JOIN WWS.ERG_ERFASSUNGSGRUPPEN ON ERG_FIRMENNR = ERZ_FIRMENNR AND ERG_ERFASSUNGSKZ = ERZ_ERFASSUNGSKZ" "~"
if joinKuerzel = KZAERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_FIRMENNR = KZA_ERZ_FIRMENNR AND ERZ_ERFASSUNGSKZ = KZA_ERFASSUNGSKZ" "~"
if joinKuerzel = ERZKZA
searchResult := searchResult "LEFT JOIN WWS.KZA_KASSENZAHLUNGSARTENZU ON KZA_ERZ_FIRMENNR = ERZ_FIRMENNR AND KZA_ERFASSUNGSKZ = ERZ_ERFASSUNGSKZ" "~"
if joinKuerzel = VPBERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_FIRMENNR = VPB_FIRMENNR AND ERZ_ERFASSUNGSKZ = VPB_ERFASSUNGSKZ" "~"
if joinKuerzel = ERZVPB
searchResult := searchResult "LEFT JOIN WWS.VPB_VPERMITTLUNG_ERKZUORD ON VPB_FIRMENNR = ERZ_FIRMENNR AND VPB_ERFASSUNGSKZ = ERZ_ERFASSUNGSKZ" "~"
if joinKuerzel = EFKESD
searchResult := searchResult "LEFT JOIN WWS.ESD_ERFASSUNGSERVICEDATEN ON ESD_ESDID = EFK_ESDID" "~"
if joinKuerzel = ESDEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_ESDID = ESD_ESDID" "~"
if joinKuerzel = ARLETK
searchResult := searchResult "LEFT JOIN WWS.ETK_ETIKETTENKLASSE ON ETK_FIRMENNR = ARL_FIRMENNR AND ETK_ETIKETT_KLASSE = ARL_ETIKETT_KLASSE" "~"
if joinKuerzel = ETKARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = ETK_FIRMENNR AND ARL_ETIKETT_KLASSE = ETK_ETIKETT_KLASSE" "~"
if joinKuerzel = EVNEVS
searchResult := searchResult "LEFT JOIN BUERO.EVS_EVENTLNSTATUS ON EVS_STATUSID = EVN_TEILNAHME_STATUSID" "~"
if joinKuerzel = EVSEVN
searchResult := searchResult "LEFT JOIN BUERO.EVN_TEILNEHMER ON EVN_TEILNAHME_STATUSID = EVS_STATUSID" "~"
if joinKuerzel = EVKEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_EVENTID = EVK_EVENTID" "~"
if joinKuerzel = EVTEVK
searchResult := searchResult "LEFT JOIN BUERO.EVK_EVENTKATEGORIEZU ON EVK_EVENTID = EVT_EVENTID" "~"
if joinKuerzel = EFHEZT
searchResult := searchResult "LEFT JOIN HRS.EZT_ELDAZENTRALTABELLE ON EZT_EZTID = EFH_EZTID" "~"
if joinKuerzel = EZTEFH
searchResult := searchResult "LEFT JOIN HRS.EFH_ELDADETAIL_FH ON EFH_EZTID = EZT_EZTID" "~"
if joinKuerzel = ESEEZT
searchResult := searchResult "LEFT JOIN HRS.EZT_ELDAZENTRALTABELLE ON EZT_EZTID = ESE_EZTID" "~"
if joinKuerzel = EZTESE
searchResult := searchResult "LEFT JOIN HRS.ESE_ELDADETAIL_SE ON ESE_EZTID = EZT_EZTID" "~"
if joinKuerzel = EVREZT
searchResult := searchResult "LEFT JOIN HRS.EZT_ELDAZENTRALTABELLE ON EZT_EZTID = EVR_EZTID" "~"
if joinKuerzel = EZTEVR
searchResult := searchResult "LEFT JOIN HRS.EVR_ELDADETAIL_VMR ON EVR_EZTID = EZT_EZTID" "~"
if joinKuerzel = ERZEKZ
searchResult := searchResult "LEFT JOIN WWS.EKZ_ERLOESAUFWANDKZ ON EKZ_FIRMENNR = ERZ_HAUPTFIRMENNR AND EKZ_ERLOESAUFWANDKZ = ERZ_ERLOESAUFWANDKZ" "~"
if joinKuerzel = EKZERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_HAUPTFIRMENNR = EKZ_FIRMENNR AND ERZ_ERLOESAUFWANDKZ = EKZ_ERLOESAUFWANDKZ" "~"
if joinKuerzel = KAKEKZ
searchResult := searchResult "LEFT JOIN WWS.EKZ_ERLOESAUFWANDKZ ON EKZ_FIRMENNR = KAK_ERLOESAUFWAND_FIRMENNR AND EKZ_ERLOESAUFWANDKZ = KAK_ERLOESAUFWANDKZ" "~"
if joinKuerzel = EKZKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_ERLOESAUFWAND_FIRMENNR = EKZ_FIRMENNR AND KAK_ERLOESAUFWANDKZ = EKZ_ERLOESAUFWANDKZ" "~"
if joinKuerzel = WAZELV
searchResult := searchResult "LEFT JOIN WWS.ELV_ERFASSPOSLIEFERVOR ON ELV_FIRMENNR = WAZ_ELV_FIRMENNR AND ELV_ERFASSUNGSKZ = WAZ_ELV_ERFASSUNGSKZ AND ELV_ERFASSUNGSNR = WAZ_ELV_ERFASSUNGSNR AND ELV_ERFASSPOSLFDNR = WAZ_ELV_ERFASSPOSLFDNR AND ELV_ERFASSPOSORIGLFDNR = WAZ_ELV_ERFASSPOSORIGLFDNR AND ELV_ERFASSPOSLVLFDNR = WAZ_ELV_ERFASSPOSLVLFDNR" "~"
if joinKuerzel = ELVWAZ
searchResult := searchResult "LEFT JOIN WWS.WAZ_WARENZUORDNUNG ON WAZ_ELV_FIRMENNR = ELV_FIRMENNR AND WAZ_ELV_ERFASSUNGSKZ = ELV_ERFASSUNGSKZ AND WAZ_ELV_ERFASSUNGSNR = ELV_ERFASSUNGSNR AND WAZ_ELV_ERFASSPOSLFDNR = ELV_ERFASSPOSLFDNR AND WAZ_ELV_ERFASSPOSORIGLFDNR = ELV_ERFASSPOSORIGLFDNR AND WAZ_ELV_ERFASSPOSLVLFDNR = ELV_ERFASSPOSLVLFDNR" "~"
if joinKuerzel = EM3EM2
searchResult := searchResult "LEFT JOIN HRS.EM2_ELDADETAIL_MBGM ON EM2_EM2ID = EM3_EM2ID" "~"
if joinKuerzel = EM2EM3
searchResult := searchResult "LEFT JOIN HRS.EM3_ELDADETAIL_TG ON EM3_EM2ID = EM2_EM2ID" "~"
if joinKuerzel = INSEMA
searchResult := searchResult "LEFT JOIN BUERO.EMA_EMAIL ON EMA_EMAILLFDNR = INS_EMAILLFDNR" "~"
if joinKuerzel = EMAINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_EMAILLFDNR = EMA_EMAILLFDNR" "~"
if joinKuerzel = EPREPR
searchResult := searchResult "LEFT JOIN WWS.EPR_ERFASSPROVISION ON EPR_ERFASSPROVID = EPR_UEBERG_ERFASSPROVID" "~"
if joinKuerzel = EPREPR
searchResult := searchResult "LEFT JOIN WWS.EPR_ERFASSPROVISION ON EPR_UEBERG_ERFASSPROVID = EPR_ERFASSPROVID" "~"
if joinKuerzel = ERPERG
searchResult := searchResult "LEFT JOIN WWS.ERG_ERFASSUNGSGRUPPEN ON ERG_ERGID = ERP_ERGID AND ERG_FIRMENNR = ERP_FIRMENNR AND ERG_ERFASSUNGSKZ = ERP_ERFASSUNGSKZ AND ERG_ERFASSUNGSNR = ERP_ERFASSUNGSNR AND ERG_ERFGRUPPE_EBENE1 = ERP_ERFGRUPPE_EBENE1 AND ERG_ERFGRUPPE_EBENE2 = ERP_ERFGRUPPE_EBENE2 AND ERG_ERFGRUPPE_EBENE3 = ERP_ERFGRUPPE_EBENE3" "~"
if joinKuerzel = ERGERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_ERGID = ERG_ERGID AND ERP_FIRMENNR = ERG_FIRMENNR AND ERP_ERFASSUNGSKZ = ERG_ERFASSUNGSKZ AND ERP_ERFASSUNGSNR = ERG_ERFASSUNGSNR AND ERP_ERFGRUPPE_EBENE1 = ERG_ERFGRUPPE_EBENE1 AND ERP_ERFGRUPPE_EBENE2 = ERG_ERFGRUPPE_EBENE2 AND ERP_ERFGRUPPE_EBENE3 = ERG_ERFGRUPPE_EBENE3" "~"
if joinKuerzel = BPPERG
searchResult := searchResult "LEFT JOIN WWS.ERG_ERFASSUNGSGRUPPEN ON ERG_ERGID = BPP_ERGID" "~"
if joinKuerzel = ERGBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_ERGID = ERG_ERGID" "~"
if joinKuerzel = ERSERK
searchResult := searchResult "LEFT JOIN FIBU.ERK_KONTO ON ERK_FIRMENNR = ERS_FIRMENNR AND ERK_FIBUNR = ERS_FIBUNR AND ERK_ERFASSNR = ERS_ERFASSNR AND ERK_ERFASSUNTERNR = ERS_ERFASSUNTERNR" "~"
if joinKuerzel = ERKERS
searchResult := searchResult "LEFT JOIN FIBU.ERS_KOST ON ERS_FIRMENNR = ERK_FIRMENNR AND ERS_FIBUNR = ERK_FIBUNR AND ERS_ERFASSNR = ERK_ERFASSNR AND ERS_ERFASSUNTERNR = ERK_ERFASSUNTERNR" "~"
if joinKuerzel = ADEERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_FIRMENNR = ADE_FIRMENNR AND ERO_ERFASSUNGSKZ = ADE_ERFASSUNGSKZ AND ERO_ERFASSUNGSNR = ADE_ERFASSUNGSNR AND ERO_ERFASSPOSLFDNR = ADE_ERFASSPOSLFDNR AND ERO_ERFASSPOSORIGLFDNR = ADE_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = EROADE
searchResult := searchResult "LEFT JOIN WWS.ADE_ABGABENDETAILERFPOS ON ADE_FIRMENNR = ERO_FIRMENNR AND ADE_ERFASSUNGSKZ = ERO_ERFASSUNGSKZ AND ADE_ERFASSUNGSNR = ERO_ERFASSUNGSNR AND ADE_ERFASSPOSLFDNR = ERO_ERFASSPOSLFDNR AND ADE_ERFASSPOSORIGLFDNR = ERO_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = ERPERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_FIRMENNR = ERP_FIRMENNR AND ERP_ERFASSUNGSKZ = ERP_ERFASSUNGSKZ AND ERP_ERFASSUNGSNR = ERP_ERFASSUNGSNR AND ERP_ERFASSPOSLFDNR = ERP_HAUPT_ERFASSPOSLFDNR" "~"
if joinKuerzel = ERPERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_FIRMENNR = ERP_FIRMENNR AND ERP_ERFASSUNGSKZ = ERP_ERFASSUNGSKZ AND ERP_ERFASSUNGSNR = ERP_ERFASSUNGSNR AND ERP_HAUPT_ERFASSPOSLFDNR = ERP_ERFASSPOSLFDNR" "~"
if joinKuerzel = GSPERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_FIRMENNR = GSP_ERP_FIRMENNR AND ERP_ERFASSUNGSKZ = GSP_ERP_ERFASSUNGSKZ AND ERP_ERFASSUNGSNR = GSP_ERP_ERFASSUNGSNR AND ERP_ERFASSPOSLFDNR = GSP_ERP_ERFASSPOSLFDNR" "~"
if joinKuerzel = ERPGSP
searchResult := searchResult "LEFT JOIN WWS.GSP_GUTSCHEINPOSITION ON GSP_ERP_FIRMENNR = ERP_FIRMENNR AND GSP_ERP_ERFASSUNGSKZ = ERP_ERFASSUNGSKZ AND GSP_ERP_ERFASSUNGSNR = ERP_ERFASSUNGSNR AND GSP_ERP_ERFASSPOSLFDNR = ERP_ERFASSPOSLFDNR" "~"
if joinKuerzel = KAPERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_FIRMENNR = KAP_ERP_FIRMENNR AND ERP_ERFASSUNGSKZ = KAP_ERP_ERFASSUNGSKZ AND ERP_ERFASSUNGSNR = KAP_ERP_ERFASSUNGSNR AND ERP_ERFASSPOSLFDNR = KAP_ERP_ERFASSPOSLFDNR" "~"
if joinKuerzel = ERPKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_ERP_FIRMENNR = ERP_FIRMENNR AND KAP_ERP_ERFASSUNGSKZ = ERP_ERFASSUNGSKZ AND KAP_ERP_ERFASSUNGSNR = ERP_ERFASSUNGSNR AND KAP_ERP_ERFASSPOSLFDNR = ERP_ERFASSPOSLFDNR" "~"
if joinKuerzel = LEBERP
searchResult := searchResult "LEFT JOIN ERP_ERFASSPOSITIONEN.ERP_ERFASSPOSITIONEN ON ERP_FIRMENNR = LEB_ERP_FIRMENNR AND ERP_ERFASSUNGSKZ = LEB_ERP_ERFASSUNGSKZ AND ERP_ERFASSUNGSNR = LEB_ERP_ERFASSUNGSNR AND ERP_ERFASSPOSLFDNR = LEB_ERP_ERFASSPOSLFDNR" "~"
if joinKuerzel = ERPLEB
searchResult := searchResult "LEFT JOIN BUERO.LEB_LEBENSLAUF ON LEB_ERP_FIRMENNR = ERP_FIRMENNR AND LEB_ERP_ERFASSUNGSKZ = ERP_ERFASSUNGSKZ AND LEB_ERP_ERFASSUNGSNR = ERP_ERFASSUNGSNR AND LEB_ERP_ERFASSPOSLFDNR = ERP_ERFASSPOSLFDNR" "~"
if joinKuerzel = KAPERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = KAP_ERT_VOR_FIRMENNR AND ERT_ERFASSTEXTNR = KAP_ERT_VOR_ERFASSTEXTNR" "~"
if joinKuerzel = ERTKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_ERT_VOR_FIRMENNR = ERT_FIRMENNR AND KAP_ERT_VOR_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = BPPERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = BPP_FIRMENNR AND ERT_ERFASSTEXTNR = BPP_KURZBEZ_ERFASSTEXTNR" "~"
if joinKuerzel = ERTBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_FIRMENNR = ERT_FIRMENNR AND BPP_KURZBEZ_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = E18EZT
searchResult := searchResult "LEFT JOIN HRS.EZT_ELDAZENTRALTABELLE ON EZT_EZTID = E18_EZTID" "~"
if joinKuerzel = EZTE18
searchResult := searchResult "LEFT JOIN HRS.E18_ELDADETAIL_E18 ON E18_EZTID = EZT_EZTID" "~"
if joinKuerzel = ESMEZT
searchResult := searchResult "LEFT JOIN HRS.EZT_ELDAZENTRALTABELLE ON EZT_EZTID = ESM_EZTID" "~"
if joinKuerzel = EZTESM
searchResult := searchResult "LEFT JOIN HRS.ESM_ELDADETAIL_SM ON ESM_EZTID = EZT_EZTID" "~"
if joinKuerzel = FADFAM
searchResult := searchResult "LEFT JOIN FIBU.FAM_AUSDRUCKMASTER ON FAM_FIBUAUSDRMASTERNR = FAD_FAM_FIBUAUSDRMASTERNR" "~"
if joinKuerzel = FAMFAD
searchResult := searchResult "LEFT JOIN FIBU.FAD_AUSDRUCKDETAIL ON FAD_FAM_FIBUAUSDRMASTERNR = FAM_FIBUAUSDRMASTERNR" "~"
if joinKuerzel = APAFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = APA_FARBCODETABNR AND FAR_FARBCODENR = APA_FARBCODENR" "~"
if joinKuerzel = FARAPA
searchResult := searchResult "LEFT JOIN PRODUKTION.APA_ARBEITSPLAN ON APA_FARBCODETABNR = FAR_FARBCODETABNR AND APA_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = BESFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = BES_FARBCODETABNR AND FAR_FARBCODENR = BES_FARBCODENR" "~"
if joinKuerzel = FARBES
searchResult := searchResult "LEFT JOIN BUERO.BES_BEWERBUNGSTATUS ON BES_FARBCODETABNR = FAR_FARBCODETABNR AND BES_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = BONFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = BON_FARBCODETABNR AND FAR_FARBCODENR = BON_FARBCODENR" "~"
if joinKuerzel = FARBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_FARBCODETABNR = FAR_FARBCODETABNR AND BON_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = DOGFAR
searchResult := searchResult "LEFT JOIN BMD.FAR_FARBCODE ON FAR_FARBCODETABNR = DOG_FARBCODETABNR AND FAR_FARBCODENR = DOG_FARBCODENR" "~"
if joinKuerzel = FARDOG
searchResult := searchResult "LEFT JOIN BMD.DOG_DATAOWNERGRUPPE ON DOG_FARBCODETABNR = FAR_FARBCODETABNR AND DOG_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = EFKFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = EFK_FARBCODETABNR AND FAR_FARBCODENR = EFK_FARBCODENR" "~"
if joinKuerzel = FAREFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FARBCODETABNR = FAR_FARBCODETABNR AND EFK_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = EPVFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = EPV_FARBCODETABNR AND FAR_FARBCODENR = EPV_FARBCODENR" "~"
if joinKuerzel = FAREPV
searchResult := searchResult "LEFT JOIN WWS.EPV_ERFASSPOSRECHVOR ON EPV_FARBCODETABNR = FAR_FARBCODETABNR AND EPV_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = PATFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = PAT_FARBCODETABNR AND FAR_FARBCODENR = PAT_FARBCODENR" "~"
if joinKuerzel = FARPAT
searchResult := searchResult "LEFT JOIN WWS.PAT_PACKSTOFFTAB ON PAT_FARBCODETABNR = FAR_FARBCODETABNR AND PAT_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = PLOFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = PLO_FARBCODETABNR AND FAR_FARBCODENR = PLO_FARBCODENR" "~"
if joinKuerzel = FARPLO
searchResult := searchResult "LEFT JOIN PRODUKTION.PLO_PPS_LOS ON PLO_FARBCODETABNR = FAR_FARBCODETABNR AND PLO_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = PSGFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = PSG_FARBCODETABNR AND FAR_FARBCODENR = PSG_FARBCODENR" "~"
if joinKuerzel = FARPSG
searchResult := searchResult "LEFT JOIN WWS.PSG_PREISGRUPPE ON PSG_FARBCODETABNR = FAR_FARBCODETABNR AND PSG_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = PTPFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = PTP_FARBCODETABNR AND FAR_FARBCODENR = PTP_FARBCODENR" "~"
if joinKuerzel = FARPTP
searchResult := searchResult "LEFT JOIN WWS.PTP_PREISTABPOS ON PTP_FARBCODETABNR = FAR_FARBCODETABNR AND PTP_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = RATFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = RAT_FARBCODETABNR AND FAR_FARBCODENR = RAT_FARBCODENR" "~"
if joinKuerzel = FARRAT
searchResult := searchResult "LEFT JOIN WWS.RAT_RABATTTAB ON RAT_FARBCODETABNR = FAR_FARBCODETABNR AND RAT_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = EVTFBN
searchResult := searchResult "LEFT JOIN BUERO.FBN_FRAGEBOGEN ON FBN_FRAGEBOGENID = EVT_FRAGEBOGENID" "~"
if joinKuerzel = FBNEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_FRAGEBOGENID = FBN_FRAGEBOGENID" "~"
if joinKuerzel = FFZFBN
searchResult := searchResult "LEFT JOIN BUERO.FBN_FRAGEBOGEN ON FBN_FRAGEBOGENID = FFZ_FRAGEBOGENID" "~"
if joinKuerzel = FBNFFZ
searchResult := searchResult "LEFT JOIN BUERO.FFZ_FRAGEBOGENFRAGEZU ON FFZ_FRAGEBOGENID = FBN_FRAGEBOGENID" "~"
if joinKuerzel = DFEFBP
searchResult := searchResult "LEFT JOIN FBP_FELDBESCHPOS.FBP_FELDBESCHPOS ON FBP_FELDBESCHNR = DFE_FELDBESCHNR AND FBP_FELDPOSITIONSNR = DFE_FELDPOSITIONSNR" "~"
if joinKuerzel = FBPDFE
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFE_DFELDBESCHPOS ON DFE_FELDBESCHNR = FBP_FELDBESCHNR AND DFE_FELDPOSITIONSNR = FBP_FELDPOSITIONSNR" "~"
if joinKuerzel = WAGFEA
searchResult := searchResult "LEFT JOIN WWS.FEA_FERTEP_AUFTEILUNG ON FEA_FEAID = WAG_FEAID" "~"
if joinKuerzel = FEAWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FEAID = FEA_FEAID" "~"
if joinKuerzel = FEDFEH
searchResult := searchResult "LEFT JOIN WWS.FEH_FERTEP_HISTORIE ON FEH_FEHID = FED_FEHID" "~"
if joinKuerzel = FEHFED
searchResult := searchResult "LEFT JOIN WWS.FED_FERTEP_AUFTDETAIL ON FED_FEHID = FEH_FEHID" "~"
if joinKuerzel = FRZFFZ
searchResult := searchResult "LEFT JOIN BUERO.FFZ_FRAGEBOGENFRAGEZU ON FFZ_FFZUID = FRZ_FFZUID" "~"
if joinKuerzel = FFZFRZ
searchResult := searchResult "LEFT JOIN BUERO.FRZ_FRAGEBOGENREGELZU ON FRZ_FFZUID = FFZ_FFZUID" "~"
if joinKuerzel = OFRFFZ
searchResult := searchResult "LEFT JOIN BUERO.FFZ_FRAGEBOGENFRAGEZU ON FFZ_FFZUID = OFR_FFZUID" "~"
if joinKuerzel = FFZOFR
searchResult := searchResult "LEFT JOIN BUERO.OFR_FRAGEBOGENREGEL ON OFR_FFZUID = FFZ_FFZUID" "~"
if joinKuerzel = PFFFFZ
searchResult := searchResult "LEFT JOIN BUERO.FFZ_FRAGEBOGENFRAGEZU ON FFZ_FFZUID = PFF_FFZUID" "~"
if joinKuerzel = FFZPFF
searchResult := searchResult "LEFT JOIN BUERO.PFF_PAGEFGBFRAGEZU ON PFF_FFZUID = FFZ_FFZUID" "~"
if joinKuerzel = BPKERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = BPK_FIRMENNR AND ERT_ERFASSTEXTNR = BPK_FUSS_ERFASSTEXTNR" "~"
if joinKuerzel = ERTBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_FIRMENNR = ERT_FIRMENNR AND BPK_FUSS_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = BVOERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = BVO_ERT_FIRMENNR AND ERT_ERFASSTEXTNR = BVO_KURZBESCH_ERFASSTEXTNR" "~"
if joinKuerzel = ERTBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_ERT_FIRMENNR = ERT_FIRMENNR AND BVO_KURZBESCH_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = BVOERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = BVO_ERT_FIRMENNR AND ERT_ERFASSTEXTNR = BVO_VOR_ERFASSTEXTNR" "~"
if joinKuerzel = ERTBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_ERT_FIRMENNR = ERT_FIRMENNR AND BVO_VOR_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = AVEERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = AVE_FIRMENNR AND ERT_ERFASSTEXTNR = AVE_FUSS_ERFASSTEXTNR" "~"
if joinKuerzel = ERTAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_FIRMENNR = ERT_FIRMENNR AND AVE_FUSS_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = BPPERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = BPP_FIRMENNR AND ERT_ERFASSTEXTNR = BPP_NACH_ERFASSTEXTNR" "~"
if joinKuerzel = ERTBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_FIRMENNR = ERT_FIRMENNR AND BPP_NACH_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = AVEERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = AVE_FIRMENNR AND ERT_ERFASSTEXTNR = AVE_EMAIL_ERFASSTEXTNR" "~"
if joinKuerzel = ERTAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_FIRMENNR = ERT_FIRMENNR AND AVE_EMAIL_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = EFKERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = EFK_FIRMENNR AND ERT_ERFASSTEXTNR = EFK_KOPF_LFS_ERFASSTEXTNR" "~"
if joinKuerzel = ERTEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = ERT_FIRMENNR AND EFK_KOPF_LFS_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = RNGERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = RNG_FIRMENNR AND ERT_ERFASSTEXTNR = RNG_STEUER_ERFASSTEXTNR_S4" "~"
if joinKuerzel = ERTRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = ERT_FIRMENNR AND RNG_STEUER_ERFASSTEXTNR_S4 = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = AMZERZ
searchResult := searchResult "LEFT JOIN ERZ_ERFASSKENNZ.ERZ_ERFASSKENNZ ON ERZ_FIRMENNR = AMZ_AUSDRERSTELL_FIRMENNR AND ERZ_ERFASSUNGSKZ = AMZ_ERFASSUNGSKZ" "~"
if joinKuerzel = ERZAMZ
searchResult := searchResult "LEFT JOIN BUERO.AMZ_AUSDRUCKMODELLZU ON AMZ_AUSDRERSTELL_FIRMENNR = ERZ_FIRMENNR AND AMZ_ERFASSUNGSKZ = ERZ_ERFASSUNGSKZ" "~"
if joinKuerzel = AVEERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_FIRMENNR = AVE_ERZ_FIRMENNR AND ERZ_ERFASSUNGSKZ = AVE_ERZ_ERFASSUNGSKZ" "~"
if joinKuerzel = ERZAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ERZ_FIRMENNR = ERZ_FIRMENNR AND AVE_ERZ_ERFASSUNGSKZ = ERZ_ERFASSUNGSKZ" "~"
if joinKuerzel = ESZERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_FIRMENNR = ESZ_FIRMENNR AND ERZ_ERFASSUNGSKZ = ESZ_ERFASSUNGSKZ" "~"
if joinKuerzel = ERZESZ
searchResult := searchResult "LEFT JOIN WWS.ESZ_ERZ_SRA_ZU ON ESZ_FIRMENNR = ERZ_FIRMENNR AND ESZ_ERFASSUNGSKZ = ERZ_ERFASSUNGSKZ" "~"
if joinKuerzel = KASERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_FIRMENNR = KAS_ERZ_FIRMENNR AND ERZ_ERFASSUNGSKZ = KAS_ERZ_ERFASSUNGSKZ" "~"
if joinKuerzel = ERZKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_ERZ_FIRMENNR = ERZ_FIRMENNR AND KAS_ERZ_ERFASSUNGSKZ = ERZ_ERFASSUNGSKZ" "~"
if joinKuerzel = ERPESD
searchResult := searchResult "LEFT JOIN WWS.ESD_ERFASSUNGSERVICEDATEN ON ESD_ESDID = ERP_ESDID" "~"
if joinKuerzel = ESDERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_ESDID = ESD_ESDID" "~"
if joinKuerzel = LFSESD
searchResult := searchResult "LEFT JOIN WWS.ESD_ERFASSUNGSERVICEDATEN ON ESD_ESDID = LFS_ESDID" "~"
if joinKuerzel = ESDLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_ESDID = ESD_ESDID" "~"
if joinKuerzel = RNGESD
searchResult := searchResult "LEFT JOIN WWS.ESD_ERFASSUNGSERVICEDATEN ON ESD_ESDID = RNG_ESDID" "~"
if joinKuerzel = ESDRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_ESDID = ESD_ESDID" "~"
if joinKuerzel = ENTEST
searchResult := searchResult "LEFT JOIN BMD.EST_EDISCHEMATYP ON EST_ESTID = ENT_ESTID" "~"
if joinKuerzel = ESTENT
searchResult := searchResult "LEFT JOIN BMD.ENT_EDINACHRICHTTYP ON ENT_ESTID = EST_ESTID" "~"
if joinKuerzel = ETZEVG
searchResult := searchResult "LEFT JOIN BUERO.EVG_EVENTTAGE ON EVG_EVGTAGID = ETZ_EVG_EVGTAGID" "~"
if joinKuerzel = EVGETZ
searchResult := searchResult "LEFT JOIN BUERO.ETZ_EVENTTERMINZU ON ETZ_EVG_EVGTAGID = EVG_EVGTAGID" "~"
if joinKuerzel = EVZEVG
searchResult := searchResult "LEFT JOIN BUERO.EVG_EVENTTAGE ON EVG_EVGTAGID = EVZ_TAGID" "~"
if joinKuerzel = EVGEVZ
searchResult := searchResult "LEFT JOIN BUERO.EVZ_TLNTAGZU ON EVZ_TAGID = EVG_EVGTAGID" "~"
if joinKuerzel = ETZEVN
searchResult := searchResult "LEFT JOIN BUERO.EVN_TEILNEHMER ON EVN_TEILNEHMERID = ETZ_EVN_TEILNEHMERID" "~"
if joinKuerzel = EVNETZ
searchResult := searchResult "LEFT JOIN BUERO.ETZ_EVENTTERMINZU ON ETZ_EVN_TEILNEHMERID = EVN_TEILNEHMERID" "~"
if joinKuerzel = AWSEVS
searchResult := searchResult "LEFT JOIN EVS_EVENTLNSTATUS.EVS_EVENTLNSTATUS ON EVS_STATUSID = AWS_EVS_STATUSID" "~"
if joinKuerzel = EVSAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_EVS_STATUSID = EVS_STATUSID" "~"
if joinKuerzel = EVZEVS
searchResult := searchResult "LEFT JOIN BUERO.EVS_EVENTLNSTATUS ON EVS_STATUSID = EVZ_TEILNAHME_STATUSID" "~"
if joinKuerzel = EVSEVZ
searchResult := searchResult "LEFT JOIN BUERO.EVZ_TLNTAGZU ON EVZ_TEILNAHME_STATUSID = EVS_STATUSID" "~"
if joinKuerzel = SFEEVS
searchResult := searchResult "LEFT JOIN BUERO.EVS_EVENTLNSTATUS ON EVS_STATUSID = SFE_EVS_ZUSAGE_STATUSID" "~"
if joinKuerzel = EVSSFE
searchResult := searchResult "LEFT JOIN BUERO.SFE_SKILLFINDERELEMENT ON SFE_EVS_ZUSAGE_STATUSID = EVS_STATUSID" "~"
if joinKuerzel = SLREVS
searchResult := searchResult "LEFT JOIN BUERO.EVS_EVENTLNSTATUS ON EVS_STATUSID = SLR_EVS_TEILNAHME_STATUSID" "~"
if joinKuerzel = EVSSLR
searchResult := searchResult "LEFT JOIN BUERO.SLR_SKILLREGELELEMENT ON SLR_EVS_TEILNAHME_STATUSID = EVS_STATUSID" "~"
if joinKuerzel = ALEEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_EVENTID = ALE_EVT_EVENTID" "~"
if joinKuerzel = EVTALE
searchResult := searchResult "LEFT JOIN BUERO.ALE_ADRESSENLISTE ON ALE_EVT_EVENTID = EVT_EVENTID" "~"
if joinKuerzel = EBGEZT
searchResult := searchResult "LEFT JOIN HRS.EZT_ELDAZENTRALTABELLE ON EZT_EZTID = EBG_EZTID" "~"
if joinKuerzel = EZTEBG
searchResult := searchResult "LEFT JOIN HRS.EBG_ELDADETAIL_BGN ON EBG_EZTID = EZT_EZTID" "~"
if joinKuerzel = EGMEZT
searchResult := searchResult "LEFT JOIN HRS.EZT_ELDAZENTRALTABELLE ON EZT_EZTID = EGM_EZTID" "~"
if joinKuerzel = EZTEGM
searchResult := searchResult "LEFT JOIN HRS.EGM_ELDADETAIL_GM ON EGM_EZTID = EZT_EZTID" "~"
if joinKuerzel = BMGFAR
searchResult := searchResult "LEFT JOIN BMD.FAR_FARBCODE ON FAR_FARBCODETABNR = BMG_FARBCODETABNR AND FAR_FARBCODENR = BMG_FARBCODENR" "~"
if joinKuerzel = FARBMG
searchResult := searchResult "LEFT JOIN BMD.BMG_BMDGRUPPE ON BMG_FARBCODETABNR = FAR_FARBCODETABNR AND BMG_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = BORFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = BOR_FARBCODETABNR AND FAR_FARBCODENR = BOR_FARBCODENR" "~"
if joinKuerzel = FARBOR
searchResult := searchResult "LEFT JOIN WWS.BOR_BONRABATT ON BOR_FARBCODETABNR = FAR_FARBCODETABNR AND BOR_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = DKZFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = DKZ_FARBCODETABNR AND FAR_FARBCODENR = DKZ_FARBCODENR" "~"
if joinKuerzel = FARDKZ
searchResult := searchResult "LEFT JOIN BUERO.DKZ_DOKUMENTZU ON DKZ_FARBCODETABNR = FAR_FARBCODETABNR AND DKZ_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = ERZFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = ERZ_FARBCODETABNR AND FAR_FARBCODENR = ERZ_FARBCODENR" "~"
if joinKuerzel = FARERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_FARBCODETABNR = FAR_FARBCODETABNR AND ERZ_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = EVTFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = EVT_FARBCODETABNR AND FAR_FARBCODENR = EVT_FARBCODENR" "~"
if joinKuerzel = FAREVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_FARBCODETABNR = FAR_FARBCODETABNR AND EVT_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = FATFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = FAT_FARBCODETABNR AND FAR_FARBCODENR = FAT_FARBCODENR" "~"
if joinKuerzel = FARFAT
searchResult := searchResult "LEFT JOIN WWS.FAT_FARBENTAB ON FAT_FARBCODETABNR = FAR_FARBCODETABNR AND FAT_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = LPOFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = LPO_FARBCODETABNR AND FAR_FARBCODENR = LPO_FARBCODENR" "~"
if joinKuerzel = FARLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_FARBCODETABNR = FAR_FARBCODETABNR AND LPO_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = OBPFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = OBP_FARBCODETABNR AND FAR_FARBCODENR = OBP_FARBCODENR" "~"
if joinKuerzel = FAROBP
searchResult := searchResult "LEFT JOIN BUERO.OBP_ONLBEWPERSSTAMM ON OBP_FARBCODETABNR = FAR_FARBCODETABNR AND OBP_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = SA1FAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = SA1_FARBCODETABNR AND FAR_FARBCODENR = SA1_FARBCODENR" "~"
if joinKuerzel = FARSA1
searchResult := searchResult "LEFT JOIN ONVIS.SA1_SAURER_BDE ON SA1_FARBCODETABNR = FAR_FARBCODETABNR AND SA1_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = SAIFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = SAI_FARBCODETABNR AND FAR_FARBCODENR = SAI_FARBCODENR" "~"
if joinKuerzel = FARSAI
searchResult := searchResult "LEFT JOIN WWS.SAI_SAISON ON SAI_FARBCODETABNR = FAR_FARBCODETABNR AND SAI_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = VAKFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = VAK_FARBCODETABNR AND FAR_FARBCODENR = VAK_FARBCODENR" "~"
if joinKuerzel = FARVAK
searchResult := searchResult "LEFT JOIN BUERO.VAK_VERANSTALTUNGSKOPF ON VAK_FARBCODETABNR = FAR_FARBCODETABNR AND VAK_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = VATFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = VAT_FARBCODETABNR AND FAR_FARBCODENR = VAT_FARBCODENR" "~"
if joinKuerzel = FARVAT
searchResult := searchResult "LEFT JOIN BUERO.VAT_TEILNAHME ON VAT_FARBCODETABNR = FAR_FARBCODETABNR AND VAT_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = WTBFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = WTB_FARBCODETABNR AND FAR_FARBCODENR = WTB_FARBCODENR" "~"
if joinKuerzel = FARWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FARBCODETABNR = FAR_FARBCODETABNR AND WTB_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = FSZFAZ
searchResult := searchResult "LEFT JOIN BUERO.FAZ_FRAGEANTWORTZU ON FAZ_FAZID = FSZ_FAZ_FAZID" "~"
if joinKuerzel = FAZFSZ
searchResult := searchResult "LEFT JOIN BUERO.FSZ_FREIEANTWORTSETZU ON FSZ_FAZ_FAZID = FAZ_FAZID" "~"
if joinKuerzel = QSTFBF
searchResult := searchResult "LEFT JOIN FBF_FELDBESCHFORMAT.FBF_FELDBESCHFORMAT ON FBF_FORMATNR = QST_FELD_FORMATNR" "~"
if joinKuerzel = FBFQST
searchResult := searchResult "LEFT JOIN PRODUKTION.QST_QM_STAMM ON QST_FELD_FORMATNR = FBF_FORMATNR" "~"
if joinKuerzel = DFBFBK
searchResult := searchResult "LEFT JOIN FBK_FELDBESCHKOPF.FBK_FELDBESCHKOPF ON FBK_FELDBESCHNR = DFB_FELDBESCHNR" "~"
if joinKuerzel = FBKDFB
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFB_DFELDBESCHKOPF ON DFB_FELDBESCHNR = FBK_FELDBESCHNR" "~"
if joinKuerzel = WFNFBN
searchResult := searchResult "LEFT JOIN BUERO.FBN_FRAGEBOGEN ON FBN_FRAGEBOGENID = WFN_FRAGEBOGENID" "~"
if joinKuerzel = FBNWFN
searchResult := searchResult "LEFT JOIN BUERO.WFN_WORKFLOWACTION ON WFN_FRAGEBOGENID = FBN_FRAGEBOGENID" "~"
if joinKuerzel = BVOFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = BVO_FARBCODETABNR AND FAR_FARBCODENR = BVO_FARBCODENR" "~"
if joinKuerzel = FARBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_FARBCODETABNR = FAR_FARBCODETABNR AND BVO_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = BWBFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = BWB_FARBCODETABNR AND FAR_FARBCODENR = BWB_FARBCODENR" "~"
if joinKuerzel = FARBWB
searchResult := searchResult "LEFT JOIN BUERO.BWB_BEWERBUNG ON BWB_FARBCODETABNR = FAR_FARBCODETABNR AND BWB_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = DBGFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = DBG_FARBCODETABNR AND FAR_FARBCODENR = DBG_FARBCODENR" "~"
if joinKuerzel = FARDBG
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DBG_DBMDGRUPPE ON DBG_FARBCODETABNR = FAR_FARBCODETABNR AND DBG_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = DIAFAR
searchResult := searchResult "LEFT JOIN BMD.FAR_FARBCODE ON FAR_FARBCODETABNR = DIA_FARBCODETABNR AND FAR_FARBCODENR = DIA_FARBCODENR" "~"
if joinKuerzel = FARDIA
searchResult := searchResult "LEFT JOIN BMD.DIA_DIAGRAMM ON DIA_FARBCODETABNR = FAR_FARBCODETABNR AND DIA_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = EVNFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = EVN_FARBCODETABNR AND FAR_FARBCODENR = EVN_FARBCODENR" "~"
if joinKuerzel = FAREVN
searchResult := searchResult "LEFT JOIN BUERO.EVN_TEILNEHMER ON EVN_FARBCODETABNR = FAR_FARBCODETABNR AND EVN_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = FRBFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = FRB_FARBCODETABNR AND FAR_FARBCODENR = FRB_FARBCODENR" "~"
if joinKuerzel = FARFRB
searchResult := searchResult "LEFT JOIN WWS.FRB_FARBE ON FRB_FARBCODETABNR = FAR_FARBCODETABNR AND FRB_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = KRAFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = KRA_FARBCODETABNR AND FAR_FARBCODENR = KRA_FARBCODENR" "~"
if joinKuerzel = FARKRA
searchResult := searchResult "LEFT JOIN WWS.KRA_KASSENRABATT ON KRA_FARBCODETABNR = FAR_FARBCODETABNR AND KRA_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = LAPFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = LAP_FARBCODETABNR AND FAR_FARBCODENR = LAP_FARBCODENR" "~"
if joinKuerzel = FARLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FARBCODETABNR = FAR_FARBCODETABNR AND LAP_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = QUOFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = QUO_FARBCODETABNR AND FAR_FARBCODENR = QUO_FARBCODENR" "~"
if joinKuerzel = FARQUO
searchResult := searchResult "LEFT JOIN BUERO.QUO_QUOTE ON QUO_FARBCODETABNR = FAR_FARBCODETABNR AND QUO_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = TCAFAR
searchResult := searchResult "LEFT JOIN BMD.FAR_FARBCODE ON FAR_FARBCODETABNR = TCA_FARBCODETABNR AND FAR_FARBCODENR = TCA_FARBCODENR" "~"
if joinKuerzel = FARTCA
searchResult := searchResult "LEFT JOIN BMD.TCA_TCACTIONLIST ON TCA_FARBCODETABNR = FAR_FARBCODETABNR AND TCA_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = TSRFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = TSR_FARBCODETABNR AND FAR_FARBCODENR = TSR_FARBCODENR" "~"
if joinKuerzel = FARTSR
searchResult := searchResult "LEFT JOIN BUERO.TSR_SERIENTERMIN ON TSR_FARBCODETABNR = FAR_FARBCODETABNR AND TSR_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = WAKFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = WAK_FARBCODETABNR AND FAR_FARBCODENR = WAK_FARBCODENR" "~"
if joinKuerzel = FARWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_FARBCODETABNR = FAR_FARBCODETABNR AND WAK_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = XMAFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = XMA_FARBCODETABNR AND FAR_FARBCODENR = XMA_FARBCODENR" "~"
if joinKuerzel = FARXMA
searchResult := searchResult "LEFT JOIN BUERO.XMA_MITARBEITER ON XMA_FARBCODETABNR = FAR_FARBCODETABNR AND XMA_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = DBTFBF
searchResult := searchResult "LEFT JOIN FBF_FELDBESCHFORMAT.FBF_FELDBESCHFORMAT ON FBF_FORMATNR = DBT_FORMATNR" "~"
if joinKuerzel = FBFDBT
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DBT_DBMDTABFELDBESCH ON DBT_FORMATNR = FBF_FORMATNR" "~"
if joinKuerzel = PSMFGK
searchResult := searchResult "LEFT JOIN FGK_FIRMENGKKZU.FGK_FIRMENGKKZU ON FGK_FIRMENNR = PSM_DVFIRMENNR AND FGK_FIRMAGUELTIGJAHR = PSM_PSMJAHR AND FGK_GKKLFDNR = PSM_GKKLFDNR_NOT" "~"
if joinKuerzel = FGKPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_DVFIRMENNR = FGK_FIRMENNR AND PSM_PSMJAHR = FGK_FIRMAGUELTIGJAHR AND PSM_GKKLFDNR_NOT = FGK_GKKLFDNR" "~"
if joinKuerzel = PSMFGM
searchResult := searchResult "LEFT JOIN FGM_FIRMENGEMEINDEZU.FGM_FIRMENGEMEINDEZU ON FGM_FIRMENNR = PSM_DVFIRMENNR AND FGM_FIRMAGUELTIGJAHR = PSM_PSMJAHR AND FGM_FGEMEINDELFDNR = PSM_GEMEINDELFDNR" "~"
if joinKuerzel = FGMPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_DVFIRMENNR = FGM_FIRMENNR AND PSM_PSMJAHR = FGM_FIRMAGUELTIGJAHR AND PSM_GEMEINDELFDNR = FGM_FGEMEINDELFDNR" "~"
if joinKuerzel = ERBFIF
searchResult := searchResult "LEFT JOIN FIBU.FIF_FILIALEFIBU ON FIF_FIRMENNR = ERB_FIRMENNR AND FIF_FIBUNR = ERB_FIBUNR AND FIF_FILIALNR = ERB_FILIALNR" "~"
if joinKuerzel = FIFERB
searchResult := searchResult "LEFT JOIN FIBU.ERB_BUCHUNG ON ERB_FIRMENNR = FIF_FIRMENNR AND ERB_FIBUNR = FIF_FIBUNR AND ERB_FILIALNR = FIF_FILIALNR" "~"
if joinKuerzel = AWSFIN
searchResult := searchResult "LEFT JOIN FIN_FINANZAMT.FIN_FINANZAMT ON FIN_LANDNR = AWS_FIN_LANDNR AND FIN_FINANZAMTNR = AWS_FIN_FINANZAMTNR" "~"
if joinKuerzel = FINAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_FIN_LANDNR = FIN_LANDNR AND AWS_FIN_FINANZAMTNR = FIN_FINANZAMTNR" "~"
if joinKuerzel = FAPFIN
searchResult := searchResult "LEFT JOIN BUERO.FIN_FINANZAMT ON FIN_LANDNR = FAP_LANDNR AND FIN_FINANZAMTNR = FAP_FINANZAMTNR" "~"
if joinKuerzel = FINFAP
searchResult := searchResult "LEFT JOIN BUERO.FAP_FAPERSZU ON FAP_LANDNR = FIN_LANDNR AND FAP_FINANZAMTNR = FIN_FINANZAMTNR" "~"
if joinKuerzel = AAPFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AAP_FIRMENNR" "~"
if joinKuerzel = FIRAAP
searchResult := searchResult "LEFT JOIN WWS.AAP_ARAPOSITION ON AAP_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BOPERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = BOP_ERT_VOR_FIRMENNR AND ERT_ERFASSTEXTNR = BOP_ERT_VOR_ERFASSTEXTNR" "~"
if joinKuerzel = ERTBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_ERT_VOR_FIRMENNR = ERT_FIRMENNR AND BOP_ERT_VOR_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = BPKERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = BPK_FIRMENNR AND ERT_ERFASSTEXTNR = BPK_KOPF_ERFASSTEXTNR" "~"
if joinKuerzel = ERTBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_FIRMENNR = ERT_FIRMENNR AND BPK_KOPF_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = ERPERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = ERP_FIRMENNR AND ERT_ERFASSTEXTNR = ERP_VOR_ERFASSTEXTNR" "~"
if joinKuerzel = ERTERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_FIRMENNR = ERT_FIRMENNR AND ERP_VOR_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = WAPERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = WAP_ERT_FIRMENNR AND ERT_ERFASSTEXTNR = WAP_BESCH_ERFASSTEXTNR" "~"
if joinKuerzel = ERTWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_ERT_FIRMENNR = ERT_FIRMENNR AND WAP_BESCH_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = BOPERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = BOP_ERT_NACH_FIRMENNR AND ERT_ERFASSTEXTNR = BOP_ERT_NACH_ERFASSTEXTNR" "~"
if joinKuerzel = ERTBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_ERT_NACH_FIRMENNR = ERT_FIRMENNR AND BOP_ERT_NACH_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = EFKERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = EFK_FIRMENNR AND ERT_ERFASSTEXTNR = EFK_FUSS_ERFASSTEXTNR" "~"
if joinKuerzel = ERTEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = ERT_FIRMENNR AND EFK_FUSS_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = KAPERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = KAP_ERT_NACH_FIRMENNR AND ERT_ERFASSTEXTNR = KAP_ERT_NACH_ERFASSTEXTNR" "~"
if joinKuerzel = ERTKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_ERT_NACH_FIRMENNR = ERT_FIRMENNR AND KAP_ERT_NACH_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = ERPERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = ERP_FIRMENNR AND ERT_ERFASSTEXTNR = ERP_KURZBEZ_ERFASSTEXTNR" "~"
if joinKuerzel = ERTERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_FIRMENNR = ERT_FIRMENNR AND ERP_KURZBEZ_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = AVEERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = AVE_FIRMENNR AND ERT_ERFASSTEXTNR = AVE_ERP_VOR_ERFASSTEXTNR" "~"
if joinKuerzel = ERTAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_FIRMENNR = ERT_FIRMENNR AND AVE_ERP_VOR_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = RNGERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = RNG_FIRMENNR AND ERT_ERFASSTEXTNR = RNG_STEUER_ERFASSTEXTNR_S2" "~"
if joinKuerzel = ERTRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = ERT_FIRMENNR AND RNG_STEUER_ERFASSTEXTNR_S2 = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = ALEEVN
searchResult := searchResult "LEFT JOIN BUERO.EVN_TEILNEHMER ON EVN_TEILNEHMERID = ALE_EVN_TEILNEHMERID" "~"
if joinKuerzel = EVNALE
searchResult := searchResult "LEFT JOIN BUERO.ALE_ADRESSENLISTE ON ALE_EVN_TEILNEHMERID = EVN_TEILNEHMERID" "~"
if joinKuerzel = EVNEVN
searchResult := searchResult "LEFT JOIN BUERO.EVN_TEILNEHMER ON EVN_TEILNEHMERID = EVN_GEHOERTZU_TEILNEHMERID" "~"
if joinKuerzel = EVNEVN
searchResult := searchResult "LEFT JOIN BUERO.EVN_TEILNEHMER ON EVN_GEHOERTZU_TEILNEHMERID = EVN_TEILNEHMERID" "~"
if joinKuerzel = WTEEVN
searchResult := searchResult "LEFT JOIN BUERO.EVN_TEILNEHMER ON EVN_TEILNEHMERID = WTE_TEILNEHMERID" "~"
if joinKuerzel = EVNWTE
searchResult := searchResult "LEFT JOIN BUERO.WTE_WEBTEILNEHMER ON WTE_TEILNEHMERID = EVN_TEILNEHMERID" "~"
if joinKuerzel = EAZEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_EVENTID = EAZ_EVT_EVENTID" "~"
if joinKuerzel = EVTEAZ
searchResult := searchResult "LEFT JOIN BUERO.EAZ_EVENTAUSBZU ON EAZ_EVT_EVENTID = EVT_EVENTID" "~"
if joinKuerzel = EVAEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_EVENTID = EVA_EVENTID" "~"
if joinKuerzel = EVTEVA
searchResult := searchResult "LEFT JOIN BUERO.EVA_EVENTTEAM ON EVA_EVENTID = EVT_EVENTID" "~"
if joinKuerzel = EWLEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_EVENTID = EWL_EVENTID" "~"
if joinKuerzel = EVTEWL
searchResult := searchResult "LEFT JOIN BUERO.EWL_EVENTWARTELISTE ON EWL_EVENTID = EVT_EVENTID" "~"
if joinKuerzel = MEVEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_EVENTID = MEV_EVT_EVENTID" "~"
if joinKuerzel = EVTMEV
searchResult := searchResult "LEFT JOIN BUERO.MEV_MEDIENVERLEIH ON MEV_EVT_EVENTID = EVT_EVENTID" "~"
if joinKuerzel = MITFAK
searchResult := searchResult "LEFT JOIN FAK_FAKTORTABELLE.FAK_FAKTORTABELLE ON FAK_FAKID = MIT_FAKID" "~"
if joinKuerzel = FAKMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FAKID = FAK_FAKID" "~"
if joinKuerzel = RZSFAK
searchResult := searchResult "LEFT JOIN FAK_FAKTORTABELLE.FAK_FAKTORTABELLE ON FAK_FAKID = RZS_FAKID" "~"
if joinKuerzel = FAKRZS
searchResult := searchResult "LEFT JOIN BUERO.RZS_RUECKZAHLUNGSSTAMM ON RZS_FAKID = FAK_FAKID" "~"
if joinKuerzel = ABFFAR
searchResult := searchResult "LEFT JOIN BMD.FAR_FARBCODE ON FAR_FARBCODETABNR = ABF_FARBCODETABNR AND FAR_FARBCODENR = ABF_FARBCODENR" "~"
if joinKuerzel = FARABF
searchResult := searchResult "LEFT JOIN BMD.ABF_ABFRAGE ON ABF_FARBCODETABNR = FAR_FARBCODETABNR AND ABF_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = ADDFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = ADD_FARBCODETABNR AND FAR_FARBCODENR = ADD_FARBCODENR" "~"
if joinKuerzel = FARADD
searchResult := searchResult "LEFT JOIN BUERO.ADD_AUSDRUCKMODELLDETAIL ON ADD_FARBCODETABNR = FAR_FARBCODETABNR AND ADD_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = ADOFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = ADO_FARBCODETABNR AND FAR_FARBCODENR = ADO_FARBCODENR" "~"
if joinKuerzel = FARADO
searchResult := searchResult "LEFT JOIN BUERO.ADO_AUSDRUCKMODELL ON ADO_FARBCODETABNR = FAR_FARBCODETABNR AND ADO_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = ERPERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = ERP_FIRMENNR AND ERT_ERFASSTEXTNR = ERP_NACH_ERFASSTEXTNR" "~"
if joinKuerzel = ERTERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_FIRMENNR = ERT_FIRMENNR AND ERP_NACH_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = AVEERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = AVE_ERFGRP_ET_FIRMENNR AND ERT_ERFASSTEXTNR = AVE_ERFGRP_ERFASSTEXTNR3" "~"
if joinKuerzel = ERTAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ERFGRP_ET_FIRMENNR = ERT_FIRMENNR AND AVE_ERFGRP_ERFASSTEXTNR3 = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = RNGERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = RNG_FIRMENNR AND ERT_ERFASSTEXTNR = RNG_EMAIL_ERFASSTEXTNR" "~"
if joinKuerzel = ERTRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = ERT_FIRMENNR AND RNG_EMAIL_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = KAPERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = KAP_ERT_BESCH_FIRMENNR AND ERT_ERFASSTEXTNR = KAP_ERT_BESCH_ERFASSTEXTNR" "~"
if joinKuerzel = ERTKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_ERT_BESCH_FIRMENNR = ERT_FIRMENNR AND KAP_ERT_BESCH_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = EFKERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = EFK_FIRMENNR AND ERT_ERFASSTEXTNR = EFK_FUSS_LFS_ERFASSTEXTNR" "~"
if joinKuerzel = ERTEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = ERT_FIRMENNR AND EFK_FUSS_LFS_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = RNGERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = RNG_FIRMENNR AND ERT_ERFASSTEXTNR = RNG_STEUER_ERFASSTEXTNR_S5" "~"
if joinKuerzel = ERTRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = ERT_FIRMENNR AND RNG_STEUER_ERFASSTEXTNR_S5 = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = EFKERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_FIRMENNR = EFK_FIRMENNR AND ERZ_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ" "~"
if joinKuerzel = ERZEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = ERZ_FIRMENNR AND EFK_ERFASSUNGSKZ = ERZ_ERFASSUNGSKZ" "~"
if joinKuerzel = GSAERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_FIRMENNR = GSA_ERZ_FIRMENNR AND ERZ_ERFASSUNGSKZ = GSA_ERZ_ERFASSUNGSKZ" "~"
if joinKuerzel = ERZGSA
searchResult := searchResult "LEFT JOIN WWS.GSA_GUTSCHEINAKTION ON GSA_ERZ_FIRMENNR = ERZ_FIRMENNR AND GSA_ERZ_ERFASSUNGSKZ = ERZ_ERFASSUNGSKZ" "~"
if joinKuerzel = ERZERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_FIRMENNR = ERZ_ABRUF_ERFKZ_FIRMENNR AND ERZ_ERFASSUNGSKZ = ERZ_ABRUF_ERFKZ" "~"
if joinKuerzel = ERZERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_ABRUF_ERFKZ_FIRMENNR = ERZ_FIRMENNR AND ERZ_ABRUF_ERFKZ = ERZ_ERFASSUNGSKZ" "~"
if joinKuerzel = AKREVN
searchResult := searchResult "LEFT JOIN BUERO.EVN_TEILNEHMER ON EVN_TEILNEHMERID = AKR_EVN_TEILNEHMERID" "~"
if joinKuerzel = EVNAKR
searchResult := searchResult "LEFT JOIN BUERO.AKR_AVKOSTENARTENZU ON AKR_EVN_TEILNEHMERID = EVN_TEILNEHMERID" "~"
if joinKuerzel = ETZEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_EVENTID = ETZ_EVT_EVENTID" "~"
if joinKuerzel = EVTETZ
searchResult := searchResult "LEFT JOIN BUERO.ETZ_EVENTTERMINZU ON ETZ_EVT_EVENTID = EVT_EVENTID" "~"
if joinKuerzel = EAFEZT
searchResult := searchResult "LEFT JOIN HRS.EZT_ELDAZENTRALTABELLE ON EZT_EZTID = EAF_EZTID" "~"
if joinKuerzel = EZTEAF
searchResult := searchResult "LEFT JOIN HRS.EAF_ELDADETAIL_AFB ON EAF_EZTID = EZT_EZTID" "~"
if joinKuerzel = EAVEZT
searchResult := searchResult "LEFT JOIN HRS.EZT_ELDAZENTRALTABELLE ON EZT_EZTID = EAV_EZTID" "~"
if joinKuerzel = EZTEAV
searchResult := searchResult "LEFT JOIN HRS.EAV_ELDADETAIL_AV ON EAV_EZTID = EZT_EZTID" "~"
if joinKuerzel = EEFEZT
searchResult := searchResult "LEFT JOIN HRS.EZT_ELDAZENTRALTABELLE ON EZT_EZTID = EEF_EZTID" "~"
if joinKuerzel = EZTEEF
searchResult := searchResult "LEFT JOIN HRS.EEF_ELDADETAIL_EFZ ON EEF_EZTID = EZT_EZTID" "~"
if joinKuerzel = ELZEZT
searchResult := searchResult "LEFT JOIN HRS.EZT_ELDAZENTRALTABELLE ON EZT_EZTID = ELZ_EZTID" "~"
if joinKuerzel = EZTELZ
searchResult := searchResult "LEFT JOIN HRS.ELZ_ELDADETAIL_LZM ON ELZ_EZTID = EZT_EZTID" "~"
if joinKuerzel = AGNFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = AGN_FARBCODETABNR AND FAR_FARBCODENR = AGN_FARBCODENR" "~"
if joinKuerzel = FARAGN
searchResult := searchResult "LEFT JOIN WWS.AGN_AG_PERSON_ZU ON AGN_FARBCODETABNR = FAR_FARBCODETABNR AND AGN_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = BRCFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = BRC_FARBCODETABNR AND FAR_FARBCODENR = BRC_FARBCODENR" "~"
if joinKuerzel = FARBRC
searchResult := searchResult "LEFT JOIN BUERO.BRC_BEREICH ON BRC_FARBCODETABNR = FAR_FARBCODETABNR AND BRC_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = EKKFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = EKK_FARBCODETABNR AND FAR_FARBCODENR = EKK_FARBCODENR" "~"
if joinKuerzel = FAREKK
searchResult := searchResult "LEFT JOIN WWS.EKK_EXTERNE_KUNDENKARTEI ON EKK_FARBCODETABNR = FAR_FARBCODETABNR AND EKK_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = INSFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = INS_FARBCODETABNR AND FAR_FARBCODENR = INS_FARBCODENR" "~"
if joinKuerzel = FARINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_FARBCODETABNR = FAR_FARBCODETABNR AND INS_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = MEDFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = MED_FARBCODETABNR AND FAR_FARBCODENR = MED_FARBCODENR" "~"
if joinKuerzel = FARMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_FARBCODETABNR = FAR_FARBCODETABNR AND MED_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = NOTFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = NOT_FARBCODETABNR AND FAR_FARBCODENR = NOT_FARBCODENR" "~"
if joinKuerzel = FARNOT
searchResult := searchResult "LEFT JOIN BUERO.NOT_NOTIZ ON NOT_FARBCODETABNR = FAR_FARBCODETABNR AND NOT_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = VAKFAK
searchResult := searchResult "LEFT JOIN FAK_FAKTORTABELLE.FAK_FAKTORTABELLE ON FAK_FAKID = VAK_RZTABELLE_FAKID" "~"
if joinKuerzel = FAKVAK
searchResult := searchResult "LEFT JOIN BUERO.VAK_VERANSTALTUNGSKOPF ON VAK_RZTABELLE_FAKID = FAK_FAKID" "~"
if joinKuerzel = BMUFAR
searchResult := searchResult "LEFT JOIN BMD.FAR_FARBCODE ON FAR_FARBCODETABNR = BMU_FARBCODETABNR AND FAR_FARBCODENR = BMU_FARBCODENR" "~"
if joinKuerzel = FARBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_FARBCODETABNR = FAR_FARBCODETABNR AND BMU_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = BPKFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = BPK_FARBCODETABNR AND FAR_FARBCODENR = BPK_FARBCODENR" "~"
if joinKuerzel = FARBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_FARBCODETABNR = FAR_FARBCODETABNR AND BPK_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = EKZFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = EKZ_FARBCODETABNR AND FAR_FARBCODENR = EKZ_FARBCODENR" "~"
if joinKuerzel = FAREKZ
searchResult := searchResult "LEFT JOIN WWS.EKZ_ERLOESAUFWANDKZ ON EKZ_FARBCODETABNR = FAR_FARBCODETABNR AND EKZ_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = FAVFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = FAV_FARBCODETABNR AND FAR_FARBCODENR = FAV_FARBCODENR" "~"
if joinKuerzel = FARFAV
searchResult := searchResult "LEFT JOIN BUERO.FAV_FAVORITEN ON FAV_FARBCODETABNR = FAR_FARBCODETABNR AND FAV_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = GATFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = GAT_FARBCODETABNR AND FAR_FARBCODENR = GAT_FARBCODENR" "~"
if joinKuerzel = FARGAT
searchResult := searchResult "LEFT JOIN ONVIS.GAT_GARNTYPEN ON GAT_FARBCODETABNR = FAR_FARBCODETABNR AND GAT_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = GTPFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = GTP_FARBCODETABNR AND FAR_FARBCODENR = GTP_FARBCODENR" "~"
if joinKuerzel = FARGTP
searchResult := searchResult "LEFT JOIN WWS.GTP_GROESSENTABPOS ON GTP_FARBCODETABNR = FAR_FARBCODETABNR AND GTP_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = KJOFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = KJO_FARBCODETABNR AND FAR_FARBCODENR = KJO_FARBCODENR" "~"
if joinKuerzel = FARKJO
searchResult := searchResult "LEFT JOIN BUERO.KJO_KONTAKTJOURNAL ON KJO_FARBCODETABNR = FAR_FARBCODETABNR AND KJO_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = LIBFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = LIB_FARBCODETABNR AND FAR_FARBCODENR = LIB_FARBCODENR" "~"
if joinKuerzel = FARLIB
searchResult := searchResult "LEFT JOIN WWS.LIB_LIEFERBEDINGUNG ON LIB_FARBCODETABNR = FAR_FARBCODETABNR AND LIB_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = PERFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = PER_FARBCODETABNR AND FAR_FARBCODENR = PER_FARBCODENR" "~"
if joinKuerzel = FARPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FARBCODETABNR = FAR_FARBCODETABNR AND PER_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = RAPFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = RAP_FARBCODETABNR AND FAR_FARBCODENR = RAP_FARBCODENR" "~"
if joinKuerzel = FARRAP
searchResult := searchResult "LEFT JOIN WWS.RAP_RABATTTABPOS ON RAP_FARBCODETABNR = FAR_FARBCODETABNR AND RAP_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = RPPFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = RPP_FARBCODETABNR AND FAR_FARBCODENR = RPP_FARBCODENR" "~"
if joinKuerzel = FARRPP
searchResult := searchResult "LEFT JOIN ONVIS.RPP_RAPPORTE ON RPP_FARBCODETABNR = FAR_FARBCODETABNR AND RPP_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = VEBFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = VEB_FARBCODETABNR AND FAR_FARBCODENR = VEB_FARBCODENR" "~"
if joinKuerzel = FARVEB
searchResult := searchResult "LEFT JOIN FIBU.VEB_VORERFASSBUCHUNG ON VEB_FARBCODETABNR = FAR_FARBCODETABNR AND VEB_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = ARLFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = ARL_LIZENZ_HP_FARBCODTABNR AND FAR_FARBCODENR = ARL_LIZENZ_HP_FARBCODENR" "~"
if joinKuerzel = FARARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_LIZENZ_HP_FARBCODTABNR = FAR_FARBCODETABNR AND ARL_LIZENZ_HP_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = FTPFAT
searchResult := searchResult "LEFT JOIN WWS.FAT_FARBENTAB ON FAT_FIRMENNR = FTP_FAT_FIRMENNR AND FAT_FARBENTABNR = FTP_FARBENTABNR" "~"
if joinKuerzel = FATFTP
searchResult := searchResult "LEFT JOIN WWS.FTP_FARBENTABPOS ON FTP_FAT_FIRMENNR = FAT_FIRMENNR AND FTP_FARBENTABNR = FAT_FARBENTABNR" "~"
if joinKuerzel = WFNFBK
searchResult := searchResult "LEFT JOIN FBK_FELDBESCHKOPF.FBK_FELDBESCHKOPF ON FBK_FELDBESCHNR = WFN_SACHB_FELDAUSWAHLID" "~"
if joinKuerzel = FBKWFN
searchResult := searchResult "LEFT JOIN BUERO.WFN_WORKFLOWACTION ON WFN_SACHB_FELDAUSWAHLID = FBK_FELDBESCHNR" "~"
if joinKuerzel = SUKFFA
searchResult := searchResult "LEFT JOIN FFA_FIRMENFINANZAMTZU.FFA_FIRMENFINANZAMTZU ON FFA_FIRMENNR = SUK_FIRMENNR AND FFA_FIRMAGUELTIGJAHR = SUK_JAHR AND FFA_FFINANZAMTLFDNR = SUK_FINANZAMTLFDNR" "~"
if joinKuerzel = FFASUK
searchResult := searchResult "LEFT JOIN HRS.SUK_SUM_KOE ON SUK_FIRMENNR = FFA_FIRMENNR AND SUK_JAHR = FFA_FIRMAGUELTIGJAHR AND SUK_FINANZAMTLFDNR = FFA_FFINANZAMTLFDNR" "~"
if joinKuerzel = FAZFFZ
searchResult := searchResult "LEFT JOIN BUERO.FFZ_FRAGEBOGENFRAGEZU ON FFZ_FFZUID = FAZ_FFZUID" "~"
if joinKuerzel = FFZFAZ
searchResult := searchResult "LEFT JOIN BUERO.FAZ_FRAGEANTWORTZU ON FAZ_FFZUID = FFZ_FFZUID" "~"
if joinKuerzel = PSMFGK
searchResult := searchResult "LEFT JOIN FGK_FIRMENGKKZU.FGK_FIRMENGKKZU ON FGK_FIRMENNR = PSM_DVFIRMENNR AND FGK_FIRMAGUELTIGJAHR = PSM_PSMJAHR AND FGK_GKKLFDNR = PSM_GKKLFDNR" "~"
if joinKuerzel = FGKPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_DVFIRMENNR = FGK_FIRMENNR AND PSM_PSMJAHR = FGK_FIRMAGUELTIGJAHR AND PSM_GKKLFDNR = FGK_GKKLFDNR" "~"
if joinKuerzel = GSKFGT
searchResult := searchResult "LEFT JOIN WWS.FGT_FREMDGUTSCHEINTYP ON FGT_FIRMENNR = GSK_FGT_FIRMENNR AND FGT_FREMDGUTSCHEINTYPLFDNR = GSK_FGT_LFDNR" "~"
if joinKuerzel = FGTGSK
searchResult := searchResult "LEFT JOIN WWS.GSK_GUTSCHEINKOPF ON GSK_FGT_FIRMENNR = FGT_FIRMENNR AND GSK_FGT_LFDNR = FGT_FREMDGUTSCHEINTYPLFDNR" "~"
if joinKuerzel = FDPFDK
searchResult := searchResult "LEFT JOIN BMD.FDK_FELDWERTEKOPF ON FDK_FELDWERTENR = FDP_FELDWERTENR" "~"
if joinKuerzel = FDKFDP
searchResult := searchResult "LEFT JOIN BMD.FDP_FELDWERTEPOS ON FDP_FELDWERTENR = FDK_FELDWERTENR" "~"
if joinKuerzel = UATFFZ
searchResult := searchResult "LEFT JOIN BUERO.FFZ_FRAGEBOGENFRAGEZU ON FFZ_FFZUID = UAT_LETZTE_FFZUID" "~"
if joinKuerzel = FFZUAT
searchResult := searchResult "LEFT JOIN BUERO.UAT_UMFRAGEANTWTEILNZU ON UAT_LETZTE_FFZUID = FFZ_FFZUID" "~"
if joinKuerzel = OFRFFZ
searchResult := searchResult "LEFT JOIN BUERO.FFZ_FRAGEBOGENFRAGEZU ON FFZ_FFZUID = OFR_LINKFFZUID" "~"
if joinKuerzel = FFZOFR
searchResult := searchResult "LEFT JOIN BUERO.OFR_FRAGEBOGENREGEL ON OFR_LINKFFZUID = FFZ_FFZUID" "~"
if joinKuerzel = FGAFGB
searchResult := searchResult "LEFT JOIN BUERO.FGB_FRAGEBOGEN ON FGB_KRITERIENNR = FGA_KRITERIENNR AND FGB_BEFRAGUNGSNR = FGA_BEFRAGUNGSNR AND FGB_FIRMENNR = FGA_FIRMENNR AND FGB_KLID = FGA_KLID" "~"
if joinKuerzel = FGBFGA
searchResult := searchResult "LEFT JOIN BUERO.FGA_FRAGEBOGENAUSWAHL ON FGA_KRITERIENNR = FGB_KRITERIENNR AND FGA_BEFRAGUNGSNR = FGB_BEFRAGUNGSNR AND FGA_FIRMENNR = FGB_FIRMENNR AND FGA_KLID = FGB_KLID" "~"
if joinKuerzel = MBMFGK
searchResult := searchResult "LEFT JOIN FGK_FIRMENGKKZU.FGK_FIRMENGKKZU ON FGK_FIRMENNR = MBM_DVFIRMENNR AND FGK_FIRMAGUELTIGJAHR = MBM_JAHR AND FGK_GKKLFDNR = MBM_GKKLFDNR" "~"
if joinKuerzel = FGKMBM
searchResult := searchResult "LEFT JOIN HRS.MBM_MBGM ON MBM_DVFIRMENNR = FGK_FIRMENNR AND MBM_JAHR = FGK_FIRMAGUELTIGJAHR AND MBM_GKKLFDNR = FGK_GKKLFDNR" "~"
if joinKuerzel = GVTFGM
searchResult := searchResult "LEFT JOIN FGM_FIRMENGEMEINDEZU.FGM_FIRMENGEMEINDEZU ON FGM_FIRMENNR = GVT_DVFIRMENNR AND FGM_FIRMAGUELTIGJAHR = GVT_JAHR AND FGM_FGEMEINDELFDNR = GVT_FGEMEINDELFDNR" "~"
if joinKuerzel = FGMGVT
searchResult := searchResult "LEFT JOIN HRS.GVT_GEMVERTEILUNG ON GVT_DVFIRMENNR = FGM_FIRMENNR AND GVT_JAHR = FGM_FIRMAGUELTIGJAHR AND GVT_FGEMEINDELFDNR = FGM_FGEMEINDELFDNR" "~"
if joinKuerzel = KSTFGM
searchResult := searchResult "LEFT JOIN FGM_FIRMENGEMEINDEZU.FGM_FIRMENGEMEINDEZU ON FGM_FIRMENNR = KST_FGM_KOMM_ST_GEM_FIRMENNR AND FGM_FIRMAGUELTIGJAHR = KST_FGM_KOMM_ST_GEM_FGJ AND FGM_FGEMEINDELFDNR = KST_FGM_KOMM_ST_GEM_LFDNR" "~"
if joinKuerzel = FGMKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FGM_KOMM_ST_GEM_FIRMENNR = FGM_FIRMENNR AND KST_FGM_KOMM_ST_GEM_FGJ = FGM_FIRMAGUELTIGJAHR AND KST_FGM_KOMM_ST_GEM_LFDNR = FGM_FGEMEINDELFDNR" "~"
if joinKuerzel = KTOFIF
searchResult := searchResult "LEFT JOIN FIBU.FIF_FILIALEFIBU ON FIF_FIRMENNR = KTO_FIRMENNR AND FIF_FIBUNR = KTO_FIBUNR AND FIF_FILIALNR = KTO_FILIALNR" "~"
if joinKuerzel = FIFKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = FIF_FIRMENNR AND KTO_FIBUNR = FIF_FIBUNR AND KTO_FILIALNR = FIF_FILIALNR" "~"
if joinKuerzel = FFAFIN
searchResult := searchResult "LEFT JOIN BUERO.FIN_FINANZAMT ON FIN_LANDNR = FFA_LANDNR AND FIN_FINANZAMTNR = FFA_FINANZAMTNR" "~"
if joinKuerzel = FINFFA
searchResult := searchResult "LEFT JOIN BUERO.FFA_FIRMENFINANZAMTZU ON FFA_LANDNR = FIN_LANDNR AND FFA_FINANZAMTNR = FIN_FINANZAMTNR" "~"
if joinKuerzel = XSEFIN
searchResult := searchResult "LEFT JOIN BUERO.FIN_FINANZAMT ON FIN_LANDNR = XSE_LANDNR AND FIN_FINANZAMTNR = XSE_FINANZAMTNR" "~"
if joinKuerzel = FINXSE
searchResult := searchResult "LEFT JOIN BUERO.XSE_XML_STEUERERKL ON XSE_LANDNR = FIN_LANDNR AND XSE_FINANZAMTNR = FIN_FINANZAMTNR" "~"
if joinKuerzel = ADWFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ADW_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRADW
searchResult := searchResult "LEFT JOIN WWS.ADW_ABGABENDETAILWAP ON ADW_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AKKFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = AKK_FIRMENNR" "~"
if joinKuerzel = FIRAKK
searchResult := searchResult "LEFT JOIN BUERO.AKK_AKTIONSTATUSKOPF ON AKK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ASKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ASK_FIRMENNR" "~"
if joinKuerzel = FIRASK
searchResult := searchResult "LEFT JOIN ZLE.ASK_SORTIERKOPF ON ASK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ASRFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = ASR_FIRMENNR" "~"
if joinKuerzel = FIRASR
searchResult := searchResult "LEFT JOIN BUERO.ASR_AUSSCHREIBUNG ON ASR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AULFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AUL_FIRMENNR" "~"
if joinKuerzel = FIRAUL
searchResult := searchResult "LEFT JOIN WWS.AUL_ARTIKELUMSTELLLISTE ON AUL_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AVGFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AVG_FIRMENNR" "~"
if joinKuerzel = FIRAVG
searchResult := searchResult "LEFT JOIN WWS.AVG_AUFTRAGSVORERFGRUPP ON AVG_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AVRFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = AVR_FIRMENNR" "~"
if joinKuerzel = FIRAVR
searchResult := searchResult "LEFT JOIN BUERO.AVR_AVRUEZAHLUNGSTABELLE ON AVR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AWRFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = AWR_FIRMENNR" "~"
if joinKuerzel = FIRAWR
searchResult := searchResult "LEFT JOIN BUERO.AWR_AUSWERTUNGSKAT ON AWR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AWSFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AWS_LEI_GEHOERT_FIRMENNR" "~"
if joinKuerzel = FIRAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_LEI_GEHOERT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BKZFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = BKZ_FIRMENNR" "~"
if joinKuerzel = FIRBKZ
searchResult := searchResult "LEFT JOIN BIL.BKZ_BILANZKENNZAHL ON BKZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BUCFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = BUC_LIZENZ_FIRMENNR" "~"
if joinKuerzel = FIRBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_LIZENZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BVLFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = BVL_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRBVL
searchResult := searchResult "LEFT JOIN WWS.BVL_LIEFERANTENZUORDNUNG ON BVL_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SFEEVS
searchResult := searchResult "LEFT JOIN BUERO.EVS_EVENTLNSTATUS ON EVS_STATUSID = SFE_EVS_TEILNAHME_STATUSID" "~"
if joinKuerzel = EVSSFE
searchResult := searchResult "LEFT JOIN BUERO.SFE_SKILLFINDERELEMENT ON SFE_EVS_TEILNAHME_STATUSID = EVS_STATUSID" "~"
if joinKuerzel = SLREVS
searchResult := searchResult "LEFT JOIN BUERO.EVS_EVENTLNSTATUS ON EVS_STATUSID = SLR_EVS_ZUSAGE_STATUSID" "~"
if joinKuerzel = EVSSLR
searchResult := searchResult "LEFT JOIN BUERO.SLR_SKILLREGELELEMENT ON SLR_EVS_ZUSAGE_STATUSID = EVS_STATUSID" "~"
if joinKuerzel = GOEEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_EVENTID = GOE_EVENTID" "~"
if joinKuerzel = EVTGOE
searchResult := searchResult "LEFT JOIN BUERO.GOE_GRUPPENORDNEREVENTZU ON GOE_EVENTID = EVT_EVENTID" "~"
if joinKuerzel = KJOEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_EVENTID = KJO_EVENTID" "~"
if joinKuerzel = EVTKJO
searchResult := searchResult "LEFT JOIN BUERO.KJO_KONTAKTJOURNAL ON KJO_EVENTID = EVT_EVENTID" "~"
if joinKuerzel = TEREVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_EVENTID = TER_EVT_EVENTID" "~"
if joinKuerzel = EVTTER
searchResult := searchResult "LEFT JOIN BUERO.TER_TERMIN ON TER_EVT_EVENTID = EVT_EVENTID" "~"
if joinKuerzel = EDAEZT
searchResult := searchResult "LEFT JOIN HRS.EZT_ELDAZENTRALTABELLE ON EZT_EZTID = EDA_EZTID" "~"
if joinKuerzel = EZTEDA
searchResult := searchResult "LEFT JOIN HRS.EDA_ELDADETAIL_ADR ON EDA_EZTID = EZT_EZTID" "~"
if joinKuerzel = EMBEZT
searchResult := searchResult "LEFT JOIN HRS.EZT_ELDAZENTRALTABELLE ON EZT_EZTID = EMB_EZTID" "~"
if joinKuerzel = EZTEMB
searchResult := searchResult "LEFT JOIN HRS.EMB_ELDADETAIL_MBGM_P ON EMB_EZTID = EZT_EZTID" "~"
if joinKuerzel = EMIEZT
searchResult := searchResult "LEFT JOIN HRS.EZT_ELDAZENTRALTABELLE ON EZT_EZTID = EMI_EZTID" "~"
if joinKuerzel = EZTEMI
searchResult := searchResult "LEFT JOIN HRS.EMI_ELDADETAIL_MA ON EMI_EZTID = EZT_EZTID" "~"
if joinKuerzel = EVMEZT
searchResult := searchResult "LEFT JOIN HRS.EZT_ELDAZENTRALTABELLE ON EZT_EZTID = EVM_EZTID" "~"
if joinKuerzel = EZTEVM
searchResult := searchResult "LEFT JOIN HRS.EVM_ELDADETAIL_VM ON EVM_EZTID = EZT_EZTID" "~"
if joinKuerzel = FAWFAK
searchResult := searchResult "LEFT JOIN HRS.FAK_FAKTORTABELLE ON FAK_FAKID = FAW_FAKID" "~"
if joinKuerzel = FAKFAW
searchResult := searchResult "LEFT JOIN HRS.FAW_FAKTORWERTE ON FAW_FAKID = FAK_FAKID" "~"
if joinKuerzel = DNEFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = DNE_FARBCODETABNR AND FAR_FARBCODENR = DNE_FARBCODENR" "~"
if joinKuerzel = FARDNE
searchResult := searchResult "LEFT JOIN DATANORM.DNE_ERSTELLER ON DNE_FARBCODETABNR = FAR_FARBCODETABNR AND DNE_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = EINFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = EIN_FARBCODETABNR AND FAR_FARBCODENR = EIN_FARBCODENR" "~"
if joinKuerzel = FAREIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_FARBCODETABNR = FAR_FARBCODETABNR AND EIN_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = ERAFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = ERA_FARBCODETABNR AND FAR_FARBCODENR = ERA_FARBCODENR" "~"
if joinKuerzel = FARERA
searchResult := searchResult "LEFT JOIN WWS.ERA_ERFASSRABATT ON ERA_FARBCODETABNR = FAR_FARBCODETABNR AND ERA_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = ERBFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = ERB_FARBCODETABNR AND FAR_FARBCODENR = ERB_FARBCODENR" "~"
if joinKuerzel = FARERB
searchResult := searchResult "LEFT JOIN FIBU.ERB_BUCHUNG ON ERB_FARBCODETABNR = FAR_FARBCODETABNR AND ERB_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = FOTFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = FOT_FARBCODETABNR AND FAR_FARBCODENR = FOT_FARBCODENR" "~"
if joinKuerzel = FARFOT
searchResult := searchResult "LEFT JOIN BUERO.FOT_FOTO ON FOT_FARBCODETABNR = FAR_FARBCODETABNR AND FOT_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = IWVFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = IWV_FARBCODETABNR AND FAR_FARBCODENR = IWV_FARBCODENR" "~"
if joinKuerzel = FARIWV
searchResult := searchResult "LEFT JOIN BUERO.IWV_INTRASTATWV ON IWV_FARBCODETABNR = FAR_FARBCODETABNR AND IWV_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = KAKFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = KAK_FARBCODETABNR AND FAR_FARBCODENR = KAK_FARBCODENR" "~"
if joinKuerzel = FARKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_FARBCODETABNR = FAR_FARBCODETABNR AND KAK_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = LAWFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = LAW_FARBCODETABNR AND FAR_FARBCODENR = LAW_FARBCODENR" "~"
if joinKuerzel = FARLAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_FARBCODETABNR = FAR_FARBCODETABNR AND LAW_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = PAPFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = PAP_FARBCODETABNR AND FAR_FARBCODENR = PAP_FARBCODENR" "~"
if joinKuerzel = FARPAP
searchResult := searchResult "LEFT JOIN WWS.PAP_PACKSTOFFTABPOS ON PAP_FARBCODETABNR = FAR_FARBCODETABNR AND PAP_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = SCMFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = SCM_FARBCODETABNR AND FAR_FARBCODENR = SCM_FARBCODENR" "~"
if joinKuerzel = FARSCM
searchResult := searchResult "LEFT JOIN BMDENTW.SCM_SCRMAIN ON SCM_FARBCODETABNR = FAR_FARBCODETABNR AND SCM_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = SRAFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = SRA_FARBCODETABNR AND FAR_FARBCODENR = SRA_FARBCODENR" "~"
if joinKuerzel = FARSRA
searchResult := searchResult "LEFT JOIN WWS.SRA_STEUERART ON SRA_FARBCODETABNR = FAR_FARBCODETABNR AND SRA_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = TERFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = TER_FARBCODETABNR AND FAR_FARBCODENR = TER_FARBCODENR" "~"
if joinKuerzel = FARTER
searchResult := searchResult "LEFT JOIN BUERO.TER_TERMIN ON TER_FARBCODETABNR = FAR_FARBCODETABNR AND TER_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = AGMFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = AGM_FARBCODETABNR AND FAR_FARBCODENR = AGM_FARBCODENR" "~"
if joinKuerzel = FARAGM
searchResult := searchResult "LEFT JOIN PRODUKTION.AGM_AG_PERSON_ZU ON AGM_FARBCODETABNR = FAR_FARBCODETABNR AND AGM_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = AGSFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = AGS_FARBCODETABNR AND FAR_FARBCODENR = AGS_FARBCODENR" "~"
if joinKuerzel = FARAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_FARBCODETABNR = FAR_FARBCODETABNR AND AGS_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = BPPFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = BPP_FARBCODETABNR AND FAR_FARBCODENR = BPP_FARBCODENR" "~"
if joinKuerzel = FARBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_FARBCODETABNR = FAR_FARBCODETABNR AND BPP_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = DOKFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = DOK_FARBCODETABNR AND FAR_FARBCODENR = DOK_FARBCODENR" "~"
if joinKuerzel = FARDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_FARBCODETABNR = FAR_FARBCODETABNR AND DOK_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = EHTFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = EHT_FARBCODETABNR AND FAR_FARBCODENR = EHT_FARBCODENR" "~"
if joinKuerzel = FAREHT
searchResult := searchResult "LEFT JOIN WWS.EHT_EINHEITENTAB ON EHT_FARBCODETABNR = FAR_FARBCODETABNR AND EHT_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = ERPFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = ERP_FARBCODETABNR AND FAR_FARBCODENR = ERP_FARBCODENR" "~"
if joinKuerzel = FARERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_FARBCODETABNR = FAR_FARBCODETABNR AND ERP_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = ERTFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = ERT_FARBCODETABNR AND FAR_FARBCODENR = ERT_FARBCODENR" "~"
if joinKuerzel = FARERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FARBCODETABNR = FAR_FARBCODETABNR AND ERT_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = GROFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = GRO_FARBCODETABNR AND FAR_FARBCODENR = GRO_FARBCODENR" "~"
if joinKuerzel = FARGRO
searchResult := searchResult "LEFT JOIN WWS.GRO_GROESSE ON GRO_FARBCODETABNR = FAR_FARBCODETABNR AND GRO_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = LAGFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = LAG_FARBCODETABNR AND FAR_FARBCODENR = LAG_FARBCODENR" "~"
if joinKuerzel = FARLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FARBCODETABNR = FAR_FARBCODETABNR AND LAG_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = PETFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = PET_FARBCODETABNR AND FAR_FARBCODENR = PET_FARBCODENR" "~"
if joinKuerzel = FARPET
searchResult := searchResult "LEFT JOIN WWS.PET_PREISTAB ON PET_FARBCODETABNR = FAR_FARBCODETABNR AND PET_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = RNPFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = RNP_FARBCODETABNR AND FAR_FARBCODENR = RNP_FARBCODENR" "~"
if joinKuerzel = FARRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FARBCODETABNR = FAR_FARBCODETABNR AND RNP_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = RNRFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = RNR_FARBCODETABNR AND FAR_FARBCODENR = RNR_FARBCODENR" "~"
if joinKuerzel = FARRNR
searchResult := searchResult "LEFT JOIN WWS.RNR_RECHNUNGSRABATT ON RNR_FARBCODETABNR = FAR_FARBCODETABNR AND RNR_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = SRTFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = SRT_FARBCODETABNR AND FAR_FARBCODENR = SRT_FARBCODENR" "~"
if joinKuerzel = FARSRT
searchResult := searchResult "LEFT JOIN WWS.SRT_STEUERTAB ON SRT_FARBCODETABNR = FAR_FARBCODETABNR AND SRT_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = STYFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = STY_FARBCODETABNR AND FAR_FARBCODENR = STY_FARBCODENR" "~"
if joinKuerzel = FARSTY
searchResult := searchResult "LEFT JOIN ONVIS.STY_STOFFTYPEN ON STY_FARBCODETABNR = FAR_FARBCODETABNR AND STY_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = UESFAR
searchResult := searchResult "LEFT JOIN BMD.FAR_FARBCODE ON FAR_FARBCODETABNR = UES_FARBCODETABNR AND FAR_FARBCODENR = UES_FARBCODENR" "~"
if joinKuerzel = FARUES
searchResult := searchResult "LEFT JOIN BMD.UES_UEBERSETZUNG ON UES_FARBCODETABNR = FAR_FARBCODETABNR AND UES_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = SPLFBF
searchResult := searchResult "LEFT JOIN BMD.FBF_FELDBESCHFORMAT ON FBF_FORMATNR = SPL_FORMATNR" "~"
if joinKuerzel = FBFSPL
searchResult := searchResult "LEFT JOIN BMD.SPL_STEMPEL ON SPL_FORMATNR = FBF_FORMATNR" "~"
if joinKuerzel = FDBFDH
searchResult := searchResult "LEFT JOIN FIBU.FDH_FIBUDASHBOARDKOPF ON FDH_FDHID = FDB_FDHID" "~"
if joinKuerzel = FDHFDB
searchResult := searchResult "LEFT JOIN FIBU.FDB_FIBUDASHBOARD ON FDB_FDHID = FDH_FDHID" "~"
if joinKuerzel = FSTFES
searchResult := searchResult "LEFT JOIN BUERO.FES_FRISTERLEDIGTSTATUS ON FES_FESNR = FST_FES_FESNR AND FES_FIRMENNR = FST_FES_FIRMENNR" "~"
if joinKuerzel = FESFST
searchResult := searchResult "LEFT JOIN BUERO.FST_FRIST ON FST_FES_FESNR = FES_FESNR AND FST_FES_FIRMENNR = FES_FIRMENNR" "~"
if joinKuerzel = LZSFGK
searchResult := searchResult "LEFT JOIN FGK_FIRMENGKKZU.FGK_FIRMENGKKZU ON FGK_FIRMENNR = LZS_DVFIRMENNR AND FGK_FIRMAGUELTIGJAHR = LZS_BZJJ AND FGK_GKKLFDNR = LZS_GKKLFDNR" "~"
if joinKuerzel = FGKLZS
searchResult := searchResult "LEFT JOIN HRS.LZS_LOHNZETTEL_SV ON LZS_DVFIRMENNR = FGK_FIRMENNR AND LZS_BZJJ = FGK_FIRMAGUELTIGJAHR AND LZS_GKKLFDNR = FGK_GKKLFDNR" "~"
if joinKuerzel = SKEFIF
searchResult := searchResult "LEFT JOIN FIF_FILIALEFIBU.FIF_FILIALEFIBU ON FIF_FIRMENNR = SKE_FIRMENNR_FIBU AND FIF_FIBUNR = SKE_FIBUNR AND FIF_FILIALNR = SKE_FILIALNR" "~"
if joinKuerzel = FIFSKE
searchResult := searchResult "LEFT JOIN BIL.SKE_STEUERKUNDENZUERW ON SKE_FIRMENNR_FIBU = FIF_FIRMENNR AND SKE_FIBUNR = FIF_FIBUNR AND SKE_FILIALNR = FIF_FILIALNR" "~"
if joinKuerzel = BUCFIF
searchResult := searchResult "LEFT JOIN FIBU.FIF_FILIALEFIBU ON FIF_FIRMENNR = BUC_FIRMENNR AND FIF_FIBUNR = BUC_FIBUNR AND FIF_FILIALNR = BUC_FIF_FILIALNR" "~"
if joinKuerzel = FIFBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_FIRMENNR = FIF_FIRMENNR AND BUC_FIBUNR = FIF_FIBUNR AND BUC_FIF_FILIALNR = FIF_FILIALNR" "~"
if joinKuerzel = FIXFIF
searchResult := searchResult "LEFT JOIN FIBU.FIF_FILIALEFIBU ON FIF_FIRMENNR = FIX_FIRMENNR AND FIF_FIBUNR = FIX_FIBUNR AND FIF_FILIALNR = FIX_FILIALNR" "~"
if joinKuerzel = FIFFIX
searchResult := searchResult "LEFT JOIN FIBU.FIX_FIXKONTEN ON FIX_FIRMENNR = FIF_FIRMENNR AND FIX_FIBUNR = FIF_FIBUNR AND FIX_FILIALNR = FIF_FILIALNR" "~"
if joinKuerzel = SADFIF
searchResult := searchResult "LEFT JOIN FIBU.FIF_FILIALEFIBU ON FIF_FIRMENNR = SAD_FIRMENNR AND FIF_FIBUNR = SAD_FIBUNR AND FIF_FILIALNR = SAD_FILIALNR" "~"
if joinKuerzel = FIFSAD
searchResult := searchResult "LEFT JOIN FIBU.SAD_SALDEN ON SAD_FIRMENNR = FIF_FIRMENNR AND SAD_FIBUNR = FIF_FIBUNR AND SAD_FILIALNR = FIF_FILIALNR" "~"
if joinKuerzel = UVWFIF
searchResult := searchResult "LEFT JOIN FIBU.FIF_FILIALEFIBU ON FIF_FIRMENNR = UVW_FIRMENNR AND FIF_FIBUNR = UVW_FIBUNR AND FIF_FILIALNR = UVW_FIF_FILIALNR" "~"
if joinKuerzel = FIFUVW
searchResult := searchResult "LEFT JOIN FIBU.UVW_UVAWERT ON UVW_FIRMENNR = FIF_FIRMENNR AND UVW_FIBUNR = FIF_FIBUNR AND UVW_FIF_FILIALNR = FIF_FILIALNR" "~"
if joinKuerzel = PFAFIN
searchResult := searchResult "LEFT JOIN BUERO.FIN_FINANZAMT ON FIN_LANDNR = PFA_LANDNR AND FIN_FINANZAMTNR = PFA_FINANZAMTNR" "~"
if joinKuerzel = FINPFA
searchResult := searchResult "LEFT JOIN BUERO.PFA_PERSONFINANZAMTZU ON PFA_LANDNR = FIN_LANDNR AND PFA_FINANZAMTNR = FIN_FINANZAMTNR" "~"
if joinKuerzel = QAWFIN
searchResult := searchResult "LEFT JOIN BUERO.FIN_FINANZAMT ON FIN_LANDNR = QAW_LANDNR AND FIN_FINANZAMTNR = QAW_FINANZAMTNR" "~"
if joinKuerzel = FINQAW
searchResult := searchResult "LEFT JOIN BUERO.QAW_QUOTENAUSWERTUNG ON QAW_LANDNR = FIN_LANDNR AND QAW_FINANZAMTNR = FIN_FINANZAMTNR" "~"
if joinKuerzel = ADEFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ADE_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRADE
searchResult := searchResult "LEFT JOIN WWS.ADE_ABGABENDETAILERFPOS ON ADE_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AFTFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AFT_FIRMENNR" "~"
if joinKuerzel = FIRAFT
searchResult := searchResult "LEFT JOIN ZLE.AFT_AUFBEREITUNG ON AFT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ARLFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ARL_FIRMENNR" "~"
if joinKuerzel = FIRARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ARRFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ARR_FIRMENNR" "~"
if joinKuerzel = FIRARR
searchResult := searchResult "LEFT JOIN PRODUKTION.ARR_PROD_ARTIKEL_REPORT ON ARR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ATFFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = ATF_FIRMENNR" "~"
if joinKuerzel = FIRATF
searchResult := searchResult "LEFT JOIN BUERO.ATF_ABTEILUNGFIRMENZU ON ATF_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AVIFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AVI_FIRMENNR" "~"
if joinKuerzel = FIRAVI
searchResult := searchResult "LEFT JOIN HRS.AVI_AVISOMELDUNG ON AVI_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BSPFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = BSP_FIRMENNR" "~"
if joinKuerzel = FIRBSP
searchResult := searchResult "LEFT JOIN WWS.BSP_BONUSERMPOS ON BSP_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BWFFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = BWF_FIRMENNR" "~"
if joinKuerzel = FIRBWF
searchResult := searchResult "LEFT JOIN BUERO.BWF_BEWERTUNGFORMEL ON BWF_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = DASFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = DAS_FIRMENNR" "~"
if joinKuerzel = FIRDAS
searchResult := searchResult "LEFT JOIN BUERO.DAS_DOCSARCHIVSTAMM ON DAS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = DSSFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = DSS_FIRMENNR" "~"
if joinKuerzel = FIRDSS
searchResult := searchResult "LEFT JOIN BUERO.DSS_DSGVO_STANDARD ON DSS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = EFKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = EFK_HAUPTFIRMENNR" "~"
if joinKuerzel = FIREFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FBKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = FBK_FIRMENNR" "~"
if joinKuerzel = FIRFBK
searchResult := searchResult "LEFT JOIN BMD.FBK_FELDBESCHKOPF ON FBK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FBRFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = FBR_FIRMENNR" "~"
if joinKuerzel = FIRFBR
searchResult := searchResult "LEFT JOIN BUERO.FBR_FIRMENBRANCHEZU ON FBR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FFRFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = FFR_FIRMENNR" "~"
if joinKuerzel = FIRFFR
searchResult := searchResult "LEFT JOIN BMD.FFR_FUNKFIRMENRECHTE ON FFR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FOTFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = FOT_FIRMENNR" "~"
if joinKuerzel = FIRFOT
searchResult := searchResult "LEFT JOIN BUERO.FOT_FOTO ON FOT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FPZFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = FPZ_FIRMENNR" "~"
if joinKuerzel = FIRFPZ
searchResult := searchResult "LEFT JOIN HRS.FPZ_FIRMENPENSIONSKASSEZU ON FPZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FTBFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = FTB_FIRMENNR" "~"
if joinKuerzel = FIRFTB
searchResult := searchResult "LEFT JOIN BUERO.FTB_FAHRTENBUCH ON FTB_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = GEKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = GEK_FIRMENNR" "~"
if joinKuerzel = FIRGEK
searchResult := searchResult "LEFT JOIN WWS.GEK_GEFAHRENGUTKOPF ON GEK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = GSAFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = GSA_FIR_FILIALNR" "~"
if joinKuerzel = FIRGSA
searchResult := searchResult "LEFT JOIN WWS.GSA_GUTSCHEINAKTION ON GSA_FIR_FILIALNR = FIR_FIRMENNR" "~"
if joinKuerzel = ABUFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ABU_FIRMENNR" "~"
if joinKuerzel = FIRABU
searchResult := searchResult "LEFT JOIN BIL.ABU_ANLAGENBUCHHALTUNG ON ABU_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ADKFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = ADK_FIRMENNR" "~"
if joinKuerzel = FIRADK
searchResult := searchResult "LEFT JOIN BUERO.ADK_ADRESSENLISTEKOPF ON ADK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ADOFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = ADO_FIRMENNR" "~"
if joinKuerzel = FIRADO
searchResult := searchResult "LEFT JOIN BUERO.ADO_AUSDRUCKMODELL ON ADO_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AGNFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AGN_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRAGN
searchResult := searchResult "LEFT JOIN WWS.AGN_AG_PERSON_ZU ON AGN_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AKLFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AKL_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRAKL
searchResult := searchResult "LEFT JOIN WWS.AKL_ARTIKELKLASSIFIKATION ON AKL_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AZBFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = AZB_FIRMENNR" "~"
if joinKuerzel = FIRAZB
searchResult := searchResult "LEFT JOIN BUERO.AZB_ARBEITSZEITBEZ ON AZB_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BEWFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = BEW_FIRMENNR" "~"
if joinKuerzel = FIRBEW
searchResult := searchResult "LEFT JOIN BUERO.BEW_BEWERTUNGSMODELL ON BEW_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BSTFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = BST_FIRMENNR" "~"
if joinKuerzel = FIRBST
searchResult := searchResult "LEFT JOIN BUERO.BST_SONDERTAGE ON BST_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BTMFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = BTM_FIRMENNR" "~"
if joinKuerzel = FIRBTM
searchResult := searchResult "LEFT JOIN BUERO.BTM_BTERMINAL ON BTM_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = DSTFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = DST_FIRMENNR" "~"
if joinKuerzel = FIRDST
searchResult := searchResult "LEFT JOIN HRS.DST_DIVSTAMM ON DST_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = DSVFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = DSV_FIRMENNR" "~"
if joinKuerzel = FIRDSV
searchResult := searchResult "LEFT JOIN BUERO.DSV_DSGVO_VERZEICHNIS ON DSV_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = EDZFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = EDZ_FIRMENNR" "~"
if joinKuerzel = FIREDZ
searchResult := searchResult "LEFT JOIN WWS.EDZ_ERFASSDOKUMENTZU ON EDZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ERAFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ERA_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRERA
searchResult := searchResult "LEFT JOIN WWS.ERA_ERFASSRABATT ON ERA_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = EVSFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = EVS_FIRMENNR" "~"
if joinKuerzel = FIREVS
searchResult := searchResult "LEFT JOIN BUERO.EVS_EVENTLNSTATUS ON EVS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = EVTFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = EVT_FIRMENNR" "~"
if joinKuerzel = FIREVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FAGFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = FAG_FIRMENNR" "~"
if joinKuerzel = FIRFAG
searchResult := searchResult "LEFT JOIN HRS.FAG_FINANZAMTGHOST ON FAG_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FIGFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = FIG_GRUPPE_FIRMENNR" "~"
if joinKuerzel = FIRFIG
searchResult := searchResult "LEFT JOIN BUERO.FIG_FIRMENGRUPPENZU ON FIG_GRUPPE_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FWOFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = FWO_FIRMENNR" "~"
if joinKuerzel = FIRFWO
searchResult := searchResult "LEFT JOIN ONVIS.FWO_FIRMENWERTEONVIS ON FWO_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = GEPFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = GEP_FIRMENNR" "~"
if joinKuerzel = FIRGEP
searchResult := searchResult "LEFT JOIN WWS.GEP_GEFAHRENGUTPOS ON GEP_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = HZLFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = HZL_FIRMENNR" "~"
if joinKuerzel = FIRHZL
searchResult := searchResult "LEFT JOIN BUERO.HZL_HAUPTZIEL ON HZL_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = IAKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = IAK_FIRMENNR" "~"
if joinKuerzel = FIRIAK
searchResult := searchResult "LEFT JOIN WWS.IAK_INVABWTKOPF ON IAK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = IDPFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = IDP_FIRMENNR" "~"
if joinKuerzel = FIRIDP
searchResult := searchResult "LEFT JOIN PRODUKTION.IDP_INFO_DELAY_BREAKDOWN ON IDP_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = IMMFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = IMM_FIRMENNR" "~"
if joinKuerzel = FIRIMM
searchResult := searchResult "LEFT JOIN BUERO.IMM_INSERATMEDIUMMASTER ON IMM_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = IMNFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = IMN_FIRMENNR" "~"
if joinKuerzel = FIRIMN
searchResult := searchResult "LEFT JOIN BUERO.IMN_INSTANDHMASSN ON IMN_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = KTAFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = KTA_FIRMENNR" "~"
if joinKuerzel = FIRKTA
searchResult := searchResult "LEFT JOIN WWS.KTA_KOSTENTYPENAUFTLG ON KTA_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = LARFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LAR_FIR_FIRMENNR" "~"
if joinKuerzel = FIRLAR
searchResult := searchResult "LEFT JOIN HRS.LAR_LOHNARTENREGEL ON LAR_FIR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PCKFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = PCK_FARBCODETABNR AND FAR_FARBCODENR = PCK_FARBCODENR" "~"
if joinKuerzel = FARPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_FARBCODETABNR = FAR_FARBCODETABNR AND PCK_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = PLCFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = PLC_FARBCODETABNR AND FAR_FARBCODENR = PLC_FARBCODENR" "~"
if joinKuerzel = FARPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_FARBCODETABNR = FAR_FARBCODETABNR AND PLC_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = POSFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = POS_FARBCODETABNR AND FAR_FARBCODENR = POS_FARBCODENR" "~"
if joinKuerzel = FARPOS
searchResult := searchResult "LEFT JOIN BUERO.POS_POST ON POS_FARBCODETABNR = FAR_FARBCODETABNR AND POS_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = PROFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = PRO_FARBCODETABNR AND FAR_FARBCODENR = PRO_FARBCODENR" "~"
if joinKuerzel = FARPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FARBCODETABNR = FAR_FARBCODETABNR AND PRO_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = VASFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = VAS_FARBCODETABNR AND FAR_FARBCODENR = VAS_FARBCODENR" "~"
if joinKuerzel = FARVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_FARBCODETABNR = FAR_FARBCODETABNR AND VAS_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = VPHFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = VPH_FARBCODETABNR AND FAR_FARBCODENR = VPH_FARBCODENR" "~"
if joinKuerzel = FARVPH
searchResult := searchResult "LEFT JOIN WWS.VPH_VPERMITTLUNG_FARZUORD ON VPH_FARBCODETABNR = FAR_FARBCODETABNR AND VPH_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = WAGFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = WAG_FARBCODETABNR AND FAR_FARBCODENR = WAG_FARBCODENR" "~"
if joinKuerzel = FARWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FARBCODETABNR = FAR_FARBCODETABNR AND WAG_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = ARLFAT
searchResult := searchResult "LEFT JOIN WWS.FAT_FARBENTAB ON FAT_FIRMENNR = ARL_FIRMENNR AND FAT_FARBENTABNR = ARL_FARBENTABNR" "~"
if joinKuerzel = FATARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = FAT_FIRMENNR AND ARL_FARBENTABNR = FAT_FARBENTABNR" "~"
if joinKuerzel = WAGFAT
searchResult := searchResult "LEFT JOIN WWS.FAT_FARBENTAB ON FAT_FIRMENNR = WAG_FIRMENNR AND FAT_FARBENTABNR = WAG_FARBENTABNR" "~"
if joinKuerzel = FATWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = FAT_FIRMENNR AND WAG_FARBENTABNR = FAT_FARBENTABNR" "~"
if joinKuerzel = DFFFBF
searchResult := searchResult "LEFT JOIN FBF_FELDBESCHFORMAT.FBF_FELDBESCHFORMAT ON FBF_FORMATNR = DFF_FORMATNR" "~"
if joinKuerzel = FBFDFF
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFF_DFELDBESCHFORMAT ON DFF_FORMATNR = FBF_FORMATNR" "~"
if joinKuerzel = SCKFBF
searchResult := searchResult "LEFT JOIN FBF_FELDBESCHFORMAT.FBF_FELDBESCHFORMAT ON FBF_FORMATNR = SCK_ERFASSUNGMASKE" "~"
if joinKuerzel = FBFSCK
searchResult := searchResult "LEFT JOIN PRODUKTION.SCK_SCKENNZAHL ON SCK_ERFASSUNGMASKE = FBF_FORMATNR" "~"
if joinKuerzel = FEHFEA
searchResult := searchResult "LEFT JOIN WWS.FEA_FERTEP_AUFTEILUNG ON FEA_FEAID = FEH_FEAID" "~"
if joinKuerzel = FEAFEH
searchResult := searchResult "LEFT JOIN WWS.FEH_FERTEP_HISTORIE ON FEH_FEAID = FEA_FEAID" "~"
if joinKuerzel = ABDFFA
searchResult := searchResult "LEFT JOIN FFA_FIRMENFINANZAMTZU.FFA_FIRMENFINANZAMTZU ON FFA_FIRMENNR = ABD_DVFIRMENNR AND FFA_FIRMAGUELTIGJAHR = ABD_JAHR AND FFA_FFINANZAMTLFDNR = ABD_FFINANZAMTLFDNR" "~"
if joinKuerzel = FFAABD
searchResult := searchResult "LEFT JOIN HRS.ABD_ABRECHNUNGSDETAIL ON ABD_DVFIRMENNR = FFA_FIRMENNR AND ABD_JAHR = FFA_FIRMAGUELTIGJAHR AND ABD_FFINANZAMTLFDNR = FFA_FFINANZAMTLFDNR" "~"
if joinKuerzel = SUDFFA
searchResult := searchResult "LEFT JOIN FFA_FIRMENFINANZAMTZU.FFA_FIRMENFINANZAMTZU ON FFA_FIRMENNR = SUD_FIRMENNR AND FFA_FIRMAGUELTIGJAHR = SUD_JAHR AND FFA_FFINANZAMTLFDNR = SUD_FINANZAMTLFDNR" "~"
if joinKuerzel = FFASUD
searchResult := searchResult "LEFT JOIN HRS.SUD_SUM_KOE_DIFF ON SUD_FIRMENNR = FFA_FIRMENNR AND SUD_JAHR = FFA_FIRMAGUELTIGJAHR AND SUD_FINANZAMTLFDNR = FFA_FFINANZAMTLFDNR" "~"
if joinKuerzel = FFZFGE
searchResult := searchResult "LEFT JOIN BUERO.FGE_FRAGE ON FGE_FRAGEID = FFZ_FRAGEID" "~"
if joinKuerzel = FGEFFZ
searchResult := searchResult "LEFT JOIN BUERO.FFZ_FRAGEBOGENFRAGEZU ON FFZ_FRAGEID = FGE_FRAGEID" "~"
if joinKuerzel = SUDFGK
searchResult := searchResult "LEFT JOIN FGK_FIRMENGKKZU.FGK_FIRMENGKKZU ON FGK_FIRMENNR = SUD_FIRMENNR AND FGK_FIRMAGUELTIGJAHR = SUD_JAHR AND FGK_GKKLFDNR = SUD_GKKLFDNR" "~"
if joinKuerzel = FGKSUD
searchResult := searchResult "LEFT JOIN HRS.SUD_SUM_KOE_DIFF ON SUD_FIRMENNR = FGK_FIRMENNR AND SUD_JAHR = FGK_FIRMAGUELTIGJAHR AND SUD_GKKLFDNR = FGK_GKKLFDNR" "~"
if joinKuerzel = BRPFGM
searchResult := searchResult "LEFT JOIN FGM_FIRMENGEMEINDEZU.FGM_FIRMENGEMEINDEZU ON FGM_FIRMENNR = BRP_FIRMENNR AND FGM_FIRMAGUELTIGJAHR = BRP_LFI_LOHNJAHR AND FGM_FGEMEINDELFDNR = BRP_FGEMEINDELFDNR" "~"
if joinKuerzel = FGMBRP
searchResult := searchResult "LEFT JOIN HRS.BRP_BRUTTOERFPROTOKOLL ON BRP_FIRMENNR = FGM_FIRMENNR AND BRP_LFI_LOHNJAHR = FGM_FIRMAGUELTIGJAHR AND BRP_FGEMEINDELFDNR = FGM_FGEMEINDELFDNR" "~"
if joinKuerzel = FIZFIF
searchResult := searchResult "LEFT JOIN FIBU.FIF_FILIALEFIBU ON FIF_FIRMENNR = FIZ_ZUGEORDNETE_FIRMENNR AND FIF_FIBUNR = FIZ_ZUGEORDNETE_FIBUNR AND FIF_FILIALNR = FIZ_ZUGEORDNETE_FILIALNR" "~"
if joinKuerzel = FIFFIZ
searchResult := searchResult "LEFT JOIN FIBU.FIZ_FILIALEKONSZU ON FIZ_ZUGEORDNETE_FIRMENNR = FIF_FIRMENNR AND FIZ_ZUGEORDNETE_FIBUNR = FIF_FIBUNR AND FIZ_ZUGEORDNETE_FILIALNR = FIF_FILIALNR" "~"
if joinKuerzel = AAKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AAK_FIRMENNR" "~"
if joinKuerzel = FIRAAK
searchResult := searchResult "LEFT JOIN WWS.AAK_ARAKOPF ON AAK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AFKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AFK_FIRMENNR" "~"
if joinKuerzel = FIRAFK
searchResult := searchResult "LEFT JOIN ZLE.AFK_EXTFILTERKOPF ON AFK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BWBFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = BWB_FILIAL_FIRMENNR" "~"
if joinKuerzel = FIRBWB
searchResult := searchResult "LEFT JOIN BUERO.BWB_BEWERBUNG ON BWB_FILIAL_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BWUFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = BWU_FIRMENNR" "~"
if joinKuerzel = FIRBWU
searchResult := searchResult "LEFT JOIN BUERO.BWU_BEWERTUNGUSERFIELD ON BWU_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BZBFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = BZB_FIRMENNR" "~"
if joinKuerzel = FIRBZB
searchResult := searchResult "LEFT JOIN PRODUKTION.BZB_BAZZEITBUCHUNG ON BZB_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = EAEFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = EAE_DVFIRMENNR" "~"
if joinKuerzel = FIREAE
searchResult := searchResult "LEFT JOIN HRS.EAE_ELDADETAIL_AEK ON EAE_DVFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = EAWFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = EAW_DVFIRMENNR" "~"
if joinKuerzel = FIREAW
searchResult := searchResult "LEFT JOIN HRS.EAW_ELDADETAIL_AEW ON EAW_DVFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FAKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = FAK_FIRMENNR" "~"
if joinKuerzel = FIRFAK
searchResult := searchResult "LEFT JOIN HRS.FAK_FAKTORTABELLE ON FAK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FTGFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = FTG_FIRMENNR" "~"
if joinKuerzel = FIRFTG
searchResult := searchResult "LEFT JOIN BUERO.FTG_FEIERTAG ON FTG_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FWKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = FWK_FIRMENNR" "~"
if joinKuerzel = FIRFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = GWZFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = GWZ_FIRMENNR" "~"
if joinKuerzel = FIRGWZ
searchResult := searchResult "LEFT JOIN HRS.GWZ_GWBANKZU ON GWZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = HRWFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = HRW_FIRMENNR" "~"
if joinKuerzel = FIRHRW
searchResult := searchResult "LEFT JOIN HRS.HRW_WORKFLOW ON HRW_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ICDFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = ICD_FIRMENNR" "~"
if joinKuerzel = FIRICD
searchResult := searchResult "LEFT JOIN BUERO.ICD_ICCODES ON ICD_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = IFPFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = IFP_FIRMENNR" "~"
if joinKuerzel = FIRIFP
searchResult := searchResult "LEFT JOIN BUERO.IFP_INFOTABPOS ON IFP_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = KODFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = KOD_FIRMENNR" "~"
if joinKuerzel = FIRKOD
searchResult := searchResult "LEFT JOIN BUERO.KOD_KONDITIONEN ON KOD_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = LAEFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LAE_FIRMENNR" "~"
if joinKuerzel = FIRLAE
searchResult := searchResult "LEFT JOIN HRS.LAE_LOHNARTEINSTELLUNG ON LAE_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = LAWFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LAW_FIRMENNR" "~"
if joinKuerzel = FIRLAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = LTKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LTK_ERSTELL_FIRMENNR" "~"
if joinKuerzel = FIRLTK
searchResult := searchResult "LEFT JOIN ZLE.LTK_LEISTTAUSCHKOPF ON LTK_ERSTELL_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = MLTFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = MLT_FIRMENNR" "~"
if joinKuerzel = FIRMLT
searchResult := searchResult "LEFT JOIN ZLE.MLT_MITARBLEISTTAUSCH ON MLT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PKMFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PKM_FIRMENNR" "~"
if joinKuerzel = FIRPKM
searchResult := searchResult "LEFT JOIN PRODUKTION.PKM_KM_MASTER ON PKM_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PLCFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PLC_FIRMENNR" "~"
if joinKuerzel = FIRPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PTPFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PTP_PREISGRUPPEN_FIRMENNR" "~"
if joinKuerzel = FIRPTP
searchResult := searchResult "LEFT JOIN WWS.PTP_PREISTABPOS ON PTP_PREISGRUPPEN_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RADFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = RAD_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRRAD
searchResult := searchResult "LEFT JOIN WWS.RAD_RABATTBETRAGDETAIL ON RAD_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RMDFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = RMD_FIRMENNR" "~"
if joinKuerzel = FIRRMD
searchResult := searchResult "LEFT JOIN BUERO.RMD_RUNDUNGSMODELLDET ON RMD_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RPZFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = RPZ_FIRMENNR" "~"
if joinKuerzel = FIRRPZ
searchResult := searchResult "LEFT JOIN ZLE.RPZ_RECHPOSZU ON RPZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RSOFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = RSO_FIRMENNR" "~"
if joinKuerzel = FIRRSO
searchResult := searchResult "LEFT JOIN HRS.RSO_REPORTSORTIERUNG ON RSO_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RZZFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = RZZ_FIRMENNR" "~"
if joinKuerzel = FIRRZZ
searchResult := searchResult "LEFT JOIN BUERO.RZZ_AZGARGZEITEN ON RZZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SBSFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = SBS_FIRMENNR" "~"
if joinKuerzel = FIRSBS
searchResult := searchResult "LEFT JOIN BUERO.SBS_STELLENBESCHREIBUNG ON SBS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = VPEFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = VPE_FARBCODETABNR AND FAR_FARBCODENR = VPE_FARBCODENR" "~"
if joinKuerzel = FARVPE
searchResult := searchResult "LEFT JOIN WWS.VPE_VPI_ERHOEHUNG ON VPE_FARBCODETABNR = FAR_FARBCODETABNR AND VPE_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = SCKFBF
searchResult := searchResult "LEFT JOIN FBF_FELDBESCHFORMAT.FBF_FELDBESCHFORMAT ON FBF_FORMATNR = SCK_DRUCKMASKE" "~"
if joinKuerzel = FBFSCK
searchResult := searchResult "LEFT JOIN PRODUKTION.SCK_SCKENNZAHL ON SCK_DRUCKMASKE = FBF_FORMATNR" "~"
if joinKuerzel = FGEFBN
searchResult := searchResult "LEFT JOIN BUERO.FBN_FRAGEBOGEN ON FBN_FRAGEBOGENID = FGE_FRAGEBOGENID" "~"
if joinKuerzel = FBNFGE
searchResult := searchResult "LEFT JOIN BUERO.FGE_FRAGE ON FGE_FRAGEBOGENID = FBN_FRAGEBOGENID" "~"
if joinKuerzel = UMFFBN
searchResult := searchResult "LEFT JOIN BUERO.FBN_FRAGEBOGEN ON FBN_FRAGEBOGENID = UMF_FRAGEBOGENID" "~"
if joinKuerzel = FBNUMF
searchResult := searchResult "LEFT JOIN BUERO.UMF_UMFRAGE ON UMF_FRAGEBOGENID = FBN_FRAGEBOGENID" "~"
if joinKuerzel = SUKFGK
searchResult := searchResult "LEFT JOIN FGK_FIRMENGKKZU.FGK_FIRMENGKKZU ON FGK_FIRMENNR = SUK_FIRMENNR AND FGK_FIRMAGUELTIGJAHR = SUK_JAHR AND FGK_GKKLFDNR = SUK_GKKLFDNR" "~"
if joinKuerzel = FGKSUK
searchResult := searchResult "LEFT JOIN HRS.SUK_SUM_KOE ON SUK_FIRMENNR = FGK_FIRMENNR AND SUK_JAHR = FGK_FIRMAGUELTIGJAHR AND SUK_GKKLFDNR = FGK_GKKLFDNR" "~"
if joinKuerzel = KZAFGT
searchResult := searchResult "LEFT JOIN WWS.FGT_FREMDGUTSCHEINTYP ON FGT_FIRMENNR = KZA_FGT_FIRMENNR AND FGT_FREMDGUTSCHEINTYPLFDNR = KZA_FREMDGUTSCHEINTYPLFDNR" "~"
if joinKuerzel = FGTKZA
searchResult := searchResult "LEFT JOIN WWS.KZA_KASSENZAHLUNGSARTENZU ON KZA_FGT_FIRMENNR = FGT_FIRMENNR AND KZA_FREMDGUTSCHEINTYPLFDNR = FGT_FREMDGUTSCHEINTYPLFDNR" "~"
if joinKuerzel = ANBFIF
searchResult := searchResult "LEFT JOIN FIF_FILIALEFIBU.FIF_FILIALEFIBU ON FIF_FIRMENNR = ANB_FIBUFIRMENNR AND FIF_FIBUNR = ANB_FIBUNR AND FIF_FILIALNR = ANB_FILIALNR" "~"
if joinKuerzel = FIFANB
searchResult := searchResult "LEFT JOIN BIL.ANB_ANBUBUCHBELEGZU ON ANB_FIBUFIRMENNR = FIF_FIRMENNR AND ANB_FIBUNR = FIF_FIBUNR AND ANB_FILIALNR = FIF_FILIALNR" "~"
if joinKuerzel = ERKFIF
searchResult := searchResult "LEFT JOIN FIBU.FIF_FILIALEFIBU ON FIF_FIRMENNR = ERK_FIRMENNR AND FIF_FIBUNR = ERK_FIBUNR AND FIF_FILIALNR = ERK_FILIALNR" "~"
if joinKuerzel = FIFERK
searchResult := searchResult "LEFT JOIN FIBU.ERK_KONTO ON ERK_FIRMENNR = FIF_FIRMENNR AND ERK_FIBUNR = FIF_FIBUNR AND ERK_FILIALNR = FIF_FILIALNR" "~"
if joinKuerzel = ZVKFIF
searchResult := searchResult "LEFT JOIN FIBU.FIF_FILIALEFIBU ON FIF_FIRMENNR = ZVK_FIRMENNR AND FIF_FIBUNR = ZVK_FIBUNR AND FIF_FILIALNR = ZVK_FILIALNR" "~"
if joinKuerzel = FIFZVK
searchResult := searchResult "LEFT JOIN FIBU.ZVK_ZAHLUNGSVORSCHLAG ON ZVK_FIRMENNR = FIF_FIRMENNR AND ZVK_FIBUNR = FIF_FIBUNR AND ZVK_FILIALNR = FIF_FILIALNR" "~"
if joinKuerzel = SLZFIN
searchResult := searchResult "LEFT JOIN FIN_FINANZAMT.FIN_FINANZAMT ON FIN_LANDNR = SLZ_AL_LAND_AG AND FIN_FINANZAMTNR = SLZ_AL_FINAMT_AG" "~"
if joinKuerzel = FINSLZ
searchResult := searchResult "LEFT JOIN BIL.SLZ_STEUERLOHNZETTEL ON SLZ_AL_LAND_AG = FIN_LANDNR AND SLZ_AL_FINAMT_AG = FIN_FINANZAMTNR" "~"
if joinKuerzel = AAEFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AAE_FILIALNR" "~"
if joinKuerzel = FIRAAE
searchResult := searchResult "LEFT JOIN WWS.AAE_ANGEBOTSAUSW_ERFQ_TMP ON AAE_FILIALNR = FIR_FIRMENNR" "~"
if joinKuerzel = AASFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = AAS_FIRMENNR" "~"
if joinKuerzel = FIRAAS
searchResult := searchResult "LEFT JOIN BUERO.AAS_ANTWORTAUSWAHLSET ON AAS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AGMFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AGM_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRAGM
searchResult := searchResult "LEFT JOIN PRODUKTION.AGM_AG_PERSON_ZU ON AGM_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AIVFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AIV_FIRMENNR" "~"
if joinKuerzel = FIRAIV
searchResult := searchResult "LEFT JOIN WWS.AIV_INTERVALL_ARBEITSPLAN ON AIV_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ALSFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ALS_FIRMENNR" "~"
if joinKuerzel = FIRALS
searchResult := searchResult "LEFT JOIN WWS.ALS_ALLERGEN ON ALS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ARGFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = ARG_FIRMENNR" "~"
if joinKuerzel = FIRARG
searchResult := searchResult "LEFT JOIN BUERO.ARG_ARBEITSRUHEZEITGESETZ ON ARG_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BAZFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = BAZ_FIRMENNR" "~"
if joinKuerzel = FIRBAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BDLFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = BDL_FIRMENNR" "~"
if joinKuerzel = FIRBDL
searchResult := searchResult "LEFT JOIN BUERO.BDL_BUERODELETE ON BDL_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BFIFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = BFI_FIR_FIRMENNR" "~"
if joinKuerzel = FIRBFI
searchResult := searchResult "LEFT JOIN HRS.BFI_BUDGETFIRMA ON BFI_FIR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = DBXFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = DBX_FIRMENNR" "~"
if joinKuerzel = FIRDBX
searchResult := searchResult "LEFT JOIN BUERO.DBX_FINONDATABOX ON DBX_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = DPOFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = DPO_FIRMENNR" "~"
if joinKuerzel = FIRDPO
searchResult := searchResult "LEFT JOIN BMD.DPO_DOCUMENTPOOL ON DPO_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = DVHFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = DVH_DIENSTV_FIRMENNR" "~"
if joinKuerzel = FIRDVH
searchResult := searchResult "LEFT JOIN BUERO.DVH_DIENSTVERHAELTNIS ON DVH_DIENSTV_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = EEZFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = EEZ_FIRMENNR" "~"
if joinKuerzel = FIREEZ
searchResult := searchResult "LEFT JOIN WWS.EEZ_EDIEINHEITENZU ON EEZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AFTFIN
searchResult := searchResult "LEFT JOIN FIN_FINANZAMT.FIN_FINANZAMT ON FIN_LANDNR = AFT_FIN_LANDNR AND FIN_FINANZAMTNR = AFT_FIN_FINANZAMTNR" "~"
if joinKuerzel = FINAFT
searchResult := searchResult "LEFT JOIN ZLE.AFT_AUFBEREITUNG ON AFT_FIN_LANDNR = FIN_LANDNR AND AFT_FIN_FINANZAMTNR = FIN_FINANZAMTNR" "~"
if joinKuerzel = GMGFIN
searchResult := searchResult "LEFT JOIN FIN_FINANZAMT.FIN_FINANZAMT ON FIN_LANDNR = GMG_FA_LANDNR AND FIN_FINANZAMTNR = GMG_FA_FINANZAMTNR" "~"
if joinKuerzel = FINGMG
searchResult := searchResult "LEFT JOIN BIL.GMG_GRUPPENMITGLIEDER ON GMG_FA_LANDNR = FIN_LANDNR AND GMG_FA_FINANZAMTNR = FIN_FINANZAMTNR" "~"
if joinKuerzel = QUOFIN
searchResult := searchResult "LEFT JOIN BUERO.FIN_FINANZAMT ON FIN_LANDNR = QUO_LANDNR AND FIN_FINANZAMTNR = QUO_FINANZAMTNR" "~"
if joinKuerzel = FINQUO
searchResult := searchResult "LEFT JOIN BUERO.QUO_QUOTE ON QUO_LANDNR = FIN_LANDNR AND QUO_FINANZAMTNR = FIN_FINANZAMTNR" "~"
if joinKuerzel = BKRFIP
searchResult := searchResult "LEFT JOIN FIP_FILTERPANEL.FIP_FILTERPANEL ON FIP_FILTERID = BKR_FIP_FILTERID" "~"
if joinKuerzel = FIPBKR
searchResult := searchResult "LEFT JOIN FIBU.BKR_BANKEINZUGSKREIS ON BKR_FIP_FILTERID = FIP_FILTERID" "~"
if joinKuerzel = MKRFIP
searchResult := searchResult "LEFT JOIN FIP_FILTERPANEL.FIP_FILTERPANEL ON FIP_FILTERID = MKR_FIP_FILTERID" "~"
if joinKuerzel = FIPMKR
searchResult := searchResult "LEFT JOIN FIBU.MKR_MAHNKREIS ON MKR_FIP_FILTERID = FIP_FILTERID" "~"
if joinKuerzel = AADFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AAD_ABU_FIRMENNR" "~"
if joinKuerzel = FIRAAD
searchResult := searchResult "LEFT JOIN BIL.AAD_AUSDRUCKDETAIL ON AAD_ABU_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ADBFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ADB_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRADB
searchResult := searchResult "LEFT JOIN WWS.ADB_ABGABENDETAILBON ON ADB_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AMZFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = AMZ_FIRMENNR" "~"
if joinKuerzel = FIRAMZ
searchResult := searchResult "LEFT JOIN BUERO.AMZ_AUSDRUCKMODELLZU ON AMZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = APAFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = APA_FIRMENNR" "~"
if joinKuerzel = FIRAPA
searchResult := searchResult "LEFT JOIN PRODUKTION.APA_ARBEITSPLAN ON APA_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = APGFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = APG_FIRMENNR" "~"
if joinKuerzel = FIRAPG
searchResult := searchResult "LEFT JOIN WWS.APG_ARAPRODUKTGRUPPEN ON APG_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = APSFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = APS_FIRMENNR" "~"
if joinKuerzel = FIRAPS
searchResult := searchResult "LEFT JOIN WWS.APS_ARBEITSPLAN_STAMM ON APS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ASWFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = ASW_FIRMENNR" "~"
if joinKuerzel = FIRASW
searchResult := searchResult "LEFT JOIN BUERO.ASW_AUSWEIS ON ASW_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ASZFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = ASZ_FIRMENNR" "~"
if joinKuerzel = FIRASZ
searchResult := searchResult "LEFT JOIN BUERO.ASZ_AUSSCHRFILIALEZU ON ASZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AWCFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AWC_FIRMENNR" "~"
if joinKuerzel = FIRAWC
searchResult := searchResult "LEFT JOIN ZLE.AWC_SPALTENAUSWAHL ON AWC_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AZKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AZK_FIRMENNR" "~"
if joinKuerzel = FIRAZK
searchResult := searchResult "LEFT JOIN BIL.AZK_ANBUZUSCHUSSKENNZEICH ON AZK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BAUFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = BAU_FIRMENNR" "~"
if joinKuerzel = FIRBAU
searchResult := searchResult "LEFT JOIN BUERO.BAU_BAUM ON BAU_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BFZFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = BFZ_BETFIRMENNR" "~"
if joinKuerzel = FIRBFZ
searchResult := searchResult "LEFT JOIN BIL.BFZ_XMLJABBETFIRZU ON BFZ_BETFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BLGFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = BLG_FIRMENNR" "~"
if joinKuerzel = FIRBLG
searchResult := searchResult "LEFT JOIN BUERO.BLG_BEZARTLISTGEN ON BLG_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BSDFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = BSD_FIRMENNR" "~"
if joinKuerzel = FIRBSD
searchResult := searchResult "LEFT JOIN WWS.BSD_BONUSERMDETAIL ON BSD_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BVOFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = BVO_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = DAZFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = DAZ_FIRMENNR" "~"
if joinKuerzel = FIRDAZ
searchResult := searchResult "LEFT JOIN DATANORM.DAZ_DATANORM_ARTIKELZU ON DAZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = EADFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = EAD_DVFIRMENNR" "~"
if joinKuerzel = FIREAD
searchResult := searchResult "LEFT JOIN HRS.EAD_ELDADETAIL_AD ON EAD_DVFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = EEFFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = EEF_DVFIRMENNR" "~"
if joinKuerzel = FIREEF
searchResult := searchResult "LEFT JOIN HRS.EEF_ELDADETAIL_EFZ ON EEF_DVFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = EEMFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = EEM_FIRMENNR" "~"
if joinKuerzel = FIREEM
searchResult := searchResult "LEFT JOIN WWS.EEM_EDIEINHEITENMAPPING ON EEM_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ERWFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ERW_FIRMENNR" "~"
if joinKuerzel = FIRERW
searchResult := searchResult "LEFT JOIN FIBU.ERW_WORKFLOW ON ERW_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = HINFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = HIN_FIR_FIRMENNR" "~"
if joinKuerzel = FIRHIN
searchResult := searchResult "LEFT JOIN HRS.HIN_HINWEIS ON HIN_FIR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = HSKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = HSK_FIRMENNR" "~"
if joinKuerzel = FIRHSK
searchResult := searchResult "LEFT JOIN HRS.HSK_SORTIERKOPF ON HSK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = INTFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = INT_FIRMENNR" "~"
if joinKuerzel = FIRINT
searchResult := searchResult "LEFT JOIN BUERO.INT_INTRASTAT ON INT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = KMAFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = KMA_GEHOERT_FIRMENNR" "~"
if joinKuerzel = FIRKMA
searchResult := searchResult "LEFT JOIN WWS.KMA_KUNDENMITARBBEZUG ON KMA_GEHOERT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = KOMFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = KOM_FIRMENNR" "~"
if joinKuerzel = FIRKOM
searchResult := searchResult "LEFT JOIN PRODUKTION.KOM_KOMBI ON KOM_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = KOSFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = KOS_FIRMENNR" "~"
if joinKuerzel = FIRKOS
searchResult := searchResult "LEFT JOIN ZLE.KOS_KOSTENFAKTOR ON KOS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = KPOFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = KPO_FIRMENNR" "~"
if joinKuerzel = FIRKPO
searchResult := searchResult "LEFT JOIN WWS.KPO_KASSENPROVISION ON KPO_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = KSZFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = KSZ_DVFIRMENNR" "~"
if joinKuerzel = FIRKSZ
searchResult := searchResult "LEFT JOIN HRS.KSZ_BBKALKSZ ON KSZ_DVFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = KUGFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = KUG_FIRMENNR" "~"
if joinKuerzel = FIRKUG
searchResult := searchResult "LEFT JOIN BUERO.KUG_KUNDENGRUPPE ON KUG_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = KZAFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = KZA_FILIALNR" "~"
if joinKuerzel = FIRKZA
searchResult := searchResult "LEFT JOIN WWS.KZA_KASSENZAHLUNGSARTENZU ON KZA_FILIALNR = FIR_FIRMENNR" "~"
if joinKuerzel = LFIFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LFI_FIRMENNR" "~"
if joinKuerzel = FIRLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = MVLFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = MVL_FIRMENNR" "~"
if joinKuerzel = FIRMVL
searchResult := searchResult "LEFT JOIN BUERO.MVL_MITARBEITERVORLAGE ON MVL_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PBSFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PBS_FIRMENNR" "~"
if joinKuerzel = FIRPBS
searchResult := searchResult "LEFT JOIN PRODUKTION.PBS_BEFEHL ON PBS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PCPFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PCP_FIRMENNR" "~"
if joinKuerzel = FIRPCP
searchResult := searchResult "LEFT JOIN PRODUKTION.PCP_PPS_PRODUKT ON PCP_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PIMFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = PIM_FIRMENNR" "~"
if joinKuerzel = FIRPIM
searchResult := searchResult "LEFT JOIN BUERO.PIM_PERSONIMPORT ON PIM_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PKSFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PKS_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRPKS
searchResult := searchResult "LEFT JOIN PRODUKTION.PKS_SOLL_KAPAZITAET ON PKS_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PLPFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PLP_FIRMENNR" "~"
if joinKuerzel = FIRPLP
searchResult := searchResult "LEFT JOIN PRODUKTION.PLP_PLANUNGSPERIODE ON PLP_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PSTFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PST_FIRMENNR" "~"
if joinKuerzel = FIRPST
searchResult := searchResult "LEFT JOIN PRODUKTION.PST_STAPEL ON PST_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = QAWFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = QAW_FIRMENNR" "~"
if joinKuerzel = FIRQAW
searchResult := searchResult "LEFT JOIN BUERO.QAW_QUOTENAUSWERTUNG ON QAW_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = QUSFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = QUS_FIRMENNR" "~"
if joinKuerzel = FIRQUS
searchResult := searchResult "LEFT JOIN BUERO.QUS_QUOTENSUMME ON QUS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RAZFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = RAZ_FIRMENNR" "~"
if joinKuerzel = FIRRAZ
searchResult := searchResult "LEFT JOIN BUERO.RAZ_RATENZAHLUNG ON RAZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = REWFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = REW_FIRMENNR" "~"
if joinKuerzel = FIRREW
searchResult := searchResult "LEFT JOIN ZLE.REW_REGELWERK ON REW_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RIAFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = RIA_FIRMENNR" "~"
if joinKuerzel = FIRRIA
searchResult := searchResult "LEFT JOIN PRODUKTION.RIA_RISIKOANALYSE ON RIA_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RPVFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = RPV_FIRMENNR" "~"
if joinKuerzel = FIRRPV
searchResult := searchResult "LEFT JOIN BMD.RPV_REPORTVORSCHLAG ON RPV_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RSBFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = RSB_FIRMENNR" "~"
if joinKuerzel = FIRRSB
searchResult := searchResult "LEFT JOIN BUERO.RSB_RESSOURCENBEDARF ON RSB_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SAPFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = SAP_FIRMENNR" "~"
if joinKuerzel = FIRSAP
searchResult := searchResult "LEFT JOIN BUERO.SAP_SAMMELPROJEKT ON SAP_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = VEBFIF
searchResult := searchResult "LEFT JOIN FIBU.FIF_FILIALEFIBU ON FIF_FIRMENNR = VEB_FIRMENNR AND FIF_FIBUNR = VEB_FIBUNR AND FIF_FILIALNR = VEB_FILIALNR" "~"
if joinKuerzel = FIFVEB
searchResult := searchResult "LEFT JOIN FIBU.VEB_VORERFASSBUCHUNG ON VEB_FIRMENNR = FIF_FIRMENNR AND VEB_FIBUNR = FIF_FIBUNR AND VEB_FILIALNR = FIF_FILIALNR" "~"
if joinKuerzel = FIWFIN
searchResult := searchResult "LEFT JOIN BUERO.FIN_FINANZAMT ON FIN_LANDNR = FIW_BFIN_LANDNR AND FIN_FINANZAMTNR = FIW_BFIN_FINANZAMTNR" "~"
if joinKuerzel = FINFIW
searchResult := searchResult "LEFT JOIN BUERO.FIW_FIRMENWERTE ON FIW_BFIN_LANDNR = FIN_LANDNR AND FIW_BFIN_FINANZAMTNR = FIN_FINANZAMTNR" "~"
if joinKuerzel = PERFIN
searchResult := searchResult "LEFT JOIN BUERO.FIN_FINANZAMT ON FIN_LANDNR = PER_FIN_LANDNR AND FIN_FINANZAMTNR = PER_FINANZAMTNR" "~"
if joinKuerzel = FINPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIN_LANDNR = FIN_LANDNR AND PER_FINANZAMTNR = FIN_FINANZAMTNR" "~"
if joinKuerzel = ZKRFIP
searchResult := searchResult "LEFT JOIN FIP_FILTERPANEL.FIP_FILTERPANEL ON FIP_FILTERID = ZKR_FILTERID" "~"
if joinKuerzel = FIPZKR
searchResult := searchResult "LEFT JOIN FIBU.ZKR_ZAHLUNGSKREIS ON ZKR_FILTERID = FIP_FILTERID" "~"
if joinKuerzel = ACSFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ACS_FIRMENNR" "~"
if joinKuerzel = FIRACS
searchResult := searchResult "LEFT JOIN WWS.ACS_TEST ON ACS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AGRFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = AGR_FIRMENNR" "~"
if joinKuerzel = FIRAGR
searchResult := searchResult "LEFT JOIN BUERO.AGR_ABRECHNUNGSGRUPPE ON AGR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AGSFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AGS_FIRMENNR" "~"
if joinKuerzel = FIRAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ATEFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = ATE_FIRMENNR" "~"
if joinKuerzel = FIRATE
searchResult := searchResult "LEFT JOIN BUERO.ATE_ABTEILUNG ON ATE_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BMIFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = BMI_DVFIRMENNR" "~"
if joinKuerzel = FIRBMI
searchResult := searchResult "LEFT JOIN HRS.BMI_BUDGETMITARBEITER ON BMI_DVFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BOAFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = BOA_FIRMENNR" "~"
if joinKuerzel = FIRBOA
searchResult := searchResult "LEFT JOIN WWS.BOA_BONUSAUSSCHLUSS ON BOA_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BRTFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = BRT_ZEIT_FIRMENNR" "~"
if joinKuerzel = FIRBRT
searchResult := searchResult "LEFT JOIN BUERO.BRT_BERICHT ON BRT_ZEIT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BSGFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = BSG_FIRMENNR" "~"
if joinKuerzel = FIRBSG
searchResult := searchResult "LEFT JOIN BUERO.BSG_BESPRECHUNG ON BSG_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BTGFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = BTG_FIRMENNR" "~"
if joinKuerzel = FIRBTG
searchResult := searchResult "LEFT JOIN BUERO.BTG_BEWERTUNGTAG ON BTG_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BWMFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = BWM_FIRMENNR" "~"
if joinKuerzel = FIRBWM
searchResult := searchResult "LEFT JOIN BUERO.BWM_BEWERTUNGMONAT ON BWM_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = CPRFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = CPR_FIRMENNR" "~"
if joinKuerzel = FIRCPR
searchResult := searchResult "LEFT JOIN BUERO.CPR_CRMPROTOKOLL ON CPR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = DAMFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = DAM_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRDAM
searchResult := searchResult "LEFT JOIN DATANORM.DAM_DATANORMARTIKELMATCH ON DAM_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = DEIFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = DEI_FIRMENNR" "~"
if joinKuerzel = FIRDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = DOVFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = DOV_FIRMENNR" "~"
if joinKuerzel = FIRDOV
searchResult := searchResult "LEFT JOIN BMD.DOV_DOGVORSCHLAG ON DOV_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = EDNFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = EDN_FIRMENNR" "~"
if joinKuerzel = FIREDN
searchResult := searchResult "LEFT JOIN BMD.EDN_EDINACHRICHT ON EDN_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ERDFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ERD_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRERD
searchResult := searchResult "LEFT JOIN WWS.ERD_ERFASSRABATTDETAIL ON ERD_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FBNFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = FBN_FIRMENNR" "~"
if joinKuerzel = FIRFBN
searchResult := searchResult "LEFT JOIN BUERO.FBN_FRAGEBOGEN ON FBN_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = GSFFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = GSF_FILIALE_FIRMENNR" "~"
if joinKuerzel = FIRGSF
searchResult := searchResult "LEFT JOIN WWS.GSF_GUTSCHEINFILIALDATEN ON GSF_FILIALE_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = GSKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = GSK_FIRMENNR" "~"
if joinKuerzel = FIRGSK
searchResult := searchResult "LEFT JOIN WWS.GSK_GUTSCHEINKOPF ON GSK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = HNPFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = HNP_GEHOERT_FIRMENNR" "~"
if joinKuerzel = FIRHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_GEHOERT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = IAPFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = IAP_FIRMENNR" "~"
if joinKuerzel = FIRIAP
searchResult := searchResult "LEFT JOIN WWS.IAP_INVABWTPOS ON IAP_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AKAFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = AKA_FIRMENNR" "~"
if joinKuerzel = FIRAKA
searchResult := searchResult "LEFT JOIN BUERO.AKA_AVKOSTENARTEN ON AKA_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ANDFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AND_FIRMENNR" "~"
if joinKuerzel = FIRAND
searchResult := searchResult "LEFT JOIN BIL.AND_ANBUNUTZUNGSDAUER ON AND_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ANTFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = ANT_FIRMENNR" "~"
if joinKuerzel = FIRANT
searchResult := searchResult "LEFT JOIN BUERO.ANT_ANTWORT ON ANT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BPDFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = BPD_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BRBFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = BRB_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRBRB
searchResult := searchResult "LEFT JOIN BUERO.BRB_BUEROBERECHT ON BRB_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BSKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = BSK_FIRMENNR" "~"
if joinKuerzel = FIRBSK
searchResult := searchResult "LEFT JOIN WWS.BSK_BONUSERMKOPF ON BSK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BTSFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = BTS_FIRMENNR" "~"
if joinKuerzel = FIRBTS
searchResult := searchResult "LEFT JOIN BUERO.BTS_BTERMSATZART ON BTS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BVRFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = BVR_FIRMENNR" "~"
if joinKuerzel = FIRBVR
searchResult := searchResult "LEFT JOIN FIBU.BVR_BANKRVREGELN ON BVR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = CWFFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = CWF_FIRMENNR" "~"
if joinKuerzel = FIRCWF
searchResult := searchResult "LEFT JOIN BUERO.CWF_CRMWORKFLOW ON CWF_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = EGSFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = EGS_FIRMENNR" "~"
if joinKuerzel = FIREGS
searchResult := searchResult "LEFT JOIN BUERO.EGS_ERFASSUNGSGRUND ON EGS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ELZFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ELZ_DVFIRMENNR" "~"
if joinKuerzel = FIRELZ
searchResult := searchResult "LEFT JOIN HRS.ELZ_ELDADETAIL_LZM ON ELZ_DVFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = EPSFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = EPS_FIRMENNR" "~"
if joinKuerzel = FIREPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FIFFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = FIF_INTERNE_FIL_FIRMENNR" "~"
if joinKuerzel = FIRFIF
searchResult := searchResult "LEFT JOIN FIBU.FIF_FILIALEFIBU ON FIF_INTERNE_FIL_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FIWFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = FIW_FIRMENNR" "~"
if joinKuerzel = FIRFIW
searchResult := searchResult "LEFT JOIN BUERO.FIW_FIRMENWERTE ON FIW_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FRTFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = FRT_FIRMENNR" "~"
if joinKuerzel = FIRFRT
searchResult := searchResult "LEFT JOIN ZLE.FRT_FAKTURATYP ON FRT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = GADFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = GAD_FIRMENNR" "~"
if joinKuerzel = FIRGAD
searchResult := searchResult "LEFT JOIN BUERO.GAD_GHOSTADRESSE ON GAD_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = HAZFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = HAZ_FIRMENNR" "~"
if joinKuerzel = FIRHAZ
searchResult := searchResult "LEFT JOIN WWS.HAZ_HAUPTLIEFARTIKELZU ON HAZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = HFTFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = HFT_FIRMENNR" "~"
if joinKuerzel = FIRHFT
searchResult := searchResult "LEFT JOIN BUERO.HFT_HILFSTABELLE ON HFT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = IVKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = IVK_FIRMENNR" "~"
if joinKuerzel = FIRIVK
searchResult := searchResult "LEFT JOIN WWS.IVK_INVENTURKOPF ON IVK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = KJOFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = KJO_FIRMENNR" "~"
if joinKuerzel = FIRKJO
searchResult := searchResult "LEFT JOIN BUERO.KJO_KONTAKTJOURNAL ON KJO_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = KONFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = KON_FIRMENNR" "~"
if joinKuerzel = FIRKON
searchResult := searchResult "LEFT JOIN BUERO.KON_KONTAKTPERSON ON KON_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = LINFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LIN_FIRMENNR" "~"
if joinKuerzel = FIRLIN
searchResult := searchResult "LEFT JOIN PRODUKTION.LIN_PROD_LINIE ON LIN_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = LOKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LOK_FIRMENNR" "~"
if joinKuerzel = FIRLOK
searchResult := searchResult "LEFT JOIN PRODUKTION.LOK_LOGIKTABELLE_KOPF ON LOK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = LXMFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = LXM_FIRMENNR" "~"
if joinKuerzel = FIRLXM
searchResult := searchResult "LEFT JOIN BUERO.LXM_LOHNEXPPROTOKOLL ON LXM_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = MEIFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = MEI_FIRMENNR" "~"
if joinKuerzel = FIRMEI
searchResult := searchResult "LEFT JOIN BUERO.MEI_MEDIENINFO ON MEI_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = MZZFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = MZZ_FIRMENNR" "~"
if joinKuerzel = FIRMZZ
searchResult := searchResult "LEFT JOIN BUERO.MZZ_MITARBZIELZU ON MZZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PKAFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = PKA_FIRMENNR" "~"
if joinKuerzel = FIRPKA
searchResult := searchResult "LEFT JOIN BUERO.PKA_PROJEKTART ON PKA_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SEKFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = SEK_FIRMENNR" "~"
if joinKuerzel = FIRSEK
searchResult := searchResult "LEFT JOIN BUERO.SEK_SEKRETARIAT ON SEK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SLAFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = SLA_FIRMENNR" "~"
if joinKuerzel = FIRSLA
searchResult := searchResult "LEFT JOIN WWS.SLA_STKLISTVARAUSWAHL ON SLA_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = STVFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = STV_FIRMENNR" "~"
if joinKuerzel = FIRSTV
searchResult := searchResult "LEFT JOIN BUERO.STV_STATVERFAHREN ON STV_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SZPFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = SZP_FIRMENNR" "~"
if joinKuerzel = FIRSZP
searchResult := searchResult "LEFT JOIN BUERO.SZP_SRVRECHZENTRUMPROT ON SZP_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = TFKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = TFK_FIRMENNR" "~"
if joinKuerzel = FIRTFK
searchResult := searchResult "LEFT JOIN BMD.TFK_FELDBESCHKOPF_TMP ON TFK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = TFTFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = TFT_FIRMENNR" "~"
if joinKuerzel = FIRTFT
searchResult := searchResult "LEFT JOIN WWS.TFT_TOURFEIERTAG ON TFT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = TLTFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = TLT_FIRMENNR" "~"
if joinKuerzel = FIRTLT
searchResult := searchResult "LEFT JOIN PRODUKTION.TLT_BT_LOGISTIK_TRANSPORT ON TLT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = TPRFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = TPR_FIRMENNR" "~"
if joinKuerzel = FIRTPR
searchResult := searchResult "LEFT JOIN BUERO.TPR_TELEFONPROTOKOLL ON TPR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = VFAFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = VFA_FIRMENNR" "~"
if joinKuerzel = FIRVFA
searchResult := searchResult "LEFT JOIN PRODUKTION.VFA_VERFAHRENSANWEISUNG_KOPF ON VFA_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = VSWFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = VSW_FIRMENNR" "~"
if joinKuerzel = FIRVSW
searchResult := searchResult "LEFT JOIN WWS.VSW_VERTRETERSTATISTIK ON VSW_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = WAFFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = WAF_FIRMENNR" "~"
if joinKuerzel = FIRWAF
searchResult := searchResult "LEFT JOIN WWS.WAF_WWSADRZUFIRMENBEZ ON WAF_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = WAKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = WAK_FIRMENNR" "~"
if joinKuerzel = FIRWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = WBTFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = WBT_FIRMENNR" "~"
if joinKuerzel = FIRWBT
searchResult := searchResult "LEFT JOIN WWS.WBT_WWSBUDGETKOPF ON WBT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = WEIFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = WEI_FIRMENNR" "~"
if joinKuerzel = FIRWEI
searchResult := searchResult "LEFT JOIN WWS.WEI_WARENEINSATZKOPF ON WEI_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ZAWFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = ZAW_FIRMENNR" "~"
if joinKuerzel = FIRZAW
searchResult := searchResult "LEFT JOIN BUERO.ZAW_ZEITAUSWERTUNG ON ZAW_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ZVMFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = ZVM_FIRMENNR" "~"
if joinKuerzel = FIRZVM
searchResult := searchResult "LEFT JOIN BUERO.ZVM_ZEITPLANUNGVERSION ON ZVM_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FIGFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = FIG_FIRMENNR" "~"
if joinKuerzel = FIRFIG
searchResult := searchResult "LEFT JOIN BUERO.FIG_FIRMENGRUPPENZU ON FIG_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PLGFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = PLG_FILIALE_FIRMENNR" "~"
if joinKuerzel = FIRPLG
searchResult := searchResult "LEFT JOIN BUERO.PLG_PLANGRUPPE ON PLG_FILIALE_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = TLPFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = TLP_TRE_FIRMENNR" "~"
if joinKuerzel = FIRTLP
searchResult := searchResult "LEFT JOIN PRODUKTION.TLP_BT_LOGISTIK_POSITION ON TLP_TRE_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = TLTFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = TLT_TRE_FIRMENNR" "~"
if joinKuerzel = FIRTLT
searchResult := searchResult "LEFT JOIN PRODUKTION.TLT_BT_LOGISTIK_TRANSPORT ON TLT_TRE_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ERTFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ERT_FIRMENNR" "~"
if joinKuerzel = FIRERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FATFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = FAT_FIRMENNR" "~"
if joinKuerzel = FIRFAT
searchResult := searchResult "LEFT JOIN WWS.FAT_FARBENTAB ON FAT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = KKKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = KKK_FIRMENNR" "~"
if joinKuerzel = FIRKKK
searchResult := searchResult "LEFT JOIN WWS.KKK_KREDITKARTENKONTENZU ON KKK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PETFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PET_FIRMENNR" "~"
if joinKuerzel = FIRPET
searchResult := searchResult "LEFT JOIN WWS.PET_PREISTAB ON PET_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RATFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = RAT_FIRMENNR" "~"
if joinKuerzel = FIRRAT
searchResult := searchResult "LEFT JOIN WWS.RAT_RABATTTAB ON RAT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RNAFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = RNA_FIRMENNR_ANZAHLUNG" "~"
if joinKuerzel = FIRRNA
searchResult := searchResult "LEFT JOIN WWS.RNA_RECHNUNGANZAHLUNG ON RNA_FIRMENNR_ANZAHLUNG = FIR_FIRMENNR" "~"
if joinKuerzel = FEAFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = FEA_FIRMENNR" "~"
if joinKuerzel = FIRFEA
searchResult := searchResult "LEFT JOIN WWS.FEA_FERTEP_AUFTEILUNG ON FEA_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FKTFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = FKT_FIRMENNR" "~"
if joinKuerzel = FIRFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FWFFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = FWF_FIRMENNR" "~"
if joinKuerzel = FIRFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = GEAFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = GEA_FIRMENNR" "~"
if joinKuerzel = FIRGEA
searchResult := searchResult "LEFT JOIN BUERO.GEA_GESCHAEFTSART ON GEA_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = GZJFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = GZJ_DVFIRMENNR" "~"
if joinKuerzel = FIRGZJ
searchResult := searchResult "LEFT JOIN HRS.GZJ_GLAEUBIGERZU_JAHR ON GZJ_DVFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = HADFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = HAD_FIRMENNR" "~"
if joinKuerzel = FIRHAD
searchResult := searchResult "LEFT JOIN HRS.HAD_AUSDRUCKDETAIL ON HAD_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = IMDFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = IMD_FIRMENNR" "~"
if joinKuerzel = FIRIMD
searchResult := searchResult "LEFT JOIN BUERO.IMD_INSERATMEDIUMDETAIL ON IMD_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = KAIFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = KAI_FIRMENNR" "~"
if joinKuerzel = FIRKAI
searchResult := searchResult "LEFT JOIN WWS.KAI_KNDARTIKELINFO ON KAI_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = LEIFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LEI_GEHOERT_FIRMENNR" "~"
if joinKuerzel = FIRLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_GEHOERT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = MDEFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = MDE_FIRMENNR" "~"
if joinKuerzel = FIRMDE
searchResult := searchResult "LEFT JOIN WWS.MDE_MOBILEDATENERFASSUNG ON MDE_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PCKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PCK_FIR_FIRMENNR" "~"
if joinKuerzel = FIRPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_FIR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PKKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PKK_FIRMENNR" "~"
if joinKuerzel = FIRPKK
searchResult := searchResult "LEFT JOIN BIL.PKK_PERSKORST_PARAMKOPF ON PKK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PNKFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = PNK_FIRMENNR" "~"
if joinKuerzel = FIRPNK
searchResult := searchResult "LEFT JOIN BUERO.PNK_PERSONNUMMERNKREIS ON PNK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PPFFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = PPF_PRUEFUNG_FIRMENNR" "~"
if joinKuerzel = FIRPPF
searchResult := searchResult "LEFT JOIN BUERO.PPF_PERSONENPRUEFUNG ON PPF_PRUEFUNG_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PPPFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PPP_FIRMENNR" "~"
if joinKuerzel = FIRPPP
searchResult := searchResult "LEFT JOIN PRODUKTION.PPP_KAPA_POOL ON PPP_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PZUFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PZU_FIRMENNR" "~"
if joinKuerzel = FIRPZU
searchResult := searchResult "LEFT JOIN WWS.PZU_PREISRABATTZUGRIFF ON PZU_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = REPFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = REP_FIRMENNR" "~"
if joinKuerzel = FIRREP
searchResult := searchResult "LEFT JOIN BUERO.REP_RESSOURCENPAKET ON REP_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RLKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = RLK_FIRMENNR" "~"
if joinKuerzel = FIRRLK
searchResult := searchResult "LEFT JOIN WWS.RLK_RECHNUNGSPLANKOPF ON RLK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RRDFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = RRD_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRRRD
searchResult := searchResult "LEFT JOIN WWS.RRD_RECHNUNGSRABATTDETAIL ON RRD_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RSEFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = RSE_FIRMENNR" "~"
if joinKuerzel = FIRRSE
searchResult := searchResult "LEFT JOIN WWS.RSE_RESERVIERUNGSSYSEXT ON RSE_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = TIPFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = TIP_FIRMENNR" "~"
if joinKuerzel = FIRTIP
searchResult := searchResult "LEFT JOIN BUERO.TIP_TERMINALIMPPROTOKOLL ON TIP_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = TMLFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = TML_FIRMENNR" "~"
if joinKuerzel = FIRTML
searchResult := searchResult "LEFT JOIN HRS.TML_TERMINISIERTELOHNART ON TML_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = VOBFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = VOB_FIRMENNR" "~"
if joinKuerzel = FIRVOB
searchResult := searchResult "LEFT JOIN PRODUKTION.VOB_VORGANGSBENACHR ON VOB_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = VTSFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = VTS_FIRMENNR" "~"
if joinKuerzel = FIRVTS
searchResult := searchResult "LEFT JOIN WWS.VTS_VERTRETERSTUFE ON VTS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = WABFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = WAB_FIRMENNR" "~"
if joinKuerzel = FIRWAB
searchResult := searchResult "LEFT JOIN WWS.WAB_WARENEINGANGBUCH ON WAB_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = WBGFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = WBG_FIRMENNR" "~"
if joinKuerzel = FIRWBG
searchResult := searchResult "LEFT JOIN WWS.WBG_WARENBESCHKOSTENGRP ON WBG_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = LMBFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LMB_FILIALNR" "~"
if joinKuerzel = FIRLMB
searchResult := searchResult "LEFT JOIN WWS.LMB_LAGERMINMAXBESTAND ON LMB_FILIALNR = FIR_FIRMENNR" "~"
if joinKuerzel = LTKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LTK_EMPF_FIRMENNR" "~"
if joinKuerzel = FIRLTK
searchResult := searchResult "LEFT JOIN ZLE.LTK_LEISTTAUSCHKOPF ON LTK_EMPF_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = MDAFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = MDA_FIRMENNR" "~"
if joinKuerzel = FIRMDA
searchResult := searchResult "LEFT JOIN WWS.MDA_MDE_ARTEN ON MDA_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = OBPFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = OBP_FIRMENNR" "~"
if joinKuerzel = FIROBP
searchResult := searchResult "LEFT JOIN BUERO.OBP_ONLBEWPERSSTAMM ON OBP_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PALFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PAL_FIRMENNR" "~"
if joinKuerzel = FIRPAL
searchResult := searchResult "LEFT JOIN ZLE.PAL_PAUSCHALE ON PAL_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PKHFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PKH_FIRMENNR" "~"
if joinKuerzel = FIRPKH
searchResult := searchResult "LEFT JOIN BIL.PKH_PERSKORSTHEADER ON PKH_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PUCFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PUC_ZUORD_FIRMENNR" "~"
if joinKuerzel = FIRPUC
searchResult := searchResult "LEFT JOIN PRODUKTION.PUC_PPSUSERSTATICOMBI ON PUC_ZUORD_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PVFFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = PVF_FIRMENNR" "~"
if joinKuerzel = FIRPVF
searchResult := searchResult "LEFT JOIN BUERO.PVF_PERSONENVORERFASSUNG ON PVF_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RATFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = RAT_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRRAT
searchResult := searchResult "LEFT JOIN WWS.RAT_RABATTTAB ON RAT_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = REHFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = REH_FIRMENNR" "~"
if joinKuerzel = FIRREH
searchResult := searchResult "LEFT JOIN PRODUKTION.REH_REIHE ON REH_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RPRFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = RPR_FIRMENNR" "~"
if joinKuerzel = FIRRPR
searchResult := searchResult "LEFT JOIN WWS.RPR_RECHNUNGSPROVISION ON RPR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RSKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = RSK_FIRMENNR" "~"
if joinKuerzel = FIRRSK
searchResult := searchResult "LEFT JOIN WWS.RSK_REGISTRIERKASSE ON RSK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RZSFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = RZS_FIRMENNR" "~"
if joinKuerzel = FIRRZS
searchResult := searchResult "LEFT JOIN BUERO.RZS_RUECKZAHLUNGSSTAMM ON RZS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SKLFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = SKL_FIRMENNR" "~"
if joinKuerzel = FIRSKL
searchResult := searchResult "LEFT JOIN WWS.SKL_STUECKELUNGSLISTE ON SKL_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SRMFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = SRM_FIRMENNR" "~"
if joinKuerzel = FIRSRM
searchResult := searchResult "LEFT JOIN WWS.SRM_SAMMELRECHNUNGSMODELL ON SRM_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = STTFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = STT_FIRMENNR" "~"
if joinKuerzel = FIRSTT
searchResult := searchResult "LEFT JOIN PRODUKTION.STT_STAKE_TYP ON STT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SWAFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = SWA_DVFIRMENNR" "~"
if joinKuerzel = FIRSWA
searchResult := searchResult "LEFT JOIN HRS.SWA_SW_ANSPRUCH ON SWA_DVFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = TOOFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = TOO_FILIALNR" "~"
if joinKuerzel = FIRTOO
searchResult := searchResult "LEFT JOIN WWS.TOO_TOUROBJEKT ON TOO_FILIALNR = FIR_FIRMENNR" "~"
if joinKuerzel = TRSFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = TRS_FIRMENNR" "~"
if joinKuerzel = FIRTRS
searchResult := searchResult "LEFT JOIN BUERO.TRS_TEILSCHLUSSRECHSCHEMA ON TRS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = UMFFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = UMF_FIRMENNR" "~"
if joinKuerzel = FIRUMF
searchResult := searchResult "LEFT JOIN BUERO.UMF_UMFRAGE ON UMF_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = VMRFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = VMR_FIRMENNR" "~"
if joinKuerzel = FIRVMR
searchResult := searchResult "LEFT JOIN WWS.VMR_VERTABRMAXRABATTE ON VMR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = WAPFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = WAP_FILIALNR" "~"
if joinKuerzel = FIRWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_FILIALNR = FIR_FIRMENNR" "~"
if joinKuerzel = WEAFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = WEA_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRWEA
searchResult := searchResult "LEFT JOIN WWS.WEA_WWSERKONTROLLERABATTE ON WEA_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = WFGFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = WFG_FIRMENNR" "~"
if joinKuerzel = FIRWFG
searchResult := searchResult "LEFT JOIN WWS.WFG_FOLGEARTIKELGRUPPE ON WFG_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = WFIFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = WFI_FIRMENNR" "~"
if joinKuerzel = FIRWFI
searchResult := searchResult "LEFT JOIN WEB.WFI_WEBFIRMA ON WFI_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = WSSFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = WSS_FIRMENNR" "~"
if joinKuerzel = FIRWSS
searchResult := searchResult "LEFT JOIN WWS.WSS_WWSSTATISTIK ON WSS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SCEFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = SCE_FIR_FIRMENNR" "~"
if joinKuerzel = FIRSCE
searchResult := searchResult "LEFT JOIN PRODUKTION.SCE_SCORECARD ON SCE_FIR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SEQFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = SEQ_FIRMENNR" "~"
if joinKuerzel = FIRSEQ
searchResult := searchResult "LEFT JOIN BMD.SEQ_SEQUENZ ON SEQ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SREFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = SRE_FIRMENNR" "~"
if joinKuerzel = FIRSRE
searchResult := searchResult "LEFT JOIN BMD.SRE_STANDARDREPORT ON SRE_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = TLPFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = TLP_FIRMENNR" "~"
if joinKuerzel = FIRTLP
searchResult := searchResult "LEFT JOIN PRODUKTION.TLP_BT_LOGISTIK_POSITION ON TLP_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = TMOFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = TMO_FIRMENNR" "~"
if joinKuerzel = FIRTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = TSAFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = TSA_FIRMENNR" "~"
if joinKuerzel = FIRTSA
searchResult := searchResult "LEFT JOIN FIBU.TSA_TEILSCHLUSSRECHABLAUF ON TSA_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = UEBFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = UEB_FIRMENNR" "~"
if joinKuerzel = FIRUEB
searchResult := searchResult "LEFT JOIN BUERO.UEB_UEBERWEISUNGSART ON UEB_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = VFDFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = VFD_FIRMENNR" "~"
if joinKuerzel = FIRVFD
searchResult := searchResult "LEFT JOIN PRODUKTION.VFD_VERFAHRENSANWEISUNG_POS ON VFD_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = WRPFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = WRP_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRWRP
searchResult := searchResult "LEFT JOIN WWS.WRP_WWSANKAUFSRNGPOS ON WRP_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = WSKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = WSK_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRWSK
searchResult := searchResult "LEFT JOIN WWS.WSK_WWSKORESTATISTIK ON WSK_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AMZFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = AMZ_AUSDRERSTELL_FIRMENNR" "~"
if joinKuerzel = FIRAMZ
searchResult := searchResult "LEFT JOIN BUERO.AMZ_AUSDRUCKMODELLZU ON AMZ_AUSDRERSTELL_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ASZFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = ASZ_FILIAL_FIRMENNR" "~"
if joinKuerzel = FIRASZ
searchResult := searchResult "LEFT JOIN BUERO.ASZ_AUSSCHRFILIALEZU ON ASZ_FILIAL_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AWSFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AWS_LEI_MITARB_FIRMENNR" "~"
if joinKuerzel = FIRAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_LEI_MITARB_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = EDVFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = EDV_HAUPTFIRMENNR" "~"
if joinKuerzel = FIREDV
searchResult := searchResult "LEFT JOIN WWS.EDV_ERFASSRABATTDETAILVOR ON EDV_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = MITFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = MIT_FIL_FIRMENNR" "~"
if joinKuerzel = FIRMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIL_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = WSSFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = WSS_FILIALNR" "~"
if joinKuerzel = FIRWSS
searchResult := searchResult "LEFT JOIN WWS.WSS_WWSSTATISTIK ON WSS_FILIALNR = FIR_FIRMENNR" "~"
if joinKuerzel = BOTFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = BOT_FIRMENNR" "~"
if joinKuerzel = FIRBOT
searchResult := searchResult "LEFT JOIN WWS_NEU.BOT_BONUSTAB ON BOT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = EHTFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = EHT_FIRMENNR" "~"
if joinKuerzel = FIREHT
searchResult := searchResult "LEFT JOIN WWS.EHT_EINHEITENTAB ON EHT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ETKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ETK_FIRMENNR" "~"
if joinKuerzel = FIRETK
searchResult := searchResult "LEFT JOIN WWS.ETK_ETIKETTENKLASSE ON ETK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = GEGFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = GEG_FIRMENNR" "~"
if joinKuerzel = FIRGEG
searchResult := searchResult "LEFT JOIN WWS_NEU.GEG_GEFAHRENGUT ON GEG_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = MOKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = MOK_FIRMENNR" "~"
if joinKuerzel = FIRMOK
searchResult := searchResult "LEFT JOIN WWS.MOK_MODELLKOPF ON MOK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PVKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PVK_FIRMENNR" "~"
if joinKuerzel = FIRPVK
searchResult := searchResult "LEFT JOIN WWS.PVK_PROVISIONTAB ON PVK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RNGFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = RNG_FIRMENNR" "~"
if joinKuerzel = FIRRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = MDEFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = MDE_FILIALNR" "~"
if joinKuerzel = FIRMDE
searchResult := searchResult "LEFT JOIN WWS.MDE_MOBILEDATENERFASSUNG ON MDE_FILIALNR = FIR_FIRMENNR" "~"
if joinKuerzel = AWEFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AWE_FIRMENGRUPPE" "~"
if joinKuerzel = FIRAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_FIRMENGRUPPE = FIR_FIRMENNR" "~"
if joinKuerzel = EVTFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = EVT_FKT_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = EVT_FIRMENKATEGORIEKZ AND FKT_FIRMENKATEGORIELFDNR = EVT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = FKTEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_FKT_FIRMENNR = FKT_FIRMENNR AND EVT_FIRMENKATEGORIEKZ = FKT_FIRMENKATEGORIEKZ AND EVT_FIRMENKATEGORIELFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = IVPFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = IVP_FIRMENNR" "~"
if joinKuerzel = FIRIVP
searchResult := searchResult "LEFT JOIN WWS.IVP_INVENTURPOSITION ON IVP_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = KAJFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = KAJ_FIRMENNR" "~"
if joinKuerzel = FIRKAJ
searchResult := searchResult "LEFT JOIN WWS.KAJ_KASSENJOURNAL ON KAJ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = KFGFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = KFG_FIRMENNR" "~"
if joinKuerzel = FIRKFG
searchResult := searchResult "LEFT JOIN WWS.KFG_KTOFINDBETRGRENZEN ON KFG_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = KFIFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = KFI_FIRMENNR" "~"
if joinKuerzel = FIRKFI
searchResult := searchResult "LEFT JOIN BUERO.KFI_KORETYPENFINDUNG ON KFI_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = KGDFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = KGD_FIRMENNR" "~"
if joinKuerzel = FIRKGD
searchResult := searchResult "LEFT JOIN WWS.KGD_KASSENGELDLADE ON KGD_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = KUKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = KUK_FIRMENNR" "~"
if joinKuerzel = FIRKUK
searchResult := searchResult "LEFT JOIN WWS.KUK_KUNDENKARTEI ON KUK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = LADFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LAD_FILIALNR" "~"
if joinKuerzel = FIRLAD
searchResult := searchResult "LEFT JOIN WWS.LAD_LAGERARTIKELDATEN ON LAD_FILIALNR = FIR_FIRMENNR" "~"
if joinKuerzel = LEDFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = LED_FIRMENNR" "~"
if joinKuerzel = FIRLED
searchResult := searchResult "LEFT JOIN BUERO.LED_LEAD ON LED_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = LISFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LIS_DVFIRMENNR" "~"
if joinKuerzel = FIRLIS
searchResult := searchResult "LEFT JOIN HRS.LIS_LOHNIMPORTSPERRE ON LIS_DVFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = MABFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = MAB_FIRMENNR" "~"
if joinKuerzel = FIRMAB
searchResult := searchResult "LEFT JOIN BUERO.MAB_MITARBAUSBILDUNG ON MAB_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = MEDFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = MED_GEHOERT_FIRMENNR" "~"
if joinKuerzel = FIRMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_GEHOERT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = MRVFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = MRV_FIRMENNR" "~"
if joinKuerzel = FIRMRV
searchResult := searchResult "LEFT JOIN ZLE.MRV_MONATSRECHNUNGVORLAGE ON MRV_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PFZFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = PFZ_FIRMENNR" "~"
if joinKuerzel = FIRPFZ
searchResult := searchResult "LEFT JOIN BUERO.PFZ_PERSONFIRMENZU ON PFZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PICFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PIC_FIRMENNR" "~"
if joinKuerzel = FIRPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PJOFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PJO_FIRMENNR" "~"
if joinKuerzel = FIRPJO
searchResult := searchResult "LEFT JOIN WWS.PJO_PREISJOURNAL ON PJO_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PLGFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = PLG_FIRMENNR" "~"
if joinKuerzel = FIRPLG
searchResult := searchResult "LEFT JOIN BUERO.PLG_PLANGRUPPE ON PLG_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PLOFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PLO_FIRMENNR" "~"
if joinKuerzel = FIRPLO
searchResult := searchResult "LEFT JOIN PRODUKTION.PLO_PPS_LOS ON PLO_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PRDFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PRD_FIRMENNR" "~"
if joinKuerzel = FIRPRD
searchResult := searchResult "LEFT JOIN ZLE.PRD_PERIODE ON PRD_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PRKFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = PRK_FIRMENNR" "~"
if joinKuerzel = FIRPRK
searchResult := searchResult "LEFT JOIN BUERO.PRK_PROJEKTKREIS ON PRK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RAPFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = RAP_PREISGRUPPEN_FIRMENNR" "~"
if joinKuerzel = FIRRAP
searchResult := searchResult "LEFT JOIN WWS.RAP_RABATTTABPOS ON RAP_PREISGRUPPEN_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RERFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = RER_FIRMENNR" "~"
if joinKuerzel = FIRRER
searchResult := searchResult "LEFT JOIN WWS.RER_RESERVIERUNGSREFERENZ ON RER_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RZWFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = RZW_FIRMENNR" "~"
if joinKuerzel = FIRRZW
searchResult := searchResult "LEFT JOIN HRS.RZW_REISEZWECK ON RZW_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SBKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = SBK_FIRMENNR" "~"
if joinKuerzel = FIRSBK
searchResult := searchResult "LEFT JOIN WWS.SBK_SAISONBESCHAFFKOPF ON SBK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SCQFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = SCQ_FIR_FIRMENNR" "~"
if joinKuerzel = FIRSCQ
searchResult := searchResult "LEFT JOIN PRODUKTION.SCQ_SCKZGRP ON SCQ_FIR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SLRFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = SLR_FIRMENNR" "~"
if joinKuerzel = FIRSLR
searchResult := searchResult "LEFT JOIN BUERO.SLR_SKILLREGELELEMENT ON SLR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SNRFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = SNR_FIRMENNR" "~"
if joinKuerzel = FIRSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PROFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = PRO_FIRMENNR" "~"
if joinKuerzel = FIRPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PUSFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PUS_FIRMENNR" "~"
if joinKuerzel = FIRPUS
searchResult := searchResult "LEFT JOIN PRODUKTION.PUS_PPSUSERSTATI ON PUS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PWFFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PWF_FIRMENNR" "~"
if joinKuerzel = FIRPWF
searchResult := searchResult "LEFT JOIN WWS.PWF_PWKFIRMENZU ON PWF_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PWKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PWK_FIRMENNR" "~"
if joinKuerzel = FIRPWK
searchResult := searchResult "LEFT JOIN WWS.PWK_PREISRABATTWARTKOPF ON PWK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = QSTFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = QST_FIRMENNR" "~"
if joinKuerzel = FIRQST
searchResult := searchResult "LEFT JOIN PRODUKTION.QST_QM_STAMM ON QST_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RCLFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = RCL_FIRMENNR" "~"
if joinKuerzel = FIRRCL
searchResult := searchResult "LEFT JOIN PRODUKTION.RCL_REPEATING_CHECKLIST ON RCL_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SCAFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = SCA_FIR_FIRMENNR" "~"
if joinKuerzel = FIRSCA
searchResult := searchResult "LEFT JOIN PRODUKTION.SCA_SCKRITERIUM ON SCA_FIR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SFZFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = SFZ_FIRMENNR" "~"
if joinKuerzel = FIRSFZ
searchResult := searchResult "LEFT JOIN BUERO.SFZ_SYNCFELDZUORDNUNG ON SFZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = TLAFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = TLA_FIRMENNR" "~"
if joinKuerzel = FIRTLA
searchResult := searchResult "LEFT JOIN PRODUKTION.TLA_BT_LOGISTIK_ANGEFORDERT ON TLA_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = TLUFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = TLU_FIRMENNR" "~"
if joinKuerzel = FIRTLU
searchResult := searchResult "LEFT JOIN PRODUKTION.TLU_BT_LOGISTIK_ZU ON TLU_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = TOZFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = TOZ_FIRMENNR" "~"
if joinKuerzel = FIRTOZ
searchResult := searchResult "LEFT JOIN WWS.TOZ_TOURZUORDNUNG ON TOZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = VFRFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = VFR_FIRMENNR" "~"
if joinKuerzel = FIRVFR
searchResult := searchResult "LEFT JOIN WWS.VFR_VERLEIHFREISTELLUNG ON VFR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = WARFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = WAR_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRWAR
searchResult := searchResult "LEFT JOIN WWS.WAR_WARENEINGANGRABATTE ON WAR_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = WKAFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = WKA_FIRMENNR" "~"
if joinKuerzel = FIRWKA
searchResult := searchResult "LEFT JOIN WWS.WKA_WARENAUSGKOSTENARTEN ON WKA_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = WMDFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = WMD_FIRMENNR" "~"
if joinKuerzel = FIRWMD
searchResult := searchResult "LEFT JOIN BUERO.WMD_WOCHENMODELL ON WMD_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ZBZFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = ZBZ_FIRMENNR" "~"
if joinKuerzel = FIRZBZ
searchResult := searchResult "LEFT JOIN BUERO.ZBZ_ZEITBEZUGSART ON ZBZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ZLTFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ZLT_FIRMENNR" "~"
if joinKuerzel = FIRZLT
searchResult := searchResult "LEFT JOIN ZLE.ZLT_ZEILENTYP ON ZLT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ZRBFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = ZRB_FIRMENNR" "~"
if joinKuerzel = FIRZRB
searchResult := searchResult "LEFT JOIN BUERO.ZRB_ZEITRUNDUNGBEDINGUNG ON ZRB_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = GSKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = GSK_FILIALE_FIRMENNR" "~"
if joinKuerzel = FIRGSK
searchResult := searchResult "LEFT JOIN WWS.GSK_GUTSCHEINKOPF ON GSK_FILIALE_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = KLIFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = KLI_QUOTEN_FIRMENNR" "~"
if joinKuerzel = FIRKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_QUOTEN_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PFZFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = PFZ_FILIAL_FIRMENNR" "~"
if joinKuerzel = FIRPFZ
searchResult := searchResult "LEFT JOIN BUERO.PFZ_PERSONFIRMENZU ON PFZ_FILIAL_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PKSFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PKS_FIRMENNR" "~"
if joinKuerzel = FIRPKS
searchResult := searchResult "LEFT JOIN PRODUKTION.PKS_SOLL_KAPAZITAET ON PKS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PROFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = PRO_FILIALE_FIRMENNR" "~"
if joinKuerzel = FIRPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FILIALE_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AIDFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AID_FIRMENNR" "~"
if joinKuerzel = FIRAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ARLFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ARL_FIRMENNR" "~"
if joinKuerzel = FIRARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ARSFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ARS_FIRMENNR" "~"
if joinKuerzel = FIRARS
searchResult := searchResult "LEFT JOIN WWS.ARS_ARTIKELSTATISTIK ON ARS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RNGFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = RNG_SAMMELNR_FIRMENNR" "~"
if joinKuerzel = FIRRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_SAMMELNR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = LAJFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LAJ_BUCHUNGS_FIRMENNR" "~"
if joinKuerzel = FIRLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_BUCHUNGS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AKLFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AKL_FIRMENNR" "~"
if joinKuerzel = FIRAKL
searchResult := searchResult "LEFT JOIN WWS.AKL_ARTIKELKLASSIFIKATION ON AKL_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FIRFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = FIR_WP_FIRMENNR" "~"
if joinKuerzel = FIRFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_WP_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FGMFIW
searchResult := searchResult "LEFT JOIN BUERO.FIW_FIRMENWERTE ON FIW_FIRMENNR = FGM_FIRMENNR AND FIW_FIRMAGUELTIGJAHR = FGM_FIRMAGUELTIGJAHR" "~"
if joinKuerzel = FIWFGM
searchResult := searchResult "LEFT JOIN BUERO.FGM_FIRMENGEMEINDEZU ON FGM_FIRMENNR = FIW_FIRMENNR AND FGM_FIRMAGUELTIGJAHR = FIW_FIRMAGUELTIGJAHR" "~"
if joinKuerzel = EVKFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = EVK_FKT_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = EVK_FIRMENKATEGORIEKZ AND FKT_FIRMENKATEGORIELFDNR = EVK_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = FKTEVK
searchResult := searchResult "LEFT JOIN BUERO.EVK_EVENTKATEGORIEZU ON EVK_FKT_FIRMENNR = FKT_FIRMENNR AND EVK_FIRMENKATEGORIEKZ = FKT_FIRMENKATEGORIEKZ AND EVK_FIRMENKATEGORIELFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = FFZFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = FFZ_FKT_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = FFZ_FIRMENKATEGORIEKZ AND FKT_FIRMENKATEGORIELFDNR = FFZ_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = FKTFFZ
searchResult := searchResult "LEFT JOIN BUERO.FFZ_FRAGEBOGENFRAGEZU ON FFZ_FKT_FIRMENNR = FKT_FIRMENNR AND FFZ_FIRMENKATEGORIEKZ = FKT_FIRMENKATEGORIEKZ AND FFZ_FIRMENKATEGORIELFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = KMAFKT
searchResult := searchResult "LEFT JOIN FKT_FIRMENKATEGORIE.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = KMA_FKT_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = KMA_FIRMENKATEGORIEKZ AND FKT_FIRMENKATEGORIELFDNR = KMA_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = FKTKMA
searchResult := searchResult "LEFT JOIN WWS.KMA_KUNDENMITARBBEZUG ON KMA_FKT_FIRMENNR = FKT_FIRMENNR AND KMA_FIRMENKATEGORIEKZ = FKT_FIRMENKATEGORIEKZ AND KMA_FIRMENKATEGORIELFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = BIMFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = BIM_MAKROID" "~"
if joinKuerzel = FORBIM
searchResult := searchResult "LEFT JOIN FIBU.BIM_BUERFIMPORTMODELL ON BIM_MAKROID = FOR_FORMELNR" "~"
if joinKuerzel = FEZFOR
searchResult := searchResult "LEFT JOIN BMD.FOR_FORMEL ON FOR_FORMELNR = FEZ_MACRO_ID" "~"
if joinKuerzel = FORFEZ
searchResult := searchResult "LEFT JOIN BMD.FEZ_FELDGRUPPENZU ON FEZ_MACRO_ID = FOR_FORMELNR" "~"
if joinKuerzel = ZAKFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = ZAK_FORMELNR" "~"
if joinKuerzel = FORZAK
searchResult := searchResult "LEFT JOIN BUERO.ZAK_ZEITAUSWERTMAKRO ON ZAK_FORMELNR = FOR_FORMELNR" "~"
if joinKuerzel = ZVTFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = ZVT_FORMELNR_PROT_SUMME" "~"
if joinKuerzel = FORZVT
searchResult := searchResult "LEFT JOIN BUERO.ZVT_TBDATEIEN ON ZVT_FORMELNR_PROT_SUMME = FOR_FORMELNR" "~"
if joinKuerzel = ZVTFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = ZVT_FORMELNR_PROT_BEWEGUNG" "~"
if joinKuerzel = FORZVT
searchResult := searchResult "LEFT JOIN BUERO.ZVT_TBDATEIEN ON ZVT_FORMELNR_PROT_BEWEGUNG = FOR_FORMELNR" "~"
if joinKuerzel = BRSFPR
searchResult := searchResult "LEFT JOIN FIBU.FPR_FIBUPROZENTSATZ ON FPR_FIRMENNR = BRS_FIRMENNR AND FPR_FIBUNR = BRS_FIBUNR AND FPR_LANDNR = BRS_LANDNR AND FPR_PROZENTSATZ = BRS_PROZENTSATZ" "~"
if joinKuerzel = FPRBRS
searchResult := searchResult "LEFT JOIN FIBU.BRS_BANKRUECKAUFTEILSATZ ON BRS_FIRMENNR = FPR_FIRMENNR AND BRS_FIBUNR = FPR_FIBUNR AND BRS_LANDNR = FPR_LANDNR AND BRS_PROZENTSATZ = FPR_PROZENTSATZ" "~"
if joinKuerzel = VEBFPR
searchResult := searchResult "LEFT JOIN FIBU.FPR_FIBUPROZENTSATZ ON FPR_FIRMENNR = VEB_FIRMENNR AND FPR_FIBUNR = VEB_FIBUNR AND FPR_LANDNR = VEB_LANDNR AND FPR_PROZENTSATZ = VEB_FPR_PROZENTSATZ" "~"
if joinKuerzel = FPRVEB
searchResult := searchResult "LEFT JOIN FIBU.VEB_VORERFASSBUCHUNG ON VEB_FIRMENNR = FPR_FIRMENNR AND VEB_FIBUNR = FPR_FIBUNR AND VEB_LANDNR = FPR_LANDNR AND VEB_FPR_PROZENTSATZ = FPR_PROZENTSATZ" "~"
if joinKuerzel = ZVKFPR
searchResult := searchResult "LEFT JOIN FIBU.FPR_FIBUPROZENTSATZ ON FPR_FIRMENNR = ZVK_FIRMENNR AND FPR_FIBUNR = ZVK_FIBUNR AND FPR_LANDNR = ZVK_LANDNR AND FPR_PROZENTSATZ = ZVK_STEUERPROZENTSATZ" "~"
if joinKuerzel = FPRZVK
searchResult := searchResult "LEFT JOIN FIBU.ZVK_ZAHLUNGSVORSCHLAG ON ZVK_FIRMENNR = FPR_FIRMENNR AND ZVK_FIBUNR = FPR_FIBUNR AND ZVK_LANDNR = FPR_LANDNR AND ZVK_STEUERPROZENTSATZ = FPR_PROZENTSATZ" "~"
if joinKuerzel = FSTFRA
searchResult := searchResult "LEFT JOIN BUERO.FRA_FRISTART ON FRA_FRISTARTLFDNR = FST_FRISTARTLFDNR" "~"
if joinKuerzel = FRAFST
searchResult := searchResult "LEFT JOIN BUERO.FST_FRIST ON FST_FRISTARTLFDNR = FRA_FRISTARTLFDNR" "~"
if joinKuerzel = QAWFRA
searchResult := searchResult "LEFT JOIN BUERO.FRA_FRISTART ON FRA_FRISTARTLFDNR = QAW_FRISTARTLFDNR" "~"
if joinKuerzel = FRAQAW
searchResult := searchResult "LEFT JOIN BUERO.QAW_QUOTENAUSWERTUNG ON QAW_FRISTARTLFDNR = FRA_FRISTARTLFDNR" "~"
if joinKuerzel = ARLFRB
searchResult := searchResult "LEFT JOIN WWS.FRB_FARBE ON FRB_FIRMENNR = ARL_VORG_FRB_FIRMENNR AND FRB_FARBENNR = ARL_VORG_FARBENNR" "~"
if joinKuerzel = FRBARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_VORG_FRB_FIRMENNR = FRB_FIRMENNR AND ARL_VORG_FARBENNR = FRB_FARBENNR" "~"
if joinKuerzel = LMBFRB
searchResult := searchResult "LEFT JOIN WWS.FRB_FARBE ON FRB_FIRMENNR = LMB_ARL_FIRMENNR AND FRB_FARBENNR = LMB_FARBENNR" "~"
if joinKuerzel = FRBLMB
searchResult := searchResult "LEFT JOIN WWS.LMB_LAGERMINMAXBESTAND ON LMB_ARL_FIRMENNR = FRB_FIRMENNR AND LMB_FARBENNR = FRB_FARBENNR" "~"
if joinKuerzel = FKKFRT
searchResult := searchResult "LEFT JOIN ZLE.FRT_FAKTURATYP ON FRT_FIRMENNR = FKK_FRT_FIRMENNR AND FRT_FAKTURATYPNR = FKK_FAKTURATYPNR" "~"
if joinKuerzel = FRTFKK
searchResult := searchResult "LEFT JOIN ZLE.FKK_FAKTURAKZKUNDENZU ON FKK_FRT_FIRMENNR = FRT_FIRMENNR AND FKK_FAKTURATYPNR = FRT_FAKTURATYPNR" "~"
if joinKuerzel = WEKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = WEK_FIRMENNR" "~"
if joinKuerzel = FIRWEK
searchResult := searchResult "LEFT JOIN PRODUKTION.WEK_WERKZEUG ON WEK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ZBKFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = ZBK_FIRMENNR" "~"
if joinKuerzel = FIRZBK
searchResult := searchResult "LEFT JOIN BUERO.ZBK_ZEITBETRIEBSKALENDER ON ZBK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ZBUFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = ZBU_FILIALE_FIRMENNR" "~"
if joinKuerzel = FIRZBU
searchResult := searchResult "LEFT JOIN BUERO.ZBU_ZEITBUCHUNG ON ZBU_FILIALE_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ZLGFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ZLG_FIRMENNR" "~"
if joinKuerzel = FIRZLG
searchResult := searchResult "LEFT JOIN ZLE.ZLG_ZAHLUNGEN ON ZLG_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ZNSFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = ZNS_FIRMENNR" "~"
if joinKuerzel = FIRZNS
searchResult := searchResult "LEFT JOIN BUERO.ZNS_ZEITNACHRICHTSYSTEM ON ZNS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ZSKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ZSK_FIRMENNR" "~"
if joinKuerzel = FIRZSK
searchResult := searchResult "LEFT JOIN ZLE.ZSK_ZEITSCHEIBEKOPF ON ZSK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ZTGFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ZTG_DVFIRMENNR" "~"
if joinKuerzel = FIRZTG
searchResult := searchResult "LEFT JOIN HRS.ZTG_ZAHLTAG ON ZTG_DVFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ADBFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ADB_FIRMENNR" "~"
if joinKuerzel = FIRADB
searchResult := searchResult "LEFT JOIN WWS.ADB_ABGABENDETAILBON ON ADB_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = DEIFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = DEI_FIBU_FIRMENNR" "~"
if joinKuerzel = FIRDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_FIBU_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = KASFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = KAS_FILIALNR" "~"
if joinKuerzel = FIRKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_FILIALNR = FIR_FIRMENNR" "~"
if joinKuerzel = LFIFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LFI_STDFIRMENNR" "~"
if joinKuerzel = FIRLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_STDFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = MDEFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = MDE_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRMDE
searchResult := searchResult "LEFT JOIN WWS.MDE_MOBILEDATENERFASSUNG ON MDE_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = POSFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = POS_HAUPT_FIRMENNR" "~"
if joinKuerzel = FIRPOS
searchResult := searchResult "LEFT JOIN BUERO.POS_POST ON POS_HAUPT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = GROFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = GRO_FIRMENNR" "~"
if joinKuerzel = FIRGRO
searchResult := searchResult "LEFT JOIN WWS.GRO_GROESSE ON GRO_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = IFAFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = IFA_FIRMENNR" "~"
if joinKuerzel = FIRIFA
searchResult := searchResult "LEFT JOIN WWSINDIV.IFA_FAKTURA ON IFA_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = LAGFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LAG_FIRMENNR" "~"
if joinKuerzel = FIRLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = LKOFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LKO_FIRMENNR" "~"
if joinKuerzel = FIRLKO
searchResult := searchResult "LEFT JOIN WWS.LKO_LAGERKONTO ON LKO_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RNRFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = RNR_FIRMENNR" "~"
if joinKuerzel = FIRRNR
searchResult := searchResult "LEFT JOIN WWS.RNR_RECHNUNGSRABATT ON RNR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SRAFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = SRA_FIRMENNR" "~"
if joinKuerzel = FIRSRA
searchResult := searchResult "LEFT JOIN WWS.SRA_STEUERART ON SRA_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SRTFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = SRT_FIRMENNR" "~"
if joinKuerzel = FIRSRT
searchResult := searchResult "LEFT JOIN WWS.SRT_STEUERTAB ON SRT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = TREFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = TRE_FIRMENNR" "~"
if joinKuerzel = FIRTRE
searchResult := searchResult "LEFT JOIN WWS.TRE_TRANSPORTEINHEIT ON TRE_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = TRTFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = TRT_FIRMENNR" "~"
if joinKuerzel = FIRTRT
searchResult := searchResult "LEFT JOIN WWS.TRT_TRANSPORTTYP ON TRT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = VSTFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = VST_FIRMENNR" "~"
if joinKuerzel = FIRVST
searchResult := searchResult "LEFT JOIN WWS.VST_VERKAUFSSTATISTIK ON VST_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = WKTFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = WKT_FIRMENNR" "~"
if joinKuerzel = FIRWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ASTFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AST_HAUPT_FIRMENNR" "~"
if joinKuerzel = FIRAST
searchResult := searchResult "LEFT JOIN WWS.AST_ARTIKELSTATISTIK ON AST_HAUPT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AWSFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AWS_LEI_ORG_FIRMENNR" "~"
if joinKuerzel = FIRAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_LEI_ORG_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FGKFIW
searchResult := searchResult "LEFT JOIN BUERO.FIW_FIRMENWERTE ON FIW_FIRMENNR = FGK_FIRMENNR AND FIW_FIRMAGUELTIGJAHR = FGK_FIRMAGUELTIGJAHR" "~"
if joinKuerzel = FIWFGK
searchResult := searchResult "LEFT JOIN BUERO.FGK_FIRMENGKKZU ON FGK_FIRMENNR = FIW_FIRMENNR AND FGK_FIRMAGUELTIGJAHR = FIW_FIRMAGUELTIGJAHR" "~"
if joinKuerzel = EVIFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = EVI_FIRMENNR" "~"
if joinKuerzel = FIREVI
searchResult := searchResult "LEFT JOIN BUERO.EVI_EVIDENZ ON EVI_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FESFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = FES_FIRMENNR" "~"
if joinKuerzel = FIRFES
searchResult := searchResult "LEFT JOIN BUERO.FES_FRISTERLEDIGTSTATUS ON FES_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FGTFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = FGT_FIRMENNR" "~"
if joinKuerzel = FIRFGT
searchResult := searchResult "LEFT JOIN WWS.FGT_FREMDGUTSCHEINTYP ON FGT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FWEFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = FWE_FIRMENNR" "~"
if joinKuerzel = FIRFWE
searchResult := searchResult "LEFT JOIN HRS.FWE_FIXWERTE ON FWE_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = IFTFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = IFT_FIRMENNR" "~"
if joinKuerzel = FIRIFT
searchResult := searchResult "LEFT JOIN BUERO.IFT_INFOTAB ON IFT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = IMZFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = IMZ_FIRMENNR" "~"
if joinKuerzel = FIRIMZ
searchResult := searchResult "LEFT JOIN BUERO.IMZ_INSTANDHMASSNZU ON IMZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = IOGFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = IOG_FIRMENNR" "~"
if joinKuerzel = FIRIOG
searchResult := searchResult "LEFT JOIN BUERO.IOG_INSTANDHOBJEKTGRUPPE ON IOG_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = KASFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = KAS_FIRMENNR" "~"
if joinKuerzel = FIRKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = KOKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = KOK_FIRMENNR" "~"
if joinKuerzel = FIRKOK
searchResult := searchResult "LEFT JOIN PRODUKTION.KOK_KOMMISSIONIERUNG_KOPF ON KOK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = KSLFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = KSL_FIRMENNR" "~"
if joinKuerzel = FIRKSL
searchResult := searchResult "LEFT JOIN BUERO.KSL_KONTAKTSYNCEINST ON KSL_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = LLDFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LLD_FIRMENNR" "~"
if joinKuerzel = FIRLLD
searchResult := searchResult "LEFT JOIN ZLE.LLD_LEALOHNDATEN ON LLD_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = LMAFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LMA_FIRMENNR" "~"
if joinKuerzel = FIRLMA
searchResult := searchResult "LEFT JOIN WWS.LMA_LAGERMONATSABSCHLUSS ON LMA_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = LUKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LUK_FIRMENNR" "~"
if joinKuerzel = FIRLUK
searchResult := searchResult "LEFT JOIN PRODUKTION.LUK_LAGER_UMS_KOPF ON LUK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = MVAFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = MVA_FIRMENNR" "~"
if joinKuerzel = FIRMVA
searchResult := searchResult "LEFT JOIN FIBU.MVA_MAHNVZINSENAB ON MVA_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PABFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PAB_FIRMENNR" "~"
if joinKuerzel = FIRPAB
searchResult := searchResult "LEFT JOIN WWS.PAB_PROVARTBAUM ON PAB_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PLIFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = PLI_FIRMENNR" "~"
if joinKuerzel = FIRPLI
searchResult := searchResult "LEFT JOIN BUERO.PLI_PIPELINE ON PLI_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PSPFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PSP_WERT_FIRMENNR" "~"
if joinKuerzel = FIRPSP
searchResult := searchResult "LEFT JOIN PRODUKTION.PSP_STAPEL_SCHRITT_PARAM ON PSP_WERT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PTZFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PTZ_FIRMENNR" "~"
if joinKuerzel = FIRPTZ
searchResult := searchResult "LEFT JOIN WWS.PTZ_PREISTABZU ON PTZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RDDFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = RDD_FIRMENNR" "~"
if joinKuerzel = FIRRDD
searchResult := searchResult "LEFT JOIN BIL.RDD_RSTDRUCKDETAIL ON RDD_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RNRFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = RNR_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRRNR
searchResult := searchResult "LEFT JOIN WWS.RNR_RECHNUNGSRABATT ON RNR_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RSIFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = RSI_FIRMENNR" "~"
if joinKuerzel = FIRRSI
searchResult := searchResult "LEFT JOIN WWS.RSI_REGKASIGNATUREH ON RSI_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RTZFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = RTZ_FIRMENNR" "~"
if joinKuerzel = FIRRTZ
searchResult := searchResult "LEFT JOIN ZLE.RTZ_RECHTEXTZU ON RTZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RUKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = RUK_FIRMENNR" "~"
if joinKuerzel = FIRRUK
searchResult := searchResult "LEFT JOIN WWS.RUK_RUNDUNGKOPF ON RUK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SA1FIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = SA1_FIRMENNR" "~"
if joinKuerzel = FIRSA1
searchResult := searchResult "LEFT JOIN ONVIS.SA1_SAURER_BDE ON SA1_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SFRFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = SFR_FIRMENNR" "~"
if joinKuerzel = FIRSFR
searchResult := searchResult "LEFT JOIN BUERO.SFR_SKILLFINDERREGEL ON SFR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SMIFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = SMI_FIRMENNR" "~"
if joinKuerzel = FIRSMI
searchResult := searchResult "LEFT JOIN ZLE.SMI_SMARTINTERFACE ON SMI_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ZASFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = ZAS_FIRMENNR" "~"
if joinKuerzel = FIRZAS
searchResult := searchResult "LEFT JOIN BUERO.ZAS_ZAHLSCHEIN ON ZAS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AKLFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AKL_FILIALE" "~"
if joinKuerzel = FIRAKL
searchResult := searchResult "LEFT JOIN WWS.AKL_ARTIKELKLASSIFIKATION ON AKL_FILIALE = FIR_FIRMENNR" "~"
if joinKuerzel = BVRFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = BVR_FILIALNR" "~"
if joinKuerzel = FIRBVR
searchResult := searchResult "LEFT JOIN FIBU.BVR_BANKRVREGELN ON BVR_FILIALNR = FIR_FIRMENNR" "~"
if joinKuerzel = TLEFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = TLE_TRE_FIRMENNR" "~"
if joinKuerzel = FIRTLE
searchResult := searchResult "LEFT JOIN PRODUKTION.TLE_BT_LOGISTIK_EMPFANGEN ON TLE_TRE_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = VLJFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = VLJ_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRVLJ
searchResult := searchResult "LEFT JOIN WWS.VLJ_VERLEIHJOURNAL ON VLJ_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ARKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ARK_FIRMENNR" "~"
if joinKuerzel = FIRARK
searchResult := searchResult "LEFT JOIN WWS.ARK_ARTIKELKLASSE ON ARK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = DUZFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = DUZ_FIRMENNR" "~"
if joinKuerzel = FIRDUZ
searchResult := searchResult "LEFT JOIN DATANORM.DUZ_DATANORM_WUGZU ON DUZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = EKZFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = EKZ_FIRMENNR" "~"
if joinKuerzel = FIREKZ
searchResult := searchResult "LEFT JOIN WWS.EKZ_ERLOESAUFWANDKZ ON EKZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = GZUFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = GZU_FIRMENNR" "~"
if joinKuerzel = FIRGZU
searchResult := searchResult "LEFT JOIN WWS.GZU_GEFAHRENGUTZU ON GZU_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = LFSFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LFS_FIRMENNR" "~"
if joinKuerzel = FIRLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PATFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PAT_FIRMENNR" "~"
if joinKuerzel = FIRPAT
searchResult := searchResult "LEFT JOIN WWS.PAT_PACKSTOFFTAB ON PAT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AWSFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AWS_TYP_FIRMENNR" "~"
if joinKuerzel = FIRAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_TYP_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ANTFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = ANT_FKT_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = ANT_FIRMENKATEGORIEKZ AND FKT_FIRMENKATEGORIELFDNR = ANT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = FKTANT
searchResult := searchResult "LEFT JOIN BUERO.ANT_ANTWORT ON ANT_FKT_FIRMENNR = FKT_FIRMENNR AND ANT_FIRMENKATEGORIEKZ = FKT_FIRMENKATEGORIEKZ AND ANT_FIRMENKATEGORIELFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = ERNFKT
searchResult := searchResult "LEFT JOIN FKT_FIRMENKATEGORIE.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = ERN_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = ERN_FIRMENKATEGORIEKZ AND FKT_FIRMENKATEGORIELFDNR = ERN_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = FKTERN
searchResult := searchResult "LEFT JOIN WWS.ERN_EXTARLKEYNAMES ON ERN_FIRMENNR = FKT_FIRMENNR AND ERN_FIRMENKATEGORIEKZ = FKT_FIRMENKATEGORIEKZ AND ERN_FIRMENKATEGORIELFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = EVAFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = EVA_FKT_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = EVA_FIRMENKATEGORIEKZ AND FKT_FIRMENKATEGORIELFDNR = EVA_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = FKTEVA
searchResult := searchResult "LEFT JOIN BUERO.EVA_EVENTTEAM ON EVA_FKT_FIRMENNR = FKT_FIRMENNR AND EVA_FIRMENKATEGORIEKZ = FKT_FIRMENKATEGORIEKZ AND EVA_FIRMENKATEGORIELFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = PROFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = PRO_FKT_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = PRO_FIRMENKATEGORIEKZ AND FKT_FIRMENKATEGORIELFDNR = PRO_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = FKTPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FKT_FIRMENNR = FKT_FIRMENNR AND PRO_FIRMENKATEGORIEKZ = FKT_FIRMENKATEGORIEKZ AND PRO_FIRMENKATEGORIELFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = WOHFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = WOH_FKT_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = WOH_FIRMENKATEGORIEKZ AND FKT_FIRMENKATEGORIELFDNR = WOH_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = FKTWOH
searchResult := searchResult "LEFT JOIN BUERO.WOH_WOHNUNG ON WOH_FKT_FIRMENNR = FKT_FIRMENNR AND WOH_FIRMENKATEGORIEKZ = FKT_FIRMENKATEGORIEKZ AND WOH_FIRMENKATEGORIELFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = DFZFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = DFZ_MACRO_ID" "~"
if joinKuerzel = FORDFZ
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFZ_DFELDGRUPPENZU ON DFZ_MACRO_ID = FOR_FORMELNR" "~"
if joinKuerzel = HSOFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = HSO_FORMELNR" "~"
if joinKuerzel = FORHSO
searchResult := searchResult "LEFT JOIN HRS.HSO_SORTIERPOS ON HSO_FORMELNR = FOR_FORMELNR" "~"
if joinKuerzel = KFIFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = KFI_MAKRO_FORMELNR" "~"
if joinKuerzel = FORKFI
searchResult := searchResult "LEFT JOIN BUERO.KFI_KORETYPENFINDUNG ON KFI_MAKRO_FORMELNR = FOR_FORMELNR" "~"
if joinKuerzel = QSTFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = QST_ONVALIDATE_MAKRO" "~"
if joinKuerzel = FORQST
searchResult := searchResult "LEFT JOIN PRODUKTION.QST_QM_STAMM ON QST_ONVALIDATE_MAKRO = FOR_FORMELNR" "~"
if joinKuerzel = SFZFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = SFZ_FORMELNR" "~"
if joinKuerzel = FORSFZ
searchResult := searchResult "LEFT JOIN BUERO.SFZ_SYNCFELDZUORDNUNG ON SFZ_FORMELNR = FOR_FORMELNR" "~"
if joinKuerzel = STMFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = STM_FIRMENNR" "~"
if joinKuerzel = FIRSTM
searchResult := searchResult "LEFT JOIN BUERO.STM_STELLENMERKMALE ON STM_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = TLEFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = TLE_FIRMENNR" "~"
if joinKuerzel = FIRTLE
searchResult := searchResult "LEFT JOIN PRODUKTION.TLE_BT_LOGISTIK_EMPFANGEN ON TLE_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = TLKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = TLK_FIRMENNR" "~"
if joinKuerzel = FIRTLK
searchResult := searchResult "LEFT JOIN PRODUKTION.TLK_BT_LOGISTIK_KOPF ON TLK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = TSKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = TSK_FIRMENNR" "~"
if joinKuerzel = FIRTSK
searchResult := searchResult "LEFT JOIN WWS.TSK_TOUCHSCREENKOPF ON TSK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = UZLFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = UZL_FIRMENNR" "~"
if joinKuerzel = FIRUZL
searchResult := searchResult "LEFT JOIN BUERO.UZL_UNTERZIEL ON UZL_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = VASFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = VAS_FIRMENNR" "~"
if joinKuerzel = FIRVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = VMKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = VMK_FIRMENNR" "~"
if joinKuerzel = FIRVMK
searchResult := searchResult "LEFT JOIN WWS.VMK_VERSMGR_KOPF ON VMK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = VPEFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = VPE_FIRMENNR" "~"
if joinKuerzel = FIRVPE
searchResult := searchResult "LEFT JOIN WWS.VPE_VPI_ERHOEHUNG ON VPE_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = VZWFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = VZW_FIRMENNR" "~"
if joinKuerzel = FIRVZW
searchResult := searchResult "LEFT JOIN BUERO.VZW_VERKEHRSZWEIG ON VZW_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = WFKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = WFK_FIRMENNR" "~"
if joinKuerzel = FIRWFK
searchResult := searchResult "LEFT JOIN WWS.WFK_FOLGEARTIKELKAT ON WFK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = WKPFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = WKP_KTO_FIRMENNR" "~"
if joinKuerzel = FIRWKP
searchResult := searchResult "LEFT JOIN WWS.WKP_WWSKONTENTABPOS ON WKP_KTO_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = XSPFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = XSP_STEUERB_FIRMENNR" "~"
if joinKuerzel = FIRXSP
searchResult := searchResult "LEFT JOIN BUERO.XSP_XML_STEUERERKL_PAKET ON XSP_STEUERB_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BRTFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = BRT_PPS_FIRMENNR" "~"
if joinKuerzel = FIRBRT
searchResult := searchResult "LEFT JOIN BUERO.BRT_BERICHT ON BRT_PPS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = IVKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = IVK_BUCH_FIRMENNR" "~"
if joinKuerzel = FIRIVK
searchResult := searchResult "LEFT JOIN WWS.IVK_INVENTURKOPF ON IVK_BUCH_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PCKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PCK_FIR_FIRMENNR2" "~"
if joinKuerzel = FIRPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_FIR_FIRMENNR2 = FIR_FIRMENNR" "~"
if joinKuerzel = STZFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = STZ_FILIALE_FIRMENNR" "~"
if joinKuerzel = FIRSTZ
searchResult := searchResult "LEFT JOIN WWS.STZ_STEUERTEXTZUORDNUNG ON STZ_FILIALE_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = TLAFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = TLA_TRE_FIRMENNR" "~"
if joinKuerzel = FIRTLA
searchResult := searchResult "LEFT JOIN PRODUKTION.TLA_BT_LOGISTIK_ANGEFORDERT ON TLA_TRE_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = DHZFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = DHZ_FIRMENNR" "~"
if joinKuerzel = FIRDHZ
searchResult := searchResult "LEFT JOIN DATANORM.DHZ_DATANORM_WHGZU ON DHZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FRBFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = FRB_FIRMENNR" "~"
if joinKuerzel = FIRFRB
searchResult := searchResult "LEFT JOIN WWS.FRB_FARBE ON FRB_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = KMAFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = KMA_FIRMENNR" "~"
if joinKuerzel = FIRKMA
searchResult := searchResult "LEFT JOIN WWS.KMA_KUNDENMITARBBEZUG ON KMA_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = LBKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LBK_FIRMENNR" "~"
if joinKuerzel = FIRLBK
searchResult := searchResult "LEFT JOIN WWS.LBK_LAGERBUCHKENNZ ON LBK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PSGFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PSG_FIRMENNR" "~"
if joinKuerzel = FIRPSG
searchResult := searchResult "LEFT JOIN WWS.PSG_PREISGRUPPE ON PSG_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AWEFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AWE_FIRMENNR" "~"
if joinKuerzel = FIRAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = EKJFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = EKJ_FIRMENNR" "~"
if joinKuerzel = FIREKJ
searchResult := searchResult "LEFT JOIN PRODUKTION.EKJ_ERP_KOREJOURNAL ON EKJ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PROFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = PRO_GEHOERTZU_FIRMENNR" "~"
if joinKuerzel = FIRPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_GEHOERTZU_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AWSFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AWS_KTO_FIRMENNR" "~"
if joinKuerzel = FIRAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_KTO_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = EXPFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = EXP_FKT_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = EXP_FIRMENKATEGORIEKZ AND FKT_FIRMENKATEGORIELFDNR = EXP_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = FKTEXP
searchResult := searchResult "LEFT JOIN BUERO.EXP_EXTERNPERSONKEY ON EXP_FKT_FIRMENNR = FKT_FIRMENNR AND EXP_FIRMENKATEGORIEKZ = FKT_FIRMENKATEGORIEKZ AND EXP_FIRMENKATEGORIELFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = HVGFKT
searchResult := searchResult "LEFT JOIN FKT_FIRMENKATEGORIE.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = HVG_FKT_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = HVG_FIRMENKATEGORIEKZ AND FKT_FIRMENKATEGORIELFDNR = HVG_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = FKTHVG
searchResult := searchResult "LEFT JOIN ZLE.HVG_HONVORGRUPPE ON HVG_FKT_FIRMENNR = FKT_FIRMENNR AND HVG_FIRMENKATEGORIEKZ = FKT_FIRMENKATEGORIEKZ AND HVG_FIRMENKATEGORIELFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = IMMFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = IMM_FKT_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = IMM_FKT_FIRMENKATEGORIEKZ AND FKT_FIRMENKATEGORIELFDNR = IMM_FKT_FIRMENKATEGORIELFD" "~"
if joinKuerzel = FKTIMM
searchResult := searchResult "LEFT JOIN BUERO.IMM_INSERATMEDIUMMASTER ON IMM_FKT_FIRMENNR = FKT_FIRMENNR AND IMM_FKT_FIRMENKATEGORIEKZ = FKT_FIRMENKATEGORIEKZ AND IMM_FKT_FIRMENKATEGORIELFD = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = PERFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = PER_FKT_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = PER_FIRMENKATEGORIEKZ AND FKT_FIRMENKATEGORIELFDNR = PER_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = FKTPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FKT_FIRMENNR = FKT_FIRMENNR AND PER_FIRMENKATEGORIEKZ = FKT_FIRMENKATEGORIEKZ AND PER_FIRMENKATEGORIELFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = PICFKT
searchResult := searchResult "LEFT JOIN FKT_FIRMENKATEGORIE.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = PIC_FKT_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = PIC_FKT_FIRMENKATKZ AND FKT_FIRMENKATEGORIELFDNR = PIC_FKT_FIRMENKATLFDNR" "~"
if joinKuerzel = FKTPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_FKT_FIRMENNR = FKT_FIRMENNR AND PIC_FKT_FIRMENKATKZ = FKT_FIRMENKATEGORIEKZ AND PIC_FKT_FIRMENKATLFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = VASFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = VAS_AVKATEGORIE_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = VAS_AVKATEGORIE_KZ AND FKT_FIRMENKATEGORIELFDNR = VAS_AVKATEGORIE_LFDNR" "~"
if joinKuerzel = FKTVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_AVKATEGORIE_FIRMENNR = FKT_FIRMENNR AND VAS_AVKATEGORIE_KZ = FKT_FIRMENKATEGORIEKZ AND VAS_AVKATEGORIE_LFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = AIVFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = AIV_ITV_MAKRO_SAVE" "~"
if joinKuerzel = FORAIV
searchResult := searchResult "LEFT JOIN WWS.AIV_INTERVALL_ARBEITSPLAN ON AIV_ITV_MAKRO_SAVE = FOR_FORMELNR" "~"
if joinKuerzel = BTDFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = BTD_FORMELNR" "~"
if joinKuerzel = FORBTD
searchResult := searchResult "LEFT JOIN BUERO.BTD_BTERMSATZARTDETAIL ON BTD_FORMELNR = FOR_FORMELNR" "~"
if joinKuerzel = PKDFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = PKD_QUELLE_MAKRO" "~"
if joinKuerzel = FORPKD
searchResult := searchResult "LEFT JOIN PRODUKTION.PKD_KM_DETAIL ON PKD_QUELLE_MAKRO = FOR_FORMELNR" "~"
if joinKuerzel = AWEFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = AWE_MAKRO_AGG" "~"
if joinKuerzel = FORAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_MAKRO_AGG = FOR_FORMELNR" "~"
if joinKuerzel = VOBFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = VOB_MAKRO_EVENT" "~"
if joinKuerzel = FORVOB
searchResult := searchResult "LEFT JOIN PRODUKTION.VOB_VORGANGSBENACHR ON VOB_MAKRO_EVENT = FOR_FORMELNR" "~"
if joinKuerzel = ZVTFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = ZVT_FORMELNR_ENDE" "~"
if joinKuerzel = FORZVT
searchResult := searchResult "LEFT JOIN BUERO.ZVT_TBDATEIEN ON ZVT_FORMELNR_ENDE = FOR_FORMELNR" "~"
if joinKuerzel = ZVTFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = ZVT_FORMELNR_PROT_KOPF" "~"
if joinKuerzel = FORZVT
searchResult := searchResult "LEFT JOIN BUERO.ZVT_TBDATEIEN ON ZVT_FORMELNR_PROT_KOPF = FOR_FORMELNR" "~"
if joinKuerzel = WAGFOT
searchResult := searchResult "LEFT JOIN FOT_FOTO.FOT_FOTO ON FOT_FIRMENNR = WAG_FIRMENNR AND FOT_FOTONR = WAG_FOTONR" "~"
if joinKuerzel = FOTWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = FOT_FIRMENNR AND WAG_FOTONR = FOT_FOTONR" "~"
if joinKuerzel = PZKFPK
searchResult := searchResult "LEFT JOIN WWSINDIV.FPK_PROZENTSATZKONTEN ON FPK_FPKID = PZK_FPKID" "~"
if joinKuerzel = FPKPZK
searchResult := searchResult "LEFT JOIN WWSINDIV.PZK_PROZKTOZU ON PZK_FPKID = FPK_FPKID" "~"
if joinKuerzel = BMOFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = BMO_FIRMENNR AND FWF_FIBUNR = BMO_FIBUNR" "~"
if joinKuerzel = FWFBMO
searchResult := searchResult "LEFT JOIN FIBU.BMO_BUDGETMODELL ON BMO_FIRMENNR = FWF_FIRMENNR AND BMO_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = BTKFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = BTK_FIRMENNR AND FWF_FIBUNR = BTK_FIBUNR" "~"
if joinKuerzel = FWFBTK
searchResult := searchResult "LEFT JOIN FIBU.BTK_BUCHUNGSTEXTKUERZEL ON BTK_FIRMENNR = FWF_FIRMENNR AND BTK_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = BUDFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = BUD_FIRMENNR AND FWF_FIBUNR = BUD_FIBUNR" "~"
if joinKuerzel = FWFBUD
searchResult := searchResult "LEFT JOIN FIBU.BUD_BUDGET ON BUD_FIRMENNR = FWF_FIRMENNR AND BUD_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = BUSFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = BUS_FIRMENNR AND FWF_FIBUNR = BUS_FIBUNR" "~"
if joinKuerzel = FWFBUS
searchResult := searchResult "LEFT JOIN FIBU.BUS_BUCHUNGSSYMBOL ON BUS_FIRMENNR = FWF_FIRMENNR AND BUS_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = DKPFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = DKP_FIRMENNR AND FWF_FIBUNR = DKP_FIBUNR" "~"
if joinKuerzel = FWFDKP
searchResult := searchResult "LEFT JOIN FIBU.DKP_DOKUMENTENPOOL ON DKP_FIRMENNR = FWF_FIRMENNR AND DKP_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = GRSFST
searchResult := searchResult "LEFT JOIN BUERO.FST_FRIST ON FST_FRISTLFDNR = GRS_FRISTLFDNR" "~"
if joinKuerzel = FSTGRS
searchResult := searchResult "LEFT JOIN BUERO.GRS_GELDRISIKO ON GRS_FRISTLFDNR = FST_FRISTLFDNR" "~"
if joinKuerzel = MABFST
searchResult := searchResult "LEFT JOIN BUERO.FST_FRIST ON FST_FRISTLFDNR = MAB_FRISTNR" "~"
if joinKuerzel = FSTMAB
searchResult := searchResult "LEFT JOIN BUERO.MAB_MITARBAUSBILDUNG ON MAB_FRISTNR = FST_FRISTLFDNR" "~"
if joinKuerzel = POSFST
searchResult := searchResult "LEFT JOIN BUERO.FST_FRIST ON FST_FRISTLFDNR = POS_FRISTLFDNR" "~"
if joinKuerzel = FSTPOS
searchResult := searchResult "LEFT JOIN BUERO.POS_POST ON POS_FRISTLFDNR = FST_FRISTLFDNR" "~"
if joinKuerzel = ALDFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = ALD_FIRMENNR AND FWF_FIBUNR = ALD_FIBUNR" "~"
if joinKuerzel = FWFALD
searchResult := searchResult "LEFT JOIN ZX_GELOESCHT.ALD_ANLAGENDETAIL ON ALD_FIRMENNR = FWF_FIRMENNR AND ALD_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = AWKFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = AWK_FIRMENNR AND FWF_FIBUNR = AWK_FIBUNR" "~"
if joinKuerzel = FWFAWK
searchResult := searchResult "LEFT JOIN BIL.AWK_AUSWERTUNGKOPF ON AWK_FIRMENNR = FWF_FIRMENNR AND AWK_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = BRTFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = BRT_FWF_FIRMENNR AND FWF_FIBUNR = BRT_FIBUNR" "~"
if joinKuerzel = FWFBRT
searchResult := searchResult "LEFT JOIN BUERO.BRT_BERICHT ON BRT_FWF_FIRMENNR = FWF_FIRMENNR AND BRT_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = BUPFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = BUP_FIRMENNR AND FWF_FIBUNR = BUP_FIBUNR" "~"
if joinKuerzel = FWFBUP
searchResult := searchResult "LEFT JOIN FIBU.BUP_BUERFIMPORTPARAMETER ON BUP_FIRMENNR = FWF_FIRMENNR AND BUP_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = DOFFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = DOF_FIRMENNR AND FWF_FIBUNR = DOF_FIBUNR" "~"
if joinKuerzel = FWFDOF
searchResult := searchResult "LEFT JOIN FIBU.DOF_FIBUDOKUMENT ON DOF_FIRMENNR = FWF_FIRMENNR AND DOF_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = ERBFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = ERB_FIRMENNR AND FWF_FIBUNR = ERB_FIBUNR" "~"
if joinKuerzel = FWFERB
searchResult := searchResult "LEFT JOIN FIBU.ERB_BUCHUNG ON ERB_FIRMENNR = FWF_FIRMENNR AND ERB_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = LRKFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = LRK_FIRMENNR AND FWF_FIBUNR = LRK_FIBUNR" "~"
if joinKuerzel = FWFLRK
searchResult := searchResult "LEFT JOIN BIL.LRK_LANGFRISTIGE_RSTKONTEN ON LRK_FIRMENNR = FWF_FIRMENNR AND LRK_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = LWPFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = LWP_FIRMENNR AND FWF_FIBUNR = LWP_FIBUNR" "~"
if joinKuerzel = FWFLWP
searchResult := searchResult "LEFT JOIN FIBU.LWP_LANDWIRT_PRODUKTE ON LWP_FIRMENNR = FWF_FIRMENNR AND LWP_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = NFZFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = NFZ_FWF_FIRMENNR AND FWF_FIBUNR = NFZ_FWF_FIBUNR" "~"
if joinKuerzel = FWFNFZ
searchResult := searchResult "LEFT JOIN BIL.NFZ_ANBUFIBUZU ON NFZ_FWF_FIRMENNR = FWF_FIRMENNR AND NFZ_FWF_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = SLBFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = SLB_FIRMENNR AND FWF_FIBUNR = SLB_FIBUNR" "~"
if joinKuerzel = FWFSLB
searchResult := searchResult "LEFT JOIN BIL.SLB_SALDENBESTAETIGUNG ON SLB_FIRMENNR = FWF_FIRMENNR AND SLB_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = UVWFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = UVW_FIRMENNR AND FWF_FIBUNR = UVW_FIBUNR" "~"
if joinKuerzel = FWFUVW
searchResult := searchResult "LEFT JOIN FIBU.UVW_UVAWERT ON UVW_FIRMENNR = FWF_FIRMENNR AND UVW_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = WSSFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = WSS_FIRMENNR AND FWF_FIBUNR = WSS_FIBUNR" "~"
if joinKuerzel = FWFWSS
searchResult := searchResult "LEFT JOIN WWS.WSS_WWSSTATISTIK ON WSS_FIRMENNR = FWF_FIRMENNR AND WSS_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = ZKRFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = ZKR_FIRMENNR AND FWF_FIBUNR = ZKR_FIBUNR" "~"
if joinKuerzel = FWFZKR
searchResult := searchResult "LEFT JOIN FIBU.ZKR_ZAHLUNGSKREIS ON ZKR_FIRMENNR = FWF_FIRMENNR AND ZKR_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = KSKFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = KSK_FIRMENNR AND FWF_FIBUNR = KSK_AUFKONTO_FIBUNR" "~"
if joinKuerzel = FWFKSK
searchResult := searchResult "LEFT JOIN KORE.KSK_KORESCHLUESSELKOPF ON KSK_FIRMENNR = FWF_FIRMENNR AND KSK_AUFKONTO_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = RNGGEA
searchResult := searchResult "LEFT JOIN GEA_GESCHAEFTSART.GEA_GESCHAEFTSART ON GEA_FIRMENNR = RNG_GEA_FIRMENNR AND GEA_GEACODE = RNG_GEACODE" "~"
if joinKuerzel = GEARNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_GEA_FIRMENNR = GEA_FIRMENNR AND RNG_GEACODE = GEA_GEACODE" "~"
if joinKuerzel = FIWGER
searchResult := searchResult "LEFT JOIN BUERO.GER_GERICHT ON GER_LANDNR = FIW_GER_LANDNR AND GER_GERICHTSNR = FIW_GER_GERICHTSNR" "~"
if joinKuerzel = GERFIW
searchResult := searchResult "LEFT JOIN BUERO.FIW_FIRMENWERTE ON FIW_GER_LANDNR = GER_LANDNR AND FIW_GER_GERICHTSNR = GER_GERICHTSNR" "~"
if joinKuerzel = PVFGKK
searchResult := searchResult "LEFT JOIN BUERO.GKK_KRANKENKASSA ON GKK_LANDNR = PVF_GKK_LANDNR AND GKK_GKKNR = PVF_GKKNR" "~"
if joinKuerzel = GKKPVF
searchResult := searchResult "LEFT JOIN BUERO.PVF_PERSONENVORERFASSUNG ON PVF_GKK_LANDNR = GKK_LANDNR AND PVF_GKKNR = GKK_GKKNR" "~"
if joinKuerzel = GOHGOH
searchResult := searchResult "LEFT JOIN ZLE.GOH_GRUPPENORDNER_HNPOS ON GOH_GOHBAUMID = GOH_GOHBAUMID_PARENT AND GOH_GOHNR = GOH_GOHNR_PARENT" "~"
if joinKuerzel = GOHGOH
searchResult := searchResult "LEFT JOIN ZLE.GOH_GRUPPENORDNER_HNPOS ON GOH_GOHBAUMID_PARENT = GOH_GOHBAUMID AND GOH_GOHNR_PARENT = GOH_GOHNR" "~"
if joinKuerzel = ASTFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AST_FIRMENNR" "~"
if joinKuerzel = FIRAST
searchResult := searchResult "LEFT JOIN WWS.AST_ARTIKELSTATISTIK ON AST_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = DEZFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = DEZ_FIRMENNR" "~"
if joinKuerzel = FIRDEZ
searchResult := searchResult "LEFT JOIN DATANORM.DEZ_DATANORM_ERSTELLERZU ON DEZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = DISFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = DIS_FIRMENNR" "~"
if joinKuerzel = FIRDIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ERAFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ERA_FIRMENNR" "~"
if joinKuerzel = FIRERA
searchResult := searchResult "LEFT JOIN WWS.ERA_ERFASSRABATT ON ERA_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = GRTFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = GRT_FIRMENNR" "~"
if joinKuerzel = FIRGRT
searchResult := searchResult "LEFT JOIN WWS.GRT_GROESSENTAB ON GRT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = LMBFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LMB_FIRMENNR" "~"
if joinKuerzel = FIRLMB
searchResult := searchResult "LEFT JOIN WWS.LMB_LAGERMINMAXBESTAND ON LMB_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = WTBFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = WTB_FIRMENNR" "~"
if joinKuerzel = FIRWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ERZFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ERZ_FIRMENNR" "~"
if joinKuerzel = FIRERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FFAFIW
searchResult := searchResult "LEFT JOIN BUERO.FIW_FIRMENWERTE ON FIW_FIRMENNR = FFA_FIRMENNR AND FIW_FIRMAGUELTIGJAHR = FFA_FIRMAGUELTIGJAHR" "~"
if joinKuerzel = FIWFFA
searchResult := searchResult "LEFT JOIN BUERO.FFA_FIRMENFINANZAMTZU ON FFA_FIRMENNR = FIW_FIRMENNR AND FFA_FIRMAGUELTIGJAHR = FIW_FIRMAGUELTIGJAHR" "~"
if joinKuerzel = BSGFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = BSG_FKT_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = BSG_FIRMENKATEGORIEKZ AND FKT_FIRMENKATEGORIELFDNR = BSG_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = FKTBSG
searchResult := searchResult "LEFT JOIN BUERO.BSG_BESPRECHUNG ON BSG_FKT_FIRMENNR = FKT_FIRMENNR AND BSG_FIRMENKATEGORIEKZ = FKT_FIRMENKATEGORIEKZ AND BSG_FIRMENKATEGORIELFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = FGEFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = FGE_FKT_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = FGE_FIRMENKATEGORIEKZ AND FKT_FIRMENKATEGORIELFDNR = FGE_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = FKTFGE
searchResult := searchResult "LEFT JOIN BUERO.FGE_FRAGE ON FGE_FKT_FIRMENNR = FKT_FIRMENNR AND FGE_FIRMENKATEGORIEKZ = FKT_FIRMENKATEGORIEKZ AND FGE_FIRMENKATEGORIELFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = MAGFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = MAG_FKT_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = MAG_FIRMENKATEGORIEKZ AND FKT_FIRMENKATEGORIELFDNR = MAG_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = FKTMAG
searchResult := searchResult "LEFT JOIN BUERO.MAG_MITARBGESPR ON MAG_FKT_FIRMENNR = FKT_FIRMENNR AND MAG_FIRMENKATEGORIEKZ = FKT_FIRMENKATEGORIEKZ AND MAG_FIRMENKATEGORIELFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = UMFFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = UMF_FKT_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = UMF_FIRMENKATEGORIEKZ AND FKT_FIRMENKATEGORIELFDNR = UMF_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = FKTUMF
searchResult := searchResult "LEFT JOIN BUERO.UMF_UMFRAGE ON UMF_FKT_FIRMENNR = FKT_FIRMENNR AND UMF_FIRMENKATEGORIEKZ = FKT_FIRMENKATEGORIEKZ AND UMF_FIRMENKATEGORIELFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = VAKFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = VAK_AVKATEGORIE_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = VAK_AVKATEGORIEKZ AND FKT_FIRMENKATEGORIELFDNR = VAK_AVKATEGORIELFDNR" "~"
if joinKuerzel = FKTVAK
searchResult := searchResult "LEFT JOIN BUERO.VAK_VERANSTALTUNGSKOPF ON VAK_AVKATEGORIE_FIRMENNR = FKT_FIRMENNR AND VAK_AVKATEGORIEKZ = FKT_FIRMENKATEGORIEKZ AND VAK_AVKATEGORIELFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = WAMFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = WAM_ANMGRUND_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = WAM_ANMGRUND_KATKZ AND FKT_FIRMENKATEGORIELFDNR = WAM_ANMGRUND_KATLFDNR" "~"
if joinKuerzel = FKTWAM
searchResult := searchResult "LEFT JOIN BUERO.WAM_WEBANMELDUNG ON WAM_ANMGRUND_FIRMENNR = FKT_FIRMENNR AND WAM_ANMGRUND_KATKZ = FKT_FIRMENKATEGORIEKZ AND WAM_ANMGRUND_KATLFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = AFPFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = AFP_MAKRO_FORMELNR" "~"
if joinKuerzel = FORAFP
searchResult := searchResult "LEFT JOIN ZLE.AFP_EXTFILTERPOS ON AFP_MAKRO_FORMELNR = FOR_FORMELNR" "~"
if joinKuerzel = AVGFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = AVG_FOR_FORMELNR" "~"
if joinKuerzel = FORAVG
searchResult := searchResult "LEFT JOIN WWS.AVG_AUFTRAGSVORERFGRUPP ON AVG_FOR_FORMELNR = FOR_FORMELNR" "~"
if joinKuerzel = BVOFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = BVO_REICHWEITE_FORMELNR" "~"
if joinKuerzel = FORBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_REICHWEITE_FORMELNR = FOR_FORMELNR" "~"
if joinKuerzel = CMZFOR
searchResult := searchResult "LEFT JOIN BMD.FOR_FORMEL ON FOR_FORMELNR = CMZ_FORMELNR" "~"
if joinKuerzel = FORCMZ
searchResult := searchResult "LEFT JOIN BMD.CMZ_CMDZEILEN ON CMZ_FORMELNR = FOR_FORMELNR" "~"
if joinKuerzel = SCGFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = SCG_MAKRO_BEWERT" "~"
if joinKuerzel = FORSCG
searchResult := searchResult "LEFT JOIN PRODUKTION.SCG_SCORECARDKRITZU ON SCG_MAKRO_BEWERT = FOR_FORMELNR" "~"
if joinKuerzel = ZFIFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = ZFI_FORMELNR" "~"
if joinKuerzel = FORZFI
searchResult := searchResult "LEFT JOIN BUERO.ZFI_ZEITFEHLERIGNORE ON ZFI_FORMELNR = FOR_FORMELNR" "~"
if joinKuerzel = SSKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = SSK_FIRMENNR" "~"
if joinKuerzel = FIRSSK
searchResult := searchResult "LEFT JOIN PRODUKTION.SSK_SAMMELSCHEIN_KOPF ON SSK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = STKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = STK_FIRMENNR" "~"
if joinKuerzel = FIRSTK
searchResult := searchResult "LEFT JOIN ZLE.STK_STAFFELKOPF ON STK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = STZFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = STZ_FIRMENNR" "~"
if joinKuerzel = FIRSTZ
searchResult := searchResult "LEFT JOIN WWS.STZ_STEUERTEXTZUORDNUNG ON STZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = TOUFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = TOU_FIRMENNR" "~"
if joinKuerzel = FIRTOU
searchResult := searchResult "LEFT JOIN WWS.TOU_TOUR ON TOU_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = VEKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = VEK_FIRMENNR" "~"
if joinKuerzel = FIRVEK
searchResult := searchResult "LEFT JOIN WWS.VEK_VERTRETERABRECHKOPF ON VEK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = VLJFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = VLJ_FIRMENNR" "~"
if joinKuerzel = FIRVLJ
searchResult := searchResult "LEFT JOIN WWS.VLJ_VERLEIHJOURNAL ON VLJ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = VMZFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = VMZ_LFS_FIRMENNR" "~"
if joinKuerzel = FIRVMZ
searchResult := searchResult "LEFT JOIN WWS.VMZ_VERSMGR_LFS_ZUORD ON VMZ_LFS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = VWAFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = VWA_FIRMENNR" "~"
if joinKuerzel = FIRVWA
searchResult := searchResult "LEFT JOIN BUERO.VWA_VORSCHLAGSWERTEARCHIV ON VWA_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = WBSFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = WBS_FIRMENNR" "~"
if joinKuerzel = FIRWBS
searchResult := searchResult "LEFT JOIN WWS.WBS_WWSBERICHT_SNAPSHOT ON WBS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = WBWFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = WBW_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRWBW
searchResult := searchResult "LEFT JOIN WWS.WBW_WWSBUDGETWERTE ON WBW_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = WRKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = WRK_FIRMENNR" "~"
if joinKuerzel = FIRWRK
searchResult := searchResult "LEFT JOIN WWS.WRK_WWSANKAUFSRNGKOPF ON WRK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = XSEFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = XSE_FIR_STB_FIRMENNR" "~"
if joinKuerzel = FIRXSE
searchResult := searchResult "LEFT JOIN BUERO.XSE_XML_STEUERERKL ON XSE_FIR_STB_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ZLAFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ZLA_LIZENZ_FIRMENNR" "~"
if joinKuerzel = FIRZLA
searchResult := searchResult "LEFT JOIN ZLE.ZLA_ZEILAB ON ZLA_LIZENZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ZPKFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = ZPK_FIRMENNR" "~"
if joinKuerzel = FIRZPK
searchResult := searchResult "LEFT JOIN BUERO.ZPK_ZEITPROFILKOPF ON ZPK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BTMFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = BTM_FILIALE_FIRMENNR" "~"
if joinKuerzel = FIRBTM
searchResult := searchResult "LEFT JOIN BUERO.BTM_BTERMINAL ON BTM_FILIALE_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = KAJFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = KAJ_FILIALNR" "~"
if joinKuerzel = FIRKAJ
searchResult := searchResult "LEFT JOIN WWS.KAJ_KASSENJOURNAL ON KAJ_FILIALNR = FIR_FIRMENNR" "~"
if joinKuerzel = PERFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = PER_ENTSPRICHT_FIRMENNR" "~"
if joinKuerzel = FIRPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_ENTSPRICHT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AKTFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AKT_FIRMENNR" "~"
if joinKuerzel = FIRAKT
searchResult := searchResult "LEFT JOIN WWS.AKT_AKTIONSSTAMM ON AKT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AVEFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AVE_FIRMENNR" "~"
if joinKuerzel = FIRAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BSIFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = BSI_FIRMENNR" "~"
if joinKuerzel = FIRBSI
searchResult := searchResult "LEFT JOIN WWSINDIV.BSI_BONUSSTATISTIK ON BSI_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = CHGFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = CHG_FIRMENNR" "~"
if joinKuerzel = FIRCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = LAAFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LAA_FIRMENNR" "~"
if joinKuerzel = FIRLAA
searchResult := searchResult "LEFT JOIN WWS.LAA_LAGERART ON LAA_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = LSGFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LSG_FIRMENNR" "~"
if joinKuerzel = FIRLSG
searchResult := searchResult "LEFT JOIN WWS.LSG_LIEFERSCHEINGRUPPEN ON LSG_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PARFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PAR_FIRMENNR" "~"
if joinKuerzel = FIRPAR
searchResult := searchResult "LEFT JOIN WWS_NEU.PAR_PHYS ON PAR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SLKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = SLK_FIRMENNR" "~"
if joinKuerzel = FIRSLK
searchResult := searchResult "LEFT JOIN WWS.SLK_STUECKLISTENKOPF ON SLK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SLPFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = SLP_FIRMENNR" "~"
if joinKuerzel = FIRSLP
searchResult := searchResult "LEFT JOIN WWS.SLP_STUECKLISTENPOSITION ON SLP_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FGRFIW
searchResult := searchResult "LEFT JOIN BUERO.FIW_FIRMENWERTE ON FIW_FIRMENNR = FGR_FIRMENNR AND FIW_FIRMAGUELTIGJAHR = FGR_FIRMAGUELTIGJAHR" "~"
if joinKuerzel = FIWFGR
searchResult := searchResult "LEFT JOIN BUERO.FGR_FIRMENGERICHTZU ON FGR_FIRMENNR = FIW_FIRMENNR AND FGR_FIRMAGUELTIGJAHR = FIW_FIRMAGUELTIGJAHR" "~"
if joinKuerzel = AKKFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = AKK_FKT_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = AKK_FIRMENKATEGORIEKZ AND FKT_FIRMENKATEGORIELFDNR = AKK_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = FKTAKK
searchResult := searchResult "LEFT JOIN BUERO.AKK_AKTIONSTATUSKOPF ON AKK_FKT_FIRMENNR = FKT_FIRMENNR AND AKK_FIRMENKATEGORIEKZ = FKT_FIRMENKATEGORIEKZ AND AKK_FIRMENKATEGORIELFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = EVNFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = EVN_ANMGRUND_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = EVN_ANMGRUND_KATKZ AND FKT_FIRMENKATEGORIELFDNR = EVN_ANMGRUND_KATLFDNR" "~"
if joinKuerzel = FKTEVN
searchResult := searchResult "LEFT JOIN BUERO.EVN_TEILNEHMER ON EVN_ANMGRUND_FIRMENNR = FKT_FIRMENNR AND EVN_ANMGRUND_KATKZ = FKT_FIRMENKATEGORIEKZ AND EVN_ANMGRUND_KATLFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = EVVFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = EVV_FKT_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = EVV_FIRMENKATEGORIEKZ AND FKT_FIRMENKATEGORIELFDNR = EVV_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = FKTEVV
searchResult := searchResult "LEFT JOIN BUERO.EVV_EVENTVORTRAGZU ON EVV_FKT_FIRMENNR = FKT_FIRMENNR AND EVV_FIRMENKATEGORIEKZ = FKT_FIRMENKATEGORIEKZ AND EVV_FIRMENKATEGORIELFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = STMFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = STM_FKT_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = STM_FKT_FIRMENKATKZ AND FKT_FIRMENKATEGORIELFDNR = STM_FKT_FIRMENKATLFDNR" "~"
if joinKuerzel = FKTSTM
searchResult := searchResult "LEFT JOIN BUERO.STM_STELLENMERKMALE ON STM_FKT_FIRMENNR = FKT_FIRMENNR AND STM_FKT_FIRMENKATKZ = FKT_FIRMENKATEGORIEKZ AND STM_FKT_FIRMENKATLFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = AWEFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = AWE_MAKRO" "~"
if joinKuerzel = FORAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_MAKRO = FOR_FORMELNR" "~"
if joinKuerzel = BKZFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = BKZ_FORMELNR" "~"
if joinKuerzel = FORBKZ
searchResult := searchResult "LEFT JOIN BIL.BKZ_BILANZKENNZAHL ON BKZ_FORMELNR = FOR_FORMELNR" "~"
if joinKuerzel = BMLFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = BML_MAKRO_AUFBEREITUNG" "~"
if joinKuerzel = FORBML
searchResult := searchResult "LEFT JOIN FIBU.BML_BANKRVMODEL ON BML_MAKRO_AUFBEREITUNG = FOR_FORMELNR" "~"
if joinKuerzel = CAWFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = CAW_AUG_FORMELNR" "~"
if joinKuerzel = FORCAW
searchResult := searchResult "LEFT JOIN BUERO.CAW_CRMABWESENHEIT ON CAW_AUG_FORMELNR = FOR_FORMELNR" "~"
if joinKuerzel = IMSFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = IMS_MACRO_AFTER_READ" "~"
if joinKuerzel = FORIMS
searchResult := searchResult "LEFT JOIN HRS.IMS_IMPORTSCHNITTSTELLEN ON IMS_MACRO_AFTER_READ = FOR_FORMELNR" "~"
if joinKuerzel = KIMFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = KIM_FORMELNR" "~"
if joinKuerzel = FORKIM
searchResult := searchResult "LEFT JOIN FIBU.KIM_KONTENCSVIMPORTMODELL ON KIM_FORMELNR = FOR_FORMELNR" "~"
if joinKuerzel = SCEFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = SCE_MAKRO_BEWERT" "~"
if joinKuerzel = FORSCE
searchResult := searchResult "LEFT JOIN PRODUKTION.SCE_SCORECARD ON SCE_MAKRO_BEWERT = FOR_FORMELNR" "~"
if joinKuerzel = SPXFOR
searchResult := searchResult "LEFT JOIN BMD.FOR_FORMEL ON FOR_FORMELNR = SPX_VORSCHLAGSWERTMAKRO" "~"
if joinKuerzel = FORSPX
searchResult := searchResult "LEFT JOIN BMD.SPX_STAPELVARIABLE ON SPX_VORSCHLAGSWERTMAKRO = FOR_FORMELNR" "~"
if joinKuerzel = WWMFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = WWM_FORMELNR" "~"
if joinKuerzel = FORWWM
searchResult := searchResult "LEFT JOIN WWS.WWM_WWSMAKROZUORD ON WWM_FORMELNR = FOR_FORMELNR" "~"
if joinKuerzel = BIMFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = BIM_EXP_MAKROID" "~"
if joinKuerzel = FORBIM
searchResult := searchResult "LEFT JOIN FIBU.BIM_BUERFIMPORTMODELL ON BIM_EXP_MAKROID = FOR_FORMELNR" "~"
if joinKuerzel = CAWFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = CAW_FRI_FORMELNR" "~"
if joinKuerzel = FORCAW
searchResult := searchResult "LEFT JOIN BUERO.CAW_CRMABWESENHEIT ON CAW_FRI_FORMELNR = FOR_FORMELNR" "~"
if joinKuerzel = AGUFOT
searchResult := searchResult "LEFT JOIN FOT_FOTO.FOT_FOTO ON FOT_FIRMENNR = AGU_FOT_FIRMENNR AND FOT_FOTONR = AGU_FOTONR" "~"
if joinKuerzel = FOTAGU
searchResult := searchResult "LEFT JOIN BIL.AGU_ANLAGEGUT ON AGU_FOT_FIRMENNR = FOT_FIRMENNR AND AGU_FOTONR = FOT_FOTONR" "~"
if joinKuerzel = ARLFOT
searchResult := searchResult "LEFT JOIN FOT_FOTO.FOT_FOTO ON FOT_FIRMENNR = ARL_FIRMENNR AND FOT_FOTONR = ARL_FOTONR" "~"
if joinKuerzel = FOTARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = FOT_FIRMENNR AND ARL_FOTONR = FOT_FOTONR" "~"
if joinKuerzel = FIRFOT
searchResult := searchResult "LEFT JOIN BUERO.FOT_FOTO ON FOT_FIRMENNR = FIR_LOGOKLEIN_FIRMENNR AND FOT_FOTONR = FIR_LOGOKLEIN_FOTONR" "~"
if joinKuerzel = FOTFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_LOGOKLEIN_FIRMENNR = FOT_FIRMENNR AND FIR_LOGOKLEIN_FOTONR = FOT_FOTONR" "~"
if joinKuerzel = SKOFPR
searchResult := searchResult "LEFT JOIN FIBU.FPR_FIBUPROZENTSATZ ON FPR_FIRMENNR = SKO_FIRMENNR AND FPR_FIBUNR = SKO_FIBUNR AND FPR_LANDNR = SKO_LANDNR AND FPR_PROZENTSATZ = SKO_PROZENTSATZ" "~"
if joinKuerzel = FPRSKO
searchResult := searchResult "LEFT JOIN FIBU.SKO_STEUERKONTEN ON SKO_FIRMENNR = FPR_FIRMENNR AND SKO_FIBUNR = FPR_FIBUNR AND SKO_LANDNR = FPR_LANDNR AND SKO_PROZENTSATZ = FPR_PROZENTSATZ" "~"
if joinKuerzel = YASFPR
searchResult := searchResult "LEFT JOIN FIBU.FPR_FIBUPROZENTSATZ ON FPR_FIRMENNR = YAS_FIRMENNR AND FPR_FIBUNR = YAS_FIBUNR AND FPR_LANDNR = YAS_LANDNR AND FPR_PROZENTSATZ = YAS_PROZENTSATZ" "~"
if joinKuerzel = FPRYAS
searchResult := searchResult "LEFT JOIN FIBU.YAS_AUTOBUCHAUFTEILSATZ ON YAS_FIRMENNR = FPR_FIRMENNR AND YAS_FIBUNR = FPR_FIBUNR AND YAS_LANDNR = FPR_LANDNR AND YAS_PROZENTSATZ = FPR_PROZENTSATZ" "~"
if joinKuerzel = ISZFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = ISZ_FKT_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = ISZ_FKT_FIRMENKATKZ AND FKT_FIRMENKATEGORIELFDNR = ISZ_FKT_FIRMENKATLFDNR" "~"
if joinKuerzel = FKTISZ
searchResult := searchResult "LEFT JOIN BUERO.ISZ_INFOTABSTELLEZU ON ISZ_FKT_FIRMENNR = FKT_FIRMENNR AND ISZ_FKT_FIRMENKATKZ = FKT_FIRMENKATEGORIEKZ AND ISZ_FKT_FIRMENKATLFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = ASOFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = ASO_FORMELNR" "~"
if joinKuerzel = FORASO
searchResult := searchResult "LEFT JOIN ZLE.ASO_SORTIERPOS ON ASO_FORMELNR = FOR_FORMELNR" "~"
if joinKuerzel = DAMFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = DAM_FORMELNR" "~"
if joinKuerzel = FORDAM
searchResult := searchResult "LEFT JOIN DATANORM.DAM_DATANORMARTIKELMATCH ON DAM_FORMELNR = FOR_FORMELNR" "~"
if joinKuerzel = IPDFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = IPD_MACROGUID" "~"
if joinKuerzel = FORIPD
searchResult := searchResult "LEFT JOIN HRS.IPD_INDIVPROGD ON IPD_MACROGUID = FOR_FORMELNR" "~"
if joinKuerzel = KSEFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = KSE_BASISFORMELNR" "~"
if joinKuerzel = FORKSE
searchResult := searchResult "LEFT JOIN KORE.KSE_KORESCHLUESSELEINGABE ON KSE_BASISFORMELNR = FOR_FORMELNR" "~"
if joinKuerzel = MAKFOR
searchResult := searchResult "LEFT JOIN BMD.FOR_FORMEL ON FOR_FORMELNR = MAK_FORMELNR" "~"
if joinKuerzel = FORMAK
searchResult := searchResult "LEFT JOIN BMD.MAK_MAKROFUNKTIONSZU ON MAK_FORMELNR = FOR_FORMELNR" "~"
if joinKuerzel = SPTFOR
searchResult := searchResult "LEFT JOIN BMD.FOR_FORMEL ON FOR_FORMELNR = SPT_MAKRO_DAVOR" "~"
if joinKuerzel = FORSPT
searchResult := searchResult "LEFT JOIN BMD.SPT_STAPELSCHRITT ON SPT_MAKRO_DAVOR = FOR_FORMELNR" "~"
if joinKuerzel = SCAFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = SCA_MAKRO_INPUT" "~"
if joinKuerzel = FORSCA
searchResult := searchResult "LEFT JOIN PRODUKTION.SCA_SCKRITERIUM ON SCA_MAKRO_INPUT = FOR_FORMELNR" "~"
if joinKuerzel = MEDFOT
searchResult := searchResult "LEFT JOIN BUERO.FOT_FOTO ON FOT_FIRMENNR = MED_FOT_FIRMENNR AND FOT_FOTONR = MED_FOTONR" "~"
if joinKuerzel = FOTMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_FOT_FIRMENNR = FOT_FIRMENNR AND MED_FOTONR = FOT_FOTONR" "~"
if joinKuerzel = PERFOT
searchResult := searchResult "LEFT JOIN BUERO.FOT_FOTO ON FOT_FIRMENNR = PER_FOT_FIRMENNR AND FOT_FOTONR = PER_FOTONR" "~"
if joinKuerzel = FOTPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FOT_FIRMENNR = FOT_FIRMENNR AND PER_FOTONR = FOT_FOTONR" "~"
if joinKuerzel = PVFFOT
searchResult := searchResult "LEFT JOIN BUERO.FOT_FOTO ON FOT_FIRMENNR = PVF_FOT_FIRMENNR AND FOT_FOTONR = PVF_FOT_FOTONR" "~"
if joinKuerzel = FOTPVF
searchResult := searchResult "LEFT JOIN BUERO.PVF_PERSONENVORERFASSUNG ON PVF_FOT_FIRMENNR = FOT_FIRMENNR AND PVF_FOT_FOTONR = FOT_FOTONR" "~"
if joinKuerzel = BUCFPR
searchResult := searchResult "LEFT JOIN FIBU.FPR_FIBUPROZENTSATZ ON FPR_FIRMENNR = BUC_FIRMENNR AND FPR_FIBUNR = BUC_FIBUNR AND FPR_LANDNR = BUC_LAN_LANDNR AND FPR_PROZENTSATZ = BUC_FPR_PROZENTSATZ" "~"
if joinKuerzel = FPRBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_FIRMENNR = FPR_FIRMENNR AND BUC_FIBUNR = FPR_FIBUNR AND BUC_LAN_LANDNR = FPR_LANDNR AND BUC_FPR_PROZENTSATZ = FPR_PROZENTSATZ" "~"
if joinKuerzel = VEUFPR
searchResult := searchResult "LEFT JOIN FIBU.FPR_FIBUPROZENTSATZ ON FPR_FIRMENNR = VEU_FIRMENNR AND FPR_FIBUNR = VEU_FIBUNR AND FPR_LANDNR = VEU_LANDNR AND FPR_PROZENTSATZ = VEU_PROZENTSATZ" "~"
if joinKuerzel = FPRVEU
searchResult := searchResult "LEFT JOIN FIBU.VEU_VORERFASSPROZAUFTEIL ON VEU_FIRMENNR = FPR_FIRMENNR AND VEU_FIBUNR = FPR_FIBUNR AND VEU_LANDNR = FPR_LANDNR AND VEU_PROZENTSATZ = FPR_PROZENTSATZ" "~"
if joinKuerzel = FRADKT
searchResult := searchResult "LEFT JOIN BUERO.DKT_DOCSKATEGORIE ON DKT_ARCHIVNR = FRA_DKT_ARCHIVNR AND DKT_DOCSKATEGORIEKZ = FRA_DKT_DOCSKATEGORIEKZ AND DKT_DOCSKATEGORIELFDNR = FRA_DKT_DOCSKATEGORIELFDNR" "~"
if joinKuerzel = DKTFRA
searchResult := searchResult "LEFT JOIN BUERO.FRA_FRISTART ON FRA_DKT_ARCHIVNR = DKT_ARCHIVNR AND FRA_DKT_DOCSKATEGORIEKZ = DKT_DOCSKATEGORIEKZ AND FRA_DKT_DOCSKATEGORIELFDNR = DKT_DOCSKATEGORIELFDNR" "~"
if joinKuerzel = BPBFRB
searchResult := searchResult "LEFT JOIN WWS.FRB_FARBE ON FRB_FIRMENNR = BPB_FIRMENNR AND FRB_FARBENNR = BPB_FARBENNR" "~"
if joinKuerzel = FRBBPB
searchResult := searchResult "LEFT JOIN WWS.BPB_LIEFBESTAET ON BPB_FIRMENNR = FRB_FIRMENNR AND BPB_FARBENNR = FRB_FARBENNR" "~"
if joinKuerzel = FTPFRB
searchResult := searchResult "LEFT JOIN WWS.FRB_FARBE ON FRB_FIRMENNR = FTP_FAT_FIRMENNR AND FRB_FARBENNR = FTP_FARBENNR" "~"
if joinKuerzel = FRBFTP
searchResult := searchResult "LEFT JOIN WWS.FTP_FARBENTABPOS ON FTP_FAT_FIRMENNR = FRB_FIRMENNR AND FTP_FARBENNR = FRB_FARBENNR" "~"
if joinKuerzel = WAGFRB
searchResult := searchResult "LEFT JOIN WWS.FRB_FARBE ON FRB_FIRMENNR = WAG_VORG_FRB_FIRMENNR AND FRB_FARBENNR = WAG_VORG_FARBENNR" "~"
if joinKuerzel = FRBWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_VORG_FRB_FIRMENNR = FRB_FIRMENNR AND WAG_VORG_FARBENNR = FRB_FARBENNR" "~"
if joinKuerzel = QUOFST
searchResult := searchResult "LEFT JOIN BUERO.FST_FRIST ON FST_FRISTLFDNR = QUO_FST_FRISTLFDNR" "~"
if joinKuerzel = FSTQUO
searchResult := searchResult "LEFT JOIN BUERO.QUO_QUOTE ON QUO_FST_FRISTLFDNR = FST_FRISTLFDNR" "~"
if joinKuerzel = BFZFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = BFZ_FIRMENNR AND FWF_FIBUNR = BFZ_FIBUNR" "~"
if joinKuerzel = FWFBFZ
searchResult := searchResult "LEFT JOIN BIL.BFZ_XMLJABBETFIRZU ON BFZ_FIRMENNR = FWF_FIRMENNR AND BFZ_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = CCEFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = CCE_FWF_FIRMENNR AND FWF_FIBUNR = CCE_FWF_FIBUNR" "~"
if joinKuerzel = FWFCCE
searchResult := searchResult "LEFT JOIN BIL.CCE_CBCENTITY ON CCE_FWF_FIRMENNR = FWF_FIRMENNR AND CCE_FWF_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = FKEFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = FKE_FIRMENNR AND FWF_FIBUNR = FKE_FIBUNR" "~"
if joinKuerzel = FWFFKE
searchResult := searchResult "LEFT JOIN FIBU.FKE_FIBUKOREERFASSUNG ON FKE_FIRMENNR = FWF_FIRMENNR AND FKE_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = KIMFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = KIM_FIRMENNR AND FWF_FIBUNR = KIM_FIBUNR" "~"
if joinKuerzel = FWFKIM
searchResult := searchResult "LEFT JOIN FIBU.KIM_KONTENCSVIMPORTMODELL ON KIM_FIRMENNR = FWF_FIRMENNR AND KIM_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = ZNSFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = ZNS_BENACHRICHT_FORMELNR" "~"
if joinKuerzel = FORZNS
searchResult := searchResult "LEFT JOIN BUERO.ZNS_ZEITNACHRICHTSYSTEM ON ZNS_BENACHRICHT_FORMELNR = FOR_FORMELNR" "~"
if joinKuerzel = BMLFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = BML_MAKRO_NACHBEARBEITUNG" "~"
if joinKuerzel = FORBML
searchResult := searchResult "LEFT JOIN FIBU.BML_BANKRVMODEL ON BML_MAKRO_NACHBEARBEITUNG = FOR_FORMELNR" "~"
if joinKuerzel = BMLFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = BML_MAKRO_BRVFELDER" "~"
if joinKuerzel = FORBML
searchResult := searchResult "LEFT JOIN FIBU.BML_BANKRVMODEL ON BML_MAKRO_BRVFELDER = FOR_FORMELNR" "~"
if joinKuerzel = DFZFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = DFZ_MACRO_F8_ID" "~"
if joinKuerzel = FORDFZ
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFZ_DFELDGRUPPENZU ON DFZ_MACRO_F8_ID = FOR_FORMELNR" "~"
if joinKuerzel = FEZFOR
searchResult := searchResult "LEFT JOIN BMD.FOR_FORMEL ON FOR_FORMELNR = FEZ_MACRO_F8_ID" "~"
if joinKuerzel = FORFEZ
searchResult := searchResult "LEFT JOIN BMD.FEZ_FELDGRUPPENZU ON FEZ_MACRO_F8_ID = FOR_FORMELNR" "~"
if joinKuerzel = BIMFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = BIM_AFTERIMPORT_MAKROID" "~"
if joinKuerzel = FORBIM
searchResult := searchResult "LEFT JOIN FIBU.BIM_BUERFIMPORTMODELL ON BIM_AFTERIMPORT_MAKROID = FOR_FORMELNR" "~"
if joinKuerzel = DFKBMF
searchResult := searchResult "LEFT JOIN BMF_BMDFORMATKATALOG.BMF_BMDFORMATKATALOG ON BMF_KATALOGNR = DFK_KATALOGNR" "~"
if joinKuerzel = BMFDFK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFK_DBMDFORMATKATALOG ON DFK_KATALOGNR = BMF_KATALOGNR" "~"
if joinKuerzel = OBPFOT
searchResult := searchResult "LEFT JOIN BUERO.FOT_FOTO ON FOT_FIRMENNR = OBP_FOT_FIRMENNR AND FOT_FOTONR = OBP_FOT_FOTONR" "~"
if joinKuerzel = FOTOBP
searchResult := searchResult "LEFT JOIN BUERO.OBP_ONLBEWPERSSTAMM ON OBP_FOT_FIRMENNR = FOT_FIRMENNR AND OBP_FOT_FOTONR = FOT_FOTONR" "~"
if joinKuerzel = SSCFPR
searchResult := searchResult "LEFT JOIN FIBU.FPR_FIBUPROZENTSATZ ON FPR_FIRMENNR = SSC_FIRMENNR AND FPR_FIBUNR = SSC_FIBUNR AND FPR_LANDNR = SSC_LANDNR AND FPR_PROZENTSATZ = SSC_FPR_PROZENTSATZ" "~"
if joinKuerzel = FPRSSC
searchResult := searchResult "LEFT JOIN FIBU.SSC_STEUERSCHLUESSEL ON SSC_FIRMENNR = FPR_FIRMENNR AND SSC_FIBUNR = FPR_FIBUNR AND SSC_LANDNR = FPR_LANDNR AND SSC_FPR_PROZENTSATZ = FPR_PROZENTSATZ" "~"
if joinKuerzel = WGDFPR
searchResult := searchResult "LEFT JOIN FIBU.FPR_FIBUPROZENTSATZ ON FPR_FIRMENNR = WGD_FIRMENNR AND FPR_FIBUNR = WGD_FIBUNR AND FPR_LANDNR = WGD_LANDNR AND FPR_PROZENTSATZ = WGD_STEUER_PROZENT" "~"
if joinKuerzel = FPRWGD
searchResult := searchResult "LEFT JOIN FIBU.WGD_WGBDETAIL ON WGD_FIRMENNR = FPR_FIRMENNR AND WGD_FIBUNR = FPR_FIBUNR AND WGD_LANDNR = FPR_LANDNR AND WGD_STEUER_PROZENT = FPR_PROZENTSATZ" "~"
if joinKuerzel = IFTFRA
searchResult := searchResult "LEFT JOIN BUERO.FRA_FRISTART ON FRA_FRISTARTLFDNR = IFT_FRA_FRISTARTLFDNR" "~"
if joinKuerzel = FRAIFT
searchResult := searchResult "LEFT JOIN BUERO.IFT_INFOTAB ON IFT_FRA_FRISTARTLFDNR = FRA_FRISTARTLFDNR" "~"
if joinKuerzel = POSFRA
searchResult := searchResult "LEFT JOIN BUERO.FRA_FRISTART ON FRA_FRISTARTLFDNR = POS_FRISTARTLFDNR" "~"
if joinKuerzel = FRAPOS
searchResult := searchResult "LEFT JOIN BUERO.POS_POST ON POS_FRISTARTLFDNR = FRA_FRISTARTLFDNR" "~"
if joinKuerzel = AIDFRB
searchResult := searchResult "LEFT JOIN WWS.FRB_FARBE ON FRB_FIRMENNR = AID_FRB_FIRMENNR AND FRB_FARBENNR = AID_ARTIKELIDFARBENNR" "~"
if joinKuerzel = FRBAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_FRB_FIRMENNR = FRB_FIRMENNR AND AID_ARTIKELIDFARBENNR = FRB_FARBENNR" "~"
if joinKuerzel = LAZFRB
searchResult := searchResult "LEFT JOIN WWS.FRB_FARBE ON FRB_FIRMENNR = LAZ_FRB_FIRMENNR AND FRB_FARBENNR = LAZ_FARBENNR" "~"
if joinKuerzel = FRBLAZ
searchResult := searchResult "LEFT JOIN WWS.LAZ_LIEFARTIKEL_ZUSATZ ON LAZ_FRB_FIRMENNR = FRB_FIRMENNR AND LAZ_FARBENNR = FRB_FARBENNR" "~"
if joinKuerzel = SAPFRT
searchResult := searchResult "LEFT JOIN FRT_FAKTURATYP.FRT_FAKTURATYP ON FRT_FIRMENNR = SAP_FRT_FIRMENNR AND FRT_FAKTURATYPNR = SAP_FAKTURATYPNR" "~"
if joinKuerzel = FRTSAP
searchResult := searchResult "LEFT JOIN BUERO.SAP_SAMMELPROJEKT ON SAP_FRT_FIRMENNR = FRT_FIRMENNR AND SAP_FAKTURATYPNR = FRT_FAKTURATYPNR" "~"
if joinKuerzel = BUCFST
searchResult := searchResult "LEFT JOIN FST_FRIST.FST_FRIST ON FST_FRISTLFDNR = BUC_FRISTLFDNR" "~"
if joinKuerzel = FSTBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_FRISTLFDNR = FST_FRISTLFDNR" "~"
if joinKuerzel = ACFFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = ACF_FIRMENNR AND FWF_FIBUNR = ACF_FIBUNR" "~"
if joinKuerzel = FWFACF
searchResult := searchResult "LEFT JOIN FIBU.ACF_TESTFALL ON ACF_FIRMENNR = FWF_FIRMENNR AND ACF_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = AJOFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = AJO_FIBUFIRMENNR AND FWF_FIBUNR = AJO_FWF_FIBUNR" "~"
if joinKuerzel = FWFAJO
searchResult := searchResult "LEFT JOIN BIL.AJO_ANBUJOURNAL ON AJO_FIBUFIRMENNR = FWF_FIRMENNR AND AJO_FWF_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = AMZFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = AMZ_FIBU_FIRMENNR AND FWF_FIBUNR = AMZ_FIBUNR" "~"
if joinKuerzel = FWFAMZ
searchResult := searchResult "LEFT JOIN BUERO.AMZ_AUSDRUCKMODELLZU ON AMZ_FIBU_FIRMENNR = FWF_FIRMENNR AND AMZ_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = AZZFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = AZZ_FIRMENNR AND FWF_FIBUNR = AZZ_FIBUNR" "~"
if joinKuerzel = FWFAZZ
searchResult := searchResult "LEFT JOIN FIBU.AZZ_AUSDRUCKZUSATZ ON AZZ_FIRMENNR = FWF_FIRMENNR AND AZZ_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = DBXFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = DBX_FIBU_FIRMENNR AND FWF_FIBUNR = DBX_FIBUNR" "~"
if joinKuerzel = FWFDBX
searchResult := searchResult "LEFT JOIN BUERO.DBX_FINONDATABOX ON DBX_FIBU_FIRMENNR = FWF_FIRMENNR AND DBX_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = FPKFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = FPK_FIRMENNR AND FWF_FIBUNR = FPK_FIBUNR" "~"
if joinKuerzel = FWFFPK
searchResult := searchResult "LEFT JOIN WWSINDIV.FPK_PROZENTSATZKONTEN ON FPK_FIRMENNR = FWF_FIRMENNR AND FPK_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = GOLGOH
searchResult := searchResult "LEFT JOIN ZLE.GOH_GRUPPENORDNER_HNPOS ON GOH_GOHBAUMID = GOL_GOHBAUMID AND GOH_GOHNR = GOL_GOHNR" "~"
if joinKuerzel = GOHGOL
searchResult := searchResult "LEFT JOIN ZLE.GOL_GRUPPENORDNER_LEISTUNG ON GOL_GOHBAUMID = GOH_GOHBAUMID AND GOL_GOHNR = GOH_GOHNR" "~"
if joinKuerzel = LAZGRO
searchResult := searchResult "LEFT JOIN WWS.GRO_GROESSE ON GRO_FIRMENNR = LAZ_GRO_FIRMENNR AND GRO_GROESSENKBEZ = LAZ_GROESSENKBEZ" "~"
if joinKuerzel = GROLAZ
searchResult := searchResult "LEFT JOIN WWS.LAZ_LIEFARTIKEL_ZUSATZ ON LAZ_GRO_FIRMENNR = GRO_FIRMENNR AND LAZ_GROESSENKBEZ = GRO_GROESSENKBEZ" "~"
if joinKuerzel = WAGGRO
searchResult := searchResult "LEFT JOIN WWS.GRO_GROESSE ON GRO_FIRMENNR = WAG_VORG_GRO_FIRMENNR AND GRO_GROESSENKBEZ = WAG_VORG_GROESSENKBEZ" "~"
if joinKuerzel = GROWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_VORG_GRO_FIRMENNR = GRO_FIRMENNR AND WAG_VORG_GROESSENKBEZ = GRO_GROESSENKBEZ" "~"
if joinKuerzel = HSKHAD
searchResult := searchResult "LEFT JOIN HRS.HAD_AUSDRUCKDETAIL ON HAD_LOHNAUSDRDETAILID = HSK_HAD_LOHNAUSDRDETAILID" "~"
if joinKuerzel = HADHSK
searchResult := searchResult "LEFT JOIN HRS.HSK_SORTIERKOPF ON HSK_HAD_LOHNAUSDRDETAILID = HAD_LOHNAUSDRDETAILID" "~"
if joinKuerzel = PAGHLB
searchResult := searchResult "LEFT JOIN HLB_HILFEBAUSTEIN.HLB_HILFEBAUSTEIN ON HLB_HLBID = PAG_HLBID" "~"
if joinKuerzel = HLBPAG
searchResult := searchResult "LEFT JOIN BMDDEFAULT.PAG_PARAMGRUPPE ON PAG_HLBID = HLB_HLBID" "~"
if joinKuerzel = BTFHLD
searchResult := searchResult "LEFT JOIN HLD_HILFEDOKUMENT.HLD_HILFEDOKUMENT ON HLD_HLDID = BTF_HLDID" "~"
if joinKuerzel = HLDBTF
searchResult := searchResult "LEFT JOIN BMD.BTF_BMDTABFELDBESCH ON BTF_HLDID = HLD_HLDID" "~"
if joinKuerzel = DFUHLD
searchResult := searchResult "LEFT JOIN HLD_HILFEDOKUMENT.HLD_HILFEDOKUMENT ON HLD_HLDID = DFU_HLDID_COM" "~"
if joinKuerzel = HLDDFU
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFU_DFUNKTION ON DFU_HLDID_COM = HLD_HLDID" "~"
if joinKuerzel = HSOHSK
searchResult := searchResult "LEFT JOIN HRS.HSK_SORTIERKOPF ON HSK_SORTKOPFSEQNR = HSO_SORTKOPFSEQNR" "~"
if joinKuerzel = HSKHSO
searchResult := searchResult "LEFT JOIN HRS.HSO_SORTIERPOS ON HSO_SORTKOPFSEQNR = HSK_SORTKOPFSEQNR" "~"
if joinKuerzel = ISZIFP
searchResult := searchResult "LEFT JOIN BUERO.IFP_INFOTABPOS ON IFP_INFOTABID = ISZ_INFOTABLFDNR AND IFP_INFOTABPOSID = ISZ_INFOTABPOSLFDNR" "~"
if joinKuerzel = IFPISZ
searchResult := searchResult "LEFT JOIN BUERO.ISZ_INFOTABSTELLEZU ON ISZ_INFOTABLFDNR = IFP_INFOTABID AND ISZ_INFOTABPOSLFDNR = IFP_INFOTABPOSID" "~"
if joinKuerzel = KIZIFP
searchResult := searchResult "LEFT JOIN BUERO.IFP_INFOTABPOS ON IFP_INFOTABID = KIZ_INFOTABID AND IFP_INFOTABPOSID = KIZ_INFOTABPOSID" "~"
if joinKuerzel = IFPKIZ
searchResult := searchResult "LEFT JOIN BUERO.KIZ_KUND_INFOTABPOSZU ON KIZ_INFOTABID = IFP_INFOTABID AND KIZ_INFOTABPOSID = IFP_INFOTABPOSID" "~"
if joinKuerzel = ONMIFP
searchResult := searchResult "LEFT JOIN IFP_INFOTABPOS.IFP_INFOTABPOS ON IFP_INFOTABID = ONM_INFOTABID AND IFP_INFOTABPOSID = ONM_INFOTABPOSID" "~"
if joinKuerzel = IFPONM
searchResult := searchResult "LEFT JOIN HRS.ONM_ONLINEBEW_MERKMALE ON ONM_INFOTABID = IFP_INFOTABID AND ONM_INFOTABPOSID = IFP_INFOTABPOSID" "~"
if joinKuerzel = AGIIFT
searchResult := searchResult "LEFT JOIN IFT_INFOTAB.IFT_INFOTAB ON IFT_INFOTABID = AGI_INFOTABID" "~"
if joinKuerzel = IFTAGI
searchResult := searchResult "LEFT JOIN WWS.AGI_AGPER_INFOZU ON AGI_INFOTABID = IFT_INFOTABID" "~"
if joinKuerzel = DUKULK
searchResult := searchResult "LEFT JOIN ULK_USERLINK.ULK_USERLINK ON ULK_LINKGUID = DUK_LINKGUID" "~"
if joinKuerzel = ULKDUK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DUK_DUSERLINK ON DUK_LINKGUID = ULK_LINKGUID" "~"
if joinKuerzel = DUFULF
searchResult := searchResult "LEFT JOIN ULF_USERLINKFIELD.ULF_USERLINKFIELD ON ULF_LINKGUID = DUF_LINKGUID AND ULF_BMDCONSTNAME = DUF_BMDCONSTNAME" "~"
if joinKuerzel = ULFDUF
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DUF_DUSERLINKFIELD ON DUF_LINKGUID = ULF_LINKGUID AND DUF_BMDCONSTNAME = ULF_BMDCONSTNAME" "~"
if joinKuerzel = DRKRKG
searchResult := searchResult "LEFT JOIN RKG_TABREPLGRUPPE.RKG_TABREPLGRUPPE ON RKG_RKGID = DRK_RKGID" "~"
if joinKuerzel = RKGDRK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DRK_DTABREPLGRUPPE ON DRK_RKGID = RKG_RKGID" "~"
if joinKuerzel = DOHOTH
searchResult := searchResult "LEFT JOIN OTH_OCRTEMPLATEHEADER.OTH_OCRTEMPLATEHEADER ON OTH_OCRID = DOH_OCRID" "~"
if joinKuerzel = OTHDOH
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DOH_OCRTEMPLATEHEADER ON DOH_OCRID = OTH_OCRID" "~"
if joinKuerzel = ADRINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_IART = ADR_IART AND INS_LANDNR = ADR_INS_LANDNR AND INS_INR = ADR_INR" "~"
if joinKuerzel = INSADR
searchResult := searchResult "LEFT JOIN BUERO.ADR_ADRESSE ON ADR_IART = INS_IART AND ADR_INS_LANDNR = INS_LANDNR AND ADR_INR = INS_INR" "~"
if joinKuerzel = LITINS
searchResult := searchResult "LEFT JOIN INS_INSTITUTION.INS_INSTITUTION ON INS_IART = LIT_INS_IART AND INS_LANDNR = LIT_INS_LANDNR AND INS_INR = LIT_INS_INR" "~"
if joinKuerzel = INSLIT
searchResult := searchResult "LEFT JOIN HRS.LIT_LOHNINSTITUTION ON LIT_INS_IART = INS_IART AND LIT_INS_LANDNR = INS_LANDNR AND LIT_INS_INR = INS_INR" "~"
if joinKuerzel = IOBIOG
searchResult := searchResult "LEFT JOIN BUERO.IOG_INSTANDHOBJEKTGRUPPE ON IOG_IOGID = IOB_IOGID" "~"
if joinKuerzel = IOGIOB
searchResult := searchResult "LEFT JOIN BUERO.IOB_INSTANDHOBJEKT ON IOB_IOGID = IOG_IOGID" "~"
if joinKuerzel = AIVITV
searchResult := searchResult "LEFT JOIN ITV_INTERVALL.ITV_INTERVALL ON ITV_INTERVALLID = AIV_ITV_INTERVALLID" "~"
if joinKuerzel = ITVAIV
searchResult := searchResult "LEFT JOIN WWS.AIV_INTERVALL_ARBEITSPLAN ON AIV_ITV_INTERVALLID = ITV_INTERVALLID" "~"
if joinKuerzel = SCHITV
searchResult := searchResult "LEFT JOIN BMD.ITV_INTERVALL ON ITV_INTERVALLID = SCH_INTERVALLID" "~"
if joinKuerzel = ITVSCH
searchResult := searchResult "LEFT JOIN BMD.SCH_SCHEDULE ON SCH_INTERVALLID = ITV_INTERVALLID" "~"
if joinKuerzel = BONKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_FIRMENNR = BON_FIRMENNR AND KAS_KASSENNR = BON_KASSENNR" "~"
if joinKuerzel = KASBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_FIRMENNR = KAS_FIRMENNR AND BON_KASSENNR = KAS_KASSENNR" "~"
if joinKuerzel = EAKFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = EAK_FIBU_FIRMENNR AND FWF_FIBUNR = EAK_FIBUNR" "~"
if joinKuerzel = FWFEAK
searchResult := searchResult "LEFT JOIN WWS.EAK_ERLOESABGRENZUNGSKOPF ON EAK_FIBU_FIRMENNR = FWF_FIRMENNR AND EAK_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = EXTFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = EXT_FIRMENNR AND FWF_FIBUNR = EXT_FIBUNR" "~"
if joinKuerzel = FWFEXT
searchResult := searchResult "LEFT JOIN FIBU.EXT_EXTJOURNAL ON EXT_FIRMENNR = FWF_FIRMENNR AND EXT_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = FADFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = FAD_FWF_FIRMENNR AND FWF_FIBUNR = FAD_FWF_FIBUNR" "~"
if joinKuerzel = FWFFAD
searchResult := searchResult "LEFT JOIN FIBU.FAD_AUSDRUCKDETAIL ON FAD_FWF_FIRMENNR = FWF_FIRMENNR AND FAD_FWF_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = FIFFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = FIF_FIRMENNR AND FWF_FIBUNR = FIF_FIBUNR" "~"
if joinKuerzel = FWFFIF
searchResult := searchResult "LEFT JOIN FIBU.FIF_FILIALEFIBU ON FIF_FIRMENNR = FWF_FIRMENNR AND FIF_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = FNSFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = FNS_FIRMENNR AND FWF_FIBUNR = FNS_FIBUNR" "~"
if joinKuerzel = FWFFNS
searchResult := searchResult "LEFT JOIN BIL.FNS_FINANZINSTRUMENTE ON FNS_FIRMENNR = FWF_FIRMENNR AND FNS_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = KEKFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = KEK_FWK_FIRMENNR AND FWF_FIBUNR = KEK_KOPF_KONTO_FIBUNR" "~"
if joinKuerzel = FWFKEK
searchResult := searchResult "LEFT JOIN KORE.KEK_ERFASSKOPF ON KEK_FWK_FIRMENNR = FWF_FIRMENNR AND KEK_KOPF_KONTO_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = LRSFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = LRS_FIRMENNR AND FWF_FIBUNR = LRS_FIBUNR" "~"
if joinKuerzel = FWFLRS
searchResult := searchResult "LEFT JOIN BIL.LRS_LANGFRISTIGE_RST ON LRS_FIRMENNR = FWF_FIRMENNR AND LRS_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = LSKFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = LSK_FIRMENNR AND FWF_FIBUNR = LSK_FIBUNR" "~"
if joinKuerzel = FWFLSK
searchResult := searchResult "LEFT JOIN BIL.LSK_LATENTESTEUERN_KONTEN ON LSK_FIRMENNR = FWF_FIRMENNR AND LSK_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = MAVFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = MAV_FIRMENNR AND FWF_FIBUNR = MAV_FIBUNR" "~"
if joinKuerzel = FWFMAV
searchResult := searchResult "LEFT JOIN FIBU.MAV_MAHNVZINSEN ON MAV_FIRMENNR = FWF_FIRMENNR AND MAV_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = MGBFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = MGB_FIRMENNR AND FWF_FIBUNR = MGB_FIBUNR" "~"
if joinKuerzel = FWFMGB
searchResult := searchResult "LEFT JOIN FIBU.MGB_MAHNGEBUEHR ON MGB_FIRMENNR = FWF_FIRMENNR AND MGB_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = MWKFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = MWK_FIRMENNR_FIBU AND FWF_FIBUNR = MWK_FIBUNR" "~"
if joinKuerzel = FWFMWK
searchResult := searchResult "LEFT JOIN BIL.MWK_MWRKOPF ON MWK_FIRMENNR_FIBU = FWF_FIRMENNR AND MWK_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = OPZFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = OPZ_FIRMENNR AND FWF_FIBUNR = OPZ_FIBUNR" "~"
if joinKuerzel = FWFOPZ
searchResult := searchResult "LEFT JOIN FIBU.OPZ_OPZUORDNUNG ON OPZ_FIRMENNR = FWF_FIRMENNR AND OPZ_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = SPNFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = SPN_FIRMENNR AND FWF_FIBUNR = SPN_FIBUNR" "~"
if joinKuerzel = FWFSPN
searchResult := searchResult "LEFT JOIN FIBU.SPN_SPENDE ON SPN_FIRMENNR = FWF_FIRMENNR AND SPN_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = STFFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = STF_FIRMENNR AND FWF_FIBUNR = STF_FIBUNR" "~"
if joinKuerzel = FWFSTF
searchResult := searchResult "LEFT JOIN FIBU.STF_FIBUSTEUERCODEZU ON STF_FIRMENNR = FWF_FIRMENNR AND STF_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = ZBSFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = ZBS_FIRMENNR AND FWF_FIBUNR = ZBS_FIBUNR" "~"
if joinKuerzel = FWFZBS
searchResult := searchResult "LEFT JOIN FIBU.ZBS_ZINSBERECHNUNG_STAMM ON ZBS_FIRMENNR = FWF_FIRMENNR AND ZBS_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = FZUFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = FZU_ZUGEORDNETE_FIRMENNR AND FWF_FIBUNR = FZU_ZUGEORDNETE_FIBUNR" "~"
if joinKuerzel = FWFFZU
searchResult := searchResult "LEFT JOIN FIBU.FZU_FIBUFIBUZU ON FZU_ZUGEORDNETE_FIRMENNR = FWF_FIRMENNR AND FZU_ZUGEORDNETE_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = KZZFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = KZZ_FIRMENNR AND FWK_KORENR = KZZ_KORENR" "~"
if joinKuerzel = FWKKZZ
searchResult := searchResult "LEFT JOIN KORE.KZZ_KSTKENNZAHLZU ON KZZ_FIRMENNR = FWK_FIRMENNR AND KZZ_KORENR = FWK_KORENR" "~"
if joinKuerzel = KZUFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = KZU_ZUGEORDNETE_FIRMENNR AND FWK_KORENR = KZU_ZUGEORDNETE_KORENR" "~"
if joinKuerzel = FWKKZU
searchResult := searchResult "LEFT JOIN KORE.KZU_KOREKOREZU ON KZU_ZUGEORDNETE_FIRMENNR = FWK_FIRMENNR AND KZU_ZUGEORDNETE_KORENR = FWK_KORENR" "~"
if joinKuerzel = BPKGEA
searchResult := searchResult "LEFT JOIN GEA_GESCHAEFTSART.GEA_GESCHAEFTSART ON GEA_FIRMENNR = BPK_GEA_FIRMENNR AND GEA_GEACODE = BPK_GEACODE" "~"
if joinKuerzel = GEABPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_GEA_FIRMENNR = GEA_FIRMENNR AND BPK_GEACODE = GEA_GEACODE" "~"
if joinKuerzel = LFSGEA
searchResult := searchResult "LEFT JOIN GEA_GESCHAEFTSART.GEA_GESCHAEFTSART ON GEA_FIRMENNR = LFS_GEA_FIRMENNR AND GEA_GEACODE = LFS_GEACODE" "~"
if joinKuerzel = GEALFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_GEA_FIRMENNR = GEA_FIRMENNR AND LFS_GEACODE = GEA_GEACODE" "~"
if joinKuerzel = WAKGEA
searchResult := searchResult "LEFT JOIN GEA_GESCHAEFTSART.GEA_GESCHAEFTSART ON GEA_FIRMENNR = WAK_GEA_FIRMENNR AND GEA_GEACODE = WAK_GEACODE" "~"
if joinKuerzel = GEAWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_GEA_FIRMENNR = GEA_FIRMENNR AND WAK_GEACODE = GEA_GEACODE" "~"
if joinKuerzel = BIMFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = BIM_EDIT_MAKROID" "~"
if joinKuerzel = FORBIM
searchResult := searchResult "LEFT JOIN FIBU.BIM_BUERFIMPORTMODELL ON BIM_EDIT_MAKROID = FOR_FORMELNR" "~"
if joinKuerzel = CAWFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = CAW_TER_FORMELNR" "~"
if joinKuerzel = FORCAW
searchResult := searchResult "LEFT JOIN BUERO.CAW_CRMABWESENHEIT ON CAW_TER_FORMELNR = FOR_FORMELNR" "~"
if joinKuerzel = SCGFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = SCG_MAKRO_INPUT" "~"
if joinKuerzel = FORSCG
searchResult := searchResult "LEFT JOIN PRODUKTION.SCG_SCORECARDKRITZU ON SCG_MAKRO_INPUT = FOR_FORMELNR" "~"
if joinKuerzel = SPTFOR
searchResult := searchResult "LEFT JOIN BMD.FOR_FORMEL ON FOR_FORMELNR = SPT_MAKRO_DANACH" "~"
if joinKuerzel = FORSPT
searchResult := searchResult "LEFT JOIN BMD.SPT_STAPELSCHRITT ON SPT_MAKRO_DANACH = FOR_FORMELNR" "~"
if joinKuerzel = ZVTFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = ZVT_FORMELNR_BEWEGUNG" "~"
if joinKuerzel = FORZVT
searchResult := searchResult "LEFT JOIN BUERO.ZVT_TBDATEIEN ON ZVT_FORMELNR_BEWEGUNG = FOR_FORMELNR" "~"
if joinKuerzel = EASFPR
searchResult := searchResult "LEFT JOIN FIBU.FPR_FIBUPROZENTSATZ ON FPR_FIRMENNR = EAS_FIRMENNR AND FPR_FIBUNR = EAS_FIBUNR AND FPR_LANDNR = EAS_LANDNR AND FPR_PROZENTSATZ = EAS_PROZENTSATZ" "~"
if joinKuerzel = FPREAS
searchResult := searchResult "LEFT JOIN FIBU.EAS_ERKONTOAUFTEILSATZ ON EAS_FIRMENNR = FPR_FIRMENNR AND EAS_FIBUNR = FPR_FIBUNR AND EAS_LANDNR = FPR_LANDNR AND EAS_PROZENTSATZ = FPR_PROZENTSATZ" "~"
if joinKuerzel = MAVFPR
searchResult := searchResult "LEFT JOIN FIBU.FPR_FIBUPROZENTSATZ ON FPR_FIRMENNR = MAV_FIRMENNR AND FPR_FIBUNR = MAV_FIBUNR AND FPR_LANDNR = MAV_LANDNR AND FPR_PROZENTSATZ = MAV_PROZENTSATZ" "~"
if joinKuerzel = FPRMAV
searchResult := searchResult "LEFT JOIN FIBU.MAV_MAHNVZINSEN ON MAV_FIRMENNR = FPR_FIRMENNR AND MAV_FIBUNR = FPR_FIBUNR AND MAV_LANDNR = FPR_LANDNR AND MAV_PROZENTSATZ = FPR_PROZENTSATZ" "~"
if joinKuerzel = UVWFPR
searchResult := searchResult "LEFT JOIN FIBU.FPR_FIBUPROZENTSATZ ON FPR_FIRMENNR = UVW_FIRMENNR AND FPR_FIBUNR = UVW_FIBUNR AND FPR_LANDNR = UVW_LAN_LANDNR AND FPR_PROZENTSATZ = UVW_FPR_PROZENTSATZ" "~"
if joinKuerzel = FPRUVW
searchResult := searchResult "LEFT JOIN FIBU.UVW_UVAWERT ON UVW_FIRMENNR = FPR_FIRMENNR AND UVW_FIBUNR = FPR_FIBUNR AND UVW_LAN_LANDNR = FPR_LANDNR AND UVW_FPR_PROZENTSATZ = FPR_PROZENTSATZ" "~"
if joinKuerzel = EANFRB
searchResult := searchResult "LEFT JOIN WWS.FRB_FARBE ON FRB_FIRMENNR = EAN_FRB_FIRMENNR AND FRB_FARBENNR = EAN_FARBENNR" "~"
if joinKuerzel = FRBEAN
searchResult := searchResult "LEFT JOIN WWS.EAN_EANNUMMERN ON EAN_FRB_FIRMENNR = FRB_FIRMENNR AND EAN_FARBENNR = FRB_FARBENNR" "~"
if joinKuerzel = PTPFRB
searchResult := searchResult "LEFT JOIN WWS.FRB_FARBE ON FRB_FIRMENNR = PTP_FRB_FIRMENNR AND FRB_FARBENNR = PTP_FARBENNR" "~"
if joinKuerzel = FRBPTP
searchResult := searchResult "LEFT JOIN WWS.PTP_PREISTABPOS ON PTP_FRB_FIRMENNR = FRB_FIRMENNR AND PTP_FARBENNR = FRB_FARBENNR" "~"
if joinKuerzel = FADFSK
searchResult := searchResult "LEFT JOIN FIBU.FSK_SORTIERKOPF ON FSK_FSORTKOPFNR = FAD_FSORTKOPFNR" "~"
if joinKuerzel = FSKFAD
searchResult := searchResult "LEFT JOIN FIBU.FAD_AUSDRUCKDETAIL ON FAD_FSORTKOPFNR = FSK_FSORTKOPFNR" "~"
if joinKuerzel = FSUFSO
searchResult := searchResult "LEFT JOIN FIBU.FSO_SORTIERPOS ON FSO_FSORTKOPFNR = FSU_SORTKOPFNR AND FSO_FSORTPOSNR = FSU_SORTPOSNR" "~"
if joinKuerzel = FSOFSU
searchResult := searchResult "LEFT JOIN FIBU.FSU_SORTSUMMENPOS ON FSU_SORTKOPFNR = FSO_FSORTKOPFNR AND FSU_SORTPOSNR = FSO_FSORTPOSNR" "~"
if joinKuerzel = BSNFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = BSN_FIRMENNR AND FWF_FIBUNR = BSN_FIBUNR" "~"
if joinKuerzel = FWFBSN
searchResult := searchResult "LEFT JOIN FIBU.BSN_BILANZSTEUERKONTEN ON BSN_FIRMENNR = FWF_FIRMENNR AND BSN_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = FIXFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = FIX_FIRMENNR AND FWF_FIBUNR = FIX_FIBUNR" "~"
if joinKuerzel = FWFFIX
searchResult := searchResult "LEFT JOIN FIBU.FIX_FIXKONTEN ON FIX_FIRMENNR = FWF_FIRMENNR AND FIX_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = FKZFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = FKZ_FIRMENNR AND FWF_FIBUNR = FKZ_FIBUNR" "~"
if joinKuerzel = FWFFKZ
searchResult := searchResult "LEFT JOIN FIBU.FKZ_FREIESKZ ON FKZ_FIRMENNR = FWF_FIRMENNR AND FKZ_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = GFZFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = GFZ_FIRMENNR AND FWF_FIBUNR = GFZ_FIBUNR" "~"
if joinKuerzel = FWFGFZ
searchResult := searchResult "LEFT JOIN BIL.GFZ_XMLJABGESFIRZU ON GFZ_FIRMENNR = FWF_FIRMENNR AND GFZ_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = GKOFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = GKO_FIRMENNR AND FWF_FIBUNR = GKO_FIBUNR" "~"
if joinKuerzel = FWFGKO
searchResult := searchResult "LEFT JOIN FIBU.GKO_GESELLSCHAFTERKONTEN ON GKO_FIRMENNR = FWF_FIRMENNR AND GKO_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = GLSFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = GLS_FIRMENNR AND FWF_FIBUNR = GLS_FIBUNR" "~"
if joinKuerzel = FWFGLS
searchResult := searchResult "LEFT JOIN BIL.GLS_GLIEDERUNGSSCHEMA ON GLS_FIRMENNR = FWF_FIRMENNR AND GLS_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = JABFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = JAB_FIRMENNR AND FWF_FIBUNR = JAB_FIBUNR" "~"
if joinKuerzel = FWFJAB
searchResult := searchResult "LEFT JOIN BIL.JAB_JABINFO ON JAB_FIRMENNR = FWF_FIRMENNR AND JAB_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = KOEFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = KOE_FIRMENNR AND FWF_FIBUNR = KOE_FIBUNR" "~"
if joinKuerzel = FWFKOE
searchResult := searchResult "LEFT JOIN ZX_GELOESCHT.KOE_KOSTENERFASSUNG ON KOE_FIRMENNR = FWF_FIRMENNR AND KOE_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = KTKFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = KTK_FIRMENNR AND FWF_FIBUNR = KTK_FIBUNR" "~"
if joinKuerzel = FWFKTK
searchResult := searchResult "LEFT JOIN FIBU.KTK_KONTENKREIS ON KTK_FIRMENNR = FWF_FIRMENNR AND KTK_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = WAGFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = WAG_FIRMENNR" "~"
if joinKuerzel = FIRWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = EFKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = EFK_SAMMELNR_FIRMENNR" "~"
if joinKuerzel = FIREFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_SAMMELNR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BRTFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = BRT_WWS_FIRMENNR" "~"
if joinKuerzel = FIRBRT
searchResult := searchResult "LEFT JOIN BUERO.BRT_BERICHT ON BRT_WWS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = LEIFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LEI_ORG_FIRMENNR" "~"
if joinKuerzel = FIRLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_ORG_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FIRFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = FIR_ORIGINAL_FIRMENNR" "~"
if joinKuerzel = FIRFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_ORIGINAL_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FBNFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = FBN_FKT_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = FBN_FIRMENKATEGORIEKZ AND FKT_FIRMENKATEGORIELFDNR = FBN_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = FKTFBN
searchResult := searchResult "LEFT JOIN BUERO.FBN_FRAGEBOGEN ON FBN_FKT_FIRMENNR = FKT_FIRMENNR AND FBN_FIRMENKATEGORIEKZ = FKT_FIRMENKATEGORIEKZ AND FBN_FIRMENKATEGORIELFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = PNKFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = PNK_FKT_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = PNK_FIRMENKATEGORIEKZ AND FKT_FIRMENKATEGORIELFDNR = PNK_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = FKTPNK
searchResult := searchResult "LEFT JOIN BUERO.PNK_PERSONNUMMERNKREIS ON PNK_FKT_FIRMENNR = FKT_FIRMENNR AND PNK_FIRMENKATEGORIEKZ = FKT_FIRMENKATEGORIEKZ AND PNK_FIRMENKATEGORIELFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = AMPFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = AMP_MAKRONR" "~"
if joinKuerzel = FORAMP
searchResult := searchResult "LEFT JOIN BUERO.AMP_AUTOMAILARCHIVPARAM ON AMP_MAKRONR = FOR_FORMELNR" "~"
if joinKuerzel = PLPFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = PLP_FOR_FORMELNR" "~"
if joinKuerzel = FORPLP
searchResult := searchResult "LEFT JOIN PRODUKTION.PLP_PLANUNGSPERIODE ON PLP_FOR_FORMELNR = FOR_FORMELNR" "~"
if joinKuerzel = SMIFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = SMI_MAKRO_FORMELNR" "~"
if joinKuerzel = FORSMI
searchResult := searchResult "LEFT JOIN ZLE.SMI_SMARTINTERFACE ON SMI_MAKRO_FORMELNR = FOR_FORMELNR" "~"
if joinKuerzel = VOBFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = VOB_MAKRO_TEST" "~"
if joinKuerzel = FORVOB
searchResult := searchResult "LEFT JOIN PRODUKTION.VOB_VORGANGSBENACHR ON VOB_MAKRO_TEST = FOR_FORMELNR" "~"
if joinKuerzel = FEZFOR
searchResult := searchResult "LEFT JOIN BMD.FOR_FORMEL ON FOR_FORMELNR = FEZ_MACRO_F4_ID" "~"
if joinKuerzel = FORFEZ
searchResult := searchResult "LEFT JOIN BMD.FEZ_FELDGRUPPENZU ON FEZ_MACRO_F4_ID = FOR_FORMELNR" "~"
if joinKuerzel = ZVTFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = ZVT_FORMELNR_SUMME" "~"
if joinKuerzel = FORZVT
searchResult := searchResult "LEFT JOIN BUERO.ZVT_TBDATEIEN ON ZVT_FORMELNR_SUMME = FOR_FORMELNR" "~"
if joinKuerzel = FIRFOT
searchResult := searchResult "LEFT JOIN BUERO.FOT_FOTO ON FOT_FIRMENNR = FIR_LOGOGROSS_FIRMENNR AND FOT_FOTONR = FIR_LOGOGROSS_FOTONR" "~"
if joinKuerzel = FOTFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_LOGOGROSS_FIRMENNR = FOT_FIRMENNR AND FIR_LOGOGROSS_FOTONR = FOT_FOTONR" "~"
if joinKuerzel = BASFPR
searchResult := searchResult "LEFT JOIN FIBU.FPR_FIBUPROZENTSATZ ON FPR_FIRMENNR = BAS_FIRMENNR AND FPR_FIBUNR = BAS_FIBUNR AND FPR_LANDNR = BAS_LANDNR AND FPR_PROZENTSATZ = BAS_PROZENTSATZ" "~"
if joinKuerzel = FPRBAS
searchResult := searchResult "LEFT JOIN FIBU.BAS_BUCAUFTEILUNGSSATZ ON BAS_FIRMENNR = FPR_FIRMENNR AND BAS_FIBUNR = FPR_FIBUNR AND BAS_LANDNR = FPR_LANDNR AND BAS_PROZENTSATZ = FPR_PROZENTSATZ" "~"
if joinKuerzel = YBUFPR
searchResult := searchResult "LEFT JOIN FIBU.FPR_FIBUPROZENTSATZ ON FPR_FIRMENNR = YBU_FIRMENNR AND FPR_FIBUNR = YBU_FIBUNR AND FPR_LANDNR = YBU_LAN_LANDNR AND FPR_PROZENTSATZ = YBU_FPR_PROZENTSATZ" "~"
if joinKuerzel = FPRYBU
searchResult := searchResult "LEFT JOIN FIBU.YBU_AUTOBUCHUNG ON YBU_FIRMENNR = FPR_FIRMENNR AND YBU_FIBUNR = FPR_FIBUNR AND YBU_LAN_LANDNR = FPR_LANDNR AND YBU_FPR_PROZENTSATZ = FPR_PROZENTSATZ" "~"
if joinKuerzel = PSJFPZ
searchResult := searchResult "LEFT JOIN HRS.FPZ_FIRMENPENSIONSKASSEZU ON FPZ_FIRMENNR = PSJ_DVFIRMENNR AND FPZ_JAHR = PSJ_LOHNJAHR AND FPZ_FPZLFDNR = PSJ_FPZLFDNR" "~"
if joinKuerzel = FPZPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_DVFIRMENNR = FPZ_FIRMENNR AND PSJ_LOHNJAHR = FPZ_JAHR AND PSJ_FPZLFDNR = FPZ_FPZLFDNR" "~"
if joinKuerzel = DKTFRA
searchResult := searchResult "LEFT JOIN BUERO.FRA_FRISTART ON FRA_FRISTARTLFDNR = DKT_FRISTARTLFDNR" "~"
if joinKuerzel = FRADKT
searchResult := searchResult "LEFT JOIN BUERO.DKT_DOCSKATEGORIE ON DKT_FRISTARTLFDNR = FRA_FRISTARTLFDNR" "~"
if joinKuerzel = PFRFRB
searchResult := searchResult "LEFT JOIN WWS.FRB_FARBE ON FRB_FIRMENNR = PFR_FRB_FIRMENNR AND FRB_FARBENNR = PFR_FRB_FARBENNR" "~"
if joinKuerzel = FRBPFR
searchResult := searchResult "LEFT JOIN WWS.PFR_PICFARBE ON PFR_FRB_FIRMENNR = FRB_FIRMENNR AND PFR_FRB_FARBENNR = FRB_FARBENNR" "~"
if joinKuerzel = RAPFRB
searchResult := searchResult "LEFT JOIN WWS.FRB_FARBE ON FRB_FIRMENNR = RAP_FRB_FIRMENNR AND FRB_FARBENNR = RAP_FARBENNR" "~"
if joinKuerzel = FRBRAP
searchResult := searchResult "LEFT JOIN WWS.RAP_RABATTTABPOS ON RAP_FRB_FIRMENNR = FRB_FIRMENNR AND RAP_FARBENNR = FRB_FARBENNR" "~"
if joinKuerzel = RERFRB
searchResult := searchResult "LEFT JOIN WWS.FRB_FARBE ON FRB_FIRMENNR = RER_FARBE_FIRMENNR AND FRB_FARBENNR = RER_FARBENNR" "~"
if joinKuerzel = FRBRER
searchResult := searchResult "LEFT JOIN WWS.RER_RESERVIERUNGSREFERENZ ON RER_FARBE_FIRMENNR = FRB_FIRMENNR AND RER_FARBENNR = FRB_FARBENNR" "~"
if joinKuerzel = BETFST
searchResult := searchResult "LEFT JOIN BUERO.FST_FRIST ON FST_FRISTLFDNR = BET_FST_FRISTLFDNR" "~"
if joinKuerzel = FSTBET
searchResult := searchResult "LEFT JOIN BUERO.BET_BETRIEBSMITTEL ON BET_FST_FRISTLFDNR = FST_FRISTLFDNR" "~"
if joinKuerzel = PKHFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = PKH_FIBU_FIRMENNR AND FWF_FIBUNR = PKH_FIBUNR" "~"
if joinKuerzel = FWFPKH
searchResult := searchResult "LEFT JOIN BIL.PKH_PERSKORSTHEADER ON PKH_FIBU_FIRMENNR = FWF_FIRMENNR AND PKH_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = RAEFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = RAE_FIRMENNR AND FWF_FIBUNR = RAE_FIBUNR" "~"
if joinKuerzel = FWFRAE
searchResult := searchResult "LEFT JOIN FIBU.RAE_RATE ON RAE_FIRMENNR = FWF_FIRMENNR AND RAE_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = SSCFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = SSC_FIRMENNR AND FWF_FIBUNR = SSC_FIBUNR" "~"
if joinKuerzel = FWFSSC
searchResult := searchResult "LEFT JOIN FIBU.SSC_STEUERSCHLUESSEL ON SSC_FIRMENNR = FWF_FIRMENNR AND SSC_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = SVKFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = SVK_FIRMENNR AND FWF_FIBUNR = SVK_FIBUNR" "~"
if joinKuerzel = FWFSVK
searchResult := searchResult "LEFT JOIN FIBU.SVK_SKONTOVERBUCHKZ ON SVK_FIRMENNR = FWF_FIRMENNR AND SVK_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = VBVFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = VBV_FIRMENNR AND FWF_FIBUNR = VBV_FIBUNR" "~"
if joinKuerzel = FWFVBV
searchResult := searchResult "LEFT JOIN FIBU.VBV_VORERFASSBUCH ON VBV_FIRMENNR = FWF_FIRMENNR AND VBV_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = WEBFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = WEB_FWF_FIRMENNR AND FWF_FIBUNR = WEB_FIBUNR" "~"
if joinKuerzel = FWFWEB
searchResult := searchResult "LEFT JOIN WWS.WEB_WARENEINSATZBUCH ON WEB_FWF_FIRMENNR = FWF_FIRMENNR AND WEB_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = WGBFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = WGB_FIRMENNR AND FWF_FIBUNR = WGB_FIBUNR" "~"
if joinKuerzel = FWFWGB
searchResult := searchResult "LEFT JOIN FIBU.WGB_WARENEINGANGSBUCH ON WGB_FIRMENNR = FWF_FIRMENNR AND WGB_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = XSEFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = XSE_FWF_FIRMENNR AND FWF_FIBUNR = XSE_FWF_FIBUNR" "~"
if joinKuerzel = FWFXSE
searchResult := searchResult "LEFT JOIN BUERO.XSE_XML_STEUERERKL ON XSE_FWF_FIRMENNR = FWF_FIRMENNR AND XSE_FWF_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = ZBRFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = ZBR_FIRMENNR AND FWF_FIBUNR = ZBR_FIBUNR" "~"
if joinKuerzel = FWFZBR
searchResult := searchResult "LEFT JOIN FIBU.ZBR_ZINSBERECHNUNG ON ZBR_FIRMENNR = FWF_FIRMENNR AND ZBR_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = ZKSFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = ZKS_FIRMENNR AND FWF_FIBUNR = ZKS_FIBUNR" "~"
if joinKuerzel = FWFZKS
searchResult := searchResult "LEFT JOIN FIBU.ZKS_ZINSBERECHNUNGSKREIS ON ZKS_FIRMENNR = FWF_FIRMENNR AND ZKS_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = FKVFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = FKV_ZIEL_FIRMENNR AND FWF_FIBUNR = FKV_ZIEL_FIBUNR" "~"
if joinKuerzel = FWFFKV
searchResult := searchResult "LEFT JOIN FIBU.FKV_KONTENVERTEILUNG ON FKV_ZIEL_FIRMENNR = FWF_FIRMENNR AND FKV_ZIEL_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = KSTFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = KST_FIRMENNR AND FWK_KORENR = KST_KORENR" "~"
if joinKuerzel = FWKKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = FWK_FIRMENNR AND KST_KORENR = FWK_KORENR" "~"
if joinKuerzel = AMOFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = AMO_FIRMENNR AND FWK_KORENR = AMO_KORENR" "~"
if joinKuerzel = FWKAMO
searchResult := searchResult "LEFT JOIN KORE.AMO_AUSWERTUNGMODELL ON AMO_FIRMENNR = FWK_FIRMENNR AND AMO_KORENR = FWK_KORENR" "~"
if joinKuerzel = WSSFWK
searchResult := searchResult "LEFT JOIN FWK_FIRMENWERTEKORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = WSS_KST_FIRMENNR AND FWK_KORENR = WSS_KST_KORENR" "~"
if joinKuerzel = FWKWSS
searchResult := searchResult "LEFT JOIN WWS.WSS_WWSSTATISTIK ON WSS_KST_FIRMENNR = FWK_FIRMENNR AND WSS_KST_KORENR = FWK_KORENR" "~"
if joinKuerzel = FWKFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = FWK_VORJAHR_FIRMENNR AND FWK_KORENR = FWK_VORJAHR_KORENR" "~"
if joinKuerzel = FWKFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_VORJAHR_FIRMENNR = FWK_FIRMENNR AND FWK_VORJAHR_KORENR = FWK_KORENR" "~"
if joinKuerzel = KVGFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = KVG_ZIEL_FIRMENNR AND FWK_KORENR = KVG_ZIEL_KORENR" "~"
if joinKuerzel = FWKKVG
searchResult := searchResult "LEFT JOIN KORE.KVG_KORESTAMMVERTEILUNG ON KVG_ZIEL_FIRMENNR = FWK_FIRMENNR AND KVG_ZIEL_KORENR = FWK_KORENR" "~"
if joinKuerzel = BZUFZU
searchResult := searchResult "LEFT JOIN FIBU.FZU_FIBUFIBUZU ON FZU_FIRMENNR = BZU_FIRMENNR AND FZU_FIBUNR = BZU_FIBUNR AND FZU_ZUGEORDNETE_FIRMENNR = BZU_ZUGEORDNETE_FIRMENNR AND FZU_ZUGEORDNETE_FIBUNR = BZU_ZUGEORDNETE_FIBUNR" "~"
if joinKuerzel = FZUBZU
searchResult := searchResult "LEFT JOIN FIBU.BZU_BUARTKONSZU ON BZU_FIRMENNR = FZU_FIRMENNR AND BZU_FIBUNR = FZU_FIBUNR AND BZU_ZUGEORDNETE_FIRMENNR = FZU_ZUGEORDNETE_FIRMENNR AND BZU_ZUGEORDNETE_FIBUNR = FZU_ZUGEORDNETE_FIBUNR" "~"
if joinKuerzel = ARLGEG
searchResult := searchResult "LEFT JOIN GEG_GEFAHRENGUT.GEG_GEFAHRENGUT ON GEG_FIRMENNR = ARL_FIRMENNR AND GEG_GEFAHRENGUTNR = ARL_GEFAHRENGUTNR" "~"
if joinKuerzel = GEGARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = GEG_FIRMENNR AND ARL_GEFAHRENGUTNR = GEG_GEFAHRENGUTNR" "~"
if joinKuerzel = PGRGER
searchResult := searchResult "LEFT JOIN BUERO.GER_GERICHT ON GER_LANDNR = PGR_LANDNR AND GER_GERICHTSNR = PGR_GERICHTSNR" "~"
if joinKuerzel = GERPGR
searchResult := searchResult "LEFT JOIN BUERO.PGR_PERSONGERICHTZU ON PGR_LANDNR = GER_LANDNR AND PGR_GERICHTSNR = GER_GERICHTSNR" "~"
if joinKuerzel = PRPGPO
searchResult := searchResult "LEFT JOIN GPO_GRUPPENORDNER.GPO_GRUPPENORDNER ON GPO_BAUMID = PRP_VERTR_BAUMID AND GPO_GRUPPENORDNERNR = PRP_VERTR_GRUPPENORDNERNR" "~"
if joinKuerzel = GPOPRP
searchResult := searchResult "LEFT JOIN WWS.PRP_PROVISIONTABPOS ON PRP_VERTR_BAUMID = GPO_BAUMID AND PRP_VERTR_GRUPPENORDNERNR = GPO_GRUPPENORDNERNR" "~"
if joinKuerzel = FSOFSK
searchResult := searchResult "LEFT JOIN FIBU.FSK_SORTIERKOPF ON FSK_FSORTKOPFNR = FSO_FSORTKOPFNR" "~"
if joinKuerzel = FSKFSO
searchResult := searchResult "LEFT JOIN FIBU.FSO_SORTIERPOS ON FSO_FSORTKOPFNR = FSK_FSORTKOPFNR" "~"
if joinKuerzel = FADFSP
searchResult := searchResult "LEFT JOIN FIBU.FSP_SPALTENKOPF ON FSP_FIBUSPALTENKOPFID = FAD_FSP_FIBUSPALTENKOPFID" "~"
if joinKuerzel = FSPFAD
searchResult := searchResult "LEFT JOIN FIBU.FAD_AUSDRUCKDETAIL ON FAD_FSP_FIBUSPALTENKOPFID = FSP_FIBUSPALTENKOPFID" "~"
if joinKuerzel = KJOFST
searchResult := searchResult "LEFT JOIN BUERO.FST_FRIST ON FST_FRISTLFDNR = KJO_FRISTLFDNR" "~"
if joinKuerzel = FSTKJO
searchResult := searchResult "LEFT JOIN BUERO.KJO_KONTAKTJOURNAL ON KJO_FRISTLFDNR = FST_FRISTLFDNR" "~"
if joinKuerzel = BEIFTB
searchResult := searchResult "LEFT JOIN BUERO.FTB_FAHRTENBUCH ON FTB_FAHRTENBUCHID = BEI_FAHRTENBUCHID" "~"
if joinKuerzel = FTBBEI
searchResult := searchResult "LEFT JOIN BUERO.BEI_BEIFAHRER ON BEI_FAHRTENBUCHID = FTB_FAHRTENBUCHID" "~"
if joinKuerzel = AKVFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = AKV_FIRMENNR AND FWF_FIBUNR = AKV_FIBUNR" "~"
if joinKuerzel = FWFAKV
searchResult := searchResult "LEFT JOIN FIBU.AKV_AUTOKONTONR_VERGABE ON AKV_FIRMENNR = FWF_FIRMENNR AND AKV_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = BKRFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = BKR_FIRMENNR AND FWF_FIBUNR = BKR_FIBUNR" "~"
if joinKuerzel = FWFBKR
searchResult := searchResult "LEFT JOIN FIBU.BKR_BANKEINZUGSKREIS ON BKR_FIRMENNR = FWF_FIRMENNR AND BKR_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = BRHFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = BRH_FIRMENNR AND FWF_FIBUNR = BRH_FIBUNR" "~"
if joinKuerzel = FWFBRH
searchResult := searchResult "LEFT JOIN FIBU.BRH_BANKRVKOPF ON BRH_FIRMENNR = FWF_FIRMENNR AND BRH_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = BUCFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = BUC_FIRMENNR AND FWF_FIBUNR = BUC_FIBUNR" "~"
if joinKuerzel = FWFBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_FIRMENNR = FWF_FIRMENNR AND BUC_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = FBAFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = FBA_FIRMENNR AND FWF_FIBUNR = FBA_FIBUNR" "~"
if joinKuerzel = FWFFBA
searchResult := searchResult "LEFT JOIN BIL.FBA_XMLJABFBANHANG ON FBA_FIRMENNR = FWF_FIRMENNR AND FBA_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = FZUFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = FZU_FIRMENNR AND FWF_FIBUNR = FZU_FIBUNR" "~"
if joinKuerzel = FWFFZU
searchResult := searchResult "LEFT JOIN FIBU.FZU_FIBUFIBUZU ON FZU_FIRMENNR = FWF_FIRMENNR AND FZU_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = LBZFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = LBZ_FIRMENNR AND FWF_FIBUNR = LBZ_FIBUNR" "~"
if joinKuerzel = FWFLBZ
searchResult := searchResult "LEFT JOIN BIL.LBZ_LANGFRISTERST_BELEGZU ON LBZ_FIRMENNR = FWF_FIRMENNR AND LBZ_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = PBOFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = PBO_FIRMENNR AND FWF_FIBUNR = PBO_FIBUNR" "~"
if joinKuerzel = FWFPBO
searchResult := searchResult "LEFT JOIN BIL.PBO_PERSKORST_KONTEN ON PBO_FIRMENNR = FWF_FIRMENNR AND PBO_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = RNGFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = RNG_FIBU_FIRMENNR AND FWF_FIBUNR = RNG_FIBUNR" "~"
if joinKuerzel = FWFRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIBU_FIRMENNR = FWF_FIRMENNR AND RNG_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = SPSFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = SPS_FIRMENNR AND FWF_FIBUNR = SPS_FIBUNR" "~"
if joinKuerzel = FWFSPS
searchResult := searchResult "LEFT JOIN FIBU.SPS_SPENDENSUMME ON SPS_FIRMENNR = FWF_FIRMENNR AND SPS_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = VEBFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = VEB_FIRMENNR AND FWF_FIBUNR = VEB_FIBUNR" "~"
if joinKuerzel = FWFVEB
searchResult := searchResult "LEFT JOIN FIBU.VEB_VORERFASSBUCHUNG ON VEB_FIRMENNR = FWF_FIRMENNR AND VEB_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = VKPFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = VKP_FIRMENNR AND FWF_FIBUNR = VKP_FIBUNR" "~"
if joinKuerzel = FWFVKP
searchResult := searchResult "LEFT JOIN FIBU.VKP_VSTKPROJEKT ON VKP_FIRMENNR = FWF_FIRMENNR AND VKP_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = VSKFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = VSK_FIRMENNR AND FWF_FIBUNR = VSK_FIBUNR" "~"
if joinKuerzel = FWFVSK
searchResult := searchResult "LEFT JOIN FIBU.VSK_VSTPAKET ON VSK_FIRMENNR = FWF_FIRMENNR AND VSK_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = WPDFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = WPD_FIRMENNR AND FWF_FIBUNR = WPD_FIBUNR" "~"
if joinKuerzel = FWFWPD
searchResult := searchResult "LEFT JOIN BIL.WPD_WPDIFFERENZEN ON WPD_FIRMENNR = FWF_FIRMENNR AND WPD_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = WRKFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = WRK_FIRMENNR AND FWF_FIBUNR = WRK_FIBUNR" "~"
if joinKuerzel = FWFWRK
searchResult := searchResult "LEFT JOIN WWS.WRK_WWSANKAUFSRNGKOPF ON WRK_FIRMENNR = FWF_FIRMENNR AND WRK_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = ZIKFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = ZIK_FIRMENNR AND FWF_FIBUNR = ZIK_FIBUNR" "~"
if joinKuerzel = FWFZIK
searchResult := searchResult "LEFT JOIN FIBU.ZIK_KONTENZINSKREIS ON ZIK_FIRMENNR = FWF_FIRMENNR AND ZIK_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = ZINFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = ZIN_FIRMENNR AND FWF_FIBUNR = ZIN_FIBUNR" "~"
if joinKuerzel = FWFZIN
searchResult := searchResult "LEFT JOIN FIBU.ZIN_ZAHLUNGSINFORMATION ON ZIN_FIRMENNR = FWF_FIRMENNR AND ZIN_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = AJOFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = AJO_FIBUFIRMENNR_KUMAFA AND FWF_FIBUNR = AJO_FIBUNR_KUMAFA" "~"
if joinKuerzel = FWFAJO
searchResult := searchResult "LEFT JOIN BIL.AJO_ANBUJOURNAL ON AJO_FIBUFIRMENNR_KUMAFA = FWF_FIRMENNR AND AJO_FIBUNR_KUMAFA = FWF_FIBUNR" "~"
if joinKuerzel = GSKKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_FIRMENNR = GSK_FIRMENNR AND KAS_KASSENNR = GSK_KAS_KASSENNR" "~"
if joinKuerzel = KASGSK
searchResult := searchResult "LEFT JOIN WWS.GSK_GUTSCHEINKOPF ON GSK_FIRMENNR = KAS_FIRMENNR AND GSK_KAS_KASSENNR = KAS_KASSENNR" "~"
if joinKuerzel = RNGKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_FIRMENNR = RNG_KAS_FIRMENNR AND KAS_KASSENNR = RNG_KAS_KASSENNR" "~"
if joinKuerzel = KASRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_KAS_FIRMENNR = KAS_FIRMENNR AND RNG_KAS_KASSENNR = KAS_KASSENNR" "~"
if joinKuerzel = AAEKAT
searchResult := searchResult "LEFT JOIN KAT_KATEGORIE.KAT_KATEGORIE ON KAT_KATEGORIEKZ = AAE_GEBIET_KATEGORIEKZ AND KAT_KATEGORIELFDNR = AAE_GEBIET_KATEGORIELFDNR" "~"
if joinKuerzel = KATAAE
searchResult := searchResult "LEFT JOIN WWS.AAE_ANGEBOTSAUSW_ERFQ_TMP ON AAE_GEBIET_KATEGORIEKZ = KAT_KATEGORIEKZ AND AAE_GEBIET_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = KGBKAT
searchResult := searchResult "LEFT JOIN KAT_KATEGORIE.KAT_KATEGORIE ON KAT_KATEGORIEKZ = KGB_KAT_KATEGORIEKZ AND KAT_KATEGORIELFDNR = KGB_KAT_KATEGORIELFDNR" "~"
if joinKuerzel = KATKGB
searchResult := searchResult "LEFT JOIN PRODUKTION.KGB_KATZU_PPSKAPA ON KGB_KAT_KATEGORIEKZ = KAT_KATEGORIEKZ AND KGB_KAT_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = MEDKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_KATEGORIEKZ = MED_KATEGORIEKZ AND KAT_KATEGORIELFDNR = MED_KATEGORIELFDNR" "~"
if joinKuerzel = KATMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_KATEGORIEKZ = KAT_KATEGORIEKZ AND MED_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = RSBKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_KATEGORIEKZ = RSB_KATEGORIEKZ AND KAT_KATEGORIELFDNR = RSB_KATEGORIELFDNR" "~"
if joinKuerzel = KATRSB
searchResult := searchResult "LEFT JOIN BUERO.RSB_RESSOURCENBEDARF ON RSB_KATEGORIEKZ = KAT_KATEGORIEKZ AND RSB_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = TPRKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_KATEGORIEKZ = TPR_KATEGORIEKZ AND KAT_KATEGORIELFDNR = TPR_KATEGORIELFDNR" "~"
if joinKuerzel = KATTPR
searchResult := searchResult "LEFT JOIN BUERO.TPR_TELEFONPROTOKOLL ON TPR_KATEGORIEKZ = KAT_KATEGORIEKZ AND TPR_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = UPHKAT
searchResult := searchResult "LEFT JOIN KAT_KATEGORIE.KAT_KATEGORIE ON KAT_KATEGORIEKZ = UPH_PAKETKZ AND KAT_KATEGORIELFDNR = UPH_PAKETLFDNR" "~"
if joinKuerzel = KATUPH
searchResult := searchResult "LEFT JOIN BMDENTW.UPH_UPDATEHINWEIS ON UPH_PAKETKZ = KAT_KATEGORIEKZ AND UPH_PAKETLFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = ZUSKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_KATEGORIEKZ = ZUS_KATEGORIEKZ AND KAT_KATEGORIELFDNR = ZUS_KATEGORIELFDNR" "~"
if joinKuerzel = KATZUS
searchResult := searchResult "LEFT JOIN BUERO.ZUS_ZUSTAENDIGKEIT ON ZUS_KATEGORIEKZ = KAT_KATEGORIEKZ AND ZUS_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = UPHKAZ
searchResult := searchResult "LEFT JOIN KAZ_KATEGORIEZU.KAZ_KATEGORIEZU ON KAZ_HKATEGORIEKZ = UPH_PAKETKZ AND KAZ_HKATEGORIELFDNR = UPH_PAKETLFDNR AND KAZ_UKATEGORIEKZ = UPH_MODULKZ AND KAZ_UKATEGORIELFDNR = UPH_MODULLFDNR" "~"
if joinKuerzel = KAZUPH
searchResult := searchResult "LEFT JOIN BMDENTW.UPH_UPDATEHINWEIS ON UPH_PAKETKZ = KAZ_HKATEGORIEKZ AND UPH_PAKETLFDNR = KAZ_HKATEGORIELFDNR AND UPH_MODULKZ = KAZ_UKATEGORIEKZ AND UPH_MODULLFDNR = KAZ_UKATEGORIELFDNR" "~"
if joinKuerzel = KSEKBA
searchResult := searchResult "LEFT JOIN KORE.KBA_KOREBAUM ON KBA_FIRMENNR = KSE_FIRMENNR AND KBA_KORENR = KSE_KORENR AND KBA_KOREBAUMNR = KSE_KOREBAUMNR" "~"
if joinKuerzel = KBAKSE
searchResult := searchResult "LEFT JOIN KORE.KSE_KORESCHLUESSELEINGABE ON KSE_FIRMENNR = KBA_FIRMENNR AND KSE_KORENR = KBA_KORENR AND KSE_KOREBAUMNR = KBA_KOREBAUMNR" "~"
if joinKuerzel = GLUKGD
searchResult := searchResult "LEFT JOIN WWS.KGD_KASSENGELDLADE ON KGD_GELDLADENID = GLU_KGD_GELDLADENID" "~"
if joinKuerzel = KGDGLU
searchResult := searchResult "LEFT JOIN WWS.GLU_GELDLADENZUORDNUNG ON GLU_KGD_GELDLADENID = KGD_GELDLADENID" "~"
if joinKuerzel = ZASKKA
searchResult := searchResult "LEFT JOIN BUERO.KKA_KREDITKARTENART ON KKA_KREDITKARTENARTNR = ZAS_KREDITKARTENARTNR" "~"
if joinKuerzel = KKAZAS
searchResult := searchResult "LEFT JOIN BUERO.ZAS_ZAHLSCHEIN ON ZAS_KREDITKARTENARTNR = KKA_KREDITKARTENARTNR" "~"
if joinKuerzel = ALZKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = ALZ_KLI_FIRMENNR AND KLI_KLID = ALZ_KLID" "~"
if joinKuerzel = KLIALZ
searchResult := searchResult "LEFT JOIN WWS.ALZ_ALLERGENDATEN ON ALZ_KLI_FIRMENNR = KLI_FIRMENNR AND ALZ_KLID = KLI_KLID" "~"
if joinKuerzel = ASWKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = ASW_KLI_FIRMENNR AND KLI_KLID = ASW_KLID" "~"
if joinKuerzel = KLIASW
searchResult := searchResult "LEFT JOIN BUERO.ASW_AUSWEIS ON ASW_KLI_FIRMENNR = KLI_FIRMENNR AND ASW_KLID = KLI_KLID" "~"
if joinKuerzel = AVEKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = AVE_AUFTRAGGEB_FIRMENNR AND KLI_KLID = AVE_AUFTRAGGEB_KLID" "~"
if joinKuerzel = KLIAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_AUFTRAGGEB_FIRMENNR = KLI_FIRMENNR AND AVE_AUFTRAGGEB_KLID = KLI_KLID" "~"
if joinKuerzel = BONKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = BON_KUNDEN_FIRMENNR AND KLI_KLID = BON_KUNDEN_KLID" "~"
if joinKuerzel = KLIBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_KUNDEN_FIRMENNR = KLI_FIRMENNR AND BON_KUNDEN_KLID = KLI_KLID" "~"
if joinKuerzel = BVOKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = BVO_LIEFERANT_FIRMENNR AND KLI_KLID = BVO_LIEFERANT_KLID" "~"
if joinKuerzel = KLIBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_LIEFERANT_FIRMENNR = KLI_FIRMENNR AND BVO_LIEFERANT_KLID = KLI_KLID" "~"
if joinKuerzel = EKKKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = EKK_FIRMENNR AND KLI_KLID = EKK_KLID" "~"
if joinKuerzel = KLIEKK
searchResult := searchResult "LEFT JOIN WWS.EKK_EXTERNE_KUNDENKARTEI ON EKK_FIRMENNR = KLI_FIRMENNR AND EKK_KLID = KLI_KLID" "~"
if joinKuerzel = WAGGEG
searchResult := searchResult "LEFT JOIN GEG_GEFAHRENGUT.GEG_GEFAHRENGUT ON GEG_FIRMENNR = WAG_FIRMENNR AND GEG_GEFAHRENGUTNR = WAG_GEFAHRENGUTNR" "~"
if joinKuerzel = GEGWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = GEG_FIRMENNR AND WAG_GEFAHRENGUTNR = GEG_GEFAHRENGUTNR" "~"
if joinKuerzel = GEPGEK
searchResult := searchResult "LEFT JOIN WWS.GEK_GEFAHRENGUTKOPF ON GEK_GEFGUTKOPFID = GEP_GEFGUTKOPFID" "~"
if joinKuerzel = GEKGEP
searchResult := searchResult "LEFT JOIN WWS.GEP_GEFAHRENGUTPOS ON GEP_GEFGUTKOPFID = GEK_GEFGUTKOPFID" "~"
if joinKuerzel = BMMGEM
searchResult := searchResult "LEFT JOIN GEM_GEMEINDE.GEM_GEMEINDE ON GEM_LANDNR = BMM_GEM_LANDNR AND GEM_GEMEINDENR = BMM_GEM_GEMEINDENR" "~"
if joinKuerzel = GEMBMM
searchResult := searchResult "LEFT JOIN HRS.BMM_BUAK_MONMELDUNG ON BMM_GEM_LANDNR = GEM_LANDNR AND BMM_GEM_GEMEINDENR = GEM_GEMEINDENR" "~"
if joinKuerzel = ORTGEM
searchResult := searchResult "LEFT JOIN BUERO.GEM_GEMEINDE ON GEM_LANDNR = ORT_GEM_LANDNR AND GEM_GEMEINDENR = ORT_GEMEINDENR" "~"
if joinKuerzel = GEMORT
searchResult := searchResult "LEFT JOIN BUERO.ORT_ORT ON ORT_GEM_LANDNR = GEM_LANDNR AND ORT_GEMEINDENR = GEM_GEMEINDENR" "~"
if joinKuerzel = GRDGRM
searchResult := searchResult "LEFT JOIN DATANORM.GRM_GRAFIK ON GRM_GR_NR = GRD_GR_NR AND GRM_ERST_NR = GRD_ERST_NR" "~"
if joinKuerzel = GRMGRD
searchResult := searchResult "LEFT JOIN DATANORM.GRD_GRAFIK_DETAIL ON GRD_GR_NR = GRM_GR_NR AND GRD_ERST_NR = GRM_ERST_NR" "~"
if joinKuerzel = ARBGRO
searchResult := searchResult "LEFT JOIN WWS.GRO_GROESSE ON GRO_FIRMENNR = ARB_VORG_GRO_FIRMENNR AND GRO_GROESSENKBEZ = ARB_VORG_GROESSENKBEZ" "~"
if joinKuerzel = GROARB
searchResult := searchResult "LEFT JOIN WWS.ARB_ARTIKELBEREICHSWERTE ON ARB_VORG_GRO_FIRMENNR = GRO_FIRMENNR AND ARB_VORG_GROESSENKBEZ = GRO_GROESSENKBEZ" "~"
if joinKuerzel = GTPGRO
searchResult := searchResult "LEFT JOIN WWS.GRO_GROESSE ON GRO_FIRMENNR = GTP_GRT_FIRMENNR AND GRO_GROESSENKBEZ = GTP_GROESSENKBEZ" "~"
if joinKuerzel = GROGTP
searchResult := searchResult "LEFT JOIN WWS.GTP_GROESSENTABPOS ON GTP_GRT_FIRMENNR = GRO_FIRMENNR AND GTP_GROESSENKBEZ = GRO_GROESSENKBEZ" "~"
if joinKuerzel = GSFGSA
searchResult := searchResult "LEFT JOIN WWS.GSA_GUTSCHEINAKTION ON GSA_GSAID = GSF_GSAID" "~"
if joinKuerzel = GSAGSF
searchResult := searchResult "LEFT JOIN WWS.GSF_GUTSCHEINFILIALDATEN ON GSF_GSAID = GSA_GSAID" "~"
if joinKuerzel = SUKGWZ
searchResult := searchResult "LEFT JOIN HRS.GWZ_GWBANKZU ON GWZ_GWZID = SUK_GWZID AND GWZ_LOHNJAHR = SUK_JAHR" "~"
if joinKuerzel = GWZSUK
searchResult := searchResult "LEFT JOIN HRS.SUK_SUM_KOE ON SUK_GWZID = GWZ_GWZID AND SUK_JAHR = GWZ_LOHNJAHR" "~"
if joinKuerzel = GZMGZJ
searchResult := searchResult "LEFT JOIN HRS.GZJ_GLAEUBIGERZU_JAHR ON GZJ_DVFIRMENNR = GZM_DVFIRMENNR AND GZJ_FIRMENNR = GZM_FIRMENNR AND GZJ_MITARBEITERID = GZM_MITARBEITERID AND GZJ_GZJNR = GZM_GZJNR" "~"
if joinKuerzel = GZJGZM
searchResult := searchResult "LEFT JOIN HRS.GZM_GLAEUBIGERZU_MONAT ON GZM_DVFIRMENNR = GZJ_DVFIRMENNR AND GZM_FIRMENNR = GZJ_FIRMENNR AND GZM_MITARBEITERID = GZJ_MITARBEITERID AND GZM_GZJNR = GZJ_GZJNR" "~"
if joinKuerzel = BOZGZK
searchResult := searchResult "LEFT JOIN WWS.GZK_GELDZAEHLKOPF ON GZK_GELDZAEHLKOPFID = BOZ_GELDZAEHLKOPFID" "~"
if joinKuerzel = GZKBOZ
searchResult := searchResult "LEFT JOIN WWS.BOZ_BONZAHLUNG ON BOZ_GELDZAEHLKOPFID = GZK_GELDZAEHLKOPFID" "~"
if joinKuerzel = HADHAM
searchResult := searchResult "LEFT JOIN HRS.HAM_AUSDRUCKMASTER ON HAM_LOHNAUSDRMASTERID = HAD_HAM_LOHNAUSDRMASTERID" "~"
if joinKuerzel = HAMHAD
searchResult := searchResult "LEFT JOIN HRS.HAD_AUSDRUCKDETAIL ON HAD_HAM_LOHNAUSDRMASTERID = HAM_LOHNAUSDRMASTERID" "~"
if joinKuerzel = HSAHAM
searchResult := searchResult "LEFT JOIN HRS.HAM_AUSDRUCKMASTER ON HAM_LOHNAUSDRMASTERID = HSA_HAM_LOHNAUSDRMASTERID" "~"
if joinKuerzel = HAMHSA
searchResult := searchResult "LEFT JOIN HRS.HSA_SORTIERART ON HSA_HAM_LOHNAUSDRMASTERID = HAM_LOHNAUSDRMASTERID" "~"
if joinKuerzel = BTFHLB
searchResult := searchResult "LEFT JOIN HLB_HILFEBAUSTEIN.HLB_HILFEBAUSTEIN ON HLB_HLBID = BTF_HLBID" "~"
if joinKuerzel = HLBBTF
searchResult := searchResult "LEFT JOIN BMD.BTF_BMDTABFELDBESCH ON BTF_HLBID = HLB_HLBID" "~"
if joinKuerzel = PAGHLD
searchResult := searchResult "LEFT JOIN HLD_HILFEDOKUMENT.HLD_HILFEDOKUMENT ON HLD_HLDID = PAG_HLDID" "~"
if joinKuerzel = HLDPAG
searchResult := searchResult "LEFT JOIN BMDDEFAULT.PAG_PARAMGRUPPE ON PAG_HLDID = HLD_HLDID" "~"
if joinKuerzel = LEIHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_HONORARPOSNR = LEI_HONORARPOSNR" "~"
if joinKuerzel = HNPLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_HONORARPOSNR = HNP_HONORARPOSNR" "~"
if joinKuerzel = MBMHVP
searchResult := searchResult "LEFT JOIN HRS.HVP_VERRECHUNGSPOSITION ON HVP_DBS_DATENBESTANDNR = MBM_DATENBESTANDNR AND HVP_LOHNJAHR = MBM_JAHR AND HVP_HVPLFDNR = MBM_POSITION" "~"
if joinKuerzel = HVPMBM
searchResult := searchResult "LEFT JOIN HRS.MBM_MBGM ON MBM_DATENBESTANDNR = HVP_DBS_DATENBESTANDNR AND MBM_JAHR = HVP_LOHNJAHR AND MBM_POSITION = HVP_HVPLFDNR" "~"
if joinKuerzel = IABIAB
searchResult := searchResult "LEFT JOIN BIL.IAB_INVESTITIONSABZUG ON IAB_IABID = IAB_PARENTID" "~"
if joinKuerzel = IABIAB
searchResult := searchResult "LEFT JOIN BIL.IAB_INVESTITIONSABZUG ON IAB_PARENTID = IAB_IABID" "~"
if joinKuerzel = AIZIFP
searchResult := searchResult "LEFT JOIN IFP_INFOTABPOS.IFP_INFOTABPOS ON IFP_INFOTABID = AIZ_INFOTABID AND IFP_INFOTABPOSID = AIZ_INFOTABPOSID" "~"
if joinKuerzel = IFPAIZ
searchResult := searchResult "LEFT JOIN PRODUKTION.AIZ_AGPER_INFOZU ON AIZ_INFOTABID = IFP_INFOTABID AND AIZ_INFOTABPOSID = IFP_INFOTABPOSID" "~"
if joinKuerzel = STMIFP
searchResult := searchResult "LEFT JOIN BUERO.IFP_INFOTABPOS ON IFP_INFOTABID = STM_INFOTABID AND IFP_INFOTABPOSID = STM_INFOTABPOSID" "~"
if joinKuerzel = IFPSTM
searchResult := searchResult "LEFT JOIN BUERO.STM_STELLENMERKMALE ON STM_INFOTABID = IFP_INFOTABID AND STM_INFOTABPOSID = IFP_INFOTABPOSID" "~"
if joinKuerzel = ESDIMN
searchResult := searchResult "LEFT JOIN IMN_INSTANDHMASSN.IMN_INSTANDHMASSN ON IMN_IMNID = ESD_IMNID" "~"
if joinKuerzel = IMNESD
searchResult := searchResult "LEFT JOIN WWS.ESD_ERFASSUNGSERVICEDATEN ON ESD_IMNID = IMN_IMNID" "~"
if joinKuerzel = DRTRGT
searchResult := searchResult "LEFT JOIN RGT_TABREPLGTABELLEZU.RGT_TABREPLGTABELLEZU ON RGT_RKGID = DRT_RKGID AND RGT_DTAID = DRT_DTAID AND RGT_HAUPTTABELLE_DTAID = DRT_HAUPTTABELLE_DTAID" "~"
if joinKuerzel = RGTDRT
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DRT_DTABREPLGTABELLEZU ON DRT_RKGID = RGT_RKGID AND DRT_DTAID = RGT_DTAID AND DRT_HAUPTTABELLE_DTAID = RGT_HAUPTTABELLE_DTAID" "~"
if joinKuerzel = LABFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = LAB_FIRMENNR AND FWF_FIBUNR = LAB_FIBUNR" "~"
if joinKuerzel = FWFLAB
searchResult := searchResult "LEFT JOIN BIL.LAB_LATENTESTEUERN_BELEGZU ON LAB_FIRMENNR = FWF_FIRMENNR AND LAB_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = MASFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = MAS_FIRMENNR AND FWF_FIBUNR = MAS_FIBUNR" "~"
if joinKuerzel = FWFMAS
searchResult := searchResult "LEFT JOIN FIBU.MAS_MAHN_STAMM ON MAS_FIRMENNR = FWF_FIRMENNR AND MAS_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = PKXFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = PKX_FIRMENNR AND FWF_FIBUNR = PKX_FIBUNR" "~"
if joinKuerzel = FWFPKX
searchResult := searchResult "LEFT JOIN FIBU.PKX_BUDGETPERSKOSTPARAMS ON PKX_FIRMENNR = FWF_FIRMENNR AND PKX_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = VSRFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = VSR_FIRMENNR AND FWF_FIBUNR = VSR_FIBUNR" "~"
if joinKuerzel = FWFVSR
searchResult := searchResult "LEFT JOIN FIBU.VSR_VSTERSTATTUNG ON VSR_FIRMENNR = FWF_FIRMENNR AND VSR_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = ZLAFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = ZLA_FIRMENNR AND FWF_FIBUNR = ZLA_QUELLNR" "~"
if joinKuerzel = FWFZLA
searchResult := searchResult "LEFT JOIN ZLE.ZLA_ZEILAB ON ZLA_FIRMENNR = FWF_FIRMENNR AND ZLA_QUELLNR = FWF_FIBUNR" "~"
if joinKuerzel = ZZUFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = ZZU_FIRMENNR AND FWF_FIBUNR = ZZU_FIBUNR" "~"
if joinKuerzel = FWFZZU
searchResult := searchResult "LEFT JOIN FIBU.ZZU_ZINSBERECHNUNG_ZLG_ZU ON ZZU_FIRMENNR = FWF_FIRMENNR AND ZZU_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = KMIFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = KMI_FIRMENNR AND FWK_KORENR = KMI_KORENR" "~"
if joinKuerzel = FWKKMI
searchResult := searchResult "LEFT JOIN KORE.KMI_ISTMENGEN ON KMI_FIRMENNR = FWK_FIRMENNR AND KMI_KORENR = FWK_KORENR" "~"
if joinKuerzel = KSKFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = KSK_FIRMENNR AND FWK_KORENR = KSK_KORENR" "~"
if joinKuerzel = FWKKSK
searchResult := searchResult "LEFT JOIN KORE.KSK_KORESCHLUESSELKOPF ON KSK_FIRMENNR = FWK_FIRMENNR AND KSK_KORENR = FWK_KORENR" "~"
if joinKuerzel = KURFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = KUR_FIRMENNR AND FWK_KORENR = KUR_KORENR" "~"
if joinKuerzel = FWKKUR
searchResult := searchResult "LEFT JOIN KORE.KUR_UMLAGEREIHENFOLGE ON KUR_FIRMENNR = FWK_FIRMENNR AND KUR_KORENR = FWK_KORENR" "~"
if joinKuerzel = LEIGEG
searchResult := searchResult "LEFT JOIN ZLE.GEG_GEGENSTANDSWERTE ON GEG_GEGENSTANDSWERTID = LEI_GEGENSTANDSWERTID" "~"
if joinKuerzel = GEGLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_GEGENSTANDSWERTID = GEG_GEGENSTANDSWERTID" "~"
if joinKuerzel = FGKGKK
searchResult := searchResult "LEFT JOIN BUERO.GKK_KRANKENKASSA ON GKK_LANDNR = FGK_LANDNR AND GKK_GKKNR = FGK_GKKNR" "~"
if joinKuerzel = GKKFGK
searchResult := searchResult "LEFT JOIN BUERO.FGK_FIRMENGKKZU ON FGK_LANDNR = GKK_LANDNR AND FGK_GKKNR = GKK_GKKNR" "~"
if joinKuerzel = PGKGKK
searchResult := searchResult "LEFT JOIN BUERO.GKK_KRANKENKASSA ON GKK_LANDNR = PGK_LANDNR AND GKK_GKKNR = PGK_GKKNR" "~"
if joinKuerzel = GKKPGK
searchResult := searchResult "LEFT JOIN BUERO.PGK_PERSONGKKZU ON PGK_LANDNR = GKK_LANDNR AND PGK_GKKNR = GKK_GKKNR" "~"
if joinKuerzel = ARLGRO
searchResult := searchResult "LEFT JOIN WWS.GRO_GROESSE ON GRO_FIRMENNR = ARL_VORG_GRO_FIRMENNR AND GRO_GROESSENKBEZ = ARL_VORG_GROESSENKBEZ" "~"
if joinKuerzel = GROARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_VORG_GRO_FIRMENNR = GRO_FIRMENNR AND ARL_VORG_GROESSENKBEZ = GRO_GROESSENKBEZ" "~"
if joinKuerzel = RAPGRO
searchResult := searchResult "LEFT JOIN WWS.GRO_GROESSE ON GRO_FIRMENNR = RAP_GRO_FIRMENNR AND GRO_GROESSENKBEZ = RAP_GROESSENKBEZ" "~"
if joinKuerzel = GRORAP
searchResult := searchResult "LEFT JOIN WWS.RAP_RABATTTABPOS ON RAP_GRO_FIRMENNR = GRO_FIRMENNR AND RAP_GROESSENKBEZ = GRO_GROESSENKBEZ" "~"
if joinKuerzel = GIDGWI
searchResult := searchResult "LEFT JOIN BUERO.GWI_GELDWIEREG ON GWI_GWIID = GID_GWIID" "~"
if joinKuerzel = GWIGID
searchResult := searchResult "LEFT JOIN BUERO.GID_GELDIDENTIFIKATION ON GID_GWIID = GWI_GWIID" "~"
if joinKuerzel = GWZGWQ
searchResult := searchResult "LEFT JOIN HRS.GWQ_GWBANKZU_SEQ ON GWQ_GWZID = GWZ_GWZID" "~"
if joinKuerzel = GWQGWZ
searchResult := searchResult "LEFT JOIN HRS.GWZ_GWBANKZU ON GWZ_GWZID = GWQ_GWZID" "~"
if joinKuerzel = GWBGWU
searchResult := searchResult "LEFT JOIN HRS.GWU_GWUNTERGRUPPE ON GWU_GWUID = GWB_GWUID" "~"
if joinKuerzel = GWUGWB
searchResult := searchResult "LEFT JOIN HRS.GWB_GWBERECHNUNG ON GWB_GWUID = GWU_GWUID" "~"
if joinKuerzel = HSPHAM
searchResult := searchResult "LEFT JOIN HRS.HAM_AUSDRUCKMASTER ON HAM_LOHNAUSDRMASTERID = HSP_HAM_LOHNAUSDRMASTERID" "~"
if joinKuerzel = HAMHSP
searchResult := searchResult "LEFT JOIN HRS.HSP_SPALTENKOPF ON HSP_HAM_LOHNAUSDRMASTERID = HAM_LOHNAUSDRMASTERID" "~"
if joinKuerzel = PAUHLD
searchResult := searchResult "LEFT JOIN HLD_HILFEDOKUMENT.HLD_HILFEDOKUMENT ON HLD_HLDID = PAU_HLDID" "~"
if joinKuerzel = HLDPAU
searchResult := searchResult "LEFT JOIN BMDDEFAULT.PAU_PARAMUNTERGRUPPE ON PAU_HLDID = HLD_HLDID" "~"
if joinKuerzel = HNPHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_HONORARPOSNR = HNP_GUTVON_HONORARPOSNR" "~"
if joinKuerzel = HNPHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_GUTVON_HONORARPOSNR = HNP_HONORARPOSNR" "~"
if joinKuerzel = BZBIDP
searchResult := searchResult "LEFT JOIN PRODUKTION.IDP_INFO_DELAY_BREAKDOWN ON IDP_DELAY_BREAK_ID = BZB_DELAY_BREAK_ID" "~"
if joinKuerzel = IDPBZB
searchResult := searchResult "LEFT JOIN PRODUKTION.BZB_BAZZEITBUCHUNG ON BZB_DELAY_BREAK_ID = IDP_DELAY_BREAK_ID" "~"
if joinKuerzel = AGIIFP
searchResult := searchResult "LEFT JOIN IFP_INFOTABPOS.IFP_INFOTABPOS ON IFP_INFOTABID = AGI_INFOTABID AND IFP_INFOTABPOSID = AGI_INFOTABPOSID" "~"
if joinKuerzel = IFPAGI
searchResult := searchResult "LEFT JOIN WWS.AGI_AGPER_INFOZU ON AGI_INFOTABID = IFP_INFOTABID AND AGI_INFOTABPOSID = IFP_INFOTABPOSID" "~"
if joinKuerzel = PCXFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = PCX_FIRMENNR AND FWF_FIBUNR = PCX_FIBUNR" "~"
if joinKuerzel = FWFPCX
searchResult := searchResult "LEFT JOIN PRODUKTION.PCX_PPS_KAPA_EXT ON PCX_FIRMENNR = FWF_FIRMENNR AND PCX_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = ZESFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = ZES_FIRMENNR AND FWF_FIBUNR = ZES_FIBUNR" "~"
if joinKuerzel = FWFZES
searchResult := searchResult "LEFT JOIN FIBU.ZES_ZESSIONSTEXT ON ZES_FIRMENNR = FWF_FIRMENNR AND ZES_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = ZOEFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = ZOE_FIRMENNR AND FWF_FIBUNR = ZOE_FIBUNR" "~"
if joinKuerzel = FWFZOE
searchResult := searchResult "LEFT JOIN FIBU.ZOE_ZOLLBELEGERFASSUNG ON ZOE_FIRMENNR = FWF_FIRMENNR AND ZOE_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = KBAFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = KBA_FIRMENNR AND FWK_KORENR = KBA_KORENR" "~"
if joinKuerzel = FWKKBA
searchResult := searchResult "LEFT JOIN KORE.KBA_KOREBAUM ON KBA_FIRMENNR = FWK_FIRMENNR AND KBA_KORENR = FWK_KORENR" "~"
if joinKuerzel = KUVFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = KUV_FIRMENNR AND FWK_KORENR = KUV_KORENR" "~"
if joinKuerzel = FWKKUV
searchResult := searchResult "LEFT JOIN KORE.KUV_UMLAGEVARIANTE ON KUV_FIRMENNR = FWK_FIRMENNR AND KUV_KORENR = FWK_KORENR" "~"
if joinKuerzel = KVGFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = KVG_QUELLE_FIRMENNR AND FWK_KORENR = KVG_QUELLE_KORENR" "~"
if joinKuerzel = FWKKVG
searchResult := searchResult "LEFT JOIN KORE.KVG_KORESTAMMVERTEILUNG ON KVG_QUELLE_FIRMENNR = FWK_FIRMENNR AND KVG_QUELLE_KORENR = FWK_KORENR" "~"
if joinKuerzel = KZKFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = KZK_FWK_FIRMENNR AND FWK_KORENR = KZK_KORENR" "~"
if joinKuerzel = FWKKZK
searchResult := searchResult "LEFT JOIN KORE.KZK_ZUSCHLAGKOPF ON KZK_FWK_FIRMENNR = FWK_FIRMENNR AND KZK_KORENR = FWK_KORENR" "~"
if joinKuerzel = RNPFWK
searchResult := searchResult "LEFT JOIN FWK_FIRMENWERTEKORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = RNP_FWK_FIRMENNR AND FWK_KORENR = RNP_KORENR" "~"
if joinKuerzel = FWKRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FWK_FIRMENNR = FWK_FIRMENNR AND RNP_KORENR = FWK_KORENR" "~"
if joinKuerzel = RNPFWK
searchResult := searchResult "LEFT JOIN FWK_FIRMENWERTEKORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = RNP_FWK_FIRMENNR AND FWK_KORENR = RNP_KORENR" "~"
if joinKuerzel = FWKRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FWK_FIRMENNR = FWK_FIRMENNR AND RNP_KORENR = FWK_KORENR" "~"
if joinKuerzel = EFKGEA
searchResult := searchResult "LEFT JOIN GEA_GESCHAEFTSART.GEA_GESCHAEFTSART ON GEA_FIRMENNR = EFK_GEA_FIRMENNR AND GEA_GEACODE = EFK_GEACODE" "~"
if joinKuerzel = GEAEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_GEA_FIRMENNR = GEA_FIRMENNR AND EFK_GEACODE = GEA_GEACODE" "~"
if joinKuerzel = ABSGEM
searchResult := searchResult "LEFT JOIN GEM_GEMEINDE.GEM_GEMEINDE ON GEM_LANDNR = ABS_GEM_LANDNR AND GEM_GEMEINDENR = ABS_GEMEINDENR" "~"
if joinKuerzel = GEMABS
searchResult := searchResult "LEFT JOIN HRS.ABS_ARBEITSSTAETTE ON ABS_GEM_LANDNR = GEM_LANDNR AND ABS_GEMEINDENR = GEM_GEMEINDENR" "~"
if joinKuerzel = ASOGPO
searchResult := searchResult "LEFT JOIN GPO_GRUPPENORDNER.GPO_GRUPPENORDNER ON GPO_BAUMID = ASO_GOP_BAUMID AND GPO_GRUPPENORDNERNR = ASO_GOP_GRUPPENORDNERNR" "~"
if joinKuerzel = GPOASO
searchResult := searchResult "LEFT JOIN ZLE.ASO_SORTIERPOS ON ASO_GOP_BAUMID = GPO_BAUMID AND ASO_GOP_GRUPPENORDNERNR = GPO_GRUPPENORDNERNR" "~"
if joinKuerzel = GOEGPO
searchResult := searchResult "LEFT JOIN BUERO.GPO_GRUPPENORDNER ON GPO_BAUMID = GOE_BAUMID AND GPO_GRUPPENORDNERNR = GOE_GRUPPENORDNERNR" "~"
if joinKuerzel = GPOGOE
searchResult := searchResult "LEFT JOIN BUERO.GOE_GRUPPENORDNEREVENTZU ON GOE_BAUMID = GPO_BAUMID AND GOE_GRUPPENORDNERNR = GPO_GRUPPENORDNERNR" "~"
if joinKuerzel = GOIGPO
searchResult := searchResult "LEFT JOIN BUERO.GPO_GRUPPENORDNER ON GPO_BAUMID = GOI_BAUMID AND GPO_GRUPPENORDNERNR = GOI_GRUPPENORDNERNR" "~"
if joinKuerzel = GPOGOI
searchResult := searchResult "LEFT JOIN BUERO.GOI_GRUPPENORDNERINSTZU ON GOI_BAUMID = GPO_BAUMID AND GOI_GRUPPENORDNERNR = GPO_GRUPPENORDNERNR" "~"
if joinKuerzel = GOPGPO
searchResult := searchResult "LEFT JOIN BUERO.GPO_GRUPPENORDNER ON GPO_BAUMID = GOP_BAUMID AND GPO_GRUPPENORDNERNR = GOP_GRUPPENORDNERNR" "~"
if joinKuerzel = GPOGOP
searchResult := searchResult "LEFT JOIN BUERO.GOP_GRUPPENORDNERPERSZU ON GOP_BAUMID = GPO_BAUMID AND GOP_GRUPPENORDNERNR = GPO_GRUPPENORDNERNR" "~"
if joinKuerzel = GPOGPO
searchResult := searchResult "LEFT JOIN BUERO.GPO_GRUPPENORDNER ON GPO_BAUMID = GPO_GPO_BAUMID AND GPO_GRUPPENORDNERNR = GPO_GPO_GRUPPENORDNERNR" "~"
if joinKuerzel = GPOGPO
searchResult := searchResult "LEFT JOIN BUERO.GPO_GRUPPENORDNER ON GPO_GPO_BAUMID = GPO_BAUMID AND GPO_GPO_GRUPPENORDNERNR = GPO_GRUPPENORDNERNR" "~"
if joinKuerzel = BPBGRO
searchResult := searchResult "LEFT JOIN WWS.GRO_GROESSE ON GRO_FIRMENNR = BPB_FIRMENNR AND GRO_GROESSENKBEZ = BPB_GROESSENKBEZ" "~"
if joinKuerzel = GROBPB
searchResult := searchResult "LEFT JOIN WWS.BPB_LIEFBESTAET ON BPB_FIRMENNR = GRO_FIRMENNR AND BPB_GROESSENKBEZ = GRO_GROESSENKBEZ" "~"
if joinKuerzel = GSPGSK
searchResult := searchResult "LEFT JOIN WWS.GSK_GUTSCHEINKOPF ON GSK_GUTSCHEINKOPFID = GSP_GUTSCHEINKOPFID" "~"
if joinKuerzel = GSKGSP
searchResult := searchResult "LEFT JOIN WWS.GSP_GUTSCHEINPOSITION ON GSP_GUTSCHEINKOPFID = GSK_GUTSCHEINKOPFID" "~"
if joinKuerzel = HNPHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_HONORARPOSNR = HNP_GEHOERTZU_HONORARPOSNR" "~"
if joinKuerzel = HNPHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_GEHOERTZU_HONORARPOSNR = HNP_HONORARPOSNR" "~"
if joinKuerzel = HNPHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_HONORARPOSNR = HNP_FOLGE_HONORARPOSNR" "~"
if joinKuerzel = HNPHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_FOLGE_HONORARPOSNR = HNP_HONORARPOSNR" "~"
if joinKuerzel = HZLHZL
searchResult := searchResult "LEFT JOIN BUERO.HZL_HAUPTZIEL ON HZL_HZLID = HZL_HZL_HZLID" "~"
if joinKuerzel = HZLHZL
searchResult := searchResult "LEFT JOIN BUERO.HZL_HAUPTZIEL ON HZL_HZL_HZLID = HZL_HZLID" "~"
if joinKuerzel = KIZFST
searchResult := searchResult "LEFT JOIN BUERO.FST_FRIST ON FST_FRISTLFDNR = KIZ_FST_FRISTLFDNR" "~"
if joinKuerzel = FSTKIZ
searchResult := searchResult "LEFT JOIN BUERO.KIZ_KUND_INFOTABPOSZU ON KIZ_FST_FRISTLFDNR = FST_FRISTLFDNR" "~"
if joinKuerzel = ASDFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = ASD_FIRMENNR AND FWF_FIBUNR = ASD_FIBUNR" "~"
if joinKuerzel = FWFASD
searchResult := searchResult "LEFT JOIN ZX_GELOESCHT.ASD_ABSTATTUNGSDETAIL ON ASD_FIRMENNR = FWF_FIRMENNR AND ASD_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = AWEFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = AWE_FWF_FIRMENNR AND FWF_FIBUNR = AWE_FWF_FIBUNR" "~"
if joinKuerzel = FWFAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_FWF_FIRMENNR = FWF_FIRMENNR AND AWE_FWF_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = BPAFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = BPA_FIRMENNR AND FWF_FIBUNR = BPA_FIBUNR" "~"
if joinKuerzel = FWFBPA
searchResult := searchResult "LEFT JOIN FIBU.BPA_BUDGETPLANBILAUFTLG ON BPA_FIRMENNR = FWF_FIRMENNR AND BPA_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = BUKFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = BUK_FIRMENNR AND FWF_FIBUNR = BUK_FIBUNR" "~"
if joinKuerzel = FWFBUK
searchResult := searchResult "LEFT JOIN FIBU.BUK_BUCHUNGSKREIS ON BUK_FIRMENNR = FWF_FIRMENNR AND BUK_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = BUMFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = BUM_FIRMENNR AND FWF_FIBUNR = BUM_FIBUNR" "~"
if joinKuerzel = FWFBUM
searchResult := searchResult "LEFT JOIN BIL.BUM_BILUMBUCHUNG ON BUM_FIRMENNR = FWF_FIRMENNR AND BUM_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = BUVFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = BUV_FIRMENNR AND FWF_FIBUNR = BUV_FIBUNR" "~"
if joinKuerzel = FWFBUV
searchResult := searchResult "LEFT JOIN FIBU.BUV_BUCHUNGSVARIANTE ON BUV_FIRMENNR = FWF_FIRMENNR AND BUV_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = FKVFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = FKV_QUELLE_FIRMENNR AND FWF_FIBUNR = FKV_QUELLE_FIBUNR" "~"
if joinKuerzel = FWFFKV
searchResult := searchResult "LEFT JOIN FIBU.FKV_KONTENVERTEILUNG ON FKV_QUELLE_FIRMENNR = FWF_FIRMENNR AND FKV_QUELLE_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = GLTFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = GLT_FIRMENNR AND FWF_FIBUNR = GLT_FIBUNR" "~"
if joinKuerzel = FWFGLT
searchResult := searchResult "LEFT JOIN BIL.GLT_GLIEDERUNGSTEXT ON GLT_FIRMENNR = FWF_FIRMENNR AND GLT_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = KGVFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = KGV_FIRMENNR AND FWF_FIBUNR = KGV_FIBUNR" "~"
if joinKuerzel = FWFKGV
searchResult := searchResult "LEFT JOIN FIBU.KGV_KONTENGUVVERBUCHUNG ON KGV_FIRMENNR = FWF_FIRMENNR AND KGV_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = KTGFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = KTG_FIRMENNR AND FWF_FIBUNR = KTG_FIBUNR" "~"
if joinKuerzel = FWFKTG
searchResult := searchResult "LEFT JOIN FIBU.KTG_KONTENGRENZEN ON KTG_FIRMENNR = FWF_FIRMENNR AND KTG_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = KZEFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = KZE_FWK_FIRMENNR AND FWF_FIBUNR = KZE_FIBUNR" "~"
if joinKuerzel = FWFKZE
searchResult := searchResult "LEFT JOIN KORE.KZE_ZUSCHLAGEINGABE ON KZE_FWK_FIRMENNR = FWF_FIRMENNR AND KZE_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = SKEFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = SKE_FIRMENNR_FIBU AND FWF_FIBUNR = SKE_FIBUNR" "~"
if joinKuerzel = FWFSKE
searchResult := searchResult "LEFT JOIN BIL.SKE_STEUERKUNDENZUERW ON SKE_FIRMENNR_FIBU = FWF_FIRMENNR AND SKE_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = STSFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = STS_FIBU_FIRMENNR AND FWF_FIBUNR = STS_FIBUNR" "~"
if joinKuerzel = FWFSTS
searchResult := searchResult "LEFT JOIN BIL.STS_STEUERSTAMMDAT ON STS_FIBU_FIRMENNR = FWF_FIRMENNR AND STS_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = UDBFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = UDB_FIRMENNR AND FWF_FIBUNR = UDB_FIBUNR" "~"
if joinKuerzel = FWFUDB
searchResult := searchResult "LEFT JOIN BIL.UDB_ANGUNTERBILANZ ON UDB_FIRMENNR = FWF_FIRMENNR AND UDB_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = XSPFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = XSP_FWF_FIRMENNR AND FWF_FIBUNR = XSP_FWF_FIBUNR" "~"
if joinKuerzel = FWFXSP
searchResult := searchResult "LEFT JOIN BUERO.XSP_XML_STEUERERKL_PAKET ON XSP_FWF_FIRMENNR = FWF_FIRMENNR AND XSP_FWF_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = YKEFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = YKE_FIRMENNR AND FWF_FIBUNR = YKE_FIBUNR" "~"
if joinKuerzel = FWFYKE
searchResult := searchResult "LEFT JOIN FIBU.YKE_AUTOBUCHKOREERF ON YKE_FIRMENNR = FWF_FIRMENNR AND YKE_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = BRTFWK
searchResult := searchResult "LEFT JOIN FWK_FIRMENWERTEKORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = BRT_FWK_FIRMENNR AND FWK_KORENR = BRT_KORENR" "~"
if joinKuerzel = FWKBRT
searchResult := searchResult "LEFT JOIN BUERO.BRT_BERICHT ON BRT_FWK_FIRMENNR = FWK_FIRMENNR AND BRT_KORENR = FWK_KORENR" "~"
if joinKuerzel = FWFFWK
searchResult := searchResult "LEFT JOIN FWK_FIRMENWERTEKORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = FWF_KORE_FIRMENNR AND FWK_KORENR = FWF_KORENR" "~"
if joinKuerzel = FWKFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_KORE_FIRMENNR = FWK_FIRMENNR AND FWF_KORENR = FWK_KORENR" "~"
if joinKuerzel = KADFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = KAD_FWK_FIRMENNR AND FWK_KORENR = KAD_FWK_KORENR" "~"
if joinKuerzel = FWKKAD
searchResult := searchResult "LEFT JOIN KORE.KAD_AUSDRUCKDETAIL ON KAD_FWK_FIRMENNR = FWK_FIRMENNR AND KAD_FWK_KORENR = FWK_KORENR" "~"
if joinKuerzel = QAWGPO
searchResult := searchResult "LEFT JOIN BUERO.GPO_GRUPPENORDNER ON GPO_BAUMID = QAW_GPO_BAUMID AND GPO_GRUPPENORDNERNR = QAW_GPO_GRUPPENORDNERNR" "~"
if joinKuerzel = GPOQAW
searchResult := searchResult "LEFT JOIN BUERO.QAW_QUOTENAUSWERTUNG ON QAW_GPO_BAUMID = GPO_BAUMID AND QAW_GPO_GRUPPENORDNERNR = GPO_GRUPPENORDNERNR" "~"
if joinKuerzel = ARLGRT
searchResult := searchResult "LEFT JOIN WWS.GRT_GROESSENTAB ON GRT_FIRMENNR = ARL_FIRMENNR AND GRT_GROESSENTABNR = ARL_GROESSENTABNR" "~"
if joinKuerzel = GRTARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = GRT_FIRMENNR AND ARL_GROESSENTABNR = GRT_GROESSENTABNR" "~"
if joinKuerzel = GTPGRT
searchResult := searchResult "LEFT JOIN WWS.GRT_GROESSENTAB ON GRT_FIRMENNR = GTP_GRT_FIRMENNR AND GRT_GROESSENTABNR = GTP_GROESSENTABNR" "~"
if joinKuerzel = GRTGTP
searchResult := searchResult "LEFT JOIN WWS.GTP_GROESSENTABPOS ON GTP_GRT_FIRMENNR = GRT_FIRMENNR AND GTP_GROESSENTABNR = GRT_GROESSENTABNR" "~"
if joinKuerzel = PFDGZJ
searchResult := searchResult "LEFT JOIN HRS.GZJ_GLAEUBIGERZU_JAHR ON GZJ_DVFIRMENNR = PFD_DVFIRMENNR AND GZJ_FIRMENNR = PFD_FIRMENNR AND GZJ_MITARBEITERID = PFD_MITARBEITERID AND GZJ_GZJNR = PFD_GZJNR" "~"
if joinKuerzel = GZJPFD
searchResult := searchResult "LEFT JOIN HRS.PFD_PFAENDUNG_DETAIL ON PFD_DVFIRMENNR = GZJ_DVFIRMENNR AND PFD_FIRMENNR = GZJ_FIRMENNR AND PFD_MITARBEITERID = GZJ_MITARBEITERID AND PFD_GZJNR = GZJ_GZJNR" "~"
if joinKuerzel = GZGGZK
searchResult := searchResult "LEFT JOIN WWS.GZK_GELDZAEHLKOPF ON GZK_GELDZAEHLKOPFID = GZG_GELDZAEHLKOPFID" "~"
if joinKuerzel = GZKGZG
searchResult := searchResult "LEFT JOIN WWS.GZG_GELDZAEHLUNG ON GZG_GELDZAEHLKOPFID = GZK_GELDZAEHLKOPFID" "~"
if joinKuerzel = KSDGZK
searchResult := searchResult "LEFT JOIN WWS.GZK_GELDZAEHLKOPF ON GZK_GELDZAEHLKOPFID = KSD_GELDZAEHLKOPFID" "~"
if joinKuerzel = GZKKSD
searchResult := searchResult "LEFT JOIN WWS.KSD_KASSENSUMMENDETAIL ON KSD_GELDZAEHLKOPFID = GZK_GELDZAEHLKOPFID" "~"
if joinKuerzel = HSKHAM
searchResult := searchResult "LEFT JOIN HRS.HAM_AUSDRUCKMASTER ON HAM_LOHNAUSDRMASTERID = HSK_HAM_LOHNAUSDRMASTERID" "~"
if joinKuerzel = HAMHSK
searchResult := searchResult "LEFT JOIN HRS.HSK_SORTIERKOPF ON HSK_HAM_LOHNAUSDRMASTERID = HAM_LOHNAUSDRMASTERID" "~"
if joinKuerzel = PARHLB
searchResult := searchResult "LEFT JOIN HLB_HILFEBAUSTEIN.HLB_HILFEBAUSTEIN ON HLB_HLBID = PAR_HLBID" "~"
if joinKuerzel = HLBPAR
searchResult := searchResult "LEFT JOIN BMDDEFAULT.PAR_PARAM ON PAR_HLBID = HLB_HLBID" "~"
if joinKuerzel = DFUHLD
searchResult := searchResult "LEFT JOIN HLD_HILFEDOKUMENT.HLD_HILFEDOKUMENT ON HLD_HLDID = DFU_HLDID" "~"
if joinKuerzel = HLDDFU
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFU_DFUNKTION ON DFU_HLDID = HLD_HLDID" "~"
if joinKuerzel = HLZHLD
searchResult := searchResult "LEFT JOIN BMDENTW.HLD_HILFEDOKUMENT ON HLD_HLDID = HLZ_HLDID" "~"
if joinKuerzel = HLDHLZ
searchResult := searchResult "LEFT JOIN BMDENTW.HLZ_HILFEDOKBAUSTEINZU ON HLZ_HLDID = HLD_HLDID" "~"
if joinKuerzel = DFUHLD
searchResult := searchResult "LEFT JOIN HLD_HILFEDOKUMENT.HLD_HILFEDOKUMENT ON HLD_HLDID = DFU_HLDID_WEB" "~"
if joinKuerzel = HLDDFU
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFU_DFUNKTION ON DFU_HLDID_WEB = HLD_HLDID" "~"
if joinKuerzel = HPTHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_HONORARPOSNR = HPT_HONORARPOSNR" "~"
if joinKuerzel = HNPHPT
searchResult := searchResult "LEFT JOIN ZLE.HPT_HONORARPOSTEXTE ON HPT_HONORARPOSNR = HNP_HONORARPOSNR" "~"
if joinKuerzel = LEIHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_HONORARPOSNR = LEI_STORNO_HONORARPOSNR" "~"
if joinKuerzel = HNPLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_STORNO_HONORARPOSNR = HNP_HONORARPOSNR" "~"
if joinKuerzel = VASIFP
searchResult := searchResult "LEFT JOIN BUERO.IFP_INFOTABPOS ON IFP_INFOTABID = VAS_IFP_INFOTABID AND IFP_INFOTABPOSID = VAS_IFP_INFOTABPOSID" "~"
if joinKuerzel = IFPVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_IFP_INFOTABID = IFP_INFOTABID AND VAS_IFP_INFOTABPOSID = IFP_INFOTABPOSID" "~"
if joinKuerzel = IFZIFR
searchResult := searchResult "LEFT JOIN HRS.IFR_IMPORTFELDREFERENZ ON IFR_DBSNR = IFZ_DBSNR AND IFR_JAHR = IFZ_JAHR AND IFR_IFRID = IFZ_IFRID" "~"
if joinKuerzel = IFRIFZ
searchResult := searchResult "LEFT JOIN HRS.IFZ_IMPORTFELDREFERENZZU ON IFZ_DBSNR = IFR_DBSNR AND IFZ_JAHR = IFR_JAHR AND IFZ_IFRID = IFR_IFRID" "~"
if joinKuerzel = IMDIMM
searchResult := searchResult "LEFT JOIN BUERO.IMM_INSERATMEDIUMMASTER ON IMM_INSMEDMASTERID = IMD_INSMEDMASTERID" "~"
if joinKuerzel = IMMIMD
searchResult := searchResult "LEFT JOIN BUERO.IMD_INSERATMEDIUMDETAIL ON IMD_INSMEDMASTERID = IMM_INSMEDMASTERID" "~"
if joinKuerzel = AGSIMN
searchResult := searchResult "LEFT JOIN IMN_INSTANDHMASSN.IMN_INSTANDHMASSN ON IMN_IMNID = AGS_IMNID" "~"
if joinKuerzel = IMNAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_IMNID = IMN_IMNID" "~"
if joinKuerzel = AVEIMN
searchResult := searchResult "LEFT JOIN IMN_INSTANDHMASSN.IMN_INSTANDHMASSN ON IMN_IMNID = AVE_EFKESD_IMNID" "~"
if joinKuerzel = IMNAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_EFKESD_IMNID = IMN_IMNID" "~"
if joinKuerzel = ALEINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_IART = ALE_IART AND INS_LANDNR = ALE_LANDNR AND INS_INR = ALE_INR" "~"
if joinKuerzel = INSALE
searchResult := searchResult "LEFT JOIN BUERO.ALE_ADRESSENLISTE ON ALE_IART = INS_IART AND ALE_LANDNR = INS_LANDNR AND ALE_INR = INS_INR" "~"
if joinKuerzel = BDLINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_IART = BDL_INS_IART AND INS_LANDNR = BDL_INS_LANDNR AND INS_INR = BDL_INS_INR" "~"
if joinKuerzel = INSBDL
searchResult := searchResult "LEFT JOIN BUERO.BDL_BUERODELETE ON BDL_INS_IART = INS_IART AND BDL_INS_LANDNR = INS_LANDNR AND BDL_INS_INR = INS_INR" "~"
if joinKuerzel = GOIINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_IART = GOI_IART AND INS_LANDNR = GOI_LANDNR AND INS_INR = GOI_INR" "~"
if joinKuerzel = INSGOI
searchResult := searchResult "LEFT JOIN BUERO.GOI_GRUPPENORDNERINSTZU ON GOI_IART = INS_IART AND GOI_LANDNR = INS_LANDNR AND GOI_INR = INS_INR" "~"
if joinKuerzel = GZJINS
searchResult := searchResult "LEFT JOIN INS_INSTITUTION.INS_INSTITUTION ON INS_IART = GZJ_IART AND INS_LANDNR = GZJ_LANDNR AND INS_INR = GZJ_INR" "~"
if joinKuerzel = INSGZJ
searchResult := searchResult "LEFT JOIN HRS.GZJ_GLAEUBIGERZU_JAHR ON GZJ_IART = INS_IART AND GZJ_LANDNR = INS_LANDNR AND GZJ_INR = INS_INR" "~"
if joinKuerzel = FWKFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = FWK_STD_FIRMENNR AND FWK_KORENR = FWK_STD_KORENR" "~"
if joinKuerzel = FWKFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_STD_FIRMENNR = FWK_FIRMENNR AND FWK_STD_KORENR = FWK_KORENR" "~"
if joinKuerzel = KDIFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = KDI_FIRMENNR AND FWK_KORENR = KDI_KORENR" "~"
if joinKuerzel = FWKKDI
searchResult := searchResult "LEFT JOIN KORE.KDI_ISTWERTE ON KDI_FIRMENNR = FWK_FIRMENNR AND KDI_KORENR = FWK_KORENR" "~"
if joinKuerzel = KEKFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = KEK_FIRMENNR AND FWK_KORENR = KEK_KORENR" "~"
if joinKuerzel = FWKKEK
searchResult := searchResult "LEFT JOIN KORE.KEK_ERFASSKOPF ON KEK_FIRMENNR = FWK_FIRMENNR AND KEK_KORENR = FWK_KORENR" "~"
if joinKuerzel = KFZFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = KFZ_FIRMENNR AND FWK_KORENR = KFZ_KORENR" "~"
if joinKuerzel = FWKKFZ
searchResult := searchResult "LEFT JOIN KORE.KFZ_KOREFIBUZU ON KFZ_FIRMENNR = FWK_FIRMENNR AND KFZ_KORENR = FWK_KORENR" "~"
if joinKuerzel = KPMFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = KPM_FIRMENNR AND FWK_KORENR = KPM_KORENR" "~"
if joinKuerzel = FWKKPM
searchResult := searchResult "LEFT JOIN KORE.KPM_PLANMODELL ON KPM_FIRMENNR = FWK_FIRMENNR AND KPM_KORENR = FWK_KORENR" "~"
if joinKuerzel = SCXFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = SCX_FIRMENNR AND FWK_KORENR = SCX_KORENR" "~"
if joinKuerzel = FWKSCX
searchResult := searchResult "LEFT JOIN KORE.SCX_SUMMENCHECKEXPORT ON SCX_FIRMENNR = FWK_FIRMENNR AND SCX_KORENR = FWK_KORENR" "~"
if joinKuerzel = WSKFWK
searchResult := searchResult "LEFT JOIN FWK_FIRMENWERTEKORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = WSK_KST_FIRMENNR AND FWK_KORENR = WSK_KST_KORENR" "~"
if joinKuerzel = FWKWSK
searchResult := searchResult "LEFT JOIN WWS.WSK_WWSKORESTATISTIK ON WSK_KST_FIRMENNR = FWK_FIRMENNR AND WSK_KST_KORENR = FWK_KORENR" "~"
if joinKuerzel = FWKFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = FWK_FOLGEJAHR_FIRMENNR AND FWK_KORENR = FWK_FOLGEJAHR_KORENR" "~"
if joinKuerzel = FWKFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_FOLGEJAHR_FIRMENNR = FWK_FIRMENNR AND FWK_FOLGEJAHR_KORENR = FWK_KORENR" "~"
if joinKuerzel = FZEFZK
searchResult := searchResult "LEFT JOIN FIBU.FZK_ZAHLSCHLUESSELKOPF ON FZK_FIRMENNR = FZE_FIRMENNR AND FZK_FIBUNR = FZE_FIBUNR AND FZK_ZAHLSCHLUESSELNR = FZE_ZAHLSCHLUESSELNR" "~"
if joinKuerzel = FZKFZE
searchResult := searchResult "LEFT JOIN FIBU.FZE_ZAHLSCHLUESSELEINGABE ON FZE_FIRMENNR = FZK_FIRMENNR AND FZE_FIBUNR = FZK_FIBUNR AND FZE_ZAHLSCHLUESSELNR = FZK_ZAHLSCHLUESSELNR" "~"
if joinKuerzel = FGRGER
searchResult := searchResult "LEFT JOIN BUERO.GER_GERICHT ON GER_LANDNR = FGR_LANDNR AND GER_GERICHTSNR = FGR_GERICHTSNR" "~"
if joinKuerzel = GERFGR
searchResult := searchResult "LEFT JOIN BUERO.FGR_FIRMENGERICHTZU ON FGR_LANDNR = GER_LANDNR AND FGR_GERICHTSNR = GER_GERICHTSNR" "~"
if joinKuerzel = FIWGKK
searchResult := searchResult "LEFT JOIN BUERO.GKK_KRANKENKASSA ON GKK_LANDNR = FIW_GKK_LANDNR AND GKK_GKKNR = FIW_GKK_GKKNR" "~"
if joinKuerzel = GKKFIW
searchResult := searchResult "LEFT JOIN BUERO.FIW_FIRMENWERTE ON FIW_GKK_LANDNR = GKK_LANDNR AND FIW_GKK_GKKNR = GKK_GKKNR" "~"
if joinKuerzel = PGOGRO
searchResult := searchResult "LEFT JOIN WWS.GRO_GROESSE ON GRO_FIRMENNR = PGO_GRO_FIRMENNR AND GRO_GROESSENKBEZ = PGO_GRO_GROESSENKBEZ" "~"
if joinKuerzel = GROPGO
searchResult := searchResult "LEFT JOIN WWS.PGO_PICGROESSE ON PGO_GRO_FIRMENNR = GRO_FIRMENNR AND PGO_GRO_GROESSENKBEZ = GRO_GROESSENKBEZ" "~"
if joinKuerzel = GSKGSA
searchResult := searchResult "LEFT JOIN WWS.GSA_GUTSCHEINAKTION ON GSA_GSAID = GSK_GSAID" "~"
if joinKuerzel = GSAGSK
searchResult := searchResult "LEFT JOIN WWS.GSK_GUTSCHEINKOPF ON GSK_GSAID = GSA_GSAID" "~"
if joinKuerzel = BSPGSK
searchResult := searchResult "LEFT JOIN WWS.GSK_GUTSCHEINKOPF ON GSK_GUTSCHEINKOPFID = BSP_BONUS_GUTSCHEINKOPFID" "~"
if joinKuerzel = GSKBSP
searchResult := searchResult "LEFT JOIN WWS.BSP_BONUSERMPOS ON BSP_BONUS_GUTSCHEINKOPFID = GSK_GUTSCHEINKOPFID" "~"
if joinKuerzel = DGWGSW
searchResult := searchResult "LEFT JOIN GSW_GESETZWERT.GSW_GESETZWERT ON GSW_GESETZWERTNR = DGW_GESETZWERTNR" "~"
if joinKuerzel = GSWDGW
searchResult := searchResult "LEFT JOIN HRS.DGW_DBGESETZWERTE ON DGW_GESETZWERTNR = GSW_GESETZWERTNR" "~"
if joinKuerzel = PSMGWU
searchResult := searchResult "LEFT JOIN HRS.GWU_GWUNTERGRUPPE ON GWU_GWUID = PSM_GEWUNTERGR_GWUID" "~"
if joinKuerzel = GWUPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_GEWUNTERGR_GWUID = GWU_GWUID" "~"
if joinKuerzel = SUDGWZ
searchResult := searchResult "LEFT JOIN HRS.GWZ_GWBANKZU ON GWZ_GWZID = SUD_GWZID AND GWZ_LOHNJAHR = SUD_JAHR" "~"
if joinKuerzel = GWZSUD
searchResult := searchResult "LEFT JOIN HRS.SUD_SUM_KOE_DIFF ON SUD_GWZID = GWZ_GWZID AND SUD_JAHR = GWZ_LOHNJAHR" "~"
if joinKuerzel = PCKHNP
searchResult := searchResult "LEFT JOIN HNP_HONORARPOS.HNP_HONORARPOS ON HNP_HONORARPOSNR = PCK_HONORARPOSNR" "~"
if joinKuerzel = HNPPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_HONORARPOSNR = HNP_HONORARPOSNR" "~"
if joinKuerzel = MBMHVB
searchResult := searchResult "LEFT JOIN HRS.HVB_VERRECHNUNGSBASIS ON HVB_DBS_DATENBESTANDNR = MBM_DATENBESTANDNR AND HVB_LOHNJAHR = MBM_JAHR AND HVB_HVBLFDNR = MBM_BASIS" "~"
if joinKuerzel = HVBMBM
searchResult := searchResult "LEFT JOIN HRS.MBM_MBGM ON MBM_DATENBESTANDNR = HVB_DBS_DATENBESTANDNR AND MBM_JAHR = HVB_LOHNJAHR AND MBM_BASIS = HVB_HVBLFDNR" "~"
if joinKuerzel = OBLIFP
searchResult := searchResult "LEFT JOIN BUERO.IFP_INFOTABPOS ON IFP_INFOTABID = OBL_IFP_INFOTABID AND IFP_INFOTABPOSID = OBL_IFP_INFOTABPOSID" "~"
if joinKuerzel = IFPOBL
searchResult := searchResult "LEFT JOIN BUERO.OBL_ONLBEWLEBENSLAUF ON OBL_IFP_INFOTABID = IFP_INFOTABID AND OBL_IFP_INFOTABPOSID = IFP_INFOTABPOSID" "~"
if joinKuerzel = ONVIFP
searchResult := searchResult "LEFT JOIN IFP_INFOTABPOS.IFP_INFOTABPOS ON IFP_INFOTABID = ONV_INFOTABID AND IFP_INFOTABPOSID = ONV_INFOTABPOSID" "~"
if joinKuerzel = IFPONV
searchResult := searchResult "LEFT JOIN HRS.ONV_ONLINEBEW_VORDIENST ON ONV_INFOTABID = IFP_INFOTABID AND ONV_INFOTABPOSID = IFP_INFOTABPOSID" "~"
if joinKuerzel = ERZKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = ERZ_LIEFEREMPF_FIRMENNR AND KLI_KLID = ERZ_LIEFEREMPF_KLID" "~"
if joinKuerzel = KLIERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_LIEFEREMPF_FIRMENNR = KLI_FIRMENNR AND ERZ_LIEFEREMPF_KLID = KLI_KLID" "~"
if joinKuerzel = KASKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = KAS_KUNDENFIRMENNR AND KLI_KLID = KAS_KUNDENKLID" "~"
if joinKuerzel = KLIKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_KUNDENFIRMENNR = KLI_FIRMENNR AND KAS_KUNDENKLID = KLI_KLID" "~"
if joinKuerzel = LAJKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = LAJ_KLI_FIRMENNR AND KLI_KLID = LAJ_KLI_KLID" "~"
if joinKuerzel = KLILAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_KLI_FIRMENNR = KLI_FIRMENNR AND LAJ_KLI_KLID = KLI_KLID" "~"
if joinKuerzel = NUIKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = NUI_KLI_FIRMENNR AND KLI_KLID = NUI_KLID" "~"
if joinKuerzel = KLINUI
searchResult := searchResult "LEFT JOIN BMD.NUI_NTCSUPDINSTALLATION ON NUI_KLI_FIRMENNR = KLI_FIRMENNR AND NUI_KLID = KLI_KLID" "~"
if joinKuerzel = SLKKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = SLK_LIEFERANT_FIRMENNR AND KLI_KLID = SLK_LIEFERANT_ID" "~"
if joinKuerzel = KLISLK
searchResult := searchResult "LEFT JOIN WWS.SLK_STUECKLISTENKOPF ON SLK_LIEFERANT_FIRMENNR = KLI_FIRMENNR AND SLK_LIEFERANT_ID = KLI_KLID" "~"
if joinKuerzel = VSWKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = VSW_KLI_FIRMENNR AND KLI_KLID = VSW_KLID" "~"
if joinKuerzel = KLIVSW
searchResult := searchResult "LEFT JOIN WWS.VSW_VERTRETERSTATISTIK ON VSW_KLI_FIRMENNR = KLI_FIRMENNR AND VSW_KLID = KLI_KLID" "~"
if joinKuerzel = BONKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = BON_LIEFEREMPF_FIRMENNR AND KLI_KLID = BON_LIEFEREMPF_KLID" "~"
if joinKuerzel = KLIBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_LIEFEREMPF_FIRMENNR = KLI_FIRMENNR AND BON_LIEFEREMPF_KLID = KLI_KLID" "~"
if joinKuerzel = ERZKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = ERZ_VORSCHL_KLID_FIRMENNR AND KLI_KLID = ERZ_VORSCHL_KLID" "~"
if joinKuerzel = KLIERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_VORSCHL_KLID_FIRMENNR = KLI_FIRMENNR AND ERZ_VORSCHL_KLID = KLI_KLID" "~"
if joinKuerzel = KAKKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = KAK_LIEFEREMPF_FIRMENNR AND KLI_KLID = KAK_LIEFEREMPF_KLID" "~"
if joinKuerzel = KLIKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_LIEFEREMPF_FIRMENNR = KLI_FIRMENNR AND KAK_LIEFEREMPF_KLID = KLI_KLID" "~"
if joinKuerzel = WSSKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = WSS_KLI_FIRMENNR AND KLI_KLID = WSS_KLID" "~"
if joinKuerzel = KLIWSS
searchResult := searchResult "LEFT JOIN WWS.WSS_WWSSTATISTIK ON WSS_KLI_FIRMENNR = KLI_FIRMENNR AND WSS_KLID = KLI_KLID" "~"
if joinKuerzel = WSSKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = WSS_RECHEMPF_FIRMENNR AND KLI_KLID = WSS_RECHEMPF_KLID" "~"
if joinKuerzel = KLIWSS
searchResult := searchResult "LEFT JOIN WWS.WSS_WWSSTATISTIK ON WSS_RECHEMPF_FIRMENNR = KLI_FIRMENNR AND WSS_RECHEMPF_KLID = KLI_KLID" "~"
if joinKuerzel = RLPKOD
searchResult := searchResult "LEFT JOIN KOD_KONDITIONEN.KOD_KONDITIONEN ON KOD_KONDNR = RLP_KOD_KONDNR" "~"
if joinKuerzel = KODRLP
searchResult := searchResult "LEFT JOIN WWS.RLP_RECHNUNGSPLANPOS ON RLP_KOD_KONDNR = KOD_KONDNR" "~"
if joinKuerzel = FIRKOD
searchResult := searchResult "LEFT JOIN BUERO.KOD_KONDITIONEN ON KOD_KONDNR = FIR_LIEF_KONDNR" "~"
if joinKuerzel = KODFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_LIEF_KONDNR = KOD_KONDNR" "~"
if joinKuerzel = KOPKOK
searchResult := searchResult "LEFT JOIN PRODUKTION.KOK_KOMMISSIONIERUNG_KOPF ON KOK_FIRMENNR = KOP_FIRMENNR AND KOK_KOMID = KOP_KOMID" "~"
if joinKuerzel = KOKKOP
searchResult := searchResult "LEFT JOIN PRODUKTION.KOP_KOMMISSIONIERUNG_POS ON KOP_FIRMENNR = KOK_FIRMENNR AND KOP_KOMID = KOK_KOMID" "~"
if joinKuerzel = LPOKOK
searchResult := searchResult "LEFT JOIN KOK_KOMMISSIONIERUNG_KOPF.KOK_KOMMISSIONIERUNG_KOPF ON KOK_FIRMENNR = LPO_FIRMENNR AND KOK_KOMID = LPO_KOMID" "~"
if joinKuerzel = KOKLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_FIRMENNR = KOK_FIRMENNR AND LPO_KOMID = KOK_KOMID" "~"
if joinKuerzel = KOCKOM
searchResult := searchResult "LEFT JOIN PRODUKTION.KOM_KOMBI ON KOM_FIRMENNR = KOC_KOM_FIRMENNR AND KOM_KOMBI_NR = KOC_KOMBI_NR" "~"
if joinKuerzel = KOMKOC
searchResult := searchResult "LEFT JOIN PRODUKTION.KOC_KOMBI_CHARGE ON KOC_KOM_FIRMENNR = KOM_FIRMENNR AND KOC_KOMBI_NR = KOM_KOMBI_NR" "~"
if joinKuerzel = DISKOP
searchResult := searchResult "LEFT JOIN KOP_KOMMISSIONIERUNG_POS.KOP_KOMMISSIONIERUNG_POS ON KOP_FIRMENNR = DIS_FIRMENNR AND KOP_KOMID = DIS_KOP_KOMID AND KOP_KOPLFDNR = DIS_KOP_KOPLFDNR" "~"
if joinKuerzel = KOPDIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_FIRMENNR = KOP_FIRMENNR AND DIS_KOP_KOMID = KOP_KOMID AND DIS_KOP_KOPLFDNR = KOP_KOPLFDNR" "~"
if joinKuerzel = BONKSM
searchResult := searchResult "LEFT JOIN WWS.KSM_KASSENSUMMEN ON KSM_KASSENSUMMENID = BON_KSM_KASSENSUMMENID" "~"
if joinKuerzel = KSMBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_KSM_KASSENSUMMENID = KSM_KASSENSUMMENID" "~"
if joinKuerzel = KZZKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = KZZ_FIRMENNR AND KST_KORENR = KZZ_KORENR AND KST_KOSTENTYP = KZZ_KOSTENTYP AND KST_KOSTENSTAMMNR = KZZ_KOSTENSTAMMNR AND KST_KNOTENREFNR = KZZ_KNOTENREFNR" "~"
if joinKuerzel = KSTKZZ
searchResult := searchResult "LEFT JOIN KORE.KZZ_KSTKENNZAHLZU ON KZZ_FIRMENNR = KST_FIRMENNR AND KZZ_KORENR = KST_KORENR AND KZZ_KOSTENTYP = KST_KOSTENTYP AND KZZ_KOSTENSTAMMNR = KST_KOSTENSTAMMNR AND KZZ_KNOTENREFNR = KST_KNOTENREFNR" "~"
if joinKuerzel = AUGINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_IART = AUG_INS_IART AND INS_LANDNR = AUG_INS_LANDNR AND INS_INR = AUG_INS_INR" "~"
if joinKuerzel = INSAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_INS_IART = INS_IART AND AUG_INS_LANDNR = INS_LANDNR AND AUG_INS_INR = INS_INR" "~"
if joinKuerzel = GWUINS
searchResult := searchResult "LEFT JOIN INS_INSTITUTION.INS_INSTITUTION ON INS_IART = GWU_IART AND INS_LANDNR = GWU_LANDNR AND INS_INR = GWU_INR" "~"
if joinKuerzel = INSGWU
searchResult := searchResult "LEFT JOIN HRS.GWU_GWUNTERGRUPPE ON GWU_IART = INS_IART AND GWU_LANDNR = INS_LANDNR AND GWU_INR = INS_INR" "~"
if joinKuerzel = QAWINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_IART = QAW_INS_IART AND INS_LANDNR = QAW_INS_LANDNR AND INS_INR = QAW_INS_INR" "~"
if joinKuerzel = INSQAW
searchResult := searchResult "LEFT JOIN BUERO.QAW_QUOTENAUSWERTUNG ON QAW_INS_IART = INS_IART AND QAW_INS_LANDNR = INS_LANDNR AND QAW_INS_INR = INS_INR" "~"
if joinKuerzel = VEBINT
searchResult := searchResult "LEFT JOIN INT_INTRASTAT.INT_INTRASTAT ON INT_INTRASTATID = VEB_INT_EXTID" "~"
if joinKuerzel = INTVEB
searchResult := searchResult "LEFT JOIN FIBU.VEB_VORERFASSBUCHUNG ON VEB_INT_EXTID = INT_INTRASTATID" "~"
if joinKuerzel = AGSIOB
searchResult := searchResult "LEFT JOIN IOB_INSTANDHOBJEKT.IOB_INSTANDHOBJEKT ON IOB_IOBID = AGS_IOBID" "~"
if joinKuerzel = IOBAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_IOBID = IOB_IOBID" "~"
if joinKuerzel = ESDIOB
searchResult := searchResult "LEFT JOIN IOB_INSTANDHOBJEKT.IOB_INSTANDHOBJEKT ON IOB_IOBID = ESD_IOBID" "~"
if joinKuerzel = IOBESD
searchResult := searchResult "LEFT JOIN WWS.ESD_ERFASSUNGSERVICEDATEN ON ESD_IOBID = IOB_IOBID" "~"
if joinKuerzel = MEDIOB
searchResult := searchResult "LEFT JOIN BUERO.IOB_INSTANDHOBJEKT ON IOB_IOBID = MED_IOBID" "~"
if joinKuerzel = IOBMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_IOBID = IOB_IOBID" "~"
if joinKuerzel = WAGIOG
searchResult := searchResult "LEFT JOIN IOG_INSTANDHOBJEKTGRUPPE.IOG_INSTANDHOBJEKTGRUPPE ON IOG_IOGID = WAG_IOGID" "~"
if joinKuerzel = IOGWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_IOGID = IOG_IOGID" "~"
if joinKuerzel = VASITB
searchResult := searchResult "LEFT JOIN BUERO.ITB_INFOTABBEWERTUNG ON ITB_INFOTABLFDNR = VAS_ITB_INFOTABID AND ITB_BEWERTUNGSNR = VAS_ITB_BEWERTUNGSNR" "~"
if joinKuerzel = ITBVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_ITB_INFOTABID = ITB_INFOTABLFDNR AND VAS_ITB_BEWERTUNGSNR = ITB_BEWERTUNGSNR" "~"
if joinKuerzel = PLZITV
searchResult := searchResult "LEFT JOIN ITV_INTERVALL.ITV_INTERVALL ON ITV_INTERVALLID = PLZ_ITV_INTERVALLID" "~"
if joinKuerzel = ITVPLZ
searchResult := searchResult "LEFT JOIN BUERO.PLZ_PLANGRUPPEZU ON PLZ_ITV_INTERVALLID = ITV_INTERVALLID" "~"
if joinKuerzel = ZMBITV
searchResult := searchResult "LEFT JOIN ITV_INTERVALL.ITV_INTERVALL ON ITV_INTERVALLID = ZMB_INTERVALLID" "~"
if joinKuerzel = ITVZMB
searchResult := searchResult "LEFT JOIN BUERO.ZMB_ZEITMINDESTBESETZUNG ON ZMB_INTERVALLID = ITV_INTERVALLID" "~"
if joinKuerzel = ZRBITV
searchResult := searchResult "LEFT JOIN ITV_INTERVALL.ITV_INTERVALL ON ITV_INTERVALLID = ZRB_ITV_INTERVALLID" "~"
if joinKuerzel = ITVZRB
searchResult := searchResult "LEFT JOIN BUERO.ZRB_ZEITRUNDUNGBEDINGUNG ON ZRB_ITV_INTERVALLID = ITV_INTERVALLID" "~"
if joinKuerzel = AAKIVK
searchResult := searchResult "LEFT JOIN WWS.IVK_INVENTURKOPF ON IVK_INVKOPFID = AAK_INVKOPFID_VJ" "~"
if joinKuerzel = IVKAAK
searchResult := searchResult "LEFT JOIN WWS.AAK_ARAKOPF ON AAK_INVKOPFID_VJ = IVK_INVKOPFID" "~"
if joinKuerzel = AAKIVK
searchResult := searchResult "LEFT JOIN WWS.IVK_INVENTURKOPF ON IVK_INVKOPFID = AAK_INVKOPFID" "~"
if joinKuerzel = IVKAAK
searchResult := searchResult "LEFT JOIN WWS.AAK_ARAKOPF ON AAK_INVKOPFID = IVK_INVKOPFID" "~"
if joinKuerzel = ADBKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_KASSENPOSID = ADB_KAP_KASSENPOSID" "~"
if joinKuerzel = KAPADB
searchResult := searchResult "LEFT JOIN WWS.ADB_ABGABENDETAILBON ON ADB_KAP_KASSENPOSID = KAP_KASSENPOSID" "~"
if joinKuerzel = FMJKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_FIRMENNR = FMJ_KAS_FIRMENNR AND KAS_KASSENNR = FMJ_KAS_KASSENNR" "~"
if joinKuerzel = KASFMJ
searchResult := searchResult "LEFT JOIN WWS.FMJ_FISKALMODULJOURNAL ON FMJ_KAS_FIRMENNR = KAS_FIRMENNR AND FMJ_KAS_KASSENNR = KAS_KASSENNR" "~"
if joinKuerzel = GSFKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_FIRMENNR = GSF_FIRMENNR AND KAS_KASSENNR = GSF_KASSENNR" "~"
if joinKuerzel = KASGSF
searchResult := searchResult "LEFT JOIN WWS.GSF_GUTSCHEINFILIALDATEN ON GSF_FIRMENNR = KAS_FIRMENNR AND GSF_KASSENNR = KAS_KASSENNR" "~"
if joinKuerzel = HNPKAS
searchResult := searchResult "LEFT JOIN KAS_KASSA.KAS_KASSA ON KAS_FIRMENNR = HNP_KAS_FIRMENNR AND KAS_KASSENNR = HNP_KASSENNR" "~"
if joinKuerzel = KASHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_KAS_FIRMENNR = KAS_FIRMENNR AND HNP_KASSENNR = KAS_KASSENNR" "~"
if joinKuerzel = KAZKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_KATEGORIEKZ = KAZ_HKATEGORIEKZ AND KAT_KATEGORIELFDNR = KAZ_HKATEGORIELFDNR" "~"
if joinKuerzel = KATKAZ
searchResult := searchResult "LEFT JOIN BUERO.KAZ_KATEGORIEZU ON KAZ_HKATEGORIEKZ = KAT_KATEGORIEKZ AND KAZ_HKATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = KGSKAT
searchResult := searchResult "LEFT JOIN KAT_KATEGORIE.KAT_KATEGORIE ON KAT_KATEGORIEKZ = KGS_KAT_KATEGORIEKZ AND KAT_KATEGORIELFDNR = KGS_KAT_KATEGORIELFDNR" "~"
if joinKuerzel = KATKGS
searchResult := searchResult "LEFT JOIN WWS.KGS_KATZU_ARBEITSGANG ON KGS_KAT_KATEGORIEKZ = KAT_KATEGORIEKZ AND KGS_KAT_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = TERKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_KATEGORIEKZ = TER_KATEGORIEKZ AND KAT_KATEGORIELFDNR = TER_KATEGORIELFDNR" "~"
if joinKuerzel = KATTER
searchResult := searchResult "LEFT JOIN BUERO.TER_TERMIN ON TER_KATEGORIEKZ = KAT_KATEGORIEKZ AND TER_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = WIIKAT
searchResult := searchResult "LEFT JOIN KAT_KATEGORIE.KAT_KATEGORIE ON KAT_KATEGORIEKZ = WII_PAKETKZ AND KAT_KATEGORIELFDNR = WII_PAKETLFDNR" "~"
if joinKuerzel = KATWII
searchResult := searchResult "LEFT JOIN BMDENTW.WII_WEITERE_INFO_ITEM ON WII_PAKETKZ = KAT_KATEGORIEKZ AND WII_PAKETLFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = AIZIFT
searchResult := searchResult "LEFT JOIN IFT_INFOTAB.IFT_INFOTAB ON IFT_INFOTABID = AIZ_INFOTABID" "~"
if joinKuerzel = IFTAIZ
searchResult := searchResult "LEFT JOIN PRODUKTION.AIZ_AGPER_INFOZU ON AIZ_INFOTABID = IFT_INFOTABID" "~"
if joinKuerzel = ITBIFT
searchResult := searchResult "LEFT JOIN BUERO.IFT_INFOTAB ON IFT_INFOTABID = ITB_INFOTABLFDNR" "~"
if joinKuerzel = IFTITB
searchResult := searchResult "LEFT JOIN BUERO.ITB_INFOTABBEWERTUNG ON ITB_INFOTABLFDNR = IFT_INFOTABID" "~"
if joinKuerzel = AINIMD
searchResult := searchResult "LEFT JOIN BUERO.IMD_INSERATMEDIUMDETAIL ON IMD_INSMEDDETAILID = AIN_IMD_INSMEDDETAILID" "~"
if joinKuerzel = IMDAIN
searchResult := searchResult "LEFT JOIN BUERO.AIN_AUSSCHREIBUNGINSERTZU ON AIN_IMD_INSMEDDETAILID = IMD_INSMEDDETAILID" "~"
if joinKuerzel = GWZINS
searchResult := searchResult "LEFT JOIN INS_INSTITUTION.INS_INSTITUTION ON INS_IART = GWZ_IART AND INS_LANDNR = GWZ_LANDNR AND INS_INR = GWZ_INR" "~"
if joinKuerzel = INSGWZ
searchResult := searchResult "LEFT JOIN HRS.GWZ_GWBANKZU ON GWZ_IART = INS_IART AND GWZ_LANDNR = INS_LANDNR AND GWZ_INR = INS_INR" "~"
if joinKuerzel = LEBIOB
searchResult := searchResult "LEFT JOIN BUERO.IOB_INSTANDHOBJEKT ON IOB_IOBID = LEB_IOBID" "~"
if joinKuerzel = IOBLEB
searchResult := searchResult "LEFT JOIN BUERO.LEB_LEBENSLAUF ON LEB_IOBID = IOB_IOBID" "~"
if joinKuerzel = PCKIOB
searchResult := searchResult "LEFT JOIN IOB_INSTANDHOBJEKT.IOB_INSTANDHOBJEKT ON IOB_IOBID = PCK_IOBID" "~"
if joinKuerzel = IOBPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_IOBID = IOB_IOBID" "~"
if joinKuerzel = ARLIOG
searchResult := searchResult "LEFT JOIN IOG_INSTANDHOBJEKTGRUPPE.IOG_INSTANDHOBJEKTGRUPPE ON IOG_IOGID = ARL_IOGID" "~"
if joinKuerzel = IOGARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_IOGID = IOG_IOGID" "~"
if joinKuerzel = AGIITB
searchResult := searchResult "LEFT JOIN ITB_INFOTABBEWERTUNG.ITB_INFOTABBEWERTUNG ON ITB_INFOTABLFDNR = AGI_INFOTABID AND ITB_BEWERTUNGSNR = AGI_BEWERTUNGSNR" "~"
if joinKuerzel = ITBAGI
searchResult := searchResult "LEFT JOIN WWS.AGI_AGPER_INFOZU ON AGI_INFOTABID = ITB_INFOTABLFDNR AND AGI_BEWERTUNGSNR = ITB_BEWERTUNGSNR" "~"
if joinKuerzel = ZBKITV
searchResult := searchResult "LEFT JOIN ITV_INTERVALL.ITV_INTERVALL ON ITV_INTERVALLID = ZBK_INTERVALLID" "~"
if joinKuerzel = ITVZBK
searchResult := searchResult "LEFT JOIN BUERO.ZBK_ZEITBETRIEBSKALENDER ON ZBK_INTERVALLID = ITV_INTERVALLID" "~"
if joinKuerzel = INZIVZ
searchResult := searchResult "LEFT JOIN BUERO.IVZ_INSOLVENZ ON IVZ_IVZID = INZ_IVZID" "~"
if joinKuerzel = IVZINZ
searchResult := searchResult "LEFT JOIN BUERO.INZ_INSOLVENZPERSONZU ON INZ_IVZID = IVZ_IVZID" "~"
if joinKuerzel = KEPKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_FIRMENNR = KEP_KAS_FIRMENNR AND KAS_KASSENNR = KEP_KAS_KASSENNR" "~"
if joinKuerzel = KASKEP
searchResult := searchResult "LEFT JOIN WWS.KEP_KASSENERFASSUNGSPROT ON KEP_KAS_FIRMENNR = KAS_FIRMENNR AND KEP_KAS_KASSENNR = KAS_KASSENNR" "~"
if joinKuerzel = KZAKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_FIRMENNR = KZA_KAS_FIRMENNR AND KAS_KASSENNR = KZA_KASSENNR" "~"
if joinKuerzel = KASKZA
searchResult := searchResult "LEFT JOIN WWS.KZA_KASSENZAHLUNGSARTENZU ON KZA_KAS_FIRMENNR = KAS_FIRMENNR AND KZA_KASSENNR = KAS_KASSENNR" "~"
if joinKuerzel = FAVKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_KATEGORIEKZ = FAV_KATEGORIEKZ AND KAT_KATEGORIELFDNR = FAV_KATEGORIELFDNR" "~"
if joinKuerzel = KATFAV
searchResult := searchResult "LEFT JOIN BUERO.FAV_FAVORITEN ON FAV_KATEGORIEKZ = KAT_KATEGORIEKZ AND FAV_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = KEEKEK
searchResult := searchResult "LEFT JOIN KORE.KEK_ERFASSKOPF ON KEK_FIRMENNR = KEE_FIRMENNR AND KEK_KORENR = KEE_KORENR AND KEK_KOPF_SATZKZ = KEE_KOPF_SATZKZ AND KEK_KOPF_ERFASSNR = KEE_KOPF_ERFASSNR" "~"
if joinKuerzel = KEKKEE
searchResult := searchResult "LEFT JOIN KORE.KEE_ERFASSEINGABE ON KEE_FIRMENNR = KEK_FIRMENNR AND KEE_KORENR = KEK_KORENR AND KEE_KOPF_SATZKZ = KEK_KOPF_SATZKZ AND KEE_KOPF_ERFASSNR = KEK_KOPF_ERFASSNR" "~"
if joinKuerzel = BAIKKA
searchResult := searchResult "LEFT JOIN BUERO.KKA_KREDITKARTENART ON KKA_KREDITKARTENARTNR = BAI_KREDITKARTENARTNR" "~"
if joinKuerzel = KKABAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_KREDITKARTENARTNR = KKA_KREDITKARTENARTNR" "~"
if joinKuerzel = AGSKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = AGS_AUFTRAGNEHMER_FIRMENNR AND KLI_KLID = AGS_AUFTRAGNEHMER_KLID" "~"
if joinKuerzel = KLIAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_AUFTRAGNEHMER_FIRMENNR = KLI_FIRMENNR AND AGS_AUFTRAGNEHMER_KLID = KLI_KLID" "~"
if joinKuerzel = BVLKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = BVL_LIEFERANT_FIRMENNR AND KLI_KLID = BVL_LIEFERANT_KLID" "~"
if joinKuerzel = KLIBVL
searchResult := searchResult "LEFT JOIN WWS.BVL_LIEFERANTENZUORDNUNG ON BVL_LIEFERANT_FIRMENNR = KLI_FIRMENNR AND BVL_LIEFERANT_KLID = KLI_KLID" "~"
if joinKuerzel = MFLKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = MFL_FIRMENNR AND KLI_KLID = MFL_KLID" "~"
if joinKuerzel = KLIMFL
searchResult := searchResult "LEFT JOIN BMDINTERN.MFL_MFLIZENZEN ON MFL_FIRMENNR = KLI_FIRMENNR AND MFL_KLID = KLI_KLID" "~"
if joinKuerzel = PIFKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = PIF_FIRMENNR AND KLI_KLID = PIF_KLID" "~"
if joinKuerzel = KLIPIF
searchResult := searchResult "LEFT JOIN WWS.PIF_PICKUNDENZU ON PIF_FIRMENNR = KLI_FIRMENNR AND PIF_KLID = KLI_KLID" "~"
if joinKuerzel = SCMKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = SCM_TIC_KUNDE_FIRMENNR AND KLI_KLID = SCM_TIC_KUNDE_KLID" "~"
if joinKuerzel = KLISCM
searchResult := searchResult "LEFT JOIN BMDENTW.SCM_SCRMAIN ON SCM_TIC_KUNDE_FIRMENNR = KLI_FIRMENNR AND SCM_TIC_KUNDE_KLID = KLI_KLID" "~"
if joinKuerzel = WLIKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = WLI_FIRMENNR AND KLI_KLID = WLI_KLID" "~"
if joinKuerzel = KLIWLI
searchResult := searchResult "LEFT JOIN WWS_NEU.WLI_WWSLIEFERANT ON WLI_FIRMENNR = KLI_FIRMENNR AND WLI_KLID = KLI_KLID" "~"
if joinKuerzel = IVPIAP
searchResult := searchResult "LEFT JOIN WWS.IAP_INVABWTPOS ON IAP_IAPSEQID = IVP_IAPSEQID" "~"
if joinKuerzel = IAPIVP
searchResult := searchResult "LEFT JOIN WWS.IVP_INVENTURPOSITION ON IVP_IAPSEQID = IAP_IAPSEQID" "~"
if joinKuerzel = ASMIFP
searchResult := searchResult "LEFT JOIN BUERO.IFP_INFOTABPOS ON IFP_INFOTABID = ASM_IFP_INFOTABID AND IFP_INFOTABPOSID = ASM_IFP_INFOTABPOSID" "~"
if joinKuerzel = IFPASM
searchResult := searchResult "LEFT JOIN BUERO.ASM_AUSBILDUNGSSTAMMMERKM ON ASM_IFP_INFOTABID = IFP_INFOTABID AND ASM_IFP_INFOTABPOSID = IFP_INFOTABPOSID" "~"
if joinKuerzel = DODOTD
searchResult := searchResult "LEFT JOIN OTD_OCRTEMPLATEDETAIL.OTD_OCRTEMPLATEDETAIL ON OTD_OCRID = DOD_OCRID_INH AND OTD_OCRLFDNR = DOD_OCRLFDNR" "~"
if joinKuerzel = OTDDOD
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DOD_OCRTEMPLATEDETAIL ON DOD_OCRID_INH = OTD_OCRID AND DOD_OCRLFDNR = OTD_OCRLFDNR" "~"
if joinKuerzel = DFZFEZ
searchResult := searchResult "LEFT JOIN FEZ_FELDGRUPPENZU.FEZ_FELDGRUPPENZU ON FEZ_HAUPTGRUPPE = DFZ_HAUPTGRUPPE AND FEZ_UNTERGRUPPE = DFZ_UNTERGRUPPE AND FEZ_TABELLENNAME = DFZ_TABELLENNAME AND FEZ_FELDNAME = DFZ_FELDNAME" "~"
if joinKuerzel = FEZDFZ
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFZ_DFELDGRUPPENZU ON DFZ_HAUPTGRUPPE = FEZ_HAUPTGRUPPE AND DFZ_UNTERGRUPPE = FEZ_UNTERGRUPPE AND DFZ_TABELLENNAME = FEZ_TABELLENNAME AND DFZ_FELDNAME = FEZ_FELDNAME" "~"
if joinKuerzel = BAIINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_IART = BAI_IART AND INS_LANDNR = BAI_LANDNR AND INS_INR = BAI_INR" "~"
if joinKuerzel = INSBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_IART = INS_IART AND BAI_LANDNR = INS_LANDNR AND BAI_INR = INS_INR" "~"
if joinKuerzel = KJOINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_IART = KJO_INS_IART AND INS_LANDNR = KJO_INS_LANDNR AND INS_INR = KJO_INS_INR" "~"
if joinKuerzel = INSKJO
searchResult := searchResult "LEFT JOIN BUERO.KJO_KONTAKTJOURNAL ON KJO_INS_IART = INS_IART AND KJO_INS_LANDNR = INS_LANDNR AND KJO_INS_INR = INS_INR" "~"
if joinKuerzel = AGUIOB
searchResult := searchResult "LEFT JOIN IOB_INSTANDHOBJEKT.IOB_INSTANDHOBJEKT ON IOB_IOBID = AGU_IOBID" "~"
if joinKuerzel = IOBAGU
searchResult := searchResult "LEFT JOIN BIL.AGU_ANLAGEGUT ON AGU_IOBID = IOB_IOBID" "~"
if joinKuerzel = IPDIPM
searchResult := searchResult "LEFT JOIN HRS.IPM_INDIVPROGM ON IPM_DBSNR = IPD_DBSNR AND IPM_IPMNR = IPD_IPMNR AND IPM_PGM = IPD_PGM" "~"
if joinKuerzel = IPMIPD
searchResult := searchResult "LEFT JOIN HRS.IPD_INDIVPROGD ON IPD_DBSNR = IPM_DBSNR AND IPD_IPMNR = IPM_IPMNR AND IPD_PGM = IPM_PGM" "~"
if joinKuerzel = MGZITB
searchResult := searchResult "LEFT JOIN BUERO.ITB_INFOTABBEWERTUNG ON ITB_INFOTABLFDNR = MGZ_IFT_INFOTABID AND ITB_BEWERTUNGSNR = MGZ_ITB_BEWERTUNGSNR" "~"
if joinKuerzel = ITBMGZ
searchResult := searchResult "LEFT JOIN BUERO.MGZ_MERKMALSGRUPPEZU ON MGZ_IFT_INFOTABID = ITB_INFOTABLFDNR AND MGZ_ITB_BEWERTUNGSNR = ITB_BEWERTUNGSNR" "~"
if joinKuerzel = WUZITV
searchResult := searchResult "LEFT JOIN ITV_INTERVALL.ITV_INTERVALL ON ITV_INTERVALLID = WUZ_INTERVALLID" "~"
if joinKuerzel = ITVWUZ
searchResult := searchResult "LEFT JOIN BUERO.WUZ_WUNSCHARBEITSZEIT ON WUZ_INTERVALLID = ITV_INTERVALLID" "~"
if joinKuerzel = AKTIVG
searchResult := searchResult "LEFT JOIN IVG_INTERVALLGRUPPE.IVG_INTERVALLGRUPPE ON IVG_INTERVALLGRPID = AKT_INTERVALLGRPID" "~"
if joinKuerzel = IVGAKT
searchResult := searchResult "LEFT JOIN WWS.AKT_AKTIONSSTAMM ON AKT_INTERVALLGRPID = IVG_INTERVALLGRPID" "~"
if joinKuerzel = ADBKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_KASSENKOPFID = ADB_KAK_KASSENKOPFID" "~"
if joinKuerzel = KAKADB
searchResult := searchResult "LEFT JOIN WWS.ADB_ABGABENDETAILBON ON ADB_KAK_KASSENKOPFID = KAK_KASSENKOPFID" "~"
if joinKuerzel = KAEKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_KASSENKOPFID = KAE_KASSENKOPFID" "~"
if joinKuerzel = KAKKAE
searchResult := searchResult "LEFT JOIN WWS.KAE_KASSENEINAUSGABEN ON KAE_KASSENKOPFID = KAK_KASSENKOPFID" "~"
if joinKuerzel = KAPKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_KASSENKOPFID = KAP_KASSENKOPFID" "~"
if joinKuerzel = KAKKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_KASSENKOPFID = KAK_KASSENKOPFID" "~"
if joinKuerzel = KSPKAM
searchResult := searchResult "LEFT JOIN KORE.KAM_AUSDRUCKMASTER ON KAM_AUSDRMASTERNR = KSP_KAM_AUSDRMASTERNR" "~"
if joinKuerzel = KAMKSP
searchResult := searchResult "LEFT JOIN KORE.KSP_SPALTENKOPF ON KSP_KAM_AUSDRMASTERNR = KAM_AUSDRMASTERNR" "~"
if joinKuerzel = KAPKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_KASSENPOSID = KAP_HAUPT_KASSENPOSID" "~"
if joinKuerzel = KAPKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_HAUPT_KASSENPOSID = KAP_KASSENPOSID" "~"
if joinKuerzel = FIRKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_KATEGORIEKZ = FIR_DBNAME_KATEGORIEKZ AND KAT_KATEGORIELFDNR = FIR_DBNAME_KATEGORIELFDNR" "~"
if joinKuerzel = KATFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_DBNAME_KATEGORIEKZ = KAT_KATEGORIEKZ AND FIR_DBNAME_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = MITKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_KATEGORIEKZ = MIT_WOHN_KATEGORIEKZ AND KAT_KATEGORIELFDNR = MIT_WOHN_KATEGORIELFDNR" "~"
if joinKuerzel = KATMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_WOHN_KATEGORIEKZ = KAT_KATEGORIEKZ AND MIT_WOHN_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = PALKAT
searchResult := searchResult "LEFT JOIN KAT_KATEGORIE.KAT_KATEGORIE ON KAT_KATEGORIEKZ = PAL_KATEGORIEKZ AND KAT_KATEGORIELFDNR = PAL_KATEGORIELFDNR" "~"
if joinKuerzel = KATPAL
searchResult := searchResult "LEFT JOIN ZLE.PAL_PAUSCHALE ON PAL_KATEGORIEKZ = KAT_KATEGORIEKZ AND PAL_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = PROKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_KATEGORIEKZ = PRO_KATEGORIEKZ AND KAT_KATEGORIELFDNR = PRO_KATEGORIELFDNR" "~"
if joinKuerzel = KATPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_KATEGORIEKZ = KAT_KATEGORIEKZ AND PRO_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = STTKAT
searchResult := searchResult "LEFT JOIN KAT_KATEGORIE.KAT_KATEGORIE ON KAT_KATEGORIEKZ = STT_KAT_KATEGORIEKZ AND KAT_KATEGORIELFDNR = STT_KAT_KATEGORIELFDNR" "~"
if joinKuerzel = KATSTT
searchResult := searchResult "LEFT JOIN PRODUKTION.STT_STAKE_TYP ON STT_KAT_KATEGORIEKZ = KAT_KATEGORIEKZ AND STT_KAT_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = KGLFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = KGL_FIRMENNR AND FWK_KORENR = KGL_KORENR" "~"
if joinKuerzel = FWKKGL
searchResult := searchResult "LEFT JOIN KORE.KGL_KOREGLIEDERUNG ON KGL_FIRMENNR = FWK_FIRMENNR AND KGL_KORENR = FWK_KORENR" "~"
if joinKuerzel = FIZFZU
searchResult := searchResult "LEFT JOIN FIBU.FZU_FIBUFIBUZU ON FZU_FIRMENNR = FIZ_FIRMENNR AND FZU_FIBUNR = FIZ_FIBUNR AND FZU_ZUGEORDNETE_FIRMENNR = FIZ_ZUGEORDNETE_FIRMENNR AND FZU_ZUGEORDNETE_FIBUNR = FIZ_ZUGEORDNETE_FIBUNR" "~"
if joinKuerzel = FZUFIZ
searchResult := searchResult "LEFT JOIN FIBU.FIZ_FILIALEKONSZU ON FIZ_FIRMENNR = FZU_FIRMENNR AND FIZ_FIBUNR = FZU_FIBUNR AND FIZ_ZUGEORDNETE_FIRMENNR = FZU_ZUGEORDNETE_FIRMENNR AND FIZ_ZUGEORDNETE_FIBUNR = FZU_ZUGEORDNETE_FIBUNR" "~"
if joinKuerzel = GVMGAD
searchResult := searchResult "LEFT JOIN BUERO.GAD_GHOSTADRESSE ON GAD_GHOSTADRESSID = GVM_GHOSTADRESSID" "~"
if joinKuerzel = GADGVM
searchResult := searchResult "LEFT JOIN BUERO.GVM_GHOSTVOLLMACHT ON GVM_GHOSTADRESSID = GAD_GHOSTADRESSID" "~"
if joinKuerzel = ERPVKG
searchResult := searchResult "LEFT JOIN KORE.VKG_VIEW_KOSTAMM_GBR ON VKG_FIRMENNR = ERP_FIRMENNR AND VKG_KOSTENSTAMMNR = ERP_GBR_KOSTENSTAMMNR" "~"
if joinKuerzel = VKGERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_FIRMENNR = VKG_FIRMENNR AND ERP_GBR_KOSTENSTAMMNR = VKG_KOSTENSTAMMNR" "~"
if joinKuerzel = ERZGEA
searchResult := searchResult "LEFT JOIN GEA_GESCHAEFTSART.GEA_GESCHAEFTSART ON GEA_FIRMENNR = ERZ_GEA_FIRMENNR AND GEA_GEACODE = ERZ_GEACODE" "~"
if joinKuerzel = GEAERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_GEA_FIRMENNR = GEA_FIRMENNR AND ERZ_GEACODE = GEA_GEACODE" "~"
if joinKuerzel = GEPGEF
searchResult := searchResult "LEFT JOIN WWS.GEF_GEFAHRENGUTSTAMM ON GEF_GEFGUTID = GEP_GEFGUTID" "~"
if joinKuerzel = GEFGEP
searchResult := searchResult "LEFT JOIN WWS.GEP_GEFAHRENGUTPOS ON GEP_GEFGUTID = GEF_GEFGUTID" "~"
if joinKuerzel = PGMGEM
searchResult := searchResult "LEFT JOIN BUERO.GEM_GEMEINDE ON GEM_LANDNR = PGM_LANDNR AND GEM_GEMEINDENR = PGM_GEMEINDENR" "~"
if joinKuerzel = GEMPGM
searchResult := searchResult "LEFT JOIN BUERO.PGM_PERSONGEMEINDEZU ON PGM_LANDNR = GEM_LANDNR AND PGM_GEMEINDENR = GEM_GEMEINDENR" "~"
if joinKuerzel = PVZGPO
searchResult := searchResult "LEFT JOIN GPO_GRUPPENORDNER.GPO_GRUPPENORDNER ON GPO_BAUMID = PVZ_GPO_BAUMID AND GPO_GRUPPENORDNERNR = PVZ_GPO_GRUPPENORDNERNR" "~"
if joinKuerzel = GPOPVZ
searchResult := searchResult "LEFT JOIN WWS.PVZ_PROVISIONTABZU ON PVZ_GPO_BAUMID = GPO_BAUMID AND PVZ_GPO_GRUPPENORDNERNR = GPO_GRUPPENORDNERNR" "~"
if joinKuerzel = RERGRO
searchResult := searchResult "LEFT JOIN WWS.GRO_GROESSE ON GRO_FIRMENNR = RER_GROESSE_FIRMENNR AND GRO_GROESSENKBEZ = RER_GROESSENKBEZ" "~"
if joinKuerzel = GRORER
searchResult := searchResult "LEFT JOIN WWS.RER_RESERVIERUNGSREFERENZ ON RER_GROESSE_FIRMENNR = GRO_FIRMENNR AND RER_GROESSENKBEZ = GRO_GROESSENKBEZ" "~"
if joinKuerzel = WAGGRT
searchResult := searchResult "LEFT JOIN WWS.GRT_GROESSENTAB ON GRT_FIRMENNR = WAG_FIRMENNR AND GRT_GROESSENTABNR = WAG_GROESSENTABNR" "~"
if joinKuerzel = GRTWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = GRT_FIRMENNR AND WAG_GROESSENTABNR = GRT_GROESSENTABNR" "~"
if joinKuerzel = PSMGWZ
searchResult := searchResult "LEFT JOIN HRS.GWZ_GWBANKZU ON GWZ_GWZID = PSM_GWZ_HRSSEQID AND GWZ_LOHNJAHR = PSM_PSMJAHR" "~"
if joinKuerzel = GWZPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_GWZ_HRSSEQID = GWZ_GWZID AND PSM_PSMJAHR = GWZ_LOHNJAHR" "~"
if joinKuerzel = HLFHLB
searchResult := searchResult "LEFT JOIN BMDENTW.HLB_HILFEBAUSTEIN ON HLB_HLBID = HLF_HLBID" "~"
if joinKuerzel = HLBHLF
searchResult := searchResult "LEFT JOIN BMDENTW.HLF_HILFEFAVORITEN ON HLF_HLBID = HLB_HLBID" "~"
if joinKuerzel = PARHLD
searchResult := searchResult "LEFT JOIN HLD_HILFEDOKUMENT.HLD_HILFEDOKUMENT ON HLD_HLDID = PAR_HLDID" "~"
if joinKuerzel = HLDPAR
searchResult := searchResult "LEFT JOIN BMDDEFAULT.PAR_PARAM ON PAR_HLDID = HLD_HLDID" "~"
if joinKuerzel = HADHSP
searchResult := searchResult "LEFT JOIN HRS.HSP_SPALTENKOPF ON HSP_LOHNSPALTENKOPFID = HAD_HSP_LOHNSPALTENKOPFID" "~"
if joinKuerzel = HSPHAD
searchResult := searchResult "LEFT JOIN HRS.HAD_AUSDRUCKDETAIL ON HAD_HSP_LOHNSPALTENKOPFID = HSP_LOHNSPALTENKOPFID" "~"
if joinKuerzel = ZLZHZL
searchResult := searchResult "LEFT JOIN BUERO.HZL_HAUPTZIEL ON HZL_HZLID = ZLZ_HZLID" "~"
if joinKuerzel = HZLZLZ
searchResult := searchResult "LEFT JOIN BUERO.ZLZ_ZIELZUORDNUNG ON ZLZ_HZLID = HZL_HZLID" "~"
if joinKuerzel = IAPIAK
searchResult := searchResult "LEFT JOIN WWS.IAK_INVABWTKOPF ON IAK_IAKSEQID = IAP_IAKSEQID" "~"
if joinKuerzel = IAKIAP
searchResult := searchResult "LEFT JOIN WWS.IAP_INVABWTPOS ON IAP_IAKSEQID = IAK_IAKSEQID" "~"
if joinKuerzel = MGZIFP
searchResult := searchResult "LEFT JOIN BUERO.IFP_INFOTABPOS ON IFP_INFOTABID = MGZ_IFT_INFOTABID AND IFP_INFOTABPOSID = MGZ_IFP_INFOTABPOSID" "~"
if joinKuerzel = IFPMGZ
searchResult := searchResult "LEFT JOIN BUERO.MGZ_MERKMALSGRUPPEZU ON MGZ_IFT_INFOTABID = IFP_INFOTABID AND MGZ_IFP_INFOTABPOSID = IFP_INFOTABPOSID" "~"
if joinKuerzel = OBQIFP
searchResult := searchResult "LEFT JOIN BUERO.IFP_INFOTABPOS ON IFP_INFOTABID = OBQ_IFP_INFOTABID AND IFP_INFOTABPOSID = OBQ_IFP_INFOTABPOSID" "~"
if joinKuerzel = IFPOBQ
searchResult := searchResult "LEFT JOIN BUERO.OBQ_ONLBEWQUALIFIKATION ON OBQ_IFP_INFOTABID = IFP_INFOTABID AND OBQ_IFP_INFOTABPOSID = IFP_INFOTABPOSID" "~"
if joinKuerzel = VZEIFP
searchResult := searchResult "LEFT JOIN BUERO.IFP_INFOTABPOS ON IFP_INFOTABID = VZE_INFOTABLFDNR AND IFP_INFOTABPOSID = VZE_INFOTABPOSLFDNR" "~"
if joinKuerzel = IFPVZE
searchResult := searchResult "LEFT JOIN BUERO.VZE_VORDIENSTZEITEN ON VZE_INFOTABLFDNR = IFP_INFOTABID AND VZE_INFOTABPOSLFDNR = IFP_INFOTABPOSID" "~"
if joinKuerzel = IFPIFT
searchResult := searchResult "LEFT JOIN BUERO.IFT_INFOTAB ON IFT_INFOTABID = IFP_INFOTABID" "~"
if joinKuerzel = IFTIFP
searchResult := searchResult "LEFT JOIN BUERO.IFP_INFOTABPOS ON IFP_INFOTABID = IFT_INFOTABID" "~"
if joinKuerzel = WOHIMO
searchResult := searchResult "LEFT JOIN BUERO.IMO_IMMOBILIE ON IMO_FIRMENNR = WOH_FIRMENNR AND IMO_IMMOBILIENNR = WOH_IMMOBILIENNR" "~"
if joinKuerzel = IMOWOH
searchResult := searchResult "LEFT JOIN BUERO.WOH_WOHNUNG ON WOH_FIRMENNR = IMO_FIRMENNR AND WOH_IMMOBILIENNR = IMO_IMMOBILIENNR" "~"
if joinKuerzel = IKZINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_IART = IKZ_IART AND INS_LANDNR = IKZ_LANDNR AND INS_INR = IKZ_INR" "~"
if joinKuerzel = INSIKZ
searchResult := searchResult "LEFT JOIN BUERO.IKZ_INSTITUTIONKONTAKTZU ON IKZ_IART = INS_IART AND IKZ_LANDNR = INS_LANDNR AND IKZ_INR = INS_INR" "~"
if joinKuerzel = KONINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_IART = KON_IART AND INS_LANDNR = KON_LANDNR AND INS_INR = KON_INR" "~"
if joinKuerzel = INSKON
searchResult := searchResult "LEFT JOIN BUERO.KON_KONTAKTPERSON ON KON_IART = INS_IART AND KON_LANDNR = INS_LANDNR AND KON_INR = INS_INR" "~"
if joinKuerzel = POSINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_IART = POS_IART AND INS_LANDNR = POS_LANDNR AND INS_INR = POS_INR" "~"
if joinKuerzel = INSPOS
searchResult := searchResult "LEFT JOIN BUERO.POS_POST ON POS_IART = INS_IART AND POS_LANDNR = INS_LANDNR AND POS_INR = INS_INR" "~"
if joinKuerzel = TELINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_IART = TEL_IART AND INS_LANDNR = TEL_LANDNR AND INS_INR = TEL_INR" "~"
if joinKuerzel = INSTEL
searchResult := searchResult "LEFT JOIN BUERO.TEL_TELEFON ON TEL_IART = INS_IART AND TEL_LANDNR = INS_LANDNR AND TEL_INR = INS_INR" "~"
if joinKuerzel = TPRINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_IART = TPR_INS_IART AND INS_LANDNR = TPR_INS_LANDNR AND INS_INR = TPR_INS_INR" "~"
if joinKuerzel = INSTPR
searchResult := searchResult "LEFT JOIN BUERO.TPR_TELEFONPROTOKOLL ON TPR_INS_IART = INS_IART AND TPR_INS_LANDNR = INS_LANDNR AND TPR_INS_INR = INS_INR" "~"
if joinKuerzel = IZJIOB
searchResult := searchResult "LEFT JOIN BUERO.IOB_INSTANDHOBJEKT ON IOB_IOBID = IZJ_IOBID" "~"
if joinKuerzel = IOBIZJ
searchResult := searchResult "LEFT JOIN BUERO.IZJ_ZAEHLERJOURNAL ON IZJ_IOBID = IOB_IOBID" "~"
if joinKuerzel = LEBIOB
searchResult := searchResult "LEFT JOIN BUERO.IOB_INSTANDHOBJEKT ON IOB_IOBID = LEB_FAHRZ_IOBID" "~"
if joinKuerzel = IOBLEB
searchResult := searchResult "LEFT JOIN BUERO.LEB_LEBENSLAUF ON LEB_FAHRZ_IOBID = IOB_IOBID" "~"
if joinKuerzel = SCEITV
searchResult := searchResult "LEFT JOIN ITV_INTERVALL.ITV_INTERVALL ON ITV_INTERVALLID = SCE_ITV_INTERVALLID" "~"
if joinKuerzel = ITVSCE
searchResult := searchResult "LEFT JOIN PRODUKTION.SCE_SCORECARD ON SCE_ITV_INTERVALLID = ITV_INTERVALLID" "~"
if joinKuerzel = IMZIVG
searchResult := searchResult "LEFT JOIN IVG_INTERVALLGRUPPE.IVG_INTERVALLGRUPPE ON IVG_INTERVALLGRPID = IMZ_INTERVALLGRPID" "~"
if joinKuerzel = IVGIMZ
searchResult := searchResult "LEFT JOIN BUERO.IMZ_INSTANDHMASSNZU ON IMZ_INTERVALLGRPID = IVG_INTERVALLGRPID" "~"
if joinKuerzel = DISKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_KASSENPOSID = DIS_KAP_KASSENPOSID" "~"
if joinKuerzel = KAPDIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_KAP_KASSENPOSID = KAP_KASSENPOSID" "~"
if joinKuerzel = KAPKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_KASSENPOSID = KAP_REFERENZ_KASSENPOSID" "~"
if joinKuerzel = KAPKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_REFERENZ_KASSENPOSID = KAP_KASSENPOSID" "~"
if joinKuerzel = GLUKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_FIRMENNR = GLU_KAS_FIRMENNR AND KAS_KASSENNR = GLU_KAS_KASSENNR" "~"
if joinKuerzel = KASGLU
searchResult := searchResult "LEFT JOIN WWS.GLU_GELDLADENZUORDNUNG ON GLU_KAS_FIRMENNR = KAS_FIRMENNR AND GLU_KAS_KASSENNR = KAS_KASSENNR" "~"
if joinKuerzel = KAKKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_FIRMENNR = KAK_FIRMENNR AND KAS_KASSENNR = KAK_KASSENNR" "~"
if joinKuerzel = KASKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_FIRMENNR = KAS_FIRMENNR AND KAK_KASSENNR = KAS_KASSENNR" "~"
if joinKuerzel = KJOKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_KATEGORIEKZ = KJO_KATEGORIEKZ AND KAT_KATEGORIELFDNR = KJO_KATEGORIELFDNR" "~"
if joinKuerzel = KATKJO
searchResult := searchResult "LEFT JOIN BUERO.KJO_KONTAKTJOURNAL ON KJO_KATEGORIEKZ = KAT_KATEGORIEKZ AND KJO_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = KRIKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_KATEGORIEKZ = KRI_KATEGORIEKZ AND KAT_KATEGORIELFDNR = KRI_KATEGORIELFDNR" "~"
if joinKuerzel = KATKRI
searchResult := searchResult "LEFT JOIN BUERO.KRI_KRITERIUM ON KRI_KATEGORIEKZ = KAT_KATEGORIEKZ AND KRI_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = PUPKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_KATEGORIEKZ = PUP_KATEGORIEKZ AND KAT_KATEGORIELFDNR = PUP_KATEGORIELFDNR" "~"
if joinKuerzel = KATPUP
searchResult := searchResult "LEFT JOIN BUERO.PUP_PERSONENUPDATE ON PUP_KATEGORIEKZ = KAT_KATEGORIEKZ AND PUP_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = RZUKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_KATEGORIEKZ = RZU_KATEGORIEKZ AND KAT_KATEGORIELFDNR = RZU_KATEGORIELFDNR" "~"
if joinKuerzel = KATRZU
searchResult := searchResult "LEFT JOIN BUERO.RZU_RESSOURCENPAKETZU ON RZU_KATEGORIEKZ = KAT_KATEGORIEKZ AND RZU_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = BOZKZA
searchResult := searchResult "LEFT JOIN WWS.KZA_KASSENZAHLUNGSARTENZU ON KZA_KZAID = BOZ_KZA_KZAID" "~"
if joinKuerzel = KZABOZ
searchResult := searchResult "LEFT JOIN WWS.BOZ_BONZAHLUNG ON BOZ_KZA_KZAID = KZA_KZAID" "~"
if joinKuerzel = KZZKBA
searchResult := searchResult "LEFT JOIN KORE.KBA_KOREBAUM ON KBA_FIRMENNR = KZZ_FIRMENNR AND KBA_KORENR = KZZ_KORENR AND KBA_KOREBAUMNR = KZZ_KBA_KOREBAUMNR" "~"
if joinKuerzel = KBAKZZ
searchResult := searchResult "LEFT JOIN KORE.KZZ_KSTKENNZAHLZU ON KZZ_FIRMENNR = KBA_FIRMENNR AND KZZ_KORENR = KBA_KORENR AND KZZ_KBA_KOREBAUMNR = KBA_KOREBAUMNR" "~"
if joinKuerzel = WKPKFG
searchResult := searchResult "LEFT JOIN WWS.KFG_KTOFINDBETRGRENZEN ON KFG_FIRMENNR = WKP_FIRMENNR AND KFG_BETRAGGRKZ = WKP_BETRAGGRKZ" "~"
if joinKuerzel = KFGWKP
searchResult := searchResult "LEFT JOIN WWS.WKP_WWSKONTENTABPOS ON WKP_FIRMENNR = KFG_FIRMENNR AND WKP_BETRAGGRKZ = KFG_BETRAGGRKZ" "~"
if joinKuerzel = KAJKGD
searchResult := searchResult "LEFT JOIN WWS.KGD_KASSENGELDLADE ON KGD_GELDLADENID = KAJ_GELDLADENID" "~"
if joinKuerzel = KGDKAJ
searchResult := searchResult "LEFT JOIN WWS.KAJ_KASSENJOURNAL ON KAJ_GELDLADENID = KGD_GELDLADENID" "~"
if joinKuerzel = KASKGD
searchResult := searchResult "LEFT JOIN WWS.KGD_KASSENGELDLADE ON KGD_GELDLADENID = KAS_KGD_GELDLADENID" "~"
if joinKuerzel = KGDKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_KGD_GELDLADENID = KGD_GELDLADENID" "~"
if joinKuerzel = ASMIFT
searchResult := searchResult "LEFT JOIN BUERO.IFT_INFOTAB ON IFT_INFOTABID = ASM_IFT_INFOTABID" "~"
if joinKuerzel = IFTASM
searchResult := searchResult "LEFT JOIN BUERO.ASM_AUSBILDUNGSSTAMMMERKM ON ASM_IFT_INFOTABID = IFT_INFOTABID" "~"
if joinKuerzel = AVEIMN
searchResult := searchResult "LEFT JOIN IMN_INSTANDHMASSN.IMN_INSTANDHMASSN ON IMN_IMNID = AVE_ERPESD_IMNID" "~"
if joinKuerzel = IMNAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ERPESD_IMNID = IMN_IMNID" "~"
if joinKuerzel = IMZIMN
searchResult := searchResult "LEFT JOIN BUERO.IMN_INSTANDHMASSN ON IMN_IMNID = IMZ_IMNID" "~"
if joinKuerzel = IMNIMZ
searchResult := searchResult "LEFT JOIN BUERO.IMZ_INSTANDHMASSNZU ON IMZ_IMNID = IMN_IMNID" "~"
if joinKuerzel = PCKIMN
searchResult := searchResult "LEFT JOIN IMN_INSTANDHMASSN.IMN_INSTANDHMASSN ON IMN_IMNID = PCK_IMNID" "~"
if joinKuerzel = IMNPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_IMNID = IMN_IMNID" "~"
if joinKuerzel = ETDETF
searchResult := searchResult "LEFT JOIN ETF_ETIKETTENFORMAT.ETF_ETIKETTENFORMAT ON ETF_ETIKETTENID = ETD_ETIKETTENID" "~"
if joinKuerzel = ETFETD
searchResult := searchResult "LEFT JOIN BMDDEFAULT.ETD_ETIKETTENFORMAT ON ETD_ETIKETTENID = ETF_ETIKETTENID" "~"
if joinKuerzel = FTBINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_IART = FTB_ABFAHRT_INS_IART AND INS_LANDNR = FTB_ABFAHRT_INS_LANDNR AND INS_INR = FTB_ABFAHRT_INS_INR" "~"
if joinKuerzel = INSFTB
searchResult := searchResult "LEFT JOIN BUERO.FTB_FAHRTENBUCH ON FTB_ABFAHRT_INS_IART = INS_IART AND FTB_ABFAHRT_INS_LANDNR = INS_LANDNR AND FTB_ABFAHRT_INS_INR = INS_INR" "~"
if joinKuerzel = AVEIOB
searchResult := searchResult "LEFT JOIN IOB_INSTANDHOBJEKT.IOB_INSTANDHOBJEKT ON IOB_IOBID = AVE_ERPESD_IOBID" "~"
if joinKuerzel = IOBAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ERPESD_IOBID = IOB_IOBID" "~"
if joinKuerzel = IMZIOG
searchResult := searchResult "LEFT JOIN BUERO.IOG_INSTANDHOBJEKTGRUPPE ON IOG_IOGID = IMZ_IOGID" "~"
if joinKuerzel = IOGIMZ
searchResult := searchResult "LEFT JOIN BUERO.IMZ_INSTANDHMASSNZU ON IMZ_IOGID = IOG_IOGID" "~"
if joinKuerzel = VETITV
searchResult := searchResult "LEFT JOIN ITV_INTERVALL.ITV_INTERVALL ON ITV_INTERVALLID = VET_INTERVALLID" "~"
if joinKuerzel = ITVVET
searchResult := searchResult "LEFT JOIN BUERO.VET_VERANSTALTUNGSTAG ON VET_INTERVALLID = ITV_INTERVALLID" "~"
if joinKuerzel = IVPIVK
searchResult := searchResult "LEFT JOIN WWS.IVK_INVENTURKOPF ON IVK_INVKOPFID = IVP_INVKOPFID" "~"
if joinKuerzel = IVKIVP
searchResult := searchResult "LEFT JOIN WWS.IVP_INVENTURPOSITION ON IVP_INVKOPFID = IVK_INVKOPFID" "~"
if joinKuerzel = KRAKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_KASSENKOPFID = KRA_KASSENKOPFID" "~"
if joinKuerzel = KAKKRA
searchResult := searchResult "LEFT JOIN WWS.KRA_KASSENRABATT ON KRA_KASSENKOPFID = KAK_KASSENKOPFID" "~"
if joinKuerzel = KAJKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_FIRMENNR = KAJ_KAS_FIRMENNR AND KAS_KASSENNR = KAJ_KAS_KASSENNR" "~"
if joinKuerzel = KASKAJ
searchResult := searchResult "LEFT JOIN WWS.KAJ_KASSENJOURNAL ON KAJ_KAS_FIRMENNR = KAS_FIRMENNR AND KAJ_KAS_KASSENNR = KAS_KASSENNR" "~"
if joinKuerzel = AGNKAT
searchResult := searchResult "LEFT JOIN KAT_KATEGORIE.KAT_KATEGORIE ON KAT_KATEGORIEKZ = AGN_KAT_KATEGORIEKZ AND KAT_KATEGORIELFDNR = AGN_KAT_KATEGORIELFDNR" "~"
if joinKuerzel = KATAGN
searchResult := searchResult "LEFT JOIN WWS.AGN_AG_PERSON_ZU ON AGN_KAT_KATEGORIEKZ = KAT_KATEGORIEKZ AND AGN_KAT_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = CLVKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_KATEGORIEKZ = CLV_KATEGORIEKZ AND KAT_KATEGORIELFDNR = CLV_KATEGORIELFDNR" "~"
if joinKuerzel = KATCLV
searchResult := searchResult "LEFT JOIN BUERO.CLV_CHECKLISTVORLAGE ON CLV_KATEGORIEKZ = KAT_KATEGORIEKZ AND CLV_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = PNKKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_KATEGORIEKZ = PNK_KAT_KATEGORIEKZ AND KAT_KATEGORIELFDNR = PNK_KATEGORIELFDNR" "~"
if joinKuerzel = KATPNK
searchResult := searchResult "LEFT JOIN BUERO.PNK_PERSONNUMMERNKREIS ON PNK_KAT_KATEGORIEKZ = KAT_KATEGORIEKZ AND PNK_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = PRSKAT
searchResult := searchResult "LEFT JOIN KAT_KATEGORIE.KAT_KATEGORIE ON KAT_KATEGORIEKZ = PRS_TERMIN_KATEGORIEKZ AND KAT_KATEGORIELFDNR = PRS_TERMIN_KATEGORIELFDNR" "~"
if joinKuerzel = KATPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_TERMIN_KATEGORIEKZ = KAT_KATEGORIEKZ AND PRS_TERMIN_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = TBTKAT
searchResult := searchResult "LEFT JOIN KAT_KATEGORIE.KAT_KATEGORIE ON KAT_KATEGORIEKZ = TBT_KATEGORIEKZ AND KAT_KATEGORIELFDNR = TBT_KATEGORIELFDNR" "~"
if joinKuerzel = KATTBT
searchResult := searchResult "LEFT JOIN BMDENTW.TBT_TICKETKATEGORIEZU ON TBT_KATEGORIEKZ = KAT_KATEGORIEKZ AND TBT_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = VATKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_KATEGORIEKZ = VAT_KATEGORIEKZ AND KAT_KATEGORIELFDNR = VAT_KATEGORIELFDNR" "~"
if joinKuerzel = KATVAT
searchResult := searchResult "LEFT JOIN BUERO.VAT_TEILNAHME ON VAT_KATEGORIEKZ = KAT_KATEGORIEKZ AND VAT_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = AUGKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_KATEGORIEKZ = AUG_UNTERKATEGORIEKZ AND KAT_KATEGORIELFDNR = AUG_UNTERKATEGORIELFDNR" "~"
if joinKuerzel = KATAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_UNTERKATEGORIEKZ = KAT_KATEGORIEKZ AND AUG_UNTERKATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = BOZKGD
searchResult := searchResult "LEFT JOIN WWS.KGD_KASSENGELDLADE ON KGD_GELDLADENID = BOZ_GELDLADENID" "~"
if joinKuerzel = KGDBOZ
searchResult := searchResult "LEFT JOIN WWS.BOZ_BONZAHLUNG ON BOZ_GELDLADENID = KGD_GELDLADENID" "~"
if joinKuerzel = KKZKKA
searchResult := searchResult "LEFT JOIN BUERO.KKA_KREDITKARTENART ON KKA_KREDITKARTENARTNR = KKZ_KREDITKARTENARTNR" "~"
if joinKuerzel = KKAKKZ
searchResult := searchResult "LEFT JOIN BUERO.KKZ_KREDITKARTENKUNDENZU ON KKZ_KREDITKARTENARTNR = KKA_KREDITKARTENARTNR" "~"
if joinKuerzel = KSEKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = KSE_FIRMENNR AND KST_KORENR = KSE_KORENR AND KST_KOSTENTYP = KSE_ZIEL_KOSTENTYP AND KST_KOSTENSTAMMNR = KSE_ZIEL_KOSTENSTAMMNR AND KST_KNOTENREFNR = KSE_ZIEL_KNOTENREFNR" "~"
if joinKuerzel = KSTKSE
searchResult := searchResult "LEFT JOIN KORE.KSE_KORESCHLUESSELEINGABE ON KSE_FIRMENNR = KST_FIRMENNR AND KSE_KORENR = KST_KORENR AND KSE_ZIEL_KOSTENTYP = KST_KOSTENTYP AND KSE_ZIEL_KOSTENSTAMMNR = KST_KOSTENSTAMMNR AND KSE_ZIEL_KNOTENREFNR = KST_KNOTENREFNR" "~"
if joinKuerzel = WEBKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = WEB_KMG_FIRMENNR AND KST_KORENR = WEB_KORENR AND KST_KOSTENSTAMMNR = WEB_AUFW_KTR_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTWEB
searchResult := searchResult "LEFT JOIN WWS.WEB_WARENEINSATZBUCH ON WEB_KMG_FIRMENNR = KST_FIRMENNR AND WEB_KORENR = KST_KORENR AND WEB_AUFW_KTR_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = KSCKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = KSC_FIRMENNR AND KST_KORENR = KSC_KORENR AND KST_KOSTENTYP = KSC_ABT_KOSTENTYP AND KST_KOSTENSTAMMNR = KSC_ABT_KOSTENSTAMMNR AND KST_KNOTENREFNR = KSC_ABT_KNOTENREFNR" "~"
if joinKuerzel = KSTKSC
searchResult := searchResult "LEFT JOIN KORE.KSC_KORESCHLUESSEL ON KSC_FIRMENNR = KST_FIRMENNR AND KSC_KORENR = KST_KORENR AND KSC_ABT_KOSTENTYP = KST_KOSTENTYP AND KSC_ABT_KOSTENSTAMMNR = KST_KOSTENSTAMMNR AND KSC_ABT_KNOTENREFNR = KST_KNOTENREFNR" "~"
if joinKuerzel = WEBKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = WEB_KMG_FIRMENNR AND KST_KORENR = WEB_KORENR AND KST_KOSTENSTAMMNR = WEB_BEST_ABT_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTWEB
searchResult := searchResult "LEFT JOIN WWS.WEB_WARENEINSATZBUCH ON WEB_KMG_FIRMENNR = KST_FIRMENNR AND WEB_KORENR = KST_KORENR AND WEB_BEST_ABT_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = WEBKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = WEB_KMG_FIRMENNR AND KST_KORENR = WEB_KORENR AND KST_KOSTENSTAMMNR = WEB_AUFW_GBR_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTWEB
searchResult := searchResult "LEFT JOIN WWS.WEB_WARENEINSATZBUCH ON WEB_KMG_FIRMENNR = KST_FIRMENNR AND WEB_KORENR = KST_KORENR AND WEB_AUFW_GBR_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = BPKKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = BPK_FIBU_FIRMENNR AND KTO_FIBUNR = BPK_FIBUNR AND KTO_KONTONR = BPK_KONTONR" "~"
if joinKuerzel = KTOBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_FIBU_FIRMENNR = KTO_FIRMENNR AND BPK_FIBUNR = KTO_FIBUNR AND BPK_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = IZSKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = IZS_FIRMENNR AND KTO_FIBUNR = IZS_FIBUNR AND KTO_KONTONR = IZS_KONTONR" "~"
if joinKuerzel = KTOIZS
searchResult := searchResult "LEFT JOIN BIL.IZS_INVZUSUMMEN ON IZS_FIRMENNR = KTO_FIRMENNR AND IZS_FIBUNR = KTO_FIBUNR AND IZS_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = KSKKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = KSK_FIRMENNR AND KTO_FIBUNR = KSK_AUFKONTO_FIBUNR AND KTO_KONTONR = KSK_ENTLAST_KONTONR" "~"
if joinKuerzel = KTOKSK
searchResult := searchResult "LEFT JOIN KORE.KSK_KORESCHLUESSELKOPF ON KSK_FIRMENNR = KTO_FIRMENNR AND KSK_AUFKONTO_FIBUNR = KTO_FIBUNR AND KSK_ENTLAST_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = KZKKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = KZK_FWK_FIRMENNR AND KTO_FIBUNR = KZK_FIBUNR AND KTO_KONTONR = KZK_ZUSCHLAGKONTONR" "~"
if joinKuerzel = KTOKZK
searchResult := searchResult "LEFT JOIN KORE.KZK_ZUSCHLAGKOPF ON KZK_FWK_FIRMENNR = KTO_FIRMENNR AND KZK_FIBUNR = KTO_FIBUNR AND KZK_ZUSCHLAGKONTONR = KTO_KONTONR" "~"
if joinKuerzel = KZPKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = KZP_FIRMENNR AND KTO_FIBUNR = KZP_FIBUNR AND KTO_KONTONR = KZP_KONTONR" "~"
if joinKuerzel = KTOKZP
searchResult := searchResult "LEFT JOIN FIBU.KZP_ZAHLUNGSPLAN ON KZP_FIRMENNR = KTO_FIRMENNR AND KZP_FIBUNR = KTO_FIBUNR AND KZP_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = PWXKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = PWX_FIRMENNR AND KTO_FIBUNR = PWX_FIBUNR AND KTO_KONTONR = PWX_KONTONR" "~"
if joinKuerzel = KTOPWX
searchResult := searchResult "LEFT JOIN KORE.PWX_PLANWERTEEXT ON PWX_FIRMENNR = KTO_FIRMENNR AND PWX_FIBUNR = KTO_FIBUNR AND PWX_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = RNPKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = RNP_FIBU_FIRMENNR AND KTO_FIBUNR = RNP_FIBUNR AND KTO_KONTONR = RNP_ERLOES_KONTONR" "~"
if joinKuerzel = KTORNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIBU_FIRMENNR = KTO_FIRMENNR AND RNP_FIBUNR = KTO_FIBUNR AND RNP_ERLOES_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = UDPKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = UDP_FIRMENNR AND KTO_FIBUNR = UDP_FIBUNR AND KTO_KONTONR = UDP_KONTONR" "~"
if joinKuerzel = KTOUDP
searchResult := searchResult "LEFT JOIN KORE.UDP_UMLAGEPLANWERTE ON UDP_FIRMENNR = KTO_FIRMENNR AND UDP_FIBUNR = KTO_FIBUNR AND UDP_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = UMPKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = UMP_FIRMENNR AND KTO_FIBUNR = UMP_FIBUNR AND KTO_KONTONR = UMP_KONTONR" "~"
if joinKuerzel = KTOUMP
searchResult := searchResult "LEFT JOIN KORE.UMP_UMLAGEPLANMENGEN ON UMP_FIRMENNR = KTO_FIRMENNR AND UMP_FIBUNR = KTO_FIBUNR AND UMP_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = VKPKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = VKP_FIRMENNR AND KTO_FIBUNR = VKP_FIBUNR AND KTO_KONTONR = VKP_KUERZUNG_KTO_KLASSE9" "~"
if joinKuerzel = KTOVKP
searchResult := searchResult "LEFT JOIN FIBU.VKP_VSTKPROJEKT ON VKP_FIRMENNR = KTO_FIRMENNR AND VKP_FIBUNR = KTO_FIBUNR AND VKP_KUERZUNG_KTO_KLASSE9 = KTO_KONTONR" "~"
if joinKuerzel = ZBRKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = ZBR_FIRMENNR AND KTO_FIBUNR = ZBR_FIBUNR AND KTO_KONTONR = ZBR_KONTONR" "~"
if joinKuerzel = KTOZBR
searchResult := searchResult "LEFT JOIN FIBU.ZBR_ZINSBERECHNUNG ON ZBR_FIRMENNR = KTO_FIRMENNR AND ZBR_FIBUNR = KTO_FIBUNR AND ZBR_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = BUMKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = BUM_FIRMENNR AND KTO_FIBUNR = BUM_FIBUNR AND KTO_KONTONR = BUM_KONTONR_HABEN" "~"
if joinKuerzel = KTOBUM
searchResult := searchResult "LEFT JOIN BIL.BUM_BILUMBUCHUNG ON BUM_FIRMENNR = KTO_FIRMENNR AND BUM_FIBUNR = KTO_FIBUNR AND BUM_KONTONR_HABEN = KTO_KONTONR" "~"
if joinKuerzel = EXTKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = EXT_FIRMENNR AND KTO_FIBUNR = EXT_FIBUNR AND KTO_KONTONR = EXT_GEGENKONTONR" "~"
if joinKuerzel = KTOEXT
searchResult := searchResult "LEFT JOIN FIBU.EXT_EXTJOURNAL ON EXT_FIRMENNR = KTO_FIRMENNR AND EXT_FIBUNR = KTO_FIBUNR AND EXT_GEGENKONTONR = KTO_KONTONR" "~"
if joinKuerzel = AVEKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = AVE_LIEFEREMPF_FIRMENNR AND KLI_KLID = AVE_LIEFEREMPF_KLID" "~"
if joinKuerzel = KLIAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_LIEFEREMPF_FIRMENNR = KLI_FIRMENNR AND AVE_LIEFEREMPF_KLID = KLI_KLID" "~"
if joinKuerzel = MDEKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = MDE_LIEFERANT_FIRMENNR AND KLI_KLID = MDE_LIEFERANT_KLID" "~"
if joinKuerzel = KLIMDE
searchResult := searchResult "LEFT JOIN WWS.MDE_MOBILEDATENERFASSUNG ON MDE_LIEFERANT_FIRMENNR = KLI_FIRMENNR AND MDE_LIEFERANT_KLID = KLI_KLID" "~"
if joinKuerzel = PTZKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = PTZ_KLIKONZERN_FIRMENNR AND KLI_KLID = PTZ_KLIKONZERN_KLID" "~"
if joinKuerzel = KLIPTZ
searchResult := searchResult "LEFT JOIN WWS.PTZ_PREISTABZU ON PTZ_KLIKONZERN_FIRMENNR = KLI_FIRMENNR AND PTZ_KLIKONZERN_KLID = KLI_KLID" "~"
if joinKuerzel = AVEKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = AVE_ZRECHEMPF_FIRMENNR AND KLI_KLID = AVE_ZRECHEMPF_KLID" "~"
if joinKuerzel = KLIAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ZRECHEMPF_FIRMENNR = KLI_FIRMENNR AND AVE_ZRECHEMPF_KLID = KLI_KLID" "~"
if joinKuerzel = WADKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = WAD_RECHLEG_FIRMENNR AND KLI_KLID = WAD_RECHLEG_KLID" "~"
if joinKuerzel = KLIWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_RECHLEG_FIRMENNR = KLI_FIRMENNR AND WAD_RECHLEG_KLID = KLI_KLID" "~"
if joinKuerzel = PWXKMG
searchResult := searchResult "LEFT JOIN KORE.KMG_KOREMENGE ON KMG_FIRMENNR = PWX_FIRMENNR AND KMG_KORENR = PWX_KORENR AND KMG_KOREMENGENNR = PWX_ABH_MENGENNR" "~"
if joinKuerzel = KMGPWX
searchResult := searchResult "LEFT JOIN KORE.PWX_PLANWERTEEXT ON PWX_FIRMENNR = KMG_FIRMENNR AND PWX_KORENR = KMG_KORENR AND PWX_ABH_MENGENNR = KMG_KOREMENGENNR" "~"
if joinKuerzel = BPKKOD
searchResult := searchResult "LEFT JOIN KOD_KONDITIONEN.KOD_KONDITIONEN ON KOD_KONDNR = BPK_ZAHLKONDNR" "~"
if joinKuerzel = KODBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_ZAHLKONDNR = KOD_KONDNR" "~"
if joinKuerzel = FIRKOD
searchResult := searchResult "LEFT JOIN BUERO.KOD_KONDITIONEN ON KOD_KONDNR = FIR_KUND_KONDNR" "~"
if joinKuerzel = KODFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_KUND_KONDNR = KOD_KONDNR" "~"
if joinKuerzel = RNGKOD
searchResult := searchResult "LEFT JOIN KOD_KONDITIONEN.KOD_KONDITIONEN ON KOD_KONDNR = RNG_ZAHLKONDNR" "~"
if joinKuerzel = KODRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_ZAHLKONDNR = KOD_KONDNR" "~"
if joinKuerzel = TSAKOD
searchResult := searchResult "LEFT JOIN KOD_KONDITIONEN.KOD_KONDITIONEN ON KOD_KONDNR = TSA_KONDNR" "~"
if joinKuerzel = KODTSA
searchResult := searchResult "LEFT JOIN FIBU.TSA_TEILSCHLUSSRECHABLAUF ON TSA_KONDNR = KOD_KONDNR" "~"
if joinKuerzel = KMPKPK
searchResult := searchResult "LEFT JOIN KORE.KPK_PLANKENNZAHLEN ON KPK_PLANKZID = KMP_MG_ALG_ABHAENGIG_ID" "~"
if joinKuerzel = KPKKMP
searchResult := searchResult "LEFT JOIN KORE.KMP_PLANMENGEN ON KMP_MG_ALG_ABHAENGIG_ID = KPK_PLANKZID" "~"
if joinKuerzel = KSKKPM
searchResult := searchResult "LEFT JOIN KORE.KPM_PLANMODELL ON KPM_FIRMENNR = KSK_FIRMENNR AND KPM_KORENR = KSK_KORENR AND KPM_MODELLNR = KSK_BERECH_MODELLNR" "~"
if joinKuerzel = KPMKSK
searchResult := searchResult "LEFT JOIN KORE.KSK_KORESCHLUESSELKOPF ON KSK_FIRMENNR = KPM_FIRMENNR AND KSK_KORENR = KPM_KORENR AND KSK_BERECH_MODELLNR = KPM_MODELLNR" "~"
if joinKuerzel = LAWKRM
searchResult := searchResult "LEFT JOIN HRS.KRM_KONTENREFERENZTAB ON KRM_DATENBESTANDNR = LAW_DBSNR AND KRM_KONTOREFNR = LAW_KONTOREFNR_B" "~"
if joinKuerzel = KRMLAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_DBSNR = KRM_DATENBESTANDNR AND LAW_KONTOREFNR_B = KRM_KONTOREFNR" "~"
if joinKuerzel = LAWKRM
searchResult := searchResult "LEFT JOIN HRS.KRM_KONTENREFERENZTAB ON KRM_DATENBESTANDNR = LAW_DBSNR AND KRM_KONTOREFNR = LAW_KONTOREFNR_GEGENKTO_B" "~"
if joinKuerzel = KRMLAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_DBSNR = KRM_DATENBESTANDNR AND LAW_KONTOREFNR_GEGENKTO_B = KRM_KONTOREFNR" "~"
if joinKuerzel = ABLKST
searchResult := searchResult "LEFT JOIN KST_KORESTAMM.KST_KORESTAMM ON KST_FIRMENNR = ABL_FIRMENNR AND KST_KORENR = ABL_KORENR AND KST_KOSTENTYP = ABL_KST_KOSTENTYP AND KST_KNOTENREFNR = ABL_KST_KNOTENREFNR AND KST_KOSTENSTAMMNR = ABL_KST_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTABL
searchResult := searchResult "LEFT JOIN BUERO.ABL_AVBUDGETDETAIL ON ABL_FIRMENNR = KST_FIRMENNR AND ABL_KORENR = KST_KORENR AND ABL_KST_KOSTENTYP = KST_KOSTENTYP AND ABL_KST_KNOTENREFNR = KST_KNOTENREFNR AND ABL_KST_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = ARLKST
searchResult := searchResult "LEFT JOIN KST_KORESTAMM.KST_KORESTAMM ON KST_KOSTENSTAMMNR = ARL_KOSTENST_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_KOSTENST_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = KSEKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = KSE_FIRMENNR AND KST_KORENR = KSE_KORENR AND KST_KOSTENTYP = KSE_KNOTEN_KOSTENTYP AND KST_KOSTENSTAMMNR = KSE_KNOTEN_KOSTENSTAMMNR AND KST_KNOTENREFNR = KSE_KNOTEN_KNOTENREFNR" "~"
if joinKuerzel = KSTKSE
searchResult := searchResult "LEFT JOIN KORE.KSE_KORESCHLUESSELEINGABE ON KSE_FIRMENNR = KST_FIRMENNR AND KSE_KORENR = KST_KORENR AND KSE_KNOTEN_KOSTENTYP = KST_KOSTENTYP AND KSE_KNOTEN_KOSTENSTAMMNR = KST_KOSTENSTAMMNR AND KSE_KNOTEN_KNOTENREFNR = KST_KNOTENREFNR" "~"
if joinKuerzel = WAGKST
searchResult := searchResult "LEFT JOIN KST_KORESTAMM.KST_KORESTAMM ON KST_KOSTENSTAMMNR = WAG_KOSTENST_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_KOSTENST_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = WEBKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = WEB_KMG_FIRMENNR AND KST_KORENR = WEB_KORENR AND KST_KOSTENSTAMMNR = WEB_AUFW_KST_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTWEB
searchResult := searchResult "LEFT JOIN WWS.WEB_WARENEINSATZBUCH ON WEB_KMG_FIRMENNR = KST_FIRMENNR AND WEB_KORENR = KST_KORENR AND WEB_AUFW_KST_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = VASKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_KATEGORIEKZ = VAS_KATEGORIEKZ AND KAT_KATEGORIELFDNR = VAS_KATEGORIELFDNR" "~"
if joinKuerzel = KATVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_KATEGORIEKZ = KAT_KATEGORIEKZ AND VAS_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = KBZKBA
searchResult := searchResult "LEFT JOIN KORE.KBA_KOREBAUM ON KBA_FIRMENNR = KBZ_FIRMENNR AND KBA_KORENR = KBZ_KORENR AND KBA_KOREBAUMNR = KBZ_KOREBAUMNR" "~"
if joinKuerzel = KBAKBZ
searchResult := searchResult "LEFT JOIN KORE.KBZ_KOSTENBAUMZU ON KBZ_FIRMENNR = KBA_FIRMENNR AND KBZ_KORENR = KBA_KORENR AND KBZ_KOREBAUMNR = KBA_KOREBAUMNR" "~"
if joinKuerzel = ARLKFG
searchResult := searchResult "LEFT JOIN WWS.KFG_KTOFINDBETRGRENZEN ON KFG_FIRMENNR = ARL_KFG_FIRMENNR AND KFG_BETRAGGRKZ = ARL_KFG_BETRAGGRKZ" "~"
if joinKuerzel = KFGARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_KFG_FIRMENNR = KFG_FIRMENNR AND ARL_KFG_BETRAGGRKZ = KFG_BETRAGGRKZ" "~"
if joinKuerzel = KSMKGD
searchResult := searchResult "LEFT JOIN WWS.KGD_KASSENGELDLADE ON KGD_GELDLADENID = KSM_GELDLADENID" "~"
if joinKuerzel = KGDKSM
searchResult := searchResult "LEFT JOIN WWS.KSM_KASSENSUMMEN ON KSM_GELDLADENID = KGD_GELDLADENID" "~"
if joinKuerzel = AWSKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = AWS_KLI_FIRMENNR AND KLI_KLID = AWS_KLI_KLID" "~"
if joinKuerzel = KLIAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_KLI_FIRMENNR = KLI_FIRMENNR AND AWS_KLI_KLID = KLI_KLID" "~"
if joinKuerzel = BCUKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = BCU_KLI_FIRMENNR AND KLI_KLID = BCU_KLI_KLID" "~"
if joinKuerzel = KLIBCU
searchResult := searchResult "LEFT JOIN BUERO.BCU_BMDCOMUSER ON BCU_KLI_FIRMENNR = KLI_FIRMENNR AND BCU_KLI_KLID = KLI_KLID" "~"
if joinKuerzel = CHGKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = CHG_KLI_FIRMENNR AND KLI_KLID = CHG_KLI_KLID" "~"
if joinKuerzel = KLICHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_KLI_FIRMENNR = KLI_FIRMENNR AND CHG_KLI_KLID = KLI_KLID" "~"
if joinKuerzel = KAIKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = KAI_KLI_FIRMENNR AND KLI_KLID = KAI_KLID" "~"
if joinKuerzel = KLIKAI
searchResult := searchResult "LEFT JOIN WWS.KAI_KNDARTIKELINFO ON KAI_KLI_FIRMENNR = KLI_FIRMENNR AND KAI_KLID = KLI_KLID" "~"
if joinKuerzel = LBDKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = LBD_LIEFERANT_FIRMENNR AND KLI_KLID = LBD_LIEFERANT_KLID" "~"
if joinKuerzel = KLILBD
searchResult := searchResult "LEFT JOIN WWS.LBD_LIEFBEWDATEN ON LBD_LIEFERANT_FIRMENNR = KLI_FIRMENNR AND LBD_LIEFERANT_KLID = KLI_KLID" "~"
if joinKuerzel = WADKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = WAD_LIEFEREMPF_FIRMENNR AND KLI_KLID = WAD_LIEFEREMPF_KLID" "~"
if joinKuerzel = KLIWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_LIEFEREMPF_FIRMENNR = KLI_FIRMENNR AND WAD_LIEFEREMPF_KLID = KLI_KLID" "~"
if joinKuerzel = WBDKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = WBD_KLI_FIRMENNR AND KLI_KLID = WBD_KLID" "~"
if joinKuerzel = KLIWBD
searchResult := searchResult "LEFT JOIN WWS.WBD_WARENBESCHKOSTENDET ON WBD_KLI_FIRMENNR = KLI_FIRMENNR AND WBD_KLID = KLI_KLID" "~"
if joinKuerzel = WRKKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = WRK_RECHLEG_FIRMENNR AND KLI_KLID = WRK_RECHLEG_KLID" "~"
if joinKuerzel = KLIWRK
searchResult := searchResult "LEFT JOIN WWS.WRK_WWSANKAUFSRNGKOPF ON WRK_RECHLEG_FIRMENNR = KLI_FIRMENNR AND WRK_RECHLEG_KLID = KLI_KLID" "~"
if joinKuerzel = EFKKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = EFK_LIEFEREMPF_FIRMENNR AND KLI_KLID = EFK_LIEFEREMPF_KLID" "~"
if joinKuerzel = KLIEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_LIEFEREMPF_FIRMENNR = KLI_FIRMENNR AND EFK_LIEFEREMPF_KLID = KLI_KLID" "~"
if joinKuerzel = WADKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = WAD_RECHEMPF_FIRMENNR AND KLI_KLID = WAD_RECHEMPF_KLID" "~"
if joinKuerzel = KLIWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_RECHEMPF_FIRMENNR = KLI_FIRMENNR AND WAD_RECHEMPF_KLID = KLI_KLID" "~"
if joinKuerzel = RNGKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = RNG_FIBU_FIRMENNR AND KLI_KLID = RNG_FIBU_KLID" "~"
if joinKuerzel = KLIRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIBU_FIRMENNR = KLI_FIRMENNR AND RNG_FIBU_KLID = KLI_KLID" "~"
if joinKuerzel = PLCKOM
searchResult := searchResult "LEFT JOIN PRODUKTION.KOM_KOMBI ON KOM_FIRMENNR = PLC_KOM_FIRMENNR AND KOM_KOMBI_NR = PLC_KOMBI_NR" "~"
if joinKuerzel = KOMPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_KOM_FIRMENNR = KOM_FIRMENNR AND PLC_KOMBI_NR = KOM_KOMBI_NR" "~"
if joinKuerzel = KDPKPK
searchResult := searchResult "LEFT JOIN KORE.KPK_PLANKENNZAHLEN ON KPK_PLANKZID = KDP_ALG_ABHAENGIG_ID" "~"
if joinKuerzel = KPKKDP
searchResult := searchResult "LEFT JOIN KORE.KDP_PLANWERTE ON KDP_ALG_ABHAENGIG_ID = KPK_PLANKZID" "~"
if joinKuerzel = KPKKPM
searchResult := searchResult "LEFT JOIN KORE.KPM_PLANMODELL ON KPM_FIRMENNR = KPK_FIRMENNR AND KPM_KORENR = KPK_KORENR AND KPM_MODELLNR = KPK_MODELLNR" "~"
if joinKuerzel = KPMKPK
searchResult := searchResult "LEFT JOIN KORE.KPK_PLANKENNZAHLEN ON KPK_FIRMENNR = KPM_FIRMENNR AND KPK_KORENR = KPM_KORENR AND KPK_MODELLNR = KPM_MODELLNR" "~"
if joinKuerzel = PMXKPM
searchResult := searchResult "LEFT JOIN KORE.KPM_PLANMODELL ON KPM_FIRMENNR = PMX_FIRMENNR AND KPM_KORENR = PMX_KORENR AND KPM_MODELLNR = PMX_MODELLNR" "~"
if joinKuerzel = KPMPMX
searchResult := searchResult "LEFT JOIN KORE.PMX_PLANMENGENEXT ON PMX_FIRMENNR = KPM_FIRMENNR AND PMX_KORENR = KPM_KORENR AND PMX_MODELLNR = KPM_MODELLNR" "~"
if joinKuerzel = UDPKPM
searchResult := searchResult "LEFT JOIN KORE.KPM_PLANMODELL ON KPM_FIRMENNR = UDP_FIRMENNR AND KPM_KORENR = UDP_KORENR AND KPM_MODELLNR = UDP_MODELLNR" "~"
if joinKuerzel = KPMUDP
searchResult := searchResult "LEFT JOIN KORE.UDP_UMLAGEPLANWERTE ON UDP_FIRMENNR = KPM_FIRMENNR AND UDP_KORENR = KPM_KORENR AND UDP_MODELLNR = KPM_MODELLNR" "~"
if joinKuerzel = BARKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = BAR_KLI_FIRMENNR AND KLI_KLID = BAR_KLI_KLID" "~"
if joinKuerzel = KLIBAR
searchResult := searchResult "LEFT JOIN HRS.BAR_BARAUSLAGEN ON BAR_KLI_FIRMENNR = KLI_FIRMENNR AND BAR_KLI_KLID = KLI_KLID" "~"
if joinKuerzel = BSIKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = BSI_FIRMENNR AND KLI_KLID = BSI_KUNDE_KLID" "~"
if joinKuerzel = KLIBSI
searchResult := searchResult "LEFT JOIN WWSINDIV.BSI_BONUSSTATISTIK ON BSI_FIRMENNR = KLI_FIRMENNR AND BSI_KUNDE_KLID = KLI_KLID" "~"
if joinKuerzel = EEZKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = EEZ_KLI_FIRMENNR AND KLI_KLID = EEZ_KLI_KLID" "~"
if joinKuerzel = KLIEEZ
searchResult := searchResult "LEFT JOIN WWS.EEZ_EDIEINHEITENZU ON EEZ_KLI_FIRMENNR = KLI_FIRMENNR AND EEZ_KLI_KLID = KLI_KLID" "~"
if joinKuerzel = KARKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = KAR_KLI_FIRMENNR AND KLI_KLID = KAR_KLI_KLID" "~"
if joinKuerzel = KLIKAR
searchResult := searchResult "LEFT JOIN WWS.KAR_KUNDENARTIKEL ON KAR_KLI_FIRMENNR = KLI_FIRMENNR AND KAR_KLI_KLID = KLI_KLID" "~"
if joinKuerzel = LAGKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = LAG_BEST_LIEF_FIRMENNR AND KLI_KLID = LAG_BEST_LIEF_KLID" "~"
if joinKuerzel = KLILAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_BEST_LIEF_FIRMENNR = KLI_FIRMENNR AND LAG_BEST_LIEF_KLID = KLI_KLID" "~"
if joinKuerzel = PTZKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = PTZ_KLI_FIRMENNR AND KLI_KLID = PTZ_KLI_KLID" "~"
if joinKuerzel = KLIPTZ
searchResult := searchResult "LEFT JOIN WWS.PTZ_PREISTABZU ON PTZ_KLI_FIRMENNR = KLI_FIRMENNR AND PTZ_KLI_KLID = KLI_KLID" "~"
if joinKuerzel = QMAKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = QMA_LIEFERANT_FIRMENNR AND KLI_KLID = QMA_LIEFERANT_KLID" "~"
if joinKuerzel = KLIQMA
searchResult := searchResult "LEFT JOIN PRODUKTION.QMA_QM_ARTIKEL ON QMA_LIEFERANT_FIRMENNR = KLI_FIRMENNR AND QMA_LIEFERANT_KLID = KLI_KLID" "~"
if joinKuerzel = TAEKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = TAE_KLI_FIRMENNR AND KLI_KLID = TAE_KLID" "~"
if joinKuerzel = KLITAE
searchResult := searchResult "LEFT JOIN WWS.TAE_AUFTSCHNELLERF_TMP ON TAE_KLI_FIRMENNR = KLI_FIRMENNR AND TAE_KLID = KLI_KLID" "~"
if joinKuerzel = TRPKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = TRP_KUNDEN_FIRMENNR AND KLI_KLID = TRP_KUNDEN_KLID" "~"
if joinKuerzel = KLITRP
searchResult := searchResult "LEFT JOIN WWS.TRP_TRANSPORTPOSITION ON TRP_KUNDEN_FIRMENNR = KLI_FIRMENNR AND TRP_KUNDEN_KLID = KLI_KLID" "~"
if joinKuerzel = VJSKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = VJS_AUFTRAGGEBER_FIRMENNR AND KLI_KLID = VJS_AUFTRAGGEBER_KLID" "~"
if joinKuerzel = KLIVJS
searchResult := searchResult "LEFT JOIN WWS.VJS_VA_VERLEIHJOURNALSUM ON VJS_AUFTRAGGEBER_FIRMENNR = KLI_FIRMENNR AND VJS_AUFTRAGGEBER_KLID = KLI_KLID" "~"
if joinKuerzel = WKUKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = WKU_FIRMENNR AND KLI_KLID = WKU_KLID" "~"
if joinKuerzel = KLIWKU
searchResult := searchResult "LEFT JOIN WWS_NEU.WKU_WWSKUNDE ON WKU_FIRMENNR = KLI_FIRMENNR AND WKU_KLID = KLI_KLID" "~"
if joinKuerzel = WSSKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = WSS_AUFTRAGGEBER_FIRMENNR AND KLI_KLID = WSS_AUFTRAGGEBER_KLID" "~"
if joinKuerzel = KLIWSS
searchResult := searchResult "LEFT JOIN WWS.WSS_WWSSTATISTIK ON WSS_AUFTRAGGEBER_FIRMENNR = KLI_FIRMENNR AND WSS_AUFTRAGGEBER_KLID = KLI_KLID" "~"
if joinKuerzel = WADKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = WAD_ZRECHEMPF_FIRMENNR AND KLI_KLID = WAD_ZRECHEMPF_KLID" "~"
if joinKuerzel = KLIWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_ZRECHEMPF_FIRMENNR = KLI_FIRMENNR AND WAD_ZRECHEMPF_KLID = KLI_KLID" "~"
if joinKuerzel = KMIKMG
searchResult := searchResult "LEFT JOIN KORE.KMG_KOREMENGE ON KMG_FIRMENNR = KMI_FIRMENNR AND KMG_KORENR = KMI_KORENR AND KMG_KOREMENGENNR = KMI_KOREMENGENNR" "~"
if joinKuerzel = KMGKMI
searchResult := searchResult "LEFT JOIN KORE.KMI_ISTMENGEN ON KMI_FIRMENNR = KMG_FIRMENNR AND KMI_KORENR = KMG_KORENR AND KMI_KOREMENGENNR = KMG_KOREMENGENNR" "~"
if joinKuerzel = WEBKMG
searchResult := searchResult "LEFT JOIN KMG_KOREMENGE.KMG_KOREMENGE ON KMG_FIRMENNR = WEB_KMG_FIRMENNR AND KMG_KORENR = WEB_KORENR AND KMG_KOREMENGENNR = WEB_KMG_KOREMENGENNR" "~"
if joinKuerzel = KMGWEB
searchResult := searchResult "LEFT JOIN WWS.WEB_WARENEINSATZBUCH ON WEB_KMG_FIRMENNR = KMG_FIRMENNR AND WEB_KORENR = KMG_KORENR AND WEB_KMG_KOREMENGENNR = KMG_KOREMENGENNR" "~"
if joinKuerzel = KLIKOD
searchResult := searchResult "LEFT JOIN BUERO.KOD_KONDITIONEN ON KOD_KONDNR = KLI_KONDNR" "~"
if joinKuerzel = KODKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_KONDNR = KOD_KONDNR" "~"
if joinKuerzel = KOFKOK
searchResult := searchResult "LEFT JOIN PRODUKTION.KOK_KOMMISSIONIERUNG_KOPF ON KOK_FIRMENNR = KOF_FIRMENNR AND KOK_KOMID = KOF_KOMID" "~"
if joinKuerzel = KOKKOF
searchResult := searchResult "LEFT JOIN PRODUKTION.KOF_KOKFILTER ON KOF_FIRMENNR = KOK_FIRMENNR AND KOF_KOMID = KOK_KOMID" "~"
if joinKuerzel = PWXKPM
searchResult := searchResult "LEFT JOIN KORE.KPM_PLANMODELL ON KPM_FIRMENNR = PWX_FIRMENNR AND KPM_KORENR = PWX_KORENR AND KPM_MODELLNR = PWX_MODELLNR" "~"
if joinKuerzel = KPMPWX
searchResult := searchResult "LEFT JOIN KORE.PWX_PLANWERTEEXT ON PWX_FIRMENNR = KPM_FIRMENNR AND PWX_KORENR = KPM_KORENR AND PWX_MODELLNR = KPM_MODELLNR" "~"
if joinKuerzel = UMPKPM
searchResult := searchResult "LEFT JOIN KORE.KPM_PLANMODELL ON KPM_FIRMENNR = UMP_FIRMENNR AND KPM_KORENR = UMP_KORENR AND KPM_MODELLNR = UMP_MODELLNR" "~"
if joinKuerzel = KPMUMP
searchResult := searchResult "LEFT JOIN KORE.UMP_UMLAGEPLANMENGEN ON UMP_FIRMENNR = KPM_FIRMENNR AND UMP_KORENR = KPM_KORENR AND UMP_MODELLNR = KPM_MODELLNR" "~"
if joinKuerzel = PRSKAT
searchResult := searchResult "LEFT JOIN KAT_KATEGORIE.KAT_KATEGORIE ON KAT_KATEGORIEKZ = PRS_AUFGABE_KATEGORIEKZ AND KAT_KATEGORIELFDNR = PRS_AUFGABE_KATEGORIELFDNR" "~"
if joinKuerzel = KATPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_AUFGABE_KATEGORIEKZ = KAT_KATEGORIEKZ AND PRS_AUFGABE_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = PRSKAT
searchResult := searchResult "LEFT JOIN KAT_KATEGORIE.KAT_KATEGORIE ON KAT_KATEGORIEKZ = PRS_KAT_KATEGORIEKZ AND KAT_KATEGORIELFDNR = PRS_KAT_KATEGORIELFDNR" "~"
if joinKuerzel = KATPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_KAT_KATEGORIEKZ = KAT_KATEGORIEKZ AND PRS_KAT_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = PRSKAT
searchResult := searchResult "LEFT JOIN KAT_KATEGORIE.KAT_KATEGORIE ON KAT_KATEGORIEKZ = PRS_GEPLANT_KATEGORIEKZ AND KAT_KATEGORIELFDNR = PRS_GEPLANT_KATEGORIELFDNR" "~"
if joinKuerzel = KATPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_GEPLANT_KATEGORIEKZ = KAT_KATEGORIEKZ AND PRS_GEPLANT_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = KKKKKA
searchResult := searchResult "LEFT JOIN KKA_KREDITKARTENART.KKA_KREDITKARTENART ON KKA_KREDITKARTENARTNR = KKK_KREDITKARTENARTNR" "~"
if joinKuerzel = KKAKKK
searchResult := searchResult "LEFT JOIN WWS.KKK_KREDITKARTENKONTENZU ON KKK_KREDITKARTENARTNR = KKA_KREDITKARTENARTNR" "~"
if joinKuerzel = ICDKKS
searchResult := searchResult "LEFT JOIN BUERO.KKS_KONSOLIDIERUNGSKREIS ON KKS_KKSID = ICD_KKSID" "~"
if joinKuerzel = KKSICD
searchResult := searchResult "LEFT JOIN BUERO.ICD_ICCODES ON ICD_KKSID = KKS_KKSID" "~"
if joinKuerzel = AVGKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = AVG_KLI_FIRMENNR AND KLI_KLID = AVG_KLI_KLID" "~"
if joinKuerzel = KLIAVG
searchResult := searchResult "LEFT JOIN WWS.AVG_AUFTRAGSVORERFGRUPP ON AVG_KLI_FIRMENNR = KLI_FIRMENNR AND AVG_KLI_KLID = KLI_KLID" "~"
if joinKuerzel = BSKKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = BSK_KUNDEN_FIRMENNR AND KLI_KLID = BSK_KUNDENID" "~"
if joinKuerzel = KLIBSK
searchResult := searchResult "LEFT JOIN WWS.BSK_BONUSERMKOPF ON BSK_KUNDEN_FIRMENNR = KLI_FIRMENNR AND BSK_KUNDENID = KLI_KLID" "~"
if joinKuerzel = GUTKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = GUT_FIRMENNR AND KLI_KLID = GUT_LIE_KLID" "~"
if joinKuerzel = KLIGUT
searchResult := searchResult "LEFT JOIN WWSINDIV.GUT_GUTHABEN_IFA ON GUT_FIRMENNR = KLI_FIRMENNR AND GUT_LIE_KLID = KLI_KLID" "~"
if joinKuerzel = KAKKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = KAK_KUNDEN_FIRMENNR AND KLI_KLID = KAK_KUNDEN_KLID" "~"
if joinKuerzel = KLIKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_KUNDEN_FIRMENNR = KLI_FIRMENNR AND KAK_KUNDEN_KLID = KLI_KLID" "~"
if joinKuerzel = KKZKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = KKZ_FIRMENNR AND KLI_KLID = KKZ_KLID" "~"
if joinKuerzel = KLIKKZ
searchResult := searchResult "LEFT JOIN BUERO.KKZ_KREDITKARTENKUNDENZU ON KKZ_FIRMENNR = KLI_FIRMENNR AND KKZ_KLID = KLI_KLID" "~"
if joinKuerzel = MDEKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = MDE_KUNDE_FIRMENNR AND KLI_KLID = MDE_KUNDE_KLID" "~"
if joinKuerzel = KLIMDE
searchResult := searchResult "LEFT JOIN WWS.MDE_MOBILEDATENERFASSUNG ON MDE_KUNDE_FIRMENNR = KLI_FIRMENNR AND MDE_KUNDE_KLID = KLI_KLID" "~"
if joinKuerzel = REAKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = REA_KLI_FIRMENNR AND KLI_KLID = REA_KLI_KLID" "~"
if joinKuerzel = KLIREA
searchResult := searchResult "LEFT JOIN HRS.REA_REISEAUFTEILUNG ON REA_KLI_FIRMENNR = KLI_FIRMENNR AND REA_KLI_KLID = KLI_KLID" "~"
if joinKuerzel = SLPKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = SLP_LIEFERANT_FIRMENNR AND KLI_KLID = SLP_LIEFERANT_ID" "~"
if joinKuerzel = KLISLP
searchResult := searchResult "LEFT JOIN WWS.SLP_STUECKLISTENPOSITION ON SLP_LIEFERANT_FIRMENNR = KLI_FIRMENNR AND SLP_LIEFERANT_ID = KLI_KLID" "~"
if joinKuerzel = TCTKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = TCT_KLI_FIRMENNR AND KLI_KLID = TCT_KLID" "~"
if joinKuerzel = KLITCT
searchResult := searchResult "LEFT JOIN BMD.TCT_TCTESTCASE ON TCT_KLI_FIRMENNR = KLI_FIRMENNR AND TCT_KLID = KLI_KLID" "~"
if joinKuerzel = TOZKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = TOZ_KLI_FIRMENNR AND KLI_KLID = TOZ_KLI_KLID" "~"
if joinKuerzel = KLITOZ
searchResult := searchResult "LEFT JOIN WWS.TOZ_TOURZUORDNUNG ON TOZ_KLI_FIRMENNR = KLI_FIRMENNR AND TOZ_KLI_KLID = KLI_KLID" "~"
if joinKuerzel = UBEKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = UBE_KLI_FIRMENNR AND KLI_KLID = UBE_KUNDE_KLID" "~"
if joinKuerzel = KLIUBE
searchResult := searchResult "LEFT JOIN BMDINTERN.UBE_UPDATEBESTELLUNG ON UBE_KLI_FIRMENNR = KLI_FIRMENNR AND UBE_KUNDE_KLID = KLI_KLID" "~"
if joinKuerzel = USPKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = USP_PER_FIRMENNR AND KLI_KLID = USP_KLI_KLID" "~"
if joinKuerzel = KLIUSP
searchResult := searchResult "LEFT JOIN BMDINTERN.USP_UPDATESPERRE ON USP_PER_FIRMENNR = KLI_FIRMENNR AND USP_KLI_KLID = KLI_KLID" "~"
if joinKuerzel = WBWKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = WBW_KLI_FIRMENNR AND KLI_KLID = WBW_KLID" "~"
if joinKuerzel = KLIWBW
searchResult := searchResult "LEFT JOIN WWS.WBW_WWSBUDGETWERTE ON WBW_KLI_FIRMENNR = KLI_FIRMENNR AND WBW_KLID = KLI_KLID" "~"
if joinKuerzel = BSPKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = BSP_LIEFERADR_FIRMENNR AND KLI_KLID = BSP_LIEFERADR_KLID" "~"
if joinKuerzel = KLIBSP
searchResult := searchResult "LEFT JOIN WWS.BSP_BONUSERMPOS ON BSP_LIEFERADR_FIRMENNR = KLI_FIRMENNR AND BSP_LIEFERADR_KLID = KLI_KLID" "~"
if joinKuerzel = APIKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = API_FIRMENNR AND KLI_KLID = API_KLID" "~"
if joinKuerzel = KLIAPI
searchResult := searchResult "LEFT JOIN BMDINTERN.API_ASPINFO ON API_FIRMENNR = KLI_FIRMENNR AND API_KLID = KLI_KLID" "~"
if joinKuerzel = BPKKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = BPK_LIEFERANT_FIRMENNR AND KLI_KLID = BPK_LIEFERANT_KLID" "~"
if joinKuerzel = KLIBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_LIEFERANT_FIRMENNR = KLI_FIRMENNR AND BPK_LIEFERANT_KLID = KLI_KLID" "~"
if joinKuerzel = FGBKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = FGB_FIRMENNR AND KLI_KLID = FGB_KLID" "~"
if joinKuerzel = KLIFGB
searchResult := searchResult "LEFT JOIN BUERO.FGB_FRAGEBOGEN ON FGB_FIRMENNR = KLI_FIRMENNR AND FGB_KLID = KLI_KLID" "~"
if joinKuerzel = GSKKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = GSK_KUNDE_FIRMENNR AND KLI_KLID = GSK_KUNDE_KLID" "~"
if joinKuerzel = KLIGSK
searchResult := searchResult "LEFT JOIN WWS.GSK_GUTSCHEINKOPF ON GSK_KUNDE_FIRMENNR = KLI_FIRMENNR AND GSK_KUNDE_KLID = KLI_KLID" "~"
if joinKuerzel = HAZKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = HAZ_KLI_FIRMENNR AND KLI_KLID = HAZ_KLID" "~"
if joinKuerzel = KLIHAZ
searchResult := searchResult "LEFT JOIN WWS.HAZ_HAUPTLIEFARTIKELZU ON HAZ_KLI_FIRMENNR = KLI_FIRMENNR AND HAZ_KLID = KLI_KLID" "~"
if joinKuerzel = LATKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = LAT_KLI_FIRMENNR AND KLI_KLID = LAT_KLI_KLID" "~"
if joinKuerzel = KLILAT
searchResult := searchResult "LEFT JOIN WWS.LAT_LIEFERANTENARTIKEL ON LAT_KLI_FIRMENNR = KLI_FIRMENNR AND LAT_KLI_KLID = KLI_KLID" "~"
if joinKuerzel = PLCKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = PLC_KLI_FIRMENNR AND KLI_KLID = PLC_KLID" "~"
if joinKuerzel = KLIPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_KLI_FIRMENNR = KLI_FIRMENNR AND PLC_KLID = KLI_KLID" "~"
if joinKuerzel = RNGKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = RNG_RECHNUNGSEMPF_FIRMENNR AND KLI_KLID = RNG_RECHNUNGSEMPF_KLID" "~"
if joinKuerzel = KLIRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_RECHNUNGSEMPF_FIRMENNR = KLI_FIRMENNR AND RNG_RECHNUNGSEMPF_KLID = KLI_KLID" "~"
if joinKuerzel = SAWKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = SAW_KUNDNR_FIRMENNR AND KLI_KLID = SAW_KUNDNR_KLID" "~"
if joinKuerzel = KLISAW
searchResult := searchResult "LEFT JOIN WWSINDIV.SAW_STATISTIKWERTE ON SAW_KUNDNR_FIRMENNR = KLI_FIRMENNR AND SAW_KUNDNR_KLID = KLI_KLID" "~"
if joinKuerzel = VASKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = VAS_KLI_FIRMENNR AND KLI_KLID = VAS_KLI_KLID" "~"
if joinKuerzel = KLIVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_KLI_FIRMENNR = KLI_FIRMENNR AND VAS_KLI_KLID = KLI_KLID" "~"
if joinKuerzel = WKZKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = WKZ_KLI_FIRMENNR AND KLI_KLID = WKZ_KLI_PERSONENID" "~"
if joinKuerzel = KLIWKZ
searchResult := searchResult "LEFT JOIN WWS.WKZ_WFKKUNDELIEFERANTZU ON WKZ_KLI_FIRMENNR = KLI_FIRMENNR AND WKZ_KLI_PERSONENID = KLI_KLID" "~"
if joinKuerzel = BSIKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = BSI_FIRMENNR AND KLI_KLID = BSI_LIEF_KLID" "~"
if joinKuerzel = KLIBSI
searchResult := searchResult "LEFT JOIN WWSINDIV.BSI_BONUSSTATISTIK ON BSI_FIRMENNR = KLI_FIRMENNR AND BSI_LIEF_KLID = KLI_KLID" "~"
if joinKuerzel = EPSKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = EPS_LIEFERANT_FIRMENNR_BS AND KLI_KLID = EPS_LIEFERANT_ID_BS" "~"
if joinKuerzel = KLIEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_LIEFERANT_FIRMENNR_BS = KLI_FIRMENNR AND EPS_LIEFERANT_ID_BS = KLI_KLID" "~"
if joinKuerzel = RNGKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = RNG_ZRECHNUNGSEMPF_FIRMENR AND KLI_KLID = RNG_ZRECHNUNGSEMPF_KLID" "~"
if joinKuerzel = KLIRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_ZRECHNUNGSEMPF_FIRMENR = KLI_FIRMENNR AND RNG_ZRECHNUNGSEMPF_KLID = KLI_KLID" "~"
if joinKuerzel = VLJKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = VLJ_LIEFEREMPF_FIRMENNR AND KLI_KLID = VLJ_LIEFEREMPF_KLID" "~"
if joinKuerzel = KLIVLJ
searchResult := searchResult "LEFT JOIN WWS.VLJ_VERLEIHJOURNAL ON VLJ_LIEFEREMPF_FIRMENNR = KLI_FIRMENNR AND VLJ_LIEFEREMPF_KLID = KLI_KLID" "~"
if joinKuerzel = AVEKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = AVE_RECHEMPF_FIRMENNR AND KLI_KLID = AVE_RECHEMPF_KLID" "~"
if joinKuerzel = KLIAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_RECHEMPF_FIRMENNR = KLI_FIRMENNR AND AVE_RECHEMPF_KLID = KLI_KLID" "~"
if joinKuerzel = BSPKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = BSP_RECHNUNGSADR_FIRMENNR AND KLI_KLID = BSP_RECHNUNGSADR_KLID" "~"
if joinKuerzel = KLIBSP
searchResult := searchResult "LEFT JOIN WWS.BSP_BONUSERMPOS ON BSP_RECHNUNGSADR_FIRMENNR = KLI_FIRMENNR AND BSP_RECHNUNGSADR_KLID = KLI_KLID" "~"
if joinKuerzel = BPKKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = BPK_ZRECHNUNGEMPF_FIRMENNR AND KLI_KLID = BPK_ZRECHNUNGEMPF_KLID" "~"
if joinKuerzel = KLIBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_ZRECHNUNGEMPF_FIRMENNR = KLI_FIRMENNR AND BPK_ZRECHNUNGEMPF_KLID = KLI_KLID" "~"
if joinKuerzel = KMDKMZ
searchResult := searchResult "LEFT JOIN HRS.KMZ_KOMMSTZENTRALE ON KMZ_KMZID = KMD_KMZID" "~"
if joinKuerzel = KMZKMD
searchResult := searchResult "LEFT JOIN HRS.KMD_KOMMSTDETAIL ON KMD_KMZID = KMZ_KMZID" "~"
if joinKuerzel = DOKINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_IART = DOK_INS_IART AND INS_LANDNR = DOK_INS_LANDNR AND INS_INR = DOK_INS_INR" "~"
if joinKuerzel = INSDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_INS_IART = INS_IART AND DOK_INS_LANDNR = INS_LANDNR AND DOK_INS_INR = INS_INR" "~"
if joinKuerzel = EMAINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_IART = EMA_IART AND INS_LANDNR = EMA_LANDNR AND INS_INR = EMA_INR" "~"
if joinKuerzel = INSEMA
searchResult := searchResult "LEFT JOIN BUERO.EMA_EMAIL ON EMA_IART = INS_IART AND EMA_LANDNR = INS_LANDNR AND EMA_INR = INS_INR" "~"
if joinKuerzel = FSTINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_IART = FST_INS_IART AND INS_LANDNR = FST_INS_LANDNR AND INS_INR = FST_INR" "~"
if joinKuerzel = INSFST
searchResult := searchResult "LEFT JOIN BUERO.FST_FRIST ON FST_INS_IART = INS_IART AND FST_INS_LANDNR = INS_LANDNR AND FST_INR = INS_INR" "~"
if joinKuerzel = TERINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_IART = TER_INS_IART AND INS_LANDNR = TER_INS_LANDNR AND INS_INR = TER_INS_INR" "~"
if joinKuerzel = INSTER
searchResult := searchResult "LEFT JOIN BUERO.TER_TERMIN ON TER_INS_IART = INS_IART AND TER_INS_LANDNR = INS_LANDNR AND TER_INS_INR = INS_INR" "~"
if joinKuerzel = UEBINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_IART = UEB_IART AND INS_LANDNR = UEB_LANDNR AND INS_INR = UEB_INR" "~"
if joinKuerzel = INSUEB
searchResult := searchResult "LEFT JOIN BUERO.UEB_UEBERWEISUNGSART ON UEB_IART = INS_IART AND UEB_LANDNR = INS_LANDNR AND UEB_INR = INS_INR" "~"
if joinKuerzel = VATINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_IART = VAT_TN_INS_IART AND INS_LANDNR = VAT_TN_INS_LANDNR AND INS_INR = VAT_TN_INS_INR" "~"
if joinKuerzel = INSVAT
searchResult := searchResult "LEFT JOIN BUERO.VAT_TEILNAHME ON VAT_TN_INS_IART = INS_IART AND VAT_TN_INS_LANDNR = INS_LANDNR AND VAT_TN_INS_INR = INS_INR" "~"
if joinKuerzel = FTBINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_IART = FTB_ZIEL_INS_IART AND INS_LANDNR = FTB_ZIEL_INS_LANDNR AND INS_INR = FTB_ZIEL_INS_INR" "~"
if joinKuerzel = INSFTB
searchResult := searchResult "LEFT JOIN BUERO.FTB_FAHRTENBUCH ON FTB_ZIEL_INS_IART = INS_IART AND FTB_ZIEL_INS_LANDNR = INS_LANDNR AND FTB_ZIEL_INS_INR = INS_INR" "~"
if joinKuerzel = GZJINS
searchResult := searchResult "LEFT JOIN INS_INSTITUTION.INS_INSTITUTION ON INS_IART = GZJ_DSE_KLAGE_IART AND INS_LANDNR = GZJ_DSE_KLAGE_LANDNR AND INS_INR = GZJ_DSE_KLAGE_INR" "~"
if joinKuerzel = INSGZJ
searchResult := searchResult "LEFT JOIN HRS.GZJ_GLAEUBIGERZU_JAHR ON GZJ_DSE_KLAGE_IART = INS_IART AND GZJ_DSE_KLAGE_LANDNR = INS_LANDNR AND GZJ_DSE_KLAGE_INR = INS_INR" "~"
if joinKuerzel = IMOIOB
searchResult := searchResult "LEFT JOIN BUERO.IOB_INSTANDHOBJEKT ON IOB_IOBID = IMO_IOBID" "~"
if joinKuerzel = IOBIMO
searchResult := searchResult "LEFT JOIN BUERO.IMO_IMMOBILIE ON IMO_IOBID = IOB_IOBID" "~"
if joinKuerzel = WOHIOB
searchResult := searchResult "LEFT JOIN BUERO.IOB_INSTANDHOBJEKT ON IOB_IOBID = WOH_IOBID" "~"
if joinKuerzel = IOBWOH
searchResult := searchResult "LEFT JOIN BUERO.WOH_WOHNUNG ON WOH_IOBID = IOB_IOBID" "~"
if joinKuerzel = AIZITB
searchResult := searchResult "LEFT JOIN ITB_INFOTABBEWERTUNG.ITB_INFOTABBEWERTUNG ON ITB_INFOTABLFDNR = AIZ_INFOTABID AND ITB_BEWERTUNGSNR = AIZ_BEWERTUNGSNR" "~"
if joinKuerzel = ITBAIZ
searchResult := searchResult "LEFT JOIN PRODUKTION.AIZ_AGPER_INFOZU ON AIZ_INFOTABID = ITB_INFOTABLFDNR AND AIZ_BEWERTUNGSNR = ITB_BEWERTUNGSNR" "~"
if joinKuerzel = ASMITB
searchResult := searchResult "LEFT JOIN BUERO.ITB_INFOTABBEWERTUNG ON ITB_INFOTABLFDNR = ASM_ITB_INFOTABID AND ITB_BEWERTUNGSNR = ASM_ITB_BEWERTUNGSNR" "~"
if joinKuerzel = ITBASM
searchResult := searchResult "LEFT JOIN BUERO.ASM_AUSBILDUNGSSTAMMMERKM ON ASM_ITB_INFOTABID = ITB_INFOTABLFDNR AND ASM_ITB_BEWERTUNGSNR = ITB_BEWERTUNGSNR" "~"
if joinKuerzel = IGZITV
searchResult := searchResult "LEFT JOIN BMD.ITV_INTERVALL ON ITV_INTERVALLID = IGZ_INTERVALLID" "~"
if joinKuerzel = ITVIGZ
searchResult := searchResult "LEFT JOIN BMD.IGZ_INTERVALLGRUPPEZU ON IGZ_INTERVALLID = ITV_INTERVALLID" "~"
if joinKuerzel = SRMITV
searchResult := searchResult "LEFT JOIN ITV_INTERVALL.ITV_INTERVALL ON ITV_INTERVALLID = SRM_INTERVALLID" "~"
if joinKuerzel = ITVSRM
searchResult := searchResult "LEFT JOIN WWS.SRM_SAMMELRECHNUNGSMODELL ON SRM_INTERVALLID = ITV_INTERVALLID" "~"
if joinKuerzel = ZPLITV
searchResult := searchResult "LEFT JOIN ITV_INTERVALL.ITV_INTERVALL ON ITV_INTERVALLID = ZPL_ITV_INTERVALLID" "~"
if joinKuerzel = ITVZPL
searchResult := searchResult "LEFT JOIN BUERO.ZPL_ZEITPLANUNG ON ZPL_ITV_INTERVALLID = ITV_INTERVALLID" "~"
if joinKuerzel = ZVDITV
searchResult := searchResult "LEFT JOIN ITV_INTERVALL.ITV_INTERVALL ON ITV_INTERVALLID = ZVD_ITV_INTERVALLID" "~"
if joinKuerzel = ITVZVD
searchResult := searchResult "LEFT JOIN BUERO.ZVD_ZEITPLANUNGVERSIONDAT ON ZVD_ITV_INTERVALLID = ITV_INTERVALLID" "~"
if joinKuerzel = BOZKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_KASSENKOPFID = BOZ_KAK_KASSENKOPFID" "~"
if joinKuerzel = KAKBOZ
searchResult := searchResult "LEFT JOIN WWS.BOZ_BONZAHLUNG ON BOZ_KAK_KASSENKOPFID = KAK_KASSENKOPFID" "~"
if joinKuerzel = KADKAM
searchResult := searchResult "LEFT JOIN KORE.KAM_AUSDRUCKMASTER ON KAM_AUSDRMASTERNR = KAD_AUSDRMASTERNR" "~"
if joinKuerzel = KAMKAD
searchResult := searchResult "LEFT JOIN KORE.KAD_AUSDRUCKDETAIL ON KAD_AUSDRMASTERNR = KAM_AUSDRMASTERNR" "~"
if joinKuerzel = GSAKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_FIRMENNR = GSA_KAS_FIRMENNR AND KAS_KASSENNR = GSA_KAS_KASSENNR" "~"
if joinKuerzel = KASGSA
searchResult := searchResult "LEFT JOIN WWS.GSA_GUTSCHEINAKTION ON GSA_KAS_FIRMENNR = KAS_FIRMENNR AND GSA_KAS_KASSENNR = KAS_KASSENNR" "~"
if joinKuerzel = AGMKAT
searchResult := searchResult "LEFT JOIN KAT_KATEGORIE.KAT_KATEGORIE ON KAT_KATEGORIEKZ = AGM_KAT_KATEGORIEKZ AND KAT_KATEGORIELFDNR = AGM_KAT_KATEGORIELFDNR" "~"
if joinKuerzel = KATAGM
searchResult := searchResult "LEFT JOIN PRODUKTION.AGM_AG_PERSON_ZU ON AGM_KAT_KATEGORIEKZ = KAT_KATEGORIEKZ AND AGM_KAT_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = AUGKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_KATEGORIEKZ = AUG_KATEGORIEKZ AND KAT_KATEGORIELFDNR = AUG_KATEGORIELFDNR" "~"
if joinKuerzel = KATAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_KATEGORIEKZ = KAT_KATEGORIEKZ AND AUG_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = KSKKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = KSK_FIRMENNR AND KTO_FIBUNR = KSK_AUFKONTO_FIBUNR AND KTO_KONTONR = KSK_AUFKONTONR_BEIBETRAG" "~"
if joinKuerzel = KTOKSK
searchResult := searchResult "LEFT JOIN KORE.KSK_KORESCHLUESSELKOPF ON KSK_FIRMENNR = KTO_FIRMENNR AND KSK_AUFKONTO_FIBUNR = KTO_FIBUNR AND KSK_AUFKONTONR_BEIBETRAG = KTO_KONTONR" "~"
if joinKuerzel = YBUKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = YBU_FIRMENNR AND KTO_FIBUNR = YBU_FIBUNR AND KTO_KONTONR = YBU_KTO_GEGENKONTONR" "~"
if joinKuerzel = KTOYBU
searchResult := searchResult "LEFT JOIN FIBU.YBU_AUTOBUCHUNG ON YBU_FIRMENNR = KTO_FIRMENNR AND YBU_FIBUNR = KTO_FIBUNR AND YBU_KTO_GEGENKONTONR = KTO_KONTONR" "~"
if joinKuerzel = FWFKUD
searchResult := searchResult "LEFT JOIN KUD_KURSDATEI.KUD_KURSDATEI ON KUD_KURSDATEINR = FWF_BH_KURSDATEINR" "~"
if joinKuerzel = KUDFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_BH_KURSDATEINR = KUD_KURSDATEINR" "~"
if joinKuerzel = AWSKUG
searchResult := searchResult "LEFT JOIN KUG_KUNDENGRUPPE.KUG_KUNDENGRUPPE ON KUG_FIRMENNR = AWS_KUG_FIRMENNR AND KUG_KUNDENHAUPTGRUPPENNR = AWS_KUG_KUNDENHAUPTGRNR AND KUG_KUNDENUNTERGRUPPENNR = AWS_KUG_KUNDENUNTERGRNR" "~"
if joinKuerzel = KUGAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_KUG_FIRMENNR = KUG_FIRMENNR AND AWS_KUG_KUNDENHAUPTGRNR = KUG_KUNDENHAUPTGRUPPENNR AND AWS_KUG_KUNDENUNTERGRNR = KUG_KUNDENUNTERGRUPPENNR" "~"
if joinKuerzel = PTZKUG
searchResult := searchResult "LEFT JOIN KUG_KUNDENGRUPPE.KUG_KUNDENGRUPPE ON KUG_FIRMENNR = PTZ_KUG_FIRMENNR AND KUG_KUNDENHAUPTGRUPPENNR = PTZ_KUG_KUNDENHAUPTGRUPPEN AND KUG_KUNDENUNTERGRUPPENNR = PTZ_KUG_KUNDENUNTERGRUPPEN" "~"
if joinKuerzel = KUGPTZ
searchResult := searchResult "LEFT JOIN WWS.PTZ_PREISTABZU ON PTZ_KUG_FIRMENNR = KUG_FIRMENNR AND PTZ_KUG_KUNDENHAUPTGRUPPEN = KUG_KUNDENHAUPTGRUPPENNR AND PTZ_KUG_KUNDENUNTERGRUPPEN = KUG_KUNDENUNTERGRUPPENNR" "~"
if joinKuerzel = ERPL12
searchResult := searchResult "LEFT JOIN PRODUKTION.L12_V_ALK_ARBEITSPLAN_KOSTEN ON L12_ALK_AP_ID = ERP_AP_ID" "~"
if joinKuerzel = L12ERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_AP_ID = L12_ALK_AP_ID" "~"
if joinKuerzel = ELVLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = ELV_LAG_FIRMENNR AND LAG_LAGERNR = ELV_LAGERNR" "~"
if joinKuerzel = LAGELV
searchResult := searchResult "LEFT JOIN WWS.ELV_ERFASSPOSLIEFERVOR ON ELV_LAG_FIRMENNR = LAG_FIRMENNR AND ELV_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = IAPLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = IAP_FIRMENNR AND LAG_LAGERNR = IAP_LAGERNR" "~"
if joinKuerzel = LAGIAP
searchResult := searchResult "LEFT JOIN WWS.IAP_INVABWTPOS ON IAP_FIRMENNR = LAG_FIRMENNR AND IAP_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = LKOLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = LKO_ARTIKELIDFIRMENNR AND LAG_LAGERNR = LKO_LAGERNR" "~"
if joinKuerzel = LAGLKO
searchResult := searchResult "LEFT JOIN WWS.LKO_LAGERKONTO ON LKO_ARTIKELIDFIRMENNR = LAG_FIRMENNR AND LKO_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = WAGLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = WAG_VORGABE_FIRMENNR AND LAG_LAGERNR = WAG_VORGABE_LAGERNR" "~"
if joinKuerzel = LAGWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_VORGABE_FIRMENNR = LAG_FIRMENNR AND WAG_VORGABE_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = WEILAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = WEI_LAG_FIRMENNR AND LAG_LAGERNR = WEI_LAGERNR" "~"
if joinKuerzel = LAGWEI
searchResult := searchResult "LEFT JOIN WWS.WEI_WARENEINSATZKOPF ON WEI_LAG_FIRMENNR = LAG_FIRMENNR AND WEI_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = EFKLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = EFK_ZIELLAGER_FIRMENNR AND LAG_LAGERNR = EFK_ZIELLAGER_LAGERNR" "~"
if joinKuerzel = LAGEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_ZIELLAGER_FIRMENNR = LAG_FIRMENNR AND EFK_ZIELLAGER_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = EPSLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = EPS_LAG_ZIEL_FIRMENNR AND LAG_LAGERNR = EPS_ZIELLAGERNR" "~"
if joinKuerzel = LAGEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_LAG_ZIEL_FIRMENNR = LAG_FIRMENNR AND EPS_ZIELLAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = ERZLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = ERZ_VORSCHLAG_LAGER_FIRMEN AND LAG_LAGERNR = ERZ_VORSCHLAG_LAGERNR" "~"
if joinKuerzel = LAGERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_VORSCHLAG_LAGER_FIRMEN = LAG_FIRMENNR AND ERZ_VORSCHLAG_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = LADLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = LAD_ARL_FIRMENNR AND LAG_LAGERNR = LAD_VORGABE_LAGERNR" "~"
if joinKuerzel = LAGLAD
searchResult := searchResult "LEFT JOIN WWS.LAD_LAGERARTIKELDATEN ON LAD_ARL_FIRMENNR = LAG_FIRMENNR AND LAD_VORGABE_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = WADLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = WAD_ABGANGSLAGER_FIRMENNR AND LAG_LAGERNR = WAD_ABGANGSLAGER_LAGERNR" "~"
if joinKuerzel = LAGWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_ABGANGSLAGER_FIRMENNR = LAG_FIRMENNR AND WAD_ABGANGSLAGER_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = ADDLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = ADD_LANDNR" "~"
if joinKuerzel = LANADD
searchResult := searchResult "LEFT JOIN BUERO.ADD_AUSDRUCKMODELLDETAIL ON ADD_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = AUBLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = AUB_LANDNR" "~"
if joinKuerzel = LANAUB
searchResult := searchResult "LEFT JOIN BIL.AUB_KIND ON AUB_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = BAKLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = BAK_LANDNR" "~"
if joinKuerzel = LANBAK
searchResult := searchResult "LEFT JOIN BUERO.BAK_BANK ON BAK_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = BBALAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = BBA_LANDNR" "~"
if joinKuerzel = LANBBA
searchResult := searchResult "LEFT JOIN BIL.BBA_BERICHTSART ON BBA_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = BUCLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = BUC_LAN_LANDNR" "~"
if joinKuerzel = LANBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_LAN_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = KRDKRM
searchResult := searchResult "LEFT JOIN HRS.KRM_KONTENREFERENZTAB ON KRM_DATENBESTANDNR = KRD_DATENBESTANDNR AND KRM_KONTOREFNR = KRD_KONTOREFNR" "~"
if joinKuerzel = KRMKRD
searchResult := searchResult "LEFT JOIN HRS.KRD_KONTENREFERENZDETAIL ON KRD_DATENBESTANDNR = KRM_DATENBESTANDNR AND KRD_KONTOREFNR = KRM_KONTOREFNR" "~"
if joinKuerzel = FMJKSM
searchResult := searchResult "LEFT JOIN WWS.KSM_KASSENSUMMEN ON KSM_KASSENSUMMENID = FMJ_KSM_KASSENSUMMENID" "~"
if joinKuerzel = KSMFMJ
searchResult := searchResult "LEFT JOIN WWS.FMJ_FISKALMODULJOURNAL ON FMJ_KSM_KASSENSUMMENID = KSM_KASSENSUMMENID" "~"
if joinKuerzel = ZKKKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = ZKK_FIRMENNR AND KST_KORENR = ZKK_KORENR AND KST_KOSTENTYP = ZKK_KOSTENTYP AND KST_KOSTENSTAMMNR = ZKK_KOSTENSTAMMNR AND KST_KNOTENREFNR = ZKK_KNOTENREFNR" "~"
if joinKuerzel = KSTZKK
searchResult := searchResult "LEFT JOIN KORE.ZKK_KSTKONTOZU ON ZKK_FIRMENNR = KST_FIRMENNR AND ZKK_KORENR = KST_KORENR AND ZKK_KOSTENTYP = KST_KOSTENTYP AND ZKK_KOSTENSTAMMNR = KST_KOSTENSTAMMNR AND ZKK_KNOTENREFNR = KST_KNOTENREFNR" "~"
if joinKuerzel = KSCKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = KSC_FIRMENNR AND KST_KORENR = KSC_KORENR AND KST_KOSTENTYP = KSC_KOST_KOSTENTYP AND KST_KOSTENSTAMMNR = KSC_KOST_KOSTENSTAMMNR AND KST_KNOTENREFNR = KSC_KOST_KNOTENREFNR" "~"
if joinKuerzel = KSTKSC
searchResult := searchResult "LEFT JOIN KORE.KSC_KORESCHLUESSEL ON KSC_FIRMENNR = KST_FIRMENNR AND KSC_KORENR = KST_KORENR AND KSC_KOST_KOSTENTYP = KST_KOSTENTYP AND KSC_KOST_KOSTENSTAMMNR = KST_KOSTENSTAMMNR AND KSC_KOST_KNOTENREFNR = KST_KNOTENREFNR" "~"
if joinKuerzel = RNGKST
searchResult := searchResult "LEFT JOIN KST_KORESTAMM.KST_KORESTAMM ON KST_KOSTENSTAMMNR = RNG_ABT_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_ABT_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = RNGKST
searchResult := searchResult "LEFT JOIN KST_KORESTAMM.KST_KORESTAMM ON KST_KOSTENSTAMMNR = RNG_DIM_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_DIM_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = KSCKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = KSC_FIRMENNR AND KST_KORENR = KSC_KORENR AND KST_KOSTENTYP = KSC_DIM_KOSTENTYP AND KST_KOSTENSTAMMNR = KSC_DIM_KOSTENSTAMMNR AND KST_KNOTENREFNR = KSC_DIM_KNOTENREFNR" "~"
if joinKuerzel = KSTKSC
searchResult := searchResult "LEFT JOIN KORE.KSC_KORESCHLUESSEL ON KSC_FIRMENNR = KST_FIRMENNR AND KSC_KORENR = KST_KORENR AND KSC_DIM_KOSTENTYP = KST_KOSTENTYP AND KSC_DIM_KOSTENSTAMMNR = KST_KOSTENSTAMMNR AND KSC_DIM_KNOTENREFNR = KST_KNOTENREFNR" "~"
if joinKuerzel = RNGKST
searchResult := searchResult "LEFT JOIN KST_KORESTAMM.KST_KORESTAMM ON KST_KOSTENSTAMMNR = RNG_GBR_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_GBR_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = KSEKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = KSE_FIRMENNR AND KST_KORENR = KSE_KORENR AND KST_KOSTENTYP = KSE_GBR_KOSTENTYP AND KST_KOSTENSTAMMNR = KSE_GBR_KOSTENSTAMMNR AND KST_KNOTENREFNR = KSE_GBR_KNOTENREFNR" "~"
if joinKuerzel = KSTKSE
searchResult := searchResult "LEFT JOIN KORE.KSE_KORESCHLUESSELEINGABE ON KSE_FIRMENNR = KST_FIRMENNR AND KSE_KORENR = KST_KORENR AND KSE_GBR_KOSTENTYP = KST_KOSTENTYP AND KSE_GBR_KOSTENSTAMMNR = KST_KOSTENSTAMMNR AND KSE_GBR_KNOTENREFNR = KST_KNOTENREFNR" "~"
if joinKuerzel = KSEKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = KSE_FIRMENNR AND KST_KORENR = KSE_KORENR AND KST_KOSTENTYP = KSE_SEL_KOSTENTYP AND KST_KOSTENSTAMMNR = KSE_SEL_KOSTENSTAMMNR AND KST_KNOTENREFNR = KSE_SEL_KNOTENREFNR" "~"
if joinKuerzel = KSTKSE
searchResult := searchResult "LEFT JOIN KORE.KSE_KORESCHLUESSELEINGABE ON KSE_FIRMENNR = KST_FIRMENNR AND KSE_KORENR = KST_KORENR AND KSE_SEL_KOSTENTYP = KST_KOSTENTYP AND KSE_SEL_KOSTENSTAMMNR = KST_KOSTENSTAMMNR AND KSE_SEL_KNOTENREFNR = KST_KNOTENREFNR" "~"
if joinKuerzel = BEZKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = BEZ_FIRMENNR AND KTO_FIBUNR = BEZ_FIBUNR AND KTO_KONTONR = BEZ_KONTONR" "~"
if joinKuerzel = KTOBEZ
searchResult := searchResult "LEFT JOIN FIBU.BEZ_BANKEINZUG ON BEZ_FIRMENNR = KTO_FIRMENNR AND BEZ_FIBUNR = KTO_FIBUNR AND BEZ_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = LBZKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = LBZ_FIRMENNR AND KTO_FIBUNR = LBZ_FIBUNR AND KTO_KONTONR = LBZ_KONTONR" "~"
if joinKuerzel = KTOLBZ
searchResult := searchResult "LEFT JOIN BIL.LBZ_LANGFRISTERST_BELEGZU ON LBZ_FIRMENNR = KTO_FIRMENNR AND LBZ_FIBUNR = KTO_FIBUNR AND LBZ_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = PMXKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = PMX_FIRMENNR AND KTO_FIBUNR = PMX_FIBUNR AND KTO_KONTONR = PMX_KONTONR" "~"
if joinKuerzel = KTOPMX
searchResult := searchResult "LEFT JOIN KORE.PMX_PLANMENGENEXT ON PMX_FIRMENNR = KTO_FIRMENNR AND PMX_FIBUNR = KTO_FIBUNR AND PMX_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = UDIKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = UDI_FIRMENNR AND KTO_FIBUNR = UDI_FIBUNR AND KTO_KONTONR = UDI_KONTONR" "~"
if joinKuerzel = KTOUDI
searchResult := searchResult "LEFT JOIN KORE.UDI_UMLAGEISTWERTE ON UDI_FIRMENNR = KTO_FIRMENNR AND UDI_FIBUNR = KTO_FIBUNR AND UDI_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = MAVKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = MAV_FIRMENNR AND KTO_FIBUNR = MAV_FIBUNR AND KTO_KONTONR = MAV_MAHNGEB_KONTONR" "~"
if joinKuerzel = KTOMAV
searchResult := searchResult "LEFT JOIN FIBU.MAV_MAHNVZINSEN ON MAV_FIRMENNR = KTO_FIRMENNR AND MAV_FIBUNR = KTO_FIBUNR AND MAV_MAHNGEB_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = PKBKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = PKB_FIBUFIRMENNR AND KTO_FIBUNR = PKB_FIBUNR AND KTO_KONTONR = PKB_GEGENKONTONR" "~"
if joinKuerzel = KTOPKB
searchResult := searchResult "LEFT JOIN BIL.PKB_PERSKORSTBUCHBELEGZU ON PKB_FIBUFIRMENNR = KTO_FIRMENNR AND PKB_FIBUNR = KTO_FIBUNR AND PKB_GEGENKONTONR = KTO_KONTONR" "~"
if joinKuerzel = KVTKVN
searchResult := searchResult "LEFT JOIN HRS.KVN_KVNAME ON KVN_HRSSEQID = KVT_HRSSEQID AND KVN_DATENBESTANDNR = KVT_DATENBESTANDNR AND KVN_KVNAME_KZ = KVT_KVTAB_KZ AND KVN_KVNAME_LFDNR = KVT_KVTAB_LFDNR" "~"
if joinKuerzel = KVNKVT
searchResult := searchResult "LEFT JOIN HRS.KVT_KVTAB ON KVT_HRSSEQID = KVN_HRSSEQID AND KVT_DATENBESTANDNR = KVN_DATENBESTANDNR AND KVT_KVTAB_KZ = KVN_KVNAME_KZ AND KVT_KVTAB_LFDNR = KVN_KVNAME_LFDNR" "~"
if joinKuerzel = AVELAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = AVE_LAG_FIRMENNR AND LAG_LAGERNR = AVE_LAG_LAGERNR" "~"
if joinKuerzel = LAGAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_LAG_FIRMENNR = LAG_FIRMENNR AND AVE_LAG_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = ARLKST
searchResult := searchResult "LEFT JOIN KST_KORESTAMM.KST_KORESTAMM ON KST_KOSTENSTAMMNR = ARL_ABT_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_ABT_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = ARLKST
searchResult := searchResult "LEFT JOIN KST_KORESTAMM.KST_KORESTAMM ON KST_KOSTENSTAMMNR = ARL_DIM_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_DIM_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = KSUKSU
searchResult := searchResult "LEFT JOIN WWS.KSU_KASSENSUMMENKOPF ON KSU_KASSENSUMMENKOPFID = KSU_FOLGE_KASSENSUMMENKOPFID" "~"
if joinKuerzel = KSUKSU
searchResult := searchResult "LEFT JOIN WWS.KSU_KASSENSUMMENKOPF ON KSU_FOLGE_KASSENSUMMENKOPFID = KSU_KASSENSUMMENKOPFID" "~"
if joinKuerzel = ASUKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = ASU_FIRMENNR AND KTO_FIBUNR = ASU_FIBUNR AND KTO_KONTONR = ASU_KONTONR" "~"
if joinKuerzel = KTOASU
searchResult := searchResult "LEFT JOIN BIL.ASU_ANLAGENSUMMEN ON ASU_FIRMENNR = KTO_FIRMENNR AND ASU_FIBUNR = KTO_FIBUNR AND ASU_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = BWRKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = BWR_FIRMENNR AND KTO_FIBUNR = BWR_FIBUNR AND KTO_KONTONR = BWR_KONTONR" "~"
if joinKuerzel = KTOBWR
searchResult := searchResult "LEFT JOIN BIL.BWR_BEWERTRESERVESUMMEN ON BWR_FIRMENNR = KTO_FIRMENNR AND BWR_FIBUNR = KTO_FIBUNR AND BWR_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = KAEKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = KAE_KTO_FIRMENNR AND KTO_FIBUNR = KAE_KTO_FIBUNR AND KTO_KONTONR = KAE_KTO_KONTONR" "~"
if joinKuerzel = KTOKAE
searchResult := searchResult "LEFT JOIN WWS.KAE_KASSENEINAUSGABEN ON KAE_KTO_FIRMENNR = KTO_FIRMENNR AND KAE_KTO_FIBUNR = KTO_FIBUNR AND KAE_KTO_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = KAKKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = KAK_FIBU_FIRMENNR AND KTO_FIBUNR = KAK_FIBUNR AND KTO_KONTONR = KAK_FIBU_KONTONR" "~"
if joinKuerzel = KTOKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_FIBU_FIRMENNR = KTO_FIRMENNR AND KAK_FIBUNR = KTO_FIBUNR AND KAK_FIBU_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = LASKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = LAS_FIRMENNR AND KTO_FIBUNR = LAS_FIBUNR AND KTO_KONTONR = LAS_KONTONR" "~"
if joinKuerzel = KTOLAS
searchResult := searchResult "LEFT JOIN BIL.LAS_LATENTE_STEUERN ON LAS_FIRMENNR = KTO_FIRMENNR AND LAS_FIBUNR = KTO_FIBUNR AND LAS_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = SRBKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = SRB_FIBUFIRMENNR AND KTO_FIBUNR = SRB_FIBUNR AND KTO_KONTONR = SRB_KONTONR" "~"
if joinKuerzel = KTOSRB
searchResult := searchResult "LEFT JOIN BIL.SRB_STEUERBUCHUNGEN ON SRB_FIBUFIRMENNR = KTO_FIRMENNR AND SRB_FIBUNR = KTO_FIBUNR AND SRB_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = VBVKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = VBV_FIRMENNR AND KTO_FIBUNR = VBV_FIBUNR AND KTO_KONTONR = VBV_KTO_KONTONR" "~"
if joinKuerzel = KTOVBV
searchResult := searchResult "LEFT JOIN FIBU.VBV_VORERFASSBUCH ON VBV_FIRMENNR = KTO_FIRMENNR AND VBV_FIBUNR = KTO_FIBUNR AND VBV_KTO_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = VKPKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = VKP_FIRMENNR AND KTO_FIBUNR = VKP_FIBUNR AND KTO_KONTONR = VKP_KUERZUNG_KTO_KLASSE0" "~"
if joinKuerzel = KTOVKP
searchResult := searchResult "LEFT JOIN FIBU.VKP_VSTKPROJEKT ON VKP_FIRMENNR = KTO_FIRMENNR AND VKP_FIBUNR = KTO_FIBUNR AND VKP_KUERZUNG_KTO_KLASSE0 = KTO_KONTONR" "~"
if joinKuerzel = VKPKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = VKP_FIRMENNR AND KTO_FIBUNR = VKP_FIBUNR AND KTO_KONTONR = VKP_KUERZUNG_KTO_KLASSE3" "~"
if joinKuerzel = KTOVKP
searchResult := searchResult "LEFT JOIN FIBU.VKP_VSTKPROJEKT ON VKP_FIRMENNR = KTO_FIRMENNR AND VKP_FIBUNR = KTO_FIBUNR AND VKP_KUERZUNG_KTO_KLASSE3 = KTO_KONTONR" "~"
if joinKuerzel = ZSUKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = ZSU_FIRMENNR AND KTO_FIBUNR = ZSU_FIBUNR AND KTO_KONTONR = ZSU_KONTONR" "~"
if joinKuerzel = KTOZSU
searchResult := searchResult "LEFT JOIN FIBU.ZSU_ZAHLSUMME ON ZSU_FIRMENNR = KTO_FIRMENNR AND ZSU_FIBUNR = KTO_FIBUNR AND ZSU_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = LBZKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = LBZ_FIRMENNR AND KTO_FIBUNR = LBZ_FIBUNR AND KTO_KONTONR = LBZ_GKONTONR" "~"
if joinKuerzel = KTOLBZ
searchResult := searchResult "LEFT JOIN BIL.LBZ_LANGFRISTERST_BELEGZU ON LBZ_FIRMENNR = KTO_FIRMENNR AND LBZ_FIBUNR = KTO_FIBUNR AND LBZ_GKONTONR = KTO_KONTONR" "~"
if joinKuerzel = UMPKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = UMP_FIRMENNR AND KTO_FIBUNR = UMP_FIBUNR AND KTO_KONTONR = UMP_ORIGINALKONTONR" "~"
if joinKuerzel = KTOUMP
searchResult := searchResult "LEFT JOIN KORE.UMP_UMLAGEPLANMENGEN ON UMP_FIRMENNR = KTO_FIRMENNR AND UMP_FIBUNR = KTO_FIBUNR AND UMP_ORIGINALKONTONR = KTO_KONTONR" "~"
if joinKuerzel = AVGKUG
searchResult := searchResult "LEFT JOIN KUG_KUNDENGRUPPE.KUG_KUNDENGRUPPE ON KUG_FIRMENNR = AVG_KUG_FIRMENNR AND KUG_KUNDENHAUPTGRUPPENNR = AVG_KUNDENHAUPTGRUPPENNR AND KUG_KUNDENUNTERGRUPPENNR = AVG_KUNDENUNTERGRUPPENNR" "~"
if joinKuerzel = KUGAVG
searchResult := searchResult "LEFT JOIN WWS.AVG_AUFTRAGSVORERFGRUPP ON AVG_KUG_FIRMENNR = KUG_FIRMENNR AND AVG_KUNDENHAUPTGRUPPENNR = KUG_KUNDENHAUPTGRUPPENNR AND AVG_KUNDENUNTERGRUPPENNR = KUG_KUNDENUNTERGRUPPENNR" "~"
if joinKuerzel = TOZKUG
searchResult := searchResult "LEFT JOIN KUG_KUNDENGRUPPE.KUG_KUNDENGRUPPE ON KUG_FIRMENNR = TOZ_KUG_FIRMENNR AND KUG_KUNDENHAUPTGRUPPENNR = TOZ_KUG_KUNDENHAUPTGRPNR AND KUG_KUNDENUNTERGRUPPENNR = TOZ_KUG_KUNDENUNTERGRPNR" "~"
if joinKuerzel = KUGTOZ
searchResult := searchResult "LEFT JOIN WWS.TOZ_TOURZUORDNUNG ON TOZ_KUG_FIRMENNR = KUG_FIRMENNR AND TOZ_KUG_KUNDENHAUPTGRPNR = KUG_KUNDENHAUPTGRUPPENNR AND TOZ_KUG_KUNDENUNTERGRPNR = KUG_KUNDENUNTERGRUPPENNR" "~"
if joinKuerzel = WKGKUG
searchResult := searchResult "LEFT JOIN KUG_KUNDENGRUPPE.KUG_KUNDENGRUPPE ON KUG_FIRMENNR = WKG_FIRMENNR AND KUG_KUNDENHAUPTGRUPPENNR = WKG_KUNDENHAUPTGRUPPENNR AND KUG_KUNDENUNTERGRUPPENNR = WKG_KUNDENUNTERGRUPPENNR" "~"
if joinKuerzel = KUGWKG
searchResult := searchResult "LEFT JOIN WWS.WKG_WWSKUNDENGRUPPE ON WKG_FIRMENNR = KUG_FIRMENNR AND WKG_KUNDENHAUPTGRUPPENNR = KUG_KUNDENHAUPTGRUPPENNR AND WKG_KUNDENUNTERGRUPPENNR = KUG_KUNDENUNTERGRUPPENNR" "~"
if joinKuerzel = LARKVT
searchResult := searchResult "LEFT JOIN HRS.KVT_KVTAB ON KVT_HRSSEQID = LAR_KVID AND KVT_DATENBESTANDNR = LAR_DBSNR AND KVT_KVTAB_KZ = LAR_KVT_KZ AND KVT_KVTAB_LFDNR = LAR_KVT_LFDNR AND KVT_KVTAB_GUELTIGAB = LAR_KVT_GUELTIGAB AND KVT_KVTAB_SPALTE = LAR_KVT_SPALTE AND KVT_KVTAB_ZEILE = LAR_KVT_ZEILE" "~"
if joinKuerzel = KVTLAR
searchResult := searchResult "LEFT JOIN HRS.LAR_LOHNARTENREGEL ON LAR_KVID = KVT_HRSSEQID AND LAR_DBSNR = KVT_DATENBESTANDNR AND LAR_KVT_KZ = KVT_KVTAB_KZ AND LAR_KVT_LFDNR = KVT_KVTAB_LFDNR AND LAR_KVT_GUELTIGAB = KVT_KVTAB_GUELTIGAB AND LAR_KVT_SPALTE = KVT_KVTAB_SPALTE AND LAR_KVT_ZEILE = KVT_KVTAB_ZEILE" "~"
if joinKuerzel = KBEKSD
searchResult := searchResult "LEFT JOIN WWS.KSD_KASSENSUMMENDETAIL ON KSD_KASSENSUMMENDETID = KBE_KSD_KASSENSUMMENDETID" "~"
if joinKuerzel = KSDKBE
searchResult := searchResult "LEFT JOIN WWS.KBE_KASSEBUCHEINZELNW ON KBE_KSD_KASSENSUMMENDETID = KSD_KASSENSUMMENDETID" "~"
if joinKuerzel = KSEKSK
searchResult := searchResult "LEFT JOIN KORE.KSK_KORESCHLUESSELKOPF ON KSK_FIRMENNR = KSE_FIRMENNR AND KSK_KORENR = KSE_KORENR AND KSK_KENNZ_UEB_UML = KSE_KENNZ_UEB_UML AND KSK_SCHLUESSEL = KSE_SCHLUESSEL AND KSK_SCHLUESSELART_ISTPLAN = KSE_SCHLUESSELART_ISTPLAN AND KSK_SCHLUESSELART_GRENZ = KSE_SCHLUESSELART_GRENZ AND KSK_SCHLUESSELMODELLNR = KSE_SCHLUESSELMODELLNR" "~"
if joinKuerzel = KSKKSE
searchResult := searchResult "LEFT JOIN KORE.KSE_KORESCHLUESSELEINGABE ON KSE_FIRMENNR = KSK_FIRMENNR AND KSE_KORENR = KSK_KORENR AND KSE_KENNZ_UEB_UML = KSK_KENNZ_UEB_UML AND KSE_SCHLUESSEL = KSK_SCHLUESSEL AND KSE_SCHLUESSELART_ISTPLAN = KSK_SCHLUESSELART_ISTPLAN AND KSE_SCHLUESSELART_GRENZ = KSK_SCHLUESSELART_GRENZ AND KSE_SCHLUESSELMODELLNR = KSK_SCHLUESSELMODELLNR" "~"
if joinKuerzel = KAJKSM
searchResult := searchResult "LEFT JOIN WWS.KSM_KASSENSUMMEN ON KSM_KASSENSUMMENID = KAJ_KASSENSUMMENID" "~"
if joinKuerzel = KSMKAJ
searchResult := searchResult "LEFT JOIN WWS.KAJ_KASSENJOURNAL ON KAJ_KASSENSUMMENID = KSM_KASSENSUMMENID" "~"
if joinKuerzel = KURKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = KUR_FIRMENNR AND KST_KORENR = KUR_KORENR AND KST_KOSTENTYP = KUR_KOSTENTYP AND KST_KOSTENSTAMMNR = KUR_KOSTENSTAMMNR AND KST_KNOTENREFNR = KUR_KNOTENREFNR" "~"
if joinKuerzel = KSTKUR
searchResult := searchResult "LEFT JOIN KORE.KUR_UMLAGEREIHENFOLGE ON KUR_FIRMENNR = KST_FIRMENNR AND KUR_KORENR = KST_KORENR AND KUR_KOSTENTYP = KST_KOSTENTYP AND KUR_KOSTENSTAMMNR = KST_KOSTENSTAMMNR AND KUR_KNOTENREFNR = KST_KNOTENREFNR" "~"
if joinKuerzel = KZEKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = KZE_FWK_FIRMENNR AND KST_KORENR = KZE_KORENR AND KST_KOSTENTYP = KZE_KNOTENKOSTENTYP AND KST_KOSTENSTAMMNR = KZE_KNOTENKOSTENSTAMMNR AND KST_KNOTENREFNR = KZE_KNOTENREFNR" "~"
if joinKuerzel = KSTKZE
searchResult := searchResult "LEFT JOIN KORE.KZE_ZUSCHLAGEINGABE ON KZE_FWK_FIRMENNR = KST_FIRMENNR AND KZE_KORENR = KST_KORENR AND KZE_KNOTENKOSTENTYP = KST_KOSTENTYP AND KZE_KNOTENKOSTENSTAMMNR = KST_KOSTENSTAMMNR AND KZE_KNOTENREFNR = KST_KNOTENREFNR" "~"
if joinKuerzel = WAGKST
searchResult := searchResult "LEFT JOIN KST_KORESTAMM.KST_KORESTAMM ON KST_KOSTENSTAMMNR = WAG_KOSTENTR_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_KOSTENTR_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = RNGKST
searchResult := searchResult "LEFT JOIN KST_KORESTAMM.KST_KORESTAMM ON KST_FIRMENNR = RNG_KORE_FIRMENNR AND KST_KORENR = RNG_KORENR AND KST_KOSTENTYP = RNG_KOSTENTR_KOSTENTYP AND KST_KNOTENREFNR = RNG_KOSTENST_KNOTENREFNR AND KST_KOSTENSTAMMNR = RNG_KOSTENST_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_KORE_FIRMENNR = KST_FIRMENNR AND RNG_KORENR = KST_KORENR AND RNG_KOSTENTR_KOSTENTYP = KST_KOSTENTYP AND RNG_KOSTENST_KNOTENREFNR = KST_KNOTENREFNR AND RNG_KOSTENST_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = WEBKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = WEB_KMG_FIRMENNR AND KST_KORENR = WEB_KORENR AND KST_KOSTENSTAMMNR = WEB_BEST_KST_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTWEB
searchResult := searchResult "LEFT JOIN WWS.WEB_WARENEINSATZBUCH ON WEB_KMG_FIRMENNR = KST_FIRMENNR AND WEB_KORENR = KST_KORENR AND WEB_BEST_KST_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = WEBKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = WEB_KMG_FIRMENNR AND KST_KORENR = WEB_KORENR AND KST_KOSTENSTAMMNR = WEB_BEST_KTR_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTWEB
searchResult := searchResult "LEFT JOIN WWS.WEB_WARENEINSATZBUCH ON WEB_KMG_FIRMENNR = KST_FIRMENNR AND WEB_KORENR = KST_KORENR AND WEB_BEST_KTR_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = WEBKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = WEB_KMG_FIRMENNR AND KST_KORENR = WEB_KORENR AND KST_KOSTENSTAMMNR = WEB_BEST_DIM_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTWEB
searchResult := searchResult "LEFT JOIN WWS.WEB_WARENEINSATZBUCH ON WEB_KMG_FIRMENNR = KST_FIRMENNR AND WEB_KORENR = KST_KORENR AND WEB_BEST_DIM_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = KAJKSU
searchResult := searchResult "LEFT JOIN WWS.KSU_KASSENSUMMENKOPF ON KSU_KASSENSUMMENKOPFID = KAJ_KASSENSUMMENKOPFID" "~"
if joinKuerzel = KSUKAJ
searchResult := searchResult "LEFT JOIN WWS.KAJ_KASSENJOURNAL ON KAJ_KASSENSUMMENKOPFID = KSU_KASSENSUMMENKOPFID" "~"
if joinKuerzel = AKAKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = AKA_KTO_FIRMENNR AND KTO_FIBUNR = AKA_KTO_FIBUNR AND KTO_KONTONR = AKA_KTO_KONTONR" "~"
if joinKuerzel = KTOAKA
searchResult := searchResult "LEFT JOIN BUERO.AKA_AVKOSTENARTEN ON AKA_KTO_FIRMENNR = KTO_FIRMENNR AND AKA_KTO_FIBUNR = KTO_FIBUNR AND AKA_KTO_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = AUSKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = AUS_FIRMENNR AND KTO_FIBUNR = AUS_FIBUNR AND KTO_KONTONR = AUS_KONTONR" "~"
if joinKuerzel = KTOAUS
searchResult := searchResult "LEFT JOIN BIL.AUS_AUSWERTUNG ON AUS_FIRMENNR = KTO_FIRMENNR AND AUS_FIBUNR = KTO_FIBUNR AND AUS_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = BONKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = BON_FIBU_FIRMENNR AND KTO_FIBUNR = BON_FIBUNR AND KTO_KONTONR = BON_KONTONR" "~"
if joinKuerzel = KTOBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_FIBU_FIRMENNR = KTO_FIRMENNR AND BON_FIBUNR = KTO_FIBUNR AND BON_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = KMPKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = KMP_FIRMENNR AND KTO_FIBUNR = KMP_FIBUNR AND KTO_KONTONR = KMP_KONTONR" "~"
if joinKuerzel = KTOKMP
searchResult := searchResult "LEFT JOIN KORE.KMP_PLANMENGEN ON KMP_FIRMENNR = KTO_FIRMENNR AND KMP_FIBUNR = KTO_FIBUNR AND KMP_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = KZAKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = KZA_KTO_FIRMENNR AND KTO_FIBUNR = KZA_KTO_FIBUNR AND KTO_KONTONR = KZA_KTO_KONTONR" "~"
if joinKuerzel = KTOKZA
searchResult := searchResult "LEFT JOIN WWS.KZA_KASSENZAHLUNGSARTENZU ON KZA_KTO_FIRMENNR = KTO_FIRMENNR AND KZA_KTO_FIBUNR = KTO_FIBUNR AND KZA_KTO_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = UVWKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = UVW_FIRMENNR AND KTO_FIBUNR = UVW_FIBUNR AND KTO_KONTONR = UVW_KONTONR" "~"
if joinKuerzel = KTOUVW
searchResult := searchResult "LEFT JOIN FIBU.UVW_UVAWERT ON UVW_FIRMENNR = KTO_FIRMENNR AND UVW_FIBUNR = KTO_FIBUNR AND UVW_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = LATKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = LAT_KLI_FIRMENNR AND KLI_KLID = LAT_KLI_HERSTELLER_KLID" "~"
if joinKuerzel = KLILAT
searchResult := searchResult "LEFT JOIN WWS.LAT_LIEFERANTENARTIKEL ON LAT_KLI_FIRMENNR = KLI_FIRMENNR AND LAT_KLI_HERSTELLER_KLID = KLI_KLID" "~"
if joinKuerzel = EFKKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = EFK_FIBU_FIRMENNR AND KLI_KLID = EFK_FIBU_KLID" "~"
if joinKuerzel = KLIEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIBU_FIRMENNR = KLI_FIRMENNR AND EFK_FIBU_KLID = KLI_KLID" "~"
if joinKuerzel = EFKKOD
searchResult := searchResult "LEFT JOIN KOD_KONDITIONEN.KOD_KONDITIONEN ON KOD_KONDNR = EFK_ZAHLKONDNR" "~"
if joinKuerzel = KODEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_ZAHLKONDNR = KOD_KONDNR" "~"
if joinKuerzel = PROKOD
searchResult := searchResult "LEFT JOIN BUERO.KOD_KONDITIONEN ON KOD_KONDNR = PRO_KONDNR" "~"
if joinKuerzel = KODPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_KONDNR = KOD_KONDNR" "~"
if joinKuerzel = EFKKOD
searchResult := searchResult "LEFT JOIN KOD_KONDITIONEN.KOD_KONDITIONEN ON KOD_KONDNR = EFK_ANZAHLTEIL_KONDNR" "~"
if joinKuerzel = KODEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_ANZAHLTEIL_KONDNR = KOD_KONDNR" "~"
if joinKuerzel = PEKKOD
searchResult := searchResult "LEFT JOIN KOD_KONDITIONEN.KOD_KONDITIONEN ON KOD_KONDNR = PEK_KOD_LIE_KONDNR" "~"
if joinKuerzel = KODPEK
searchResult := searchResult "LEFT JOIN FIBU.PEK_PERSONENKONTO ON PEK_KOD_LIE_KONDNR = KOD_KONDNR" "~"
if joinKuerzel = PROKOD
searchResult := searchResult "LEFT JOIN BUERO.KOD_KONDITIONEN ON KOD_KONDNR = PRO_ANZAHLTEIL_KONDNR" "~"
if joinKuerzel = KODPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_ANZAHLTEIL_KONDNR = KOD_KONDNR" "~"
if joinKuerzel = ELVKOK
searchResult := searchResult "LEFT JOIN KOK_KOMMISSIONIERUNG_KOPF.KOK_KOMMISSIONIERUNG_KOPF ON KOK_FIRMENNR = ELV_FIRMENNR AND KOK_KOMID = ELV_KOK_KOMID" "~"
if joinKuerzel = KOKELV
searchResult := searchResult "LEFT JOIN WWS.ELV_ERFASSPOSLIEFERVOR ON ELV_FIRMENNR = KOK_FIRMENNR AND ELV_KOK_KOMID = KOK_KOMID" "~"
if joinKuerzel = EPSKOK
searchResult := searchResult "LEFT JOIN KOK_KOMMISSIONIERUNG_KOPF.KOK_KOMMISSIONIERUNG_KOPF ON KOK_FIRMENNR = EPS_FIRMENNR AND KOK_KOMID = EPS_KOMID" "~"
if joinKuerzel = KOKEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_FIRMENNR = KOK_FIRMENNR AND EPS_KOMID = KOK_KOMID" "~"
if joinKuerzel = KZKKPM
searchResult := searchResult "LEFT JOIN KORE.KPM_PLANMODELL ON KPM_FIRMENNR = KZK_FWK_FIRMENNR AND KPM_KORENR = KZK_KORENR AND KPM_MODELLNR = KZK_MODELLNR" "~"
if joinKuerzel = KPMKZK
searchResult := searchResult "LEFT JOIN KORE.KZK_ZUSCHLAGKOPF ON KZK_FWK_FIRMENNR = KPM_FIRMENNR AND KZK_KORENR = KPM_KORENR AND KZK_MODELLNR = KPM_MODELLNR" "~"
if joinKuerzel = KPMKPM
searchResult := searchResult "LEFT JOIN KORE.KPM_PLANMODELL ON KPM_FIRMENNR = KPM_FIRMENNR AND KPM_KORENR = KPM_KORENR AND KPM_MODELLNR = KPM_VERGLEICHSMODELLNR2" "~"
if joinKuerzel = KPMKPM
searchResult := searchResult "LEFT JOIN KORE.KPM_PLANMODELL ON KPM_FIRMENNR = KPM_FIRMENNR AND KPM_KORENR = KPM_KORENR AND KPM_VERGLEICHSMODELLNR2 = KPM_MODELLNR" "~"
if joinKuerzel = KPMKPM
searchResult := searchResult "LEFT JOIN KORE.KPM_PLANMODELL ON KPM_FIRMENNR = KPM_FIRMENNR AND KPM_KORENR = KPM_KORENR AND KPM_MODELLNR = KPM_ORIG_MODELLNR" "~"
if joinKuerzel = KPMKPM
searchResult := searchResult "LEFT JOIN KORE.KPM_PLANMODELL ON KPM_FIRMENNR = KPM_FIRMENNR AND KPM_KORENR = KPM_KORENR AND KPM_ORIG_MODELLNR = KPM_MODELLNR" "~"
if joinKuerzel = KEPKSD
searchResult := searchResult "LEFT JOIN WWS.KSD_KASSENSUMMENDETAIL ON KSD_KASSENSUMMENDETID = KEP_KSD_KASSENSUMMENDETID" "~"
if joinKuerzel = KSDKEP
searchResult := searchResult "LEFT JOIN WWS.KEP_KASSENERFASSUNGSPROT ON KEP_KSD_KASSENSUMMENDETID = KSD_KASSENSUMMENDETID" "~"
if joinKuerzel = KEPKSM
searchResult := searchResult "LEFT JOIN WWS.KSM_KASSENSUMMEN ON KSM_KASSENSUMMENID = KEP_KSM_KASSENSUMMENID" "~"
if joinKuerzel = KSMKEP
searchResult := searchResult "LEFT JOIN WWS.KEP_KASSENERFASSUNGSPROT ON KEP_KSM_KASSENSUMMENID = KSM_KASSENSUMMENID" "~"
if joinKuerzel = RNGKST
searchResult := searchResult "LEFT JOIN KST_KORESTAMM.KST_KORESTAMM ON KST_FIRMENNR = RNG_KORE_FIRMENNR AND KST_KORENR = RNG_KORENR AND KST_KOSTENTYP = RNG_KOSTENTR_KOSTENTYP AND KST_KNOTENREFNR = RNG_KOSTENTR_KNOTENREFNR AND KST_KOSTENSTAMMNR = RNG_KOSTENTR_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_KORE_FIRMENNR = KST_FIRMENNR AND RNG_KORENR = KST_KORENR AND RNG_KOSTENTR_KOSTENTYP = KST_KOSTENTYP AND RNG_KOSTENTR_KNOTENREFNR = KST_KNOTENREFNR AND RNG_KOSTENTR_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = KSCKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = KSC_FIRMENNR AND KST_KORENR = KSC_KORENR AND KST_KOSTENTYP = KSC_KOTR_KOSTENTYP AND KST_KOSTENSTAMMNR = KSC_KOTR_KOSTENSTAMMNR AND KST_KNOTENREFNR = KSC_KOTR_KNOTENREFNR" "~"
if joinKuerzel = KSTKSC
searchResult := searchResult "LEFT JOIN KORE.KSC_KORESCHLUESSEL ON KSC_FIRMENNR = KST_FIRMENNR AND KSC_KORENR = KST_KORENR AND KSC_KOTR_KOSTENTYP = KST_KOSTENTYP AND KSC_KOTR_KOSTENSTAMMNR = KST_KOSTENSTAMMNR AND KSC_KOTR_KNOTENREFNR = KST_KNOTENREFNR" "~"
if joinKuerzel = RNPKST
searchResult := searchResult "LEFT JOIN KST_KORESTAMM.KST_KORESTAMM ON KST_KOSTENSTAMMNR = RNP_GBR_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_GBR_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = WAGKST
searchResult := searchResult "LEFT JOIN KST_KORESTAMM.KST_KORESTAMM ON KST_KOSTENSTAMMNR = WAG_GBR_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_GBR_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = KSDKSU
searchResult := searchResult "LEFT JOIN WWS.KSU_KASSENSUMMENKOPF ON KSU_KASSENSUMMENKOPFID = KSD_KSU_KASSENSUMMENKOPFID" "~"
if joinKuerzel = KSUKSD
searchResult := searchResult "LEFT JOIN WWS.KSD_KASSENSUMMENDETAIL ON KSD_KSU_KASSENSUMMENKOPFID = KSU_KASSENSUMMENKOPFID" "~"
if joinKuerzel = KTGKTK
searchResult := searchResult "LEFT JOIN FIBU.KTK_KONTENKREIS ON KTK_FIRMENNR = KTG_FIRMENNR AND KTK_FIBUNR = KTG_FIBUNR AND KTK_KONTENKREISNR = KTG_KONTENKREISNR AND KTK_KREIS_KONTENGRUPPE = KTG_KREIS_KONTENGRUPPE" "~"
if joinKuerzel = KTKKTG
searchResult := searchResult "LEFT JOIN FIBU.KTG_KONTENGRENZEN ON KTG_FIRMENNR = KTK_FIRMENNR AND KTG_FIBUNR = KTK_FIBUNR AND KTG_KONTENKREISNR = KTK_KONTENKREISNR AND KTG_KREIS_KONTENGRUPPE = KTK_KREIS_KONTENGRUPPE" "~"
if joinKuerzel = KLIKOD
searchResult := searchResult "LEFT JOIN BUERO.KOD_KONDITIONEN ON KOD_KONDNR = KLI_LIEF_KONDNR" "~"
if joinKuerzel = KODKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_LIEF_KONDNR = KOD_KONDNR" "~"
if joinKuerzel = KOZKOK
searchResult := searchResult "LEFT JOIN PRODUKTION.KOK_KOMMISSIONIERUNG_KOPF ON KOK_FIRMENNR = KOZ_FIRMENNR AND KOK_KOMID = KOZ_KOMID" "~"
if joinKuerzel = KOKKOZ
searchResult := searchResult "LEFT JOIN PRODUKTION.KOZ_KOMMISSIONIERUNG_ZU ON KOZ_FIRMENNR = KOK_FIRMENNR AND KOZ_KOMID = KOK_KOMID" "~"
if joinKuerzel = KMPKPM
searchResult := searchResult "LEFT JOIN KORE.KPM_PLANMODELL ON KPM_FIRMENNR = KMP_FIRMENNR AND KPM_KORENR = KMP_KORENR AND KPM_MODELLNR = KMP_MODELLNR" "~"
if joinKuerzel = KPMKMP
searchResult := searchResult "LEFT JOIN KORE.KMP_PLANMENGEN ON KMP_FIRMENNR = KPM_FIRMENNR AND KMP_KORENR = KPM_KORENR AND KMP_MODELLNR = KPM_MODELLNR" "~"
if joinKuerzel = VPZKPP
searchResult := searchResult "LEFT JOIN BUERO.KPP_KAMPAGNENPHASE ON KPP_KPPID = VPZ_KPPID" "~"
if joinKuerzel = KPPVPZ
searchResult := searchResult "LEFT JOIN BUERO.VPZ_VORGANGPHASEZU ON VPZ_KPPID = KPP_KPPID" "~"
if joinKuerzel = AUWKRI
searchResult := searchResult "LEFT JOIN BUERO.KRI_KRITERIUM ON KRI_KRITERIENNR = AUW_KRITERIENNR" "~"
if joinKuerzel = KRIAUW
searchResult := searchResult "LEFT JOIN BUERO.AUW_AUSWAHL ON AUW_KRITERIENNR = KRI_KRITERIENNR" "~"
if joinKuerzel = LAWKRM
searchResult := searchResult "LEFT JOIN HRS.KRM_KONTENREFERENZTAB ON KRM_DATENBESTANDNR = LAW_DBSNR AND KRM_KONTOREFNR = LAW_KONTOREFNR_ZLE" "~"
if joinKuerzel = KRMLAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_DBSNR = KRM_DATENBESTANDNR AND LAW_KONTOREFNR_ZLE = KRM_KONTOREFNR" "~"
if joinKuerzel = KSUKSM
searchResult := searchResult "LEFT JOIN WWS.KSM_KASSENSUMMEN ON KSM_KASSENSUMMENID = KSU_KSM_KASSENSUMMENID" "~"
if joinKuerzel = KSMKSU
searchResult := searchResult "LEFT JOIN WWS.KSU_KASSENSUMMENKOPF ON KSU_KSM_KASSENSUMMENID = KSM_KASSENSUMMENID" "~"
if joinKuerzel = KADKSP
searchResult := searchResult "LEFT JOIN KORE.KSP_SPALTENKOPF ON KSP_SPALTENKOPFID = KAD_KSP_SPALTENKOPFID" "~"
if joinKuerzel = KSPKAD
searchResult := searchResult "LEFT JOIN KORE.KAD_AUSDRUCKDETAIL ON KAD_KSP_SPALTENKOPFID = KSP_SPALTENKOPFID" "~"
if joinKuerzel = AGSKST
searchResult := searchResult "LEFT JOIN KST_KORESTAMM.KST_KORESTAMM ON KST_KOSTENSTAMMNR = AGS_KOSTENST_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_KOSTENST_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = KBZKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = KBZ_FIRMENNR AND KST_KORENR = KBZ_KORENR AND KST_KOSTENTYP = KBZ_KOSTENTYP AND KST_KOSTENSTAMMNR = KBZ_KOSTENSTAMMNR AND KST_KNOTENREFNR = KBZ_KNOTENREFNR" "~"
if joinKuerzel = KSTKBZ
searchResult := searchResult "LEFT JOIN KORE.KBZ_KOSTENBAUMZU ON KBZ_FIRMENNR = KST_FIRMENNR AND KBZ_KORENR = KST_KORENR AND KBZ_KOSTENTYP = KST_KOSTENTYP AND KBZ_KOSTENSTAMMNR = KST_KOSTENSTAMMNR AND KBZ_KNOTENREFNR = KST_KNOTENREFNR" "~"
if joinKuerzel = KSWKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = KSW_FIRMENNR AND KST_KORENR = KSW_KORENR AND KST_KOSTENTYP = KSW_KOSTENTYP AND KST_KOSTENSTAMMNR = KSW_KOSTENSTAMMNR AND KST_KNOTENREFNR = KSW_KNOTENREFNR" "~"
if joinKuerzel = KSTKSW
searchResult := searchResult "LEFT JOIN KORE.KSW_KORESTAMMWERTE ON KSW_FIRMENNR = KST_FIRMENNR AND KSW_KORENR = KST_KORENR AND KSW_KOSTENTYP = KST_KOSTENTYP AND KSW_KOSTENSTAMMNR = KST_KOSTENSTAMMNR AND KSW_KNOTENREFNR = KST_KNOTENREFNR" "~"
if joinKuerzel = SBBKST
searchResult := searchResult "LEFT JOIN KST_KORESTAMM.KST_KORESTAMM ON KST_FIRMENNR = SBB_KST_FIRMENNR AND KST_KORENR = SBB_KST_KORENR AND KST_KOSTENTYP = SBB_KST_KOSTENTYP AND KST_KNOTENREFNR = SBB_KST_KNOTENREFNR AND KST_KOSTENSTAMMNR = SBB_KST_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTSBB
searchResult := searchResult "LEFT JOIN HRS.SBB_SOLLBESETZUNG ON SBB_KST_FIRMENNR = KST_FIRMENNR AND SBB_KST_KORENR = KST_KORENR AND SBB_KST_KOSTENTYP = KST_KOSTENTYP AND SBB_KST_KNOTENREFNR = KST_KNOTENREFNR AND SBB_KST_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = AGSKST
searchResult := searchResult "LEFT JOIN KST_KORESTAMM.KST_KORESTAMM ON KST_KOSTENSTAMMNR = AGS_KOSTENTR_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_KOSTENTR_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = RNPKST
searchResult := searchResult "LEFT JOIN KST_KORESTAMM.KST_KORESTAMM ON KST_KOSTENSTAMMNR = RNP_ABT_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_ABT_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = WAGKST
searchResult := searchResult "LEFT JOIN KST_KORESTAMM.KST_KORESTAMM ON KST_KOSTENSTAMMNR = WAG_ABT_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_ABT_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = KSEKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = KSE_FIRMENNR AND KST_KORENR = KSE_KORENR AND KST_KOSTENTYP = KSE_KOST_KOSTENTYP AND KST_KOSTENSTAMMNR = KSE_KOST_KOSTENSTAMMNR AND KST_KNOTENREFNR = KSE_KOST_KNOTENREFNR" "~"
if joinKuerzel = KSTKSE
searchResult := searchResult "LEFT JOIN KORE.KSE_KORESCHLUESSELEINGABE ON KSE_FIRMENNR = KST_FIRMENNR AND KSE_KORENR = KST_KORENR AND KSE_KOST_KOSTENTYP = KST_KOSTENTYP AND KSE_KOST_KOSTENSTAMMNR = KST_KOSTENSTAMMNR AND KSE_KOST_KNOTENREFNR = KST_KNOTENREFNR" "~"
if joinKuerzel = RNPKST
searchResult := searchResult "LEFT JOIN KST_KORESTAMM.KST_KORESTAMM ON KST_KOSTENSTAMMNR = RNP_DIM_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_DIM_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = ARLKST
searchResult := searchResult "LEFT JOIN KST_KORESTAMM.KST_KORESTAMM ON KST_KOSTENSTAMMNR = ARL_GBR_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_GBR_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = KSCKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = KSC_FIRMENNR AND KST_KORENR = KSC_KORENR AND KST_KOSTENTYP = KSC_GBR_KOSTENTYP AND KST_KOSTENSTAMMNR = KSC_GBR_KOSTENSTAMMNR AND KST_KNOTENREFNR = KSC_GBR_KNOTENREFNR" "~"
if joinKuerzel = KSTKSC
searchResult := searchResult "LEFT JOIN KORE.KSC_KORESCHLUESSEL ON KSC_FIRMENNR = KST_FIRMENNR AND KSC_KORENR = KST_KORENR AND KSC_GBR_KOSTENTYP = KST_KOSTENTYP AND KSC_GBR_KOSTENSTAMMNR = KST_KOSTENSTAMMNR AND KSC_GBR_KNOTENREFNR = KST_KNOTENREFNR" "~"
if joinKuerzel = FIRLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = FIR_FIRMA_LANDNR" "~"
if joinKuerzel = LANFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMA_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = GKKLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = GKK_LANDNR" "~"
if joinKuerzel = LANGKK
searchResult := searchResult "LEFT JOIN BUERO.GKK_KRANKENKASSA ON GKK_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = HFTLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = HFT_LANDNR" "~"
if joinKuerzel = LANHFT
searchResult := searchResult "LEFT JOIN BUERO.HFT_HILFSTABELLE ON HFT_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = WAALAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = WAA_LANDNR" "~"
if joinKuerzel = LANWAA
searchResult := searchResult "LEFT JOIN BIL.WAA_WIPAKTENART ON WAA_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = WAFLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = WAF_LANDNR" "~"
if joinKuerzel = LANWAF
searchResult := searchResult "LEFT JOIN WWS.WAF_WWSADRZUFIRMENBEZ ON WAF_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = ONSLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = ONS_STAATSANGEHOERIGKEIT" "~"
if joinKuerzel = LANONS
searchResult := searchResult "LEFT JOIN HRS.ONS_ONLINEBEW_STAMMDATEN ON ONS_STAATSANGEHOERIGKEIT = LAN_LANDNR" "~"
if joinKuerzel = WKPLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = WKP_FILIALE_LANDNR" "~"
if joinKuerzel = LANWKP
searchResult := searchResult "LEFT JOIN WWS.WKP_WWSKONTENTABPOS ON WKP_FILIALE_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = ZASLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = ZAS_EMPFAENGERLANDNR" "~"
if joinKuerzel = LANZAS
searchResult := searchResult "LEFT JOIN BUERO.ZAS_ZAHLSCHEIN ON ZAS_EMPFAENGERLANDNR = LAN_LANDNR" "~"
if joinKuerzel = BVOLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = BVO_HAUPTFIRMENNR AND LAP_LAGERNR = BVO_LAGERNR AND LAP_LAGERPLATZ = BVO_LAGERPLATZ" "~"
if joinKuerzel = LAPBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_HAUPTFIRMENNR = LAP_FIRMENNR AND BVO_LAGERNR = LAP_LAGERNR AND BVO_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = LPOLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = LPO_LAG_FIRMENNR AND LAP_LAGERNR = LPO_LAGERNR AND LAP_LAGERPLATZ = LPO_LAGERPLATZ" "~"
if joinKuerzel = LAPLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_LAG_FIRMENNR = LAP_FIRMENNR AND LPO_LAGERNR = LAP_LAGERNR AND LPO_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = TLELAP
searchResult := searchResult "LEFT JOIN LAP_LAGERPLATZ.LAP_LAGERPLATZ ON LAP_FIRMENNR = TLE_DST_LAG_FIRMENNR AND LAP_LAGERNR = TLE_DST_LAGERNR AND LAP_LAGERPLATZ = TLE_DST_LAGERPLATZ" "~"
if joinKuerzel = LAPTLE
searchResult := searchResult "LEFT JOIN PRODUKTION.TLE_BT_LOGISTIK_EMPFANGEN ON TLE_DST_LAG_FIRMENNR = LAP_FIRMENNR AND TLE_DST_LAGERNR = LAP_LAGERNR AND TLE_DST_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = TRELAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = TRE_LAG_FIRMENNR AND LAP_LAGERNR = TRE_LAGERNR AND LAP_LAGERPLATZ = TRE_LAGERPLATZ" "~"
if joinKuerzel = LAPTRE
searchResult := searchResult "LEFT JOIN WWS.TRE_TRANSPORTEINHEIT ON TRE_LAG_FIRMENNR = LAP_FIRMENNR AND TRE_LAGERNR = LAP_LAGERNR AND TRE_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = WADLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = WAD_ZIELLAGER_FIRMENNR AND LAP_LAGERNR = WAD_ZIELLAGER_LAGERNR AND LAP_LAGERPLATZ = WAD_ZIELLAGER_LAGERPLATZ" "~"
if joinKuerzel = LAPWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_ZIELLAGER_FIRMENNR = LAP_FIRMENNR AND WAD_ZIELLAGER_LAGERNR = LAP_LAGERNR AND WAD_ZIELLAGER_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = BARLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = BAR_LAQ_LOHNARTID" "~"
if joinKuerzel = LAQBAR
searchResult := searchResult "LEFT JOIN HRS.BAR_BARAUSLAGEN ON BAR_LAQ_LOHNARTID = LAQ_LOHNARTID" "~"
if joinKuerzel = DNZLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = DNZ_LOHNARTID" "~"
if joinKuerzel = LAQDNZ
searchResult := searchResult "LEFT JOIN HRS.DNZ_DIVNICHTLEISTUNGSZEIT ON DNZ_LOHNARTID = LAQ_LOHNARTID" "~"
if joinKuerzel = KSZLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = KSZ_LOHNARTID" "~"
if joinKuerzel = LAQKSZ
searchResult := searchResult "LEFT JOIN HRS.KSZ_BBKALKSZ ON KSZ_LOHNARTID = LAQ_LOHNARTID" "~"
if joinKuerzel = LAWLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = LAW_LS_ADD_LA_5" "~"
if joinKuerzel = LAQLAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_LS_ADD_LA_5 = LAQ_LOHNARTID" "~"
if joinKuerzel = MDALBK
searchResult := searchResult "LEFT JOIN WWS.LBK_LAGERBUCHKENNZ ON LBK_FIRMENNR = MDA_FIRMENNR AND LBK_LAGERBUCHKENNZ = MDA_LAGERBUCHKENNZ" "~"
if joinKuerzel = LBKMDA
searchResult := searchResult "LEFT JOIN WWS.MDA_MDE_ARTEN ON MDA_FIRMENNR = LBK_FIRMENNR AND MDA_LAGERBUCHKENNZ = LBK_LAGERBUCHKENNZ" "~"
if joinKuerzel = WKPLBK
searchResult := searchResult "LEFT JOIN WWS.LBK_LAGERBUCHKENNZ ON LBK_FIRMENNR = WKP_FIRMENNR AND LBK_LAGERBUCHKENNZ = WKP_LAGERBUCHKENNZ" "~"
if joinKuerzel = LBKWKP
searchResult := searchResult "LEFT JOIN WWS.WKP_WWSKONTENTABPOS ON WKP_FIRMENNR = LBK_FIRMENNR AND WKP_LAGERBUCHKENNZ = LBK_LAGERBUCHKENNZ" "~"
if joinKuerzel = LVELEI
searchResult := searchResult "LEFT JOIN LEI_LEISTUNG.LEI_LEISTUNG ON LEI_LEISTUNGLFDNR = LVE_LEISTUNGLFDNR" "~"
if joinKuerzel = LEILVE
searchResult := searchResult "LEFT JOIN PRODUKTION.LVE_LEISTUNGSVORERFASSUNG ON LVE_LEISTUNGLFDNR = LEI_LEISTUNGLFDNR" "~"
if joinKuerzel = REILEI
searchResult := searchResult "LEFT JOIN LEI_LEISTUNG.LEI_LEISTUNG ON LEI_LEISTUNGLFDNR = REI_LEISTUNGLFDNR" "~"
if joinKuerzel = LEIREI
searchResult := searchResult "LEFT JOIN HRS.REI_REISESTAMM ON REI_LEISTUNGLFDNR = LEI_LEISTUNGLFDNR" "~"
if joinKuerzel = ABDLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = ABD_DVFIRMENNR AND LFI_LOHNJAHR = ABD_JAHR" "~"
if joinKuerzel = LFIABD
searchResult := searchResult "LEFT JOIN HRS.ABD_ABRECHNUNGSDETAIL ON ABD_DVFIRMENNR = LFI_FIRMENNR AND ABD_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = DNVLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = DNV_FIRMENNR AND LFI_LOHNJAHR = DNV_LOHNJAHR" "~"
if joinKuerzel = LFIDNV
searchResult := searchResult "LEFT JOIN HRS.DNV_DNVORSCHLAG ON DNV_FIRMENNR = LFI_FIRMENNR AND DNV_LOHNJAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = KZEKZK
searchResult := searchResult "LEFT JOIN KORE.KZK_ZUSCHLAGKOPF ON KZK_FWK_FIRMENNR = KZE_FWK_FIRMENNR AND KZK_KORENR = KZE_KORENR AND KZK_ZUSCHLAGLFDNR = KZE_ZUSCHLAGLFDNR" "~"
if joinKuerzel = KZKKZE
searchResult := searchResult "LEFT JOIN KORE.KZE_ZUSCHLAGEINGABE ON KZE_FWK_FIRMENNR = KZK_FWK_FIRMENNR AND KZE_KORENR = KZK_KORENR AND KZE_ZUSCHLAGLFDNR = KZK_ZUSCHLAGLFDNR" "~"
if joinKuerzel = BVOLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = BVO_HAUPTFIRMENNR AND LAG_LAGERNR = BVO_LAGERNR" "~"
if joinKuerzel = LAGBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_HAUPTFIRMENNR = LAG_FIRMENNR AND BVO_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = KOFLAG
searchResult := searchResult "LEFT JOIN LAG_LAGER.LAG_LAGER ON LAG_FIRMENNR = KOF_LAG_FIRMENNR AND LAG_LAGERNR = KOF_LAGERNR" "~"
if joinKuerzel = LAGKOF
searchResult := searchResult "LEFT JOIN PRODUKTION.KOF_KOKFILTER ON KOF_LAG_FIRMENNR = LAG_FIRMENNR AND KOF_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = TRJLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = TRJ_FIRMENNR AND LAG_LAGERNR = TRJ_LAGERNR" "~"
if joinKuerzel = LAGTRJ
searchResult := searchResult "LEFT JOIN WWS.TRJ_TRANSPORTEINH_JOURNAL ON TRJ_FIRMENNR = LAG_FIRMENNR AND TRJ_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = WAFLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = WAF_ZIELLAGER_FIRMENNR AND LAG_LAGERNR = WAF_ZIELLAGER_LAGERNR" "~"
if joinKuerzel = LAGWAF
searchResult := searchResult "LEFT JOIN WWS.WAF_WWSADRZUFIRMENBEZ ON WAF_ZIELLAGER_FIRMENNR = LAG_FIRMENNR AND WAF_ZIELLAGER_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = AVELAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = AVE_ZIELLAGER_FIRMENNR AND LAG_LAGERNR = AVE_ZIELLAGER_LAGERNR" "~"
if joinKuerzel = LAGAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ZIELLAGER_FIRMENNR = LAG_FIRMENNR AND AVE_ZIELLAGER_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = LUKLAG
searchResult := searchResult "LEFT JOIN LAG_LAGER.LAG_LAGER ON LAG_FIRMENNR = LUK_DST_LAG_FIRMENNR AND LAG_LAGERNR = LUK_DST_LAGERNR" "~"
if joinKuerzel = LAGLUK
searchResult := searchResult "LEFT JOIN PRODUKTION.LUK_LAGER_UMS_KOPF ON LUK_DST_LAG_FIRMENNR = LAG_FIRMENNR AND LUK_DST_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = TRJLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = TRJ_FIRMENNR AND LAG_LAGERNR = TRJ_UMB_LAGERNR" "~"
if joinKuerzel = LAGTRJ
searchResult := searchResult "LEFT JOIN WWS.TRJ_TRANSPORTEINH_JOURNAL ON TRJ_FIRMENNR = LAG_FIRMENNR AND TRJ_UMB_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = LAJLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_FIRMENNR = LAJ_FIRMENNR AND LAJ_LAGERJOURNALLFDNR = LAJ_UMB_ABG_LAGERJOURNALLFDNR" "~"
if joinKuerzel = LAJLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_FIRMENNR = LAJ_FIRMENNR AND LAJ_UMB_ABG_LAGERJOURNALLFDNR = LAJ_LAGERJOURNALLFDNR" "~"
if joinKuerzel = LAJLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_FIRMENNR = LAJ_FIRMENNR AND LAJ_LAGERJOURNALLFDNR = LAJ_ORIG_LAGERJOURNALLFDNR" "~"
if joinKuerzel = LAJLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_FIRMENNR = LAJ_FIRMENNR AND LAJ_ORIG_LAGERJOURNALLFDNR = LAJ_LAGERJOURNALLFDNR" "~"
if joinKuerzel = AADLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = AAD_LANDNR" "~"
if joinKuerzel = LANAAD
searchResult := searchResult "LEFT JOIN BIL.AAD_AUSDRUCKDETAIL ON AAD_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = ADBLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = ADB_LANDNR" "~"
if joinKuerzel = LANADB
searchResult := searchResult "LEFT JOIN WWS.ADB_ABGABENDETAILBON ON ADB_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = BMMLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = BMM_LANDNR" "~"
if joinKuerzel = LANBMM
searchResult := searchResult "LEFT JOIN HRS.BMM_BUAK_MONMELDUNG ON BMM_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = BRALAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = BRA_LANDNR" "~"
if joinKuerzel = LANBRA
searchResult := searchResult "LEFT JOIN BUERO.BRA_BRANCHE ON BRA_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = FPKLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = FPK_LANDNR" "~"
if joinKuerzel = LANFPK
searchResult := searchResult "LEFT JOIN WWSINDIV.FPK_PROZENTSATZKONTEN ON FPK_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = ORTLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = ORT_LANDNR" "~"
if joinKuerzel = LANORT
searchResult := searchResult "LEFT JOIN BUERO.ORT_ORT ON ORT_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = STCLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = STC_LANDNR" "~"
if joinKuerzel = LANSTC
searchResult := searchResult "LEFT JOIN FIBU.STC_STEUERCODE ON STC_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = STELAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = STE_LANDNR" "~"
if joinKuerzel = LANSTE
searchResult := searchResult "LEFT JOIN BUERO.STE_STEUERERKLAERUNG ON STE_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = WIPLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = WIP_LANDNR" "~"
if joinKuerzel = LANWIP
searchResult := searchResult "LEFT JOIN BIL.WIP_WIPRUEFUNG ON WIP_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = WKPLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = WKP_LANDNR" "~"
if joinKuerzel = LANWKP
searchResult := searchResult "LEFT JOIN WWS.WKP_WWSKONTENTABPOS ON WKP_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = INTLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = INT_HANDELSPLANDNR" "~"
if joinKuerzel = LANINT
searchResult := searchResult "LEFT JOIN BUERO.INT_INTRASTAT ON INT_HANDELSPLANDNR = LAN_LANDNR" "~"
if joinKuerzel = INTLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = INT_URSPRUNGSLANDNR" "~"
if joinKuerzel = LANINT
searchResult := searchResult "LEFT JOIN BUERO.INT_INTRASTAT ON INT_URSPRUNGSLANDNR = LAN_LANDNR" "~"
if joinKuerzel = KOPLAP
searchResult := searchResult "LEFT JOIN LAP_LAGERPLATZ.LAP_LAGERPLATZ ON LAP_FIRMENNR = KOP_LAG_FIRMENNR AND LAP_LAGERNR = KOP_LAGERNR AND LAP_LAGERPLATZ = KOP_LAGERPLATZ" "~"
if joinKuerzel = LAPKOP
searchResult := searchResult "LEFT JOIN PRODUKTION.KOP_KOMMISSIONIERUNG_POS ON KOP_LAG_FIRMENNR = LAP_FIRMENNR AND KOP_LAGERNR = LAP_LAGERNR AND KOP_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = EROLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = ERO_LAG_FIRMENNR AND LAG_LAGERNR = ERO_LAG_LAGERNR" "~"
if joinKuerzel = LAGERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_LAG_FIRMENNR = LAG_FIRMENNR AND ERO_LAG_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = KAPLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = KAP_LAG_FIRMENNR AND LAG_LAGERNR = KAP_LAG_LAGERNR" "~"
if joinKuerzel = LAGKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_LAG_FIRMENNR = LAG_FIRMENNR AND KAP_LAG_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = KASLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = KAS_LAG_FIRMENNR AND LAG_LAGERNR = KAS_LAG_LAGERNR" "~"
if joinKuerzel = LAGKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_LAG_FIRMENNR = LAG_FIRMENNR AND KAS_LAG_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = LAJLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = LAJ_ARTIKELFIRMENNR AND LAG_LAGERNR = LAJ_LAGERNR" "~"
if joinKuerzel = LAGLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_ARTIKELFIRMENNR = LAG_FIRMENNR AND LAJ_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = WAPLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = WAP_LAG_FIRMENNR AND LAG_LAGERNR = WAP_LAGERNR" "~"
if joinKuerzel = LAGWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_LAG_FIRMENNR = LAG_FIRMENNR AND WAP_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = ARLLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = ARL_PRODABG_LAG_FIRMENNR AND LAG_LAGERNR = ARL_PRODABG_LAGERNR" "~"
if joinKuerzel = LAGARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_PRODABG_LAG_FIRMENNR = LAG_FIRMENNR AND ARL_PRODABG_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = WKGLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = WKG_ABGANGSLAGER_FIRMENNR AND LAG_LAGERNR = WKG_ABGANGSLAGER_LAGERNR" "~"
if joinKuerzel = LAGWKG
searchResult := searchResult "LEFT JOIN WWS.WKG_WWSKUNDENGRUPPE ON WKG_ABGANGSLAGER_FIRMENNR = LAG_FIRMENNR AND WKG_ABGANGSLAGER_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = AALLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = AAL_LANDNR" "~"
if joinKuerzel = LANAAL
searchResult := searchResult "LEFT JOIN BIL.AAL_AUSDRUCKLAENDER ON AAL_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = ADLLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = ADL_LANDNR" "~"
if joinKuerzel = LANADL
searchResult := searchResult "LEFT JOIN BUERO.ADL_ADLCODE ON ADL_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = ANDLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = AND_LANDNR" "~"
if joinKuerzel = LANAND
searchResult := searchResult "LEFT JOIN BIL.AND_ANBUNUTZUNGSDAUER ON AND_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = BLDLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = BLD_LANDNR" "~"
if joinKuerzel = LANBLD
searchResult := searchResult "LEFT JOIN BUERO.BLD_BUNDESLAND ON BLD_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = BPPLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = BPP_URSPR_LANDNR" "~"
if joinKuerzel = LANBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_URSPR_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = DLNLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = DLN_LANDNR" "~"
if joinKuerzel = LANDLN
searchResult := searchResult "LEFT JOIN BUERO.DLN_DIENSTLEISTUNGSNUMMER ON DLN_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = ERPLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = ERP_URSPR_LANDNR" "~"
if joinKuerzel = LANERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_URSPR_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = GEMLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = GEM_LANDNR" "~"
if joinKuerzel = LANGEM
searchResult := searchResult "LEFT JOIN BUERO.GEM_GEMEINDE ON GEM_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = GWKLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = GWK_LANDNR" "~"
if joinKuerzel = LANGWK
searchResult := searchResult "LEFT JOIN BUERO.GWK_GEWERKSCHAFT ON GWK_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = INTLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = INT_LANDNR" "~"
if joinKuerzel = LANINT
searchResult := searchResult "LEFT JOIN BUERO.INT_INTRASTAT ON INT_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = LAGLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = LAG_ADR_ORT_LANDNR" "~"
if joinKuerzel = LANLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_ADR_ORT_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = LATLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = LAT_URSPR_LANDNR" "~"
if joinKuerzel = LANLAT
searchResult := searchResult "LEFT JOIN WWS.LAT_LIEFERANTENARTIKEL ON LAT_URSPR_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = MITLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = MIT_LANDNR" "~"
if joinKuerzel = LANMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = PASLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = PAS_LANDNR" "~"
if joinKuerzel = LANPAS
searchResult := searchResult "LEFT JOIN WWS.PAS_PACKSTOFFPREIS ON PAS_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = PSWLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = PSW_BERUF_LANDNR" "~"
if joinKuerzel = LANPSW
searchResult := searchResult "LEFT JOIN BUERO.PSW_PERSONENWERTE ON PSW_BERUF_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = RFMLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = RFM_LANDNR" "~"
if joinKuerzel = LANRFM
searchResult := searchResult "LEFT JOIN BUERO.RFM_RECHTSFORM ON RFM_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = RNPLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = RNP_URSPR_LANDNR" "~"
if joinKuerzel = LANRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_URSPR_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = VWVLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = VWV_LANDNR" "~"
if joinKuerzel = LANVWV
searchResult := searchResult "LEFT JOIN FIBU.VWV_VSTERST_WERTEVORRAT ON VWV_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = WADLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = WAD_LANDNR" "~"
if joinKuerzel = LANWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = ADEKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = ADE_STEUERFIRMENNR AND KTO_FIBUNR = ADE_STEUERFIBUNR AND KTO_KONTONR = ADE_STEUERKONTONR" "~"
if joinKuerzel = KTOADE
searchResult := searchResult "LEFT JOIN WWS.ADE_ABGABENDETAILERFPOS ON ADE_STEUERFIRMENNR = KTO_FIRMENNR AND ADE_STEUERFIBUNR = KTO_FIBUNR AND ADE_STEUERKONTONR = KTO_KONTONR" "~"
if joinKuerzel = BUCKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = BUC_FIRMENNR AND KTO_FIBUNR = BUC_FIBUNR AND KTO_KONTONR = BUC_KONTONR" "~"
if joinKuerzel = KTOBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_FIRMENNR = KTO_FIRMENNR AND BUC_FIBUNR = KTO_FIBUNR AND BUC_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = KAUKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = KAU_FIRMENNR AND KTO_FIBUNR = KAU_FIBUNR AND KTO_KONTONR = KAU_ZUORD_KONTONR" "~"
if joinKuerzel = KTOKAU
searchResult := searchResult "LEFT JOIN BIL.KAU_KONTOAUFGLIEDERUNG ON KAU_FIRMENNR = KTO_FIRMENNR AND KAU_FIBUNR = KTO_FIBUNR AND KAU_ZUORD_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = KDIKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = KDI_FIRMENNR AND KTO_FIBUNR = KDI_FIBUNR AND KTO_KONTONR = KDI_KONTONR" "~"
if joinKuerzel = KTOKDI
searchResult := searchResult "LEFT JOIN KORE.KDI_ISTWERTE ON KDI_FIRMENNR = KTO_FIRMENNR AND KDI_FIBUNR = KTO_FIBUNR AND KDI_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = KKVKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = KKV_FIRMENNR AND KTO_FIBUNR = KKV_FIBUNR AND KTO_KONTONR = KKV_KONTONR" "~"
if joinKuerzel = KTOKKV
searchResult := searchResult "LEFT JOIN KORE.KKV_KSTKONTOVARIATOR ON KKV_FIRMENNR = KTO_FIRMENNR AND KKV_FIBUNR = KTO_FIBUNR AND KKV_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = KSEKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = KSE_FIRMENNR AND KTO_FIBUNR = KSE_BASISKONTO_FIBUNR AND KTO_KONTONR = KSE_BASISKONTONR" "~"
if joinKuerzel = KTOKSE
searchResult := searchResult "LEFT JOIN KORE.KSE_KORESCHLUESSELEINGABE ON KSE_FIRMENNR = KTO_FIRMENNR AND KSE_BASISKONTO_FIBUNR = KTO_FIBUNR AND KSE_BASISKONTONR = KTO_KONTONR" "~"
if joinKuerzel = MAVKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = MAV_FIRMENNR AND KTO_FIBUNR = MAV_FIBUNR AND KTO_KONTONR = MAV_VZINSEN_KONTONR" "~"
if joinKuerzel = KTOMAV
searchResult := searchResult "LEFT JOIN FIBU.MAV_MAHNVZINSEN ON MAV_FIRMENNR = KTO_FIRMENNR AND MAV_FIBUNR = KTO_FIBUNR AND MAV_VZINSEN_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = MSUKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = MSU_FIRMENNR AND KTO_FIBUNR = MSU_FIBUNR AND KTO_KONTONR = MSU_KONTONR" "~"
if joinKuerzel = KTOMSU
searchResult := searchResult "LEFT JOIN FIBU.MSU_MAHNSUMME ON MSU_FIRMENNR = KTO_FIRMENNR AND MSU_FIBUNR = KTO_FIBUNR AND MSU_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = PKBKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = PKB_FIBUFIRMENNR AND KTO_FIBUNR = PKB_FIBUNR AND KTO_KONTONR = PKB_KONTONR" "~"
if joinKuerzel = KTOPKB
searchResult := searchResult "LEFT JOIN BIL.PKB_PERSKORSTBUCHBELEGZU ON PKB_FIBUFIRMENNR = KTO_FIRMENNR AND PKB_FIBUNR = KTO_FIBUNR AND PKB_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = VEBKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = VEB_FIRMENNR AND KTO_FIBUNR = VEB_FIBUNR AND KTO_KONTONR = VEB_KONTONR" "~"
if joinKuerzel = KTOVEB
searchResult := searchResult "LEFT JOIN FIBU.VEB_VORERFASSBUCHUNG ON VEB_FIRMENNR = KTO_FIRMENNR AND VEB_FIBUNR = KTO_FIBUNR AND VEB_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = WSSKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = WSS_FIRMENNR AND KTO_FIBUNR = WSS_FIBUNR AND KTO_KONTONR = WSS_KONTONR" "~"
if joinKuerzel = KTOWSS
searchResult := searchResult "LEFT JOIN WWS.WSS_WWSSTATISTIK ON WSS_FIRMENNR = KTO_FIRMENNR AND WSS_FIBUNR = KTO_FIBUNR AND WSS_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = YBMKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = YBM_FIRMENNR AND KTO_FIBUNR = YBM_FIBUNR AND KTO_KONTONR = YBM_ABGR_ORIGKONTONR" "~"
if joinKuerzel = KTOYBM
searchResult := searchResult "LEFT JOIN FIBU.YBM_AUTOBUCHMODELLE ON YBM_FIRMENNR = KTO_FIRMENNR AND YBM_FIBUNR = KTO_FIBUNR AND YBM_ABGR_ORIGKONTONR = KTO_KONTONR" "~"
if joinKuerzel = ZBMKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = ZBM_FIRMENNR AND KTO_FIBUNR = ZBM_FIBUNR AND KTO_KONTONR = ZBM_KONTONR" "~"
if joinKuerzel = KTOZBM
searchResult := searchResult "LEFT JOIN FIBU.ZBM_ZINSBERECHNUNG_SUMME ON ZBM_FIRMENNR = KTO_FIRMENNR AND ZBM_FIBUNR = KTO_FIBUNR AND ZBM_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = MAHKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = MAH_FIRMENNR AND KTO_FIBUNR = MAH_FIBUNR AND KTO_KONTONR = MAH_GEGENVERRKONTO" "~"
if joinKuerzel = KTOMAH
searchResult := searchResult "LEFT JOIN FIBU.MAH_MAHNUNG ON MAH_FIRMENNR = KTO_FIRMENNR AND MAH_FIBUNR = KTO_FIBUNR AND MAH_GEGENVERRKONTO = KTO_KONTONR" "~"
if joinKuerzel = UDIKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = UDI_FIRMENNR AND KTO_FIBUNR = UDI_FIBUNR AND KTO_KONTONR = UDI_ORIGINALKONTONR" "~"
if joinKuerzel = KTOUDI
searchResult := searchResult "LEFT JOIN KORE.UDI_UMLAGEISTWERTE ON UDI_FIRMENNR = KTO_FIRMENNR AND UDI_FIBUNR = KTO_FIBUNR AND UDI_ORIGINALKONTONR = KTO_KONTONR" "~"
if joinKuerzel = FIWKUD
searchResult := searchResult "LEFT JOIN KUD_KURSDATEI.KUD_KURSDATEI ON KUD_KURSDATEINR = FIW_KURSTABELLENNR" "~"
if joinKuerzel = KUDFIW
searchResult := searchResult "LEFT JOIN BUERO.FIW_FIRMENWERTE ON FIW_KURSTABELLENNR = KUD_KURSDATEINR" "~"
if joinKuerzel = VKEKUK
searchResult := searchResult "LEFT JOIN WWS.KUK_KUNDENKARTEI ON KUK_KUKID = VKE_KUKID" "~"
if joinKuerzel = KUKVKE
searchResult := searchResult "LEFT JOIN WWS.VKE_VIEW_KUKSUM_ARTIKEL ON VKE_KUKID = KUK_KUKID" "~"
if joinKuerzel = KURKUV
searchResult := searchResult "LEFT JOIN KORE.KUV_UMLAGEVARIANTE ON KUV_FIRMENNR = KUR_FIRMENNR AND KUV_KORENR = KUR_KORENR AND KUV_VARIANTE = KUR_VARIANTE" "~"
if joinKuerzel = KUVKUR
searchResult := searchResult "LEFT JOIN KORE.KUR_UMLAGEREIHENFOLGE ON KUR_FIRMENNR = KUV_FIRMENNR AND KUR_KORENR = KUV_KORENR AND KUR_VARIANTE = KUV_VARIANTE" "~"
if joinKuerzel = ARLLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = ARL_VORGABE_FIL_FIRMENNR AND LAG_LAGERNR = ARL_VORGABE_LAGERNR" "~"
if joinKuerzel = LAGARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_VORGABE_FIL_FIRMENNR = LAG_FIRMENNR AND ARL_VORGABE_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = BPKLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = BPK_LAG_FIRMENNR AND LAG_LAGERNR = BPK_LAGERNR" "~"
if joinKuerzel = LAGBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_LAG_FIRMENNR = LAG_FIRMENNR AND BPK_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = VKPKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = VKP_FIRMENNR AND KTO_FIBUNR = VKP_FIBUNR AND KTO_KONTONR = VKP_KUERZUNG_KTO_KLASSE1" "~"
if joinKuerzel = KTOVKP
searchResult := searchResult "LEFT JOIN FIBU.VKP_VSTKPROJEKT ON VKP_FIRMENNR = KTO_FIRMENNR AND VKP_FIBUNR = KTO_FIBUNR AND VKP_KUERZUNG_KTO_KLASSE1 = KTO_KONTONR" "~"
if joinKuerzel = ZKKKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = ZKK_FIRMENNR AND KTO_FIBUNR = ZKK_FIBUNR AND KTO_KONTONR = ZKK_KONTONR" "~"
if joinKuerzel = KTOZKK
searchResult := searchResult "LEFT JOIN KORE.ZKK_KSTKONTOZU ON ZKK_FIRMENNR = KTO_FIRMENNR AND ZKK_FIBUNR = KTO_FIBUNR AND ZKK_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = ZVKKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = ZVK_FIRMENNR AND KTO_FIBUNR = ZVK_FIBUNR AND KTO_KONTONR = ZVK_GEGENVERRKONTONR" "~"
if joinKuerzel = KTOZVK
searchResult := searchResult "LEFT JOIN FIBU.ZVK_ZAHLUNGSVORSCHLAG ON ZVK_FIRMENNR = KTO_FIRMENNR AND ZVK_FIBUNR = KTO_FIBUNR AND ZVK_GEGENVERRKONTONR = KTO_KONTONR" "~"
if joinKuerzel = FWKKUD
searchResult := searchResult "LEFT JOIN KUD_KURSDATEI.KUD_KURSDATEI ON KUD_KURSDATEINR = FWK_KURSDATEINR" "~"
if joinKuerzel = KUDFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_KURSDATEINR = KUD_KURSDATEINR" "~"
if joinKuerzel = LANKUD
searchResult := searchResult "LEFT JOIN KUD_KURSDATEI.KUD_KURSDATEI ON KUD_KURSDATEINR = LAN_KURSTABELLENNR" "~"
if joinKuerzel = KUDLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_KURSTABELLENNR = KUD_KURSDATEINR" "~"
if joinKuerzel = AAEKUG
searchResult := searchResult "LEFT JOIN KUG_KUNDENGRUPPE.KUG_KUNDENGRUPPE ON KUG_FIRMENNR = AAE_KUG_FIRMENNR AND KUG_KUNDENHAUPTGRUPPENNR = AAE_KUG_HAUPTGRUPPENNR AND KUG_KUNDENUNTERGRUPPENNR = AAE_KUG_UNTERGRUPPENNR" "~"
if joinKuerzel = KUGAAE
searchResult := searchResult "LEFT JOIN WWS.AAE_ANGEBOTSAUSW_ERFQ_TMP ON AAE_KUG_FIRMENNR = KUG_FIRMENNR AND AAE_KUG_HAUPTGRUPPENNR = KUG_KUNDENHAUPTGRUPPENNR AND AAE_KUG_UNTERGRUPPENNR = KUG_KUNDENUNTERGRUPPENNR" "~"
if joinKuerzel = PIIKUG
searchResult := searchResult "LEFT JOIN KUG_KUNDENGRUPPE.KUG_KUNDENGRUPPE ON KUG_FIRMENNR = PII_KUG_FIRMENNR AND KUG_KUNDENHAUPTGRUPPENNR = PII_KUG_KUNDENHAUPTGRNR AND KUG_KUNDENUNTERGRUPPENNR = PII_KUG_KUNDENUNTERGRNR" "~"
if joinKuerzel = KUGPII
searchResult := searchResult "LEFT JOIN WWS.PII_PICKUNDENGRUPPE ON PII_KUG_FIRMENNR = KUG_FIRMENNR AND PII_KUG_KUNDENHAUPTGRNR = KUG_KUNDENHAUPTGRUPPENNR AND PII_KUG_KUNDENUNTERGRNR = KUG_KUNDENUNTERGRUPPENNR" "~"
if joinKuerzel = ERPL05
searchResult := searchResult "LEFT JOIN PRODUKTION.L05_V_ALK_ARBEITSPLAN_KOSTEN ON L05_ALK_AP_ID = ERP_AP_ID" "~"
if joinKuerzel = L05ERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_AP_ID = L05_ALK_AP_ID" "~"
if joinKuerzel = BPDLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = BPD_LAG_FIRMENNR AND LAG_LAGERNR = BPD_LAG_LAGERNR" "~"
if joinKuerzel = LAGBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_LAG_FIRMENNR = LAG_FIRMENNR AND BPD_LAG_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = LPOLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = LPO_LAG_FIRMENNR AND LAG_LAGERNR = LPO_LAGERNR" "~"
if joinKuerzel = LAGLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_LAG_FIRMENNR = LAG_FIRMENNR AND LPO_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = RPMLAG
searchResult := searchResult "LEFT JOIN LAG_LAGER.LAG_LAGER ON LAG_FIRMENNR = RPM_LAG_FIRMENNR AND LAG_LAGERNR = RPM_LAGERNR" "~"
if joinKuerzel = LAGRPM
searchResult := searchResult "LEFT JOIN PRODUKTION.RPM_RETRO_PRODUKT_MATERIAL ON RPM_LAG_FIRMENNR = LAG_FIRMENNR AND RPM_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = TRELAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = TRE_LAG_FIRMENNR AND LAG_LAGERNR = TRE_LAGERNR" "~"
if joinKuerzel = LAGTRE
searchResult := searchResult "LEFT JOIN WWS.TRE_TRANSPORTEINHEIT ON TRE_LAG_FIRMENNR = LAG_FIRMENNR AND TRE_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = WKGLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = WKG_ZIELLAGER_FIRMENNR AND LAG_LAGERNR = WKG_ZIELLAGER_LAGERNR" "~"
if joinKuerzel = LAGWKG
searchResult := searchResult "LEFT JOIN WWS.WKG_WWSKUNDENGRUPPE ON WKG_ZIELLAGER_FIRMENNR = LAG_FIRMENNR AND WKG_ZIELLAGER_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = BPDLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = BPD_QUELLLAGER_FIRMENNR AND LAG_LAGERNR = BPD_QUELLLAGER_LAGERNR" "~"
if joinKuerzel = LAGBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_QUELLLAGER_FIRMENNR = LAG_FIRMENNR AND BPD_QUELLLAGER_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = BPKLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = BPK_QUELLLAGER_FIRMENNR AND LAG_LAGERNR = BPK_QUELLLAGER_LAGERNR" "~"
if joinKuerzel = LAGBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_QUELLLAGER_FIRMENNR = LAG_FIRMENNR AND BPK_QUELLLAGER_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = LAJLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = LAJ_ARTIKELFIRMENNR AND LAG_LAGERNR = LAJ_UMB_LAGERNR" "~"
if joinKuerzel = LAGLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_ARTIKELFIRMENNR = LAG_FIRMENNR AND LAJ_UMB_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = LPOLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = LPO_ZIELLAGER_FIRMENNR AND LAG_LAGERNR = LPO_ZIELLAGER_LAGERNR" "~"
if joinKuerzel = LAGLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_ZIELLAGER_FIRMENNR = LAG_FIRMENNR AND LPO_ZIELLAGER_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = ADRLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = ADR_ORT_LANDNR" "~"
if joinKuerzel = LANADR
searchResult := searchResult "LEFT JOIN BUERO.ADR_ADRESSE ON ADR_ORT_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = AGALAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = AGA_LANDNR" "~"
if joinKuerzel = LANAGA
searchResult := searchResult "LEFT JOIN BUERO.AGA_ABGABENART ON AGA_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = AWSLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = AWS_LAN_LANDNR" "~"
if joinKuerzel = LANAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_LAN_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = INSLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = INS_LANDNR" "~"
if joinKuerzel = LANINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = FMJKSU
searchResult := searchResult "LEFT JOIN WWS.KSU_KASSENSUMMENKOPF ON KSU_KASSENSUMMENKOPFID = FMJ_KSU_KASSENSUMMENKOPFID" "~"
if joinKuerzel = KSUFMJ
searchResult := searchResult "LEFT JOIN WWS.FMJ_FISKALMODULJOURNAL ON FMJ_KSU_KASSENSUMMENKOPFID = KSU_KASSENSUMMENKOPFID" "~"
if joinKuerzel = ADCKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = ADC_STEUERFIRMENNR AND KTO_FIBUNR = ADC_STEUERFIBUNR AND KTO_KONTONR = ADC_STEUERKONTONR" "~"
if joinKuerzel = KTOADC
searchResult := searchResult "LEFT JOIN WWS.ADC_ABGABENDETAILRECH ON ADC_STEUERFIRMENNR = KTO_FIRMENNR AND ADC_STEUERFIBUNR = KTO_FIBUNR AND ADC_STEUERKONTONR = KTO_KONTONR" "~"
if joinKuerzel = KDPKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = KDP_FIRMENNR AND KTO_FIBUNR = KDP_FIBUNR AND KTO_KONTONR = KDP_KONTONR" "~"
if joinKuerzel = KTOKDP
searchResult := searchResult "LEFT JOIN KORE.KDP_PLANWERTE ON KDP_FIRMENNR = KTO_FIRMENNR AND KDP_FIBUNR = KTO_FIBUNR AND KDP_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = KSZKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = KSZ_FIRMENNR AND KTO_FIBUNR = KSZ_FIBUNR AND KTO_KONTONR = KSZ_KONTONR" "~"
if joinKuerzel = KTOKSZ
searchResult := searchResult "LEFT JOIN HRS.KSZ_BBKALKSZ ON KSZ_FIRMENNR = KTO_FIRMENNR AND KSZ_FIBUNR = KTO_FIBUNR AND KSZ_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = MAHKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = MAH_FIRMENNR AND KTO_FIBUNR = MAH_FIBUNR AND KTO_KONTONR = MAH_KONTONR" "~"
if joinKuerzel = KTOMAH
searchResult := searchResult "LEFT JOIN FIBU.MAH_MAHNUNG ON MAH_FIRMENNR = KTO_FIRMENNR AND MAH_FIBUNR = KTO_FIBUNR AND MAH_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = VKPKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = VKP_FIRMENNR AND KTO_FIBUNR = VKP_FIBUNR AND KTO_KONTONR = VKP_KUERZUNG_KTO_KLASSE5" "~"
if joinKuerzel = KTOVKP
searchResult := searchResult "LEFT JOIN FIBU.VKP_VSTKPROJEKT ON VKP_FIRMENNR = KTO_FIRMENNR AND VKP_FIBUNR = KTO_FIBUNR AND VKP_KUERZUNG_KTO_KLASSE5 = KTO_KONTONR" "~"
if joinKuerzel = VKPKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = VKP_FIRMENNR AND KTO_FIBUNR = VKP_FIBUNR AND KTO_KONTONR = VKP_KUERZUNG_KTO_KLASSE6" "~"
if joinKuerzel = KTOVKP
searchResult := searchResult "LEFT JOIN FIBU.VKP_VSTKPROJEKT ON VKP_FIRMENNR = KTO_FIRMENNR AND VKP_FIBUNR = KTO_FIBUNR AND VKP_KUERZUNG_KTO_KLASSE6 = KTO_KONTONR" "~"
if joinKuerzel = BUCKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = BUC_FIRMENNR AND KTO_FIBUNR = BUC_FIBUNR AND KTO_KONTONR = BUC_KTO_GEGENKONTONR" "~"
if joinKuerzel = KTOBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_FIRMENNR = KTO_FIRMENNR AND BUC_FIBUNR = KTO_FIBUNR AND BUC_KTO_GEGENKONTONR = KTO_KONTONR" "~"
if joinKuerzel = KLIKUG
searchResult := searchResult "LEFT JOIN BUERO.KUG_KUNDENGRUPPE ON KUG_FIRMENNR = KLI_KUG_FIRMENNR AND KUG_KUNDENHAUPTGRUPPENNR = KLI_KUNDENHAUPTGRUPPENNR AND KUG_KUNDENUNTERGRUPPENNR = KLI_KUNDENUNTERGRUPPENNR" "~"
if joinKuerzel = KUGKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_KUG_FIRMENNR = KUG_FIRMENNR AND KLI_KUNDENHAUPTGRUPPENNR = KUG_KUNDENHAUPTGRUPPENNR AND KLI_KUNDENUNTERGRUPPENNR = KUG_KUNDENUNTERGRUPPENNR" "~"
if joinKuerzel = PVZKUG
searchResult := searchResult "LEFT JOIN KUG_KUNDENGRUPPE.KUG_KUNDENGRUPPE ON KUG_FIRMENNR = PVZ_KUG_FIRMENNR AND KUG_KUNDENHAUPTGRUPPENNR = PVZ_KUG_KUNDENHAUPTGRNR AND KUG_KUNDENUNTERGRUPPENNR = PVZ_KUG_KUNDENUNTERGRNR" "~"
if joinKuerzel = KUGPVZ
searchResult := searchResult "LEFT JOIN WWS.PVZ_PROVISIONTABZU ON PVZ_KUG_FIRMENNR = KUG_FIRMENNR AND PVZ_KUG_KUNDENHAUPTGRNR = KUG_KUNDENHAUPTGRUPPENNR AND PVZ_KUG_KUNDENUNTERGRNR = KUG_KUNDENUNTERGRUPPENNR" "~"
if joinKuerzel = UDPKUV
searchResult := searchResult "LEFT JOIN KORE.KUV_UMLAGEVARIANTE ON KUV_FIRMENNR = UDP_FIRMENNR AND KUV_KORENR = UDP_KORENR AND KUV_VARIANTE = UDP_VARIANTE" "~"
if joinKuerzel = KUVUDP
searchResult := searchResult "LEFT JOIN KORE.UDP_UMLAGEPLANWERTE ON UDP_FIRMENNR = KUV_FIRMENNR AND UDP_KORENR = KUV_KORENR AND UDP_VARIANTE = KUV_VARIANTE" "~"
if joinKuerzel = BOPLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = BOP_LAG_FIRMENNR AND LAG_LAGERNR = BOP_LAG_LAGERNR" "~"
if joinKuerzel = LAGBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_LAG_FIRMENNR = LAG_FIRMENNR AND BOP_LAG_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = DISLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = DIS_LAG_FIRMENNR AND LAG_LAGERNR = DIS_LAGERNR" "~"
if joinKuerzel = LAGDIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_LAG_FIRMENNR = LAG_FIRMENNR AND DIS_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = EPSLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = EPS_LAG_FIRMENNR AND LAG_LAGERNR = EPS_LAGERNR" "~"
if joinKuerzel = LAGEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_LAG_FIRMENNR = LAG_FIRMENNR AND EPS_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = IVKLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = IVK_FIRMENNR AND LAG_LAGERNR = IVK_LAGERNR" "~"
if joinKuerzel = LAGIVK
searchResult := searchResult "LEFT JOIN WWS.IVK_INVENTURKOPF ON IVK_FIRMENNR = LAG_FIRMENNR AND IVK_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = LMBLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = LMB_FIRMENNR AND LAG_LAGERNR = LMB_LAGERNR" "~"
if joinKuerzel = LAGLMB
searchResult := searchResult "LEFT JOIN WWS.LMB_LAGERMINMAXBESTAND ON LMB_FIRMENNR = LAG_FIRMENNR AND LMB_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = PCPLAG
searchResult := searchResult "LEFT JOIN LAG_LAGER.LAG_LAGER ON LAG_FIRMENNR = PCP_LAG_FIRMENNR AND LAG_LAGERNR = PCP_LAGERNR" "~"
if joinKuerzel = LAGPCP
searchResult := searchResult "LEFT JOIN PRODUKTION.PCP_PPS_PRODUKT ON PCP_LAG_FIRMENNR = LAG_FIRMENNR AND PCP_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = WEJLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = WEJ_LAG_FIRMENNR AND LAG_LAGERNR = WEJ_LAGERNR" "~"
if joinKuerzel = LAGWEJ
searchResult := searchResult "LEFT JOIN WWS.WEJ_WARENEINSATZLAJ ON WEJ_LAG_FIRMENNR = LAG_FIRMENNR AND WEJ_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = WELLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = WEL_LAG_FIRMENNR AND LAG_LAGERNR = WEL_LAGERNR" "~"
if joinKuerzel = LAGWEL
searchResult := searchResult "LEFT JOIN WWS.WEL_WARENEINSATZLAGERZU ON WEL_LAG_FIRMENNR = LAG_FIRMENNR AND WEL_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = AAKLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = AAK_LANDNR" "~"
if joinKuerzel = LANAAK
searchResult := searchResult "LEFT JOIN WWS.AAK_ARAKOPF ON AAK_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = KONKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_KATEGORIEKZ = KON_KATEGORIEKZ AND KAT_KATEGORIELFDNR = KON_KATEGORIELFDNR" "~"
if joinKuerzel = KATKON
searchResult := searchResult "LEFT JOIN BUERO.KON_KONTAKTPERSON ON KON_KATEGORIEKZ = KAT_KATEGORIEKZ AND KON_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = MEVKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_KATEGORIEKZ = MEV_KATEGORIEKZ AND KAT_KATEGORIELFDNR = MEV_KATEGORIELFDNR" "~"
if joinKuerzel = KATMEV
searchResult := searchResult "LEFT JOIN BUERO.MEV_MEDIENVERLEIH ON MEV_KATEGORIEKZ = KAT_KATEGORIEKZ AND MEV_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = PERKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_KATEGORIEKZ = PER_GEBIET_KATEGORIEKZ AND KAT_KATEGORIELFDNR = PER_GEBIET_KATEGORIELFDNR" "~"
if joinKuerzel = KATPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_GEBIET_KATEGORIEKZ = KAT_KATEGORIEKZ AND PER_GEBIET_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = WFNKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_KATEGORIEKZ = WFN_HAUPTKATEGORIEKZ AND KAT_KATEGORIELFDNR = WFN_HAUPTKATEGORIELFDNR" "~"
if joinKuerzel = KATWFN
searchResult := searchResult "LEFT JOIN BUERO.WFN_WORKFLOWACTION ON WFN_HAUPTKATEGORIEKZ = KAT_KATEGORIEKZ AND WFN_HAUPTKATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = ZBAKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_KATEGORIEKZ = ZBA_KATEGORIEKZ AND KAT_KATEGORIELFDNR = ZBA_KATEGORIELFDNR" "~"
if joinKuerzel = KATZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_KATEGORIEKZ = KAT_KATEGORIEKZ AND ZBA_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = KFFKFI
searchResult := searchResult "LEFT JOIN BUERO.KFI_KORETYPENFINDUNG ON KFI_KORETYPENFINDUNGID = KFF_KORETYPENFINDUNGID" "~"
if joinKuerzel = KFIKFF
searchResult := searchResult "LEFT JOIN BUERO.KFF_KORETYPFIND_FELDERZU ON KFF_KORETYPENFINDUNGID = KFI_KORETYPENFINDUNGID" "~"
if joinKuerzel = KEPKGD
searchResult := searchResult "LEFT JOIN WWS.KGD_KASSENGELDLADE ON KGD_GELDLADENID = KEP_KGD_GELDLADENID" "~"
if joinKuerzel = KGDKEP
searchResult := searchResult "LEFT JOIN WWS.KEP_KASSENERFASSUNGSPROT ON KEP_KGD_GELDLADENID = KGD_GELDLADENID" "~"
if joinKuerzel = APPKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = APP_FIRMENNR AND KLI_KLID = APP_KLID" "~"
if joinKuerzel = KLIAPP
searchResult := searchResult "LEFT JOIN BMDINTERN.APP_ASPPWD ON APP_FIRMENNR = KLI_FIRMENNR AND APP_KLID = KLI_KLID" "~"
if joinKuerzel = BSPKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = BSP_KUNDE_FIRMENNR AND KLI_KLID = BSP_KUNDE_KLID" "~"
if joinKuerzel = KLIBSP
searchResult := searchResult "LEFT JOIN WWS.BSP_BONUSERMPOS ON BSP_KUNDE_FIRMENNR = KLI_FIRMENNR AND BSP_KUNDE_KLID = KLI_KLID" "~"
if joinKuerzel = EFKKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = EFK_AUFTRAGGEBER_FIRMENNR AND KLI_KLID = EFK_AUFTRAGGEBER_KLID" "~"
if joinKuerzel = KLIEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_AUFTRAGGEBER_FIRMENNR = KLI_FIRMENNR AND EFK_AUFTRAGGEBER_KLID = KLI_KLID" "~"
if joinKuerzel = EPSKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = EPS_LIEFERANT_FIRMENNR_FF AND KLI_KLID = EPS_LIEFERANT_ID_FF" "~"
if joinKuerzel = KLIEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_LIEFERANT_FIRMENNR_FF = KLI_FIRMENNR AND EPS_LIEFERANT_ID_FF = KLI_KLID" "~"
if joinKuerzel = GSFKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = GSF_KUNDE_FIRMENNR AND KLI_KLID = GSF_KUNDE_KLID" "~"
if joinKuerzel = KLIGSF
searchResult := searchResult "LEFT JOIN WWS.GSF_GUTSCHEINFILIALDATEN ON GSF_KUNDE_FIRMENNR = KLI_FIRMENNR AND GSF_KUNDE_KLID = KLI_KLID" "~"
if joinKuerzel = KAEKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = KAE_KUNDEN_FIRMENNR AND KLI_KLID = KAE_KUNDEN_KLID" "~"
if joinKuerzel = KLIKAE
searchResult := searchResult "LEFT JOIN WWS.KAE_KASSENEINAUSGABEN ON KAE_KUNDEN_FIRMENNR = KLI_FIRMENNR AND KAE_KUNDEN_KLID = KLI_KLID" "~"
if joinKuerzel = LAAKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = LAA_BEST_LIEF_FIRMENNR AND KLI_KLID = LAA_BEST_LIEF_KLID" "~"
if joinKuerzel = KLILAA
searchResult := searchResult "LEFT JOIN WWS.LAA_LAGERART ON LAA_BEST_LIEF_FIRMENNR = KLI_FIRMENNR AND LAA_BEST_LIEF_KLID = KLI_KLID" "~"
if joinKuerzel = NUBKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = NUB_KLI_FIRMENNR AND KLI_KLID = NUB_KLID" "~"
if joinKuerzel = KLINUB
searchResult := searchResult "LEFT JOIN BMD.NUB_NTCSUPDBEZIEHER ON NUB_KLI_FIRMENNR = KLI_FIRMENNR AND NUB_KLID = KLI_KLID" "~"
if joinKuerzel = PCKKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = PCK_RE_FIRMENNR AND KLI_KLID = PCK_RE_RECHEMPF_KLID" "~"
if joinKuerzel = KLIPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_RE_FIRMENNR = KLI_FIRMENNR AND PCK_RE_RECHEMPF_KLID = KLI_KLID" "~"
if joinKuerzel = PILKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = PIL_KLI_FIRMENNR AND KLI_KLID = PIL_KLID" "~"
if joinKuerzel = KLIPIL
searchResult := searchResult "LEFT JOIN WWS.PIL_PICKUNDE ON PIL_KLI_FIRMENNR = KLI_FIRMENNR AND PIL_KLID = KLI_KLID" "~"
if joinKuerzel = PWZKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = PWZ_FIRMENNR AND KLI_KLID = PWZ_KLID" "~"
if joinKuerzel = KLIPWZ
searchResult := searchResult "LEFT JOIN WWS.PWZ_PWKKUNDELIEFERANTZU ON PWZ_FIRMENNR = KLI_FIRMENNR AND PWZ_KLID = KLI_KLID" "~"
if joinKuerzel = TCAKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = TCA_KLI_FIRMENNR AND KLI_KLID = TCA_KLID" "~"
if joinKuerzel = KLITCA
searchResult := searchResult "LEFT JOIN BMD.TCA_TCACTIONLIST ON TCA_KLI_FIRMENNR = KLI_FIRMENNR AND TCA_KLID = KLI_KLID" "~"
if joinKuerzel = VLJKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = VLJ_AUFTRAGGEBER_FIRMENNR AND KLI_KLID = VLJ_AUFTRAGGEBER_KLID" "~"
if joinKuerzel = KLIVLJ
searchResult := searchResult "LEFT JOIN WWS.VLJ_VERLEIHJOURNAL ON VLJ_AUFTRAGGEBER_FIRMENNR = KLI_FIRMENNR AND VLJ_AUFTRAGGEBER_KLID = KLI_KLID" "~"
if joinKuerzel = EDKLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = EDK_DVH_FIRMENNR AND LFI_LOHNJAHR = EDK_LOHNJAHR" "~"
if joinKuerzel = LFIEDK
searchResult := searchResult "LEFT JOIN HRS.EDK_ERWDIVKOERPER ON EDK_DVH_FIRMENNR = LFI_FIRMENNR AND EDK_LOHNJAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = HAKLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = HAK_DVH_FIRMENNR AND LFI_LOHNJAHR = HAK_LOHNJAHR" "~"
if joinKuerzel = LFIHAK
searchResult := searchResult "LEFT JOIN HRS.HAK_AKONTO ON HAK_DVH_FIRMENNR = LFI_FIRMENNR AND HAK_LOHNJAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = KMDLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = KMD_FIRMENNR AND LFI_LOHNJAHR = KMD_JAHR" "~"
if joinKuerzel = LFIKMD
searchResult := searchResult "LEFT JOIN HRS.KMD_KOMMSTDETAIL ON KMD_FIRMENNR = LFI_FIRMENNR AND KMD_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = LFILFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = LFI_DATENSENDER_FIRMENNR AND LFI_LOHNJAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = LFILFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_DATENSENDER_FIRMENNR = LFI_FIRMENNR AND LFI_LOHNJAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = SNVLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = SNV_FIRMENNR AND LFI_LOHNJAHR = SNV_LOHNJAHR" "~"
if joinKuerzel = LFISNV
searchResult := searchResult "LEFT JOIN HRS.SNV_SCHNITTVORSCHLAG ON SNV_FIRMENNR = LFI_FIRMENNR AND SNV_LOHNJAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = EGMLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = EGM_DVFIRMENNR AND LFI_LOHNJAHR = EGM_JAHR" "~"
if joinKuerzel = LFIEGM
searchResult := searchResult "LEFT JOIN HRS.EGM_ELDADETAIL_GM ON EGM_DVFIRMENNR = LFI_FIRMENNR AND EGM_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = LAJLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_FIRMENNR = LAJ_BUCHUNGS_FIRMENNR AND LFS_LIEFERSCHEINLFDNR = LAJ_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = LFSLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_BUCHUNGS_FIRMENNR = LFS_FIRMENNR AND LAJ_LIEFERSCHEINLFDNR = LFS_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = TRJLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_FIRMENNR = TRJ_BUCHUNGS_FIRMENNR AND LFS_LIEFERSCHEINLFDNR = TRJ_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = LFSTRJ
searchResult := searchResult "LEFT JOIN WWS.TRJ_TRANSPORTEINH_JOURNAL ON TRJ_BUCHUNGS_FIRMENNR = LFS_FIRMENNR AND TRJ_LIEFERSCHEINLFDNR = LFS_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = KOWLGR
searchResult := searchResult "LEFT JOIN HRS.LGR_LOHNARTGRUPPE ON LGR_LGRID = KOW_LGRID_LG AND LGR_JAHR = KOW_LGR_JAHR AND LGR_DBSNR = KOW_DATENBESTANDNR" "~"
if joinKuerzel = LGRKOW
searchResult := searchResult "LEFT JOIN HRS.KOW_KOLLEKTIVVERTRAGWERTE ON KOW_LGRID_LG = LGR_LGRID AND KOW_LGR_JAHR = LGR_JAHR AND KOW_DATENBESTANDNR = LGR_DBSNR" "~"
if joinKuerzel = WADLIB
searchResult := searchResult "LEFT JOIN WWS.LIB_LIEFERBEDINGUNG ON LIB_LIEFERBNR = WAD_LIEFERBNR" "~"
if joinKuerzel = LIBWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_LIEFERBNR = LIB_LIEFERBNR" "~"
if joinKuerzel = ALTLOA
searchResult := searchResult "LEFT JOIN LOA_LOHNART.LOA_LOHNART ON LOA_LOHNARTID = ALT_LOHNARTID" "~"
if joinKuerzel = LOAALT
searchResult := searchResult "LEFT JOIN ZLE.ALT_ARCHIVLEISTUNGEN ON ALT_LOHNARTID = LOA_LOHNARTID" "~"
if joinKuerzel = AAPLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_FIRMENNR = AAP_LFS_FIRMENNR AND LPO_LIEFERSCHEINLFDNR = AAP_LIEFERSCHEINLFDNR AND LPO_LIEFERSCHEINPOSLFDNR = AAP_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = LPOAAP
searchResult := searchResult "LEFT JOIN WWS.AAP_ARAPOSITION ON AAP_LFS_FIRMENNR = LPO_FIRMENNR AND AAP_LIEFERSCHEINLFDNR = LPO_LIEFERSCHEINLFDNR AND AAP_LIEFERSCHEINPOSLFDNR = LPO_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = LPOLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_FIRMENNR = LPO_FIRMENNR AND LPO_LIEFERSCHEINLFDNR = LPO_LIEFERSCHEINLFDNR AND LPO_LIEFERSCHEINPOSLFDNR = LPO_H_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = LPOLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_FIRMENNR = LPO_FIRMENNR AND LPO_LIEFERSCHEINLFDNR = LPO_LIEFERSCHEINLFDNR AND LPO_H_LIEFERSCHEINPOSLFDNR = LPO_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = RNPLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_FIRMENNR = RNP_FIRMENNR AND LPO_LIEFERSCHEINLFDNR = RNP_LIEFERSCHEINLFDNR AND LPO_LIEFERSCHEINPOSLFDNR = RNP_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = LPORNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = LPO_FIRMENNR AND RNP_LIEFERSCHEINLFDNR = LPO_LIEFERSCHEINLFDNR AND RNP_LIEFERSCHEINPOSLFDNR = LPO_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = WPZLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_FIRMENNR = WPZ_LPO_FIRMENNR AND LPO_LIEFERSCHEINLFDNR = WPZ_LPO_LIEFERSCHEINLFDNR AND LPO_LIEFERSCHEINPOSLFDNR = WPZ_LPO_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = LPOWPZ
searchResult := searchResult "LEFT JOIN WWS.WPZ_WESLPO_ZUORDNUNG ON WPZ_LPO_FIRMENNR = LPO_FIRMENNR AND WPZ_LPO_LIEFERSCHEINLFDNR = LPO_LIEFERSCHEINLFDNR AND WPZ_LPO_LIEFERSCHEINPOSLFDNR = LPO_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = WAGLSG
searchResult := searchResult "LEFT JOIN WWS.LSG_LIEFERSCHEINGRUPPEN ON LSG_FIRMENNR = WAG_LSG_FIRMENNR AND LSG_LFSHAUPTGRUPPE = WAG_LSG_LFSHAUPTGRUPPE AND LSG_LFSUNTERGRUPPE = WAG_LSG_LFSUNTERGRUPPE" "~"
if joinKuerzel = LSGWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_LSG_FIRMENNR = LSG_FIRMENNR AND WAG_LSG_LFSHAUPTGRUPPE = LSG_LFSHAUPTGRUPPE AND WAG_LSG_LFSUNTERGRUPPE = LSG_LFSUNTERGRUPPE" "~"
if joinKuerzel = NOTLTX
searchResult := searchResult "LEFT JOIN BUERO.LTX_LANGTEXT ON LTX_LANGTXTLFDNR = NOT_LANGTXTLFDNR" "~"
if joinKuerzel = LTXNOT
searchResult := searchResult "LEFT JOIN BUERO.NOT_NOTIZ ON NOT_LANGTXTLFDNR = LTX_LANGTXTLFDNR" "~"
if joinKuerzel = EVTLTX
searchResult := searchResult "LEFT JOIN BUERO.LTX_LANGTEXT ON LTX_LANGTXTLFDNR = EVT_TEI_LANGTXTLFDNR" "~"
if joinKuerzel = LTXEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_TEI_LANGTXTLFDNR = LTX_LANGTXTLFDNR" "~"
if joinKuerzel = MWTMBZ
searchResult := searchResult "LEFT JOIN BUERO.MBZ_MEDIENBETRIEBSZEIT ON MBZ_BETRIEBSZEITID = MWT_BETRIEBSZEITID" "~"
if joinKuerzel = MBZMWT
searchResult := searchResult "LEFT JOIN BUERO.MWT_MEDIENWERTE ON MWT_BETRIEBSZEITID = MBZ_BETRIEBSZEITID" "~"
if joinKuerzel = MITMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_GEHOERT_FIRMENNR = MIT_ZIMMER_FIRMENNR AND MED_MEDIENKZ = MIT_ZIMMER_MEDIENKZ AND MED_MEDIENLFDNR = MIT_ZIMMER_MEDIENLFDNR" "~"
if joinKuerzel = MEDMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_ZIMMER_FIRMENNR = MED_GEHOERT_FIRMENNR AND MIT_ZIMMER_MEDIENKZ = MED_MEDIENKZ AND MIT_ZIMMER_MEDIENLFDNR = MED_MEDIENLFDNR" "~"
if joinKuerzel = LADLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = LAD_ARL_FIRMENNR AND LAP_LAGERNR = LAD_LAGERNR AND LAP_LAGERPLATZ = LAD_VORGABE_LAGERPLATZ" "~"
if joinKuerzel = LAPLAD
searchResult := searchResult "LEFT JOIN WWS.LAD_LAGERARTIKELDATEN ON LAD_ARL_FIRMENNR = LAP_FIRMENNR AND LAD_LAGERNR = LAP_LAGERNR AND LAD_VORGABE_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = LUALAP
searchResult := searchResult "LEFT JOIN LAP_LAGERPLATZ.LAP_LAGERPLATZ ON LAP_FIRMENNR = LUA_LAG_FIRMENNR AND LAP_LAGERNR = LUA_LAGERNR AND LAP_LAGERPLATZ = LUA_LAGERPLATZ" "~"
if joinKuerzel = LAPLUA
searchResult := searchResult "LEFT JOIN PRODUKTION.LUA_LAGER_UMS_ANGEFORDERT ON LUA_LAG_FIRMENNR = LAP_FIRMENNR AND LUA_LAGERNR = LAP_LAGERNR AND LUA_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = WAPLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = WAP_LAG_FIRMENNR AND LAP_LAGERNR = WAP_LAGERNR AND LAP_LAGERPLATZ = WAP_LAGERPLATZ" "~"
if joinKuerzel = LAPWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_LAG_FIRMENNR = LAP_FIRMENNR AND WAP_LAGERNR = LAP_LAGERNR AND WAP_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = WKGLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = WKG_ZIELLAGER_FIRMENNR AND LAP_LAGERNR = WKG_ZIELLAGER_LAGERNR AND LAP_LAGERPLATZ = WKG_ZIELLAGER_LAGERPLATZ" "~"
if joinKuerzel = LAPWKG
searchResult := searchResult "LEFT JOIN WWS.WKG_WWSKUNDENGRUPPE ON WKG_ZIELLAGER_FIRMENNR = LAP_FIRMENNR AND WKG_ZIELLAGER_LAGERNR = LAP_LAGERNR AND WKG_ZIELLAGER_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = KOPLAP
searchResult := searchResult "LEFT JOIN LAP_LAGERPLATZ.LAP_LAGERPLATZ ON LAP_FIRMENNR = KOP_ZIEL_LAG_FIRMENNR AND LAP_LAGERNR = KOP_ZIEL_LAGERNR AND LAP_LAGERPLATZ = KOP_ZIEL_LAGERPLATZ" "~"
if joinKuerzel = LAPKOP
searchResult := searchResult "LEFT JOIN PRODUKTION.KOP_KOMMISSIONIERUNG_POS ON KOP_ZIEL_LAG_FIRMENNR = LAP_FIRMENNR AND KOP_ZIEL_LAGERNR = LAP_LAGERNR AND KOP_ZIEL_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = WAPLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = WAP_QUELLLAGER_FIRMENNR AND LAP_LAGERNR = WAP_QUELLLAGER_LAGERNR AND LAP_LAGERPLATZ = WAP_QUELLLAGER_LAGERPLATZ" "~"
if joinKuerzel = LAPWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_QUELLLAGER_FIRMENNR = LAP_FIRMENNR AND WAP_QUELLLAGER_LAGERNR = LAP_LAGERNR AND WAP_QUELLLAGER_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = BRELAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = BRE_LOHNARTID" "~"
if joinKuerzel = LAQBRE
searchResult := searchResult "LEFT JOIN HRS.BRE_BRUTTOERFASSUNG ON BRE_LOHNARTID = LAQ_LOHNARTID" "~"
if joinKuerzel = BRPLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = BRP_LOHNARTID" "~"
if joinKuerzel = LAQBRP
searchResult := searchResult "LEFT JOIN HRS.BRP_BRUTTOERFPROTOKOLL ON BRP_LOHNARTID = LAQ_LOHNARTID" "~"
if joinKuerzel = PTPLAQ
searchResult := searchResult "LEFT JOIN LAQ_LOHNART_SEQ.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = PTP_LOHNARTID" "~"
if joinKuerzel = LAQPTP
searchResult := searchResult "LEFT JOIN WWS.PTP_PREISTABPOS ON PTP_LOHNARTID = LAQ_LOHNARTID" "~"
if joinKuerzel = SBALAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = SBA_LOHNARTID" "~"
if joinKuerzel = LAQSBA
searchResult := searchResult "LEFT JOIN HRS.SBA_SCHNITTBASIS ON SBA_LOHNARTID = LAQ_LOHNARTID" "~"
if joinKuerzel = BFILAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = BFI_SONDERZAHLUNG3_LOHNART" "~"
if joinKuerzel = LAQBFI
searchResult := searchResult "LEFT JOIN HRS.BFI_BUDGETFIRMA ON BFI_SONDERZAHLUNG3_LOHNART = LAQ_LOHNARTID" "~"
if joinKuerzel = DNZLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = DNZ_LOHNART_AUSBEZAHLT" "~"
if joinKuerzel = LAQDNZ
searchResult := searchResult "LEFT JOIN HRS.DNZ_DIVNICHTLEISTUNGSZEIT ON DNZ_LOHNART_AUSBEZAHLT = LAQ_LOHNARTID" "~"
if joinKuerzel = LAZLAT
searchResult := searchResult "LEFT JOIN WWS.LAT_LIEFERANTENARTIKEL ON LAT_LIEFERANTENARTIKELID = LAZ_LIEFERANTENARTIKELID" "~"
if joinKuerzel = LATLAZ
searchResult := searchResult "LEFT JOIN WWS.LAZ_LIEFARTIKEL_ZUSATZ ON LAZ_LIEFERANTENARTIKELID = LAT_LIEFERANTENARTIKELID" "~"
if joinKuerzel = AVELEI
searchResult := searchResult "LEFT JOIN LEI_LEISTUNG.LEI_LEISTUNG ON LEI_LEISTUNGLFDNR = AVE_LEI_LEISTUNGLFDNR" "~"
if joinKuerzel = LEIAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_LEI_LEISTUNGLFDNR = LEI_LEISTUNGLFDNR" "~"
if joinKuerzel = HPTLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_LEISTUNGLFDNR = HPT_LEISTUNGLFDNR" "~"
if joinKuerzel = LEIHPT
searchResult := searchResult "LEFT JOIN ZLE.HPT_HONORARPOSTEXTE ON HPT_LEISTUNGLFDNR = LEI_LEISTUNGLFDNR" "~"
if joinKuerzel = EKOLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = EKO_FIRMENNR AND LFI_LOHNJAHR = EKO_JAHR" "~"
if joinKuerzel = LFIEKO
searchResult := searchResult "LEFT JOIN HRS.EKO_ELDAKONTROLLE ON EKO_FIRMENNR = LFI_FIRMENNR AND EKO_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = LEHLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = LEH_LOHNFIRMENNR AND LFI_LOHNJAHR = LEH_JAHR" "~"
if joinKuerzel = LFILEH
searchResult := searchResult "LEFT JOIN HRS.LEH_LOHNEINSTELLUNG_HIST ON LEH_LOHNFIRMENNR = LFI_FIRMENNR AND LEH_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = EMBLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = EMB_DVFIRMENNR AND LFI_LOHNJAHR = EMB_JAHR" "~"
if joinKuerzel = LFIEMB
searchResult := searchResult "LEFT JOIN HRS.EMB_ELDADETAIL_MBGM_P ON EMB_DVFIRMENNR = LFI_FIRMENNR AND EMB_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = RNGLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_FIRMENNR = RNG_FIRMENNR AND LFS_LIEFERSCHEINLFDNR = RNG_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = LFSRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = LFS_FIRMENNR AND RNG_LIEFERSCHEINLFDNR = LFS_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = AVELIB
searchResult := searchResult "LEFT JOIN WWS.LIB_LIEFERBEDINGUNG ON LIB_LIEFERBNR = AVE_LIB_LIEFERBNR" "~"
if joinKuerzel = LIBAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_LIB_LIEFERBNR = LIB_LIEFERBNR" "~"
if joinKuerzel = BPKLIB
searchResult := searchResult "LEFT JOIN WWS.LIB_LIEFERBEDINGUNG ON LIB_LIEFERBNR = BPK_LIEFERBNR" "~"
if joinKuerzel = LIBBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_LIEFERBNR = LIB_LIEFERBNR" "~"
if joinKuerzel = EFKLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = EFK_LAG_FIRMENNR AND LAG_LAGERNR = EFK_LAGERNR" "~"
if joinKuerzel = LAGEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_LAG_FIRMENNR = LAG_FIRMENNR AND EFK_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = ERZLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = ERZ_ZIELLAGER_FIRMENNR AND LAG_LAGERNR = ERZ_ZIELLAGER_LAGERNR" "~"
if joinKuerzel = LAGERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_ZIELLAGER_FIRMENNR = LAG_FIRMENNR AND ERZ_ZIELLAGER_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = LADLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = LAD_ARL_FIRMENNR AND LAG_LAGERNR = LAD_LAGERNR" "~"
if joinKuerzel = LAGLAD
searchResult := searchResult "LEFT JOIN WWS.LAD_LAGERARTIKELDATEN ON LAD_ARL_FIRMENNR = LAG_FIRMENNR AND LAD_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = LUELAG
searchResult := searchResult "LEFT JOIN LAG_LAGER.LAG_LAGER ON LAG_FIRMENNR = LUE_LAG_FIRMENNR AND LAG_LAGERNR = LUE_LAGERNR" "~"
if joinKuerzel = LAGLUE
searchResult := searchResult "LEFT JOIN PRODUKTION.LUE_LAGER_UMS_EMPFANGEN ON LUE_LAG_FIRMENNR = LAG_FIRMENNR AND LUE_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = PLCLAG
searchResult := searchResult "LEFT JOIN LAG_LAGER.LAG_LAGER ON LAG_FIRMENNR = PLC_LAG_FIRMENNR AND LAG_LAGERNR = PLC_LAG_LAGERNR" "~"
if joinKuerzel = LAGPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_LAG_FIRMENNR = LAG_FIRMENNR AND PLC_LAG_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = VFALAG
searchResult := searchResult "LEFT JOIN LAG_LAGER.LAG_LAGER ON LAG_FIRMENNR = VFA_ZIEL_LAG_FIRMENNR AND LAG_LAGERNR = VFA_ZIEL_LAGERNR" "~"
if joinKuerzel = LAGVFA
searchResult := searchResult "LEFT JOIN PRODUKTION.VFA_VERFAHRENSANWEISUNG_KOPF ON VFA_ZIEL_LAG_FIRMENNR = LAG_FIRMENNR AND VFA_ZIEL_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = WADLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = WAD_ZIELLAGER_FIRMENNR AND LAG_LAGERNR = WAD_ZIELLAGER_LAGERNR" "~"
if joinKuerzel = LAGWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_ZIELLAGER_FIRMENNR = LAG_FIRMENNR AND WAD_ZIELLAGER_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = WEOLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = WEO_LAG_FIRMENNR AND LAG_LAGERNR = WEO_LAGERNR" "~"
if joinKuerzel = LAGWEO
searchResult := searchResult "LEFT JOIN WWS.WEO_WARENEINSATZLPO ON WEO_LAG_FIRMENNR = LAG_FIRMENNR AND WEO_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = WKPLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = WKP_FIRMENNR AND LAG_LAGERNR = WKP_LAGERNR" "~"
if joinKuerzel = LAGWKP
searchResult := searchResult "LEFT JOIN WWS.WKP_WWSKONTENTABPOS ON WKP_FIRMENNR = LAG_FIRMENNR AND WKP_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = EROLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = ERO_ZIELLAGER_FIRMENNR AND LAG_LAGERNR = ERO_ZIELLAGER_LAGERNR" "~"
if joinKuerzel = LAGERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_ZIELLAGER_FIRMENNR = LAG_FIRMENNR AND ERO_ZIELLAGER_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = WAPLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = WAP_QUELLLAGER_FIRMENNR AND LAG_LAGERNR = WAP_QUELLLAGER_LAGERNR" "~"
if joinKuerzel = LAGWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_QUELLLAGER_FIRMENNR = LAG_FIRMENNR AND WAP_QUELLLAGER_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = ADCLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = ADC_LANDNR" "~"
if joinKuerzel = LANADC
searchResult := searchResult "LEFT JOIN WWS.ADC_ABGABENDETAILRECH ON ADC_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = ADELAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = ADE_LANDNR" "~"
if joinKuerzel = LANADE
searchResult := searchResult "LEFT JOIN WWS.ADE_ABGABENDETAILERFPOS ON ADE_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = BKZLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = BKZ_LANDNR" "~"
if joinKuerzel = LANBKZ
searchResult := searchResult "LEFT JOIN BIL.BKZ_BILANZKENNZAHL ON BKZ_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = ERBLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = ERB_LANDNR" "~"
if joinKuerzel = LANERB
searchResult := searchResult "LEFT JOIN FIBU.ERB_BUCHUNG ON ERB_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = FTGLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = FTG_LANDNR" "~"
if joinKuerzel = LANFTG
searchResult := searchResult "LEFT JOIN BUERO.FTG_FEIERTAG ON FTG_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = GLSLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = GLS_LANDNR" "~"
if joinKuerzel = LANGLS
searchResult := searchResult "LEFT JOIN BIL.GLS_GLIEDERUNGSSCHEMA ON GLS_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = GSWLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = GSW_LANDNR" "~"
if joinKuerzel = LANGSW
searchResult := searchResult "LEFT JOIN BMDDEFAULT.GSW_GESETZWERT ON GSW_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = ONSLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = ONS_LANDNR" "~"
if joinKuerzel = LANONS
searchResult := searchResult "LEFT JOIN HRS.ONS_ONLINEBEW_STAMMDATEN ON ONS_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = PACLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = PAC_LANDNR" "~"
if joinKuerzel = LANPAC
searchResult := searchResult "LEFT JOIN WWS.PAC_PACKSTOFF ON PAC_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = PVFLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = PVF_LANDNR" "~"
if joinKuerzel = LANPVF
searchResult := searchResult "LEFT JOIN BUERO.PVF_PERSONENVORERFASSUNG ON PVF_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = RVCLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = RVC_LANDNR" "~"
if joinKuerzel = LANRVC
searchResult := searchResult "LEFT JOIN FIBU.RVC_RVCODE ON RVC_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = SSCLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = SSC_LANDNR" "~"
if joinKuerzel = LANSSC
searchResult := searchResult "LEFT JOIN FIBU.SSC_STEUERSCHLUESSEL ON SSC_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = WIBLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = WIB_LANDNR" "~"
if joinKuerzel = LANWIB
searchResult := searchResult "LEFT JOIN BIL.WIB_WIBERICHTSART ON WIB_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = YBULAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = YBU_LAN_LANDNR" "~"
if joinKuerzel = LANYBU
searchResult := searchResult "LEFT JOIN FIBU.YBU_AUTOBUCHUNG ON YBU_LAN_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = LANLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = LAN_INTRASTAT_LANDNR" "~"
if joinKuerzel = LANLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_INTRASTAT_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = ARLLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = ARL_VORGABE_FIL_FIRMENNR AND LAP_LAGERNR = ARL_VORGABE_LAGERNR AND LAP_LAGERPLATZ = ARL_VORGABE_LAGERPLATZ" "~"
if joinKuerzel = LAPARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_VORGABE_FIL_FIRMENNR = LAP_FIRMENNR AND ARL_VORGABE_LAGERNR = LAP_LAGERNR AND ARL_VORGABE_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = BOPLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = BOP_LAG_FIRMENNR AND LAP_LAGERNR = BOP_LAG_LAGERNR AND LAP_LAGERPLATZ = BOP_LAP_LAGERPLATZ" "~"
if joinKuerzel = LAPBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_LAG_FIRMENNR = LAP_FIRMENNR AND BOP_LAG_LAGERNR = LAP_LAGERNR AND BOP_LAP_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = BPKLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = BPK_QUELLLAGER_FIRMENNR AND LAP_LAGERNR = BPK_QUELLLAGER_LAGERNR AND LAP_LAGERPLATZ = BPK_QUELLLAGER_LAGERPLATZ" "~"
if joinKuerzel = LAPBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_QUELLLAGER_FIRMENNR = LAP_FIRMENNR AND BPK_QUELLLAGER_LAGERNR = LAP_LAGERNR AND BPK_QUELLLAGER_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = EFKLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = EFK_LAG_FIRMENNR AND LAP_LAGERNR = EFK_LAGERNR AND LAP_LAGERPLATZ = EFK_LAGERPLATZ" "~"
if joinKuerzel = LAPEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_LAG_FIRMENNR = LAP_FIRMENNR AND EFK_LAGERNR = LAP_LAGERNR AND EFK_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = EPSLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = EPS_LAG_FIRMENNR AND LAP_LAGERNR = EPS_LAGERNR AND LAP_LAGERPLATZ = EPS_LAGERPLATZ" "~"
if joinKuerzel = LAPEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_LAG_FIRMENNR = LAP_FIRMENNR AND EPS_LAGERNR = LAP_LAGERNR AND EPS_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = WAGLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = WAG_VORGABE_FIRMENNR AND LAP_LAGERNR = WAG_VORGABE_LAGERNR AND LAP_LAGERPLATZ = WAG_VORGABE_LAGERPLATZ" "~"
if joinKuerzel = LAPWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_VORGABE_FIRMENNR = LAP_FIRMENNR AND WAG_VORGABE_LAGERNR = LAP_LAGERNR AND WAG_VORGABE_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = AVELAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = AVE_ZIELLAGER_FIRMENNR AND LAP_LAGERNR = AVE_ZIELLAGER_LAGERNR AND LAP_LAGERPLATZ = AVE_ZIELLAGER_LAGERPLATZ" "~"
if joinKuerzel = LAPAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ZIELLAGER_FIRMENNR = LAP_FIRMENNR AND AVE_ZIELLAGER_LAGERNR = LAP_LAGERNR AND AVE_ZIELLAGER_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = EROLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = ERO_ZIELLAGER_FIRMENNR AND LAP_LAGERNR = ERO_ZIELLAGER_LAGERNR AND LAP_LAGERPLATZ = ERO_ZIELLAGER_LAGERPLATZ" "~"
if joinKuerzel = LAPERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_ZIELLAGER_FIRMENNR = LAP_FIRMENNR AND ERO_ZIELLAGER_LAGERNR = LAP_LAGERNR AND ERO_ZIELLAGER_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = LARLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = LAR_LAQID" "~"
if joinKuerzel = LAQLAR
searchResult := searchResult "LEFT JOIN HRS.LAR_LOHNARTENREGEL ON LAR_LAQID = LAQ_LOHNARTID" "~"
if joinKuerzel = LAWLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = LAW_LOHNARTID" "~"
if joinKuerzel = LAQLAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_LOHNARTID = LAQ_LOHNARTID" "~"
if joinKuerzel = MADLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = MAD_LOHNARTID" "~"
if joinKuerzel = LAQMAD
searchResult := searchResult "LEFT JOIN HRS.MAD_MASSENDATENERF ON MAD_LOHNARTID = LAQ_LOHNARTID" "~"
if joinKuerzel = SPELAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = SPE_LOHNARTID" "~"
if joinKuerzel = LAQSPE
searchResult := searchResult "LEFT JOIN HRS.SPE_SPESENARTEN ON SPE_LOHNARTID = LAQ_LOHNARTID" "~"
if joinKuerzel = TMLLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = TML_LOHNARTID" "~"
if joinKuerzel = LAQTML
searchResult := searchResult "LEFT JOIN HRS.TML_TERMINISIERTELOHNART ON TML_LOHNARTID = LAQ_LOHNARTID" "~"
if joinKuerzel = EFPLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = EFP_LOHNART3" "~"
if joinKuerzel = LAQEFP
searchResult := searchResult "LEFT JOIN HRS.EFP_ENTGELTFORTZAHLPROZ ON EFP_LOHNART3 = LAQ_LOHNARTID" "~"
if joinKuerzel = LAWLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = LAW_LS_ADD_LA_4" "~"
if joinKuerzel = LAQLAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_LS_ADD_LA_4 = LAQ_LOHNARTID" "~"
if joinKuerzel = ERPLEI
searchResult := searchResult "LEFT JOIN LEI_LEISTUNG.LEI_LEISTUNG ON LEI_LEISTUNGLFDNR = ERP_LEISTUNGLFDNR" "~"
if joinKuerzel = LEIERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_LEISTUNGLFDNR = LEI_LEISTUNGLFDNR" "~"
if joinKuerzel = DNZLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = DNZ_LFIFIRMENNR AND LFI_LOHNJAHR = DNZ_JAHR" "~"
if joinKuerzel = LFIDNZ
searchResult := searchResult "LEFT JOIN HRS.DNZ_DIVNICHTLEISTUNGSZEIT ON DNZ_LFIFIRMENNR = LFI_FIRMENNR AND DNZ_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = EM2LFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = EM2_FIRMENNR AND LFI_LOHNJAHR = EM2_JAHR" "~"
if joinKuerzel = LFIEM2
searchResult := searchResult "LEFT JOIN HRS.EM2_ELDADETAIL_MBGM ON EM2_FIRMENNR = LFI_FIRMENNR AND EM2_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = EVMLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = EVM_FIRMENNR AND LFI_LOHNJAHR = EVM_JAHR" "~"
if joinKuerzel = LFIEVM
searchResult := searchResult "LEFT JOIN HRS.EVM_ELDADETAIL_VM ON EVM_FIRMENNR = LFI_FIRMENNR AND EVM_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = FPRLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = FPR_LANDNR" "~"
if joinKuerzel = LANFPR
searchResult := searchResult "LEFT JOIN FIBU.FPR_FIBUPROZENTSATZ ON FPR_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = KSTLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = KST_LAN_SPESEN_LANDNR" "~"
if joinKuerzel = LANKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_LAN_SPESEN_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = PKMLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = PKM_LAN_LANDNR" "~"
if joinKuerzel = LANPKM
searchResult := searchResult "LEFT JOIN PRODUKTION.PKM_KM_MASTER ON PKM_LAN_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = PROLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = PRO_LANDNR" "~"
if joinKuerzel = LANPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = PSMLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = PSM_DBA_LANDNR" "~"
if joinKuerzel = LANPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_DBA_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = SRALAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = SRA_LANDNR" "~"
if joinKuerzel = LANSRA
searchResult := searchResult "LEFT JOIN WWS.SRA_STEUERART ON SRA_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = BUCLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = BUC_ZM_LANDNR" "~"
if joinKuerzel = LANBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_ZM_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = AWSLAP
searchResult := searchResult "LEFT JOIN LAP_LAGERPLATZ.LAP_LAGERPLATZ ON LAP_FIRMENNR = AWS_LAG_FIRMENNR AND LAP_LAGERNR = AWS_LAG_LAGERNR AND LAP_LAGERPLATZ = AWS_LAP_LAGERPLATZ" "~"
if joinKuerzel = LAPAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_LAG_FIRMENNR = LAP_FIRMENNR AND AWS_LAG_LAGERNR = LAP_LAGERNR AND AWS_LAP_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = ERZLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = ERZ_ZIELLAGER_FIRMENNR AND LAP_LAGERNR = ERZ_ZIELLAGER_LAGERNR AND LAP_LAGERPLATZ = ERZ_ZIELLAGER_LAGERPLATZ" "~"
if joinKuerzel = LAPERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_ZIELLAGER_FIRMENNR = LAP_FIRMENNR AND ERZ_ZIELLAGER_LAGERNR = LAP_LAGERNR AND ERZ_ZIELLAGER_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = LKOLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = LKO_ARTIKELIDFIRMENNR AND LAP_LAGERNR = LKO_LAGERNR AND LAP_LAGERPLATZ = LKO_LAGERPLATZ" "~"
if joinKuerzel = LAPLKO
searchResult := searchResult "LEFT JOIN WWS.LKO_LAGERKONTO ON LKO_ARTIKELIDFIRMENNR = LAP_FIRMENNR AND LKO_LAGERNR = LAP_LAGERNR AND LKO_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = LUPLAP
searchResult := searchResult "LEFT JOIN LAP_LAGERPLATZ.LAP_LAGERPLATZ ON LAP_FIRMENNR = LUP_DST_LAG_FIRMENNR AND LAP_LAGERNR = LUP_DST_LAGERNR AND LAP_LAGERPLATZ = LUP_DST_LAGERPLATZ" "~"
if joinKuerzel = LAPLUP
searchResult := searchResult "LEFT JOIN PRODUKTION.LUP_LAGER_UMS_POS ON LUP_DST_LAG_FIRMENNR = LAP_FIRMENNR AND LUP_DST_LAGERNR = LAP_LAGERNR AND LUP_DST_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = RPMLAP
searchResult := searchResult "LEFT JOIN LAP_LAGERPLATZ.LAP_LAGERPLATZ ON LAP_FIRMENNR = RPM_LAG_FIRMENNR AND LAP_LAGERNR = RPM_LAGERNR AND LAP_LAGERPLATZ = RPM_LAGERPLATZ" "~"
if joinKuerzel = LAPRPM
searchResult := searchResult "LEFT JOIN PRODUKTION.RPM_RETRO_PRODUKT_MATERIAL ON RPM_LAG_FIRMENNR = LAP_FIRMENNR AND RPM_LAGERNR = LAP_LAGERNR AND RPM_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = WAFLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = WAF_ZIELLAGER_FIRMENNR AND LAP_LAGERNR = WAF_ZIELLAGER_LAGERNR AND LAP_LAGERPLATZ = WAF_ZIELLAGER_LAGERPLATZ" "~"
if joinKuerzel = LAPWAF
searchResult := searchResult "LEFT JOIN WWS.WAF_WWSADRZUFIRMENBEZ ON WAF_ZIELLAGER_FIRMENNR = LAP_FIRMENNR AND WAF_ZIELLAGER_LAGERNR = LAP_LAGERNR AND WAF_ZIELLAGER_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = BPKLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = BPK_LAG_FIRMENNR AND LAP_LAGERNR = BPK_LAGERNR AND LAP_LAGERPLATZ = BPK_LAGERPLATZ" "~"
if joinKuerzel = LAPBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_LAG_FIRMENNR = LAP_FIRMENNR AND BPK_LAGERNR = LAP_LAGERNR AND BPK_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = EFKLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = EFK_ZIELLAGER_FIRMENNR AND LAP_LAGERNR = EFK_ZIELLAGER_LAGERNR AND LAP_LAGERPLATZ = EFK_ZIELLAGER_LAGERPLATZ" "~"
if joinKuerzel = LAPEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_ZIELLAGER_FIRMENNR = LAP_FIRMENNR AND EFK_ZIELLAGER_LAGERNR = LAP_LAGERNR AND EFK_ZIELLAGER_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = ELVLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = ELV_ZIELLAGER_FIRMENNR AND LAP_LAGERNR = ELV_ZIELLAGER_LAGERNR AND LAP_LAGERPLATZ = ELV_ZIELLAGER_LAGERPLATZ" "~"
if joinKuerzel = LAPELV
searchResult := searchResult "LEFT JOIN WWS.ELV_ERFASSPOSLIEFERVOR ON ELV_ZIELLAGER_FIRMENNR = LAP_FIRMENNR AND ELV_ZIELLAGER_LAGERNR = LAP_LAGERNR AND ELV_ZIELLAGER_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = ABDLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = ABD_LOHNARTID" "~"
if joinKuerzel = LAQABD
searchResult := searchResult "LEFT JOIN HRS.ABD_ABRECHNUNGSDETAIL ON ABD_LOHNARTID = LAQ_LOHNARTID" "~"
if joinKuerzel = ABGLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = ABG_LOHNARTID" "~"
if joinKuerzel = LAQABG
searchResult := searchResult "LEFT JOIN HRS.ABG_ABRECHNUNG ON ABG_LOHNARTID = LAQ_LOHNARTID" "~"
if joinKuerzel = EFPLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = EFP_LOHNART_FW" "~"
if joinKuerzel = LAQEFP
searchResult := searchResult "LEFT JOIN HRS.EFP_ENTGELTFORTZAHLPROZ ON EFP_LOHNART_FW = LAQ_LOHNARTID" "~"
if joinKuerzel = FWELAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = FWE_LOHNARTID" "~"
if joinKuerzel = LAQFWE
searchResult := searchResult "LEFT JOIN HRS.FWE_FIXWERTE ON FWE_LOHNARTID = LAQ_LOHNARTID" "~"
if joinKuerzel = LAKLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = LAK_NACHFOLGE_LOHNARTID" "~"
if joinKuerzel = LAQLAK
searchResult := searchResult "LEFT JOIN HRS.LAK_LOHNARTENKOPPELUNG ON LAK_NACHFOLGE_LOHNARTID = LAQ_LOHNARTID" "~"
if joinKuerzel = VSTKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = VST_KLI_FIRMENNR AND KLI_KLID = VST_KLID" "~"
if joinKuerzel = KLIVST
searchResult := searchResult "LEFT JOIN WWS.VST_VERKAUFSSTATISTIK ON VST_KLI_FIRMENNR = KLI_FIRMENNR AND VST_KLID = KLI_KLID" "~"
if joinKuerzel = BPKKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = BPK_LIEFEREMPF_FIRMENNR AND KLI_KLID = BPK_LIEFEREMPF_KLID" "~"
if joinKuerzel = KLIBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_LIEFEREMPF_FIRMENNR = KLI_FIRMENNR AND BPK_LIEFEREMPF_KLID = KLI_KLID" "~"
if joinKuerzel = BVOKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = BVO_LIEFEREMPF_FIRMENNR AND KLI_KLID = BVO_LIEFEREMPF_KLID" "~"
if joinKuerzel = KLIBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_LIEFEREMPF_FIRMENNR = KLI_FIRMENNR AND BVO_LIEFEREMPF_KLID = KLI_KLID" "~"
if joinKuerzel = SAWKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = SAW_LIEFNR_FIRMENNR AND KLI_KLID = SAW_LIEFNR_KLID" "~"
if joinKuerzel = KLISAW
searchResult := searchResult "LEFT JOIN WWSINDIV.SAW_STATISTIKWERTE ON SAW_LIEFNR_FIRMENNR = KLI_FIRMENNR AND SAW_LIEFNR_KLID = KLI_KLID" "~"
if joinKuerzel = EFKKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = EFK_ZRECHNUNGEMPF_FIRMENNR AND KLI_KLID = EFK_ZRECHNUNGEMPF_KLID" "~"
if joinKuerzel = KLIEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_ZRECHNUNGEMPF_FIRMENNR = KLI_FIRMENNR AND EFK_ZRECHNUNGEMPF_KLID = KLI_KLID" "~"
if joinKuerzel = MDEKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = MDE_RECHNUNGSEMPF_FIRMENNR AND KLI_KLID = MDE_RECHNUNGSEMPF_KLID" "~"
if joinKuerzel = KLIMDE
searchResult := searchResult "LEFT JOIN WWS.MDE_MOBILEDATENERFASSUNG ON MDE_RECHNUNGSEMPF_FIRMENNR = KLI_FIRMENNR AND MDE_RECHNUNGSEMPF_KLID = KLI_KLID" "~"
if joinKuerzel = KMPKMG
searchResult := searchResult "LEFT JOIN KORE.KMG_KOREMENGE ON KMG_FIRMENNR = KMP_FIRMENNR AND KMG_KORENR = KMP_KORENR AND KMG_KOREMENGENNR = KMP_KOREMENGENNR" "~"
if joinKuerzel = KMGKMP
searchResult := searchResult "LEFT JOIN KORE.KMP_PLANMENGEN ON KMP_FIRMENNR = KMG_FIRMENNR AND KMP_KORENR = KMG_KORENR AND KMP_KOREMENGENNR = KMG_KOREMENGENNR" "~"
if joinKuerzel = KSEKMG
searchResult := searchResult "LEFT JOIN KORE.KMG_KOREMENGE ON KMG_FIRMENNR = KSE_FIRMENNR AND KMG_KORENR = KSE_KORENR AND KMG_KOREMENGENNR = KSE_BASISKOREMENGENNR" "~"
if joinKuerzel = KMGKSE
searchResult := searchResult "LEFT JOIN KORE.KSE_KORESCHLUESSELEINGABE ON KSE_FIRMENNR = KMG_FIRMENNR AND KSE_KORENR = KMG_KORENR AND KSE_BASISKOREMENGENNR = KMG_KOREMENGENNR" "~"
if joinKuerzel = KZKKMG
searchResult := searchResult "LEFT JOIN KORE.KMG_KOREMENGE ON KMG_FIRMENNR = KZK_FWK_FIRMENNR AND KMG_KORENR = KZK_KORENR AND KMG_KOREMENGENNR = KZK_KOREMENGENNR" "~"
if joinKuerzel = KMGKZK
searchResult := searchResult "LEFT JOIN KORE.KZK_ZUSCHLAGKOPF ON KZK_FWK_FIRMENNR = KMG_FIRMENNR AND KZK_KORENR = KMG_KORENR AND KZK_KOREMENGENNR = KMG_KOREMENGENNR" "~"
if joinKuerzel = UMIKMG
searchResult := searchResult "LEFT JOIN KORE.KMG_KOREMENGE ON KMG_FIRMENNR = UMI_FIRMENNR AND KMG_KORENR = UMI_KORENR AND KMG_KOREMENGENNR = UMI_KOREMENGENNR" "~"
if joinKuerzel = KMGUMI
searchResult := searchResult "LEFT JOIN KORE.UMI_UMLAGEISTMENGE ON UMI_FIRMENNR = KMG_FIRMENNR AND UMI_KORENR = KMG_KORENR AND UMI_KOREMENGENNR = KMG_KOREMENGENNR" "~"
if joinKuerzel = ZBAKMG
searchResult := searchResult "LEFT JOIN KMG_KOREMENGE.KMG_KOREMENGE ON KMG_FIRMENNR = ZBA_KMG_FIRMENNR AND KMG_KORENR = ZBA_KMG_KORENR AND KMG_KOREMENGENNR = ZBA_KMG_KOREMENGENNR" "~"
if joinKuerzel = KMGZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_KMG_FIRMENNR = KMG_FIRMENNR AND ZBA_KMG_KORENR = KMG_KORENR AND ZBA_KMG_KOREMENGENNR = KMG_KOREMENGENNR" "~"
if joinKuerzel = AVEKOD
searchResult := searchResult "LEFT JOIN KOD_KONDITIONEN.KOD_KONDITIONEN ON KOD_KONDNR = AVE_ZAHLKONDNR" "~"
if joinKuerzel = KODAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ZAHLKONDNR = KOD_KONDNR" "~"
if joinKuerzel = KDPKPM
searchResult := searchResult "LEFT JOIN KORE.KPM_PLANMODELL ON KPM_FIRMENNR = KDP_FIRMENNR AND KPM_KORENR = KDP_KORENR AND KPM_MODELLNR = KDP_MODELLNR" "~"
if joinKuerzel = KPMKDP
searchResult := searchResult "LEFT JOIN KORE.KDP_PLANWERTE ON KDP_FIRMENNR = KPM_FIRMENNR AND KDP_KORENR = KPM_KORENR AND KDP_MODELLNR = KPM_MODELLNR" "~"
if joinKuerzel = KPMKPM
searchResult := searchResult "LEFT JOIN KORE.KPM_PLANMODELL ON KPM_FIRMENNR = KPM_FIRMENNR AND KPM_KORENR = KPM_KORENR AND KPM_MODELLNR = KPM_VERGLEICHSMODELLNR" "~"
if joinKuerzel = KPMKPM
searchResult := searchResult "LEFT JOIN KORE.KPM_PLANMODELL ON KPM_FIRMENNR = KPM_FIRMENNR AND KPM_KORENR = KPM_KORENR AND KPM_VERGLEICHSMODELLNR = KPM_MODELLNR" "~"
if joinKuerzel = KPRKPM
searchResult := searchResult "LEFT JOIN KORE.KPM_PLANMODELL ON KPM_FIRMENNR = KPR_FIRMENNR AND KPM_KORENR = KPR_KORENR AND KPM_MODELLNR = KPR_MODELLNR" "~"
if joinKuerzel = KPMKPR
searchResult := searchResult "LEFT JOIN KORE.KPR_PLANPREISE ON KPR_FIRMENNR = KPM_FIRMENNR AND KPR_KORENR = KPM_KORENR AND KPR_MODELLNR = KPM_MODELLNR" "~"
if joinKuerzel = LAWKRM
searchResult := searchResult "LEFT JOIN HRS.KRM_KONTENREFERENZTAB ON KRM_DATENBESTANDNR = LAW_DBSNR AND KRM_KONTOREFNR = LAW_KONTOREFNR_GEGENKTO" "~"
if joinKuerzel = KRMLAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_DBSNR = KRM_DATENBESTANDNR AND LAW_KONTOREFNR_GEGENKTO = KRM_KONTOREFNR" "~"
if joinKuerzel = ARLKST
searchResult := searchResult "LEFT JOIN KST_KORESTAMM.KST_KORESTAMM ON KST_KOSTENSTAMMNR = ARL_KOSTENTR_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_KOSTENTR_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = BZBKST
searchResult := searchResult "LEFT JOIN KST_KORESTAMM.KST_KORESTAMM ON KST_KOSTENSTAMMNR = BZB_KOST_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTBZB
searchResult := searchResult "LEFT JOIN PRODUKTION.BZB_BAZZEITBUCHUNG ON BZB_KOST_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = KFZKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = KFZ_FIRMENNR AND KST_KORENR = KFZ_KORENR AND KST_KOSTENTYP = KFZ_IMPORT_KOSTENTYP AND KST_KOSTENSTAMMNR = KFZ_IMPORT_KOSTENSTAMMNR AND KST_KNOTENREFNR = KFZ_IMPORT_KNOTENREFNR" "~"
if joinKuerzel = KSTKFZ
searchResult := searchResult "LEFT JOIN KORE.KFZ_KOREFIBUZU ON KFZ_FIRMENNR = KST_FIRMENNR AND KFZ_KORENR = KST_KORENR AND KFZ_IMPORT_KOSTENTYP = KST_KOSTENTYP AND KFZ_IMPORT_KOSTENSTAMMNR = KST_KOSTENSTAMMNR AND KFZ_IMPORT_KNOTENREFNR = KST_KNOTENREFNR" "~"
if joinKuerzel = IWVLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = IWV_LANDNR" "~"
if joinKuerzel = LANIWV
searchResult := searchResult "LEFT JOIN BUERO.IWV_INTRASTATWV ON IWV_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = KTOLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = KTO_LANDNR" "~"
if joinKuerzel = LANKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = LANLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = LAN_STEUER_LANDNR" "~"
if joinKuerzel = LANLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_STEUER_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = LGNLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = LGN_LANDNR" "~"
if joinKuerzel = LANLGN
searchResult := searchResult "LEFT JOIN BIL.LGN_LAENDERGRUPPEN ON LGN_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = MVKLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = MVK_LANDNR" "~"
if joinKuerzel = LANMVK
searchResult := searchResult "LEFT JOIN BUERO.MVK_MITARBVORSORGEKASSE ON MVK_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = PAPLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = PAP_LANDNR" "~"
if joinKuerzel = LANPAP
searchResult := searchResult "LEFT JOIN WWS.PAP_PACKSTOFFTABPOS ON PAP_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = XSELAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = XSE_KEY_LANDNR" "~"
if joinKuerzel = LANXSE
searchResult := searchResult "LEFT JOIN BUERO.XSE_XML_STEUERERKL ON XSE_KEY_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = ZVBLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = ZVB_LANDNR" "~"
if joinKuerzel = LANZVB
searchResult := searchResult "LEFT JOIN BUERO.ZVB_TBBEZEICHNUNGSZWECK ON ZVB_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = BAZLAP
searchResult := searchResult "LEFT JOIN LAP_LAGERPLATZ.LAP_LAGERPLATZ ON LAP_FIRMENNR = BAZ_LAG_FIRMENNR AND LAP_LAGERNR = BAZ_LAGERNR AND LAP_LAGERPLATZ = BAZ_LAGERPLATZ" "~"
if joinKuerzel = LAPBAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_LAG_FIRMENNR = LAP_FIRMENNR AND BAZ_LAGERNR = LAP_LAGERNR AND BAZ_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = KAPLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = KAP_LAG_FIRMENNR AND LAP_LAGERNR = KAP_LAG_LAGERNR AND LAP_LAGERPLATZ = KAP_LAP_LAGERPLATZ" "~"
if joinKuerzel = LAPKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_LAG_FIRMENNR = LAP_FIRMENNR AND KAP_LAG_LAGERNR = LAP_LAGERNR AND KAP_LAP_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = LPMLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = LPM_FIRMENNR AND LAP_LAGERNR = LPM_LAGERNR AND LAP_LAGERPLATZ = LPM_LAGERPLATZ" "~"
if joinKuerzel = LAPLPM
searchResult := searchResult "LEFT JOIN WWS.LPM_LAGERPLATZMONATSWERTE ON LPM_FIRMENNR = LAP_FIRMENNR AND LPM_LAGERNR = LAP_LAGERNR AND LPM_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = LUELAP
searchResult := searchResult "LEFT JOIN LAP_LAGERPLATZ.LAP_LAGERPLATZ ON LAP_FIRMENNR = LUE_LAG_FIRMENNR AND LAP_LAGERNR = LUE_LAGERNR AND LAP_LAGERPLATZ = LUE_LAGERPLATZ" "~"
if joinKuerzel = LAPLUE
searchResult := searchResult "LEFT JOIN PRODUKTION.LUE_LAGER_UMS_EMPFANGEN ON LUE_LAG_FIRMENNR = LAP_FIRMENNR AND LUE_LAGERNR = LAP_LAGERNR AND LUE_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = PCPLAP
searchResult := searchResult "LEFT JOIN LAP_LAGERPLATZ.LAP_LAGERPLATZ ON LAP_FIRMENNR = PCP_LAG_FIRMENNR AND LAP_LAGERNR = PCP_LAGERNR AND LAP_LAGERPLATZ = PCP_LAGERPLATZ" "~"
if joinKuerzel = LAPPCP
searchResult := searchResult "LEFT JOIN PRODUKTION.PCP_PPS_PRODUKT ON PCP_LAG_FIRMENNR = LAP_FIRMENNR AND PCP_LAGERNR = LAP_LAGERNR AND PCP_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = TLPLAP
searchResult := searchResult "LEFT JOIN LAP_LAGERPLATZ.LAP_LAGERPLATZ ON LAP_FIRMENNR = TLP_DST_LAG_FIRMENNR AND LAP_LAGERNR = TLP_DST_LAGERNR AND LAP_LAGERPLATZ = TLP_DST_LAGERPLATZ" "~"
if joinKuerzel = LAPTLP
searchResult := searchResult "LEFT JOIN PRODUKTION.TLP_BT_LOGISTIK_POSITION ON TLP_DST_LAG_FIRMENNR = LAP_FIRMENNR AND TLP_DST_LAGERNR = LAP_LAGERNR AND TLP_DST_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = ARLLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = ARL_PRODABG_LAG_FIRMENNR AND LAP_LAGERNR = ARL_PRODABG_LAGERNR AND LAP_LAGERPLATZ = ARL_PRODABG_LAGERPLATZ" "~"
if joinKuerzel = LAPARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_PRODABG_LAG_FIRMENNR = LAP_FIRMENNR AND ARL_PRODABG_LAGERNR = LAP_LAGERNR AND ARL_PRODABG_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = BPDLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = BPD_QUELLLAGER_FIRMENNR AND LAP_LAGERNR = BPD_QUELLLAGER_LAGERNR AND LAP_LAGERPLATZ = BPD_QUELLLAGER_LAGERPLATZ" "~"
if joinKuerzel = LAPBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_QUELLLAGER_FIRMENNR = LAP_FIRMENNR AND BPD_QUELLLAGER_LAGERNR = LAP_LAGERNR AND BPD_QUELLLAGER_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = WAGLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = WAG_VORG_LAG_FIRMENNR_EK AND LAP_LAGERNR = WAG_VORG_LAGERNR_EK AND LAP_LAGERPLATZ = WAG_VORG_LAGERPLATZ_EK" "~"
if joinKuerzel = LAPWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_VORG_LAG_FIRMENNR_EK = LAP_FIRMENNR AND WAG_VORG_LAGERNR_EK = LAP_LAGERNR AND WAG_VORG_LAGERPLATZ_EK = LAP_LAGERPLATZ" "~"
if joinKuerzel = ARLLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = ARL_VORG_LAG_FIRMENNR_EK AND LAP_LAGERNR = ARL_VORG_LAGERNR_EK AND LAP_LAGERPLATZ = ARL_VORG_LAGERPLATZ_EK" "~"
if joinKuerzel = LAPARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_VORG_LAG_FIRMENNR_EK = LAP_FIRMENNR AND ARL_VORG_LAGERNR_EK = LAP_LAGERNR AND ARL_VORG_LAGERPLATZ_EK = LAP_LAGERPLATZ" "~"
if joinKuerzel = GSMLEI
searchResult := searchResult "LEFT JOIN LEI_LEISTUNG.LEI_LEISTUNG ON LEI_LEISTUNGLFDNR = GSM_LEISTUNGLFDNR" "~"
if joinKuerzel = LEIGSM
searchResult := searchResult "LEFT JOIN PRODUKTION.GSM_GUTSCHLECHTMENGE ON GSM_LEISTUNGLFDNR = LEI_LEISTUNGLFDNR" "~"
if joinKuerzel = LEILEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_LEISTUNGLFDNR = LEI_STORNO_LEISTUNGLFDNR" "~"
if joinKuerzel = LEILEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_STORNO_LEISTUNGLFDNR = LEI_LEISTUNGLFDNR" "~"
if joinKuerzel = ESELFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = ESE_FIRMENNR AND LFI_LOHNJAHR = ESE_JAHR" "~"
if joinKuerzel = LFIESE
searchResult := searchResult "LEFT JOIN HRS.ESE_ELDADETAIL_SE ON ESE_FIRMENNR = LFI_FIRMENNR AND ESE_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = MEDMEI
searchResult := searchResult "LEFT JOIN BUERO.MEI_MEDIENINFO ON MEI_FIRMENNR = MED_HERST_FIRMENNR AND MEI_MEDIENINFOKZ = MED_HERST_MEDIENINFOKZ AND MEI_MEDIENINFOLFDNR = MED_HERST_MEDIENINFOLFDNR" "~"
if joinKuerzel = MEIMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_HERST_FIRMENNR = MEI_FIRMENNR AND MED_HERST_MEDIENINFOKZ = MEI_MEDIENINFOKZ AND MED_HERST_MEDIENINFOLFDNR = MEI_MEDIENINFOLFDNR" "~"
if joinKuerzel = DVHMIK
searchResult := searchResult "LEFT JOIN BUERO.MIK_MITARBEITERKARRIERE ON MIK_FIRMENNR = DVH_MIK_FIRMENNR AND MIK_KARRIERELFDNR = DVH_KARRIERELFDNR" "~"
if joinKuerzel = MIKDVH
searchResult := searchResult "LEFT JOIN BUERO.DVH_DIENSTVERHAELTNIS ON DVH_MIK_FIRMENNR = MIK_FIRMENNR AND DVH_KARRIERELFDNR = MIK_KARRIERELFDNR" "~"
if joinKuerzel = IAMMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = IAM_MITFIRMENNR AND MIT_MITARBEITERID = IAM_MITARBEITERID" "~"
if joinKuerzel = MITIAM
searchResult := searchResult "LEFT JOIN BUERO.IAM_INSTANZAKTIONMITARB ON IAM_MITFIRMENNR = MIT_FIRMENNR AND IAM_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = ASCMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = ASC_MIT_FIRMENNR AND MIT_MITARBEITERID = ASC_MITARBEITERID" "~"
if joinKuerzel = MITASC
searchResult := searchResult "LEFT JOIN ZLE.ASC_ARBEITSSCHEIN ON ASC_MIT_FIRMENNR = MIT_FIRMENNR AND ASC_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = ATEMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = ATE_MIT_FIRMENNR AND MIT_MITARBEITERID = ATE_MITARBEITERID" "~"
if joinKuerzel = MITATE
searchResult := searchResult "LEFT JOIN BUERO.ATE_ABTEILUNG ON ATE_MIT_FIRMENNR = MIT_FIRMENNR AND ATE_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = AUGMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = AUG_MIT_FIRMENNR AND MIT_MITARBEITERID = AUG_MITARBEITERID" "~"
if joinKuerzel = MITAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_MIT_FIRMENNR = MIT_FIRMENNR AND AUG_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = AVEMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = AVE_MITARBEITER_FIRMENNR AND MIT_MITARBEITERID = AVE_MITARBEITERID" "~"
if joinKuerzel = MITAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_MITARBEITER_FIRMENNR = MIT_FIRMENNR AND AVE_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = BOZMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = BOZ_KASSIER_FIRMENNR AND MIT_MITARBEITERID = BOZ_KASSIER_MITARBEITERID" "~"
if joinKuerzel = MITBOZ
searchResult := searchResult "LEFT JOIN WWS.BOZ_BONZAHLUNG ON BOZ_KASSIER_FIRMENNR = MIT_FIRMENNR AND BOZ_KASSIER_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = BVOMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = BVO_BEARBEITER_FIRMENNR AND MIT_MITARBEITERID = BVO_BEARBEITER_MITARBEITERID" "~"
if joinKuerzel = MITBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_BEARBEITER_FIRMENNR = MIT_FIRMENNR AND BVO_BEARBEITER_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = FGBMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = FGB_MIT_FIRMENNR AND MIT_MITARBEITERID = FGB_MITARBEITERID" "~"
if joinKuerzel = MITFGB
searchResult := searchResult "LEFT JOIN BUERO.FGB_FRAGEBOGEN ON FGB_MIT_FIRMENNR = MIT_FIRMENNR AND FGB_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = GBTMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = GBT_MIT_FIRMENNR AND MIT_MITARBEITERID = GBT_MITARBEITERID" "~"
if joinKuerzel = MITGBT
searchResult := searchResult "LEFT JOIN BUERO.GBT_GEBURTSTAG ON GBT_MIT_FIRMENNR = MIT_FIRMENNR AND GBT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = GIDMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = GID_MIT_FIR_FIRMENNR AND MIT_MITARBEITERID = GID_MITARBEITERID" "~"
if joinKuerzel = MITGID
searchResult := searchResult "LEFT JOIN BUERO.GID_GELDIDENTIFIKATION ON GID_MIT_FIR_FIRMENNR = MIT_FIRMENNR AND GID_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = GRSMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = GRS_MIT_FIRMENNR AND MIT_MITARBEITERID = GRS_MITARBEITERID" "~"
if joinKuerzel = MITGRS
searchResult := searchResult "LEFT JOIN BUERO.GRS_GELDRISIKO ON GRS_MIT_FIRMENNR = MIT_FIRMENNR AND GRS_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = HFTMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = HFT_MIT_FIRMENNR AND MIT_MITARBEITERID = HFT_MITARBEITERID" "~"
if joinKuerzel = MITHFT
searchResult := searchResult "LEFT JOIN BUERO.HFT_HILFSTABELLE ON HFT_MIT_FIRMENNR = MIT_FIRMENNR AND HFT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = KPOMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = KPO_VERTR_FIRMENNR AND MIT_MITARBEITERID = KPO_VERTR_MITARBEITERID" "~"
if joinKuerzel = MITKPO
searchResult := searchResult "LEFT JOIN WWS.KPO_KASSENPROVISION ON KPO_VERTR_FIRMENNR = MIT_FIRMENNR AND KPO_VERTR_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = LADMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = LAD_VORG_KOMMISS_FIRMENNR AND MIT_MITARBEITERID = LAD_VORG_KOMMISS_MITARBID" "~"
if joinKuerzel = MITLAD
searchResult := searchResult "LEFT JOIN WWS.LAD_LAGERARTIKELDATEN ON LAD_VORG_KOMMISS_FIRMENNR = MIT_FIRMENNR AND LAD_VORG_KOMMISS_MITARBID = MIT_MITARBEITERID" "~"
if joinKuerzel = LXDMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = LXD_FIRMENNR AND MIT_MITARBEITERID = LXD_MITARBEITERID" "~"
if joinKuerzel = MITLXD
searchResult := searchResult "LEFT JOIN BUERO.LXD_LOHNEXPPROTDETAIL ON LXD_FIRMENNR = MIT_FIRMENNR AND LXD_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = MSTMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = MST_FIRMENNR AND MIT_MITARBEITERID = MST_MITARBEITERID" "~"
if joinKuerzel = MITMST
searchResult := searchResult "LEFT JOIN BUERO.MST_MITARBSTELLVERTRETER ON MST_FIRMENNR = MIT_FIRMENNR AND MST_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = PTZMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = PTZ_VERTRETER_FIRMENNR AND MIT_MITARBEITERID = PTZ_VERTRETER_MITARBID" "~"
if joinKuerzel = MITPTZ
searchResult := searchResult "LEFT JOIN WWS.PTZ_PREISTABZU ON PTZ_VERTRETER_FIRMENNR = MIT_FIRMENNR AND PTZ_VERTRETER_MITARBID = MIT_MITARBEITERID" "~"
if joinKuerzel = KRDLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = KRD_FIRMENNR AND LFI_LOHNJAHR = KRD_JAHR" "~"
if joinKuerzel = LFIKRD
searchResult := searchResult "LEFT JOIN HRS.KRD_KONTENREFERENZDETAIL ON KRD_FIRMENNR = LFI_FIRMENNR AND KRD_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = LZULFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = LZU_DVFIRMENNR AND LFI_LOHNJAHR = LZU_PSJ_JAHR" "~"
if joinKuerzel = LFILZU
searchResult := searchResult "LEFT JOIN HRS.LZU_LOHNZUSTAND ON LZU_DVFIRMENNR = LFI_FIRMENNR AND LZU_PSJ_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = SUDLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = SUD_FIRMENNR AND LFI_LOHNJAHR = SUD_JAHR" "~"
if joinKuerzel = LFISUD
searchResult := searchResult "LEFT JOIN HRS.SUD_SUM_KOE_DIFF ON SUD_FIRMENNR = LFI_FIRMENNR AND SUD_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = SUKLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = SUK_FIRMENNR AND LFI_LOHNJAHR = SUK_JAHR" "~"
if joinKuerzel = LFISUK
searchResult := searchResult "LEFT JOIN HRS.SUK_SUM_KOE ON SUK_FIRMENNR = LFI_FIRMENNR AND SUK_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = KRDLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = KRD_MIT_FIRMENNR AND LFI_LOHNJAHR = KRD_JAHR" "~"
if joinKuerzel = LFIKRD
searchResult := searchResult "LEFT JOIN HRS.KRD_KONTENREFERENZDETAIL ON KRD_MIT_FIRMENNR = LFI_FIRMENNR AND KRD_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = EPSLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_FIRMENNR = EPS_LPO_FIRMENNR AND LFS_LIEFERSCHEINLFDNR = EPS_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = LFSEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_LPO_FIRMENNR = LFS_FIRMENNR AND EPS_LIEFERSCHEINLFDNR = LFS_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = GEPLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_FIRMENNR = GEP_LFS_FIRMENNR AND LFS_LIEFERSCHEINLFDNR = GEP_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = LFSGEP
searchResult := searchResult "LEFT JOIN WWS.GEP_GEFAHRENGUTPOS ON GEP_LFS_FIRMENNR = LFS_FIRMENNR AND GEP_LIEFERSCHEINLFDNR = LFS_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = KAKLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_FIRMENNR = KAK_LFS_FIRMENNR AND LFS_LIEFERSCHEINLFDNR = KAK_LFS_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = LFSKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_LFS_FIRMENNR = LFS_FIRMENNR AND KAK_LFS_LIEFERSCHEINLFDNR = LFS_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = PZELGF
searchResult := searchResult "LEFT JOIN HRS.LGF_LGVMAKROS ON LGF_LGVMAKROID = PZE_FILTER_LGF_LGVMAKROID" "~"
if joinKuerzel = LGFPZE
searchResult := searchResult "LEFT JOIN HRS.PZE_PERKZGLGZEILE ON PZE_FILTER_LGF_LGVMAKROID = LGF_LGVMAKROID" "~"
if joinKuerzel = LGBLGF
searchResult := searchResult "LEFT JOIN HRS.LGF_LGVMAKROS ON LGF_LGVMAKROID = LGB_LGVMAKROID" "~"
if joinKuerzel = LGFLGB
searchResult := searchResult "LEFT JOIN HRS.LGB_LGVBERECHNUNGEN ON LGB_LGVMAKROID = LGF_LGVMAKROID" "~"
if joinKuerzel = BAZLIN
searchResult := searchResult "LEFT JOIN PRODUKTION.LIN_PROD_LINIE ON LIN_FIRMENNR = BAZ_LIN_FIRMENNR AND LIN_LINIE = BAZ_LINIE" "~"
if joinKuerzel = LINBAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_LIN_FIRMENNR = LIN_FIRMENNR AND BAZ_LINIE = LIN_LINIE" "~"
if joinKuerzel = EPSLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_FIRMENNR = EPS_LPO_FIRMENNR AND LPO_LIEFERSCHEINLFDNR = EPS_LIEFERSCHEINLFDNR AND LPO_LIEFERSCHEINPOSLFDNR = EPS_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = LPOEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_LPO_FIRMENNR = LPO_FIRMENNR AND EPS_LIEFERSCHEINLFDNR = LPO_LIEFERSCHEINLFDNR AND EPS_LIEFERSCHEINPOSLFDNR = LPO_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = EPVLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_FIRMENNR = EPV_FIRMENNR AND LPO_LIEFERSCHEINLFDNR = EPV_LIEFERSCHEINLFDNR AND LPO_LIEFERSCHEINPOSLFDNR = EPV_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = LPOEPV
searchResult := searchResult "LEFT JOIN WWS.EPV_ERFASSPOSRECHVOR ON EPV_FIRMENNR = LPO_FIRMENNR AND EPV_LIEFERSCHEINLFDNR = LPO_LIEFERSCHEINLFDNR AND EPV_LIEFERSCHEINPOSLFDNR = LPO_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = LAJLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_FIRMENNR = LAJ_BUCHUNGS_FIRMENNR AND LPO_LIEFERSCHEINLFDNR = LAJ_LIEFERSCHEINLFDNR AND LPO_LIEFERSCHEINPOSLFDNR = LAJ_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = LPOLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_BUCHUNGS_FIRMENNR = LPO_FIRMENNR AND LAJ_LIEFERSCHEINLFDNR = LPO_LIEFERSCHEINLFDNR AND LAJ_LIEFERSCHEINPOSLFDNR = LPO_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = WEPLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_FIRMENNR = WEP_LPO_FIRMENNR AND LPO_LIEFERSCHEINLFDNR = WEP_LIEFERSCHEINLFDNR AND LPO_LIEFERSCHEINPOSLFDNR = WEP_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = LPOWEP
searchResult := searchResult "LEFT JOIN WWS.WEP_WWSERKONTROLLEPOS ON WEP_LPO_FIRMENNR = LPO_FIRMENNR AND WEP_LIEFERSCHEINLFDNR = LPO_LIEFERSCHEINLFDNR AND WEP_LIEFERSCHEINPOSLFDNR = LPO_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = WEULPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_FIRMENNR = WEU_LPO_FIRMENNR AND LPO_LIEFERSCHEINLFDNR = WEU_LIEFERSCHEINLFDNR AND LPO_LIEFERSCHEINPOSLFDNR = WEU_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = LPOWEU
searchResult := searchResult "LEFT JOIN WWS.WEU_WWSERKONTROLLEZU ON WEU_LPO_FIRMENNR = LPO_FIRMENNR AND WEU_LIEFERSCHEINLFDNR = LPO_LIEFERSCHEINLFDNR AND WEU_LIEFERSCHEINPOSLFDNR = LPO_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = ADDLSG
searchResult := searchResult "LEFT JOIN LSG_LIEFERSCHEINGRUPPEN.LSG_LIEFERSCHEINGRUPPEN ON LSG_FIRMENNR = ADD_LSG_FIRMENNR AND LSG_LFSHAUPTGRUPPE = ADD_LSG_LFSHAUPTGRUPPE AND LSG_LFSUNTERGRUPPE = ADD_LSG_LFSUNTERGRUPPE" "~"
if joinKuerzel = LSGADD
searchResult := searchResult "LEFT JOIN BUERO.ADD_AUSDRUCKMODELLDETAIL ON ADD_LSG_FIRMENNR = LSG_FIRMENNR AND ADD_LSG_LFSHAUPTGRUPPE = LSG_LFSHAUPTGRUPPE AND ADD_LSG_LFSUNTERGRUPPE = LSG_LFSUNTERGRUPPE" "~"
if joinKuerzel = EVTLTX
searchResult := searchResult "LEFT JOIN BUERO.LTX_LANGTEXT ON LTX_LANGTXTLFDNR = EVT_EQU_LANGTXTLFDNR" "~"
if joinKuerzel = LTXEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_EQU_LANGTXTLFDNR = LTX_LANGTXTLFDNR" "~"
if joinKuerzel = FSTLTX
searchResult := searchResult "LEFT JOIN BUERO.LTX_LANGTEXT ON LTX_LANGTXTLFDNR = FST_LANGTXTLFDNR" "~"
if joinKuerzel = LTXFST
searchResult := searchResult "LEFT JOIN BUERO.FST_FRIST ON FST_LANGTXTLFDNR = LTX_LANGTXTLFDNR" "~"
if joinKuerzel = ZASLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = ZAS_LANDNR" "~"
if joinKuerzel = LANZAS
searchResult := searchResult "LEFT JOIN BUERO.ZAS_ZAHLSCHEIN ON ZAS_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = AVLLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = AVL_LAG_FIRMENNR AND LAP_LAGERNR = AVL_LAG_LAGERNR AND LAP_LAGERPLATZ = AVL_LAP_LAGERPLATZ" "~"
if joinKuerzel = LAPAVL
searchResult := searchResult "LEFT JOIN WWS.AVL_AUFTRAGSVORERFLIEFDAT ON AVL_LAG_FIRMENNR = LAP_FIRMENNR AND AVL_LAG_LAGERNR = LAP_LAGERNR AND AVL_LAP_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = IVPLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = IVP_FIRMENNR AND LAP_LAGERNR = IVP_LAGERNR AND LAP_LAGERPLATZ = IVP_LAGERPLATZ" "~"
if joinKuerzel = LAPIVP
searchResult := searchResult "LEFT JOIN WWS.IVP_INVENTURPOSITION ON IVP_FIRMENNR = LAP_FIRMENNR AND IVP_LAGERNR = LAP_LAGERNR AND IVP_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = LAGLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = LAG_FIRMENNR AND LAP_LAGERNR = LAG_LAGERNR AND LAP_LAGERPLATZ = LAG_WARENANN_LAGERPLATZ" "~"
if joinKuerzel = LAPLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = LAP_FIRMENNR AND LAG_LAGERNR = LAP_LAGERNR AND LAG_WARENANN_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = LMBLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = LMB_FIRMENNR AND LAP_LAGERNR = LMB_LAGERNR AND LAP_LAGERPLATZ = LMB_LAGERPLATZ" "~"
if joinKuerzel = LAPLMB
searchResult := searchResult "LEFT JOIN WWS.LMB_LAGERMINMAXBESTAND ON LMB_FIRMENNR = LAP_FIRMENNR AND LMB_LAGERNR = LAP_LAGERNR AND LMB_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = PLCLAP
searchResult := searchResult "LEFT JOIN LAP_LAGERPLATZ.LAP_LAGERPLATZ ON LAP_FIRMENNR = PLC_LAG_FIRMENNR AND LAP_LAGERNR = PLC_LAG_LAGERNR AND LAP_LAGERPLATZ = PLC_LAG_LAGERPLATZ" "~"
if joinKuerzel = LAPPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_LAG_FIRMENNR = LAP_FIRMENNR AND PLC_LAG_LAGERNR = LAP_LAGERNR AND PLC_LAG_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = TLTLAP
searchResult := searchResult "LEFT JOIN LAP_LAGERPLATZ.LAP_LAGERPLATZ ON LAP_FIRMENNR = TLT_TRN_LAG_FIRMENNR AND LAP_LAGERNR = TLT_TRN_LAGERNR AND LAP_LAGERPLATZ = TLT_TRN_LAGERPLATZ" "~"
if joinKuerzel = LAPTLT
searchResult := searchResult "LEFT JOIN PRODUKTION.TLT_BT_LOGISTIK_TRANSPORT ON TLT_TRN_LAG_FIRMENNR = LAP_FIRMENNR AND TLT_TRN_LAGERNR = LAP_LAGERNR AND TLT_TRN_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = TRJLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = TRJ_FIRMENNR AND LAP_LAGERNR = TRJ_UMB_LAGERNR AND LAP_LAGERPLATZ = TRJ_UMB_LAGERPLATZ" "~"
if joinKuerzel = LAPTRJ
searchResult := searchResult "LEFT JOIN WWS.TRJ_TRANSPORTEINH_JOURNAL ON TRJ_FIRMENNR = LAP_FIRMENNR AND TRJ_UMB_LAGERNR = LAP_LAGERNR AND TRJ_UMB_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = HAKLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = HAK_LOHNARTID" "~"
if joinKuerzel = LAQHAK
searchResult := searchResult "LEFT JOIN HRS.HAK_AKONTO ON HAK_LOHNARTID = LAQ_LOHNARTID" "~"
if joinKuerzel = LAKLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = LAK_BASIS_LOHNARTID" "~"
if joinKuerzel = LAQLAK
searchResult := searchResult "LEFT JOIN HRS.LAK_LOHNARTENKOPPELUNG ON LAK_BASIS_LOHNARTID = LAQ_LOHNARTID" "~"
if joinKuerzel = LNSLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = LNS_INPUTLAID" "~"
if joinKuerzel = LAQLNS
searchResult := searchResult "LEFT JOIN HRS.LNS_LOHNSCHABLONENDETAILS ON LNS_INPUTLAID = LAQ_LOHNARTID" "~"
if joinKuerzel = LOZLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = LOZ_LOHNARTID" "~"
if joinKuerzel = LAQLOZ
searchResult := searchResult "LEFT JOIN HRS.LOZ_LOHNERFASSUNGZLE ON LOZ_LOHNARTID = LAQ_LOHNARTID" "~"
if joinKuerzel = SZULAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = SZU_LOHNARTID" "~"
if joinKuerzel = LAQSZU
searchResult := searchResult "LEFT JOIN HRS.SZU_SCHNITTLAZU ON SZU_LOHNARTID = LAQ_LOHNARTID" "~"
if joinKuerzel = LAWLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = LAW_LS_ADD_LA_1" "~"
if joinKuerzel = LAQLAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_LS_ADD_LA_1 = LAQ_LOHNARTID" "~"
if joinKuerzel = LAWLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = LAW_LS_ADD_LA_2" "~"
if joinKuerzel = LAQLAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_LS_ADD_LA_2 = LAQ_LOHNARTID" "~"
if joinKuerzel = LNSLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = LNS_P68_2LAID" "~"
if joinKuerzel = LAQLNS
searchResult := searchResult "LEFT JOIN HRS.LNS_LOHNSCHABLONENDETAILS ON LNS_P68_2LAID = LAQ_LOHNARTID" "~"
if joinKuerzel = LAWLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = LAW_LS_ADD_LA_3" "~"
if joinKuerzel = LAQLAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_LS_ADD_LA_3 = LAQ_LOHNARTID" "~"
if joinKuerzel = LNSLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = LNS_GRUNDLOHNID" "~"
if joinKuerzel = LAQLNS
searchResult := searchResult "LEFT JOIN HRS.LNS_LOHNSCHABLONENDETAILS ON LNS_GRUNDLOHNID = LAQ_LOHNARTID" "~"
if joinKuerzel = LAWLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = LAW_LK_ADD_LA_3" "~"
if joinKuerzel = LAQLAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_LK_ADD_LA_3 = LAQ_LOHNARTID" "~"
if joinKuerzel = LAWLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = LAW_LK_ADD_LA_4" "~"
if joinKuerzel = LAQLAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_LK_ADD_LA_4 = LAQ_LOHNARTID" "~"
if joinKuerzel = PIALAT
searchResult := searchResult "LEFT JOIN WWS.LAT_LIEFERANTENARTIKEL ON LAT_LIEFERANTENARTIKELID = PIA_LIEFERANTENARTIKELID" "~"
if joinKuerzel = LATPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_LIEFERANTENARTIKELID = LAT_LIEFERANTENARTIKELID" "~"
if joinKuerzel = WEJLBK
searchResult := searchResult "LEFT JOIN WWS.LBK_LAGERBUCHKENNZ ON LBK_FIRMENNR = WEJ_LBK_FIRMENNR AND LBK_LAGERBUCHKENNZ = WEJ_LAGERBUCHKENNZ" "~"
if joinKuerzel = LBKWEJ
searchResult := searchResult "LEFT JOIN WWS.WEJ_WARENEINSATZLAJ ON WEJ_LBK_FIRMENNR = LBK_FIRMENNR AND WEJ_LAGERBUCHKENNZ = LBK_LAGERBUCHKENNZ" "~"
if joinKuerzel = WAGKST
searchResult := searchResult "LEFT JOIN KST_KORESTAMM.KST_KORESTAMM ON KST_KOSTENSTAMMNR = WAG_DIM_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_DIM_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = WEBKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = WEB_KMG_FIRMENNR AND KST_KORENR = WEB_KORENR AND KST_KOSTENSTAMMNR = WEB_AUFW_ABT_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTWEB
searchResult := searchResult "LEFT JOIN WWS.WEB_WARENEINSATZBUCH ON WEB_KMG_FIRMENNR = KST_FIRMENNR AND WEB_KORENR = KST_KORENR AND WEB_AUFW_ABT_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = WEBKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = WEB_KMG_FIRMENNR AND KST_KORENR = WEB_KORENR AND KST_KOSTENSTAMMNR = WEB_AUFW_DIM_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTWEB
searchResult := searchResult "LEFT JOIN WWS.WEB_WARENEINSATZBUCH ON WEB_KMG_FIRMENNR = KST_FIRMENNR AND WEB_KORENR = KST_KORENR AND WEB_AUFW_DIM_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = WEBKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = WEB_KMG_FIRMENNR AND KST_KORENR = WEB_KORENR AND KST_KOSTENSTAMMNR = WEB_BEST_GBR_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTWEB
searchResult := searchResult "LEFT JOIN WWS.WEB_WARENEINSATZBUCH ON WEB_KMG_FIRMENNR = KST_FIRMENNR AND WEB_KORENR = KST_KORENR AND WEB_BEST_GBR_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = KSEKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = KSE_FIRMENNR AND KST_KORENR = KSE_KORENR AND KST_KOSTENTYP = KSE_DIM_KOSTENTYP AND KST_KOSTENSTAMMNR = KSE_DIM_KOSTENSTAMMNR AND KST_KNOTENREFNR = KSE_DIM_KNOTENREFNR" "~"
if joinKuerzel = KSTKSE
searchResult := searchResult "LEFT JOIN KORE.KSE_KORESCHLUESSELEINGABE ON KSE_FIRMENNR = KST_FIRMENNR AND KSE_KORENR = KST_KORENR AND KSE_DIM_KOSTENTYP = KST_KOSTENTYP AND KSE_DIM_KOSTENSTAMMNR = KST_KOSTENSTAMMNR AND KSE_DIM_KNOTENREFNR = KST_KNOTENREFNR" "~"
if joinKuerzel = ADWKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = ADW_STEUERFIRMENNR AND KTO_FIBUNR = ADW_STEUERFIBUNR AND KTO_KONTONR = ADW_STEUERKONTONR" "~"
if joinKuerzel = KTOADW
searchResult := searchResult "LEFT JOIN WWS.ADW_ABGABENDETAILWAP ON ADW_STEUERFIRMENNR = KTO_FIRMENNR AND ADW_STEUERFIBUNR = KTO_FIBUNR AND ADW_STEUERKONTONR = KTO_KONTONR" "~"
if joinKuerzel = KKKKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = KKK_FIRMENNR AND KTO_FIBUNR = KKK_FIBUNR AND KTO_KONTONR = KKK_KONTONR" "~"
if joinKuerzel = KTOKKK
searchResult := searchResult "LEFT JOIN WWS.KKK_KREDITKARTENKONTENZU ON KKK_FIRMENNR = KTO_FIRMENNR AND KKK_FIBUNR = KTO_FIBUNR AND KKK_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = KPKKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = KPK_FIRMENNR AND KTO_FIBUNR = KPK_FIBUNR AND KTO_KONTONR = KPK_KONTONR" "~"
if joinKuerzel = KTOKPK
searchResult := searchResult "LEFT JOIN KORE.KPK_PLANKENNZAHLEN ON KPK_FIRMENNR = KTO_FIRMENNR AND KPK_FIBUNR = KTO_FIBUNR AND KPK_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = UMIKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = UMI_FIRMENNR AND KTO_FIBUNR = UMI_FIBUNR AND KTO_KONTONR = UMI_KONTONR" "~"
if joinKuerzel = KTOUMI
searchResult := searchResult "LEFT JOIN KORE.UMI_UMLAGEISTMENGE ON UMI_FIRMENNR = KTO_FIRMENNR AND UMI_FIBUNR = KTO_FIBUNR AND UMI_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = VKPKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = VKP_FIRMENNR AND KTO_FIBUNR = VKP_FIBUNR AND KTO_KONTONR = VKP_KUERZUNG_KTO_KLASSE4" "~"
if joinKuerzel = KTOVKP
searchResult := searchResult "LEFT JOIN FIBU.VKP_VSTKPROJEKT ON VKP_FIRMENNR = KTO_FIRMENNR AND VKP_FIBUNR = KTO_FIBUNR AND VKP_KUERZUNG_KTO_KLASSE4 = KTO_KONTONR" "~"
if joinKuerzel = WEBKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = WEB_FWF_FIRMENNR AND KTO_FIBUNR = WEB_FIBUNR AND KTO_KONTONR = WEB_BESTANDSKONTO" "~"
if joinKuerzel = KTOWEB
searchResult := searchResult "LEFT JOIN WWS.WEB_WARENEINSATZBUCH ON WEB_FWF_FIRMENNR = KTO_FIRMENNR AND WEB_FIBUNR = KTO_FIBUNR AND WEB_BESTANDSKONTO = KTO_KONTONR" "~"
if joinKuerzel = YBUKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = YBU_FIRMENNR AND KTO_FIBUNR = YBU_FIBUNR AND KTO_KONTONR = YBU_KONTONR" "~"
if joinKuerzel = KTOYBU
searchResult := searchResult "LEFT JOIN FIBU.YBU_AUTOBUCHUNG ON YBU_FIRMENNR = KTO_FIRMENNR AND YBU_FIBUNR = KTO_FIBUNR AND YBU_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = KSEKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = KSE_FIRMENNR AND KTO_FIBUNR = KSE_BASISKONTO_FIBUNR AND KTO_KONTONR = KSE_BELASTUNGSKONTO" "~"
if joinKuerzel = KTOKSE
searchResult := searchResult "LEFT JOIN KORE.KSE_KORESCHLUESSELEINGABE ON KSE_FIRMENNR = KTO_FIRMENNR AND KSE_BASISKONTO_FIBUNR = KTO_FIBUNR AND KSE_BELASTUNGSKONTO = KTO_KONTONR" "~"
if joinKuerzel = AMZKUG
searchResult := searchResult "LEFT JOIN BUERO.KUG_KUNDENGRUPPE ON KUG_FIRMENNR = AMZ_KUNDENGRUPPEN_FIRMENNR AND KUG_KUNDENHAUPTGRUPPENNR = AMZ_KUNDENHAUPTGRUPPENNR AND KUG_KUNDENUNTERGRUPPENNR = AMZ_KUNDENUNTERGRUPPENNR" "~"
if joinKuerzel = KUGAMZ
searchResult := searchResult "LEFT JOIN BUERO.AMZ_AUSDRUCKMODELLZU ON AMZ_KUNDENGRUPPEN_FIRMENNR = KUG_FIRMENNR AND AMZ_KUNDENHAUPTGRUPPENNR = KUG_KUNDENHAUPTGRUPPENNR AND AMZ_KUNDENUNTERGRUPPENNR = KUG_KUNDENUNTERGRUPPENNR" "~"
if joinKuerzel = MVAKUG
searchResult := searchResult "LEFT JOIN KUG_KUNDENGRUPPE.KUG_KUNDENGRUPPE ON KUG_FIRMENNR = MVA_KUG_FIRMENNR AND KUG_KUNDENHAUPTGRUPPENNR = MVA_KUNDENHAUPTGRUPPENNR AND KUG_KUNDENUNTERGRUPPENNR = MVA_KUNDENUNTERGRUPPENNR" "~"
if joinKuerzel = KUGMVA
searchResult := searchResult "LEFT JOIN FIBU.MVA_MAHNVZINSENAB ON MVA_KUG_FIRMENNR = KUG_FIRMENNR AND MVA_KUNDENHAUPTGRUPPENNR = KUG_KUNDENHAUPTGRUPPENNR AND MVA_KUNDENUNTERGRUPPENNR = KUG_KUNDENUNTERGRUPPENNR" "~"
if joinKuerzel = PIJKUG
searchResult := searchResult "LEFT JOIN KUG_KUNDENGRUPPE.KUG_KUNDENGRUPPE ON KUG_FIRMENNR = PIJ_FIRMENNR AND KUG_KUNDENHAUPTGRUPPENNR = PIJ_KUNDENHAUPTGRNR AND KUG_KUNDENUNTERGRUPPENNR = PIJ_KUNDENUNTERGRNR" "~"
if joinKuerzel = KUGPIJ
searchResult := searchResult "LEFT JOIN WWS.PIJ_PICKUNDENGRZU ON PIJ_FIRMENNR = KUG_FIRMENNR AND PIJ_KUNDENHAUPTGRNR = KUG_KUNDENHAUPTGRUPPENNR AND PIJ_KUNDENUNTERGRNR = KUG_KUNDENUNTERGRUPPENNR" "~"
if joinKuerzel = UMIKUV
searchResult := searchResult "LEFT JOIN KORE.KUV_UMLAGEVARIANTE ON KUV_FIRMENNR = UMI_FIRMENNR AND KUV_KORENR = UMI_KORENR AND KUV_VARIANTE = UMI_VARIANTE" "~"
if joinKuerzel = KUVUMI
searchResult := searchResult "LEFT JOIN KORE.UMI_UMLAGEISTMENGE ON UMI_FIRMENNR = KUV_FIRMENNR AND UMI_KORENR = KUV_KORENR AND UMI_VARIANTE = KUV_VARIANTE" "~"
if joinKuerzel = LNSLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = LNS_SPLITTLAID" "~"
if joinKuerzel = LAQLNS
searchResult := searchResult "LEFT JOIN HRS.LNS_LOHNSCHABLONENDETAILS ON LNS_SPLITTLAID = LAQ_LOHNARTID" "~"
if joinKuerzel = BFILAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = BFI_URLAUBSZUSCHUSS_LOHNART" "~"
if joinKuerzel = LAQBFI
searchResult := searchResult "LEFT JOIN HRS.BFI_BUDGETFIRMA ON BFI_URLAUBSZUSCHUSS_LOHNART = LAQ_LOHNARTID" "~"
if joinKuerzel = EFPLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = EFP_LOHNART_OHNEENTGELT" "~"
if joinKuerzel = LAQEFP
searchResult := searchResult "LEFT JOIN HRS.EFP_ENTGELTFORTZAHLPROZ ON EFP_LOHNART_OHNEENTGELT = LAQ_LOHNARTID" "~"
if joinKuerzel = LAWLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = LAW_LK_ADD_LA_2" "~"
if joinKuerzel = LAQLAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_LK_ADD_LA_2 = LAQ_LOHNARTID" "~"
if joinKuerzel = AMZLBK
searchResult := searchResult "LEFT JOIN LBK_LAGERBUCHKENNZ.LBK_LAGERBUCHKENNZ ON LBK_FIRMENNR = AMZ_LBK_FIRMENNR AND LBK_LAGERBUCHKENNZ = AMZ_LAGERBUCHKENNZ" "~"
if joinKuerzel = LBKAMZ
searchResult := searchResult "LEFT JOIN BUERO.AMZ_AUSDRUCKMODELLZU ON AMZ_LBK_FIRMENNR = LBK_FIRMENNR AND AMZ_LAGERBUCHKENNZ = LBK_LAGERBUCHKENNZ" "~"
if joinKuerzel = LAJLBK
searchResult := searchResult "LEFT JOIN WWS.LBK_LAGERBUCHKENNZ ON LBK_FIRMENNR = LAJ_ARTIKELFIRMENNR AND LBK_LAGERBUCHKENNZ = LAJ_LAGERBUCHKENNZ" "~"
if joinKuerzel = LBKLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_ARTIKELFIRMENNR = LBK_FIRMENNR AND LAJ_LAGERBUCHKENNZ = LBK_LAGERBUCHKENNZ" "~"
if joinKuerzel = LBKLBK
searchResult := searchResult "LEFT JOIN WWS.LBK_LAGERBUCHKENNZ ON LBK_FIRMENNR = LBK_FIRMENNR AND LBK_LAGERBUCHKENNZ = LBK_ZUGEORD_LAGERBUCHKENNZ" "~"
if joinKuerzel = LBKLBK
searchResult := searchResult "LEFT JOIN WWS.LBK_LAGERBUCHKENNZ ON LBK_FIRMENNR = LBK_FIRMENNR AND LBK_ZUGEORD_LAGERBUCHKENNZ = LBK_LAGERBUCHKENNZ" "~"
if joinKuerzel = LDOLEB
searchResult := searchResult "LEFT JOIN BUERO.LEB_LEBENSLAUF ON LEB_LEBENSLAUFID = LDO_LEBENSLAUFID" "~"
if joinKuerzel = LEBLDO
searchResult := searchResult "LEFT JOIN BUERO.LDO_LEBENSLAUFDOKUMENTZU ON LDO_LEBENSLAUFID = LEB_LEBENSLAUFID" "~"
if joinKuerzel = LFKLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_LEISTUNGLFDNR = LFK_LEISTUNGLFDNR" "~"
if joinKuerzel = LEILFK
searchResult := searchResult "LEFT JOIN ZLE.LFK_LEISTFIBKOREZU ON LFK_LEISTUNGLFDNR = LEI_LEISTUNGLFDNR" "~"
if joinKuerzel = LOZLEI
searchResult := searchResult "LEFT JOIN LEI_LEISTUNG.LEI_LEISTUNG ON LEI_LEISTUNGLFDNR = LOZ_LEISTUNGLFDNR" "~"
if joinKuerzel = LEILOZ
searchResult := searchResult "LEFT JOIN HRS.LOZ_LOHNERFASSUNGZLE ON LOZ_LEISTUNGLFDNR = LEI_LEISTUNGLFDNR" "~"
if joinKuerzel = EGMLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = EGM_FIRMENNR AND LFI_LOHNJAHR = EGM_JAHR" "~"
if joinKuerzel = LFIEGM
searchResult := searchResult "LEFT JOIN HRS.EGM_ELDADETAIL_GM ON EGM_FIRMENNR = LFI_FIRMENNR AND EGM_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = EMBLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = EMB_FIRMENNR AND LFI_LOHNJAHR = EMB_JAHR" "~"
if joinKuerzel = LFIEMB
searchResult := searchResult "LEFT JOIN HRS.EMB_ELDADETAIL_MBGM_P ON EMB_FIRMENNR = LFI_FIRMENNR AND EMB_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = EVQLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = EVQ_FIRMENNR AND LFI_LOHNJAHR = EVQ_JAHR" "~"
if joinKuerzel = LFIEVQ
searchResult := searchResult "LEFT JOIN HRS.EVQ_ELDADETAIL_VSNR ON EVQ_FIRMENNR = LFI_FIRMENNR AND EVQ_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = EZTLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = EZT_FIRMENNR AND LFI_LOHNJAHR = EZT_LOHNJAHR" "~"
if joinKuerzel = LFIEZT
searchResult := searchResult "LEFT JOIN HRS.EZT_ELDAZENTRALTABELLE ON EZT_FIRMENNR = LFI_FIRMENNR AND EZT_LOHNJAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = GVTLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = GVT_DVFIRMENNR AND LFI_LOHNJAHR = GVT_JAHR" "~"
if joinKuerzel = LFIGVT
searchResult := searchResult "LEFT JOIN HRS.GVT_GEMVERTEILUNG ON GVT_DVFIRMENNR = LFI_FIRMENNR AND GVT_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = KMZLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = KMZ_FIRMENNR AND LFI_LOHNJAHR = KMZ_JAHR" "~"
if joinKuerzel = LFIKMZ
searchResult := searchResult "LEFT JOIN HRS.KMZ_KOMMSTZENTRALE ON KMZ_FIRMENNR = LFI_FIRMENNR AND KMZ_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = EAFLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = EAF_DVFIRMENNR AND LFI_LOHNJAHR = EAF_JAHR" "~"
if joinKuerzel = LFIEAF
searchResult := searchResult "LEFT JOIN HRS.EAF_ELDADETAIL_AFB ON EAF_DVFIRMENNR = LFI_FIRMENNR AND EAF_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = EDALFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = EDA_DVFIRMENNR AND LFI_LOHNJAHR = EDA_JAHR" "~"
if joinKuerzel = LFIEDA
searchResult := searchResult "LEFT JOIN HRS.EDA_ELDADETAIL_ADR ON EDA_DVFIRMENNR = LFI_FIRMENNR AND EDA_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = EVMLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = EVM_DVFIRMENNR AND LFI_LOHNJAHR = EVM_JAHR" "~"
if joinKuerzel = LFIEVM
searchResult := searchResult "LEFT JOIN HRS.EVM_ELDADETAIL_VM ON EVM_DVFIRMENNR = LFI_FIRMENNR AND EVM_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = EZTLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = EZT_DVFIRMENNR AND LFI_LOHNJAHR = EZT_LOHNJAHR" "~"
if joinKuerzel = LFIEZT
searchResult := searchResult "LEFT JOIN HRS.EZT_ELDAZENTRALTABELLE ON EZT_DVFIRMENNR = LFI_FIRMENNR AND EZT_LOHNJAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = EPVLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_FIRMENNR = EPV_FIRMENNR AND LFS_LIEFERSCHEINLFDNR = EPV_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = LFSEPV
searchResult := searchResult "LEFT JOIN WWS.EPV_ERFASSPOSRECHVOR ON EPV_FIRMENNR = LFS_FIRMENNR AND EPV_LIEFERSCHEINLFDNR = LFS_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = WAZLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_FIRMENNR = WAZ_LFS_FIRMENNR AND LFS_LIEFERSCHEINLFDNR = WAZ_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = LFSWAZ
searchResult := searchResult "LEFT JOIN WWS.WAZ_WARENZUORDNUNG ON WAZ_LFS_FIRMENNR = LFS_FIRMENNR AND WAZ_LIEFERSCHEINLFDNR = LFS_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = SBMMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = SBM_FIRMENNR AND MIT_MITARBEITERID = SBM_MITARBEITERID" "~"
if joinKuerzel = MITSBM
searchResult := searchResult "LEFT JOIN BUERO.SBM_SBSPERSONALMITARBZU ON SBM_FIRMENNR = MIT_FIRMENNR AND SBM_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = SFZMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = SFZ_MIT_FIRMENNR AND MIT_MITARBEITERID = SFZ_MITARBEITERID" "~"
if joinKuerzel = MITSFZ
searchResult := searchResult "LEFT JOIN BUERO.SFZ_SYNCFELDZUORDNUNG ON SFZ_MIT_FIRMENNR = MIT_FIRMENNR AND SFZ_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = SWAMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = SWA_FIRMENNR AND MIT_MITARBEITERID = SWA_MITARBEITERID" "~"
if joinKuerzel = MITSWA
searchResult := searchResult "LEFT JOIN HRS.SWA_SW_ANSPRUCH ON SWA_FIRMENNR = MIT_FIRMENNR AND SWA_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = TPRMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = TPR_MIT_FIRMENNR AND MIT_MITARBEITERID = TPR_MIT_MITARBEITERID" "~"
if joinKuerzel = MITTPR
searchResult := searchResult "LEFT JOIN BUERO.TPR_TELEFONPROTOKOLL ON TPR_MIT_FIRMENNR = MIT_FIRMENNR AND TPR_MIT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = UMFMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = UMF_ABTL_FIRMENNR AND MIT_MITARBEITERID = UMF_ABTL_MITARBEITERID" "~"
if joinKuerzel = MITUMF
searchResult := searchResult "LEFT JOIN BUERO.UMF_UMFRAGE ON UMF_ABTL_FIRMENNR = MIT_FIRMENNR AND UMF_ABTL_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = VASMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = VAS_VR_FIRMENNR AND MIT_MITARBEITERID = VAS_VR_MITARBEITERID" "~"
if joinKuerzel = MITVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_VR_FIRMENNR = MIT_FIRMENNR AND VAS_VR_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = WADMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = WAD_BEARB_FIRMENNR AND MIT_MITARBEITERID = WAD_BEARB_MITARBEITERID" "~"
if joinKuerzel = MITWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_BEARB_FIRMENNR = MIT_FIRMENNR AND WAD_BEARB_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = WUZMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = WUZ_MIT_FIRMENNR AND MIT_MITARBEITERID = WUZ_MITARBEITERID" "~"
if joinKuerzel = MITWUZ
searchResult := searchResult "LEFT JOIN BUERO.WUZ_WUNSCHARBEITSZEIT ON WUZ_MIT_FIRMENNR = MIT_FIRMENNR AND WUZ_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = ZBUMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = ZBU_FIRMENNR AND MIT_MITARBEITERID = ZBU_MITARBEITERID" "~"
if joinKuerzel = MITZBU
searchResult := searchResult "LEFT JOIN BUERO.ZBU_ZEITBUCHUNG ON ZBU_FIRMENNR = MIT_FIRMENNR AND ZBU_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = ZFIMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = ZFI_FIRMENNR AND MIT_MITARBEITERID = ZFI_MITARBEITERID" "~"
if joinKuerzel = MITZFI
searchResult := searchResult "LEFT JOIN BUERO.ZFI_ZEITFEHLERIGNORE ON ZFI_FIRMENNR = MIT_FIRMENNR AND ZFI_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = ZMAMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = ZMA_MIT_FIRMENNR AND MIT_MITARBEITERID = ZMA_MITARBEITERID" "~"
if joinKuerzel = MITZMA
searchResult := searchResult "LEFT JOIN BUERO.ZMA_ZEITBABERECHTMAZUORD ON ZMA_MIT_FIRMENNR = MIT_FIRMENNR AND ZMA_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = ZSWMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = ZSW_FIRMENNR AND MIT_MITARBEITERID = ZSW_MITARBEITERID" "~"
if joinKuerzel = MITZSW
searchResult := searchResult "LEFT JOIN BUERO.ZSW_ZEITSUPERWE ON ZSW_FIRMENNR = MIT_FIRMENNR AND ZSW_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = ZUHMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = ZUH_MIT_FIRMENNR AND MIT_MITARBEITERID = ZUH_MITARBEITERID" "~"
if joinKuerzel = MITZUH
searchResult := searchResult "LEFT JOIN BUERO.ZUH_ZEITUMBUCHHIST ON ZUH_MIT_FIRMENNR = MIT_FIRMENNR AND ZUH_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = ALEMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = ALE_AL_FIRMENNR AND MIT_MITARBEITERID = ALE_AL_MITARBEITERID" "~"
if joinKuerzel = MITALE
searchResult := searchResult "LEFT JOIN BUERO.ALE_ADRESSENLISTE ON ALE_AL_FIRMENNR = MIT_FIRMENNR AND ALE_AL_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = DVHMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = DVH_GF_FIRMENNR AND MIT_MITARBEITERID = DVH_GF_MITARBEITERID" "~"
if joinKuerzel = MITDVH
searchResult := searchResult "LEFT JOIN BUERO.DVH_DIENSTVERHAELTNIS ON DVH_GF_FIRMENNR = MIT_FIRMENNR AND DVH_GF_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = KMAMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = KMA_SACHB_FIRMENNR AND MIT_MITARBEITERID = KMA_SACHB_MITARBEITERID" "~"
if joinKuerzel = MITKMA
searchResult := searchResult "LEFT JOIN WWS.KMA_KUNDENMITARBBEZUG ON KMA_SACHB_FIRMENNR = MIT_FIRMENNR AND KMA_SACHB_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = PERMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = PER_GF_FIRMENNR AND MIT_MITARBEITERID = PER_GF_MITARBEITERID" "~"
if joinKuerzel = MITPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_GF_FIRMENNR = MIT_FIRMENNR AND PER_GF_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = QUOMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = QUO_GF_FIRMENNR AND MIT_MITARBEITERID = QUO_GF_MITARBEITERID" "~"
if joinKuerzel = MITQUO
searchResult := searchResult "LEFT JOIN BUERO.QUO_QUOTE ON QUO_GF_FIRMENNR = MIT_FIRMENNR AND QUO_GF_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = WIPMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = WIP_MIT_STV_FIRMENNR AND MIT_MITARBEITERID = WIP_MIT_STV_MITARBEITERID" "~"
if joinKuerzel = MITWIP
searchResult := searchResult "LEFT JOIN BIL.WIP_WIPRUEFUNG ON WIP_MIT_STV_FIRMENNR = MIT_FIRMENNR AND WIP_MIT_STV_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = DRGMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = DRG_GENEHMIGT_VON_FIRMENNR AND MIT_MITARBEITERID = DRG_GENEHMIGT_VON" "~"
if joinKuerzel = MITDRG
searchResult := searchResult "LEFT JOIN HRS.DRG_DIENSTREISEGEN ON DRG_GENEHMIGT_VON_FIRMENNR = MIT_FIRMENNR AND DRG_GENEHMIGT_VON = MIT_MITARBEITERID" "~"
if joinKuerzel = KSZLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = KSZ_FIRMENNR AND LFI_LOHNJAHR = KSZ_JAHR" "~"
if joinKuerzel = LFIKSZ
searchResult := searchResult "LEFT JOIN HRS.KSZ_BBKALKSZ ON KSZ_FIRMENNR = LFI_FIRMENNR AND KSZ_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = LITLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = LIT_FIRMENNR AND LFI_LOHNJAHR = LIT_LOHNJAHR" "~"
if joinKuerzel = LFILIT
searchResult := searchResult "LEFT JOIN HRS.LIT_LOHNINSTITUTION ON LIT_FIRMENNR = LFI_FIRMENNR AND LIT_LOHNJAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = LPELFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = LPE_FIRMENNR AND LFI_LOHNJAHR = LPE_LOHNJAHR" "~"
if joinKuerzel = LFILPE
searchResult := searchResult "LEFT JOIN HRS.LPE_PERSON ON LPE_FIRMENNR = LFI_FIRMENNR AND LPE_LOHNJAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = LZFLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = LZF_DVFIRMENNR AND LFI_LOHNJAHR = LZF_JALZ" "~"
if joinKuerzel = LFILZF
searchResult := searchResult "LEFT JOIN HRS.LZF_LOHNZETTEL_FIN ON LZF_DVFIRMENNR = LFI_FIRMENNR AND LZF_JALZ = LFI_LOHNJAHR" "~"
if joinKuerzel = MBMLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = MBM_DVFIRMENNR AND LFI_LOHNJAHR = MBM_JAHR" "~"
if joinKuerzel = LFIMBM
searchResult := searchResult "LEFT JOIN HRS.MBM_MBGM ON MBM_DVFIRMENNR = LFI_FIRMENNR AND MBM_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = MIVLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = MIV_FIRMENNR AND LFI_LOHNJAHR = MIV_LOHNJAHR" "~"
if joinKuerzel = LFIMIV
searchResult := searchResult "LEFT JOIN HRS.MIV_MITARBVORSCHLAG ON MIV_FIRMENNR = LFI_FIRMENNR AND MIV_LOHNJAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = EM2LFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = EM2_DVFIRMENNR AND LFI_LOHNJAHR = EM2_JAHR" "~"
if joinKuerzel = LFIEM2
searchResult := searchResult "LEFT JOIN HRS.EM2_ELDADETAIL_MBGM ON EM2_DVFIRMENNR = LFI_FIRMENNR AND EM2_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = EFKLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_FIRMENNR = EFK_FIRMENNR AND LFS_LIEFERSCHEINLFDNR = EFK_LETZTE_LFSLFDNR" "~"
if joinKuerzel = LFSEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = LFS_FIRMENNR AND EFK_LETZTE_LFSLFDNR = LFS_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = KOWLGR
searchResult := searchResult "LEFT JOIN HRS.LGR_LOHNARTGRUPPE ON LGR_LGRID = KOW_LGRID_ARBANG AND LGR_JAHR = KOW_LGR_JAHR AND LGR_DBSNR = KOW_DATENBESTANDNR" "~"
if joinKuerzel = LGRKOW
searchResult := searchResult "LEFT JOIN HRS.KOW_KOLLEKTIVVERTRAGWERTE ON KOW_LGRID_ARBANG = LGR_LGRID AND KOW_LGR_JAHR = LGR_JAHR AND KOW_DATENBESTANDNR = LGR_DBSNR" "~"
if joinKuerzel = EFKLIB
searchResult := searchResult "LEFT JOIN WWS.LIB_LIEFERBEDINGUNG ON LIB_LIEFERBNR = EFK_LIEFERBNR" "~"
if joinKuerzel = LIBEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_LIEFERBNR = LIB_LIEFERBNR" "~"
if joinKuerzel = LFSLIB
searchResult := searchResult "LEFT JOIN WWS.LIB_LIEFERBEDINGUNG ON LIB_LIEFERBNR = LFS_LIEFERBNR" "~"
if joinKuerzel = LIBLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_LIEFERBNR = LIB_LIEFERBNR" "~"
if joinKuerzel = AGBLIN
searchResult := searchResult "LEFT JOIN LIN_PROD_LINIE.LIN_PROD_LINIE ON LIN_FIRMENNR = AGB_LIN_FIRMENNR AND LIN_LINIE = AGB_LINIE" "~"
if joinKuerzel = LINAGB
searchResult := searchResult "LEFT JOIN WWS.AGB_ARBEITSGANGBAZZU ON AGB_LIN_FIRMENNR = LIN_FIRMENNR AND AGB_LINIE = LIN_LINIE" "~"
if joinKuerzel = APLLIN
searchResult := searchResult "LEFT JOIN LIN_PROD_LINIE.LIN_PROD_LINIE ON LIN_FIRMENNR = APL_FIRMENNR AND LIN_LINIE = APL_LINIE" "~"
if joinKuerzel = LINAPL
searchResult := searchResult "LEFT JOIN WWS.APL_ARBEITSPLANLINIEZU ON APL_FIRMENNR = LIN_FIRMENNR AND APL_LINIE = LIN_LINIE" "~"
if joinKuerzel = IVPLKO
searchResult := searchResult "LEFT JOIN WWS.LKO_LAGERKONTO ON LKO_FIRMENNR = IVP_FIRMENNR AND LKO_LAGERKONTOLFDNR = IVP_LAGERKONTOLFDNR" "~"
if joinKuerzel = LKOIVP
searchResult := searchResult "LEFT JOIN WWS.IVP_INVENTURPOSITION ON IVP_FIRMENNR = LKO_FIRMENNR AND IVP_LAGERKONTOLFDNR = LKO_LAGERKONTOLFDNR" "~"
if joinKuerzel = WAZLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_FIRMENNR = WAZ_LFS_FIRMENNR AND LPO_LIEFERSCHEINLFDNR = WAZ_LIEFERSCHEINLFDNR AND LPO_LIEFERSCHEINPOSLFDNR = WAZ_LPO_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = LPOWAZ
searchResult := searchResult "LEFT JOIN WWS.WAZ_WARENZUORDNUNG ON WAZ_LFS_FIRMENNR = LPO_FIRMENNR AND WAZ_LIEFERSCHEINLFDNR = LPO_LIEFERSCHEINLFDNR AND WAZ_LPO_LIEFERSCHEINPOSLFDNR = LPO_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = WKLLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_FIRMENNR = WKL_FIRMENNR AND LPO_LIEFERSCHEINLFDNR = WKL_LIEFERSCHEINLFDNR AND LPO_LIEFERSCHEINPOSLFDNR = WKL_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = LPOWKL
searchResult := searchResult "LEFT JOIN WWS.WKL_WARENAUSGKOSTENLIEF ON WKL_FIRMENNR = LPO_FIRMENNR AND WKL_LIEFERSCHEINLFDNR = LPO_LIEFERSCHEINLFDNR AND WKL_LIEFERSCHEINPOSLFDNR = LPO_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = LSDLSP
searchResult := searchResult "LEFT JOIN HRS.LSP_LOHNSTAPEL ON LSP_DBSNR = LSD_DBSNR AND LSP_LOHNJAHR = LSD_LOHNJAHR AND LSP_LSPNR = LSD_LSPNR" "~"
if joinKuerzel = LSPLSD
searchResult := searchResult "LEFT JOIN HRS.LSD_LOHNSTAPEL_DETAIL ON LSD_DBSNR = LSP_DBSNR AND LSD_LOHNJAHR = LSP_LOHNJAHR AND LSD_LSPNR = LSP_LSPNR" "~"
if joinKuerzel = MEVLTX
searchResult := searchResult "LEFT JOIN BUERO.LTX_LANGTEXT ON LTX_LANGTXTLFDNR = MEV_LANGTXTLFDNR" "~"
if joinKuerzel = LTXMEV
searchResult := searchResult "LEFT JOIN BUERO.MEV_MEDIENVERLEIH ON MEV_LANGTXTLFDNR = LTX_LANGTXTLFDNR" "~"
if joinKuerzel = LVZLVK
searchResult := searchResult "LEFT JOIN BMDINTERN.LVK_LIZENZVKP ON LVK_LVKID = LVZ_LVKID" "~"
if joinKuerzel = LVKLVZ
searchResult := searchResult "LEFT JOIN BMDINTERN.LVZ_LIZENZVKPZU ON LVZ_LVKID = LVK_LVKID" "~"
if joinKuerzel = AWSMED
searchResult := searchResult "LEFT JOIN MED_MEDIEN.MED_MEDIEN ON MED_MEDIENID = AWS_MED_MEDIENID" "~"
if joinKuerzel = MEDAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_MED_MEDIENID = MED_MEDIENID" "~"
if joinKuerzel = WEKMED
searchResult := searchResult "LEFT JOIN MED_MEDIEN.MED_MEDIEN ON MED_MEDIENID = WEK_MED_MEDIENID" "~"
if joinKuerzel = MEDWEK
searchResult := searchResult "LEFT JOIN PRODUKTION.WEK_WERKZEUG ON WEK_MED_MEDIENID = MED_MEDIENID" "~"
if joinKuerzel = TRZMEV
searchResult := searchResult "LEFT JOIN BUERO.MEV_MEDIENVERLEIH ON MEV_MEDIENVERLEIHLFDNR = TRZ_MEV_LFDNR" "~"
if joinKuerzel = MEVTRZ
searchResult := searchResult "LEFT JOIN BUERO.TRZ_TERMINRESSOURCENZU ON TRZ_MEV_LFDNR = MEV_MEDIENVERLEIHLFDNR" "~"
if joinKuerzel = ERZLIB
searchResult := searchResult "LEFT JOIN WWS.LIB_LIEFERBEDINGUNG ON LIB_LIEFERBNR = ERZ_LIEFERBNR" "~"
if joinKuerzel = LIBERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_LIEFERBNR = LIB_LIEFERBNR" "~"
if joinKuerzel = PLCLIN
searchResult := searchResult "LEFT JOIN PRODUKTION.LIN_PROD_LINIE ON LIN_FIRMENNR = PLC_LIN_FIRMENNR AND LIN_LINIE = PLC_LINIE" "~"
if joinKuerzel = LINPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_LIN_FIRMENNR = LIN_FIRMENNR AND PLC_LINIE = LIN_LINIE" "~"
if joinKuerzel = LAJLKO
searchResult := searchResult "LEFT JOIN WWS.LKO_LAGERKONTO ON LKO_FIRMENNR = LAJ_FIRMENNR AND LKO_LAGERKONTOLFDNR = LAJ_LAGERKONTOLFDNR" "~"
if joinKuerzel = LKOLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_FIRMENNR = LKO_FIRMENNR AND LAJ_LAGERKONTOLFDNR = LKO_LAGERKONTOLFDNR" "~"
if joinKuerzel = LKMLKO
searchResult := searchResult "LEFT JOIN WWS.LKO_LAGERKONTO ON LKO_FIRMENNR = LKM_FIRMENNR AND LKO_LAGERKONTOLFDNR = LKM_LAGERKONTOLFDNR" "~"
if joinKuerzel = LKOLKM
searchResult := searchResult "LEFT JOIN WWS.LKM_LAGERKONTOMONATSWERTE ON LKM_FIRMENNR = LKO_FIRMENNR AND LKM_LAGERKONTOLFDNR = LKO_LAGERKONTOLFDNR" "~"
if joinKuerzel = LOPLOK
searchResult := searchResult "LEFT JOIN PRODUKTION.LOK_LOGIKTABELLE_KOPF ON LOK_FIRMENNR = LOP_FIRMENNR AND LOK_LOG_NR = LOP_LOG_NR" "~"
if joinKuerzel = LOKLOP
searchResult := searchResult "LEFT JOIN PRODUKTION.LOP_LOGIKTABELLE_POS ON LOP_FIRMENNR = LOK_FIRMENNR AND LOP_LOG_NR = LOK_LOG_NR" "~"
if joinKuerzel = GEPLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_FIRMENNR = GEP_LFS_FIRMENNR AND LPO_LIEFERSCHEINLFDNR = GEP_LIEFERSCHEINLFDNR AND LPO_LIEFERSCHEINPOSLFDNR = GEP_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = LPOGEP
searchResult := searchResult "LEFT JOIN WWS.GEP_GEFAHRENGUTPOS ON GEP_LFS_FIRMENNR = LPO_FIRMENNR AND GEP_LIEFERSCHEINLFDNR = LPO_LIEFERSCHEINLFDNR AND GEP_LIEFERSCHEINPOSLFDNR = LPO_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = MEDLTX
searchResult := searchResult "LEFT JOIN BUERO.LTX_LANGTEXT ON LTX_LANGTXTLFDNR = MED_LANGTXTLFDNR" "~"
if joinKuerzel = LTXMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_LANGTXTLFDNR = LTX_LANGTXTLFDNR" "~"
if joinKuerzel = POSLTX
searchResult := searchResult "LEFT JOIN BUERO.LTX_LANGTEXT ON LTX_LANGTXTLFDNR = POS_LANGTXTLFDNR" "~"
if joinKuerzel = LTXPOS
searchResult := searchResult "LEFT JOIN BUERO.POS_POST ON POS_LANGTXTLFDNR = LTX_LANGTXTLFDNR" "~"
if joinKuerzel = TPRLTX
searchResult := searchResult "LEFT JOIN BUERO.LTX_LANGTEXT ON LTX_LANGTXTLFDNR = TPR_LANGTXTLFDNR" "~"
if joinKuerzel = LTXTPR
searchResult := searchResult "LEFT JOIN BUERO.TPR_TELEFONPROTOKOLL ON TPR_LANGTXTLFDNR = LTX_LANGTXTLFDNR" "~"
if joinKuerzel = VASLTX
searchResult := searchResult "LEFT JOIN BUERO.LTX_LANGTEXT ON LTX_LANGTXTLFDNR = VAS_TEI_LANGTXTLFDNR" "~"
if joinKuerzel = LTXVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_TEI_LANGTXTLFDNR = LTX_LANGTXTLFDNR" "~"
if joinKuerzel = LUELUP
searchResult := searchResult "LEFT JOIN PRODUKTION.LUP_LAGER_UMS_POS ON LUP_FIRMENNR = LUE_FIRMENNR AND LUP_LU_NR = LUE_LU_NR AND LUP_POS_SEQ = LUE_POS_SEQ" "~"
if joinKuerzel = LUPLUE
searchResult := searchResult "LEFT JOIN PRODUKTION.LUE_LAGER_UMS_EMPFANGEN ON LUE_FIRMENNR = LUP_FIRMENNR AND LUE_LU_NR = LUP_LU_NR AND LUE_POS_SEQ = LUP_POS_SEQ" "~"
if joinKuerzel = MABMAG
searchResult := searchResult "LEFT JOIN BUERO.MAG_MITARBGESPR ON MAG_MAGID = MAB_MAG_MAGID" "~"
if joinKuerzel = MAGMAB
searchResult := searchResult "LEFT JOIN BUERO.MAB_MITARBAUSBILDUNG ON MAB_MAG_MAGID = MAG_MAGID" "~"
if joinKuerzel = MMZMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_MEDIENID = MMZ_MED_MEDIENID" "~"
if joinKuerzel = MEDMMZ
searchResult := searchResult "LEFT JOIN BUERO.MMZ_MAGBETRIEBSMITTELZU ON MMZ_MED_MEDIENID = MED_MEDIENID" "~"
if joinKuerzel = MIZMEI
searchResult := searchResult "LEFT JOIN BUERO.MEI_MEDIENINFO ON MEI_FIRMENNR = MIZ_FIRMENNR AND MEI_MEDIENINFOKZ = MIZ_MEDIENINFOKZ AND MEI_MEDIENINFOLFDNR = MIZ_MEDIENINFOLFDNR" "~"
if joinKuerzel = MEIMIZ
searchResult := searchResult "LEFT JOIN BUERO.MIZ_MEDIENINFOZU ON MIZ_FIRMENNR = MEI_FIRMENNR AND MIZ_MEDIENINFOKZ = MEI_MEDIENINFOKZ AND MIZ_MEDIENINFOLFDNR = MEI_MEDIENINFOLFDNR" "~"
if joinKuerzel = LEIMIK
searchResult := searchResult "LEFT JOIN MIK_MITARBEITERKARRIERE.MIK_MITARBEITERKARRIERE ON MIK_FIRMENNR = LEI_MIK_FIRMENNR AND MIK_KARRIERELFDNR = LEI_KARRIERELFDNR" "~"
if joinKuerzel = MIKLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_MIK_FIRMENNR = MIK_FIRMENNR AND LEI_KARRIERELFDNR = MIK_KARRIERELFDNR" "~"
if joinKuerzel = AKZMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = AKZ_V1_FIRMENNR AND MIT_MITARBEITERID = AKZ_V1_MITARBEITERID" "~"
if joinKuerzel = MITAKZ
searchResult := searchResult "LEFT JOIN WWS.AKZ_ARTIKELKUNDENZU ON AKZ_V1_FIRMENNR = MIT_FIRMENNR AND AKZ_V1_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = BPKMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = BPK_BEARB_FIRMENNR AND MIT_MITARBEITERID = BPK_BEARB_MITARBEITERID" "~"
if joinKuerzel = MITBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_BEARB_FIRMENNR = MIT_FIRMENNR AND BPK_BEARB_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = BZBMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = BZB_MIT_FIRMENNR AND MIT_MITARBEITERID = BZB_MIT_MITARBEITERID" "~"
if joinKuerzel = MITBZB
searchResult := searchResult "LEFT JOIN PRODUKTION.BZB_BAZZEITBUCHUNG ON BZB_MIT_FIRMENNR = MIT_FIRMENNR AND BZB_MIT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = EFKMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = EFK_V1_FIRMENNR AND MIT_MITARBEITERID = EFK_V1_MITARBEITERID" "~"
if joinKuerzel = MITEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_V1_FIRMENNR = MIT_FIRMENNR AND EFK_V1_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = ERBMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = ERB_MIT_FIRMENNR AND MIT_MITARBEITERID = ERB_MIT_MITARBEITERID" "~"
if joinKuerzel = MITERB
searchResult := searchResult "LEFT JOIN FIBU.ERB_BUCHUNG ON ERB_MIT_FIRMENNR = MIT_FIRMENNR AND ERB_MIT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = ITMLTX
searchResult := searchResult "LEFT JOIN BUERO.LTX_LANGTEXT ON LTX_LANGTXTLFDNR = ITM_LANGTXTLFDNR" "~"
if joinKuerzel = LTXITM
searchResult := searchResult "LEFT JOIN BUERO.ITM_INTERNMAIL ON ITM_LANGTXTLFDNR = LTX_LANGTXTLFDNR" "~"
if joinKuerzel = PROLTX
searchResult := searchResult "LEFT JOIN BUERO.LTX_LANGTEXT ON LTX_LANGTXTLFDNR = PRO_BESCH_LANGTXTLFDNR" "~"
if joinKuerzel = LTXPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_BESCH_LANGTXTLFDNR = LTX_LANGTXTLFDNR" "~"
if joinKuerzel = VATMAB
searchResult := searchResult "LEFT JOIN BUERO.MAB_MITARBAUSBILDUNG ON MAB_MITARBAUSBID = VAT_MAB_MITARBAUSBID" "~"
if joinKuerzel = MABVAT
searchResult := searchResult "LEFT JOIN BUERO.VAT_TEILNAHME ON VAT_MAB_MITARBAUSBID = MAB_MITARBAUSBID" "~"
if joinKuerzel = MBZMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_MEDIENID = MBZ_MEDIENID" "~"
if joinKuerzel = MEDMBZ
searchResult := searchResult "LEFT JOIN BUERO.MBZ_MEDIENBETRIEBSZEIT ON MBZ_MEDIENID = MED_MEDIENID" "~"
if joinKuerzel = REIMED
searchResult := searchResult "LEFT JOIN MED_MEDIEN.MED_MEDIEN ON MED_MEDIENID = REI_MEDIENID" "~"
if joinKuerzel = MEDREI
searchResult := searchResult "LEFT JOIN HRS.REI_REISESTAMM ON REI_MEDIENID = MED_MEDIENID" "~"
if joinKuerzel = MVTMEV
searchResult := searchResult "LEFT JOIN BUERO.MEV_MEDIENVERLEIH ON MEV_MEDIENVERLEIHLFDNR = MVT_MEDIENVERLEIHLFDNR" "~"
if joinKuerzel = MEVMVT
searchResult := searchResult "LEFT JOIN BUERO.MVT_MEDIENVERLEIHTERMINZU ON MVT_MEDIENVERLEIHLFDNR = MEV_MEDIENVERLEIHLFDNR" "~"
if joinKuerzel = MITMIK
searchResult := searchResult "LEFT JOIN BUERO.MIK_MITARBEITERKARRIERE ON MIK_FIRMENNR = MIT_MIK_FIRMENNR AND MIK_KARRIERELFDNR = MIT_KARRIERELFDNR" "~"
if joinKuerzel = MIKMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_MIK_FIRMENNR = MIK_FIRMENNR AND MIT_KARRIERELFDNR = MIK_KARRIERELFDNR" "~"
if joinKuerzel = PSWMIK
searchResult := searchResult "LEFT JOIN BUERO.MIK_MITARBEITERKARRIERE ON MIK_FIRMENNR = PSW_MIK_FIRMENNR AND MIK_KARRIERELFDNR = PSW_KARRIERELFDNR" "~"
if joinKuerzel = MIKPSW
searchResult := searchResult "LEFT JOIN BUERO.PSW_PERSONENWERTE ON PSW_MIK_FIRMENNR = MIK_FIRMENNR AND PSW_KARRIERELFDNR = MIK_KARRIERELFDNR" "~"
if joinKuerzel = TMOMIK
searchResult := searchResult "LEFT JOIN BUERO.MIK_MITARBEITERKARRIERE ON MIK_FIRMENNR = TMO_MIK_FIRMENNR AND MIK_KARRIERELFDNR = TMO_KARRIERELFDNR" "~"
if joinKuerzel = MIKTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_MIK_FIRMENNR = MIK_FIRMENNR AND TMO_KARRIERELFDNR = MIK_KARRIERELFDNR" "~"
if joinKuerzel = ADKMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = ADK_MIT_FIRMENNR AND MIT_MITARBEITERID = ADK_MITARBEITERID" "~"
if joinKuerzel = MITADK
searchResult := searchResult "LEFT JOIN BUERO.ADK_ADRESSENLISTEKOPF ON ADK_MIT_FIRMENNR = MIT_FIRMENNR AND ADK_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = BCIMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = BCI_PROG_FIRMENNR AND MIT_MITARBEITERID = BCI_PROG_MITARBEITERID" "~"
if joinKuerzel = MITBCI
searchResult := searchResult "LEFT JOIN BMDENTW.BCI_BMDCONSTINTERN ON BCI_PROG_FIRMENNR = MIT_FIRMENNR AND BCI_PROG_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = BDLMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = BDL_MIT_FIRMENNR AND MIT_MITARBEITERID = BDL_MIT_MITARBEITERID" "~"
if joinKuerzel = MITBDL
searchResult := searchResult "LEFT JOIN BUERO.BDL_BUERODELETE ON BDL_MIT_FIRMENNR = MIT_FIRMENNR AND BDL_MIT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = BSGMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = BSG_MIT_FIRMENNR AND MIT_MITARBEITERID = BSG_MITARBEITERID" "~"
if joinKuerzel = MITBSG
searchResult := searchResult "LEFT JOIN BUERO.BSG_BESPRECHUNG ON BSG_MIT_FIRMENNR = MIT_FIRMENNR AND BSG_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = CPRMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = CPR_MIT_FIRMENNR AND MIT_MITARBEITERID = CPR_MITARBEITERID" "~"
if joinKuerzel = MITCPR
searchResult := searchResult "LEFT JOIN BUERO.CPR_CRMPROTOKOLL ON CPR_MIT_FIRMENNR = MIT_FIRMENNR AND CPR_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = ERPMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = ERP_MITARBEITER_FIRMENNR AND MIT_MITARBEITERID = ERP_MITARBEITERID" "~"
if joinKuerzel = MITERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_MITARBEITER_FIRMENNR = MIT_FIRMENNR AND ERP_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = ETLMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = ETL_MIT_FIRMENNR AND MIT_MITARBEITERID = ETL_MIT_MITARBEITERID" "~"
if joinKuerzel = MITETL
searchResult := searchResult "LEFT JOIN BUERO.ETL_ENTGANGENE_TELEFONATE ON ETL_MIT_FIRMENNR = MIT_FIRMENNR AND ETL_MIT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = GLUMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = GLU_KASSIER_FIRMENNR AND MIT_MITARBEITERID = GLU_KASSIER_MITARBEITERID" "~"
if joinKuerzel = MITGLU
searchResult := searchResult "LEFT JOIN WWS.GLU_GELDLADENZUORDNUNG ON GLU_KASSIER_FIRMENNR = MIT_FIRMENNR AND GLU_KASSIER_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = IVPMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = IVP_MIT_FIRMENNR AND MIT_MITARBEITERID = IVP_MITARBEITERID" "~"
if joinKuerzel = MITIVP
searchResult := searchResult "LEFT JOIN WWS.IVP_INVENTURPOSITION ON IVP_MIT_FIRMENNR = MIT_FIRMENNR AND IVP_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = LAGMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = LAG_LAGERVERANTW_FIRMENNR AND MIT_MITARBEITERID = LAG_LAGERVERANTWORTLICHER" "~"
if joinKuerzel = MITLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_LAGERVERANTW_FIRMENNR = MIT_FIRMENNR AND LAG_LAGERVERANTWORTLICHER = MIT_MITARBEITERID" "~"
if joinKuerzel = LEIMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = LEI_MIT_FIRMENNR AND MIT_MITARBEITERID = LEI_MIT_MITARBEITERID" "~"
if joinKuerzel = MITLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_MIT_FIRMENNR = MIT_FIRMENNR AND LEI_MIT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = KSDKZA
searchResult := searchResult "LEFT JOIN WWS.KZA_KASSENZAHLUNGSARTENZU ON KZA_KZAID = KSD_KZA_KZAID" "~"
if joinKuerzel = KZAKSD
searchResult := searchResult "LEFT JOIN WWS.KSD_KASSENSUMMENDETAIL ON KSD_KZA_KZAID = KZA_KZAID" "~"
if joinKuerzel = LAGLAA
searchResult := searchResult "LEFT JOIN WWS.LAA_LAGERART ON LAA_FIRMENNR = LAG_FIRMENNR AND LAA_LAGERARTNR = LAG_LAGERARTNR" "~"
if joinKuerzel = LAALAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = LAA_FIRMENNR AND LAG_LAGERARTNR = LAA_LAGERARTNR" "~"
if joinKuerzel = AVLLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = AVL_LAG_FIRMENNR AND LAG_LAGERNR = AVL_LAG_LAGERNR" "~"
if joinKuerzel = LAGAVL
searchResult := searchResult "LEFT JOIN WWS.AVL_AUFTRAGSVORERFLIEFDAT ON AVL_LAG_FIRMENNR = LAG_FIRMENNR AND AVL_LAG_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = AWSLAG
searchResult := searchResult "LEFT JOIN LAG_LAGER.LAG_LAGER ON LAG_FIRMENNR = AWS_LAG_FIRMENNR AND LAG_LAGERNR = AWS_LAG_LAGERNR" "~"
if joinKuerzel = LAGAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_LAG_FIRMENNR = LAG_FIRMENNR AND AWS_LAG_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = BALLAG
searchResult := searchResult "LEFT JOIN LAG_LAGER.LAG_LAGER ON LAG_FIRMENNR = BAL_LAG_FIRMENNR AND LAG_LAGERNR = BAL_LAG_LAGERNR" "~"
if joinKuerzel = LAGBAL
searchResult := searchResult "LEFT JOIN PRODUKTION.BAL_ARBEITSPLATZLAGERZU ON BAL_LAG_FIRMENNR = LAG_FIRMENNR AND BAL_LAG_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = LAPLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = LAP_FIRMENNR AND LAG_LAGERNR = LAP_LAGERNR" "~"
if joinKuerzel = LAGLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = LAG_FIRMENNR AND LAP_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = ELVLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = ELV_ZIELLAGER_FIRMENNR AND LAG_LAGERNR = ELV_ZIELLAGER_LAGERNR" "~"
if joinKuerzel = LAGELV
searchResult := searchResult "LEFT JOIN WWS.ELV_ERFASSPOSLIEFERVOR ON ELV_ZIELLAGER_FIRMENNR = LAG_FIRMENNR AND ELV_ZIELLAGER_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = WAGLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = WAG_VORG_LAG_FIRMENNR_EK AND LAG_LAGERNR = WAG_VORG_LAGERNR_EK" "~"
if joinKuerzel = LAGWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_VORG_LAG_FIRMENNR_EK = LAG_FIRMENNR AND WAG_VORG_LAGERNR_EK = LAG_LAGERNR" "~"
if joinKuerzel = IVPLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_FIRMENNR = IVP_LAJ_FIRMENNR AND LAJ_LAGERJOURNALLFDNR = IVP_LAGERJOURNALLFDNR" "~"
if joinKuerzel = LAJIVP
searchResult := searchResult "LEFT JOIN WWS.IVP_INVENTURPOSITION ON IVP_LAJ_FIRMENNR = LAJ_FIRMENNR AND IVP_LAGERJOURNALLFDNR = LAJ_LAGERJOURNALLFDNR" "~"
if joinKuerzel = AZKLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = AZK_LANDNR" "~"
if joinKuerzel = LANAZK
searchResult := searchResult "LEFT JOIN BIL.AZK_ANBUZUSCHUSSKENNZEICH ON AZK_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = FADLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = FAD_LANDNR" "~"
if joinKuerzel = LANFAD
searchResult := searchResult "LEFT JOIN FIBU.FAD_AUSDRUCKDETAIL ON FAD_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = GERLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = GER_LANDNR" "~"
if joinKuerzel = LANGER
searchResult := searchResult "LEFT JOIN BUERO.GER_GERICHT ON GER_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = LPOLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = LPO_URSPR_LANDNR" "~"
if joinKuerzel = LANLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_URSPR_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = OBPLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = OBP_LANDNR" "~"
if joinKuerzel = LANOBP
searchResult := searchResult "LEFT JOIN BUERO.OBP_ONLBEWPERSSTAMM ON OBP_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = PKXLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = PKX_LANDNR" "~"
if joinKuerzel = LANPKX
searchResult := searchResult "LEFT JOIN FIBU.PKX_BUDGETPERSKOSTPARAMS ON PKX_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = SEBLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = SEB_LANDNR" "~"
if joinKuerzel = LANSEB
searchResult := searchResult "LEFT JOIN BIL.SEB_STEUERERKLBAUM ON SEB_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = SOZLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = SOZ_LANDNR" "~"
if joinKuerzel = LANSOZ
searchResult := searchResult "LEFT JOIN BUERO.SOZ_SOZIAL ON SOZ_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = STGLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = STG_LANDNR" "~"
if joinKuerzel = LANSTG
searchResult := searchResult "LEFT JOIN FIBU.STG_STEUERGRUPPE ON STG_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = STZLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = STZ_BESTIMMUNGSLANDNR" "~"
if joinKuerzel = LANSTZ
searchResult := searchResult "LEFT JOIN WWS.STZ_STEUERTEXTZUORDNUNG ON STZ_BESTIMMUNGSLANDNR = LAN_LANDNR" "~"
if joinKuerzel = VEBLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = VEB_LANDNR" "~"
if joinKuerzel = LANVEB
searchResult := searchResult "LEFT JOIN FIBU.VEB_VORERFASSBUCHUNG ON VEB_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = WAPLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = WAP_URSPR_LANDNR" "~"
if joinKuerzel = LANWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_URSPR_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = OBPLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = OBP_STAATSANGEHOERIGKEIT" "~"
if joinKuerzel = LANOBP
searchResult := searchResult "LEFT JOIN BUERO.OBP_ONLBEWPERSSTAMM ON OBP_STAATSANGEHOERIGKEIT = LAN_LANDNR" "~"
if joinKuerzel = BALLAP
searchResult := searchResult "LEFT JOIN LAP_LAGERPLATZ.LAP_LAGERPLATZ ON LAP_FIRMENNR = BAL_LAG_FIRMENNR AND LAP_LAGERNR = BAL_LAG_LAGERNR AND LAP_LAGERPLATZ = BAL_LAP_LAGERPLATZ" "~"
if joinKuerzel = LAPBAL
searchResult := searchResult "LEFT JOIN PRODUKTION.BAL_ARBEITSPLATZLAGERZU ON BAL_LAG_FIRMENNR = LAP_FIRMENNR AND BAL_LAG_LAGERNR = LAP_LAGERNR AND BAL_LAP_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = DISLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = DIS_LAG_FIRMENNR AND LAP_LAGERNR = DIS_LAGERNR AND LAP_LAGERPLATZ = DIS_LAGERPLATZ" "~"
if joinKuerzel = LAPDIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_LAG_FIRMENNR = LAP_FIRMENNR AND DIS_LAGERNR = LAP_LAGERNR AND DIS_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = WEMLBK
searchResult := searchResult "LEFT JOIN WWS.LBK_LAGERBUCHKENNZ ON LBK_FIRMENNR = WEM_LBK_FIRMENNR AND LBK_LAGERBUCHKENNZ = WEM_LAGERBUCHKENNZ" "~"
if joinKuerzel = LBKWEM
searchResult := searchResult "LEFT JOIN WWS.WEM_WARENEINSATZBUCHKZZU ON WEM_LBK_FIRMENNR = LBK_FIRMENNR AND WEM_LAGERBUCHKENNZ = LBK_LAGERBUCHKENNZ" "~"
if joinKuerzel = EBVLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = EBV_FIRMENNR AND LFI_LOHNJAHR = EBV_JAHR" "~"
if joinKuerzel = LFIEBV
searchResult := searchResult "LEFT JOIN HRS.EBV_ELDADETAIL_BV ON EBV_FIRMENNR = LFI_FIRMENNR AND EBV_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = LZSLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = LZS_DVFIRMENNR AND LFI_LOHNJAHR = LZS_BZJJ" "~"
if joinKuerzel = LFILZS
searchResult := searchResult "LEFT JOIN HRS.LZS_LOHNZETTEL_SV ON LZS_DVFIRMENNR = LFI_FIRMENNR AND LZS_BZJJ = LFI_LOHNJAHR" "~"
if joinKuerzel = XSELFI
searchResult := searchResult "LEFT JOIN LFI_LOHNFIRMA.LFI_LOHNFIRMA ON LFI_FIRMENNR = XSE_LFI_FIRMENNR AND LFI_LOHNJAHR = XSE_LFI_LOHNJAHR" "~"
if joinKuerzel = LFIXSE
searchResult := searchResult "LEFT JOIN BUERO.XSE_XML_STEUERERKL ON XSE_LFI_FIRMENNR = LFI_FIRMENNR AND XSE_LFI_LOHNJAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = EVQLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = EVQ_DVFIRMENNR AND LFI_LOHNJAHR = EVQ_JAHR" "~"
if joinKuerzel = LFIEVQ
searchResult := searchResult "LEFT JOIN HRS.EVQ_ELDADETAIL_VSNR ON EVQ_DVFIRMENNR = LFI_FIRMENNR AND EVQ_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = LFSLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_FIRMENNR = LFS_FIRMENNR AND LFS_LIEFERSCHEINLFDNR = LFS_URSPRLFSLFDNR" "~"
if joinKuerzel = LFSLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_FIRMENNR = LFS_FIRMENNR AND LFS_URSPRLFSLFDNR = LFS_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = LPOLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_FIRMENNR = LPO_FIRMENNR AND LFS_LIEFERSCHEINLFDNR = LPO_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = LFSLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_FIRMENNR = LFS_FIRMENNR AND LPO_LIEFERSCHEINLFDNR = LFS_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = PGLLGF
searchResult := searchResult "LEFT JOIN HRS.LGF_LGVMAKROS ON LGF_LGVMAKROID = PGL_FILTER_LGF_LGVMAKROID" "~"
if joinKuerzel = LGFPGL
searchResult := searchResult "LEFT JOIN HRS.PGL_PERKZGLIEDERUNG ON PGL_FILTER_LGF_LGVMAKROID = LGF_LGVMAKROID" "~"
if joinKuerzel = DIPLGR
searchResult := searchResult "LEFT JOIN LGR_LOHNARTGRUPPE.LGR_LOHNARTGRUPPE ON LGR_LGRID = DIP_LGRID AND LGR_JAHR = DIP_LGR_JAHR AND LGR_DBSNR = DIP_DBSNR" "~"
if joinKuerzel = LGRDIP
searchResult := searchResult "LEFT JOIN BUERO.DIP_DIVERSE ON DIP_LGRID = LGR_LGRID AND DIP_LGR_JAHR = LGR_JAHR AND DIP_DBSNR = LGR_DBSNR" "~"
if joinKuerzel = EDKLGR
searchResult := searchResult "LEFT JOIN HRS.LGR_LOHNARTGRUPPE ON LGR_LGRID = EDK_LGRID AND LGR_JAHR = EDK_JAHR AND LGR_DBSNR = EDK_DBSNR" "~"
if joinKuerzel = LGREDK
searchResult := searchResult "LEFT JOIN HRS.EDK_ERWDIVKOERPER ON EDK_LGRID = LGR_LGRID AND EDK_JAHR = LGR_JAHR AND EDK_DBSNR = LGR_DBSNR" "~"
if joinKuerzel = WBTLIB
searchResult := searchResult "LEFT JOIN WWS.LIB_LIEFERBEDINGUNG ON LIB_LIEFERBNR = WBT_LIEFERBNR" "~"
if joinKuerzel = LIBWBT
searchResult := searchResult "LEFT JOIN WWS.WBT_WWSBUDGETKOPF ON WBT_LIEFERBNR = LIB_LIEFERBNR" "~"
if joinKuerzel = APALIN
searchResult := searchResult "LEFT JOIN PRODUKTION.LIN_PROD_LINIE ON LIN_FIRMENNR = APA_LIN_FIRMENNR AND LIN_LINIE = APA_LINIE" "~"
if joinKuerzel = LINAPA
searchResult := searchResult "LEFT JOIN PRODUKTION.APA_ARBEITSPLAN ON APA_LIN_FIRMENNR = LIN_FIRMENNR AND APA_LINIE = LIN_LINIE" "~"
if joinKuerzel = APSLIN
searchResult := searchResult "LEFT JOIN LIN_PROD_LINIE.LIN_PROD_LINIE ON LIN_FIRMENNR = APS_LIN_FIRMENNR AND LIN_LINIE = APS_LINIE" "~"
if joinKuerzel = LINAPS
searchResult := searchResult "LEFT JOIN WWS.APS_ARBEITSPLAN_STAMM ON APS_LIN_FIRMENNR = LIN_FIRMENNR AND APS_LINIE = LIN_LINIE" "~"
if joinKuerzel = EKJLIN
searchResult := searchResult "LEFT JOIN PRODUKTION.LIN_PROD_LINIE ON LIN_FIRMENNR = EKJ_KEY_LIN_FIRMENNR AND LIN_LINIE = EKJ_KEY_LIN_LINIE" "~"
if joinKuerzel = LINEKJ
searchResult := searchResult "LEFT JOIN PRODUKTION.EKJ_ERP_KOREJOURNAL ON EKJ_KEY_LIN_FIRMENNR = LIN_FIRMENNR AND EKJ_KEY_LIN_LINIE = LIN_LINIE" "~"
if joinKuerzel = WKGLMS
searchResult := searchResult "LEFT JOIN WWS.LMS_LIEFERMAHNUNG ON LMS_FIRMENNR = WKG_MAHNSCHEMAFIRMENNR AND LMS_MAHNSCHEMANR = WKG_MAHNSCHEMANR" "~"
if joinKuerzel = LMSWKG
searchResult := searchResult "LEFT JOIN WWS.WKG_WWSKUNDENGRUPPE ON WKG_MAHNSCHEMAFIRMENNR = LMS_FIRMENNR AND WKG_MAHNSCHEMANR = LMS_MAHNSCHEMANR" "~"
if joinKuerzel = EROLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_FIRMENNR = ERO_FIRMENNR AND LPO_LIEFERSCHEINLFDNR = ERO_EINSTPREISREF_LSNR AND LPO_LIEFERSCHEINPOSLFDNR = ERO_EINSTPREISREF_LSPOSNR" "~"
if joinKuerzel = LPOERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_FIRMENNR = LPO_FIRMENNR AND ERO_EINSTPREISREF_LSNR = LPO_LIEFERSCHEINLFDNR AND ERO_EINSTPREISREF_LSPOSNR = LPO_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = VLJLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_FIRMENNR = VLJ_FIRMENNR AND LPO_LIEFERSCHEINLFDNR = VLJ_LFS_LFDNR AND LPO_LIEFERSCHEINPOSLFDNR = VLJ_LFS_POSLFDNR" "~"
if joinKuerzel = LPOVLJ
searchResult := searchResult "LEFT JOIN WWS.VLJ_VERLEIHJOURNAL ON VLJ_FIRMENNR = LPO_FIRMENNR AND VLJ_LFS_LFDNR = LPO_LIEFERSCHEINLFDNR AND VLJ_LFS_POSLFDNR = LPO_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = ARLLTX
searchResult := searchResult "LEFT JOIN LTX_LANGTEXT.LTX_LANGTEXT ON LTX_LANGTXTLFDNR = ARL_PRODUKTLANGTEXT" "~"
if joinKuerzel = LTXARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_PRODUKTLANGTEXT = LTX_LANGTXTLFDNR" "~"
if joinKuerzel = BWPLTX
searchResult := searchResult "LEFT JOIN BUERO.LTX_LANGTEXT ON LTX_LANGTXTLFDNR = BWP_LANGTXTLFDNR" "~"
if joinKuerzel = LTXBWP
searchResult := searchResult "LEFT JOIN BUERO.BWP_BENUTZERWERTEPARAM ON BWP_LANGTXTLFDNR = LTX_LANGTXTLFDNR" "~"
if joinKuerzel = LXDLXM
searchResult := searchResult "LEFT JOIN BUERO.LXM_LOHNEXPPROTOKOLL ON LXM_LPMID = LXD_LPMID" "~"
if joinKuerzel = LXMLXD
searchResult := searchResult "LEFT JOIN BUERO.LXD_LOHNEXPPROTDETAIL ON LXD_LPMID = LXM_LPMID" "~"
if joinKuerzel = RSBMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_MEDIENID = RSB_MEDIENID" "~"
if joinKuerzel = MEDRSB
searchResult := searchResult "LEFT JOIN BUERO.RSB_RESSOURCENBEDARF ON RSB_MEDIENID = MED_MEDIENID" "~"
if joinKuerzel = LGDLGM
searchResult := searchResult "LEFT JOIN DATANORM.LGM_LEISTUNG ON LGM_ERST_NR = LGD_ERST_NR AND LGM_ART_NR = LGD_ART_NR AND LGM_VERWENDUNG = LGD_VERWENDUNG" "~"
if joinKuerzel = LGMLGD
searchResult := searchResult "LEFT JOIN DATANORM.LGD_LEISTUNG_DETAIL ON LGD_ERST_NR = LGM_ERST_NR AND LGD_ART_NR = LGM_ART_NR AND LGD_VERWENDUNG = LGM_VERWENDUNG" "~"
if joinKuerzel = LGZLGR
searchResult := searchResult "LEFT JOIN HRS.LGR_LOHNARTGRUPPE ON LGR_LGRID = LGZ_LGRID AND LGR_JAHR = LGZ_JAHR AND LGR_DBSNR = LGZ_DBSNR" "~"
if joinKuerzel = LGRLGZ
searchResult := searchResult "LEFT JOIN HRS.LGZ_LOHNARTGRUPPEZU ON LGZ_LGRID = LGR_LGRID AND LGZ_JAHR = LGR_JAHR AND LGZ_DBSNR = LGR_DBSNR" "~"
if joinKuerzel = WADLMS
searchResult := searchResult "LEFT JOIN WWS.LMS_LIEFERMAHNUNG ON LMS_FIRMENNR = WAD_MAHNSCHEMAFIRMENNR AND LMS_MAHNSCHEMANR = WAD_MAHNSCHEMANR" "~"
if joinKuerzel = LMSWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_MAHNSCHEMAFIRMENNR = LMS_FIRMENNR AND WAD_MAHNSCHEMANR = LMS_MAHNSCHEMANR" "~"
if joinKuerzel = TRPLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_FIRMENNR = TRP_LIEFER_FIRMENNR AND LPO_LIEFERSCHEINLFDNR = TRP_LPO_LIEFERSCHEINLFDNR AND LPO_LIEFERSCHEINPOSLFDNR = TRP_LPO_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = LPOTRP
searchResult := searchResult "LEFT JOIN WWS.TRP_TRANSPORTPOSITION ON TRP_LIEFER_FIRMENNR = LPO_FIRMENNR AND TRP_LPO_LIEFERSCHEINLFDNR = LPO_LIEFERSCHEINLFDNR AND TRP_LPO_LIEFERSCHEINPOSLFDNR = LPO_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = WEOLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_FIRMENNR = WEO_LPO_FIRMENNR AND LPO_LIEFERSCHEINLFDNR = WEO_LIEFERSCHEINLFDNR AND LPO_LIEFERSCHEINPOSLFDNR = WEO_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = LPOWEO
searchResult := searchResult "LEFT JOIN WWS.WEO_WARENEINSATZLPO ON WEO_LPO_FIRMENNR = LPO_FIRMENNR AND WEO_LIEFERSCHEINLFDNR = LPO_LIEFERSCHEINLFDNR AND WEO_LIEFERSCHEINPOSLFDNR = LPO_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = LNSLSE
searchResult := searchResult "LEFT JOIN HRS.LSE_LOHNSCHABLONE ON LSE_DBSNR = LNS_DBSNR AND LSE_JAHR = LNS_JAHR AND LSE_LSENR = LNS_LSENR" "~"
if joinKuerzel = LSELNS
searchResult := searchResult "LEFT JOIN HRS.LNS_LOHNSCHABLONENDETAILS ON LNS_DBSNR = LSE_DBSNR AND LNS_JAHR = LSE_JAHR AND LNS_LSENR = LSE_LSENR" "~"
if joinKuerzel = ARLLSG
searchResult := searchResult "LEFT JOIN WWS.LSG_LIEFERSCHEINGRUPPEN ON LSG_FIRMENNR = ARL_LSG_FIRMENNR AND LSG_LFSHAUPTGRUPPE = ARL_LSG_LFSHAUPTGRUPPE AND LSG_LFSUNTERGRUPPE = ARL_LSG_LFSUNTERGRUPPE" "~"
if joinKuerzel = LSGARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_LSG_FIRMENNR = LSG_FIRMENNR AND ARL_LSG_LFSHAUPTGRUPPE = LSG_LFSHAUPTGRUPPE AND ARL_LSG_LFSUNTERGRUPPE = LSG_LFSUNTERGRUPPE" "~"
if joinKuerzel = LTSLTK
searchResult := searchResult "LEFT JOIN ZLE.LTK_LEISTTAUSCHKOPF ON LTK_ERSTELL_FIRMENNR = LTS_ERSTELL_FIRMENNR AND LTK_EMPF_FIRMENNR = LTS_EMPF_FIRMENNR AND LTK_GUELTIGAB = LTS_GUELTIGAB" "~"
if joinKuerzel = LTKLTS
searchResult := searchResult "LEFT JOIN ZLE.LTS_LEISTTAUSCHSATZ ON LTS_ERSTELL_FIRMENNR = LTK_ERSTELL_FIRMENNR AND LTS_EMPF_FIRMENNR = LTK_EMPF_FIRMENNR AND LTS_GUELTIGAB = LTK_GUELTIGAB" "~"
if joinKuerzel = AUGLTX
searchResult := searchResult "LEFT JOIN BUERO.LTX_LANGTEXT ON LTX_LANGTXTLFDNR = AUG_LANGTXTLFDNR" "~"
if joinKuerzel = LTXAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_LANGTXTLFDNR = LTX_LANGTXTLFDNR" "~"
if joinKuerzel = VASLTX
searchResult := searchResult "LEFT JOIN BUERO.LTX_LANGTEXT ON LTX_LANGTXTLFDNR = VAS_VOR_LANGTXTLFDNR" "~"
if joinKuerzel = LTXVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_VOR_LANGTXTLFDNR = LTX_LANGTXTLFDNR" "~"
if joinKuerzel = EVTLTX
searchResult := searchResult "LEFT JOIN BUERO.LTX_LANGTEXT ON LTX_LANGTXTLFDNR = EVT_VOR_LANGTXTLFDNR" "~"
if joinKuerzel = LTXEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_VOR_LANGTXTLFDNR = LTX_LANGTXTLFDNR" "~"
if joinKuerzel = VASLTX
searchResult := searchResult "LEFT JOIN BUERO.LTX_LANGTEXT ON LTX_LANGTXTLFDNR = VAS_KUI_LANGTXTLFDNR" "~"
if joinKuerzel = LTXVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_KUI_LANGTXTLFDNR = LTX_LANGTXTLFDNR" "~"
if joinKuerzel = VASLTX
searchResult := searchResult "LEFT JOIN BUERO.LTX_LANGTEXT ON LTX_LANGTXTLFDNR = VAS_EQU_LANGTXTLFDNR" "~"
if joinKuerzel = LTXVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_EQU_LANGTXTLFDNR = LTX_LANGTXTLFDNR" "~"
if joinKuerzel = LUPLUK
searchResult := searchResult "LEFT JOIN PRODUKTION.LUK_LAGER_UMS_KOPF ON LUK_FIRMENNR = LUP_FIRMENNR AND LUK_LU_NR = LUP_LU_NR" "~"
if joinKuerzel = LUKLUP
searchResult := searchResult "LEFT JOIN PRODUKTION.LUP_LAGER_UMS_POS ON LUP_FIRMENNR = LUK_FIRMENNR AND LUP_LU_NR = LUK_LU_NR" "~"
if joinKuerzel = LUALUP
searchResult := searchResult "LEFT JOIN PRODUKTION.LUP_LAGER_UMS_POS ON LUP_FIRMENNR = LUA_FIRMENNR AND LUP_LU_NR = LUA_LU_NR AND LUP_POS_SEQ = LUA_POS_SEQ" "~"
if joinKuerzel = LUPLUA
searchResult := searchResult "LEFT JOIN PRODUKTION.LUA_LAGER_UMS_ANGEFORDERT ON LUA_FIRMENNR = LUP_FIRMENNR AND LUA_LU_NR = LUP_LU_NR AND LUA_POS_SEQ = LUP_POS_SEQ" "~"
if joinKuerzel = MMZMAG
searchResult := searchResult "LEFT JOIN BUERO.MAG_MITARBGESPR ON MAG_MAGID = MMZ_MAGID" "~"
if joinKuerzel = MAGMMZ
searchResult := searchResult "LEFT JOIN BUERO.MMZ_MAGBETRIEBSMITTELZU ON MMZ_MAGID = MAG_MAGID" "~"
if joinKuerzel = KASMDA
searchResult := searchResult "LEFT JOIN WWS.MDA_MDE_ARTEN ON MDA_MDEARTID = KAS_MDA_MDEARTID" "~"
if joinKuerzel = MDAKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_MDA_MDEARTID = MDA_MDEARTID" "~"
if joinKuerzel = AFTMED
searchResult := searchResult "LEFT JOIN MED_MEDIEN.MED_MEDIEN ON MED_MEDIENID = AFT_MED_MEDIENID" "~"
if joinKuerzel = MEDAFT
searchResult := searchResult "LEFT JOIN ZLE.AFT_AUFBEREITUNG ON AFT_MED_MEDIENID = MED_MEDIENID" "~"
if joinKuerzel = BSGMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_MEDIENID = BSG_MEDIENID" "~"
if joinKuerzel = MEDBSG
searchResult := searchResult "LEFT JOIN BUERO.BSG_BESPRECHUNG ON BSG_MEDIENID = MED_MEDIENID" "~"
if joinKuerzel = ERBMED
searchResult := searchResult "LEFT JOIN MED_MEDIEN.MED_MEDIEN ON MED_MEDIENID = ERB_MEDIENID" "~"
if joinKuerzel = MEDERB
searchResult := searchResult "LEFT JOIN FIBU.ERB_BUCHUNG ON ERB_MEDIENID = MED_MEDIENID" "~"
if joinKuerzel = FTBMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_MEDIENID = FTB_MEDIENID" "~"
if joinKuerzel = MEDFTB
searchResult := searchResult "LEFT JOIN BUERO.FTB_FAHRTENBUCH ON FTB_MEDIENID = MED_MEDIENID" "~"
if joinKuerzel = EKKMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = EKK_VERTR3_FIRMENNR AND MIT_MITARBEITERID = EKK_VERTR3_MITARBEITERID" "~"
if joinKuerzel = MITEKK
searchResult := searchResult "LEFT JOIN WWS.EKK_EXTERNE_KUNDENKARTEI ON EKK_VERTR3_FIRMENNR = MIT_FIRMENNR AND EKK_VERTR3_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = FSTMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = FST_GF_FIRMENNR AND MIT_MITARBEITERID = FST_GF_MITARBEITERID" "~"
if joinKuerzel = MITFST
searchResult := searchResult "LEFT JOIN BUERO.FST_FRIST ON FST_GF_FIRMENNR = MIT_FIRMENNR AND FST_GF_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = AVEMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = AVE_V4_FIRMENNR AND MIT_MITARBEITERID = AVE_V4_MITARBEITERID" "~"
if joinKuerzel = MITAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_V4_FIRMENNR = MIT_FIRMENNR AND AVE_V4_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = PROMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = PRO_VWSTB_FIRMENNR AND MIT_MITARBEITERID = PRO_VWSTB_MITARBEITERID" "~"
if joinKuerzel = MITPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_VWSTB_FIRMENNR = MIT_FIRMENNR AND PRO_VWSTB_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = AVEMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = AVE_EFKESD_MIT_FIRMENNR AND MIT_MITARBEITERID = AVE_EFKESD_MITARBEITERID" "~"
if joinKuerzel = MITAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_EFKESD_MIT_FIRMENNR = MIT_FIRMENNR AND AVE_EFKESD_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = FAGMON
searchResult := searchResult "LEFT JOIN MON_MONAT.MON_MONAT ON MON_MONAT = FAG_MONAT" "~"
if joinKuerzel = MONFAG
searchResult := searchResult "LEFT JOIN HRS.FAG_FINANZAMTGHOST ON FAG_MONAT = MON_MONAT" "~"
if joinKuerzel = CBRMVL
searchResult := searchResult "LEFT JOIN BUERO.MVL_MITARBEITERVORLAGE ON MVL_MVLID = CBR_TOCHECKMITARB_MVLID" "~"
if joinKuerzel = MVLCBR
searchResult := searchResult "LEFT JOIN BUERO.CBR_CRMABWBENACHREGEL ON CBR_TOCHECKMITARB_MVLID = MVL_MVLID" "~"
if joinKuerzel = NGZNOT
searchResult := searchResult "LEFT JOIN BUERO.NOT_NOTIZ ON NOT_NOTIZNR = NGZ_NOTIZNR" "~"
if joinKuerzel = NOTNGZ
searchResult := searchResult "LEFT JOIN BUERO.NGZ_NOTIZGRUPPEZU ON NGZ_NOTIZNR = NOT_NOTIZNR" "~"
if joinKuerzel = WIPNTC
searchResult := searchResult "LEFT JOIN NTC_NTCSARTIKEL.NTC_NTCSARTIKEL ON NTC_NTCSBMDPAKETNR = WIP_NTCSBMDPAKETNR" "~"
if joinKuerzel = NTCWIP
searchResult := searchResult "LEFT JOIN BIL.WIP_WIPRUEFUNG ON WIP_NTCSBMDPAKETNR = NTC_NTCSBMDPAKETNR" "~"
if joinKuerzel = OPEOAZ
searchResult := searchResult "LEFT JOIN LOHN.OAZ_OF_ARBEITSZEITMODELL ON OAZ_KLIENTENNR = OPE_KLIENTENNR AND OAZ_OFAZNR = OPE_OAZ_OFAZNR" "~"
if joinKuerzel = OAZOPE
searchResult := searchResult "LEFT JOIN LOHN.OPE_OF_PERSON ON OPE_KLIENTENNR = OAZ_KLIENTENNR AND OPE_OAZ_OFAZNR = OAZ_OFAZNR" "~"
if joinKuerzel = ODTODA
searchResult := searchResult "LEFT JOIN BMD.ODA_ONLINEDATEI ON ODA_ONLDATEIID = ODT_ONLDATEIID" "~"
if joinKuerzel = ODAODT
searchResult := searchResult "LEFT JOIN BMD.ODT_ONLINEDATEITRANSFER ON ODT_ONLDATEIID = ODA_ONLDATEIID" "~"
if joinKuerzel = OPEOFA
searchResult := searchResult "LEFT JOIN LOHN.OFA_OF_FINANZAMT ON OFA_KLIENTENNR = OPE_KLIENTENNR AND OFA_FANR = OPE_FINANZNR" "~"
if joinKuerzel = OFAOPE
searchResult := searchResult "LEFT JOIN LOHN.OPE_OF_PERSON ON OPE_KLIENTENNR = OFA_KLIENTENNR AND OPE_FINANZNR = OFA_FANR" "~"
if joinKuerzel = OPEOGW
searchResult := searchResult "LEFT JOIN LOHN.OGW_OF_GEWERKSCHAFT ON OGW_GWNR = OPE_GWNR" "~"
if joinKuerzel = OGWOPE
searchResult := searchResult "LEFT JOIN LOHN.OPE_OF_PERSON ON OPE_GWNR = OGW_GWNR" "~"
if joinKuerzel = OSZOKL
searchResult := searchResult "LEFT JOIN LOHN.OKL_OF_KLIENT ON OKL_KLIENTENNR = OSZ_KLIENTENNR" "~"
if joinKuerzel = OKLOSZ
searchResult := searchResult "LEFT JOIN LOHN.OSZ_OF_SWBKZ ON OSZ_KLIENTENNR = OKL_KLIENTENNR" "~"
if joinKuerzel = ORSOKZ
searchResult := searchResult "LEFT JOIN LOHN.OKZ_OF_KFZVERWALTUNG ON OKZ_KFZVERW_LFDNR = ORS_KFZVERW_LFDNR" "~"
if joinKuerzel = OKZORS
searchResult := searchResult "LEFT JOIN LOHN.ORS_OF_REISESTAMM ON ORS_KFZVERW_LFDNR = OKZ_KFZVERW_LFDNR" "~"
if joinKuerzel = PVGORT
searchResult := searchResult "LEFT JOIN BUERO.ORT_ORT ON ORT_LANDNR = PVG_ORT_LANDNR AND ORT_PLZ = PVG_PLZ AND ORT_PLZUNTERNR = PVG_PLZUNTERNR" "~"
if joinKuerzel = ORTPVG
searchResult := searchResult "LEFT JOIN BUERO.PVG_PERSONVORERFASSANGEH ON PVG_ORT_LANDNR = ORT_LANDNR AND PVG_PLZ = ORT_PLZ AND PVG_PLZUNTERNR = ORT_PLZUNTERNR" "~"
if joinKuerzel = PFFOUP
searchResult := searchResult "LEFT JOIN BUERO.OUP_ONLUMFRAGEPAGE ON OUP_OUPID = PFF_OUPID" "~"
if joinKuerzel = OUPPFF
searchResult := searchResult "LEFT JOIN BUERO.PFF_PAGEFGBFRAGEZU ON PFF_OUPID = OUP_OUPID" "~"
if joinKuerzel = PAPPAC
searchResult := searchResult "LEFT JOIN WWS.PAC_PACKSTOFF ON PAC_FIRMENNR = PAP_FIRMENNR AND PAC_PACKSTOFFNR = PAP_PACKSTOFFNR" "~"
if joinKuerzel = PACPAP
searchResult := searchResult "LEFT JOIN WWS.PAP_PACKSTOFFTABPOS ON PAP_FIRMENNR = PAC_FIRMENNR AND PAP_PACKSTOFFNR = PAC_PACKSTOFFNR" "~"
if joinKuerzel = PAEPAI
searchResult := searchResult "LEFT JOIN BMDINTERN.PAI_PRODUKTANFINTERESSENT ON PAI_PAINR = PAE_PAINR" "~"
if joinKuerzel = PAIPAE
searchResult := searchResult "LEFT JOIN BMDINTERN.PAE_PRODUKTANFRAGE ON PAE_PAINR = PAI_PAINR" "~"
if joinKuerzel = PWTPAK
searchResult := searchResult "LEFT JOIN BIL.PAK_PERSRST_ALTERSV_KOPF ON PAK_PAKLFDNR = PWT_PAKLFDNR" "~"
if joinKuerzel = PAKPWT
searchResult := searchResult "LEFT JOIN BIL.PWT_PERSRST_ALTERSV_WERT ON PWT_PAKLFDNR = PAK_PAKLFDNR" "~"
if joinKuerzel = TPZPAL
searchResult := searchResult "LEFT JOIN ZLE.PAL_PAUSCHALE ON PAL_FIRMENNR = TPZ_PAL_FIRMENNR AND PAL_PAUSCHALNR = TPZ_PAUSCHALNR" "~"
if joinKuerzel = PALTPZ
searchResult := searchResult "LEFT JOIN ZLE.TPZ_TAETIGKEITPAUSCHALEZU ON TPZ_PAL_FIRMENNR = PAL_FIRMENNR AND TPZ_PAUSCHALNR = PAL_PAUSCHALNR" "~"
if joinKuerzel = ARLPAT
searchResult := searchResult "LEFT JOIN WWS.PAT_PACKSTOFFTAB ON PAT_FIRMENNR = ARL_FIRMENNR AND PAT_PACKSTOFFTABNR = ARL_PACKSTOFFTABNR" "~"
if joinKuerzel = PATARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = PAT_FIRMENNR AND ARL_PACKSTOFFTABNR = PAT_PACKSTOFFTABNR" "~"
if joinKuerzel = MDAMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = MDA_FIRMENNR AND MIT_MITARBEITERID = MDA_KOMMISS_MITARBEITERID" "~"
if joinKuerzel = MITMDA
searchResult := searchResult "LEFT JOIN WWS.MDA_MDE_ARTEN ON MDA_FIRMENNR = MIT_FIRMENNR AND MDA_KOMMISS_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = MLTMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = MLT_VON_GF_FIRMENNR AND MIT_MITARBEITERID = MLT_VON_GF_MITARBEITERID" "~"
if joinKuerzel = MITMLT
searchResult := searchResult "LEFT JOIN ZLE.MLT_MITARBLEISTTAUSCH ON MLT_VON_GF_FIRMENNR = MIT_FIRMENNR AND MLT_VON_GF_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = RAPMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = RAP_MIT_FIRMENNR AND MIT_MITARBEITERID = RAP_MITARBEITERID" "~"
if joinKuerzel = MITRAP
searchResult := searchResult "LEFT JOIN WWS.RAP_RABATTTABPOS ON RAP_MIT_FIRMENNR = MIT_FIRMENNR AND RAP_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = RNGMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = RNG_MITARBEITER_FIRMENNR AND MIT_MITARBEITERID = RNG_MITARBEITERID" "~"
if joinKuerzel = MITRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_MITARBEITER_FIRMENNR = MIT_FIRMENNR AND RNG_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = TAPMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = TAP_FIRMENNR AND MIT_MITARBEITERID = TAP_MITARBEITERID" "~"
if joinKuerzel = MITTAP
searchResult := searchResult "LEFT JOIN ZLE.TAP_TAGESABPROTOKOLL ON TAP_FIRMENNR = MIT_FIRMENNR AND TAP_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = TIPMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = TIP_MIT_FIRMENNR AND MIT_MITARBEITERID = TIP_MIT_MITARBEITERID" "~"
if joinKuerzel = MITTIP
searchResult := searchResult "LEFT JOIN BUERO.TIP_TERMINALIMPPROTOKOLL ON TIP_MIT_FIRMENNR = MIT_FIRMENNR AND TIP_MIT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = ZMSMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = ZMS_MIT_FIRMENNR AND MIT_MITARBEITERID = ZMS_MITARBEITERID" "~"
if joinKuerzel = MITZMS
searchResult := searchResult "LEFT JOIN BUERO.ZMS_ZEITMONATSSUMMEN ON ZMS_MIT_FIRMENNR = MIT_FIRMENNR AND ZMS_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = BCIMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = BCI_VERANTW_FIRMENNR AND MIT_MITARBEITERID = BCI_VERANTW_MITARBEITERID" "~"
if joinKuerzel = MITBCI
searchResult := searchResult "LEFT JOIN BMDENTW.BCI_BMDCONSTINTERN ON BCI_VERANTW_FIRMENNR = MIT_FIRMENNR AND BCI_VERANTW_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = BONMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = BON_V2_FIRMENNR AND MIT_MITARBEITERID = BON_V2_MITARBEITERID" "~"
if joinKuerzel = MITBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_V2_FIRMENNR = MIT_FIRMENNR AND BON_V2_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = EPSMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = EPS_VERARBEIT_MIT_FIRMENNR AND MIT_MITARBEITERID = EPS_VERARBEIT_MAID" "~"
if joinKuerzel = MITEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_VERARBEIT_MIT_FIRMENNR = MIT_FIRMENNR AND EPS_VERARBEIT_MAID = MIT_MITARBEITERID" "~"
if joinKuerzel = POSMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = POS_MIT_FIRMENNR AND MIT_MITARBEITERID = POS_MIT_MITARBEITERID" "~"
if joinKuerzel = MITPOS
searchResult := searchResult "LEFT JOIN BUERO.POS_POST ON POS_MIT_FIRMENNR = MIT_FIRMENNR AND POS_MIT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = PSWMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = PSW_MIT_GF_FIRMENNR AND MIT_MITARBEITERID = PSW_MIT_GF_MITARBEITERID" "~"
if joinKuerzel = MITPSW
searchResult := searchResult "LEFT JOIN BUERO.PSW_PERSONENWERTE ON PSW_MIT_GF_FIRMENNR = MIT_FIRMENNR AND PSW_MIT_GF_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = RNGMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = RNG_V1_FIRMENNR AND MIT_MITARBEITERID = RNG_V1_MITARBEITERID" "~"
if joinKuerzel = MITRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_V1_FIRMENNR = MIT_FIRMENNR AND RNG_V1_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = FSTMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = FST_AL_FIRMENNR AND MIT_MITARBEITERID = FST_AL_MITARBEITERID" "~"
if joinKuerzel = MITFST
searchResult := searchResult "LEFT JOIN BUERO.FST_FRIST ON FST_AL_FIRMENNR = MIT_FIRMENNR AND FST_AL_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = KMAMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = KMA_TEAMLEITER_FIRMENNR AND MIT_MITARBEITERID = KMA_TEAM_MITARBEITERID" "~"
if joinKuerzel = MITKMA
searchResult := searchResult "LEFT JOIN WWS.KMA_KUNDENMITARBBEZUG ON KMA_TEAMLEITER_FIRMENNR = MIT_FIRMENNR AND KMA_TEAM_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = VASMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = VAS_GF_FIRMENNR AND MIT_MITARBEITERID = VAS_GF_MITARBEITERID" "~"
if joinKuerzel = MITVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_GF_FIRMENNR = MIT_FIRMENNR AND VAS_GF_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = AVEMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = AVE_V3_FIRMENNR AND MIT_MITARBEITERID = AVE_V3_MITARBEITERID" "~"
if joinKuerzel = MITAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_V3_FIRMENNR = MIT_FIRMENNR AND AVE_V3_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = LEIMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = LEI_GF_PER_FIRMENNR AND MIT_MITARBEITERID = LEI_GF_PER_MITARBEITERID" "~"
if joinKuerzel = MITLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_GF_PER_FIRMENNR = MIT_FIRMENNR AND LEI_GF_PER_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = PROMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = PRO_MIT_RV_FIRMENNR AND MIT_MITARBEITERID = PRO_MIT_RV_MITARBEITERID" "~"
if joinKuerzel = MITPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_MIT_RV_FIRMENNR = MIT_FIRMENNR AND PRO_MIT_RV_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = PVFMIK
searchResult := searchResult "LEFT JOIN BUERO.MIK_MITARBEITERKARRIERE ON MIK_FIRMENNR = PVF_MIK_FIRMENNR AND MIK_KARRIERELFDNR = PVF_KARRIERELFDNR" "~"
if joinKuerzel = MIKPVF
searchResult := searchResult "LEFT JOIN BUERO.PVF_PERSONENVORERFASSUNG ON PVF_MIK_FIRMENNR = MIK_FIRMENNR AND PVF_KARRIERELFDNR = MIK_KARRIERELFDNR" "~"
if joinKuerzel = AMZMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = AMZ_MIT_FIRMENNR AND MIT_MITARBEITERID = AMZ_MITARBEITERID" "~"
if joinKuerzel = MITAMZ
searchResult := searchResult "LEFT JOIN BUERO.AMZ_AUSDRUCKMODELLZU ON AMZ_MIT_FIRMENNR = MIT_FIRMENNR AND AMZ_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = AWSMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = AWS_MIT_FIRMENNR AND MIT_MITARBEITERID = AWS_MIT_MITARBEITERID" "~"
if joinKuerzel = MITAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_MIT_FIRMENNR = MIT_FIRMENNR AND AWS_MIT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = BETMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = BET_MIT_FIRMENNR AND MIT_MITARBEITERID = BET_MIT_MITARBEITERID" "~"
if joinKuerzel = MITBET
searchResult := searchResult "LEFT JOIN BUERO.BET_BETRIEBSMITTEL ON BET_MIT_FIRMENNR = MIT_FIRMENNR AND BET_MIT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = CAWMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = CAW_MIT_FIRMENNR AND MIT_MITARBEITERID = CAW_MIT_MITARBEITERID" "~"
if joinKuerzel = MITCAW
searchResult := searchResult "LEFT JOIN BUERO.CAW_CRMABWESENHEIT ON CAW_MIT_FIRMENNR = MIT_FIRMENNR AND CAW_MIT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = DISMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = DIS_KOMMISS_FIRMENNR AND MIT_MITARBEITERID = DIS_KOMMISS_MITARBEITERID" "~"
if joinKuerzel = MITDIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_KOMMISS_FIRMENNR = MIT_FIRMENNR AND DIS_KOMMISS_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = DOKMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = DOK_MIT_FIRMENNR AND MIT_MITARBEITERID = DOK_MITARBEITERID" "~"
if joinKuerzel = MITDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_MIT_FIRMENNR = MIT_FIRMENNR AND DOK_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = DRGMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = DRG_GENEHMIGER_FIRMENNR AND MIT_MITARBEITERID = DRG_GENEHMIGER" "~"
if joinKuerzel = MITDRG
searchResult := searchResult "LEFT JOIN HRS.DRG_DIENSTREISEGEN ON DRG_GENEHMIGER_FIRMENNR = MIT_FIRMENNR AND DRG_GENEHMIGER = MIT_MITARBEITERID" "~"
if joinKuerzel = DVHMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = DVH_MIT_FIRMENNR AND MIT_MITARBEITERID = DVH_MITARBEITERID" "~"
if joinKuerzel = MITDVH
searchResult := searchResult "LEFT JOIN BUERO.DVH_DIENSTVERHAELTNIS ON DVH_MIT_FIRMENNR = MIT_FIRMENNR AND DVH_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = KOSMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = KOS_MIT_FIRMENNR AND MIT_MITARBEITERID = KOS_MIT_MITARBEITERID" "~"
if joinKuerzel = MITKOS
searchResult := searchResult "LEFT JOIN ZLE.KOS_KOSTENFAKTOR ON KOS_MIT_FIRMENNR = MIT_FIRMENNR AND KOS_MIT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = LZFMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = LZF_MIT_FIRMENNR AND MIT_MITARBEITERID = LZF_MITARBEITERID" "~"
if joinKuerzel = MITLZF
searchResult := searchResult "LEFT JOIN HRS.LZF_LOHNZETTEL_FIN ON LZF_MIT_FIRMENNR = MIT_FIRMENNR AND LZF_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = QUSMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = QUS_MIT_FIRMENNR AND MIT_MITARBEITERID = QUS_MITARBEITERID" "~"
if joinKuerzel = MITQUS
searchResult := searchResult "LEFT JOIN BUERO.QUS_QUOTENSUMME ON QUS_MIT_FIRMENNR = MIT_FIRMENNR AND QUS_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = TLKMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = TLK_MIT_FIRMENNR AND MIT_MITARBEITERID = TLK_MITARBEITERID" "~"
if joinKuerzel = MITTLK
searchResult := searchResult "LEFT JOIN PRODUKTION.TLK_BT_LOGISTIK_KOPF ON TLK_MIT_FIRMENNR = MIT_FIRMENNR AND TLK_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = UBEMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = UBE_BEARB_FIRMENNR AND MIT_MITARBEITERID = UBE_BEARB_MITARBEITERID" "~"
if joinKuerzel = MITUBE
searchResult := searchResult "LEFT JOIN BMDINTERN.UBE_UPDATEBESTELLUNG ON UBE_BEARB_FIRMENNR = MIT_FIRMENNR AND UBE_BEARB_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = WAPMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = WAP_KOMMISS_FIRMENNR AND MIT_MITARBEITERID = WAP_KOMMISS_MITARBEITERID" "~"
if joinKuerzel = MITWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_KOMMISS_FIRMENNR = MIT_FIRMENNR AND WAP_KOMMISS_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = AWSMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = AWS_GF_MIT_FIRMENNR AND MIT_MITARBEITERID = AWS_GF_MIT_MITARBEITERID" "~"
if joinKuerzel = MITAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_GF_MIT_FIRMENNR = MIT_FIRMENNR AND AWS_GF_MIT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = HNPMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = HNP_AL_FIRMENNR AND MIT_MITARBEITERID = HNP_AL_MITARBEITERID" "~"
if joinKuerzel = MITHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_AL_FIRMENNR = MIT_FIRMENNR AND HNP_AL_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = UBEMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = UBE_SENDER_FIRMENNR AND MIT_MITARBEITERID = UBE_SENDER_MITARBEITERID" "~"
if joinKuerzel = MITUBE
searchResult := searchResult "LEFT JOIN BMDINTERN.UBE_UPDATEBESTELLUNG ON UBE_SENDER_FIRMENNR = MIT_FIRMENNR AND UBE_SENDER_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = AVEMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = AVE_V2_FIRMENNR AND MIT_MITARBEITERID = AVE_V2_MITARBEITERID" "~"
if joinKuerzel = MITAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_V2_FIRMENNR = MIT_FIRMENNR AND AVE_V2_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = ELVLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = ELV_LAG_FIRMENNR AND LAP_LAGERNR = ELV_LAGERNR AND LAP_LAGERPLATZ = ELV_LAGERPLATZ" "~"
if joinKuerzel = LAPELV
searchResult := searchResult "LEFT JOIN WWS.ELV_ERFASSPOSLIEFERVOR ON ELV_LAG_FIRMENNR = LAP_FIRMENNR AND ELV_LAGERNR = LAP_LAGERNR AND ELV_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = EROLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = ERO_LAG_FIRMENNR AND LAP_LAGERNR = ERO_LAG_LAGERNR AND LAP_LAGERPLATZ = ERO_LAGERPLATZ" "~"
if joinKuerzel = LAPERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_LAG_FIRMENNR = LAP_FIRMENNR AND ERO_LAG_LAGERNR = LAP_LAGERNR AND ERO_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = LPWLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = LPW_FIRMENNR AND LAP_LAGERNR = LPW_LAGERNR AND LAP_LAGERPLATZ = LPW_LAGERPLATZ" "~"
if joinKuerzel = LAPLPW
searchResult := searchResult "LEFT JOIN WWS.LPW_LAGERPLATZWERTE ON LPW_FIRMENNR = LAP_FIRMENNR AND LPW_LAGERNR = LAP_LAGERNR AND LPW_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = TLALAP
searchResult := searchResult "LEFT JOIN LAP_LAGERPLATZ.LAP_LAGERPLATZ ON LAP_FIRMENNR = TLA_SRC_LAG_FIRMENNR AND LAP_LAGERNR = TLA_SRC_LAGERNR AND LAP_LAGERPLATZ = TLA_SRC_LAGERPLATZ" "~"
if joinKuerzel = LAPTLA
searchResult := searchResult "LEFT JOIN PRODUKTION.TLA_BT_LOGISTIK_ANGEFORDERT ON TLA_SRC_LAG_FIRMENNR = LAP_FIRMENNR AND TLA_SRC_LAGERNR = LAP_LAGERNR AND TLA_SRC_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = EPSLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = EPS_LAG_ZIEL_FIRMENNR AND LAP_LAGERNR = EPS_ZIELLAGERNR AND LAP_LAGERPLATZ = EPS_ZIELLAGERPLATZ" "~"
if joinKuerzel = LAPEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_LAG_ZIEL_FIRMENNR = LAP_FIRMENNR AND EPS_ZIELLAGERNR = LAP_LAGERNR AND EPS_ZIELLAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = ERZLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = ERZ_VORSCHLAG_LAGER_FIRMEN AND LAP_LAGERNR = ERZ_VORSCHLAG_LAGERNR AND LAP_LAGERPLATZ = ERZ_VORSCHLAG_LAGERPLATZ" "~"
if joinKuerzel = LAPERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_VORSCHLAG_LAGER_FIRMEN = LAP_FIRMENNR AND ERZ_VORSCHLAG_LAGERNR = LAP_LAGERNR AND ERZ_VORSCHLAG_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = LAJLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = LAJ_ARTIKELFIRMENNR AND LAP_LAGERNR = LAJ_UMB_LAGERNR AND LAP_LAGERPLATZ = LAJ_UMB_LAGERPLATZ" "~"
if joinKuerzel = LAPLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_ARTIKELFIRMENNR = LAP_FIRMENNR AND LAJ_UMB_LAGERNR = LAP_LAGERNR AND LAJ_UMB_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = ABMLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = ABM_LAQ_LOHNARTID" "~"
if joinKuerzel = LAQABM
searchResult := searchResult "LEFT JOIN HRS.ABM_ABWESENHEIT ON ABM_LAQ_LOHNARTID = LAQ_LOHNARTID" "~"
if joinKuerzel = BFILAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = BFI_WEIHNACHTREMUN_LOHNART" "~"
if joinKuerzel = LAQBFI
searchResult := searchResult "LEFT JOIN HRS.BFI_BUDGETFIRMA ON BFI_WEIHNACHTREMUN_LOHNART = LAQ_LOHNARTID" "~"
if joinKuerzel = ERXLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = ERX_LOHNARTID" "~"
if joinKuerzel = LAQERX
searchResult := searchResult "LEFT JOIN HRS.ERX_ERFASSUNGSZUSATZ ON ERX_LOHNARTID = LAQ_LOHNARTID" "~"
if joinKuerzel = LGZLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = LGZ_LOHNARTID" "~"
if joinKuerzel = LAQLGZ
searchResult := searchResult "LEFT JOIN HRS.LGZ_LOHNARTGRUPPEZU ON LGZ_LOHNARTID = LAQ_LOHNARTID" "~"
if joinKuerzel = PSMLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = PSM_NETTOBRUTTO_LOHNARTID" "~"
if joinKuerzel = LAQPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_NETTOBRUTTO_LOHNARTID = LAQ_LOHNARTID" "~"
if joinKuerzel = EFPLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = EFP_LOHNART1" "~"
if joinKuerzel = LAQEFP
searchResult := searchResult "LEFT JOIN HRS.EFP_ENTGELTFORTZAHLPROZ ON EFP_LOHNART1 = LAQ_LOHNARTID" "~"
if joinKuerzel = LAWLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = LAW_LK_ADD_LA_5" "~"
if joinKuerzel = LAQLAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_LK_ADD_LA_5 = LAQ_LOHNARTID" "~"
if joinKuerzel = LAWLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = LAW_GRUNDLOHN_PAR68_1_2" "~"
if joinKuerzel = LAQLAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_GRUNDLOHN_PAR68_1_2 = LAQ_LOHNARTID" "~"
if joinKuerzel = EFPLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = EFP_LOHNART2" "~"
if joinKuerzel = LAQEFP
searchResult := searchResult "LEFT JOIN HRS.EFP_ENTGELTFORTZAHLPROZ ON EFP_LOHNART2 = LAQ_LOHNARTID" "~"
if joinKuerzel = EFPLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = EFP_LOHNART4" "~"
if joinKuerzel = LAQEFP
searchResult := searchResult "LEFT JOIN HRS.EFP_ENTGELTFORTZAHLPROZ ON EFP_LOHNART4 = LAQ_LOHNARTID" "~"
if joinKuerzel = LAWLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = LAW_LK_ADD_LA_1" "~"
if joinKuerzel = LAQLAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_LK_ADD_LA_1 = LAQ_LOHNARTID" "~"
if joinKuerzel = RNPLEI
searchResult := searchResult "LEFT JOIN LEI_LEISTUNG.LEI_LEISTUNG ON LEI_LEISTUNGLFDNR = RNP_LEISTUNGLFDNR" "~"
if joinKuerzel = LEIRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_LEISTUNGLFDNR = LEI_LEISTUNGLFDNR" "~"
if joinKuerzel = LEILEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_LEISTUNGLFDNR = LEI_TRAK_LEISTUNGLFDNR" "~"
if joinKuerzel = LEILEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_TRAK_LEISTUNGLFDNR = LEI_LEISTUNGLFDNR" "~"
if joinKuerzel = EAVLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = EAV_FIRMENNR AND LFI_LOHNJAHR = EAV_JAHR" "~"
if joinKuerzel = LFIEAV
searchResult := searchResult "LEFT JOIN HRS.EAV_ELDADETAIL_AV ON EAV_FIRMENNR = LFI_FIRMENNR AND EAV_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = EBNLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = EBN_FIRMENNR AND LFI_LOHNJAHR = EBN_LOHNJAHR" "~"
if joinKuerzel = LFIEBN
searchResult := searchResult "LEFT JOIN HRS.EBN_ELDADETAIL_BN ON EBN_FIRMENNR = LFI_FIRMENNR AND EBN_LOHNJAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = ESDMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = ESD_MIT_FIRMENNR AND MIT_MITARBEITERID = ESD_MITARBEITERID" "~"
if joinKuerzel = MITESD
searchResult := searchResult "LEFT JOIN WWS.ESD_ERFASSUNGSERVICEDATEN ON ESD_MIT_FIRMENNR = MIT_FIRMENNR AND ESD_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = FGAMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = FGA_MIT_FIRMENNR AND MIT_MITARBEITERID = FGA_MITARBEITERID" "~"
if joinKuerzel = MITFGA
searchResult := searchResult "LEFT JOIN BUERO.FGA_FRAGEBOGENAUSWAHL ON FGA_MIT_FIRMENNR = MIT_FIRMENNR AND FGA_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = GVMMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = GVM_MIT_FIRMENNR AND MIT_MITARBEITERID = GVM_MITARBEITERID" "~"
if joinKuerzel = MITGVM
searchResult := searchResult "LEFT JOIN BUERO.GVM_GHOSTVOLLMACHT ON GVM_MIT_FIRMENNR = MIT_FIRMENNR AND GVM_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = KAKMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = KAK_V1_FIRMENNR AND MIT_MITARBEITERID = KAK_V1_MITARBEITERID" "~"
if joinKuerzel = MITKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_V1_FIRMENNR = MIT_FIRMENNR AND KAK_V1_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = KAPMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = KAP_VERK_FIRMENNR AND MIT_MITARBEITERID = KAP_VERK_MITARBEITERID" "~"
if joinKuerzel = MITKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_VERK_FIRMENNR = MIT_FIRMENNR AND KAP_VERK_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = KASMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = KAS_KASSIER_FIRMENNR AND MIT_MITARBEITERID = KAS_KASSIER_MITARBEITERID" "~"
if joinKuerzel = MITKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_KASSIER_FIRMENNR = MIT_FIRMENNR AND KAS_KASSIER_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = MLSMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = MLS_MIT_FIRMENNR AND MIT_MITARBEITERID = MLS_MIT_MITARBEITERID" "~"
if joinKuerzel = MITMLS
searchResult := searchResult "LEFT JOIN ZLE.MLS_MITARBLEISTTAUSCHSATZ ON MLS_MIT_FIRMENNR = MIT_FIRMENNR AND MLS_MIT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = MUZMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = MUZ_FIRMENNR AND MIT_MITARBEITERID = MUZ_MITARBEITERID" "~"
if joinKuerzel = MITMUZ
searchResult := searchResult "LEFT JOIN BUERO.MUZ_MITARBUSERZU ON MUZ_FIRMENNR = MIT_FIRMENNR AND MUZ_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = PCPMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = PCP_MIT_FIRMENNR AND MIT_MITARBEITERID = PCP_MITARBEITERID" "~"
if joinKuerzel = MITPCP
searchResult := searchResult "LEFT JOIN PRODUKTION.PCP_PPS_PRODUKT ON PCP_MIT_FIRMENNR = MIT_FIRMENNR AND PCP_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = PLIMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = PLI_MIT_FIRMENNR AND MIT_MITARBEITERID = PLI_MITARBEITERID" "~"
if joinKuerzel = MITPLI
searchResult := searchResult "LEFT JOIN BUERO.PLI_PIPELINE ON PLI_MIT_FIRMENNR = MIT_FIRMENNR AND PLI_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = POSMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = POS_MIT_FIRMENNR2 AND MIT_MITARBEITERID = POS_MIT_MITARBEITERID2" "~"
if joinKuerzel = MITPOS
searchResult := searchResult "LEFT JOIN BUERO.POS_POST ON POS_MIT_FIRMENNR2 = MIT_FIRMENNR AND POS_MIT_MITARBEITERID2 = MIT_MITARBEITERID" "~"
if joinKuerzel = PPFMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = PPF_MIT_FIRMENNR AND MIT_MITARBEITERID = PPF_MITARBEITERID" "~"
if joinKuerzel = MITPPF
searchResult := searchResult "LEFT JOIN BUERO.PPF_PERSONENPRUEFUNG ON PPF_MIT_FIRMENNR = MIT_FIRMENNR AND PPF_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = PRPMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = PRP_VERTR_FIRMENNR AND MIT_MITARBEITERID = PRP_VERTR_MITARBEITERID" "~"
if joinKuerzel = MITPRP
searchResult := searchResult "LEFT JOIN WWS.PRP_PROVISIONTABPOS ON PRP_VERTR_FIRMENNR = MIT_FIRMENNR AND PRP_VERTR_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = PRTMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = PRT_MIT_FIRMENNR AND MIT_MITARBEITERID = PRT_MITARBEITERID" "~"
if joinKuerzel = MITPRT
searchResult := searchResult "LEFT JOIN BUERO.PRT_PROJEKTTAETIGKEIT ON PRT_MIT_FIRMENNR = MIT_FIRMENNR AND PRT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = QUOMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = QUO_MIT_FIRMENNR AND MIT_MITARBEITERID = QUO_MIT_MITARBEITERID" "~"
if joinKuerzel = MITQUO
searchResult := searchResult "LEFT JOIN BUERO.QUO_QUOTE ON QUO_MIT_FIRMENNR = MIT_FIRMENNR AND QUO_MIT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = RNPMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = RNP_MITARBEITER_FIRMENNR AND MIT_MITARBEITERID = RNP_MITARBEITERID" "~"
if joinKuerzel = MITRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_MITARBEITER_FIRMENNR = MIT_FIRMENNR AND RNP_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = RPRMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = RPR_VERTRETER_FIRMENNR AND MIT_MITARBEITERID = RPR_VERTRETER_MITARBEITERID" "~"
if joinKuerzel = MITRPR
searchResult := searchResult "LEFT JOIN WWS.RPR_RECHNUNGSPROVISION ON RPR_VERTRETER_FIRMENNR = MIT_FIRMENNR AND RPR_VERTRETER_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = SEKMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = SEK_MIT_FIRMENNR AND MIT_MITARBEITERID = SEK_MITARBEITERID" "~"
if joinKuerzel = MITSEK
searchResult := searchResult "LEFT JOIN BUERO.SEK_SEKRETARIAT ON SEK_MIT_FIRMENNR = MIT_FIRMENNR AND SEK_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = TCAMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = TCA_MIT_FIRMENNR AND MIT_MITARBEITERID = TCA_MITARBEITERID" "~"
if joinKuerzel = MITTCA
searchResult := searchResult "LEFT JOIN BMD.TCA_TCACTIONLIST ON TCA_MIT_FIRMENNR = MIT_FIRMENNR AND TCA_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = PLGMIK
searchResult := searchResult "LEFT JOIN BUERO.MIK_MITARBEITERKARRIERE ON MIK_FIRMENNR = PLG_MIK_FIRMENNR AND MIK_KARRIERELFDNR = PLG_MIK_KARRIERELFDNR" "~"
if joinKuerzel = MIKPLG
searchResult := searchResult "LEFT JOIN BUERO.PLG_PLANGRUPPE ON PLG_MIK_FIRMENNR = MIK_FIRMENNR AND PLG_MIK_KARRIERELFDNR = MIK_KARRIERELFDNR" "~"
if joinKuerzel = ZMDMIK
searchResult := searchResult "LEFT JOIN BUERO.MIK_MITARBEITERKARRIERE ON MIK_FIRMENNR = ZMD_KARRIERE_FIRMENNR AND MIK_KARRIERELFDNR = ZMD_KARRIERELFDNR" "~"
if joinKuerzel = MIKZMD
searchResult := searchResult "LEFT JOIN BUERO.ZMD_ZEITMINDESTBESETZDET ON ZMD_KARRIERE_FIRMENNR = MIK_FIRMENNR AND ZMD_KARRIERELFDNR = MIK_KARRIERELFDNR" "~"
if joinKuerzel = AFTMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = AFT_MIT_FIRMENNR AND MIT_MITARBEITERID = AFT_MITARBEITERID" "~"
if joinKuerzel = MITAFT
searchResult := searchResult "LEFT JOIN ZLE.AFT_AUFBEREITUNG ON AFT_MIT_FIRMENNR = MIT_FIRMENNR AND AFT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = BMIMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = BMI_MIT_FIRMENNR AND MIT_MITARBEITERID = BMI_MITARBEITERID" "~"
if joinKuerzel = MITBMI
searchResult := searchResult "LEFT JOIN HRS.BMI_BUDGETMITARBEITER ON BMI_MIT_FIRMENNR = MIT_FIRMENNR AND BMI_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = BOPMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = BOP_VERK_FIRMENNR AND MIT_MITARBEITERID = BOP_VERK_MITARBEITERID" "~"
if joinKuerzel = MITBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_VERK_FIRMENNR = MIT_FIRMENNR AND BOP_VERK_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = BRZMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = BRZ_MIT_FIRMENNR AND MIT_MITARBEITERID = BRZ_MIT_MITARBEITERID" "~"
if joinKuerzel = MITBRZ
searchResult := searchResult "LEFT JOIN BUERO.BRZ_BUEROBERECHTZU ON BRZ_MIT_FIRMENNR = MIT_FIRMENNR AND BRZ_MIT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = BWHMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = BWH_MIT_FIRMENNR AND MIT_MITARBEITERID = BWH_MITARBEITERID" "~"
if joinKuerzel = MITBWH
searchResult := searchResult "LEFT JOIN BUERO.BWH_BEWERBERHISTORIE ON BWH_MIT_FIRMENNR = MIT_FIRMENNR AND BWH_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = CBRMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = CBR_MIT_FIRMENNR AND MIT_MITARBEITERID = CBR_MITARBID" "~"
if joinKuerzel = MITCBR
searchResult := searchResult "LEFT JOIN BUERO.CBR_CRMABWBENACHREGEL ON CBR_MIT_FIRMENNR = MIT_FIRMENNR AND CBR_MITARBID = MIT_MITARBEITERID" "~"
if joinKuerzel = EPRMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = EPR_VERTRETER_FIRMENNR AND MIT_MITARBEITERID = EPR_VERTRETER_MITARBEITERID" "~"
if joinKuerzel = MITEPR
searchResult := searchResult "LEFT JOIN WWS.EPR_ERFASSPROVISION ON EPR_VERTRETER_FIRMENNR = MIT_FIRMENNR AND EPR_VERTRETER_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = EPSMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = EPS_KOMMISS_MIT_FIRMENNR AND MIT_MITARBEITERID = EPS_KOMMISS_MITARBEITERID" "~"
if joinKuerzel = MITEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_KOMMISS_MIT_FIRMENNR = MIT_FIRMENNR AND EPS_KOMMISS_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = FIRMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = FIR_MIT_FIRMENNR AND MIT_MITARBEITERID = FIR_MITARBEITERID" "~"
if joinKuerzel = MITFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_MIT_FIRMENNR = MIT_FIRMENNR AND FIR_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = GWIMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = GWI_MIT_FIRMENNR AND MIT_MITARBEITERID = GWI_MITARBEITERID" "~"
if joinKuerzel = MITGWI
searchResult := searchResult "LEFT JOIN BUERO.GWI_GELDWIEREG ON GWI_MIT_FIRMENNR = MIT_FIRMENNR AND GWI_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = KEPMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = KEP_MIT_FIRMENNR AND MIT_MITARBEITERID = KEP_MITARBEITERID" "~"
if joinKuerzel = MITKEP
searchResult := searchResult "LEFT JOIN WWS.KEP_KASSENERFASSUNGSPROT ON KEP_MIT_FIRMENNR = MIT_FIRMENNR AND KEP_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = KMAMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = KMA_GF_FIRMENNR AND MIT_MITARBEITERID = KMA_GF_MITARBEITERID" "~"
if joinKuerzel = MITKMA
searchResult := searchResult "LEFT JOIN WWS.KMA_KUNDENMITARBBEZUG ON KMA_GF_FIRMENNR = MIT_FIRMENNR AND KMA_GF_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = KOPMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = KOP_MIT_FIRMENNR AND MIT_MITARBEITERID = KOP_MITARBEITERID" "~"
if joinKuerzel = MITKOP
searchResult := searchResult "LEFT JOIN PRODUKTION.KOP_KOMMISSIONIERUNG_POS ON KOP_MIT_FIRMENNR = MIT_FIRMENNR AND KOP_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = KSDMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = KSD_KASSIER_FIRMENNR AND MIT_MITARBEITERID = KSD_KASSIER_MITARBEITERID" "~"
if joinKuerzel = MITKSD
searchResult := searchResult "LEFT JOIN WWS.KSD_KASSENSUMMENDETAIL ON KSD_KASSIER_FIRMENNR = MIT_FIRMENNR AND KSD_KASSIER_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = KSLMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = KSL_MIT_FIRMENNR AND MIT_MITARBEITERID = KSL_MITARBEITERID" "~"
if joinKuerzel = MITKSL
searchResult := searchResult "LEFT JOIN BUERO.KSL_KONTAKTSYNCEINST ON KSL_MIT_FIRMENNR = MIT_FIRMENNR AND KSL_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = KZAMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = KZA_VERTRETERFIRMENNR AND MIT_MITARBEITERID = KZA_VERTRETERMITARBEITERID" "~"
if joinKuerzel = MITKZA
searchResult := searchResult "LEFT JOIN WWS.KZA_KASSENZAHLUNGSARTENZU ON KZA_VERTRETERFIRMENNR = MIT_FIRMENNR AND KZA_VERTRETERMITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = LEDMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = LED_MIT_FIRMENNR AND MIT_MITARBEITERID = LED_MITARBEITERID" "~"
if joinKuerzel = MITLED
searchResult := searchResult "LEFT JOIN BUERO.LED_LEAD ON LED_MIT_FIRMENNR = MIT_FIRMENNR AND LED_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = LEIMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = LEI_ORG_MIT_FIRMENNR AND MIT_MITARBEITERID = LEI_ORG_MITARBEITERID" "~"
if joinKuerzel = MITLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_ORG_MIT_FIRMENNR = MIT_FIRMENNR AND LEI_ORG_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = KAKMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = KAK_MIT_FIRMENNR AND MIT_MITARBEITERID = KAK_MIT_MITARBEITERID" "~"
if joinKuerzel = MITKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_MIT_FIRMENNR = MIT_FIRMENNR AND KAK_MIT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = DZAMON
searchResult := searchResult "LEFT JOIN MON_MONAT.MON_MONAT ON MON_MONAT = DZA_MONAT" "~"
if joinKuerzel = MONDZA
searchResult := searchResult "LEFT JOIN HRS.DZA_ANRECHNUNG ON DZA_MONAT = MON_MONAT" "~"
if joinKuerzel = HNPMRV
searchResult := searchResult "LEFT JOIN ZLE.MRV_MONATSRECHNUNGVORLAGE ON MRV_MRVID = HNP_MRVID" "~"
if joinKuerzel = MRVHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_MRVID = MRV_MRVID" "~"
if joinKuerzel = NGZNOG
searchResult := searchResult "LEFT JOIN BUERO.NOG_NOTIZGRUPPE ON NOG_NOTIZGRUPPENR = NGZ_NOTIZGRUPPENR" "~"
if joinKuerzel = NOGNGZ
searchResult := searchResult "LEFT JOIN BUERO.NGZ_NOTIZGRUPPEZU ON NGZ_NOTIZGRUPPENR = NOG_NOTIZGRUPPENR" "~"
if joinKuerzel = DOKNOT
searchResult := searchResult "LEFT JOIN BUERO.NOT_NOTIZ ON NOT_NOTIZNR = DOK_NOTIZNR" "~"
if joinKuerzel = NOTDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_NOTIZNR = NOT_NOTIZNR" "~"
if joinKuerzel = DFUNTC
searchResult := searchResult "LEFT JOIN NTC_NTCSARTIKEL.NTC_NTCSARTIKEL ON NTC_NTCSBMDPAKETNR = DFU_NTCSBMDPAKETNR" "~"
if joinKuerzel = NTCDFU
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFU_DFUNKTION ON DFU_NTCSBMDPAKETNR = NTC_NTCSBMDPAKETNR" "~"
if joinKuerzel = OBLOBP
searchResult := searchResult "LEFT JOIN BUERO.OBP_ONLBEWPERSSTAMM ON OBP_PERSSTAMMID = OBL_OBP_PERSSTAMMID" "~"
if joinKuerzel = OBPOBL
searchResult := searchResult "LEFT JOIN BUERO.OBL_ONLBEWLEBENSLAUF ON OBL_OBP_PERSSTAMMID = OBP_PERSSTAMMID" "~"
if joinKuerzel = OPEOBZ
searchResult := searchResult "LEFT JOIN LOHN.OBZ_OF_BANKLEITZAHL ON OBZ_BLZ = OPE_BLZ" "~"
if joinKuerzel = OBZOPE
searchResult := searchResult "LEFT JOIN LOHN.OPE_OF_PERSON ON OPE_BLZ = OBZ_BLZ" "~"
if joinKuerzel = OKTOKL
searchResult := searchResult "LEFT JOIN LOHN.OKL_OF_KLIENT ON OKL_KLIENTENNR = OKT_KLIENTENNR" "~"
if joinKuerzel = OKLOKT
searchResult := searchResult "LEFT JOIN LOHN.OKT_OF_KOSTENTRAEGER ON OKT_KLIENTENNR = OKL_KLIENTENNR" "~"
if joinKuerzel = OVEOKL
searchResult := searchResult "LEFT JOIN LOHN.OKL_OF_KLIENT ON OKL_KLIENTENNR = OVE_KLIENTENNR" "~"
if joinKuerzel = OKLOVE
searchResult := searchResult "LEFT JOIN LOHN.OVE_OF_VERANTWORTLICHER ON OVE_KLIENTENNR = OKL_KLIENTENNR" "~"
if joinKuerzel = OLEOKS
searchResult := searchResult "LEFT JOIN LOHN.OKS_OF_KOSTENSTELLE ON OKS_KLIENTENNR = OLE_KLIENTENNR AND OKS_KOSTENSTELLENNR = OLE_OKS_KOSTENSTELLENNR" "~"
if joinKuerzel = OKSOLE
searchResult := searchResult "LEFT JOIN LOHN.OLE_OF_LEISTUNG ON OLE_KLIENTENNR = OKS_KLIENTENNR AND OLE_OKS_KOSTENSTELLENNR = OKS_KOSTENSTELLENNR" "~"
if joinKuerzel = OPKOKS
searchResult := searchResult "LEFT JOIN LOHN.OKS_OF_KOSTENSTELLE ON OKS_KLIENTENNR = OPK_KLIENTENNR AND OKS_KOSTENSTELLENNR = OPK_KOSTENSTELLENNR" "~"
if joinKuerzel = OKSOPK
searchResult := searchResult "LEFT JOIN LOHN.OPK_OF_PROZKO ON OPK_KLIENTENNR = OKS_KLIENTENNR AND OPK_KOSTENSTELLENNR = OKS_KOSTENSTELLENNR" "~"
if joinKuerzel = OLAOKV
searchResult := searchResult "LEFT JOIN LOHN.OKV_OF_KOLLEKTIV ON OKV_OFKVID = OLA_OKV_OFKVID" "~"
if joinKuerzel = OKVOLA
searchResult := searchResult "LEFT JOIN LOHN.OLA_OF_LOHNART ON OLA_OKV_OFKVID = OKV_OFKVID" "~"
if joinKuerzel = OLEOLA
searchResult := searchResult "LEFT JOIN LOHN.OLA_OF_LOHNART ON OLA_KLIENTENNR = OLE_KLIENTENNR AND OLA_OKV_OFKVID = OLE_OLA_OFKVID AND OLA_LANR = OLE_OLA_LANR" "~"
if joinKuerzel = OLAOLE
searchResult := searchResult "LEFT JOIN LOHN.OLE_OF_LEISTUNG ON OLE_KLIENTENNR = OLA_KLIENTENNR AND OLE_OLA_OFKVID = OLA_OKV_OFKVID AND OLE_OLA_LANR = OLA_LANR" "~"
if joinKuerzel = OFMOPE
searchResult := searchResult "LEFT JOIN LOHN.OPE_OF_PERSON ON OPE_KLIENTENNR = OFM_KLIENTENNR AND OPE_PERSNR = OFM_PERSNR AND OPE_PERSUNTERNR = OFM_PERSUNTERNR" "~"
if joinKuerzel = OPEOFM
searchResult := searchResult "LEFT JOIN LOHN.OFM_OF_FAMILIE ON OFM_KLIENTENNR = OPE_KLIENTENNR AND OFM_PERSNR = OPE_PERSNR AND OFM_PERSUNTERNR = OPE_PERSUNTERNR" "~"
if joinKuerzel = OTROPE
searchResult := searchResult "LEFT JOIN LOHN.OPE_OF_PERSON ON OPE_KLIENTENNR = OTR_KLIENTENNR AND OPE_PERSNR = OTR_PERSNR AND OPE_PERSUNTERNR = OTR_PERSUNTERNR" "~"
if joinKuerzel = OPEOTR
searchResult := searchResult "LEFT JOIN LOHN.OTR_OF_TRANSFER ON OTR_KLIENTENNR = OPE_KLIENTENNR AND OTR_PERSNR = OPE_PERSNR AND OTR_PERSUNTERNR = OPE_PERSUNTERNR" "~"
if joinKuerzel = KAKORT
searchResult := searchResult "LEFT JOIN ORT_ORT.ORT_ORT ON ORT_LANDNR = KAK_ORT_LANDNR AND ORT_PLZ = KAK_ORT_PLZ AND ORT_PLZUNTERNR = KAK_ORT_PLZUNTERNR" "~"
if joinKuerzel = ORTKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_ORT_LANDNR = ORT_LANDNR AND KAK_ORT_PLZ = ORT_PLZ AND KAK_ORT_PLZUNTERNR = ORT_PLZUNTERNR" "~"
if joinKuerzel = PERORT
searchResult := searchResult "LEFT JOIN BUERO.ORT_ORT ON ORT_LANDNR = PER_FIRMSITZ_LANDNR AND ORT_PLZ = PER_FIRMSITZ_ORT_PLZ AND ORT_PLZUNTERNR = PER_FIRMSITZ_PLZUNTERNR" "~"
if joinKuerzel = ORTPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMSITZ_LANDNR = ORT_LANDNR AND PER_FIRMSITZ_ORT_PLZ = ORT_PLZ AND PER_FIRMSITZ_PLZUNTERNR = ORT_PLZUNTERNR" "~"
if joinKuerzel = FTBORT
searchResult := searchResult "LEFT JOIN BUERO.ORT_ORT ON ORT_LANDNR = FTB_ZIEL_ORT_LANDNR AND ORT_PLZ = FTB_ZIEL_ORT_PLZ AND ORT_PLZUNTERNR = FTB_ZIEL_ORT_PLZUNTERNR" "~"
if joinKuerzel = ORTFTB
searchResult := searchResult "LEFT JOIN BUERO.FTB_FAHRTENBUCH ON FTB_ZIEL_ORT_LANDNR = ORT_LANDNR AND FTB_ZIEL_ORT_PLZ = ORT_PLZ AND FTB_ZIEL_ORT_PLZUNTERNR = ORT_PLZUNTERNR" "~"
if joinKuerzel = OLEOVE
searchResult := searchResult "LEFT JOIN LOHN.OVE_OF_VERANTWORTLICHER ON OVE_KLIENTENNR = OLE_KLIENTENNR AND OVE_VERANTWNR = OLE_VERANTWNR" "~"
if joinKuerzel = OVEOLE
searchResult := searchResult "LEFT JOIN LOHN.OLE_OF_LEISTUNG ON OLE_KLIENTENNR = OVE_KLIENTENNR AND OLE_VERANTWNR = OVE_VERANTWNR" "~"
if joinKuerzel = PANPAB
searchResult := searchResult "LEFT JOIN WWS.PAB_PROVARTBAUM ON PAB_FIRMENNR = PAN_FIRMENNR AND PAB_PABLFDNR = PAN_PABLFDNR" "~"
if joinKuerzel = PABPAN
searchResult := searchResult "LEFT JOIN WWS.PAN_PROVARTBAUMKNOTEN ON PAN_FIRMENNR = PAB_FIRMENNR AND PAN_PABLFDNR = PAB_PABLFDNR" "~"
if joinKuerzel = KGBPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_PPSKAPAID = KGB_PCK_PPSKAPAID" "~"
if joinKuerzel = PCKKGB
searchResult := searchResult "LEFT JOIN PRODUKTION.KGB_KATZU_PPSKAPA ON KGB_PCK_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = LAJPCK
searchResult := searchResult "LEFT JOIN PCK_PPS_KAPA.PCK_PPS_KAPA ON PCK_PPSKAPAID = LAJ_PPSKAPAID" "~"
if joinKuerzel = PCKLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = LVEPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_PPSKAPAID = LVE_PPSKAPAID" "~"
if joinKuerzel = PCKLVE
searchResult := searchResult "LEFT JOIN PRODUKTION.LVE_LEISTUNGSVORERFASSUNG ON LVE_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = PPAPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_PPSKAPAID = PPA_PPSKAPAID" "~"
if joinKuerzel = PCKPPA
searchResult := searchResult "LEFT JOIN PRODUKTION.PPA_PSPPARAM ON PPA_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = SSPPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_PPSKAPAID = SSP_PPSKAPAID" "~"
if joinKuerzel = PCKSSP
searchResult := searchResult "LEFT JOIN PRODUKTION.SSP_SAMMELSCHEIN_POS ON SSP_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = VPZPCK
searchResult := searchResult "LEFT JOIN PCK_PPS_KAPA.PCK_PPS_KAPA ON PCK_PPSKAPAID = VPZ_PPSKAPAID" "~"
if joinKuerzel = PCKVPZ
searchResult := searchResult "LEFT JOIN BUERO.VPZ_VORGANGPHASEZU ON VPZ_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = RIAPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_PPSKAPAID = RIA_PPSKAPAID" "~"
if joinKuerzel = PCKRIA
searchResult := searchResult "LEFT JOIN PRODUKTION.RIA_RISIKOANALYSE ON RIA_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = SCMPCK
searchResult := searchResult "LEFT JOIN PCK_PPS_KAPA.PCK_PPS_KAPA ON PCK_PPSKAPAID = SCM_TEST_PPSKAPAID" "~"
if joinKuerzel = PCKSCM
searchResult := searchResult "LEFT JOIN BMDENTW.SCM_SCRMAIN ON SCM_TEST_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = PILPDG
searchResult := searchResult "LEFT JOIN WWS.PDG_PICDYNAMICGROUP ON PDG_PDGID = PIL_PDGID_PID" "~"
if joinKuerzel = PDGPIL
searchResult := searchResult "LEFT JOIN WWS.PIL_PICKUNDE ON PIL_PDGID_PID = PDG_PDGID" "~"
if joinKuerzel = PIRPDG
searchResult := searchResult "LEFT JOIN WWS.PDG_PICDYNAMICGROUP ON PDG_PDGID = PIR_PDGID" "~"
if joinKuerzel = PDGPIR
searchResult := searchResult "LEFT JOIN WWS.PIR_PICRABATTDETAIL ON PIR_PDGID = PDG_PDGID" "~"
if joinKuerzel = PILPDG
searchResult := searchResult "LEFT JOIN WWS.PDG_PICDYNAMICGROUP ON PDG_PDGID = PIL_PDGID_PIR" "~"
if joinKuerzel = PDGPIL
searchResult := searchResult "LEFT JOIN WWS.PIL_PICKUNDE ON PIL_PDGID_PIR = PDG_PDGID" "~"
if joinKuerzel = PKKPDS
searchResult := searchResult "LEFT JOIN BIL.PDS_PERSKORST_DNSTAMM ON PDS_PDSID = PKK_PDSID" "~"
if joinKuerzel = PDSPKK
searchResult := searchResult "LEFT JOIN BIL.PKK_PERSKORST_PARAMKOPF ON PKK_PDSID = PDS_PDSID" "~"
if joinKuerzel = BSBPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = BSB_FIRMENNR AND PER_PERSONENID = BSB_PERSONENID" "~"
if joinKuerzel = PERBSB
searchResult := searchResult "LEFT JOIN WWS.BSB_BONUSERMPERSONENZU ON BSB_FIRMENNR = PER_FIRMENNR AND BSB_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = BWBPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = BWB_PER_FIRMENNR AND PER_PERSONENID = BWB_PER_PERSONENID" "~"
if joinKuerzel = PERBWB
searchResult := searchResult "LEFT JOIN BUERO.BWB_BEWERBUNG ON BWB_PER_FIRMENNR = PER_FIRMENNR AND BWB_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = EWLPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = EWL_FIRMENNR AND PER_PERSONENID = EWL_PERSONENID" "~"
if joinKuerzel = PEREWL
searchResult := searchResult "LEFT JOIN BUERO.EWL_EVENTWARTELISTE ON EWL_FIRMENNR = PER_FIRMENNR AND EWL_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = FKKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = FKK_PER_FIRMENNR AND PER_PERSONENID = FKK_PERSONENID" "~"
if joinKuerzel = PERFKK
searchResult := searchResult "LEFT JOIN ZLE.FKK_FAKTURAKZKUNDENZU ON FKK_PER_FIRMENNR = PER_FIRMENNR AND FKK_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = GFZPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = GFZ_PER_FIRMENNR AND PER_PERSONENID = GFZ_PER_PERSONENID" "~"
if joinKuerzel = PERGFZ
searchResult := searchResult "LEFT JOIN BIL.GFZ_XMLJABGESFIRZU ON GFZ_PER_FIRMENNR = PER_FIRMENNR AND GFZ_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = GOPPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = GOP_FIRMENNR AND PER_PERSONENID = GOP_PERSONENID" "~"
if joinKuerzel = PERGOP
searchResult := searchResult "LEFT JOIN BUERO.GOP_GRUPPENORDNERPERSZU ON GOP_FIRMENNR = PER_FIRMENNR AND GOP_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = GRSPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = GRS_PER_FIRMENNR AND PER_PERSONENID = GRS_PERSONENID" "~"
if joinKuerzel = PERGRS
searchResult := searchResult "LEFT JOIN BUERO.GRS_GELDRISIKO ON GRS_PER_FIRMENNR = PER_FIRMENNR AND GRS_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = KUKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = KUK_PER_FIRMENNR AND PER_PERSONENID = KUK_PERSONENID" "~"
if joinKuerzel = PERKUK
searchResult := searchResult "LEFT JOIN WWS.KUK_KUNDENKARTEI ON KUK_PER_FIRMENNR = PER_FIRMENNR AND KUK_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = LLDPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = LLD_PER_FIRMENNR AND PER_PERSONENID = LLD_PER_PERSONENID" "~"
if joinKuerzel = PERLLD
searchResult := searchResult "LEFT JOIN ZLE.LLD_LEALOHNDATEN ON LLD_PER_FIRMENNR = PER_FIRMENNR AND LLD_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = MABPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = MAB_PER_FIRMENNR AND PER_PERSONENID = MAB_PER_PERSONENID" "~"
if joinKuerzel = PERMAB
searchResult := searchResult "LEFT JOIN BUERO.MAB_MITARBAUSBILDUNG ON MAB_PER_FIRMENNR = PER_FIRMENNR AND MAB_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = MAHPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = MAH_PER_FIRMENNR AND PER_PERSONENID = MAH_PER_PERSONENID" "~"
if joinKuerzel = PERMAH
searchResult := searchResult "LEFT JOIN FIBU.MAH_MAHNUNG ON MAH_PER_FIRMENNR = PER_FIRMENNR AND MAH_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = BONMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = BON_V3_FIRMENNR AND MIT_MITARBEITERID = BON_V3_MITARBEITERID" "~"
if joinKuerzel = MITBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_V3_FIRMENNR = MIT_FIRMENNR AND BON_V3_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = DVHMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = DVH_AL_FIRMENNR AND MIT_MITARBEITERID = DVH_AL_MITARBEITERID" "~"
if joinKuerzel = MITDVH
searchResult := searchResult "LEFT JOIN BUERO.DVH_DIENSTVERHAELTNIS ON DVH_AL_FIRMENNR = MIT_FIRMENNR AND DVH_AL_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = EKKMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = EKK_VERTR2_FIRMENNR AND MIT_MITARBEITERID = EKK_VERTR2_MITARBEITERID" "~"
if joinKuerzel = MITEKK
searchResult := searchResult "LEFT JOIN WWS.EKK_EXTERNE_KUNDENKARTEI ON EKK_VERTR2_FIRMENNR = MIT_FIRMENNR AND EKK_VERTR2_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = KAKMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = KAK_V3_FIRMENNR AND MIT_MITARBEITERID = KAK_V3_MITARBEITERID" "~"
if joinKuerzel = MITKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_V3_FIRMENNR = MIT_FIRMENNR AND KAK_V3_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = UBEMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = UBE_AUFTRAG_FIRMENNR AND MIT_MITARBEITERID = UBE_AUFTRAG_MITARBEITERID" "~"
if joinKuerzel = MITUBE
searchResult := searchResult "LEFT JOIN BMDINTERN.UBE_UPDATEBESTELLUNG ON UBE_AUFTRAG_FIRMENNR = MIT_FIRMENNR AND UBE_AUFTRAG_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = WIPMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = WIP_MIT_PRUEFER_FIRMENNR AND MIT_MITARBEITERID = WIP_MIT_PRUEFER_ID" "~"
if joinKuerzel = MITWIP
searchResult := searchResult "LEFT JOIN BIL.WIP_WIPRUEFUNG ON WIP_MIT_PRUEFER_FIRMENNR = MIT_FIRMENNR AND WIP_MIT_PRUEFER_ID = MIT_MITARBEITERID" "~"
if joinKuerzel = KAKMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = KAK_V4_FIRMENNR AND MIT_MITARBEITERID = KAK_V4_MITARBEITERID" "~"
if joinKuerzel = MITKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_V4_FIRMENNR = MIT_FIRMENNR AND KAK_V4_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = AWSMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = AWS_AL_KLI_FIRMENNR AND MIT_MITARBEITERID = AWS_AL_KLI_MITARBEITERID" "~"
if joinKuerzel = MITAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_AL_KLI_FIRMENNR = MIT_FIRMENNR AND AWS_AL_KLI_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = EFKMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = EFK_BEARB_FIRMENNR AND MIT_MITARBEITERID = EFK_BEARB_MITARBEITERID" "~"
if joinKuerzel = MITEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_BEARB_FIRMENNR = MIT_FIRMENNR AND EFK_BEARB_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = LEIMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = LEI_AL_MIT_FIRMENNR AND MIT_MITARBEITERID = LEI_AL_MIT_MITARBEITERID" "~"
if joinKuerzel = MITLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_AL_MIT_FIRMENNR = MIT_FIRMENNR AND LEI_AL_MIT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = MLSMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = MLS_GF_PER_FIRMENNR AND MIT_MITARBEITERID = MLS_GF_PER_MITARBEITERID" "~"
if joinKuerzel = MITMLS
searchResult := searchResult "LEFT JOIN ZLE.MLS_MITARBLEISTTAUSCHSATZ ON MLS_GF_PER_FIRMENNR = MIT_FIRMENNR AND MLS_GF_PER_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = FGKMVK
searchResult := searchResult "LEFT JOIN BUERO.MVK_MITARBVORSORGEKASSE ON MVK_LANDNR = FGK_MVK_LANDNR AND MVK_KASSENR = FGK_MVK_KASSENR" "~"
if joinKuerzel = MVKFGK
searchResult := searchResult "LEFT JOIN BUERO.FGK_FIRMENGKKZU ON FGK_MVK_LANDNR = MVK_LANDNR AND FGK_MVK_KASSENR = MVK_KASSENR" "~"
if joinKuerzel = FPZMVK
searchResult := searchResult "LEFT JOIN MVK_MITARBVORSORGEKASSE.MVK_MITARBVORSORGEKASSE ON MVK_LANDNR = FPZ_MVK_LANDNR AND MVK_KASSENR = FPZ_MVK_KASSENR" "~"
if joinKuerzel = MVKFPZ
searchResult := searchResult "LEFT JOIN HRS.FPZ_FIRMENPENSIONSKASSEZU ON FPZ_MVK_LANDNR = MVK_LANDNR AND FPZ_MVK_KASSENR = MVK_KASSENR" "~"
if joinKuerzel = MLZMVK
searchResult := searchResult "LEFT JOIN BUERO.MVK_MITARBVORSORGEKASSE ON MVK_LANDNR = MLZ_LANDNR AND MVK_KASSENR = MLZ_KASSENR" "~"
if joinKuerzel = MVKMLZ
searchResult := searchResult "LEFT JOIN BUERO.MLZ_VORSKASSELEITZAHLZU ON MLZ_LANDNR = MVK_LANDNR AND MLZ_KASSENR = MVK_KASSENR" "~"
if joinKuerzel = NOGNOG
searchResult := searchResult "LEFT JOIN BUERO.NOG_NOTIZGRUPPE ON NOG_NOTIZGRUPPENR = NOG_HAUPT_NOTIZGRUPPENR" "~"
if joinKuerzel = NOGNOG
searchResult := searchResult "LEFT JOIN BUERO.NOG_NOTIZGRUPPE ON NOG_HAUPT_NOTIZGRUPPENR = NOG_NOTIZGRUPPENR" "~"
if joinKuerzel = BBANTC
searchResult := searchResult "LEFT JOIN NTC_NTCSARTIKEL.NTC_NTCSARTIKEL ON NTC_NTCSBMDPAKETNR = BBA_NTCSBMDPAKETNR" "~"
if joinKuerzel = NTCBBA
searchResult := searchResult "LEFT JOIN BIL.BBA_BERICHTSART ON BBA_NTCSBMDPAKETNR = NTC_NTCSBMDPAKETNR" "~"
if joinKuerzel = NUBNUP
searchResult := searchResult "LEFT JOIN BMD.NUP_NTCSUPDATE ON NUP_NTCSUPDATEID = NUB_NTCSUPDATEID" "~"
if joinKuerzel = NUPNUB
searchResult := searchResult "LEFT JOIN BMD.NUB_NTCSUPDBEZIEHER ON NUB_NTCSUPDATEID = NUP_NTCSUPDATEID" "~"
if joinKuerzel = OPEOKL
searchResult := searchResult "LEFT JOIN LOHN.OKL_OF_KLIENT ON OKL_KLIENTENNR = OPE_KLIENTENNR" "~"
if joinKuerzel = OKLOPE
searchResult := searchResult "LEFT JOIN LOHN.OPE_OF_PERSON ON OPE_KLIENTENNR = OKL_KLIENTENNR" "~"
if joinKuerzel = OPEOKV
searchResult := searchResult "LEFT JOIN LOHN.OKV_OF_KOLLEKTIV ON OKV_OFKVID = OPE_OKV_OFKVID" "~"
if joinKuerzel = OKVOPE
searchResult := searchResult "LEFT JOIN LOHN.OPE_OF_PERSON ON OPE_OKV_OFKVID = OKV_OFKVID" "~"
if joinKuerzel = ONVONS
searchResult := searchResult "LEFT JOIN HRS.ONS_ONLINEBEW_STAMMDATEN ON ONS_ONLBEWID = ONV_ONLBEWID" "~"
if joinKuerzel = ONSONV
searchResult := searchResult "LEFT JOIN HRS.ONV_ONLINEBEW_VORDIENST ON ONV_ONLBEWID = ONS_ONLBEWID" "~"
if joinKuerzel = MDDMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_MEDIENID = MDD_MEDIENID" "~"
if joinKuerzel = MEDMDD
searchResult := searchResult "LEFT JOIN BUERO.MDD_MEDIENDOKUMENTZU ON MDD_MEDIENID = MED_MEDIENID" "~"
if joinKuerzel = MEVMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_GEHOERT_FIRMENNR = MEV_MED_FIRMENNR AND MED_MEDIENKZ = MEV_MEDIENKZ AND MED_MEDIENLFDNR = MEV_MEDIENLFDNR" "~"
if joinKuerzel = MEDMEV
searchResult := searchResult "LEFT JOIN BUERO.MEV_MEDIENVERLEIH ON MEV_MED_FIRMENNR = MED_GEHOERT_FIRMENNR AND MEV_MEDIENKZ = MED_MEDIENKZ AND MEV_MEDIENLFDNR = MED_MEDIENLFDNR" "~"
if joinKuerzel = MIZMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_MEDIENID = MIZ_MEDIENID" "~"
if joinKuerzel = MEDMIZ
searchResult := searchResult "LEFT JOIN BUERO.MIZ_MEDIENINFOZU ON MIZ_MEDIENID = MED_MEDIENID" "~"
if joinKuerzel = TRZMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_MEDIENID = TRZ_MED_MEDIENID" "~"
if joinKuerzel = MEDTRZ
searchResult := searchResult "LEFT JOIN BUERO.TRZ_TERMINRESSOURCENZU ON TRZ_MED_MEDIENID = MED_MEDIENID" "~"
if joinKuerzel = ALEMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = ALE_GF_FIRMENNR AND MIT_MITARBEITERID = ALE_GF_MITARBEITERID" "~"
if joinKuerzel = MITALE
searchResult := searchResult "LEFT JOIN BUERO.ALE_ADRESSENLISTE ON ALE_GF_FIRMENNR = MIT_FIRMENNR AND ALE_GF_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = ASWMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = ASW_MIT_FIRMENNR AND MIT_MITARBEITERID = ASW_MITARBEITERID" "~"
if joinKuerzel = MITASW
searchResult := searchResult "LEFT JOIN BUERO.ASW_AUSWEIS ON ASW_MIT_FIRMENNR = MIT_FIRMENNR AND ASW_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = BONMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = BON_V1_FIRMENNR AND MIT_MITARBEITERID = BON_V1_MITARBEITERID" "~"
if joinKuerzel = MITBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_V1_FIRMENNR = MIT_FIRMENNR AND BON_V1_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = BRBMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = BRB_FIR_FIRMENNR AND MIT_MITARBEITERID = BRB_MITARBEITERID" "~"
if joinKuerzel = MITBRB
searchResult := searchResult "LEFT JOIN BUERO.BRB_BUEROBERECHT ON BRB_FIR_FIRMENNR = MIT_FIRMENNR AND BRB_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = EROMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = ERO_KOMISS_FIRMENNR AND MIT_MITARBEITERID = ERO_KOMISS_MITARBEITERID" "~"
if joinKuerzel = MITERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_KOMISS_FIRMENNR = MIT_FIRMENNR AND ERO_KOMISS_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = EVTMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = EVT_MIT_FIRMENNR AND MIT_MITARBEITERID = EVT_MITARBEITERID" "~"
if joinKuerzel = MITEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_MIT_FIRMENNR = MIT_FIRMENNR AND EVT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = FAZMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = FAZ_MIT_FIRMENNR AND MIT_MITARBEITERID = FAZ_MITARBEITERID" "~"
if joinKuerzel = MITFAZ
searchResult := searchResult "LEFT JOIN BUERO.FAZ_FRAGEANTWORTZU ON FAZ_MIT_FIRMENNR = MIT_FIRMENNR AND FAZ_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = KAJMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = KAJ_KASSIER_FIRMENNR AND MIT_MITARBEITERID = KAJ_KASSIER_MITARBEITERID" "~"
if joinKuerzel = MITKAJ
searchResult := searchResult "LEFT JOIN WWS.KAJ_KASSENJOURNAL ON KAJ_KASSIER_FIRMENNR = MIT_FIRMENNR AND KAJ_KASSIER_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = KJOMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = KJO_MIT_FIRMENNR AND MIT_MITARBEITERID = KJO_MITARBEITERID" "~"
if joinKuerzel = MITKJO
searchResult := searchResult "LEFT JOIN BUERO.KJO_KONTAKTJOURNAL ON KJO_MIT_FIRMENNR = MIT_FIRMENNR AND KJO_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = LTSMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = LTS_MIT_FIRMENNR AND MIT_MITARBEITERID = LTS_MITARBEITERID" "~"
if joinKuerzel = MITLTS
searchResult := searchResult "LEFT JOIN ZLE.LTS_LEISTTAUSCHSATZ ON LTS_MIT_FIRMENNR = MIT_FIRMENNR AND LTS_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = MAGMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = MAG_MITARB_FIRMENNR AND MIT_MITARBEITERID = MAG_MITARB_MITARBEITERID" "~"
if joinKuerzel = MITMAG
searchResult := searchResult "LEFT JOIN BUERO.MAG_MITARBGESPR ON MAG_MITARB_FIRMENNR = MIT_FIRMENNR AND MAG_MITARB_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = MHTMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = MHT_FIRMENNR AND MIT_MITARBEITERID = MHT_MITARBEITERID" "~"
if joinKuerzel = MITMHT
searchResult := searchResult "LEFT JOIN BUERO.MHT_MITARBHISTORIE ON MHT_FIRMENNR = MIT_FIRMENNR AND MHT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = REMMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = REM_FIRMENNR AND MIT_MITARBEITERID = REM_MITARBEITERID" "~"
if joinKuerzel = MITREM
searchResult := searchResult "LEFT JOIN BUERO.REM_REMINDER ON REM_FIRMENNR = MIT_FIRMENNR AND REM_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = TMSMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = TMS_MIT_FIRMENNR AND MIT_MITARBEITERID = TMS_MIT_MITARBEITERID" "~"
if joinKuerzel = MITTMS
searchResult := searchResult "LEFT JOIN BUERO.TMS_TMSICHERUNG ON TMS_MIT_FIRMENNR = MIT_FIRMENNR AND TMS_MIT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = TSSMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = TSS_FIRMENNR AND MIT_MITARBEITERID = TSS_MITARBEITERID" "~"
if joinKuerzel = MITTSS
searchResult := searchResult "LEFT JOIN ZLE.TSS_TAGESABSOLLSTUNDEN ON TSS_FIRMENNR = MIT_FIRMENNR AND TSS_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = VAKMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = VAK_SACHB_FIRMENNR AND MIT_MITARBEITERID = VAK_SACHB_MITARBEITERID" "~"
if joinKuerzel = MITVAK
searchResult := searchResult "LEFT JOIN BUERO.VAK_VERANSTALTUNGSKOPF ON VAK_SACHB_FIRMENNR = MIT_FIRMENNR AND VAK_SACHB_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = TCTMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = TCT_MIT_FIRMENNR AND MIT_MITARBEITERID = TCT_MITARBEITERID" "~"
if joinKuerzel = MITTCT
searchResult := searchResult "LEFT JOIN BMD.TCT_TCTESTCASE ON TCT_MIT_FIRMENNR = MIT_FIRMENNR AND TCT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = TLPMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = TLP_MIT_FIRMENNR AND MIT_MITARBEITERID = TLP_MITARBEITERID" "~"
if joinKuerzel = MITTLP
searchResult := searchResult "LEFT JOIN PRODUKTION.TLP_BT_LOGISTIK_POSITION ON TLP_MIT_FIRMENNR = MIT_FIRMENNR AND TLP_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = VAZMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = VAZ_VERTRETER_FIRMENNR AND MIT_MITARBEITERID = VAZ_VERTRETER_MITARBID" "~"
if joinKuerzel = MITVAZ
searchResult := searchResult "LEFT JOIN WWS.VAZ_VERTABRZAHLUNGEN ON VAZ_VERTRETER_FIRMENNR = MIT_FIRMENNR AND VAZ_VERTRETER_MITARBID = MIT_MITARBEITERID" "~"
if joinKuerzel = VRSMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = VRS_VERTRETER_FIRMENNR AND MIT_MITARBEITERID = VRS_VERTRETER_MITARBID" "~"
if joinKuerzel = MITVRS
searchResult := searchResult "LEFT JOIN WWS.VRS_VERTABRRECHPOSSTORNO ON VRS_VERTRETER_FIRMENNR = MIT_FIRMENNR AND VRS_VERTRETER_MITARBID = MIT_MITARBEITERID" "~"
if joinKuerzel = ZKSMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = ZKS_SACHBEARB_FIRMENNR AND MIT_MITARBEITERID = ZKS_SACHBEARB_PERSID" "~"
if joinKuerzel = MITZKS
searchResult := searchResult "LEFT JOIN FIBU.ZKS_ZINSBERECHNUNGSKREIS ON ZKS_SACHBEARB_FIRMENNR = MIT_FIRMENNR AND ZKS_SACHBEARB_PERSID = MIT_MITARBEITERID" "~"
if joinKuerzel = ZSAMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = ZSA_MIT_FIRMENNR AND MIT_MITARBEITERID = ZSA_MITARBEITERID" "~"
if joinKuerzel = MITZSA
searchResult := searchResult "LEFT JOIN BUERO.ZSA_ZEITSALDEN ON ZSA_MIT_FIRMENNR = MIT_FIRMENNR AND ZSA_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = ZUSMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = ZUS_MIT_FIRMENNR AND MIT_MITARBEITERID = ZUS_MITARBEITERID" "~"
if joinKuerzel = MITZUS
searchResult := searchResult "LEFT JOIN BUERO.ZUS_ZUSTAENDIGKEIT ON ZUS_MIT_FIRMENNR = MIT_FIRMENNR AND ZUS_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = ZVDMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = ZVD_MIT_FIRMENNR AND MIT_MITARBEITERID = ZVD_MITARBEITERID" "~"
if joinKuerzel = MITZVD
searchResult := searchResult "LEFT JOIN BUERO.ZVD_ZEITPLANUNGVERSIONDAT ON ZVD_MIT_FIRMENNR = MIT_FIRMENNR AND ZVD_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = AVEMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = AVE_V1_FIRMENNR AND MIT_MITARBEITERID = AVE_V1_MITARBEITERID" "~"
if joinKuerzel = MITAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_V1_FIRMENNR = MIT_FIRMENNR AND AVE_V1_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = BPKMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = BPK_SACHB1_FIRMENNR AND MIT_MITARBEITERID = BPK_SACHB1_MITARBEITERID" "~"
if joinKuerzel = MITBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_SACHB1_FIRMENNR = MIT_FIRMENNR AND BPK_SACHB1_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = CAWMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = CAW_VERT_FIRMENNR AND MIT_MITARBEITERID = CAW_VERT_MITARBEITERID" "~"
if joinKuerzel = MITCAW
searchResult := searchResult "LEFT JOIN BUERO.CAW_CRMABWESENHEIT ON CAW_VERT_FIRMENNR = MIT_FIRMENNR AND CAW_VERT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = EKKMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = EKK_VERTR1_FIRMENNR AND MIT_MITARBEITERID = EKK_VERTR1_MITARBEITERID" "~"
if joinKuerzel = MITEKK
searchResult := searchResult "LEFT JOIN WWS.EKK_EXTERNE_KUNDENKARTEI ON EKK_VERTR1_FIRMENNR = MIT_FIRMENNR AND EKK_VERTR1_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = KAKMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = KAK_V2_FIRMENNR AND MIT_MITARBEITERID = KAK_V2_MITARBEITERID" "~"
if joinKuerzel = MITKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_V2_FIRMENNR = MIT_FIRMENNR AND KAK_V2_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = LAGMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = LAG_VORG_KOMMISS_FIRMENNR AND MIT_MITARBEITERID = LAG_VORG_KOMMISS_MITARBID" "~"
if joinKuerzel = MITLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_VORG_KOMMISS_FIRMENNR = MIT_FIRMENNR AND LAG_VORG_KOMMISS_MITARBID = MIT_MITARBEITERID" "~"
if joinKuerzel = MAGMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = MAG_VORG_FIRMENNR AND MIT_MITARBEITERID = MAG_VORG_MITARBEITERID" "~"
if joinKuerzel = MITMAG
searchResult := searchResult "LEFT JOIN BUERO.MAG_MITARBGESPR ON MAG_VORG_FIRMENNR = MIT_FIRMENNR AND MAG_VORG_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = TAVMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = TAV_ZIEL_MIT_FIRMENNR AND MIT_MITARBEITERID = TAV_ZIEL_MITARBEITERID" "~"
if joinKuerzel = MITTAV
searchResult := searchResult "LEFT JOIN BUERO.TAV_VERTEILUNG ON TAV_ZIEL_MIT_FIRMENNR = MIT_FIRMENNR AND TAV_ZIEL_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = AWSMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = AWS_GF_KLI_FIRMENNR AND MIT_MITARBEITERID = AWS_GF_KLI_MITARBEITERID" "~"
if joinKuerzel = MITAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_GF_KLI_FIRMENNR = MIT_FIRMENNR AND AWS_GF_KLI_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = LEIMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = LEI_AL_PER_FIRMENNR AND MIT_MITARBEITERID = LEI_AL_PER_MITARBEITERID" "~"
if joinKuerzel = MITLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_AL_PER_FIRMENNR = MIT_FIRMENNR AND LEI_AL_PER_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = MLTMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = MLT_AUF_GF_FIRMENNR AND MIT_MITARBEITERID = MLT_AUF_GF_MITARBEITERID" "~"
if joinKuerzel = MITMLT
searchResult := searchResult "LEFT JOIN ZLE.MLT_MITARBLEISTTAUSCH ON MLT_AUF_GF_FIRMENNR = MIT_FIRMENNR AND MLT_AUF_GF_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = LPOMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = LPO_KOMMISS_FIRMENNR AND MIT_MITARBEITERID = LPO_KOMMISS_MITARBEITERID" "~"
if joinKuerzel = MITLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_KOMMISS_FIRMENNR = MIT_FIRMENNR AND LPO_KOMMISS_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = PLZMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = PLZ_MIT_FIRMENNR AND MIT_MITARBEITERID = PLZ_MIT_MITARBEITERID" "~"
if joinKuerzel = MITPLZ
searchResult := searchResult "LEFT JOIN BUERO.PLZ_PLANGRUPPEZU ON PLZ_MIT_FIRMENNR = MIT_FIRMENNR AND PLZ_MIT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = WAGMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = WAG_BEARB_EK_FIRMENNR AND MIT_MITARBEITERID = WAG_BEARB_EK_MITARBID" "~"
if joinKuerzel = MITWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_BEARB_EK_FIRMENNR = MIT_FIRMENNR AND WAG_BEARB_EK_MITARBID = MIT_MITARBEITERID" "~"
if joinKuerzel = WMZMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = WMZ_MIT_FIRMENNR AND MIT_MITARBEITERID = WMZ_MITARBEITERID" "~"
if joinKuerzel = MITWMZ
searchResult := searchResult "LEFT JOIN BUERO.WMZ_WFAMITARBZU ON WMZ_MIT_FIRMENNR = MIT_FIRMENNR AND WMZ_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = XSEMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = XSE_MIT_FIRMENNR AND MIT_MITARBEITERID = XSE_MIT_MITARBEITERID" "~"
if joinKuerzel = MITXSE
searchResult := searchResult "LEFT JOIN BUERO.XSE_XML_STEUERERKL ON XSE_MIT_FIRMENNR = MIT_FIRMENNR AND XSE_MIT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = ZTGMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = ZTG_MAFIRMENNR AND MIT_MITARBEITERID = ZTG_MITARBEITERID" "~"
if joinKuerzel = MITZTG
searchResult := searchResult "LEFT JOIN HRS.ZTG_ZAHLTAG ON ZTG_MAFIRMENNR = MIT_FIRMENNR AND ZTG_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = FSTMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = FST_MIT_FIRMENNR2 AND MIT_MITARBEITERID = FST_MITARBEITERID2" "~"
if joinKuerzel = MITFST
searchResult := searchResult "LEFT JOIN BUERO.FST_FRIST ON FST_MIT_FIRMENNR2 = MIT_FIRMENNR AND FST_MITARBEITERID2 = MIT_MITARBEITERID" "~"
if joinKuerzel = PROMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = PRO_GF_FIRMENNR AND MIT_MITARBEITERID = PRO_GF_MITARBEITERID" "~"
if joinKuerzel = MITPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_GF_FIRMENNR = MIT_FIRMENNR AND PRO_GF_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = RNPMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = RNP_V1_FIRMENNR AND MIT_MITARBEITERID = RNP_V1_MITARBEITERID" "~"
if joinKuerzel = MITRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_V1_FIRMENNR = MIT_FIRMENNR AND RNP_V1_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = PERMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = PER_AL_FIRMENNR AND MIT_MITARBEITERID = PER_AL_MITARBEITERID" "~"
if joinKuerzel = MITPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_AL_FIRMENNR = MIT_FIRMENNR AND PER_AL_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = POSMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = POS_AL_FIRMENNR AND MIT_MITARBEITERID = POS_AL_MITARBEITERID" "~"
if joinKuerzel = MITPOS
searchResult := searchResult "LEFT JOIN BUERO.POS_POST ON POS_AL_FIRMENNR = MIT_FIRMENNR AND POS_AL_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = MLTMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = MLT_AUF_AL_FIRMENNR AND MIT_MITARBEITERID = MLT_AUF_AL_MITARBEITERID" "~"
if joinKuerzel = MITMLT
searchResult := searchResult "LEFT JOIN ZLE.MLT_MITARBLEISTTAUSCH ON MLT_AUF_AL_FIRMENNR = MIT_FIRMENNR AND MLT_AUF_AL_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = RNGMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = RNG_V3_FIRMENNR AND MIT_MITARBEITERID = RNG_V3_MITARBEITERID" "~"
if joinKuerzel = MITRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_V3_FIRMENNR = MIT_FIRMENNR AND RNG_V3_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = RNPMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = RNP_V3_FIRMENNR AND MIT_MITARBEITERID = RNP_V3_MITARBEITERID" "~"
if joinKuerzel = MITRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_V3_FIRMENNR = MIT_FIRMENNR AND RNP_V3_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = AWSMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = AWS_PRO_MIT_FIRMENNR AND MIT_MITARBEITERID = AWS_PRO_MITARBEITERID" "~"
if joinKuerzel = MITAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_PRO_MIT_FIRMENNR = MIT_FIRMENNR AND AWS_PRO_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = EFKMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = EFK_KOMMISS_FIRMENNR AND MIT_MITARBEITERID = EFK_KOMMISS_MITARBEITERID" "~"
if joinKuerzel = MITEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_KOMMISS_FIRMENNR = MIT_FIRMENNR AND EFK_KOMMISS_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = PROMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = PRO_AKQUI_FIRMENNR AND MIT_MITARBEITERID = PRO_AKQUI_MITARBEITERID" "~"
if joinKuerzel = MITPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_AKQUI_FIRMENNR = MIT_FIRMENNR AND PRO_AKQUI_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = MAHMKR
searchResult := searchResult "LEFT JOIN FIBU.MKR_MAHNKREIS ON MKR_FIRMENNR = MAH_FIRMENNR AND MKR_FIBUNR = MAH_FIBUNR AND MKR_MAHNKREISLFDNR = MAH_MAHNKREISLFDNR" "~"
if joinKuerzel = MKRMAH
searchResult := searchResult "LEFT JOIN FIBU.MAH_MAHNUNG ON MAH_FIRMENNR = MKR_FIRMENNR AND MAH_FIBUNR = MKR_FIBUNR AND MAH_MAHNKREISLFDNR = MKR_MAHNKREISLFDNR" "~"
if joinKuerzel = FGKMLZ
searchResult := searchResult "LEFT JOIN BUERO.MLZ_VORSKASSELEITZAHLZU ON MLZ_LANDNR = FGK_MLZ_LANDNR AND MLZ_KASSENR = FGK_MLZ_KASSENR AND MLZ_LEITZAHL = FGK_MLZ_LEITZAHL" "~"
if joinKuerzel = MLZFGK
searchResult := searchResult "LEFT JOIN BUERO.FGK_FIRMENGKKZU ON FGK_MLZ_LANDNR = MLZ_LANDNR AND FGK_MLZ_KASSENR = MLZ_KASSENR AND FGK_MLZ_LEITZAHL = MLZ_LEITZAHL" "~"
if joinKuerzel = LPOPAT
searchResult := searchResult "LEFT JOIN WWS.PAT_PACKSTOFFTAB ON PAT_FIRMENNR = LPO_PAT_FIRMENNR AND PAT_PACKSTOFFTABNR = LPO_PACKSTOFFTABNR" "~"
if joinKuerzel = PATLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_PAT_FIRMENNR = PAT_FIRMENNR AND LPO_PACKSTOFFTABNR = PAT_PACKSTOFFTABNR" "~"
if joinKuerzel = AWEPBA
searchResult := searchResult "LEFT JOIN PBA_PLAN_BAS_ALT.PBA_PLAN_BAS_ALT ON PBA_PBAID = AWE_PEP_BL_PBA_PBAID1" "~"
if joinKuerzel = PBAAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_PEP_BL_PBA_PBAID1 = PBA_PBAID" "~"
if joinKuerzel = BSGPCK
searchResult := searchResult "LEFT JOIN PCK_PPS_KAPA.PCK_PPS_KAPA ON PCK_PPSKAPAID = BSG_PPSKAPAID" "~"
if joinKuerzel = PCKBSG
searchResult := searchResult "LEFT JOIN BUERO.BSG_BESPRECHUNG ON BSG_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = TPRPCK
searchResult := searchResult "LEFT JOIN PCK_PPS_KAPA.PCK_PPS_KAPA ON PCK_PPSKAPAID = TPR_PPSKAPAID" "~"
if joinKuerzel = PCKTPR
searchResult := searchResult "LEFT JOIN BUERO.TPR_TELEFONPROTOKOLL ON TPR_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = SCMPCK
searchResult := searchResult "LEFT JOIN PCK_PPS_KAPA.PCK_PPS_KAPA ON PCK_PPSKAPAID = SCM_PPSKAPAID" "~"
if joinKuerzel = PCKSCM
searchResult := searchResult "LEFT JOIN BMDENTW.SCM_SCRMAIN ON SCM_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = DISPCP
searchResult := searchResult "LEFT JOIN PCP_PPS_PRODUKT.PCP_PPS_PRODUKT ON PCP_PCHARGENID = DIS_PCHARGENID AND PCP_PPSPROD_LFDNR = DIS_PPSPROD_LFDNR" "~"
if joinKuerzel = PCPDIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_PCHARGENID = PCP_PCHARGENID AND DIS_PPSPROD_LFDNR = PCP_PPSPROD_LFDNR" "~"
if joinKuerzel = WGBPEK
searchResult := searchResult "LEFT JOIN FIBU.PEK_PERSONENKONTO ON PEK_FIRMENNR = WGB_FIRMENNR AND PEK_FIBUNR = WGB_FIBUNR AND PEK_KONTONR = WGB_KONTONR" "~"
if joinKuerzel = PEKWGB
searchResult := searchResult "LEFT JOIN FIBU.WGB_WARENEINGANGSBUCH ON WGB_FIRMENNR = PEK_FIRMENNR AND WGB_FIBUNR = PEK_FIBUNR AND WGB_KONTONR = PEK_KONTONR" "~"
if joinKuerzel = AAEPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = AAE_V1_FIRMENNR AND PER_PERSONENID = AAE_V1_MITARBEITERID" "~"
if joinKuerzel = PERAAE
searchResult := searchResult "LEFT JOIN WWS.AAE_ANGEBOTSAUSW_ERFQ_TMP ON AAE_V1_FIRMENNR = PER_FIRMENNR AND AAE_V1_MITARBEITERID = PER_PERSONENID" "~"
if joinKuerzel = AKUPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = AKU_PER_FIRMENNR AND PER_PERSONENID = AKU_PERSONENID" "~"
if joinKuerzel = PERAKU
searchResult := searchResult "LEFT JOIN ZLE.AKU_ARTIKELKUNDEZULEA ON AKU_PER_FIRMENNR = PER_FIRMENNR AND AKU_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = AMZPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = AMZ_GF_FIRMENNR AND PER_PERSONENID = AMZ_GF_PERSONENID" "~"
if joinKuerzel = PERAMZ
searchResult := searchResult "LEFT JOIN BUERO.AMZ_AUSDRUCKMODELLZU ON AMZ_GF_FIRMENNR = PER_FIRMENNR AND AMZ_GF_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = BONPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = BON_KUNDEN_FIRMENNR AND PER_PERSONENID = BON_KUNDEN_KONPERSID" "~"
if joinKuerzel = PERBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_KUNDEN_FIRMENNR = PER_FIRMENNR AND BON_KUNDEN_KONPERSID = PER_PERSONENID" "~"
if joinKuerzel = BRUPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = BRU_FIRMENNR AND PER_PERSONENID = BRU_PERSONENID" "~"
if joinKuerzel = PERBRU
searchResult := searchResult "LEFT JOIN HRS.BRU_BETRIEBSRATSUMLAGE ON BRU_FIRMENNR = PER_FIRMENNR AND BRU_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = BVRPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = BVR_PER_FIRMENNR AND PER_PERSONENID = BVR_PER_PERSONENID" "~"
if joinKuerzel = PERBVR
searchResult := searchResult "LEFT JOIN FIBU.BVR_BANKRVREGELN ON BVR_PER_FIRMENNR = PER_FIRMENNR AND BVR_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = EMAPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = EMA_FIRMENNR AND PER_PERSONENID = EMA_PERSONENID" "~"
if joinKuerzel = PEREMA
searchResult := searchResult "LEFT JOIN BUERO.EMA_EMAIL ON EMA_FIRMENNR = PER_FIRMENNR AND EMA_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = EVLPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = EVL_FIRMENNR AND PER_PERSONENID = EVL_PERSONENID" "~"
if joinKuerzel = PEREVL
searchResult := searchResult "LEFT JOIN BUERO.EVL_EVENTLIEFERANTZU ON EVL_FIRMENNR = PER_FIRMENNR AND EVL_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = GKOPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = GKO_PER_FIRMENNR AND PER_PERSONENID = GKO_PERSONENID" "~"
if joinKuerzel = PERGKO
searchResult := searchResult "LEFT JOIN FIBU.GKO_GESELLSCHAFTERKONTEN ON GKO_PER_FIRMENNR = PER_FIRMENNR AND GKO_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = GPHPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = GPH_PER_FIRMENNR AND PER_PERSONENID = GPH_PER_PERSONENID" "~"
if joinKuerzel = PERGPH
searchResult := searchResult "LEFT JOIN BUERO.GPH_GOPHILFSTABELLE ON GPH_PER_FIRMENNR = PER_FIRMENNR AND GPH_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = GSMPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = GSM_PER_FIRMENNR AND PER_PERSONENID = GSM_PER_PERSONENID" "~"
if joinKuerzel = PERGSM
searchResult := searchResult "LEFT JOIN PRODUKTION.GSM_GUTSCHLECHTMENGE ON GSM_PER_FIRMENNR = PER_FIRMENNR AND GSM_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = INTPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = INT_HPARTNER_FIRMENNR AND PER_PERSONENID = INT_HPARTNER_PERSONENID" "~"
if joinKuerzel = PERINT
searchResult := searchResult "LEFT JOIN BUERO.INT_INTRASTAT ON INT_HPARTNER_FIRMENNR = PER_FIRMENNR AND INT_HPARTNER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = KSTPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = KST_PER_FIRMENNR AND PER_PERSONENID = KST_PERSONENID" "~"
if joinKuerzel = PERKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_PER_FIRMENNR = PER_FIRMENNR AND KST_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = LAJPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = LAJ_PER_FIRMENNR AND PER_PERSONENID = LAJ_PERSONENID" "~"
if joinKuerzel = PERLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_PER_FIRMENNR = PER_FIRMENNR AND LAJ_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = MISPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = MIS_PER_FIRMENNR AND PER_PERSONENID = MIS_PER_PERSONENID" "~"
if joinKuerzel = PERMIS
searchResult := searchResult "LEFT JOIN BUERO.MIS_MITARBSTELLEZU ON MIS_PER_FIRMENNR = PER_FIRMENNR AND MIS_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = MITPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = MIT_PATE_FIRMENNR AND PER_PERSONENID = MIT_PATE_PERSONENID" "~"
if joinKuerzel = PERMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_PATE_FIRMENNR = PER_FIRMENNR AND MIT_PATE_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = NVSPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = NVS_PER_FIRMENNR AND PER_PERSONENID = NVS_PERSONENID" "~"
if joinKuerzel = PERNVS
searchResult := searchResult "LEFT JOIN BIL.NVS_NACHVERSTEU_PAR11A ON NVS_PER_FIRMENNR = PER_FIRMENNR AND NVS_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PALPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = PAL_PER_FIRMENNR AND PER_PERSONENID = PAL_PERSONENID" "~"
if joinKuerzel = PERPAL
searchResult := searchResult "LEFT JOIN ZLE.PAL_PAUSCHALE ON PAL_PER_FIRMENNR = PER_FIRMENNR AND PAL_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PBSPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = PBS_PER_FIRMENNR AND PER_PERSONENID = PBS_PER_PERSONENID" "~"
if joinKuerzel = PERPBS
searchResult := searchResult "LEFT JOIN PRODUKTION.PBS_BEFEHL ON PBS_PER_FIRMENNR = PER_FIRMENNR AND PBS_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PFAPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PFA_FIRMENNR AND PER_PERSONENID = PFA_PERSONENID" "~"
if joinKuerzel = PERPFA
searchResult := searchResult "LEFT JOIN BUERO.PFA_PERSONFINANZAMTZU ON PFA_FIRMENNR = PER_FIRMENNR AND PFA_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PSOPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PSO_FIRMENNR AND PER_PERSONENID = PSO_PERSONENID" "~"
if joinKuerzel = PERPSO
searchResult := searchResult "LEFT JOIN BUERO.PSO_PERSONSOZIALZU ON PSO_FIRMENNR = PER_FIRMENNR AND PSO_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PWOPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PWO_FIRMENNR AND PER_PERSONENID = PWO_PERSONENID" "~"
if joinKuerzel = PERPWO
searchResult := searchResult "LEFT JOIN BUERO.PWO_PASSWORT ON PWO_FIRMENNR = PER_FIRMENNR AND PWO_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = TOOPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = TOO_SPEDITION_FIRMENNR AND PER_PERSONENID = TOO_SPEDITION_EXTPERSID" "~"
if joinKuerzel = PERTOO
searchResult := searchResult "LEFT JOIN WWS.TOO_TOUROBJEKT ON TOO_SPEDITION_FIRMENNR = PER_FIRMENNR AND TOO_SPEDITION_EXTPERSID = PER_PERSONENID" "~"
if joinKuerzel = TOUPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = TOU_SPEDITION_FIRMENNR AND PER_PERSONENID = TOU_SPEDITION_KONTAKTPERSID" "~"
if joinKuerzel = PERTOU
searchResult := searchResult "LEFT JOIN WWS.TOU_TOUR ON TOU_SPEDITION_FIRMENNR = PER_FIRMENNR AND TOU_SPEDITION_KONTAKTPERSID = PER_PERSONENID" "~"
if joinKuerzel = TTBPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = TTB_GES_FIRMENNR AND PER_PERSONENID = TTB_GES_PERSONENID" "~"
if joinKuerzel = PERTTB
searchResult := searchResult "LEFT JOIN BIL.TTB_TANTIEMENBERECHNUNG ON TTB_GES_FIRMENNR = PER_FIRMENNR AND TTB_GES_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = UEVPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = UEV_PER_FIRMENNR AND PER_PERSONENID = UEV_PERSONENID" "~"
if joinKuerzel = PERUEV
searchResult := searchResult "LEFT JOIN BIL.UEV_STEUERUEBERGVERLUSTE ON UEV_PER_FIRMENNR = PER_FIRMENNR AND UEV_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = VERPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = VER_PER_FIRMENNR AND PER_PERSONENID = VER_PERSONENID" "~"
if joinKuerzel = PERVER
searchResult := searchResult "LEFT JOIN BUERO.VER_VERSANDART ON VER_PER_FIRMENNR = PER_FIRMENNR AND VER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = WADPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = WAD_SPEDITION_FIRMENNR AND PER_PERSONENID = WAD_SPEDITION_PERSONENID" "~"
if joinKuerzel = PERWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_SPEDITION_FIRMENNR = PER_FIRMENNR AND WAD_SPEDITION_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = AGSPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = AGS_LEISTEMPF_FIRMENNR AND PER_PERSONENID = AGS_LEISTEMPF_PERSONENID" "~"
if joinKuerzel = PERAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_LEISTEMPF_FIRMENNR = PER_FIRMENNR AND AGS_LEISTEMPF_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = APSPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = APS_VERANTW2_FIRMENNR AND PER_PERSONENID = APS_VERANTW2_PERID" "~"
if joinKuerzel = PERAPS
searchResult := searchResult "LEFT JOIN WWS.APS_ARBEITSPLAN_STAMM ON APS_VERANTW2_FIRMENNR = PER_FIRMENNR AND APS_VERANTW2_PERID = PER_PERSONENID" "~"
if joinKuerzel = BONPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = BON_LIEFEREMPF_FIRMENNR AND PER_PERSONENID = BON_LIEFEREMPF_KONPERSID" "~"
if joinKuerzel = PERBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_LIEFEREMPF_FIRMENNR = PER_FIRMENNR AND BON_LIEFEREMPF_KONPERSID = PER_PERSONENID" "~"
if joinKuerzel = BRVPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = BRV_V2_FIRMENNR AND PER_PERSONENID = BRV_V2_PERSONENID" "~"
if joinKuerzel = PERBRV
searchResult := searchResult "LEFT JOIN FIBU.BRV_BANKRUECKVERBUCHUNG ON BRV_V2_FIRMENNR = PER_FIRMENNR AND BRV_V2_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = BUCPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = BUC_V2_FIRMENNR AND PER_PERSONENID = BUC_V2_PERSONENID" "~"
if joinKuerzel = PERBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_V2_FIRMENNR = PER_FIRMENNR AND BUC_V2_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = EVNPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = EVN_KONTAKT_FIRMENNR AND PER_PERSONENID = EVN_KONTAKT_PERSONENID" "~"
if joinKuerzel = PEREVN
searchResult := searchResult "LEFT JOIN BUERO.EVN_TEILNEHMER ON EVN_KONTAKT_FIRMENNR = PER_FIRMENNR AND EVN_KONTAKT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = OLEOPE
searchResult := searchResult "LEFT JOIN LOHN.OPE_OF_PERSON ON OPE_KLIENTENNR = OLE_KLIENTENNR AND OPE_PERSNR = OLE_PERSNR AND OPE_PERSUNTERNR = OLE_PERSUNTERNR" "~"
if joinKuerzel = OPEOLE
searchResult := searchResult "LEFT JOIN LOHN.OLE_OF_LEISTUNG ON OLE_KLIENTENNR = OPE_KLIENTENNR AND OLE_PERSNR = OPE_PERSNR AND OLE_PERSUNTERNR = OPE_PERSUNTERNR" "~"
if joinKuerzel = OKLOPL
searchResult := searchResult "LEFT JOIN LOHN.OPL_OF_PLZ ON OPL_STAATENNR = OKL_LAND_STAATENNR AND OPL_OFPLZ = OKL_KLIENTENPLZ" "~"
if joinKuerzel = OPLOKL
searchResult := searchResult "LEFT JOIN LOHN.OKL_OF_KLIENT ON OKL_LAND_STAATENNR = OPL_STAATENNR AND OKL_KLIENTENPLZ = OPL_OFPLZ" "~"
if joinKuerzel = OLEORS
searchResult := searchResult "LEFT JOIN LOHN.ORS_OF_REISESTAMM ON ORS_REISENR = OLE_ORS_REISENR" "~"
if joinKuerzel = ORSOLE
searchResult := searchResult "LEFT JOIN LOHN.OLE_OF_LEISTUNG ON OLE_ORS_REISENR = ORS_REISENR" "~"
if joinKuerzel = TOZORT
searchResult := searchResult "LEFT JOIN ORT_ORT.ORT_ORT ON ORT_LANDNR = TOZ_ORT_LANDNR AND ORT_PLZ = TOZ_VON_PLZ AND ORT_PLZUNTERNR = TOZ_VON_PLZUNTERNR" "~"
if joinKuerzel = ORTTOZ
searchResult := searchResult "LEFT JOIN WWS.TOZ_TOURZUORDNUNG ON TOZ_ORT_LANDNR = ORT_LANDNR AND TOZ_VON_PLZ = ORT_PLZ AND TOZ_VON_PLZUNTERNR = ORT_PLZUNTERNR" "~"
if joinKuerzel = OTDOTH
searchResult := searchResult "LEFT JOIN BMD.OTH_OCRTEMPLATEHEADER ON OTH_OCRID = OTD_OCRID" "~"
if joinKuerzel = OTHOTD
searchResult := searchResult "LEFT JOIN BMD.OTD_OCRTEMPLATEDETAIL ON OTD_OCRID = OTH_OCRID" "~"
if joinKuerzel = PRPPAB
searchResult := searchResult "LEFT JOIN WWS.PAB_PROVARTBAUM ON PAB_FIRMENNR = PRP_PAB_FIRMENNR AND PAB_PABLFDNR = PRP_PAB_PABLFDNR" "~"
if joinKuerzel = PABPRP
searchResult := searchResult "LEFT JOIN WWS.PRP_PROVISIONTABPOS ON PRP_PAB_FIRMENNR = PAB_FIRMENNR AND PRP_PAB_PABLFDNR = PAB_PABLFDNR" "~"
if joinKuerzel = RPRPAB
searchResult := searchResult "LEFT JOIN WWS.PAB_PROVARTBAUM ON PAB_FIRMENNR = RPR_PAB_FIRMENNR AND PAB_PABLFDNR = RPR_PAB_PABLFDNR" "~"
if joinKuerzel = PABRPR
searchResult := searchResult "LEFT JOIN WWS.RPR_RECHNUNGSPROVISION ON RPR_PAB_FIRMENNR = PAB_FIRMENNR AND RPR_PAB_PABLFDNR = PAB_PABLFDNR" "~"
if joinKuerzel = VEPPAB
searchResult := searchResult "LEFT JOIN WWS.PAB_PROVARTBAUM ON PAB_FIRMENNR = VEP_PAB_FIRMENNR AND PAB_PABLFDNR = VEP_PAB_PABLFDNR" "~"
if joinKuerzel = PABVEP
searchResult := searchResult "LEFT JOIN WWS.VEP_VERTRETERABRECHPOS ON VEP_PAB_FIRMENNR = PAB_FIRMENNR AND VEP_PAB_PABLFDNR = PAB_PABLFDNR" "~"
if joinKuerzel = AGMPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_PPSKAPAID = AGM_PPSKAPAID" "~"
if joinKuerzel = PCKAGM
searchResult := searchResult "LEFT JOIN PRODUKTION.AGM_AG_PERSON_ZU ON AGM_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = KOZPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_PPSKAPAID = KOZ_PPSKAPAID" "~"
if joinKuerzel = PCKKOZ
searchResult := searchResult "LEFT JOIN PRODUKTION.KOZ_KOMMISSIONIERUNG_ZU ON KOZ_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = LEBPCK
searchResult := searchResult "LEFT JOIN PCK_PPS_KAPA.PCK_PPS_KAPA ON PCK_PPSKAPAID = LEB_PPSKAPAID" "~"
if joinKuerzel = PCKLEB
searchResult := searchResult "LEFT JOIN BUERO.LEB_LEBENSLAUF ON LEB_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = MEVPCK
searchResult := searchResult "LEFT JOIN PCK_PPS_KAPA.PCK_PPS_KAPA ON PCK_PPSKAPAID = MEV_PPSKAPAID" "~"
if joinKuerzel = PCKMEV
searchResult := searchResult "LEFT JOIN BUERO.MEV_MEDIENVERLEIH ON MEV_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = RSBPCK
searchResult := searchResult "LEFT JOIN PCK_PPS_KAPA.PCK_PPS_KAPA ON PCK_PPSKAPAID = RSB_PCK_PPSKAPAID" "~"
if joinKuerzel = PCKRSB
searchResult := searchResult "LEFT JOIN BUERO.RSB_RESSOURCENBEDARF ON RSB_PCK_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = PBDPDS
searchResult := searchResult "LEFT JOIN BIL.PDS_PERSKORST_DNSTAMM ON PDS_PDSID = PBD_PDSID" "~"
if joinKuerzel = PDSPBD
searchResult := searchResult "LEFT JOIN BIL.PBD_PERSKORST_BERECHDET ON PBD_PDSID = PDS_PDSID" "~"
if joinKuerzel = CPRPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = CPR_PER_FIRMENNR AND PER_PERSONENID = CPR_PERSONENID" "~"
if joinKuerzel = PERCPR
searchResult := searchResult "LEFT JOIN BUERO.CPR_CRMPROTOKOLL ON CPR_PER_FIRMENNR = PER_FIRMENNR AND CPR_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = EFKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = EFK_AUFTRAGGEBER_FIRMENNR AND PER_PERSONENID = EFK_AUFTRAGGEBER_KLID" "~"
if joinKuerzel = EFKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = EFK_RECHNUNGEMPF_FIRMENNR AND PER_PERSONENID = EFK_RECHNUNGEMPF_KLID" "~"
if joinKuerzel = EFKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = EFK_LIEFEREMPF_FIRMENNR AND PER_PERSONENID = EFK_LIEFEREMPF_KLID" "~"
if joinKuerzel = EFKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = EFK_AUFTRGEB_KONT_FIRMENNR AND PER_PERSONENID = EFK_AUFTRGEB_KONTAKTPERSID" "~"
if joinKuerzel = PEREFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_AUFTRGEB_KONT_FIRMENNR = PER_FIRMENNR AND EFK_AUFTRGEB_KONTAKTPERSID = PER_PERSONENID" "~"
if joinKuerzel = GMGPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = GMG_PER_FIRMENNR AND PER_PERSONENID = GMG_PER_PERSONENID" "~"
if joinKuerzel = PERGMG
searchResult := searchResult "LEFT JOIN BIL.GMG_GRUPPENMITGLIEDER ON GMG_PER_FIRMENNR = PER_FIRMENNR AND GMG_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = GZJPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = GZJ_GL_FIRMENNR AND PER_PERSONENID = GZJ_GL_PERSONENID" "~"
if joinKuerzel = PERGZJ
searchResult := searchResult "LEFT JOIN HRS.GZJ_GLAEUBIGERZU_JAHR ON GZJ_GL_FIRMENNR = PER_FIRMENNR AND GZJ_GL_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = HFTPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = HFT_PER_FIRMENNR AND PER_PERSONENID = HFT_PERSONENID" "~"
if joinKuerzel = PERHFT
searchResult := searchResult "LEFT JOIN BUERO.HFT_HILFSTABELLE ON HFT_PER_FIRMENNR = PER_FIRMENNR AND HFT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = HTKPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = HTK_FIRMENNR AND PER_PERSONENID = HTK_PERSONENID" "~"
if joinKuerzel = PERHTK
searchResult := searchResult "LEFT JOIN BUERO.HTK_HILFSTAB_KONTAKTKONTR ON HTK_FIRMENNR = PER_FIRMENNR AND HTK_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = KIZPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = KIZ_KUN_FIRMENNR AND PER_PERSONENID = KIZ_KUN_KLID" "~"
if joinKuerzel = PERKIZ
searchResult := searchResult "LEFT JOIN BUERO.KIZ_KUND_INFOTABPOSZU ON KIZ_KUN_FIRMENNR = PER_FIRMENNR AND KIZ_KUN_KLID = PER_PERSONENID" "~"
if joinKuerzel = KLIPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = KLI_STZUGEHOER_FIRMENNR AND PER_PERSONENID = KLI_STZUGEHOER_PERSONENID" "~"
if joinKuerzel = PERKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_STZUGEHOER_FIRMENNR = PER_FIRMENNR AND KLI_STZUGEHOER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PROMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = PRO_AL_FIRMENNR AND MIT_MITARBEITERID = PRO_AL_MITARBEITERID" "~"
if joinKuerzel = MITPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_AL_FIRMENNR = MIT_FIRMENNR AND PRO_AL_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = RNPMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = RNP_V2_FIRMENNR AND MIT_MITARBEITERID = RNP_V2_MITARBEITERID" "~"
if joinKuerzel = MITRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_V2_FIRMENNR = MIT_FIRMENNR AND RNP_V2_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = AWSMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = AWS_AL_MIT_FIRMENNR AND MIT_MITARBEITERID = AWS_AL_MIT_MITARBEITERID" "~"
if joinKuerzel = MITAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_AL_MIT_FIRMENNR = MIT_FIRMENNR AND AWS_AL_MIT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = EKKMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = EKK_VERTR4_FIRMENNR AND MIT_MITARBEITERID = EKK_VERTR4_MITARBEITERID" "~"
if joinKuerzel = MITEKK
searchResult := searchResult "LEFT JOIN WWS.EKK_EXTERNE_KUNDENKARTEI ON EKK_VERTR4_FIRMENNR = MIT_FIRMENNR AND EKK_VERTR4_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = RNGMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = RNG_V4_FIRMENNR AND MIT_MITARBEITERID = RNG_V4_MITARBEITERID" "~"
if joinKuerzel = MITRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_V4_FIRMENNR = MIT_FIRMENNR AND RNG_V4_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = KAKMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = KAK_VERK_FIRMENNR AND MIT_MITARBEITERID = KAK_VERK_MITARBEITERID" "~"
if joinKuerzel = MITKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_VERK_FIRMENNR = MIT_FIRMENNR AND KAK_VERK_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = AVEMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = AVE_ERPESD_MIT_FIRMENNR AND MIT_MITARBEITERID = AVE_ERPESD_MITARBEITERID" "~"
if joinKuerzel = MITAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ERPESD_MIT_FIRMENNR = MIT_FIRMENNR AND AVE_ERPESD_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = MOPMOK
searchResult := searchResult "LEFT JOIN WWS.MOK_MODELLKOPF ON MOK_FIRMENNR = MOP_FIRMENNR AND MOK_MODELLKOPFGUID = MOP_MODELLKOPFGUID" "~"
if joinKuerzel = MOKMOP
searchResult := searchResult "LEFT JOIN WWS.MOP_MODELLPOSVAR ON MOP_FIRMENNR = MOK_FIRMENNR AND MOP_MODELLKOPFGUID = MOK_MODELLKOPFGUID" "~"
if joinKuerzel = MPDMOK
searchResult := searchResult "LEFT JOIN WWS.MOK_MODELLKOPF ON MOK_FIRMENNR = MPD_FIRMENNR AND MOK_MODELLKOPFGUID = MPD_MODELLKOPFGUID" "~"
if joinKuerzel = MOKMPD
searchResult := searchResult "LEFT JOIN WWS.MPD_MODELLPOSVARDETAIL ON MPD_FIRMENNR = MOK_FIRMENNR AND MPD_MODELLKOPFGUID = MOK_MODELLKOPFGUID" "~"
if joinKuerzel = MVPMVL
searchResult := searchResult "LEFT JOIN BUERO.MVL_MITARBEITERVORLAGE ON MVL_MVLID = MVP_MVLID" "~"
if joinKuerzel = MVLMVP
searchResult := searchResult "LEFT JOIN BUERO.MVP_MITARBEITERVORLAGEPOS ON MVP_MVLID = MVL_MVLID" "~"
if joinKuerzel = MWRMWK
searchResult := searchResult "LEFT JOIN BIL.MWK_MWRKOPF ON MWK_MWRKOPFID = MWR_MWRKOPFID" "~"
if joinKuerzel = MWKMWR
searchResult := searchResult "LEFT JOIN BIL.MWR_MWRDATEN ON MWR_MWRKOPFID = MWK_MWRKOPFID" "~"
if joinKuerzel = NUDNUP
searchResult := searchResult "LEFT JOIN BMD.NUP_NTCSUPDATE ON NUP_NTCSUPDATEID = NUD_NTCSUPDATEID" "~"
if joinKuerzel = NUPNUD
searchResult := searchResult "LEFT JOIN BMD.NUD_NTCSUPDATEONLINEDATEI ON NUD_NTCSUPDATEID = NUP_NTCSUPDATEID" "~"
if joinKuerzel = OBAOBP
searchResult := searchResult "LEFT JOIN BUERO.OBP_ONLBEWPERSSTAMM ON OBP_PERSSTAMMID = OBA_OBP_PERSSTAMMID" "~"
if joinKuerzel = OBPOBA
searchResult := searchResult "LEFT JOIN BUERO.OBA_ONLBEWAUSSCHREIBUNGZU ON OBA_OBP_PERSSTAMMID = OBP_PERSSTAMMID" "~"
if joinKuerzel = NUDODA
searchResult := searchResult "LEFT JOIN BMD.ODA_ONLINEDATEI ON ODA_ONLDATEIID = NUD_ONLDATEIID" "~"
if joinKuerzel = ODANUD
searchResult := searchResult "LEFT JOIN BMD.NUD_NTCSUPDATEONLINEDATEI ON NUD_ONLDATEIID = ODA_ONLDATEIID" "~"
if joinKuerzel = LDDODT
searchResult := searchResult "LEFT JOIN BMD.ODT_ONLINEDATEITRANSFER ON ODT_ONLDATEITRANSFERID = LDD_ONLDATEITRANSFERID" "~"
if joinKuerzel = ODTLDD
searchResult := searchResult "LEFT JOIN BMD.LDD_LIZDATEIDOWNLOAD ON LDD_ONLDATEITRANSFERID = ODT_ONLDATEITRANSFERID" "~"
if joinKuerzel = FRZOFR
searchResult := searchResult "LEFT JOIN BUERO.OFR_FRAGEBOGENREGEL ON OFR_OFRID = FRZ_OFRID" "~"
if joinKuerzel = OFRFRZ
searchResult := searchResult "LEFT JOIN BUERO.FRZ_FRAGEBOGENREGELZU ON FRZ_OFRID = OFR_OFRID" "~"
if joinKuerzel = OPEOGE
searchResult := searchResult "LEFT JOIN LOHN.OGE_OF_GEMEINDE ON OGE_KLIENTENNR = OPE_KLIENTENNR AND OGE_GEMNR = OPE_GEMNR" "~"
if joinKuerzel = OGEOPE
searchResult := searchResult "LEFT JOIN LOHN.OPE_OF_PERSON ON OPE_KLIENTENNR = OGE_KLIENTENNR AND OPE_GEMNR = OGE_GEMNR" "~"
if joinKuerzel = OAZOKL
searchResult := searchResult "LEFT JOIN LOHN.OKL_OF_KLIENT ON OKL_KLIENTENNR = OAZ_KLIENTENNR" "~"
if joinKuerzel = OKLOAZ
searchResult := searchResult "LEFT JOIN LOHN.OAZ_OF_ARBEITSZEITMODELL ON OAZ_KLIENTENNR = OKL_KLIENTENNR" "~"
if joinKuerzel = OFAOKL
searchResult := searchResult "LEFT JOIN LOHN.OKL_OF_KLIENT ON OKL_KLIENTENNR = OFA_KLIENTENNR" "~"
if joinKuerzel = OKLOFA
searchResult := searchResult "LEFT JOIN LOHN.OFA_OF_FINANZAMT ON OFA_KLIENTENNR = OKL_KLIENTENNR" "~"
if joinKuerzel = OFKOKL
searchResult := searchResult "LEFT JOIN LOHN.OKL_OF_KLIENT ON OKL_KLIENTENNR = OFK_KLIENTENNR" "~"
if joinKuerzel = OKLOFK
searchResult := searchResult "LEFT JOIN LOHN.OFK_OF_FKREIS ON OFK_KLIENTENNR = OKL_KLIENTENNR" "~"
if joinKuerzel = OKSOKL
searchResult := searchResult "LEFT JOIN LOHN.OKL_OF_KLIENT ON OKL_KLIENTENNR = OKS_KLIENTENNR" "~"
if joinKuerzel = OKLOKS
searchResult := searchResult "LEFT JOIN LOHN.OKS_OF_KOSTENSTELLE ON OKS_KLIENTENNR = OKL_KLIENTENNR" "~"
if joinKuerzel = OLEOKT
searchResult := searchResult "LEFT JOIN LOHN.OKT_OF_KOSTENTRAEGER ON OKT_KLIENTENNR = OLE_KLIENTENNR AND OKT_KOSTENTRAEGERNR = OLE_OKT_KOSTENTRAEGERNR" "~"
if joinKuerzel = OKTOLE
searchResult := searchResult "LEFT JOIN LOHN.OLE_OF_LEISTUNG ON OLE_KLIENTENNR = OKT_KLIENTENNR AND OLE_OKT_KOSTENTRAEGERNR = OKT_KOSTENTRAEGERNR" "~"
if joinKuerzel = MRDMRV
searchResult := searchResult "LEFT JOIN ZLE.MRV_MONATSRECHNUNGVORLAGE ON MRV_MRVID = MRD_MRVID" "~"
if joinKuerzel = MRVMRD
searchResult := searchResult "LEFT JOIN ZLE.MRD_MONATSRECHNUNGDETAIL ON MRD_MRVID = MRV_MRVID" "~"
if joinKuerzel = ANONOR
searchResult := searchResult "LEFT JOIN WWS.NOR_NORM ON NOR_NORMID = ANO_NORMID" "~"
if joinKuerzel = NORANO
searchResult := searchResult "LEFT JOIN WWS.ANO_ARTIKELNORM ON ANO_NORMID = NOR_NORMID" "~"
if joinKuerzel = LFZNTC
searchResult := searchResult "LEFT JOIN NTC_NTCSARTIKEL.NTC_NTCSARTIKEL ON NTC_NTCSARTIKELNR = LFZ_NTCSARTIKELNR" "~"
if joinKuerzel = NTCLFZ
searchResult := searchResult "LEFT JOIN BMDDEFAULT.LFZ_DLIZG_NTC_ZU ON LFZ_NTCSARTIKELNR = NTC_NTCSARTIKELNR" "~"
if joinKuerzel = OGEOKL
searchResult := searchResult "LEFT JOIN LOHN.OKL_OF_KLIENT ON OKL_KLIENTENNR = OGE_KLIENTENNR" "~"
if joinKuerzel = OKLOGE
searchResult := searchResult "LEFT JOIN LOHN.OGE_OF_GEMEINDE ON OGE_KLIENTENNR = OKL_KLIENTENNR" "~"
if joinKuerzel = OGKOKL
searchResult := searchResult "LEFT JOIN LOHN.OKL_OF_KLIENT ON OKL_KLIENTENNR = OGK_KLIENTENNR" "~"
if joinKuerzel = OKLOGK
searchResult := searchResult "LEFT JOIN LOHN.OGK_OF_GKK ON OGK_KLIENTENNR = OKL_KLIENTENNR" "~"
if joinKuerzel = ONZONS
searchResult := searchResult "LEFT JOIN HRS.ONS_ONLINEBEW_STAMMDATEN ON ONS_ONLBEWID = ONZ_ONLBEWID" "~"
if joinKuerzel = ONSONZ
searchResult := searchResult "LEFT JOIN HRS.ONZ_ONLINEBEW_STELLE_ZU ON ONZ_ONLBEWID = ONS_ONLBEWID" "~"
if joinKuerzel = OLEORO
searchResult := searchResult "LEFT JOIN LOHN.ORO_OF_REGORT ON ORO_STAATENNR = OLE_ORO_STAATENNR AND ORO_REGNR = OLE_ORO_REGNR" "~"
if joinKuerzel = OROOLE
searchResult := searchResult "LEFT JOIN LOHN.OLE_OF_LEISTUNG ON OLE_ORO_STAATENNR = ORO_STAATENNR AND OLE_ORO_REGNR = ORO_REGNR" "~"
if joinKuerzel = ADRORT
searchResult := searchResult "LEFT JOIN BUERO.ORT_ORT ON ORT_LANDNR = ADR_ORT_LANDNR AND ORT_PLZ = ADR_PLZ AND ORT_PLZUNTERNR = ADR_PLZUNTERNR" "~"
if joinKuerzel = ORTADR
searchResult := searchResult "LEFT JOIN BUERO.ADR_ADRESSE ON ADR_ORT_LANDNR = ORT_LANDNR AND ADR_PLZ = ORT_PLZ AND ADR_PLZUNTERNR = ORT_PLZUNTERNR" "~"
if joinKuerzel = FTBORT
searchResult := searchResult "LEFT JOIN BUERO.ORT_ORT ON ORT_LANDNR = FTB_ABFAHRT_ORT_LANDNR AND ORT_PLZ = FTB_ABFAHRT_ORT_PLZ AND ORT_PLZUNTERNR = FTB_ABFAHRT_ORT_PLZUNTERNR" "~"
if joinKuerzel = ORTFTB
searchResult := searchResult "LEFT JOIN BUERO.FTB_FAHRTENBUCH ON FTB_ABFAHRT_ORT_LANDNR = ORT_LANDNR AND FTB_ABFAHRT_ORT_PLZ = ORT_PLZ AND FTB_ABFAHRT_ORT_PLZUNTERNR = ORT_PLZUNTERNR" "~"
if joinKuerzel = PVFORT
searchResult := searchResult "LEFT JOIN BUERO.ORT_ORT ON ORT_LANDNR = PVF_ORT_LANDNR AND ORT_PLZ = PVF_PLZ AND ORT_PLZUNTERNR = PVF_PLZUNTERNR" "~"
if joinKuerzel = ORTPVF
searchResult := searchResult "LEFT JOIN BUERO.PVF_PERSONENVORERFASSUNG ON PVF_ORT_LANDNR = ORT_LANDNR AND PVF_PLZ = ORT_PLZ AND PVF_PLZUNTERNR = ORT_PLZUNTERNR" "~"
if joinKuerzel = SLZORT
searchResult := searchResult "LEFT JOIN ORT_ORT.ORT_ORT ON ORT_LANDNR = SLZ_AL_LAND_AG AND ORT_PLZ = SLZ_AL_PLZ_AG AND ORT_PLZUNTERNR = SLZ_AL_PLZ_UNR_AG" "~"
if joinKuerzel = ORTSLZ
searchResult := searchResult "LEFT JOIN BIL.SLZ_STEUERLOHNZETTEL ON SLZ_AL_LAND_AG = ORT_LANDNR AND SLZ_AL_PLZ_AG = ORT_PLZ AND SLZ_AL_PLZ_UNR_AG = ORT_PLZUNTERNR" "~"
if joinKuerzel = OPEOSZ
searchResult := searchResult "LEFT JOIN LOHN.OSZ_OF_SWBKZ ON OSZ_KLIENTENNR = OPE_KLIENTENNR AND OSZ_SWBKZLFDNR = OPE_SWBKZLFDNR" "~"
if joinKuerzel = OSZOPE
searchResult := searchResult "LEFT JOIN LOHN.OPE_OF_PERSON ON OPE_KLIENTENNR = OSZ_KLIENTENNR AND OPE_SWBKZLFDNR = OSZ_SWBKZLFDNR" "~"
if joinKuerzel = OPEOVE
searchResult := searchResult "LEFT JOIN LOHN.OVE_OF_VERANTWORTLICHER ON OVE_KLIENTENNR = OPE_KLIENTENNR AND OVE_VERANTWNR = OPE_VERANTWNR" "~"
if joinKuerzel = OVEOPE
searchResult := searchResult "LEFT JOIN LOHN.OPE_OF_PERSON ON OPE_KLIENTENNR = OVE_KLIENTENNR AND OPE_VERANTWNR = OVE_VERANTWNR" "~"
if joinKuerzel = PANPAN
searchResult := searchResult "LEFT JOIN WWS.PAN_PROVARTBAUMKNOTEN ON PAN_FIRMENNR = PAN_FIRMENNR AND PAN_PABLFDNR = PAN_PABLFDNR AND PAN_PANLFDNR = PAN_PAN_PANLFDNR" "~"
if joinKuerzel = PANPAN
searchResult := searchResult "LEFT JOIN WWS.PAN_PROVARTBAUMKNOTEN ON PAN_FIRMENNR = PAN_FIRMENNR AND PAN_PABLFDNR = PAN_PABLFDNR AND PAN_PAN_PANLFDNR = PAN_PANLFDNR" "~"
if joinKuerzel = APZPAT
searchResult := searchResult "LEFT JOIN WWS.PAT_PACKSTOFFTAB ON PAT_FIRMENNR = APZ_FIRMENNR AND PAT_PACKSTOFFTABNR = APZ_PAT_PACKSTOFFTABNR" "~"
if joinKuerzel = PATAPZ
searchResult := searchResult "LEFT JOIN WWS.APZ_ARAPACKSTOFFTABZU ON APZ_FIRMENNR = PAT_FIRMENNR AND APZ_PAT_PACKSTOFFTABNR = PAT_PACKSTOFFTABNR" "~"
if joinKuerzel = APAPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_PPSKAPAID = APA_AG_END_ID" "~"
if joinKuerzel = PCKAPA
searchResult := searchResult "LEFT JOIN PRODUKTION.APA_ARBEITSPLAN ON APA_AG_END_ID = PCK_PPSKAPAID" "~"
if joinKuerzel = AVEPCK
searchResult := searchResult "LEFT JOIN PCK_PPS_KAPA.PCK_PPS_KAPA ON PCK_PPSKAPAID = AVE_PCK_PPSKAPAID" "~"
if joinKuerzel = PCKAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_PCK_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = BPOPCK
searchResult := searchResult "LEFT JOIN PCK_PPS_KAPA.PCK_PPS_KAPA ON PCK_PPSKAPAID = BPO_PPSKAPAID" "~"
if joinKuerzel = PCKBPO
searchResult := searchResult "LEFT JOIN BUERO.BPO_BESPRECHOBJEKTE ON BPO_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = FSTPCK
searchResult := searchResult "LEFT JOIN PCK_PPS_KAPA.PCK_PPS_KAPA ON PCK_PPSKAPAID = FST_PPSKAPAID" "~"
if joinKuerzel = PCKFST
searchResult := searchResult "LEFT JOIN BUERO.FST_FRIST ON FST_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = IDPPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_PPSKAPAID = IDP_PPSKAPAID" "~"
if joinKuerzel = PCKIDP
searchResult := searchResult "LEFT JOIN PRODUKTION.IDP_INFO_DELAY_BREAKDOWN ON IDP_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = VOBPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_PPSKAPAID = VOB_PCK_PPSKAPAID" "~"
if joinKuerzel = PCKVOB
searchResult := searchResult "LEFT JOIN PRODUKTION.VOB_VORGANGSBENACHR ON VOB_PCK_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = PSAPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = PSA_FIRMENNR AND PSJ_MITARBEITERID = PSA_MITARBEITERID AND PSJ_DIENSTVLFDNR = PSA_DIENSTVLFDNR AND PSJ_LOHNJAHR = PSA_LOHNJAHR" "~"
if joinKuerzel = PSJPSA
searchResult := searchResult "LEFT JOIN HRS.PSA_PERSONALSTAMM_ABRSTAT ON PSA_FIRMENNR = PSJ_FIRMENNR AND PSA_MITARBEITERID = PSJ_MITARBEITERID AND PSA_DIENSTVLFDNR = PSJ_DIENSTVLFDNR AND PSA_LOHNJAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = ELILFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = ELI_FIRMENNR AND LFI_LOHNJAHR = ELI_JAHR" "~"
if joinKuerzel = LFIELI
searchResult := searchResult "LEFT JOIN HRS.ELI_ELDADETAIL_LZI ON ELI_FIRMENNR = LFI_FIRMENNR AND ELI_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = EVRLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = EVR_FIRMENNR AND LFI_LOHNJAHR = EVR_JAHR" "~"
if joinKuerzel = LFIEVR
searchResult := searchResult "LEFT JOIN HRS.EVR_ELDADETAIL_VMR ON EVR_FIRMENNR = LFI_FIRMENNR AND EVR_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = LENLFI
searchResult := searchResult "LEFT JOIN LFI_LOHNFIRMA.LFI_LOHNFIRMA ON LFI_FIRMENNR = LEN_LFI_FIRMENNR AND LFI_LOHNJAHR = LEN_LFI_LOHNJAHR" "~"
if joinKuerzel = LFILEN
searchResult := searchResult "LEFT JOIN BUERO.LEN_LOHNEINSTELLUNGEN ON LEN_LFI_FIRMENNR = LFI_FIRMENNR AND LEN_LFI_LOHNJAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = PSMLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = PSM_DVFIRMENNR AND LFI_LOHNJAHR = PSM_PSMJAHR" "~"
if joinKuerzel = LFIPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_DVFIRMENNR = LFI_FIRMENNR AND PSM_PSMJAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = EVRLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = EVR_DVFIRMENNR AND LFI_LOHNJAHR = EVR_JAHR" "~"
if joinKuerzel = LFIEVR
searchResult := searchResult "LEFT JOIN HRS.EVR_ELDADETAIL_VMR ON EVR_DVFIRMENNR = LFI_FIRMENNR AND EVR_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = PSJLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = PSJ_DVFIRMENNR AND LFI_LOHNJAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = LFIPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_DVFIRMENNR = LFI_FIRMENNR AND PSJ_LOHNJAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = BONLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_FIRMENNR = BON_LFS_FIRMENNR AND LFS_LIEFERSCHEINLFDNR = BON_LFS_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = LFSBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_LFS_FIRMENNR = LFS_FIRMENNR AND BON_LFS_LIEFERSCHEINLFDNR = LFS_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = EKLLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_FIRMENNR = EKL_FIRMENNR AND LFS_LIEFERSCHEINLFDNR = EKL_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = LFSEKL
searchResult := searchResult "LEFT JOIN WWS.EKL_ERFASSKOPFLFS ON EKL_FIRMENNR = LFS_FIRMENNR AND EKL_LIEFERSCHEINLFDNR = LFS_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = WAKLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_FIRMENNR = WAK_LFS_FIRMENNR AND LFS_LIEFERSCHEINLFDNR = WAK_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = LFSWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_LFS_FIRMENNR = LFS_FIRMENNR AND WAK_LIEFERSCHEINLFDNR = LFS_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = PTZLIB
searchResult := searchResult "LEFT JOIN WWS.LIB_LIEFERBEDINGUNG ON LIB_LIEFERBNR = PTZ_LIEFERBNR" "~"
if joinKuerzel = LIBPTZ
searchResult := searchResult "LEFT JOIN WWS.PTZ_PREISTABZU ON PTZ_LIEFERBNR = LIB_LIEFERBNR" "~"
if joinKuerzel = LEILOA
searchResult := searchResult "LEFT JOIN LOA_LOHNART.LOA_LOHNART ON LOA_LOHNARTID = LEI_LOHNARTID" "~"
if joinKuerzel = LOALEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_LOHNARTID = LOA_LOHNARTID" "~"
if joinKuerzel = DISLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_FIRMENNR = DIS_FIRMENNR AND LPO_LIEFERSCHEINLFDNR = DIS_LIEFERSCHEINLFDNR AND LPO_LIEFERSCHEINPOSLFDNR = DIS_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = LPODIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_FIRMENNR = LPO_FIRMENNR AND DIS_LIEFERSCHEINLFDNR = LPO_LIEFERSCHEINLFDNR AND DIS_LIEFERSCHEINPOSLFDNR = LPO_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = KJOLTX
searchResult := searchResult "LEFT JOIN BUERO.LTX_LANGTEXT ON LTX_LANGTXTLFDNR = KJO_LANGTXTLFDNR" "~"
if joinKuerzel = LTXKJO
searchResult := searchResult "LEFT JOIN BUERO.KJO_KONTAKTJOURNAL ON KJO_LANGTXTLFDNR = LTX_LANGTXTLFDNR" "~"
if joinKuerzel = VASLTX
searchResult := searchResult "LEFT JOIN BUERO.LTX_LANGTEXT ON LTX_LANGTXTLFDNR = VAS_KUZ_LANGTXTLFDNR" "~"
if joinKuerzel = LTXVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_KUZ_LANGTXTLFDNR = LTX_LANGTXTLFDNR" "~"
if joinKuerzel = EVTLTX
searchResult := searchResult "LEFT JOIN BUERO.LTX_LANGTEXT ON LTX_LANGTXTLFDNR = EVT_KUZ_LANGTXTLFDNR" "~"
if joinKuerzel = LTXEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_KUZ_LANGTXTLFDNR = LTX_LANGTXTLFDNR" "~"
if joinKuerzel = EVTLTX
searchResult := searchResult "LEFT JOIN BUERO.LTX_LANGTEXT ON LTX_LANGTXTLFDNR = EVT_KUI_LANGTXTLFDNR" "~"
if joinKuerzel = LTXEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_KUI_LANGTXTLFDNR = LTX_LANGTXTLFDNR" "~"
if joinKuerzel = LKPLVK
searchResult := searchResult "LEFT JOIN BMDINTERN.LVK_LIZENZVKP ON LVK_LVKID = LKP_LVKID" "~"
if joinKuerzel = LVKLKP
searchResult := searchResult "LEFT JOIN BMDINTERN.LKP_LIZENZKUNDENVKPZU ON LKP_LVKID = LVK_LVKID" "~"
if joinKuerzel = BETMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_MEDIENID = BET_MED_MEDIENID" "~"
if joinKuerzel = MEDBET
searchResult := searchResult "LEFT JOIN BUERO.BET_BETRIEBSMITTEL ON BET_MED_MEDIENID = MED_MEDIENID" "~"
if joinKuerzel = RZUMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_MEDIENID = RZU_MEDIENID" "~"
if joinKuerzel = MEDRZU
searchResult := searchResult "LEFT JOIN BUERO.RZU_RESSOURCENPAKETZU ON RZU_MEDIENID = MED_MEDIENID" "~"
if joinKuerzel = SNRMED
searchResult := searchResult "LEFT JOIN MED_MEDIEN.MED_MEDIEN ON MED_MEDIENID = SNR_MEDIENID" "~"
if joinKuerzel = MEDSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_MEDIENID = MED_MEDIENID" "~"
if joinKuerzel = ARLMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = ARL_BEARB_EK_FIRMENNR AND MIT_MITARBEITERID = ARL_BEARB_EK_MITARBID" "~"
if joinKuerzel = MITARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_BEARB_EK_FIRMENNR = MIT_FIRMENNR AND ARL_BEARB_EK_MITARBID = MIT_MITARBEITERID" "~"
if joinKuerzel = AVKMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = AVK_MIT_FIRMENNR AND MIT_MITARBEITERID = AVK_MIT_ID" "~"
if joinKuerzel = MITAVK
searchResult := searchResult "LEFT JOIN BUERO.AVK_AVKONTINGENTE ON AVK_MIT_FIRMENNR = MIT_FIRMENNR AND AVK_MIT_ID = MIT_MITARBEITERID" "~"
if joinKuerzel = LEDPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = LED_PER_FIRMENNR AND PER_PERSONENID = LED_PER_PERSONENID" "~"
if joinKuerzel = PERLED
searchResult := searchResult "LEFT JOIN BUERO.LED_LEAD ON LED_PER_FIRMENNR = PER_FIRMENNR AND LED_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = LEIPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = LEI_PER_FIRMENNR AND PER_PERSONENID = LEI_PER_PERSONENID" "~"
if joinKuerzel = PERLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_PER_FIRMENNR = PER_FIRMENNR AND LEI_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = MKOPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = MKO_FIRMENNR AND PER_PERSONENID = MKO_PERSONENID" "~"
if joinKuerzel = PERMKO
searchResult := searchResult "LEFT JOIN BIL.MKO_MINDESTKOEST ON MKO_FIRMENNR = PER_FIRMENNR AND MKO_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = OBPPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = OBP_PER_FIRMENNR AND PER_PERSONENID = OBP_PER_PERSONENID" "~"
if joinKuerzel = PEROBP
searchResult := searchResult "LEFT JOIN BUERO.OBP_ONLBEWPERSSTAMM ON OBP_PER_FIRMENNR = PER_FIRMENNR AND OBP_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PICPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = PIC_PROV_FIRMENNR AND PER_PERSONENID = PIC_PROV_PERSONENID" "~"
if joinKuerzel = PERPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_PROV_FIRMENNR = PER_FIRMENNR AND PIC_PROV_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PKZPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PKZ_KONTAKT_FIRMENNR AND PER_PERSONENID = PKZ_KONTAKT_PERSONENID" "~"
if joinKuerzel = PERPKZ
searchResult := searchResult "LEFT JOIN BUERO.PKZ_PERSONKONTAKTZU ON PKZ_KONTAKT_FIRMENNR = PER_FIRMENNR AND PKZ_KONTAKT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PPFPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PPF_FIRMENNR AND PER_PERSONENID = PPF_PERSONENID" "~"
if joinKuerzel = PERPPF
searchResult := searchResult "LEFT JOIN BUERO.PPF_PERSONENPRUEFUNG ON PPF_FIRMENNR = PER_FIRMENNR AND PPF_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PROPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PRO_PER_FIRMENNR AND PER_PERSONENID = PRO_PERSONENID" "~"
if joinKuerzel = PERPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_PER_FIRMENNR = PER_FIRMENNR AND PRO_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = RPZPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = RPZ_PER_FIRMENNR AND PER_PERSONENID = RPZ_PERSONENID" "~"
if joinKuerzel = PERRPZ
searchResult := searchResult "LEFT JOIN ZLE.RPZ_RECHPOSZU ON RPZ_PER_FIRMENNR = PER_FIRMENNR AND RPZ_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = RTZPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = RTZ_PER_FIRMENNR AND PER_PERSONENID = RTZ_PER_PERSONENID" "~"
if joinKuerzel = PERRTZ
searchResult := searchResult "LEFT JOIN ZLE.RTZ_RECHTEXTZU ON RTZ_PER_FIRMENNR = PER_FIRMENNR AND RTZ_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = SCAPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = SCA_PER_FIRMENNR AND PER_PERSONENID = SCA_PER_PERSONENID" "~"
if joinKuerzel = PERSCA
searchResult := searchResult "LEFT JOIN PRODUKTION.SCA_SCKRITERIUM ON SCA_PER_FIRMENNR = PER_FIRMENNR AND SCA_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = SCOPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = SCO_PER_FIRMENNR AND PER_PERSONENID = SCO_PER_PERSONENID" "~"
if joinKuerzel = PERSCO
searchResult := searchResult "LEFT JOIN PRODUKTION.SCO_SCINSTANZ ON SCO_PER_FIRMENNR = PER_FIRMENNR AND SCO_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = SLZPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = SLZ_PER_FIRMENNR AND PER_PERSONENID = SLZ_PERSONENID" "~"
if joinKuerzel = PERSLZ
searchResult := searchResult "LEFT JOIN BIL.SLZ_STEUERLOHNZETTEL ON SLZ_PER_FIRMENNR = PER_FIRMENNR AND SLZ_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = SPNPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = SPN_PER_FIRMENNR AND PER_PERSONENID = SPN_PER_PERSONENID" "~"
if joinKuerzel = PERSPN
searchResult := searchResult "LEFT JOIN FIBU.SPN_SPENDE ON SPN_PER_FIRMENNR = PER_FIRMENNR AND SPN_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = TPRPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = TPR_PER_FIRMENNR AND PER_PERSONENID = TPR_PER_PERSONENID" "~"
if joinKuerzel = PERTPR
searchResult := searchResult "LEFT JOIN BUERO.TPR_TELEFONPROTOKOLL ON TPR_PER_FIRMENNR = PER_FIRMENNR AND TPR_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = VMKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = VMK_PERSONEN_FIRMENNR AND PER_PERSONENID = VMK_PERSONEN_ID" "~"
if joinKuerzel = PERVMK
searchResult := searchResult "LEFT JOIN WWS.VMK_VERSMGR_KOPF ON VMK_PERSONEN_FIRMENNR = PER_FIRMENNR AND VMK_PERSONEN_ID = PER_PERSONENID" "~"
if joinKuerzel = WAMPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = WAM_BEST_FIRMENNR AND PER_PERSONENID = WAM_BEST_PERSONENID" "~"
if joinKuerzel = PERWAM
searchResult := searchResult "LEFT JOIN BUERO.WAM_WEBANMELDUNG ON WAM_BEST_FIRMENNR = PER_FIRMENNR AND WAM_BEST_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = WBWPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = WBW_VERTRETER_FIRMENNR AND PER_PERSONENID = WBW_VERTRETER_PERSONENID" "~"
if joinKuerzel = PERWBW
searchResult := searchResult "LEFT JOIN WWS.WBW_WWSBUDGETWERTE ON WBW_VERTRETER_FIRMENNR = PER_FIRMENNR AND WBW_VERTRETER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = WEPPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = WEP_PER_FIRMENNR AND PER_PERSONENID = WEP_PERSONENID" "~"
if joinKuerzel = PERWEP
searchResult := searchResult "LEFT JOIN WWS.WEP_WWSERKONTROLLEPOS ON WEP_PER_FIRMENNR = PER_FIRMENNR AND WEP_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = WOLPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = WOL_PER_FIRMENNR AND PER_PERSONENID = WOL_PER_PERSONENID" "~"
if joinKuerzel = PERWOL
searchResult := searchResult "LEFT JOIN ZLE.WOL_WOCHENLEISTUNG ON WOL_PER_FIRMENNR = PER_FIRMENNR AND WOL_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = ZKRPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = ZKR_SACHBEARB_FIRMENNR AND PER_PERSONENID = ZKR_SACHBEARB_PERSID" "~"
if joinKuerzel = PERZKR
searchResult := searchResult "LEFT JOIN FIBU.ZKR_ZAHLUNGSKREIS ON ZKR_SACHBEARB_FIRMENNR = PER_FIRMENNR AND ZKR_SACHBEARB_PERSID = PER_PERSONENID" "~"
if joinKuerzel = LPEPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = LPE_PER_FIRMENNR AND PER_PERSONENID = LPE_PER_PERSONENID" "~"
if joinKuerzel = PERLPE
searchResult := searchResult "LEFT JOIN HRS.LPE_PERSON ON LPE_PER_FIRMENNR = PER_FIRMENNR AND LPE_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = LVEPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = LVE_PER_FIRMENNR AND PER_PERSONENID = LVE_PER_PERSONENID" "~"
if joinKuerzel = PERLVE
searchResult := searchResult "LEFT JOIN PRODUKTION.LVE_LEISTUNGSVORERFASSUNG ON LVE_PER_FIRMENNR = PER_FIRMENNR AND LVE_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = MWKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = MWK_PER_FIRMENNR AND PER_PERSONENID = MWK_PERSONENID" "~"
if joinKuerzel = PERMWK
searchResult := searchResult "LEFT JOIN BIL.MWK_MWRKOPF ON MWK_PER_FIRMENNR = PER_FIRMENNR AND MWK_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = NUTPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = NUT_PER_FIRMENNR AND PER_PERSONENID = NUT_PERSONENID" "~"
if joinKuerzel = PERNUT
searchResult := searchResult "LEFT JOIN BMD.NUT_NTCSUPDTRANSFER ON NUT_PER_FIRMENNR = PER_FIRMENNR AND NUT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PLCPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = PLC_HERKUNFT_PER_FIRMENNR AND PER_PERSONENID = PLC_HERKUNFT_PERID" "~"
if joinKuerzel = PERPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_HERKUNFT_PER_FIRMENNR = PER_FIRMENNR AND PLC_HERKUNFT_PERID = PER_PERSONENID" "~"
if joinKuerzel = PUPPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PUP_FIRMENNR AND PER_PERSONENID = PUP_PERSONENID" "~"
if joinKuerzel = PERPUP
searchResult := searchResult "LEFT JOIN BUERO.PUP_PERSONENUPDATE ON PUP_FIRMENNR = PER_FIRMENNR AND PUP_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = QAWPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = QAW_AL_FIRMENNR AND PER_PERSONENID = QAW_AL_PERSONENID" "~"
if joinKuerzel = PERQAW
searchResult := searchResult "LEFT JOIN BUERO.QAW_QUOTENAUSWERTUNG ON QAW_AL_FIRMENNR = PER_FIRMENNR AND QAW_AL_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = SCEPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = SCE_PER_FIRMENNR AND PER_PERSONENID = SCE_PER_PERSONENID" "~"
if joinKuerzel = PERSCE
searchResult := searchResult "LEFT JOIN PRODUKTION.SCE_SCORECARD ON SCE_PER_FIRMENNR = PER_FIRMENNR AND SCE_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = SCMPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = SCM_PER_FIRMENNR AND PER_PERSONENID = SCM_PERSONENID" "~"
if joinKuerzel = PERSCM
searchResult := searchResult "LEFT JOIN BMDENTW.SCM_SCRMAIN ON SCM_PER_FIRMENNR = PER_FIRMENNR AND SCM_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = TERPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = TER_PER_FIRMENNR AND PER_PERSONENID = TER_PERSONENID" "~"
if joinKuerzel = PERTER
searchResult := searchResult "LEFT JOIN BUERO.TER_TERMIN ON TER_PER_FIRMENNR = PER_FIRMENNR AND TER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = UBEPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = UBE_PER_FIRMENNR AND PER_PERSONENID = UBE_KONTAKT_PERSONENID" "~"
if joinKuerzel = PERUBE
searchResult := searchResult "LEFT JOIN BMDINTERN.UBE_UPDATEBESTELLUNG ON UBE_PER_FIRMENNR = PER_FIRMENNR AND UBE_KONTAKT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = VASPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = VAS_PER_FIRMENNR AND PER_PERSONENID = VAS_PER_PERSONENID" "~"
if joinKuerzel = PERVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_PER_FIRMENNR = PER_FIRMENNR AND VAS_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = VATPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = VAT_TN_PER_FIRMENNR AND PER_PERSONENID = VAT_TN_PER_PERSONENID" "~"
if joinKuerzel = PERVAT
searchResult := searchResult "LEFT JOIN BUERO.VAT_TEILNAHME ON VAT_TN_PER_FIRMENNR = PER_FIRMENNR AND VAT_TN_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = VETPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = VET_PER_FIRMENNR AND PER_PERSONENID = VET_PER_PERSONENID" "~"
if joinKuerzel = PERVET
searchResult := searchResult "LEFT JOIN BUERO.VET_VERANSTALTUNGSTAG ON VET_PER_FIRMENNR = PER_FIRMENNR AND VET_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = VLAPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = VLA_KLI_FIRMENNR AND PER_PERSONENID = VLA_KLI_KLID" "~"
if joinKuerzel = PERVLA
searchResult := searchResult "LEFT JOIN WWS.VLA_VIEW_HAUPTLIEFERARTIKEL ON VLA_KLI_FIRMENNR = PER_FIRMENNR AND VLA_KLI_KLID = PER_PERSONENID" "~"
if joinKuerzel = VTZPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = VTZ_PER_FIRMENNR AND PER_PERSONENID = VTZ_PER_PERSONENID" "~"
if joinKuerzel = PERVTZ
searchResult := searchResult "LEFT JOIN BUERO.VTZ_VERANSTALTUNGTERMINZU ON VTZ_PER_FIRMENNR = PER_FIRMENNR AND VTZ_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = VZPPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = VZP_FIRMENNR AND PER_PERSONENID = VZP_KLID" "~"
if joinKuerzel = PERVZP
searchResult := searchResult "LEFT JOIN WWS.VZP_VA_PWEINZKUNDLIEFZU ON VZP_FIRMENNR = PER_FIRMENNR AND VZP_KLID = PER_PERSONENID" "~"
if joinKuerzel = WEUPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = WEU_PER_FIRMENNR AND PER_PERSONENID = WEU_PERSONENID" "~"
if joinKuerzel = PERWEU
searchResult := searchResult "LEFT JOIN WWS.WEU_WWSERKONTROLLEZU ON WEU_PER_FIRMENNR = PER_FIRMENNR AND WEU_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = WKTPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = WKT_LEISTENDERUNTERN_FIRNR AND PER_PERSONENID = WKT_LEISTENDERUNTERN_PERID" "~"
if joinKuerzel = PERWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_LEISTENDERUNTERN_FIRNR = PER_FIRMENNR AND WKT_LEISTENDERUNTERN_PERID = PER_PERSONENID" "~"
if joinKuerzel = WSSPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = WSS_LIEF_FIRMENNR AND PER_PERSONENID = WSS_LIEF_PERSONENID" "~"
if joinKuerzel = PERWSS
searchResult := searchResult "LEFT JOIN WWS.WSS_WWSSTATISTIK ON WSS_LIEF_FIRMENNR = PER_FIRMENNR AND WSS_LIEF_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = ZPLMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = ZPL_FIRMENNR AND MIT_MITARBEITERID = ZPL_MITARBEITERID" "~"
if joinKuerzel = MITZPL
searchResult := searchResult "LEFT JOIN BUERO.ZPL_ZEITPLANUNG ON ZPL_FIRMENNR = MIT_FIRMENNR AND ZPL_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = ZTSMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = ZTS_MIT_FIRMENNR AND MIT_MITARBEITERID = ZTS_MITARBEITERID" "~"
if joinKuerzel = MITZTS
searchResult := searchResult "LEFT JOIN BUERO.ZTS_ZEITTAGESSUMMEN ON ZTS_MIT_FIRMENNR = MIT_FIRMENNR AND ZTS_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = ZUWMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = ZUW_MIT_FIRMENNR AND MIT_MITARBEITERID = ZUW_MITARBEITERID" "~"
if joinKuerzel = MITZUW
searchResult := searchResult "LEFT JOIN BUERO.ZUW_ZEITUMSATZWERTE ON ZUW_MIT_FIRMENNR = MIT_FIRMENNR AND ZUW_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = LEIMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = LEI_GF_MIT_FIRMENNR AND MIT_MITARBEITERID = LEI_GF_MIT_MITARBEITERID" "~"
if joinKuerzel = MITLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_GF_MIT_FIRMENNR = MIT_FIRMENNR AND LEI_GF_MIT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = MLTMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = MLT_VON_AL_FIRMENNR AND MIT_MITARBEITERID = MLT_VON_AL_MITARBEITERID" "~"
if joinKuerzel = MITMLT
searchResult := searchResult "LEFT JOIN ZLE.MLT_MITARBLEISTTAUSCH ON MLT_VON_AL_FIRMENNR = MIT_FIRMENNR AND MLT_VON_AL_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = BPKMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = BPK_SACHB2_FIRMENNR AND MIT_MITARBEITERID = BPK_SACHB2_MITARBEITERID" "~"
if joinKuerzel = MITBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_SACHB2_FIRMENNR = MIT_FIRMENNR AND BPK_SACHB2_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = EFKMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = EFK_V4_FIRMENNR AND MIT_MITARBEITERID = EFK_V4_MITARBEITERID" "~"
if joinKuerzel = MITEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_V4_FIRMENNR = MIT_FIRMENNR AND EFK_V4_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = MLSMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = MLS_AL_PER_FIRMENNR AND MIT_MITARBEITERID = MLS_AL_PER_MITARBEITERID" "~"
if joinKuerzel = MITMLS
searchResult := searchResult "LEFT JOIN ZLE.MLS_MITARBLEISTTAUSCHSATZ ON MLS_AL_PER_FIRMENNR = MIT_FIRMENNR AND MLS_AL_PER_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = POSMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = POS_GF_FIRMENNR AND MIT_MITARBEITERID = POS_GF_MITARBEITERID" "~"
if joinKuerzel = MITPOS
searchResult := searchResult "LEFT JOIN BUERO.POS_POST ON POS_GF_FIRMENNR = MIT_FIRMENNR AND POS_GF_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = PROMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = PRO_STV_FIRMENNR AND MIT_MITARBEITERID = PRO_STV_MITARBEITERID" "~"
if joinKuerzel = MITPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_STV_FIRMENNR = MIT_FIRMENNR AND PRO_STV_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = BONMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = BON_MIT_FIRMENNR AND MIT_MITARBEITERID = BON_MIT_MITARBEITERID" "~"
if joinKuerzel = MITBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_MIT_FIRMENNR = MIT_FIRMENNR AND BON_MIT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = WFNMVL
searchResult := searchResult "LEFT JOIN BUERO.MVL_MITARBEITERVORLAGE ON MVL_MVLID = WFN_MITARB_MVLID" "~"
if joinKuerzel = MVLWFN
searchResult := searchResult "LEFT JOIN BUERO.WFN_WORKFLOWACTION ON WFN_MITARB_MVLID = MVL_MVLID" "~"
if joinKuerzel = ALINTC
searchResult := searchResult "LEFT JOIN NTC_NTCSARTIKEL.NTC_NTCSARTIKEL ON NTC_NTCSARTIKELNR = ALI_NTCSARTIKELNR" "~"
if joinKuerzel = NTCALI
searchResult := searchResult "LEFT JOIN BMDDEFAULT.ALI_ATTRGRUPPELIZENZZU ON ALI_NTCSARTIKELNR = NTC_NTCSARTIKELNR" "~"
if joinKuerzel = OPEOBG
searchResult := searchResult "LEFT JOIN LOHN.OBG_OF_BEITRAGSGRUPPE ON OBG_OFBEITRAGSGRPNR = OPE_OBG_OFBEITRAGSGRPNR" "~"
if joinKuerzel = OBGOPE
searchResult := searchResult "LEFT JOIN LOHN.OPE_OF_PERSON ON OPE_OBG_OFBEITRAGSGRPNR = OBG_OFBEITRAGSGRPNR" "~"
if joinKuerzel = OBQOBP
searchResult := searchResult "LEFT JOIN BUERO.OBP_ONLBEWPERSSTAMM ON OBP_PERSSTAMMID = OBQ_OBP_PERSSTAMMID" "~"
if joinKuerzel = OBPOBQ
searchResult := searchResult "LEFT JOIN BUERO.OBQ_ONLBEWQUALIFIKATION ON OBQ_OBP_PERSSTAMMID = OBP_PERSSTAMMID" "~"
if joinKuerzel = OPEOGK
searchResult := searchResult "LEFT JOIN LOHN.OGK_OF_GKK ON OGK_KLIENTENNR = OPE_KLIENTENNR AND OGK_GKK_NR = OPE_GKK_NR" "~"
if joinKuerzel = OGKOPE
searchResult := searchResult "LEFT JOIN LOHN.OPE_OF_PERSON ON OPE_KLIENTENNR = OGK_KLIENTENNR AND OPE_GKK_NR = OGK_GKK_NR" "~"
if joinKuerzel = OLAOKL
searchResult := searchResult "LEFT JOIN LOHN.OKL_OF_KLIENT ON OKL_KLIENTENNR = OLA_KLIENTENNR" "~"
if joinKuerzel = OKLOLA
searchResult := searchResult "LEFT JOIN LOHN.OLA_OF_LOHNART ON OLA_KLIENTENNR = OKL_KLIENTENNR" "~"
if joinKuerzel = OPEOKS
searchResult := searchResult "LEFT JOIN LOHN.OKS_OF_KOSTENSTELLE ON OKS_KLIENTENNR = OPE_KLIENTENNR AND OKS_KOSTENSTELLENNR = OPE_STAMMKOSTENSTELLENNR" "~"
if joinKuerzel = OKSOPE
searchResult := searchResult "LEFT JOIN LOHN.OPE_OF_PERSON ON OPE_KLIENTENNR = OKS_KLIENTENNR AND OPE_STAMMKOSTENSTELLENNR = OKS_KOSTENSTELLENNR" "~"
if joinKuerzel = OVGOKV
searchResult := searchResult "LEFT JOIN LOHN.OKV_OF_KOLLEKTIV ON OKV_OFKVID = OVG_OFKVID" "~"
if joinKuerzel = OKVOVG
searchResult := searchResult "LEFT JOIN LOHN.OVG_OF_VERWENDUNGSGRUPPE ON OVG_OFKVID = OKV_OFKVID" "~"
if joinKuerzel = OPKOPE
searchResult := searchResult "LEFT JOIN LOHN.OPE_OF_PERSON ON OPE_KLIENTENNR = OPK_KLIENTENNR AND OPE_PERSNR = OPK_PERSNR AND OPE_PERSUNTERNR = OPK_PERSUNTERNR" "~"
if joinKuerzel = OPEOPK
searchResult := searchResult "LEFT JOIN LOHN.OPK_OF_PROZKO ON OPK_KLIENTENNR = OPE_KLIENTENNR AND OPK_PERSNR = OPE_PERSNR AND OPK_PERSUNTERNR = OPE_PERSUNTERNR" "~"
if joinKuerzel = ARUPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = ARU_PER_FIRMENNR AND PER_PERSONENID = ARU_PER_PERSONENID" "~"
if joinKuerzel = PERARU
searchResult := searchResult "LEFT JOIN ZLE.ARU_ARTIKELUMLAGE ON ARU_PER_FIRMENNR = PER_FIRMENNR AND ARU_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = ASCPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = ASC_PER_FIRMENNR AND PER_PERSONENID = ASC_PERSONENID" "~"
if joinKuerzel = PERASC
searchResult := searchResult "LEFT JOIN ZLE.ASC_ARBEITSSCHEIN ON ASC_PER_FIRMENNR = PER_FIRMENNR AND ASC_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = ASRPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = ASR_PER_FIRMENNR AND PER_PERSONENID = ASR_PER_PERSONENID" "~"
if joinKuerzel = PERASR
searchResult := searchResult "LEFT JOIN BUERO.ASR_AUSSCHREIBUNG ON ASR_PER_FIRMENNR = PER_FIRMENNR AND ASR_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = AWSPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = AWS_STZUGEHOER_FIRMENNR AND PER_PERSONENID = AWS_STZUGEHOER_PERSONENID" "~"
if joinKuerzel = PERAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_STZUGEHOER_FIRMENNR = PER_FIRMENNR AND AWS_STZUGEHOER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = BEZPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = BEZ_PER_FIRMENNR AND PER_PERSONENID = BEZ_PER_PERSONENID" "~"
if joinKuerzel = PERBEZ
searchResult := searchResult "LEFT JOIN FIBU.BEZ_BANKEINZUG ON BEZ_PER_FIRMENNR = PER_FIRMENNR AND BEZ_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = BTWPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = BTW_FIR_FIRMENNR AND PER_PERSONENID = BTW_PERSONENID" "~"
if joinKuerzel = PERBTW
searchResult := searchResult "LEFT JOIN BIL.BTW_BESCHEIDWERT ON BTW_FIR_FIRMENNR = PER_FIRMENNR AND BTW_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = DOKPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = DOK_PER_FIRMENNR AND PER_PERSONENID = DOK_PERSONENID" "~"
if joinKuerzel = PERDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_PER_FIRMENNR = PER_FIRMENNR AND DOK_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = ETLPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = ETL_PER_FIRMENNR AND PER_PERSONENID = ETL_PER_PERSONENID" "~"
if joinKuerzel = PERETL
searchResult := searchResult "LEFT JOIN BUERO.ETL_ENTGANGENE_TELEFONATE ON ETL_PER_FIRMENNR = PER_FIRMENNR AND ETL_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = EVTPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = EVT_PER_FIRMENNR AND PER_PERSONENID = EVT_PERSONENID" "~"
if joinKuerzel = PEREVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_PER_FIRMENNR = PER_FIRMENNR AND EVT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = FPOPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = FPO_PER_FIRMENNR AND PER_PERSONENID = FPO_PERSONENID" "~"
if joinKuerzel = PERFPO
searchResult := searchResult "LEFT JOIN BUERO.FPO_FAVPROJEKTZU ON FPO_PER_FIRMENNR = PER_FIRMENNR AND FPO_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = FSTPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = FST_PER_FIRMENNR AND PER_PERSONENID = FST_PERSONENID" "~"
if joinKuerzel = PERFST
searchResult := searchResult "LEFT JOIN BUERO.FST_FRIST ON FST_PER_FIRMENNR = PER_FIRMENNR AND FST_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = IMDPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = IMD_PER_LIEF_FIRMENNR AND PER_PERSONENID = IMD_PER_LIEF_PERSONENID" "~"
if joinKuerzel = PERIMD
searchResult := searchResult "LEFT JOIN BUERO.IMD_INSERATMEDIUMDETAIL ON IMD_PER_LIEF_FIRMENNR = PER_FIRMENNR AND IMD_PER_LIEF_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = INZPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = INZ_FIRMENNR AND PER_PERSONENID = INZ_PERSONENID" "~"
if joinKuerzel = PERINZ
searchResult := searchResult "LEFT JOIN BUERO.INZ_INSOLVENZPERSONZU ON INZ_FIRMENNR = PER_FIRMENNR AND INZ_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = KAKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = KAK_KUNDEN_FIRMENNR AND PER_PERSONENID = KAK_KUNDEN_KONPERSID" "~"
if joinKuerzel = PERKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_KUNDEN_FIRMENNR = PER_FIRMENNR AND KAK_KUNDEN_KONPERSID = PER_PERSONENID" "~"
if joinKuerzel = KMAPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = KMA_PER_FIRMENNR AND PER_PERSONENID = KMA_PERSONENID" "~"
if joinKuerzel = PERKMA
searchResult := searchResult "LEFT JOIN WWS.KMA_KUNDENMITARBBEZUG ON KMA_PER_FIRMENNR = PER_FIRMENNR AND KMA_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = LFSPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = LFS_SPEDITION_FIRMENNR AND PER_PERSONENID = LFS_SPEDITION_KONTAKTPERSID" "~"
if joinKuerzel = PERLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_SPEDITION_FIRMENNR = PER_FIRMENNR AND LFS_SPEDITION_KONTAKTPERSID = PER_PERSONENID" "~"
if joinKuerzel = LKOPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = LKO_PER_FIRMENNR AND PER_PERSONENID = LKO_PERSONENID" "~"
if joinKuerzel = PERLKO
searchResult := searchResult "LEFT JOIN WWS.LKO_LAGERKONTO ON LKO_PER_FIRMENNR = PER_FIRMENNR AND LKO_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = MEDPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = MED_PER_FIRMENNR AND PER_PERSONENID = MED_PERSONENID" "~"
if joinKuerzel = PERMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_PER_FIRMENNR = PER_FIRMENNR AND MED_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = MEVPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = MEV_FIRMENNR AND PER_PERSONENID = MEV_PERSONENID" "~"
if joinKuerzel = PERMEV
searchResult := searchResult "LEFT JOIN BUERO.MEV_MEDIENVERLEIH ON MEV_FIRMENNR = PER_FIRMENNR AND MEV_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = MZZPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = MZZ_PER_FIRMENNR AND PER_PERSONENID = MZZ_PER_PERSONENID" "~"
if joinKuerzel = PERMZZ
searchResult := searchResult "LEFT JOIN BUERO.MZZ_MITARBZIELZU ON MZZ_PER_FIRMENNR = PER_FIRMENNR AND MZZ_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = NEBPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = NEB_FIRMENNR AND PER_PERSONENID = NEB_PERSONENID" "~"
if joinKuerzel = PERNEB
searchResult := searchResult "LEFT JOIN BIL.NEB_NEBENWERTE ON NEB_FIRMENNR = PER_FIRMENNR AND NEB_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = AZNMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = AZN_MIT_FIRMENNR AND MIT_MITARBEITERID = AZN_MITARBEITERID" "~"
if joinKuerzel = MITAZN
searchResult := searchResult "LEFT JOIN BUERO.AZN_AZNACHRICHTEN ON AZN_MIT_FIRMENNR = MIT_FIRMENNR AND AZN_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = ERMMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = ERM_MIT_FIRMENNR AND MIT_MITARBEITERID = ERM_MIT_MITARBEITERID" "~"
if joinKuerzel = MITERM
searchResult := searchResult "LEFT JOIN FIBU.ERM_ERMITARBEITERZU ON ERM_MIT_FIRMENNR = MIT_FIRMENNR AND ERM_MIT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = GZJMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = GZJ_FIRMENNR AND MIT_MITARBEITERID = GZJ_MITARBEITERID" "~"
if joinKuerzel = MITGZJ
searchResult := searchResult "LEFT JOIN HRS.GZJ_GLAEUBIGERZU_JAHR ON GZJ_FIRMENNR = MIT_FIRMENNR AND GZJ_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = INLMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = INL_MIT_FIRMENNR AND MIT_MITARBEITERID = INL_MITARBEITERID" "~"
if joinKuerzel = MITINL
searchResult := searchResult "LEFT JOIN ZLE.INL_INTERNLEISTUNG ON INL_MIT_FIRMENNR = MIT_FIRMENNR AND INL_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = LAJMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = LAJ_BEARB_FIRMENNR AND MIT_MITARBEITERID = LAJ_BEARB_MITARBEITERID" "~"
if joinKuerzel = MITLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_BEARB_FIRMENNR = MIT_FIRMENNR AND LAJ_BEARB_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = LTKMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = LTK_MIT_FIRMENNR AND MIT_MITARBEITERID = LTK_MITARBEITERID" "~"
if joinKuerzel = MITLTK
searchResult := searchResult "LEFT JOIN ZLE.LTK_LEISTTAUSCHKOPF ON LTK_MIT_FIRMENNR = MIT_FIRMENNR AND LTK_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = MEDMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = MED_FIRMENNR AND MIT_MITARBEITERID = MED_MITARBEITERID" "~"
if joinKuerzel = MITMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_FIRMENNR = MIT_FIRMENNR AND MED_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = PALMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = PAL_MIT_FIRMENNR AND MIT_MITARBEITERID = PAL_MIT_MITARBEITERID" "~"
if joinKuerzel = MITPAL
searchResult := searchResult "LEFT JOIN ZLE.PAL_PAUSCHALE ON PAL_MIT_FIRMENNR = MIT_FIRMENNR AND PAL_MIT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = PERMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = PER_SACHB_FIRMENNR AND MIT_MITARBEITERID = PER_SACHB_MITARBEITERID" "~"
if joinKuerzel = MITPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_SACHB_FIRMENNR = MIT_FIRMENNR AND PER_SACHB_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = PLOMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = PLO_MA_FIRMENNR AND MIT_MITARBEITERID = PLO_MA_MITARBEITERID" "~"
if joinKuerzel = MITPLO
searchResult := searchResult "LEFT JOIN PRODUKTION.PLO_PPS_LOS ON PLO_MA_FIRMENNR = MIT_FIRMENNR AND PLO_MA_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = PROMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = PRO_MIT_FIRMENNR AND MIT_MITARBEITERID = PRO_MITARBEITERID" "~"
if joinKuerzel = MITPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_MIT_FIRMENNR = MIT_FIRMENNR AND PRO_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = PVZMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = PVZ_MIT_FIRMENNR AND MIT_MITARBEITERID = PVZ_MIT_MITARBEITERID" "~"
if joinKuerzel = MITPVZ
searchResult := searchResult "LEFT JOIN WWS.PVZ_PROVISIONTABZU ON PVZ_MIT_FIRMENNR = MIT_FIRMENNR AND PVZ_MIT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = TAGMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = TAG_FIRMENNR AND MIT_MITARBEITERID = TAG_MITARBEITERID" "~"
if joinKuerzel = MITTAG
searchResult := searchResult "LEFT JOIN ZLE.TAG_TAGESABSCHLUSS ON TAG_FIRMENNR = MIT_FIRMENNR AND TAG_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = TAVMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = TAV_MIT_FIRMENNR AND MIT_MITARBEITERID = TAV_MITARBEITERID" "~"
if joinKuerzel = MITTAV
searchResult := searchResult "LEFT JOIN BUERO.TAV_VERTEILUNG ON TAV_MIT_FIRMENNR = MIT_FIRMENNR AND TAV_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = USPMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = USP_MIT_FIRMENNR AND MIT_MITARBEITERID = USP_MIT_MITARBEITERID" "~"
if joinKuerzel = MITUSP
searchResult := searchResult "LEFT JOIN BMDINTERN.USP_UPDATESPERRE ON USP_MIT_FIRMENNR = MIT_FIRMENNR AND USP_MIT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = WIPMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = WIP_MIT_LEITER_FIRMENNR AND MIT_MITARBEITERID = WIP_MIT_LEITER_MITARBEITERID" "~"
if joinKuerzel = MITWIP
searchResult := searchResult "LEFT JOIN BIL.WIP_WIPRUEFUNG ON WIP_MIT_LEITER_FIRMENNR = MIT_FIRMENNR AND WIP_MIT_LEITER_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = ZNGMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = ZNG_FIRMENNR AND MIT_MITARBEITERID = ZNG_MITARBEITERID" "~"
if joinKuerzel = MITZNG
searchResult := searchResult "LEFT JOIN BUERO.ZNG_ZEITNACHRICHTGESENDET ON ZNG_FIRMENNR = MIT_FIRMENNR AND ZNG_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = ZXAMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = ZXA_FIRMENNR AND MIT_MITARBEITERID = ZXA_MITARBEITERID" "~"
if joinKuerzel = MITZXA
searchResult := searchResult "LEFT JOIN BUERO.ZXA_ZEITAENDERUNGSANTRAG ON ZXA_FIRMENNR = MIT_FIRMENNR AND ZXA_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = BDLMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = BDL_LOESCH_FIRMENNR AND MIT_MITARBEITERID = BDL_LOESCH_MITARBEITERID" "~"
if joinKuerzel = MITBDL
searchResult := searchResult "LEFT JOIN BUERO.BDL_BUERODELETE ON BDL_LOESCH_FIRMENNR = MIT_FIRMENNR AND BDL_LOESCH_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = GAZPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = GAZ_ANG_FIRMENNR AND PER_PERSONENID = GAZ_ANG_PERSONENID" "~"
if joinKuerzel = PERGAZ
searchResult := searchResult "LEFT JOIN BUERO.GAZ_GESELLANGEHOERIGERZU ON GAZ_ANG_FIRMENNR = PER_FIRMENNR AND GAZ_ANG_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = MABPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = MAB_KONTAKT_FIRMENNR AND PER_PERSONENID = MAB_KONTAKT_PERSONENID" "~"
if joinKuerzel = PERMAB
searchResult := searchResult "LEFT JOIN BUERO.MAB_MITARBAUSBILDUNG ON MAB_KONTAKT_FIRMENNR = PER_FIRMENNR AND MAB_KONTAKT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PICPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = PIC_HERST_FIRMENNR AND PER_PERSONENID = PIC_HERST_PERSONENID" "~"
if joinKuerzel = PERPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_HERST_FIRMENNR = PER_FIRMENNR AND PIC_HERST_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = RNGPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = RNG_ZRECHEMPF_KON_FIRMENNR AND PER_PERSONENID = RNG_ZRECHEMPF_KON_PERSID" "~"
if joinKuerzel = PERRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_ZRECHEMPF_KON_FIRMENNR = PER_FIRMENNR AND RNG_ZRECHEMPF_KON_PERSID = PER_PERSONENID" "~"
if joinKuerzel = TPRPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = TPR_BEZ_FIRMENNR AND PER_PERSONENID = TPR_BEZ_PERSONENID" "~"
if joinKuerzel = PERTPR
searchResult := searchResult "LEFT JOIN BUERO.TPR_TELEFONPROTOKOLL ON TPR_BEZ_FIRMENNR = PER_FIRMENNR AND TPR_BEZ_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = ZUSPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = ZUS_GES_FIRMENNR AND PER_PERSONENID = ZUS_GES_PERSONENID" "~"
if joinKuerzel = PERZUS
searchResult := searchResult "LEFT JOIN BUERO.ZUS_ZUSTAENDIGKEIT ON ZUS_GES_FIRMENNR = PER_FIRMENNR AND ZUS_GES_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = AGSPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = AGS_AUFTRAGGEBER_FIRMENNR AND PER_PERSONENID = AGS_AUFTRAGGEBER_PERID" "~"
if joinKuerzel = PERAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_AUFTRAGGEBER_FIRMENNR = PER_FIRMENNR AND AGS_AUFTRAGGEBER_PERID = PER_PERSONENID" "~"
if joinKuerzel = AVEPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = AVE_LIEFEMPF_KONT_FIRMENNR AND PER_PERSONENID = AVE_LIEFEMPF_KONTAKTPERSID" "~"
if joinKuerzel = PERAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_LIEFEMPF_KONT_FIRMENNR = PER_FIRMENNR AND AVE_LIEFEMPF_KONTAKTPERSID = PER_PERSONENID" "~"
if joinKuerzel = PICPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = PIC_RECEIVER_FIRMENNR AND PER_PERSONENID = PIC_RECEIVER_PERSONENID" "~"
if joinKuerzel = PERPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_RECEIVER_FIRMENNR = PER_FIRMENNR AND PIC_RECEIVER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = WSKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = WSK_KLI_FIRMENNR AND PER_PERSONENID = WSK_KLID" "~"
if joinKuerzel = PERWSK
searchResult := searchResult "LEFT JOIN WWS.WSK_WWSKORESTATISTIK ON WSK_KLI_FIRMENNR = PER_FIRMENNR AND WSK_KLID = PER_PERSONENID" "~"
if joinKuerzel = VSTPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = VST_AUFTRAGGEBER_FIRMENNR AND PER_PERSONENID = VST_AUFTRAGGEBER_KLID" "~"
if joinKuerzel = PERVST
searchResult := searchResult "LEFT JOIN WWS.VST_VERKAUFSSTATISTIK ON VST_AUFTRAGGEBER_FIRMENNR = PER_FIRMENNR AND VST_AUFTRAGGEBER_KLID = PER_PERSONENID" "~"
if joinKuerzel = KLIPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = KLI_OFFZUGEHOER_FIRMENNR AND PER_PERSONENID = KLI_OFFZUGEHOER_PERSONENID" "~"
if joinKuerzel = PERKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_OFFZUGEHOER_FIRMENNR = PER_FIRMENNR AND KLI_OFFZUGEHOER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = EFKPET
searchResult := searchResult "LEFT JOIN WWS.PET_PREISTAB ON PET_FIRMENNR = EFK_PREISTAB_FIRMENNR AND PET_PREISTABNR = EFK_PREISTABNR" "~"
if joinKuerzel = PETEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_PREISTAB_FIRMENNR = PET_FIRMENNR AND EFK_PREISTABNR = PET_PREISTABNR" "~"
if joinKuerzel = PICPET
searchResult := searchResult "LEFT JOIN WWS.PET_PREISTAB ON PET_FIRMENNR = PIC_PET_FIRMENNR AND PET_PREISTABNR = PIC_PET_PREISTABNR" "~"
if joinKuerzel = PETPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_PET_FIRMENNR = PET_FIRMENNR AND PIC_PET_PREISTABNR = PET_PREISTABNR" "~"
if joinKuerzel = PTPPET
searchResult := searchResult "LEFT JOIN WWS.PET_PREISTAB ON PET_FIRMENNR = PTP_FIRMENNR AND PET_PREISTABNR = PTP_PREISTABNR" "~"
if joinKuerzel = PETPTP
searchResult := searchResult "LEFT JOIN WWS.PTP_PREISTABPOS ON PTP_FIRMENNR = PET_FIRMENNR AND PTP_PREISTABNR = PET_PREISTABNR" "~"
if joinKuerzel = PIDPFR
searchResult := searchResult "LEFT JOIN WWS.PFR_PICFARBE ON PFR_PICID = PID_PICID AND PFR_PICFARBENNR = PID_PICFARBENNR" "~"
if joinKuerzel = PFRPID
searchResult := searchResult "LEFT JOIN WWS.PID_PICPREISDETAIL ON PID_PICID = PFR_PICID AND PID_PICFARBENNR = PFR_PICFARBENNR" "~"
if joinKuerzel = PGAPGI
searchResult := searchResult "LEFT JOIN WWS.PGI_PICARTIKELGRPITEM ON PGI_PICID = PGA_PICID AND PGI_PGPLFDNR = PGA_PGPLFDNR AND PGI_PGILFDNR = PGA_PGILFDNR" "~"
if joinKuerzel = PGIPGA
searchResult := searchResult "LEFT JOIN WWS.PGA_PICARTIKELGRPZU ON PGA_PICID = PGI_PICID AND PGA_PGPLFDNR = PGI_PGPLFDNR AND PGA_PGILFDNR = PGI_PGILFDNR" "~"
if joinKuerzel = PIEPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_PICID = PIE_PICID AND PIA_PICARTIKELNR = PIE_PICARTIKELNR" "~"
if joinKuerzel = PIAPIE
searchResult := searchResult "LEFT JOIN WWS.PIE_PICEINHEIT ON PIE_PICID = PIA_PICID AND PIE_PICARTIKELNR = PIA_PICARTIKELNR" "~"
if joinKuerzel = PUIPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_PICID = PUI_PICID_HAUPT AND PIA_PICARTIKELNR = PUI_PIA_PICARTIKELNR_HAUPT" "~"
if joinKuerzel = PIAPUI
searchResult := searchResult "LEFT JOIN WWS.PUI_PICSTKL ON PUI_PICID_HAUPT = PIA_PICID AND PUI_PIA_PICARTIKELNR_HAUPT = PIA_PICARTIKELNR" "~"
if joinKuerzel = PZFPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_PICID = PZF_PICID AND PIA_PICARTIKELNR = PZF_PICARTIKELNR" "~"
if joinKuerzel = PIAPZF
searchResult := searchResult "LEFT JOIN WWS.PZF_PICFARBENZU ON PZF_PICID = PIA_PICID AND PZF_PICARTIKELNR = PIA_PICARTIKELNR" "~"
if joinKuerzel = ZUSPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = ZUS_FIRMENNR AND PER_PERSONENID = ZUS_PERSONENID" "~"
if joinKuerzel = PERZUS
searchResult := searchResult "LEFT JOIN BUERO.ZUS_ZUSTAENDIGKEIT ON ZUS_FIRMENNR = PER_FIRMENNR AND ZUS_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = AMZPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = AMZ_KLID_FIRMENNR AND PER_PERSONENID = AMZ_KLID" "~"
if joinKuerzel = PERAMZ
searchResult := searchResult "LEFT JOIN BUERO.AMZ_AUSDRUCKMODELLZU ON AMZ_KLID_FIRMENNR = PER_FIRMENNR AND AMZ_KLID = PER_PERSONENID" "~"
if joinKuerzel = AVEPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = AVE_SPEDITION_FIRMENNR AND PER_PERSONENID = AVE_SPEDITION_PERSONENID" "~"
if joinKuerzel = PERAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_SPEDITION_FIRMENNR = PER_FIRMENNR AND AVE_SPEDITION_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = ETLPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = ETL_KON_FIRMENNR AND PER_PERSONENID = ETL_KON_KONTAKTPERSID" "~"
if joinKuerzel = PERETL
searchResult := searchResult "LEFT JOIN BUERO.ETL_ENTGANGENE_TELEFONATE ON ETL_KON_FIRMENNR = PER_FIRMENNR AND ETL_KON_KONTAKTPERSID = PER_PERSONENID" "~"
if joinKuerzel = GZJPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = GZJ_RV_FIRMENNR AND PER_PERSONENID = GZJ_RV_PERSONENID" "~"
if joinKuerzel = PERGZJ
searchResult := searchResult "LEFT JOIN HRS.GZJ_GLAEUBIGERZU_JAHR ON GZJ_RV_FIRMENNR = PER_FIRMENNR AND GZJ_RV_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = LAJPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = LAJ_UMB_PER_FIRMENNR AND PER_PERSONENID = LAJ_UMB_PERSONENID" "~"
if joinKuerzel = PERLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_UMB_PER_FIRMENNR = PER_FIRMENNR AND LAJ_UMB_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = MEDPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = MED_LIEF_FIRMENNR AND PER_PERSONENID = MED_LIEF_PERSONENID" "~"
if joinKuerzel = PERMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_LIEF_FIRMENNR = PER_FIRMENNR AND MED_LIEF_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PCKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = PCK_AUFTRAGGEBER_FIRMENNR AND PER_PERSONENID = PCK_AUFTRAGGEBER_PERID" "~"
if joinKuerzel = PERPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_AUFTRAGGEBER_FIRMENNR = PER_FIRMENNR AND PCK_AUFTRAGGEBER_PERID = PER_PERSONENID" "~"
if joinKuerzel = PEZPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PEZ_PER_FIRMENNR AND PER_PERSONENID = PEZ_PER_PERSONENID" "~"
if joinKuerzel = PERPEZ
searchResult := searchResult "LEFT JOIN BUERO.PEZ_PERSONENZU ON PEZ_PER_FIRMENNR = PER_FIRMENNR AND PEZ_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = RIAPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = RIA_PER_FIRMENNR_BEWERTUNG AND PER_PERSONENID = RIA_PER_ID_BEWERTUNG" "~"
if joinKuerzel = PERRIA
searchResult := searchResult "LEFT JOIN PRODUKTION.RIA_RISIKOANALYSE ON RIA_PER_FIRMENNR_BEWERTUNG = PER_FIRMENNR AND RIA_PER_ID_BEWERTUNG = PER_PERSONENID" "~"
if joinKuerzel = SCEPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = SCE_PER_MANAGER_FIRMENNR AND PER_PERSONENID = SCE_PER_MANAGER_ID" "~"
if joinKuerzel = PERSCE
searchResult := searchResult "LEFT JOIN PRODUKTION.SCE_SCORECARD ON SCE_PER_MANAGER_FIRMENNR = PER_FIRMENNR AND SCE_PER_MANAGER_ID = PER_PERSONENID" "~"
if joinKuerzel = YBUPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = YBU_V1_FIRMENNR AND PER_PERSONENID = YBU_V1_PERSONENID" "~"
if joinKuerzel = PERYBU
searchResult := searchResult "LEFT JOIN FIBU.YBU_AUTOBUCHUNG ON YBU_V1_FIRMENNR = PER_FIRMENNR AND YBU_V1_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = AWSPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = AWS_PER_FIRMENNR AND PER_PERSONENID = AWS_PER_PERSONENID" "~"
if joinKuerzel = PERAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_PER_FIRMENNR = PER_FIRMENNR AND AWS_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = POSPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = POS_PER_GES_FIRMENNR AND PER_PERSONENID = POS_PER_GES_PERSONENID" "~"
if joinKuerzel = PERPOS
searchResult := searchResult "LEFT JOIN BUERO.POS_POST ON POS_PER_GES_FIRMENNR = PER_FIRMENNR AND POS_PER_GES_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = QAWPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = QAW_PER_FIRMENNR AND PER_PERSONENID = QAW_PERSONENID" "~"
if joinKuerzel = PERQAW
searchResult := searchResult "LEFT JOIN BUERO.QAW_QUOTENAUSWERTUNG ON QAW_PER_FIRMENNR = PER_FIRMENNR AND QAW_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = AVEPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = AVE_ZRECHEMPF_KON_FIRMENNR AND PER_PERSONENID = AVE_ZRECHEMPF_KON_PERSID" "~"
if joinKuerzel = PERAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ZRECHEMPF_KON_FIRMENNR = PER_FIRMENNR AND AVE_ZRECHEMPF_KON_PERSID = PER_PERSONENID" "~"
if joinKuerzel = KUKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = KUK_VERTR3_FIRMENNR AND PER_PERSONENID = KUK_VERTR3_PERSONENID" "~"
if joinKuerzel = PERKUK
searchResult := searchResult "LEFT JOIN WWS.KUK_KUNDENKARTEI ON KUK_VERTR3_FIRMENNR = PER_FIRMENNR AND KUK_VERTR3_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = KUKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = KUK_VERTR4_FIRMENNR AND PER_PERSONENID = KUK_VERTR4_PERSONENID" "~"
if joinKuerzel = PERKUK
searchResult := searchResult "LEFT JOIN WWS.KUK_KUNDENKARTEI ON KUK_VERTR4_FIRMENNR = PER_FIRMENNR AND KUK_VERTR4_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PERPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PER_FOLGEVERT1_FIRMENNR AND PER_PERSONENID = PER_FOLGEVERT1_PERSONENID" "~"
if joinKuerzel = PERPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FOLGEVERT1_FIRMENNR = PER_FIRMENNR AND PER_FOLGEVERT1_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = KLIPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = KLI_LIZENZ_FIRMENNR AND PER_PERSONENID = KLI_LIZENZ_PERSONENID" "~"
if joinKuerzel = PERKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_LIZENZ_FIRMENNR = PER_FIRMENNR AND KLI_LIZENZ_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = SWSORT
searchResult := searchResult "LEFT JOIN ORT_ORT.ORT_ORT ON ORT_LANDNR = SWS_LANDNR AND ORT_PLZ = SWS_PLZ AND ORT_PLZUNTERNR = SWS_PLZUNTERNR" "~"
if joinKuerzel = ORTSWS
searchResult := searchResult "LEFT JOIN HRS.SWS_SW_STUNDEN ON SWS_LANDNR = ORT_LANDNR AND SWS_PLZ = ORT_PLZ AND SWS_PLZUNTERNR = ORT_PLZUNTERNR" "~"
if joinKuerzel = OPEOTI
searchResult := searchResult "LEFT JOIN LOHN.OTI_OF_TITEL ON OTI_OFTITELNR = OPE_OFTITELNR" "~"
if joinKuerzel = OTIOPE
searchResult := searchResult "LEFT JOIN LOHN.OPE_OF_PERSON ON OPE_OFTITELNR = OTI_OFTITELNR" "~"
if joinKuerzel = OPEOVG
searchResult := searchResult "LEFT JOIN LOHN.OVG_OF_VERWENDUNGSGRUPPE ON OVG_OFKVID = OPE_OVG_OFKVID AND OVG_VGNR = OPE_VGNR" "~"
if joinKuerzel = OVGOPE
searchResult := searchResult "LEFT JOIN LOHN.OPE_OF_PERSON ON OPE_OVG_OFKVID = OVG_OFKVID AND OPE_VGNR = OVG_VGNR" "~"
if joinKuerzel = PAZPAG
searchResult := searchResult "LEFT JOIN BMDDEFAULT.PAG_PARAMGRUPPE ON PAG_PARAMGRUPPE = PAZ_PARAMGRUPPE" "~"
if joinKuerzel = PAGPAZ
searchResult := searchResult "LEFT JOIN BMDDEFAULT.PAZ_PARAMGRUPPEPAKETZU ON PAZ_PARAMGRUPPE = PAG_PARAMGRUPPE" "~"
if joinKuerzel = PAWPAR
searchResult := searchResult "LEFT JOIN PAR_PARAM.PAR_PARAM ON PAR_PARAMGRUPPE = PAW_PARAMGRUPPE AND PAR_PARAMUNTERGRUPPE = PAW_PARAMUNTERGRUPPE AND PAR_PARAMNR = PAW_PARAMNR" "~"
if joinKuerzel = PARPAW
searchResult := searchResult "LEFT JOIN BMD.PAW_PARAMWERT ON PAW_PARAMGRUPPE = PAR_PARAMGRUPPE AND PAW_PARAMUNTERGRUPPE = PAR_PARAMUNTERGRUPPE AND PAW_PARAMNR = PAR_PARAMNR" "~"
if joinKuerzel = WAPPAT
searchResult := searchResult "LEFT JOIN WWS.PAT_PACKSTOFFTAB ON PAT_FIRMENNR = WAP_PAT_FIRMENNR AND PAT_PACKSTOFFTABNR = WAP_PAT_PACKSTOFFTABNR" "~"
if joinKuerzel = PATWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_PAT_FIRMENNR = PAT_FIRMENNR AND WAP_PAT_PACKSTOFFTABNR = PAT_PACKSTOFFTABNR" "~"
if joinKuerzel = PBWPBA
searchResult := searchResult "LEFT JOIN PRODUKTION.PBA_PLAN_BAS_ALT ON PBA_PBAID = PBW_PBAID" "~"
if joinKuerzel = PBAPBW
searchResult := searchResult "LEFT JOIN PRODUKTION.PBW_PLAN_WERT ON PBW_PBAID = PBA_PBAID" "~"
if joinKuerzel = AFTPCK
searchResult := searchResult "LEFT JOIN PCK_PPS_KAPA.PCK_PPS_KAPA ON PCK_PPSKAPAID = AFT_PPSKAPAID" "~"
if joinKuerzel = PCKAFT
searchResult := searchResult "LEFT JOIN ZLE.AFT_AUFBEREITUNG ON AFT_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = LEIPCK
searchResult := searchResult "LEFT JOIN PCK_PPS_KAPA.PCK_PPS_KAPA ON PCK_PPSKAPAID = LEI_PPSKAPAID" "~"
if joinKuerzel = PCKLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = RIAPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_PPSKAPAID = RIA_PPSKAPAID_MASSNAHME" "~"
if joinKuerzel = PCKRIA
searchResult := searchResult "LEFT JOIN PRODUKTION.RIA_RISIKOANALYSE ON RIA_PPSKAPAID_MASSNAHME = PCK_PPSKAPAID" "~"
if joinKuerzel = VWZPCK
searchResult := searchResult "LEFT JOIN PCK_PPS_KAPA.PCK_PPS_KAPA ON PCK_PPSKAPAID = VWZ_PPSKAPAID" "~"
if joinKuerzel = PCKVWZ
searchResult := searchResult "LEFT JOIN BUERO.VWZ_VORGANGWORKFLOWZU ON VWZ_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = WOLPCK
searchResult := searchResult "LEFT JOIN PCK_PPS_KAPA.PCK_PPS_KAPA ON PCK_PPSKAPAID = WOL_PPSKAPAID" "~"
if joinKuerzel = PCKWOL
searchResult := searchResult "LEFT JOIN ZLE.WOL_WOCHENLEISTUNG ON WOL_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = RPMPCP
searchResult := searchResult "LEFT JOIN PRODUKTION.PCP_PPS_PRODUKT ON PCP_PCHARGENID = RPM_PCHARGENID AND PCP_PPSPROD_LFDNR = RPM_PPSPROD_LFDNR" "~"
if joinKuerzel = PCPRPM
searchResult := searchResult "LEFT JOIN PRODUKTION.RPM_RETRO_PRODUKT_MATERIAL ON RPM_PCHARGENID = PCP_PCHARGENID AND RPM_PPSPROD_LFDNR = PCP_PPSPROD_LFDNR" "~"
if joinKuerzel = AFBPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = AFB_LIEFERANT_FIRMENNR AND PER_PERSONENID = AFB_LIEFERANT_PERSONENID" "~"
if joinKuerzel = PERAFB
searchResult := searchResult "LEFT JOIN FIBU.AFB_ANBUFIB ON AFB_LIEFERANT_FIRMENNR = PER_FIRMENNR AND AFB_LIEFERANT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = AGNPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = AGN_PER_FIRMENNR AND PER_PERSONENID = AGN_PER_PERSONENID" "~"
if joinKuerzel = PERAGN
searchResult := searchResult "LEFT JOIN WWS.AGN_AG_PERSON_ZU ON AGN_PER_FIRMENNR = PER_FIRMENNR AND AGN_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = APAPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = APA_VERANTW_FIRMENNR AND PER_PERSONENID = APA_VERANTW_PERSONENID" "~"
if joinKuerzel = PERAPA
searchResult := searchResult "LEFT JOIN PRODUKTION.APA_ARBEITSPLAN ON APA_VERANTW_FIRMENNR = PER_FIRMENNR AND APA_VERANTW_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = BDLPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = BDL_PER_FIRMENNR AND PER_PERSONENID = BDL_PER_PERSONENID" "~"
if joinKuerzel = PERBDL
searchResult := searchResult "LEFT JOIN BUERO.BDL_BUERODELETE ON BDL_PER_FIRMENNR = PER_FIRMENNR AND BDL_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = BOPPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = BOP_LEISTENDERUNTERN_FIRNR AND PER_PERSONENID = BOP_LEISTENDERUNTERN_PERID" "~"
if joinKuerzel = PERBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_LEISTENDERUNTERN_FIRNR = PER_FIRMENNR AND BOP_LEISTENDERUNTERN_PERID = PER_PERSONENID" "~"
if joinKuerzel = CWFPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = CWF_PER_FIRMENNR AND PER_PERSONENID = CWF_PERSONENID" "~"
if joinKuerzel = PERCWF
searchResult := searchResult "LEFT JOIN BUERO.CWF_CRMWORKFLOW ON CWF_PER_FIRMENNR = PER_FIRMENNR AND CWF_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = DBXPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = DBX_PER_FIRMENNR AND PER_PERSONENID = DBX_PERSONENID" "~"
if joinKuerzel = PERDBX
searchResult := searchResult "LEFT JOIN BUERO.DBX_FINONDATABOX ON DBX_PER_FIRMENNR = PER_FIRMENNR AND DBX_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = EXPPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = EXP_FIRMENNR AND PER_PERSONENID = EXP_PERSONENID" "~"
if joinKuerzel = PEREXP
searchResult := searchResult "LEFT JOIN BUERO.EXP_EXTERNPERSONKEY ON EXP_FIRMENNR = PER_FIRMENNR AND EXP_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = FTBPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = FTB_FAHRER_FIRMENNR AND PER_PERSONENID = FTB_FAHRER_PERSONENID" "~"
if joinKuerzel = PERFTB
searchResult := searchResult "LEFT JOIN BUERO.FTB_FAHRTENBUCH ON FTB_FAHRER_FIRMENNR = PER_FIRMENNR AND FTB_FAHRER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PGKPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PGK_FIRMENNR AND PER_PERSONENID = PGK_PERSONENID" "~"
if joinKuerzel = PERPGK
searchResult := searchResult "LEFT JOIN BUERO.PGK_PERSONGKKZU ON PGK_FIRMENNR = PER_FIRMENNR AND PGK_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PGRPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PGR_FIRMENNR AND PER_PERSONENID = PGR_PERSONENID" "~"
if joinKuerzel = PERPGR
searchResult := searchResult "LEFT JOIN BUERO.PGR_PERSONGERICHTZU ON PGR_FIRMENNR = PER_FIRMENNR AND PGR_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = POZPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = POZ_PER_FIRMENNR AND PER_PERSONENID = POZ_PER_PERSONENID" "~"
if joinKuerzel = PERPOZ
searchResult := searchResult "LEFT JOIN BUERO.POZ_PISWORKFLOWZU ON POZ_PER_FIRMENNR = PER_FIRMENNR AND POZ_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PSWPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PSW_FIRMENNR AND PER_PERSONENID = PSW_PERSONENID" "~"
if joinKuerzel = PERPSW
searchResult := searchResult "LEFT JOIN BUERO.PSW_PERSONENWERTE ON PSW_FIRMENNR = PER_FIRMENNR AND PSW_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PERDVH
searchResult := searchResult "LEFT JOIN BUERO.DVH_DIENSTVERHAELTNIS ON DVH_MIT_FIRMENNR = PER_FIRMENNR AND DVH_MITARBEITERID = PER_PERSONENID" "~"
if joinKuerzel = DVHPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = DVH_MIT_FIRMENNR AND PER_PERSONENID = DVH_MITARBEITERID" "~"
if joinKuerzel = PVAPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PVA_PER_FIRMENNR AND PER_PERSONENID = PVA_PERSONENID" "~"
if joinKuerzel = PERPVA
searchResult := searchResult "LEFT JOIN BUERO.PVA_PERSONVORERFASSAKTION ON PVA_PER_FIRMENNR = PER_FIRMENNR AND PVA_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PVFPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PVF_PER_FIRMENNR AND PER_PERSONENID = PVF_PERSONENID" "~"
if joinKuerzel = PERPVF
searchResult := searchResult "LEFT JOIN BUERO.PVF_PERSONENVORERFASSUNG ON PVF_PER_FIRMENNR = PER_FIRMENNR AND PVF_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = RIAPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = RIA_PER_FIRMENNR_SCHADEN AND PER_PERSONENID = RIA_PER_ID_SCHADEN" "~"
if joinKuerzel = PERRIA
searchResult := searchResult "LEFT JOIN PRODUKTION.RIA_RISIKOANALYSE ON RIA_PER_FIRMENNR_SCHADEN = PER_FIRMENNR AND RIA_PER_ID_SCHADEN = PER_PERSONENID" "~"
if joinKuerzel = SCGPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = SCG_PER_FIRMENNR AND PER_PERSONENID = SCG_PER_PERSONENID" "~"
if joinKuerzel = PERSCG
searchResult := searchResult "LEFT JOIN PRODUKTION.SCG_SCORECARDKRITZU ON SCG_PER_FIRMENNR = PER_FIRMENNR AND SCG_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = STWPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = STW_FIR_FIRMENNR AND PER_PERSONENID = STW_PERSONENID" "~"
if joinKuerzel = PERSTW
searchResult := searchResult "LEFT JOIN BIL.STW_STEUERWERT ON STW_FIR_FIRMENNR = PER_FIRMENNR AND STW_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = STZPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = STZ_LEISTENDERUNTERN_FIRNR AND PER_PERSONENID = STZ_LEISTENDERUNTERN_PERID" "~"
if joinKuerzel = PERSTZ
searchResult := searchResult "LEFT JOIN WWS.STZ_STEUERTEXTZUORDNUNG ON STZ_LEISTENDERUNTERN_FIRNR = PER_FIRMENNR AND STZ_LEISTENDERUNTERN_PERID = PER_PERSONENID" "~"
if joinKuerzel = TSWPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = TSW_PER_FIRMENNR AND PER_PERSONENID = TSW_PERSONENID" "~"
if joinKuerzel = PERTSW
searchResult := searchResult "LEFT JOIN WWS.TSW_STATISTIKWERTE_TMP ON TSW_PER_FIRMENNR = PER_FIRMENNR AND TSW_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = VEBPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = VEB_PER_FIRMENNR AND PER_PERSONENID = VEB_PER_PERSONENID" "~"
if joinKuerzel = PERVEB
searchResult := searchResult "LEFT JOIN FIBU.VEB_VORERFASSBUCHUNG ON VEB_PER_FIRMENNR = PER_FIRMENNR AND VEB_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = WAFPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = WAF_PERFIRMENNR AND PER_PERSONENID = WAF_PERSONENID" "~"
if joinKuerzel = PERWAF
searchResult := searchResult "LEFT JOIN WWS.WAF_WWSADRZUFIRMENBEZ ON WAF_PERFIRMENNR = PER_FIRMENNR AND WAF_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = WPTPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = WPT_FIRMENNR AND PER_PERSONENID = WPT_PERSONENID" "~"
if joinKuerzel = PERWPT
searchResult := searchResult "LEFT JOIN WWS.WPT_WWSPERSON_TMP ON WPT_FIRMENNR = PER_FIRMENNR AND WPT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = ZVKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = ZVK_PER_FIRMENNR AND PER_PERSONENID = ZVK_PERSONENID" "~"
if joinKuerzel = PERZVK
searchResult := searchResult "LEFT JOIN FIBU.ZVK_ZAHLUNGSVORSCHLAG ON ZVK_PER_FIRMENNR = PER_FIRMENNR AND ZVK_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = AAEPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = AAE_BEARB_FIRMENNR AND PER_PERSONENID = AAE_BEARB_MITARBEITERID" "~"
if joinKuerzel = PERAAE
searchResult := searchResult "LEFT JOIN WWS.AAE_ANGEBOTSAUSW_ERFQ_TMP ON AAE_BEARB_FIRMENNR = PER_FIRMENNR AND AAE_BEARB_MITARBEITERID = PER_PERSONENID" "~"
if joinKuerzel = APAPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = APA_VERANTW2_FIRMENNR AND PER_PERSONENID = APA_VERANTW2_PERID" "~"
if joinKuerzel = PERAPA
searchResult := searchResult "LEFT JOIN PRODUKTION.APA_ARBEITSPLAN ON APA_VERANTW2_FIRMENNR = PER_FIRMENNR AND APA_VERANTW2_PERID = PER_PERSONENID" "~"
if joinKuerzel = AUGPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = AUG_KONTAKT_FIRMENNR AND PER_PERSONENID = AUG_KONTAKT_PERSONENID" "~"
if joinKuerzel = PERAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_KONTAKT_FIRMENNR = PER_FIRMENNR AND AUG_KONTAKT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = HNPPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = HNP_EMPF_FIRMENNR AND PER_PERSONENID = HNP_EMPF_PERSONENID" "~"
if joinKuerzel = PERHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_EMPF_FIRMENNR = PER_FIRMENNR AND HNP_EMPF_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = KLIPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = KLI_V1_FIRMENNR AND PER_PERSONENID = KLI_V1_PERSONENID" "~"
if joinKuerzel = PERKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_V1_FIRMENNR = PER_FIRMENNR AND KLI_V1_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = BPKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = BPK_LTBESTAET_FIRMENNR AND PER_PERSONENID = BPK_LTBESTAET_PERSONENID" "~"
if joinKuerzel = PERBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_LTBESTAET_FIRMENNR = PER_FIRMENNR AND BPK_LTBESTAET_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = ERBPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = ERB_DIVPER_FIRMENNR AND PER_PERSONENID = ERB_DIVPER_PERSONENID" "~"
if joinKuerzel = PERERB
searchResult := searchResult "LEFT JOIN FIBU.ERB_BUCHUNG ON ERB_DIVPER_FIRMENNR = PER_FIRMENNR AND ERB_DIVPER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = FSTPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = FST_PER_GES_FIRMENNR AND PER_PERSONENID = FST_PER_GES_PERSONENID" "~"
if joinKuerzel = PERFST
searchResult := searchResult "LEFT JOIN BUERO.FST_FRIST ON FST_PER_GES_FIRMENNR = PER_FIRMENNR AND FST_PER_GES_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = FTBPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = FTB_ABFAHRT_PER_FIRMENNR AND PER_PERSONENID = FTB_ABFAHRT_PER_PERSONENID" "~"
if joinKuerzel = PERFTB
searchResult := searchResult "LEFT JOIN BUERO.FTB_FAHRTENBUCH ON FTB_ABFAHRT_PER_FIRMENNR = PER_FIRMENNR AND FTB_ABFAHRT_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = KSTPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = KST_PER_FIRMENNR2 AND PER_PERSONENID = KST_PERSONENID2" "~"
if joinKuerzel = PERKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_PER_FIRMENNR2 = PER_FIRMENNR AND KST_PERSONENID2 = PER_PERSONENID" "~"
if joinKuerzel = KUKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = KUK_VERTR1_FIRMENNR AND PER_PERSONENID = KUK_VERTR1_PERSONENID" "~"
if joinKuerzel = PERKUK
searchResult := searchResult "LEFT JOIN WWS.KUK_KUNDENKARTEI ON KUK_VERTR1_FIRMENNR = PER_FIRMENNR AND KUK_VERTR1_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = LEDPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = LED_KON_FIRMENNR AND PER_PERSONENID = LED_KON_PERSONENID" "~"
if joinKuerzel = PERLED
searchResult := searchResult "LEFT JOIN BUERO.LED_LEAD ON LED_KON_FIRMENNR = PER_FIRMENNR AND LED_KON_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = TTBPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = TTB_PER_FIRMENNR AND PER_PERSONENID = TTB_PER_PERSONENID" "~"
if joinKuerzel = PERTTB
searchResult := searchResult "LEFT JOIN BIL.TTB_TANTIEMENBERECHNUNG ON TTB_PER_FIRMENNR = PER_FIRMENNR AND TTB_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = WADPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = WAD_AUSLIEFADR_FIRMENNR AND PER_PERSONENID = WAD_AUSLIEFADR_PERSONENID" "~"
if joinKuerzel = PERWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_AUSLIEFADR_FIRMENNR = PER_FIRMENNR AND WAD_AUSLIEFADR_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = WSKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = WSK_LIEF_FIRMENNR AND PER_PERSONENID = WSK_LIEF_PERSONENID" "~"
if joinKuerzel = PERWSK
searchResult := searchResult "LEFT JOIN WWS.WSK_WWSKORESTATISTIK ON WSK_LIEF_FIRMENNR = PER_FIRMENNR AND WSK_LIEF_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = KLIPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = KLI_V2_FIRMENNR AND PER_PERSONENID = KLI_V2_PERSONENID" "~"
if joinKuerzel = PERKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_V2_FIRMENNR = PER_FIRMENNR AND KLI_V2_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = LEIPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = LEI_STZUGEHOER_FIRMENNR AND PER_PERSONENID = LEI_STZUGEHOER_PERSONENID" "~"
if joinKuerzel = PERLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_STZUGEHOER_FIRMENNR = PER_FIRMENNR AND LEI_STZUGEHOER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = VSTPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = VST_LIEF_FIRMENNR AND PER_PERSONENID = VST_LIEF_PERSONENID" "~"
if joinKuerzel = PERVST
searchResult := searchResult "LEFT JOIN WWS.VST_VERKAUFSSTATISTIK ON VST_LIEF_FIRMENNR = PER_FIRMENNR AND VST_LIEF_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = BPKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = BPK_LIEFEMPF_KONT_FIRMENNR AND PER_PERSONENID = BPK_LIEFEMPF_KONTAKTPERSID" "~"
if joinKuerzel = PERBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_LIEFEMPF_KONT_FIRMENNR = PER_FIRMENNR AND BPK_LIEFEMPF_KONTAKTPERSID = PER_PERSONENID" "~"
if joinKuerzel = AVEPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = AVE_RECHEMPF_KONT_FIRMENNR AND PER_PERSONENID = AVE_RECHEMPF_KONTAKTPERSID" "~"
if joinKuerzel = PERAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_RECHEMPF_KONT_FIRMENNR = PER_FIRMENNR AND AVE_RECHEMPF_KONTAKTPERSID = PER_PERSONENID" "~"
if joinKuerzel = EVTPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = EVT_VERTRETER_FIRMENNR AND PER_PERSONENID = EVT_VERTRETER_PERSONENID" "~"
if joinKuerzel = PEREVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_VERTRETER_FIRMENNR = PER_FIRMENNR AND EVT_VERTRETER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = WBTPET
searchResult := searchResult "LEFT JOIN WWS.PET_PREISTAB ON PET_FIRMENNR = WBT_PET_FIRMENNR AND PET_PREISTABNR = WBT_PET_PREISTABNR" "~"
if joinKuerzel = PETWBT
searchResult := searchResult "LEFT JOIN WWS.WBT_WWSBUDGETKOPF ON WBT_PET_FIRMENNR = PET_FIRMENNR AND WBT_PET_PREISTABNR = PET_PREISTABNR" "~"
if joinKuerzel = PIRPFR
searchResult := searchResult "LEFT JOIN WWS.PFR_PICFARBE ON PFR_PICID = PIR_PICID AND PFR_PICFARBENNR = PIR_PICFARBENNR" "~"
if joinKuerzel = PFRPIR
searchResult := searchResult "LEFT JOIN WWS.PIR_PICRABATTDETAIL ON PIR_PICID = PFR_PICID AND PIR_PICFARBENNR = PFR_PICFARBENNR" "~"
if joinKuerzel = PIDPGO
searchResult := searchResult "LEFT JOIN WWS.PGO_PICGROESSE ON PGO_PICID = PID_PICID AND PGO_PICGROESSENKBEZ = PID_PICGROESSENKBEZ" "~"
if joinKuerzel = PGOPID
searchResult := searchResult "LEFT JOIN WWS.PID_PICPREISDETAIL ON PID_PICID = PGO_PICID AND PID_PICGROESSENKBEZ = PGO_PICGROESSENKBEZ" "~"
if joinKuerzel = PAVPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_PICID = PAV_PIA_PICID_HAUPT AND PIA_PICARTIKELNR = PAV_PIA_PICARTIKELNR_HAUPT" "~"
if joinKuerzel = PIAPAV
searchResult := searchResult "LEFT JOIN WWS.PAV_PICALTERNATIVARTIKEL ON PAV_PIA_PICID_HAUPT = PIA_PICID AND PAV_PIA_PICARTIKELNR_HAUPT = PIA_PICARTIKELNR" "~"
if joinKuerzel = AVIORT
searchResult := searchResult "LEFT JOIN ORT_ORT.ORT_ORT ON ORT_LANDNR = AVI_BORT_LANDNR AND ORT_PLZ = AVI_BORT_PLZ AND ORT_PLZUNTERNR = AVI_BORT_PLZUNTERNR" "~"
if joinKuerzel = ORTAVI
searchResult := searchResult "LEFT JOIN HRS.AVI_AVISOMELDUNG ON AVI_BORT_LANDNR = ORT_LANDNR AND AVI_BORT_PLZ = ORT_PLZ AND AVI_BORT_PLZUNTERNR = ORT_PLZUNTERNR" "~"
if joinKuerzel = TOZORT
searchResult := searchResult "LEFT JOIN ORT_ORT.ORT_ORT ON ORT_LANDNR = TOZ_ORT_LANDNR AND ORT_PLZ = TOZ_ORT_PLZ AND ORT_PLZUNTERNR = TOZ_ORT_PLZUNTERNR" "~"
if joinKuerzel = ORTTOZ
searchResult := searchResult "LEFT JOIN WWS.TOZ_TOURZUORDNUNG ON TOZ_ORT_LANDNR = ORT_LANDNR AND TOZ_ORT_PLZ = ORT_PLZ AND TOZ_ORT_PLZUNTERNR = ORT_PLZUNTERNR" "~"
if joinKuerzel = ORSOTE
searchResult := searchResult "LEFT JOIN LOHN.OTE_OF_TEXTE ON OTE_TEXTID = ORS_OTE_TEXTID" "~"
if joinKuerzel = OTEORS
searchResult := searchResult "LEFT JOIN LOHN.ORS_OF_REISESTAMM ON ORS_OTE_TEXTID = OTE_TEXTID" "~"
if joinKuerzel = OUPOUF
searchResult := searchResult "LEFT JOIN BUERO.OUF_ONLINEUMFRAGE ON OUF_OUFID = OUP_OUFID" "~"
if joinKuerzel = OUFOUP
searchResult := searchResult "LEFT JOIN BUERO.OUP_ONLUMFRAGEPAGE ON OUP_OUFID = OUF_OUFID" "~"
if joinKuerzel = OUPOUP
searchResult := searchResult "LEFT JOIN BUERO.OUP_ONLUMFRAGEPAGE ON OUP_OUPID = OUP_PARENTPAGEID" "~"
if joinKuerzel = OUPOUP
searchResult := searchResult "LEFT JOIN BUERO.OUP_ONLUMFRAGEPAGE ON OUP_PARENTPAGEID = OUP_OUPID" "~"
if joinKuerzel = PASPAC
searchResult := searchResult "LEFT JOIN WWS.PAC_PACKSTOFF ON PAC_FIRMENNR = PAS_FIRMENNR AND PAC_PACKSTOFFNR = PAS_PACKSTOFFNR" "~"
if joinKuerzel = PACPAS
searchResult := searchResult "LEFT JOIN WWS.PAS_PACKSTOFFPREIS ON PAS_FIRMENNR = PAC_FIRMENNR AND PAS_PACKSTOFFNR = PAC_PACKSTOFFNR" "~"
if joinKuerzel = LATPAT
searchResult := searchResult "LEFT JOIN WWS.PAT_PACKSTOFFTAB ON PAT_FIRMENNR = LAT_PAT_FIRMENNR AND PAT_PACKSTOFFTABNR = LAT_PAT_PACKSTOFFTABNR" "~"
if joinKuerzel = PATLAT
searchResult := searchResult "LEFT JOIN WWS.LAT_LIEFERANTENARTIKEL ON LAT_PAT_FIRMENNR = PAT_FIRMENNR AND LAT_PAT_PACKSTOFFTABNR = PAT_PACKSTOFFTABNR" "~"
if joinKuerzel = AWEPBA
searchResult := searchResult "LEFT JOIN PBA_PLAN_BAS_ALT.PBA_PLAN_BAS_ALT ON PBA_PBAID = AWE_PEP_BL_PBA_PBAID2" "~"
if joinKuerzel = PBAAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_PEP_BL_PBA_PBAID2 = PBA_PBAID" "~"
if joinKuerzel = AUGPCK
searchResult := searchResult "LEFT JOIN PCK_PPS_KAPA.PCK_PPS_KAPA ON PCK_PPSKAPAID = AUG_PPSKAPAID" "~"
if joinKuerzel = PCKAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = BZBPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_PPSKAPAID = BZB_PPSKAPAID" "~"
if joinKuerzel = PCKBZB
searchResult := searchResult "LEFT JOIN PRODUKTION.BZB_BAZZEITBUCHUNG ON BZB_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = TIZPCK
searchResult := searchResult "LEFT JOIN PCK_PPS_KAPA.PCK_PPS_KAPA ON PCK_PPSKAPAID = TIZ_PCK_PPSKAPAID" "~"
if joinKuerzel = PCKTIZ
searchResult := searchResult "LEFT JOIN BMDENTW.TIZ_TICKETPEPAUFGABEZU ON TIZ_PCK_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = PIDPDG
searchResult := searchResult "LEFT JOIN WWS.PDG_PICDYNAMICGROUP ON PDG_PDGID = PID_PDGID" "~"
if joinKuerzel = PDGPID
searchResult := searchResult "LEFT JOIN WWS.PID_PICPREISDETAIL ON PID_PDGID = PDG_PDGID" "~"
if joinKuerzel = PDZPDS
searchResult := searchResult "LEFT JOIN BIL.PDS_PERSKORST_DNSTAMM ON PDS_PDSID = PDZ_PDSID" "~"
if joinKuerzel = PDSPDZ
searchResult := searchResult "LEFT JOIN BIL.PDZ_PERSRST_ZUSATZDATEN ON PDZ_PDSID = PDS_PDSID" "~"
if joinKuerzel = PGSPDS
searchResult := searchResult "LEFT JOIN BIL.PDS_PERSKORST_DNSTAMM ON PDS_PDSID = PGS_PDSID" "~"
if joinKuerzel = PDSPGS
searchResult := searchResult "LEFT JOIN BIL.PGS_PERSKORST_GUTSTUNDEN ON PGS_PDSID = PDS_PDSID" "~"
if joinKuerzel = AJOPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = AJO_LIEFERANT_FIRMENNR AND PER_PERSONENID = AJO_LIEFERANT_PERSONENID" "~"
if joinKuerzel = PERAJO
searchResult := searchResult "LEFT JOIN BIL.AJO_ANBUJOURNAL ON AJO_LIEFERANT_FIRMENNR = PER_FIRMENNR AND AJO_LIEFERANT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = BCUPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = BCU_KON_FIRMENNR AND PER_PERSONENID = BCU_KON_KONTAKTPERSID" "~"
if joinKuerzel = PERBCU
searchResult := searchResult "LEFT JOIN BUERO.BCU_BMDCOMUSER ON BCU_KON_FIRMENNR = PER_FIRMENNR AND BCU_KON_KONTAKTPERSID = PER_PERSONENID" "~"
if joinKuerzel = BFZPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = BFZ_PER_FIRMENNR AND PER_PERSONENID = BFZ_PER_PERSONENID" "~"
if joinKuerzel = PERBFZ
searchResult := searchResult "LEFT JOIN BIL.BFZ_XMLJABBETFIRZU ON BFZ_PER_FIRMENNR = PER_FIRMENNR AND BFZ_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = BKRPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = BKR_SACHBEARB_FIRMENNR AND PER_PERSONENID = BKR_SACHBEARB_PERSID" "~"
if joinKuerzel = PERBKR
searchResult := searchResult "LEFT JOIN FIBU.BKR_BANKEINZUGSKREIS ON BKR_SACHBEARB_FIRMENNR = PER_FIRMENNR AND BKR_SACHBEARB_PERSID = PER_PERSONENID" "~"
if joinKuerzel = DASPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = DAS_PER_FIRMENNR AND PER_PERSONENID = DAS_PER_PERSONENID" "~"
if joinKuerzel = PERDAS
searchResult := searchResult "LEFT JOIN BUERO.DAS_DOCSARCHIVSTAMM ON DAS_PER_FIRMENNR = PER_FIRMENNR AND DAS_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = DPZPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = DPZ_FIRMENNR AND PER_PERSONENID = DPZ_PERSONENID" "~"
if joinKuerzel = PERDPZ
searchResult := searchResult "LEFT JOIN BUERO.DPZ_DOCSPERSZU ON DPZ_FIRMENNR = PER_FIRMENNR AND DPZ_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = ERBPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = ERB_PER_FIRMENNR AND PER_PERSONENID = ERB_PER_PERSONENID" "~"
if joinKuerzel = PERERB
searchResult := searchResult "LEFT JOIN FIBU.ERB_BUCHUNG ON ERB_PER_FIRMENNR = PER_FIRMENNR AND ERB_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = EVGPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = EVG_PER_FIRMENNR AND PER_PERSONENID = EVG_PERSONENID" "~"
if joinKuerzel = PEREVG
searchResult := searchResult "LEFT JOIN BUERO.EVG_EVENTTAGE ON EVG_PER_FIRMENNR = PER_FIRMENNR AND EVG_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = VASMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = VAS_AL_FIRMENNR AND MIT_MITARBEITERID = VAS_AL_MITARBEITERID" "~"
if joinKuerzel = MITVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_AL_FIRMENNR = MIT_FIRMENNR AND VAS_AL_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = CAWMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = CAW_ERKO_VERT_FIRMENNR AND MIT_MITARBEITERID = CAW_ERKO_VERT_MITARBID" "~"
if joinKuerzel = MITCAW
searchResult := searchResult "LEFT JOIN BUERO.CAW_CRMABWESENHEIT ON CAW_ERKO_VERT_FIRMENNR = MIT_FIRMENNR AND CAW_ERKO_VERT_MITARBID = MIT_MITARBEITERID" "~"
if joinKuerzel = EFKMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = EFK_V3_FIRMENNR AND MIT_MITARBEITERID = EFK_V3_MITARBEITERID" "~"
if joinKuerzel = MITEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_V3_FIRMENNR = MIT_FIRMENNR AND EFK_V3_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = KMAMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = KMA_SACHBSTV_FIRMENNR AND MIT_MITARBEITERID = KMA_SACHBSTV_MITARBEITERID" "~"
if joinKuerzel = MITKMA
searchResult := searchResult "LEFT JOIN WWS.KMA_KUNDENMITARBBEZUG ON KMA_SACHBSTV_FIRMENNR = MIT_FIRMENNR AND KMA_SACHBSTV_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = RNPMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = RNP_V4_FIRMENNR AND MIT_MITARBEITERID = RNP_V4_MITARBEITERID" "~"
if joinKuerzel = MITRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_V4_FIRMENNR = MIT_FIRMENNR AND RNP_V4_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = AVEMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = AVE_BEARB_FIRMENNR AND MIT_MITARBEITERID = AVE_BEARB_MITARBEITERID" "~"
if joinKuerzel = MITAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_BEARB_FIRMENNR = MIT_FIRMENNR AND AVE_BEARB_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = KAKMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = KAK_KASSIER_FIRMENNR AND MIT_MITARBEITERID = KAK_KASSIER_MITARBEITERID" "~"
if joinKuerzel = MITKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_KASSIER_FIRMENNR = MIT_FIRMENNR AND KAK_KASSIER_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = MSUMKR
searchResult := searchResult "LEFT JOIN FIBU.MKR_MAHNKREIS ON MKR_FIRMENNR = MSU_FIRMENNR AND MKR_FIBUNR = MSU_FIBUNR AND MKR_MAHNKREISLFDNR = MSU_MAHNKREISLFDNR" "~"
if joinKuerzel = MKRMSU
searchResult := searchResult "LEFT JOIN FIBU.MSU_MAHNSUMME ON MSU_FIRMENNR = MKR_FIRMENNR AND MSU_FIBUNR = MKR_FIBUNR AND MSU_MAHNKREISLFDNR = MKR_MAHNKREISLFDNR" "~"
if joinKuerzel = LFIMVK
searchResult := searchResult "LEFT JOIN MVK_MITARBVORSORGEKASSE.MVK_MITARBVORSORGEKASSE ON MVK_LANDNR = LFI_MVK_LANDNR AND MVK_KASSENR = LFI_MVK_KASSENR" "~"
if joinKuerzel = MVKLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_MVK_LANDNR = MVK_LANDNR AND LFI_MVK_KASSENR = MVK_KASSENR" "~"
if joinKuerzel = MITMVL
searchResult := searchResult "LEFT JOIN BUERO.MVL_MITARBEITERVORLAGE ON MVL_MVLID = MIT_MVLID" "~"
if joinKuerzel = MVLMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_MVLID = MVL_MVLID" "~"
if joinKuerzel = NUINUP
searchResult := searchResult "LEFT JOIN BMD.NUP_NTCSUPDATE ON NUP_NTCSUPDATEID = NUI_NTCSUPDATEID" "~"
if joinKuerzel = NUPNUI
searchResult := searchResult "LEFT JOIN BMD.NUI_NTCSUPDINSTALLATION ON NUI_NTCSUPDATEID = NUP_NTCSUPDATEID" "~"
if joinKuerzel = NUPNUY
searchResult := searchResult "LEFT JOIN BMD.NUY_NTCSUPDTYP ON NUY_NTCSUPDTYPID = NUP_NTCSUPDTYPID" "~"
if joinKuerzel = NUYNUP
searchResult := searchResult "LEFT JOIN BMD.NUP_NTCSUPDATE ON NUP_NTCSUPDTYPID = NUY_NTCSUPDTYPID" "~"
if joinKuerzel = OBBOBP
searchResult := searchResult "LEFT JOIN BUERO.OBP_ONLBEWPERSSTAMM ON OBP_PERSSTAMMID = OBB_OBP_PERSSTAMMID" "~"
if joinKuerzel = OBPOBB
searchResult := searchResult "LEFT JOIN BUERO.OBB_ONLBEWBEWERBUNG ON OBB_OBP_PERSSTAMMID = OBP_PERSSTAMMID" "~"
if joinKuerzel = OPEOBR
searchResult := searchResult "LEFT JOIN LOHN.OBR_OF_BETRIEBSRAT ON OBR_BRNR = OPE_BRNR" "~"
if joinKuerzel = OBROPE
searchResult := searchResult "LEFT JOIN LOHN.OPE_OF_PERSON ON OPE_BRNR = OBR_BRNR" "~"
if joinKuerzel = OAKOKL
searchResult := searchResult "LEFT JOIN LOHN.OKL_OF_KLIENT ON OKL_KLIENTENNR = OAK_KLIENTENNR" "~"
if joinKuerzel = OKLOAK
searchResult := searchResult "LEFT JOIN LOHN.OAK_OF_AKREIS ON OAK_KLIENTENNR = OKL_KLIENTENNR" "~"
if joinKuerzel = OTAOKL
searchResult := searchResult "LEFT JOIN LOHN.OKL_OF_KLIENT ON OKL_KLIENTENNR = OTA_KLIENTENNR" "~"
if joinKuerzel = OKLOTA
searchResult := searchResult "LEFT JOIN LOHN.OTA_OF_TAETIGKEIT ON OTA_KLIENTENNR = OKL_KLIENTENNR" "~"
if joinKuerzel = OUTOKL
searchResult := searchResult "LEFT JOIN LOHN.OKL_OF_KLIENT ON OKL_KLIENTENNR = OUT_KLIENTENNR" "~"
if joinKuerzel = OKLOUT
searchResult := searchResult "LEFT JOIN LOHN.OUT_OF_ULTIMO ON OUT_KLIENTENNR = OKL_KLIENTENNR" "~"
if joinKuerzel = OFIOLA
searchResult := searchResult "LEFT JOIN LOHN.OLA_OF_LOHNART ON OLA_KLIENTENNR = OFI_OLA_KLIENTENNR AND OLA_OKV_OFKVID = OFI_OFKVID AND OLA_LANR = OFI_LANR" "~"
if joinKuerzel = OLAOFI
searchResult := searchResult "LEFT JOIN LOHN.OFI_OF_FIXWERTE ON OFI_OLA_KLIENTENNR = OLA_KLIENTENNR AND OFI_OFKVID = OLA_OKV_OFKVID AND OFI_LANR = OLA_LANR" "~"
if joinKuerzel = ONMONS
searchResult := searchResult "LEFT JOIN HRS.ONS_ONLINEBEW_STAMMDATEN ON ONS_ONLBEWID = ONM_ONLBEWID" "~"
if joinKuerzel = ONSONM
searchResult := searchResult "LEFT JOIN HRS.ONM_ONLINEBEW_MERKMALE ON ONM_ONLBEWID = ONS_ONLBEWID" "~"
if joinKuerzel = OFIOPE
searchResult := searchResult "LEFT JOIN LOHN.OPE_OF_PERSON ON OPE_KLIENTENNR = OFI_KLIENTENNR AND OPE_PERSNR = OFI_PERSNR AND OPE_PERSUNTERNR = OFI_PERSUNTERNR" "~"
if joinKuerzel = OPEOFI
searchResult := searchResult "LEFT JOIN LOHN.OFI_OF_FIXWERTE ON OFI_KLIENTENNR = OPE_KLIENTENNR AND OFI_PERSNR = OPE_PERSNR AND OFI_PERSUNTERNR = OPE_PERSUNTERNR" "~"
if joinKuerzel = ORSOPE
searchResult := searchResult "LEFT JOIN LOHN.OPE_OF_PERSON ON OPE_KLIENTENNR = ORS_OPE_KLIENTENNR AND OPE_PERSNR = ORS_OPE_PERSNR AND OPE_PERSUNTERNR = ORS_OPE_PERSUNTERNR" "~"
if joinKuerzel = OPEORS
searchResult := searchResult "LEFT JOIN LOHN.ORS_OF_REISESTAMM ON ORS_OPE_KLIENTENNR = OPE_KLIENTENNR AND ORS_OPE_PERSNR = OPE_PERSNR AND ORS_OPE_PERSUNTERNR = OPE_PERSUNTERNR" "~"
if joinKuerzel = BONPET
searchResult := searchResult "LEFT JOIN WWS.PET_PREISTAB ON PET_FIRMENNR = BON_PREISTAB_FIRMENNR AND PET_PREISTABNR = BON_PREISTABNR" "~"
if joinKuerzel = PETBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_PREISTAB_FIRMENNR = PET_FIRMENNR AND BON_PREISTABNR = PET_PREISTABNR" "~"
if joinKuerzel = PWEPET
searchResult := searchResult "LEFT JOIN WWS.PET_PREISTAB ON PET_FIRMENNR = PWE_FIRMENNR AND PET_PREISTABNR = PWE_PREISTABNR" "~"
if joinKuerzel = PETPWE
searchResult := searchResult "LEFT JOIN WWS.PWE_PWKPREISTABZU ON PWE_FIRMENNR = PET_FIRMENNR AND PWE_PREISTABNR = PET_PREISTABNR" "~"
if joinKuerzel = WEIPET
searchResult := searchResult "LEFT JOIN WWS.PET_PREISTAB ON PET_FIRMENNR = WEI_PREISFIRMENNR AND PET_PREISTABNR = WEI_PREISTABNR" "~"
if joinKuerzel = PETWEI
searchResult := searchResult "LEFT JOIN WWS.WEI_WARENEINSATZKOPF ON WEI_PREISFIRMENNR = PET_FIRMENNR AND WEI_PREISTABNR = PET_PREISTABNR" "~"
if joinKuerzel = PZEPGL
searchResult := searchResult "LEFT JOIN HRS.PGL_PERKZGLIEDERUNG ON PGL_GLIEDERUNGID = PZE_PGL_GLIEDERUNGID" "~"
if joinKuerzel = PGLPZE
searchResult := searchResult "LEFT JOIN HRS.PZE_PERKZGLGZEILE ON PZE_PGL_GLIEDERUNGID = PGL_GLIEDERUNGID" "~"
if joinKuerzel = PEAPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_PICID = PEA_PICID AND PIA_PICARTIKELNR = PEA_PICARTIKELNR" "~"
if joinKuerzel = PIAPEA
searchResult := searchResult "LEFT JOIN WWS.PEA_PICEANNUMMERN ON PEA_PICID = PIA_PICID AND PEA_PICARTIKELNR = PIA_PICARTIKELNR" "~"
if joinKuerzel = PISPID
searchResult := searchResult "LEFT JOIN WWS.PID_PICPREISDETAIL ON PID_PIDID = PIS_PIDID" "~"
if joinKuerzel = PIDPIS
searchResult := searchResult "LEFT JOIN WWS.PIS_PICPREISSTAFFEL ON PIS_PIDID = PID_PIDID" "~"
if joinKuerzel = AAAPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = AAA_POSTITNR" "~"
if joinKuerzel = PITAAA
searchResult := searchResult "LEFT JOIN BUERO.AAA_AUSDRUCKABLAUFART ON AAA_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = AGLPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = AGL_POSTITNR" "~"
if joinKuerzel = PITAGL
searchResult := searchResult "LEFT JOIN PRODUKTION.AGL_ARBEITSGANGZU ON AGL_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = AGSPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = AGS_POSTITNR" "~"
if joinKuerzel = PITAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = AIZPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = AIZ_POSTITNR" "~"
if joinKuerzel = PITAIZ
searchResult := searchResult "LEFT JOIN PRODUKTION.AIZ_AGPER_INFOZU ON AIZ_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = ASRPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = ASR_POSTITNR" "~"
if joinKuerzel = PITASR
searchResult := searchResult "LEFT JOIN BUERO.ASR_AUSSCHREIBUNG ON ASR_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = AUGPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = AUG_POSTITNR" "~"
if joinKuerzel = PITAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = BALPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = BAL_POSTITNR" "~"
if joinKuerzel = PITBAL
searchResult := searchResult "LEFT JOIN PRODUKTION.BAL_ARBEITSPLATZLAGERZU ON BAL_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = BEWPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = BEW_POSTITNR" "~"
if joinKuerzel = PITBEW
searchResult := searchResult "LEFT JOIN BUERO.BEW_BEWERTUNGSMODELL ON BEW_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = BWBPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = BWB_POSTITNR" "~"
if joinKuerzel = PITBWB
searchResult := searchResult "LEFT JOIN BUERO.BWB_BEWERBUNG ON BWB_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = DELPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = DEL_POSTITNR" "~"
if joinKuerzel = PITDEL
searchResult := searchResult "LEFT JOIN BUERO.DEL_DELETE ON DEL_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = DIHPIT
searchResult := searchResult "LEFT JOIN BMD.PIT_POSTIT ON PIT_POSTITNR = DIH_POSTITNR" "~"
if joinKuerzel = PITDIH
searchResult := searchResult "LEFT JOIN BMD.DIH_DIAGRAMMINSTANZKOPF ON DIH_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = DOKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = DOK_POSTITNR" "~"
if joinKuerzel = PITDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = DQUPIT
searchResult := searchResult "LEFT JOIN BMD.PIT_POSTIT ON PIT_POSTITNR = DQU_POSTITNR" "~"
if joinKuerzel = PITDQU
searchResult := searchResult "LEFT JOIN BMD.DQU_DATENQUELLE ON DQU_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = EANPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = EAN_POSTITNR" "~"
if joinKuerzel = PITEAN
searchResult := searchResult "LEFT JOIN WWS.EAN_EANNUMMERN ON EAN_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = EVGPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = EVG_POSTITNR" "~"
if joinKuerzel = PITEVG
searchResult := searchResult "LEFT JOIN BUERO.EVG_EVENTTAGE ON EVG_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = FOTPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = FOT_POSTITNR" "~"
if joinKuerzel = PITFOT
searchResult := searchResult "LEFT JOIN BUERO.FOT_FOTO ON FOT_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = GSKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = GSK_POSTITNR" "~"
if joinKuerzel = PITGSK
searchResult := searchResult "LEFT JOIN WWS.GSK_GUTSCHEINKOPF ON GSK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = HSKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = HSK_POSTITNR" "~"
if joinKuerzel = PITHSK
searchResult := searchResult "LEFT JOIN HRS.HSK_SORTIERKOPF ON HSK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = INSPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = INS_POSTITNR" "~"
if joinKuerzel = PITINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = QAWPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = QAW_GF_FIRMENNR AND PER_PERSONENID = QAW_GF_PERSONENID" "~"
if joinKuerzel = PERQAW
searchResult := searchResult "LEFT JOIN BUERO.QAW_QUOTENAUSWERTUNG ON QAW_GF_FIRMENNR = PER_FIRMENNR AND QAW_GF_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = TLNPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = TLN_KONTAKT_FIRMENNR AND PER_PERSONENID = TLN_KONTAKT_PERSONENID" "~"
if joinKuerzel = PERTLN
searchResult := searchResult "LEFT JOIN BUERO.TLN_TEILNEHMER ON TLN_KONTAKT_FIRMENNR = PER_FIRMENNR AND TLN_KONTAKT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = VASPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = VAS_ORT_FIRMENNR AND PER_PERSONENID = VAS_ORT_PERSONENID" "~"
if joinKuerzel = PERVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_ORT_FIRMENNR = PER_FIRMENNR AND VAS_ORT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = WAKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = WAK_RECH_FIRMENNR AND PER_PERSONENID = WAK_RECH_KLID" "~"
if joinKuerzel = WAKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = WAK_LIEFERANT_FIRMENNR AND PER_PERSONENID = WAK_LIEFERANT_KLID" "~"
if joinKuerzel = PERWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_RECH_FIRMENNR = PER_FIRMENNR AND WAK_RECH_KLID = PER_PERSONENID" "~"
if joinKuerzel = WSSPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = WSS_VERTRETER_FIRMENNR AND PER_PERSONENID = WSS_VERTRETER_PERSONENID" "~"
if joinKuerzel = PERWSS
searchResult := searchResult "LEFT JOIN WWS.WSS_WWSSTATISTIK ON WSS_VERTRETER_FIRMENNR = PER_FIRMENNR AND WSS_VERTRETER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = BUCPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = BUC_V1_FIRMENNR AND PER_PERSONENID = BUC_V1_PERSONENID" "~"
if joinKuerzel = PERBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_V1_FIRMENNR = PER_FIRMENNR AND BUC_V1_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = DOKPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = DOK_KONTAKT_FIRMENNR AND PER_PERSONENID = DOK_KONTAKT_PERSONENID" "~"
if joinKuerzel = PERDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_KONTAKT_FIRMENNR = PER_FIRMENNR AND DOK_KONTAKT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = EVGPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = EVG_BET_FIRMENNR AND PER_PERSONENID = EVG_BET_PERSONENID" "~"
if joinKuerzel = PEREVG
searchResult := searchResult "LEFT JOIN BUERO.EVG_EVENTTAGE ON EVG_BET_FIRMENNR = PER_FIRMENNR AND EVG_BET_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PCKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = PCK_KONTAKTPERSON_FIRMENNR AND PER_PERSONENID = PCK_KONTAKTPERSON_PERID" "~"
if joinKuerzel = PERPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_KONTAKTPERSON_FIRMENNR = PER_FIRMENNR AND PCK_KONTAKTPERSON_PERID = PER_PERSONENID" "~"
if joinKuerzel = VSWPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = VSW_LIEF_FIRMENNR AND PER_PERSONENID = VSW_LIEF_PERSONENID" "~"
if joinKuerzel = PERVSW
searchResult := searchResult "LEFT JOIN WWS.VSW_VERTRETERSTATISTIK ON VSW_LIEF_FIRMENNR = PER_FIRMENNR AND VSW_LIEF_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = DOKPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = DOK_GF_PER_FIRMENNR AND PER_PERSONENID = DOK_GF_PERSONENID" "~"
if joinKuerzel = PERDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_GF_PER_FIRMENNR = PER_FIRMENNR AND DOK_GF_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = EFKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = EFK_ZRECHEMPF_KON_FIRMENNR AND PER_PERSONENID = EFK_ZRECHEMPF_KON_PERSID" "~"
if joinKuerzel = PEREFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_ZRECHEMPF_KON_FIRMENNR = PER_FIRMENNR AND EFK_ZRECHEMPF_KON_PERSID = PER_PERSONENID" "~"
if joinKuerzel = VSWPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = VSW_AUFTRAGGEBER_FIRMENNR AND PER_PERSONENID = VSW_AUFTRAGGEBER_KLID" "~"
if joinKuerzel = PERVSW
searchResult := searchResult "LEFT JOIN WWS.VSW_VERTRETERSTATISTIK ON VSW_AUFTRAGGEBER_FIRMENNR = PER_FIRMENNR AND VSW_AUFTRAGGEBER_KLID = PER_PERSONENID" "~"
if joinKuerzel = AVEPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = AVE_AUFTRGEB_KONT_FIRMENNR AND PER_PERSONENID = AVE_AUFTRGEB_KONTAKTPERSID" "~"
if joinKuerzel = PERAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_AUFTRGEB_KONT_FIRMENNR = PER_FIRMENNR AND AVE_AUFTRGEB_KONTAKTPERSID = PER_PERSONENID" "~"
if joinKuerzel = PERPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PER_FOLGEVERT2_FIRMENNR AND PER_PERSONENID = PER_FOLGEVERT2_PERSONENID" "~"
if joinKuerzel = PERPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FOLGEVERT2_FIRMENNR = PER_FIRMENNR AND PER_FOLGEVERT2_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PERPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PER_FOLGEVERT3_FIRMENNR AND PER_PERSONENID = PER_FOLGEVERT3_PERSONENID" "~"
if joinKuerzel = PERPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FOLGEVERT3_FIRMENNR = PER_FIRMENNR AND PER_FOLGEVERT3_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = KAKPET
searchResult := searchResult "LEFT JOIN WWS.PET_PREISTAB ON PET_FIRMENNR = KAK_PREISTAB_FIRMENNR AND PET_PREISTABNR = KAK_PREISTABNR" "~"
if joinKuerzel = PETKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_PREISTAB_FIRMENNR = PET_FIRMENNR AND KAK_PREISTABNR = PET_PREISTABNR" "~"
if joinKuerzel = SFFPFI
searchResult := searchResult "LEFT JOIN PFI_PANELFILTER.PFI_PANELFILTER ON PFI_PFILTERNR = SFF_PFI_PFILTERNR" "~"
if joinKuerzel = PFISFF
searchResult := searchResult "LEFT JOIN BUERO.SFF_SKILLFINDERFILTER ON SFF_PFI_PFILTERNR = PFI_PFILTERNR" "~"
if joinKuerzel = PEAPFR
searchResult := searchResult "LEFT JOIN WWS.PFR_PICFARBE ON PFR_PICID = PEA_PICID AND PFR_PICFARBENNR = PEA_PICFARBENNR" "~"
if joinKuerzel = PFRPEA
searchResult := searchResult "LEFT JOIN WWS.PEA_PICEANNUMMERN ON PEA_PICID = PFR_PICID AND PEA_PICFARBENNR = PFR_PICFARBENNR" "~"
if joinKuerzel = SKEPGE
searchResult := searchResult "LEFT JOIN PGE_PERSONGESELLZU.PGE_PERSONGESELLZU ON PGE_KUN_FIRMENNR = SKE_KUN_FIRMENNR AND PGE_KUN_PERSONENID = SKE_KUN_PERSONENID AND PGE_GES_FIRMENNR = SKE_GES_FIRMENNR AND PGE_GES_PERSONENID = SKE_GES_PERSONENID" "~"
if joinKuerzel = PGESKE
searchResult := searchResult "LEFT JOIN BIL.SKE_STEUERKUNDENZUERW ON SKE_KUN_FIRMENNR = PGE_KUN_FIRMENNR AND SKE_KUN_PERSONENID = PGE_KUN_PERSONENID AND SKE_GES_FIRMENNR = PGE_GES_FIRMENNR AND SKE_GES_PERSONENID = PGE_GES_PERSONENID" "~"
if joinKuerzel = PGPPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_PICID = PGP_PICID" "~"
if joinKuerzel = PICPGP
searchResult := searchResult "LEFT JOIN WWS.PGP_PICARTIKELGRP ON PGP_PICID = PIC_PICID" "~"
if joinKuerzel = PLSPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_PICID = PLS_PICID" "~"
if joinKuerzel = PICPLS
searchResult := searchResult "LEFT JOIN WWS.PLS_PICALLERGEN ON PLS_PICID = PIC_PICID" "~"
if joinKuerzel = AFKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = AFK_POSTITNR" "~"
if joinKuerzel = PITAFK
searchResult := searchResult "LEFT JOIN ZLE.AFK_EXTFILTERKOPF ON AFK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = ARGPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = ARG_POSTITNR" "~"
if joinKuerzel = PITARG
searchResult := searchResult "LEFT JOIN BUERO.ARG_ARBEITSRUHEZEITGESETZ ON ARG_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = BORPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = BOR_POSTITNR" "~"
if joinKuerzel = PITBOR
searchResult := searchResult "LEFT JOIN WWS.BOR_BONRABATT ON BOR_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = BUCPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = BUC_POSTITNR" "~"
if joinKuerzel = PITBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = BZBPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = BZB_POSTITNR" "~"
if joinKuerzel = PITBZB
searchResult := searchResult "LEFT JOIN PRODUKTION.BZB_BAZZEITBUCHUNG ON BZB_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = DIGPIT
searchResult := searchResult "LEFT JOIN BMD.PIT_POSTIT ON PIT_POSTITNR = DIG_POSTITNR" "~"
if joinKuerzel = PITDIG
searchResult := searchResult "LEFT JOIN BMD.DIG_DIAGRAMMGRUPPE ON DIG_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = EKLPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = EKL_POSTITNR" "~"
if joinKuerzel = PITEKL
searchResult := searchResult "LEFT JOIN WWS.EKL_ERFASSKOPFLFS ON EKL_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = ERTPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = ERT_POSTITNR" "~"
if joinKuerzel = PITERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = ERXPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = ERX_POSTITNR" "~"
if joinKuerzel = PITERX
searchResult := searchResult "LEFT JOIN HRS.ERX_ERFASSUNGSZUSATZ ON ERX_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = ERZPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = ERZ_POSTITNR" "~"
if joinKuerzel = PITERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = FGEPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = FGE_POSTITNR" "~"
if joinKuerzel = PITFGE
searchResult := searchResult "LEFT JOIN BUERO.FGE_FRAGE ON FGE_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = FSTPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = FST_POSTITNR" "~"
if joinKuerzel = PITFST
searchResult := searchResult "LEFT JOIN BUERO.FST_FRIST ON FST_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = FTPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = FTP_POSTITNR" "~"
if joinKuerzel = PITFTP
searchResult := searchResult "LEFT JOIN WWS.FTP_FARBENTABPOS ON FTP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = GATPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = GAT_POSTITNR" "~"
if joinKuerzel = PITGAT
searchResult := searchResult "LEFT JOIN ONVIS.GAT_GARNTYPEN ON GAT_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = KGSPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = KGS_POSTITNR" "~"
if joinKuerzel = PITKGS
searchResult := searchResult "LEFT JOIN WWS.KGS_KATZU_ARBEITSGANG ON KGS_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = KOMPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = KOM_POSTITNR" "~"
if joinKuerzel = PITKOM
searchResult := searchResult "LEFT JOIN PRODUKTION.KOM_KOMBI ON KOM_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = KPMPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = KPM_POSTITNR" "~"
if joinKuerzel = PITKPM
searchResult := searchResult "LEFT JOIN KORE.KPM_PLANMODELL ON KPM_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = KZPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = KZP_POSTITNR" "~"
if joinKuerzel = PITKZP
searchResult := searchResult "LEFT JOIN FIBU.KZP_ZAHLUNGSPLAN ON KZP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = LFIPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = LFI_POSTITNR" "~"
if joinKuerzel = PITLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PALPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PAL_POSTITNR" "~"
if joinKuerzel = PITPAL
searchResult := searchResult "LEFT JOIN ZLE.PAL_PAUSCHALE ON PAL_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = RATPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = RAT_POSTITNR" "~"
if joinKuerzel = PITRAT
searchResult := searchResult "LEFT JOIN WWS.RAT_RABATTTAB ON RAT_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = RGMPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = RGM_POSTITNR" "~"
if joinKuerzel = PITRGM
searchResult := searchResult "LEFT JOIN DATANORM.RGM_RABATT_GRUPPE ON RGM_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = RMDPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = RMD_POSTITNR" "~"
if joinKuerzel = PITRMD
searchResult := searchResult "LEFT JOIN BUERO.RMD_RUNDUNGSMODELLDET ON RMD_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = SLPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = SLP_POSTITNR" "~"
if joinKuerzel = PITSLP
searchResult := searchResult "LEFT JOIN WWS.SLP_STUECKLISTENPOSITION ON SLP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PIRPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_PICID = PIR_PICID" "~"
if joinKuerzel = PICPIR
searchResult := searchResult "LEFT JOIN WWS.PIR_PICRABATTDETAIL ON PIR_PICID = PIC_PICID" "~"
if joinKuerzel = PIZPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_PICID = PIZ_PICID" "~"
if joinKuerzel = PICPIZ
searchResult := searchResult "LEFT JOIN WWS.PIZ_PICARTIKELZU ON PIZ_PICID = PIC_PICID" "~"
if joinKuerzel = PSYPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_PICID = PSY_PICID" "~"
if joinKuerzel = PICPSY
searchResult := searchResult "LEFT JOIN WWS.PSY_PICSYNCFELDZU ON PSY_PICID = PIC_PICID" "~"
if joinKuerzel = PIDPIG
searchResult := searchResult "LEFT JOIN WWS.PIG_PICPREISGRUPPE ON PIG_PICID = PID_PICID AND PIG_PIGHAUPTGRNR = PID_PIGHAUPTGRNR AND PIG_PIGUNTERGRNR = PID_PIGUNTERGRNR" "~"
if joinKuerzel = PIGPID
searchResult := searchResult "LEFT JOIN WWS.PID_PICPREISDETAIL ON PID_PICID = PIG_PICID AND PID_PIGHAUPTGRNR = PIG_PIGHAUPTGRNR AND PID_PIGUNTERGRNR = PIG_PIGUNTERGRNR" "~"
if joinKuerzel = PIBPIR
searchResult := searchResult "LEFT JOIN WWS.PIR_PICRABATTDETAIL ON PIR_PIRID = PIB_PIRID" "~"
if joinKuerzel = PIRPIB
searchResult := searchResult "LEFT JOIN WWS.PIB_PICRABATTSTAFFEL ON PIB_PIRID = PIR_PIRID" "~"
if joinKuerzel = ALSPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = ALS_POSTITNR" "~"
if joinKuerzel = PITALS
searchResult := searchResult "LEFT JOIN WWS.ALS_ALLERGEN ON ALS_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = BEFPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = BEF_POSTITNR" "~"
if joinKuerzel = PITBEF
searchResult := searchResult "LEFT JOIN BUERO.BEF_BEFRAGUNG ON BEF_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = BRVPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = BRV_POSTITNR" "~"
if joinKuerzel = PITBRV
searchResult := searchResult "LEFT JOIN FIBU.BRV_BANKRUECKVERBUCHUNG ON BRV_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = BTGPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = BTG_POSTITNR" "~"
if joinKuerzel = PITBTG
searchResult := searchResult "LEFT JOIN BUERO.BTG_BEWERTUNGTAG ON BTG_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = DBGPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = DBG_POSTITNR" "~"
if joinKuerzel = PITDBG
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DBG_DBMDGRUPPE ON DBG_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = DKZPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = DKZ_POSTITNR" "~"
if joinKuerzel = PITDKZ
searchResult := searchResult "LEFT JOIN BUERO.DKZ_DOKUMENTZU ON DKZ_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = DRGPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = DRG_POSTITNR" "~"
if joinKuerzel = PITDRG
searchResult := searchResult "LEFT JOIN HRS.DRG_DIENSTREISEGEN ON DRG_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = EHTPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = EHT_POSTITNR" "~"
if joinKuerzel = PITEHT
searchResult := searchResult "LEFT JOIN WWS.EHT_EINHEITENTAB ON EHT_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = ERPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = ERP_POSTITNR" "~"
if joinKuerzel = PITERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = FBNPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = FBN_POSTITNR" "~"
if joinKuerzel = PITFBN
searchResult := searchResult "LEFT JOIN BUERO.FBN_FRAGEBOGEN ON FBN_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = GEPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = GEP_POSTITNR" "~"
if joinKuerzel = PITGEP
searchResult := searchResult "LEFT JOIN WWS.GEP_GEFAHRENGUTPOS ON GEP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = GROPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = GRO_POSTITNR" "~"
if joinKuerzel = PITGRO
searchResult := searchResult "LEFT JOIN WWS.GRO_GROESSE ON GRO_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = GTPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = GTP_POSTITNR" "~"
if joinKuerzel = PITGTP
searchResult := searchResult "LEFT JOIN WWS.GTP_GROESSENTABPOS ON GTP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = KGBPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = KGB_POSTITNR" "~"
if joinKuerzel = PITKGB
searchResult := searchResult "LEFT JOIN PRODUKTION.KGB_KATZU_PPSKAPA ON KGB_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = KMPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = KMP_POSTITNR" "~"
if joinKuerzel = PITKMP
searchResult := searchResult "LEFT JOIN KORE.KMP_PLANMENGEN ON KMP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = LFSPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = LFS_POSTITNR" "~"
if joinKuerzel = PITLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PACPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PAC_POSTITNR" "~"
if joinKuerzel = PITPAC
searchResult := searchResult "LEFT JOIN WWS.PAC_PACKSTOFF ON PAC_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PATPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PAT_POSTITNR" "~"
if joinKuerzel = PITPAT
searchResult := searchResult "LEFT JOIN WWS.PAT_PACKSTOFFTAB ON PAT_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PDSPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PDS_POSTITNR" "~"
if joinKuerzel = PITPDS
searchResult := searchResult "LEFT JOIN BIL.PDS_PERSKORST_DNSTAMM ON PDS_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PLTPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PLT_POSTITNR" "~"
if joinKuerzel = PITPLT
searchResult := searchResult "LEFT JOIN BUERO.PLT_PIPELINESTUFE ON PLT_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = RPPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = RPP_POSTITNR" "~"
if joinKuerzel = PITRPP
searchResult := searchResult "LEFT JOIN ONVIS.RPP_RAPPORTE ON RPP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = KAPPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = KAP_LEISTENDERUNTERN_FIRNR AND PER_PERSONENID = KAP_LEISTENDERUNTERN_PERID" "~"
if joinKuerzel = PERKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_LEISTENDERUNTERN_FIRNR = PER_FIRMENNR AND KAP_LEISTENDERUNTERN_PERID = PER_PERSONENID" "~"
if joinKuerzel = KUZPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = KUZ_FIRMENNR AND PER_PERSONENID = KUZ_PERSONENID" "~"
if joinKuerzel = PERKUZ
searchResult := searchResult "LEFT JOIN BUERO.KUZ_KUNDENZAHLUNGEN ON KUZ_FIRMENNR = PER_FIRMENNR AND KUZ_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = ODTPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = ODT_PER_FIRMENNR AND PER_PERSONENID = ODT_PERSONENID" "~"
if joinKuerzel = PERODT
searchResult := searchResult "LEFT JOIN BMD.ODT_ONLINEDATEITRANSFER ON ODT_PER_FIRMENNR = PER_FIRMENNR AND ODT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PKSPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = PKS_PER_FIRMENNR AND PER_PERSONENID = PKS_PER_PERSONENID" "~"
if joinKuerzel = PERPKS
searchResult := searchResult "LEFT JOIN PRODUKTION.PKS_SOLL_KAPAZITAET ON PKS_PER_FIRMENNR = PER_FIRMENNR AND PKS_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PMAPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PMA_FIRMENNR AND PER_PERSONENID = PMA_PERSONENID" "~"
if joinKuerzel = PERPMA
searchResult := searchResult "LEFT JOIN BUERO.PMA_PROJEKTMITARBZU ON PMA_FIRMENNR = PER_FIRMENNR AND PMA_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PSMPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = PSM_DVFIRMENNR AND PER_PERSONENID = PSM_MITARBEITERID" "~"
if joinKuerzel = PSMPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = PSM_DVFIRMENNR AND PER_PERSONENID = PSM_BETRIEBSRAT_PERSONENID" "~"
if joinKuerzel = PERPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_DVFIRMENNR = PER_FIRMENNR AND PSM_MITARBEITERID = PER_PERSONENID" "~"
if joinKuerzel = PSMDVH
searchResult := searchResult "LEFT JOIN BUERO.DVH_DIENSTVERHAELTNIS ON DVH_DIENSTV_FIRMENNR = PSM_DVFIRMENNR AND DVH_MITARBEITERID = PSM_MITARBEITERID AND DVH_DIENSTVLFDNR = PSM_DIENSTVLFDNR" "~"
if joinKuerzel = DVHPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_DVFIRMENNR = DVH_DIENSTV_FIRMENNR AND PSM_MITARBEITERID = DVH_MITARBEITERID AND PSM_DIENSTVLFDNR = DVH_DIENSTVLFDNR" "~"
if joinKuerzel = QUOPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = QUO_FIRMENNR AND PER_PERSONENID = QUO_PERSONENID" "~"
if joinKuerzel = PERQUO
searchResult := searchResult "LEFT JOIN BUERO.QUO_QUOTE ON QUO_FIRMENNR = PER_FIRMENNR AND QUO_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = SPSPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = SPS_PER_FIRMENNR AND PER_PERSONENID = SPS_PER_PERSONENID" "~"
if joinKuerzel = PERSPS
searchResult := searchResult "LEFT JOIN FIBU.SPS_SPENDENSUMME ON SPS_PER_FIRMENNR = PER_FIRMENNR AND SPS_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = TOZPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = TOZ_LIEFEREMPF_FIRMENNR AND PER_PERSONENID = TOZ_LIEFEREMPF_PERSID" "~"
if joinKuerzel = PERTOZ
searchResult := searchResult "LEFT JOIN WWS.TOZ_TOURZUORDNUNG ON TOZ_LIEFEREMPF_FIRMENNR = PER_FIRMENNR AND TOZ_LIEFEREMPF_PERSID = PER_PERSONENID" "~"
if joinKuerzel = UIDPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = UID_PER_FIRMENNR AND PER_PERSONENID = UID_PERSONENID" "~"
if joinKuerzel = PERUID
searchResult := searchResult "LEFT JOIN BUERO.UID_UIDNUMMER ON UID_PER_FIRMENNR = PER_FIRMENNR AND UID_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = VAKPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = VAK_PER_FIRMENNR AND PER_PERSONENID = VAK_PER_PERSONENID" "~"
if joinKuerzel = PERVAK
searchResult := searchResult "LEFT JOIN BUERO.VAK_VERANSTALTUNGSKOPF ON VAK_PER_FIRMENNR = PER_FIRMENNR AND VAK_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = VOPPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = VOP_FIRMENNR AND PER_PERSONENID = VOP_PERSONENID" "~"
if joinKuerzel = PERVOP
searchResult := searchResult "LEFT JOIN PRODUKTION.VOP_VOB_PERSON_ZU ON VOP_FIRMENNR = PER_FIRMENNR AND VOP_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = WVTPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = WVT_PER_FIRMENNR AND PER_PERSONENID = WVT_PERSONENID" "~"
if joinKuerzel = PERWVT
searchResult := searchResult "LEFT JOIN WWS.WVT_WARTUNGSVERTRAG ON WVT_PER_FIRMENNR = PER_FIRMENNR AND WVT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = YBUPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = YBU_PER_FIRMENNR AND PER_PERSONENID = YBU_PER_PERSONENID" "~"
if joinKuerzel = PERYBU
searchResult := searchResult "LEFT JOIN FIBU.YBU_AUTOBUCHUNG ON YBU_PER_FIRMENNR = PER_FIRMENNR AND YBU_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = ZSUPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = ZSU_PER_FIRMENNR AND PER_PERSONENID = ZSU_PER_PERSONENID" "~"
if joinKuerzel = PERZSU
searchResult := searchResult "LEFT JOIN FIBU.ZSU_ZAHLSUMME ON ZSU_PER_FIRMENNR = PER_FIRMENNR AND ZSU_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = ALEPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = ALE_ANSPRECH_FIRMENNR AND PER_PERSONENID = ALE_ANSPRECH_PERSONENID" "~"
if joinKuerzel = PERALE
searchResult := searchResult "LEFT JOIN BUERO.ALE_ADRESSENLISTE ON ALE_ANSPRECH_FIRMENNR = PER_FIRMENNR AND ALE_ANSPRECH_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = AWSPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = AWS_KONZERN_FIRMENNR AND PER_PERSONENID = AWS_KONZERN_PERSONENID" "~"
if joinKuerzel = PERAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_KONZERN_FIRMENNR = PER_FIRMENNR AND AWS_KONZERN_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = KJOPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = KJO_KON_FIRMENNR AND PER_PERSONENID = KJO_KONTAKTPERSID" "~"
if joinKuerzel = PERKJO
searchResult := searchResult "LEFT JOIN BUERO.KJO_KONTAKTJOURNAL ON KJO_KON_FIRMENNR = PER_FIRMENNR AND KJO_KONTAKTPERSID = PER_PERSONENID" "~"
if joinKuerzel = PGEPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PGE_GES_FIRMENNR AND PER_PERSONENID = PGE_GES_PERSONENID" "~"
if joinKuerzel = PERPGE
searchResult := searchResult "LEFT JOIN BUERO.PGE_PERSONGESELLZU ON PGE_GES_FIRMENNR = PER_FIRMENNR AND PGE_GES_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PKZPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PKZ_FIRMENNR AND PER_PERSONENID = PKZ_PERSONENID" "~"
if joinKuerzel = PERPKZ
searchResult := searchResult "LEFT JOIN BUERO.PKZ_PERSONKONTAKTZU ON PKZ_FIRMENNR = PER_FIRMENNR AND PKZ_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = HNPPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = HNP_PER_FIRMENNR AND PER_PERSONENID = HNP_PER_PERSONENID" "~"
if joinKuerzel = PERHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_PER_FIRMENNR = PER_FIRMENNR AND HNP_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = HPTPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = HPT_PER_FIRMENNR AND PER_PERSONENID = HPT_PERSONENID" "~"
if joinKuerzel = PERHPT
searchResult := searchResult "LEFT JOIN ZLE.HPT_HONORARPOSTEXTE ON HPT_PER_FIRMENNR = PER_FIRMENNR AND HPT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = MLSPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = MLS_PER_FIRMENNR AND PER_PERSONENID = MLS_PER_PERSONENID" "~"
if joinKuerzel = PERMLS
searchResult := searchResult "LEFT JOIN ZLE.MLS_MITARBLEISTTAUSCHSATZ ON MLS_PER_FIRMENNR = PER_FIRMENNR AND MLS_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = MVAPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = MVA_PER_FIRMENNR AND PER_PERSONENID = MVA_PERSONENID" "~"
if joinKuerzel = PERMVA
searchResult := searchResult "LEFT JOIN FIBU.MVA_MAHNVZINSENAB ON MVA_PER_FIRMENNR = PER_FIRMENNR AND MVA_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = MVPPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = MVP_FIRMENNR AND PER_PERSONENID = MVP_PERSONENID" "~"
if joinKuerzel = PERMVP
searchResult := searchResult "LEFT JOIN BUERO.MVP_MITARBEITERVORLAGEPOS ON MVP_FIRMENNR = PER_FIRMENNR AND MVP_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PCLPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PCL_PER_FIRMENNR AND PER_PERSONENID = PCL_PERSONENID" "~"
if joinKuerzel = PERPCL
searchResult := searchResult "LEFT JOIN BUERO.PCL_PERSONCLOUDZU ON PCL_PER_FIRMENNR = PER_FIRMENNR AND PCL_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PIPPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PIP_PER_FIRMENNR AND PER_PERSONENID = PIP_PER_PERSONENID" "~"
if joinKuerzel = PERPIP
searchResult := searchResult "LEFT JOIN BUERO.PIP_PERSINFOPROTOKOLL ON PIP_PER_FIRMENNR = PER_FIRMENNR AND PIP_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PTZPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PTZ_KLI_FIRMENNR AND PER_PERSONENID = PTZ_KLI_KLID" "~"
if joinKuerzel = PERPTZ
searchResult := searchResult "LEFT JOIN WWS.PTZ_PREISTABZU ON PTZ_KLI_FIRMENNR = PER_FIRMENNR AND PTZ_KLI_KLID = PER_PERSONENID" "~"
if joinKuerzel = RVDPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = RVD_PER_FIRMENNR AND PER_PERSONENID = RVD_PER_PERSONENID" "~"
if joinKuerzel = PERRVD
searchResult := searchResult "LEFT JOIN ZLE.RVD_RECHVORLAGEDETAIL ON RVD_PER_FIRMENNR = PER_FIRMENNR AND RVD_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = SNRPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = SNR_AKT_BESITZER_FIRMENNR AND PER_PERSONENID = SNR_AKT_BESITZER_PERSONENID" "~"
if joinKuerzel = PERSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_AKT_BESITZER_FIRMENNR = PER_FIRMENNR AND SNR_AKT_BESITZER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = STLPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = STL_PER_FIRMENNR AND PER_PERSONENID = STL_PER_PERSONENID" "~"
if joinKuerzel = PERSTL
searchResult := searchResult "LEFT JOIN BUERO.STL_STELLE ON STL_PER_FIRMENNR = PER_FIRMENNR AND STL_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = STSPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = STS_FIR_FIRMENNR AND PER_PERSONENID = STS_PERSONENID" "~"
if joinKuerzel = PERSTS
searchResult := searchResult "LEFT JOIN BIL.STS_STEUERSTAMMDAT ON STS_FIR_FIRMENNR = PER_FIRMENNR AND STS_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = TLNPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = TLN_PER_FIRMENNR AND PER_PERSONENID = TLN_PERSONENID" "~"
if joinKuerzel = PERTLN
searchResult := searchResult "LEFT JOIN BUERO.TLN_TEILNEHMER ON TLN_PER_FIRMENNR = PER_FIRMENNR AND TLN_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = TSAPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = TSA_PER_FIRMENNR AND PER_PERSONENID = TSA_PERSONENID" "~"
if joinKuerzel = PERTSA
searchResult := searchResult "LEFT JOIN FIBU.TSA_TEILSCHLUSSRECHABLAUF ON TSA_PER_FIRMENNR = PER_FIRMENNR AND TSA_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = VOBPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = VOB_PER_FIRMENNR AND PER_PERSONENID = VOB_PER_PERSONENID" "~"
if joinKuerzel = PERVOB
searchResult := searchResult "LEFT JOIN PRODUKTION.VOB_VORGANGSBENACHR ON VOB_PER_FIRMENNR = PER_FIRMENNR AND VOB_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = VRGPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = VRG_PER_FIRMENNR AND PER_PERSONENID = VRG_PERSONENID" "~"
if joinKuerzel = PERVRG
searchResult := searchResult "LEFT JOIN BIL.VRG_VERLUSTANRECHNUNG ON VRG_PER_FIRMENNR = PER_FIRMENNR AND VRG_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = VVVPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = VVV_PER_FIRMENNR AND PER_PERSONENID = VVV_PERSONENID" "~"
if joinKuerzel = PERVVV
searchResult := searchResult "LEFT JOIN BIL.VVV_STEUERVERLUSTVV ON VVV_PER_FIRMENNR = PER_FIRMENNR AND VVV_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = VWAPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = VWA_PER_FIRMENNR AND PER_PERSONENID = VWA_PER_PERSONENID" "~"
if joinKuerzel = PERVWA
searchResult := searchResult "LEFT JOIN BUERO.VWA_VORSCHLAGSWERTEARCHIV ON VWA_PER_FIRMENNR = PER_FIRMENNR AND VWA_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = VZEPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = VZE_FIRMENNR AND PER_PERSONENID = VZE_PERSONENID" "~"
if joinKuerzel = PERVZE
searchResult := searchResult "LEFT JOIN BUERO.VZE_VORDIENSTZEITEN ON VZE_FIRMENNR = PER_FIRMENNR AND VZE_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = WSKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = WSK_VERTRETER_FIRMENNR AND PER_PERSONENID = WSK_VERTRETER_PERSONENID" "~"
if joinKuerzel = PERWSK
searchResult := searchResult "LEFT JOIN WWS.WSK_WWSKORESTATISTIK ON WSK_VERTRETER_FIRMENNR = PER_FIRMENNR AND WSK_VERTRETER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = LEIPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = LEI_POSTITNR" "~"
if joinKuerzel = PITLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = LUAPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = LUA_POSTITNR" "~"
if joinKuerzel = PITLUA
searchResult := searchResult "LEFT JOIN PRODUKTION.LUA_LAGER_UMS_ANGEFORDERT ON LUA_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = LUKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = LUK_POSTITNR" "~"
if joinKuerzel = PITLUK
searchResult := searchResult "LEFT JOIN PRODUKTION.LUK_LAGER_UMS_KOPF ON LUK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PBAPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PBA_POSTITNR" "~"
if joinKuerzel = PITPBA
searchResult := searchResult "LEFT JOIN PRODUKTION.PBA_PLAN_BAS_ALT ON PBA_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PCPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PCP_POSTITNR" "~"
if joinKuerzel = PITPCP
searchResult := searchResult "LEFT JOIN PRODUKTION.PCP_PPS_PRODUKT ON PCP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PIPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PIP_POSTITNR" "~"
if joinKuerzel = PITPIP
searchResult := searchResult "LEFT JOIN BUERO.PIP_PERSINFOPROTOKOLL ON PIP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PKSPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PKS_POSTITNR" "~"
if joinKuerzel = PITPKS
searchResult := searchResult "LEFT JOIN PRODUKTION.PKS_SOLL_KAPAZITAET ON PKS_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PLIPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PLI_POSTITNR" "~"
if joinKuerzel = PITPLI
searchResult := searchResult "LEFT JOIN BUERO.PLI_PIPELINE ON PLI_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PTSPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PTS_POSTITNR" "~"
if joinKuerzel = PITPTS
searchResult := searchResult "LEFT JOIN WWS.PTS_PREISTABPOSSTAFFEL ON PTS_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PVFPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PVF_POSTITNR" "~"
if joinKuerzel = PITPVF
searchResult := searchResult "LEFT JOIN BUERO.PVF_PERSONENVORERFASSUNG ON PVF_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PWXPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PWX_POSTITNR" "~"
if joinKuerzel = PITPWX
searchResult := searchResult "LEFT JOIN KORE.PWX_PLANWERTEEXT ON PWX_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = SCEPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = SCE_POSTITNR" "~"
if joinKuerzel = PITSCE
searchResult := searchResult "LEFT JOIN PRODUKTION.SCE_SCORECARD ON SCE_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = SCKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = SCK_POSTITNR" "~"
if joinKuerzel = PITSCK
searchResult := searchResult "LEFT JOIN PRODUKTION.SCK_SCKENNZAHL ON SCK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = SLKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = SLK_POSTITNR" "~"
if joinKuerzel = PITSLK
searchResult := searchResult "LEFT JOIN WWS.SLK_STUECKLISTENKOPF ON SLK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = SSKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = SSK_POSTITNR" "~"
if joinKuerzel = PITSSK
searchResult := searchResult "LEFT JOIN PRODUKTION.SSK_SAMMELSCHEIN_KOPF ON SSK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = STHPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = STH_POSTITNR" "~"
if joinKuerzel = PITSTH
searchResult := searchResult "LEFT JOIN PRODUKTION.STH_STAKE ON STH_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = TLPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = TLP_POSTITNR" "~"
if joinKuerzel = PITTLP
searchResult := searchResult "LEFT JOIN PRODUKTION.TLP_BT_LOGISTIK_POSITION ON TLP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = VRGPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = VRG_POSTITNR" "~"
if joinKuerzel = PITVRG
searchResult := searchResult "LEFT JOIN BIL.VRG_VERLUSTANRECHNUNG ON VRG_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = VVVPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = VVV_POSTITNR" "~"
if joinKuerzel = PITVVV
searchResult := searchResult "LEFT JOIN BIL.VVV_STEUERVERLUSTVV ON VVV_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = WKTPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = WKT_POSTITNR" "~"
if joinKuerzel = PITWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = XSEPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = XSE_POSTITNR" "~"
if joinKuerzel = PITXSE
searchResult := searchResult "LEFT JOIN BUERO.XSE_XML_STEUERERKL ON XSE_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PKBPKR
searchResult := searchResult "LEFT JOIN BIL.PKR_PERSKOSTEN_RST ON PKR_FIRMENNR = PKB_FIRMENNR AND PKR_PKRMODELL = PKB_MODELL AND PKR_PKRSTICHTAG = PKB_STICHTAG AND PKR_PKRRSTKZ = PKB_RSTKZ" "~"
if joinKuerzel = PKRPKB
searchResult := searchResult "LEFT JOIN BIL.PKB_PERSKORSTBUCHBELEGZU ON PKB_FIRMENNR = PKR_FIRMENNR AND PKB_MODELL = PKR_PKRMODELL AND PKB_STICHTAG = PKR_PKRSTICHTAG AND PKB_RSTKZ = PKR_PKRRSTKZ" "~"
if joinKuerzel = PERPKZ
searchResult := searchResult "LEFT JOIN BUERO.PKZ_PERSONKONTAKTZU ON PKZ_FIRMENNR = PER_FIRMENNR AND PKZ_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PKZPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PKZ_FIRMENNR AND PER_PERSONENID = PKZ_PERSONENID" "~"
if joinKuerzel = GSMPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_PCHARGENID = GSM_PCHARGENID" "~"
if joinKuerzel = PLCGSM
searchResult := searchResult "LEFT JOIN PRODUKTION.GSM_GUTSCHLECHTMENGE ON GSM_PCHARGENID = PLC_PCHARGENID" "~"
if joinKuerzel = PGIPGP
searchResult := searchResult "LEFT JOIN WWS.PGP_PICARTIKELGRP ON PGP_PICID = PGI_PICID AND PGP_PGPLFDNR = PGI_PGPLFDNR" "~"
if joinKuerzel = PGPPGI
searchResult := searchResult "LEFT JOIN WWS.PGI_PICARTIKELGRPITEM ON PGI_PICID = PGP_PICID AND PGI_PGPLFDNR = PGP_PGPLFDNR" "~"
if joinKuerzel = PIOPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_PICID = PIO_PICID AND PIA_PICARTIKELNR = PIO_PICARTIKELNR" "~"
if joinKuerzel = PIAPIO
searchResult := searchResult "LEFT JOIN WWS.PIO_PICARTIKELDOKZU ON PIO_PICID = PIA_PICID AND PIO_PICARTIKELNR = PIA_PICARTIKELNR" "~"
if joinKuerzel = PZGPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_PICID = PZG_PICID AND PIA_PICARTIKELNR = PZG_PICARTIKELNR" "~"
if joinKuerzel = PIAPZG
searchResult := searchResult "LEFT JOIN WWS.PZG_PICGROESSENZU ON PZG_PICID = PIA_PICID AND PZG_PICARTIKELNR = PIA_PICARTIKELNR" "~"
if joinKuerzel = PGOPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_PICID = PGO_PICID" "~"
if joinKuerzel = PICPGO
searchResult := searchResult "LEFT JOIN WWS.PGO_PICGROESSE ON PGO_PICID = PIC_PICID" "~"
if joinKuerzel = PIHPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_PICID = PIH_PICID" "~"
if joinKuerzel = PICPIH
searchResult := searchResult "LEFT JOIN WWS.PIH_PICSPRACHEZU ON PIH_PICID = PIC_PICID" "~"
if joinKuerzel = PIJPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_PICID = PIJ_PICID" "~"
if joinKuerzel = PICPIJ
searchResult := searchResult "LEFT JOIN WWS.PIJ_PICKUNDENGRZU ON PIJ_PICID = PIC_PICID" "~"
if joinKuerzel = PIRPII
searchResult := searchResult "LEFT JOIN WWS.PII_PICKUNDENGRUPPE ON PII_PICID = PIR_PICID AND PII_PIIHAUPTGRNR = PIR_PIIHAUPTGRNR AND PII_PIIUNTERGRNR = PIR_PIIUNTERGRNR" "~"
if joinKuerzel = PIIPIR
searchResult := searchResult "LEFT JOIN WWS.PIR_PICRABATTDETAIL ON PIR_PICID = PII_PICID AND PIR_PIIHAUPTGRNR = PII_PIIHAUPTGRNR AND PIR_PIIUNTERGRNR = PII_PIIUNTERGRNR" "~"
if joinKuerzel = ADDPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = ADD_POSTITNR" "~"
if joinKuerzel = PITADD
searchResult := searchResult "LEFT JOIN BUERO.ADD_AUSDRUCKMODELLDETAIL ON ADD_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = AGIPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = AGI_POSTITNR" "~"
if joinKuerzel = PITAGI
searchResult := searchResult "LEFT JOIN WWS.AGI_AGPER_INFOZU ON AGI_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = ALZPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = ALZ_POSTITNR" "~"
if joinKuerzel = PITALZ
searchResult := searchResult "LEFT JOIN WWS.ALZ_ALLERGENDATEN ON ALZ_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = ASKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = ASK_POSTITNR" "~"
if joinKuerzel = PITASK
searchResult := searchResult "LEFT JOIN ZLE.ASK_SORTIERKOPF ON ASK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = AWKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = AWK_POSTITNR" "~"
if joinKuerzel = PITAWK
searchResult := searchResult "LEFT JOIN BIL.AWK_AUSWERTUNGKOPF ON AWK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = BRCPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = BRC_POSTITNR" "~"
if joinKuerzel = PITBRC
searchResult := searchResult "LEFT JOIN BUERO.BRC_BEREICH ON BRC_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = BSGPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = BSG_POSTITNR" "~"
if joinKuerzel = PITBSG
searchResult := searchResult "LEFT JOIN BUERO.BSG_BESPRECHUNG ON BSG_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = BTWPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = BTW_POSTITNR" "~"
if joinKuerzel = PITBTW
searchResult := searchResult "LEFT JOIN BIL.BTW_BESCHEIDWERT ON BTW_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = BVOPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = BVO_POSTITNR" "~"
if joinKuerzel = PITBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = EPVPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = EPV_POSTITNR" "~"
if joinKuerzel = PITEPV
searchResult := searchResult "LEFT JOIN WWS.EPV_ERFASSPOSRECHVOR ON EPV_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = GEFPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = GEF_POSTITNR" "~"
if joinKuerzel = PITGEF
searchResult := searchResult "LEFT JOIN WWS.GEF_GEFAHRENGUTSTAMM ON GEF_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = GSAPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = GSA_POSTITNR" "~"
if joinKuerzel = PITGSA
searchResult := searchResult "LEFT JOIN WWS.GSA_GUTSCHEINAKTION ON GSA_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = IFRPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = IFR_POSTITNR" "~"
if joinKuerzel = PITIFR
searchResult := searchResult "LEFT JOIN HRS.IFR_IMPORTFELDREFERENZ ON IFR_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = IWVPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = IWV_POSTITNR" "~"
if joinKuerzel = PITIWV
searchResult := searchResult "LEFT JOIN BUERO.IWV_INTRASTATWV ON IWV_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = KAIPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = KAI_POSTITNR" "~"
if joinKuerzel = PITKAI
searchResult := searchResult "LEFT JOIN WWS.KAI_KNDARTIKELINFO ON KAI_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = KAKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = KAK_POSTITNR" "~"
if joinKuerzel = PITKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = KKAPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = KKA_POSTITNR" "~"
if joinKuerzel = PITKKA
searchResult := searchResult "LEFT JOIN BUERO.KKA_KREDITKARTENART ON KKA_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = KKZPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = KKZ_POSTITNR" "~"
if joinKuerzel = PITKKZ
searchResult := searchResult "LEFT JOIN BUERO.KKZ_KREDITKARTENKUNDENZU ON KKZ_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = ABSORT
searchResult := searchResult "LEFT JOIN ORT_ORT.ORT_ORT ON ORT_LANDNR = ABS_ORT_LANDNR AND ORT_PLZ = ABS_PLZ AND ORT_PLZUNTERNR = ABS_PLZUNTERNR" "~"
if joinKuerzel = ORTABS
searchResult := searchResult "LEFT JOIN HRS.ABS_ARBEITSSTAETTE ON ABS_ORT_LANDNR = ORT_LANDNR AND ABS_PLZ = ORT_PLZ AND ABS_PLZUNTERNR = ORT_PLZUNTERNR" "~"
if joinKuerzel = BONORT
searchResult := searchResult "LEFT JOIN ORT_ORT.ORT_ORT ON ORT_LANDNR = BON_ORT_LANDNR AND ORT_PLZ = BON_ORT_PLZ AND ORT_PLZUNTERNR = BON_ORT_PLZUNTERNR" "~"
if joinKuerzel = ORTBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_ORT_LANDNR = ORT_LANDNR AND BON_ORT_PLZ = ORT_PLZ AND BON_ORT_PLZUNTERNR = ORT_PLZUNTERNR" "~"
if joinKuerzel = KSTORT
searchResult := searchResult "LEFT JOIN ORT_ORT.ORT_ORT ON ORT_LANDNR = KST_LAN_SPESEN_LANDNR AND ORT_PLZ = KST_PLZ AND ORT_PLZUNTERNR = KST_PLZUNTERNR" "~"
if joinKuerzel = ORTKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_LAN_SPESEN_LANDNR = ORT_LANDNR AND KST_PLZ = ORT_PLZ AND KST_PLZUNTERNR = ORT_PLZUNTERNR" "~"
if joinKuerzel = TOZORT
searchResult := searchResult "LEFT JOIN ORT_ORT.ORT_ORT ON ORT_LANDNR = TOZ_ORT_LANDNR AND ORT_PLZ = TOZ_BIS_PLZ AND ORT_PLZUNTERNR = TOZ_BIS_PLZUNTERNR" "~"
if joinKuerzel = ORTTOZ
searchResult := searchResult "LEFT JOIN WWS.TOZ_TOURZUORDNUNG ON TOZ_ORT_LANDNR = ORT_LANDNR AND TOZ_BIS_PLZ = ORT_PLZ AND TOZ_BIS_PLZUNTERNR = ORT_PLZUNTERNR" "~"
if joinKuerzel = OPLOST
searchResult := searchResult "LEFT JOIN LOHN.OST_OF_STAAT ON OST_STAATENNR = OPL_STAATENNR" "~"
if joinKuerzel = OSTOPL
searchResult := searchResult "LEFT JOIN LOHN.OPL_OF_PLZ ON OPL_STAATENNR = OST_STAATENNR" "~"
if joinKuerzel = OROOST
searchResult := searchResult "LEFT JOIN LOHN.OST_OF_STAAT ON OST_STAATENNR = ORO_STAATENNR" "~"
if joinKuerzel = OSTORO
searchResult := searchResult "LEFT JOIN LOHN.ORO_OF_REGORT ON ORO_STAATENNR = OST_STAATENNR" "~"
if joinKuerzel = EPRPAB
searchResult := searchResult "LEFT JOIN WWS.PAB_PROVARTBAUM ON PAB_FIRMENNR = EPR_PAB_FIRMENNR AND PAB_PABLFDNR = EPR_PAB_PABLFDNR" "~"
if joinKuerzel = PABEPR
searchResult := searchResult "LEFT JOIN WWS.EPR_ERFASSPROVISION ON EPR_PAB_FIRMENNR = PAB_FIRMENNR AND EPR_PAB_PABLFDNR = PAB_PABLFDNR" "~"
if joinKuerzel = AAPPAC
searchResult := searchResult "LEFT JOIN WWS.PAC_PACKSTOFF ON PAC_FIRMENNR = AAP_PAC_FIRMENNR AND PAC_PACKSTOFFNR = AAP_PACKSTOFFNR" "~"
if joinKuerzel = PACAAP
searchResult := searchResult "LEFT JOIN WWS.AAP_ARAPOSITION ON AAP_PAC_FIRMENNR = PAC_FIRMENNR AND AAP_PACKSTOFFNR = PAC_PACKSTOFFNR" "~"
if joinKuerzel = ARLPAC
searchResult := searchResult "LEFT JOIN WWS.PAC_PACKSTOFF ON PAC_FIRMENNR = ARL_FIRMENNR AND PAC_PACKSTOFFNR = ARL_EAG_PACKSTOFFNR" "~"
if joinKuerzel = PACARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = PAC_FIRMENNR AND ARL_EAG_PACKSTOFFNR = PAC_PACKSTOFFNR" "~"
if joinKuerzel = WAGPAC
searchResult := searchResult "LEFT JOIN WWS.PAC_PACKSTOFF ON PAC_FIRMENNR = WAG_FIRMENNR AND PAC_PACKSTOFFNR = WAG_EAG_PACKSTOFFNR" "~"
if joinKuerzel = PACWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = PAC_FIRMENNR AND WAG_EAG_PACKSTOFFNR = PAC_PACKSTOFFNR" "~"
if joinKuerzel = DPMPAD
searchResult := searchResult "LEFT JOIN BMDDEFAULT.PAD_PARAMETERDEF ON PAD_PARAMETERDEFNR = DPM_PARAMETERDEFNR" "~"
if joinKuerzel = PADDPM
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DPM_DFUNKTIONSPARAM ON DPM_PARAMETERDEFNR = PAD_PARAMETERDEFNR" "~"
if joinKuerzel = PBSPBC
searchResult := searchResult "LEFT JOIN PRODUKTION.PBC_CODEPAGE ON PBC_CODEPAGE = PBS_CODEPAGE" "~"
if joinKuerzel = PBCPBS
searchResult := searchResult "LEFT JOIN PRODUKTION.PBS_BEFEHL ON PBS_CODEPAGE = PBC_CODEPAGE" "~"
if joinKuerzel = AGLPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_PPSKAPAID = AGL_AG_ID" "~"
if joinKuerzel = PCKAGL
searchResult := searchResult "LEFT JOIN PRODUKTION.AGL_ARBEITSGANGZU ON AGL_AG_ID = PCK_PPSKAPAID" "~"
if joinKuerzel = AWSPCK
searchResult := searchResult "LEFT JOIN PCK_PPS_KAPA.PCK_PPS_KAPA ON PCK_PPSKAPAID = AWS_PCK_PPSKAPAID" "~"
if joinKuerzel = PCKAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_PCK_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = DISPCK
searchResult := searchResult "LEFT JOIN PCK_PPS_KAPA.PCK_PPS_KAPA ON PCK_PPSKAPAID = DIS_AG_ID" "~"
if joinKuerzel = PCKDIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_AG_ID = PCK_PPSKAPAID" "~"
if joinKuerzel = PCWPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_PPSKAPAID = PCW_PPSKAPAID" "~"
if joinKuerzel = PCKPCW
searchResult := searchResult "LEFT JOIN PRODUKTION.PCW_PPSKAPAWERT ON PCW_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = PCKPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_PPSKAPAID = PCK_AG_SPLITT_PARENT_ID" "~"
if joinKuerzel = PCKPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_AG_SPLITT_PARENT_ID = PCK_PPSKAPAID" "~"
if joinKuerzel = LAJPCP
searchResult := searchResult "LEFT JOIN PCP_PPS_PRODUKT.PCP_PPS_PRODUKT ON PCP_PCHARGENID = LAJ_PCHARGENID AND PCP_PPSPROD_LFDNR = LAJ_PPSPROD_LFDNR" "~"
if joinKuerzel = PCPLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_PCHARGENID = PCP_PCHARGENID AND LAJ_PPSPROD_LFDNR = PCP_PPSPROD_LFDNR" "~"
if joinKuerzel = WAPPCP
searchResult := searchResult "LEFT JOIN PCP_PPS_PRODUKT.PCP_PPS_PRODUKT ON PCP_PCHARGENID = WAP_PCHARGENID AND PCP_PPSPROD_LFDNR = WAP_PPSPROD_LFDNR" "~"
if joinKuerzel = PCPWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_PCHARGENID = PCP_PCHARGENID AND WAP_PPSPROD_LFDNR = PCP_PPSPROD_LFDNR" "~"
if joinKuerzel = AFTPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = AFT_PER_FIRMENNR AND PER_PERSONENID = AFT_PERSONENID" "~"
if joinKuerzel = PERAFT
searchResult := searchResult "LEFT JOIN ZLE.AFT_AUFBEREITUNG ON AFT_PER_FIRMENNR = PER_FIRMENNR AND AFT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = AGMPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = AGM_PER_FIRMENNR AND PER_PERSONENID = AGM_PER_PERSONENID" "~"
if joinKuerzel = PERAGM
searchResult := searchResult "LEFT JOIN PRODUKTION.AGM_AG_PERSON_ZU ON AGM_PER_FIRMENNR = PER_FIRMENNR AND AGM_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = AGUPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = AGU_LIEFERANT_FIRMENNR AND PER_PERSONENID = AGU_LIEFERANT_PERSONENID" "~"
if joinKuerzel = PERAGU
searchResult := searchResult "LEFT JOIN BIL.AGU_ANLAGEGUT ON AGU_LIEFERANT_FIRMENNR = PER_FIRMENNR AND AGU_LIEFERANT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = SNRPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = SNR_POSTITNR" "~"
if joinKuerzel = PITSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = SRTPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = SRT_POSTITNR" "~"
if joinKuerzel = PITSRT
searchResult := searchResult "LEFT JOIN WWS.SRT_STEUERTAB ON SRT_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = TLKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = TLK_POSTITNR" "~"
if joinKuerzel = PITTLK
searchResult := searchResult "LEFT JOIN PRODUKTION.TLK_BT_LOGISTIK_KOPF ON TLK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = TLTPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = TLT_POSTITNR" "~"
if joinKuerzel = PITTLT
searchResult := searchResult "LEFT JOIN PRODUKTION.TLT_BT_LOGISTIK_TRANSPORT ON TLT_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = TMOPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = TMO_POSTITNR" "~"
if joinKuerzel = PITTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = ULEPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = ULE_POSTITNR" "~"
if joinKuerzel = PITULE
searchResult := searchResult "LEFT JOIN BMDINTERN.ULE_UPDATELEBENSLAUF ON ULE_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = UMFPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = UMF_POSTITNR" "~"
if joinKuerzel = PITUMF
searchResult := searchResult "LEFT JOIN BUERO.UMF_UMFRAGE ON UMF_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = UVWPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = UVW_POSTITNR" "~"
if joinKuerzel = PITUVW
searchResult := searchResult "LEFT JOIN FIBU.UVW_UVAWERT ON UVW_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = VASPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = VAS_POSTITNR" "~"
if joinKuerzel = PITVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = VETPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = VET_POSTITNR" "~"
if joinKuerzel = PITVET
searchResult := searchResult "LEFT JOIN BUERO.VET_VERANSTALTUNGSTAG ON VET_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = VZEPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = VZE_POSTITNR" "~"
if joinKuerzel = PITVZE
searchResult := searchResult "LEFT JOIN BUERO.VZE_VORDIENSTZEITEN ON VZE_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = WEZPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = WEZ_POSTITNR" "~"
if joinKuerzel = PITWEZ
searchResult := searchResult "LEFT JOIN PRODUKTION.WEZ_WERKZEUGBAZZU ON WEZ_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = WTBPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = WTB_POSTITNR" "~"
if joinKuerzel = PITWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = EFKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = EFK_ERFABSCHL_POSTITNR" "~"
if joinKuerzel = PITEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_ERFABSCHL_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = ERBPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = ERB_KOSTENDATEN_NOTIZ" "~"
if joinKuerzel = PITERB
searchResult := searchResult "LEFT JOIN FIBU.ERB_BUCHUNG ON ERB_KOSTENDATEN_NOTIZ = PIT_POSTITNR" "~"
if joinKuerzel = PSWPKM
searchResult := searchResult "LEFT JOIN PKM_KM_MASTER.PKM_KM_MASTER ON PKM_FIRMENNR = PSW_PKM_FIRMENNR AND PKM_KM_NR = PSW_KM_NR" "~"
if joinKuerzel = PKMPSW
searchResult := searchResult "LEFT JOIN BUERO.PSW_PERSONENWERTE ON PSW_PKM_FIRMENNR = PKM_FIRMENNR AND PSW_KM_NR = PKM_KM_NR" "~"
if joinKuerzel = LAJPLC
searchResult := searchResult "LEFT JOIN PLC_PPS_CHARGE.PLC_PPS_CHARGE ON PLC_PCHARGENID = LAJ_PCHARGENID" "~"
if joinKuerzel = PLCLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_PCHARGENID = PLC_PCHARGENID" "~"
if joinKuerzel = ZMBPLG
searchResult := searchResult "LEFT JOIN BUERO.PLG_PLANGRUPPE ON PLG_FIRMENNR = ZMB_PLG_FIRMENNR AND PLG_PLANGRLFDNR = ZMB_PLG_PLANGRLFDNR" "~"
if joinKuerzel = PLGZMB
searchResult := searchResult "LEFT JOIN BUERO.ZMB_ZEITMINDESTBESETZUNG ON ZMB_PLG_FIRMENNR = PLG_FIRMENNR AND ZMB_PLG_PLANGRLFDNR = PLG_PLANGRLFDNR" "~"
if joinKuerzel = OPEOPL
searchResult := searchResult "LEFT JOIN LOHN.OPL_OF_PLZ ON OPL_STAATENNR = OPE_LAND_STAATENNR AND OPL_OFPLZ = OPE_PERSPLZ" "~"
if joinKuerzel = OPLOPE
searchResult := searchResult "LEFT JOIN LOHN.OPE_OF_PERSON ON OPE_LAND_STAATENNR = OPL_STAATENNR AND OPE_PERSPLZ = OPL_OFPLZ" "~"
if joinKuerzel = KJOPOS
searchResult := searchResult "LEFT JOIN BUERO.POS_POST ON POS_POSTNR = KJO_POSTNR AND POS_POSTKZ = KJO_POSTKZ" "~"
if joinKuerzel = POSKJO
searchResult := searchResult "LEFT JOIN BUERO.KJO_KONTAKTJOURNAL ON KJO_POSTNR = POS_POSTNR AND KJO_POSTKZ = POS_POSTKZ" "~"
if joinKuerzel = BCIPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = BCI_PROJEKT_FIRMENNR AND PRO_PROJEKTNR = BCI_PROJEKTNR AND PRO_PROJEKTTYP = BCI_PROJEKTTYP" "~"
if joinKuerzel = PROBCI
searchResult := searchResult "LEFT JOIN BMDENTW.BCI_BMDCONSTINTERN ON BCI_PROJEKT_FIRMENNR = PRO_FIRMENNR AND BCI_PROJEKTNR = PRO_PROJEKTNR AND BCI_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = EPSPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = EPS_PRO_FIRMENNR AND PRO_PROJEKTNR = EPS_PROJEKTNR AND PRO_PROJEKTTYP = EPS_PROJEKTTYP" "~"
if joinKuerzel = PROEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_PRO_FIRMENNR = PRO_FIRMENNR AND EPS_PROJEKTNR = PRO_PROJEKTNR AND EPS_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = KAKPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = KAK_PRO_FIRMENNR AND PRO_PROJEKTNR = KAK_PRO_PROJEKTNR AND PRO_PROJEKTTYP = KAK_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_PRO_FIRMENNR = PRO_FIRMENNR AND KAK_PRO_PROJEKTNR = PRO_PROJEKTNR AND KAK_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = SAIPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = SAI_POSTITNR" "~"
if joinKuerzel = PITSAI
searchResult := searchResult "LEFT JOIN WWS.SAI_SAISON ON SAI_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = SCOPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = SCO_POSTITNR" "~"
if joinKuerzel = PITSCO
searchResult := searchResult "LEFT JOIN PRODUKTION.SCO_SCINSTANZ ON SCO_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = SSPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = SSP_POSTITNR" "~"
if joinKuerzel = PITSSP
searchResult := searchResult "LEFT JOIN PRODUKTION.SSP_SAMMELSCHEIN_POS ON SSP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = STYPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = STY_POSTITNR" "~"
if joinKuerzel = PITSTY
searchResult := searchResult "LEFT JOIN ONVIS.STY_STOFFTYPEN ON STY_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = SZZPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = SZZ_POSTITNR" "~"
if joinKuerzel = PITSZZ
searchResult := searchResult "LEFT JOIN PRODUKTION.SZZ_STAKE_ZU ON SZZ_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = TOZPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = TOZ_POSTITNR" "~"
if joinKuerzel = PITTOZ
searchResult := searchResult "LEFT JOIN WWS.TOZ_TOURZUORDNUNG ON TOZ_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = UEVPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = UEV_POSTITNR" "~"
if joinKuerzel = PITUEV
searchResult := searchResult "LEFT JOIN BIL.UEV_STEUERUEBERGVERLUSTE ON UEV_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = VAKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = VAK_POSTITNR" "~"
if joinKuerzel = PITVAK
searchResult := searchResult "LEFT JOIN BUERO.VAK_VERANSTALTUNGSKOPF ON VAK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = VATPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = VAT_POSTITNR" "~"
if joinKuerzel = PITVAT
searchResult := searchResult "LEFT JOIN BUERO.VAT_TEILNAHME ON VAT_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = VFDPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = VFD_POSTITNR" "~"
if joinKuerzel = PITVFD
searchResult := searchResult "LEFT JOIN PRODUKTION.VFD_VERFAHRENSANWEISUNG_POS ON VFD_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = VPEPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = VPE_POSTITNR" "~"
if joinKuerzel = PITVPE
searchResult := searchResult "LEFT JOIN WWS.VPE_VPI_ERHOEHUNG ON VPE_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = VPWPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = VPW_POSTITNR" "~"
if joinKuerzel = PITVPW
searchResult := searchResult "LEFT JOIN BUERO.VPW_VPIWERTE ON VPW_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = WAKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = WAK_POSTITNR" "~"
if joinKuerzel = PITWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = WANPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = WAN_POSTITNR" "~"
if joinKuerzel = PITWAN
searchResult := searchResult "LEFT JOIN ONVIS.WAN_WARENNUMMERN ON WAN_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = WRPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = WRP_POSTITNR" "~"
if joinKuerzel = PITWRP
searchResult := searchResult "LEFT JOIN WWS.WRP_WWSANKAUFSRNGPOS ON WRP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = WVTPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = WVT_POSTITNR" "~"
if joinKuerzel = PITWVT
searchResult := searchResult "LEFT JOIN WWS.WVT_WARTUNGSVERTRAG ON WVT_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = XSPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = XSP_POSTITNR" "~"
if joinKuerzel = PITXSP
searchResult := searchResult "LEFT JOIN BUERO.XSP_XML_STEUERERKL_PAKET ON XSP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = ZBAPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = ZBA_POSTITNR" "~"
if joinKuerzel = PITZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = AVEPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = AVE_POSITION_POSTITNR" "~"
if joinKuerzel = PITAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_POSITION_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PKDPKD
searchResult := searchResult "LEFT JOIN PRODUKTION.PKD_KM_DETAIL ON PKD_KM_DETAIL_ID = PKD_KM_DETAIL_ID_REF" "~"
if joinKuerzel = PKDPKD
searchResult := searchResult "LEFT JOIN PRODUKTION.PKD_KM_DETAIL ON PKD_KM_DETAIL_ID_REF = PKD_KM_DETAIL_ID" "~"
if joinKuerzel = PKSPKD
searchResult := searchResult "LEFT JOIN PRODUKTION.PKD_KM_DETAIL ON PKD_KM_DETAIL_ID = PKS_KM_DETAIL_ID_RAHMEN" "~"
if joinKuerzel = PKDPKS
searchResult := searchResult "LEFT JOIN PRODUKTION.PKS_SOLL_KAPAZITAET ON PKS_KM_DETAIL_ID_RAHMEN = PKD_KM_DETAIL_ID" "~"
if joinKuerzel = PKRPKH
searchResult := searchResult "LEFT JOIN BIL.PKH_PERSKORSTHEADER ON PKH_FIRMENNR = PKR_FIRMENNR AND PKH_PKHMODELL = PKR_PKRMODELL AND PKH_PKHSTICHTAG = PKR_PKRSTICHTAG" "~"
if joinKuerzel = PKHPKR
searchResult := searchResult "LEFT JOIN BIL.PKR_PERSKOSTEN_RST ON PKR_FIRMENNR = PKH_FIRMENNR AND PKR_PKRMODELL = PKH_PKHMODELL AND PKR_PKRSTICHTAG = PKH_PKHSTICHTAG" "~"
if joinKuerzel = PPZPLG
searchResult := searchResult "LEFT JOIN BUERO.PLG_PLANGRUPPE ON PLG_FIRMENNR = PPZ_PLG_FIRMENNR AND PLG_PLANGRLFDNR = PPZ_PLG_PLANGRLFDNR" "~"
if joinKuerzel = PLGPPZ
searchResult := searchResult "LEFT JOIN BUERO.PPZ_PLANGRUPPEPROZESSZU ON PPZ_PLG_FIRMENNR = PLG_FIRMENNR AND PPZ_PLG_PLANGRLFDNR = PLG_PLANGRLFDNR" "~"
if joinKuerzel = ZPTPLG
searchResult := searchResult "LEFT JOIN BUERO.PLG_PLANGRUPPE ON PLG_FIRMENNR = ZPT_PLG_FIRMENNR AND PLG_PLANGRLFDNR = ZPT_PLANGRLFDNR" "~"
if joinKuerzel = PLGZPT
searchResult := searchResult "LEFT JOIN BUERO.ZPT_ZEITPLANTEXT ON ZPT_PLG_FIRMENNR = PLG_FIRMENNR AND ZPT_PLANGRLFDNR = PLG_PLANGRLFDNR" "~"
if joinKuerzel = PROPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PRO_KONTAKT_FIRMENNR AND PER_PERSONENID = PRO_KONTAKT_PERSONENID" "~"
if joinKuerzel = PERPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_KONTAKT_FIRMENNR = PER_FIRMENNR AND PRO_KONTAKT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = TERPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = TER_KONTAKT_FIRMENNR AND PER_PERSONENID = TER_KONTAKT_PERSONENID" "~"
if joinKuerzel = PERTER
searchResult := searchResult "LEFT JOIN BUERO.TER_TERMIN ON TER_KONTAKT_FIRMENNR = PER_FIRMENNR AND TER_KONTAKT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = TRJPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = TRJ_PER_FIRMENNR AND PER_PERSONENID = TRJ_PER_PERSONENID" "~"
if joinKuerzel = PERTRJ
searchResult := searchResult "LEFT JOIN WWS.TRJ_TRANSPORTEINH_JOURNAL ON TRJ_PER_FIRMENNR = PER_FIRMENNR AND TRJ_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = FSTPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = FST_KONTAKT_FIRMENNR AND PER_PERSONENID = FST_KONTAKT_PERSONENID" "~"
if joinKuerzel = PERFST
searchResult := searchResult "LEFT JOIN BUERO.FST_FRIST ON FST_KONTAKT_FIRMENNR = PER_FIRMENNR AND FST_KONTAKT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = KAKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = KAK_ZRECHNUNGEMPF_FIRMENNR AND PER_PERSONENID = KAK_ZRECHNUNGEMPF_KLID" "~"
if joinKuerzel = PERKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_ZRECHNUNGEMPF_FIRMENNR = PER_FIRMENNR AND KAK_ZRECHNUNGEMPF_KLID = PER_PERSONENID" "~"
if joinKuerzel = KUKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = KUK_VERTR2_FIRMENNR AND PER_PERSONENID = KUK_VERTR2_PERSONENID" "~"
if joinKuerzel = PERKUK
searchResult := searchResult "LEFT JOIN WWS.KUK_KUNDENKARTEI ON KUK_VERTR2_FIRMENNR = PER_FIRMENNR AND KUK_VERTR2_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = TPRPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = TPR_KON_FIRMENNR AND PER_PERSONENID = TPR_KON_KONTAKTPERSID" "~"
if joinKuerzel = PERTPR
searchResult := searchResult "LEFT JOIN BUERO.TPR_TELEFONPROTOKOLL ON TPR_KON_FIRMENNR = PER_FIRMENNR AND TPR_KON_KONTAKTPERSID = PER_PERSONENID" "~"
if joinKuerzel = VASPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = VAS_VOR_FIRMENNR AND PER_PERSONENID = VAS_VOR_PERSONENID" "~"
if joinKuerzel = PERVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_VOR_FIRMENNR = PER_FIRMENNR AND VAS_VOR_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = YBUPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = YBU_V2_FIRMENNR AND PER_PERSONENID = YBU_V2_PERSONENID" "~"
if joinKuerzel = PERYBU
searchResult := searchResult "LEFT JOIN FIBU.YBU_AUTOBUCHUNG ON YBU_V2_FIRMENNR = PER_FIRMENNR AND YBU_V2_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = AGSPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = AGS_KONTAKTPERSON_FIRMENNR AND PER_PERSONENID = AGS_KONTAKTPERSON_PERID" "~"
if joinKuerzel = PERAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_KONTAKTPERSON_FIRMENNR = PER_FIRMENNR AND AGS_KONTAKTPERSON_PERID = PER_PERSONENID" "~"
if joinKuerzel = EVTPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = EVT_ORT_FIRMENNR AND PER_PERSONENID = EVT_ORT_PERSONENID" "~"
if joinKuerzel = PEREVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_ORT_FIRMENNR = PER_FIRMENNR AND EVT_ORT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PROPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PRO_V2_FIRMENNR AND PER_PERSONENID = PRO_V2_PERSONENID" "~"
if joinKuerzel = PERPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_V2_FIRMENNR = PER_FIRMENNR AND PRO_V2_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = KLIPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = KLI_V4_FIRMENNR AND PER_PERSONENID = KLI_V4_PERSONENID" "~"
if joinKuerzel = PERKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_V4_FIRMENNR = PER_FIRMENNR AND KLI_V4_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = BPKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = BPK_ZRECHEMPF_KONT_FIRMENNR AND PER_PERSONENID = BPK_ZRECHEMPF_KONTAKTPERSID" "~"
if joinKuerzel = PERBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_ZRECHEMPF_KONT_FIRMENNR = PER_FIRMENNR AND BPK_ZRECHEMPF_KONTAKTPERSID = PER_PERSONENID" "~"
if joinKuerzel = BPKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = BPK_RECHLEG_FIRMENNR AND PER_PERSONENID = BPK_RECHLEG_KLID" "~"
if joinKuerzel = PERBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_RECHLEG_FIRMENNR = PER_FIRMENNR AND BPK_RECHLEG_KLID = PER_PERSONENID" "~"
if joinKuerzel = BPKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = BPK_SPEDITION_FIRMENNR AND PER_PERSONENID = BPK_SPEDITION_KON_PERSID" "~"
if joinKuerzel = PERBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_SPEDITION_FIRMENNR = PER_FIRMENNR AND BPK_SPEDITION_KON_PERSID = PER_PERSONENID" "~"
if joinKuerzel = AVEPET
searchResult := searchResult "LEFT JOIN WWS.PET_PREISTAB ON PET_FIRMENNR = AVE_PET_FIRMENNR AND PET_PREISTABNR = AVE_PREISTABNR" "~"
if joinKuerzel = PETAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_PET_FIRMENNR = PET_FIRMENNR AND AVE_PREISTABNR = PET_PREISTABNR" "~"
if joinKuerzel = PFEPFI
searchResult := searchResult "LEFT JOIN BMD.PFI_PANELFILTER ON PFI_PFILTERNR = PFE_PFILTERNR" "~"
if joinKuerzel = PFIPFE
searchResult := searchResult "LEFT JOIN BMD.PFE_PANELFILTERELEM ON PFE_PFILTERNR = PFI_PFILTERNR" "~"
if joinKuerzel = GAUPGE
searchResult := searchResult "LEFT JOIN PGE_PERSONGESELLZU.PGE_PERSONGESELLZU ON PGE_KUN_FIRMENNR = GAU_KUN_FIRMENNR AND PGE_KUN_PERSONENID = GAU_KUN_PERSONENID AND PGE_GES_FIRMENNR = GAU_GES_FIRMENNR AND PGE_GES_PERSONENID = GAU_GES_PERSONENID" "~"
if joinKuerzel = PGEGAU
searchResult := searchResult "LEFT JOIN BIL.GAU_GEWINNAUFTEILUNG ON GAU_KUN_FIRMENNR = PGE_KUN_FIRMENNR AND GAU_KUN_PERSONENID = PGE_KUN_PERSONENID AND GAU_GES_FIRMENNR = PGE_GES_FIRMENNR AND GAU_GES_PERSONENID = PGE_GES_PERSONENID" "~"
if joinKuerzel = PGIPGI
searchResult := searchResult "LEFT JOIN WWS.PGI_PICARTIKELGRPITEM ON PGI_PICID = PGI_PICID AND PGI_PGPLFDNR = PGI_PGPLFDNR AND PGI_PGILFDNR = PGI_PGI_PGILFDNR" "~"
if joinKuerzel = PGIPGI
searchResult := searchResult "LEFT JOIN WWS.PGI_PICARTIKELGRPITEM ON PGI_PICID = PGI_PICID AND PGI_PGPLFDNR = PGI_PGPLFDNR AND PGI_PGI_PGILFDNR = PGI_PGILFDNR" "~"
if joinKuerzel = KOZPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_PCHARGENID = KOZ_PCHARGENID" "~"
if joinKuerzel = PLCKOZ
searchResult := searchResult "LEFT JOIN PRODUKTION.KOZ_KOMMISSIONIERUNG_ZU ON KOZ_PCHARGENID = PLC_PCHARGENID" "~"
if joinKuerzel = RPMPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_PCHARGENID = RPM_PCHARGENID" "~"
if joinKuerzel = PLCRPM
searchResult := searchResult "LEFT JOIN PRODUKTION.RPM_RETRO_PRODUKT_MATERIAL ON RPM_PCHARGENID = PLC_PCHARGENID" "~"
if joinKuerzel = ZPLPLG
searchResult := searchResult "LEFT JOIN BUERO.PLG_PLANGRUPPE ON PLG_FIRMENNR = ZPL_PLG_FIRMENNR AND PLG_PLANGRLFDNR = ZPL_PLG_PLANGRLFDNR" "~"
if joinKuerzel = PLGZPL
searchResult := searchResult "LEFT JOIN BUERO.ZPL_ZEITPLANUNG ON ZPL_PLG_FIRMENNR = PLG_FIRMENNR AND ZPL_PLG_PLANGRLFDNR = PLG_PLANGRLFDNR" "~"
if joinKuerzel = PLTPLI
searchResult := searchResult "LEFT JOIN BUERO.PLI_PIPELINE ON PLI_PLIID = PLT_PLIID" "~"
if joinKuerzel = PLIPLT
searchResult := searchResult "LEFT JOIN BUERO.PLT_PIPELINESTUFE ON PLT_PLIID = PLI_PLIID" "~"
if joinKuerzel = AWEPLP
searchResult := searchResult "LEFT JOIN PLP_PLANUNGSPERIODE.PLP_PLANUNGSPERIODE ON PLP_FIRMENNR = AWE_PLP_FIRMENNR AND PLP_PLANPERID = AWE_PLP_PLANPERID" "~"
if joinKuerzel = PLPAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_PLP_FIRMENNR = PLP_FIRMENNR AND AWE_PLP_PLANPERID = PLP_PLANPERID" "~"
if joinKuerzel = LEDPLT
searchResult := searchResult "LEFT JOIN BUERO.PLT_PIPELINESTUFE ON PLT_PLTID = LED_PLTID" "~"
if joinKuerzel = PLTLED
searchResult := searchResult "LEFT JOIN BUERO.LED_LEAD ON LED_PLTID = PLT_PLTID" "~"
if joinKuerzel = HFTPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = HFT_PRO_FIRMENNR AND PRO_PROJEKTNR = HFT_PROJEKTNR AND PRO_PROJEKTTYP = HFT_PROJEKTTYP" "~"
if joinKuerzel = PROHFT
searchResult := searchResult "LEFT JOIN BUERO.HFT_HILFSTABELLE ON HFT_PRO_FIRMENNR = PRO_FIRMENNR AND HFT_PROJEKTNR = PRO_PROJEKTNR AND HFT_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = HPTPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = HPT_PRO_FIRMENNR AND PRO_PROJEKTNR = HPT_PROJEKTNR AND PRO_PROJEKTTYP = HPT_PROJEKTTYP" "~"
if joinKuerzel = PROHPT
searchResult := searchResult "LEFT JOIN ZLE.HPT_HONORARPOSTEXTE ON HPT_PRO_FIRMENNR = PRO_FIRMENNR AND HPT_PROJEKTNR = PRO_PROJEKTNR AND HPT_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = IFAPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = IFA_PRO_FIRMENNR AND PRO_PROJEKTNR = IFA_PROJEKTNR AND PRO_PROJEKTTYP = IFA_PROJEKTTYP" "~"
if joinKuerzel = PROIFA
searchResult := searchResult "LEFT JOIN WWSINDIV.IFA_FAKTURA ON IFA_PRO_FIRMENNR = PRO_FIRMENNR AND IFA_PROJEKTNR = PRO_PROJEKTNR AND IFA_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = KJOPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = KJO_PRO_FIRMENNR AND PRO_PROJEKTNR = KJO_PROJEKTNR AND PRO_PROJEKTTYP = KJO_PROJEKTTYP" "~"
if joinKuerzel = PROKJO
searchResult := searchResult "LEFT JOIN BUERO.KJO_KONTAKTJOURNAL ON KJO_PRO_FIRMENNR = PRO_FIRMENNR AND KJO_PROJEKTNR = PRO_PROJEKTNR AND KJO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = LTSPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = LTS_PRO_FIRMENNR AND PRO_PROJEKTNR = LTS_PROJEKTNR AND PRO_PROJEKTTYP = LTS_PROJEKTTYP" "~"
if joinKuerzel = PROLTS
searchResult := searchResult "LEFT JOIN ZLE.LTS_LEISTTAUSCHSATZ ON LTS_PRO_FIRMENNR = PRO_FIRMENNR AND LTS_PROJEKTNR = PRO_PROJEKTNR AND LTS_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = MLSPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = MLS_PRO_FIRMENNR AND PRO_PROJEKTNR = MLS_PRO_PROJEKTNR AND PRO_PROJEKTTYP = MLS_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROMLS
searchResult := searchResult "LEFT JOIN ZLE.MLS_MITARBLEISTTAUSCHSATZ ON MLS_PRO_FIRMENNR = PRO_FIRMENNR AND MLS_PRO_PROJEKTNR = PRO_PROJEKTNR AND MLS_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = REIPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = REI_PRO_FIRMENNR AND PRO_PROJEKTNR = REI_PRO_PROJEKTNR AND PRO_PROJEKTTYP = REI_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROREI
searchResult := searchResult "LEFT JOIN HRS.REI_REISESTAMM ON REI_PRO_FIRMENNR = PRO_FIRMENNR AND REI_PRO_PROJEKTNR = PRO_PROJEKTNR AND REI_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = SPFPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = SPF_FIRMENNR AND PRO_PROJEKTNR = SPF_PROJEKTNR AND PRO_PROJEKTTYP = SPF_PROJEKTTYP" "~"
if joinKuerzel = PROSPF
searchResult := searchResult "LEFT JOIN BMDENTW.SPF_SCRPROJFILEZU ON SPF_FIRMENNR = PRO_FIRMENNR AND SPF_PROJEKTNR = PRO_PROJEKTNR AND SPF_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = WOLPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = WOL_PRO_FIRMENNR AND PRO_PROJEKTNR = WOL_PRO_PROJEKTNR AND PRO_PROJEKTTYP = WOL_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROWOL
searchResult := searchResult "LEFT JOIN ZLE.WOL_WOCHENLEISTUNG ON WOL_PRO_FIRMENNR = PRO_FIRMENNR AND WOL_PRO_PROJEKTNR = PRO_PROJEKTNR AND WOL_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = PROPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = PRO_FOLGEIST_FIRMENNR AND PRO_PROJEKTNR = PRO_FOLGEIST_PROJEKTNR AND PRO_PROJEKTTYP = PRO_FOLGEIST_PROJEKTTYP" "~"
if joinKuerzel = PROPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FOLGEIST_FIRMENNR = PRO_FIRMENNR AND PRO_FOLGEIST_PROJEKTNR = PRO_PROJEKTNR AND PRO_FOLGEIST_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = RPRPRP
searchResult := searchResult "LEFT JOIN WWS.PRP_PROVISIONTABPOS ON PRP_PROVTABPOSID = RPR_PROVTABPOSID" "~"
if joinKuerzel = PRPRPR
searchResult := searchResult "LEFT JOIN WWS.RPR_RECHNUNGSPROVISION ON RPR_PROVTABPOSID = PRP_PROVTABPOSID" "~"
if joinKuerzel = AFTPRS
searchResult := searchResult "LEFT JOIN PRS_PROZESS.PRS_PROZESS ON PRS_FIRMENNR = AFT_PRS_FIRMENNR AND PRS_PROZESSID = AFT_PRS_PROZESSID" "~"
if joinKuerzel = PRSAFT
searchResult := searchResult "LEFT JOIN ZLE.AFT_AUFBEREITUNG ON AFT_PRS_FIRMENNR = PRS_FIRMENNR AND AFT_PRS_PROZESSID = PRS_PROZESSID" "~"
if joinKuerzel = PCKPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_FIRMENNR = PCK_PRS_FIRMENNR AND PRS_PROZESSID = PCK_PROZESSID" "~"
if joinKuerzel = PRSPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_PRS_FIRMENNR = PRS_FIRMENNR AND PCK_PROZESSID = PRS_PROZESSID" "~"
if joinKuerzel = PCKPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_FIRMENNR = PCK_PRS_FIRMENNR AND PRS_PROZESSID = PCK_NEXT_PROZESSID" "~"
if joinKuerzel = PRSPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_PRS_FIRMENNR = PRS_FIRMENNR AND PCK_NEXT_PROZESSID = PRS_PROZESSID" "~"
if joinKuerzel = WTEPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = WTE_TN_FIRMENNR AND PER_PERSONENID = WTE_TN_PERSONENID" "~"
if joinKuerzel = PERWTE
searchResult := searchResult "LEFT JOIN BUERO.WTE_WEBTEILNEHMER ON WTE_TN_FIRMENNR = PER_FIRMENNR AND WTE_TN_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = ZBRPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = ZBR_PER_FIRMENNR AND PER_PERSONENID = ZBR_PERSONENID" "~"
if joinKuerzel = PERZBR
searchResult := searchResult "LEFT JOIN FIBU.ZBR_ZINSBERECHNUNG ON ZBR_PER_FIRMENNR = PER_FIRMENNR AND ZBR_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = EVGPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = EVG_ORT_FIRMENNR AND PER_PERSONENID = EVG_ORT_PERSONENID" "~"
if joinKuerzel = PEREVG
searchResult := searchResult "LEFT JOIN BUERO.EVG_EVENTTAGE ON EVG_ORT_FIRMENNR = PER_FIRMENNR AND EVG_ORT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = EVTPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = EVT_LIEF_FIRMENNR AND PER_PERSONENID = EVT_LIEF_PERSONENID" "~"
if joinKuerzel = PEREVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_LIEF_FIRMENNR = PER_FIRMENNR AND EVT_LIEF_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = LEIPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = LEI_KONZERN_FIRMENNR AND PER_PERSONENID = LEI_KONZERN_PERSONENID" "~"
if joinKuerzel = PERLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_KONZERN_FIRMENNR = PER_FIRMENNR AND LEI_KONZERN_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = SNRPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = SNR_LIEFERANT_FIRMENNR AND PER_PERSONENID = SNR_LIEFERANT_PERSONENID" "~"
if joinKuerzel = PERSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_LIEFERANT_FIRMENNR = PER_FIRMENNR AND SNR_LIEFERANT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = VATPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = VAT_KONT_FIRMENNR AND PER_PERSONENID = VAT_KONT_PERSONENID" "~"
if joinKuerzel = PERVAT
searchResult := searchResult "LEFT JOIN BUERO.VAT_TEILNAHME ON VAT_KONT_FIRMENNR = PER_FIRMENNR AND VAT_KONT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = BPKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = BPK_LIEFERANT_KONT_FIRMENNR AND PER_PERSONENID = BPK_LIEFERANT_KONTAKTPERSID" "~"
if joinKuerzel = PERBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_LIEFERANT_KONT_FIRMENNR = PER_FIRMENNR AND BPK_LIEFERANT_KONTAKTPERSID = PER_PERSONENID" "~"
if joinKuerzel = BRVPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = BRV_PER_FIRMENNR AND PER_PERSONENID = BRV_PER_PERSONENID" "~"
if joinKuerzel = PERBRV
searchResult := searchResult "LEFT JOIN FIBU.BRV_BANKRUECKVERBUCHUNG ON BRV_PER_FIRMENNR = PER_FIRMENNR AND BRV_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PERPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PER_PERSKONZERN_FIRMENNR AND PER_PERSONENID = PER_PERSKONZERN_PERSONENID" "~"
if joinKuerzel = PERPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_PERSKONZERN_FIRMENNR = PER_FIRMENNR AND PER_PERSKONZERN_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = HNPPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = HNP_KONTAKT_FIRMENNR AND PER_PERSONENID = HNP_KONTAKT_PERSONENID" "~"
if joinKuerzel = PERHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_KONTAKT_FIRMENNR = PER_FIRMENNR AND HNP_KONTAKT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = KLIPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = KLI_V3_FIRMENNR AND PER_PERSONENID = KLI_V3_PERSONENID" "~"
if joinKuerzel = PERKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_V3_FIRMENNR = PER_FIRMENNR AND KLI_V3_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PROPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PRO_V4_FIRMENNR AND PER_PERSONENID = PRO_V4_PERSONENID" "~"
if joinKuerzel = PERPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_V4_FIRMENNR = PER_FIRMENNR AND PRO_V4_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = WBTPET
searchResult := searchResult "LEFT JOIN WWS.PET_PREISTAB ON PET_FIRMENNR = WBT_PET_AUFW_FIRMENNR AND PET_PREISTABNR = WBT_PET_AUFW_PREISTABNR" "~"
if joinKuerzel = PETWBT
searchResult := searchResult "LEFT JOIN WWS.WBT_WWSBUDGETKOPF ON WBT_PET_AUFW_FIRMENNR = PET_FIRMENNR AND WBT_PET_AUFW_PREISTABNR = PET_PREISTABNR" "~"
if joinKuerzel = GFZPGE
searchResult := searchResult "LEFT JOIN PGE_PERSONGESELLZU.PGE_PERSONGESELLZU ON PGE_KUN_FIRMENNR = GFZ_PGE_GES_FIRMENNR AND PGE_KUN_PERSONENID = GFZ_PGE_KUN_PERSONENID AND PGE_GES_FIRMENNR = GFZ_PGE_KUN_FIRMENNR AND PGE_GES_PERSONENID = GFZ_PGE_GES_PERSONENID" "~"
if joinKuerzel = PGEGFZ
searchResult := searchResult "LEFT JOIN BIL.GFZ_XMLJABGESFIRZU ON GFZ_PGE_GES_FIRMENNR = PGE_KUN_FIRMENNR AND GFZ_PGE_KUN_PERSONENID = PGE_KUN_PERSONENID AND GFZ_PGE_KUN_FIRMENNR = PGE_GES_FIRMENNR AND GFZ_PGE_GES_PERSONENID = PGE_GES_PERSONENID" "~"
if joinKuerzel = PIRPGO
searchResult := searchResult "LEFT JOIN WWS.PGO_PICGROESSE ON PGO_PICID = PIR_PICID AND PGO_PICGROESSENKBEZ = PIR_PICGROESSENKBEZ" "~"
if joinKuerzel = PGOPIR
searchResult := searchResult "LEFT JOIN WWS.PIR_PICRABATTDETAIL ON PIR_PICID = PGO_PICID AND PIR_PICGROESSENKBEZ = PGO_PICGROESSENKBEZ" "~"
if joinKuerzel = PAVPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_PICID = PAV_PIA_PICID AND PIA_PICARTIKELNR = PAV_PIA_PICARTIKELNR" "~"
if joinKuerzel = PIAPAV
searchResult := searchResult "LEFT JOIN WWS.PAV_PICALTERNATIVARTIKEL ON PAV_PIA_PICID = PIA_PICID AND PAV_PIA_PICARTIKELNR = PIA_PICARTIKELNR" "~"
if joinKuerzel = PIAPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_PICID = PIA_PICID" "~"
if joinKuerzel = PICPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_PICID = PIC_PICID" "~"
if joinKuerzel = PICPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_PICID = PIC_DELTA_PICID" "~"
if joinKuerzel = PICPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_DELTA_PICID = PIC_PICID" "~"
if joinKuerzel = PIFPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_PICID = PIF_PICID" "~"
if joinKuerzel = PICPIF
searchResult := searchResult "LEFT JOIN WWS.PIF_PICKUNDENZU ON PIF_PICID = PIC_PICID" "~"
if joinKuerzel = KRAPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = KRA_POSTITNR" "~"
if joinKuerzel = PITKRA
searchResult := searchResult "LEFT JOIN WWS.KRA_KASSENRABATT ON KRA_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = KTOPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = KTO_BILANZ_POSTITNR" "~"
if joinKuerzel = PITKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_BILANZ_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = LEDPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = LED_POSTITNR" "~"
if joinKuerzel = PITLED
searchResult := searchResult "LEFT JOIN BUERO.LED_LEAD ON LED_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PCKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PCK_POSTITNR" "~"
if joinKuerzel = PITPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PERPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PER_POSTITNR" "~"
if joinKuerzel = PITPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PLPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PLP_POSTITNR" "~"
if joinKuerzel = PITPLP
searchResult := searchResult "LEFT JOIN PRODUKTION.PLP_PLANUNGSPERIODE ON PLP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PMDPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PMD_POSTITNR" "~"
if joinKuerzel = PITPMD
searchResult := searchResult "LEFT JOIN BUERO.PMD_PAUSENMODELL ON PMD_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = POSPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = POS_POSTITNR" "~"
if joinKuerzel = PITPOS
searchResult := searchResult "LEFT JOIN BUERO.POS_POST ON POS_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PSJPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PSJ_POSTITNR" "~"
if joinKuerzel = PITPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PTPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PTP_POSTITNR" "~"
if joinKuerzel = PITPTP
searchResult := searchResult "LEFT JOIN WWS.PTP_PREISTABPOS ON PTP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PUSPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PUS_POSTITNR" "~"
if joinKuerzel = PITPUS
searchResult := searchResult "LEFT JOIN PRODUKTION.PUS_PPSUSERSTATI ON PUS_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = RIAPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = RIA_POSTITNR" "~"
if joinKuerzel = PITRIA
searchResult := searchResult "LEFT JOIN PRODUKTION.RIA_RISIKOANALYSE ON RIA_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = RIHPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = RIH_POSTITNR" "~"
if joinKuerzel = PITRIH
searchResult := searchResult "LEFT JOIN PRODUKTION.RIH_RISIKOANALYSE_HEAD ON RIH_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = RMOPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = RMO_POSTITNR" "~"
if joinKuerzel = PITRMO
searchResult := searchResult "LEFT JOIN BUERO.RMO_RUNDUNGSMODELL ON RMO_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = RZSPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = RZS_POSTITNR" "~"
if joinKuerzel = PITRZS
searchResult := searchResult "LEFT JOIN BUERO.RZS_RUECKZAHLUNGSSTAMM ON RZS_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = SA1PIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = SA1_POSTITNR" "~"
if joinKuerzel = PITSA1
searchResult := searchResult "LEFT JOIN ONVIS.SA1_SAURER_BDE ON SA1_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = SCIPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = SCI_POSTITNR" "~"
if joinKuerzel = PITSCI
searchResult := searchResult "LEFT JOIN PRODUKTION.SCI_SCINSTANZDET ON SCI_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = TRSPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = TRS_POSTITNR" "~"
if joinKuerzel = PITTRS
searchResult := searchResult "LEFT JOIN BUERO.TRS_TEILSCHLUSSRECHSCHEMA ON TRS_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = WAPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = WAP_POSTITNR" "~"
if joinKuerzel = PITWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = WEPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = WEP_POSTITNR" "~"
if joinKuerzel = PITWEP
searchResult := searchResult "LEFT JOIN WWS.WEP_WWSERKONTROLLEPOS ON WEP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = XMAPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = XMA_POSTITNR" "~"
if joinKuerzel = PITXMA
searchResult := searchResult "LEFT JOIN BUERO.XMA_MITARBEITER ON XMA_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = ZAWPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = ZAW_POSTITNR" "~"
if joinKuerzel = PITZAW
searchResult := searchResult "LEFT JOIN BUERO.ZAW_ZEITAUSWERTUNG ON ZAW_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = ZITPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = ZIT_POSTITNR" "~"
if joinKuerzel = PITZIT
searchResult := searchResult "LEFT JOIN BUERO.ZIT_ZUSATZINFOTYP ON ZIT_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = ZSAPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = ZSA_POSTITNR" "~"
if joinKuerzel = PITZSA
searchResult := searchResult "LEFT JOIN BUERO.ZSA_ZEITSALDEN ON ZSA_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PKSPKD
searchResult := searchResult "LEFT JOIN PRODUKTION.PKD_KM_DETAIL ON PKD_KM_DETAIL_ID = PKS_KM_DETAIL_ID_MODELL" "~"
if joinKuerzel = PKDPKS
searchResult := searchResult "LEFT JOIN PRODUKTION.PKS_SOLL_KAPAZITAET ON PKS_KM_DETAIL_ID_MODELL = PKD_KM_DETAIL_ID" "~"
if joinKuerzel = MITPKM
searchResult := searchResult "LEFT JOIN PKM_KM_MASTER.PKM_KM_MASTER ON PKM_FIRMENNR = MIT_PKM_FIRMENNR AND PKM_KM_NR = MIT_KM_NR" "~"
if joinKuerzel = PKMMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_PKM_FIRMENNR = PKM_FIRMENNR AND MIT_KM_NR = PKM_KM_NR" "~"
if joinKuerzel = ZUWPLG
searchResult := searchResult "LEFT JOIN BUERO.PLG_PLANGRUPPE ON PLG_FIRMENNR = ZUW_PLG_FIRMENNR AND PLG_PLANGRLFDNR = ZUW_PLANGRLFDNR" "~"
if joinKuerzel = PLGZUW
searchResult := searchResult "LEFT JOIN BUERO.ZUW_ZEITUMSATZWERTE ON ZUW_PLG_FIRMENNR = PLG_FIRMENNR AND ZUW_PLANGRLFDNR = PLG_PLANGRLFDNR" "~"
if joinKuerzel = PLDPLS
searchResult := searchResult "LEFT JOIN WWS.PLS_PICALLERGEN ON PLS_PICID = PLD_PICID AND PLS_PLSMODUS = PLD_PLSMODUS AND PLS_ALLERGENNR = PLD_ALLERGENNR" "~"
if joinKuerzel = PLSPLD
searchResult := searchResult "LEFT JOIN WWS.PLD_PICALLERGENDATEN ON PLD_PICID = PLS_PICID AND PLD_PLSMODUS = PLS_PLSMODUS AND PLD_ALLERGENNR = PLS_ALLERGENNR" "~"
if joinKuerzel = AGSPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_PROJEKTID = AGS_PROJEKTID" "~"
if joinKuerzel = PROAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_PROJEKTID = PRO_PROJEKTID" "~"
if joinKuerzel = AVGPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = AVG_PRO_FIRMENNR AND PRO_PROJEKTNR = AVG_PRO_PROJEKTNR AND PRO_PROJEKTTYP = AVG_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROAVG
searchResult := searchResult "LEFT JOIN WWS.AVG_AUFTRAGSVORERFGRUPP ON AVG_PRO_FIRMENNR = PRO_FIRMENNR AND AVG_PRO_PROJEKTNR = PRO_PROJEKTNR AND AVG_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = BSGPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = BSG_PRO_FIRMENNR AND PRO_PROJEKTNR = BSG_PROJEKTNR AND PRO_PROJEKTTYP = BSG_PROJEKTTYP" "~"
if joinKuerzel = PROBSG
searchResult := searchResult "LEFT JOIN BUERO.BSG_BESPRECHUNG ON BSG_PRO_FIRMENNR = PRO_FIRMENNR AND BSG_PROJEKTNR = PRO_PROJEKTNR AND BSG_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = BVRPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = BVR_PRO_FIRMENNR AND PRO_PROJEKTNR = BVR_PRO_PROJEKTNR AND PRO_PROJEKTTYP = BVR_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROBVR
searchResult := searchResult "LEFT JOIN FIBU.BVR_BANKRVREGELN ON BVR_PRO_FIRMENNR = PRO_FIRMENNR AND BVR_PRO_PROJEKTNR = PRO_PROJEKTNR AND BVR_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = CLAPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = CLA_FIRMENNR AND PRO_PROJEKTNR = CLA_PROJEKTNR AND PRO_PROJEKTTYP = CLA_PROJEKTTYP" "~"
if joinKuerzel = PROCLA
searchResult := searchResult "LEFT JOIN BUERO.CLA_CLAUSWAHLFELD ON CLA_FIRMENNR = PRO_FIRMENNR AND CLA_PROJEKTNR = PRO_PROJEKTNR AND CLA_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = FPOPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = FPO_PRO_FIRMENNR AND PRO_PROJEKTNR = FPO_PROJEKTNR AND PRO_PROJEKTTYP = FPO_PROJEKTTYP" "~"
if joinKuerzel = PROFPO
searchResult := searchResult "LEFT JOIN BUERO.FPO_FAVPROJEKTZU ON FPO_PRO_FIRMENNR = PRO_FIRMENNR AND FPO_PROJEKTNR = PRO_PROJEKTNR AND FPO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = HKPPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_PROJEKTID = HKP_PROJEKTID" "~"
if joinKuerzel = PROHKP
searchResult := searchResult "LEFT JOIN BMDENTW.HKP_HILFEKAPITEL ON HKP_PROJEKTID = PRO_PROJEKTID" "~"
if joinKuerzel = KPPPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = KPP_PRO_FIRMENNR AND PRO_PROJEKTNR = KPP_PROJEKTNR AND PRO_PROJEKTTYP = KPP_PROJEKTTYP" "~"
if joinKuerzel = PROKPP
searchResult := searchResult "LEFT JOIN BUERO.KPP_KAMPAGNENPHASE ON KPP_PRO_FIRMENNR = PRO_FIRMENNR AND KPP_PROJEKTNR = PRO_PROJEKTNR AND KPP_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = LAPPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = LAP_PRO_FIRMENNR AND PRO_PROJEKTNR = LAP_PRO_PROJEKTNR AND PRO_PROJEKTTYP = LAP_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_PRO_FIRMENNR = PRO_FIRMENNR AND LAP_PRO_PROJEKTNR = PRO_PROJEKTNR AND LAP_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = POSPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = POS_PRO_FIRMENNR AND PRO_PROJEKTNR = POS_PROJEKTNR AND PRO_PROJEKTTYP = POS_PROJEKTTYP" "~"
if joinKuerzel = PROPOS
searchResult := searchResult "LEFT JOIN BUERO.POS_POST ON POS_PRO_FIRMENNR = PRO_FIRMENNR AND POS_PROJEKTNR = PRO_PROJEKTNR AND POS_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = PPAPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_PROJEKTID = PPA_PROJEKTID" "~"
if joinKuerzel = PROPPA
searchResult := searchResult "LEFT JOIN PRODUKTION.PPA_PSPPARAM ON PPA_PROJEKTID = PRO_PROJEKTID" "~"
if joinKuerzel = PRWPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = PRW_PRO_FIRMENNR AND PRO_PROJEKTNR = PRW_PROJEKTNR AND PRO_PROJEKTTYP = PRW_PROJEKTTYP" "~"
if joinKuerzel = PROPRW
searchResult := searchResult "LEFT JOIN BUERO.PRW_PROJEKTWERT ON PRW_PRO_FIRMENNR = PRO_FIRMENNR AND PRW_PROJEKTNR = PRO_PROJEKTNR AND PRW_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = PVZPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = PVZ_PRO_FIRMENNR AND PRO_PROJEKTNR = PVZ_PRO_PROJEKTNR AND PRO_PROJEKTTYP = PVZ_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROPVZ
searchResult := searchResult "LEFT JOIN WWS.PVZ_PROVISIONTABZU ON PVZ_PRO_FIRMENNR = PRO_FIRMENNR AND PVZ_PRO_PROJEKTNR = PRO_PROJEKTNR AND PVZ_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = QAWPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = QAW_PRO_FIRMENNR AND PRO_PROJEKTNR = QAW_PROJEKTNR AND PRO_PROJEKTTYP = QAW_PROJEKTTYP" "~"
if joinKuerzel = PROQAW
searchResult := searchResult "LEFT JOIN BUERO.QAW_QUOTENAUSWERTUNG ON QAW_PRO_FIRMENNR = PRO_FIRMENNR AND QAW_PROJEKTNR = PRO_PROJEKTNR AND QAW_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = RIAPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_PROJEKTID = RIA_PROJEKTID" "~"
if joinKuerzel = PRORIA
searchResult := searchResult "LEFT JOIN PRODUKTION.RIA_RISIKOANALYSE ON RIA_PROJEKTID = PRO_PROJEKTID" "~"
if joinKuerzel = TBAPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = TBA_PRO_FIRMENNR AND PRO_PROJEKTNR = TBA_PRO_PROJEKTNR AND PRO_PROJEKTTYP = TBA_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROTBA
searchResult := searchResult "LEFT JOIN BUERO.TBA_TAGESMDBUCHARTAUFB ON TBA_PRO_FIRMENNR = PRO_FIRMENNR AND TBA_PRO_PROJEKTNR = PRO_PROJEKTNR AND TBA_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = VAKPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = VAK_PRO_FIRMENNR AND PRO_PROJEKTNR = VAK_PRO_PROJEKTNR AND PRO_PROJEKTTYP = VAK_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROVAK
searchResult := searchResult "LEFT JOIN BUERO.VAK_VERANSTALTUNGSKOPF ON VAK_PRO_FIRMENNR = PRO_FIRMENNR AND VAK_PRO_PROJEKTNR = PRO_PROJEKTNR AND VAK_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = BEIPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = BEI_PER_FIRMENNR AND PER_PERSONENID = BEI_PER_PERSONENID" "~"
if joinKuerzel = PERBEI
searchResult := searchResult "LEFT JOIN BUERO.BEI_BEIFAHRER ON BEI_PER_FIRMENNR = PER_FIRMENNR AND BEI_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = BPKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = BPK_SPEDITION_FIRMENNR AND PER_PERSONENID = BPK_SPEDITION_EXTPERSID" "~"
if joinKuerzel = PERBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_SPEDITION_FIRMENNR = PER_FIRMENNR AND BPK_SPEDITION_EXTPERSID = PER_PERSONENID" "~"
if joinKuerzel = BRVPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = BRV_V1_FIRMENNR AND PER_PERSONENID = BRV_V1_PERSONENID" "~"
if joinKuerzel = PERBRV
searchResult := searchResult "LEFT JOIN FIBU.BRV_BANKRUECKVERBUCHUNG ON BRV_V1_FIRMENNR = PER_FIRMENNR AND BRV_V1_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = BSGPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = BSG_PER_FIRMENNR AND PER_PERSONENID = BSG_PERSONENID" "~"
if joinKuerzel = PERBSG
searchResult := searchResult "LEFT JOIN BUERO.BSG_BESPRECHUNG ON BSG_PER_FIRMENNR = PER_FIRMENNR AND BSG_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = EVNPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = EVN_FIRMENNR AND PER_PERSONENID = EVN_PERSONENID" "~"
if joinKuerzel = PEREVN
searchResult := searchResult "LEFT JOIN BUERO.EVN_TEILNEHMER ON EVN_FIRMENNR = PER_FIRMENNR AND EVN_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = GE2PER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = GE2_FIRMENNR AND PER_PERSONENID = GE2_PERSONENID" "~"
if joinKuerzel = PERGE2
searchResult := searchResult "LEFT JOIN BIL.GE2_GEB2RECHTSGESCHAEFT ON GE2_FIRMENNR = PER_FIRMENNR AND GE2_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = GEGPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = GEG_PER_FIRMENNR AND PER_PERSONENID = GEG_PERSONENID" "~"
if joinKuerzel = PERGEG
searchResult := searchResult "LEFT JOIN ZLE.GEG_GEGENSTANDSWERTE ON GEG_PER_FIRMENNR = PER_FIRMENNR AND GEG_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = GIDPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = GID_FIR_FIRMENNR AND PER_PERSONENID = GID_PERSONENID" "~"
if joinKuerzel = PERGID
searchResult := searchResult "LEFT JOIN BUERO.GID_GELDIDENTIFIKATION ON GID_FIR_FIRMENNR = PER_FIRMENNR AND GID_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = GVMPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = GVM_FIRMENNR AND PER_PERSONENID = GVM_PERSONENID" "~"
if joinKuerzel = PERGVM
searchResult := searchResult "LEFT JOIN BUERO.GVM_GHOSTVOLLMACHT ON GVM_FIRMENNR = PER_FIRMENNR AND GVM_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = LKPPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = LKP_PER_FIRMENNR AND PER_PERSONENID = LKP_PER_PERSONENID" "~"
if joinKuerzel = PERLKP
searchResult := searchResult "LEFT JOIN BMDINTERN.LKP_LIZENZKUNDENVKPZU ON LKP_PER_FIRMENNR = PER_FIRMENNR AND LKP_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = LPRPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = LPR_FIRMENNR AND PER_PERSONENID = LPR_PERSONENID" "~"
if joinKuerzel = PERLPR
searchResult := searchResult "LEFT JOIN ZLE.LPR_LEAPERSON ON LPR_FIRMENNR = PER_FIRMENNR AND LPR_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = LPZPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = LPZ_FIRMENNR AND PER_PERSONENID = LPZ_PERSONENID" "~"
if joinKuerzel = PERLPZ
searchResult := searchResult "LEFT JOIN BUERO.LPZ_LIZENZKEYPERSONZU ON LPZ_FIRMENNR = PER_FIRMENNR AND LPZ_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = MKRPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = MKR_SACHBEARB_FIRMENNR AND PER_PERSONENID = MKR_SACHBEARB_PERSID" "~"
if joinKuerzel = PERMKR
searchResult := searchResult "LEFT JOIN FIBU.MKR_MAHNKREIS ON MKR_SACHBEARB_FIRMENNR = PER_FIRMENNR AND MKR_SACHBEARB_PERSID = PER_PERSONENID" "~"
if joinKuerzel = NEGPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = NEG_FIRMENNR AND PER_PERSONENID = NEG_PERSONENID" "~"
if joinKuerzel = PERNEG
searchResult := searchResult "LEFT JOIN BIL.NEG_NICHTENTGEWINNE ON NEG_FIRMENNR = PER_FIRMENNR AND NEG_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PBRPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PBR_FIRMENNR AND PER_PERSONENID = PBR_PERSONENID" "~"
if joinKuerzel = PERPBR
searchResult := searchResult "LEFT JOIN BUERO.PBR_PERSONBRANCHEZU ON PBR_FIRMENNR = PER_FIRMENNR AND PBR_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PGMPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PGM_FIRMENNR AND PER_PERSONENID = PGM_PERSONENID" "~"
if joinKuerzel = PERPGM
searchResult := searchResult "LEFT JOIN BUERO.PGM_PERSONGEMEINDEZU ON PGM_FIRMENNR = PER_FIRMENNR AND PGM_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PVGPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PVG_PER_FIRMENNR AND PER_PERSONENID = PVG_PERSONENID" "~"
if joinKuerzel = PERPVG
searchResult := searchResult "LEFT JOIN BUERO.PVG_PERSONVORERFASSANGEH ON PVG_PER_FIRMENNR = PER_FIRMENNR AND PVG_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = SCBPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = SCB_FIRMENNR AND PER_PERSONENID = SCB_PERSONENID" "~"
if joinKuerzel = PERSCB
searchResult := searchResult "LEFT JOIN BMDENTW.SCB_SCRPERSZU ON SCB_FIRMENNR = PER_FIRMENNR AND SCB_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = STHPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = STH_PER_FIRMENNR AND PER_PERSONENID = STH_PER_PERSONENID" "~"
if joinKuerzel = PERSTH
searchResult := searchResult "LEFT JOIN PRODUKTION.STH_STAKE ON STH_PER_FIRMENNR = PER_FIRMENNR AND STH_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = VSRPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = VSR_PERSON_FIRMENNR AND PER_PERSONENID = VSR_PERSON_PERSID" "~"
if joinKuerzel = PERVSR
searchResult := searchResult "LEFT JOIN FIBU.VSR_VSTERSTATTUNG ON VSR_PERSON_FIRMENNR = PER_FIRMENNR AND VSR_PERSON_PERSID = PER_PERSONENID" "~"
if joinKuerzel = WAKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = WAK_SPEDITION_FIRMENNR AND PER_PERSONENID = WAK_SPEDITION_PERSONENID" "~"
if joinKuerzel = PERWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_SPEDITION_FIRMENNR = PER_FIRMENNR AND WAK_SPEDITION_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PIDPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_PICID = PID_PICID AND PIA_PICARTIKELNR = PID_PICARTIKELNR" "~"
if joinKuerzel = PIAPID
searchResult := searchResult "LEFT JOIN WWS.PID_PICPREISDETAIL ON PID_PICID = PIA_PICID AND PID_PICARTIKELNR = PIA_PICARTIKELNR" "~"
if joinKuerzel = PDGPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_PICID = PDG_PICID" "~"
if joinKuerzel = PICPDG
searchResult := searchResult "LEFT JOIN WWS.PDG_PICDYNAMICGROUP ON PDG_PICID = PIC_PICID" "~"
if joinKuerzel = PIDPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_PICID = PID_PICID" "~"
if joinKuerzel = PICPID
searchResult := searchResult "LEFT JOIN WWS.PID_PICPREISDETAIL ON PID_PICID = PIC_PICID" "~"
if joinKuerzel = PIGPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_PICID = PIG_PICID" "~"
if joinKuerzel = PICPIG
searchResult := searchResult "LEFT JOIN WWS.PIG_PICPREISGRUPPE ON PIG_PICID = PIC_PICID" "~"
if joinKuerzel = PWKPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_PICID = PWK_PICID" "~"
if joinKuerzel = PICPWK
searchResult := searchResult "LEFT JOIN WWS.PWK_PREISRABATTWARTKOPF ON PWK_PICID = PIC_PICID" "~"
if joinKuerzel = PIRPIG
searchResult := searchResult "LEFT JOIN WWS.PIG_PICPREISGRUPPE ON PIG_PICID = PIR_PICID AND PIG_PIGHAUPTGRNR = PIR_PIGHAUPTGRNR AND PIG_PIGUNTERGRNR = PIR_PIGUNTERGRNR" "~"
if joinKuerzel = PIGPIR
searchResult := searchResult "LEFT JOIN WWS.PIR_PICRABATTDETAIL ON PIR_PICID = PIG_PICID AND PIR_PIGHAUPTGRNR = PIG_PIGHAUPTGRNR AND PIR_PIGUNTERGRNR = PIG_PIGUNTERGRNR" "~"
if joinKuerzel = PIRPIL
searchResult := searchResult "LEFT JOIN WWS.PIL_PICKUNDE ON PIL_PILID = PIR_PILID" "~"
if joinKuerzel = PILPIR
searchResult := searchResult "LEFT JOIN WWS.PIR_PICRABATTDETAIL ON PIR_PILID = PIL_PILID" "~"
if joinKuerzel = ADOPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = ADO_POSTITNR" "~"
if joinKuerzel = PITADO
searchResult := searchResult "LEFT JOIN BUERO.ADO_AUSDRUCKMODELL ON ADO_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = BETPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = BET_POSTITNR" "~"
if joinKuerzel = PITBET
searchResult := searchResult "LEFT JOIN BUERO.BET_BETRIEBSMITTEL ON BET_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = BPBPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = BPB_POSTITNR" "~"
if joinKuerzel = PITBPB
searchResult := searchResult "LEFT JOIN WWS.BPB_LIEFBESTAET ON BPB_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = DIAPIT
searchResult := searchResult "LEFT JOIN BMD.PIT_POSTIT ON PIT_POSTITNR = DIA_POSTITNR" "~"
if joinKuerzel = PITDIA
searchResult := searchResult "LEFT JOIN BMD.DIA_DIAGRAMM ON DIA_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = DNEPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = DNE_POSTITNR" "~"
if joinKuerzel = PITDNE
searchResult := searchResult "LEFT JOIN DATANORM.DNE_ERSTELLER ON DNE_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = DTZPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = DTZ_POSTITNR" "~"
if joinKuerzel = PITDTZ
searchResult := searchResult "LEFT JOIN DATANORM.DTZ_DATANORM_TEXTZU ON DTZ_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = EFKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = EFK_POSTITNR" "~"
if joinKuerzel = PITEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = EINPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = EIN_POSTITNR" "~"
if joinKuerzel = PITEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = EKJPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = EKJ_POSTITNR" "~"
if joinKuerzel = PITEKJ
searchResult := searchResult "LEFT JOIN PRODUKTION.EKJ_ERP_KOREJOURNAL ON EKJ_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = ERBPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = ERB_POSTITNR" "~"
if joinKuerzel = PITERB
searchResult := searchResult "LEFT JOIN FIBU.ERB_BUCHUNG ON ERB_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = EVNPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = EVN_POSTITNR" "~"
if joinKuerzel = PITEVN
searchResult := searchResult "LEFT JOIN BUERO.EVN_TEILNEHMER ON EVN_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = IDPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = IDP_POSTITNR" "~"
if joinKuerzel = PITIDP
searchResult := searchResult "LEFT JOIN PRODUKTION.IDP_INFO_DELAY_BREAKDOWN ON IDP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = IFAPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = IFA_POSTITNR" "~"
if joinKuerzel = PITIFA
searchResult := searchResult "LEFT JOIN WWSINDIV.IFA_FAKTURA ON IFA_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = KASPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = KAS_POSTITNR" "~"
if joinKuerzel = PITKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = KDPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = KDP_POSTITNR" "~"
if joinKuerzel = PITKDP
searchResult := searchResult "LEFT JOIN KORE.KDP_PLANWERTE ON KDP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = KKKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = KKK_POSTITNR" "~"
if joinKuerzel = PITKKK
searchResult := searchResult "LEFT JOIN WWS.KKK_KREDITKARTENKONTENZU ON KKK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = KSTPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = KST_POSTITNR" "~"
if joinKuerzel = PITKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = LATPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = LAT_POSTITNR" "~"
if joinKuerzel = LATPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = LAT_KLI_FIRMENNR AND PER_PERSONENID = LAT_KLI_KLID" "~"
if joinKuerzel = PITLAT
searchResult := searchResult "LEFT JOIN WWS.LAT_LIEFERANTENARTIKEL ON LAT_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = LAWPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = LAW_POSTITNR" "~"
if joinKuerzel = PITLAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PDZPRW
searchResult := searchResult "LEFT JOIN BUERO.PRW_PROJEKTWERT ON PRW_PRO_FIRMENNR = PDZ_PRW_PRO_FIRMENNR AND PRW_PTNR = PDZ_PRW_PTNR AND PRW_PROJEKTNR = PDZ_PRW_PROJEKTNR AND PRW_PROJEKTTYP = PDZ_PRW_PROJEKTTYP AND PRW_FIRMENNR = PDZ_PRW_FIRMENNR" "~"
if joinKuerzel = PRWPDZ
searchResult := searchResult "LEFT JOIN BUERO.PDZ_PROJEKTWERTDOKZU ON PDZ_PRW_PRO_FIRMENNR = PRW_PRO_FIRMENNR AND PDZ_PRW_PTNR = PRW_PTNR AND PDZ_PRW_PROJEKTNR = PRW_PROJEKTNR AND PDZ_PRW_PROJEKTTYP = PRW_PROJEKTTYP AND PDZ_PRW_FIRMENNR = PRW_FIRMENNR" "~"
if joinKuerzel = RAPPSG
searchResult := searchResult "LEFT JOIN WWS.PSG_PREISGRUPPE ON PSG_FIRMENNR = RAP_PREISGRUPPEN_FIRMENNR AND PSG_PREISHAUPTGRNR = RAP_PREISHAUPTGRNR AND PSG_PREISUNTERGRNR = RAP_PREISUNTERGRNR" "~"
if joinKuerzel = PSGRAP
searchResult := searchResult "LEFT JOIN WWS.RAP_RABATTTABPOS ON RAP_PREISGRUPPEN_FIRMENNR = PSG_FIRMENNR AND RAP_PREISHAUPTGRNR = PSG_PREISHAUPTGRNR AND RAP_PREISUNTERGRNR = PSG_PREISUNTERGRNR" "~"
if joinKuerzel = PFTPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = PFT_FIRMENNR AND PSJ_MITARBEITERID = PFT_MITARBEITERID AND PSJ_DIENSTVLFDNR = PFT_DIENSTVLFDNR AND PSJ_LOHNJAHR = PFT_JAHR" "~"
if joinKuerzel = PSJPFT
searchResult := searchResult "LEFT JOIN HRS.PFT_PFAENDUNG_TILGUNG ON PFT_FIRMENNR = PSJ_FIRMENNR AND PFT_MITARBEITERID = PSJ_MITARBEITERID AND PFT_DIENSTVLFDNR = PSJ_DIENSTVLFDNR AND PFT_JAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = FWEPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_FIRMENNR = FWE_FIRMENNR AND PSM_MITARBEITERID = FWE_MITARBEITERID AND PSM_DIENSTVLFDNR = FWE_DIENSTVLFDNR AND PSM_PSMJAHR = FWE_JAHR AND PSM_PSMMONAT = FWE_MONAT AND PSM_PSMISTORG = FWE_PSMISTORG" "~"
if joinKuerzel = PSMFWE
searchResult := searchResult "LEFT JOIN HRS.FWE_FIXWERTE ON FWE_FIRMENNR = PSM_FIRMENNR AND FWE_MITARBEITERID = PSM_MITARBEITERID AND FWE_DIENSTVLFDNR = PSM_DIENSTVLFDNR AND FWE_JAHR = PSM_PSMJAHR AND FWE_MONAT = PSM_PSMMONAT AND FWE_PSMISTORG = PSM_PSMISTORG" "~"
if joinKuerzel = IKZPSZ
searchResult := searchResult "LEFT JOIN BUERO.PSZ_PERSONENKZ ON PSZ_PERSONENART = IKZ_PERSONENART AND PSZ_PERSONKZNR = IKZ_PERSONKZNR" "~"
if joinKuerzel = PSZIKZ
searchResult := searchResult "LEFT JOIN BUERO.IKZ_INSTITUTIONKONTAKTZU ON IKZ_PERSONENART = PSZ_PERSONENART AND IKZ_PERSONKZNR = PSZ_PERSONKZNR" "~"
if joinKuerzel = BPDPTP
searchResult := searchResult "LEFT JOIN WWS.PTP_PREISTABPOS ON PTP_FIRMENNR = BPD_PTP_FIRMENNR AND PTP_PREISTABNR = BPD_PTP_PREISTABNR AND PTP_PREISTABPOSNR = BPD_PTP_PREISTABPOSNR" "~"
if joinKuerzel = PTPBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_PTP_FIRMENNR = PTP_FIRMENNR AND BPD_PTP_PREISTABNR = PTP_PREISTABNR AND BPD_PTP_PREISTABPOSNR = PTP_PREISTABPOSNR" "~"
if joinKuerzel = PVWPVA
searchResult := searchResult "LEFT JOIN BUERO.PVA_PERSONVORERFASSAKTION ON PVA_PVAID = PVW_PVAID" "~"
if joinKuerzel = PVAPVW
searchResult := searchResult "LEFT JOIN BUERO.PVW_PERSONVORERFASSWOCHSTD ON PVW_PVAID = PVA_PVAID" "~"
if joinKuerzel = PICRAT
searchResult := searchResult "LEFT JOIN WWS.RAT_RABATTTAB ON RAT_FIRMENNR = PIC_RAT_FIRMENNR AND RAT_RABTABNR = PIC_RAT_RABTABNR" "~"
if joinKuerzel = RATPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_RAT_FIRMENNR = RAT_FIRMENNR AND PIC_RAT_RABTABNR = RAT_RABTABNR" "~"
if joinKuerzel = PTZRAT
searchResult := searchResult "LEFT JOIN WWS.RAT_RABATTTAB ON RAT_FIRMENNR = PTZ_RAT_FIRMENNR AND RAT_RABTABNR = PTZ_RAT_RABTABNR" "~"
if joinKuerzel = RATPTZ
searchResult := searchResult "LEFT JOIN WWS.PTZ_PREISTABZU ON PTZ_RAT_FIRMENNR = RAT_FIRMENNR AND PTZ_RAT_RABTABNR = RAT_RABTABNR" "~"
if joinKuerzel = PWBRAT
searchResult := searchResult "LEFT JOIN WWS.RAT_RABATTTAB ON RAT_FIRMENNR = PWB_FIRMENNR AND RAT_RABTABNR = PWB_RABTABNR" "~"
if joinKuerzel = RATPWB
searchResult := searchResult "LEFT JOIN WWS.PWB_PWKRABATTTABZU ON PWB_FIRMENNR = RAT_FIRMENNR AND PWB_RABTABNR = RAT_RABTABNR" "~"
if joinKuerzel = EFKRAZ
searchResult := searchResult "LEFT JOIN RAZ_RATENZAHLUNG.RAZ_RATENZAHLUNG ON RAZ_RATENMODELLID = EFK_RATENMODELLID" "~"
if joinKuerzel = RAZEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_RATENMODELLID = RAZ_RATENMODELLID" "~"
if joinKuerzel = REDREI
searchResult := searchResult "LEFT JOIN HRS.REI_REISESTAMM ON REI_REISENUMMER = RED_REISENUMMER" "~"
if joinKuerzel = REIRED
searchResult := searchResult "LEFT JOIN HRS.RED_REISEDETAIL ON RED_REISENUMMER = REI_REISENUMMER" "~"
if joinKuerzel = REZREW
searchResult := searchResult "LEFT JOIN ZLE.REW_REGELWERK ON REW_REGELWERKID = REZ_REGELWERKID" "~"
if joinKuerzel = REWREZ
searchResult := searchResult "LEFT JOIN ZLE.REZ_REGELWERKZU ON REZ_REGELWERKID = REW_REGELWERKID" "~"
if joinKuerzel = DIPRFM
searchResult := searchResult "LEFT JOIN BUERO.RFM_RECHTSFORM ON RFM_LANDNR = DIP_RFM_LANDNR AND RFM_RECHTSFORMNR = DIP_RECHTSFORMNR" "~"
if joinKuerzel = RFMDIP
searchResult := searchResult "LEFT JOIN BUERO.DIP_DIVERSE_PERSON ON DIP_RFM_LANDNR = RFM_LANDNR AND DIP_RECHTSFORMNR = RFM_RECHTSFORMNR" "~"
if joinKuerzel = KLIRFM
searchResult := searchResult "LEFT JOIN BUERO.RFM_RECHTSFORM ON RFM_LANDNR = KLI_RFM_LANDNR AND RFM_RECHTSFORMNR = KLI_RFM_RECHTSFORMNR" "~"
if joinKuerzel = RFMKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_RFM_LANDNR = RFM_LANDNR AND KLI_RFM_RECHTSFORMNR = RFM_RECHTSFORMNR" "~"
if joinKuerzel = RGDRGM
searchResult := searchResult "LEFT JOIN DATANORM.RGM_RABATT_GRUPPE ON RGM_RG_GRP = RGD_RG_GRP AND RGM_ERST_NR = RGD_ERST_NR" "~"
if joinKuerzel = RGMRGD
searchResult := searchResult "LEFT JOIN DATANORM.RGD_RABATT_GRUPPE_DETAIL ON RGD_RG_GRP = RGM_RG_GRP AND RGD_ERST_NR = RGM_ERST_NR" "~"
if joinKuerzel = RKTRKA
searchResult := searchResult "LEFT JOIN BMD.RKA_REPLIKATIONSAENDERUNG ON RKA_REPLAENDID = RKT_REPLAENDID AND RKA_REPLSTANDONR = RKT_REPLSTANDONR" "~"
if joinKuerzel = RKARKT
searchResult := searchResult "LEFT JOIN BMD.RKT_REPLIKATIONSTRANSFER ON RKT_REPLAENDID = RKA_REPLAENDID AND RKT_REPLSTANDONR = RKA_REPLSTANDONR" "~"
if joinKuerzel = GSFRKS
searchResult := searchResult "LEFT JOIN RKS_REPLIKATIONSSTANDORT.RKS_REPLIKATIONSSTANDORT ON RKS_REPLSTANDONR = GSF_ERSTELLREPLSTANDONR" "~"
if joinKuerzel = RKSGSF
searchResult := searchResult "LEFT JOIN WWS.GSF_GUTSCHEINFILIALDATEN ON GSF_ERSTELLREPLSTANDONR = RKS_REPLSTANDONR" "~"
if joinKuerzel = RGSRKS
searchResult := searchResult "LEFT JOIN BMD.RKS_REPLIKATIONSSTANDORT ON RKS_REPLSTANDONR = RGS_REPLSTANDONR" "~"
if joinKuerzel = RKSRGS
searchResult := searchResult "LEFT JOIN BMD.RGS_TABREPLGSTANDORTZU ON RGS_REPLSTANDONR = RKS_REPLSTANDONR" "~"
if joinKuerzel = PKDPKM
searchResult := searchResult "LEFT JOIN PRODUKTION.PKM_KM_MASTER ON PKM_FIRMENNR = PKD_FIRMENNR AND PKM_KM_NR = PKD_KM_NR" "~"
if joinKuerzel = PKMPKD
searchResult := searchResult "LEFT JOIN PRODUKTION.PKD_KM_DETAIL ON PKD_FIRMENNR = PKM_FIRMENNR AND PKD_KM_NR = PKM_KM_NR" "~"
if joinKuerzel = DISPLC
searchResult := searchResult "LEFT JOIN PLC_PPS_CHARGE.PLC_PPS_CHARGE ON PLC_PCHARGENID = DIS_PCHARGENID" "~"
if joinKuerzel = PLCDIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_PCHARGENID = PLC_PCHARGENID" "~"
if joinKuerzel = IDPPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_PCHARGENID = IDP_PCHARGENID" "~"
if joinKuerzel = PLCIDP
searchResult := searchResult "LEFT JOIN PRODUKTION.IDP_INFO_DELAY_BREAKDOWN ON IDP_PCHARGENID = PLC_PCHARGENID" "~"
if joinKuerzel = WAPPLC
searchResult := searchResult "LEFT JOIN PLC_PPS_CHARGE.PLC_PPS_CHARGE ON PLC_PCHARGENID = WAP_PCHARGENID" "~"
if joinKuerzel = PLCWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_PCHARGENID = PLC_PCHARGENID" "~"
if joinKuerzel = ZIWPLG
searchResult := searchResult "LEFT JOIN BUERO.PLG_PLANGRUPPE ON PLG_FIRMENNR = ZIW_PLG_FIRMENNR AND PLG_PLANGRLFDNR = ZIW_PLG_PLANGRLFDNR" "~"
if joinKuerzel = PLGZIW
searchResult := searchResult "LEFT JOIN BUERO.ZIW_ZUSATZINFOWERT ON ZIW_PLG_FIRMENNR = PLG_FIRMENNR AND ZIW_PLG_PLANGRLFDNR = PLG_PLANGRLFDNR" "~"
if joinKuerzel = ZVMPLG
searchResult := searchResult "LEFT JOIN BUERO.PLG_PLANGRUPPE ON PLG_FIRMENNR = ZVM_PLG_FIRMENNR AND PLG_PLANGRLFDNR = ZVM_PLANGRLFDNR" "~"
if joinKuerzel = PLGZVM
searchResult := searchResult "LEFT JOIN BUERO.ZVM_ZEITPLANUNGVERSION ON ZVM_PLG_FIRMENNR = PLG_FIRMENNR AND ZVM_PLANGRLFDNR = PLG_PLANGRLFDNR" "~"
if joinKuerzel = PKSPPP
searchResult := searchResult "LEFT JOIN PRODUKTION.PPP_KAPA_POOL ON PPP_PPPID = PKS_PPPID" "~"
if joinKuerzel = PPPPKS
searchResult := searchResult "LEFT JOIN PRODUKTION.PKS_SOLL_KAPAZITAET ON PKS_PPPID = PPP_PPPID" "~"
if joinKuerzel = AAEPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = AAE_PROJEKT_FIRMENNR AND PRO_PROJEKTNR = AAE_PROJEKTNR AND PRO_PROJEKTTYP = AAE_PROJEKTTYP" "~"
if joinKuerzel = PROAAE
searchResult := searchResult "LEFT JOIN WWS.AAE_ANGEBOTSAUSW_ERFQ_TMP ON AAE_PROJEKT_FIRMENNR = PRO_FIRMENNR AND AAE_PROJEKTNR = PRO_PROJEKTNR AND AAE_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = AFTPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = AFT_PRO_FIRMENNR AND PRO_PROJEKTNR = AFT_PROJEKTNR AND PRO_PROJEKTTYP = AFT_PROJEKTTYP" "~"
if joinKuerzel = PROAFT
searchResult := searchResult "LEFT JOIN ZLE.AFT_AUFBEREITUNG ON AFT_PRO_FIRMENNR = PRO_FIRMENNR AND AFT_PROJEKTNR = PRO_PROJEKTNR AND AFT_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = BONPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = BON_PRO_FIRMENNR AND PRO_PROJEKTNR = BON_PRO_PROJEKTNR AND PRO_PROJEKTTYP = BON_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_PRO_FIRMENNR = PRO_FIRMENNR AND BON_PRO_PROJEKTNR = PRO_PROJEKTNR AND BON_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = BPKPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = BPK_PROJEKT_FIRMENNR AND PRO_PROJEKTNR = BPK_PROJEKTNR AND PRO_PROJEKTTYP = BPK_PROJEKTTYP" "~"
if joinKuerzel = PROBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_PROJEKT_FIRMENNR = PRO_FIRMENNR AND BPK_PROJEKTNR = PRO_PROJEKTNR AND BPK_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = BRTPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_PROJEKTID = BRT_PROJEKTID" "~"
if joinKuerzel = PROBRT
searchResult := searchResult "LEFT JOIN BUERO.BRT_BERICHT ON BRT_PROJEKTID = PRO_PROJEKTID" "~"
if joinKuerzel = BRVPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = BRV_PRO_FIRMENNR AND PRO_PROJEKTNR = BRV_PRO_PROJEKTNR AND PRO_PROJEKTTYP = BRV_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROBRV
searchResult := searchResult "LEFT JOIN FIBU.BRV_BANKRUECKVERBUCHUNG ON BRV_PRO_FIRMENNR = PRO_FIRMENNR AND BRV_PRO_PROJEKTNR = PRO_PROJEKTNR AND BRV_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = ERBPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = ERB_PRO_FIRMENNR AND PRO_PROJEKTNR = ERB_PRO_PROJEKTNR AND PRO_PROJEKTTYP = ERB_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROERB
searchResult := searchResult "LEFT JOIN FIBU.ERB_BUCHUNG ON ERB_PRO_FIRMENNR = PRO_FIRMENNR AND ERB_PRO_PROJEKTNR = PRO_PROJEKTNR AND ERB_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = LEIPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = LEI_PRO_FIRMENNR AND PRO_PROJEKTNR = LEI_PROJEKTNR AND PRO_PROJEKTTYP = LEI_PROJEKTTYP" "~"
if joinKuerzel = PROLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_PRO_FIRMENNR = PRO_FIRMENNR AND LEI_PROJEKTNR = PRO_PROJEKTNR AND LEI_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = LTKPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = LTK_PRO_FIRMENNR AND PRO_PROJEKTNR = LTK_PROJEKTNR AND PRO_PROJEKTTYP = LTK_PROJEKTTYP" "~"
if joinKuerzel = PROLTK
searchResult := searchResult "LEFT JOIN ZLE.LTK_LEISTTAUSCHKOPF ON LTK_PRO_FIRMENNR = PRO_FIRMENNR AND LTK_PROJEKTNR = PRO_PROJEKTNR AND LTK_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = PCKPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_PROJEKTID = PCK_PROJEKTID" "~"
if joinKuerzel = PROPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_PROJEKTID = PRO_PROJEKTID" "~"
if joinKuerzel = REOPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = REO_FIRMENNR AND PRO_PROJEKTNR = REO_PROJEKTNR AND PRO_PROJEKTTYP = REO_PROJEKTTYP" "~"
if joinKuerzel = PROREO
searchResult := searchResult "LEFT JOIN BUERO.REO_RELEVANTEOBJEKTE ON REO_FIRMENNR = PRO_FIRMENNR AND REO_PROJEKTNR = PRO_PROJEKTNR AND REO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = SCEPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_PROJEKTID = SCE_PRO_PROJEKTID" "~"
if joinKuerzel = PROSCE
searchResult := searchResult "LEFT JOIN PRODUKTION.SCE_SCORECARD ON SCE_PRO_PROJEKTID = PRO_PROJEKTID" "~"
if joinKuerzel = TSAPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = TSA_PRO_FIRMENNR AND PRO_PROJEKTNR = TSA_PROJEKTNR AND PRO_PROJEKTTYP = TSA_PROJEKTTYP" "~"
if joinKuerzel = PROTSA
searchResult := searchResult "LEFT JOIN FIBU.TSA_TEILSCHLUSSRECHABLAUF ON TSA_PRO_FIRMENNR = PRO_FIRMENNR AND TSA_PROJEKTNR = PRO_PROJEKTNR AND TSA_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = KUKPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = KUK_PRO_FIRMENNR AND PRO_PROJEKTNR = KUK_PRO_PROJEKTNR AND PRO_PROJEKTTYP = KUK_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROKUK
searchResult := searchResult "LEFT JOIN WWS.KUK_KUNDENKARTEI ON KUK_PRO_FIRMENNR = PRO_FIRMENNR AND KUK_PRO_PROJEKTNR = PRO_PROJEKTNR AND KUK_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = MSGPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = MSG_PRO_FIRMENNR AND PRO_PROJEKTNR = MSG_PROJEKTNR AND PRO_PROJEKTTYP = MSG_PROJEKTTYP" "~"
if joinKuerzel = PROMSG
searchResult := searchResult "LEFT JOIN BMDENTW.MSG_MESSAGE ON MSG_PRO_FIRMENNR = PRO_FIRMENNR AND MSG_PROJEKTNR = PRO_PROJEKTNR AND MSG_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = PLCPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_PROJEKTID = PLC_PRO_PROJEKTID" "~"
if joinKuerzel = PROPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_PRO_PROJEKTID = PRO_PROJEKTID" "~"
if joinKuerzel = PRTPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = PRT_FIRMENNR AND PRO_PROJEKTNR = PRT_PROJEKTNR AND PRO_PROJEKTTYP = PRT_PROJEKTTYP" "~"
if joinKuerzel = PROPRT
searchResult := searchResult "LEFT JOIN BUERO.PRT_PROJEKTTAETIGKEIT ON PRT_FIRMENNR = PRO_FIRMENNR AND PRT_PROJEKTNR = PRO_PROJEKTNR AND PRT_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = RSBPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_PROJEKTID = RSB_PROJEKTID" "~"
if joinKuerzel = PRORSB
searchResult := searchResult "LEFT JOIN BUERO.RSB_RESSOURCENBEDARF ON RSB_PROJEKTID = PRO_PROJEKTID" "~"
if joinKuerzel = STBPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_PROJEKTID = STB_PRO_PROJEKTID" "~"
if joinKuerzel = PROSTB
searchResult := searchResult "LEFT JOIN PRODUKTION.STB_STAKE_HEAD ON STB_PRO_PROJEKTID = PRO_PROJEKTID" "~"
if joinKuerzel = VEBPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = VEB_PRO_FIRMENNR AND PRO_PROJEKTNR = VEB_PRO_PROJEKTNR AND PRO_PROJEKTTYP = VEB_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROVEB
searchResult := searchResult "LEFT JOIN FIBU.VEB_VORERFASSBUCHUNG ON VEB_PRO_FIRMENNR = PRO_FIRMENNR AND VEB_PRO_PROJEKTNR = PRO_PROJEKTNR AND VEB_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = VWZPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = VWZ_PRO_FIRMENNR AND PRO_PROJEKTNR = VWZ_PROJEKTNR AND PRO_PROJEKTTYP = VWZ_PROJEKTTYP" "~"
if joinKuerzel = PROVWZ
searchResult := searchResult "LEFT JOIN BUERO.VWZ_VORGANGWORKFLOWZU ON VWZ_PRO_FIRMENNR = PRO_FIRMENNR AND VWZ_PROJEKTNR = PRO_PROJEKTNR AND VWZ_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = UZLPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = UZL_BASIS_PRO_FIRMENNR AND PRO_PROJEKTNR = UZL_BASIS_PROJEKTNR AND PRO_PROJEKTTYP = UZL_BASIS_PROJEKTTYP" "~"
if joinKuerzel = PROUZL
searchResult := searchResult "LEFT JOIN BUERO.UZL_UNTERZIEL ON UZL_BASIS_PRO_FIRMENNR = PRO_FIRMENNR AND UZL_BASIS_PROJEKTNR = PRO_PROJEKTNR AND UZL_BASIS_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = ARLPRS
searchResult := searchResult "LEFT JOIN PRS_PROZESS.PRS_PROZESS ON PRS_FIRMENNR = ARL_FIRMENNR AND PRS_PROZESSID = ARL_VORGABE_PROZESSID" "~"
if joinKuerzel = PRSARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = PRS_FIRMENNR AND ARL_VORGABE_PROZESSID = PRS_PROZESSID" "~"
if joinKuerzel = PRZPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_FIRMENNR = PRZ_FIRMENNR AND PRS_PROZESSID = PRZ_PROZESSID" "~"
if joinKuerzel = PRSPRZ
searchResult := searchResult "LEFT JOIN PRODUKTION.PRZ_PROZESSBAZZU ON PRZ_FIRMENNR = PRS_FIRMENNR AND PRZ_PROZESSID = PRS_PROZESSID" "~"
if joinKuerzel = PTPPRS
searchResult := searchResult "LEFT JOIN PRS_PROZESS.PRS_PROZESS ON PRS_FIRMENNR = PTP_PRS_FIRMENNR AND PRS_PROZESSID = PTP_PRS_PROZESSID" "~"
if joinKuerzel = PRSPTP
searchResult := searchResult "LEFT JOIN WWS.PTP_PREISTABPOS ON PTP_PRS_FIRMENNR = PRS_FIRMENNR AND PTP_PRS_PROZESSID = PRS_PROZESSID" "~"
if joinKuerzel = PRZPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_FIRMENNR = PRZ_FIRMENNR AND PRS_PROZESSID = PRZ_PROZESSID_ALTERNATIV_3" "~"
if joinKuerzel = PRSPRZ
searchResult := searchResult "LEFT JOIN PRODUKTION.PRZ_PROZESSBAZZU ON PRZ_FIRMENNR = PRS_FIRMENNR AND PRZ_PROZESSID_ALTERNATIV_3 = PRS_PROZESSID" "~"
if joinKuerzel = WARPSG
searchResult := searchResult "LEFT JOIN WWS.PSG_PREISGRUPPE ON PSG_FIRMENNR = WAR_HAUPTFIRMENNR AND PSG_PREISHAUPTGRNR = WAR_PREISHAUPTGRNR AND PSG_PREISUNTERGRNR = WAR_PREISUNTERGRNR" "~"
if joinKuerzel = PSGWAR
searchResult := searchResult "LEFT JOIN WWS.WAR_WARENEINGANGRABATTE ON WAR_HAUPTFIRMENNR = PSG_FIRMENNR AND WAR_PREISHAUPTGRNR = PSG_PREISHAUPTGRNR AND WAR_PREISUNTERGRNR = PSG_PREISUNTERGRNR" "~"
if joinKuerzel = ERXPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = ERX_FIRMENNR AND PSJ_MITARBEITERID = ERX_MITARBEITERID AND PSJ_DIENSTVLFDNR = ERX_DIENSTVLFDNR AND PSJ_LOHNJAHR = ERX_LOHNJAHR" "~"
if joinKuerzel = PSJERX
searchResult := searchResult "LEFT JOIN HRS.ERX_ERFASSUNGSZUSATZ ON ERX_FIRMENNR = PSJ_FIRMENNR AND ERX_MITARBEITERID = PSJ_MITARBEITERID AND ERX_DIENSTVLFDNR = PSJ_DIENSTVLFDNR AND ERX_LOHNJAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = SMAPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = SMA_FIRMENNR AND PSJ_MITARBEITERID = SMA_MITARBEITERID AND PSJ_DIENSTVLFDNR = SMA_DIENSTVLFDNR AND PSJ_LOHNJAHR = SMA_LOHNJAHR" "~"
if joinKuerzel = PSJSMA
searchResult := searchResult "LEFT JOIN HRS.SMA_SCHNITTMA ON SMA_FIRMENNR = PSJ_FIRMENNR AND SMA_MITARBEITERID = PSJ_MITARBEITERID AND SMA_DIENSTVLFDNR = PSJ_DIENSTVLFDNR AND SMA_LOHNJAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = DIPPSZ
searchResult := searchResult "LEFT JOIN BUERO.PSZ_PERSONENKZ ON PSZ_PERSONENART = DIP_PSZ_PERSONENART AND PSZ_PERSONKZNR = DIP_PERSONKZNR" "~"
if joinKuerzel = PSZDIP
searchResult := searchResult "LEFT JOIN BUERO.DIP_DIVERSE_PERSON ON DIP_PSZ_PERSONENART = PSZ_PERSONENART AND DIP_PERSONKZNR = PSZ_PERSONKZNR" "~"
if joinKuerzel = PNKPSZ
searchResult := searchResult "LEFT JOIN BUERO.PSZ_PERSONENKZ ON PSZ_PERSONENART = PNK_PERSONENART AND PSZ_PERSONKZNR = PNK_PERSONKZNR" "~"
if joinKuerzel = PSZPNK
searchResult := searchResult "LEFT JOIN BUERO.PNK_PERSONNUMMERNKREIS ON PNK_PERSONENART = PSZ_PERSONENART AND PNK_PERSONKZNR = PSZ_PERSONKZNR" "~"
if joinKuerzel = KOSPTP
searchResult := searchResult "LEFT JOIN PTP_PREISTABPOS.PTP_PREISTABPOS ON PTP_FIRMENNR = KOS_PTP_FIRMENNR AND PTP_PREISTABNR = KOS_PTP_PREISTABNR AND PTP_PREISTABPOSNR = KOS_PTP_PREISTABPOSNR" "~"
if joinKuerzel = PTPKOS
searchResult := searchResult "LEFT JOIN ZLE.KOS_KOSTENFAKTOR ON KOS_PTP_FIRMENNR = PTP_FIRMENNR AND KOS_PTP_PREISTABNR = PTP_PREISTABNR AND KOS_PTP_PREISTABPOSNR = PTP_PREISTABPOSNR" "~"
if joinKuerzel = AGMPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = AGM_POSTITNR" "~"
if joinKuerzel = PITAGM
searchResult := searchResult "LEFT JOIN PRODUKTION.AGM_AG_PERSON_ZU ON AGM_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = AGNPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = AGN_POSTITNR" "~"
if joinKuerzel = PITAGN
searchResult := searchResult "LEFT JOIN WWS.AGN_AG_PERSON_ZU ON AGN_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = APAPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = APA_POSTITNR" "~"
if joinKuerzel = PITAPA
searchResult := searchResult "LEFT JOIN PRODUKTION.APA_ARBEITSPLAN ON APA_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = APSPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = APS_POSTITNR" "~"
if joinKuerzel = PITAPS
searchResult := searchResult "LEFT JOIN WWS.APS_ARBEITSPLAN_STAMM ON APS_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = ARLPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = ARL_POSTITNR" "~"
if joinKuerzel = PITARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = BONPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = BON_POSTITNR" "~"
if joinKuerzel = PITBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = BPKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = BPK_POSTITNR" "~"
if joinKuerzel = PITBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = BPPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = BPP_POSTITNR" "~"
if joinKuerzel = PITBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = BWMPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = BWM_POSTITNR" "~"
if joinKuerzel = PITBWM
searchResult := searchResult "LEFT JOIN BUERO.BWM_BEWERTUNGMONAT ON BWM_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = CHGPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = CHG_POSTITNR" "~"
if joinKuerzel = PITCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = EHPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = EHP_POSTITNR" "~"
if joinKuerzel = PITEHP
searchResult := searchResult "LEFT JOIN WWS.EHP_EINHEITENTABPOS ON EHP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = EKZPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = EKZ_POSTITNR" "~"
if joinKuerzel = PITEKZ
searchResult := searchResult "LEFT JOIN WWS.EKZ_ERLOESAUFWANDKZ ON EKZ_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = EPSPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = EPS_POSTITNR" "~"
if joinKuerzel = PITEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = FATPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = FAT_POSTITNR" "~"
if joinKuerzel = PITFAT
searchResult := searchResult "LEFT JOIN WWS.FAT_FARBENTAB ON FAT_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = FAVPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = FAV_POSTITNR" "~"
if joinKuerzel = PITFAV
searchResult := searchResult "LEFT JOIN BUERO.FAV_FAVORITEN ON FAV_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = FRBPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = FRB_POSTITNR" "~"
if joinKuerzel = PITFRB
searchResult := searchResult "LEFT JOIN WWS.FRB_FARBE ON FRB_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = GRSPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = GRS_POSTITNR" "~"
if joinKuerzel = PITGRS
searchResult := searchResult "LEFT JOIN BUERO.GRS_GELDRISIKO ON GRS_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = GRTPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = GRT_POSTITNR" "~"
if joinKuerzel = PITGRT
searchResult := searchResult "LEFT JOIN WWS.GRT_GROESSENTAB ON GRT_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = GSVPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = GSV_POSTITNR" "~"
if joinKuerzel = PITGSV
searchResult := searchResult "LEFT JOIN BIL.GSV_GSVGBERECH ON GSV_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = IVPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = IVP_POSTITNR" "~"
if joinKuerzel = PITIVP
searchResult := searchResult "LEFT JOIN WWS.IVP_INVENTURPOSITION ON IVP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = KFIPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = KFI_POSTITNR" "~"
if joinKuerzel = PITKFI
searchResult := searchResult "LEFT JOIN BUERO.KFI_KORETYPENFINDUNG ON KFI_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = KJOPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = KJO_POSTITNR" "~"
if joinKuerzel = PITKJO
searchResult := searchResult "LEFT JOIN BUERO.KJO_KONTAKTJOURNAL ON KJO_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = KOWPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = KOW_POSTITNR" "~"
if joinKuerzel = PITKOW
searchResult := searchResult "LEFT JOIN HRS.KOW_KOLLEKTIVVERTRAGWERTE ON KOW_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = KRIPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = KRI_POSTITNR" "~"
if joinKuerzel = PITKRI
searchResult := searchResult "LEFT JOIN BUERO.KRI_KRITERIUM ON KRI_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = LAGPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = LAG_POSTITNR" "~"
if joinKuerzel = PITLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = LAPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = LAP_POSTITNR" "~"
if joinKuerzel = PITLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = VASPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = VAS_PRO_FIRMENNR AND PRO_PROJEKTNR = VAS_PRO_PROJEKTNR AND PRO_PROJEKTTYP = VAS_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_PRO_FIRMENNR = PRO_FIRMENNR AND VAS_PRO_PROJEKTNR = PRO_PROJEKTNR AND VAS_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = WFNPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = WFN_BASIS_PRO_FIRMENNR AND PRO_PROJEKTNR = WFN_BASIS_PROJEKTNR AND PRO_PROJEKTTYP = WFN_BASIS_PROJEKTTYP" "~"
if joinKuerzel = PROWFN
searchResult := searchResult "LEFT JOIN BUERO.WFN_WORKFLOWACTION ON WFN_BASIS_PRO_FIRMENNR = PRO_FIRMENNR AND WFN_BASIS_PROJEKTNR = PRO_PROJEKTNR AND WFN_BASIS_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = ZBUPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = ZBU_PRO_FIRMENNR AND PRO_PROJEKTNR = ZBU_PRO_PROJEKTNR AND PRO_PROJEKTTYP = ZBU_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROZBU
searchResult := searchResult "LEFT JOIN BUERO.ZBU_ZEITBUCHUNG ON ZBU_PRO_FIRMENNR = PRO_FIRMENNR AND ZBU_PRO_PROJEKTNR = PRO_PROJEKTNR AND ZBU_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = AIDPRS
searchResult := searchResult "LEFT JOIN PRS_PROZESS.PRS_PROZESS ON PRS_FIRMENNR = AID_PRS_FIRMENNR AND PRS_PROZESSID = AID_PRS_PROZESSID" "~"
if joinKuerzel = PRSAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_PRS_FIRMENNR = PRS_FIRMENNR AND AID_PRS_PROZESSID = PRS_PROZESSID" "~"
if joinKuerzel = BAZPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_FIRMENNR = BAZ_PRS_FIRMENNR AND PRS_PROZESSID = BAZ_VORGABE_PROZESSID" "~"
if joinKuerzel = PRSBAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_PRS_FIRMENNR = PRS_FIRMENNR AND BAZ_VORGABE_PROZESSID = PRS_PROZESSID" "~"
if joinKuerzel = PKSPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_FIRMENNR = PKS_PRS_FIRMENNR AND PRS_PROZESSID = PKS_PRS_PROZESSID" "~"
if joinKuerzel = PRSPKS
searchResult := searchResult "LEFT JOIN PRODUKTION.PKS_SOLL_KAPAZITAET ON PKS_PRS_FIRMENNR = PRS_FIRMENNR AND PKS_PRS_PROZESSID = PRS_PROZESSID" "~"
if joinKuerzel = VFAPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_FIRMENNR = VFA_PRS_FIRMENNR AND PRS_PROZESSID = VFA_PROZESSID" "~"
if joinKuerzel = PRSVFA
searchResult := searchResult "LEFT JOIN PRODUKTION.VFA_VERFAHRENSANWEISUNG_KOPF ON VFA_PRS_FIRMENNR = PRS_FIRMENNR AND VFA_PROZESSID = PRS_PROZESSID" "~"
if joinKuerzel = ABVPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = ABV_FIRMENNR AND PSJ_MITARBEITERID = ABV_MITARBEITERID AND PSJ_DIENSTVLFDNR = ABV_DIENSTVLFDNR AND PSJ_LOHNJAHR = ABV_LOHNJAHR" "~"
if joinKuerzel = PSJABV
searchResult := searchResult "LEFT JOIN HRS.ABV_ABREVERGLEICH ON ABV_FIRMENNR = PSJ_FIRMENNR AND ABV_MITARBEITERID = PSJ_MITARBEITERID AND ABV_DIENSTVLFDNR = PSJ_DIENSTVLFDNR AND ABV_LOHNJAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = EAWPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = EAW_FIRMENNR AND PSJ_MITARBEITERID = EAW_MITARBEITERID AND PSJ_DIENSTVLFDNR = EAW_DIENSTVLFDNR AND PSJ_LOHNJAHR = EAW_JAHR" "~"
if joinKuerzel = PSJEAW
searchResult := searchResult "LEFT JOIN HRS.EAW_ELDADETAIL_AEW ON EAW_FIRMENNR = PSJ_FIRMENNR AND EAW_MITARBEITERID = PSJ_MITARBEITERID AND EAW_DIENSTVLFDNR = PSJ_DIENSTVLFDNR AND EAW_JAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = PFDPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = PFD_FIRMENNR AND PSJ_MITARBEITERID = PFD_MITARBEITERID AND PSJ_DIENSTVLFDNR = PFD_DIENSTVLFDNR AND PSJ_LOHNJAHR = PFD_JAHR" "~"
if joinKuerzel = PSJPFD
searchResult := searchResult "LEFT JOIN HRS.PFD_PFAENDUNG_DETAIL ON PFD_FIRMENNR = PSJ_FIRMENNR AND PFD_MITARBEITERID = PSJ_MITARBEITERID AND PFD_DIENSTVLFDNR = PSJ_DIENSTVLFDNR AND PFD_JAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = PSRPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = PSR_PSJ_FIRMENNR AND PSJ_MITARBEITERID = PSR_PSJ_MITARBEITERID AND PSJ_DIENSTVLFDNR = PSR_PSJ_DIENSTVLFDNR AND PSJ_LOHNJAHR = PSR_PSJ_LOHNJAHR" "~"
if joinKuerzel = PSJPSR
searchResult := searchResult "LEFT JOIN HRS.PSR_PROGRAMMSPERRE ON PSR_PSJ_FIRMENNR = PSJ_FIRMENNR AND PSR_PSJ_MITARBEITERID = PSJ_MITARBEITERID AND PSR_PSJ_DIENSTVLFDNR = PSJ_DIENSTVLFDNR AND PSR_PSJ_LOHNJAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = PCZPSZ
searchResult := searchResult "LEFT JOIN BUERO.PSZ_PERSONENKZ ON PSZ_PERSONENART = PCZ_PERSONENART AND PSZ_PERSONKZNR = PCZ_PERSONKZNR" "~"
if joinKuerzel = PSZPCZ
searchResult := searchResult "LEFT JOIN BUERO.PCZ_PERSONKZCOMPASSZU ON PCZ_PERSONENART = PSZ_PERSONENART AND PCZ_PERSONKZNR = PSZ_PERSONKZNR" "~"
if joinKuerzel = IOBQMK
searchResult := searchResult "LEFT JOIN QMK_QM_KOPF.QMK_QM_KOPF ON QMK_QM_KOPF_ID = IOB_QM_KOPF_ID" "~"
if joinKuerzel = QMKIOB
searchResult := searchResult "LEFT JOIN BUERO.IOB_INSTANDHOBJEKT ON IOB_QM_KOPF_ID = QMK_QM_KOPF_ID" "~"
if joinKuerzel = QMAQMK
searchResult := searchResult "LEFT JOIN PRODUKTION.QMK_QM_KOPF ON QMK_QM_KOPF_ID = QMA_QMK_QM_KOPF_ID" "~"
if joinKuerzel = QMKQMA
searchResult := searchResult "LEFT JOIN PRODUKTION.QMA_QM_ARTIKEL ON QMA_QMK_QM_KOPF_ID = QMK_QM_KOPF_ID" "~"
if joinKuerzel = WAKQMK
searchResult := searchResult "LEFT JOIN QMK_QM_KOPF.QMK_QM_KOPF ON QMK_QM_KOPF_ID = WAK_QM_KOPF_ID" "~"
if joinKuerzel = QMKWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_QM_KOPF_ID = QMK_QM_KOPF_ID" "~"
if joinKuerzel = PWRRAP
searchResult := searchResult "LEFT JOIN WWS.RAP_RABATTTABPOS ON RAP_FIRMENNR = PWR_RAP_FIRMENNR AND RAP_RABTABNR = PWR_RAP_RABTABNR AND RAP_RABTABPOSNR = PWR_RAP_RABTABPOSNR" "~"
if joinKuerzel = RAPPWR
searchResult := searchResult "LEFT JOIN WWS.PWR_RABATTWARTUNGPOS ON PWR_RAP_FIRMENNR = RAP_FIRMENNR AND PWR_RAP_RABTABNR = RAP_RABTABNR AND PWR_RAP_RABTABPOSNR = RAP_RABTABPOSNR" "~"
if joinKuerzel = AKTRAT
searchResult := searchResult "LEFT JOIN WWS.RAT_RABATTTAB ON RAT_FIRMENNR = AKT_FIRMENNR AND RAT_RABTABNR = AKT_RABTABNR" "~"
if joinKuerzel = RATAKT
searchResult := searchResult "LEFT JOIN WWS.AKT_AKTIONSSTAMM ON AKT_FIRMENNR = RAT_FIRMENNR AND AKT_RABTABNR = RAT_RABTABNR" "~"
if joinKuerzel = PLCREH
searchResult := searchResult "LEFT JOIN PRODUKTION.REH_REIHE ON REH_FIRMENNR = PLC_REH_FIRMENNR AND REH_REIHE = PLC_REIHE" "~"
if joinKuerzel = REHPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_REH_FIRMENNR = REH_FIRMENNR AND PLC_REIHE = REH_REIHE" "~"
if joinKuerzel = REAREI
searchResult := searchResult "LEFT JOIN HRS.REI_REISESTAMM ON REI_REISENUMMER = REA_REISENUMMER" "~"
if joinKuerzel = REIREA
searchResult := searchResult "LEFT JOIN HRS.REA_REISEAUFTEILUNG ON REA_REISENUMMER = REI_REISENUMMER" "~"
if joinKuerzel = LUPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = LUP_POSTITNR" "~"
if joinKuerzel = PITLUP
searchResult := searchResult "LEFT JOIN PRODUKTION.LUP_LAGER_UMS_POS ON LUP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = MDEPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = MDE_POSTITNR" "~"
if joinKuerzel = PITMDE
searchResult := searchResult "LEFT JOIN WWS.MDE_MOBILEDATENERFASSUNG ON MDE_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = NEGPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = NEG_POSTITNR" "~"
if joinKuerzel = PITNEG
searchResult := searchResult "LEFT JOIN BIL.NEG_NICHTENTGEWINNE ON NEG_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PRZPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PRZ_POSTITNR" "~"
if joinKuerzel = PITPRZ
searchResult := searchResult "LEFT JOIN PRODUKTION.PRZ_PROZESSBAZZU ON PRZ_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PSGPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PSG_POSTITNR" "~"
if joinKuerzel = PITPSG
searchResult := searchResult "LEFT JOIN WWS.PSG_PREISGRUPPE ON PSG_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = QMKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = QMK_POSTITNR" "~"
if joinKuerzel = PITQMK
searchResult := searchResult "LEFT JOIN PRODUKTION.QMK_QM_KOPF ON QMK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = QUOPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = QUO_POSTITNR" "~"
if joinKuerzel = PITQUO
searchResult := searchResult "LEFT JOIN BUERO.QUO_QUOTE ON QUO_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = REIPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = REI_POSTITNR" "~"
if joinKuerzel = PITREI
searchResult := searchResult "LEFT JOIN HRS.REI_REISESTAMM ON REI_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = RSBPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = RSB_POSTITNR" "~"
if joinKuerzel = PITRSB
searchResult := searchResult "LEFT JOIN BUERO.RSB_RESSOURCENBEDARF ON RSB_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = SCLPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = SCL_POSTITNR" "~"
if joinKuerzel = PITSCL
searchResult := searchResult "LEFT JOIN PRODUKTION.SCL_SCKENNZAHLDET ON SCL_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = TOUPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = TOU_POSTITNR" "~"
if joinKuerzel = PITTOU
searchResult := searchResult "LEFT JOIN WWS.TOU_TOUR ON TOU_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = TRPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = TRP_POSTITNR" "~"
if joinKuerzel = PITTRP
searchResult := searchResult "LEFT JOIN WWS.TRP_TRANSPORTPOSITION ON TRP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = VFAPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = VFA_POSTITNR" "~"
if joinKuerzel = PITVFA
searchResult := searchResult "LEFT JOIN PRODUKTION.VFA_VERFAHRENSANWEISUNG_KOPF ON VFA_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = WAGPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = WAG_POSTITNR" "~"
if joinKuerzel = PITWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = WERPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = WER_POSTITNR" "~"
if joinKuerzel = PITWER
searchResult := searchResult "LEFT JOIN WWS.WER_WWSERKONTROLLEKOPF ON WER_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = ZBUPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = ZBU_POSTITNR" "~"
if joinKuerzel = PITZBU
searchResult := searchResult "LEFT JOIN BUERO.ZBU_ZEITBUCHUNG ON ZBU_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = ZVMPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = ZVM_POSTITNR" "~"
if joinKuerzel = PITZVM
searchResult := searchResult "LEFT JOIN BUERO.ZVM_ZEITPLANUNGVERSION ON ZVM_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = BPKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = BPK_ERFABSCHL_POSTITNR" "~"
if joinKuerzel = PITBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_ERFABSCHL_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PSMPKC
searchResult := searchResult "LEFT JOIN HRS.PKC_PENSKASSA_CALC ON PKC_DBSNR = PSM_DBS_DATENBESTANDNR AND PKC_PKCJAHR = PSM_PSMJAHR AND PKC_PKCLFDNR = PSM_PKC_PKCLFDNR" "~"
if joinKuerzel = PKCPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_DBS_DATENBESTANDNR = PKC_DBSNR AND PSM_PSMJAHR = PKC_PKCJAHR AND PSM_PKC_PKCLFDNR = PKC_PKCLFDNR" "~"
if joinKuerzel = PERPKZ
searchResult := searchResult "LEFT JOIN BUERO.PKZ_PERSONKONTAKTZU ON PKZ_FIRMENNR = PER_FIRMENNR AND PKZ_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PKZPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PKZ_FIRMENNR AND PER_PERSONENID = PKZ_PERSONENID" "~"
if joinKuerzel = APSPLG
searchResult := searchResult "LEFT JOIN PLG_PLANGRUPPE.PLG_PLANGRUPPE ON PLG_FIRMENNR = APS_PLG_FIRMENNR AND PLG_PLANGRLFDNR = APS_PLG_PLANGRLFDNR" "~"
if joinKuerzel = PLGAPS
searchResult := searchResult "LEFT JOIN WWS.APS_ARBEITSPLAN_STAMM ON APS_PLG_FIRMENNR = PLG_FIRMENNR AND APS_PLG_PLANGRLFDNR = PLG_PLANGRLFDNR" "~"
if joinKuerzel = PGZPLG
searchResult := searchResult "LEFT JOIN BUERO.PLG_PLANGRUPPE ON PLG_FIRMENNR = PGZ_PLG_FIRMENNR AND PLG_PLANGRLFDNR = PGZ_PLG_PLANGRLFDNR" "~"
if joinKuerzel = PLGPGZ
searchResult := searchResult "LEFT JOIN BUERO.PGZ_PLANGRUPPETMOZU ON PGZ_PLG_FIRMENNR = PLG_FIRMENNR AND PGZ_PLG_PLANGRLFDNR = PLG_PLANGRLFDNR" "~"
if joinKuerzel = PLCPLO
searchResult := searchResult "LEFT JOIN PRODUKTION.PLO_PPS_LOS ON PLO_FIRMENNR = PLC_PLO_FIRMENNR AND PLO_PL_NR = PLC_PL_NR" "~"
if joinKuerzel = PLOPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_PLO_FIRMENNR = PLO_FIRMENNR AND PLC_PL_NR = PLO_PL_NR" "~"
if joinKuerzel = PKMPLP
searchResult := searchResult "LEFT JOIN PRODUKTION.PLP_PLANUNGSPERIODE ON PLP_FIRMENNR = PKM_PLP_FIRMENNR AND PLP_PLANPERID = PKM_PLP_PLANPERID" "~"
if joinKuerzel = PLPPKM
searchResult := searchResult "LEFT JOIN PRODUKTION.PKM_KM_MASTER ON PKM_PLP_FIRMENNR = PLP_FIRMENNR AND PKM_PLP_PLANPERID = PLP_PLANPERID" "~"
if joinKuerzel = XSEPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = XSE_PER_FIRMENNR AND PER_PERSONENID = XSE_PER_PERSONENID" "~"
if joinKuerzel = PERXSE
searchResult := searchResult "LEFT JOIN BUERO.XSE_XML_STEUERERKL ON XSE_PER_FIRMENNR = PER_FIRMENNR AND XSE_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = ZASPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = ZAS_AUFTGEBER_FIRMENNR AND PER_PERSONENID = ZAS_AUFTGEBER_PERSONENID" "~"
if joinKuerzel = PERZAS
searchResult := searchResult "LEFT JOIN BUERO.ZAS_ZAHLSCHEIN ON ZAS_AUFTGEBER_FIRMENNR = PER_FIRMENNR AND ZAS_AUFTGEBER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = ZLGPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = ZLG_PER_FIRMENNR AND PER_PERSONENID = ZLG_PERSONENID" "~"
if joinKuerzel = PERZLG
searchResult := searchResult "LEFT JOIN ZLE.ZLG_ZAHLUNGEN ON ZLG_PER_FIRMENNR = PER_FIRMENNR AND ZLG_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = BSGPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = BSG_MODERATOR_FIRMENNR AND PER_PERSONENID = BSG_MODERATOR_PERSONENID" "~"
if joinKuerzel = PERBSG
searchResult := searchResult "LEFT JOIN BUERO.BSG_BESPRECHUNG ON BSG_MODERATOR_FIRMENNR = PER_FIRMENNR AND BSG_MODERATOR_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = EWLPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = EWL_KONTAKT_FIRMENNR AND PER_PERSONENID = EWL_KONTAKT_PERSONENID" "~"
if joinKuerzel = PEREWL
searchResult := searchResult "LEFT JOIN BUERO.EWL_EVENTWARTELISTE ON EWL_KONTAKT_FIRMENNR = PER_FIRMENNR AND EWL_KONTAKT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = STHPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = STH_KON_FIRMENNR AND PER_PERSONENID = STH_KON_KONTAKTPERSID" "~"
if joinKuerzel = PERSTH
searchResult := searchResult "LEFT JOIN PRODUKTION.STH_STAKE ON STH_KON_FIRMENNR = PER_FIRMENNR AND STH_KON_KONTAKTPERSID = PER_PERSONENID" "~"
if joinKuerzel = TOOPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = TOO_SPEDITION_FIRMENNR AND PER_PERSONENID = TOO_SPEDITION_KONPERSID" "~"
if joinKuerzel = PERTOO
searchResult := searchResult "LEFT JOIN WWS.TOO_TOUROBJEKT ON TOO_SPEDITION_FIRMENNR = PER_FIRMENNR AND TOO_SPEDITION_KONPERSID = PER_PERSONENID" "~"
if joinKuerzel = WAMPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = WAM_RG_FIRMENNR AND PER_PERSONENID = WAM_RG_PERSONENID" "~"
if joinKuerzel = PERWAM
searchResult := searchResult "LEFT JOIN BUERO.WAM_WEBANMELDUNG ON WAM_RG_FIRMENNR = PER_FIRMENNR AND WAM_RG_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = ZASPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = ZAS_EMPFAENGER_FIRMENNR AND PER_PERSONENID = ZAS_EMPFAENGER_PERSONENID" "~"
if joinKuerzel = PERZAS
searchResult := searchResult "LEFT JOIN BUERO.ZAS_ZAHLSCHEIN ON ZAS_EMPFAENGER_FIRMENNR = PER_FIRMENNR AND ZAS_EMPFAENGER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = AUGPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = AUG_VERANTW_FIRMENNR AND PER_PERSONENID = AUG_VERANTW_PERSONENID" "~"
if joinKuerzel = PERAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_VERANTW_FIRMENNR = PER_FIRMENNR AND AUG_VERANTW_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = BONPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = BON_ZRECHNUNGEMPF_FIRMENNR AND PER_PERSONENID = BON_ZRECHNUNGEMPF_KLID" "~"
if joinKuerzel = PERBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_ZRECHNUNGEMPF_FIRMENNR = PER_FIRMENNR AND BON_ZRECHNUNGEMPF_KLID = PER_PERSONENID" "~"
if joinKuerzel = EFKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = EFK_RECHEMPF_KONT_FIRMENNR AND PER_PERSONENID = EFK_RECHEMPF_KONTAKTPERSID" "~"
if joinKuerzel = PEREFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_RECHEMPF_KONT_FIRMENNR = PER_FIRMENNR AND EFK_RECHEMPF_KONTAKTPERSID = PER_PERSONENID" "~"
if joinKuerzel = EVTPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = EVT_KUND_FIRMENNR AND PER_PERSONENID = EVT_KUND_PERSONENID" "~"
if joinKuerzel = PEREVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_KUND_FIRMENNR = PER_FIRMENNR AND EVT_KUND_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = FTBPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = FTB_ZIEL_PER_FIRMENNR AND PER_PERSONENID = FTB_ZIEL_PER_PERSONENID" "~"
if joinKuerzel = PERFTB
searchResult := searchResult "LEFT JOIN BUERO.FTB_FAHRTENBUCH ON FTB_ZIEL_PER_FIRMENNR = PER_FIRMENNR AND FTB_ZIEL_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PROPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PRO_V1_FIRMENNR AND PER_PERSONENID = PRO_V1_PERSONENID" "~"
if joinKuerzel = PERPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_V1_FIRMENNR = PER_FIRMENNR AND PRO_V1_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = LEIPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = LEI_ORG_PER_FIRMENNR AND PER_PERSONENID = LEI_ORG_PERSONENID" "~"
if joinKuerzel = PERLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_ORG_PER_FIRMENNR = PER_FIRMENNR AND LEI_ORG_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PERPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PER_EHEGATTE_FIRMENNR AND PER_PERSONENID = PER_EHEGATTE_PERSONENID" "~"
if joinKuerzel = PERPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_EHEGATTE_FIRMENNR = PER_FIRMENNR AND PER_EHEGATTE_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = WSKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = WSK_AUFTRAGGEBER_FIRMENNR AND PER_PERSONENID = WSK_AUFTRAGGEBER_KLID" "~"
if joinKuerzel = PERWSK
searchResult := searchResult "LEFT JOIN WWS.WSK_WWSKORESTATISTIK ON WSK_AUFTRAGGEBER_FIRMENNR = PER_FIRMENNR AND WSK_AUFTRAGGEBER_KLID = PER_PERSONENID" "~"
if joinKuerzel = EFKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = EFK_SPEDITION_FIRMENNR AND PER_PERSONENID = EFK_SPEDITION_KON_PERSID" "~"
if joinKuerzel = PEREFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_SPEDITION_FIRMENNR = PER_FIRMENNR AND EFK_SPEDITION_KON_PERSID = PER_PERSONENID" "~"
if joinKuerzel = PROPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PRO_V3_FIRMENNR AND PER_PERSONENID = PRO_V3_PERSONENID" "~"
if joinKuerzel = PERPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_V3_FIRMENNR = PER_FIRMENNR AND PRO_V3_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = EFKRLK
searchResult := searchResult "LEFT JOIN WWS.RLK_RECHNUNGSPLANKOPF ON RLK_RLKID = EFK_RECHNUNGSPLANKOPFID" "~"
if joinKuerzel = RLKEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_RECHNUNGSPLANKOPFID = RLK_RLKID" "~"
if joinKuerzel = ZBURMD
searchResult := searchResult "LEFT JOIN BUERO.RMD_RUNDUNGSMODELLDET ON RMD_FIRMENNR = ZBU_ASW_FIRMENNR AND RMD_RMODELLNR = ZBU_RMODELLNR AND RMD_RMODELLDETAILNR = ZBU_RMODELLDETAILNR" "~"
if joinKuerzel = RMDZBU
searchResult := searchResult "LEFT JOIN BUERO.ZBU_ZEITBUCHUNG ON ZBU_ASW_FIRMENNR = RMD_FIRMENNR AND ZBU_RMODELLNR = RMD_RMODELLNR AND ZBU_RMODELLDETAILNR = RMD_RMODELLDETAILNR" "~"
if joinKuerzel = RMDRMO
searchResult := searchResult "LEFT JOIN BUERO.RMO_RUNDUNGSMODELL ON RMO_FIRMENNR = RMD_FIRMENNR AND RMO_RMODELLNR = RMD_RMODELLNR" "~"
if joinKuerzel = RMORMD
searchResult := searchResult "LEFT JOIN BUERO.RMD_RUNDUNGSMODELLDET ON RMD_FIRMENNR = RMO_FIRMENNR AND RMD_RMODELLNR = RMO_RMODELLNR" "~"
if joinKuerzel = EPSRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = EPS_RNP_FIRMENNR AND RNG_RECHNUNGLFDNR = EPS_RECHNUNGLFDNR" "~"
if joinKuerzel = RNGEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_RNP_FIRMENNR = RNG_FIRMENNR AND EPS_RECHNUNGLFDNR = RNG_RECHNUNGLFDNR" "~"
if joinKuerzel = KAKRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = KAK_RNG_FIRMENNR AND RNG_RECHNUNGLFDNR = KAK_RNG_RECHNUNGLFDNR" "~"
if joinKuerzel = RNGKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_RNG_FIRMENNR = RNG_FIRMENNR AND KAK_RNG_RECHNUNGLFDNR = RNG_RECHNUNGLFDNR" "~"
if joinKuerzel = RNARNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = RNA_FIRMENNR AND RNG_RECHNUNGLFDNR = RNA_RECHNUNGLFDNR" "~"
if joinKuerzel = RNGRNA
searchResult := searchResult "LEFT JOIN WWS.RNA_RECHNUNGANZAHLUNG ON RNA_FIRMENNR = RNG_FIRMENNR AND RNA_RECHNUNGLFDNR = RNG_RECHNUNGLFDNR" "~"
if joinKuerzel = EPSRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = EPS_RNP_FIRMENNR AND RNP_RECHNUNGLFDNR = EPS_RECHNUNGLFDNR AND RNP_RECHNUNGSPOSLFDNR = EPS_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = RNPEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_RNP_FIRMENNR = RNP_FIRMENNR AND EPS_RECHNUNGLFDNR = RNP_RECHNUNGLFDNR AND EPS_RECHNUNGSPOSLFDNR = RNP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = ASRRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = ASR_REPORTNR" "~"
if joinKuerzel = RPTASR
searchResult := searchResult "LEFT JOIN BUERO.ASR_AUSSCHREIBUNG ON ASR_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = FKTRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = FKT_REPORTNR" "~"
if joinKuerzel = RPTFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = GLSRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = GLS_REPORTNR" "~"
if joinKuerzel = RPTGLS
searchResult := searchResult "LEFT JOIN BIL.GLS_GLIEDERUNGSSCHEMA ON GLS_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = KADRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = KAD_REPORTNR" "~"
if joinKuerzel = RPTKAD
searchResult := searchResult "LEFT JOIN KORE.KAD_AUSDRUCKDETAIL ON KAD_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = KASRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = KAS_BERICHTE_GZREPNR" "~"
if joinKuerzel = RPTKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_BERICHTE_GZREPNR = RPT_REPORTNR" "~"
if joinKuerzel = SRERPT
searchResult := searchResult "LEFT JOIN BMD.RPT_REPORT ON RPT_REPORTNR = SRE_FUSS_REPORTNR" "~"
if joinKuerzel = RPTSRE
searchResult := searchResult "LEFT JOIN BMD.SRE_STANDARDREPORT ON SRE_FUSS_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = RSKRSD
searchResult := searchResult "LEFT JOIN WWS.RSD_REGKASPROTOKOLL ON RSD_REGKASPROTOKOLLID = RSK_RSD_STOERUNGBEGINN" "~"
if joinKuerzel = RSDRSK
searchResult := searchResult "LEFT JOIN WWS.RSK_REGISTRIERKASSE ON RSK_RSD_STOERUNGBEGINN = RSD_REGKASPROTOKOLLID" "~"
if joinKuerzel = ALSRUK
searchResult := searchResult "LEFT JOIN WWS.RUK_RUNDUNGKOPF ON RUK_FIRMENNR = ALS_RUK_FIRMENNR AND RUK_RUNDUNGSKOPFNR = ALS_RUK_RUNDUNGSKOPFNR" "~"
if joinKuerzel = RUKALS
searchResult := searchResult "LEFT JOIN WWS.ALS_ALLERGEN ON ALS_RUK_FIRMENNR = RUK_FIRMENNR AND ALS_RUK_RUNDUNGSKOPFNR = RUK_RUNDUNGSKOPFNR" "~"
if joinKuerzel = VSRRVC
searchResult := searchResult "LEFT JOIN FIBU.RVC_RVCODE ON RVC_LANDNR = VSR_RVCODE_LANDNR AND RVC_RVCCODE = VSR_RVCODE2" "~"
if joinKuerzel = RVCVSR
searchResult := searchResult "LEFT JOIN FIBU.VSR_VSTERSTATTUNG ON VSR_RVCODE_LANDNR = RVC_LANDNR AND VSR_RVCODE2 = RVC_RVCCODE" "~"
if joinKuerzel = VSRRVC
searchResult := searchResult "LEFT JOIN FIBU.RVC_RVCODE ON RVC_LANDNR = VSR_RVCODE_LANDNR AND RVC_RVCCODE = VSR_RVCODE4" "~"
if joinKuerzel = RVCVSR
searchResult := searchResult "LEFT JOIN FIBU.VSR_VSTERSTATTUNG ON VSR_RVCODE_LANDNR = RVC_LANDNR AND VSR_RVCODE4 = RVC_RVCCODE" "~"
if joinKuerzel = SA3SA1
searchResult := searchResult "LEFT JOIN ONVIS.SA1_SAURER_BDE ON SA1_FIRMENNR = SA3_FIRMENNR AND SA1_SAURERID = SA3_SAURERID" "~"
if joinKuerzel = SA1SA3
searchResult := searchResult "LEFT JOIN ONVIS.SA3_SAURER_BDE ON SA3_FIRMENNR = SA1_FIRMENNR AND SA3_SAURERID = SA1_SAURERID" "~"
if joinKuerzel = AZUSAG
searchResult := searchResult "LEFT JOIN WWS.SAG_STATISTIKARTIKELGRP ON SAG_FIRMENNR = AZU_SAG_FIRMENNR AND SAG_STATISTIKLFDNR = AZU_SAG_STATISTIKLFDNR AND SAG_STATISTIKGRPLFDNR = AZU_SAG_STATISTIKGRPLFDNR" "~"
if joinKuerzel = SAGAZU
searchResult := searchResult "LEFT JOIN WWS.AZU_STATISTIKARTIKELZU ON AZU_SAG_FIRMENNR = SAG_FIRMENNR AND AZU_SAG_STATISTIKLFDNR = SAG_STATISTIKLFDNR AND AZU_SAG_STATISTIKGRPLFDNR = SAG_STATISTIKGRPLFDNR" "~"
if joinKuerzel = IAPSAG
searchResult := searchResult "LEFT JOIN WWS.SAG_STATISTIKARTIKELGRP ON SAG_FIRMENNR = IAP_SAG_FIRMENNR AND SAG_STATISTIKLFDNR = IAP_STATISTIKLFDNR AND SAG_STATISTIKGRPLFDNR = IAP_STATISTIKGRPLFDNR" "~"
if joinKuerzel = SAGIAP
searchResult := searchResult "LEFT JOIN WWS.IAP_INVABWTPOS ON IAP_SAG_FIRMENNR = SAG_FIRMENNR AND IAP_STATISTIKLFDNR = SAG_STATISTIKLFDNR AND IAP_STATISTIKGRPLFDNR = SAG_STATISTIKGRPLFDNR" "~"
if joinKuerzel = ARLSAI
searchResult := searchResult "LEFT JOIN WWS.SAI_SAISON ON SAI_FIRMENNR = ARL_FIRMENNR AND SAI_SAISONNR = ARL_SAISONNR" "~"
if joinKuerzel = SAIARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = SAI_FIRMENNR AND ARL_SAISONNR = SAI_SAISONNR" "~"
if joinKuerzel = PWPPTP
searchResult := searchResult "LEFT JOIN WWS.PTP_PREISTABPOS ON PTP_FIRMENNR = PWP_PTP_UEB_FIRMENNR AND PTP_PREISTABNR = PWP_PTP_UEB_PREISTABNR AND PTP_PREISTABPOSNR = PWP_PTP_UEB_PREISTABPOSNR" "~"
if joinKuerzel = PTPPWP
searchResult := searchResult "LEFT JOIN WWS.PWP_PREISWARTUNGPOS ON PWP_PTP_UEB_FIRMENNR = PTP_FIRMENNR AND PWP_PTP_UEB_PREISTABNR = PTP_PREISTABNR AND PWP_PTP_UEB_PREISTABPOSNR = PTP_PREISTABPOSNR" "~"
if joinKuerzel = PWEPWK
searchResult := searchResult "LEFT JOIN WWS.PWK_PREISRABATTWARTKOPF ON PWK_PWKID = PWE_PWKID" "~"
if joinKuerzel = PWKPWE
searchResult := searchResult "LEFT JOIN WWS.PWE_PWKPREISTABZU ON PWE_PWKID = PWK_PWKID" "~"
if joinKuerzel = PWRPWK
searchResult := searchResult "LEFT JOIN WWS.PWK_PREISRABATTWARTKOPF ON PWK_PWKID = PWR_PWKID" "~"
if joinKuerzel = PWKPWR
searchResult := searchResult "LEFT JOIN WWS.PWR_RABATTWARTUNGPOS ON PWR_PWKID = PWK_PWKID" "~"
if joinKuerzel = RZZRAG
searchResult := searchResult "LEFT JOIN BUERO.RAG_RUHEARBEITSZEITGESETZ ON RAG_RAGID = RZZ_RAGID" "~"
if joinKuerzel = RAGRZZ
searchResult := searchResult "LEFT JOIN BUERO.RZZ_AZGARGZEITEN ON RZZ_RAGID = RAG_RAGID" "~"
if joinKuerzel = PWRRAP
searchResult := searchResult "LEFT JOIN WWS.RAP_RABATTTABPOS ON RAP_FIRMENNR = PWR_RAP_UEB_FIRMENNR AND RAP_RABTABNR = PWR_RAP_UEB_RABTABNR AND RAP_RABTABPOSNR = PWR_RAP_UEB_RABTABPOSNR" "~"
if joinKuerzel = RAPPWR
searchResult := searchResult "LEFT JOIN WWS.PWR_RABATTWARTUNGPOS ON PWR_RAP_UEB_FIRMENNR = RAP_FIRMENNR AND PWR_RAP_UEB_RABTABNR = RAP_RABTABNR AND PWR_RAP_UEB_RABTABPOSNR = RAP_RABTABPOSNR" "~"
if joinKuerzel = PWRRAS
searchResult := searchResult "LEFT JOIN WWS.RAS_RABATTTABPOSSTAFFEL ON RAS_FIRMENNR = PWR_RAP_FIRMENNR AND RAS_RABTABNR = PWR_RAP_RABTABNR AND RAS_RABTABPOSNR = PWR_RAP_RABTABPOSNR AND RAS_RABATTSTAFFELNR = PWR_RAS_RABATTSTAFFELNR" "~"
if joinKuerzel = RASPWR
searchResult := searchResult "LEFT JOIN WWS.PWR_RABATTWARTUNGPOS ON PWR_RAP_FIRMENNR = RAS_FIRMENNR AND PWR_RAP_RABTABNR = RAS_RABTABNR AND PWR_RAP_RABTABPOSNR = RAS_RABTABPOSNR AND PWR_RAS_RABATTSTAFFELNR = RAS_RABATTSTAFFELNR" "~"
if joinKuerzel = RSPRDM
searchResult := searchResult "LEFT JOIN BIL.RDM_RSTDRUCKMASTER ON RDM_RDMNR = RSP_RDMNR" "~"
if joinKuerzel = RDMRSP
searchResult := searchResult "LEFT JOIN BIL.RSP_RSTSPALTENKOPF ON RSP_RDMNR = RDM_RDMNR" "~"
if joinKuerzel = TOZREG
searchResult := searchResult "LEFT JOIN REG_REGION.REG_REGION ON REG_LANDNR = TOZ_REG_LANDNR AND REG_REGIONNR = TOZ_REG_REGIONNR" "~"
if joinKuerzel = REGTOZ
searchResult := searchResult "LEFT JOIN WWS.TOZ_TOURZUORDNUNG ON TOZ_REG_LANDNR = REG_LANDNR AND TOZ_REG_REGIONNR = REG_REGIONNR" "~"
if joinKuerzel = AFTREI
searchResult := searchResult "LEFT JOIN REI_REISESTAMM.REI_REISESTAMM ON REI_REISENUMMER = AFT_REI_REISENUMMER" "~"
if joinKuerzel = REIAFT
searchResult := searchResult "LEFT JOIN ZLE.AFT_AUFBEREITUNG ON AFT_REI_REISENUMMER = REI_REISENUMMER" "~"
if joinKuerzel = DRGREI
searchResult := searchResult "LEFT JOIN HRS.REI_REISESTAMM ON REI_REISENUMMER = DRG_DIENSTREISENR" "~"
if joinKuerzel = REIDRG
searchResult := searchResult "LEFT JOIN HRS.DRG_DIENSTREISEGEN ON DRG_DIENSTREISENR = REI_REISENUMMER" "~"
if joinKuerzel = PVFREL
searchResult := searchResult "LEFT JOIN BUERO.REL_RELIGION ON REL_RELIGIONSNR = PVF_RELIGIONSNR" "~"
if joinKuerzel = RELPVF
searchResult := searchResult "LEFT JOIN BUERO.PVF_PERSONENVORERFASSUNG ON PVF_RELIGIONSNR = REL_RELIGIONSNR" "~"
if joinKuerzel = RZUREP
searchResult := searchResult "LEFT JOIN BUERO.REP_RESSOURCENPAKET ON REP_RESPAKETID = RZU_RESPAKETID" "~"
if joinKuerzel = REPRZU
searchResult := searchResult "LEFT JOIN BUERO.RZU_RESSOURCENPAKETZU ON RZU_RESPAKETID = REP_RESPAKETID" "~"
if joinKuerzel = SBSREP
searchResult := searchResult "LEFT JOIN BUERO.REP_RESSOURCENPAKET ON REP_RESPAKETID = SBS_RESPAKETID" "~"
if joinKuerzel = REPSBS
searchResult := searchResult "LEFT JOIN BUERO.SBS_STELLENBESCHREIBUNG ON SBS_RESPAKETID = REP_RESPAKETID" "~"
if joinKuerzel = EPSRER
searchResult := searchResult "LEFT JOIN WWS.RER_RESERVIERUNGSREFERENZ ON RER_RESERVIERUNGS_ID_BMD = EPS_RESERVIERUNGS_ID" "~"
if joinKuerzel = REREPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_RESERVIERUNGS_ID = RER_RESERVIERUNGS_ID_BMD" "~"
if joinKuerzel = FIWRFM
searchResult := searchResult "LEFT JOIN BUERO.RFM_RECHTSFORM ON RFM_LANDNR = FIW_RFM_LANDNR AND RFM_RECHTSFORMNR = FIW_RFM_RECHTSFORMNR" "~"
if joinKuerzel = RFMFIW
searchResult := searchResult "LEFT JOIN BUERO.FIW_FIRMENWERTE ON FIW_RFM_LANDNR = RFM_LANDNR AND FIW_RFM_RECHTSFORMNR = RFM_RECHTSFORMNR" "~"
if joinKuerzel = EFKRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = EFK_FIRMENNR AND RNG_RECHNUNGLFDNR = EFK_LETZTE_RECHNUNGLFDNR" "~"
if joinKuerzel = RNGEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = RNG_FIRMENNR AND EFK_LETZTE_RECHNUNGLFDNR = RNG_RECHNUNGLFDNR" "~"
if joinKuerzel = KAERNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = KAE_RNG_FIRMENNR AND RNG_RECHNUNGLFDNR = KAE_RNG_RECHNUNGLFDNR" "~"
if joinKuerzel = RNGKAE
searchResult := searchResult "LEFT JOIN WWS.KAE_KASSENEINAUSGABEN ON KAE_RNG_FIRMENNR = RNG_FIRMENNR AND KAE_RNG_RECHNUNGLFDNR = RNG_RECHNUNGLFDNR" "~"
if joinKuerzel = KUKRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = KUK_VERRECH_FIRMENNR AND RNG_RECHNUNGLFDNR = KUK_LETZTE_RECHNUNGLFDNR" "~"
if joinKuerzel = RNGKUK
searchResult := searchResult "LEFT JOIN WWS.KUK_KUNDENKARTEI ON KUK_VERRECH_FIRMENNR = RNG_FIRMENNR AND KUK_LETZTE_RECHNUNGLFDNR = RNG_RECHNUNGLFDNR" "~"
if joinKuerzel = LPORNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = LPO_FIRMENNR AND RNG_RECHNUNGLFDNR = LPO_RECHNUNGLFDNR" "~"
if joinKuerzel = RNGLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_FIRMENNR = RNG_FIRMENNR AND LPO_RECHNUNGLFDNR = RNG_RECHNUNGLFDNR" "~"
if joinKuerzel = RNGRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = RNG_FIRMENNR AND RNG_RECHNUNGLFDNR = RNG_STORNO_RECHNUNGLFDNR" "~"
if joinKuerzel = RNGRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = RNG_FIRMENNR AND RNG_STORNO_RECHNUNGLFDNR = RNG_RECHNUNGLFDNR" "~"
if joinKuerzel = EAPRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = EAP_RECHNUNG_FIRMENNR AND RNP_RECHNUNGLFDNR = EAP_RECHNUNGLFDNR AND RNP_RECHNUNGSPOSLFDNR = EAP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = RNPEAP
searchResult := searchResult "LEFT JOIN WWS.EAP_ERLOESABGRENZUNGSPOS ON EAP_RECHNUNG_FIRMENNR = RNP_FIRMENNR AND EAP_RECHNUNGLFDNR = RNP_RECHNUNGLFDNR AND EAP_RECHNUNGSPOSLFDNR = RNP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = WAPPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = WAP_PRO_FIRMENNR AND PRO_PROJEKTNR = WAP_PROJEKTNR AND PRO_PROJEKTTYP = WAP_PROJEKTTYP" "~"
if joinKuerzel = PROWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_PRO_FIRMENNR = PRO_FIRMENNR AND WAP_PROJEKTNR = PRO_PROJEKTNR AND WAP_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = ZBAPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = ZBA_PRO_FIRMENNR AND PRO_PROJEKTNR = ZBA_PROJEKTNR AND PRO_PROJEKTTYP = ZBA_PROJEKTTYP" "~"
if joinKuerzel = PROZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_PRO_FIRMENNR = PRO_FIRMENNR AND ZBA_PROJEKTNR = PRO_PROJEKTNR AND ZBA_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = PPZPRS
searchResult := searchResult "LEFT JOIN PRS_PROZESS.PRS_PROZESS ON PRS_FIRMENNR = PPZ_PRS_FIRMENNR AND PRS_PROZESSID = PPZ_PRS_PROZESSID" "~"
if joinKuerzel = PRSPPZ
searchResult := searchResult "LEFT JOIN BUERO.PPZ_PLANGRUPPEPROZESSZU ON PPZ_PRS_FIRMENNR = PRS_FIRMENNR AND PPZ_PRS_PROZESSID = PRS_PROZESSID" "~"
if joinKuerzel = PRZPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_FIRMENNR = PRZ_FIRMENNR AND PRS_PROZESSID = PRZ_PROZESSID_ALTERNATIV_1" "~"
if joinKuerzel = PRSPRZ
searchResult := searchResult "LEFT JOIN PRODUKTION.PRZ_PROZESSBAZZU ON PRZ_FIRMENNR = PRS_FIRMENNR AND PRZ_PROZESSID_ALTERNATIV_1 = PRS_PROZESSID" "~"
if joinKuerzel = VFDPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_FIRMENNR = VFD_PRS_FIRMENNR AND PRS_PROZESSID = VFD_PROZESSID" "~"
if joinKuerzel = PRSVFD
searchResult := searchResult "LEFT JOIN PRODUKTION.VFD_VERFAHRENSANWEISUNG_POS ON VFD_PRS_FIRMENNR = PRS_FIRMENNR AND VFD_PROZESSID = PRS_PROZESSID" "~"
if joinKuerzel = PRZPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_FIRMENNR = PRZ_FIRMENNR AND PRS_PROZESSID = PRZ_PROZESSID_ALTERNATIV_2" "~"
if joinKuerzel = PRSPRZ
searchResult := searchResult "LEFT JOIN PRODUKTION.PRZ_PROZESSBAZZU ON PRZ_FIRMENNR = PRS_FIRMENNR AND PRZ_PROZESSID_ALTERNATIV_2 = PRS_PROZESSID" "~"
if joinKuerzel = PRZPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_FIRMENNR = PRZ_FIRMENNR AND PRS_PROZESSID = PRZ_PROZESSID_ALTERNATIV_5" "~"
if joinKuerzel = PRSPRZ
searchResult := searchResult "LEFT JOIN PRODUKTION.PRZ_PROZESSBAZZU ON PRZ_FIRMENNR = PRS_FIRMENNR AND PRZ_PROZESSID_ALTERNATIV_5 = PRS_PROZESSID" "~"
if joinKuerzel = PRWPRT
searchResult := searchResult "LEFT JOIN BUERO.PRT_PROJEKTTAETIGKEIT ON PRT_PTNR = PRW_PTNR" "~"
if joinKuerzel = PRTPRW
searchResult := searchResult "LEFT JOIN BUERO.PRW_PROJEKTWERT ON PRW_PTNR = PRT_PTNR" "~"
if joinKuerzel = BSDPSG
searchResult := searchResult "LEFT JOIN WWS.PSG_PREISGRUPPE ON PSG_FIRMENNR = BSD_PSG_FIRMENNR AND PSG_PREISHAUPTGRNR = BSD_PREISHAUPTGRNR AND PSG_PREISUNTERGRNR = BSD_PREISUNTERGRNR" "~"
if joinKuerzel = PSGBSD
searchResult := searchResult "LEFT JOIN WWS.BSD_BONUSERMDETAIL ON BSD_PSG_FIRMENNR = PSG_FIRMENNR AND BSD_PREISHAUPTGRNR = PSG_PREISHAUPTGRNR AND BSD_PREISUNTERGRNR = PSG_PREISUNTERGRNR" "~"
if joinKuerzel = PWKPSG
searchResult := searchResult "LEFT JOIN WWS.PSG_PREISGRUPPE ON PSG_FIRMENNR = PWK_PSG_FIRMENNR AND PSG_PREISHAUPTGRNR = PWK_PSG_PREISHAUPTGRNR AND PSG_PREISUNTERGRNR = PWK_PSG_PREISUNTERGRNR" "~"
if joinKuerzel = PSGPWK
searchResult := searchResult "LEFT JOIN WWS.PWK_PREISRABATTWARTKOPF ON PWK_PSG_FIRMENNR = PSG_FIRMENNR AND PWK_PSG_PREISHAUPTGRNR = PSG_PREISHAUPTGRNR AND PWK_PSG_PREISUNTERGRNR = PSG_PREISUNTERGRNR" "~"
if joinKuerzel = ABMPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = ABM_PSJ_FIRMENNR AND PSJ_MITARBEITERID = ABM_PSJ_MITARBEITERID AND PSJ_DIENSTVLFDNR = ABM_PSJ_DIENSTVLFDNR AND PSJ_LOHNJAHR = ABM_PSJ_LOHNJAHR" "~"
if joinKuerzel = PSJABM
searchResult := searchResult "LEFT JOIN HRS.ABM_ABWESENHEIT ON ABM_PSJ_FIRMENNR = PSJ_FIRMENNR AND ABM_PSJ_MITARBEITERID = PSJ_MITARBEITERID AND ABM_PSJ_DIENSTVLFDNR = PSJ_DIENSTVLFDNR AND ABM_PSJ_LOHNJAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = MBMPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_FIRMENNR = MBM_FIRMENNR AND PSM_MITARBEITERID = MBM_MITARBEITERID AND PSM_DIENSTVLFDNR = MBM_DIENSTVLFDNR AND PSM_PSMJAHR = MBM_JAHR AND PSM_PSMMONAT = MBM_MONAT_AUFR AND PSM_PSMISTORG = MBM_PSMISTORG" "~"
if joinKuerzel = PSMMBM
searchResult := searchResult "LEFT JOIN HRS.MBM_MBGM ON MBM_FIRMENNR = PSM_FIRMENNR AND MBM_MITARBEITERID = PSM_MITARBEITERID AND MBM_DIENSTVLFDNR = PSM_DIENSTVLFDNR AND MBM_JAHR = PSM_PSMJAHR AND MBM_MONAT_AUFR = PSM_PSMMONAT AND MBM_PSMISTORG = PSM_PSMISTORG" "~"
if joinKuerzel = PWPPTS
searchResult := searchResult "LEFT JOIN WWS.PTS_PREISTABPOSSTAFFEL ON PTS_FIRMENNR = PWP_PTP_FIRMENNR AND PTS_PREISTABNR = PWP_PTP_PREISTABNR AND PTS_PREISTABPOSNR = PWP_PTP_PREISTABPOSNR AND PTS_PREISSTAFFELNR = PWP_PTS_PREISSTAFFELNR" "~"
if joinKuerzel = PTSPWP
searchResult := searchResult "LEFT JOIN WWS.PWP_PREISWARTUNGPOS ON PWP_PTP_FIRMENNR = PTS_FIRMENNR AND PWP_PTP_PREISTABNR = PTS_PREISTABNR AND PWP_PTP_PREISTABPOSNR = PTS_PREISTABPOSNR AND PWP_PTS_PREISSTAFFELNR = PTS_PREISSTAFFELNR" "~"
if joinKuerzel = PWAPWK
searchResult := searchResult "LEFT JOIN WWS.PWK_PREISRABATTWARTKOPF ON PWK_PWKID = PWA_PWKID" "~"
if joinKuerzel = PWKPWA
searchResult := searchResult "LEFT JOIN WWS.PWA_PWKARTIKELZU ON PWA_PWKID = PWK_PWKID" "~"
if joinKuerzel = QMAQST
searchResult := searchResult "LEFT JOIN PRODUKTION.QST_QM_STAMM ON QST_FIRMENNR = QMA_QST_FIRMENNR AND QST_QM_NR = QMA_QST_QM_NR" "~"
if joinKuerzel = QSTQMA
searchResult := searchResult "LEFT JOIN PRODUKTION.QMA_QM_ARTIKEL ON QMA_QST_FIRMENNR = QST_FIRMENNR AND QMA_QST_QM_NR = QST_QM_NR" "~"
if joinKuerzel = ERARAP
searchResult := searchResult "LEFT JOIN WWS.RAP_RABATTTABPOS ON RAP_FIRMENNR = ERA_RAP_FIRMENNR AND RAP_RABTABNR = ERA_RAP_RABTABNR AND RAP_RABTABPOSNR = ERA_RAP_RABTABPOSNR" "~"
if joinKuerzel = RAPERA
searchResult := searchResult "LEFT JOIN WWS.ERA_ERFASSRABATT ON ERA_RAP_FIRMENNR = RAP_FIRMENNR AND ERA_RAP_RABTABNR = RAP_RABTABNR AND ERA_RAP_RABTABPOSNR = RAP_RABTABPOSNR" "~"
if joinKuerzel = PWRRAS
searchResult := searchResult "LEFT JOIN WWS.RAS_RABATTTABPOSSTAFFEL ON RAS_FIRMENNR = PWR_RAP_UEB_FIRMENNR AND RAS_RABTABNR = PWR_RAP_UEB_RABTABNR AND RAS_RABTABPOSNR = PWR_RAP_UEB_RABTABPOSNR AND RAS_RABATTSTAFFELNR = PWR_RAS_UEB_RABATTSTAFFELNR" "~"
if joinKuerzel = RASPWR
searchResult := searchResult "LEFT JOIN WWS.PWR_RABATTWARTUNGPOS ON PWR_RAP_UEB_FIRMENNR = RAS_FIRMENNR AND PWR_RAP_UEB_RABTABNR = RAS_RABTABNR AND PWR_RAP_UEB_RABTABPOSNR = RAS_RABTABPOSNR AND PWR_RAS_UEB_RABATTSTAFFELNR = RAS_RABATTSTAFFELNR" "~"
if joinKuerzel = OBPREL
searchResult := searchResult "LEFT JOIN BUERO.REL_RELIGION ON REL_RELIGIONSNR = OBP_RELIGIONSNR" "~"
if joinKuerzel = RELOBP
searchResult := searchResult "LEFT JOIN BUERO.OBP_ONLBEWPERSSTAMM ON OBP_RELIGIONSNR = REL_RELIGIONSNR" "~"
if joinKuerzel = NTCLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = NTC_LANDNR" "~"
if joinKuerzel = LANNTC
searchResult := searchResult "LEFT JOIN BMDENTW.NTC_NTCSARTIKEL ON NTC_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = DRTDRK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DRK_DTABREPLGRUPPE ON DRK_RKGID = DRT_RKGID" "~"
if joinKuerzel = DRKDRT
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DRT_DTABREPLGTABELLEZU ON DRT_RKGID = DRK_RKGID" "~"
if joinKuerzel = AIDRKS
searchResult := searchResult "LEFT JOIN RKS_REPLIKATIONSSTANDORT.RKS_REPLIKATIONSSTANDORT ON RKS_REPLSTANDONR = AID_REPLSTANDONR" "~"
if joinKuerzel = RKSAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_REPLSTANDONR = RKS_REPLSTANDONR" "~"
if joinKuerzel = GSKRKS
searchResult := searchResult "LEFT JOIN RKS_REPLIKATIONSSTANDORT.RKS_REPLIKATIONSSTANDORT ON RKS_REPLSTANDONR = GSK_ERSTELLREPLSTANDONR" "~"
if joinKuerzel = RKSGSK
searchResult := searchResult "LEFT JOIN WWS.GSK_GUTSCHEINKOPF ON GSK_ERSTELLREPLSTANDONR = RKS_REPLSTANDONR" "~"
if joinKuerzel = GSPRKS
searchResult := searchResult "LEFT JOIN RKS_REPLIKATIONSSTANDORT.RKS_REPLIKATIONSSTANDORT ON RKS_REPLSTANDONR = GSP_ERSTELLREPLSTANDONR" "~"
if joinKuerzel = RKSGSP
searchResult := searchResult "LEFT JOIN WWS.GSP_GUTSCHEINPOSITION ON GSP_ERSTELLREPLSTANDONR = RKS_REPLSTANDONR" "~"
if joinKuerzel = KASRKS
searchResult := searchResult "LEFT JOIN RKS_REPLIKATIONSSTANDORT.RKS_REPLIKATIONSSTANDORT ON RKS_REPLSTANDONR = KAS_REPLSTANDONR" "~"
if joinKuerzel = RKSKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_REPLSTANDONR = RKS_REPLSTANDONR" "~"
if joinKuerzel = KEPRKS
searchResult := searchResult "LEFT JOIN RKS_REPLIKATIONSSTANDORT.RKS_REPLIKATIONSSTANDORT ON RKS_REPLSTANDONR = KEP_RKS_REPLSTANDONR" "~"
if joinKuerzel = RKSKEP
searchResult := searchResult "LEFT JOIN WWS.KEP_KASSENERFASSUNGSPROT ON KEP_RKS_REPLSTANDONR = RKS_REPLSTANDONR" "~"
if joinKuerzel = SNRRKS
searchResult := searchResult "LEFT JOIN RKS_REPLIKATIONSSTANDORT.RKS_REPLIKATIONSSTANDORT ON RKS_REPLSTANDONR = SNR_REPLSTANDONR" "~"
if joinKuerzel = RKSSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_REPLSTANDONR = RKS_REPLSTANDONR" "~"
if joinKuerzel = BPKRLK
searchResult := searchResult "LEFT JOIN WWS.RLK_RECHNUNGSPLANKOPF ON RLK_RLKID = BPK_RECHNUNGSPLANKOPFID" "~"
if joinKuerzel = RLKBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_RECHNUNGSPLANKOPFID = RLK_RLKID" "~"
if joinKuerzel = EPVRLP
searchResult := searchResult "LEFT JOIN WWS.RLP_RECHNUNGSPLANPOS ON RLP_RLKID = EPV_RLKID AND RLP_RLPID = EPV_RLPID" "~"
if joinKuerzel = RLPEPV
searchResult := searchResult "LEFT JOIN WWS.EPV_ERFASSPOSRECHVOR ON EPV_RLKID = RLP_RLKID AND EPV_RLPID = RLP_RLPID" "~"
if joinKuerzel = LFSRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = LFS_FIRMENNR AND RNG_RECHNUNGLFDNR = LFS_RECHNUNGLFDNR" "~"
if joinKuerzel = RNGLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_FIRMENNR = RNG_FIRMENNR AND LFS_RECHNUNGLFDNR = RNG_RECHNUNGLFDNR" "~"
if joinKuerzel = ADCRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = ADC_FIRMENNR AND RNP_RECHNUNGLFDNR = ADC_RECHNUNGLFDNR AND RNP_RECHNUNGSPOSLFDNR = ADC_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = RNPADC
searchResult := searchResult "LEFT JOIN WWS.ADC_ABGABENDETAILRECH ON ADC_FIRMENNR = RNP_FIRMENNR AND ADC_RECHNUNGLFDNR = RNP_RECHNUNGLFDNR AND ADC_RECHNUNGSPOSLFDNR = RNP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = BOARNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = BOA_RECHNUNGFIRMENNR AND RNP_RECHNUNGLFDNR = BOA_RECHNUNGLFDNR AND RNP_RECHNUNGSPOSLFDNR = BOA_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = RNPBOA
searchResult := searchResult "LEFT JOIN WWS.BOA_BONUSAUSSCHLUSS ON BOA_RECHNUNGFIRMENNR = RNP_FIRMENNR AND BOA_RECHNUNGLFDNR = RNP_RECHNUNGLFDNR AND BOA_RECHNUNGSPOSLFDNR = RNP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = KAPRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = KAP_RNP_FIRMENNR AND RNP_RECHNUNGLFDNR = KAP_RNP_RECHNUNGLFDNR AND RNP_RECHNUNGSPOSLFDNR = KAP_RNP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = RNPKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_RNP_FIRMENNR = RNP_FIRMENNR AND KAP_RNP_RECHNUNGLFDNR = RNP_RECHNUNGLFDNR AND KAP_RNP_RECHNUNGSPOSLFDNR = RNP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = RPRRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = RPR_FIRMENNR AND RNP_RECHNUNGLFDNR = RPR_RECHNUNGLFDNR AND RNP_RECHNUNGSPOSLFDNR = RPR_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = RNPRPR
searchResult := searchResult "LEFT JOIN WWS.RPR_RECHNUNGSPROVISION ON RPR_FIRMENNR = RNP_FIRMENNR AND RPR_RECHNUNGLFDNR = RNP_RECHNUNGLFDNR AND RPR_RECHNUNGSPOSLFDNR = RNP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = VEPRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_RECHNUNGSPOSID = VEP_RNP_RECHNUNGSPOSID" "~"
if joinKuerzel = RNPVEP
searchResult := searchResult "LEFT JOIN WWS.VEP_VERTRETERABRECHPOS ON VEP_RNP_RECHNUNGSPOSID = RNP_RECHNUNGSPOSID" "~"
if joinKuerzel = RRDRNR
searchResult := searchResult "LEFT JOIN WWS.RNR_RECHNUNGSRABATT ON RNR_FIRMENNR = RRD_FIRMENNR AND RNR_RECHNUNGRABATTNR = RRD_RECHNUNGRABATTNR" "~"
if joinKuerzel = RNRRRD
searchResult := searchResult "LEFT JOIN WWS.RRD_RECHNUNGSRABATTDETAIL ON RRD_FIRMENNR = RNR_FIRMENNR AND RRD_RECHNUNGRABATTNR = RNR_RECHNUNGRABATTNR" "~"
if joinKuerzel = R2ARPM
searchResult := searchResult "LEFT JOIN PRODUKTION.RPM_RETRO_PRODUKT_MATERIAL ON RPM_RPMID = R2A_RPMID" "~"
if joinKuerzel = RPMR2A
searchResult := searchResult "LEFT JOIN PRODUKTION.R2A_RPM_ARK_ZU ON R2A_RPMID = RPM_RPMID" "~"
if joinKuerzel = RPRRPR
searchResult := searchResult "LEFT JOIN WWS.RPR_RECHNUNGSPROVISION ON RPR_RECHPROVID = RPR_UEBERG_RECHPROVID" "~"
if joinKuerzel = RPRRPR
searchResult := searchResult "LEFT JOIN WWS.RPR_RECHNUNGSPROVISION ON RPR_UEBERG_RECHPROVID = RPR_RECHPROVID" "~"
if joinKuerzel = MOPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = MOP_POSTITNR" "~"
if joinKuerzel = PITMOP
searchResult := searchResult "LEFT JOIN WWS.MOP_MODELLPOSVAR ON MOP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = MWRPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = MWR_POSTITNR" "~"
if joinKuerzel = PITMWR
searchResult := searchResult "LEFT JOIN BIL.MWR_MWRDATEN ON MWR_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PLCPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PLC_POSTITNR" "~"
if joinKuerzel = PITPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PLOPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PLO_POSTITNR" "~"
if joinKuerzel = PITPLO
searchResult := searchResult "LEFT JOIN PRODUKTION.PLO_PPS_LOS ON PLO_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = QMAPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = QMA_POSTITNR" "~"
if joinKuerzel = PITQMA
searchResult := searchResult "LEFT JOIN PRODUKTION.QMA_QM_ARTIKEL ON QMA_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = RCLPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = RCL_POSTITNR" "~"
if joinKuerzel = PITRCL
searchResult := searchResult "LEFT JOIN PRODUKTION.RCL_REPEATING_CHECKLIST ON RCL_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = RNPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = RNP_POSTITNR" "~"
if joinKuerzel = PITRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = RNRPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = RNR_POSTITNR" "~"
if joinKuerzel = PITRNR
searchResult := searchResult "LEFT JOIN WWS.RNR_RECHNUNGSRABATT ON RNR_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = RPMPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = RPM_POSTITNR" "~"
if joinKuerzel = PITRPM
searchResult := searchResult "LEFT JOIN PRODUKTION.RPM_RETRO_PRODUKT_MATERIAL ON RPM_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = SBKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = SBK_POSTITNR" "~"
if joinKuerzel = PITSBK
searchResult := searchResult "LEFT JOIN WWS.SBK_SAISONBESCHAFFKOPF ON SBK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = SCGPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = SCG_POSTITNR" "~"
if joinKuerzel = PITSCG
searchResult := searchResult "LEFT JOIN PRODUKTION.SCG_SCORECARDKRITZU ON SCG_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = STBPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = STB_POSTITNR" "~"
if joinKuerzel = PITSTB
searchResult := searchResult "LEFT JOIN PRODUKTION.STB_STAKE_HEAD ON STB_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = STMPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = STM_POSTITNR" "~"
if joinKuerzel = PITSTM
searchResult := searchResult "LEFT JOIN BUERO.STM_STELLENMERKMALE ON STM_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = TREPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = TRE_POSTITNR" "~"
if joinKuerzel = PITTRE
searchResult := searchResult "LEFT JOIN WWS.TRE_TRANSPORTEINHEIT ON TRE_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = VEKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = VEK_POSTITNR" "~"
if joinKuerzel = PITVEK
searchResult := searchResult "LEFT JOIN WWS.VEK_VERTRETERABRECHKOPF ON VEK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = VMKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = VMK_POSTITNR" "~"
if joinKuerzel = PITVMK
searchResult := searchResult "LEFT JOIN WWS.VMK_VERSMGR_KOPF ON VMK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = ZBZPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = ZBZ_POSTITNR" "~"
if joinKuerzel = PITZBZ
searchResult := searchResult "LEFT JOIN BUERO.ZBZ_ZEITBEZUGSART ON ZBZ_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = ZRBPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = ZRB_POSTITNR" "~"
if joinKuerzel = PITZRB
searchResult := searchResult "LEFT JOIN BUERO.ZRB_ZEITRUNDUNGBEDINGUNG ON ZRB_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = KTOPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = KTO_POSTITNR" "~"
if joinKuerzel = PITKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = AIVPKA
searchResult := searchResult "LEFT JOIN PKA_PROJEKTART.PKA_PROJEKTART ON PKA_FIRMENNR = AIV_PKA_FIRMENNR AND PKA_PROJEKTARTNR = AIV_PKA_PROJEKTARTNR" "~"
if joinKuerzel = PKAAIV
searchResult := searchResult "LEFT JOIN WWS.AIV_INTERVALL_ARBEITSPLAN ON AIV_PKA_FIRMENNR = PKA_FIRMENNR AND AIV_PKA_PROJEKTARTNR = PKA_PROJEKTARTNR" "~"
if joinKuerzel = PKCDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = PKC_DBSNR" "~"
if joinKuerzel = DBSPKC
searchResult := searchResult "LEFT JOIN HRS.PKC_PENSKASSA_CALC ON PKC_DBSNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = PIXPKH
searchResult := searchResult "LEFT JOIN BIL.PKH_PERSKORSTHEADER ON PKH_FIRMENNR = PIX_FIRMENNR AND PKH_PKHMODELL = PIX_PKHMODELL AND PKH_PKHSTICHTAG = PIX_PKHSTICHTAG" "~"
if joinKuerzel = PKHPIX
searchResult := searchResult "LEFT JOIN BIL.PIX_PERSRSTIMP_FELDSPERRE ON PIX_FIRMENNR = PKH_FIRMENNR AND PIX_PKHMODELL = PKH_PKHMODELL AND PIX_PKHSTICHTAG = PKH_PKHSTICHTAG" "~"
if joinKuerzel = PKYPKH
searchResult := searchResult "LEFT JOIN BIL.PKH_PERSKORSTHEADER ON PKH_FIRMENNR = PKY_FIRMENNR AND PKH_PKHMODELL = PKY_MODELL AND PKH_PKHSTICHTAG = PKY_STICHTAG" "~"
if joinKuerzel = PKHPKY
searchResult := searchResult "LEFT JOIN BIL.PKY_PKH_PKK_ZU ON PKY_FIRMENNR = PKH_FIRMENNR AND PKY_MODELL = PKH_PKHMODELL AND PKY_STICHTAG = PKH_PKHSTICHTAG" "~"
if joinKuerzel = PKYPKK
searchResult := searchResult "LEFT JOIN BIL.PKK_PERSKORST_PARAMKOPF ON PKK_PKKSEQID = PKY_PKKID" "~"
if joinKuerzel = PKKPKY
searchResult := searchResult "LEFT JOIN BIL.PKY_PKH_PKK_ZU ON PKY_PKKID = PKK_PKKSEQID" "~"
if joinKuerzel = TPOPOS
searchResult := searchResult "LEFT JOIN BUERO.POS_POST ON POS_POSTNR = TPO_POSTNR AND POS_POSTKZ = TPO_POSTKZ" "~"
if joinKuerzel = POSTPO
searchResult := searchResult "LEFT JOIN BUERO.TPO_TERMINPOSTZU ON TPO_POSTNR = POS_POSTNR AND TPO_POSTKZ = POS_POSTKZ" "~"
if joinKuerzel = AUGPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = AUG_PRO_FIRMENNR AND PRO_PROJEKTNR = AUG_PROJEKTNR AND PRO_PROJEKTTYP = AUG_PROJEKTTYP" "~"
if joinKuerzel = PROAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_PRO_FIRMENNR = PRO_FIRMENNR AND AUG_PROJEKTNR = PRO_PROJEKTNR AND AUG_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = AWSPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = AWS_PRO_FIRMENNR AND PRO_PROJEKTNR = AWS_PRO_PROJEKTNR AND PRO_PROJEKTTYP = AWS_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_PRO_FIRMENNR = PRO_FIRMENNR AND AWS_PRO_PROJEKTNR = PRO_PROJEKTNR AND AWS_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = AZGPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = AZG_PRO_FIRMENNR AND PRO_PROJEKTNR = AZG_PROJEKTNR AND PRO_PROJEKTTYP = AZG_PROJEKTTYP" "~"
if joinKuerzel = PROAZG
searchResult := searchResult "LEFT JOIN BUERO.AZG_ABZUG ON AZG_PRO_FIRMENNR = PRO_FIRMENNR AND AZG_PROJEKTNR = PRO_PROJEKTNR AND AZG_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = BPPPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = BPP_PRO_FIRMENNR AND PRO_PROJEKTNR = BPP_PROJEKTNR AND PRO_PROJEKTTYP = BPP_PROJEKTTYP" "~"
if joinKuerzel = PROBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_PRO_FIRMENNR = PRO_FIRMENNR AND BPP_PROJEKTNR = PRO_PROJEKTNR AND BPP_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = BUCPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = BUC_PRO_FIRMENNR AND PRO_PROJEKTNR = BUC_PRO_PROJEKTNR AND PRO_PROJEKTTYP = BUC_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_PRO_FIRMENNR = PRO_FIRMENNR AND BUC_PRO_PROJEKTNR = PRO_PROJEKTNR AND BUC_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = DPRPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = DPR_PRO_FIRMENNR AND PRO_PROJEKTNR = DPR_PRO_PROJEKTNR AND PRO_PROJEKTTYP = DPR_PRO_PROJEKTTYP" "~"
if joinKuerzel = PRODPR
searchResult := searchResult "LEFT JOIN BUERO.DPR_DOKUMENTPROJEKTZU ON DPR_PRO_FIRMENNR = PRO_FIRMENNR AND DPR_PRO_PROJEKTNR = PRO_PROJEKTNR AND DPR_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = EKKPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = EKK_PRO_FIRMENNR AND PRO_PROJEKTNR = EKK_PRO_PROJEKTNR AND PRO_PROJEKTTYP = EKK_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROEKK
searchResult := searchResult "LEFT JOIN WWS.EKK_EXTERNE_KUNDENKARTEI ON EKK_PRO_FIRMENNR = PRO_FIRMENNR AND EKK_PRO_PROJEKTNR = PRO_PROJEKTNR AND EKK_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = ERKPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = ERK_PRO_FIRMENNR AND PRO_PROJEKTNR = ERK_PRO_PROJEKTNR AND PRO_PROJEKTTYP = ERK_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROERK
searchResult := searchResult "LEFT JOIN FIBU.ERK_KONTO ON ERK_PRO_FIRMENNR = PRO_FIRMENNR AND ERK_PRO_PROJEKTNR = PRO_PROJEKTNR AND ERK_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = FSTPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = FST_PRO_FIRMENNR AND PRO_PROJEKTNR = FST_PROJEKTNR AND PRO_PROJEKTTYP = FST_PROJEKTTYP" "~"
if joinKuerzel = PROFST
searchResult := searchResult "LEFT JOIN BUERO.FST_FRIST ON FST_PRO_FIRMENNR = PRO_FIRMENNR AND FST_PROJEKTNR = PRO_PROJEKTNR AND FST_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = LPOPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = LPO_PRO_FIRMENNR AND PRO_PROJEKTNR = LPO_PROJEKTNR AND PRO_PROJEKTTYP = LPO_PROJEKTTYP" "~"
if joinKuerzel = PROLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_PRO_FIRMENNR = PRO_FIRMENNR AND LPO_PROJEKTNR = PRO_PROJEKTNR AND LPO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = MEVPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = MEV_PRO_FIRMENNR AND PRO_PROJEKTNR = MEV_PROJEKTNR AND PRO_PROJEKTTYP = MEV_PROJEKTTYP" "~"
if joinKuerzel = PROMEV
searchResult := searchResult "LEFT JOIN BUERO.MEV_MEDIENVERLEIH ON MEV_PRO_FIRMENNR = PRO_FIRMENNR AND MEV_PROJEKTNR = PRO_PROJEKTNR AND MEV_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = PKLPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = PKL_PRO_FIRMENNR AND PRO_PROJEKTNR = PKL_PROJEKTNR AND PRO_PROJEKTTYP = PKL_PROJEKTTYP" "~"
if joinKuerzel = PROPKL
searchResult := searchResult "LEFT JOIN BUERO.PKL_PROJEKTPERSZU ON PKL_PRO_FIRMENNR = PRO_FIRMENNR AND PKL_PROJEKTNR = PRO_PROJEKTNR AND PKL_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = PMAPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = PMA_PRO_FIRMENNR AND PRO_PROJEKTNR = PMA_PROJEKTNR AND PRO_PROJEKTTYP = PMA_PROJEKTTYP" "~"
if joinKuerzel = PROPMA
searchResult := searchResult "LEFT JOIN BUERO.PMA_PROJEKTMITARBZU ON PMA_PRO_FIRMENNR = PRO_FIRMENNR AND PMA_PROJEKTNR = PRO_PROJEKTNR AND PMA_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = REDPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = RED_PRO_FIRMENNR AND PRO_PROJEKTNR = RED_PRO_PROJEKTNR AND PRO_PROJEKTTYP = RED_PRO_PROJEKTTYP" "~"
if joinKuerzel = PRORED
searchResult := searchResult "LEFT JOIN HRS.RED_REISEDETAIL ON RED_PRO_FIRMENNR = PRO_FIRMENNR AND RED_PRO_PROJEKTNR = PRO_PROJEKTNR AND RED_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = RNGPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = RNG_PRO_FIRMENNR AND PRO_PROJEKTNR = RNG_PROJEKTNR AND PRO_PROJEKTTYP = RNG_PROJEKTTYP" "~"
if joinKuerzel = PRORNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_PRO_FIRMENNR = PRO_FIRMENNR AND RNG_PROJEKTNR = PRO_PROJEKTNR AND RNG_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = TPRPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = TPR_PRO_FIRMENNR AND PRO_PROJEKTNR = TPR_PROJEKTNR AND PRO_PROJEKTTYP = TPR_PROJEKTTYP" "~"
if joinKuerzel = PROTPR
searchResult := searchResult "LEFT JOIN BUERO.TPR_TELEFONPROTOKOLL ON TPR_PRO_FIRMENNR = PRO_FIRMENNR AND TPR_PROJEKTNR = PRO_PROJEKTNR AND TPR_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = WIPPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = WIP_PRO_FIRMENNR AND PRO_PROJEKTNR = WIP_PROJEKTNR AND PRO_PROJEKTTYP = WIP_PROJEKTTYP" "~"
if joinKuerzel = PROWIP
searchResult := searchResult "LEFT JOIN BIL.WIP_WIPRUEFUNG ON WIP_PRO_FIRMENNR = PRO_FIRMENNR AND WIP_PROJEKTNR = PRO_PROJEKTNR AND WIP_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = EVTPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = EVT_BET_FIRMENNR AND PER_PERSONENID = EVT_BET_PERSONENID" "~"
if joinKuerzel = PEREVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_BET_FIRMENNR = PER_FIRMENNR AND EVT_BET_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = AKTPET
searchResult := searchResult "LEFT JOIN WWS.PET_PREISTAB ON PET_FIRMENNR = AKT_FIRMENNR AND PET_PREISTABNR = AKT_PREISTABNR" "~"
if joinKuerzel = PETAKT
searchResult := searchResult "LEFT JOIN WWS.AKT_AKTIONSSTAMM ON AKT_FIRMENNR = PET_FIRMENNR AND AKT_PREISTABNR = PET_PREISTABNR" "~"
if joinKuerzel = BPKPET
searchResult := searchResult "LEFT JOIN WWS.PET_PREISTAB ON PET_FIRMENNR = BPK_PREISTAB_FIRMENNR AND PET_PREISTABNR = BPK_PREISTABNR" "~"
if joinKuerzel = PETBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_PREISTAB_FIRMENNR = PET_FIRMENNR AND BPK_PREISTABNR = PET_PREISTABNR" "~"
if joinKuerzel = PTZPET
searchResult := searchResult "LEFT JOIN WWS.PET_PREISTAB ON PET_FIRMENNR = PTZ_PET_FIRMENNR AND PET_PREISTABNR = PTZ_PET_PREISTABNR" "~"
if joinKuerzel = PETPTZ
searchResult := searchResult "LEFT JOIN WWS.PTZ_PREISTABZU ON PTZ_PET_FIRMENNR = PET_FIRMENNR AND PTZ_PET_PREISTABNR = PET_PREISTABNR" "~"
if joinKuerzel = PGAPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_PICID = PGA_PICID AND PIA_PICARTIKELNR = PGA_PICARTIKELNR" "~"
if joinKuerzel = PIAPGA
searchResult := searchResult "LEFT JOIN WWS.PGA_PICARTIKELGRPZU ON PGA_PICID = PIA_PICID AND PGA_PICARTIKELNR = PIA_PICARTIKELNR" "~"
if joinKuerzel = PIRPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_PICID = PIR_PICID AND PIA_PICARTIKELNR = PIR_PICARTIKELNR" "~"
if joinKuerzel = PIAPIR
searchResult := searchResult "LEFT JOIN WWS.PIR_PICRABATTDETAIL ON PIR_PICID = PIA_PICID AND PIR_PICARTIKELNR = PIA_PICARTIKELNR" "~"
if joinKuerzel = PUIPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_PICID = PUI_PICID AND PIA_PICARTIKELNR = PUI_PICARTIKELNR" "~"
if joinKuerzel = PIAPUI
searchResult := searchResult "LEFT JOIN WWS.PUI_PICSTKL ON PUI_PICID = PIA_PICID AND PUI_PICARTIKELNR = PIA_PICARTIKELNR" "~"
if joinKuerzel = PFRPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_PICID = PFR_PICID" "~"
if joinKuerzel = PICPFR
searchResult := searchResult "LEFT JOIN WWS.PFR_PICFARBE ON PFR_PICID = PIC_PICID" "~"
if joinKuerzel = PIIPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_PICID = PII_PICID" "~"
if joinKuerzel = PICPII
searchResult := searchResult "LEFT JOIN WWS.PII_PICKUNDENGRUPPE ON PII_PICID = PIC_PICID" "~"
if joinKuerzel = PIKPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_PICID = PIK_PICID" "~"
if joinKuerzel = PICPIK
searchResult := searchResult "LEFT JOIN WWS.PIK_PICKATEGORIEZU ON PIK_PICID = PIC_PICID" "~"
if joinKuerzel = PILPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_PICID = PIL_PICID" "~"
if joinKuerzel = PICPIL
searchResult := searchResult "LEFT JOIN WWS.PIL_PICKUNDE ON PIL_PICID = PIC_PICID" "~"
if joinKuerzel = PINPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_PICID = PIN_PICID" "~"
if joinKuerzel = PICPIN
searchResult := searchResult "LEFT JOIN WWS.PIN_PICANBINDEARTZU ON PIN_PICID = PIC_PICID" "~"
if joinKuerzel = PIWPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_PICID = PIW_PICID" "~"
if joinKuerzel = PICPIW
searchResult := searchResult "LEFT JOIN WWS.PIW_PICWARENGRUPPE ON PIW_PICID = PIC_PICID" "~"
if joinKuerzel = PRNPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_PICID = PRN_PICID" "~"
if joinKuerzel = PICPRN
searchResult := searchResult "LEFT JOIN WWS.PRN_EXTPIAKEYNAMES ON PRN_PICID = PIC_PICID" "~"
if joinKuerzel = PIAPIG
searchResult := searchResult "LEFT JOIN WWS.PIG_PICPREISGRUPPE ON PIG_PICID = PIA_PICID AND PIG_PIGHAUPTGRNR = PIA_PIGHAUPTGRNR AND PIG_PIGUNTERGRNR = PIA_PIGUNTERGRNR" "~"
if joinKuerzel = PIGPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_PICID = PIG_PICID AND PIA_PIGHAUPTGRNR = PIG_PIGHAUPTGRNR AND PIA_PIGUNTERGRNR = PIG_PIGUNTERGRNR" "~"
if joinKuerzel = PIDPII
searchResult := searchResult "LEFT JOIN WWS.PII_PICKUNDENGRUPPE ON PII_PICID = PID_PICID AND PII_PIIHAUPTGRNR = PID_PIIHAUPTGRNR AND PII_PIIUNTERGRNR = PID_PIIUNTERGRNR" "~"
if joinKuerzel = PIIPID
searchResult := searchResult "LEFT JOIN WWS.PID_PICPREISDETAIL ON PID_PICID = PII_PICID AND PID_PIIHAUPTGRNR = PII_PIIHAUPTGRNR AND PID_PIIUNTERGRNR = PII_PIIUNTERGRNR" "~"
if joinKuerzel = PIDPIL
searchResult := searchResult "LEFT JOIN WWS.PIL_PICKUNDE ON PIL_PILID = PID_PILID" "~"
if joinKuerzel = PILPID
searchResult := searchResult "LEFT JOIN WWS.PID_PICPREISDETAIL ON PID_PILID = PIL_PILID" "~"
if joinKuerzel = AGZPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = AGZ_POSTITNR" "~"
if joinKuerzel = PITAGZ
searchResult := searchResult "LEFT JOIN WWS.AGZ_ARBEITSGANGZU ON AGZ_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = AKZPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = AKZ_POSTITNR" "~"
if joinKuerzel = PITAKZ
searchResult := searchResult "LEFT JOIN WWS.AKZ_ARTIKELKUNDENZU ON AKZ_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = AVEPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = AVE_PIT_POSTITNR2" "~"
if joinKuerzel = PITAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_PIT_POSTITNR2 = PIT_POSTITNR" "~"
if joinKuerzel = AWEPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = AWE_POSTITNR" "~"
if joinKuerzel = PITAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = BUDPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = BUD_POSTITNR" "~"
if joinKuerzel = PITBUD
searchResult := searchResult "LEFT JOIN FIBU.BUD_BUDGET ON BUD_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = DBXPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = DBX_POSTITNR" "~"
if joinKuerzel = PITDBX
searchResult := searchResult "LEFT JOIN BUERO.DBX_FINONDATABOX ON DBX_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = DKTPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = DKT_POSTITNR" "~"
if joinKuerzel = PITDKT
searchResult := searchResult "LEFT JOIN BUERO.DKT_DOCSKATEGORIE ON DKT_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = EFKRAT
searchResult := searchResult "LEFT JOIN WWS.RAT_RABATTTAB ON RAT_FIRMENNR = EFK_RABTAB_FIRMENNR AND RAT_RABTABNR = EFK_RABTABNR" "~"
if joinKuerzel = RATEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_RABTAB_FIRMENNR = RAT_FIRMENNR AND EFK_RABTABNR = RAT_RABTABNR" "~"
if joinKuerzel = WBTRAT
searchResult := searchResult "LEFT JOIN WWS.RAT_RABATTTAB ON RAT_FIRMENNR = WBT_RAT_FIRMENNR AND RAT_RABTABNR = WBT_RAT_RABTABNR" "~"
if joinKuerzel = RATWBT
searchResult := searchResult "LEFT JOIN WWS.WBT_WWSBUDGETKOPF ON WBT_RAT_FIRMENNR = RAT_FIRMENNR AND WBT_RAT_RABTABNR = RAT_RABTABNR" "~"
if joinKuerzel = RNGRAZ
searchResult := searchResult "LEFT JOIN RAZ_RATENZAHLUNG.RAZ_RATENZAHLUNG ON RAZ_RATENMODELLID = RNG_RATENMODELLID" "~"
if joinKuerzel = RAZRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_RATENMODELLID = RAZ_RATENMODELLID" "~"
if joinKuerzel = AWSREI
searchResult := searchResult "LEFT JOIN REI_REISESTAMM.REI_REISESTAMM ON REI_REISENUMMER = AWS_REI_REISENUMMER" "~"
if joinKuerzel = REIAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_REI_REISENUMMER = REI_REISENUMMER" "~"
if joinKuerzel = FTGREL
searchResult := searchResult "LEFT JOIN BUERO.REL_RELIGION ON REL_RELIGIONSNR = FTG_RELIGIONSNR" "~"
if joinKuerzel = RELFTG
searchResult := searchResult "LEFT JOIN BUERO.FTG_FEIERTAG ON FTG_RELIGIONSNR = REL_RELIGIONSNR" "~"
if joinKuerzel = ONSREL
searchResult := searchResult "LEFT JOIN REL_RELIGION.REL_RELIGION ON REL_RELIGIONSNR = ONS_RELIGIONSNR" "~"
if joinKuerzel = RELONS
searchResult := searchResult "LEFT JOIN HRS.ONS_ONLINEBEW_STAMMDATEN ON ONS_RELIGIONSNR = REL_RELIGIONSNR" "~"
if joinKuerzel = FSTREM
searchResult := searchResult "LEFT JOIN BUERO.REM_REMINDER ON REM_REMINDERLFDNR = FST_REMINDERLFDNR" "~"
if joinKuerzel = REMFST
searchResult := searchResult "LEFT JOIN BUERO.FST_FRIST ON FST_REMINDERLFDNR = REM_REMINDERLFDNR" "~"
if joinKuerzel = RGTRKG
searchResult := searchResult "LEFT JOIN BMD.RKG_TABREPLGRUPPE ON RKG_RKGID = RGT_RKGID" "~"
if joinKuerzel = RKGRGT
searchResult := searchResult "LEFT JOIN BMD.RGT_TABREPLGTABELLEZU ON RGT_RKGID = RKG_RKGID" "~"
if joinKuerzel = FIRRKS
searchResult := searchResult "LEFT JOIN RKS_REPLIKATIONSSTANDORT.RKS_REPLIKATIONSSTANDORT ON RKS_REPLSTANDONR = FIR_REPLSTANDONR" "~"
if joinKuerzel = RKSFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_REPLSTANDONR = RKS_REPLSTANDONR" "~"
if joinKuerzel = KGDRKS
searchResult := searchResult "LEFT JOIN RKS_REPLIKATIONSSTANDORT.RKS_REPLIKATIONSSTANDORT ON RKS_REPLSTANDONR = KGD_REPLSTANDONR" "~"
if joinKuerzel = RKSKGD
searchResult := searchResult "LEFT JOIN WWS.KGD_KASSENGELDLADE ON KGD_REPLSTANDONR = RKS_REPLSTANDONR" "~"
if joinKuerzel = ERZRLK
searchResult := searchResult "LEFT JOIN WWS.RLK_RECHNUNGSPLANKOPF ON RLK_RLKID = ERZ_RECHNUNGSPLANKOPFID" "~"
if joinKuerzel = RLKERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_RECHNUNGSPLANKOPFID = RLK_RLKID" "~"
if joinKuerzel = EFKRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = EFK_FIRMENNR AND RNG_RECHNUNGLFDNR = EFK_REFERENZ_RECHNUNGLFDNR" "~"
if joinKuerzel = RNGEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = RNG_FIRMENNR AND EFK_REFERENZ_RECHNUNGLFDNR = RNG_RECHNUNGLFDNR" "~"
if joinKuerzel = RNGRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = RNG_FIRMENNR AND RNG_RECHNUNGLFDNR = RNG_ERSETZT_RECHNUNGLFDNR" "~"
if joinKuerzel = RNGRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = RNG_FIRMENNR AND RNG_ERSETZT_RECHNUNGLFDNR = RNG_RECHNUNGLFDNR" "~"
if joinKuerzel = WKRRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = WKR_FIRMENNR AND RNP_RECHNUNGLFDNR = WKR_RECHNUNGLFDNR AND RNP_RECHNUNGSPOSLFDNR = WKR_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = RNPWKR
searchResult := searchResult "LEFT JOIN WWS.WKR_WARENAUSGKOSTENRECH ON WKR_FIRMENNR = RNP_FIRMENNR AND WKR_RECHNUNGLFDNR = RNP_RECHNUNGLFDNR AND WKR_RECHNUNGSPOSLFDNR = RNP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = DRVRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = DRV_REP_REPORTNR" "~"
if joinKuerzel = RPTDRV
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DRV_DREPORTVORSCHLAG ON DRV_REP_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = GSARPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = GSA_REPORTNR" "~"
if joinKuerzel = RPTGSA
searchResult := searchResult "LEFT JOIN WWS.GSA_GUTSCHEINAKTION ON GSA_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = HADRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = HAD_REPORTNR" "~"
if joinKuerzel = RPTHAD
searchResult := searchResult "LEFT JOIN HRS.HAD_AUSDRUCKDETAIL ON HAD_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = RVKRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = RVK_REPORTNR" "~"
if joinKuerzel = RPTRVK
searchResult := searchResult "LEFT JOIN ZLE.RVK_RECHVORLAGEKOPF ON RVK_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = TRSRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = TRS_REPORTNR_SR" "~"
if joinKuerzel = RPTTRS
searchResult := searchResult "LEFT JOIN BUERO.TRS_TEILSCHLUSSRECHSCHEMA ON TRS_REPORTNR_SR = RPT_REPORTNR" "~"
if joinKuerzel = TRSRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = TRS_REPORTNR_TR" "~"
if joinKuerzel = RPTTRS
searchResult := searchResult "LEFT JOIN BUERO.TRS_TEILSCHLUSSRECHSCHEMA ON TRS_REPORTNR_TR = RPT_REPORTNR" "~"
if joinKuerzel = HADRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = HAD_FORMULERFUSS" "~"
if joinKuerzel = RPTHAD
searchResult := searchResult "LEFT JOIN HRS.HAD_AUSDRUCKDETAIL ON HAD_FORMULERFUSS = RPT_REPORTNR" "~"
if joinKuerzel = JABRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = JAB_REPORTNR" "~"
if joinKuerzel = RPTJAB
searchResult := searchResult "LEFT JOIN BIL.JAB_JABINFO ON JAB_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = KASRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = KAS_BERICHTE_ABREPNR" "~"
if joinKuerzel = RPTKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_BERICHTE_ABREPNR = RPT_REPORTNR" "~"
if joinKuerzel = AWERPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = AWE_REPORTNR" "~"
if joinKuerzel = RPTAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = KASRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = KAS_BONDRUCK_REPORTNR" "~"
if joinKuerzel = RPTKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_BONDRUCK_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = QAWRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = QAW_REPORTNR" "~"
if joinKuerzel = RPTQAW
searchResult := searchResult "LEFT JOIN BUERO.QAW_QUOTENAUSWERTUNG ON QAW_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = HADRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = HAD_FORMULARKOPF" "~"
if joinKuerzel = RPTHAD
searchResult := searchResult "LEFT JOIN HRS.HAD_AUSDRUCKDETAIL ON HAD_FORMULARKOPF = RPT_REPORTNR" "~"
if joinKuerzel = ZAWRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = ZAW_REPORTNR_PLGTAGTXT" "~"
if joinKuerzel = RPTZAW
searchResult := searchResult "LEFT JOIN BUERO.ZAW_ZEITAUSWERTUNG ON ZAW_REPORTNR_PLGTAGTXT = RPT_REPORTNR" "~"
if joinKuerzel = WAGRSE
searchResult := searchResult "LEFT JOIN WWS.RSE_RESERVIERUNGSSYSEXT ON RSE_RSEID = WAG_RSEID" "~"
if joinKuerzel = RSEWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_RSEID = RSE_RSEID" "~"
if joinKuerzel = WAGRUK
searchResult := searchResult "LEFT JOIN WWS.RUK_RUNDUNGKOPF ON RUK_FIRMENNR = WAG_FIRMENNR AND RUK_RUNDUNGSKOPFNR = WAG_RUNDUNGSKOPFNR" "~"
if joinKuerzel = RUKWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = RUK_FIRMENNR AND WAG_RUNDUNGSKOPFNR = RUK_RUNDUNGSKOPFNR" "~"
if joinKuerzel = LEIRVD
searchResult := searchResult "LEFT JOIN ZLE.RVD_RECHVORLAGEDETAIL ON RVD_RVDID = LEI_RVDID" "~"
if joinKuerzel = RVDLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_RVDID = RVD_RVDID" "~"
if joinKuerzel = SA4SA1
searchResult := searchResult "LEFT JOIN ONVIS.SA1_SAURER_BDE ON SA1_FIRMENNR = SA4_FIRMENNR AND SA1_SAURERID = SA4_SAURERID" "~"
if joinKuerzel = SA1SA4
searchResult := searchResult "LEFT JOIN ONVIS.SA4_SAURER_BDE ON SA4_FIRMENNR = SA1_FIRMENNR AND SA4_SAURERID = SA1_SAURERID" "~"
if joinKuerzel = BAPSAK
searchResult := searchResult "LEFT JOIN FIBU.SAK_SACHKONTO ON SAK_FIRMENNR = BAP_FIRMENNR AND SAK_FIBUNR = BAP_FIBUNR AND SAK_KONTONR = BAP_EINKAUF_KONTONR" "~"
if joinKuerzel = SAKBAP
searchResult := searchResult "LEFT JOIN FIBU.BAP_BUDGETARTIKELPLAN ON BAP_FIRMENNR = SAK_FIRMENNR AND BAP_FIBUNR = SAK_FIBUNR AND BAP_EINKAUF_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = BUDSAK
searchResult := searchResult "LEFT JOIN FIBU.SAK_SACHKONTO ON SAK_FIRMENNR = BUD_FIRMENNR AND SAK_FIBUNR = BUD_FIBUNR AND SAK_KONTONR = BUD_KONTONR" "~"
if joinKuerzel = SAKBUD
searchResult := searchResult "LEFT JOIN FIBU.BUD_BUDGET ON BUD_FIRMENNR = SAK_FIRMENNR AND BUD_FIBUNR = SAK_FIBUNR AND BUD_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = PKPSAK
searchResult := searchResult "LEFT JOIN FIBU.SAK_SACHKONTO ON SAK_FIRMENNR = PKP_FIRMENNR AND SAK_FIBUNR = PKP_FIBUNR AND SAK_KONTONR = PKP_KONTONR" "~"
if joinKuerzel = SAKPKP
searchResult := searchResult "LEFT JOIN FIBU.PKP_BUDGETPERSKOSTPLAN ON PKP_FIRMENNR = SAK_FIRMENNR AND PKP_FIBUNR = SAK_FIBUNR AND PKP_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = PKPSAK
searchResult := searchResult "LEFT JOIN FIBU.SAK_SACHKONTO ON SAK_FIRMENNR = PKP_FIRMENNR AND SAK_FIBUNR = PKP_FIBUNR AND SAK_KONTONR = PKP_MVK_KONTONR" "~"
if joinKuerzel = SAKPKP
searchResult := searchResult "LEFT JOIN FIBU.PKP_BUDGETPERSKOSTPLAN ON PKP_FIRMENNR = SAK_FIRMENNR AND PKP_FIBUNR = SAK_FIBUNR AND PKP_MVK_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = EFKSAP
searchResult := searchResult "LEFT JOIN SAP_SAMMELPROJEKT.SAP_SAMMELPROJEKT ON SAP_FIRMENNR = EFK_SAMMELPROJEKT_FIRMENNR AND SAP_SAMMELPROJEKTNR = EFK_SAMMELPROJEKTNR" "~"
if joinKuerzel = SAPEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_SAMMELPROJEKT_FIRMENNR = SAP_FIRMENNR AND EFK_SAMMELPROJEKTNR = SAP_SAMMELPROJEKTNR" "~"
if joinKuerzel = LTKSAP
searchResult := searchResult "LEFT JOIN SAP_SAMMELPROJEKT.SAP_SAMMELPROJEKT ON SAP_FIRMENNR = LTK_SAP_FIRMENNR AND SAP_SAMMELPROJEKTNR = LTK_SAMMELPROJEKTNR" "~"
if joinKuerzel = SAPLTK
searchResult := searchResult "LEFT JOIN ZLE.LTK_LEISTTAUSCHKOPF ON LTK_SAP_FIRMENNR = SAP_FIRMENNR AND LTK_SAMMELPROJEKTNR = SAP_SAMMELPROJEKTNR" "~"
if joinKuerzel = PROSAP
searchResult := searchResult "LEFT JOIN BUERO.SAP_SAMMELPROJEKT ON SAP_FIRMENNR = PRO_SAP_FIRMENNR AND SAP_SAMMELPROJEKTNR = PRO_SAP_SAMMELPROJEKTNR" "~"
if joinKuerzel = SAPPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_SAP_FIRMENNR = SAP_FIRMENNR AND PRO_SAP_SAMMELPROJEKTNR = SAP_SAMMELPROJEKTNR" "~"
if joinKuerzel = VZWSBC
searchResult := searchResult "LEFT JOIN BUERO.SBC_SPEZBEWEGUNGSCODE ON SBC_FIRMENNR = VZW_SBC_FIRMENNR AND SBC_SBCCODE = VZW_SBC_SBCCODE" "~"
if joinKuerzel = SBCVZW
searchResult := searchResult "LEFT JOIN BUERO.VZW_VERKEHRSZWEIG ON VZW_SBC_FIRMENNR = SBC_FIRMENNR AND VZW_SBC_SBCCODE = SBC_SBCCODE" "~"
if joinKuerzel = SABSBS
searchResult := searchResult "LEFT JOIN BUERO.SBS_STELLENBESCHREIBUNG ON SBS_STELLBESCHID = SAB_SBS_STELLBESCHID" "~"
if joinKuerzel = SBSSAB
searchResult := searchResult "LEFT JOIN BUERO.SAB_STELLENAUSBILDUNG ON SAB_SBS_STELLBESCHID = SBS_STELLBESCHID" "~"
if joinKuerzel = OUPSBV
searchResult := searchResult "LEFT JOIN BUERO.SBV_STELLENBESCHVORLAGE ON SBV_VORLAGENID = OUP_TEXTBAUSTEINID05" "~"
if joinKuerzel = SBVOUP
searchResult := searchResult "LEFT JOIN BUERO.OUP_ONLUMFRAGEPAGE ON OUP_TEXTBAUSTEINID05 = SBV_VORLAGENID" "~"
if joinKuerzel = OUPSBV
searchResult := searchResult "LEFT JOIN BUERO.SBV_STELLENBESCHVORLAGE ON SBV_VORLAGENID = OUP_TEXTBAUSTEINID01" "~"
if joinKuerzel = SBVOUP
searchResult := searchResult "LEFT JOIN BUERO.OUP_ONLUMFRAGEPAGE ON OUP_TEXTBAUSTEINID01 = SBV_VORLAGENID" "~"
if joinKuerzel = SCCSCC
searchResult := searchResult "LEFT JOIN BMDENTW.SCC_SCRREVISIONFILE ON SCC_REVISIONSNR_SVN = SCC_UNDO_REVISIONSNR_SVN AND SCC_FILELFDNR = SCC_UNDO_FILELFDNR AND SCC_FILEFULLNAME_SVN = SCC_FILEFULLNAME_SVN AND SCC_SVNREPOKZ = SCC_SVNREPOKZ" "~"
if joinKuerzel = SCCSCC
searchResult := searchResult "LEFT JOIN BMDENTW.SCC_SCRREVISIONFILE ON SCC_UNDO_REVISIONSNR_SVN = SCC_REVISIONSNR_SVN AND SCC_UNDO_FILELFDNR = SCC_FILELFDNR AND SCC_FILEFULLNAME_SVN = SCC_FILEFULLNAME_SVN AND SCC_SVNREPOKZ = SCC_SVNREPOKZ" "~"
if joinKuerzel = FADRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = FAD_REPORTNR" "~"
if joinKuerzel = RPTFAD
searchResult := searchResult "LEFT JOIN FIBU.FAD_AUSDRUCKDETAIL ON FAD_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = JABRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = JAB_KOPF_REPORTNR" "~"
if joinKuerzel = RPTJAB
searchResult := searchResult "LEFT JOIN BIL.JAB_JABINFO ON JAB_KOPF_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = ZAWRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = ZAW_REPORTNR" "~"
if joinKuerzel = RPTZAW
searchResult := searchResult "LEFT JOIN BUERO.ZAW_ZEITAUSWERTUNG ON ZAW_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = SRERPT
searchResult := searchResult "LEFT JOIN BMD.RPT_REPORT ON RPT_REPORTNR = SRE_KOPF_REPORTNR" "~"
if joinKuerzel = RPTSRE
searchResult := searchResult "LEFT JOIN BMD.SRE_STANDARDREPORT ON SRE_KOPF_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = ARLRSE
searchResult := searchResult "LEFT JOIN WWS.RSE_RESERVIERUNGSSYSEXT ON RSE_RSEID = ARL_RSEID" "~"
if joinKuerzel = RSEARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_RSEID = RSE_RSEID" "~"
if joinKuerzel = RSDRSK
searchResult := searchResult "LEFT JOIN WWS.RSK_REGISTRIERKASSE ON RSK_FIRMENNR = RSD_RSK_FIRMENNR AND RSK_KASSENID = RSD_RSK_KASSENID" "~"
if joinKuerzel = RSKRSD
searchResult := searchResult "LEFT JOIN WWS.RSD_REGKASPROTOKOLL ON RSD_RSK_FIRMENNR = RSK_FIRMENNR AND RSD_RSK_KASSENID = RSK_KASSENID" "~"
if joinKuerzel = VATRZS
searchResult := searchResult "LEFT JOIN BUERO.RZS_RUECKZAHLUNGSSTAMM ON RZS_FIRMENNR = VAT_RZS_FIRMENNR AND RZS_RZSTAMMNR = VAT_RZSTAMMNR" "~"
if joinKuerzel = RZSVAT
searchResult := searchResult "LEFT JOIN BUERO.VAT_TEILNAHME ON VAT_RZS_FIRMENNR = RZS_FIRMENNR AND VAT_RZSTAMMNR = RZS_RZSTAMMNR" "~"
if joinKuerzel = SAGSAG
searchResult := searchResult "LEFT JOIN WWS.SAG_STATISTIKARTIKELGRP ON SAG_FIRMENNR = SAG_SAG_FIRMENNR AND SAG_STATISTIKLFDNR = SAG_SAG_STATISTIKLFDNR AND SAG_STATISTIKGRPLFDNR = SAG_SAG_STATISTIKGRPLFDNR" "~"
if joinKuerzel = SAGSAG
searchResult := searchResult "LEFT JOIN WWS.SAG_STATISTIKARTIKELGRP ON SAG_SAG_FIRMENNR = SAG_FIRMENNR AND SAG_SAG_STATISTIKLFDNR = SAG_STATISTIKLFDNR AND SAG_SAG_STATISTIKGRPLFDNR = SAG_STATISTIKGRPLFDNR" "~"
if joinKuerzel = TSKSAG
searchResult := searchResult "LEFT JOIN WWS.SAG_STATISTIKARTIKELGRP ON SAG_FIRMENNR = TSK_ARS_FIRMENNR AND SAG_STATISTIKLFDNR = TSK_ARS_STATISTIKLFDNR AND SAG_STATISTIKGRPLFDNR = TSK_SAG_STATISTIKGRPLFDNR" "~"
if joinKuerzel = SAGTSK
searchResult := searchResult "LEFT JOIN WWS.TSK_TOUCHSCREENKOPF ON TSK_ARS_FIRMENNR = SAG_FIRMENNR AND TSK_ARS_STATISTIKLFDNR = SAG_STATISTIKLFDNR AND TSK_SAG_STATISTIKGRPLFDNR = SAG_STATISTIKGRPLFDNR" "~"
if joinKuerzel = RNRSAK
searchResult := searchResult "LEFT JOIN SAK_SACHKONTO.SAK_SACHKONTO ON SAK_FIRMENNR = RNR_SAK_FIRMENNR AND SAK_FIBUNR = RNR_SAK_FIBUNR AND SAK_KONTONR = RNR_SAK_KONTONR" "~"
if joinKuerzel = SAKRNR
searchResult := searchResult "LEFT JOIN WWS.RNR_RECHNUNGSRABATT ON RNR_SAK_FIRMENNR = SAK_FIRMENNR AND RNR_SAK_FIBUNR = SAK_FIBUNR AND RNR_SAK_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = BWESAK
searchResult := searchResult "LEFT JOIN FIBU.SAK_SACHKONTO ON SAK_FIRMENNR = BWE_FIRMENNR AND SAK_FIBUNR = BWE_FIBUNR AND SAK_KONTONR = BWE_WES_KONTONR" "~"
if joinKuerzel = SAKBWE
searchResult := searchResult "LEFT JOIN FIBU.BWE_BUDGETWARENEINKPLAN ON BWE_FIRMENNR = SAK_FIRMENNR AND BWE_FIBUNR = SAK_FIBUNR AND BWE_WES_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = ABOSAK
searchResult := searchResult "LEFT JOIN SAK_SACHKONTO.SAK_SACHKONTO ON SAK_FIRMENNR = ABO_SAK_FIRMENNR AND SAK_FIBUNR = ABO_SAK_FIBUNR AND SAK_KONTONR = ABO_GEGENKONTONR" "~"
if joinKuerzel = SAKABO
searchResult := searchResult "LEFT JOIN BIL.ABO_ANBUBUCHUNGKONTEN ON ABO_SAK_FIRMENNR = SAK_FIRMENNR AND ABO_SAK_FIBUNR = SAK_FIBUNR AND ABO_GEGENKONTONR = SAK_KONTONR" "~"
if joinKuerzel = AWSSBS
searchResult := searchResult "LEFT JOIN SBS_STELLENBESCHREIBUNG.SBS_STELLENBESCHREIBUNG ON SBS_STELLBESCHID = AWS_SBS_STELLBESCHID" "~"
if joinKuerzel = SBSAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_SBS_STELLBESCHID = SBS_STELLBESCHID" "~"
if joinKuerzel = SBBSBS
searchResult := searchResult "LEFT JOIN SBS_STELLENBESCHREIBUNG.SBS_STELLENBESCHREIBUNG ON SBS_STELLBESCHID = SBB_SBS_STELLBESCHID" "~"
if joinKuerzel = SBSSBB
searchResult := searchResult "LEFT JOIN HRS.SBB_SOLLBESETZUNG ON SBB_SBS_STELLBESCHID = SBS_STELLBESCHID" "~"
if joinKuerzel = OUPSBV
searchResult := searchResult "LEFT JOIN BUERO.SBV_STELLENBESCHVORLAGE ON SBV_VORLAGENID = OUP_TEXTBAUSTEINID10" "~"
if joinKuerzel = SBVOUP
searchResult := searchResult "LEFT JOIN BUERO.OUP_ONLUMFRAGEPAGE ON OUP_TEXTBAUSTEINID10 = SBV_VORLAGENID" "~"
if joinKuerzel = OUPSBV
searchResult := searchResult "LEFT JOIN BUERO.SBV_STELLENBESCHVORLAGE ON SBV_VORLAGENID = OUP_TEXTBAUSTEINID02" "~"
if joinKuerzel = SBVOUP
searchResult := searchResult "LEFT JOIN BUERO.OUP_ONLUMFRAGEPAGE ON OUP_TEXTBAUSTEINID02 = SBV_VORLAGENID" "~"
if joinKuerzel = RIHSCK
searchResult := searchResult "LEFT JOIN PRODUKTION.SCK_SCKENNZAHL ON SCK_SCKID = RIH_SCKID_SEVERITY" "~"
if joinKuerzel = SCKRIH
searchResult := searchResult "LEFT JOIN PRODUKTION.RIH_RISIKOANALYSE_HEAD ON RIH_SCKID_SEVERITY = SCK_SCKID" "~"
if joinKuerzel = ITMSCM
searchResult := searchResult "LEFT JOIN SCM_SCRMAIN.SCM_SCRMAIN ON SCM_SCRNR = ITM_SCRNR" "~"
if joinKuerzel = SCMITM
searchResult := searchResult "LEFT JOIN BUERO.ITM_INTERNMAIL ON ITM_SCRNR = SCM_SCRNR" "~"
if joinKuerzel = SEBSEB
searchResult := searchResult "LEFT JOIN BIL.SEB_STEUERERKLBAUM ON SEB_SEBID = SEB_PARENT_SEBID" "~"
if joinKuerzel = SEBSEB
searchResult := searchResult "LEFT JOIN BIL.SEB_STEUERERKLBAUM ON SEB_PARENT_SEBID = SEB_SEBID" "~"
if joinKuerzel = POSSEK
searchResult := searchResult "LEFT JOIN BUERO.SEK_SEKRETARIAT ON SEK_FIRMENNR = POS_SEK_FIRMENNR AND SEK_SEKRETARIATSNR = POS_SEKRETARIATSNR" "~"
if joinKuerzel = SEKPOS
searchResult := searchResult "LEFT JOIN BUERO.POS_POST ON POS_SEK_FIRMENNR = SEK_FIRMENNR AND POS_SEKRETARIATSNR = SEK_SEKRETARIATSNR" "~"
if joinKuerzel = PSYSFZ
searchResult := searchResult "LEFT JOIN SFZ_SYNCFELDZUORDNUNG.SFZ_SYNCFELDZUORDNUNG ON SFZ_SYNCFELDZUID = PSY_SYNCFELDZUID" "~"
if joinKuerzel = SFZPSY
searchResult := searchResult "LEFT JOIN WWS.PSY_PICSYNCFELDZU ON PSY_SYNCFELDZUID = SFZ_SYNCFELDZUID" "~"
if joinKuerzel = ERDSAK
searchResult := searchResult "LEFT JOIN SAK_SACHKONTO.SAK_SACHKONTO ON SAK_FIRMENNR = ERD_SAK_FIRMENNR AND SAK_FIBUNR = ERD_SAK_FIBUNR AND SAK_KONTONR = ERD_SAK_KONTONR" "~"
if joinKuerzel = SAKERD
searchResult := searchResult "LEFT JOIN WWS.ERD_ERFASSRABATTDETAIL ON ERD_SAK_FIRMENNR = SAK_FIRMENNR AND ERD_SAK_FIBUNR = SAK_FIBUNR AND ERD_SAK_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = ABOSAK
searchResult := searchResult "LEFT JOIN SAK_SACHKONTO.SAK_SACHKONTO ON SAK_FIRMENNR = ABO_SAK_FIRMENNR AND SAK_FIBUNR = ABO_SAK_FIBUNR AND SAK_KONTONR = ABO_KONTONR" "~"
if joinKuerzel = SAKABO
searchResult := searchResult "LEFT JOIN BIL.ABO_ANBUBUCHUNGKONTEN ON ABO_SAK_FIRMENNR = SAK_FIRMENNR AND ABO_SAK_FIBUNR = SAK_FIBUNR AND ABO_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = PBOSAK
searchResult := searchResult "LEFT JOIN SAK_SACHKONTO.SAK_SACHKONTO ON SAK_FIRMENNR = PBO_FIRMENNR AND SAK_FIBUNR = PBO_FIBUNR AND SAK_KONTONR = PBO_GEGENKONTONR" "~"
if joinKuerzel = SAKPBO
searchResult := searchResult "LEFT JOIN BIL.PBO_PERSKORST_KONTEN ON PBO_FIRMENNR = SAK_FIRMENNR AND PBO_FIBUNR = SAK_FIBUNR AND PBO_GEGENKONTONR = SAK_KONTONR" "~"
if joinKuerzel = PKPSAK
searchResult := searchResult "LEFT JOIN FIBU.SAK_SACHKONTO ON SAK_FIRMENNR = PKP_FIRMENNR AND SAK_FIBUNR = PKP_FIBUNR AND SAK_KONTONR = PKP_SZ_KONTONR" "~"
if joinKuerzel = SAKPKP
searchResult := searchResult "LEFT JOIN FIBU.PKP_BUDGETPERSKOSTPLAN ON PKP_FIRMENNR = SAK_FIRMENNR AND PKP_FIBUNR = SAK_FIBUNR AND PKP_SZ_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = PKPSAK
searchResult := searchResult "LEFT JOIN FIBU.SAK_SACHKONTO ON SAK_FIRMENNR = PKP_FIRMENNR AND SAK_FIBUNR = PKP_FIBUNR AND SAK_KONTONR = PKP_DB_KONTONR" "~"
if joinKuerzel = SAKPKP
searchResult := searchResult "LEFT JOIN FIBU.PKP_BUDGETPERSKOSTPLAN ON PKP_FIRMENNR = SAK_FIRMENNR AND PKP_FIBUNR = SAK_FIBUNR AND PKP_DB_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = SBGSBG
searchResult := searchResult "LEFT JOIN BMD.SBG_STDBRIEFGRUPPE ON SBG_BRIEFGRUPPENNR = SBG_SBG_BRIEFGRUPPENNR" "~"
if joinKuerzel = SBGSBG
searchResult := searchResult "LEFT JOIN BMD.SBG_STDBRIEFGRUPPE ON SBG_SBG_BRIEFGRUPPENNR = SBG_BRIEFGRUPPENNR" "~"
if joinKuerzel = LMBSBK
searchResult := searchResult "LEFT JOIN WWS.SBK_SAISONBESCHAFFKOPF ON SBK_FIRMENNR = LMB_FIRMENNR AND SBK_SBKNR = LMB_SBKNR" "~"
if joinKuerzel = SBKLMB
searchResult := searchResult "LEFT JOIN WWS.LMB_LAGERMINMAXBESTAND ON LMB_FIRMENNR = SBK_FIRMENNR AND LMB_SBKNR = SBK_SBKNR" "~"
if joinKuerzel = RIASCA
searchResult := searchResult "LEFT JOIN PRODUKTION.SCA_SCKRITERIUM ON SCA_SCAID = RIA_SCAID" "~"
if joinKuerzel = SCARIA
searchResult := searchResult "LEFT JOIN PRODUKTION.RIA_RISIKOANALYSE ON RIA_SCAID = SCA_SCAID" "~"
if joinKuerzel = SCGSCA
searchResult := searchResult "LEFT JOIN PRODUKTION.SCA_SCKRITERIUM ON SCA_SCAID = SCG_SCAID" "~"
if joinKuerzel = SCASCG
searchResult := searchResult "LEFT JOIN PRODUKTION.SCG_SCORECARDKRITZU ON SCG_SCAID = SCA_SCAID" "~"
if joinKuerzel = SCISCG
searchResult := searchResult "LEFT JOIN PRODUKTION.SCG_SCORECARDKRITZU ON SCG_SCGID = SCI_SCGID" "~"
if joinKuerzel = SCGSCI
searchResult := searchResult "LEFT JOIN PRODUKTION.SCI_SCINSTANZDET ON SCI_SCGID = SCG_SCGID" "~"
if joinKuerzel = SCASCK
searchResult := searchResult "LEFT JOIN PRODUKTION.SCK_SCKENNZAHL ON SCK_SCKID = SCA_SCKID_OCCURENCE" "~"
if joinKuerzel = SCKSCA
searchResult := searchResult "LEFT JOIN PRODUKTION.SCA_SCKRITERIUM ON SCA_SCKID_OCCURENCE = SCK_SCKID" "~"
if joinKuerzel = GBKSID
searchResult := searchResult "LEFT JOIN HRS.SID_HRSSEQ ON SID_HRSSEQID = GBK_HRSSEQID" "~"
if joinKuerzel = SIDGBK
searchResult := searchResult "LEFT JOIN HRS.GBK_GEBIETEKV ON GBK_HRSSEQID = SID_HRSSEQID" "~"
if joinKuerzel = LGFSID
searchResult := searchResult "LEFT JOIN HRS.SID_HRSSEQ ON SID_HRSSEQID = LGF_KVID" "~"
if joinKuerzel = SIDLGF
searchResult := searchResult "LEFT JOIN HRS.LGF_LGVMAKROS ON LGF_KVID = SID_HRSSEQID" "~"
if joinKuerzel = SSUSKE
searchResult := searchResult "LEFT JOIN BIL.SKE_STEUERKUNDENZUERW ON SKE_STEUER_ERKLZUNR = SSU_STEUER_ERKLZUNR AND SKE_STEUER_ERKLNR = SSU_STEUER_ERKLNR AND SKE_ERKLERWLFDNR = SSU_ERKLERWLFDNR AND SKE_ERKLERWMODELL = SSU_ERKLERWMODELL" "~"
if joinKuerzel = SKESSU
searchResult := searchResult "LEFT JOIN BIL.SSU_STEUERSACHKONTOZU ON SSU_STEUER_ERKLZUNR = SKE_STEUER_ERKLZUNR AND SSU_STEUER_ERKLNR = SKE_STEUER_ERKLNR AND SSU_ERKLERWLFDNR = SKE_ERKLERWLFDNR AND SSU_ERKLERWMODELL = SKE_ERKLERWMODELL" "~"
if joinKuerzel = SKESKZ
searchResult := searchResult "LEFT JOIN SKZ_STEUERKUNDENZU.SKZ_STEUERKUNDENZU ON SKZ_STEUER_ERKLZUNR = SKE_STEUER_ERKLZUNR" "~"
if joinKuerzel = SKZSKE
searchResult := searchResult "LEFT JOIN BIL.SKE_STEUERKUNDENZUERW ON SKE_STEUER_ERKLZUNR = SKZ_STEUER_ERKLZUNR" "~"
if joinKuerzel = SLPSLK
searchResult := searchResult "LEFT JOIN WWS.SLK_STUECKLISTENKOPF ON SLK_STKLISTKOPFID = SLP_BAUGRUPPENID" "~"
if joinKuerzel = SLKSLP
searchResult := searchResult "LEFT JOIN WWS.SLP_STUECKLISTENPOSITION ON SLP_BAUGRUPPENID = SLK_STKLISTKOPFID" "~"
if joinKuerzel = SMASNI
searchResult := searchResult "LEFT JOIN HRS.SNI_SCHNITTE ON SNI_DBSNR = SMA_DBSNR AND SNI_SCHNITTNR = SMA_SCHNITTNR" "~"
if joinKuerzel = SNISMA
searchResult := searchResult "LEFT JOIN HRS.SMA_SCHNITTMA ON SMA_DBSNR = SNI_DBSNR AND SMA_SCHNITTNR = SNI_SCHNITTNR" "~"
if joinKuerzel = LAWSNI
searchResult := searchResult "LEFT JOIN HRS.SNI_SCHNITTE ON SNI_DBSNR = LAW_DBSNR AND SNI_SCHNITTNR = LAW_SCHNITTNR_6" "~"
if joinKuerzel = SNILAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_DBSNR = SNI_DBSNR AND LAW_SCHNITTNR_6 = SNI_SCHNITTNR" "~"
if joinKuerzel = AVLSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_SERNRID = AVL_SNR_SERNRID" "~"
if joinKuerzel = SNRAVL
searchResult := searchResult "LEFT JOIN WWS.AVL_AUFTRAGSVORERFLIEFDAT ON AVL_SNR_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = BPDSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_SERNRID = BPD_SERNRID" "~"
if joinKuerzel = SNRBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = BVOSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_SERNRID = BVO_SNR_SERNRID" "~"
if joinKuerzel = SNRBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_SNR_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = PCPSNR
searchResult := searchResult "LEFT JOIN SNR_SERIENNUMMERN.SNR_SERIENNUMMERN ON SNR_SERNRID = PCP_SERNRID" "~"
if joinKuerzel = SNRPCP
searchResult := searchResult "LEFT JOIN PRODUKTION.PCP_PPS_PRODUKT ON PCP_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = EKJPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_PCHARGENID = EKJ_KEY_PCHARGENID" "~"
if joinKuerzel = PLCEKJ
searchResult := searchResult "LEFT JOIN PRODUKTION.EKJ_ERP_KOREJOURNAL ON EKJ_KEY_PCHARGENID = PLC_PCHARGENID" "~"
if joinKuerzel = EPSPLC
searchResult := searchResult "LEFT JOIN PLC_PPS_CHARGE.PLC_PPS_CHARGE ON PLC_PCHARGENID = EPS_PCHARGENID" "~"
if joinKuerzel = PLCEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_PCHARGENID = PLC_PCHARGENID" "~"
if joinKuerzel = LVEPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_PCHARGENID = LVE_PLC_PCHARGENID" "~"
if joinKuerzel = PLCLVE
searchResult := searchResult "LEFT JOIN PRODUKTION.LVE_LEISTUNGSVORERFASSUNG ON LVE_PLC_PCHARGENID = PLC_PCHARGENID" "~"
if joinKuerzel = PCPPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_PCHARGENID = PCP_PCHARGENID" "~"
if joinKuerzel = PLCPCP
searchResult := searchResult "LEFT JOIN PRODUKTION.PCP_PPS_PRODUKT ON PCP_PCHARGENID = PLC_PCHARGENID" "~"
if joinKuerzel = ZIZPLG
searchResult := searchResult "LEFT JOIN BUERO.PLG_PLANGRUPPE ON PLG_FIRMENNR = ZIZ_PLG_FIRMENNR AND PLG_PLANGRLFDNR = ZIZ_PLG_PLANGRLFDNR" "~"
if joinKuerzel = PLGZIZ
searchResult := searchResult "LEFT JOIN BUERO.ZIZ_ZUSATZINFOZUORD ON ZIZ_PLG_FIRMENNR = PLG_FIRMENNR AND ZIZ_PLG_PLANGRLFDNR = PLG_PLANGRLFDNR" "~"
if joinKuerzel = KOZPLO
searchResult := searchResult "LEFT JOIN PRODUKTION.PLO_PPS_LOS ON PLO_FIRMENNR = KOZ_PLO_FIRMENNR AND PLO_PL_NR = KOZ_PL_NR" "~"
if joinKuerzel = PLOKOZ
searchResult := searchResult "LEFT JOIN PRODUKTION.KOZ_KOMMISSIONIERUNG_ZU ON KOZ_PLO_FIRMENNR = PLO_FIRMENNR AND KOZ_PL_NR = PLO_PL_NR" "~"
if joinKuerzel = AWEPLP
searchResult := searchResult "LEFT JOIN PLP_PLANUNGSPERIODE.PLP_PLANUNGSPERIODE ON PLP_FIRMENNR = AWE_PEP_BL_PLP_FIRMENNR AND PLP_PLANPERID = AWE_PEP_BL_PLP_PLANPERID" "~"
if joinKuerzel = PLPAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_PEP_BL_PLP_FIRMENNR = PLP_FIRMENNR AND AWE_PEP_BL_PLP_PLANPERID = PLP_PLANPERID" "~"
if joinKuerzel = PREPRN
searchResult := searchResult "LEFT JOIN WWS.PRN_EXTPIAKEYNAMES ON PRN_PICID = PRE_PICID AND PRN_KEYNAME = PRE_KEYNAME" "~"
if joinKuerzel = PRNPRE
searchResult := searchResult "LEFT JOIN WWS.PRE_EXTPARTIKELWERTE ON PRE_PICID = PRN_PICID AND PRE_KEYNAME = PRN_KEYNAME" "~"
if joinKuerzel = BARPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = BAR_PRO_FIRMENNR AND PRO_PROJEKTNR = BAR_PRO_PROJEKTNR AND PRO_PROJEKTTYP = BAR_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROBAR
searchResult := searchResult "LEFT JOIN HRS.BAR_BARAUSLAGEN ON BAR_PRO_FIRMENNR = PRO_FIRMENNR AND BAR_PRO_PROJEKTNR = PRO_PROJEKTNR AND BAR_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = DOKPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = DOK_PRO_FIRMENNR AND PRO_PROJEKTNR = DOK_PRO_PROJEKTNR AND PRO_PROJEKTTYP = DOK_PRO_PROJEKTTYP" "~"
if joinKuerzel = PRODOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_PRO_FIRMENNR = PRO_FIRMENNR AND DOK_PRO_PROJEKTNR = PRO_PROJEKTNR AND DOK_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = EFKPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = EFK_PROJEKT_FIRMENNR AND PRO_PROJEKTNR = EFK_PROJEKTNR AND PRO_PROJEKTTYP = EFK_PROJEKTTYP" "~"
if joinKuerzel = PROEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_PROJEKT_FIRMENNR = PRO_FIRMENNR AND EFK_PROJEKTNR = PRO_PROJEKTNR AND EFK_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = LAJPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = LAJ_PRO_FIRMENNR AND PRO_PROJEKTNR = LAJ_PROJEKTNR AND PRO_PROJEKTTYP = LAJ_PROJEKTTYP" "~"
if joinKuerzel = PROLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_PRO_FIRMENNR = PRO_FIRMENNR AND LAJ_PROJEKTNR = PRO_PROJEKTNR AND LAJ_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = PALPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = PAL_PRO_FIR_FIRMENNR AND PRO_PROJEKTNR = PAL_PRO_PROJEKTNR AND PRO_PROJEKTTYP = PAL_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROPAL
searchResult := searchResult "LEFT JOIN ZLE.PAL_PAUSCHALE ON PAL_PRO_FIR_FIRMENNR = PRO_FIRMENNR AND PAL_PRO_PROJEKTNR = PRO_PROJEKTNR AND PAL_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = PTZPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = PTZ_PRO_FIRMENNR AND PRO_PROJEKTNR = PTZ_PRO_PROJEKTNR AND PRO_PROJEKTTYP = PTZ_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROPTZ
searchResult := searchResult "LEFT JOIN WWS.PTZ_PREISTABZU ON PTZ_PRO_FIRMENNR = PRO_FIRMENNR AND PTZ_PRO_PROJEKTNR = PRO_PROJEKTNR AND PTZ_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = RIHPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_PROJEKTID = RIH_PRO_PROJEKTID" "~"
if joinKuerzel = PRORIH
searchResult := searchResult "LEFT JOIN PRODUKTION.RIH_RISIKOANALYSE_HEAD ON RIH_PRO_PROJEKTID = PRO_PROJEKTID" "~"
if joinKuerzel = SKZPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = SKZ_PRO_FIRMENNR AND PRO_PROJEKTNR = SKZ_PRO_PROJEKTNR AND PRO_PROJEKTTYP = SKZ_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROSKZ
searchResult := searchResult "LEFT JOIN BUERO.SKZ_STEUERKUNDENZU ON SKZ_PRO_FIRMENNR = PRO_FIRMENNR AND SKZ_PRO_PROJEKTNR = PRO_PROJEKTNR AND SKZ_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = UZLPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_PROJEKTID = UZL_PRO_PROJEKTID" "~"
if joinKuerzel = PROUZL
searchResult := searchResult "LEFT JOIN BUERO.UZL_UNTERZIEL ON UZL_PRO_PROJEKTID = PRO_PROJEKTID" "~"
if joinKuerzel = WAKPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = WAK_PRO_FIRMENNR AND PRO_PROJEKTNR = WAK_PROJEKTNR AND PRO_PROJEKTTYP = WAK_PROJEKTTYP" "~"
if joinKuerzel = PROWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_PRO_FIRMENNR = PRO_FIRMENNR AND WAK_PROJEKTNR = PRO_PROJEKTNR AND WAK_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = SPVPRP
searchResult := searchResult "LEFT JOIN WWS.PRP_PROVISIONTABPOS ON PRP_PROVTABPOSID = SPV_PROVTABPOSID" "~"
if joinKuerzel = PRPSPV
searchResult := searchResult "LEFT JOIN WWS.SPV_PROVISIONSTAFFEL ON SPV_PROVTABPOSID = PRP_PROVTABPOSID" "~"
if joinKuerzel = EPSPRS
searchResult := searchResult "LEFT JOIN PRS_PROZESS.PRS_PROZESS ON PRS_FIRMENNR = EPS_PRS_FIRMENNR AND PRS_PROZESSID = EPS_PRS_PROZESSID" "~"
if joinKuerzel = PRSEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_PRS_FIRMENNR = PRS_FIRMENNR AND EPS_PRS_PROZESSID = PRS_PROZESSID" "~"
if joinKuerzel = ERAPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = ERA_POSTITNR" "~"
if joinKuerzel = PITERA
searchResult := searchResult "LEFT JOIN WWS.ERA_ERFASSRABATT ON ERA_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = FAZPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = FAZ_POSTITNR" "~"
if joinKuerzel = PITFAZ
searchResult := searchResult "LEFT JOIN BUERO.FAZ_FRAGEANTWORTZU ON FAZ_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = FSKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = FSK_POSTITNR" "~"
if joinKuerzel = PITFSK
searchResult := searchResult "LEFT JOIN FIBU.FSK_SORTIERKOPF ON FSK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = FZPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = FZP_POSTITNR" "~"
if joinKuerzel = PITFZP
searchResult := searchResult "LEFT JOIN FIBU.FZP_FINANZPLAN ON FZP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = HNPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = HNP_POSTITNR" "~"
if joinKuerzel = PITHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = ISZPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = ISZ_POSTITNR" "~"
if joinKuerzel = PITISZ
searchResult := searchResult "LEFT JOIN BUERO.ISZ_INFOTABSTELLEZU ON ISZ_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = KOCPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = KOC_POSTITNR" "~"
if joinKuerzel = PITKOC
searchResult := searchResult "LEFT JOIN PRODUKTION.KOC_KOMBI_CHARGE ON KOC_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = KOKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = KOK_POSTITNR" "~"
if joinKuerzel = PITKOK
searchResult := searchResult "LEFT JOIN PRODUKTION.KOK_KOMMISSIONIERUNG_KOPF ON KOK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = LASPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = LAS_POSTITNR" "~"
if joinKuerzel = PITLAS
searchResult := searchResult "LEFT JOIN BIL.LAS_LATENTE_STEUERN ON LAS_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = LEBPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = LEB_POSTITNR" "~"
if joinKuerzel = PITLEB
searchResult := searchResult "LEFT JOIN BUERO.LEB_LEBENSLAUF ON LEB_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = LUEPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = LUE_POSTITNR" "~"
if joinKuerzel = PITLUE
searchResult := searchResult "LEFT JOIN PRODUKTION.LUE_LAGER_UMS_EMPFANGEN ON LUE_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = MOKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = MOK_POSTITNR" "~"
if joinKuerzel = PITMOK
searchResult := searchResult "LEFT JOIN WWS.MOK_MODELLKOPF ON MOK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = NEBPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = NEB_POSTITNR" "~"
if joinKuerzel = PITNEB
searchResult := searchResult "LEFT JOIN BIL.NEB_NEBENWERTE ON NEB_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PAPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PAP_POSTITNR" "~"
if joinKuerzel = PITPAP
searchResult := searchResult "LEFT JOIN WWS.PAP_PACKSTOFFTABPOS ON PAP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PETPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PET_POSTITNR" "~"
if joinKuerzel = PITPET
searchResult := searchResult "LEFT JOIN WWS.PET_PREISTAB ON PET_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PKMPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PKM_POSTITNR" "~"
if joinKuerzel = PITPKM
searchResult := searchResult "LEFT JOIN PRODUKTION.PKM_KM_MASTER ON PKM_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = RAPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = RAP_POSTITNR" "~"
if joinKuerzel = PITRAP
searchResult := searchResult "LEFT JOIN WWS.RAP_RABATTTABPOS ON RAP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = SBSPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = SBS_POSTITNR" "~"
if joinKuerzel = PITSBS
searchResult := searchResult "LEFT JOIN BUERO.SBS_STELLENBESCHREIBUNG ON SBS_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = SCAPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = SCA_POSTITNR" "~"
if joinKuerzel = PITSCA
searchResult := searchResult "LEFT JOIN PRODUKTION.SCA_SCKRITERIUM ON SCA_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = SEKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = SEK_POSTITNR" "~"
if joinKuerzel = PITSEK
searchResult := searchResult "LEFT JOIN BUERO.SEK_SEKRETARIAT ON SEK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = SEQPIT
searchResult := searchResult "LEFT JOIN BMD.PIT_POSTIT ON PIT_POSTITNR = SEQ_POSTITNR" "~"
if joinKuerzel = PITSEQ
searchResult := searchResult "LEFT JOIN BMD.SEQ_SEQUENZ ON SEQ_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = SKEPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = SKE_POSTITNR" "~"
if joinKuerzel = PITSKE
searchResult := searchResult "LEFT JOIN BIL.SKE_STEUERKUNDENZUERW ON SKE_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = SRAPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = SRA_POSTITNR" "~"
if joinKuerzel = PITSRA
searchResult := searchResult "LEFT JOIN WWS.SRA_STEUERART ON SRA_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = TRTPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = TRT_POSTITNR" "~"
if joinKuerzel = PITTRT
searchResult := searchResult "LEFT JOIN WWS.TRT_TRANSPORTTYP ON TRT_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = UBEPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = UBE_POSTITNR" "~"
if joinKuerzel = PITUBE
searchResult := searchResult "LEFT JOIN BMDINTERN.UBE_UPDATEBESTELLUNG ON UBE_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = VPIPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = VPI_POSTITNR" "~"
if joinKuerzel = PITVPI
searchResult := searchResult "LEFT JOIN BUERO.VPI_VPINDEX ON VPI_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = WRPPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = WRP_PRO_FIRMENNR AND PRO_PROJEKTNR = WRP_PROJEKTNR AND PRO_PROJEKTTYP = WRP_PROJEKTTYP" "~"
if joinKuerzel = PROWRP
searchResult := searchResult "LEFT JOIN WWS.WRP_WWSANKAUFSRNGPOS ON WRP_PRO_FIRMENNR = PRO_FIRMENNR AND WRP_PROJEKTNR = PRO_PROJEKTNR AND WRP_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = YBUPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = YBU_PRO_FIRMENNR AND PRO_PROJEKTNR = YBU_PRO_PROJEKTNR AND PRO_PROJEKTTYP = YBU_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROYBU
searchResult := searchResult "LEFT JOIN FIBU.YBU_AUTOBUCHUNG ON YBU_PRO_FIRMENNR = PRO_FIRMENNR AND YBU_PRO_PROJEKTNR = PRO_PROJEKTNR AND YBU_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = AUGPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = AUG_BASIS_PRO_FIRMENNR AND PRO_PROJEKTNR = AUG_BASIS_PROJEKTNR AND PRO_PROJEKTTYP = AUG_BASIS_PROJEKTTYP" "~"
if joinKuerzel = PROAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_BASIS_PRO_FIRMENNR = PRO_FIRMENNR AND AUG_BASIS_PROJEKTNR = PRO_PROJEKTNR AND AUG_BASIS_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = AVEPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = AVE_KOPF_PRO_FIRMENNR AND PRO_PROJEKTNR = AVE_KOPF_PRO_PROJEKTNR AND PRO_PROJEKTTYP = AVE_KOPF_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_KOPF_PRO_FIRMENNR = PRO_FIRMENNR AND AVE_KOPF_PRO_PROJEKTNR = PRO_PROJEKTNR AND AVE_KOPF_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = PKUPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_PROJEKTID = PKU_SUBPROJEKTID" "~"
if joinKuerzel = PROPKU
searchResult := searchResult "LEFT JOIN BUERO.PKU_PROJEKTZU ON PKU_SUBPROJEKTID = PRO_PROJEKTID" "~"
if joinKuerzel = TERPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = TER_BASIS_PRO_FIRMENNR AND PRO_PROJEKTNR = TER_BASIS_PROJEKTNR AND PRO_PROJEKTTYP = TER_BASIS_PROJEKTTYP" "~"
if joinKuerzel = PROTER
searchResult := searchResult "LEFT JOIN BUERO.TER_TERMIN ON TER_BASIS_PRO_FIRMENNR = PRO_FIRMENNR AND TER_BASIS_PROJEKTNR = PRO_PROJEKTNR AND TER_BASIS_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = AGNPRS
searchResult := searchResult "LEFT JOIN PRS_PROZESS.PRS_PROZESS ON PRS_FIRMENNR = AGN_PRS_FIRMENNR AND PRS_PROZESSID = AGN_PROZESSID" "~"
if joinKuerzel = PRSAGN
searchResult := searchResult "LEFT JOIN WWS.AGN_AG_PERSON_ZU ON AGN_PRS_FIRMENNR = PRS_FIRMENNR AND AGN_PROZESSID = PRS_PROZESSID" "~"
if joinKuerzel = ERAPSG
searchResult := searchResult "LEFT JOIN WWS.PSG_PREISGRUPPE ON PSG_FIRMENNR = ERA_HAUPTFIRMENNR AND PSG_PREISHAUPTGRNR = ERA_PREISHAUPTGRNR AND PSG_PREISUNTERGRNR = ERA_PREISUNTERGRNR" "~"
if joinKuerzel = PSGERA
searchResult := searchResult "LEFT JOIN WWS.ERA_ERFASSRABATT ON ERA_HAUPTFIRMENNR = PSG_FIRMENNR AND ERA_PREISHAUPTGRNR = PSG_PREISHAUPTGRNR AND ERA_PREISUNTERGRNR = PSG_PREISUNTERGRNR" "~"
if joinKuerzel = GSAPSG
searchResult := searchResult "LEFT JOIN WWS.PSG_PREISGRUPPE ON PSG_FIRMENNR = GSA_PSG_FIRMENNR AND PSG_PREISHAUPTGRNR = GSA_PREISHAUPTGRNR AND PSG_PREISUNTERGRNR = GSA_PREISUNTERGRNR" "~"
if joinKuerzel = PSGGSA
searchResult := searchResult "LEFT JOIN WWS.GSA_GUTSCHEINAKTION ON GSA_PSG_FIRMENNR = PSG_FIRMENNR AND GSA_PREISHAUPTGRNR = PSG_PREISHAUPTGRNR AND GSA_PREISUNTERGRNR = PSG_PREISUNTERGRNR" "~"
if joinKuerzel = PIGPSG
searchResult := searchResult "LEFT JOIN WWS.PSG_PREISGRUPPE ON PSG_FIRMENNR = PIG_PSG_FIRMENNR AND PSG_PREISHAUPTGRNR = PIG_PSG_PREISHAUPTGRNR AND PSG_PREISUNTERGRNR = PIG_PSG_PREISUNTERGRNR" "~"
if joinKuerzel = PSGPIG
searchResult := searchResult "LEFT JOIN WWS.PIG_PICPREISGRUPPE ON PIG_PSG_FIRMENNR = PSG_FIRMENNR AND PIG_PSG_PREISHAUPTGRNR = PSG_PREISHAUPTGRNR AND PIG_PSG_PREISUNTERGRNR = PSG_PREISUNTERGRNR" "~"
if joinKuerzel = WEAPSG
searchResult := searchResult "LEFT JOIN WWS.PSG_PREISGRUPPE ON PSG_FIRMENNR = WEA_HAUPTFIRMENNR AND PSG_PREISHAUPTGRNR = WEA_PREISHAUPTGRNR AND PSG_PREISUNTERGRNR = WEA_PREISUNTERGRNR" "~"
if joinKuerzel = PSGWEA
searchResult := searchResult "LEFT JOIN WWS.WEA_WWSERKONTROLLERABATTE ON WEA_HAUPTFIRMENNR = PSG_FIRMENNR AND WEA_PREISHAUPTGRNR = PSG_PREISHAUPTGRNR AND WEA_PREISUNTERGRNR = PSG_PREISUNTERGRNR" "~"
if joinKuerzel = BRPPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = BRP_FIRMENNR AND PSJ_MITARBEITERID = BRP_MITARBEITERID AND PSJ_DIENSTVLFDNR = BRP_DIENSTVLFDNR AND PSJ_LOHNJAHR = BRP_LOHNJAHR" "~"
if joinKuerzel = PSJBRP
searchResult := searchResult "LEFT JOIN HRS.BRP_BRUTTOERFPROTOKOLL ON BRP_FIRMENNR = PSJ_FIRMENNR AND BRP_MITARBEITERID = PSJ_MITARBEITERID AND BRP_DIENSTVLFDNR = PSJ_DIENSTVLFDNR AND BRP_LOHNJAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = EAEPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = EAE_FIRMENNR AND PSJ_MITARBEITERID = EAE_MITARBEITERID AND PSJ_DIENSTVLFDNR = EAE_DIENSTVLFDNR AND PSJ_LOHNJAHR = EAE_JAHR" "~"
if joinKuerzel = PSJEAE
searchResult := searchResult "LEFT JOIN HRS.EAE_ELDADETAIL_AEK ON EAE_FIRMENNR = PSJ_FIRMENNR AND EAE_MITARBEITERID = PSJ_MITARBEITERID AND EAE_DIENSTVLFDNR = PSJ_DIENSTVLFDNR AND EAE_JAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = EM2PSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = EM2_FIRMENNR AND PSJ_MITARBEITERID = EM2_MITARBEITERID AND PSJ_DIENSTVLFDNR = EM2_DIENSTVLFDNR AND PSJ_LOHNJAHR = EM2_JAHR" "~"
if joinKuerzel = PSJEM2
searchResult := searchResult "LEFT JOIN HRS.EM2_ELDADETAIL_MBGM ON EM2_FIRMENNR = PSJ_FIRMENNR AND EM2_MITARBEITERID = PSJ_MITARBEITERID AND EM2_DIENSTVLFDNR = PSJ_DIENSTVLFDNR AND EM2_JAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = PSMPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = PSM_FIRMENNR AND PSJ_MITARBEITERID = PSM_MITARBEITERID AND PSJ_DIENSTVLFDNR = PSM_DIENSTVLFDNR AND PSJ_LOHNJAHR = PSM_PSMJAHR" "~"
if joinKuerzel = PSJPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_FIRMENNR = PSJ_FIRMENNR AND PSM_MITARBEITERID = PSJ_MITARBEITERID AND PSM_DIENSTVLFDNR = PSJ_DIENSTVLFDNR AND PSM_PSMJAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = PSPPSS
searchResult := searchResult "LEFT JOIN PRODUKTION.PSS_STAPEL_SCHRITT ON PSS_FIRMENNR = PSP_FIRMENNR AND PSS_NR = PSP_NR AND PSS_SCHRITT = PSP_SCHRITT" "~"
if joinKuerzel = PSSPSP
searchResult := searchResult "LEFT JOIN PRODUKTION.PSP_STAPEL_SCHRITT_PARAM ON PSP_FIRMENNR = PSS_FIRMENNR AND PSP_NR = PSS_NR AND PSP_SCHRITT = PSS_SCHRITT" "~"
if joinKuerzel = BAUPSZ
searchResult := searchResult "LEFT JOIN BUERO.PSZ_PERSONENKZ ON PSZ_PERSONENART = BAU_PERSONENART AND PSZ_PERSONKZNR = BAU_PERSONKZNR" "~"
if joinKuerzel = PSZBAU
searchResult := searchResult "LEFT JOIN BUERO.BAU_BAUM ON BAU_PERSONENART = PSZ_PERSONENART AND BAU_PERSONKZNR = PSZ_PERSONKZNR" "~"
if joinKuerzel = SCASCK
searchResult := searchResult "LEFT JOIN PRODUKTION.SCK_SCKENNZAHL ON SCK_SCKID = SCA_SCKID_SEVERITY" "~"
if joinKuerzel = SCKSCA
searchResult := searchResult "LEFT JOIN PRODUKTION.SCA_SCKRITERIUM ON SCA_SCKID_SEVERITY = SCK_SCKID" "~"
if joinKuerzel = SCYSCM
searchResult := searchResult "LEFT JOIN BMDENTW.SCM_SCRMAIN ON SCM_SCRNR = SCY_SCRNR_ABHAENGIG" "~"
if joinKuerzel = SCMSCY
searchResult := searchResult "LEFT JOIN BMDENTW.SCY_SCRDEPENDENCY ON SCY_SCRNR_ABHAENGIG = SCM_SCRNR" "~"
if joinKuerzel = SCMSCM
searchResult := searchResult "LEFT JOIN BMDENTW.SCM_SCRMAIN ON SCM_SCRNR = SCM_TIC_MERGE_SCRNR" "~"
if joinKuerzel = SCMSCM
searchResult := searchResult "LEFT JOIN BMDENTW.SCM_SCRMAIN ON SCM_TIC_MERGE_SCRNR = SCM_SCRNR" "~"
if joinKuerzel = PICSFZ
searchResult := searchResult "LEFT JOIN SFZ_SYNCFELDZUORDNUNG.SFZ_SYNCFELDZUORDNUNG ON SFZ_SYNCFELDZUID = PIC_SYNCFELDZUID_KLI" "~"
if joinKuerzel = SFZPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_SYNCFELDZUID_KLI = SFZ_SYNCFELDZUID" "~"
if joinKuerzel = GKVSID
searchResult := searchResult "LEFT JOIN HRS.SID_HRSSEQ ON SID_HRSSEQID = GKV_SID_KVID" "~"
if joinKuerzel = SIDGKV
searchResult := searchResult "LEFT JOIN HRS.GKV_GEBSTUFEKV ON GKV_SID_KVID = SID_HRSSEQID" "~"
if joinKuerzel = HINSID
searchResult := searchResult "LEFT JOIN HRS.SID_HRSSEQ ON SID_HRSSEQID = HIN_SID_HRSSEQID_KOV" "~"
if joinKuerzel = SIDHIN
searchResult := searchResult "LEFT JOIN HRS.HIN_HINWEIS ON HIN_SID_HRSSEQID_KOV = SID_HRSSEQID" "~"
if joinKuerzel = PSMSID
searchResult := searchResult "LEFT JOIN HRS.SID_HRSSEQ ON SID_HRSSEQID = PSM_HRSSEQID" "~"
if joinKuerzel = SIDPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_HRSSEQID = SID_HRSSEQID" "~"
if joinKuerzel = ARLSLK
searchResult := searchResult "LEFT JOIN WWS.SLK_STUECKLISTENKOPF ON SLK_STKLISTKOPFID = ARL_STKLISTKOPFID" "~"
if joinKuerzel = SLKARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_STKLISTKOPFID = SLK_STKLISTKOPFID" "~"
if joinKuerzel = ASBSMI
searchResult := searchResult "LEFT JOIN ZLE.SMI_SMARTINTERFACE ON SMI_FIRMENNR = ASB_SMI_FIRMENNR AND SMI_SMARTINTERFACEID = ASB_SMARTINTERFACEID" "~"
if joinKuerzel = SMIASB
searchResult := searchResult "LEFT JOIN ZLE.ASB_AUSWERT_SUB ON ASB_SMI_FIRMENNR = SMI_FIRMENNR AND ASB_SMARTINTERFACEID = SMI_SMARTINTERFACEID" "~"
if joinKuerzel = SNVSNI
searchResult := searchResult "LEFT JOIN HRS.SNI_SCHNITTE ON SNI_DBSNR = SNV_DBSNR AND SNI_SCHNITTNR = SNV_SCHNITTNR" "~"
if joinKuerzel = SNISNV
searchResult := searchResult "LEFT JOIN HRS.SNV_SCHNITTVORSCHLAG ON SNV_DBSNR = SNI_DBSNR AND SNV_SCHNITTNR = SNI_SCHNITTNR" "~"
if joinKuerzel = LAWSNI
searchResult := searchResult "LEFT JOIN HRS.SNI_SCHNITTE ON SNI_DBSNR = LAW_DBSNR AND SNI_SCHNITTNR = LAW_SCHNITTNR_2" "~"
if joinKuerzel = SNILAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_DBSNR = SNI_DBSNR AND LAW_SCHNITTNR_2 = SNI_SCHNITTNR" "~"
if joinKuerzel = LAJSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_SERNRID = LAJ_SERNRID" "~"
if joinKuerzel = SNRLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = AVESPR
searchResult := searchResult "LEFT JOIN SPR_SPRACHE.SPR_SPRACHE ON SPR_SPRACHNR = AVE_AUFTRGEB_SPRACHNR" "~"
if joinKuerzel = SPRAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_AUFTRGEB_SPRACHNR = SPR_SPRACHNR" "~"
if joinKuerzel = BUCSPR
searchResult := searchResult "LEFT JOIN SPR_SPRACHE.SPR_SPRACHE ON SPR_SPRACHNR = BUC_SPRACHNR" "~"
if joinKuerzel = SPRBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_SPRACHNR = SPR_SPRACHNR" "~"
if joinKuerzel = PIHSPR
searchResult := searchResult "LEFT JOIN SPR_SPRACHE.SPR_SPRACHE ON SPR_SPRACHNR = PIH_SPRACHNR" "~"
if joinKuerzel = SPRPIH
searchResult := searchResult "LEFT JOIN WWS.PIH_PICSPRACHEZU ON PIH_SPRACHNR = SPR_SPRACHNR" "~"
if joinKuerzel = AVESPR
searchResult := searchResult "LEFT JOIN SPR_SPRACHE.SPR_SPRACHE ON SPR_SPRACHNR = AVE_LIEFEMPF_SPRACHNR" "~"
if joinKuerzel = SPRAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_LIEFEMPF_SPRACHNR = SPR_SPRACHNR" "~"
if joinKuerzel = AVESPR
searchResult := searchResult "LEFT JOIN SPR_SPRACHE.SPR_SPRACHE ON SPR_SPRACHNR = AVE_RECHEMPF_SPRACHNR" "~"
if joinKuerzel = SPRAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_RECHEMPF_SPRACHNR = SPR_SPRACHNR" "~"
if joinKuerzel = ADESRA
searchResult := searchResult "LEFT JOIN WWS.SRA_STEUERART ON SRA_FIRMENNR = ADE_HAUPTFIRMENNR AND SRA_STEUERARTNR = ADE_STEUERARTNR" "~"
if joinKuerzel = SRAADE
searchResult := searchResult "LEFT JOIN WWS.ADE_ABGABENDETAILERFPOS ON ADE_HAUPTFIRMENNR = SRA_FIRMENNR AND ADE_STEUERARTNR = SRA_STEUERARTNR" "~"
if joinKuerzel = SRWSRA
searchResult := searchResult "LEFT JOIN WWS.SRA_STEUERART ON SRA_FIRMENNR = SRW_FIRMENNR AND SRA_STEUERARTNR = SRW_STEUERARTNR" "~"
if joinKuerzel = SRASRW
searchResult := searchResult "LEFT JOIN WWS.SRW_STEUERARTWERT ON SRW_FIRMENNR = SRA_FIRMENNR AND SRW_STEUERARTNR = SRA_STEUERARTNR" "~"
if joinKuerzel = BUCSSC
searchResult := searchResult "LEFT JOIN FIBU.SSC_STEUERSCHLUESSEL ON SSC_FIRMENNR = BUC_FIRMENNR AND SSC_FIBUNR = BUC_FIBUNR AND SSC_LANDNR = BUC_LAN_LANDNR AND SSC_STEUERSCHLUESSELNR = BUC_STEUERSCHLUESSELNR" "~"
if joinKuerzel = SSCBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_FIRMENNR = SSC_FIRMENNR AND BUC_FIBUNR = SSC_FIBUNR AND BUC_LAN_LANDNR = SSC_LANDNR AND BUC_STEUERSCHLUESSELNR = SSC_STEUERSCHLUESSELNR" "~"
if joinKuerzel = STZSTC
searchResult := searchResult "LEFT JOIN STC_STEUERCODE.STC_STEUERCODE ON STC_LANDNR = STZ_STEUERGRUPPENLANDNR AND STC_STEUERCODENR = STZ_STEUERCODENR" "~"
if joinKuerzel = STCSTZ
searchResult := searchResult "LEFT JOIN WWS.STZ_STEUERTEXTZUORDNUNG ON STZ_STEUERGRUPPENLANDNR = STC_LANDNR AND STZ_STEUERCODENR = STC_STEUERCODENR" "~"
if joinKuerzel = YASSTC
searchResult := searchResult "LEFT JOIN FIBU.STC_STEUERCODE ON STC_LANDNR = YAS_LANDNR AND STC_STEUERCODENR = YAS_STEUERCODENR" "~"
if joinKuerzel = STCYAS
searchResult := searchResult "LEFT JOIN FIBU.YAS_AUTOBUCHAUFTEILSATZ ON YAS_LANDNR = STC_LANDNR AND YAS_STEUERCODENR = STC_STEUERCODENR" "~"
if joinKuerzel = SKESTE
searchResult := searchResult "LEFT JOIN STE_STEUERERKLAERUNG.STE_STEUERERKLAERUNG ON STE_STEUER_ERKLNR = SKE_STEUER_ERKLNR" "~"
if joinKuerzel = STESKE
searchResult := searchResult "LEFT JOIN BIL.SKE_STEUERKUNDENZUERW ON SKE_STEUER_ERKLNR = STE_STEUER_ERKLNR" "~"
if joinKuerzel = ZAWRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = ZAW_GESSUM_REPORTNR" "~"
if joinKuerzel = RPTZAW
searchResult := searchResult "LEFT JOIN BUERO.ZAW_ZEITAUSWERTUNG ON ZAW_GESSUM_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = RERRSE
searchResult := searchResult "LEFT JOIN WWS.RSE_RESERVIERUNGSSYSEXT ON RSE_RSEID = RER_RSEID" "~"
if joinKuerzel = RSERER
searchResult := searchResult "LEFT JOIN WWS.RER_RESERVIERUNGSREFERENZ ON RER_RSEID = RSE_RSEID" "~"
if joinKuerzel = RDDRSP
searchResult := searchResult "LEFT JOIN BIL.RSP_RSTSPALTENKOPF ON RSP_RSPID = RDD_RSPID" "~"
if joinKuerzel = RSPRDD
searchResult := searchResult "LEFT JOIN BIL.RDD_RSTDRUCKDETAIL ON RDD_RSPID = RSP_RSPID" "~"
if joinKuerzel = PRPSAG
searchResult := searchResult "LEFT JOIN WWS.SAG_STATISTIKARTIKELGRP ON SAG_FIRMENNR = PRP_SAG_FIRMENNR AND SAG_STATISTIKLFDNR = PRP_STATISTIKLFDNR AND SAG_STATISTIKGRPLFDNR = PRP_STATISTIKGRPLFDNR" "~"
if joinKuerzel = SAGPRP
searchResult := searchResult "LEFT JOIN WWS.PRP_PROVISIONTABPOS ON PRP_SAG_FIRMENNR = SAG_FIRMENNR AND PRP_STATISTIKLFDNR = SAG_STATISTIKLFDNR AND PRP_STATISTIKGRPLFDNR = SAG_STATISTIKGRPLFDNR" "~"
if joinKuerzel = BPASAK
searchResult := searchResult "LEFT JOIN FIBU.SAK_SACHKONTO ON SAK_FIRMENNR = BPA_FIRMENNR AND SAK_FIBUNR = BPA_FIBUNR AND SAK_KONTONR = BPA_KONTONR" "~"
if joinKuerzel = SAKBPA
searchResult := searchResult "LEFT JOIN FIBU.BPA_BUDGETPLANBILAUFTLG ON BPA_FIRMENNR = SAK_FIRMENNR AND BPA_FIBUNR = SAK_FIBUNR AND BPA_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = EDVSAK
searchResult := searchResult "LEFT JOIN SAK_SACHKONTO.SAK_SACHKONTO ON SAK_FIRMENNR = EDV_SAK_FIRMENNR AND SAK_FIBUNR = EDV_SAK_FIBUNR AND SAK_KONTONR = EDV_SAK_KONTONR" "~"
if joinKuerzel = SAKEDV
searchResult := searchResult "LEFT JOIN WWS.EDV_ERFASSRABATTDETAILVOR ON EDV_SAK_FIRMENNR = SAK_FIRMENNR AND EDV_SAK_FIBUNR = SAK_FIBUNR AND EDV_SAK_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = PBOSAK
searchResult := searchResult "LEFT JOIN SAK_SACHKONTO.SAK_SACHKONTO ON SAK_FIRMENNR = PBO_FIRMENNR AND SAK_FIBUNR = PBO_FIBUNR AND SAK_KONTONR = PBO_KTO_KONTONR" "~"
if joinKuerzel = SAKPBO
searchResult := searchResult "LEFT JOIN BIL.PBO_PERSKORST_KONTEN ON PBO_FIRMENNR = SAK_FIRMENNR AND PBO_FIBUNR = SAK_FIBUNR AND PBO_KTO_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = BIPSAK
searchResult := searchResult "LEFT JOIN FIBU.SAK_SACHKONTO ON SAK_FIRMENNR = BIP_FIRMENNR AND SAK_FIBUNR = BIP_FIBUNR AND SAK_KONTONR = BIP_AFA_KONTONR" "~"
if joinKuerzel = SAKBIP
searchResult := searchResult "LEFT JOIN FIBU.BIP_BUDGETINVESTPLAN ON BIP_FIRMENNR = SAK_FIRMENNR AND BIP_FIBUNR = SAK_FIBUNR AND BIP_AFA_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = PKPSAK
searchResult := searchResult "LEFT JOIN FIBU.SAK_SACHKONTO ON SAK_FIRMENNR = PKP_FIRMENNR AND SAK_FIBUNR = PKP_FIBUNR AND SAK_KONTONR = PKP_PROVISION_KONTONR" "~"
if joinKuerzel = SAKPKP
searchResult := searchResult "LEFT JOIN FIBU.PKP_BUDGETPERSKOSTPLAN ON PKP_FIRMENNR = SAK_FIRMENNR AND PKP_FIBUNR = SAK_FIBUNR AND PKP_PROVISION_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = PKPSAK
searchResult := searchResult "LEFT JOIN FIBU.SAK_SACHKONTO ON SAK_FIRMENNR = PKP_FIRMENNR AND SAK_FIBUNR = PKP_FIBUNR AND SAK_KONTONR = PKP_SV_KONTONR" "~"
if joinKuerzel = SAKPKP
searchResult := searchResult "LEFT JOIN FIBU.PKP_BUDGETPERSKOSTPLAN ON PKP_FIRMENNR = SAK_FIRMENNR AND PKP_FIBUNR = SAK_FIBUNR AND PKP_SV_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = LEISAP
searchResult := searchResult "LEFT JOIN SAP_SAMMELPROJEKT.SAP_SAMMELPROJEKT ON SAP_FIRMENNR = LEI_SAP_FIRMENNR AND SAP_SAMMELPROJEKTNR = LEI_SAP_SAMMELPROJEKTNR" "~"
if joinKuerzel = SAPLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_SAP_FIRMENNR = SAP_FIRMENNR AND LEI_SAP_SAMMELPROJEKTNR = SAP_SAMMELPROJEKTNR" "~"
if joinKuerzel = FSZSAT
searchResult := searchResult "LEFT JOIN BUERO.SAT_SETANTWORT ON SAT_AAS_AASID = FSZ_AAS_AASID AND SAT_SETANTWDETAILLFDNR = FSZ_SAT_SETANTWDETAILLFDNR" "~"
if joinKuerzel = SATFSZ
searchResult := searchResult "LEFT JOIN BUERO.FSZ_FREIEANTWORTSETZU ON FSZ_AAS_AASID = SAT_AAS_AASID AND FSZ_SAT_SETANTWDETAILLFDNR = SAT_SETANTWDETAILLFDNR" "~"
if joinKuerzel = BPKSBC
searchResult := searchResult "LEFT JOIN SBC_SPEZBEWEGUNGSCODE.SBC_SPEZBEWEGUNGSCODE ON SBC_FIRMENNR = BPK_SBC_FIRMENNR AND SBC_SBCCODE = BPK_SBC_SBCCODE" "~"
if joinKuerzel = SBCBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_SBC_FIRMENNR = SBC_FIRMENNR AND BPK_SBC_SBCCODE = SBC_SBCCODE" "~"
if joinKuerzel = MISSBS
searchResult := searchResult "LEFT JOIN BUERO.SBS_STELLENBESCHREIBUNG ON SBS_STELLBESCHID = MIS_SBS_STELLBESCHID" "~"
if joinKuerzel = SBSMIS
searchResult := searchResult "LEFT JOIN BUERO.MIS_MITARBSTELLEZU ON MIS_SBS_STELLBESCHID = SBS_STELLBESCHID" "~"
if joinKuerzel = STMSBS
searchResult := searchResult "LEFT JOIN BUERO.SBS_STELLENBESCHREIBUNG ON SBS_STELLBESCHID = STM_SBS_STELLBESCHID" "~"
if joinKuerzel = SBSSTM
searchResult := searchResult "LEFT JOIN BUERO.STM_STELLENMERKMALE ON STM_SBS_STELLBESCHID = SBS_STELLBESCHID" "~"
if joinKuerzel = OUPSBV
searchResult := searchResult "LEFT JOIN BUERO.SBV_STELLENBESCHVORLAGE ON SBV_VORLAGENID = OUP_TEXTBAUSTEINID09" "~"
if joinKuerzel = SBVOUP
searchResult := searchResult "LEFT JOIN BUERO.OUP_ONLUMFRAGEPAGE ON OUP_TEXTBAUSTEINID09 = SBV_VORLAGENID" "~"
if joinKuerzel = RIHSCA
searchResult := searchResult "LEFT JOIN PRODUKTION.SCA_SCKRITERIUM ON SCA_SCAID = RIH_SCAID" "~"
if joinKuerzel = SCARIH
searchResult := searchResult "LEFT JOIN PRODUKTION.RIH_RISIKOANALYSE_HEAD ON RIH_SCAID = SCA_SCAID" "~"
if joinKuerzel = SCASCA
searchResult := searchResult "LEFT JOIN PRODUKTION.SCA_SCKRITERIUM ON SCA_SCAID = SCA_SCA_REF_ID" "~"
if joinKuerzel = SCASCA
searchResult := searchResult "LEFT JOIN PRODUKTION.SCA_SCKRITERIUM ON SCA_SCA_REF_ID = SCA_SCAID" "~"
if joinKuerzel = SCGSCE
searchResult := searchResult "LEFT JOIN PRODUKTION.SCE_SCORECARD ON SCE_SCEID = SCG_SCEID" "~"
if joinKuerzel = SCESCG
searchResult := searchResult "LEFT JOIN PRODUKTION.SCG_SCORECARDKRITZU ON SCG_SCEID = SCE_SCEID" "~"
if joinKuerzel = SCASCK
searchResult := searchResult "LEFT JOIN PRODUKTION.SCK_SCKENNZAHL ON SCK_SCKID = SCA_SCKID" "~"
if joinKuerzel = SCKSCA
searchResult := searchResult "LEFT JOIN PRODUKTION.SCA_SCKRITERIUM ON SCA_SCKID = SCK_SCKID" "~"
if joinKuerzel = SCGSCK
searchResult := searchResult "LEFT JOIN PRODUKTION.SCK_SCKENNZAHL ON SCK_SCKID = SCG_SCKID" "~"
if joinKuerzel = SCKSCG
searchResult := searchResult "LEFT JOIN PRODUKTION.SCG_SCORECARDKRITZU ON SCG_SCKID = SCK_SCKID" "~"
if joinKuerzel = TLPSNR
searchResult := searchResult "LEFT JOIN SNR_SERIENNUMMERN.SNR_SERIENNUMMERN ON SNR_SERNRID = TLP_SERNRID" "~"
if joinKuerzel = SNRTLP
searchResult := searchResult "LEFT JOIN PRODUKTION.TLP_BT_LOGISTIK_POSITION ON TLP_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = SPESPQ
searchResult := searchResult "LEFT JOIN HRS.SPQ_SPESENARTEN_SEQ ON SPQ_SPESENARTID = SPE_VERTEIL_SPESENARTID1" "~"
if joinKuerzel = SPQSPE
searchResult := searchResult "LEFT JOIN HRS.SPE_SPESENARTEN ON SPE_VERTEIL_SPESENARTID1 = SPQ_SPESENARTID" "~"
if joinKuerzel = SPESPQ
searchResult := searchResult "LEFT JOIN HRS.SPQ_SPESENARTEN_SEQ ON SPQ_SPESENARTID = SPE_VERTEIL_SPESENARTID7" "~"
if joinKuerzel = SPQSPE
searchResult := searchResult "LEFT JOIN HRS.SPE_SPESENARTEN ON SPE_VERTEIL_SPESENARTID7 = SPQ_SPESENARTID" "~"
if joinKuerzel = MAHSPR
searchResult := searchResult "LEFT JOIN SPR_SPRACHE.SPR_SPRACHE ON SPR_SPRACHNR = MAH_SPRACHNR" "~"
if joinKuerzel = SPRMAH
searchResult := searchResult "LEFT JOIN FIBU.MAH_MAHNUNG ON MAH_SPRACHNR = SPR_SPRACHNR" "~"
if joinKuerzel = WRKSPR
searchResult := searchResult "LEFT JOIN SPR_SPRACHE.SPR_SPRACHE ON SPR_SPRACHNR = WRK_SPRACHNR" "~"
if joinKuerzel = SPRWRK
searchResult := searchResult "LEFT JOIN WWS.WRK_WWSANKAUFSRNGKOPF ON WRK_SPRACHNR = SPR_SPRACHNR" "~"
if joinKuerzel = WURSPR
searchResult := searchResult "LEFT JOIN SPR_SPRACHE.SPR_SPRACHE ON SPR_SPRACHNR = WUR_SPRACHNR" "~"
if joinKuerzel = SPRWUR
searchResult := searchResult "LEFT JOIN WWS.WUR_WWSURL ON WUR_SPRACHNR = SPR_SPRACHNR" "~"
if joinKuerzel = SRPSRA
searchResult := searchResult "LEFT JOIN WWS.SRA_STEUERART ON SRA_FIRMENNR = SRP_FIRMENNR AND SRA_STEUERARTNR = SRP_STEUERARTNR" "~"
if joinKuerzel = SRASRP
searchResult := searchResult "LEFT JOIN WWS.SRP_STEUERTABPOS ON SRP_FIRMENNR = SRA_FIRMENNR AND SRP_STEUERARTNR = SRA_STEUERARTNR" "~"
if joinKuerzel = ERZSRM
searchResult := searchResult "LEFT JOIN WWS.SRM_SAMMELRECHNUNGSMODELL ON SRM_SAMMELRECHID = ERZ_ABRECHNUNGSMODELL_WHF" "~"
if joinKuerzel = SRMERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_ABRECHNUNGSMODELL_WHF = SRM_SAMMELRECHID" "~"
if joinKuerzel = RNPSTC
searchResult := searchResult "LEFT JOIN STC_STEUERCODE.STC_STEUERCODE ON STC_LANDNR = RNP_STEUERCODE_LANDNR AND STC_STEUERCODENR = RNP_STEUERCODENR" "~"
if joinKuerzel = STCRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_STEUERCODE_LANDNR = STC_LANDNR AND RNP_STEUERCODENR = STC_STEUERCODENR" "~"
if joinKuerzel = STFSTC
searchResult := searchResult "LEFT JOIN FIBU.STC_STEUERCODE ON STC_LANDNR = STF_LANDNR AND STC_STEUERCODENR = STF_STEUERCODENR" "~"
if joinKuerzel = STCSTF
searchResult := searchResult "LEFT JOIN FIBU.STF_FIBUSTEUERCODEZU ON STF_LANDNR = STC_LANDNR AND STF_STEUERCODENR = STC_STEUERCODENR" "~"
if joinKuerzel = WESSTC
searchResult := searchResult "LEFT JOIN STC_STEUERCODE.STC_STEUERCODE ON STC_LANDNR = WES_STEUERCODE_LANDNR AND STC_STEUERCODENR = WES_STEUERCODENR" "~"
if joinKuerzel = STCWES
searchResult := searchResult "LEFT JOIN WWS.WES_WWSERKONTROLLEWBK ON WES_STEUERCODE_LANDNR = STC_LANDNR AND WES_STEUERCODENR = STC_STEUERCODENR" "~"
if joinKuerzel = UVASTE
searchResult := searchResult "LEFT JOIN STE_STEUERERKLAERUNG.STE_STEUERERKLAERUNG ON STE_STEUER_ERKLNR = UVA_STEUER_ERKLNR" "~"
if joinKuerzel = STEUVA
searchResult := searchResult "LEFT JOIN FIBU.UVA_UVAMODELL ON UVA_STEUER_ERKLNR = STE_STEUER_ERKLNR" "~"
if joinKuerzel = UVFSTE
searchResult := searchResult "LEFT JOIN STE_STEUERERKLAERUNG.STE_STEUERERKLAERUNG ON STE_STEUER_ERKLNR = UVF_JAHRESUST_ERKLNR" "~"
if joinKuerzel = STEUVF
searchResult := searchResult "LEFT JOIN FIBU.UVF_UVAFELDZU ON UVF_JAHRESUST_ERKLNR = STE_STEUER_ERKLNR" "~"
if joinKuerzel = XSESTE
searchResult := searchResult "LEFT JOIN BUERO.STE_STEUERERKLAERUNG ON STE_STEUER_ERKLNR = XSE_STEUER_ERKLNR" "~"
if joinKuerzel = STEXSE
searchResult := searchResult "LEFT JOIN BUERO.XSE_XML_STEUERERKL ON XSE_STEUER_ERKLNR = STE_STEUER_ERKLNR" "~"
if joinKuerzel = STZSTG
searchResult := searchResult "LEFT JOIN STG_STEUERGRUPPE.STG_STEUERGRUPPE ON STG_LANDNR = STZ_STEUERGRUPPENLANDNR AND STG_STEUERGRUPPENNR = STZ_STEUERGRUPPENNR" "~"
if joinKuerzel = STGSTZ
searchResult := searchResult "LEFT JOIN WWS.STZ_STEUERTEXTZUORDNUNG ON STZ_STEUERGRUPPENLANDNR = STG_LANDNR AND STZ_STEUERGRUPPENNR = STG_STEUERGRUPPENNR" "~"
if joinKuerzel = DSDDSM
searchResult := searchResult "LEFT JOIN DATANORM.DSM_SET ON DSM_ERST_NR = DSD_ERST_NR AND DSM_SET_NR = DSD_SET_NR AND DSM_SET_KZ = DSD_SET_KZ" "~"
if joinKuerzel = DSMDSD
searchResult := searchResult "LEFT JOIN DATANORM.DSD_SET_DETAIL ON DSD_ERST_NR = DSM_ERST_NR AND DSD_SET_NR = DSM_SET_NR AND DSD_SET_KZ = DSM_SET_KZ" "~"
if joinKuerzel = SKOSVK
searchResult := searchResult "LEFT JOIN FIBU.SVK_SKONTOVERBUCHKZ ON SVK_FIRMENNR = SKO_FIRMENNR AND SVK_FIBUNR = SKO_FIBUNR AND SVK_SKONTOVERBUCHKZ = SKO_SVK_SKONTOVERBUCHKZ" "~"
if joinKuerzel = SVKSKO
searchResult := searchResult "LEFT JOIN FIBU.SKO_STEUERKONTEN ON SKO_FIRMENNR = SVK_FIRMENNR AND SKO_FIBUNR = SVK_FIBUNR AND SKO_SVK_SKONTOVERBUCHKZ = SVK_SKONTOVERBUCHKZ" "~"
if joinKuerzel = TCZTCA
searchResult := searchResult "LEFT JOIN BMD.TCA_TCACTIONLIST ON TCA_TCAID = TCZ_TCA_TCAID AND TCA_VERSIONSNR = TCZ_VERSIONSNR" "~"
if joinKuerzel = TCATCZ
searchResult := searchResult "LEFT JOIN BMD.TCZ_TCVERSIONCASEALZU ON TCZ_TCA_TCAID = TCA_TCAID AND TCZ_VERSIONSNR = TCA_VERSIONSNR" "~"
if joinKuerzel = TCDTCN
searchResult := searchResult "LEFT JOIN BMD.TCN_TCVERSIONEN ON TCN_VERSIONSNR = TCD_VERSIONSNR" "~"
if joinKuerzel = TCNTCD
searchResult := searchResult "LEFT JOIN BMD.TCD_TCKONFDATEN ON TCD_VERSIONSNR = TCN_VERSIONSNR" "~"
if joinKuerzel = DBTBTF
searchResult := searchResult "LEFT JOIN BTF_BMDTABFELDBESCH.BTF_BMDTABFELDBESCH ON BTF_TABELLENNAME = DBT_TABELLENNAME AND BTF_FELDNAME = DBT_FELDNAME" "~"
if joinKuerzel = BTFDBT
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DBT_DBMDTABFELDBESCH ON DBT_TABELLENNAME = BTF_TABELLENNAME AND DBT_FELDNAME = BTF_FELDNAME" "~"
if joinKuerzel = PSMTGR
searchResult := searchResult "LEFT JOIN HRS.TGR_TARIFGRUPPE ON TGR_DBS_DATENBESTAND = PSM_DBS_DATENBESTANDNR AND TGR_LOHNJAHR = PSM_PSMJAHR AND TGR_TARIFGRUPPENNR = PSM_TGR_TARIFGRUPPENNR" "~"
if joinKuerzel = TGRPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_DBS_DATENBESTANDNR = TGR_DBS_DATENBESTAND AND PSM_PSMJAHR = TGR_LOHNJAHR AND PSM_TGR_TARIFGRUPPENNR = TGR_TARIFGRUPPENNR" "~"
if joinKuerzel = VOBPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_FIRMENNR = VOB_PRS_FIRMENNR AND PRS_PROZESSID = VOB_PRS_PROZESSID" "~"
if joinKuerzel = PRSVOB
searchResult := searchResult "LEFT JOIN PRODUKTION.VOB_VORGANGSBENACHR ON VOB_PRS_FIRMENNR = PRS_FIRMENNR AND VOB_PRS_PROZESSID = PRS_PROZESSID" "~"
if joinKuerzel = AGSPRS
searchResult := searchResult "LEFT JOIN PRS_PROZESS.PRS_PROZESS ON PRS_FIRMENNR = AGS_PRS_FIRMENNR AND PRS_PROZESSID = AGS_NEXT_PROZESSID" "~"
if joinKuerzel = PRSAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_PRS_FIRMENNR = PRS_FIRMENNR AND AGS_NEXT_PROZESSID = PRS_PROZESSID" "~"
if joinKuerzel = ARLPSG
searchResult := searchResult "LEFT JOIN WWS.PSG_PREISGRUPPE ON PSG_FIRMENNR = ARL_FIRMENNR AND PSG_PREISHAUPTGRNR = ARL_PREISHAUPTGRNR AND PSG_PREISUNTERGRNR = ARL_PREISUNTERGRNR" "~"
if joinKuerzel = PSGARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = PSG_FIRMENNR AND ARL_PREISHAUPTGRNR = PSG_PREISHAUPTGRNR AND ARL_PREISUNTERGRNR = PSG_PREISUNTERGRNR" "~"
if joinKuerzel = PTPPSG
searchResult := searchResult "LEFT JOIN WWS.PSG_PREISGRUPPE ON PSG_FIRMENNR = PTP_PREISGRUPPEN_FIRMENNR AND PSG_PREISHAUPTGRNR = PTP_PREISHAUPTGRNR AND PSG_PREISUNTERGRNR = PTP_PREISUNTERGRNR" "~"
if joinKuerzel = PSGPTP
searchResult := searchResult "LEFT JOIN WWS.PTP_PREISTABPOS ON PTP_PREISGRUPPEN_FIRMENNR = PSG_FIRMENNR AND PTP_PREISHAUPTGRNR = PSG_PREISHAUPTGRNR AND PTP_PREISUNTERGRNR = PSG_PREISUNTERGRNR" "~"
if joinKuerzel = EEFPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = EEF_FIRMENNR AND PSJ_MITARBEITERID = EEF_MITARBEITERID AND PSJ_DIENSTVLFDNR = EEF_DIENSTVLFDNR AND PSJ_LOHNJAHR = EEF_JAHR" "~"
if joinKuerzel = PSJEEF
searchResult := searchResult "LEFT JOIN HRS.EEF_ELDADETAIL_EFZ ON EEF_FIRMENNR = PSJ_FIRMENNR AND EEF_MITARBEITERID = PSJ_MITARBEITERID AND EEF_DIENSTVLFDNR = PSJ_DIENSTVLFDNR AND EEF_JAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = EVMPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = EVM_FIRMENNR AND PSJ_MITARBEITERID = EVM_MITARBEITERID AND PSJ_DIENSTVLFDNR = EVM_DIENSTVLFDNR AND PSJ_LOHNJAHR = EVM_JAHR" "~"
if joinKuerzel = PSJEVM
searchResult := searchResult "LEFT JOIN HRS.EVM_ELDADETAIL_VM ON EVM_FIRMENNR = PSJ_FIRMENNR AND EVM_MITARBEITERID = PSJ_MITARBEITERID AND EVM_DIENSTVLFDNR = PSJ_DIENSTVLFDNR AND EVM_JAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = EVQPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = EVQ_FIRMENNR AND PSJ_MITARBEITERID = EVQ_MITARBEITERID AND PSJ_DIENSTVLFDNR = EVQ_DIENSTVLFDNR AND PSJ_LOHNJAHR = EVQ_JAHR" "~"
if joinKuerzel = PSJEVQ
searchResult := searchResult "LEFT JOIN HRS.EVQ_ELDADETAIL_VSNR ON EVQ_FIRMENNR = PSJ_FIRMENNR AND EVQ_MITARBEITERID = PSJ_MITARBEITERID AND EVQ_DIENSTVLFDNR = PSJ_DIENSTVLFDNR AND EVQ_JAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = GVTPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = GVT_FIRMENNR AND PSJ_MITARBEITERID = GVT_MITARBEITERID AND PSJ_DIENSTVLFDNR = GVT_DIENSTVLFDNR AND PSJ_LOHNJAHR = GVT_JAHR" "~"
if joinKuerzel = PSJGVT
searchResult := searchResult "LEFT JOIN HRS.GVT_GEMVERTEILUNG ON GVT_FIRMENNR = PSJ_FIRMENNR AND GVT_MITARBEITERID = PSJ_MITARBEITERID AND GVT_DIENSTVLFDNR = PSJ_DIENSTVLFDNR AND GVT_JAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = MADPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = MAD_FIRMENNR AND PSJ_MITARBEITERID = MAD_MITARBEITERID AND PSJ_DIENSTVLFDNR = MAD_DIENSTVLFDNR AND PSJ_LOHNJAHR = MAD_LOHNJAHR" "~"
if joinKuerzel = PSJMAD
searchResult := searchResult "LEFT JOIN HRS.MAD_MASSENDATENERF ON MAD_FIRMENNR = PSJ_FIRMENNR AND MAD_MITARBEITERID = PSJ_MITARBEITERID AND MAD_DIENSTVLFDNR = PSJ_DIENSTVLFDNR AND MAD_LOHNJAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = ABDPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_FIRMENNR = ABD_FIRMENNR AND PSM_MITARBEITERID = ABD_MITARBEITERID AND PSM_DIENSTVLFDNR = ABD_DIENSTVLFDNR AND PSM_PSMJAHR = ABD_JAHR AND PSM_PSMMONAT = ABD_MONAT AND PSM_PSMISTORG = ABD_PSMISTORG" "~"
if joinKuerzel = PSMABD
searchResult := searchResult "LEFT JOIN HRS.ABD_ABRECHNUNGSDETAIL ON ABD_FIRMENNR = PSM_FIRMENNR AND ABD_MITARBEITERID = PSM_MITARBEITERID AND ABD_DIENSTVLFDNR = PSM_DIENSTVLFDNR AND ABD_JAHR = PSM_PSMJAHR AND ABD_MONAT = PSM_PSMMONAT AND ABD_PSMISTORG = PSM_PSMISTORG" "~"
if joinKuerzel = WFNPSZ
searchResult := searchResult "LEFT JOIN BUERO.PSZ_PERSONENKZ ON PSZ_PERSONENART = WFN_PSZ_PERSONENART AND PSZ_PERSONKZNR = WFN_PSZ_PERSONKZNR" "~"
if joinKuerzel = PSZWFN
searchResult := searchResult "LEFT JOIN BUERO.WFN_WORKFLOWACTION ON WFN_PSZ_PERSONENART = PSZ_PERSONENART AND WFN_PSZ_PERSONKZNR = PSZ_PERSONKZNR" "~"
if joinKuerzel = PRPPVK
searchResult := searchResult "LEFT JOIN WWS.PVK_PROVISIONTAB ON PVK_PROVTABID = PRP_PROVTABID" "~"
if joinKuerzel = PVKPRP
searchResult := searchResult "LEFT JOIN WWS.PRP_PROVISIONTABPOS ON PRP_PROVTABID = PVK_PROVTABID" "~"
if joinKuerzel = PWPPWK
searchResult := searchResult "LEFT JOIN WWS.PWK_PREISRABATTWARTKOPF ON PWK_PWKID = PWP_PWKID" "~"
if joinKuerzel = PWKPWP
searchResult := searchResult "LEFT JOIN WWS.PWP_PREISWARTUNGPOS ON PWP_PWKID = PWK_PWKID" "~"
if joinKuerzel = RAGRAG
searchResult := searchResult "LEFT JOIN BUERO.RAG_RUHEARBEITSZEITGESETZ ON RAG_RAGID = RAG_MASTERID" "~"
if joinKuerzel = RAGRAG
searchResult := searchResult "LEFT JOIN BUERO.RAG_RUHEARBEITSZEITGESETZ ON RAG_MASTERID = RAG_RAGID" "~"
if joinKuerzel = RADRAS
searchResult := searchResult "LEFT JOIN WWS.RAS_RABATTTABPOSSTAFFEL ON RAS_FIRMENNR = RAD_FIRMENNR AND RAS_RABTABNR = RAD_RABTABNR AND RAS_RABTABPOSNR = RAD_RABTABPOSNR AND RAS_RABATTSTAFFELNR = RAD_RABATTSTAFFELNR" "~"
if joinKuerzel = RASRAD
searchResult := searchResult "LEFT JOIN WWS.RAD_RABATTBETRAGDETAIL ON RAD_FIRMENNR = RAS_FIRMENNR AND RAD_RABTABNR = RAS_RABTABNR AND RAD_RABTABPOSNR = RAS_RABTABPOSNR AND RAD_RABATTSTAFFELNR = RAS_RABATTSTAFFELNR" "~"
if joinKuerzel = BONRAT
searchResult := searchResult "LEFT JOIN WWS.RAT_RABATTTAB ON RAT_FIRMENNR = BON_RABTAB_FIRMENNR AND RAT_RABTABNR = BON_RABTABNR" "~"
if joinKuerzel = RATBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_RABTAB_FIRMENNR = RAT_FIRMENNR AND BON_RABTABNR = RAT_RABTABNR" "~"
if joinKuerzel = GKVREG
searchResult := searchResult "LEFT JOIN REG_REGION.REG_REGION ON REG_LANDNR = GKV_REG_LANDNR AND REG_REGIONNR = GKV_REG_REGIONNR" "~"
if joinKuerzel = REGGKV
searchResult := searchResult "LEFT JOIN HRS.GKV_GEBSTUFEKV ON GKV_REG_LANDNR = REG_LANDNR AND GKV_REG_REGIONNR = REG_REGIONNR" "~"
if joinKuerzel = BKJKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_FIRMENNR = BKJ_KAS_FIRMENNR AND KAS_KASSENNR = BKJ_KAS_KASSENNR" "~"
if joinKuerzel = KASBKJ
searchResult := searchResult "LEFT JOIN WWS.BKJ_BANKOMATJOURNAL ON BKJ_KAS_FIRMENNR = KAS_FIRMENNR AND BKJ_KAS_KASSENNR = KAS_KASSENNR" "~"
if joinKuerzel = PICSFZ
searchResult := searchResult "LEFT JOIN SFZ_SYNCFELDZUORDNUNG.SFZ_SYNCFELDZUORDNUNG ON SFZ_SYNCFELDZUID = PIC_SYNCFELDZUID_PREIS_EK" "~"
if joinKuerzel = SFZPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_SYNCFELDZUID_PREIS_EK = SFZ_SYNCFELDZUID" "~"
if joinKuerzel = PICSFZ
searchResult := searchResult "LEFT JOIN SFZ_SYNCFELDZUORDNUNG.SFZ_SYNCFELDZUORDNUNG ON SFZ_SYNCFELDZUID = PIC_SYNCFELDZUID_RAB_EK" "~"
if joinKuerzel = SFZPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_SYNCFELDZUID_RAB_EK = SFZ_SYNCFELDZUID" "~"
if joinKuerzel = KRDSID
searchResult := searchResult "LEFT JOIN HRS.SID_HRSSEQ ON SID_HRSSEQID = KRD_KVID" "~"
if joinKuerzel = SIDKRD
searchResult := searchResult "LEFT JOIN HRS.KRD_KONTENREFERENZDETAIL ON KRD_KVID = SID_HRSSEQID" "~"
if joinKuerzel = TMLSID
searchResult := searchResult "LEFT JOIN HRS.SID_HRSSEQ ON SID_HRSSEQID = TML_SID_KVID" "~"
if joinKuerzel = SIDTML
searchResult := searchResult "LEFT JOIN HRS.TML_TERMINISIERTELOHNART ON TML_SID_KVID = SID_HRSSEQID" "~"
if joinKuerzel = BPDSLK
searchResult := searchResult "LEFT JOIN WWS.SLK_STUECKLISTENKOPF ON SLK_STKLISTKOPFID = BPD_SLK_STKLISTKOPFID" "~"
if joinKuerzel = SLKBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_SLK_STKLISTKOPFID = SLK_STKLISTKOPFID" "~"
if joinKuerzel = ARLSLK
searchResult := searchResult "LEFT JOIN WWS.SLK_STUECKLISTENKOPF ON SLK_STKLISTKOPFID = ARL_PRD_STKLISTKOPFID" "~"
if joinKuerzel = SLKARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_PRD_STKLISTKOPFID = SLK_STKLISTKOPFID" "~"
if joinKuerzel = SSZSMI
searchResult := searchResult "LEFT JOIN ZLE.SMI_SMARTINTERFACE ON SMI_FIRMENNR = SSZ_SMI_FIRMENNR AND SMI_SMARTINTERFACEID = SSZ_SMI_SMARTINTERFACEID" "~"
if joinKuerzel = SMISSZ
searchResult := searchResult "LEFT JOIN ZLE.SSZ_SORTIERKOPFSMIZU ON SSZ_SMI_FIRMENNR = SMI_FIRMENNR AND SSZ_SMI_SMARTINTERFACEID = SMI_SMARTINTERFACEID" "~"
if joinKuerzel = LAWSNI
searchResult := searchResult "LEFT JOIN HRS.SNI_SCHNITTE ON SNI_DBSNR = LAW_DBSNR AND SNI_SCHNITTNR = LAW_SCHNITTNR_8" "~"
if joinKuerzel = SNILAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_DBSNR = SNI_DBSNR AND LAW_SCHNITTNR_8 = SNI_SCHNITTNR" "~"
if joinKuerzel = DISSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_SERNRID = DIS_SERNRID" "~"
if joinKuerzel = SNRDIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = KAPSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_SERNRID = KAP_SNR_SERNRID" "~"
if joinKuerzel = SNRKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_SNR_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = LKOSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_SERNRID = LKO_SERNRID" "~"
if joinKuerzel = SNRLKO
searchResult := searchResult "LEFT JOIN WWS.LKO_LAGERKONTO ON LKO_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = WFNSPK
searchResult := searchResult "LEFT JOIN SPK_STAPELKOPF.SPK_STAPELKOPF ON SPK_STAPELID = WFN_FUNKTION_STAPELID" "~"
if joinKuerzel = SPKWFN
searchResult := searchResult "LEFT JOIN BUERO.WFN_WORKFLOWACTION ON WFN_FUNKTION_STAPELID = SPK_STAPELID" "~"
if joinKuerzel = SPESPQ
searchResult := searchResult "LEFT JOIN HRS.SPQ_SPESENARTEN_SEQ ON SPQ_SPESENARTID = SPE_VERTEIL_SPESENARTID4" "~"
if joinKuerzel = SPQSPE
searchResult := searchResult "LEFT JOIN HRS.SPE_SPESENARTEN ON SPE_VERTEIL_SPESENARTID4 = SPQ_SPESENARTID" "~"
if joinKuerzel = WADSRM
searchResult := searchResult "LEFT JOIN WWS.SRM_SAMMELRECHNUNGSMODELL ON SRM_SAMMELRECHID = WAD_SAMMELRECHID" "~"
if joinKuerzel = SRMWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_SAMMELRECHID = SRM_SAMMELRECHID" "~"
if joinKuerzel = WKGSRM
searchResult := searchResult "LEFT JOIN WWS.SRM_SAMMELRECHNUNGSMODELL ON SRM_SAMMELRECHID = WKG_SAMMELRECHID" "~"
if joinKuerzel = SRMWKG
searchResult := searchResult "LEFT JOIN WWS.WKG_WWSKUNDENGRUPPE ON WKG_SAMMELRECHID = SRM_SAMMELRECHID" "~"
if joinKuerzel = UVSSTC
searchResult := searchResult "LEFT JOIN FIBU.STC_STEUERCODE ON STC_LANDNR = UVS_LANDNR AND STC_STEUERCODENR = UVS_STEUERCODENR" "~"
if joinKuerzel = STCUVS
searchResult := searchResult "LEFT JOIN FIBU.UVS_MCUSTEUERCODEZU ON UVS_LANDNR = STC_LANDNR AND UVS_STEUERCODENR = STC_STEUERCODENR" "~"
if joinKuerzel = VEUSTC
searchResult := searchResult "LEFT JOIN FIBU.STC_STEUERCODE ON STC_LANDNR = VEU_LANDNR AND STC_STEUERCODENR = VEU_STEUERCODENR" "~"
if joinKuerzel = STCVEU
searchResult := searchResult "LEFT JOIN FIBU.VEU_VORERFASSPROZAUFTEIL ON VEU_LANDNR = STC_LANDNR AND VEU_STEUERCODENR = STC_STEUERCODENR" "~"
if joinKuerzel = SCESTT
searchResult := searchResult "LEFT JOIN PRODUKTION.STT_STAKE_TYP ON STT_STTID = SCE_STT_STTID" "~"
if joinKuerzel = STTSCE
searchResult := searchResult "LEFT JOIN PRODUKTION.SCE_SCORECARD ON SCE_STT_STTID = STT_STTID" "~"
if joinKuerzel = STHSTT
searchResult := searchResult "LEFT JOIN PRODUKTION.STT_STAKE_TYP ON STT_STTID = STH_STTID" "~"
if joinKuerzel = STTSTH
searchResult := searchResult "LEFT JOIN PRODUKTION.STH_STAKE ON STH_STTID = STT_STTID" "~"
if joinKuerzel = WAPSTV
searchResult := searchResult "LEFT JOIN STV_STATVERFAHREN.STV_STATVERFAHREN ON STV_FIRMENNR = WAP_STV_FIRMENNR AND STV_STVCODE = WAP_STVCODE" "~"
if joinKuerzel = STVWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_STV_FIRMENNR = STV_FIRMENNR AND WAP_STVCODE = STV_STVCODE" "~"
if joinKuerzel = TCVTCN
searchResult := searchResult "LEFT JOIN BMD.TCN_TCVERSIONEN ON TCN_VERSIONSNR = TCV_VERSIONSNR" "~"
if joinKuerzel = TCNTCV
searchResult := searchResult "LEFT JOIN BMD.TCV_TCVERSIONCASE ON TCV_VERSIONSNR = TCN_VERSIONSNR" "~"
if joinKuerzel = TPRTEL
searchResult := searchResult "LEFT JOIN BUERO.TEL_TELEFON ON TEL_TELLFDNR = TPR_TELLFDNR" "~"
if joinKuerzel = TELTPR
searchResult := searchResult "LEFT JOIN BUERO.TPR_TELEFONPROTOKOLL ON TPR_TELLFDNR = TEL_TELLFDNR" "~"
if joinKuerzel = TLUTLA
searchResult := searchResult "LEFT JOIN PRODUKTION.TLA_BT_LOGISTIK_ANGEFORDERT ON TLA_TLAID = TLU_TLAID" "~"
if joinKuerzel = TLATLU
searchResult := searchResult "LEFT JOIN PRODUKTION.TLU_BT_LOGISTIK_ZU ON TLU_TLAID = TLA_TLAID" "~"
if joinKuerzel = WARPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = WAR_POSTITNR" "~"
if joinKuerzel = PITWAR
searchResult := searchResult "LEFT JOIN WWS.WAR_WARENEINGANGRABATTE ON WAR_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = WEKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = WEK_POSTITNR" "~"
if joinKuerzel = PITWEK
searchResult := searchResult "LEFT JOIN PRODUKTION.WEK_WERKZEUG ON WEK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = WRKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = WRK_POSTITNR" "~"
if joinKuerzel = PITWRK
searchResult := searchResult "LEFT JOIN WWS.WRK_WWSANKAUFSRNGKOPF ON WRK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PIAPIW
searchResult := searchResult "LEFT JOIN WWS.PIW_PICWARENGRUPPE ON PIW_PICID = PIA_PICID AND PIW_PIWHAUPTGRNR = PIA_PIWHAUPTGRNR AND PIW_PIWUNTERGRNR = PIA_PIWUNTERGRNR" "~"
if joinKuerzel = PIWPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_PICID = PIW_PICID AND PIA_PIWHAUPTGRNR = PIW_PIWHAUPTGRNR AND PIA_PIWUNTERGRNR = PIW_PIWUNTERGRNR" "~"
if joinKuerzel = PROPKA
searchResult := searchResult "LEFT JOIN BUERO.PKA_PROJEKTART ON PKA_FIRMENNR = PRO_PKA_FIRMENNR AND PKA_PROJEKTARTNR = PRO_PROJEKTARTNR" "~"
if joinKuerzel = PKAPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_PKA_FIRMENNR = PKA_FIRMENNR AND PRO_PROJEKTARTNR = PKA_PROJEKTARTNR" "~"
if joinKuerzel = PKDPKD
searchResult := searchResult "LEFT JOIN PRODUKTION.PKD_KM_DETAIL ON PKD_KM_DETAIL_ID = PKD_KM_DETAIL_ID_PRZ" "~"
if joinKuerzel = PKDPKD
searchResult := searchResult "LEFT JOIN PRODUKTION.PKD_KM_DETAIL ON PKD_KM_DETAIL_ID_PRZ = PKD_KM_DETAIL_ID" "~"
if joinKuerzel = PDSPKH
searchResult := searchResult "LEFT JOIN BIL.PKH_PERSKORSTHEADER ON PKH_FIRMENNR = PDS_FIRMENNR AND PKH_PKHMODELL = PDS_MODELL AND PKH_PKHSTICHTAG = PDS_STICHTAG" "~"
if joinKuerzel = PKHPDS
searchResult := searchResult "LEFT JOIN BIL.PDS_PERSKORST_DNSTAMM ON PDS_FIRMENNR = PKH_FIRMENNR AND PDS_MODELL = PKH_PKHMODELL AND PDS_STICHTAG = PKH_PKHSTICHTAG" "~"
if joinKuerzel = BAZPKM
searchResult := searchResult "LEFT JOIN PRODUKTION.PKM_KM_MASTER ON PKM_FIRMENNR = BAZ_PKM_FIRMENNR AND PKM_KM_NR = BAZ_KM_NR" "~"
if joinKuerzel = PKMBAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_PKM_FIRMENNR = PKM_FIRMENNR AND BAZ_KM_NR = PKM_KM_NR" "~"
if joinKuerzel = PKSPKM
searchResult := searchResult "LEFT JOIN PRODUKTION.PKM_KM_MASTER ON PKM_FIRMENNR = PKS_PKM_FIRMENNR AND PKM_KM_NR = PKS_PKM_KM_NR" "~"
if joinKuerzel = PKMPKS
searchResult := searchResult "LEFT JOIN PRODUKTION.PKS_SOLL_KAPAZITAET ON PKS_PKM_FIRMENNR = PKM_FIRMENNR AND PKS_PKM_KM_NR = PKM_KM_NR" "~"
if joinKuerzel = BZBPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_PCHARGENID = BZB_PCHARGENID" "~"
if joinKuerzel = PLCBZB
searchResult := searchResult "LEFT JOIN PRODUKTION.BZB_BAZZEITBUCHUNG ON BZB_PCHARGENID = PLC_PCHARGENID" "~"
if joinKuerzel = PCKPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_PCHARGENID = PCK_PCHARGENID" "~"
if joinKuerzel = PLCPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_PCHARGENID = PLC_PCHARGENID" "~"
if joinKuerzel = BLFPMD
searchResult := searchResult "LEFT JOIN BUERO.PMD_PAUSENMODELL ON PMD_FIRMENNR = BLF_PMD_FIRMENNR AND PMD_PAUSENMODELLNR = BLF_PAUSENMODELLNR" "~"
if joinKuerzel = PMDBLF
searchResult := searchResult "LEFT JOIN BUERO.BLF_BLOCKLOCHFUELLER ON BLF_PMD_FIRMENNR = PMD_FIRMENNR AND BLF_PAUSENMODELLNR = PMD_PAUSENMODELLNR" "~"
if joinKuerzel = BAZPPP
searchResult := searchResult "LEFT JOIN PRODUKTION.PPP_KAPA_POOL ON PPP_PPPID = BAZ_PPPID" "~"
if joinKuerzel = PPPBAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_PPPID = PPP_PPPID" "~"
if joinKuerzel = PPUPPP
searchResult := searchResult "LEFT JOIN PRODUKTION.PPP_KAPA_POOL ON PPP_PPPID = PPU_PPPID" "~"
if joinKuerzel = PPPPPU
searchResult := searchResult "LEFT JOIN PRODUKTION.PPU_KAPA_POOL_ZU ON PPU_PPPID = PPP_PPPID" "~"
if joinKuerzel = BVOPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = BVO_PRO_FIRMENNR AND PRO_PROJEKTNR = BVO_PROJEKTNR AND PRO_PROJEKTTYP = BVO_PROJEKTTYP" "~"
if joinKuerzel = PROBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_PRO_FIRMENNR = PRO_FIRMENNR AND BVO_PROJEKTNR = PRO_PROJEKTNR AND BVO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = CWFPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = CWF_BASIS_PRO_FIRMENNR AND PRO_PROJEKTNR = CWF_BASIS_PROJEKTNR AND PRO_PROJEKTTYP = CWF_BASIS_PROJEKTTYP" "~"
if joinKuerzel = PROCWF
searchResult := searchResult "LEFT JOIN BUERO.CWF_CRMWORKFLOW ON CWF_BASIS_PRO_FIRMENNR = PRO_FIRMENNR AND CWF_BASIS_PROJEKTNR = PRO_PROJEKTNR AND CWF_BASIS_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = ERPPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = ERP_PRO_FIRMENNR AND PRO_PROJEKTNR = ERP_PROJEKTNR AND PRO_PROJEKTTYP = ERP_PROJEKTTYP" "~"
if joinKuerzel = PROERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_PRO_FIRMENNR = PRO_FIRMENNR AND ERP_PROJEKTNR = PRO_PROJEKTNR AND ERP_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = LFSPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = LFS_PRO_FIRMENNR AND PRO_PROJEKTNR = LFS_PROJEKTNR AND PRO_PROJEKTTYP = LFS_PROJEKTTYP" "~"
if joinKuerzel = PROLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_PRO_FIRMENNR = PRO_FIRMENNR AND LFS_PROJEKTNR = PRO_PROJEKTNR AND LFS_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = REAPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = REA_PRO_FIRMENNR AND PRO_PROJEKTNR = REA_PRO_PROJEKTNR AND PRO_PROJEKTTYP = REA_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROREA
searchResult := searchResult "LEFT JOIN HRS.REA_REISEAUFTEILUNG ON REA_PRO_FIRMENNR = PRO_FIRMENNR AND REA_PRO_PROJEKTNR = PRO_PROJEKTNR AND REA_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = VJSPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = VJS_PROJEKT_FIRMENNR AND PRO_PROJEKTNR = VJS_PROJEKTNR AND PRO_PROJEKTTYP = VJS_PROJEKTTYP" "~"
if joinKuerzel = PROVJS
searchResult := searchResult "LEFT JOIN WWS.VJS_VA_VERLEIHJOURNALSUM ON VJS_PROJEKT_FIRMENNR = PRO_FIRMENNR AND VJS_PROJEKTNR = PRO_PROJEKTNR AND VJS_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = VKPPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = VKP_PRO_FIRMENNR AND PRO_PROJEKTNR = VKP_PRO_PROJEKTNR AND PRO_PROJEKTTYP = VKP_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROVKP
searchResult := searchResult "LEFT JOIN FIBU.VKP_VSTKPROJEKT ON VKP_PRO_FIRMENNR = PRO_FIRMENNR AND VKP_PRO_PROJEKTNR = PRO_PROJEKTNR AND VKP_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = EROPTP
searchResult := searchResult "LEFT JOIN WWS.PTP_PREISTABPOS ON PTP_FIRMENNR = ERO_PTP_FIRMENNR AND PTP_PREISTABNR = ERO_PTP_PREISTABNR AND PTP_PREISTABPOSNR = ERO_PTP_PREISTABPOSNR" "~"
if joinKuerzel = PTPERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_PTP_FIRMENNR = PTP_FIRMENNR AND ERO_PTP_PREISTABNR = PTP_PREISTABNR AND ERO_PTP_PREISTABPOSNR = PTP_PREISTABPOSNR" "~"
if joinKuerzel = PWPPTS
searchResult := searchResult "LEFT JOIN WWS.PTS_PREISTABPOSSTAFFEL ON PTS_FIRMENNR = PWP_PTP_UEB_FIRMENNR AND PTS_PREISTABNR = PWP_PTP_UEB_PREISTABNR AND PTS_PREISTABPOSNR = PWP_PTP_UEB_PREISTABPOSNR AND PTS_PREISSTAFFELNR = PWP_PTS_UEB_PREISSTAFFELNR" "~"
if joinKuerzel = PTSPWP
searchResult := searchResult "LEFT JOIN WWS.PWP_PREISWARTUNGPOS ON PWP_PTP_UEB_FIRMENNR = PTS_FIRMENNR AND PWP_PTP_UEB_PREISTABNR = PTS_PREISTABNR AND PWP_PTP_UEB_PREISTABPOSNR = PTS_PREISTABPOSNR AND PWP_PTS_UEB_PREISSTAFFELNR = PTS_PREISSTAFFELNR" "~"
if joinKuerzel = PVFPVA
searchResult := searchResult "LEFT JOIN BUERO.PVA_PERSONVORERFASSAKTION ON PVA_PVAID = PVF_PVAID" "~"
if joinKuerzel = PVAPVF
searchResult := searchResult "LEFT JOIN BUERO.PVF_PERSONENVORERFASSUNG ON PVF_PVAID = PVA_PVAID" "~"
if joinKuerzel = PVGPVF
searchResult := searchResult "LEFT JOIN BUERO.PVF_PERSONENVORERFASSUNG ON PVF_PVFID = PVG_PVFID" "~"
if joinKuerzel = PVFPVG
searchResult := searchResult "LEFT JOIN BUERO.PVG_PERSONVORERFASSANGEH ON PVG_PVFID = PVF_PVFID" "~"
if joinKuerzel = PVWPVF
searchResult := searchResult "LEFT JOIN BUERO.PVF_PERSONENVORERFASSUNG ON PVF_PVFID = PVW_PVFID" "~"
if joinKuerzel = PVFPVW
searchResult := searchResult "LEFT JOIN BUERO.PVW_PERSONVORERFASSWOCHSTD ON PVW_PVFID = PVF_PVFID" "~"
if joinKuerzel = BPKRAT
searchResult := searchResult "LEFT JOIN WWS.RAT_RABATTTAB ON RAT_FIRMENNR = BPK_RABTAB_FIRMENNR AND RAT_RABTABNR = BPK_RABTABNR" "~"
if joinKuerzel = RATBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_RABTAB_FIRMENNR = RAT_FIRMENNR AND BPK_RABTABNR = RAT_RABTABNR" "~"
if joinKuerzel = BSPRAT
searchResult := searchResult "LEFT JOIN WWS.RAT_RABATTTAB ON RAT_FIRMENNR = BSP_RAT_FIRMENNR AND RAT_RABTABNR = BSP_RABTABNR" "~"
if joinKuerzel = RATBSP
searchResult := searchResult "LEFT JOIN WWS.BSP_BONUSERMPOS ON BSP_RAT_FIRMENNR = RAT_FIRMENNR AND BSP_RABTABNR = RAT_RABTABNR" "~"
if joinKuerzel = BARREI
searchResult := searchResult "LEFT JOIN HRS.REI_REISESTAMM ON REI_REISENUMMER = BAR_REISENUMMER" "~"
if joinKuerzel = REIBAR
searchResult := searchResult "LEFT JOIN HRS.BAR_BARAUSLAGEN ON BAR_REISENUMMER = REI_REISENUMMER" "~"
if joinKuerzel = VASREP
searchResult := searchResult "LEFT JOIN BUERO.REP_RESSOURCENPAKET ON REP_RESPAKETID = VAS_RESPAKETID" "~"
if joinKuerzel = REPVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_RESPAKETID = REP_RESPAKETID" "~"
if joinKuerzel = RPTDRE
searchResult := searchResult "LEFT JOIN DREPORTTYP.DREPORTTYP ON REPORTTYPNAME = RPT_REPORTTYPNAME" "~"
if joinKuerzel = DRERPT
searchResult := searchResult "LEFT JOIN BMD.RPT_REPORT ON RPT_REPORTTYPNAME = REPORTTYPNAME" "~"
if joinKuerzel = RKTRKS
searchResult := searchResult "LEFT JOIN BMD.RKS_REPLIKATIONSSTANDORT ON RKS_REPLSTANDONR = RKT_ZUREPLSTANDONR" "~"
if joinKuerzel = RKSRKT
searchResult := searchResult "LEFT JOIN BMD.RKT_REPLIKATIONSTRANSFER ON RKT_ZUREPLSTANDONR = RKS_REPLSTANDONR" "~"
if joinKuerzel = TRSRLK
searchResult := searchResult "LEFT JOIN RLK_RECHNUNGSPLANKOPF.RLK_RECHNUNGSPLANKOPF ON RLK_RLKID = TRS_RECHNUNGSPLANKOPFID" "~"
if joinKuerzel = RLKTRS
searchResult := searchResult "LEFT JOIN BUERO.TRS_TEILSCHLUSSRECHSCHEMA ON TRS_RECHNUNGSPLANKOPFID = RLK_RLKID" "~"
if joinKuerzel = RNPRLP
searchResult := searchResult "LEFT JOIN WWS.RLP_RECHNUNGSPLANPOS ON RLP_RLKID = RNP_RLKID AND RLP_RLPID = RNP_RLPID" "~"
if joinKuerzel = RLPRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_RLKID = RLP_RLKID AND RNP_RLPID = RLP_RLPID" "~"
if joinKuerzel = TMORMO
searchResult := searchResult "LEFT JOIN BUERO.RMO_RUNDUNGSMODELL ON RMO_FIRMENNR = TMO_FIRMENNR AND RMO_RMODELLNR = TMO_RMODELLNR" "~"
if joinKuerzel = RMOTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = RMO_FIRMENNR AND TMO_RMODELLNR = RMO_RMODELLNR" "~"
if joinKuerzel = RNPRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = RNP_FIRMENNR AND RNP_RECHNUNGLFDNR = RNP_RECHNUNGLFDNR AND RNP_RECHNUNGSPOSLFDNR = RNP_H_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = RNPRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = RNP_FIRMENNR AND RNP_RECHNUNGLFDNR = RNP_RECHNUNGLFDNR AND RNP_H_RECHNUNGSPOSLFDNR = RNP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = AADRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = AAD_REPORTNR" "~"
if joinKuerzel = RPTAAD
searchResult := searchResult "LEFT JOIN BIL.AAD_AUSDRUCKDETAIL ON AAD_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = VOBRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = VOB_REPORTNR" "~"
if joinKuerzel = RPTVOB
searchResult := searchResult "LEFT JOIN PRODUKTION.VOB_VORGANGSBENACHR ON VOB_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = RUPRUK
searchResult := searchResult "LEFT JOIN WWS.RUK_RUNDUNGKOPF ON RUK_FIRMENNR = RUP_FIRMENNR AND RUK_RUNDUNGSKOPFNR = RUP_RUNDUNGSKOPFNR" "~"
if joinKuerzel = RUKRUP
searchResult := searchResult "LEFT JOIN WWS.RUP_RUNDUNGPOSITION ON RUP_FIRMENNR = RUK_FIRMENNR AND RUP_RUNDUNGSKOPFNR = RUK_RUNDUNGSKOPFNR" "~"
if joinKuerzel = VSRRVC
searchResult := searchResult "LEFT JOIN FIBU.RVC_RVCODE ON RVC_LANDNR = VSR_RVCODE_LANDNR AND RVC_RVCCODE = VSR_RVCODE1" "~"
if joinKuerzel = RVCVSR
searchResult := searchResult "LEFT JOIN FIBU.VSR_VSTERSTATTUNG ON VSR_RVCODE_LANDNR = RVC_LANDNR AND VSR_RVCODE1 = RVC_RVCCODE" "~"
if joinKuerzel = VSRRVC
searchResult := searchResult "LEFT JOIN FIBU.RVC_RVCODE ON RVC_LANDNR = VSR_RVCODE_LANDNR AND RVC_RVCCODE = VSR_RVCODE3" "~"
if joinKuerzel = RVCVSR
searchResult := searchResult "LEFT JOIN FIBU.VSR_VSTERSTATTUNG ON VSR_RVCODE_LANDNR = RVC_LANDNR AND VSR_RVCODE3 = RVC_RVCCODE" "~"
if joinKuerzel = VSRRVC
searchResult := searchResult "LEFT JOIN FIBU.RVC_RVCODE ON RVC_LANDNR = VSR_RVCODE_LANDNR AND RVC_RVCCODE = VSR_RVCODE5" "~"
if joinKuerzel = RVCVSR
searchResult := searchResult "LEFT JOIN FIBU.VSR_VSTERSTATTUNG ON VSR_RVCODE_LANDNR = RVC_LANDNR AND VSR_RVCODE5 = RVC_RVCCODE" "~"
if joinKuerzel = SKZSTE
searchResult := searchResult "LEFT JOIN BUERO.STE_STEUERERKLAERUNG ON STE_STEUER_ERKLNR = SKZ_STEUER_ERKLNR" "~"
if joinKuerzel = STESKZ
searchResult := searchResult "LEFT JOIN BUERO.SKZ_STEUERKUNDENZU ON SKZ_STEUER_ERKLNR = STE_STEUER_ERKLNR" "~"
if joinKuerzel = SZZSTH
searchResult := searchResult "LEFT JOIN PRODUKTION.STH_STAKE ON STH_STHID = SZZ_STH_LEFT_ID" "~"
if joinKuerzel = STHSZZ
searchResult := searchResult "LEFT JOIN PRODUKTION.SZZ_STAKE_ZU ON SZZ_STH_LEFT_ID = STH_STHID" "~"
if joinKuerzel = GEASTV
searchResult := searchResult "LEFT JOIN BUERO.STV_STATVERFAHREN ON STV_FIRMENNR = GEA_STV_EK_FIRMENNR AND STV_STVCODE = GEA_STV_EK_STVCODE" "~"
if joinKuerzel = STVGEA
searchResult := searchResult "LEFT JOIN BUERO.GEA_GESCHAEFTSART ON GEA_STV_EK_FIRMENNR = STV_FIRMENNR AND GEA_STV_EK_STVCODE = STV_STVCODE" "~"
if joinKuerzel = TELTEA
searchResult := searchResult "LEFT JOIN BUERO.TEA_TELEFONART ON TEA_TELEFONARTNR = TEL_TELEFONARTNR" "~"
if joinKuerzel = TEATEL
searchResult := searchResult "LEFT JOIN BUERO.TEL_TELEFON ON TEL_TELEFONARTNR = TEA_TELEFONARTNR" "~"
if joinKuerzel = TLATLK
searchResult := searchResult "LEFT JOIN PRODUKTION.TLK_BT_LOGISTIK_KOPF ON TLK_TLKID = TLA_TLKID" "~"
if joinKuerzel = TLKTLA
searchResult := searchResult "LEFT JOIN PRODUKTION.TLA_BT_LOGISTIK_ANGEFORDERT ON TLA_TLKID = TLK_TLKID" "~"
if joinKuerzel = TLUTLT
searchResult := searchResult "LEFT JOIN PRODUKTION.TLT_BT_LOGISTIK_TRANSPORT ON TLT_TLTID = TLU_TLTID" "~"
if joinKuerzel = TLTTLU
searchResult := searchResult "LEFT JOIN PRODUKTION.TLU_BT_LOGISTIK_ZU ON TLU_TLTID = TLT_TLTID" "~"
if joinKuerzel = TOOTOU
searchResult := searchResult "LEFT JOIN WWS.TOU_TOUR ON TOU_FIRMENNR = TOO_TOU_FIRMENNR AND TOU_TOURNR = TOO_TOU_TOURNR" "~"
if joinKuerzel = TOUTOO
searchResult := searchResult "LEFT JOIN WWS.TOO_TOUROBJEKT ON TOO_TOU_FIRMENNR = TOU_FIRMENNR AND TOO_TOU_TOURNR = TOU_TOURNR" "~"
if joinKuerzel = DBZBUZ
searchResult := searchResult "LEFT JOIN BUZ_BMDUSERGRUPPENZU.BUZ_BMDUSERGRUPPENZU ON BUZ_BMDUSERID = DBZ_BMDUSERID AND BUZ_BMDGRUPPENID = DBZ_BMDGRUPPENID" "~"
if joinKuerzel = BUZDBZ
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DBZ_DBMDUSERGRUPPENZU ON DBZ_BMDUSERID = BUZ_BMDUSERID AND DBZ_BMDGRUPPENID = BUZ_BMDGRUPPENID" "~"
if joinKuerzel = ULKULK
searchResult := searchResult "LEFT JOIN BMD.ULK_USERLINK ON ULK_LINKGUID = ULK_DEPENDSON_LINKGUID" "~"
if joinKuerzel = ULKULK
searchResult := searchResult "LEFT JOIN BMD.ULK_USERLINK ON ULK_DEPENDSON_LINKGUID = ULK_LINKGUID" "~"
if joinKuerzel = OUFUMF
searchResult := searchResult "LEFT JOIN BUERO.UMF_UMFRAGE ON UMF_UMFRAGEID = OUF_UMFRAGEID" "~"
if joinKuerzel = UMFOUF
searchResult := searchResult "LEFT JOIN BUERO.OUF_ONLINEUMFRAGE ON OUF_UMFRAGEID = UMF_UMFRAGEID" "~"
if joinKuerzel = OUZUMF
searchResult := searchResult "LEFT JOIN BUERO.UMF_UMFRAGE ON UMF_UMFRAGEID = OUZ_UMFRAGEID" "~"
if joinKuerzel = UMFOUZ
searchResult := searchResult "LEFT JOIN BUERO.OUZ_ONLINEKONFUMFRAGEZU ON OUZ_UMFRAGEID = UMF_UMFRAGEID" "~"
if joinKuerzel = UPNUPK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.UPK_UPH_KUNDE ON UPK_SCRNR = UPN_SCRNR" "~"
if joinKuerzel = UPKUPN
searchResult := searchResult "LEFT JOIN BMDDEFAULT.UPN_UPH_KUNDE_NLS ON UPN_SCRNR = UPK_SCRNR" "~"
if joinKuerzel = ZLZUZL
searchResult := searchResult "LEFT JOIN BUERO.UZL_UNTERZIEL ON UZL_UZLID = ZLZ_UZLID" "~"
if joinKuerzel = UZLZLZ
searchResult := searchResult "LEFT JOIN BUERO.ZLZ_ZIELZUORDNUNG ON ZLZ_UZLID = UZL_UZLID" "~"
if joinKuerzel = VETVAK
searchResult := searchResult "LEFT JOIN BUERO.VAK_VERANSTALTUNGSKOPF ON VAK_FIRMENNR = VET_FIRMENNR AND VAK_VSTAMMNR = VET_VSTAMMNR AND VAK_VERANSTALTUNGSNR = VET_VERANSTALTUNGSNR" "~"
if joinKuerzel = VAKVET
searchResult := searchResult "LEFT JOIN BUERO.VET_VERANSTALTUNGSTAG ON VET_FIRMENNR = VAK_FIRMENNR AND VET_VSTAMMNR = VAK_VSTAMMNR AND VET_VERANSTALTUNGSNR = VAK_VERANSTALTUNGSNR" "~"
if joinKuerzel = EVVVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_FIRMENNR = EVV_VAS_FIRMENNR AND VAS_VSTAMMNR = EVV_VAS_VSTAMMNR" "~"
if joinKuerzel = VASEVV
searchResult := searchResult "LEFT JOIN BUERO.EVV_EVENTVORTRAGZU ON EVV_VAS_FIRMENNR = VAS_FIRMENNR AND EVV_VAS_VSTAMMNR = VAS_VSTAMMNR" "~"
if joinKuerzel = SFEVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_FIRMENNR = SFE_VAS_GRP_FIRMENNR AND VAS_VSTAMMNR = SFE_VAS_GRP_VSTAMMNR" "~"
if joinKuerzel = VASSFE
searchResult := searchResult "LEFT JOIN BUERO.SFE_SKILLFINDERELEMENT ON SFE_VAS_GRP_FIRMENNR = VAS_FIRMENNR AND SFE_VAS_GRP_VSTAMMNR = VAS_VSTAMMNR" "~"
if joinKuerzel = BOPVAT
searchResult := searchResult "LEFT JOIN VAT_TEILNAHME.VAT_TEILNAHME ON VAT_FIRMENNR = BOP_VAT_FIRMENNR AND VAT_VSTAMMNR = BOP_VAT_VSTAMMNR AND VAT_VERANSTALTUNGSNR = BOP_VAT_VERANSTALTUNGSNR AND VAT_TEILNAHMENR = BOP_VAT_TEILNAHMENR" "~"
if joinKuerzel = VATBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_VAT_FIRMENNR = VAT_FIRMENNR AND BOP_VAT_VSTAMMNR = VAT_VSTAMMNR AND BOP_VAT_VERANSTALTUNGSNR = VAT_VERANSTALTUNGSNR AND BOP_VAT_TEILNAHMENR = VAT_TEILNAHMENR" "~"
if joinKuerzel = BPDVEC
searchResult := searchResult "LEFT JOIN WWS.VEC_VIEW_BESTPOSDET_ERINFO ON VEC_BPD_FIRMENNR = BPD_FIRMENNR AND VEC_BPD_BESTPRODERFASSNR = BPD_BESTPRODERFASSUNGSNR AND VEC_BPD_ERFASSUNGSKZ = BPD_ERFASSUNGSKZ AND VEC_BPD_BESTPOSLFDNR = BPD_BESTPOSLFDNR AND VEC_BPD_ERFASSPOSDETLFDNR = BPD_ERFASSPOSDETAILLFDNR" "~"
if joinKuerzel = VECBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_FIRMENNR = VEC_BPD_FIRMENNR AND BPD_BESTPRODERFASSUNGSNR = VEC_BPD_BESTPRODERFASSNR AND BPD_ERFASSUNGSKZ = VEC_BPD_ERFASSUNGSKZ AND BPD_BESTPOSLFDNR = VEC_BPD_BESTPOSLFDNR AND BPD_ERFASSPOSDETAILLFDNR = VEC_BPD_ERFASSPOSDETLFDNR" "~"
if joinKuerzel = UPKVEV
searchResult := searchResult "LEFT JOIN VEV_NTCSVERSION.VEV_NTCSVERSION ON VEV_VEV_VERSIONSNR = UPK_VERSIONSNR" "~"
if joinKuerzel = VEVUPK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.UPK_UPH_KUNDE ON UPK_VERSIONSNR = VEV_VEV_VERSIONSNR" "~"
if joinKuerzel = AGBVFA
searchResult := searchResult "LEFT JOIN VFA_VERFAHRENSANWEISUNG_K.VFA_VERFAHRENSANWEISUNG_K ON VFA_VERFAID = AGB_VERFAID" "~"
if joinKuerzel = VFAAGB
searchResult := searchResult "LEFT JOIN WWS.AGB_ARBEITSGANGBAZZU ON AGB_VERFAID = VFA_VERFAID" "~"
if joinKuerzel = RNPVKI
searchResult := searchResult "LEFT JOIN KORE.VKI_VIEW_KOSTAMM_DIM ON VKI_FIRMENNR = RNP_FIRMENNR AND VKI_KOSTENSTAMMNR = RNP_DIM_KOSTENSTAMMNR" "~"
if joinKuerzel = VKIRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = VKI_FIRMENNR AND RNP_DIM_KOSTENSTAMMNR = VKI_KOSTENSTAMMNR" "~"
if joinKuerzel = RPVDRE
searchResult := searchResult "LEFT JOIN DREPORTTYP.DREPORTTYP ON REPORTTYPNAME = RPV_REPORTTYPNAME" "~"
if joinKuerzel = DRERPV
searchResult := searchResult "LEFT JOIN BMD.RPV_REPORTVORSCHLAG ON RPV_REPORTTYPNAME = REPORTTYPNAME" "~"
if joinKuerzel = DRVRPV
searchResult := searchResult "LEFT JOIN RPV_REPORTVORSCHLAG.RPV_REPORTVORSCHLAG ON RPV_REPORTTYPNAME = DRV_REPORTTYPNAME AND RPV_BMDUSERID = DRV_BMDUSERID AND RPV_FIRMENNR = DRV_FIRMENNR" "~"
if joinKuerzel = RPVDRV
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DRV_DREPORTVORSCHLAG ON DRV_REPORTTYPNAME = RPV_REPORTTYPNAME AND DRV_BMDUSERID = RPV_BMDUSERID AND DRV_FIRMENNR = RPV_FIRMENNR" "~"
if joinKuerzel = RIARIA
searchResult := searchResult "LEFT JOIN PRODUKTION.RIA_RISIKOANALYSE ON RIA_RIAID = RIA_RIAID_NEXT" "~"
if joinKuerzel = RIARIA
searchResult := searchResult "LEFT JOIN PRODUKTION.RIA_RISIKOANALYSE ON RIA_RIAID_NEXT = RIA_RIAID" "~"
if joinKuerzel = BONRKS
searchResult := searchResult "LEFT JOIN RKS_REPLIKATIONSSTANDORT.RKS_REPLIKATIONSSTANDORT ON RKS_REPLSTANDONR = BON_ERSTELLREPLSTANDONR" "~"
if joinKuerzel = RKSBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_ERSTELLREPLSTANDONR = RKS_REPLSTANDONR" "~"
if joinKuerzel = CHGRKS
searchResult := searchResult "LEFT JOIN RKS_REPLIKATIONSSTANDORT.RKS_REPLIKATIONSSTANDORT ON RKS_REPLSTANDONR = CHG_REPLSTANDONR" "~"
if joinKuerzel = RKSCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_REPLSTANDONR = RKS_REPLSTANDONR" "~"
if joinKuerzel = KAKRKS
searchResult := searchResult "LEFT JOIN RKS_REPLIKATIONSSTANDORT.RKS_REPLIKATIONSSTANDORT ON RKS_REPLSTANDONR = KAK_ERSTELLREPLSTANDONR" "~"
if joinKuerzel = RKSKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_ERSTELLREPLSTANDONR = RKS_REPLSTANDONR" "~"
if joinKuerzel = LPFDFU
searchResult := searchResult "LEFT JOIN DFU_DFUNKTION.DFU_DFUNKTION ON DFU_FUNKTIONSNR = LPF_FUNKTIONSNR" "~"
if joinKuerzel = DFULPF
searchResult := searchResult "LEFT JOIN BMDENTW.LPF_LIZENZPAKETFUNKZU ON LPF_FUNKTIONSNR = DFU_FUNKTIONSNR" "~"
if joinKuerzel = DVHRMO
searchResult := searchResult "LEFT JOIN BUERO.RMO_RUNDUNGSMODELL ON RMO_FIRMENNR = DVH_RMO_FIRMENNR AND RMO_RMODELLNR = DVH_RMODELLNR" "~"
if joinKuerzel = RMODVH
searchResult := searchResult "LEFT JOIN BUERO.DVH_DIENSTVERHAELTNIS ON DVH_RMO_FIRMENNR = RMO_FIRMENNR AND DVH_RMODELLNR = RMO_RMODELLNR" "~"
if joinKuerzel = BOPRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = BOP_RNP_FIRMENNR AND RNP_RECHNUNGLFDNR = BOP_RNP_RECHNUNGLFDNR AND RNP_RECHNUNGSPOSLFDNR = BOP_RNP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = RNPBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_RNP_FIRMENNR = RNP_FIRMENNR AND BOP_RNP_RECHNUNGLFDNR = RNP_RECHNUNGLFDNR AND BOP_RNP_RECHNUNGSPOSLFDNR = RNP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = KUKRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = KUK_VERRECH_FIRMENNR AND RNP_RECHNUNGLFDNR = KUK_LETZTE_RECHNUNGLFDNR AND RNP_RECHNUNGSPOSLFDNR = KUK_LETZTE_RECHPOSLFDNR" "~"
if joinKuerzel = RNPKUK
searchResult := searchResult "LEFT JOIN WWS.KUK_KUNDENKARTEI ON KUK_VERRECH_FIRMENNR = RNP_FIRMENNR AND KUK_LETZTE_RECHNUNGLFDNR = RNP_RECHNUNGLFDNR AND KUK_LETZTE_RECHPOSLFDNR = RNP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = RNRRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = RNR_FIRMENNR AND RNP_RECHNUNGLFDNR = RNR_RECHNUNGLFDNR AND RNP_RECHNUNGSPOSLFDNR = RNR_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = RNPRNR
searchResult := searchResult "LEFT JOIN WWS.RNR_RECHNUNGSRABATT ON RNR_FIRMENNR = RNP_FIRMENNR AND RNR_RECHNUNGLFDNR = RNP_RECHNUNGLFDNR AND RNR_RECHNUNGSPOSLFDNR = RNP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = VRSRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = VRS_RNP_FIRMENNR AND RNP_RECHNUNGLFDNR = VRS_RNP_RECHNUNGLFDNR AND RNP_RECHNUNGSPOSLFDNR = VRS_RNP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = RNPVRS
searchResult := searchResult "LEFT JOIN WWS.VRS_VERTABRRECHPOSSTORNO ON VRS_RNP_FIRMENNR = RNP_FIRMENNR AND VRS_RNP_RECHNUNGLFDNR = RNP_RECHNUNGLFDNR AND VRS_RNP_RECHNUNGSPOSLFDNR = RNP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = WENRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = WEN_RNP_FIRMENNR AND RNP_RECHNUNGLFDNR = WEN_RECHNUNGLFDNR AND RNP_RECHNUNGSPOSLFDNR = WEN_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = RNPWEN
searchResult := searchResult "LEFT JOIN WWS.WEN_WARENEINSATZRNP ON WEN_RNP_FIRMENNR = RNP_FIRMENNR AND WEN_RECHNUNGLFDNR = RNP_RECHNUNGLFDNR AND WEN_RECHNUNGSPOSLFDNR = RNP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = WEURNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = WEU_RNP_FIRMENNR AND RNP_RECHNUNGLFDNR = WEU_RECHNUNGLFDNR AND RNP_RECHNUNGSPOSLFDNR = WEU_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = RNPWEU
searchResult := searchResult "LEFT JOIN WWS.WEU_WWSERKONTROLLEZU ON WEU_RNP_FIRMENNR = RNP_FIRMENNR AND WEU_RECHNUNGLFDNR = RNP_RECHNUNGLFDNR AND WEU_RECHNUNGSPOSLFDNR = RNP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = WRZRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = WRZ_RNP_FIRMENNR AND RNP_RECHNUNGLFDNR = WRZ_RNP_RECHNUNGLFDNR AND RNP_RECHNUNGSPOSLFDNR = WRZ_RNP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = RNPWRZ
searchResult := searchResult "LEFT JOIN WWS.WRZ_WESRNP_ZUORDNUNG ON WRZ_RNP_FIRMENNR = RNP_FIRMENNR AND WRZ_RNP_RECHNUNGLFDNR = RNP_RECHNUNGLFDNR AND WRZ_RNP_RECHNUNGSPOSLFDNR = RNP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = AUERPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = AUE_STDKOPF_REPORTNR" "~"
if joinKuerzel = RPTAUE
searchResult := searchResult "LEFT JOIN ZLE.AUE_AUSWERTTYPEINST ON AUE_STDKOPF_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = ZSGRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = ZSG_REPORTNR" "~"
if joinKuerzel = RPTZSG
searchResult := searchResult "LEFT JOIN BUERO.ZSG_ZEITSALDENGRENZWERT ON ZSG_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = ZAWRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = ZAW_REPORTNR_KOPF" "~"
if joinKuerzel = RPTZAW
searchResult := searchResult "LEFT JOIN BUERO.ZAW_ZEITAUSWERTUNG ON ZAW_REPORTNR_KOPF = RPT_REPORTNR" "~"
if joinKuerzel = DRVRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = DRV_REPORTNR" "~"
if joinKuerzel = RPTDRV
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DRV_DREPORTVORSCHLAG ON DRV_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = SCLSCK
searchResult := searchResult "LEFT JOIN PRODUKTION.SCK_SCKENNZAHL ON SCK_SCKID = SCL_SCKID" "~"
if joinKuerzel = SCKSCL
searchResult := searchResult "LEFT JOIN PRODUKTION.SCL_SCKENNZAHLDET ON SCL_SCKID = SCK_SCKID" "~"
if joinKuerzel = SCASCK
searchResult := searchResult "LEFT JOIN PRODUKTION.SCK_SCKENNZAHL ON SCK_SCKID = SCA_SCKID_DETECTION" "~"
if joinKuerzel = SCKSCA
searchResult := searchResult "LEFT JOIN PRODUKTION.SCA_SCKRITERIUM ON SCA_SCKID_DETECTION = SCK_SCKID" "~"
if joinKuerzel = RIASCL
searchResult := searchResult "LEFT JOIN PRODUKTION.SCL_SCKENNZAHLDET ON SCL_SCLID = RIA_SCLID" "~"
if joinKuerzel = SCLRIA
searchResult := searchResult "LEFT JOIN PRODUKTION.RIA_RISIKOANALYSE ON RIA_SCLID = SCL_SCLID" "~"
if joinKuerzel = TBTSCM
searchResult := searchResult "LEFT JOIN BMDENTW.SCM_SCRMAIN ON SCM_SCRNR = TBT_SCRNR" "~"
if joinKuerzel = SCMTBT
searchResult := searchResult "LEFT JOIN BMDENTW.TBT_TICKETKATEGORIEZU ON TBT_SCRNR = SCM_SCRNR" "~"
if joinKuerzel = TKZSCM
searchResult := searchResult "LEFT JOIN BMDENTW.SCM_SCRMAIN ON SCM_SCRNR = TKZ_SCRNR" "~"
if joinKuerzel = SCMTKZ
searchResult := searchResult "LEFT JOIN BMDENTW.TKZ_TICKETKONTAKTZU ON TKZ_SCRNR = SCM_SCRNR" "~"
if joinKuerzel = SCISCO
searchResult := searchResult "LEFT JOIN PRODUKTION.SCO_SCINSTANZ ON SCO_SCOID = SCI_SCOID" "~"
if joinKuerzel = SCOSCI
searchResult := searchResult "LEFT JOIN PRODUKTION.SCI_SCINSTANZDET ON SCI_SCOID = SCO_SCOID" "~"
if joinKuerzel = MITSEK
searchResult := searchResult "LEFT JOIN BUERO.SEK_SEKRETARIAT ON SEK_FIRMENNR = MIT_SEK_FIRMENNR AND SEK_SEKRETARIATSNR = MIT_SEKRETARIATSNR" "~"
if joinKuerzel = SEKMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_SEK_FIRMENNR = SEK_FIRMENNR AND MIT_SEKRETARIATSNR = SEK_SEKRETARIATSNR" "~"
if joinKuerzel = AWESFF
searchResult := searchResult "LEFT JOIN SFF_SKILLFINDERFILTER.SFF_SKILLFINDERFILTER ON SFF_SKILLFILTERID = AWE_SKILLFILTERID" "~"
if joinKuerzel = SFFAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_SKILLFILTERID = SFF_SKILLFILTERID" "~"
if joinKuerzel = SFZSFK
searchResult := searchResult "LEFT JOIN BUERO.SFK_SYNCFELDKOPF ON SFK_SFKID = SFZ_SFKID" "~"
if joinKuerzel = SFKSFZ
searchResult := searchResult "LEFT JOIN BUERO.SFZ_SYNCFELDZUORDNUNG ON SFZ_SFKID = SFK_SFKID" "~"
if joinKuerzel = KVNSID
searchResult := searchResult "LEFT JOIN HRS.SID_HRSSEQ ON SID_HRSSEQID = KVN_HRSSEQID" "~"
if joinKuerzel = SIDKVN
searchResult := searchResult "LEFT JOIN HRS.KVN_KVNAME ON KVN_HRSSEQID = SID_HRSSEQID" "~"
if joinKuerzel = LARSID
searchResult := searchResult "LEFT JOIN HRS.SID_HRSSEQ ON SID_HRSSEQID = LAR_KVID" "~"
if joinKuerzel = SIDLAR
searchResult := searchResult "LEFT JOIN HRS.LAR_LOHNARTENREGEL ON LAR_KVID = SID_HRSSEQID" "~"
if joinKuerzel = LGBSID
searchResult := searchResult "LEFT JOIN HRS.SID_HRSSEQ ON SID_HRSSEQID = LGB_KVID" "~"
if joinKuerzel = SIDLGB
searchResult := searchResult "LEFT JOIN HRS.LGB_LGVBERECHNUNGEN ON LGB_KVID = SID_HRSSEQID" "~"
if joinKuerzel = EROSLK
searchResult := searchResult "LEFT JOIN WWS.SLK_STUECKLISTENKOPF ON SLK_STKLISTKOPFID = ERO_SLK_STKLISTKOPFID" "~"
if joinKuerzel = SLKERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_SLK_STKLISTKOPFID = SLK_STKLISTKOPFID" "~"
if joinKuerzel = SMZSMI
searchResult := searchResult "LEFT JOIN ZLE.SMI_SMARTINTERFACE ON SMI_FIRMENNR = SMZ_STAPEL_FIRMENNR AND SMI_SMARTINTERFACEID = SMZ_STAPELINTERFACEID" "~"
if joinKuerzel = SMISMZ
searchResult := searchResult "LEFT JOIN ZLE.SMZ_SMARTINTERFACEZU ON SMZ_STAPEL_FIRMENNR = SMI_FIRMENNR AND SMZ_STAPELINTERFACEID = SMI_SMARTINTERFACEID" "~"
if joinKuerzel = LAWSNI
searchResult := searchResult "LEFT JOIN HRS.SNI_SCHNITTE ON SNI_DBSNR = LAW_DBSNR AND SNI_SCHNITTNR = LAW_SCHNITTNR_7" "~"
if joinKuerzel = SNILAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_DBSNR = SNI_DBSNR AND LAW_SCHNITTNR_7 = SNI_SCHNITTNR" "~"
if joinKuerzel = LPOSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_SERNRID = LPO_SERNRID" "~"
if joinKuerzel = SNRLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = LUASNR
searchResult := searchResult "LEFT JOIN SNR_SERIENNUMMERN.SNR_SERIENNUMMERN ON SNR_SERNRID = LUA_SERNRID" "~"
if joinKuerzel = SNRLUA
searchResult := searchResult "LEFT JOIN PRODUKTION.LUA_LAGER_UMS_ANGEFORDERT ON LUA_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = MEDSNR
searchResult := searchResult "LEFT JOIN SNR_SERIENNUMMERN.SNR_SERIENNUMMERN ON SNR_SERNRID = MED_SERNRID" "~"
if joinKuerzel = SNRMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = SCHSPK
searchResult := searchResult "LEFT JOIN BMD.SPK_STAPELKOPF ON SPK_STAPELID = SCH_STAPELID" "~"
if joinKuerzel = SPKSCH
searchResult := searchResult "LEFT JOIN BMD.SCH_SCHEDULE ON SCH_STAPELID = SPK_STAPELID" "~"
if joinKuerzel = SPTSPK
searchResult := searchResult "LEFT JOIN BMD.SPK_STAPELKOPF ON SPK_STAPELID = SPT_STAPELID" "~"
if joinKuerzel = SPKSPT
searchResult := searchResult "LEFT JOIN BMD.SPT_STAPELSCHRITT ON SPT_STAPELID = SPK_STAPELID" "~"
if joinKuerzel = SPESPQ
searchResult := searchResult "LEFT JOIN HRS.SPQ_SPESENARTEN_SEQ ON SPQ_SPESENARTID = SPE_VERTEIL_SPESENARTID2" "~"
if joinKuerzel = SPQSPE
searchResult := searchResult "LEFT JOIN HRS.SPE_SPESENARTEN ON SPE_VERTEIL_SPESENARTID2 = SPQ_SPESENARTID" "~"
if joinKuerzel = SPESPQ
searchResult := searchResult "LEFT JOIN HRS.SPQ_SPESENARTEN_SEQ ON SPQ_SPESENARTID = SPE_VERTEIL_SPESENARTID8" "~"
if joinKuerzel = SPQSPE
searchResult := searchResult "LEFT JOIN HRS.SPE_SPESENARTEN ON SPE_VERTEIL_SPESENARTID8 = SPQ_SPESENARTID" "~"
if joinKuerzel = WIPSPR
searchResult := searchResult "LEFT JOIN SPR_SPRACHE.SPR_SPRACHE ON SPR_SPRACHNR = WIP_SPRACHNR" "~"
if joinKuerzel = SPRWIP
searchResult := searchResult "LEFT JOIN BIL.WIP_WIPRUEFUNG ON WIP_SPRACHNR = SPR_SPRACHNR" "~"
if joinKuerzel = ADCSRA
searchResult := searchResult "LEFT JOIN WWS.SRA_STEUERART ON SRA_FIRMENNR = ADC_HAUPTFIRMENNR AND SRA_STEUERARTNR = ADC_STEUERARTNR" "~"
if joinKuerzel = SRAADC
searchResult := searchResult "LEFT JOIN WWS.ADC_ABGABENDETAILRECH ON ADC_HAUPTFIRMENNR = SRA_FIRMENNR AND ADC_STEUERARTNR = SRA_STEUERARTNR" "~"
if joinKuerzel = PSMTGR
searchResult := searchResult "LEFT JOIN HRS.TGR_TARIFGRUPPE ON TGR_DBS_DATENBESTAND = PSM_DBS_DATENBESTANDNR AND TGR_LOHNJAHR = PSM_PSMJAHR AND TGR_TARIFGRUPPENNR = PSM_TGR_VG_WECHSEL_NR" "~"
if joinKuerzel = TGRPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_DBS_DATENBESTANDNR = TGR_DBS_DATENBESTAND AND PSM_PSMJAHR = TGR_LOHNJAHR AND PSM_TGR_VG_WECHSEL_NR = TGR_TARIFGRUPPENNR" "~"
if joinKuerzel = TLUTLE
searchResult := searchResult "LEFT JOIN PRODUKTION.TLE_BT_LOGISTIK_EMPFANGEN ON TLE_TLEID = TLU_TLEID" "~"
if joinKuerzel = TLETLU
searchResult := searchResult "LEFT JOIN PRODUKTION.TLU_BT_LOGISTIK_ZU ON TLU_TLEID = TLE_TLEID" "~"
if joinKuerzel = DISTLT
searchResult := searchResult "LEFT JOIN TLT_BT_LOGISTIK_TRANSPORT.TLT_BT_LOGISTIK_TRANSPORT ON TLT_TLTID = DIS_TLTID" "~"
if joinKuerzel = TLTDIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_TLTID = TLT_TLTID" "~"
if joinKuerzel = MDETLT
searchResult := searchResult "LEFT JOIN TLT_BT_LOGISTIK_TRANSPORT.TLT_BT_LOGISTIK_TRANSPORT ON TLT_TLTID = MDE_TLTID" "~"
if joinKuerzel = TLTMDE
searchResult := searchResult "LEFT JOIN WWS.MDE_MOBILEDATENERFASSUNG ON MDE_TLTID = TLT_TLTID" "~"
if joinKuerzel = TMZTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = TMZ_TMO_FIRMENNR AND TMO_TAGESMODELLNR = TMZ_SLAVE_TAGESMDNR" "~"
if joinKuerzel = TMOTMZ
searchResult := searchResult "LEFT JOIN BUERO.TMZ_TAGESMDPRUEFZUORD ON TMZ_TMO_FIRMENNR = TMO_FIRMENNR AND TMZ_SLAVE_TAGESMDNR = TMO_TAGESMODELLNR" "~"
if joinKuerzel = TMOTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = TMO_FIRMENNR AND TMO_TAGESMODELLNR = TMO_BEWERT_TAGESMODELLNR" "~"
if joinKuerzel = TMOTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = TMO_FIRMENNR AND TMO_BEWERT_TAGESMODELLNR = TMO_TAGESMODELLNR" "~"
if joinKuerzel = LPOTOO
searchResult := searchResult "LEFT JOIN WWS.TOO_TOUROBJEKT ON TOO_TOUROBJEKTID = LPO_TOO_TOUROBJEKTID" "~"
if joinKuerzel = TOOLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_TOO_TOUROBJEKTID = TOO_TOUROBJEKTID" "~"
if joinKuerzel = EFKTOU
searchResult := searchResult "LEFT JOIN WWS.TOU_TOUR ON TOU_FIRMENNR = EFK_TOU_FIRMENNR AND TOU_TOURNR = EFK_TOU_TOURNR" "~"
if joinKuerzel = TOUEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_TOU_FIRMENNR = TOU_FIRMENNR AND EFK_TOU_TOURNR = TOU_TOURNR" "~"
if joinKuerzel = KJOTPR
searchResult := searchResult "LEFT JOIN BUERO.TPR_TELEFONPROTOKOLL ON TPR_FIRMENNR = KJO_TPR_FIRMENNR AND TPR_TELPROTLFDNR = KJO_TELPROTLFDNR" "~"
if joinKuerzel = TPRKJO
searchResult := searchResult "LEFT JOIN BUERO.KJO_KONTAKTJOURNAL ON KJO_TPR_FIRMENNR = TPR_FIRMENNR AND KJO_TELPROTLFDNR = TPR_TELPROTLFDNR" "~"
if joinKuerzel = LAJTRJ
searchResult := searchResult "LEFT JOIN WWS.TRJ_TRANSPORTEINH_JOURNAL ON TRJ_FIRMENNR = LAJ_FIRMENNR AND TRJ_TRJLFDNR = LAJ_TRJLFDNR" "~"
if joinKuerzel = TRJLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_FIRMENNR = TRJ_FIRMENNR AND LAJ_TRJLFDNR = TRJ_TRJLFDNR" "~"
if joinKuerzel = EFKTRS
searchResult := searchResult "LEFT JOIN TRS_TEILSCHLUSSRECHSCHEMA.TRS_TEILSCHLUSSRECHSCHEMA ON TRS_TRSID = EFK_TRSID" "~"
if joinKuerzel = TRSEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_TRSID = TRS_TRSID" "~"
if joinKuerzel = KZATRS
searchResult := searchResult "LEFT JOIN TRS_TEILSCHLUSSRECHSCHEMA.TRS_TEILSCHLUSSRECHSCHEMA ON TRS_TRSID = KZA_TRS_TRSID" "~"
if joinKuerzel = TRSKZA
searchResult := searchResult "LEFT JOIN WWS.KZA_KASSENZAHLUNGSARTENZU ON KZA_TRS_TRSID = TRS_TRSID" "~"
if joinKuerzel = RAPTRT
searchResult := searchResult "LEFT JOIN WWS.TRT_TRANSPORTTYP ON TRT_FIRMENNR = RAP_TRT_FIRMENNR AND TRT_TRANSPORTTYPID = RAP_TRT_TRANSPORTTYPID" "~"
if joinKuerzel = TRTRAP
searchResult := searchResult "LEFT JOIN WWS.RAP_RABATTTABPOS ON RAP_TRT_FIRMENNR = TRT_FIRMENNR AND RAP_TRT_TRANSPORTTYPID = TRT_TRANSPORTTYPID" "~"
if joinKuerzel = ERBTSA
searchResult := searchResult "LEFT JOIN FIBU.TSA_TEILSCHLUSSRECHABLAUF ON TSA_FIRMENNR = ERB_FIRMENNR AND TSA_ABLAUFNR = ERB_TS_ABLAUFNR" "~"
if joinKuerzel = TSAERB
searchResult := searchResult "LEFT JOIN FIBU.ERB_BUCHUNG ON ERB_FIRMENNR = TSA_FIRMENNR AND ERB_TS_ABLAUFNR = TSA_ABLAUFNR" "~"
if joinKuerzel = KASTSK
searchResult := searchResult "LEFT JOIN WWS.TSK_TOUCHSCREENKOPF ON TSK_TOUCHSCREENID = KAS_TSK_ID_BOZ_MI" "~"
if joinKuerzel = TSKKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_TSK_ID_BOZ_MI = TSK_TOUCHSCREENID" "~"
if joinKuerzel = KASTSK
searchResult := searchResult "LEFT JOIN WWS.TSK_TOUCHSCREENKOPF ON TSK_TOUCHSCREENID = KAS_TSK_ID_KRAS_MI" "~"
if joinKuerzel = TSKKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_TSK_ID_KRAS_MI = TSK_TOUCHSCREENID" "~"
if joinKuerzel = KASTSK
searchResult := searchResult "LEFT JOIN WWS.TSK_TOUCHSCREENKOPF ON TSK_TOUCHSCREENID = KAS_TSK_ID_KAK_MI" "~"
if joinKuerzel = TSKKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_TSK_ID_KAK_MI = TSK_TOUCHSCREENID" "~"
if joinKuerzel = UTPUTH
searchResult := searchResult "LEFT JOIN BMD.UTH_USERTREEHEAD ON UTH_BAUMID = UTP_BAUMID" "~"
if joinKuerzel = UTHUTP
searchResult := searchResult "LEFT JOIN BMD.UTP_USERTREEPOS ON UTP_BAUMID = UTH_BAUMID" "~"
if joinKuerzel = EAZVAK
searchResult := searchResult "LEFT JOIN BUERO.VAK_VERANSTALTUNGSKOPF ON VAK_FIRMENNR = EAZ_VAK_FIRMENNR AND VAK_VSTAMMNR = EAZ_VAK_VSTAMMNR AND VAK_VERANSTALTUNGSNR = EAZ_VAK_VERANSTALTUNGSNR" "~"
if joinKuerzel = VAKEAZ
searchResult := searchResult "LEFT JOIN BUERO.EAZ_EVENTAUSBZU ON EAZ_VAK_FIRMENNR = VAK_FIRMENNR AND EAZ_VAK_VSTAMMNR = VAK_VSTAMMNR AND EAZ_VAK_VERANSTALTUNGSNR = VAK_VERANSTALTUNGSNR" "~"
if joinKuerzel = MABVAK
searchResult := searchResult "LEFT JOIN BUERO.VAK_VERANSTALTUNGSKOPF ON VAK_FIRMENNR = MAB_VAK_FIRMENNR AND VAK_VSTAMMNR = MAB_VAK_VSTAMMNR AND VAK_VERANSTALTUNGSNR = MAB_VAK_VERANSTALTUNGSNR" "~"
if joinKuerzel = VAKMAB
searchResult := searchResult "LEFT JOIN BUERO.MAB_MITARBAUSBILDUNG ON MAB_VAK_FIRMENNR = VAK_FIRMENNR AND MAB_VAK_VSTAMMNR = VAK_VSTAMMNR AND MAB_VAK_VERANSTALTUNGSNR = VAK_VERANSTALTUNGSNR" "~"
if joinKuerzel = ARLVAO
searchResult := searchResult "LEFT JOIN WWS.VAO_VIEW_STDARTIKELVALUES ON VAO_FIRMENNR = ARL_FIRMENNR AND VAO_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = VAOARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = VAO_FIRMENNR AND ARL_ARTIKELNR = VAO_ARTIKELNR" "~"
if joinKuerzel = AVDRZS
searchResult := searchResult "LEFT JOIN BUERO.RZS_RUECKZAHLUNGSSTAMM ON RZS_FIRMENNR = AVD_RZS_FIRMENNR AND RZS_RZSTAMMNR = AVD_RZS_RZSTAMMNR" "~"
if joinKuerzel = RZSAVD
searchResult := searchResult "LEFT JOIN BUERO.AVD_AUSBVERWDOKUMENTZU ON AVD_RZS_FIRMENNR = RZS_FIRMENNR AND AVD_RZS_RZSTAMMNR = RZS_RZSTAMMNR" "~"
if joinKuerzel = ANBSAK
searchResult := searchResult "LEFT JOIN SAK_SACHKONTO.SAK_SACHKONTO ON SAK_FIRMENNR = ANB_FIBUFIRMENNR AND SAK_FIBUNR = ANB_FIBUNR AND SAK_KONTONR = ANB_KTO_KONTONR" "~"
if joinKuerzel = SAKANB
searchResult := searchResult "LEFT JOIN BIL.ANB_ANBUBUCHBELEGZU ON ANB_FIBUFIRMENNR = SAK_FIRMENNR AND ANB_FIBUNR = SAK_FIBUNR AND ANB_KTO_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = BIPSAK
searchResult := searchResult "LEFT JOIN FIBU.SAK_SACHKONTO ON SAK_FIRMENNR = BIP_FIRMENNR AND SAK_FIBUNR = BIP_FIBUNR AND SAK_KONTONR = BIP_KONTONR" "~"
if joinKuerzel = SAKBIP
searchResult := searchResult "LEFT JOIN FIBU.BIP_BUDGETINVESTPLAN ON BIP_FIRMENNR = SAK_FIRMENNR AND BIP_FIBUNR = SAK_FIBUNR AND BIP_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = PKPSAK
searchResult := searchResult "LEFT JOIN FIBU.SAK_SACHKONTO ON SAK_FIRMENNR = PKP_FIRMENNR AND SAK_FIBUNR = PKP_FIBUNR AND SAK_KONTONR = PKP_PRAEMIE_KONTONR" "~"
if joinKuerzel = SAKPKP
searchResult := searchResult "LEFT JOIN FIBU.PKP_BUDGETPERSKOSTPLAN ON PKP_FIRMENNR = SAK_FIRMENNR AND PKP_FIBUNR = SAK_FIBUNR AND PKP_PRAEMIE_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = PKPSAK
searchResult := searchResult "LEFT JOIN FIBU.SAK_SACHKONTO ON SAK_FIRMENNR = PKP_FIRMENNR AND SAK_FIBUNR = PKP_FIBUNR AND SAK_KONTONR = PKP_DZ_KONTONR" "~"
if joinKuerzel = SAKPKP
searchResult := searchResult "LEFT JOIN FIBU.PKP_BUDGETPERSKOSTPLAN ON PKP_FIRMENNR = SAK_FIRMENNR AND PKP_FIBUNR = SAK_FIBUNR AND PKP_DZ_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = AWSSAP
searchResult := searchResult "LEFT JOIN SAP_SAMMELPROJEKT.SAP_SAMMELPROJEKT ON SAP_FIRMENNR = AWS_SAP_FIRMENNR AND SAP_SAMMELPROJEKTNR = AWS_SAP_SAMMELPROJEKTNR" "~"
if joinKuerzel = SAPAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_SAP_FIRMENNR = SAP_FIRMENNR AND AWS_SAP_SAMMELPROJEKTNR = SAP_SAMMELPROJEKTNR" "~"
if joinKuerzel = HNPSAP
searchResult := searchResult "LEFT JOIN SAP_SAMMELPROJEKT.SAP_SAMMELPROJEKT ON SAP_FIRMENNR = HNP_SAP_FIRMENNR AND SAP_SAMMELPROJEKTNR = HNP_SAP_SAMMELPROJEKTNR" "~"
if joinKuerzel = SAPHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_SAP_FIRMENNR = SAP_FIRMENNR AND HNP_SAP_SAMMELPROJEKTNR = SAP_SAMMELPROJEKTNR" "~"
if joinKuerzel = EFKSBC
searchResult := searchResult "LEFT JOIN SBC_SPEZBEWEGUNGSCODE.SBC_SPEZBEWEGUNGSCODE ON SBC_FIRMENNR = EFK_SBC_FIRMENNR AND SBC_SBCCODE = EFK_SBC_SBCCODE" "~"
if joinKuerzel = SBCEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_SBC_FIRMENNR = SBC_FIRMENNR AND EFK_SBC_SBCCODE = SBC_SBCCODE" "~"
if joinKuerzel = RNGSBC
searchResult := searchResult "LEFT JOIN SBC_SPEZBEWEGUNGSCODE.SBC_SPEZBEWEGUNGSCODE ON SBC_FIRMENNR = RNG_SBC_FIRMENNR AND SBC_SBCCODE = RNG_SBC_SBCCODE" "~"
if joinKuerzel = SBCRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_SBC_FIRMENNR = SBC_FIRMENNR AND RNG_SBC_SBCCODE = SBC_SBCCODE" "~"
if joinKuerzel = KATSBF
searchResult := searchResult "LEFT JOIN SBF_STDBRIEF.SBF_STDBRIEF ON SBF_BRIEFNR = KAT_SBF_BRIEFNR" "~"
if joinKuerzel = SBFKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_SBF_BRIEFNR = SBF_BRIEFNR" "~"
if joinKuerzel = ASRSBS
searchResult := searchResult "LEFT JOIN BUERO.SBS_STELLENBESCHREIBUNG ON SBS_STELLBESCHID = ASR_SBS_STELLBESCHID" "~"
if joinKuerzel = SBSASR
searchResult := searchResult "LEFT JOIN BUERO.ASR_AUSSCHREIBUNG ON ASR_SBS_STELLBESCHID = SBS_STELLBESCHID" "~"
if joinKuerzel = STASBS
searchResult := searchResult "LEFT JOIN BUERO.SBS_STELLENBESCHREIBUNG ON SBS_STELLBESCHID = STA_SBS_STELLBESCHID" "~"
if joinKuerzel = SBSSTA
searchResult := searchResult "LEFT JOIN BUERO.STA_STELLENAUSBILDUNGEN ON STA_SBS_STELLBESCHID = SBS_STELLBESCHID" "~"
if joinKuerzel = OUFSBV
searchResult := searchResult "LEFT JOIN BUERO.SBV_STELLENBESCHVORLAGE ON SBV_VORLAGENID = OUF_KOPFTEXTID" "~"
if joinKuerzel = SBVOUF
searchResult := searchResult "LEFT JOIN BUERO.OUF_ONLINEUMFRAGE ON OUF_KOPFTEXTID = SBV_VORLAGENID" "~"
if joinKuerzel = OUFSBV
searchResult := searchResult "LEFT JOIN BUERO.SBV_STELLENBESCHVORLAGE ON SBV_VORLAGENID = OUF_FUSSTEXTID" "~"
if joinKuerzel = SBVOUF
searchResult := searchResult "LEFT JOIN BUERO.OUF_ONLINEUMFRAGE ON OUF_FUSSTEXTID = SBV_VORLAGENID" "~"
if joinKuerzel = OUPSBV
searchResult := searchResult "LEFT JOIN BUERO.SBV_STELLENBESCHVORLAGE ON SBV_VORLAGENID = OUP_TEXTBAUSTEINID04" "~"
if joinKuerzel = SBVOUP
searchResult := searchResult "LEFT JOIN BUERO.OUP_ONLUMFRAGEPAGE ON OUP_TEXTBAUSTEINID04 = SBV_VORLAGENID" "~"
if joinKuerzel = OUPSBV
searchResult := searchResult "LEFT JOIN BUERO.SBV_STELLENBESCHVORLAGE ON SBV_VORLAGENID = OUP_TEXTBAUSTEINID06" "~"
if joinKuerzel = SBVOUP
searchResult := searchResult "LEFT JOIN BUERO.OUP_ONLUMFRAGEPAGE ON OUP_TEXTBAUSTEINID06 = SBV_VORLAGENID" "~"
if joinKuerzel = SCASCA
searchResult := searchResult "LEFT JOIN PRODUKTION.SCA_SCKRITERIUM ON SCA_SCAID = SCA_HAUPT_ID" "~"
if joinKuerzel = SCASCA
searchResult := searchResult "LEFT JOIN PRODUKTION.SCA_SCKRITERIUM ON SCA_HAUPT_ID = SCA_SCAID" "~"
if joinKuerzel = SCKSCQ
searchResult := searchResult "LEFT JOIN PRODUKTION.SCQ_SCKZGRP ON SCQ_SCQID = SCK_SCQID" "~"
if joinKuerzel = SCQSCK
searchResult := searchResult "LEFT JOIN PRODUKTION.SCK_SCKENNZAHL ON SCK_SCQID = SCQ_SCQID" "~"
if joinKuerzel = SFESFF
searchResult := searchResult "LEFT JOIN BUERO.SFF_SKILLFINDERFILTER ON SFF_SKILLFILTERID = SFE_SKILLFILTERID" "~"
if joinKuerzel = SFFSFE
searchResult := searchResult "LEFT JOIN BUERO.SFE_SKILLFINDERELEMENT ON SFE_SKILLFILTERID = SFF_SKILLFILTERID" "~"
if joinKuerzel = PICSFZ
searchResult := searchResult "LEFT JOIN SFZ_SYNCFELDZUORDNUNG.SFZ_SYNCFELDZUORDNUNG ON SFZ_SYNCFELDZUID = PIC_SFZ_SYNCFELDZUID7" "~"
if joinKuerzel = SFZPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_SFZ_SYNCFELDZUID7 = SFZ_SYNCFELDZUID" "~"
if joinKuerzel = APSSLK
searchResult := searchResult "LEFT JOIN WWS.SLK_STUECKLISTENKOPF ON SLK_STKLISTKOPFID = APS_STKLISTKOPFID" "~"
if joinKuerzel = SLKAPS
searchResult := searchResult "LEFT JOIN WWS.APS_ARBEITSPLAN_STAMM ON APS_STKLISTKOPFID = SLK_STKLISTKOPFID" "~"
if joinKuerzel = BPPVKS
searchResult := searchResult "LEFT JOIN KORE.VKS_VIEW_KOSTAMM_KST ON VKS_FIRMENNR = BPP_FIRMENNR AND VKS_KOSTENSTAMMNR = BPP_KOSTENST_KOSTENSTAMMNR" "~"
if joinKuerzel = VKSBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_FIRMENNR = VKS_FIRMENNR AND BPP_KOSTENST_KOSTENSTAMMNR = VKS_KOSTENSTAMMNR" "~"
if joinKuerzel = LAJVKS
searchResult := searchResult "LEFT JOIN KORE.VKS_VIEW_KOSTAMM_KST ON VKS_FIRMENNR = LAJ_FIRMENNR AND VKS_KOSTENSTAMMNR = LAJ_KOST_KOSTENSTAMMNR" "~"
if joinKuerzel = VKSLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_FIRMENNR = VKS_FIRMENNR AND LAJ_KOST_KOSTENSTAMMNR = VKS_KOSTENSTAMMNR" "~"
if joinKuerzel = AWEVOB
searchResult := searchResult "LEFT JOIN VOB_VORGANGSBENACHR.VOB_VORGANGSBENACHR ON VOB_VOBID = AWE_PEP_BL_VOB_VOBID2" "~"
if joinKuerzel = VOBAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_PEP_BL_VOB_VOBID2 = VOB_VOBID" "~"
if joinKuerzel = VLAVP1
searchResult := searchResult "LEFT JOIN BUERO.VP1_V_PER_PERSON ON VP1_PER_FIRMENNR = VLA_KLI_FIRMENNR AND VP1_PER_PERSONENID = VLA_KLI_KLID" "~"
if joinKuerzel = VP1VLA
searchResult := searchResult "LEFT JOIN WWS.VLA_VIEW_HAUPTLIEFERARTIKEL ON VLA_KLI_FIRMENNR = VP1_PER_FIRMENNR AND VLA_KLI_KLID = VP1_PER_PERSONENID" "~"
if joinKuerzel = DDCVPF
searchResult := searchResult "LEFT JOIN VPF_VPFAD.VPF_VPFAD ON VPF_VPFADNR = DDC_PFADNR" "~"
if joinKuerzel = VPFDDC
searchResult := searchResult "LEFT JOIN BUERO.DDC_DELETEDDOCS ON DDC_PFADNR = VPF_VPFADNR" "~"
if joinKuerzel = EROVPO
searchResult := searchResult "LEFT JOIN WWS.VPO_VIEW_ERFPOSANDORIG ON VPO_FIRMENNR = ERO_FIRMENNR AND VPO_ERFASSUNGSKZ = ERO_ERFASSUNGSKZ AND VPO_ERFASSUNGSNR = ERO_ERFASSUNGSNR AND VPO_ERFASSPOSLFDNR = ERO_ERFASSPOSLFDNR AND VPO_ERFASSPOSORIGLFDNR = ERO_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = VPOERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_FIRMENNR = VPO_FIRMENNR AND ERO_ERFASSUNGSKZ = VPO_ERFASSUNGSKZ AND ERO_ERFASSUNGSNR = VPO_ERFASSUNGSNR AND ERO_ERFASSPOSLFDNR = VPO_ERFASSPOSLFDNR AND ERO_ERFASSPOSORIGLFDNR = VPO_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = VPEVPW
searchResult := searchResult "LEFT JOIN VPW_VPIWERTE.VPW_VPIWERTE ON VPW_VPINDEXID = VPE_VPINDEXID AND VPW_VPINDEXWERTDATUM = VPE_VPINDEXWERTDATUM" "~"
if joinKuerzel = VPWVPE
searchResult := searchResult "LEFT JOIN WWS.VPE_VPI_ERHOEHUNG ON VPE_VPINDEXID = VPW_VPINDEXID AND VPE_VPINDEXWERTDATUM = VPW_VPINDEXWERTDATUM" "~"
if joinKuerzel = WAPVWD
searchResult := searchResult "LEFT JOIN WWS.VWD_VA_WEINGPOS_DOKINFO ON VWD_WAK_WARENEINGKOPFID = WAP_WARENEINGKOPFID AND VWD_WAP_WARENEINGPOSLFDNR = WAP_WARENEINGPOSLFDNR" "~"
if joinKuerzel = VWDWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_WARENEINGKOPFID = VWD_WAK_WARENEINGKOPFID AND WAP_WARENEINGPOSLFDNR = VWD_WAP_WARENEINGPOSLFDNR" "~"
if joinKuerzel = LAJVWF
searchResult := searchResult "LEFT JOIN WWS.VWF_VA_WARENEINSBUCHFIB ON VWF_FIBU_EINZNWID = LAJ_WES_FIBU_EINZNWID" "~"
if joinKuerzel = VWFLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_WES_FIBU_EINZNWID = VWF_FIBU_EINZNWID" "~"
if joinKuerzel = WAKVWT
searchResult := searchResult "LEFT JOIN WWS.VWT_VA_WEINGKOPF_ERINFO ON VWT_WARENEINGKOPFID = WAK_WARENEINGKOPFID" "~"
if joinKuerzel = VWTWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_WARENEINGKOPFID = VWT_WARENEINGKOPFID" "~"
if joinKuerzel = PERWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_ADRESSLFDNR = PER_ADRESSLFDNR" "~"
if joinKuerzel = WADPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_ADRESSLFDNR = WAD_ADRESSLFDNR" "~"
if joinKuerzel = BOZWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = BOZ_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEBOZ
searchResult := searchResult "LEFT JOIN WWS.BOZ_BONZAHLUNG ON BOZ_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = BSUWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = BSU_WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEBSU
searchResult := searchResult "LEFT JOIN FIBU.BSU_BANKEINZUGSSUMME ON BSU_WAE_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = GSFWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = GSF_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEGSF
searchResult := searchResult "LEFT JOIN WWS.GSF_GUTSCHEINFILIALDATEN ON GSF_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = KUSWAE
searchResult := searchResult "LEFT JOIN BMD.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = KUS_BASIS_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEKUS
searchResult := searchResult "LEFT JOIN BMD.KUS_KURS ON KUS_BASIS_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = RATWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = RAT_WAEHRUNGSCODE" "~"
if joinKuerzel = WAERAT
searchResult := searchResult "LEFT JOIN WWS.RAT_RABATTTAB ON RAT_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = SADWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = SAD_WAEHRUNGSCODE" "~"
if joinKuerzel = WAESAD
searchResult := searchResult "LEFT JOIN FIBU.SAD_SALDEN ON SAD_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = SRWWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = SRW_WAEHRUNGSCODE" "~"
if joinKuerzel = WAESRW
searchResult := searchResult "LEFT JOIN WWS.SRW_STEUERARTWERT ON SRW_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = WAKWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = WAK_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = ZWRWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = ZWR_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEZWR
searchResult := searchResult "LEFT JOIN BUERO.ZWR_ZAHLART_WAEHRUNGRUND ON ZWR_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = WEPWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = WEP_WAEHRUNGSCODE_WEW" "~"
if joinKuerzel = WAEWEP
searchResult := searchResult "LEFT JOIN WWS.WEP_WWSERKONTROLLEPOS ON WEP_WAEHRUNGSCODE_WEW = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = VLJPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = VLJ_PROJEKT_FIRMENNR AND PRO_PROJEKTNR = VLJ_PROJEKTNR AND PRO_PROJEKTTYP = VLJ_PROJEKTTYP" "~"
if joinKuerzel = PROVLJ
searchResult := searchResult "LEFT JOIN WWS.VLJ_VERLEIHJOURNAL ON VLJ_PROJEKT_FIRMENNR = PRO_FIRMENNR AND VLJ_PROJEKTNR = PRO_PROJEKTNR AND VLJ_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = IPZPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = IPZ_INTERN_FIRMENNR AND PRO_PROJEKTNR = IPZ_INTERN_PROJEKTNR AND PRO_PROJEKTTYP = IPZ_INTERN_PROJEKTTYP" "~"
if joinKuerzel = PROIPZ
searchResult := searchResult "LEFT JOIN BUERO.IPZ_INTERNPROJEKTZU ON IPZ_INTERN_FIRMENNR = PRO_FIRMENNR AND IPZ_INTERN_PROJEKTNR = PRO_PROJEKTNR AND IPZ_INTERN_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = PALPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = PAL_POL_PRO_FIRMENNR AND PRO_PROJEKTNR = PAL_POL_PROJEKTNR AND PRO_PROJEKTTYP = PAL_POL_PROJEKTTYP" "~"
if joinKuerzel = PROPAL
searchResult := searchResult "LEFT JOIN ZLE.PAL_PAUSCHALE ON PAL_POL_PRO_FIRMENNR = PRO_FIRMENNR AND PAL_POL_PROJEKTNR = PRO_PROJEKTNR AND PAL_POL_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = EPRPRP
searchResult := searchResult "LEFT JOIN WWS.PRP_PROVISIONTABPOS ON PRP_PROVTABPOSID = EPR_PROVTABPOSID" "~"
if joinKuerzel = PRPEPR
searchResult := searchResult "LEFT JOIN WWS.EPR_ERFASSPROVISION ON EPR_PROVTABPOSID = PRP_PROVTABPOSID" "~"
if joinKuerzel = AGMPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_FIRMENNR = AGM_PRS_FIRMENNR AND PRS_PROZESSID = AGM_PROZESSID" "~"
if joinKuerzel = PRSAGM
searchResult := searchResult "LEFT JOIN PRODUKTION.AGM_AG_PERSON_ZU ON AGM_PRS_FIRMENNR = PRS_FIRMENNR AND AGM_PROZESSID = PRS_PROZESSID" "~"
if joinKuerzel = AWSPRS
searchResult := searchResult "LEFT JOIN PRS_PROZESS.PRS_PROZESS ON PRS_FIRMENNR = AWS_PRS_FIRMENNR AND PRS_PROZESSID = AWS_PRS_PROZESSID" "~"
if joinKuerzel = PRSAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_PRS_FIRMENNR = PRS_FIRMENNR AND AWS_PRS_PROZESSID = PRS_PROZESSID" "~"
if joinKuerzel = PKDPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_FIRMENNR = PKD_PRS_FIRMENNR AND PRS_PROZESSID = PKD_PRS_PROZESSID" "~"
if joinKuerzel = PRSPKD
searchResult := searchResult "LEFT JOIN PRODUKTION.PKD_KM_DETAIL ON PKD_PRS_FIRMENNR = PRS_FIRMENNR AND PKD_PRS_PROZESSID = PRS_PROZESSID" "~"
if joinKuerzel = EAFPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = EAF_FIRMENNR AND PSJ_MITARBEITERID = EAF_MITARBEITERID AND PSJ_DIENSTVLFDNR = EAF_DIENSTVLFDNR AND PSJ_LOHNJAHR = EAF_JAHR" "~"
if joinKuerzel = PSJEAF
searchResult := searchResult "LEFT JOIN HRS.EAF_ELDADETAIL_AFB ON EAF_FIRMENNR = PSJ_FIRMENNR AND EAF_MITARBEITERID = PSJ_MITARBEITERID AND EAF_DIENSTVLFDNR = PSJ_DIENSTVLFDNR AND EAF_JAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = EDAPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = EDA_FIRMENNR AND PSJ_MITARBEITERID = EDA_MITARBEITERID AND PSJ_DIENSTVLFDNR = EDA_DIENSTVLFDNR AND PSJ_LOHNJAHR = EDA_JAHR" "~"
if joinKuerzel = PSJEDA
searchResult := searchResult "LEFT JOIN HRS.EDA_ELDADETAIL_ADR ON EDA_FIRMENNR = PSJ_FIRMENNR AND EDA_MITARBEITERID = PSJ_MITARBEITERID AND EDA_DIENSTVLFDNR = PSJ_DIENSTVLFDNR AND EDA_JAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = EFHPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = EFH_FIRMENNR AND PSJ_MITARBEITERID = EFH_MITARBEITERID AND PSJ_DIENSTVLFDNR = EFH_DIENSTVLFDNR AND PSJ_LOHNJAHR = EFH_JAHR" "~"
if joinKuerzel = PSJEFH
searchResult := searchResult "LEFT JOIN HRS.EFH_ELDADETAIL_FH ON EFH_FIRMENNR = PSJ_FIRMENNR AND EFH_MITARBEITERID = PSJ_MITARBEITERID AND EFH_DIENSTVLFDNR = PSJ_DIENSTVLFDNR AND EFH_JAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = EGMPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = EGM_FIRMENNR AND PSJ_MITARBEITERID = EGM_MITARBEITERID AND PSJ_DIENSTVLFDNR = EGM_DIENSTVLFDNR AND PSJ_LOHNJAHR = EGM_JAHR" "~"
if joinKuerzel = PSJEGM
searchResult := searchResult "LEFT JOIN HRS.EGM_ELDADETAIL_GM ON EGM_FIRMENNR = PSJ_FIRMENNR AND EGM_MITARBEITERID = PSJ_MITARBEITERID AND EGM_DIENSTVLFDNR = PSJ_DIENSTVLFDNR AND EGM_JAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = EMIPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = EMI_FIRMENNR AND PSJ_MITARBEITERID = EMI_MITARBEITERID AND PSJ_DIENSTVLFDNR = EMI_DIENSTVLFDNR AND PSJ_LOHNJAHR = EMI_JAHR" "~"
if joinKuerzel = PSJEMI
searchResult := searchResult "LEFT JOIN HRS.EMI_ELDADETAIL_MA ON EMI_FIRMENNR = PSJ_FIRMENNR AND EMI_MITARBEITERID = PSJ_MITARBEITERID AND EMI_DIENSTVLFDNR = PSJ_DIENSTVLFDNR AND EMI_JAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = ESMPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = ESM_FIRMENNR AND PSJ_MITARBEITERID = ESM_MITARBEITERID AND PSJ_DIENSTVLFDNR = ESM_DIENSTVLFDNR AND PSJ_LOHNJAHR = ESM_JAHR" "~"
if joinKuerzel = PSJESM
searchResult := searchResult "LEFT JOIN HRS.ESM_ELDADETAIL_SM ON ESM_FIRMENNR = PSJ_FIRMENNR AND ESM_MITARBEITERID = PSJ_MITARBEITERID AND ESM_DIENSTVLFDNR = PSJ_DIENSTVLFDNR AND ESM_JAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = EVRPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = EVR_FIRMENNR AND PSJ_MITARBEITERID = EVR_MITARBEITERID AND PSJ_DIENSTVLFDNR = EVR_DIENSTVLFDNR AND PSJ_LOHNJAHR = EVR_JAHR" "~"
if joinKuerzel = PSJEVR
searchResult := searchResult "LEFT JOIN HRS.EVR_ELDADETAIL_VMR ON EVR_FIRMENNR = PSJ_FIRMENNR AND EVR_MITARBEITERID = PSJ_MITARBEITERID AND EVR_DIENSTVLFDNR = PSJ_DIENSTVLFDNR AND EVR_JAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = MBMPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_FIRMENNR = MBM_FIRMENNR AND PSM_MITARBEITERID = MBM_MITARBEITERID AND PSM_DIENSTVLFDNR = MBM_DIENSTVLFDNR AND PSM_PSMJAHR = MBM_JAHR AND PSM_PSMMONAT = MBM_MONAT AND PSM_PSMISTORG = MBM_PSMISTORG" "~"
if joinKuerzel = PSMMBM
searchResult := searchResult "LEFT JOIN HRS.MBM_MBGM ON MBM_FIRMENNR = PSM_FIRMENNR AND MBM_MITARBEITERID = PSM_MITARBEITERID AND MBM_DIENSTVLFDNR = PSM_DIENSTVLFDNR AND MBM_JAHR = PSM_PSMJAHR AND MBM_MONAT = PSM_PSMMONAT AND MBM_PSMISTORG = PSM_PSMISTORG" "~"
if joinKuerzel = ALEPSZ
searchResult := searchResult "LEFT JOIN BUERO.PSZ_PERSONENKZ ON PSZ_PERSONENART = ALE_PERSONENART AND PSZ_PERSONKZNR = ALE_PERSONKZNR" "~"
if joinKuerzel = PSZALE
searchResult := searchResult "LEFT JOIN BUERO.ALE_ADRESSENLISTE ON ALE_PERSONENART = PSZ_PERSONENART AND ALE_PERSONKZNR = PSZ_PERSONKZNR" "~"
if joinKuerzel = PKLPSZ
searchResult := searchResult "LEFT JOIN BUERO.PSZ_PERSONENKZ ON PSZ_PERSONENART = PKL_PERSONENART AND PSZ_PERSONKZNR = PKL_PERSONKZNR" "~"
if joinKuerzel = PSZPKL
searchResult := searchResult "LEFT JOIN BUERO.PKL_PROJEKTPERSZU ON PKL_PERSONENART = PSZ_PERSONENART AND PKL_PERSONKZNR = PSZ_PERSONKZNR" "~"
if joinKuerzel = TLETLK
searchResult := searchResult "LEFT JOIN PRODUKTION.TLK_BT_LOGISTIK_KOPF ON TLK_TLKID = TLE_TLKID" "~"
if joinKuerzel = TLKTLE
searchResult := searchResult "LEFT JOIN PRODUKTION.TLE_BT_LOGISTIK_EMPFANGEN ON TLE_TLKID = TLK_TLKID" "~"
if joinKuerzel = TLPTLK
searchResult := searchResult "LEFT JOIN PRODUKTION.TLK_BT_LOGISTIK_KOPF ON TLK_TLKID = TLP_TLKID" "~"
if joinKuerzel = TLKTLP
searchResult := searchResult "LEFT JOIN PRODUKTION.TLP_BT_LOGISTIK_POSITION ON TLP_TLKID = TLK_TLKID" "~"
if joinKuerzel = DISTLP
searchResult := searchResult "LEFT JOIN TLP_BT_LOGISTIK_POSITION.TLP_BT_LOGISTIK_POSITION ON TLP_TLPID = DIS_TLPID" "~"
if joinKuerzel = TLPDIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_TLPID = TLP_TLPID" "~"
if joinKuerzel = AZMTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = AZM_TMO_FIRMENNR AND TMO_TAGESMODELLNR = AZM_TAGESMODELLNR" "~"
if joinKuerzel = TMOAZM
searchResult := searchResult "LEFT JOIN BUERO.AZM_ARBEITSZEITMODELL ON AZM_TMO_FIRMENNR = TMO_FIRMENNR AND AZM_TAGESMODELLNR = TMO_TAGESMODELLNR" "~"
if joinKuerzel = BTGTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = BTG_TMO_FIRMENNR AND TMO_TAGESMODELLNR = BTG_TAGESMODELLNR" "~"
if joinKuerzel = TMOBTG
searchResult := searchResult "LEFT JOIN BUERO.BTG_BEWERTUNGTAG ON BTG_TMO_FIRMENNR = TMO_FIRMENNR AND BTG_TAGESMODELLNR = TMO_TAGESMODELLNR" "~"
if joinKuerzel = TBATMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = TBA_FIRMENNR AND TMO_TAGESMODELLNR = TBA_TAGESMODELLNR" "~"
if joinKuerzel = TMOTBA
searchResult := searchResult "LEFT JOIN BUERO.TBA_TAGESMDBUCHARTAUFB ON TBA_FIRMENNR = TMO_FIRMENNR AND TBA_TAGESMODELLNR = TMO_TAGESMODELLNR" "~"
if joinKuerzel = ERPTOO
searchResult := searchResult "LEFT JOIN WWS.TOO_TOUROBJEKT ON TOO_TOUROBJEKTID = ERP_TOO_TOUROBJEKTID" "~"
if joinKuerzel = TOOERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_TOO_TOUROBJEKTID = TOO_TOUROBJEKTID" "~"
if joinKuerzel = BPKTOU
searchResult := searchResult "LEFT JOIN WWS.TOU_TOUR ON TOU_FIRMENNR = BPK_TOU_FIRMENNR AND TOU_TOURNR = BPK_TOU_TOURNR" "~"
if joinKuerzel = TOUBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_TOU_FIRMENNR = TOU_FIRMENNR AND BPK_TOU_TOURNR = TOU_TOURNR" "~"
if joinKuerzel = TPDTPR
searchResult := searchResult "LEFT JOIN BUERO.TPR_TELEFONPROTOKOLL ON TPR_FIRMENNR = TPD_FIRMENNR AND TPR_TELPROTLFDNR = TPD_TELPROTLFDNR" "~"
if joinKuerzel = TPRTPD
searchResult := searchResult "LEFT JOIN BUERO.TPD_TELPROTDOKUMENTZU ON TPD_FIRMENNR = TPR_FIRMENNR AND TPD_TELPROTLFDNR = TPR_TELPROTLFDNR" "~"
if joinKuerzel = RAZTRS
searchResult := searchResult "LEFT JOIN BUERO.TRS_TEILSCHLUSSRECHSCHEMA ON TRS_TRSID = RAZ_TRSID" "~"
if joinKuerzel = TRSRAZ
searchResult := searchResult "LEFT JOIN BUERO.RAZ_RATENZAHLUNG ON RAZ_TRSID = TRS_TRSID" "~"
if joinKuerzel = RNGTRS
searchResult := searchResult "LEFT JOIN TRS_TEILSCHLUSSRECHSCHEMA.TRS_TEILSCHLUSSRECHSCHEMA ON TRS_TRSID = RNG_TRSID" "~"
if joinKuerzel = TRSRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_TRSID = TRS_TRSID" "~"
if joinKuerzel = TSATRS
searchResult := searchResult "LEFT JOIN TRS_TEILSCHLUSSRECHSCHEMA.TRS_TEILSCHLUSSRECHSCHEMA ON TRS_TRSID = TSA_TRSID" "~"
if joinKuerzel = TRSTSA
searchResult := searchResult "LEFT JOIN FIBU.TSA_TEILSCHLUSSRECHABLAUF ON TSA_TRSID = TRS_TRSID" "~"
if joinKuerzel = TRETRT
searchResult := searchResult "LEFT JOIN WWS.TRT_TRANSPORTTYP ON TRT_FIRMENNR = TRE_TRT_FIRMENNR AND TRT_TRANSPORTTYPID = TRE_TRANSPORTTYPID" "~"
if joinKuerzel = TRTTRE
searchResult := searchResult "LEFT JOIN WWS.TRE_TRANSPORTEINHEIT ON TRE_TRT_FIRMENNR = TRT_FIRMENNR AND TRE_TRANSPORTTYPID = TRT_TRANSPORTTYPID" "~"
if joinKuerzel = TSDTSK
searchResult := searchResult "LEFT JOIN WWS.TSK_TOUCHSCREENKOPF ON TSK_TOUCHSCREENID = TSD_TOUCHSCREENID" "~"
if joinKuerzel = TSKTSD
searchResult := searchResult "LEFT JOIN WWS.TSD_TOUCHSCREENDETAIL ON TSD_TOUCHSCREENID = TSK_TOUCHSCREENID" "~"
if joinKuerzel = KASTSK
searchResult := searchResult "LEFT JOIN WWS.TSK_TOUCHSCREENKOPF ON TSK_TOUCHSCREENID = KAS_TSK_ID_UN" "~"
if joinKuerzel = TSKKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_TSK_ID_UN = TSK_TOUCHSCREENID" "~"
if joinKuerzel = KASTSK
searchResult := searchResult "LEFT JOIN WWS.TSK_TOUCHSCREENKOPF ON TSK_TOUCHSCREENID = KAS_TSK_ID_KAE_RE" "~"
if joinKuerzel = TSKKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_TSK_ID_KAE_RE = TSK_TOUCHSCREENID" "~"
if joinKuerzel = FAZUAT
searchResult := searchResult "LEFT JOIN BUERO.UAT_UMFRAGEANTWTEILNZU ON UAT_SESSIONID = FAZ_UATSESSIONID" "~"
if joinKuerzel = UATFAZ
searchResult := searchResult "LEFT JOIN BUERO.FAZ_FRAGEANTWORTZU ON FAZ_UATSESSIONID = UAT_SESSIONID" "~"
if joinKuerzel = EVTUMF
searchResult := searchResult "LEFT JOIN BUERO.UMF_UMFRAGE ON UMF_UMFRAGEID = EVT_UMFRAGEID" "~"
if joinKuerzel = UMFEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_UMFRAGEID = UMF_UMFRAGEID" "~"
if joinKuerzel = FAZUMF
searchResult := searchResult "LEFT JOIN BUERO.UMF_UMFRAGE ON UMF_UMFRAGEID = FAZ_UMFRAGEID" "~"
if joinKuerzel = UMFFAZ
searchResult := searchResult "LEFT JOIN BUERO.FAZ_FRAGEANTWORTZU ON FAZ_UMFRAGEID = UMF_UMFRAGEID" "~"
if joinKuerzel = UVFUVA
searchResult := searchResult "LEFT JOIN FIBU.UVA_UVAMODELL ON UVA_STEUER_ERKLNR = UVF_STEUER_ERKLNR AND UVA_UVAMODELLLFDNR = UVF_UVAMODELLLFDNR" "~"
if joinKuerzel = UVAUVF
searchResult := searchResult "LEFT JOIN FIBU.UVF_UVAFELDZU ON UVF_STEUER_ERKLNR = UVA_STEUER_ERKLNR AND UVF_UVAMODELLLFDNR = UVA_UVAMODELLLFDNR" "~"
if joinKuerzel = SFEVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_FIRMENNR = SFE_VAS_FIRMENNR AND VAS_VSTAMMNR = SFE_VAS_VSTAMMNR" "~"
if joinKuerzel = VASSFE
searchResult := searchResult "LEFT JOIN BUERO.SFE_SKILLFINDERELEMENT ON SFE_VAS_FIRMENNR = VAS_FIRMENNR AND SFE_VAS_VSTAMMNR = VAS_VSTAMMNR" "~"
if joinKuerzel = VSZVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_FIRMENNR = VSZ_VAS_FIRMENNR AND VAS_VSTAMMNR = VSZ_VAS_VSTAMMNR" "~"
if joinKuerzel = VASVSZ
searchResult := searchResult "LEFT JOIN BUERO.VSZ_AVSTAMMVORAUSSETZZU ON VSZ_VAS_FIRMENNR = VAS_FIRMENNR AND VSZ_VAS_VSTAMMNR = VAS_VSTAMMNR" "~"
if joinKuerzel = ZAWRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = ZAW_REPORTNR_FUSS" "~"
if joinKuerzel = RPTZAW
searchResult := searchResult "LEFT JOIN BUERO.ZAW_ZEITAUSWERTUNG ON ZAW_REPORTNR_FUSS = RPT_REPORTNR" "~"
if joinKuerzel = KASRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = KAS_GUTSCHRIFT_REPORTNR" "~"
if joinKuerzel = RPTKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_GUTSCHRIFT_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = KASRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = KAS_ZWBON_REPORTNR" "~"
if joinKuerzel = RPTKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_ZWBON_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = RSKRSD
searchResult := searchResult "LEFT JOIN WWS.RSD_REGKASPROTOKOLL ON RSD_REGKASPROTOKOLLID = RSK_RSD_LETZTERBON" "~"
if joinKuerzel = RSDRSK
searchResult := searchResult "LEFT JOIN WWS.RSK_REGISTRIERKASSE ON RSK_RSD_LETZTERBON = RSD_REGKASPROTOKOLLID" "~"
if joinKuerzel = VAKRZS
searchResult := searchResult "LEFT JOIN BUERO.RZS_RUECKZAHLUNGSSTAMM ON RZS_FIRMENNR = VAK_RZS_FIRMENNR AND RZS_RZSTAMMNR = VAK_RZSTAMMNR" "~"
if joinKuerzel = RZSVAK
searchResult := searchResult "LEFT JOIN BUERO.VAK_VERANSTALTUNGSKOPF ON VAK_RZS_FIRMENNR = RZS_FIRMENNR AND VAK_RZSTAMMNR = RZS_RZSTAMMNR" "~"
if joinKuerzel = SLPSAG
searchResult := searchResult "LEFT JOIN WWS.SAG_STATISTIKARTIKELGRP ON SAG_FIRMENNR = SLP_SAG_FIRMENNR AND SAG_STATISTIKLFDNR = SLP_SAG_STATISTIKLFDNR AND SAG_STATISTIKGRPLFDNR = SLP_SAG_STATISTIKGRPLFDNR" "~"
if joinKuerzel = SAGSLP
searchResult := searchResult "LEFT JOIN WWS.SLP_STUECKLISTENPOSITION ON SLP_SAG_FIRMENNR = SAG_FIRMENNR AND SLP_SAG_STATISTIKLFDNR = SAG_STATISTIKLFDNR AND SLP_SAG_STATISTIKGRPLFDNR = SAG_STATISTIKGRPLFDNR" "~"
if joinKuerzel = BEXSAK
searchResult := searchResult "LEFT JOIN FIBU.SAK_SACHKONTO ON SAK_FIRMENNR = BEX_FIRMENNR AND SAK_FIBUNR = BEX_FIBUNR AND SAK_KONTONR = BEX_KONTONR" "~"
if joinKuerzel = SAKBEX
searchResult := searchResult "LEFT JOIN FIBU.BEX_BUDGETEXT ON BEX_FIRMENNR = SAK_FIRMENNR AND BEX_FIBUNR = SAK_FIBUNR AND BEX_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = SSUSAK
searchResult := searchResult "LEFT JOIN SAK_SACHKONTO.SAK_SACHKONTO ON SAK_FIRMENNR = SSU_FIRMENNR AND SAK_FIBUNR = SSU_FIBUNR AND SAK_KONTONR = SSU_KTO_KONTONR" "~"
if joinKuerzel = SAKSSU
searchResult := searchResult "LEFT JOIN BIL.SSU_STEUERSACHKONTOZU ON SSU_FIRMENNR = SAK_FIRMENNR AND SSU_FIBUNR = SAK_FIBUNR AND SSU_KTO_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = ABOSAK
searchResult := searchResult "LEFT JOIN SAK_SACHKONTO.SAK_SACHKONTO ON SAK_FIRMENNR = ABO_SAK_FIRMENNR AND SAK_FIBUNR = ABO_SAK_FIBUNR AND SAK_KONTONR = ABO_ERLOESKONTONR" "~"
if joinKuerzel = SAKABO
searchResult := searchResult "LEFT JOIN BIL.ABO_ANBUBUCHUNGKONTEN ON ABO_SAK_FIRMENNR = SAK_FIRMENNR AND ABO_SAK_FIBUNR = SAK_FIBUNR AND ABO_ERLOESKONTONR = SAK_KONTONR" "~"
if joinKuerzel = WAKSBC
searchResult := searchResult "LEFT JOIN SBC_SPEZBEWEGUNGSCODE.SBC_SPEZBEWEGUNGSCODE ON SBC_FIRMENNR = WAK_SBC_FIRMENNR AND SBC_SBCCODE = WAK_SBC_SBCCODE" "~"
if joinKuerzel = SBCWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_SBC_FIRMENNR = SBC_FIRMENNR AND WAK_SBC_SBCCODE = SBC_SBCCODE" "~"
if joinKuerzel = SBPSBK
searchResult := searchResult "LEFT JOIN WWS.SBK_SAISONBESCHAFFKOPF ON SBK_FIRMENNR = SBP_FIRMENNR AND SBK_SBKNR = SBP_SBKNR" "~"
if joinKuerzel = SBKSBP
searchResult := searchResult "LEFT JOIN WWS.SBP_SAISONBESCHAFFPOS ON SBP_FIRMENNR = SBK_FIRMENNR AND SBP_SBKNR = SBK_SBKNR" "~"
if joinKuerzel = TMOSBS
searchResult := searchResult "LEFT JOIN BUERO.SBS_STELLENBESCHREIBUNG ON SBS_STELLBESCHID = TMO_STELLENBESCH_ID" "~"
if joinKuerzel = SBSTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_STELLENBESCH_ID = SBS_STELLBESCHID" "~"
if joinKuerzel = OUPSBV
searchResult := searchResult "LEFT JOIN BUERO.SBV_STELLENBESCHVORLAGE ON SBV_VORLAGENID = OUP_HEADERTEXTID" "~"
if joinKuerzel = SBVOUP
searchResult := searchResult "LEFT JOIN BUERO.OUP_ONLUMFRAGEPAGE ON OUP_HEADERTEXTID = SBV_VORLAGENID" "~"
if joinKuerzel = OUPSBV
searchResult := searchResult "LEFT JOIN BUERO.SBV_STELLENBESCHVORLAGE ON SBV_VORLAGENID = OUP_TEXTBAUSTEINID07" "~"
if joinKuerzel = SBVOUP
searchResult := searchResult "LEFT JOIN BUERO.OUP_ONLUMFRAGEPAGE ON OUP_TEXTBAUSTEINID07 = SBV_VORLAGENID" "~"
if joinKuerzel = SCESCE
searchResult := searchResult "LEFT JOIN PRODUKTION.SCE_SCORECARD ON SCE_SCEID = SCE_SCE_REF_ID" "~"
if joinKuerzel = SCESCE
searchResult := searchResult "LEFT JOIN PRODUKTION.SCE_SCORECARD ON SCE_SCE_REF_ID = SCE_SCEID" "~"
if joinKuerzel = RIASCK
searchResult := searchResult "LEFT JOIN PRODUKTION.SCK_SCKENNZAHL ON SCK_SCKID = RIA_SCKID" "~"
if joinKuerzel = SCKRIA
searchResult := searchResult "LEFT JOIN PRODUKTION.RIA_RISIKOANALYSE ON RIA_SCKID = SCK_SCKID" "~"
if joinKuerzel = KJOSCM
searchResult := searchResult "LEFT JOIN SCM_SCRMAIN.SCM_SCRMAIN ON SCM_SCRNR = KJO_SCRNR" "~"
if joinKuerzel = SCMKJO
searchResult := searchResult "LEFT JOIN BUERO.KJO_KONTAKTJOURNAL ON KJO_SCRNR = SCM_SCRNR" "~"
if joinKuerzel = TAZSCM
searchResult := searchResult "LEFT JOIN BMDENTW.SCM_SCRMAIN ON SCM_SCRNR = TAZ_SCRNR" "~"
if joinKuerzel = SCMTAZ
searchResult := searchResult "LEFT JOIN BMDENTW.TAZ_TICKETAUFGABEZU ON TAZ_SCRNR = SCM_SCRNR" "~"
if joinKuerzel = WSNSEQ
searchResult := searchResult "LEFT JOIN SEQ_SEQUENZ.SEQ_SEQUENZ ON SEQ_SEQUENZID = WSN_SEQUENZID" "~"
if joinKuerzel = SEQWSN
searchResult := searchResult "LEFT JOIN WWS.WSN_WWSSEQUENZNUMMERN ON WSN_SEQUENZID = SEQ_SEQUENZID" "~"
if joinKuerzel = PICSFZ
searchResult := searchResult "LEFT JOIN SFZ_SYNCFELDZUORDNUNG.SFZ_SYNCFELDZUORDNUNG ON SFZ_SYNCFELDZUID = PIC_SYNCFELDZUID_ARTIKEL" "~"
if joinKuerzel = SFZPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_SYNCFELDZUID_ARTIKEL = SFZ_SYNCFELDZUID" "~"
if joinKuerzel = PICSFZ
searchResult := searchResult "LEFT JOIN SFZ_SYNCFELDZUORDNUNG.SFZ_SYNCFELDZUORDNUNG ON SFZ_SYNCFELDZUID = PIC_SYNCFELDZUID_RAB_VK" "~"
if joinKuerzel = SFZPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_SYNCFELDZUID_RAB_VK = SFZ_SYNCFELDZUID" "~"
if joinKuerzel = PICSFZ
searchResult := searchResult "LEFT JOIN SFZ_SYNCFELDZUORDNUNG.SFZ_SYNCFELDZUORDNUNG ON SFZ_SYNCFELDZUID = PIC_SYNCFELDZUID_PREIS_EVK" "~"
if joinKuerzel = SFZPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_SYNCFELDZUID_PREIS_EVK = SFZ_SYNCFELDZUID" "~"
if joinKuerzel = ESZSRA
searchResult := searchResult "LEFT JOIN WWS.SRA_STEUERART ON SRA_FIRMENNR = ESZ_HAUPTFIRMENNR AND SRA_STEUERARTNR = ESZ_STEUERARTNR" "~"
if joinKuerzel = SRAESZ
searchResult := searchResult "LEFT JOIN WWS.ESZ_ERZ_SRA_ZU ON ESZ_HAUPTFIRMENNR = SRA_FIRMENNR AND ESZ_STEUERARTNR = SRA_STEUERARTNR" "~"
if joinKuerzel = RNGSRM
searchResult := searchResult "LEFT JOIN WWS.SRM_SAMMELRECHNUNGSMODELL ON SRM_SAMMELRECHID = RNG_SAMMELRECHID" "~"
if joinKuerzel = SRMRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_SAMMELRECHID = SRM_SAMMELRECHID" "~"
if joinKuerzel = BPPSTC
searchResult := searchResult "LEFT JOIN STC_STEUERCODE.STC_STEUERCODE ON STC_LANDNR = BPP_STEUERCODE_LANDNR AND STC_STEUERCODENR = BPP_STEUERCODENR" "~"
if joinKuerzel = STCBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_STEUERCODE_LANDNR = STC_LANDNR AND BPP_STEUERCODENR = STC_STEUERCODENR" "~"
if joinKuerzel = KAPSTC
searchResult := searchResult "LEFT JOIN STC_STEUERCODE.STC_STEUERCODE ON STC_LANDNR = KAP_STC_LANDNR AND STC_STEUERCODENR = KAP_STC_STEUERCODENR" "~"
if joinKuerzel = STCKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_STC_LANDNR = STC_LANDNR AND KAP_STC_STEUERCODENR = STC_STEUERCODENR" "~"
if joinKuerzel = TRSSTC
searchResult := searchResult "LEFT JOIN STC_STEUERCODE.STC_STEUERCODE ON STC_LANDNR = TRS_STC_LANDNR AND STC_STEUERCODENR = TRS_STC_STEUERCODENR" "~"
if joinKuerzel = STCTRS
searchResult := searchResult "LEFT JOIN BUERO.TRS_TEILSCHLUSSRECHSCHEMA ON TRS_STC_LANDNR = STC_LANDNR AND TRS_STC_STEUERCODENR = STC_STEUERCODENR" "~"
if joinKuerzel = UVWSTC
searchResult := searchResult "LEFT JOIN FIBU.STC_STEUERCODE ON STC_LANDNR = UVW_LAN_LANDNR AND STC_STEUERCODENR = UVW_STC_STEUERCODENR" "~"
if joinKuerzel = STCUVW
searchResult := searchResult "LEFT JOIN FIBU.UVW_UVAWERT ON UVW_LAN_LANDNR = STC_LANDNR AND UVW_STC_STEUERCODENR = STC_STEUERCODENR" "~"
if joinKuerzel = WRPSTC
searchResult := searchResult "LEFT JOIN STC_STEUERCODE.STC_STEUERCODE ON STC_LANDNR = WRP_STEUERCODE_LANDNR AND STC_STEUERCODENR = WRP_STEUERCODENR" "~"
if joinKuerzel = STCWRP
searchResult := searchResult "LEFT JOIN WWS.WRP_WWSANKAUFSRNGPOS ON WRP_STEUERCODE_LANDNR = STC_LANDNR AND WRP_STEUERCODENR = STC_STEUERCODENR" "~"
if joinKuerzel = YBUSTC
searchResult := searchResult "LEFT JOIN FIBU.STC_STEUERCODE ON STC_LANDNR = YBU_LAN_LANDNR AND STC_STEUERCODENR = YBU_STC_STEUERCODENR" "~"
if joinKuerzel = STCYBU
searchResult := searchResult "LEFT JOIN FIBU.YBU_AUTOBUCHUNG ON YBU_LAN_LANDNR = STC_LANDNR AND YBU_STC_STEUERCODENR = STC_STEUERCODENR" "~"
if joinKuerzel = UVSSTE
searchResult := searchResult "LEFT JOIN STE_STEUERERKLAERUNG.STE_STEUERERKLAERUNG ON STE_STEUER_ERKLNR = UVS_JAHRESUST_ERKLNR" "~"
if joinKuerzel = STEUVS
searchResult := searchResult "LEFT JOIN FIBU.UVS_MCUSTEUERCODEZU ON UVS_JAHRESUST_ERKLNR = STE_STEUER_ERKLNR" "~"
if joinKuerzel = STOSTT
searchResult := searchResult "LEFT JOIN PRODUKTION.STT_STAKE_TYP ON STT_STTID = STO_STTID" "~"
if joinKuerzel = STTSTO
searchResult := searchResult "LEFT JOIN PRODUKTION.STO_STAKE_TYPORDER ON STO_STTID = STT_STTID" "~"
if joinKuerzel = BPPSTV
searchResult := searchResult "LEFT JOIN STV_STATVERFAHREN.STV_STATVERFAHREN ON STV_FIRMENNR = BPP_STV_FIRMENNR AND STV_STVCODE = BPP_STVCODE" "~"
if joinKuerzel = STVBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_STV_FIRMENNR = STV_FIRMENNR AND BPP_STVCODE = STV_STVCODE" "~"
if joinKuerzel = TGRTBR
searchResult := searchResult "LEFT JOIN HRS.TBR_BESCHAEFTIGTENGRUPPE ON TBR_DATENBESTAND = TGR_DBS_DATENBESTAND AND TBR_JAHR = TGR_LOHNJAHR AND TBR_BESCHGR_NR = TGR_TBR_NR" "~"
if joinKuerzel = TBRTGR
searchResult := searchResult "LEFT JOIN HRS.TGR_TARIFGRUPPE ON TGR_DBS_DATENBESTAND = TBR_DATENBESTAND AND TGR_LOHNJAHR = TBR_JAHR AND TGR_TBR_NR = TBR_BESCHGR_NR" "~"
if joinKuerzel = TCVTCT
searchResult := searchResult "LEFT JOIN BMD.TCT_TCTESTCASE ON TCT_TCTID = TCV_TCTID" "~"
if joinKuerzel = TCTTCV
searchResult := searchResult "LEFT JOIN BMD.TCV_TCVERSIONCASE ON TCV_TCTID = TCT_TCTID" "~"
if joinKuerzel = ETZTER
searchResult := searchResult "LEFT JOIN BUERO.TER_TERMIN ON TER_TERMINNR = ETZ_TER_TERMINNR" "~"
if joinKuerzel = TERETZ
searchResult := searchResult "LEFT JOIN BUERO.ETZ_EVENTTERMINZU ON ETZ_TER_TERMINNR = TER_TERMINNR" "~"
if joinKuerzel = TGPTGR
searchResult := searchResult "LEFT JOIN HRS.TGR_TARIFGRUPPE ON TGR_DBS_DATENBESTAND = TGP_DATENBESTAND AND TGR_LOHNJAHR = TGP_LOHNJAHR AND TGR_TARIFGRUPPENNR = TGP_TGR_NR" "~"
if joinKuerzel = TGRTGP
searchResult := searchResult "LEFT JOIN HRS.TGP_TARIFGR_TARIFPROZ_ZU ON TGP_DATENBESTAND = TGR_DBS_DATENBESTAND AND TGP_LOHNJAHR = TGR_LOHNJAHR AND TGP_TGR_NR = TGR_TARIFGRUPPENNR" "~"
if joinKuerzel = TLETLP
searchResult := searchResult "LEFT JOIN PRODUKTION.TLP_BT_LOGISTIK_POSITION ON TLP_TLPID = TLE_TLPID" "~"
if joinKuerzel = TLPTLE
searchResult := searchResult "LEFT JOIN PRODUKTION.TLE_BT_LOGISTIK_EMPFANGEN ON TLE_TLPID = TLP_TLPID" "~"
if joinKuerzel = PCKTMO
searchResult := searchResult "LEFT JOIN TMO_TAGESMODELL.TMO_TAGESMODELL ON TMO_FIRMENNR = PCK_TMO_FIRMENNR AND TMO_TAGESMODELLNR = PCK_TMO_TAGESMODELLNR" "~"
if joinKuerzel = TMOPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_TMO_FIRMENNR = TMO_FIRMENNR AND PCK_TMO_TAGESMODELLNR = TMO_TAGESMODELLNR" "~"
if joinKuerzel = ZPLTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = ZPL_TMO_FIRMENNR AND TMO_TAGESMODELLNR = ZPL_TAGESMODELLNR" "~"
if joinKuerzel = TMOZPL
searchResult := searchResult "LEFT JOIN BUERO.ZPL_ZEITPLANUNG ON ZPL_TMO_FIRMENNR = TMO_FIRMENNR AND ZPL_TAGESMODELLNR = TMO_TAGESMODELLNR" "~"
if joinKuerzel = WMDTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = WMD_FIRMENNR AND TMO_TAGESMODELLNR = WMD_TAGESMODELLNR5" "~"
if joinKuerzel = TMOWMD
searchResult := searchResult "LEFT JOIN BUERO.WMD_WOCHENMODELL ON WMD_FIRMENNR = TMO_FIRMENNR AND WMD_TAGESMODELLNR5 = TMO_TAGESMODELLNR" "~"
if joinKuerzel = TREVTI
searchResult := searchResult "LEFT JOIN WWS.VTI_VA_TRANSPORTENHINFO ON VTI_TRANSPORTEINHEITID = TRE_TRANSPORTEINHEITID" "~"
if joinKuerzel = VTITRE
searchResult := searchResult "LEFT JOIN WWS.TRE_TRANSPORTEINHEIT ON TRE_TRANSPORTEINHEITID = VTI_TRANSPORTEINHEITID" "~"
if joinKuerzel = ERZTRS
searchResult := searchResult "LEFT JOIN TRS_TEILSCHLUSSRECHSCHEMA.TRS_TEILSCHLUSSRECHSCHEMA ON TRS_TRSID = ERZ_TRSID" "~"
if joinKuerzel = TRSERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_TRSID = TRS_TRSID" "~"
if joinKuerzel = SLASLK
searchResult := searchResult "LEFT JOIN WWS.SLK_STUECKLISTENKOPF ON SLK_STKLISTKOPFID = SLA_STKLISTKOPFID" "~"
if joinKuerzel = SLKSLA
searchResult := searchResult "LEFT JOIN WWS.SLA_STKLISTVARAUSWAHL ON SLA_STKLISTKOPFID = SLK_STKLISTKOPFID" "~"
if joinKuerzel = SMZSMI
searchResult := searchResult "LEFT JOIN ZLE.SMI_SMARTINTERFACE ON SMI_FIRMENNR = SMZ_SMI_FIRMENNR AND SMI_SMARTINTERFACEID = SMZ_SMARTINTERFACEID" "~"
if joinKuerzel = SMISMZ
searchResult := searchResult "LEFT JOIN ZLE.SMZ_SMARTINTERFACEZU ON SMZ_SMI_FIRMENNR = SMI_FIRMENNR AND SMZ_SMARTINTERFACEID = SMI_SMARTINTERFACEID" "~"
if joinKuerzel = LAWSNI
searchResult := searchResult "LEFT JOIN HRS.SNI_SCHNITTE ON SNI_DBSNR = LAW_DBSNR AND SNI_SCHNITTNR = LAW_SCHNITTNR_4" "~"
if joinKuerzel = SNILAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_DBSNR = SNI_DBSNR AND LAW_SCHNITTNR_4 = SNI_SCHNITTNR" "~"
if joinKuerzel = ACSSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_SERNRID = ACS_SNR_ID" "~"
if joinKuerzel = SNRACS
searchResult := searchResult "LEFT JOIN WWS.ACS_TEST ON ACS_SNR_ID = SNR_SERNRID" "~"
if joinKuerzel = ELVSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_SERNRID = ELV_SERNRID" "~"
if joinKuerzel = SNRELV
searchResult := searchResult "LEFT JOIN WWS.ELV_ERFASSPOSLIEFERVOR ON ELV_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = TLESNR
searchResult := searchResult "LEFT JOIN SNR_SERIENNUMMERN.SNR_SERIENNUMMERN ON SNR_SERNRID = TLE_SERNRID" "~"
if joinKuerzel = SNRTLE
searchResult := searchResult "LEFT JOIN PRODUKTION.TLE_BT_LOGISTIK_EMPFANGEN ON TLE_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = VLJSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_SERNRID = VLJ_SERNRID" "~"
if joinKuerzel = SNRVLJ
searchResult := searchResult "LEFT JOIN WWS.VLJ_VERLEIHJOURNAL ON VLJ_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = WAPSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_SERNRID = WAP_SERNRID" "~"
if joinKuerzel = SNRWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = LAJSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_SERNRID = LAJ_UMB_SERNRID" "~"
if joinKuerzel = SNRLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_UMB_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = PSOSOZ
searchResult := searchResult "LEFT JOIN BUERO.SOZ_SOZIAL ON SOZ_LANDNR = PSO_LANDNR AND SOZ_SOZIALNR = PSO_SOZIALNR" "~"
if joinKuerzel = SOZPSO
searchResult := searchResult "LEFT JOIN BUERO.PSO_PERSONSOZIALZU ON PSO_LANDNR = SOZ_LANDNR AND PSO_SOZIALNR = SOZ_SOZIALNR" "~"
if joinKuerzel = SPXSPK
searchResult := searchResult "LEFT JOIN BMD.SPK_STAPELKOPF ON SPK_STAPELID = SPX_STAPELID" "~"
if joinKuerzel = SPKSPX
searchResult := searchResult "LEFT JOIN BMD.SPX_STAPELVARIABLE ON SPX_STAPELID = SPK_STAPELID" "~"
if joinKuerzel = SPESPQ
searchResult := searchResult "LEFT JOIN HRS.SPQ_SPESENARTEN_SEQ ON SPQ_SPESENARTID = SPE_VERTEIL_SPESENARTID10" "~"
if joinKuerzel = SPQSPE
searchResult := searchResult "LEFT JOIN HRS.SPE_SPESENARTEN ON SPE_VERTEIL_SPESENARTID10 = SPQ_SPESENARTID" "~"
if joinKuerzel = SPESPQ
searchResult := searchResult "LEFT JOIN HRS.SPQ_SPESENARTEN_SEQ ON SPQ_SPESENARTID = SPE_VERTEIL_SPESENARTID3" "~"
if joinKuerzel = SPQSPE
searchResult := searchResult "LEFT JOIN HRS.SPE_SPESENARTEN ON SPE_VERTEIL_SPESENARTID3 = SPQ_SPESENARTID" "~"
if joinKuerzel = SPESPQ
searchResult := searchResult "LEFT JOIN HRS.SPQ_SPESENARTEN_SEQ ON SPQ_SPESENARTID = SPE_VERTEIL_SPESENARTID5" "~"
if joinKuerzel = SPQSPE
searchResult := searchResult "LEFT JOIN HRS.SPE_SPESENARTEN ON SPE_VERTEIL_SPESENARTID5 = SPQ_SPESENARTID" "~"
if joinKuerzel = SRPSRT
searchResult := searchResult "LEFT JOIN WWS.SRT_STEUERTAB ON SRT_FIRMENNR = SRP_FIRMENNR AND SRT_STEUERTABNR = SRP_STEUERTABNR" "~"
if joinKuerzel = SRTSRP
searchResult := searchResult "LEFT JOIN WWS.SRP_STEUERTABPOS ON SRP_FIRMENNR = SRT_FIRMENNR AND SRP_STEUERTABNR = SRT_STEUERTABNR" "~"
if joinKuerzel = WAGSRT
searchResult := searchResult "LEFT JOIN WWS.SRT_STEUERTAB ON SRT_FIRMENNR = WAG_FIRMENNR AND SRT_STEUERTABNR = WAG_STEUERTABNR" "~"
if joinKuerzel = SRTWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = SRT_FIRMENNR AND WAG_STEUERTABNR = SRT_STEUERTABNR" "~"
if joinKuerzel = STHSTB
searchResult := searchResult "LEFT JOIN PRODUKTION.STB_STAKE_HEAD ON STB_STBID = STH_STBID" "~"
if joinKuerzel = STBSTH
searchResult := searchResult "LEFT JOIN PRODUKTION.STH_STAKE ON STH_STBID = STB_STBID" "~"
if joinKuerzel = STOSTB
searchResult := searchResult "LEFT JOIN PRODUKTION.STB_STAKE_HEAD ON STB_STBID = STO_STBID" "~"
if joinKuerzel = STBSTO
searchResult := searchResult "LEFT JOIN PRODUKTION.STO_STAKE_TYPORDER ON STO_STBID = STB_STBID" "~"
if joinKuerzel = BASSTC
searchResult := searchResult "LEFT JOIN FIBU.STC_STEUERCODE ON STC_LANDNR = BAS_LANDNR AND STC_STEUERCODENR = BAS_STEUERCODENR" "~"
if joinKuerzel = STCBAS
searchResult := searchResult "LEFT JOIN FIBU.BAS_BUCAUFTEILUNGSSATZ ON BAS_LANDNR = STC_LANDNR AND BAS_STEUERCODENR = STC_STEUERCODENR" "~"
if joinKuerzel = BRSSTC
searchResult := searchResult "LEFT JOIN FIBU.STC_STEUERCODE ON STC_LANDNR = BRS_LANDNR AND STC_STEUERCODENR = BRS_STEUERCODENR" "~"
if joinKuerzel = STCBRS
searchResult := searchResult "LEFT JOIN FIBU.BRS_BANKRUECKAUFTEILSATZ ON BRS_LANDNR = STC_LANDNR AND BRS_STEUERCODENR = STC_STEUERCODENR" "~"
if joinKuerzel = SESSTE
searchResult := searchResult "LEFT JOIN STE_STEUERERKLAERUNG.STE_STEUERERKLAERUNG ON STE_STEUER_ERKLNR = SES_ERKLNR" "~"
if joinKuerzel = STESES
searchResult := searchResult "LEFT JOIN BIL.SES_STEUERERKLSETTINGS ON SES_ERKLNR = STE_STEUER_ERKLNR" "~"
if joinKuerzel = STCSTG
searchResult := searchResult "LEFT JOIN FIBU.STG_STEUERGRUPPE ON STG_LANDNR = STC_LANDNR AND STG_STEUERGRUPPENNR = STC_STG_STEUERGRUPPENNR" "~"
if joinKuerzel = STGSTC
searchResult := searchResult "LEFT JOIN FIBU.STC_STEUERCODE ON STC_LANDNR = STG_LANDNR AND STC_STG_STEUERGRUPPENNR = STG_STEUERGRUPPENNR" "~"
if joinKuerzel = SZZSTH
searchResult := searchResult "LEFT JOIN PRODUKTION.STH_STAKE ON STH_STHID = SZZ_STH_RIGHT_ID" "~"
if joinKuerzel = STHSZZ
searchResult := searchResult "LEFT JOIN PRODUKTION.SZZ_STAKE_ZU ON SZZ_STH_RIGHT_ID = STH_STHID" "~"
if joinKuerzel = ASMVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_FIRMENNR = ASM_VAS_FIRMENNR AND VAS_VSTAMMNR = ASM_VAS_VSTAMMNR" "~"
if joinKuerzel = VASASM
searchResult := searchResult "LEFT JOIN BUERO.ASM_AUSBILDUNGSSTAMMMERKM ON ASM_VAS_FIRMENNR = VAS_FIRMENNR AND ASM_VAS_VSTAMMNR = VAS_VSTAMMNR" "~"
if joinKuerzel = AVDVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_FIRMENNR = AVD_VAS_FIRMENNR AND VAS_VSTAMMNR = AVD_VAS_VSTAMMNR" "~"
if joinKuerzel = VASAVD
searchResult := searchResult "LEFT JOIN BUERO.AVD_AUSBVERWDOKUMENTZU ON AVD_VAS_FIRMENNR = VAS_FIRMENNR AND AVD_VAS_VSTAMMNR = VAS_VSTAMMNR" "~"
if joinKuerzel = AWSVAS
searchResult := searchResult "LEFT JOIN VAS_VERANSTALTUNGSTAMM.VAS_VERANSTALTUNGSTAMM ON VAS_FIRMENNR = AWS_VAS_FIRMENNR AND VAS_VSTAMMNR = AWS_VAS_VSTAMMNR" "~"
if joinKuerzel = VASAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_VAS_FIRMENNR = VAS_FIRMENNR AND AWS_VAS_VSTAMMNR = VAS_VSTAMMNR" "~"
if joinKuerzel = MABVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_FIRMENNR = MAB_VAS_FIRMENNR AND VAS_VSTAMMNR = MAB_VAS_VSTAMMNR" "~"
if joinKuerzel = VASMAB
searchResult := searchResult "LEFT JOIN BUERO.MAB_MITARBAUSBILDUNG ON MAB_VAS_FIRMENNR = VAS_FIRMENNR AND MAB_VAS_VSTAMMNR = VAS_VSTAMMNR" "~"
if joinKuerzel = VAHVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_FIRMENNR = VAH_VAS_FIRMENNR AND VAS_VSTAMMNR = VAH_VAS_VSTAMMNR" "~"
if joinKuerzel = VASVAH
searchResult := searchResult "LEFT JOIN BUERO.VAH_VERANSTABHAENGIG ON VAH_VAS_FIRMENNR = VAS_FIRMENNR AND VAH_VAS_VSTAMMNR = VAS_VSTAMMNR" "~"
if joinKuerzel = VDZVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_FIRMENNR = VDZ_VAS_FIRMENNR AND VAS_VSTAMMNR = VDZ_VAS_VSTAMMNR" "~"
if joinKuerzel = VASVDZ
searchResult := searchResult "LEFT JOIN BUERO.VDZ_VSTAMMDOKZU ON VDZ_VAS_FIRMENNR = VAS_FIRMENNR AND VDZ_VAS_VSTAMMNR = VAS_VSTAMMNR" "~"
if joinKuerzel = MABVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_FIRMENNR = MAB_VAS_ABHAENGIG_FIRMENNR AND VAS_VSTAMMNR = MAB_VAS_ABHAENGIG_VSTAMMNR" "~"
if joinKuerzel = VASMAB
searchResult := searchResult "LEFT JOIN BUERO.MAB_MITARBAUSBILDUNG ON MAB_VAS_ABHAENGIG_FIRMENNR = VAS_FIRMENNR AND MAB_VAS_ABHAENGIG_VSTAMMNR = VAS_VSTAMMNR" "~"
if joinKuerzel = EFKVEE
searchResult := searchResult "LEFT JOIN WWS.VEE_VA_ERFKOPFPOSWERTE ON VEE_FIRMENNR = EFK_FIRMENNR AND VEE_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND VEE_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = VEEEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = VEE_FIRMENNR AND EFK_ERFASSUNGSKZ = VEE_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = VEE_ERFASSUNGSNR" "~"
if joinKuerzel = WADVER
searchResult := searchResult "LEFT JOIN VER_VERSANDART.VER_VERSANDART ON VER_VERSANDARTNR = WAD_VERSANDARTNR" "~"
if joinKuerzel = VERWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_VERSANDARTNR = VER_VERSANDARTNR" "~"
if joinKuerzel = EFKVKS
searchResult := searchResult "LEFT JOIN KORE.VKS_VIEW_KOSTAMM_KST ON VKS_FIRMENNR = EFK_FIRMENNR AND VKS_KOSTENSTAMMNR = EFK_KOSTENST_KOSTENSTAMMNR" "~"
if joinKuerzel = VKSEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = VKS_FIRMENNR AND EFK_KOSTENST_KOSTENSTAMMNR = VKS_KOSTENSTAMMNR" "~"
if joinKuerzel = LAJVKT
searchResult := searchResult "LEFT JOIN KORE.VKT_VIEW_KOSTAMM_KTR ON VKT_FIRMENNR = LAJ_FIRMENNR AND VKT_KOSTENSTAMMNR = LAJ_KOTR_KOSTENSTAMMNR" "~"
if joinKuerzel = VKTLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_FIRMENNR = VKT_FIRMENNR AND LAJ_KOTR_KOSTENSTAMMNR = VKT_KOSTENSTAMMNR" "~"
if joinKuerzel = VJDVLJ
searchResult := searchResult "LEFT JOIN WWS.VLJ_VERLEIHJOURNAL ON VLJ_JOURNALID = VJD_JOURNALID" "~"
if joinKuerzel = VLJVJD
searchResult := searchResult "LEFT JOIN WWS.VJD_VERLEIHJOURNALDETAIL ON VJD_JOURNALID = VLJ_JOURNALID" "~"
if joinKuerzel = LAJVLN
searchResult := searchResult "LEFT JOIN WWS.VLN_VA_LAGERJOURNALVOR ON VLN_FIRMENNR = LAJ_FIRMENNR AND VLN_LAGERJOURNALLFDNR = LAJ_LAGERJOURNALLFDNR" "~"
if joinKuerzel = VLNLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_FIRMENNR = VLN_FIRMENNR AND LAJ_LAGERJOURNALLFDNR = VLN_LAGERJOURNALLFDNR" "~"
if joinKuerzel = VOPVOB
searchResult := searchResult "LEFT JOIN PRODUKTION.VOB_VORGANGSBENACHR ON VOB_VOBID = VOP_VOBID" "~"
if joinKuerzel = VOBVOP
searchResult := searchResult "LEFT JOIN PRODUKTION.VOP_VOB_PERSON_ZU ON VOP_VOBID = VOB_VOBID" "~"
if joinKuerzel = BPVVPI
searchResult := searchResult "LEFT JOIN VPI_VPINDEX.VPI_VPINDEX ON VPI_VPINDEXID = BPV_VPINDEXID" "~"
if joinKuerzel = VPIBPV
searchResult := searchResult "LEFT JOIN BIL.BPV_ANBU_BERECHPARAMS ON BPV_VPINDEXID = VPI_VPINDEXID" "~"
if joinKuerzel = PWPVRL
searchResult := searchResult "LEFT JOIN WWS.VRL_VA_PWLIEFARTIKEL ON VRL_PWKID = PWP_PWKID AND VRL_PWPLFDNR = PWP_PWPLFDNR" "~"
if joinKuerzel = VRLPWP
searchResult := searchResult "LEFT JOIN WWS.PWP_PREISWARTUNGPOS ON PWP_PWKID = VRL_PWKID AND PWP_PWPLFDNR = VRL_PWPLFDNR" "~"
if joinKuerzel = EROVSP
searchResult := searchResult "LEFT JOIN WWS.VSP_VA_EPSSUMWERTE ON VSP_FIRMENNR = ERO_FIRMENNR AND VSP_ERO_ERFASSUNGSKZ = ERO_ERFASSUNGSKZ AND VSP_ERO_ERFASSUNGSNR = ERO_ERFASSUNGSNR AND VSP_ERO_ERFASSPOSLFDNR = ERO_ERFASSPOSLFDNR AND VSP_ERO_ERFASSPOSORIGLFDNR = ERO_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = VSPERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_FIRMENNR = VSP_FIRMENNR AND ERO_ERFASSUNGSKZ = VSP_ERO_ERFASSUNGSKZ AND ERO_ERFASSUNGSNR = VSP_ERO_ERFASSUNGSNR AND ERO_ERFASSPOSLFDNR = VSP_ERO_ERFASSPOSLFDNR AND ERO_ERFASSPOSORIGLFDNR = VSP_ERO_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = MITVTS
searchResult := searchResult "LEFT JOIN VTS_VERTRETERSTUFE.VTS_VERTRETERSTUFE ON VTS_FIRMENNR = MIT_VTS_FIRMENNR AND VTS_STUFE = MIT_STUFE" "~"
if joinKuerzel = VTSMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_VTS_FIRMENNR = VTS_FIRMENNR AND MIT_STUFE = VTS_STUFE" "~"
if joinKuerzel = BONWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = BON_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = HNPWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = HNP_WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_WAE_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = WKRWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = WKR_WAEHRUNGSCODE_KOPIE" "~"
if joinKuerzel = WAEWKR
searchResult := searchResult "LEFT JOIN WWS.WKR_WARENAUSGKOSTENRECH ON WKR_WAEHRUNGSCODE_KOPIE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = AFTWAG
searchResult := searchResult "LEFT JOIN WAG_WARENGRUPPE.WAG_WARENGRUPPE ON WAG_FIRMENNR = AFT_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = AFT_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = AFT_WARENUNTERGRNR" "~"
if joinKuerzel = WAGAFT
searchResult := searchResult "LEFT JOIN ZLE.AFT_AUFBEREITUNG ON AFT_WAG_FIRMENNR = WAG_FIRMENNR AND AFT_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND AFT_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = KOSWAG
searchResult := searchResult "LEFT JOIN WAG_WARENGRUPPE.WAG_WARENGRUPPE ON WAG_FIRMENNR = KOS_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = KOS_WAG_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = KOS_WAG_WARENUNTERGRNR" "~"
if joinKuerzel = WAGKOS
searchResult := searchResult "LEFT JOIN ZLE.KOS_KOSTENFAKTOR ON KOS_WAG_FIRMENNR = WAG_FIRMENNR AND KOS_WAG_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND KOS_WAG_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = TPZWAG
searchResult := searchResult "LEFT JOIN WAG_WARENGRUPPE.WAG_WARENGRUPPE ON WAG_FIRMENNR = TPZ_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = TPZ_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = TPZ_WARENUNTERGRNR" "~"
if joinKuerzel = WAGTPZ
searchResult := searchResult "LEFT JOIN ZLE.TPZ_TAETIGKEITPAUSCHALEZU ON TPZ_WAG_FIRMENNR = WAG_FIRMENNR AND TPZ_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND TPZ_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = WOLWAG
searchResult := searchResult "LEFT JOIN WAG_WARENGRUPPE.WAG_WARENGRUPPE ON WAG_FIRMENNR = WOL_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = WOL_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = WOL_WARENUNTERGRNR" "~"
if joinKuerzel = WAGWOL
searchResult := searchResult "LEFT JOIN ZLE.WOL_WOCHENLEISTUNG ON WOL_WAG_FIRMENNR = WAG_FIRMENNR AND WOL_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND WOL_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = EDZWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_WARENEINGKOPFID = EDZ_WAK_WARENEINGKOPFID AND WAP_WARENEINGPOSLFDNR = EDZ_WAP_WARENEINGPOSLFDNR" "~"
if joinKuerzel = WAPEDZ
searchResult := searchResult "LEFT JOIN WWS.EDZ_ERFASSDOKUMENTZU ON EDZ_WAK_WARENEINGKOPFID = WAP_WARENEINGKOPFID AND EDZ_WAP_WARENEINGPOSLFDNR = WAP_WARENEINGPOSLFDNR" "~"
if joinKuerzel = WABWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_WARENEINGKOPFID = WAB_WARENEINGKOPFID AND WAP_WARENEINGPOSLFDNR = WAB_WARENEINGPOSLFDNR" "~"
if joinKuerzel = WAPWAB
searchResult := searchResult "LEFT JOIN WWS.WAB_WARENEINGANGBUCH ON WAB_WARENEINGKOPFID = WAP_WARENEINGKOPFID AND WAB_WARENEINGPOSLFDNR = WAP_WARENEINGPOSLFDNR" "~"
if joinKuerzel = WLZWEB
searchResult := searchResult "LEFT JOIN WWS.WEB_WARENEINSATZBUCH ON WEB_WEINSATZKOPFID = WLZ_WEINSATZKOPFID AND WEB_WEINSATZBUCHLFDNR = WLZ_WEINSATZBUCHLFDNR" "~"
if joinKuerzel = WEBWLZ
searchResult := searchResult "LEFT JOIN WWS.WLZ_WESLAJ_ZUORDNUNG ON WLZ_WEINSATZKOPFID = WEB_WEINSATZKOPFID AND WLZ_WEINSATZBUCHLFDNR = WEB_WEINSATZBUCHLFDNR" "~"
if joinKuerzel = WPZWEB
searchResult := searchResult "LEFT JOIN WWS.WEB_WARENEINSATZBUCH ON WEB_WEINSATZKOPFID = WPZ_WEINSATZKOPFID AND WEB_WEINSATZBUCHLFDNR = WPZ_WEINSATZBUCHLFDNR" "~"
if joinKuerzel = WEBWPZ
searchResult := searchResult "LEFT JOIN WWS.WPZ_WESLPO_ZUORDNUNG ON WPZ_WEINSATZKOPFID = WEB_WEINSATZKOPFID AND WPZ_WEINSATZBUCHLFDNR = WEB_WEINSATZBUCHLFDNR" "~"
if joinKuerzel = RNPWEI
searchResult := searchResult "LEFT JOIN WWS.WEI_WARENEINSATZKOPF ON WEI_WEINSATZKOPFID = RNP_WEINSATZKOPFID" "~"
if joinKuerzel = WEIRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_WEINSATZKOPFID = WEI_WEINSATZKOPFID" "~"
if joinKuerzel = WELWEI
searchResult := searchResult "LEFT JOIN WWS.WEI_WARENEINSATZKOPF ON WEI_WEINSATZKOPFID = WEL_WEINSATZKOPFID" "~"
if joinKuerzel = WEIWEL
searchResult := searchResult "LEFT JOIN WWS.WEL_WARENEINSATZLAGERZU ON WEL_WEINSATZKOPFID = WEI_WEINSATZKOPFID" "~"
if joinKuerzel = WEMWEI
searchResult := searchResult "LEFT JOIN WWS.WEI_WARENEINSATZKOPF ON WEI_WEINSATZKOPFID = WEM_WEINSATZKOPFID" "~"
if joinKuerzel = WEIWEM
searchResult := searchResult "LEFT JOIN WWS.WEM_WARENEINSATZBUCHKZZU ON WEM_WEINSATZKOPFID = WEI_WEINSATZKOPFID" "~"
if joinKuerzel = WVDWEI
searchResult := searchResult "LEFT JOIN WWS.WEI_WARENEINSATZKOPF ON WEI_WEINSATZKOPFID = WVD_WEINSATZKOPFID" "~"
if joinKuerzel = WEIWVD
searchResult := searchResult "LEFT JOIN WWS.WVD_WEINSATZVERLEIHDAT ON WVD_WEINSATZKOPFID = WEI_WEINSATZKOPFID" "~"
if joinKuerzel = WZZWEI
searchResult := searchResult "LEFT JOIN WWS.WEI_WARENEINSATZKOPF ON WEI_WEINSATZKOPFID = WZZ_WEINSATZKOPFID" "~"
if joinKuerzel = WEIWZZ
searchResult := searchResult "LEFT JOIN WWS.WZZ_WARENEINSATZERZZU ON WZZ_WEINSATZKOPFID = WEI_WEINSATZKOPFID" "~"
if joinKuerzel = WIZWEI
searchResult := searchResult "LEFT JOIN WWS.WEI_WARENEINSATZKOPF ON WEI_WEINSATZKOPFID = WIZ_ZU_WEINSATZKOPFID" "~"
if joinKuerzel = WEIWIZ
searchResult := searchResult "LEFT JOIN WWS.WIZ_WEINSATZKOPFZU ON WIZ_ZU_WEINSATZKOPFID = WEI_WEINSATZKOPFID" "~"
if joinKuerzel = LAJWEJ
searchResult := searchResult "LEFT JOIN WWS.WEJ_WARENEINSATZLAJ ON WEJ_WEINSATZKOPFID = LAJ_WEINSATZKOPFID AND WEJ_LAJ_FIRMENNR = LAJ_FIRMENNR AND WEJ_LAGERJOURNALLFDNR = LAJ_LAGERJOURNALLFDNR" "~"
if joinKuerzel = WEJLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_WEINSATZKOPFID = WEJ_WEINSATZKOPFID AND LAJ_FIRMENNR = WEJ_LAJ_FIRMENNR AND LAJ_LAGERJOURNALLFDNR = WEJ_LAGERJOURNALLFDNR" "~"
if joinKuerzel = PCPWEK
searchResult := searchResult "LEFT JOIN PRODUKTION.WEK_WERKZEUG ON WEK_FIRMENNR = PCP_QTY_WEK_FIRMENNR AND WEK_WERKZEUGID = PCP_QTY_WERKZEUGID" "~"
if joinKuerzel = WEKPCP
searchResult := searchResult "LEFT JOIN PRODUKTION.PCP_PPS_PRODUKT ON PCP_QTY_WEK_FIRMENNR = WEK_FIRMENNR AND PCP_QTY_WERKZEUGID = WEK_WERKZEUGID" "~"
if joinKuerzel = WEZWEK
searchResult := searchResult "LEFT JOIN PRODUKTION.WEK_WERKZEUG ON WEK_FIRMENNR = WEZ_FIRMENNR AND WEK_WERKZEUGID = WEZ_WERKZEUGID" "~"
if joinKuerzel = WEKWEZ
searchResult := searchResult "LEFT JOIN PRODUKTION.WEZ_WERKZEUGBAZZU ON WEZ_FIRMENNR = WEK_FIRMENNR AND WEZ_WERKZEUGID = WEK_WERKZEUGID" "~"
if joinKuerzel = WFDWFG
searchResult := searchResult "LEFT JOIN WWS.WFG_FOLGEARTIKELGRUPPE ON WFG_WFGID = WFD_WFGID" "~"
if joinKuerzel = WFGWFD
searchResult := searchResult "LEFT JOIN WWS.WFD_FOLGEARTIKELDETAIL ON WFD_WFGID = WFG_WFGID" "~"
if joinKuerzel = AKRVAT
searchResult := searchResult "LEFT JOIN BUERO.VAT_TEILNAHME ON VAT_FIRMENNR = AKR_VAT_FIRMENNR AND VAT_VSTAMMNR = AKR_VAT_VSTAMMNR AND VAT_VERANSTALTUNGSNR = AKR_VAT_VERANSTALTUNGSNR AND VAT_TEILNAHMENR = AKR_VAT_TEILNAHMENR" "~"
if joinKuerzel = VATAKR
searchResult := searchResult "LEFT JOIN BUERO.AKR_AVKOSTENARTENZU ON AKR_VAT_FIRMENNR = VAT_FIRMENNR AND AKR_VAT_VSTAMMNR = VAT_VSTAMMNR AND AKR_VAT_VERANSTALTUNGSNR = VAT_VERANSTALTUNGSNR AND AKR_VAT_TEILNAHMENR = VAT_TEILNAHMENR" "~"
if joinKuerzel = LFSVER
searchResult := searchResult "LEFT JOIN VER_VERSANDART.VER_VERSANDART ON VER_VERSANDARTNR = LFS_VERSANDARTNR" "~"
if joinKuerzel = VERLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_VERSANDARTNR = VER_VERSANDARTNR" "~"
if joinKuerzel = BPPVKA
searchResult := searchResult "LEFT JOIN KORE.VKA_VIEW_KOSTAMM_ABT ON VKA_FIRMENNR = BPP_FIRMENNR AND VKA_KOSTENSTAMMNR = BPP_ABT_KOSTENSTAMMNR" "~"
if joinKuerzel = VKABPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_FIRMENNR = VKA_FIRMENNR AND BPP_ABT_KOSTENSTAMMNR = VKA_KOSTENSTAMMNR" "~"
if joinKuerzel = LAJVKG
searchResult := searchResult "LEFT JOIN KORE.VKG_VIEW_KOSTAMM_GBR ON VKG_FIRMENNR = LAJ_FIRMENNR AND VKG_KOSTENSTAMMNR = LAJ_GBR_KOSTENSTAMMNR" "~"
if joinKuerzel = VKGLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_FIRMENNR = VKG_FIRMENNR AND LAJ_GBR_KOSTENSTAMMNR = VKG_KOSTENSTAMMNR" "~"
if joinKuerzel = BPKVKI
searchResult := searchResult "LEFT JOIN KORE.VKI_VIEW_KOSTAMM_DIM ON VKI_FIRMENNR = BPK_FIRMENNR AND VKI_KOSTENSTAMMNR = BPK_KST_DIM_KOSTENSTAMMNR" "~"
if joinKuerzel = VKIBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_FIRMENNR = VKI_FIRMENNR AND BPK_KST_DIM_KOSTENSTAMMNR = VKI_KOSTENSTAMMNR" "~"
if joinKuerzel = BPPVKI
searchResult := searchResult "LEFT JOIN KORE.VKI_VIEW_KOSTAMM_DIM ON VKI_FIRMENNR = BPP_FIRMENNR AND VKI_KOSTENSTAMMNR = BPP_DIM_KOSTENSTAMMNR" "~"
if joinKuerzel = VKIBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_FIRMENNR = VKI_FIRMENNR AND BPP_DIM_KOSTENSTAMMNR = VKI_KOSTENSTAMMNR" "~"
if joinKuerzel = RNPVKR
searchResult := searchResult "LEFT JOIN WWS.VKR_VA_AUSGKOSTRECHSUM ON VKR_FIRMENNR = RNP_FIRMENNR AND VKR_RECHNUNGLFDNR = RNP_RECHNUNGLFDNR AND VKR_RECHNUNGSPOSLFDNR = RNP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = VKRRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = VKR_FIRMENNR AND RNP_RECHNUNGLFDNR = VKR_RECHNUNGLFDNR AND RNP_RECHNUNGSPOSLFDNR = VKR_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = DISVLJ
searchResult := searchResult "LEFT JOIN WWS.VLJ_VERLEIHJOURNAL ON VLJ_JOURNALID = DIS_VLJ_ID" "~"
if joinKuerzel = VLJDIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_VLJ_ID = VLJ_JOURNALID" "~"
if joinKuerzel = VPNVMK
searchResult := searchResult "LEFT JOIN WWS.VMK_VERSMGR_KOPF ON VMK_VERSMGR_KOPFID = VPN_VMK_VERSMGR_KOPFID" "~"
if joinKuerzel = VMKVPN
searchResult := searchResult "LEFT JOIN WWS.VPN_VERSMGR_POS ON VPN_VMK_VERSMGR_KOPFID = VMK_VERSMGR_KOPFID" "~"
if joinKuerzel = ERBVWE
searchResult := searchResult "LEFT JOIN WWS.VWE_VIEW_WWSERKOPFINFO ON VWE_FIRMENNR = ERB_FIRMENNR AND VWE_FIBUNR = ERB_FIBUNR AND VWE_ERFASSNR = ERB_ERFASSNR" "~"
if joinKuerzel = VWEERB
searchResult := searchResult "LEFT JOIN FIBU.ERB_BUCHUNG ON ERB_FIRMENNR = VWE_FIRMENNR AND ERB_FIBUNR = VWE_FIBUNR AND ERB_ERFASSNR = VWE_ERFASSNR" "~"
if joinKuerzel = LATVWL
searchResult := searchResult "LEFT JOIN WWS.VWL_VA_WEINGLIEFARTINFO ON VWL_ARL_FIRMENNR = LAT_ARL_FIRMENNR AND VWL_ARTIKELNR = LAT_ARL_ARTIKELNR AND VWL_LIEFERANT_FIRMENNR = LAT_KLI_FIRMENNR AND VWL_LIEFERANT_KLID = LAT_KLI_KLID" "~"
if joinKuerzel = VWLLAT
searchResult := searchResult "LEFT JOIN WWS.LAT_LIEFERANTENARTIKEL ON LAT_ARL_FIRMENNR = VWL_ARL_FIRMENNR AND LAT_ARL_ARTIKELNR = VWL_ARTIKELNR AND LAT_KLI_FIRMENNR = VWL_LIEFERANT_FIRMENNR AND LAT_KLI_KLID = VWL_LIEFERANT_KLID" "~"
if joinKuerzel = ADRWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = WADADR
searchResult := searchResult "LEFT JOIN BUERO.ADR_ADRESSE ON ADR_ADRESSLFDNR = WAD_ADRESSLFDNR" "~"
if joinKuerzel = AAPWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = AAP_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEAAP
searchResult := searchResult "LEFT JOIN WWS.AAP_ARAPOSITION ON AAP_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = ADBWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = ADB_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEADB
searchResult := searchResult "LEFT JOIN WWS.ADB_ABGABENDETAILBON ON ADB_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = BAIWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = BAI_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = KZAWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = KZA_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEKZA
searchResult := searchResult "LEFT JOIN WWS.KZA_KASSENZAHLUNGSARTENZU ON KZA_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = KZUWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = KZU_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEKZU
searchResult := searchResult "LEFT JOIN KORE.KZU_KOREKOREZU ON KZU_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = MAHWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = MAH_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEMAH
searchResult := searchResult "LEFT JOIN FIBU.MAH_MAHNUNG ON MAH_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = WKEWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = WKE_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEWKE
searchResult := searchResult "LEFT JOIN WWS.WKE_WARENAUSGKOSTENERF ON WKE_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = ZBMWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = ZBM_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEZBM
searchResult := searchResult "LEFT JOIN FIBU.ZBM_ZINSBERECHNUNG_SUMME ON ZBM_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = HNPWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = HNP_WAEHRUNGSCODE2" "~"
if joinKuerzel = WAEHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_WAEHRUNGSCODE2 = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = KVXSID
searchResult := searchResult "LEFT JOIN HRS.SID_HRSSEQ ON SID_HRSSEQID = KVX_HRSSEQID" "~"
if joinKuerzel = SIDKVX
searchResult := searchResult "LEFT JOIN HRS.KVX_KVTEXT ON KVX_HRSSEQID = SID_HRSSEQID" "~"
if joinKuerzel = SNVSID
searchResult := searchResult "LEFT JOIN HRS.SID_HRSSEQ ON SID_HRSSEQID = SNV_HRSSEQID" "~"
if joinKuerzel = SIDSNV
searchResult := searchResult "LEFT JOIN HRS.SNV_SCHNITTVORSCHLAG ON SNV_HRSSEQID = SID_HRSSEQID" "~"
if joinKuerzel = MOPSLK
searchResult := searchResult "LEFT JOIN WWS.SLK_STUECKLISTENKOPF ON SLK_STKLISTKOPFID = MOP_STKLISTKOPFID" "~"
if joinKuerzel = SLKMOP
searchResult := searchResult "LEFT JOIN WWS.MOP_MODELLPOSVAR ON MOP_STKLISTKOPFID = SLK_STKLISTKOPFID" "~"
if joinKuerzel = MPDSLP
searchResult := searchResult "LEFT JOIN WWS.SLP_STUECKLISTENPOSITION ON SLP_STKLISTPOSID = MPD_STKLISTPOSID AND SLP_STKLISTPOSLFDNR = MPD_STKLISTPOSLFDNR" "~"
if joinKuerzel = SLPMPD
searchResult := searchResult "LEFT JOIN WWS.MPD_MODELLPOSVARDETAIL ON MPD_STKLISTPOSID = SLP_STKLISTPOSID AND MPD_STKLISTPOSLFDNR = SLP_STKLISTPOSLFDNR" "~"
if joinKuerzel = LAWSNI
searchResult := searchResult "LEFT JOIN HRS.SNI_SCHNITTE ON SNI_DBSNR = LAW_DBSNR AND SNI_SCHNITTNR = LAW_SCHNITTNR_5" "~"
if joinKuerzel = SNILAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_DBSNR = SNI_DBSNR AND LAW_SCHNITTNR_5 = SNI_SCHNITTNR" "~"
if joinKuerzel = LAWSNI
searchResult := searchResult "LEFT JOIN HRS.SNI_SCHNITTE ON SNI_DBSNR = LAW_DBSNR AND SNI_SCHNITTNR = LAW_SCHNITTNR_9" "~"
if joinKuerzel = SNILAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_DBSNR = SNI_DBSNR AND LAW_SCHNITTNR_9 = SNI_SCHNITTNR" "~"
if joinKuerzel = EPSSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_SERNRID = EPS_SERNRID" "~"
if joinKuerzel = SNREPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = DKTSPL
searchResult := searchResult "LEFT JOIN SPL_STEMPEL.SPL_STEMPEL ON SPL_STEMPELLFDNR = DKT_STEMPELNR_POSTAUSGANG" "~"
if joinKuerzel = SPLDKT
searchResult := searchResult "LEFT JOIN BUERO.DKT_DOCSKATEGORIE ON DKT_STEMPELNR_POSTAUSGANG = SPL_STEMPELLFDNR" "~"
if joinKuerzel = SPESPQ
searchResult := searchResult "LEFT JOIN HRS.SPQ_SPESENARTEN_SEQ ON SPQ_SPESENARTID = SPE_SPESENARTID" "~"
if joinKuerzel = SPQSPE
searchResult := searchResult "LEFT JOIN HRS.SPE_SPESENARTEN ON SPE_SPESENARTID = SPQ_SPESENARTID" "~"
if joinKuerzel = SPESPQ
searchResult := searchResult "LEFT JOIN HRS.SPQ_SPESENARTEN_SEQ ON SPQ_SPESENARTID = SPE_VERTEIL_SPESENARTID6" "~"
if joinKuerzel = SPQSPE
searchResult := searchResult "LEFT JOIN HRS.SPE_SPESENARTEN ON SPE_VERTEIL_SPESENARTID6 = SPQ_SPESENARTID" "~"
if joinKuerzel = SPESPQ
searchResult := searchResult "LEFT JOIN HRS.SPQ_SPESENARTEN_SEQ ON SPQ_SPESENARTID = SPE_VERTEIL_SPESENARTID9" "~"
if joinKuerzel = SPQSPE
searchResult := searchResult "LEFT JOIN HRS.SPE_SPESENARTEN ON SPE_VERTEIL_SPESENARTID9 = SPQ_SPESENARTID" "~"
if joinKuerzel = AWESPR
searchResult := searchResult "LEFT JOIN SPR_SPRACHE.SPR_SPRACHE ON SPR_SPRACHNR = AWE_SPR_SPRACHNR" "~"
if joinKuerzel = SPRAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_SPR_SPRACHNR = SPR_SPRACHNR" "~"
if joinKuerzel = WWBSPR
searchResult := searchResult "LEFT JOIN SPR_SPRACHE.SPR_SPRACHE ON SPR_SPRACHNR = WWB_SPRACHNR" "~"
if joinKuerzel = SPRWWB
searchResult := searchResult "LEFT JOIN WWS.WWB_WWSBERICHTE ON WWB_SPRACHNR = SPR_SPRACHNR" "~"
if joinKuerzel = BOPSTC
searchResult := searchResult "LEFT JOIN STC_STEUERCODE.STC_STEUERCODE ON STC_LANDNR = BOP_STC_LANDNR AND STC_STEUERCODENR = BOP_STC_STEUERCODENR" "~"
if joinKuerzel = STCBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_STC_LANDNR = STC_LANDNR AND BOP_STC_STEUERCODENR = STC_STEUERCODENR" "~"
if joinKuerzel = BUCSTC
searchResult := searchResult "LEFT JOIN FIBU.STC_STEUERCODE ON STC_LANDNR = BUC_LAN_LANDNR AND STC_STEUERCODENR = BUC_STC_STEUERCODENR" "~"
if joinKuerzel = STCBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_LAN_LANDNR = STC_LANDNR AND BUC_STC_STEUERCODENR = STC_STEUERCODENR" "~"
if joinKuerzel = ERPSTC
searchResult := searchResult "LEFT JOIN STC_STEUERCODE.STC_STEUERCODE ON STC_LANDNR = ERP_STEUERCODE_LANDNR AND STC_STEUERCODENR = ERP_STEUERCODE" "~"
if joinKuerzel = STCERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_STEUERCODE_LANDNR = STC_LANDNR AND ERP_STEUERCODE = STC_STEUERCODENR" "~"
if joinKuerzel = TSASTC
searchResult := searchResult "LEFT JOIN FIBU.STC_STEUERCODE ON STC_LANDNR = TSA_LANDNR AND STC_STEUERCODENR = TSA_STEUERCODENR" "~"
if joinKuerzel = STCTSA
searchResult := searchResult "LEFT JOIN FIBU.TSA_TEILSCHLUSSRECHABLAUF ON TSA_LANDNR = STC_LANDNR AND TSA_STEUERCODENR = STC_STEUERCODENR" "~"
if joinKuerzel = GAUSTE
searchResult := searchResult "LEFT JOIN STE_STEUERERKLAERUNG.STE_STEUERERKLAERUNG ON STE_STEUER_ERKLNR = GAU_STEUER_ERKLNR" "~"
if joinKuerzel = STEGAU
searchResult := searchResult "LEFT JOIN BIL.GAU_GEWINNAUFTEILUNG ON GAU_STEUER_ERKLNR = STE_STEUER_ERKLNR" "~"
if joinKuerzel = TCZTCV
searchResult := searchResult "LEFT JOIN BMD.TCV_TCVERSIONCASE ON TCV_TCTID = TCZ_TCV_TCTID AND TCV_VERSIONSNR = TCZ_VERSIONSNR" "~"
if joinKuerzel = TCVTCZ
searchResult := searchResult "LEFT JOIN BMD.TCZ_TCVERSIONCASEALZU ON TCZ_TCV_TCTID = TCV_TCTID AND TCZ_VERSIONSNR = TCV_VERSIONSNR" "~"
if joinKuerzel = MBMTGR
searchResult := searchResult "LEFT JOIN HRS.TGR_TARIFGRUPPE ON TGR_DBS_DATENBESTAND = MBM_DATENBESTANDNR AND TGR_LOHNJAHR = MBM_JAHR AND TGR_TARIFGRUPPENNR = MBM_TARIFGRUPPENNR" "~"
if joinKuerzel = TGRMBM
searchResult := searchResult "LEFT JOIN HRS.MBM_MBGM ON MBM_DATENBESTANDNR = TGR_DBS_DATENBESTAND AND MBM_JAHR = TGR_LOHNJAHR AND MBM_TARIFGRUPPENNR = TGR_TARIFGRUPPENNR" "~"
if joinKuerzel = MDETLA
searchResult := searchResult "LEFT JOIN TLA_BT_LOGISTIK_ANGEFORDER.TLA_BT_LOGISTIK_ANGEFORDER ON TLA_TLAID = MDE_TLAID" "~"
if joinKuerzel = TLAMDE
searchResult := searchResult "LEFT JOIN WWS.MDE_MOBILEDATENERFASSUNG ON MDE_TLAID = TLA_TLAID" "~"
if joinKuerzel = MDETLK
searchResult := searchResult "LEFT JOIN TLK_BT_LOGISTIK_KOPF.TLK_BT_LOGISTIK_KOPF ON TLK_TLKID = MDE_TLKID" "~"
if joinKuerzel = TLKMDE
searchResult := searchResult "LEFT JOIN WWS.MDE_MOBILEDATENERFASSUNG ON MDE_TLKID = TLK_TLKID" "~"
if joinKuerzel = BRVTSA
searchResult := searchResult "LEFT JOIN FIBU.TSA_TEILSCHLUSSRECHABLAUF ON TSA_FIRMENNR = BRV_BRH_FIRMENNR AND TSA_ABLAUFNR = BRV_ABLAUFNR" "~"
if joinKuerzel = TSABRV
searchResult := searchResult "LEFT JOIN FIBU.BRV_BANKRUECKVERBUCHUNG ON BRV_BRH_FIRMENNR = TSA_FIRMENNR AND BRV_ABLAUFNR = TSA_ABLAUFNR" "~"
if joinKuerzel = BUCTSA
searchResult := searchResult "LEFT JOIN FIBU.TSA_TEILSCHLUSSRECHABLAUF ON TSA_FIRMENNR = BUC_FIRMENNR AND TSA_ABLAUFNR = BUC_TS_ABLAUFNR" "~"
if joinKuerzel = TSABUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_FIRMENNR = TSA_FIRMENNR AND BUC_TS_ABLAUFNR = TSA_ABLAUFNR" "~"
if joinKuerzel = KASTSK
searchResult := searchResult "LEFT JOIN WWS.TSK_TOUCHSCREENKOPF ON TSK_TOUCHSCREENID = KAS_TSK_ID_KAE_UN" "~"
if joinKuerzel = TSKKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_TSK_ID_KAE_UN = TSK_TOUCHSCREENID" "~"
if joinKuerzel = KASTSK
searchResult := searchResult "LEFT JOIN WWS.TSK_TOUCHSCREENKOPF ON TSK_TOUCHSCREENID = KAS_TSK_ID_BOZ_RE" "~"
if joinKuerzel = TSKKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_TSK_ID_BOZ_RE = TSK_TOUCHSCREENID" "~"
if joinKuerzel = KASTSK
searchResult := searchResult "LEFT JOIN WWS.TSK_TOUCHSCREENKOPF ON TSK_TOUCHSCREENID = KAS_TSK_ID_KRAS_RE" "~"
if joinKuerzel = TSKKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_TSK_ID_KRAS_RE = TSK_TOUCHSCREENID" "~"
if joinKuerzel = KASTSK
searchResult := searchResult "LEFT JOIN WWS.TSK_TOUCHSCREENKOPF ON TSK_TOUCHSCREENID = KAS_TSK_ID_KAK_UN" "~"
if joinKuerzel = TSKKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_TSK_ID_KAK_UN = TSK_TOUCHSCREENID" "~"
if joinKuerzel = AWSUMF
searchResult := searchResult "LEFT JOIN UMF_UMFRAGE.UMF_UMFRAGE ON UMF_UMFRAGEID = AWS_UMF_UMFRAGEID" "~"
if joinKuerzel = UMFAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_UMF_UMFRAGEID = UMF_UMFRAGEID" "~"
if joinKuerzel = AWSVAK
searchResult := searchResult "LEFT JOIN VAK_VERANSTALTUNGSKOPF.VAK_VERANSTALTUNGSKOPF ON VAK_FIRMENNR = AWS_VAK_FIRMENNR AND VAK_VSTAMMNR = AWS_VAK_VSTAMMNR AND VAK_VERANSTALTUNGSNR = AWS_VAK_VERANSTALTUNGSNR" "~"
if joinKuerzel = VAKAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_VAK_FIRMENNR = VAK_FIRMENNR AND AWS_VAK_VSTAMMNR = VAK_VSTAMMNR AND AWS_VAK_VERANSTALTUNGSNR = VAK_VERANSTALTUNGSNR" "~"
if joinKuerzel = SFEVAK
searchResult := searchResult "LEFT JOIN BUERO.VAK_VERANSTALTUNGSKOPF ON VAK_FIRMENNR = SFE_VAK_FIRMENNR AND VAK_VSTAMMNR = SFE_VAK_VSTAMMNR AND VAK_VERANSTALTUNGSNR = SFE_VAK_VERANSTALTUNGSNR" "~"
if joinKuerzel = VAKSFE
searchResult := searchResult "LEFT JOIN BUERO.SFE_SKILLFINDERELEMENT ON SFE_VAK_FIRMENNR = VAK_FIRMENNR AND SFE_VAK_VSTAMMNR = VAK_VSTAMMNR AND SFE_VAK_VERANSTALTUNGSNR = VAK_VERANSTALTUNGSNR" "~"
if joinKuerzel = VATVAK
searchResult := searchResult "LEFT JOIN BUERO.VAK_VERANSTALTUNGSKOPF ON VAK_FIRMENNR = VAT_FIRMENNR AND VAK_VSTAMMNR = VAT_VSTAMMNR AND VAK_VERANSTALTUNGSNR = VAT_VERANSTALTUNGSNR" "~"
if joinKuerzel = VAKVAT
searchResult := searchResult "LEFT JOIN BUERO.VAT_TEILNAHME ON VAT_FIRMENNR = VAK_FIRMENNR AND VAT_VSTAMMNR = VAK_VSTAMMNR AND VAT_VERANSTALTUNGSNR = VAK_VERANSTALTUNGSNR" "~"
if joinKuerzel = VAZVEK
searchResult := searchResult "LEFT JOIN WWS.VEK_VERTRETERABRECHKOPF ON VEK_VERTABRKOPFID = VAZ_VERTABRKOPFID" "~"
if joinKuerzel = VEKVAZ
searchResult := searchResult "LEFT JOIN WWS.VAZ_VERTABRZAHLUNGEN ON VAZ_VERTABRKOPFID = VEK_VERTABRKOPFID" "~"
if joinKuerzel = VSZVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_FIRMENNR = VSZ_VORAUSSETZ_FIRMENNR AND VAS_VSTAMMNR = VSZ_VORAUSSETZ_VSTAMMNR" "~"
if joinKuerzel = VASVSZ
searchResult := searchResult "LEFT JOIN BUERO.VSZ_AVSTAMMVORAUSSETZZU ON VSZ_VORAUSSETZ_FIRMENNR = VAS_FIRMENNR AND VSZ_VORAUSSETZ_VSTAMMNR = VAS_VSTAMMNR" "~"
if joinKuerzel = VPOVAU
searchResult := searchResult "LEFT JOIN WWS.VAU_VIEW_LKODISPO_ARTLAG ON VAU_FIRMENNR = VPO_ARL_FIRMENNR AND VAU_ARTIKELNR = VPO_ARTIKELNR AND VAU_LAGERNR = VPO_LAG_LAGERNR" "~"
if joinKuerzel = VAUVPO
searchResult := searchResult "LEFT JOIN WWS.VPO_VIEW_ERFPOSANDORIG ON VPO_ARL_FIRMENNR = VAU_FIRMENNR AND VPO_ARTIKELNR = VAU_ARTIKELNR AND VPO_LAG_LAGERNR = VAU_LAGERNR" "~"
if joinKuerzel = EFKVHR
searchResult := searchResult "LEFT JOIN WWS.VHR_VIEW_ERFASSHRABATT ON VHR_FIRMENNR = EFK_FIRMENNR AND VHR_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND VHR_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = VHREFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = VHR_FIRMENNR AND EFK_ERFASSUNGSKZ = VHR_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = VHR_ERFASSUNGSNR" "~"
if joinKuerzel = LAJVKA
searchResult := searchResult "LEFT JOIN KORE.VKA_VIEW_KOSTAMM_ABT ON VKA_FIRMENNR = LAJ_FIRMENNR AND VKA_KOSTENSTAMMNR = LAJ_ABT_KOSTENSTAMMNR" "~"
if joinKuerzel = VKALAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_FIRMENNR = VKA_FIRMENNR AND LAJ_ABT_KOSTENSTAMMNR = VKA_KOSTENSTAMMNR" "~"
if joinKuerzel = RNPVKA
searchResult := searchResult "LEFT JOIN KORE.VKA_VIEW_KOSTAMM_ABT ON VKA_FIRMENNR = RNP_FIRMENNR AND VKA_KOSTENSTAMMNR = RNP_ABT_KOSTENSTAMMNR" "~"
if joinKuerzel = VKARNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = VKA_FIRMENNR AND RNP_ABT_KOSTENSTAMMNR = VKA_KOSTENSTAMMNR" "~"
if joinKuerzel = ERPVKI
searchResult := searchResult "LEFT JOIN KORE.VKI_VIEW_KOSTAMM_DIM ON VKI_FIRMENNR = ERP_FIRMENNR AND VKI_KOSTENSTAMMNR = ERP_DIM_KOSTENSTAMMNR" "~"
if joinKuerzel = VKIERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_FIRMENNR = VKI_FIRMENNR AND ERP_DIM_KOSTENSTAMMNR = VKI_KOSTENSTAMMNR" "~"
if joinKuerzel = EFKVKT
searchResult := searchResult "LEFT JOIN KORE.VKT_VIEW_KOSTAMM_KTR ON VKT_FIRMENNR = EFK_FIRMENNR AND VKT_KOSTENSTAMMNR = EFK_KOSTENTR_KOSTENSTAMMNR" "~"
if joinKuerzel = VKTEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = VKT_FIRMENNR AND EFK_KOSTENTR_KOSTENSTAMMNR = VKT_KOSTENSTAMMNR" "~"
if joinKuerzel = RNPVKT
searchResult := searchResult "LEFT JOIN KORE.VKT_VIEW_KOSTAMM_KTR ON VKT_FIRMENNR = RNP_FIRMENNR AND VKT_KOSTENSTAMMNR = RNP_KOSTENTR_KOSTENSTAMMNR" "~"
if joinKuerzel = VKTRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = VKT_FIRMENNR AND RNP_KOSTENTR_KOSTENSTAMMNR = VKT_KOSTENSTAMMNR" "~"
if joinKuerzel = VOBVOB
searchResult := searchResult "LEFT JOIN PRODUKTION.VOB_VORGANGSBENACHR ON VOB_VOBID = VOB_VORLAGE_VOBID" "~"
if joinKuerzel = VOBVOB
searchResult := searchResult "LEFT JOIN PRODUKTION.VOB_VORGANGSBENACHR ON VOB_VORLAGE_VOBID = VOB_VOBID" "~"
if joinKuerzel = PKZPSZ
searchResult := searchResult "LEFT JOIN BUERO.PSZ_PERSONENKZ ON PSZ_PERSONENART = PKZ_PERSONENART AND PSZ_PERSONKZNR = PKZ_PERSONKZNR" "~"
if joinKuerzel = PSZPKZ
searchResult := searchResult "LEFT JOIN BUERO.PKZ_PERSONKONTAKTZU ON PKZ_PERSONENART = PSZ_PERSONENART AND PKZ_PERSONKZNR = PSZ_PERSONKZNR" "~"
if joinKuerzel = STTPSZ
searchResult := searchResult "LEFT JOIN PSZ_PERSONENKZ.PSZ_PERSONENKZ ON PSZ_PERSONENART = STT_PSZ_PERSONENART AND PSZ_PERSONKZNR = STT_PSZ_PERSONKZNR" "~"
if joinKuerzel = PSZSTT
searchResult := searchResult "LEFT JOIN PRODUKTION.STT_STAKE_TYP ON STT_PSZ_PERSONENART = PSZ_PERSONENART AND STT_PSZ_PERSONKZNR = PSZ_PERSONKZNR" "~"
if joinKuerzel = PWPPTP
searchResult := searchResult "LEFT JOIN WWS.PTP_PREISTABPOS ON PTP_FIRMENNR = PWP_PTP_FIRMENNR AND PTP_PREISTABNR = PWP_PTP_PREISTABNR AND PTP_PREISTABPOSNR = PWP_PTP_PREISTABPOSNR" "~"
if joinKuerzel = PTPPWP
searchResult := searchResult "LEFT JOIN WWS.PWP_PREISWARTUNGPOS ON PWP_PTP_FIRMENNR = PTP_FIRMENNR AND PWP_PTP_PREISTABNR = PTP_PREISTABNR AND PWP_PTP_PREISTABPOSNR = PTP_PREISTABPOSNR" "~"
if joinKuerzel = AWEPUC
searchResult := searchResult "LEFT JOIN PUC_PPSUSERSTATICOMBI.PUC_PPSUSERSTATICOMBI ON PUC_PUCID = AWE_PUC_PUCID" "~"
if joinKuerzel = PUCAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_PUC_PUCID = PUC_PUCID" "~"
if joinKuerzel = PUCPUS
searchResult := searchResult "LEFT JOIN PRODUKTION.PUS_PPSUSERSTATI ON PUS_STATUSFELD = PUC_STATUSFELD AND PUS_FIRMENNR = PUC_FIRMENNR AND PUS_STATUSVERWENDUNG = PUC_STATUSVERWENDUNG AND PUS_STATUSWERT = PUC_STATUSWERT" "~"
if joinKuerzel = PUSPUC
searchResult := searchResult "LEFT JOIN PRODUKTION.PUC_PPSUSERSTATICOMBI ON PUC_STATUSFELD = PUS_STATUSFELD AND PUC_FIRMENNR = PUS_FIRMENNR AND PUC_STATUSVERWENDUNG = PUS_STATUSVERWENDUNG AND PUC_STATUSWERT = PUS_STATUSWERT" "~"
if joinKuerzel = PVGPVA
searchResult := searchResult "LEFT JOIN BUERO.PVA_PERSONVORERFASSAKTION ON PVA_PVAID = PVG_PVAID" "~"
if joinKuerzel = PVAPVG
searchResult := searchResult "LEFT JOIN BUERO.PVG_PERSONVORERFASSANGEH ON PVG_PVAID = PVA_PVAID" "~"
if joinKuerzel = PWZPWK
searchResult := searchResult "LEFT JOIN WWS.PWK_PREISRABATTWARTKOPF ON PWK_PWKID = PWZ_PWKID" "~"
if joinKuerzel = PWKPWZ
searchResult := searchResult "LEFT JOIN WWS.PWZ_PWKKUNDELIEFERANTZU ON PWZ_PWKID = PWK_PWKID" "~"
if joinKuerzel = PCPQMK
searchResult := searchResult "LEFT JOIN PRODUKTION.QMK_QM_KOPF ON QMK_QM_KOPF_ID = PCP_QM_KOPF_ID" "~"
if joinKuerzel = QMKPCP
searchResult := searchResult "LEFT JOIN PRODUKTION.PCP_PPS_PRODUKT ON PCP_QM_KOPF_ID = QMK_QM_KOPF_ID" "~"
if joinKuerzel = SNRQMK
searchResult := searchResult "LEFT JOIN QMK_QM_KOPF.QMK_QM_KOPF ON QMK_QM_KOPF_ID = SNR_QM_KOPF_ID" "~"
if joinKuerzel = QMKSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_QM_KOPF_ID = QMK_QM_KOPF_ID" "~"
if joinKuerzel = WAPQMK
searchResult := searchResult "LEFT JOIN QMK_QM_KOPF.QMK_QM_KOPF ON QMK_QM_KOPF_ID = WAP_QM_KOPF_ID" "~"
if joinKuerzel = QMKWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_QM_KOPF_ID = QMK_QM_KOPF_ID" "~"
if joinKuerzel = QUVQUO
searchResult := searchResult "LEFT JOIN BUERO.QUO_QUOTE ON QUO_QUOTENNR = QUV_QUOTENNR" "~"
if joinKuerzel = QUOQUV
searchResult := searchResult "LEFT JOIN BUERO.QUV_QUOTENVERLAENGERUNG ON QUV_QUOTENNR = QUO_QUOTENNR" "~"
if joinKuerzel = AVERAT
searchResult := searchResult "LEFT JOIN WWS.RAT_RABATTTAB ON RAT_FIRMENNR = AVE_RAT_FIRMENNR AND RAT_RABTABNR = AVE_RABTABNR" "~"
if joinKuerzel = RATAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_RAT_FIRMENNR = RAT_FIRMENNR AND AVE_RABTABNR = RAT_RABTABNR" "~"
if joinKuerzel = KAKRAT
searchResult := searchResult "LEFT JOIN WWS.RAT_RABATTTAB ON RAT_FIRMENNR = KAK_RABTAB_FIRMENNR AND RAT_RABTABNR = KAK_RABTABNR" "~"
if joinKuerzel = RATKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_RABTAB_FIRMENNR = RAT_FIRMENNR AND KAK_RABTABNR = RAT_RABTABNR" "~"
if joinKuerzel = RAPRAT
searchResult := searchResult "LEFT JOIN WWS.RAT_RABATTTAB ON RAT_FIRMENNR = RAP_FIRMENNR AND RAT_RABTABNR = RAP_RABTABNR" "~"
if joinKuerzel = RATRAP
searchResult := searchResult "LEFT JOIN WWS.RAP_RABATTTABPOS ON RAP_FIRMENNR = RAT_FIRMENNR AND RAP_RABTABNR = RAT_RABTABNR" "~"
if joinKuerzel = LEIREI
searchResult := searchResult "LEFT JOIN REI_REISESTAMM.REI_REISESTAMM ON REI_REISENUMMER = LEI_REI_REISENUMMER" "~"
if joinKuerzel = REILEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_REI_REISENUMMER = REI_REISENUMMER" "~"
if joinKuerzel = LANREL
searchResult := searchResult "LEFT JOIN BUERO.REL_RELIGION ON REL_RELIGIONSNR = LAN_RELIGIONSNR" "~"
if joinKuerzel = RELLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_RELIGIONSNR = REL_RELIGIONSNR" "~"
if joinKuerzel = AUGREM
searchResult := searchResult "LEFT JOIN BUERO.REM_REMINDER ON REM_REMINDERLFDNR = AUG_REMINDERLFDNR" "~"
if joinKuerzel = REMAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_REMINDERLFDNR = REM_REMINDERLFDNR" "~"
if joinKuerzel = KAPRER
searchResult := searchResult "LEFT JOIN WWS.RER_RESERVIERUNGSREFERENZ ON RER_RESERVIERUNGS_ID_BMD = KAP_RESERVIERUNGS_ID_BMD" "~"
if joinKuerzel = RERKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_RESERVIERUNGS_ID_BMD = RER_RESERVIERUNGS_ID_BMD" "~"
if joinKuerzel = RKARKS
searchResult := searchResult "LEFT JOIN BMD.RKS_REPLIKATIONSSTANDORT ON RKS_REPLSTANDONR = RKA_REPLSTANDONR" "~"
if joinKuerzel = RKSRKA
searchResult := searchResult "LEFT JOIN BMD.RKA_REPLIKATIONSAENDERUNG ON RKA_REPLSTANDONR = RKS_REPLSTANDONR" "~"
if joinKuerzel = RKTRKS
searchResult := searchResult "LEFT JOIN BMD.RKS_REPLIKATIONSSTANDORT ON RKS_REPLSTANDONR = RKT_VONREPLSTANDONR" "~"
if joinKuerzel = RKSRKT
searchResult := searchResult "LEFT JOIN BMD.RKT_REPLIKATIONSTRANSFER ON RKT_VONREPLSTANDONR = RKS_REPLSTANDONR" "~"
if joinKuerzel = RLPRLK
searchResult := searchResult "LEFT JOIN WWS.RLK_RECHNUNGSPLANKOPF ON RLK_RLKID = RLP_RLKID" "~"
if joinKuerzel = RLKRLP
searchResult := searchResult "LEFT JOIN WWS.RLP_RECHNUNGSPLANPOS ON RLP_RLKID = RLK_RLKID" "~"
if joinKuerzel = MITRMO
searchResult := searchResult "LEFT JOIN BUERO.RMO_RUNDUNGSMODELL ON RMO_FIRMENNR = MIT_RMO_FIRMENNR AND RMO_RMODELLNR = MIT_RMODELLNR" "~"
if joinKuerzel = RMOMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_RMO_FIRMENNR = RMO_FIRMENNR AND MIT_RMODELLNR = RMO_RMODELLNR" "~"
if joinKuerzel = ERPSTV
searchResult := searchResult "LEFT JOIN STV_STATVERFAHREN.STV_STATVERFAHREN ON STV_FIRMENNR = ERP_STV_FIRMENNR AND STV_STVCODE = ERP_STVCODE" "~"
if joinKuerzel = STVERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_STV_FIRMENNR = STV_FIRMENNR AND ERP_STVCODE = STV_STVCODE" "~"
if joinKuerzel = LPOSTV
searchResult := searchResult "LEFT JOIN STV_STATVERFAHREN.STV_STATVERFAHREN ON STV_FIRMENNR = LPO_STV_FIRMENNR AND STV_STVCODE = LPO_STVCODE" "~"
if joinKuerzel = STVLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_STV_FIRMENNR = STV_FIRMENNR AND LPO_STVCODE = STV_STVCODE" "~"
if joinKuerzel = RPVSYD
searchResult := searchResult "LEFT JOIN BMD.SYD_SYSTEMDRUCKER ON SYD_DRUCKERNAME = RPV_DRUCKERNAME" "~"
if joinKuerzel = SYDRPV
searchResult := searchResult "LEFT JOIN BMD.RPV_REPORTVORSCHLAG ON RPV_DRUCKERNAME = SYD_DRUCKERNAME" "~"
if joinKuerzel = TCATCN
searchResult := searchResult "LEFT JOIN BMD.TCN_TCVERSIONEN ON TCN_VERSIONSNR = TCA_VERSIONSNR" "~"
if joinKuerzel = TCNTCA
searchResult := searchResult "LEFT JOIN BMD.TCA_TCACTIONLIST ON TCA_VERSIONSNR = TCN_VERSIONSNR" "~"
if joinKuerzel = SFZTEA
searchResult := searchResult "LEFT JOIN BUERO.TEA_TELEFONART ON TEA_TELEFONARTNR = SFZ_TEA_TELEFONARTNR" "~"
if joinKuerzel = TEASFZ
searchResult := searchResult "LEFT JOIN BUERO.SFZ_SYNCFELDZUORDNUNG ON SFZ_TEA_TELEFONARTNR = TEA_TELEFONARTNR" "~"
if joinKuerzel = INSTEL
searchResult := searchResult "LEFT JOIN BUERO.TEL_TELEFON ON TEL_TELLFDNR = INS_TELLFDNR" "~"
if joinKuerzel = TELINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_TELLFDNR = TEL_TELLFDNR" "~"
if joinKuerzel = AGSTMO
searchResult := searchResult "LEFT JOIN TMO_TAGESMODELL.TMO_TAGESMODELL ON TMO_FIRMENNR = AGS_TMO_FIRMENNR AND TMO_TAGESMODELLNR = AGS_TMO_TAGESMODELLNR" "~"
if joinKuerzel = TMOAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_TMO_FIRMENNR = TMO_FIRMENNR AND AGS_TMO_TAGESMODELLNR = TMO_TAGESMODELLNR" "~"
if joinKuerzel = PKDTMO
searchResult := searchResult "LEFT JOIN TMO_TAGESMODELL.TMO_TAGESMODELL ON TMO_FIRMENNR = PKD_TMO_FIRMENNR AND TMO_TAGESMODELLNR = PKD_TMO_TAGESMODELLNR" "~"
if joinKuerzel = TMOPKD
searchResult := searchResult "LEFT JOIN PRODUKTION.PKD_KM_DETAIL ON PKD_TMO_FIRMENNR = TMO_FIRMENNR AND PKD_TMO_TAGESMODELLNR = TMO_TAGESMODELLNR" "~"
if joinKuerzel = TMSTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = TMS_TMO_FIRMENNR AND TMO_TAGESMODELLNR = TMS_TAGESMODELLNR" "~"
if joinKuerzel = TMOTMS
searchResult := searchResult "LEFT JOIN BUERO.TMS_TMSICHERUNG ON TMS_TMO_FIRMENNR = TMO_FIRMENNR AND TMS_TAGESMODELLNR = TMO_TAGESMODELLNR" "~"
if joinKuerzel = WMDTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = WMD_FIRMENNR AND TMO_TAGESMODELLNR = WMD_TAGESMODELLNR7" "~"
if joinKuerzel = TMOWMD
searchResult := searchResult "LEFT JOIN BUERO.WMD_WOCHENMODELL ON WMD_FIRMENNR = TMO_FIRMENNR AND WMD_TAGESMODELLNR7 = TMO_TAGESMODELLNR" "~"
if joinKuerzel = ZMDTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = ZMD_TMO_FIRMENNR AND TMO_TAGESMODELLNR = ZMD_TAGESMODELLNR" "~"
if joinKuerzel = TMOZMD
searchResult := searchResult "LEFT JOIN BUERO.ZMD_ZEITMINDESTBESETZDET ON ZMD_TMO_FIRMENNR = TMO_FIRMENNR AND ZMD_TAGESMODELLNR = TMO_TAGESMODELLNR" "~"
if joinKuerzel = WMDTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = WMD_FIRMENNR AND TMO_TAGESMODELLNR = WMD_TAGESMODELLNR4" "~"
if joinKuerzel = TMOWMD
searchResult := searchResult "LEFT JOIN BUERO.WMD_WOCHENMODELL ON WMD_FIRMENNR = TMO_FIRMENNR AND WMD_TAGESMODELLNR4 = TMO_TAGESMODELLNR" "~"
if joinKuerzel = AVETOU
searchResult := searchResult "LEFT JOIN WWS.TOU_TOUR ON TOU_FIRMENNR = AVE_TOU_FIRMENNR AND TOU_TOURNR = AVE_TOU_TOURNR" "~"
if joinKuerzel = TOUAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_TOU_FIRMENNR = TOU_FIRMENNR AND AVE_TOU_TOURNR = TOU_TOURNR" "~"
if joinKuerzel = TOZTOU
searchResult := searchResult "LEFT JOIN WWS.TOU_TOUR ON TOU_FIRMENNR = TOZ_TOU_FIRMENNR AND TOU_TOURNR = TOZ_TOU_TOURNR" "~"
if joinKuerzel = TOUTOZ
searchResult := searchResult "LEFT JOIN WWS.TOZ_TOURZUORDNUNG ON TOZ_TOU_FIRMENNR = TOU_FIRMENNR AND TOZ_TOU_TOURNR = TOU_TOURNR" "~"
if joinKuerzel = TRETOU
searchResult := searchResult "LEFT JOIN WWS.TOU_TOUR ON TOU_FIRMENNR = TRE_TOU_FIRMENNR AND TOU_TOURNR = TRE_TOU_TOURNR" "~"
if joinKuerzel = TOUTRE
searchResult := searchResult "LEFT JOIN WWS.TRE_TRANSPORTEINHEIT ON TRE_TOU_FIRMENNR = TOU_FIRMENNR AND TRE_TOU_TOURNR = TOU_TOURNR" "~"
if joinKuerzel = TRPTRE
searchResult := searchResult "LEFT JOIN WWS.TRE_TRANSPORTEINHEIT ON TRE_TRANSPORTEINHEITID = TRP_TRE_TRANSPORTEINHEITID" "~"
if joinKuerzel = TRETRP
searchResult := searchResult "LEFT JOIN WWS.TRP_TRANSPORTPOSITION ON TRP_TRE_TRANSPORTEINHEITID = TRE_TRANSPORTEINHEITID" "~"
if joinKuerzel = KLITRS
searchResult := searchResult "LEFT JOIN BUERO.TRS_TEILSCHLUSSRECHSCHEMA ON TRS_TRSID = KLI_TRSID" "~"
if joinKuerzel = TRSKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_TRSID = TRS_TRSID" "~"
if joinKuerzel = YBUTSA
searchResult := searchResult "LEFT JOIN FIBU.TSA_TEILSCHLUSSRECHABLAUF ON TSA_FIRMENNR = YBU_FIRMENNR AND TSA_ABLAUFNR = YBU_TS_ABLAUFNR" "~"
if joinKuerzel = TSAYBU
searchResult := searchResult "LEFT JOIN FIBU.YBU_AUTOBUCHUNG ON YBU_FIRMENNR = TSA_FIRMENNR AND YBU_TS_ABLAUFNR = TSA_ABLAUFNR" "~"
if joinKuerzel = KASTSK
searchResult := searchResult "LEFT JOIN WWS.TSK_TOUCHSCREENKOPF ON TSK_TOUCHSCREENID = KAS_TSK_ID_KRAP_RE" "~"
if joinKuerzel = TSKKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_TSK_ID_KRAP_RE = TSK_TOUCHSCREENID" "~"
if joinKuerzel = KASTSK
searchResult := searchResult "LEFT JOIN WWS.TSK_TOUCHSCREENKOPF ON TSK_TOUCHSCREENID = KAS_TSK_ID_KRAP_MI" "~"
if joinKuerzel = TSKKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_TSK_ID_KRAP_MI = TSK_TOUCHSCREENID" "~"
if joinKuerzel = KASTSK
searchResult := searchResult "LEFT JOIN WWS.TSK_TOUCHSCREENKOPF ON TSK_TOUCHSCREENID = KAS_TSK_ID_KAP_MI" "~"
if joinKuerzel = TSKKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_TSK_ID_KAP_MI = TSK_TOUCHSCREENID" "~"
if joinKuerzel = UANULE
searchResult := searchResult "LEFT JOIN BMDINTERN.ULE_UPDATELEBENSLAUF ON ULE_LEBENSLAUFNR = UAN_ULE_LEBENSLAUFNR" "~"
if joinKuerzel = ULEUAN
searchResult := searchResult "LEFT JOIN BMDINTERN.UAN_UPDATEANFORDERUNGSTXT ON UAN_ULE_LEBENSLAUFNR = ULE_LEBENSLAUFNR" "~"
if joinKuerzel = WGZWFK
searchResult := searchResult "LEFT JOIN WWS.WFK_FOLGEARTIKELKAT ON WFK_WFKID = WGZ_WFKID" "~"
if joinKuerzel = WFKWGZ
searchResult := searchResult "LEFT JOIN WWS.WGZ_WFK_WKG_ZU ON WGZ_WFKID = WFK_WFKID" "~"
if joinKuerzel = WIFWII
searchResult := searchResult "LEFT JOIN BMDENTW.WII_WEITERE_INFO_ITEM ON WII_WIIID = WIF_WIIID" "~"
if joinKuerzel = WIIWIF
searchResult := searchResult "LEFT JOIN BMDENTW.WIF_WII_DFU_ZU ON WIF_WIIID = WII_WIIID" "~"
if joinKuerzel = WIKWIP
searchResult := searchResult "LEFT JOIN BIL.WIP_WIPRUEFUNG ON WIP_WIPRUEFUNGSID = WIK_WIPRUEFUNGSID" "~"
if joinKuerzel = WIPWIK
searchResult := searchResult "LEFT JOIN BIL.WIK_WIVARIABLENKATEGORIE ON WIK_WIPRUEFUNGSID = WIP_WIPRUEFUNGSID" "~"
if joinKuerzel = EDVWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = EDV_HAUPTFIRMENNR AND WKT_KONTENTABNR = EDV_KONTENTABNR" "~"
if joinKuerzel = WKTEDV
searchResult := searchResult "LEFT JOIN WWS.EDV_ERFASSRABATTDETAILVOR ON EDV_HAUPTFIRMENNR = WKT_FIRMENNR AND EDV_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = PROWKT
searchResult := searchResult "LEFT JOIN WKT_WWSKONTENTAB.WKT_WWSKONTENTAB ON WKT_FIRMENNR = PRO_WKT_FIRMENNR AND WKT_KONTENTABNR = PRO_WKT_KONTENTABNR" "~"
if joinKuerzel = WKTPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_WKT_FIRMENNR = WKT_FIRMENNR AND PRO_WKT_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = WAGWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = WAG_FIRMENNR AND WKT_KONTENTABNR = WAG_KONTENTABNR" "~"
if joinKuerzel = WKTWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = WKT_FIRMENNR AND WAG_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = WKPWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = WKP_FIRMENNR AND WKT_KONTENTABNR = WKP_KONTENTABNR" "~"
if joinKuerzel = WKTWKP
searchResult := searchResult "LEFT JOIN WWS.WKP_WWSKONTENTABPOS ON WKP_FIRMENNR = WKT_FIRMENNR AND WKP_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = BMMWMD
searchResult := searchResult "LEFT JOIN WMD_WOCHENMODELL.WMD_WOCHENMODELL ON WMD_FIRMENNR = BMM_WMD_FIRMENNR AND WMD_WOCHENMODELLNR = BMM_WMD_NR" "~"
if joinKuerzel = WMDBMM
searchResult := searchResult "LEFT JOIN HRS.BMM_BUAK_MONMELDUNG ON BMM_WMD_FIRMENNR = WMD_FIRMENNR AND BMM_WMD_NR = WMD_WOCHENMODELLNR" "~"
if joinKuerzel = RWZWMD
searchResult := searchResult "LEFT JOIN BUERO.WMD_WOCHENMODELL ON WMD_FIRMENNR = RWZ_WMD_FIRMENNR AND WMD_WOCHENMODELLNR = RWZ_WOCHENMODELLNR" "~"
if joinKuerzel = WMDRWZ
searchResult := searchResult "LEFT JOIN BUERO.RWZ_ZEITRUNDWOCHENMDZUORD ON RWZ_WMD_FIRMENNR = WMD_FIRMENNR AND RWZ_WOCHENMODELLNR = WMD_WOCHENMODELLNR" "~"
if joinKuerzel = WRPWRK
searchResult := searchResult "LEFT JOIN WWS.WRK_WWSANKAUFSRNGKOPF ON WRK_FIRMENNR = WRP_FIRMENNR AND WRK_RECHNUNGLFDNR = WRP_RECHNUNGLFDNR" "~"
if joinKuerzel = WRKWRP
searchResult := searchResult "LEFT JOIN WWS.WRP_WWSANKAUFSRNGPOS ON WRP_FIRMENNR = WRK_FIRMENNR AND WRP_RECHNUNGLFDNR = WRK_RECHNUNGLFDNR" "~"
if joinKuerzel = KODWTB
searchResult := searchResult "LEFT JOIN WTB_WWSTEXTBAUSTEIN.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = KOD_WTB_FIRMENNR AND WTB_WWSTEXTBNR = KOD_WTB_WWSTEXTBNR" "~"
if joinKuerzel = WTBKOD
searchResult := searchResult "LEFT JOIN BUERO.KOD_KONDITIONEN ON KOD_WTB_FIRMENNR = WTB_FIRMENNR AND KOD_WTB_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = RTZWTB
searchResult := searchResult "LEFT JOIN WTB_WWSTEXTBAUSTEIN.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = RTZ_WTB_FIRMENNR AND WTB_WWSTEXTBNR = RTZ_WTB_WWSTEXTBNR" "~"
if joinKuerzel = WTBRTZ
searchResult := searchResult "LEFT JOIN ZLE.RTZ_RECHTEXTZU ON RTZ_WTB_FIRMENNR = WTB_FIRMENNR AND RTZ_WTB_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = ZESWTB
searchResult := searchResult "LEFT JOIN WTB_WWSTEXTBAUSTEIN.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = ZES_WTB_FIRMENNR AND WTB_WWSTEXTBNR = ZES_WTB_WWSTEXTBNR" "~"
if joinKuerzel = WTBZES
searchResult := searchResult "LEFT JOIN FIBU.ZES_ZESSIONSTEXT ON ZES_WTB_FIRMENNR = WTB_FIRMENNR AND ZES_WTB_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = ERZWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = ERZ_HAUPTFIRMENNR AND WTB_WWSTEXTBNR = ERZ_FUSS_WWSTEXTBNR" "~"
if joinKuerzel = WTBERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_HAUPTFIRMENNR = WTB_FIRMENNR AND ERZ_FUSS_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = LMSWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = LMS_WWSTEXTBNR_FIRMENNR AND WTB_WWSTEXTBNR = LMS_WWSTEXTBNR_STUFE3" "~"
if joinKuerzel = WTBLMS
searchResult := searchResult "LEFT JOIN WWS.LMS_LIEFERMAHNUNG ON LMS_WWSTEXTBNR_FIRMENNR = WTB_FIRMENNR AND LMS_WWSTEXTBNR_STUFE3 = WTB_WWSTEXTBNR" "~"
if joinKuerzel = LMSWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = LMS_WWSTEXTBNR_FIRMENNR AND WTB_WWSTEXTBNR = LMS_WWSTEXTBNR_STUFE4" "~"
if joinKuerzel = WTBLMS
searchResult := searchResult "LEFT JOIN WWS.LMS_LIEFERMAHNUNG ON LMS_WWSTEXTBNR_FIRMENNR = WTB_FIRMENNR AND LMS_WWSTEXTBNR_STUFE4 = WTB_WWSTEXTBNR" "~"
if joinKuerzel = WADWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = WAD_HAUPTFIRMENNR AND WTB_WWSTEXTBNR = WAD_KOPF_RNG_WWSTEXTBNR" "~"
if joinKuerzel = WTBWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_HAUPTFIRMENNR = WTB_FIRMENNR AND WAD_KOPF_RNG_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = WKGWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = WKG_FIRMENNR AND WTB_WWSTEXTBNR = WKG_KOPF_LFS_WWSTEXTBNR" "~"
if joinKuerzel = WTBWKG
searchResult := searchResult "LEFT JOIN WWS.WKG_WWSKUNDENGRUPPE ON WKG_FIRMENNR = WTB_FIRMENNR AND WKG_KOPF_LFS_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = LMSWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = LMS_WWSTEXTBNR_FIRMENNR AND WTB_WWSTEXTBNR = LMS_WWSTEXTBNR_SCH_STUFE4" "~"
if joinKuerzel = WTBLMS
searchResult := searchResult "LEFT JOIN WWS.LMS_LIEFERMAHNUNG ON LMS_WWSTEXTBNR_FIRMENNR = WTB_FIRMENNR AND LMS_WWSTEXTBNR_SCH_STUFE4 = WTB_WWSTEXTBNR" "~"
if joinKuerzel = WBSWWB
searchResult := searchResult "LEFT JOIN WWS.WWB_WWSBERICHTE ON WWB_WWSBERICHTNR = WBS_WWSBERICHTNR" "~"
if joinKuerzel = WWBWBS
searchResult := searchResult "LEFT JOIN WWS.WBS_WWSBERICHT_SNAPSHOT ON WBS_WWSBERICHTNR = WWB_WWSBERICHTNR" "~"
if joinKuerzel = LWPWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = LWP_WAEHRUNGSCODE" "~"
if joinKuerzel = WAELWP
searchResult := searchResult "LEFT JOIN FIBU.LWP_LANDWIRT_PRODUKTE ON LWP_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = PASWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = PAS_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEPAS
searchResult := searchResult "LEFT JOIN WWS.PAS_PACKSTOFFPREIS ON PAS_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = PIRWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = PIR_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEPIR
searchResult := searchResult "LEFT JOIN WWS.PIR_PICRABATTDETAIL ON PIR_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = RNAWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = RNA_WAEHRUNGSCODE" "~"
if joinKuerzel = WAERNA
searchResult := searchResult "LEFT JOIN WWS.RNA_RECHNUNGANZAHLUNG ON RNA_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = YBUWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = YBU_WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEYBU
searchResult := searchResult "LEFT JOIN FIBU.YBU_AUTOBUCHUNG ON YBU_WAE_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = ADWWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = ADW_WAEHRUNGSCODE_ERW" "~"
if joinKuerzel = WAEADW
searchResult := searchResult "LEFT JOIN WWS.ADW_ABGABENDETAILWAP ON ADW_WAEHRUNGSCODE_ERW = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = FWKWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = FWK_ZWEIT_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_ZWEIT_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = PERWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = PER_ZWEIT_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_ZWEIT_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = WKLWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = WKL_WAEHRUNGSCODE_KOPIE" "~"
if joinKuerzel = WAEWKL
searchResult := searchResult "LEFT JOIN WWS.WKL_WARENAUSGKOSTENLIEF ON WKL_WAEHRUNGSCODE_KOPIE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = ADWWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = ADW_WAEHRUNGSCODE_FW" "~"
if joinKuerzel = WAEADW
searchResult := searchResult "LEFT JOIN WWS.ADW_ABGABENDETAILWAP ON ADW_WAEHRUNGSCODE_FW = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = AWSWAG
searchResult := searchResult "LEFT JOIN WAG_WARENGRUPPE.WAG_WARENGRUPPE ON WAG_FIRMENNR = AWS_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = AWS_WAG_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = AWS_WAG_WARENUNTERGRNR" "~"
if joinKuerzel = WAGAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_WAG_FIRMENNR = WAG_FIRMENNR AND AWS_WAG_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND AWS_WAG_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = GZUWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = GZU_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = GZU_WAG_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = GZU_WAG_WARENUNTERGRNR" "~"
if joinKuerzel = WAGGZU
searchResult := searchResult "LEFT JOIN WWS.GZU_GEFAHRENGUTZU ON GZU_WAG_FIRMENNR = WAG_FIRMENNR AND GZU_WAG_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND GZU_WAG_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = IAPWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = IAP_FIRMENNR AND WAG_WARENHAUPTGRNR = IAP_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = IAP_WARENUNTERGRNR" "~"
if joinKuerzel = WAGIAP
searchResult := searchResult "LEFT JOIN WWS.IAP_INVABWTPOS ON IAP_FIRMENNR = WAG_FIRMENNR AND IAP_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND IAP_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = WAKWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_WARENEINGKOPFID = WAK_URSPRWAK" "~"
if joinKuerzel = WAKWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_URSPRWAK = WAK_WARENEINGKOPFID" "~"
if joinKuerzel = WAOWBG
searchResult := searchResult "LEFT JOIN WWS.WBG_WARENBESCHKOSTENGRP ON WBG_WBGID = WAO_WBGID" "~"
if joinKuerzel = WBGWAO
searchResult := searchResult "LEFT JOIN WWS.WAO_WARENEINGWBK ON WAO_WBGID = WBG_WBGID" "~"
if joinKuerzel = WBDWBG
searchResult := searchResult "LEFT JOIN WWS.WBG_WARENBESCHKOSTENGRP ON WBG_WBGID = WBD_WBGID" "~"
if joinKuerzel = WBGWBD
searchResult := searchResult "LEFT JOIN WWS.WBD_WARENBESCHKOSTENDET ON WBD_WBGID = WBG_WBGID" "~"
if joinKuerzel = WCUWCA
searchResult := searchResult "LEFT JOIN BMD.WCA_WAAUSDRUCKE ON WCA_WAAUSDRUCKNR = WCU_WAAUSDRUCKNR" "~"
if joinKuerzel = WCAWCU
searchResult := searchResult "LEFT JOIN BMD.WCU_WAAUSDRUCKUSERZU ON WCU_WAAUSDRUCKNR = WCA_WAAUSDRUCKNR" "~"
if joinKuerzel = WCZWCA
searchResult := searchResult "LEFT JOIN BMD.WCA_WAAUSDRUCKE ON WCA_WAAUSDRUCKNR = WCZ_WAAUSDRUCKNR" "~"
if joinKuerzel = WCAWCZ
searchResult := searchResult "LEFT JOIN BMD.WCZ_WAAUSDRUCK_PARAM_ZU ON WCZ_WAAUSDRUCKNR = WCA_WAAUSDRUCKNR" "~"
if joinKuerzel = WCPWCD
searchResult := searchResult "LEFT JOIN BMD.WCD_WAPARAMDEF ON WCD_WAPARAMDEFNR = WCP_WAPARAMDEFNR" "~"
if joinKuerzel = WCDWCP
searchResult := searchResult "LEFT JOIN BMD.WCP_WAAUSDRUCKPARAMETER ON WCP_WAPARAMDEFNR = WCD_WAPARAMDEFNR" "~"
if joinKuerzel = WCEWCE
searchResult := searchResult "LEFT JOIN BMD.WCE_WABAUMELEMENTE ON WCE_WABAUMELEMNR = WCE_WABAUMELEMNR_PARENT" "~"
if joinKuerzel = WCEWCE
searchResult := searchResult "LEFT JOIN BMD.WCE_WABAUMELEMENTE ON WCE_WABAUMELEMNR_PARENT = WCE_WABAUMELEMNR" "~"
if joinKuerzel = WETWEI
searchResult := searchResult "LEFT JOIN WWS.WEI_WARENEINSATZKOPF ON WEI_WEINSATZKOPFID = WET_WEINSATZKOPFID" "~"
if joinKuerzel = WEIWET
searchResult := searchResult "LEFT JOIN WWS.WET_WARENEINSATZARTZU ON WET_WEINSATZKOPFID = WEI_WEINSATZKOPFID" "~"
if joinKuerzel = WEUWEU
searchResult := searchResult "LEFT JOIN WWS.WEU_WWSERKONTROLLEZU ON WEU_ERKONTRZU_ID = WEU_SETERKONTRZU_ID" "~"
if joinKuerzel = WEUWEU
searchResult := searchResult "LEFT JOIN WWS.WEU_WWSERKONTROLLEZU ON WEU_SETERKONTRZU_ID = WEU_ERKONTRZU_ID" "~"
if joinKuerzel = SAPWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = SAP_ZWEIT_WAEHRUNGSCODE" "~"
if joinKuerzel = WAESAP
searchResult := searchResult "LEFT JOIN BUERO.SAP_SAMMELPROJEKT ON SAP_ZWEIT_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = FWFWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = FWF_BH_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_BH_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = EEPWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = EEP_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = EEP_WAG_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = EEP_WAG_WARENUNTERGRNR" "~"
if joinKuerzel = WAGEEP
searchResult := searchResult "LEFT JOIN WWS.EEP_EDIEINHEITENPOS ON EEP_WAG_FIRMENNR = WAG_FIRMENNR AND EEP_WAG_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND EEP_WAG_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = HPTWAG
searchResult := searchResult "LEFT JOIN WAG_WARENGRUPPE.WAG_WARENGRUPPE ON WAG_FIRMENNR = HPT_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = HPT_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = HPT_WARENUNTERGRNR" "~"
if joinKuerzel = WAGHPT
searchResult := searchResult "LEFT JOIN ZLE.HPT_HONORARPOSTEXTE ON HPT_WAG_FIRMENNR = WAG_FIRMENNR AND HPT_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND HPT_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = HVGWAG
searchResult := searchResult "LEFT JOIN WAG_WARENGRUPPE.WAG_WARENGRUPPE ON WAG_FIRMENNR = HVG_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = HVG_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = HVG_WARENUNTERGRNR" "~"
if joinKuerzel = WAGHVG
searchResult := searchResult "LEFT JOIN ZLE.HVG_HONVORGRUPPE ON HVG_WAG_FIRMENNR = WAG_FIRMENNR AND HVG_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND HVG_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = MRDWAG
searchResult := searchResult "LEFT JOIN WAG_WARENGRUPPE.WAG_WARENGRUPPE ON WAG_FIRMENNR = MRD_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = MRD_WAG_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = MRD_WAG_WARENUNTERGRNR" "~"
if joinKuerzel = WAGMRD
searchResult := searchResult "LEFT JOIN ZLE.MRD_MONATSRECHNUNGDETAIL ON MRD_WAG_FIRMENNR = WAG_FIRMENNR AND MRD_WAG_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND MRD_WAG_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = PRSWAG
searchResult := searchResult "LEFT JOIN WAG_WARENGRUPPE.WAG_WARENGRUPPE ON WAG_FIRMENNR = PRS_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = PRS_WAG_TAET_HGR AND WAG_WARENUNTERGRNR = PRS_WAG_TAET_UGR" "~"
if joinKuerzel = WAGPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_WAG_FIRMENNR = WAG_FIRMENNR AND PRS_WAG_TAET_HGR = WAG_WARENHAUPTGRNR AND PRS_WAG_TAET_UGR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = PSWWAG
searchResult := searchResult "LEFT JOIN WAG_WARENGRUPPE.WAG_WARENGRUPPE ON WAG_FIRMENNR = PSW_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = PSW_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = PSW_WARENUNTERGRNR" "~"
if joinKuerzel = WAGPSW
searchResult := searchResult "LEFT JOIN BUERO.PSW_PERSONENWERTE ON PSW_WAG_FIRMENNR = WAG_FIRMENNR AND PSW_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND PSW_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = LAJWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_WARENEINGKOPFID = LAJ_WARENEINGKOPFID" "~"
if joinKuerzel = WAKLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_WARENEINGKOPFID = WAK_WARENEINGKOPFID" "~"
if joinKuerzel = AAPWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_WARENEINGKOPFID = AAP_WARENEINGKOPFID AND WAP_WARENEINGPOSLFDNR = AAP_WARENEINGPOSLFDNR" "~"
if joinKuerzel = WAPAAP
searchResult := searchResult "LEFT JOIN WWS.AAP_ARAPOSITION ON AAP_WARENEINGKOPFID = WAP_WARENEINGKOPFID AND AAP_WARENEINGPOSLFDNR = WAP_WARENEINGPOSLFDNR" "~"
if joinKuerzel = WEPWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_WARENEINGKOPFID = WEP_WAP_WARENEINGKOPFID AND WAP_WARENEINGPOSLFDNR = WEP_WAP_WARENEINGPOSLFDNR" "~"
if joinKuerzel = WAPWEP
searchResult := searchResult "LEFT JOIN WWS.WEP_WWSERKONTROLLEPOS ON WEP_WAP_WARENEINGKOPFID = WAP_WARENEINGKOPFID AND WEP_WAP_WARENEINGPOSLFDNR = WAP_WARENEINGPOSLFDNR" "~"
if joinKuerzel = WEAWAR
searchResult := searchResult "LEFT JOIN WWS.WAR_WARENEINGANGRABATTE ON WAR_WARENEINGRABATTID = WEA_WARENEINGRABATTID" "~"
if joinKuerzel = WARWEA
searchResult := searchResult "LEFT JOIN WWS.WEA_WWSERKONTROLLERABATTE ON WEA_WARENEINGRABATTID = WAR_WARENEINGRABATTID" "~"
if joinKuerzel = BSPWBW
searchResult := searchResult "LEFT JOIN WWS.WBW_WWSBUDGETWERTE ON WBW_WWSBUDGETWERTEID = BSP_WWSBUDGETWERTEID" "~"
if joinKuerzel = WBWBSP
searchResult := searchResult "LEFT JOIN WWS.BSP_BONUSERMPOS ON BSP_WWSBUDGETWERTEID = WBW_WWSBUDGETWERTEID" "~"
if joinKuerzel = WIZWEI
searchResult := searchResult "LEFT JOIN WWS.WEI_WARENEINSATZKOPF ON WEI_WEINSATZKOPFID = WIZ_WEINSATZKOPFID" "~"
if joinKuerzel = WEIWIZ
searchResult := searchResult "LEFT JOIN WWS.WIZ_WEINSATZKOPFZU ON WIZ_WEINSATZKOPFID = WEI_WEINSATZKOPFID" "~"
if joinKuerzel = WKRWEP
searchResult := searchResult "LEFT JOIN WWS.WEP_WWSERKONTROLLEPOS ON WEP_ERKONTRPOS_ID = WKR_ERKONTRPOS_ID" "~"
if joinKuerzel = WEPWKR
searchResult := searchResult "LEFT JOIN WWS.WKR_WARENAUSGKOSTENRECH ON WKR_ERKONTRPOS_ID = WEP_ERKONTRPOS_ID" "~"
if joinKuerzel = WESWER
searchResult := searchResult "LEFT JOIN WWS.WER_WWSERKONTROLLEKOPF ON WER_ERKONTRKOPF_ID = WES_ERKONTRKOPF_ID" "~"
if joinKuerzel = WERWES
searchResult := searchResult "LEFT JOIN WWS.WES_WWSERKONTROLLEWBK ON WES_ERKONTRKOPF_ID = WER_ERKONTRKOPF_ID" "~"
if joinKuerzel = WKRWER
searchResult := searchResult "LEFT JOIN WWS.WER_WWSERKONTROLLEKOPF ON WER_ERKONTRKOPF_ID = WKR_ERKONTRKOPF_ID" "~"
if joinKuerzel = WERWKR
searchResult := searchResult "LEFT JOIN WWS.WKR_WARENAUSGKOSTENRECH ON WKR_ERKONTRKOPF_ID = WER_ERKONTRKOPF_ID" "~"
if joinKuerzel = WPRWFI
searchResult := searchResult "LEFT JOIN WEB.WFI_WEBFIRMA ON WFI_FIRMENNR = WPR_FIRMENNR" "~"
if joinKuerzel = WFIWPR
searchResult := searchResult "LEFT JOIN WEB.WPR_WEBPROTOKOLL ON WPR_FIRMENNR = WFI_FIRMENNR" "~"
if joinKuerzel = WKZWFK
searchResult := searchResult "LEFT JOIN WWS.WFK_FOLGEARTIKELKAT ON WFK_WFKID = WKZ_WFKID" "~"
if joinKuerzel = WFKWKZ
searchResult := searchResult "LEFT JOIN WWS.WKZ_WFKKUNDELIEFERANTZU ON WKZ_WFKID = WFK_WFKID" "~"
if joinKuerzel = TLUTLP
searchResult := searchResult "LEFT JOIN PRODUKTION.TLP_BT_LOGISTIK_POSITION ON TLP_TLPID = TLU_TLPID" "~"
if joinKuerzel = TLPTLU
searchResult := searchResult "LEFT JOIN PRODUKTION.TLU_BT_LOGISTIK_ZU ON TLU_TLPID = TLP_TLPID" "~"
if joinKuerzel = PGZTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = PGZ_TMO_FIRMENNR AND TMO_TAGESMODELLNR = PGZ_TMO_TAGESMODELLNR" "~"
if joinKuerzel = TMOPGZ
searchResult := searchResult "LEFT JOIN BUERO.PGZ_PLANGRUPPETMOZU ON PGZ_TMO_FIRMENNR = TMO_FIRMENNR AND PGZ_TMO_TAGESMODELLNR = TMO_TAGESMODELLNR" "~"
if joinKuerzel = PLGTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = PLG_FREI_TMO_FIRMENNR AND TMO_TAGESMODELLNR = PLG_FREI_TAGESMODELLNR" "~"
if joinKuerzel = TMOPLG
searchResult := searchResult "LEFT JOIN BUERO.PLG_PLANGRUPPE ON PLG_FREI_TMO_FIRMENNR = TMO_FIRMENNR AND PLG_FREI_TAGESMODELLNR = TMO_TAGESMODELLNR" "~"
if joinKuerzel = ZBUTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = ZBU_ASW_FIRMENNR AND TMO_TAGESMODELLNR = ZBU_TAGESMODELLNR" "~"
if joinKuerzel = TMOZBU
searchResult := searchResult "LEFT JOIN BUERO.ZBU_ZEITBUCHUNG ON ZBU_ASW_FIRMENNR = TMO_FIRMENNR AND ZBU_TAGESMODELLNR = TMO_TAGESMODELLNR" "~"
if joinKuerzel = TMOTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = TMO_FIRMENNR AND TMO_TAGESMODELLNR = TMO_FEIERTAG_TAGESMODELLNR" "~"
if joinKuerzel = TMOTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = TMO_FIRMENNR AND TMO_FEIERTAG_TAGESMODELLNR = TMO_TAGESMODELLNR" "~"
if joinKuerzel = WMDTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = WMD_FIRMENNR AND TMO_TAGESMODELLNR = WMD_TAGESMODELLNR1" "~"
if joinKuerzel = TMOWMD
searchResult := searchResult "LEFT JOIN BUERO.WMD_WOCHENMODELL ON WMD_FIRMENNR = TMO_FIRMENNR AND WMD_TAGESMODELLNR1 = TMO_TAGESMODELLNR" "~"
if joinKuerzel = LFSTOU
searchResult := searchResult "LEFT JOIN WWS.TOU_TOUR ON TOU_FIRMENNR = LFS_TOU_FIRMENNR AND TOU_TOURNR = LFS_TOU_TOURNR" "~"
if joinKuerzel = TOULFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_TOU_FIRMENNR = TOU_FIRMENNR AND LFS_TOU_TOURNR = TOU_TOURNR" "~"
if joinKuerzel = RAPTOU
searchResult := searchResult "LEFT JOIN WWS.TOU_TOUR ON TOU_FIRMENNR = RAP_TOU_FIRMENNR AND TOU_TOURNR = RAP_TOU_TOURNR" "~"
if joinKuerzel = TOURAP
searchResult := searchResult "LEFT JOIN WWS.RAP_RABATTTABPOS ON RAP_TOU_FIRMENNR = TOU_FIRMENNR AND RAP_TOU_TOURNR = TOU_TOURNR" "~"
if joinKuerzel = TFTTOU
searchResult := searchResult "LEFT JOIN WWS.TOU_TOUR ON TOU_FIRMENNR = TFT_TOU_AUF_FIRMENNR AND TOU_TOURNR = TFT_TOU_AUF_TOURNR" "~"
if joinKuerzel = TOUTFT
searchResult := searchResult "LEFT JOIN WWS.TFT_TOURFEIERTAG ON TFT_TOU_AUF_FIRMENNR = TOU_FIRMENNR AND TFT_TOU_AUF_TOURNR = TOU_TOURNR" "~"
if joinKuerzel = KASTSK
searchResult := searchResult "LEFT JOIN WWS.TSK_TOUCHSCREENKOPF ON TSK_TOUCHSCREENID = KAS_TSK_ID_RE" "~"
if joinKuerzel = TSKKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_TSK_ID_RE = TSK_TOUCHSCREENID" "~"
if joinKuerzel = KASTSK
searchResult := searchResult "LEFT JOIN WWS.TSK_TOUCHSCREENKOPF ON TSK_TOUCHSCREENID = KAS_TSK_ID_KAE_MI" "~"
if joinKuerzel = TSKKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_TSK_ID_KAE_MI = TSK_TOUCHSCREENID" "~"
if joinKuerzel = KASTSK
searchResult := searchResult "LEFT JOIN WWS.TSK_TOUCHSCREENKOPF ON TSK_TOUCHSCREENID = KAS_TSK_ID_KAP_UN" "~"
if joinKuerzel = TSKKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_TSK_ID_KAP_UN = TSK_TOUCHSCREENID" "~"
if joinKuerzel = UPLUPH
searchResult := searchResult "LEFT JOIN BMDENTW.UPH_UPDATEHINWEIS ON UPH_SCRNR = UPL_SCRNR" "~"
if joinKuerzel = UPHUPL
searchResult := searchResult "LEFT JOIN BMDENTW.UPL_UPDATEHINWEIS_NLS ON UPL_SCRNR = UPH_SCRNR" "~"
if joinKuerzel = VAHVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_FIRMENNR = VAH_VAS_ABHAENGIG_FIRMENNR AND VAS_VSTAMMNR = VAH_VAS_ABHAENGIG_VSTAMMNR" "~"
if joinKuerzel = VASVAH
searchResult := searchResult "LEFT JOIN BUERO.VAH_VERANSTABHAENGIG ON VAH_VAS_ABHAENGIG_FIRMENNR = VAS_FIRMENNR AND VAH_VAS_ABHAENGIG_VSTAMMNR = VAS_VSTAMMNR" "~"
if joinKuerzel = KJOVAT
searchResult := searchResult "LEFT JOIN BUERO.VAT_TEILNAHME ON VAT_FIRMENNR = KJO_VAT_FIRMENNR AND VAT_VSTAMMNR = KJO_VSTAMMNR AND VAT_VERANSTALTUNGSNR = KJO_VERANSTALTUNGSNR AND VAT_TEILNAHMENR = KJO_TEILNAHMENR" "~"
if joinKuerzel = VATKJO
searchResult := searchResult "LEFT JOIN BUERO.KJO_KONTAKTJOURNAL ON KJO_VAT_FIRMENNR = VAT_FIRMENNR AND KJO_VSTAMMNR = VAT_VSTAMMNR AND KJO_VERANSTALTUNGSNR = VAT_VERANSTALTUNGSNR AND KJO_TEILNAHMENR = VAT_TEILNAHMENR" "~"
if joinKuerzel = MABVAT
searchResult := searchResult "LEFT JOIN BUERO.VAT_TEILNAHME ON VAT_FIRMENNR = MAB_VAT_FIRMENNR AND VAT_VSTAMMNR = MAB_VAT_VSTAMMNR AND VAT_VERANSTALTUNGSNR = MAB_VAT_VERANSTALTUNGSNR AND VAT_TEILNAHMENR = MAB_VAT_TEILNAHMENR" "~"
if joinKuerzel = VATMAB
searchResult := searchResult "LEFT JOIN BUERO.MAB_MITARBAUSBILDUNG ON MAB_VAT_FIRMENNR = VAT_FIRMENNR AND MAB_VAT_VSTAMMNR = VAT_VSTAMMNR AND MAB_VAT_VERANSTALTUNGSNR = VAT_VERANSTALTUNGSNR AND MAB_VAT_TEILNAHMENR = VAT_TEILNAHMENR" "~"
if joinKuerzel = VVZVET
searchResult := searchResult "LEFT JOIN BUERO.VET_VERANSTALTUNGSTAG ON VET_FIRMENNR = VVZ_VET_FIRMENNR AND VET_VSTAMMNR = VVZ_VET_VSTAMMNR AND VET_VERANSTALTUNGSNR = VVZ_VET_VERANSTALTUNGSNR AND VET_VERANSTALTUNGSTAGNR = VVZ_VET_VERANSTALTUNGSTAGNR" "~"
if joinKuerzel = VETVVZ
searchResult := searchResult "LEFT JOIN BUERO.VVZ_VTAGVORTRAGENDERZU ON VVZ_VET_FIRMENNR = VET_FIRMENNR AND VVZ_VET_VSTAMMNR = VET_VSTAMMNR AND VVZ_VET_VERANSTALTUNGSNR = VET_VERANSTALTUNGSNR AND VVZ_VET_VERANSTALTUNGSTAGNR = VET_VERANSTALTUNGSTAGNR" "~"
if joinKuerzel = EFKVEW
searchResult := searchResult "LEFT JOIN WWS.VEW_VIEW_ERFASSKOPFWERTE ON VEW_FIRMENNR = EFK_FIRMENNR AND VEW_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND VEW_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = VEWEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = VEW_FIRMENNR AND EFK_ERFASSUNGSKZ = VEW_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = VEW_ERFASSUNGSNR" "~"
if joinKuerzel = ARLVHE
searchResult := searchResult "LEFT JOIN WWS.VHE_VA_HAUPTEANNUMMERN ON VHE_FIRMENNR = ARL_FIRMENNR AND VHE_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = VHEARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = VHE_FIRMENNR AND ARL_ARTIKELNR = VHE_ARTIKELNR" "~"
if joinKuerzel = EFKVKI
searchResult := searchResult "LEFT JOIN KORE.VKI_VIEW_KOSTAMM_DIM ON VKI_FIRMENNR = EFK_FIRMENNR AND VKI_KOSTENSTAMMNR = EFK_DIM_KOSTENSTAMMNR" "~"
if joinKuerzel = VKIEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = VKI_FIRMENNR AND EFK_DIM_KOSTENSTAMMNR = VKI_KOSTENSTAMMNR" "~"
if joinKuerzel = VPKVPE
searchResult := searchResult "LEFT JOIN WWS.VPE_VPI_ERHOEHUNG ON VPE_VPEID = VPK_VPEID" "~"
if joinKuerzel = VPEVPK
searchResult := searchResult "LEFT JOIN WWS.VPK_VPERMITTLUNG_KUGZUORD ON VPK_VPEID = VPE_VPEID" "~"
if joinKuerzel = BLSVPF
searchResult := searchResult "LEFT JOIN BMD.VPF_VPFAD ON VPF_VPFADNR = BLS_VPFADNR" "~"
if joinKuerzel = VPFBLS
searchResult := searchResult "LEFT JOIN BMD.BLS_BLOB_STAMM ON BLS_VPFADNR = VPF_VPFADNR" "~"
if joinKuerzel = VPWVPI
searchResult := searchResult "LEFT JOIN BUERO.VPI_VPINDEX ON VPI_VPINDEXID = VPW_VPINDEXID" "~"
if joinKuerzel = VPIVPW
searchResult := searchResult "LEFT JOIN BUERO.VPW_VPIWERTE ON VPW_VPINDEXID = VPI_VPINDEXID" "~"
if joinKuerzel = VSRVSK
searchResult := searchResult "LEFT JOIN FIBU.VSK_VSTPAKET ON VSK_FIRMENNR = VSR_FIRMENNR AND VSK_FIBUNR = VSR_FIBUNR AND VSK_VSKPAKETID = VSR_VSKPAKETID" "~"
if joinKuerzel = VSKVSR
searchResult := searchResult "LEFT JOIN FIBU.VSR_VSTERSTATTUNG ON VSR_FIRMENNR = VSK_FIRMENNR AND VSR_FIBUNR = VSK_FIBUNR AND VSR_VSKPAKETID = VSK_VSKPAKETID" "~"
if joinKuerzel = VSTVST
searchResult := searchResult "LEFT JOIN WWS.VST_VERKAUFSSTATISTIK ON VST_VSTATISTIKID = VST_SET_VSTATISTIKID" "~"
if joinKuerzel = VSTVST
searchResult := searchResult "LEFT JOIN WWS.VST_VERKAUFSSTATISTIK ON VST_SET_VSTATISTIKID = VST_VSTATISTIKID" "~"
if joinKuerzel = VSWVSW
searchResult := searchResult "LEFT JOIN WWS.VSW_VERTRETERSTATISTIK ON VSW_VTSTATISTIKID = VSW_SET_VTSTATISTIKID" "~"
if joinKuerzel = VSWVSW
searchResult := searchResult "LEFT JOIN WWS.VSW_VERTRETERSTATISTIK ON VSW_SET_VTSTATISTIKID = VSW_VTSTATISTIKID" "~"
if joinKuerzel = PERVWK
searchResult := searchResult "LEFT JOIN WWS.VWK_VA_KUNDENSTATWJSUM ON VWK_KLI_FIRMENNR = PER_FIRMENNR AND VWK_KLID = PER_PERSONENID" "~"
if joinKuerzel = VWKPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = VWK_KLI_FIRMENNR AND PER_PERSONENID = VWK_KLID" "~"
if joinKuerzel = WAPVWR
searchResult := searchResult "LEFT JOIN WWS.VWR_VA_WEINGPOS_ERINFO ON VWR_WARENEINGKOPFID = WAP_WARENEINGKOPFID AND VWR_WARENEINGPOSLFDNR = WAP_WARENEINGPOSLFDNR" "~"
if joinKuerzel = VWRWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_WARENEINGKOPFID = VWR_WARENEINGKOPFID AND WAP_WARENEINGPOSLFDNR = VWR_WARENEINGPOSLFDNR" "~"
if joinKuerzel = VERVZW
searchResult := searchResult "LEFT JOIN BUERO.VZW_VERKEHRSZWEIG ON VZW_FIRMENNR = VER_VZW_FIRMENNR AND VZW_VZWCODE = VER_VZWCODE" "~"
if joinKuerzel = VZWVER
searchResult := searchResult "LEFT JOIN BUERO.VER_VERSANDART ON VER_VZW_FIRMENNR = VZW_FIRMENNR AND VER_VZWCODE = VZW_VZWCODE" "~"
if joinKuerzel = BUCWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = BUC_WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_WAE_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = BVOWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = BVO_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = EFKWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = EFK_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = EXTWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = EXT_FWAEHRUNGSCODE" "~"
if joinKuerzel = WAEEXT
searchResult := searchResult "LEFT JOIN FIBU.EXT_EXTJOURNAL ON EXT_FWAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = FWKWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = FWK_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = KAKWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = KAK_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = LANWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = LAN_WAEHRUNGSCODE" "~"
if joinKuerzel = WAELAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = MVAWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = MVA_WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEMVA
searchResult := searchResult "LEFT JOIN FIBU.MVA_MAHNVZINSENAB ON MVA_WAE_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = VEBWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = VEB_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEVEB
searchResult := searchResult "LEFT JOIN FIBU.VEB_VORERFASSBUCHUNG ON VEB_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = WEPWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = WEP_WAEHRUNGSCODE_ERW" "~"
if joinKuerzel = WAEWEP
searchResult := searchResult "LEFT JOIN WWS.WEP_WWSERKONTROLLEPOS ON WEP_WAEHRUNGSCODE_ERW = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = WKRWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = WKR_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEWKR
searchResult := searchResult "LEFT JOIN WWS.WKR_WARENAUSGKOSTENRECH ON WKR_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = WRKWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = WRK_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEWRK
searchResult := searchResult "LEFT JOIN WWS.WRK_WWSANKAUFSRNGKOPF ON WRK_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = ZASWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = ZAS_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEZAS
searchResult := searchResult "LEFT JOIN BUERO.ZAS_ZAHLSCHEIN ON ZAS_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = ZSUWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = ZSU_WAECODE" "~"
if joinKuerzel = WAEZSU
searchResult := searchResult "LEFT JOIN FIBU.ZSU_ZAHLSUMME ON ZSU_WAECODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = RNPRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = RNP_FIRMENNR AND RNG_RECHNUNGLFDNR = RNP_RECHNUNGLFDNR" "~"
if joinKuerzel = RNGRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = RNG_FIRMENNR AND RNP_RECHNUNGLFDNR = RNG_RECHNUNGLFDNR" "~"
if joinKuerzel = TRJRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = TRJ_BUCHUNGS_FIRMENNR AND RNG_RECHNUNGLFDNR = TRJ_RECHNUNGLFDNR" "~"
if joinKuerzel = RNGTRJ
searchResult := searchResult "LEFT JOIN WWS.TRJ_TRANSPORTEINH_JOURNAL ON TRJ_BUCHUNGS_FIRMENNR = RNG_FIRMENNR AND TRJ_RECHNUNGLFDNR = RNG_RECHNUNGLFDNR" "~"
if joinKuerzel = VAZRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = VAZ_FIRMENNR AND RNG_RECHNUNGLFDNR = VAZ_RECHNUNGLFDNR" "~"
if joinKuerzel = RNGVAZ
searchResult := searchResult "LEFT JOIN WWS.VAZ_VERTABRZAHLUNGEN ON VAZ_FIRMENNR = RNG_FIRMENNR AND VAZ_RECHNUNGLFDNR = RNG_RECHNUNGLFDNR" "~"
if joinKuerzel = RNGRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = RNG_FIRMENNR AND RNG_RECHNUNGLFDNR = RNG_URSPR_RECHNUNGLFDNR" "~"
if joinKuerzel = RNGRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = RNG_FIRMENNR AND RNG_URSPR_RECHNUNGLFDNR = RNG_RECHNUNGLFDNR" "~"
if joinKuerzel = LPORNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = LPO_FIRMENNR AND RNP_RECHNUNGLFDNR = LPO_RECHNUNGLFDNR AND RNP_RECHNUNGSPOSLFDNR = LPO_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = RNPLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_FIRMENNR = RNP_FIRMENNR AND LPO_RECHNUNGLFDNR = RNP_RECHNUNGLFDNR AND LPO_RECHNUNGSPOSLFDNR = RNP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = WEPRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = WEP_RNP_FIRMENNR AND RNP_RECHNUNGLFDNR = WEP_RECHNUNGLFDNR AND RNP_RECHNUNGSPOSLFDNR = WEP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = RNPWEP
searchResult := searchResult "LEFT JOIN WWS.WEP_WWSERKONTROLLEPOS ON WEP_RNP_FIRMENNR = RNP_FIRMENNR AND WEP_RECHNUNGLFDNR = RNP_RECHNUNGLFDNR AND WEP_RECHNUNGSPOSLFDNR = RNP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = BEWRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = BEW_REPORTNR" "~"
if joinKuerzel = RPTBEW
searchResult := searchResult "LEFT JOIN BUERO.BEW_BEWERTUNGSMODELL ON BEW_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = HNPRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = HNP_REPORTNR" "~"
if joinKuerzel = RPTHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = MRVRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = MRV_REPORTNR" "~"
if joinKuerzel = RPTMRV
searchResult := searchResult "LEFT JOIN ZLE.MRV_MONATSRECHNUNGVORLAGE ON MRV_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = SRERPT
searchResult := searchResult "LEFT JOIN BMD.RPT_REPORT ON RPT_REPORTNR = SRE_REPORTNR" "~"
if joinKuerzel = RPTSRE
searchResult := searchResult "LEFT JOIN BMD.SRE_STANDARDREPORT ON SRE_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = ZASRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = ZAS_REPORTNR" "~"
if joinKuerzel = RPTZAS
searchResult := searchResult "LEFT JOIN BUERO.ZAS_ZAHLSCHEIN ON ZAS_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = AUERPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = AUE_STDFUSS_REPORTNR" "~"
if joinKuerzel = RPTAUE
searchResult := searchResult "LEFT JOIN ZLE.AUE_AUSWERTTYPEINST ON AUE_STDFUSS_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = JABRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = JAB_FUSS_REPORTNR" "~"
if joinKuerzel = RPTJAB
searchResult := searchResult "LEFT JOIN BIL.JAB_JABINFO ON JAB_FUSS_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = ZAWRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = ZAW_LEGENDE_REPORTNR" "~"
if joinKuerzel = RPTZAW
searchResult := searchResult "LEFT JOIN BUERO.ZAW_ZEITAUSWERTUNG ON ZAW_LEGENDE_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = KASRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = KAS_ASCHEIN_REPORTNR" "~"
if joinKuerzel = RPTKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_ASCHEIN_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = KASRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = KAS_FISKALBON_REPORTNR" "~"
if joinKuerzel = RPTKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_FISKALBON_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = RSKRSD
searchResult := searchResult "LEFT JOIN WWS.RSD_REGKASPROTOKOLL ON RSD_REGKASPROTOKOLLID = RSK_RSD_MONATSABSCHLUSS" "~"
if joinKuerzel = RSDRSK
searchResult := searchResult "LEFT JOIN WWS.RSK_REGISTRIERKASSE ON RSK_RSD_MONATSABSCHLUSS = RSD_REGKASPROTOKOLLID" "~"
if joinKuerzel = RSKRSD
searchResult := searchResult "LEFT JOIN WWS.RSD_REGKASPROTOKOLL ON RSD_REGKASPROTOKOLLID = RSK_RSD_BELEGPRUEFUNG" "~"
if joinKuerzel = RSDRSK
searchResult := searchResult "LEFT JOIN WWS.RSK_REGISTRIERKASSE ON RSK_RSD_BELEGPRUEFUNG = RSD_REGKASPROTOKOLLID" "~"
if joinKuerzel = KASRSK
searchResult := searchResult "LEFT JOIN WWS.RSK_REGISTRIERKASSE ON RSK_FIRMENNR = KAS_RSK_FIRMENNR AND RSK_KASSENID = KAS_RSK_KASSENID" "~"
if joinKuerzel = RSKKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_RSK_FIRMENNR = RSK_FIRMENNR AND KAS_RSK_KASSENID = RSK_KASSENID" "~"
if joinKuerzel = HNPRVK
searchResult := searchResult "LEFT JOIN ZLE.RVK_RECHVORLAGEKOPF ON RVK_RVKID = HNP_RVKID" "~"
if joinKuerzel = RVKHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_RVKID = RVK_RVKID" "~"
if joinKuerzel = RVDRVK
searchResult := searchResult "LEFT JOIN ZLE.RVK_RECHVORLAGEKOPF ON RVK_RVKID = RVD_RVKID" "~"
if joinKuerzel = RVKRVD
searchResult := searchResult "LEFT JOIN ZLE.RVD_RECHVORLAGEDETAIL ON RVD_RVKID = RVK_RVKID" "~"
if joinKuerzel = SA2SA1
searchResult := searchResult "LEFT JOIN ONVIS.SA1_SAURER_BDE ON SA1_FIRMENNR = SA2_FIRMENNR AND SA1_SAURERID = SA2_SAURERID" "~"
if joinKuerzel = SA1SA2
searchResult := searchResult "LEFT JOIN ONVIS.SA2_SAURER_BDE ON SA2_FIRMENNR = SA1_FIRMENNR AND SA2_SAURERID = SA1_SAURERID" "~"
if joinKuerzel = FZPSAK
searchResult := searchResult "LEFT JOIN FIBU.SAK_SACHKONTO ON SAK_FIRMENNR = FZP_FIRMENNR AND SAK_FIBUNR = FZP_FIBUNR AND SAK_KONTONR = FZP_KONTONR" "~"
if joinKuerzel = SAKFZP
searchResult := searchResult "LEFT JOIN FIBU.FZP_FINANZPLAN ON FZP_FIRMENNR = SAK_FIRMENNR AND FZP_FIBUNR = SAK_FIBUNR AND FZP_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = DBXXSP
searchResult := searchResult "LEFT JOIN BUERO.XSP_XML_STEUERERKL_PAKET ON XSP_PAKETID = DBX_PAKETID" "~"
if joinKuerzel = XSPDBX
searchResult := searchResult "LEFT JOIN BUERO.DBX_FINONDATABOX ON DBX_PAKETID = XSP_PAKETID" "~"
if joinKuerzel = ZBKZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_FIRMENNR = ZBK_ZBA_FIRMENNR AND ZBA_ZBUCHARTNR = ZBK_ZBA_ZBUCHARTNR" "~"
if joinKuerzel = ZBAZBK
searchResult := searchResult "LEFT JOIN BUERO.ZBK_ZEITBETRIEBSKALENDER ON ZBK_ZBA_FIRMENNR = ZBA_FIRMENNR AND ZBK_ZBA_ZBUCHARTNR = ZBA_ZBUCHARTNR" "~"
if joinKuerzel = STTZBE
searchResult := searchResult "LEFT JOIN ZBE_ZUSTAENDIGKEITBEREICH.ZBE_ZUSTAENDIGKEITBEREICH ON ZBE_ZUSTBEREICHLFDNR = STT_ZBE_ZUSTBEREICHLFDNR" "~"
if joinKuerzel = ZBESTT
searchResult := searchResult "LEFT JOIN PRODUKTION.STT_STAKE_TYP ON STT_ZBE_ZUSTBEREICHLFDNR = ZBE_ZUSTBEREICHLFDNR" "~"
if joinKuerzel = ZMAZBG
searchResult := searchResult "LEFT JOIN BUERO.ZBG_ZEITBABERECHTGRUPPE ON ZBG_FIRMENNR = ZMA_ZBG_FIRMENNR AND ZBG_BAGRUPPNR = ZMA_BAGRUPPNR" "~"
if joinKuerzel = ZBGZMA
searchResult := searchResult "LEFT JOIN BUERO.ZMA_ZEITBABERECHTMAZUORD ON ZMA_ZBG_FIRMENNR = ZBG_FIRMENNR AND ZMA_BAGRUPPNR = ZBG_BAGRUPPNR" "~"
if joinKuerzel = LEIZBU
searchResult := searchResult "LEFT JOIN ZBU_ZEITBUCHUNG.ZBU_ZEITBUCHUNG ON ZBU_BUCHUNG_ID = LEI_BUCHUNG_ID" "~"
if joinKuerzel = ZBULEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_BUCHUNG_ID = ZBU_BUCHUNG_ID" "~"
if joinKuerzel = ZSGZBZ
searchResult := searchResult "LEFT JOIN BUERO.ZBZ_ZEITBEZUGSART ON ZBZ_FIRMENNR = ZSG_ZBZ_FIRMENNR AND ZBZ_ZBEZUGSARTNR = ZSG_ZBEZUGSARTNR" "~"
if joinKuerzel = ZBZZSG
searchResult := searchResult "LEFT JOIN BUERO.ZSG_ZEITSALDENGRENZWERT ON ZSG_ZBZ_FIRMENNR = ZBZ_FIRMENNR AND ZSG_ZBEZUGSARTNR = ZBZ_ZBEZUGSARTNR" "~"
if joinKuerzel = ZLBZBZ
searchResult := searchResult "LEFT JOIN BUERO.ZBZ_ZEITBEZUGSART ON ZBZ_FIRMENNR = ZLB_FIRMENNR AND ZBZ_ZBEZUGSARTNR = ZLB_ZBEZUGSARTNR" "~"
if joinKuerzel = ZBZZLB
searchResult := searchResult "LEFT JOIN BUERO.ZLB_ZBEZARTLOHNBLOCKSORT ON ZLB_FIRMENNR = ZBZ_FIRMENNR AND ZLB_ZBEZUGSARTNR = ZBZ_ZBEZUGSARTNR" "~"
if joinKuerzel = ZUZZBZ
searchResult := searchResult "LEFT JOIN BUERO.ZBZ_ZEITBEZUGSART ON ZBZ_FIRMENNR = ZUZ_ZBZ_FIRMENNR AND ZBZ_ZBEZUGSARTNR = ZUZ_ZBEZUGSARTNR" "~"
if joinKuerzel = ZBZZUZ
searchResult := searchResult "LEFT JOIN BUERO.ZUZ_ZEITUMBUCHZUORD ON ZUZ_ZBZ_FIRMENNR = ZBZ_FIRMENNR AND ZUZ_ZBEZUGSARTNR = ZBZ_ZBEZUGSARTNR" "~"
if joinKuerzel = ZIZZIT
searchResult := searchResult "LEFT JOIN BUERO.ZIT_ZUSATZINFOTYP ON ZIT_FIRMENNR = ZIZ_ZIT_FIRMENNR AND ZIT_TYPNR = ZIZ_ZIT_TYPNR" "~"
if joinKuerzel = ZITZIZ
searchResult := searchResult "LEFT JOIN BUERO.ZIZ_ZUSATZINFOZUORD ON ZIZ_ZIT_FIRMENNR = ZIT_FIRMENNR AND ZIZ_ZIT_TYPNR = ZIT_TYPNR" "~"
if joinKuerzel = ZSUZKR
searchResult := searchResult "LEFT JOIN FIBU.ZKR_ZAHLUNGSKREIS ON ZKR_FIRMENNR = ZSU_FIRMENNR AND ZKR_FIBUNR = ZSU_FIBUNR AND ZKR_ZAHLUNGSKREISLFDNR = ZSU_ZAHLKREISLFDNR" "~"
if joinKuerzel = ZKRZSU
searchResult := searchResult "LEFT JOIN FIBU.ZSU_ZAHLSUMME ON ZSU_FIRMENNR = ZKR_FIRMENNR AND ZSU_FIBUNR = ZKR_FIBUNR AND ZSU_ZAHLKREISLFDNR = ZKR_ZAHLUNGSKREISLFDNR" "~"
if joinKuerzel = HNPZRT
searchResult := searchResult "LEFT JOIN ZRT_ZAHLUNGSART.ZRT_ZAHLUNGSART ON ZRT_ZAHLARTNR = HNP_ZAHLARTNR" "~"
if joinKuerzel = ZRTHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_ZAHLARTNR = ZRT_ZAHLARTNR" "~"
if joinKuerzel = SKLZRT
searchResult := searchResult "LEFT JOIN ZRT_ZAHLUNGSART.ZRT_ZAHLUNGSART ON ZRT_ZAHLARTNR = SKL_ZAHLARTNR" "~"
if joinKuerzel = ZRTSKL
searchResult := searchResult "LEFT JOIN WWS.SKL_STUECKELUNGSLISTE ON SKL_ZAHLARTNR = ZRT_ZAHLARTNR" "~"
if joinKuerzel = ZVTZVB
searchResult := searchResult "LEFT JOIN BUERO.ZVB_TBBEZEICHNUNGSZWECK ON ZVB_LANDNR = ZVT_LANDNR AND ZVB_TBFILEFORMAT = ZVT_TBFILEFORMAT" "~"
if joinKuerzel = ZVBZVT
searchResult := searchResult "LEFT JOIN BUERO.ZVT_TBDATEIEN ON ZVT_LANDNR = ZVB_LANDNR AND ZVT_TBFILEFORMAT = ZVB_TBFILEFORMAT" "~"
if joinKuerzel = MITPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = MIT_FIRMENNR AND PER_PERSONENID = MIT_MITARBEITERID" "~"
if joinKuerzel = PERMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = PER_FIRMENNR AND MIT_MITARBEITERID = PER_PERSONENID" "~"
if joinKuerzel = PERKIV
searchResult := searchResult "LEFT JOIN BUERO.KIV_V_KLINFO ON KIV_FIRMENNR = PER_FIRMENNR AND KIV_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = KIVPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = KIV_FIRMENNR AND PER_PERSONENID = KIV_PERSONENID" "~"
if joinKuerzel = AAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AAK_LETZTERUSER" "~"
if joinKuerzel = BMUAAK
searchResult := searchResult "LEFT JOIN WWS.AAK_ARAKOPF ON AAK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AAZ_LETZTERUSER" "~"
if joinKuerzel = BMUAAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.AAZ_AUFARBEITARTIKELZU ON AAZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABG_LETZTERUSER" "~"
if joinKuerzel = BMUABG
searchResult := searchResult "LEFT JOIN HRS.ABG_ABRECHNUNG ON ABG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABM_LETZTERUSER" "~"
if joinKuerzel = BMUABM
searchResult := searchResult "LEFT JOIN HRS.ABM_ABWESENHEIT ON ABM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABZ_LETZTERUSER" "~"
if joinKuerzel = BMUABZ
searchResult := searchResult "LEFT JOIN BUERO.ABZ_ARCHIVBAUMKOPFZU ON ABZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ADRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ADR_LETZTERUSER" "~"
if joinKuerzel = BMUADR
searchResult := searchResult "LEFT JOIN BUERO.ADR_ADRESSE ON ADR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AFSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AFS_LETZTERUSER" "~"
if joinKuerzel = BMUAFS
searchResult := searchResult "LEFT JOIN BUERO.AFS_AUFGABENSTATUS ON AFS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WFPWFT
searchResult := searchResult "LEFT JOIN WEB.WFT_WEBFARBENTAB ON WFT_FIRMENNR = WFP_FIRMENNR AND WFT_WTYP = WFP_WTYP AND WFT_WFARBENTABNR = WFP_WFARBENTABNR" "~"
if joinKuerzel = WFTWFP
searchResult := searchResult "LEFT JOIN WEB.WFP_WEBFARBENPOS ON WFP_FIRMENNR = WFT_FIRMENNR AND WFP_WTYP = WFT_WTYP AND WFP_WFARBENTABNR = WFT_WFARBENTABNR" "~"
if joinKuerzel = AKTWKG
searchResult := searchResult "LEFT JOIN WWS.WKG_WWSKUNDENGRUPPE ON WKG_FIRMENNR = AKT_FIRMENNR AND WKG_KUNDENHAUPTGRUPPENNR = AKT_KUNDENHAUPTGRUPPENNR AND WKG_KUNDENUNTERGRUPPENNR = AKT_KUNDENUNTERGRUPPENNR" "~"
if joinKuerzel = WKGAKT
searchResult := searchResult "LEFT JOIN WWS.AKT_AKTIONSSTAMM ON AKT_FIRMENNR = WKG_FIRMENNR AND AKT_KUNDENHAUPTGRUPPENNR = WKG_KUNDENHAUPTGRUPPENNR AND AKT_KUNDENUNTERGRUPPENNR = WKG_KUNDENUNTERGRUPPENNR" "~"
if joinKuerzel = QMAWKG
searchResult := searchResult "LEFT JOIN WKG_WWSKUNDENGRUPPE.WKG_WWSKUNDENGRUPPE ON WKG_FIRMENNR = QMA_LIEFERANT_FIRMENNR AND WKG_KUNDENHAUPTGRUPPENNR = QMA_LIEFERANT_HPTGRP AND WKG_KUNDENUNTERGRUPPENNR = QMA_LIEFERANT_UNTGRP" "~"
if joinKuerzel = WKGQMA
searchResult := searchResult "LEFT JOIN PRODUKTION.QMA_QM_ARTIKEL ON QMA_LIEFERANT_FIRMENNR = WKG_FIRMENNR AND QMA_LIEFERANT_HPTGRP = WKG_KUNDENHAUPTGRUPPENNR AND QMA_LIEFERANT_UNTGRP = WKG_KUNDENUNTERGRUPPENNR" "~"
if joinKuerzel = FRTWKT
searchResult := searchResult "LEFT JOIN WKT_WWSKONTENTAB.WKT_WWSKONTENTAB ON WKT_FIRMENNR = FRT_WKT_FIRMENNR AND WKT_KONTENTABNR = FRT_WKT_KONTENTABNR" "~"
if joinKuerzel = WKTFRT
searchResult := searchResult "LEFT JOIN ZLE.FRT_FAKTURATYP ON FRT_WKT_FIRMENNR = WKT_FIRMENNR AND FRT_WKT_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = RRDWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = RRD_HAUPTFIRMENNR AND WKT_KONTENTABNR = RRD_KONTENTABNR" "~"
if joinKuerzel = WKTRRD
searchResult := searchResult "LEFT JOIN WWS.RRD_RECHNUNGSRABATTDETAIL ON RRD_HAUPTFIRMENNR = WKT_FIRMENNR AND RRD_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = WKAWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = WKA_WKT_FIRMENNR AND WKT_KONTENTABNR = WKA_WKT_KONTENTABNR" "~"
if joinKuerzel = WKTWKA
searchResult := searchResult "LEFT JOIN WWS.WKA_WARENAUSGKOSTENARTEN ON WKA_WKT_FIRMENNR = WKT_FIRMENNR AND WKA_WKT_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = DEZWKU
searchResult := searchResult "LEFT JOIN WKU_WWSKUNDE.WKU_WWSKUNDE ON WKU_FIRMENNR = DEZ_KLI_FIRMENNR AND WKU_KLID = DEZ_KLID" "~"
if joinKuerzel = WKUDEZ
searchResult := searchResult "LEFT JOIN DATANORM.DEZ_DATANORM_ERSTELLERZU ON DEZ_KLI_FIRMENNR = WKU_FIRMENNR AND DEZ_KLID = WKU_KLID" "~"
if joinKuerzel = FIRWMD
searchResult := searchResult "LEFT JOIN BUERO.WMD_WOCHENMODELL ON WMD_FIRMENNR = FIR_WMD_FIRMENNR AND WMD_WOCHENMODELLNR = FIR_WOCHENMODELLNR" "~"
if joinKuerzel = WMDFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_WMD_FIRMENNR = WMD_FIRMENNR AND FIR_WOCHENMODELLNR = WMD_WOCHENMODELLNR" "~"
if joinKuerzel = PKSWMD
searchResult := searchResult "LEFT JOIN WMD_WOCHENMODELL.WMD_WOCHENMODELL ON WMD_FIRMENNR = PKS_WMD_FIRMENNR AND WMD_WOCHENMODELLNR = PKS_WOCHENMODELL" "~"
if joinKuerzel = WMDPKS
searchResult := searchResult "LEFT JOIN PRODUKTION.PKS_SOLL_KAPAZITAET ON PKS_WMD_FIRMENNR = WMD_FIRMENNR AND PKS_WOCHENMODELL = WMD_WOCHENMODELLNR" "~"
if joinKuerzel = PKSWMD
searchResult := searchResult "LEFT JOIN WMD_WOCHENMODELL.WMD_WOCHENMODELL ON WMD_FIRMENNR = PKS_WMD_FIRMENNR_QUELLE AND WMD_WOCHENMODELLNR = PKS_WOCHENMODELL_QUELLE" "~"
if joinKuerzel = WMDPKS
searchResult := searchResult "LEFT JOIN PRODUKTION.PKS_SOLL_KAPAZITAET ON PKS_WMD_FIRMENNR_QUELLE = WMD_FIRMENNR AND PKS_WOCHENMODELL_QUELLE = WMD_WOCHENMODELLNR" "~"
if joinKuerzel = WQUWQI
searchResult := searchResult "LEFT JOIN WQI_WEITERE_INFO_ITEM.WQI_WEITERE_INFO_ITEM ON WQI_WIIID = WQU_WIIID" "~"
if joinKuerzel = WQIWQU
searchResult := searchResult "LEFT JOIN BMD.WQU_WQI_BMU_ZU ON WQU_WIIID = WQI_WIIID" "~"
if joinKuerzel = WARWRP
searchResult := searchResult "LEFT JOIN WWS.WRP_WWSANKAUFSRNGPOS ON WRP_FIRMENNR = WAR_WRKFIRMENNR AND WRP_RECHNUNGLFDNR = WAR_RECHNUNGLFDNR AND WRP_RECHNUNGSPOSLFDNR = WAR_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = WRPWAR
searchResult := searchResult "LEFT JOIN WWS.WAR_WARENEINGANGRABATTE ON WAR_WRKFIRMENNR = WRP_FIRMENNR AND WAR_RECHNUNGLFDNR = WRP_RECHNUNGLFDNR AND WAR_RECHNUNGSPOSLFDNR = WRP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = ADWWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = ADW_HAUPTFIRMENNR AND WTB_WWSTEXTBNR = ADW_WWSTEXTBNR_BEIBETRAG0" "~"
if joinKuerzel = WTBADW
searchResult := searchResult "LEFT JOIN WWS.ADW_ABGABENDETAILWAP ON ADW_HAUPTFIRMENNR = WTB_FIRMENNR AND ADW_WWSTEXTBNR_BEIBETRAG0 = WTB_WWSTEXTBNR" "~"
if joinKuerzel = ARBWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = ARB_BESCHR_FIRMENNR AND WTB_WWSTEXTBNR = ARB_BESCHR_WWSTEXTBNR" "~"
if joinKuerzel = WTBARB
searchResult := searchResult "LEFT JOIN WWS.ARB_ARTIKELBEREICHSWERTE ON ARB_BESCHR_FIRMENNR = WTB_FIRMENNR AND ARB_BESCHR_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = ARLWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = ARL_FIRMENNR AND WTB_WWSTEXTBNR = ARL_KURZBESCHR_WWSTEXTBNR" "~"
if joinKuerzel = WTBARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = WTB_FIRMENNR AND ARL_KURZBESCHR_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = BPPWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = BPP_ARL_FIRMENNR AND WTB_WWSTEXTBNR = BPP_KURZBEZ_WWSTEXTBNR" "~"
if joinKuerzel = WTBBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_ARL_FIRMENNR = WTB_FIRMENNR AND BPP_KURZBEZ_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = PCKWTB
searchResult := searchResult "LEFT JOIN WTB_WWSTEXTBAUSTEIN.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = PCK_WTB_FIRMENNR AND WTB_WWSTEXTBNR = PCK_WTB_WWSTEXTBNR" "~"
if joinKuerzel = WTBPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_WTB_FIRMENNR = WTB_FIRMENNR AND PCK_WTB_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = SRAWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = SRA_FIRMENNR AND WTB_WWSTEXTBNR = SRA_WWSTEXTBNR_BEIBETRAG0" "~"
if joinKuerzel = WTBSRA
searchResult := searchResult "LEFT JOIN WWS.SRA_STEUERART ON SRA_FIRMENNR = WTB_FIRMENNR AND SRA_WWSTEXTBNR_BEIBETRAG0 = WTB_WWSTEXTBNR" "~"
if joinKuerzel = WDAWFN
searchResult := searchResult "LEFT JOIN BUERO.WFN_WORKFLOWACTION ON WFN_WFNID = WDA_WFN_WFNID" "~"
if joinKuerzel = WFNWDA
searchResult := searchResult "LEFT JOIN BUERO.WDA_WFDAGRAMMZU ON WDA_WFN_WFNID = WFN_WFNID" "~"
if joinKuerzel = WGZWKG
searchResult := searchResult "LEFT JOIN WWS.WKG_WWSKUNDENGRUPPE ON WKG_FIRMENNR = WGZ_WKG_FIRMENNR AND WKG_KUNDENHAUPTGRUPPENNR = WGZ_WKG_KUNDENHAUPTGRNR AND WKG_KUNDENUNTERGRUPPENNR = WGZ_WKG_KUNDENUNTERGRNR" "~"
if joinKuerzel = WKGWGZ
searchResult := searchResult "LEFT JOIN WWS.WGZ_WFK_WKG_ZU ON WGZ_WKG_FIRMENNR = WKG_FIRMENNR AND WGZ_WKG_KUNDENHAUPTGRNR = WKG_KUNDENHAUPTGRUPPENNR AND WGZ_WKG_KUNDENUNTERGRNR = WKG_KUNDENUNTERGRUPPENNR" "~"
if joinKuerzel = WKRWKL
searchResult := searchResult "LEFT JOIN WWS.WKL_WARENAUSGKOSTENLIEF ON WKL_WKLID = WKR_WKLID" "~"
if joinKuerzel = WKLWKR
searchResult := searchResult "LEFT JOIN WWS.WKR_WARENAUSGKOSTENRECH ON WKR_WKLID = WKL_WKLID" "~"
if joinKuerzel = ADCWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = ADC_HAUPTFIRMENNR AND WKT_KONTENTABNR = ADC_STEUERKONTENTABNR" "~"
if joinKuerzel = WKTADC
searchResult := searchResult "LEFT JOIN WWS.ADC_ABGABENDETAILRECH ON ADC_HAUPTFIRMENNR = WKT_FIRMENNR AND ADC_STEUERKONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = ADWWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = ADW_HAUPTFIRMENNR AND WKT_KONTENTABNR = ADW_STEUERKONTENTABNR" "~"
if joinKuerzel = WKTADW
searchResult := searchResult "LEFT JOIN WWS.ADW_ABGABENDETAILWAP ON ADW_HAUPTFIRMENNR = WKT_FIRMENNR AND ADW_STEUERKONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = BAZWMD
searchResult := searchResult "LEFT JOIN WMD_WOCHENMODELL.WMD_WOCHENMODELL ON WMD_FIRMENNR = BAZ_WMD_FIRMENNR AND WMD_WOCHENMODELLNR = BAZ_WOCHENMODELLNR" "~"
if joinKuerzel = WMDBAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_WMD_FIRMENNR = WMD_FIRMENNR AND BAZ_WOCHENMODELLNR = WMD_WOCHENMODELLNR" "~"
if joinKuerzel = DVHWMD
searchResult := searchResult "LEFT JOIN BUERO.WMD_WOCHENMODELL ON WMD_FIRMENNR = DVH_WMD_FIRMENNR AND WMD_WOCHENMODELLNR = DVH_WOCHENMODELLNR" "~"
if joinKuerzel = WMDDVH
searchResult := searchResult "LEFT JOIN BUERO.DVH_DIENSTVERHAELTNIS ON DVH_WMD_FIRMENNR = WMD_FIRMENNR AND DVH_WOCHENMODELLNR = WMD_WOCHENMODELLNR" "~"
if joinKuerzel = BOPWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = BOP_WTB_KBEZ_FIRMENNR AND WTB_WWSTEXTBNR = BOP_WTB_KBEZ_WWSTEXTBNR" "~"
if joinKuerzel = WTBBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_WTB_KBEZ_FIRMENNR = WTB_FIRMENNR AND BOP_WTB_KBEZ_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = KASWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = KAS_WTB_KOPFTEXT_FIRMENNR AND WTB_WWSTEXTBNR = KAS_WTB_KOPFTEXTNR" "~"
if joinKuerzel = WTBKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_WTB_KOPFTEXT_FIRMENNR = WTB_FIRMENNR AND KAS_WTB_KOPFTEXTNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = STZWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = STZ_WTB_FIRMENNR AND WTB_WWSTEXTBNR = STZ_WWSTEXTBNR" "~"
if joinKuerzel = WTBSTZ
searchResult := searchResult "LEFT JOIN WWS.STZ_STEUERTEXTZUORDNUNG ON STZ_WTB_FIRMENNR = WTB_FIRMENNR AND STZ_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = AVEWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = AVE_KURZBESCHR_FIRMENNR AND WTB_WWSTEXTBNR = AVE_KURZBESCHR_WWSTEXTBNR" "~"
if joinKuerzel = WTBAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_KURZBESCHR_FIRMENNR = WTB_FIRMENNR AND AVE_KURZBESCHR_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = WADWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = WAD_HAUPTFIRMENNR AND WTB_WWSTEXTBNR = WAD_SCHLUSSTEXT_WWSTEXTBNR" "~"
if joinKuerzel = WTBWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_HAUPTFIRMENNR = WTB_FIRMENNR AND WAD_SCHLUSSTEXT_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = ERZWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = ERZ_HAUPTFIRMENNR AND WTB_WWSTEXTBNR = ERZ_KOPF_RNG_WWSTEXTBNR" "~"
if joinKuerzel = WTBERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_HAUPTFIRMENNR = WTB_FIRMENNR AND ERZ_KOPF_RNG_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = ERZWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = ERZ_HAUPTFIRMENNR AND WTB_WWSTEXTBNR = ERZ_FUSS_RNG_WWSTEXTBNR" "~"
if joinKuerzel = WTBERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_HAUPTFIRMENNR = WTB_FIRMENNR AND ERZ_FUSS_RNG_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = WADWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = WAD_HAUPTFIRMENNR AND WTB_WWSTEXTBNR = WAD_FUSS_LFS_WWSTEXTBNR" "~"
if joinKuerzel = WTBWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_HAUPTFIRMENNR = WTB_FIRMENNR AND WAD_FUSS_LFS_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = BLFZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_FIRMENNR = BLF_ZBA_FIRMENNR1 AND ZBA_ZBUCHARTNR = BLF_ZBUCHARTNR1" "~"
if joinKuerzel = ZBABLF
searchResult := searchResult "LEFT JOIN BUERO.BLF_BLOCKLOCHFUELLER ON BLF_ZBA_FIRMENNR1 = ZBA_FIRMENNR AND BLF_ZBUCHARTNR1 = ZBA_ZBUCHARTNR" "~"
if joinKuerzel = TBZZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_FIRMENNR = TBZ_ZBA_FIRMENNR AND ZBA_ZBUCHARTNR = TBZ_ZBUCHARTNR" "~"
if joinKuerzel = ZBATBZ
searchResult := searchResult "LEFT JOIN BUERO.TBZ_TERMINALBUCHARTZUORD ON TBZ_ZBA_FIRMENNR = ZBA_FIRMENNR AND TBZ_ZBUCHARTNR = ZBA_ZBUCHARTNR" "~"
if joinKuerzel = BLFZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_FIRMENNR = BLF_ZBA_FIRMENNR2 AND ZBA_ZBUCHARTNR = BLF_ZBUCHARTNR2" "~"
if joinKuerzel = ZBABLF
searchResult := searchResult "LEFT JOIN BUERO.BLF_BLOCKLOCHFUELLER ON BLF_ZBA_FIRMENNR2 = ZBA_FIRMENNR AND BLF_ZBUCHARTNR2 = ZBA_ZBUCHARTNR" "~"
if joinKuerzel = ZTSZBZ
searchResult := searchResult "LEFT JOIN BUERO.ZBZ_ZEITBEZUGSART ON ZBZ_FIRMENNR = ZTS_ZBZ_FIRMENNR AND ZBZ_ZBEZUGSARTNR = ZTS_ZBEZUGSARTNR" "~"
if joinKuerzel = ZBZZTS
searchResult := searchResult "LEFT JOIN BUERO.ZTS_ZEITTAGESSUMMEN ON ZTS_ZBZ_FIRMENNR = ZBZ_FIRMENNR AND ZTS_ZBEZUGSARTNR = ZBZ_ZBEZUGSARTNR" "~"
if joinKuerzel = ZIAZIK
searchResult := searchResult "LEFT JOIN FIBU.ZIK_KONTENZINSKREIS ON ZIK_FIRMENNR = ZIA_FIRMENNR AND ZIK_FIBUNR = ZIA_FIBUNR AND ZIK_ZIKLFDNR = ZIA_ZIKLFDNR" "~"
if joinKuerzel = ZIKZIA
searchResult := searchResult "LEFT JOIN FIBU.ZIA_KONTENZINSAB ON ZIA_FIRMENNR = ZIK_FIRMENNR AND ZIA_FIBUNR = ZIK_FIBUNR AND ZIA_ZIKLFDNR = ZIK_ZIKLFDNR" "~"
if joinKuerzel = FEZULK
searchResult := searchResult "LEFT JOIN BMD.ULK_USERLINK ON ULK_LINKGUID = FEZ_LINKGUID" "~"
if joinKuerzel = ULKFEZ
searchResult := searchResult "LEFT JOIN BMD.FEZ_FELDGRUPPENZU ON FEZ_LINKGUID = ULK_LINKGUID" "~"
if joinKuerzel = UATUMF
searchResult := searchResult "LEFT JOIN BUERO.UMF_UMFRAGE ON UMF_UMFRAGEID = UAT_UMFRAGEID" "~"
if joinKuerzel = UMFUAT
searchResult := searchResult "LEFT JOIN BUERO.UAT_UMFRAGEANTWTEILNZU ON UAT_UMFRAGEID = UMF_UMFRAGEID" "~"
if joinKuerzel = UTPUTP
searchResult := searchResult "LEFT JOIN BMD.UTP_USERTREEPOS ON UTP_BAUMID = UTP_BAUMID AND UTP_OBJPOSID = UTP_PARENTID" "~"
if joinKuerzel = UTPUTP
searchResult := searchResult "LEFT JOIN BMD.UTP_USERTREEPOS ON UTP_BAUMID = UTP_BAUMID AND UTP_PARENTID = UTP_OBJPOSID" "~"
if joinKuerzel = UVSUVF
searchResult := searchResult "LEFT JOIN FIBU.UVF_UVAFELDZU ON UVF_STEUER_ERKLNR = UVS_STEUER_ERKLNR AND UVF_UVAMODELLLFDNR = UVS_UVAMODELLLFDNR AND UVF_FELD_LFDNR = UVS_FELD_LFDNR" "~"
if joinKuerzel = UVFUVS
searchResult := searchResult "LEFT JOIN FIBU.UVS_MCUSTEUERCODEZU ON UVS_STEUER_ERKLNR = UVF_STEUER_ERKLNR AND UVS_UVAMODELLLFDNR = UVF_UVAMODELLLFDNR AND UVS_FELD_LFDNR = UVF_FELD_LFDNR" "~"
if joinKuerzel = MZZUZL
searchResult := searchResult "LEFT JOIN BUERO.UZL_UNTERZIEL ON UZL_UZLID = MZZ_UZLID" "~"
if joinKuerzel = UZLMZZ
searchResult := searchResult "LEFT JOIN BUERO.MZZ_MITARBZIELZU ON MZZ_UZLID = UZL_UZLID" "~"
if joinKuerzel = AKRVAK
searchResult := searchResult "LEFT JOIN BUERO.VAK_VERANSTALTUNGSKOPF ON VAK_FIRMENNR = AKR_VAK_FIRMENNR AND VAK_VSTAMMNR = AKR_VAK_VSTAMMNR AND VAK_VERANSTALTUNGSNR = AKR_VAK_VERANSTALTUNGSNR" "~"
if joinKuerzel = VAKAKR
searchResult := searchResult "LEFT JOIN BUERO.AKR_AVKOSTENARTENZU ON AKR_VAK_FIRMENNR = VAK_FIRMENNR AND AKR_VAK_VSTAMMNR = VAK_VSTAMMNR AND AKR_VAK_VERANSTALTUNGSNR = VAK_VERANSTALTUNGSNR" "~"
if joinKuerzel = VKDVAK
searchResult := searchResult "LEFT JOIN BUERO.VAK_VERANSTALTUNGSKOPF ON VAK_FIRMENNR = VKD_VAK_FIRMENNR AND VAK_VSTAMMNR = VKD_VAK_VSTAMMNR AND VAK_VERANSTALTUNGSNR = VKD_VAK_VERANSTALTUNGSNR" "~"
if joinKuerzel = VAKVKD
searchResult := searchResult "LEFT JOIN BUERO.VKD_VKOPFDOKZU ON VKD_VAK_FIRMENNR = VAK_FIRMENNR AND VKD_VAK_VSTAMMNR = VAK_VSTAMMNR AND VKD_VAK_VERANSTALTUNGSNR = VAK_VERANSTALTUNGSNR" "~"
if joinKuerzel = SABVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_FIRMENNR = SAB_VAS_FIRMENNR AND VAS_VSTAMMNR = SAB_VAS_VSTAMMNR" "~"
if joinKuerzel = VASSAB
searchResult := searchResult "LEFT JOIN BUERO.SAB_STELLENAUSBILDUNG ON SAB_VAS_FIRMENNR = VAS_FIRMENNR AND SAB_VAS_VSTAMMNR = VAS_VSTAMMNR" "~"
if joinKuerzel = STAVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_FIRMENNR = STA_VAT_FIRMENNR AND VAS_VSTAMMNR = STA_VAT_VSTAMMNR" "~"
if joinKuerzel = VASSTA
searchResult := searchResult "LEFT JOIN BUERO.STA_STELLENAUSBILDUNGEN ON STA_VAT_FIRMENNR = VAS_FIRMENNR AND STA_VAT_VSTAMMNR = VAS_VSTAMMNR" "~"
if joinKuerzel = VASVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_FIRMENNR = VAS_PARENT_FIRMENNR AND VAS_VSTAMMNR = VAS_PARENT_VSTAMMNR" "~"
if joinKuerzel = VASVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_PARENT_FIRMENNR = VAS_FIRMENNR AND VAS_PARENT_VSTAMMNR = VAS_VSTAMMNR" "~"
if joinKuerzel = KAPVAT
searchResult := searchResult "LEFT JOIN VAT_TEILNAHME.VAT_TEILNAHME ON VAT_FIRMENNR = KAP_VAT_FIRMENNR AND VAT_VSTAMMNR = KAP_VAT_VSTAMMNR AND VAT_VERANSTALTUNGSNR = KAP_VAT_VERANSTALTUNGSNR AND VAT_TEILNAHMENR = KAP_VAT_TEILNAHMENR" "~"
if joinKuerzel = VATKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_VAT_FIRMENNR = VAT_FIRMENNR AND KAP_VAT_VSTAMMNR = VAT_VSTAMMNR AND KAP_VAT_VERANSTALTUNGSNR = VAT_VERANSTALTUNGSNR AND KAP_VAT_TEILNAHMENR = VAT_TEILNAHMENR" "~"
if joinKuerzel = BPKVBN
searchResult := searchResult "LEFT JOIN WWS.VBN_VA_BESTKOPF_ERINFO ON VBN_FIRMENNR = BPK_FIRMENNR AND VBN_ERFASSUNGSKZ = BPK_ERFASSUNGSKZ AND VBN_BESTPRODERFASSUNGSNR = BPK_BESTPRODERFASSUNGSNR" "~"
if joinKuerzel = VBNBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_FIRMENNR = VBN_FIRMENNR AND BPK_ERFASSUNGSKZ = VBN_ERFASSUNGSKZ AND BPK_BESTPRODERFASSUNGSNR = VBN_BESTPRODERFASSUNGSNR" "~"
if joinKuerzel = VEBVBV
searchResult := searchResult "LEFT JOIN FIBU.VBV_VORERFASSBUCH ON VBV_FIRMENNR = VEB_FIRMENNR AND VBV_FIBUNR = VEB_FIBUNR AND VBV_VORERFASSTYP = VEB_VBV_VORERFASSTYP AND VBV_VORERFASSBUCHLFDNR = VEB_VBV_VORERFASSBUCHLFDNR" "~"
if joinKuerzel = VBVVEB
searchResult := searchResult "LEFT JOIN FIBU.VEB_VORERFASSBUCHUNG ON VEB_FIRMENNR = VBV_FIRMENNR AND VEB_FIBUNR = VBV_FIBUNR AND VEB_VBV_VORERFASSTYP = VBV_VORERFASSTYP AND VEB_VBV_VORERFASSBUCHLFDNR = VBV_VORERFASSBUCHLFDNR" "~"
if joinKuerzel = BPKVBW
searchResult := searchResult "LEFT JOIN WWS.VBW_VIEW_BESTPRODWERTE ON VBW_FIRMENNR = BPK_FIRMENNR AND VBW_ERFASSUNGSKZ = BPK_ERFASSUNGSKZ AND VBW_BESTPRODERFASSUNGSNR = BPK_BESTPRODERFASSUNGSNR" "~"
if joinKuerzel = VBWBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_FIRMENNR = VBW_FIRMENNR AND BPK_ERFASSUNGSKZ = VBW_ERFASSUNGSKZ AND BPK_BESTPRODERFASSUNGSNR = VBW_BESTPRODERFASSUNGSNR" "~"
if joinKuerzel = VEUVEB
searchResult := searchResult "LEFT JOIN FIBU.VEB_VORERFASSBUCHUNG ON VEB_FIRMENNR = VEU_FIRMENNR AND VEB_FIBUNR = VEU_FIBUNR AND VEB_VORERFASSID = VEU_VORERFASSID" "~"
if joinKuerzel = VEBVEU
searchResult := searchResult "LEFT JOIN FIBU.VEU_VORERFASSPROZAUFTEIL ON VEU_FIRMENNR = VEB_FIRMENNR AND VEU_FIBUNR = VEB_FIBUNR AND VEU_VORERFASSID = VEB_VORERFASSID" "~"
if joinKuerzel = BONVEK
searchResult := searchResult "LEFT JOIN WWS.VEK_VERTRETERABRECHKOPF ON VEK_VERTABRKOPFID = BON_VERTABRKOPFID" "~"
if joinKuerzel = VEKBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_VERTABRKOPFID = VEK_VERTABRKOPFID" "~"
if joinKuerzel = VEPVEK
searchResult := searchResult "LEFT JOIN WWS.VEK_VERTRETERABRECHKOPF ON VEK_VERTABRKOPFID = VEP_VERTABRKOPFID" "~"
if joinKuerzel = VEKVEP
searchResult := searchResult "LEFT JOIN WWS.VEP_VERTRETERABRECHPOS ON VEP_VERTABRKOPFID = VEK_VERTABRKOPFID" "~"
if joinKuerzel = AVEVER
searchResult := searchResult "LEFT JOIN VER_VERSANDART.VER_VERSANDART ON VER_VERSANDARTNR = AVE_VER_VERSANDARTNR" "~"
if joinKuerzel = VERAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_VER_VERSANDARTNR = VER_VERSANDARTNR" "~"
if joinKuerzel = BPKVER
searchResult := searchResult "LEFT JOIN VER_VERSANDART.VER_VERSANDART ON VER_VERSANDARTNR = BPK_VERSANDARTNR" "~"
if joinKuerzel = VERBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_VERSANDARTNR = VER_VERSANDARTNR" "~"
if joinKuerzel = LAJVKI
searchResult := searchResult "LEFT JOIN KORE.VKI_VIEW_KOSTAMM_DIM ON VKI_FIRMENNR = LAJ_FIRMENNR AND VKI_KOSTENSTAMMNR = LAJ_DIM_KOSTENSTAMMNR" "~"
if joinKuerzel = VKILAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_FIRMENNR = VKI_FIRMENNR AND LAJ_DIM_KOSTENSTAMMNR = VKI_KOSTENSTAMMNR" "~"
if joinKuerzel = LPOVKJ
searchResult := searchResult "LEFT JOIN WWS.VKJ_VA_AUSGKOSTLIEFSUM ON VKJ_FIRMENNR = LPO_FIRMENNR AND VKJ_LIEFERSCHEINLFDNR = LPO_LIEFERSCHEINLFDNR AND VKJ_LIEFERSCHEINPOSLFDNR = LPO_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = VKJLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_FIRMENNR = VKJ_FIRMENNR AND LPO_LIEFERSCHEINLFDNR = VKJ_LIEFERSCHEINLFDNR AND LPO_LIEFERSCHEINPOSLFDNR = VKJ_LIEFERSCHEINPOSLFDNR" "~"
if joinKuerzel = BPKVKS
searchResult := searchResult "LEFT JOIN KORE.VKS_VIEW_KOSTAMM_KST ON VKS_FIRMENNR = BPK_FIRMENNR AND VKS_KOSTENSTAMMNR = BPK_KOSTENST_KOSTENSTAMMNR" "~"
if joinKuerzel = VKSBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_FIRMENNR = VKS_FIRMENNR AND BPK_KOSTENST_KOSTENSTAMMNR = VKS_KOSTENSTAMMNR" "~"
if joinKuerzel = RNPVKS
searchResult := searchResult "LEFT JOIN KORE.VKS_VIEW_KOSTAMM_KST ON VKS_FIRMENNR = RNP_FIRMENNR AND VKS_KOSTENSTAMMNR = RNP_KOSTENST_KOSTENSTAMMNR" "~"
if joinKuerzel = VKSRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = VKS_FIRMENNR AND RNP_KOSTENST_KOSTENSTAMMNR = VKS_KOSTENSTAMMNR" "~"
if joinKuerzel = BPKVKT
searchResult := searchResult "LEFT JOIN KORE.VKT_VIEW_KOSTAMM_KTR ON VKT_FIRMENNR = BPK_FIRMENNR AND VKT_KOSTENSTAMMNR = BPK_KOSTENTR_KOSTENSTAMMNR" "~"
if joinKuerzel = VKTBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_FIRMENNR = VKT_FIRMENNR AND BPK_KOSTENTR_KOSTENSTAMMNR = VKT_KOSTENSTAMMNR" "~"
if joinKuerzel = PERVKZ
searchResult := searchResult "LEFT JOIN WWS.VKZ_VIEW_ARTIKELKUNDZUGRP ON VKZ_KUN_FIRMENNR = PER_FIRMENNR AND VKZ_KUN_KLID = PER_PERSONENID" "~"
if joinKuerzel = VKZPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = VKZ_KUN_FIRMENNR AND PER_PERSONENID = VKZ_KUN_KLID" "~"
if joinKuerzel = VMZVMK
searchResult := searchResult "LEFT JOIN WWS.VMK_VERSMGR_KOPF ON VMK_VERSMGR_KOPFID = VMZ_VMK_VERSMGR_KOPFID" "~"
if joinKuerzel = VMKVMZ
searchResult := searchResult "LEFT JOIN WWS.VMZ_VERSMGR_LFS_ZUORD ON VMZ_VMK_VERSMGR_KOPFID = VMK_VERSMGR_KOPFID" "~"
if joinKuerzel = EROVOW
searchResult := searchResult "LEFT JOIN WWS.VOW_VIEW_ERFPOSORIGWERTE ON VOW_FIRMENNR = ERO_FIRMENNR AND VOW_ERFASSUNGSKZ = ERO_ERFASSUNGSKZ AND VOW_ERFASSUNGSNR = ERO_ERFASSUNGSNR AND VOW_ERFASSPOSLFDNR = ERO_ERFASSPOSLFDNR AND VOW_ERFASSPOSORIGLFDNR = ERO_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = VOWERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_FIRMENNR = VOW_FIRMENNR AND ERO_ERFASSUNGSKZ = VOW_ERFASSUNGSKZ AND ERO_ERFASSUNGSNR = VOW_ERFASSUNGSNR AND ERO_ERFASSPOSLFDNR = VOW_ERFASSPOSLFDNR AND ERO_ERFASSPOSORIGLFDNR = VOW_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = VPBVPE
searchResult := searchResult "LEFT JOIN WWS.VPE_VPI_ERHOEHUNG ON VPE_VPEID = VPB_VPEID" "~"
if joinKuerzel = VPEVPB
searchResult := searchResult "LEFT JOIN WWS.VPB_VPERMITTLUNG_ERKZUORD ON VPB_VPEID = VPE_VPEID" "~"
if joinKuerzel = VPGVPE
searchResult := searchResult "LEFT JOIN WWS.VPE_VPI_ERHOEHUNG ON VPE_VPEID = VPG_VPEID" "~"
if joinKuerzel = VPEVPG
searchResult := searchResult "LEFT JOIN WWS.VPG_VPERMITTLUNG_EFKZUORD ON VPG_VPEID = VPE_VPEID" "~"
if joinKuerzel = VPHVPE
searchResult := searchResult "LEFT JOIN WWS.VPE_VPI_ERHOEHUNG ON VPE_VPEID = VPH_VPEID" "~"
if joinKuerzel = VPEVPH
searchResult := searchResult "LEFT JOIN WWS.VPH_VPERMITTLUNG_FARZUORD ON VPH_VPEID = VPE_VPEID" "~"
if joinKuerzel = RNGVRW
searchResult := searchResult "LEFT JOIN WWS.VRW_VIEW_RECHNUNGSWERTE ON VRW_FIRMENNR = RNG_FIRMENNR AND VRW_RECHNUNGLFDNR = RNG_RECHNUNGLFDNR" "~"
if joinKuerzel = VRWRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = VRW_FIRMENNR AND RNG_RECHNUNGLFDNR = VRW_RECHNUNGLFDNR" "~"
if joinKuerzel = VZZVZA
searchResult := searchResult "LEFT JOIN HRS.VZA_VORZEITART ON VZA_DBSNR = VZZ_DBSNR AND VZA_VZANR = VZZ_VZANR" "~"
if joinKuerzel = VZAVZZ
searchResult := searchResult "LEFT JOIN HRS.VZZ_VORZEITZU ON VZZ_DBSNR = VZA_DBSNR AND VZZ_VZANR = VZA_VZANR" "~"
if joinKuerzel = BPKVZW
searchResult := searchResult "LEFT JOIN VZW_VERKEHRSZWEIG.VZW_VERKEHRSZWEIG ON VZW_FIRMENNR = BPK_VZW_FIRMENNR AND VZW_VZWCODE = BPK_VZWCODE" "~"
if joinKuerzel = VZWBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_VZW_FIRMENNR = VZW_FIRMENNR AND BPK_VZWCODE = VZW_VZWCODE" "~"
if joinKuerzel = AWEWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = AWE_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = BARWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = BAR_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEBAR
searchResult := searchResult "LEFT JOIN HRS.BAR_BARAUSLAGEN ON BAR_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = BEZWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = BEZ_WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEBEZ
searchResult := searchResult "LEFT JOIN FIBU.BEZ_BANKEINZUG ON BEZ_WAE_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = BPKWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = BPK_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = FIFWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = FIF_UVA_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEFIF
searchResult := searchResult "LEFT JOIN FIBU.FIF_FILIALEFIBU ON FIF_UVA_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = LEIWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = LEI_WAEHRUNGSCODE" "~"
if joinKuerzel = WAELEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = RNGWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = RNG_WAEHRUNGSCODE" "~"
if joinKuerzel = WAERNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = ZZUWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = ZZU_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEZZU
searchResult := searchResult "LEFT JOIN FIBU.ZZU_ZINSBERECHNUNG_ZLG_ZU ON ZZU_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = FIRWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = FIR_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = KTOWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = KTO_ZWEIT_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_ZWEIT_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = ARLWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = ARL_FIRMENNR AND WAG_WARENHAUPTGRNR = ARL_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = ARL_WARENUNTERGRNR" "~"
if joinKuerzel = WAGARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = WAG_FIRMENNR AND ARL_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND ARL_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = DVHWAG
searchResult := searchResult "LEFT JOIN WAG_WARENGRUPPE.WAG_WARENGRUPPE ON WAG_FIRMENNR = DVH_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = DVH_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = DVH_WARENUNTERGRNR" "~"
if joinKuerzel = WAGDVH
searchResult := searchResult "LEFT JOIN BUERO.DVH_DIENSTVERHAELTNIS ON DVH_WAG_FIRMENNR = WAG_FIRMENNR AND DVH_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND DVH_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = PALWAG
searchResult := searchResult "LEFT JOIN WAG_WARENGRUPPE.WAG_WARENGRUPPE ON WAG_FIRMENNR = PAL_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = PAL_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = PAL_WARENUNTERGRNR" "~"
if joinKuerzel = WAGPAL
searchResult := searchResult "LEFT JOIN ZLE.PAL_PAUSCHALE ON PAL_WAG_FIRMENNR = WAG_FIRMENNR AND PAL_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND PAL_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = RVDWAG
searchResult := searchResult "LEFT JOIN WAG_WARENGRUPPE.WAG_WARENGRUPPE ON WAG_FIRMENNR = RVD_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = RVD_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = RVD_WARENUNTERGRNR" "~"
if joinKuerzel = WAGRVD
searchResult := searchResult "LEFT JOIN ZLE.RVD_RECHVORLAGEDETAIL ON RVD_WAG_FIRMENNR = WAG_FIRMENNR AND RVD_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND RVD_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = WASWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = WAS_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = WAS_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = WAS_WARENUNTERGRNR" "~"
if joinKuerzel = WAGWAS
searchResult := searchResult "LEFT JOIN WWS.WAS_WARENEINSATZARL ON WAS_WAG_FIRMENNR = WAG_FIRMENNR AND WAS_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND WAS_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = LAJWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_WARENEINGKOPFID = LAJ_WARENEINGKOPFID AND WAP_WARENEINGPOSLFDNR = LAJ_WARENEINGPOSLFDNR" "~"
if joinKuerzel = WAPLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_WARENEINGKOPFID = WAP_WARENEINGKOPFID AND LAJ_WARENEINGPOSLFDNR = WAP_WARENEINGPOSLFDNR" "~"
if joinKuerzel = TRPWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_WARENEINGKOPFID = TRP_WARENEINGKOPFID AND WAP_WARENEINGPOSLFDNR = TRP_WARENEINGPOSLFDNR" "~"
if joinKuerzel = WAPTRP
searchResult := searchResult "LEFT JOIN WWS.TRP_TRANSPORTPOSITION ON TRP_WARENEINGKOPFID = WAP_WARENEINGKOPFID AND TRP_WARENEINGPOSLFDNR = WAP_WARENEINGPOSLFDNR" "~"
if joinKuerzel = WAOWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_WARENEINGKOPFID = WAO_WARENEINGKOPFID AND WAP_WARENEINGPOSLFDNR = WAO_WARENEINGPOSLFDNR" "~"
if joinKuerzel = WAPWAO
searchResult := searchResult "LEFT JOIN WWS.WAO_WARENEINGWBK ON WAO_WARENEINGKOPFID = WAP_WARENEINGKOPFID AND WAO_WARENEINGPOSLFDNR = WAP_WARENEINGPOSLFDNR" "~"
if joinKuerzel = BSDWBW
searchResult := searchResult "LEFT JOIN WWS.WBW_WWSBUDGETWERTE ON WBW_WWSBUDGETWERTEID = BSD_WWSBUDGETWERTEID" "~"
if joinKuerzel = WBWBSD
searchResult := searchResult "LEFT JOIN WWS.BSD_BONUSERMDETAIL ON BSD_WWSBUDGETWERTEID = WBW_WWSBUDGETWERTEID" "~"
if joinKuerzel = WCFWCA
searchResult := searchResult "LEFT JOIN BMD.WCA_WAAUSDRUCKE ON WCA_WAAUSDRUCKNR = WCF_WAAUSDRUCKNR" "~"
if joinKuerzel = WCAWCF
searchResult := searchResult "LEFT JOIN BMD.WCF_WABAUMELEMAUSDRUCKZU ON WCF_WAAUSDRUCKNR = WCA_WAAUSDRUCKNR" "~"
if joinKuerzel = WCWWCA
searchResult := searchResult "LEFT JOIN BMD.WCA_WAAUSDRUCKE ON WCA_WAAUSDRUCKNR = WCW_WAAUSDRUCKNR" "~"
if joinKuerzel = WCAWCW
searchResult := searchResult "LEFT JOIN BMD.WCW_WAAUSDRUCKPARAMWERT ON WCW_WAAUSDRUCKNR = WCA_WAAUSDRUCKNR" "~"
if joinKuerzel = LAJWEI
searchResult := searchResult "LEFT JOIN WWS.WEI_WARENEINSATZKOPF ON WEI_WEINSATZKOPFID = LAJ_WEINSATZKOPFID" "~"
if joinKuerzel = WEILAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_WEINSATZKOPFID = WEI_WEINSATZKOPFID" "~"
if joinKuerzel = WEBWEI
searchResult := searchResult "LEFT JOIN WWS.WEI_WARENEINSATZKOPF ON WEI_WEINSATZKOPFID = WEB_WEINSATZKOPFID" "~"
if joinKuerzel = WEIWEB
searchResult := searchResult "LEFT JOIN WWS.WEB_WARENEINSATZBUCH ON WEB_WEINSATZKOPFID = WEI_WEINSATZKOPFID" "~"
if joinKuerzel = WEBWEI
searchResult := searchResult "LEFT JOIN WWS.WEI_WARENEINSATZKOPF ON WEI_WEINSATZKOPFID = WEB_DIFF_WEINSATZKOPFID" "~"
if joinKuerzel = WEIWEB
searchResult := searchResult "LEFT JOIN WWS.WEB_WARENEINSATZBUCH ON WEB_DIFF_WEINSATZKOPFID = WEI_WEINSATZKOPFID" "~"
if joinKuerzel = VFDWEK
searchResult := searchResult "LEFT JOIN PRODUKTION.WEK_WERKZEUG ON WEK_FIRMENNR = VFD_WEK_FIRMENNR AND WEK_WERKZEUGID = VFD_WERKZEUGID" "~"
if joinKuerzel = WEKVFD
searchResult := searchResult "LEFT JOIN PRODUKTION.VFD_VERFAHRENSANWEISUNG_POS ON VFD_WEK_FIRMENNR = WEK_FIRMENNR AND VFD_WERKZEUGID = WEK_WERKZEUGID" "~"
if joinKuerzel = WKLWEP
searchResult := searchResult "LEFT JOIN WWS.WEP_WWSERKONTROLLEPOS ON WEP_ERKONTRPOS_ID = WKL_ERKONTRPOS_ID" "~"
if joinKuerzel = WEPWKL
searchResult := searchResult "LEFT JOIN WWS.WKL_WARENAUSGKOSTENLIEF ON WKL_ERKONTRPOS_ID = WEP_ERKONTRPOS_ID" "~"
if joinKuerzel = VEBWGB
searchResult := searchResult "LEFT JOIN FIBU.WGB_WARENEINGANGSBUCH ON WGB_FIRMENNR = VEB_FIRMENNR AND WGB_FIBUNR = VEB_FIBUNR AND WGB_WGBID = VEB_WGBID" "~"
if joinKuerzel = WGBVEB
searchResult := searchResult "LEFT JOIN FIBU.VEB_VORERFASSBUCHUNG ON VEB_FIRMENNR = WGB_FIRMENNR AND VEB_FIBUNR = WGB_FIBUNR AND VEB_WGBID = WGB_WGBID" "~"
if joinKuerzel = WKGWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = WKG_FIRMENNR AND WTB_WWSTEXTBNR = WKG_KOPFTEXT_WWSTEXTBNR" "~"
if joinKuerzel = WTBWKG
searchResult := searchResult "LEFT JOIN WWS.WKG_WWSKUNDENGRUPPE ON WKG_FIRMENNR = WTB_FIRMENNR AND WKG_KOPFTEXT_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = ZRTWTB
searchResult := searchResult "LEFT JOIN WTB_WWSTEXTBAUSTEIN.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = ZRT_WTB_FIRMENNR AND WTB_WWSTEXTBNR = ZRT_WTB_WWSTEXTBNR" "~"
if joinKuerzel = WTBZRT
searchResult := searchResult "LEFT JOIN BUERO.ZRT_ZAHLUNGSART ON ZRT_WTB_FIRMENNR = WTB_FIRMENNR AND ZRT_WTB_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = BPPWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = BPP_ARL_FIRMENNR AND WTB_WWSTEXTBNR = BPP_BESCH_WWSTEXTBNR" "~"
if joinKuerzel = WTBBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_ARL_FIRMENNR = WTB_FIRMENNR AND BPP_BESCH_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = ERPWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = ERP_ARL_FIRMENNR AND WTB_WWSTEXTBNR = ERP_BESCH_WWSTEXTBNR" "~"
if joinKuerzel = WTBERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_ARL_FIRMENNR = WTB_FIRMENNR AND ERP_BESCH_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = LMSWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = LMS_WWSTEXTBNR_FIRMENNR AND WTB_WWSTEXTBNR = LMS_WWSTEXTBNR_STUFE2" "~"
if joinKuerzel = WTBLMS
searchResult := searchResult "LEFT JOIN WWS.LMS_LIEFERMAHNUNG ON LMS_WWSTEXTBNR_FIRMENNR = WTB_FIRMENNR AND LMS_WWSTEXTBNR_STUFE2 = WTB_WWSTEXTBNR" "~"
if joinKuerzel = YASYBU
searchResult := searchResult "LEFT JOIN FIBU.YBU_AUTOBUCHUNG ON YBU_YBMODELLID = YAS_YBMODELLID AND YBU_AUTOBUCHUNGID = YAS_AUTOBUCHUNGID" "~"
if joinKuerzel = YBUYAS
searchResult := searchResult "LEFT JOIN FIBU.YAS_AUTOBUCHAUFTEILSATZ ON YAS_YBMODELLID = YBU_YBMODELLID AND YAS_AUTOBUCHUNGID = YBU_AUTOBUCHUNGID" "~"
if joinKuerzel = MITZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_FIRMENNR = MIT_ZBA_FIRMENNR AND ZBA_ZBUCHARTNR = MIT_ZBA_ZBUCHARTNR" "~"
if joinKuerzel = ZBAMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_ZBA_FIRMENNR = ZBA_FIRMENNR AND MIT_ZBA_ZBUCHARTNR = ZBA_ZBUCHARTNR" "~"
if joinKuerzel = TBAZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_FIRMENNR = TBA_ZBA_FIRMENNR AND ZBA_ZBUCHARTNR = TBA_ZBUCHARTNR" "~"
if joinKuerzel = ZBATBA
searchResult := searchResult "LEFT JOIN BUERO.TBA_TAGESMDBUCHARTAUFB ON TBA_ZBA_FIRMENNR = ZBA_FIRMENNR AND TBA_ZBUCHARTNR = ZBA_ZBUCHARTNR" "~"
if joinKuerzel = ZABZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_FIRMENNR = ZAB_ZBA_FIRMENNR AND ZBA_ZBUCHARTNR = ZAB_ZBA_ZBUCHARTNR" "~"
if joinKuerzel = ZBAZAB
searchResult := searchResult "LEFT JOIN BUERO.ZAB_AUSWEISBUCHARTZU ON ZAB_ZBA_FIRMENNR = ZBA_FIRMENNR AND ZAB_ZBA_ZBUCHARTNR = ZBA_ZBUCHARTNR" "~"
if joinKuerzel = WFNZBE
searchResult := searchResult "LEFT JOIN BUERO.ZBE_ZUSTAENDIGKEITBEREICH ON ZBE_ZUSTBEREICHLFDNR = WFN_ZBE_ZUSTBEREICHLFDNR" "~"
if joinKuerzel = ZBEWFN
searchResult := searchResult "LEFT JOIN BUERO.WFN_WORKFLOWACTION ON WFN_ZBE_ZUSTBEREICHLFDNR = ZBE_ZUSTBEREICHLFDNR" "~"
if joinKuerzel = ZBOZBG
searchResult := searchResult "LEFT JOIN BUERO.ZBG_ZEITBABERECHTGRUPPE ON ZBG_FIRMENNR = ZBO_ZBG_FIRMENNR AND ZBG_BAGRUPPNR = ZBO_BAGRUPPNR" "~"
if joinKuerzel = ZBGZBO
searchResult := searchResult "LEFT JOIN BUERO.ZBO_ZEITBABERECHTZUORD ON ZBO_ZBG_FIRMENNR = ZBG_FIRMENNR AND ZBO_BAGRUPPNR = ZBG_BAGRUPPNR" "~"
if joinKuerzel = ZBZZBZ
searchResult := searchResult "LEFT JOIN BUERO.ZBZ_ZEITBEZUGSART ON ZBZ_FIRMENNR = ZBZ_UMBU_FIRMENNR AND ZBZ_ZBEZUGSARTNR = ZBZ_UMBU_ZBEZUGSARTNR" "~"
if joinKuerzel = ZBZZBZ
searchResult := searchResult "LEFT JOIN BUERO.ZBZ_ZEITBEZUGSART ON ZBZ_UMBU_FIRMENNR = ZBZ_FIRMENNR AND ZBZ_UMBU_ZBEZUGSARTNR = ZBZ_ZBEZUGSARTNR" "~"
if joinKuerzel = ZMSZBZ
searchResult := searchResult "LEFT JOIN BUERO.ZBZ_ZEITBEZUGSART ON ZBZ_FIRMENNR = ZMS_ZBZ_FIRMENNR AND ZBZ_ZBEZUGSARTNR = ZMS_ZBEZUGSARTNR" "~"
if joinKuerzel = ZBZZMS
searchResult := searchResult "LEFT JOIN BUERO.ZMS_ZEITMONATSSUMMEN ON ZMS_ZBZ_FIRMENNR = ZBZ_FIRMENNR AND ZMS_ZBEZUGSARTNR = ZBZ_ZBEZUGSARTNR" "~"
if joinKuerzel = ZSAZBZ
searchResult := searchResult "LEFT JOIN BUERO.ZBZ_ZEITBEZUGSART ON ZBZ_FIRMENNR = ZSA_ZBZ_FIRMENNR AND ZBZ_ZBEZUGSARTNR = ZSA_ZBEZUGSARTNR" "~"
if joinKuerzel = ZBZZSA
searchResult := searchResult "LEFT JOIN BUERO.ZSA_ZEITSALDEN ON ZSA_ZBZ_FIRMENNR = ZBZ_FIRMENNR AND ZSA_ZBEZUGSARTNR = ZBZ_ZBEZUGSARTNR" "~"
if joinKuerzel = ZUUZBZ
searchResult := searchResult "LEFT JOIN BUERO.ZBZ_ZEITBEZUGSART ON ZBZ_FIRMENNR = ZUU_FIRMENNR AND ZBZ_ZBEZUGSARTNR = ZUU_ZBEZUGSARTNR" "~"
if joinKuerzel = ZBZZUU
searchResult := searchResult "LEFT JOIN BUERO.ZUU_ZEITBEZARTUMBUCHUSER ON ZUU_FIRMENNR = ZBZ_FIRMENNR AND ZUU_ZBEZUGSARTNR = ZBZ_ZBEZUGSARTNR" "~"
if joinKuerzel = BEWZBZ
searchResult := searchResult "LEFT JOIN BUERO.ZBZ_ZEITBEZUGSART ON ZBZ_FIRMENNR = BEW_ZBZ_FIRMENNR AND ZBZ_ZBEZUGSARTNR = BEW_ZBEZUGSARTNR_NEXTMON2" "~"
if joinKuerzel = ZBZBEW
searchResult := searchResult "LEFT JOIN BUERO.BEW_BEWERTUNGSMODELL ON BEW_ZBZ_FIRMENNR = ZBZ_FIRMENNR AND BEW_ZBEZUGSARTNR_NEXTMON2 = ZBZ_ZBEZUGSARTNR" "~"
if joinKuerzel = ZIDZDK
searchResult := searchResult "LEFT JOIN BMD.ZDK_ZEILENDEFKOPF ON ZDK_ZEILENDEFKOPFID = ZID_ZEILENDEFKOPFID" "~"
if joinKuerzel = ZDKZID
searchResult := searchResult "LEFT JOIN BMD.ZID_ZEILENDEF ON ZID_ZEILENDEFKOPFID = ZDK_ZEILENDEFKOPFID" "~"
if joinKuerzel = ERZZRT
searchResult := searchResult "LEFT JOIN ZRT_ZAHLUNGSART.ZRT_ZAHLUNGSART ON ZRT_ZAHLARTNR = ERZ_ZAHLARTNR" "~"
if joinKuerzel = ZRTERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_ZAHLARTNR = ZRT_ZAHLARTNR" "~"
if joinKuerzel = KAKZRT
searchResult := searchResult "LEFT JOIN ZRT_ZAHLUNGSART.ZRT_ZAHLUNGSART ON ZRT_ZAHLARTNR = KAK_ZRT_ZAHLARTNR" "~"
if joinKuerzel = ZRTKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_ZRT_ZAHLARTNR = ZRT_ZAHLARTNR" "~"
if joinKuerzel = PERZRT
searchResult := searchResult "LEFT JOIN BUERO.ZRT_ZAHLUNGSART ON ZRT_ZAHLARTNR = PER_LIEF_ZAHLARTNR" "~"
if joinKuerzel = ZRTPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_LIEF_ZAHLARTNR = ZRT_ZAHLARTNR" "~"
if joinKuerzel = AGIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AGI_LETZTERUSER" "~"
if joinKuerzel = BMUAGI
searchResult := searchResult "LEFT JOIN WWS.AGI_AGPER_INFOZU ON AGI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AGZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AGZ_LETZTERUSER" "~"
if joinKuerzel = BMUAGZ
searchResult := searchResult "LEFT JOIN WWS.AGZ_ARBEITSGANGZU ON AGZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AIDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AID_LETZTERUSER" "~"
if joinKuerzel = BMUAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AJOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AJO_LETZTERUSER" "~"
if joinKuerzel = BMUAJO
searchResult := searchResult "LEFT JOIN BIL.AJO_ANBUJOURNAL ON AJO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AKZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AKZ_LETZTERUSER" "~"
if joinKuerzel = BMUAKZ
searchResult := searchResult "LEFT JOIN WWS.AKZ_ARTIKELKUNDENZU ON AKZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ALKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ALK_LETZTERUSER" "~"
if joinKuerzel = BMUALK
searchResult := searchResult "LEFT JOIN PRODUKTION.ALK_ARBEITSPLAN_KOSTEN ON ALK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ANOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ANO_LETZTERUSER" "~"
if joinKuerzel = BMUANO
searchResult := searchResult "LEFT JOIN WWS.ANO_ARTIKELNORM ON ANO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ARKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ARK_LETZTERUSER" "~"
if joinKuerzel = BMUARK
searchResult := searchResult "LEFT JOIN WWS.ARK_ARTIKELKLASSE ON ARK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ARRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ARR_LETZTERUSER" "~"
if joinKuerzel = BMUARR
searchResult := searchResult "LEFT JOIN PRODUKTION.ARR_PROD_ARTIKEL_REPORT ON ARR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ASBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ASB_LETZTERUSER" "~"
if joinKuerzel = BMUASB
searchResult := searchResult "LEFT JOIN ZLE.ASB_AUSWERT_SUB ON ASB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ASJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ASJ_LETZTERUSER" "~"
if joinKuerzel = BMUASJ
searchResult := searchResult "LEFT JOIN BMD.ASJ_ASYNCHRONEJOBS ON ASJ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ASOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ASO_LETZTERUSER" "~"
if joinKuerzel = BMUASO
searchResult := searchResult "LEFT JOIN ZLE.ASO_SORTIERPOS ON ASO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ASRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ASR_LETZTERUSER" "~"
if joinKuerzel = BMUASR
searchResult := searchResult "LEFT JOIN BUERO.ASR_AUSSCHREIBUNG ON ASR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ATEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ATE_LETZTERUSER" "~"
if joinKuerzel = BMUATE
searchResult := searchResult "LEFT JOIN BUERO.ATE_ABTEILUNG ON ATE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AUBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AUB_LETZTERUSER" "~"
if joinKuerzel = BMUAUB
searchResult := searchResult "LEFT JOIN BIL.AUB_KIND ON AUB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AVHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AVH_LETZTERUSER" "~"
if joinKuerzel = BMUAVH
searchResult := searchResult "LEFT JOIN BUERO.AVH_AVIMPORTHEADER ON AVH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AVLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AVL_LETZTERUSER" "~"
if joinKuerzel = BMUAVL
searchResult := searchResult "LEFT JOIN WWS.AVL_AUFTRAGSVORERFLIEFDAT ON AVL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AWZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AWZ_LETZTERUSER" "~"
if joinKuerzel = BMUAWZ
searchResult := searchResult "LEFT JOIN ZLE.AWZ_AUSWERTUNGZU ON AWZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BBABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BBA_LETZTERUSER" "~"
if joinKuerzel = BMUBBA
searchResult := searchResult "LEFT JOIN BIL.BBA_BERICHTSART ON BBA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BEFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BEF_LETZTERUSER" "~"
if joinKuerzel = BMUBEF
searchResult := searchResult "LEFT JOIN BUERO.BEF_BEFRAGUNG ON BEF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BEIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BEI_LETZTERUSER" "~"
if joinKuerzel = BMUBEI
searchResult := searchResult "LEFT JOIN BUERO.BEI_BEIFAHRER ON BEI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BEXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BEX_LETZTERUSER" "~"
if joinKuerzel = BMUBEX
searchResult := searchResult "LEFT JOIN FIBU.BEX_BUDGETEXT ON BEX_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BMLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BML_LETZTERUSER" "~"
if joinKuerzel = BMUBML
searchResult := searchResult "LEFT JOIN FIBU.BML_BANKRVMODEL ON BML_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BSABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BSA_LETZTERUSER" "~"
if joinKuerzel = BMUBSA
searchResult := searchResult "LEFT JOIN BMD.BSA_BMDSESSIONARCHIV ON BSA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BSBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BSB_LETZTERUSER" "~"
if joinKuerzel = BMUBSB
searchResult := searchResult "LEFT JOIN WWS.BSB_BONUSERMPERSONENZU ON BSB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BUKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BUK_LETZTERUSER" "~"
if joinKuerzel = BMUBUK
searchResult := searchResult "LEFT JOIN FIBU.BUK_BUCHUNGSKREIS ON BUK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RRDSAK
searchResult := searchResult "LEFT JOIN SAK_SACHKONTO.SAK_SACHKONTO ON SAK_FIRMENNR = RRD_SAK_FIRMENNR AND SAK_FIBUNR = RRD_SAK_FIBUNR AND SAK_KONTONR = RRD_SAK_KONTONR" "~"
if joinKuerzel = SAKRRD
searchResult := searchResult "LEFT JOIN WWS.RRD_RECHNUNGSRABATTDETAIL ON RRD_SAK_FIRMENNR = SAK_FIRMENNR AND RRD_SAK_FIBUNR = SAK_FIBUNR AND RRD_SAK_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = ANBSAK
searchResult := searchResult "LEFT JOIN SAK_SACHKONTO.SAK_SACHKONTO ON SAK_FIRMENNR = ANB_FIBUFIRMENNR AND SAK_FIBUNR = ANB_FIBUNR AND SAK_KONTONR = ANB_GEGENKONTONR" "~"
if joinKuerzel = SAKANB
searchResult := searchResult "LEFT JOIN BIL.ANB_ANBUBUCHBELEGZU ON ANB_FIBUFIRMENNR = SAK_FIRMENNR AND ANB_FIBUNR = SAK_FIBUNR AND ANB_GEGENKONTONR = SAK_KONTONR" "~"
if joinKuerzel = BAPSAK
searchResult := searchResult "LEFT JOIN FIBU.SAK_SACHKONTO ON SAK_FIRMENNR = BAP_FIRMENNR AND SAK_FIBUNR = BAP_FIBUNR AND SAK_KONTONR = BAP_VERKAUF_KONTONR" "~"
if joinKuerzel = SAKBAP
searchResult := searchResult "LEFT JOIN FIBU.BAP_BUDGETARTIKELPLAN ON BAP_FIRMENNR = SAK_FIRMENNR AND BAP_FIBUNR = SAK_FIBUNR AND BAP_VERKAUF_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = PKPSAK
searchResult := searchResult "LEFT JOIN FIBU.SAK_SACHKONTO ON SAK_FIRMENNR = PKP_FIRMENNR AND SAK_FIBUNR = PKP_FIBUNR AND SAK_KONTONR = PKP_KOMMUNALSTEUER_KONTONR" "~"
if joinKuerzel = SAKPKP
searchResult := searchResult "LEFT JOIN FIBU.PKP_BUDGETPERSKOSTPLAN ON PKP_FIRMENNR = SAK_FIRMENNR AND PKP_FIBUNR = SAK_FIBUNR AND PKP_KOMMUNALSTEUER_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = PKPSAK
searchResult := searchResult "LEFT JOIN FIBU.SAK_SACHKONTO ON SAK_FIRMENNR = PKP_FIRMENNR AND SAK_FIBUNR = PKP_FIBUNR AND SAK_KONTONR = PKP_MINIJOB_KONTONR" "~"
if joinKuerzel = SAKPKP
searchResult := searchResult "LEFT JOIN FIBU.PKP_BUDGETPERSKOSTPLAN ON PKP_FIRMENNR = SAK_FIRMENNR AND PKP_FIBUNR = SAK_FIBUNR AND PKP_MINIJOB_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = OUPSBV
searchResult := searchResult "LEFT JOIN BUERO.SBV_STELLENBESCHVORLAGE ON SBV_VORLAGENID = OUP_TEXTBAUSTEINID03" "~"
if joinKuerzel = SBVOUP
searchResult := searchResult "LEFT JOIN BUERO.OUP_ONLUMFRAGEPAGE ON OUP_TEXTBAUSTEINID03 = SBV_VORLAGENID" "~"
if joinKuerzel = OUPSBV
searchResult := searchResult "LEFT JOIN BUERO.SBV_STELLENBESCHVORLAGE ON SBV_VORLAGENID = OUP_TEXTBAUSTEINID08" "~"
if joinKuerzel = SBVOUP
searchResult := searchResult "LEFT JOIN BUERO.OUP_ONLUMFRAGEPAGE ON OUP_TEXTBAUSTEINID08 = SBV_VORLAGENID" "~"
if joinKuerzel = SCESCK
searchResult := searchResult "LEFT JOIN PRODUKTION.SCK_SCKENNZAHL ON SCK_SCKID = SCE_SCKID" "~"
if joinKuerzel = SCKSCE
searchResult := searchResult "LEFT JOIN PRODUKTION.SCE_SCORECARD ON SCE_SCKID = SCK_SCKID" "~"
if joinKuerzel = RIHSCK
searchResult := searchResult "LEFT JOIN PRODUKTION.SCK_SCKENNZAHL ON SCK_SCKID = RIH_SCKID_OCCURENCE" "~"
if joinKuerzel = SCKRIH
searchResult := searchResult "LEFT JOIN PRODUKTION.RIH_RISIKOANALYSE_HEAD ON RIH_SCKID_OCCURENCE = SCK_SCKID" "~"
if joinKuerzel = SCYSCM
searchResult := searchResult "LEFT JOIN BMDENTW.SCM_SCRMAIN ON SCM_SCRNR = SCY_SCRNR_UEBEL" "~"
if joinKuerzel = SCMSCY
searchResult := searchResult "LEFT JOIN BMDENTW.SCY_SCRDEPENDENCY ON SCY_SCRNR_UEBEL = SCM_SCRNR" "~"
if joinKuerzel = SCASCQ
searchResult := searchResult "LEFT JOIN PRODUKTION.SCQ_SCKZGRP ON SCQ_SCQID = SCA_SCQID" "~"
if joinKuerzel = SCQSCA
searchResult := searchResult "LEFT JOIN PRODUKTION.SCA_SCKRITERIUM ON SCA_SCQID = SCQ_SCQID" "~"
if joinKuerzel = SFFSFR
searchResult := searchResult "LEFT JOIN BUERO.SFR_SKILLFINDERREGEL ON SFR_SKILLREGELID = SFF_SFR_SKILLREGELID_STAMM" "~"
if joinKuerzel = SFRSFF
searchResult := searchResult "LEFT JOIN BUERO.SFF_SKILLFINDERFILTER ON SFF_SFR_SKILLREGELID_STAMM = SFR_SKILLREGELID" "~"
if joinKuerzel = PICSFZ
searchResult := searchResult "LEFT JOIN SFZ_SYNCFELDZUORDNUNG.SFZ_SYNCFELDZUORDNUNG ON SFZ_SYNCFELDZUID = PIC_SYNCFELDZUID_PREIS_VK" "~"
if joinKuerzel = SFZPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_SYNCFELDZUID_PREIS_VK = SFZ_SYNCFELDZUID" "~"
if joinKuerzel = DNVSID
searchResult := searchResult "LEFT JOIN HRS.SID_HRSSEQ ON SID_HRSSEQID = DNV_HRSSEQID" "~"
if joinKuerzel = SIDDNV
searchResult := searchResult "LEFT JOIN HRS.DNV_DNVORSCHLAG ON DNV_HRSSEQID = SID_HRSSEQID" "~"
if joinKuerzel = LAWSID
searchResult := searchResult "LEFT JOIN HRS.SID_HRSSEQ ON SID_HRSSEQID = LAW_KVID" "~"
if joinKuerzel = SIDLAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_KVID = SID_HRSSEQID" "~"
if joinKuerzel = SLKSLK
searchResult := searchResult "LEFT JOIN WWS.SLK_STUECKLISTENKOPF ON SLK_STKLISTKOPFID = SLK_URSPRUNG_STKLISTKOPFID" "~"
if joinKuerzel = SLKSLK
searchResult := searchResult "LEFT JOIN WWS.SLK_STUECKLISTENKOPF ON SLK_URSPRUNG_STKLISTKOPFID = SLK_STKLISTKOPFID" "~"
if joinKuerzel = ADDSMI
searchResult := searchResult "LEFT JOIN SMI_SMARTINTERFACE.SMI_SMARTINTERFACE ON SMI_FIRMENNR = ADD_FIRMENNR AND SMI_SMARTINTERFACEID = ADD_WWSAWDSMIID" "~"
if joinKuerzel = SMIADD
searchResult := searchResult "LEFT JOIN BUERO.ADD_AUSDRUCKMODELLDETAIL ON ADD_FIRMENNR = SMI_FIRMENNR AND ADD_WWSAWDSMIID = SMI_SMARTINTERFACEID" "~"
if joinKuerzel = AWESMI
searchResult := searchResult "LEFT JOIN ZLE.SMI_SMARTINTERFACE ON SMI_FIRMENNR = AWE_SMI_FIRMENNR AND SMI_SMARTINTERFACEID = AWE_SMI_SMARTINTERFACEID" "~"
if joinKuerzel = SMIAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_SMI_FIRMENNR = SMI_FIRMENNR AND AWE_SMI_SMARTINTERFACEID = SMI_SMARTINTERFACEID" "~"
if joinKuerzel = SZUSNI
searchResult := searchResult "LEFT JOIN HRS.SNI_SCHNITTE ON SNI_DBSNR = SZU_DBSNR AND SNI_SCHNITTNR = SZU_SCHNITTNR" "~"
if joinKuerzel = SNISZU
searchResult := searchResult "LEFT JOIN HRS.SZU_SCHNITTLAZU ON SZU_DBSNR = SNI_DBSNR AND SZU_SCHNITTNR = SNI_SCHNITTNR" "~"
if joinKuerzel = LAWSNI
searchResult := searchResult "LEFT JOIN HRS.SNI_SCHNITTE ON SNI_DBSNR = LAW_DBSNR AND SNI_SCHNITTNR = LAW_SCHNITTNR_1" "~"
if joinKuerzel = SNILAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_DBSNR = SNI_DBSNR AND LAW_SCHNITTNR_1 = SNI_SCHNITTNR" "~"
if joinKuerzel = KOPSNR
searchResult := searchResult "LEFT JOIN SNR_SERIENNUMMERN.SNR_SERIENNUMMERN ON SNR_SERNRID = KOP_SERNRID" "~"
if joinKuerzel = SNRKOP
searchResult := searchResult "LEFT JOIN PRODUKTION.KOP_KOMMISSIONIERUNG_POS ON KOP_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = LUESNR
searchResult := searchResult "LEFT JOIN SNR_SERIENNUMMERN.SNR_SERIENNUMMERN ON SNR_SERNRID = LUE_SERNRID" "~"
if joinKuerzel = SNRLUE
searchResult := searchResult "LEFT JOIN PRODUKTION.LUE_LAGER_UMS_EMPFANGEN ON LUE_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = EFKVER
searchResult := searchResult "LEFT JOIN VER_VERSANDART.VER_VERSANDART ON VER_VERSANDARTNR = EFK_VERSANDARTNR" "~"
if joinKuerzel = VEREFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_VERSANDARTNR = VER_VERSANDARTNR" "~"
if joinKuerzel = POSVER
searchResult := searchResult "LEFT JOIN BUERO.VER_VERSANDART ON VER_VERSANDARTNR = POS_VERSANDARTNR" "~"
if joinKuerzel = VERPOS
searchResult := searchResult "LEFT JOIN BUERO.POS_POST ON POS_VERSANDARTNR = VER_VERSANDARTNR" "~"
if joinKuerzel = AVDVET
searchResult := searchResult "LEFT JOIN BUERO.VET_VERANSTALTUNGSTAG ON VET_FIRMENNR = AVD_VET_FIRMENNR AND VET_VSTAMMNR = AVD_VET_VSTAMMNR AND VET_VERANSTALTUNGSNR = AVD_VET_VERANSTALTUNGSNR AND VET_VERANSTALTUNGSTAGNR = AVD_VET_VERANSTALTUNGSTAGNR" "~"
if joinKuerzel = VETAVD
searchResult := searchResult "LEFT JOIN BUERO.AVD_AUSBVERWDOKUMENTZU ON AVD_VET_FIRMENNR = VET_FIRMENNR AND AVD_VET_VSTAMMNR = VET_VSTAMMNR AND AVD_VET_VERANSTALTUNGSNR = VET_VERANSTALTUNGSNR AND AVD_VET_VERANSTALTUNGSTAGNR = VET_VERANSTALTUNGSTAGNR" "~"
if joinKuerzel = VTZVET
searchResult := searchResult "LEFT JOIN BUERO.VET_VERANSTALTUNGSTAG ON VET_FIRMENNR = VTZ_VET_FIRMENNR AND VET_VSTAMMNR = VTZ_VET_VSTAMMNR AND VET_VERANSTALTUNGSNR = VTZ_VET_VERANSTALTUNGSNR AND VET_VERANSTALTUNGSTAGNR = VTZ_VETVERANSTALTUNGSTAGNR" "~"
if joinKuerzel = VETVTZ
searchResult := searchResult "LEFT JOIN BUERO.VTZ_VERANSTALTUNGTERMINZU ON VTZ_VET_FIRMENNR = VET_FIRMENNR AND VTZ_VET_VSTAMMNR = VET_VSTAMMNR AND VTZ_VET_VERANSTALTUNGSNR = VET_VERANSTALTUNGSNR AND VTZ_VETVERANSTALTUNGSTAGNR = VET_VERANSTALTUNGSTAGNR" "~"
if joinKuerzel = DVEVEV
searchResult := searchResult "LEFT JOIN VEV_NTCSVERSION.VEV_NTCSVERSION ON VEV_VEV_VERSIONSNR = DVE_VERSIONSNR" "~"
if joinKuerzel = VEVDVE
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DVE_VERSION ON DVE_VERSIONSNR = VEV_VEV_VERSIONSNR" "~"
if joinKuerzel = EFKVKA
searchResult := searchResult "LEFT JOIN KORE.VKA_VIEW_KOSTAMM_ABT ON VKA_FIRMENNR = EFK_FIRMENNR AND VKA_KOSTENSTAMMNR = EFK_ABTEIL_KOSTENSTAMMNR" "~"
if joinKuerzel = VKAEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = VKA_FIRMENNR AND EFK_ABTEIL_KOSTENSTAMMNR = VKA_KOSTENSTAMMNR" "~"
if joinKuerzel = WVDVLJ
searchResult := searchResult "LEFT JOIN WWS.VLJ_VERLEIHJOURNAL ON VLJ_JOURNALID = WVD_LIEFERUNG_VLJID" "~"
if joinKuerzel = VLJWVD
searchResult := searchResult "LEFT JOIN WWS.WVD_WEINSATZVERLEIHDAT ON WVD_LIEFERUNG_VLJID = VLJ_JOURNALID" "~"
if joinKuerzel = PBAVOB
searchResult := searchResult "LEFT JOIN PRODUKTION.VOB_VORGANGSBENACHR ON VOB_VOBID = PBA_VOBID" "~"
if joinKuerzel = VOBPBA
searchResult := searchResult "LEFT JOIN PRODUKTION.PBA_PLAN_BAS_ALT ON PBA_VOBID = VOB_VOBID" "~"
if joinKuerzel = WAPVWU
searchResult := searchResult "LEFT JOIN WWS.VWU_VA_WAPZUORDINFO ON VWU_WARENEINGKOPFID = WAP_WARENEINGKOPFID AND VWU_WARENEINGPOSLFDNR = WAP_WARENEINGPOSLFDNR" "~"
if joinKuerzel = VWUWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_WARENEINGKOPFID = VWU_WARENEINGKOPFID AND WAP_WARENEINGPOSLFDNR = VWU_WARENEINGPOSLFDNR" "~"
if joinKuerzel = AVEWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = AVE_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = KAEWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = KAE_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEKAE
searchResult := searchResult "LEFT JOIN WWS.KAE_KASSENEINAUSGABEN ON KAE_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = KSDWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = KSD_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEKSD
searchResult := searchResult "LEFT JOIN WWS.KSD_KASSENSUMMENDETAIL ON KSD_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = PERWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = PER_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = SAPWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = SAP_WAEHRUNGSCODE" "~"
if joinKuerzel = WAESAP
searchResult := searchResult "LEFT JOIN BUERO.SAP_SAMMELPROJEKT ON SAP_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = VSRWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = VSR_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEVSR
searchResult := searchResult "LEFT JOIN FIBU.VSR_VSTERSTATTUNG ON VSR_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = ZBRWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = ZBR_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEZBR
searchResult := searchResult "LEFT JOIN FIBU.ZBR_ZINSBERECHNUNG ON ZBR_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = ZINWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = ZIN_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEZIN
searchResult := searchResult "LEFT JOIN FIBU.ZIN_ZAHLUNGSINFORMATION ON ZIN_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = KTOWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = KTO_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = AMZWAG
searchResult := searchResult "LEFT JOIN WAG_WARENGRUPPE.WAG_WARENGRUPPE ON WAG_FIRMENNR = AMZ_FIRMENNR AND WAG_WARENHAUPTGRNR = AMZ_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = AMZ_WARENUNTERGRNR" "~"
if joinKuerzel = WAGAMZ
searchResult := searchResult "LEFT JOIN BUERO.AMZ_AUSDRUCKMODELLZU ON AMZ_FIRMENNR = WAG_FIRMENNR AND AMZ_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND AMZ_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = MITWAG
searchResult := searchResult "LEFT JOIN WAG_WARENGRUPPE.WAG_WARENGRUPPE ON WAG_FIRMENNR = MIT_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = MIT_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = MIT_WARENUNTERGRNR" "~"
if joinKuerzel = WAGMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_WAG_FIRMENNR = WAG_FIRMENNR AND MIT_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND MIT_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = SKLWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = SKL_WAEHRUNGSCODE" "~"
if joinKuerzel = WAESKL
searchResult := searchResult "LEFT JOIN WWS.SKL_STUECKELUNGSLISTE ON SKL_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = WBDWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = WBD_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEWBD
searchResult := searchResult "LEFT JOIN WWS.WBD_WARENBESCHKOSTENDET ON WBD_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = ZKRWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = ZKR_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEZKR
searchResult := searchResult "LEFT JOIN FIBU.ZKR_ZAHLUNGSKREIS ON ZKR_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = ZLGWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = ZLG_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEZLG
searchResult := searchResult "LEFT JOIN ZLE.ZLG_ZAHLUNGEN ON ZLG_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = KUSWAE
searchResult := searchResult "LEFT JOIN BMD.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = KUS_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEKUS
searchResult := searchResult "LEFT JOIN BMD.KUS_KURS ON KUS_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = RNAWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = RNA_RECH_WAEHRUNGSCODE" "~"
if joinKuerzel = WAERNA
searchResult := searchResult "LEFT JOIN WWS.RNA_RECHNUNGANZAHLUNG ON RNA_RECH_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = GEGWAG
searchResult := searchResult "LEFT JOIN WAG_WARENGRUPPE.WAG_WARENGRUPPE ON WAG_FIRMENNR = GEG_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = GEG_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = GEG_WARENUNTERGRNR" "~"
if joinKuerzel = WAGGEG
searchResult := searchResult "LEFT JOIN ZLE.GEG_GEGENSTANDSWERTE ON GEG_WAG_FIRMENNR = WAG_FIRMENNR AND GEG_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND GEG_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = IVKWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = IVK_FIRMENNR AND WAG_WARENHAUPTGRNR = IVK_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = IVK_WARENUNTERGRNR" "~"
if joinKuerzel = WAGIVK
searchResult := searchResult "LEFT JOIN WWS.IVK_INVENTURKOPF ON IVK_FIRMENNR = WAG_FIRMENNR AND IVK_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND IVK_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = PKAWAG
searchResult := searchResult "LEFT JOIN WAG_WARENGRUPPE.WAG_WARENGRUPPE ON WAG_FIRMENNR = PKA_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = PKA_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = PKA_WARENUNTERGRNR" "~"
if joinKuerzel = WAGPKA
searchResult := searchResult "LEFT JOIN BUERO.PKA_PROJEKTART ON PKA_WAG_FIRMENNR = WAG_FIRMENNR AND PKA_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND PKA_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = REZWAG
searchResult := searchResult "LEFT JOIN WAG_WARENGRUPPE.WAG_WARENGRUPPE ON WAG_FIRMENNR = REZ_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = REZ_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = REZ_WARENUNTERGRNR" "~"
if joinKuerzel = WAGREZ
searchResult := searchResult "LEFT JOIN ZLE.REZ_REGELWERKZU ON REZ_WAG_FIRMENNR = WAG_FIRMENNR AND REZ_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND REZ_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = WEWWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = WEW_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = WEW_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = WEW_WARENUNTERGRNR" "~"
if joinKuerzel = WAGWEW
searchResult := searchResult "LEFT JOIN WWS.WEW_WARENEINSATZWGZU ON WEW_WAG_FIRMENNR = WAG_FIRMENNR AND WEW_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND WEW_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = LEIWAG
searchResult := searchResult "LEFT JOIN WAG_WARENGRUPPE.WAG_WARENGRUPPE ON WAG_FIRMENNR = LEI_MA_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = LEI_MA_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = LEI_MA_WARENUNTERGRNR" "~"
if joinKuerzel = WAGLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_MA_WAG_FIRMENNR = WAG_FIRMENNR AND LEI_MA_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND LEI_MA_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = EPSWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_WARENEINGKOPFID = EPS_WARENEINGKOPFID" "~"
if joinKuerzel = WAKEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_WARENEINGKOPFID = WAK_WARENEINGKOPFID" "~"
if joinKuerzel = TRJWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_WARENEINGKOPFID = TRJ_WARENEINGKOPFID" "~"
if joinKuerzel = WAKTRJ
searchResult := searchResult "LEFT JOIN WWS.TRJ_TRANSPORTEINH_JOURNAL ON TRJ_WARENEINGKOPFID = WAK_WARENEINGKOPFID" "~"
if joinKuerzel = WTEWAM
searchResult := searchResult "LEFT JOIN BUERO.WAM_WEBANMELDUNG ON WAM_WAMID = WTE_WAMID" "~"
if joinKuerzel = WAMWTE
searchResult := searchResult "LEFT JOIN BUERO.WTE_WEBTEILNEHMER ON WTE_WAMID = WAM_WAMID" "~"
if joinKuerzel = WEUWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_WARENEINGKOPFID = WEU_WAK_WARENEINGKOPFID AND WAP_WARENEINGPOSLFDNR = WEU_WAP_WARENEINGPOSLFDNR" "~"
if joinKuerzel = WAPWEU
searchResult := searchResult "LEFT JOIN WWS.WEU_WWSERKONTROLLEZU ON WEU_WAK_WARENEINGKOPFID = WAP_WARENEINGKOPFID AND WEU_WAP_WARENEINGPOSLFDNR = WAP_WARENEINGPOSLFDNR" "~"
if joinKuerzel = WRPWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_WARENEINGKOPFID = WRP_WARENEINGKOPFID AND WAP_WARENEINGPOSLFDNR = WRP_WARENEINGPOSLFDNR" "~"
if joinKuerzel = WAPWRP
searchResult := searchResult "LEFT JOIN WWS.WRP_WWSANKAUFSRNGPOS ON WRP_WARENEINGKOPFID = WAP_WARENEINGKOPFID AND WRP_WARENEINGPOSLFDNR = WAP_WARENEINGPOSLFDNR" "~"
if joinKuerzel = BSKWBT
searchResult := searchResult "LEFT JOIN WWS.WBT_WWSBUDGETKOPF ON WBT_WWSBUDGETKOPFID = BSK_BUDGETKOPFID" "~"
if joinKuerzel = WBTBSK
searchResult := searchResult "LEFT JOIN WWS.BSK_BONUSERMKOPF ON BSK_BUDGETKOPFID = WBT_WWSBUDGETKOPFID" "~"
if joinKuerzel = WBTWBT
searchResult := searchResult "LEFT JOIN WWS.WBT_WWSBUDGETKOPF ON WBT_WWSBUDGETKOPFID = WBT_PLAN_BUDGETKOPFID" "~"
if joinKuerzel = WBTWBT
searchResult := searchResult "LEFT JOIN WWS.WBT_WWSBUDGETKOPF ON WBT_PLAN_BUDGETKOPFID = WBT_WWSBUDGETKOPFID" "~"
if joinKuerzel = WCFWCE
searchResult := searchResult "LEFT JOIN BMD.WCE_WABAUMELEMENTE ON WCE_WABAUMELEMNR = WCF_WABAUMELEMNR" "~"
if joinKuerzel = WCEWCF
searchResult := searchResult "LEFT JOIN BMD.WCF_WABAUMELEMAUSDRUCKZU ON WCF_WABAUMELEMNR = WCE_WABAUMELEMNR" "~"
if joinKuerzel = ZIBZIK
searchResult := searchResult "LEFT JOIN FIBU.ZIK_KONTENZINSKREIS ON ZIK_FIRMENNR = ZIB_FIRMENNR AND ZIK_FIBUNR = ZIB_FIBUNR AND ZIK_ZIKLFDNR = ZIB_ZIKLFDNR" "~"
if joinKuerzel = ZIKZIB
searchResult := searchResult "LEFT JOIN FIBU.ZIB_KONTENZINSENBERECHNG ON ZIB_FIRMENNR = ZIK_FIRMENNR AND ZIB_FIBUNR = ZIK_FIBUNR AND ZIB_ZIKLFDNR = ZIK_ZIKLFDNR" "~"
if joinKuerzel = ZNGZNS
searchResult := searchResult "LEFT JOIN BUERO.ZNS_ZEITNACHRICHTSYSTEM ON ZNS_ZEITNACHRICHTID = ZNG_ZEITNACHRICHTID" "~"
if joinKuerzel = ZNSZNG
searchResult := searchResult "LEFT JOIN BUERO.ZNG_ZEITNACHRICHTGESENDET ON ZNG_ZEITNACHRICHTID = ZNS_ZEITNACHRICHTID" "~"
if joinKuerzel = ASWZPK
searchResult := searchResult "LEFT JOIN BUERO.ZPK_ZEITPROFILKOPF ON ZPK_FIRMENNR = ASW_ZEITPROFIL_FIRMENNR AND ZPK_ZEITPROFILNR = ASW_ZEITPROFILNR" "~"
if joinKuerzel = ZPKASW
searchResult := searchResult "LEFT JOIN BUERO.ASW_AUSWEIS ON ASW_ZEITPROFIL_FIRMENNR = ZPK_FIRMENNR AND ASW_ZEITPROFILNR = ZPK_ZEITPROFILNR" "~"
if joinKuerzel = ZPDZPK
searchResult := searchResult "LEFT JOIN BUERO.ZPK_ZEITPROFILKOPF ON ZPK_FIRMENNR = ZPD_FIRMENNR AND ZPK_ZEITPROFILNR = ZPD_ZEITPROFILNR" "~"
if joinKuerzel = ZPKZPD
searchResult := searchResult "LEFT JOIN BUERO.ZPD_ZEITPROFILDETAIL ON ZPD_FIRMENNR = ZPK_FIRMENNR AND ZPD_ZEITPROFILNR = ZPK_ZEITPROFILNR" "~"
if joinKuerzel = EFKZRT
searchResult := searchResult "LEFT JOIN ZRT_ZAHLUNGSART.ZRT_ZAHLUNGSART ON ZRT_ZAHLARTNR = EFK_ZAHLARTNR" "~"
if joinKuerzel = ZRTEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_ZAHLARTNR = ZRT_ZAHLARTNR" "~"
if joinKuerzel = KZAZRT
searchResult := searchResult "LEFT JOIN ZRT_ZAHLUNGSART.ZRT_ZAHLUNGSART ON ZRT_ZAHLARTNR = KZA_ZAHLARTNR" "~"
if joinKuerzel = ZRTKZA
searchResult := searchResult "LEFT JOIN WWS.KZA_KASSENZAHLUNGSARTENZU ON KZA_ZAHLARTNR = ZRT_ZAHLARTNR" "~"
if joinKuerzel = RVKZRT
searchResult := searchResult "LEFT JOIN ZRT_ZAHLUNGSART.ZRT_ZAHLUNGSART ON ZRT_ZAHLARTNR = RVK_ZAHLARTNR" "~"
if joinKuerzel = ZRTRVK
searchResult := searchResult "LEFT JOIN ZLE.RVK_RECHVORLAGEKOPF ON RVK_ZAHLARTNR = ZRT_ZAHLARTNR" "~"
if joinKuerzel = BAIZRT
searchResult := searchResult "LEFT JOIN BUERO.ZRT_ZAHLUNGSART ON ZRT_ZAHLARTNR = BAI_ZRT_ZAHLARTNR4" "~"
if joinKuerzel = ZRTBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_ZRT_ZAHLARTNR4 = ZRT_ZAHLARTNR" "~"
if joinKuerzel = BAIZRT
searchResult := searchResult "LEFT JOIN BUERO.ZRT_ZAHLUNGSART ON ZRT_ZAHLARTNR = BAI_ZRT_ZAHLARTNR5" "~"
if joinKuerzel = ZRTBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_ZRT_ZAHLARTNR5 = ZRT_ZAHLARTNR" "~"
if joinKuerzel = TSRZTZ
searchResult := searchResult "LEFT JOIN ZTZ_ZEITZONEN.ZTZ_ZEITZONEN ON ZTZ_ZEITZONENR = TSR_VON_ZEITZONENR" "~"
if joinKuerzel = ZTZTSR
searchResult := searchResult "LEFT JOIN BUERO.TSR_SERIENTERMIN ON TSR_VON_ZEITZONENR = ZTZ_ZEITZONENR" "~"
if joinKuerzel = ZVDZVK
searchResult := searchResult "LEFT JOIN ZVK_ZAHLUNGSVORSCHLAG.ZVK_ZAHLUNGSVORSCHLAG ON ZVK_FIRMENNR = ZVD_ZKR_FIRMENNR AND ZVK_FIBUNR = ZVD_FIBUNR AND ZVK_ZKR_ZAHLUNGSKREISLFDNR = ZVD_ZAHLUNGSKREISLFDNR AND ZVK_BUC_BUCHUNGSID = ZVD_BUC_BUCHUNGSID" "~"
if joinKuerzel = ZVKZVD
searchResult := searchResult "LEFT JOIN ZX_GELOESCHT.ZVD_ZAHLUNGSDETAILS ON ZVD_ZKR_FIRMENNR = ZVK_FIRMENNR AND ZVD_FIBUNR = ZVK_FIBUNR AND ZVD_ZAHLUNGSKREISLFDNR = ZVK_ZKR_ZAHLUNGSKREISLFDNR AND ZVD_BUC_BUCHUNGSID = ZVK_BUC_BUCHUNGSID" "~"
if joinKuerzel = BAIZVT
searchResult := searchResult "LEFT JOIN BUERO.ZVT_TBDATEIEN ON ZVT_ZVTID = BAI_TBFDRITTLAND_ZVTID" "~"
if joinKuerzel = ZVTBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_TBFDRITTLAND_ZVTID = ZVT_ZVTID" "~"
if joinKuerzel = EQIZWK
searchResult := searchResult "LEFT JOIN WWS.ZWK_ZUORDNUNGWVKOPF ON ZWK_BMDWVNR = EQI_BMDWVNR" "~"
if joinKuerzel = ZWKEQI
searchResult := searchResult "LEFT JOIN WWS.EQI_ERFASSPOSQUELLINFO ON EQI_BMDWVNR = ZWK_BMDWVNR" "~"
if joinKuerzel = WAPZWK
searchResult := searchResult "LEFT JOIN WWS.ZWK_ZUORDNUNGWVKOPF ON ZWK_BMDWVNR = WAP_BMDWVNR" "~"
if joinKuerzel = ZWKWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_BMDWVNR = ZWK_BMDWVNR" "~"
if joinKuerzel = ZWPZWK
searchResult := searchResult "LEFT JOIN WWS.ZWK_ZUORDNUNGWVKOPF ON ZWK_BMDWVNR = ZWP_BMDWVNR" "~"
if joinKuerzel = ZWKZWP
searchResult := searchResult "LEFT JOIN WWS.ZWP_ZUORDNUNGWVPOS ON ZWP_BMDWVNR = ZWK_BMDWVNR" "~"
if joinKuerzel = ZXDZXA
searchResult := searchResult "LEFT JOIN BUERO.ZXA_ZEITAENDERUNGSANTRAG ON ZXA_ZANTRAGID = ZXD_ZANTRAGID" "~"
if joinKuerzel = ZXAZXD
searchResult := searchResult "LEFT JOIN BUERO.ZXD_ZEITAENDERUNG ON ZXD_ZANTRAGID = ZXA_ZANTRAGID" "~"
if joinKuerzel = KLIPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = KLI_FIRMENNR AND PER_PERSONENID = KLI_KLID" "~"
if joinKuerzel = PERKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = PER_FIRMENNR AND KLI_KLID = PER_PERSONENID" "~"
if joinKuerzel = BPKVKG
searchResult := searchResult "LEFT JOIN KORE.VKG_VIEW_KOSTAMM_GBR ON VKG_FIRMENNR = BPK_FIRMENNR AND VKG_KOSTENSTAMMNR = BPK_KST_GBR_KOSTENSTAMMNR" "~"
if joinKuerzel = VKGBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_FIRMENNR = VKG_FIRMENNR AND BPK_KST_GBR_KOSTENSTAMMNR = VKG_KOSTENSTAMMNR" "~"
if joinKuerzel = AADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AAD_LETZTERUSER" "~"
if joinKuerzel = BMUAAD
searchResult := searchResult "LEFT JOIN BIL.AAD_AUSDRUCKDETAIL ON AAD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AAEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AAE_LETZTERUSER" "~"
if joinKuerzel = BMUAAE
searchResult := searchResult "LEFT JOIN WWS.AAE_ANGEBOTSAUSW_ERFQ_TMP ON AAE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABJ_LETZTERUSER" "~"
if joinKuerzel = BMUABJ
searchResult := searchResult "LEFT JOIN BIL.ABJ_ANBUBEWRESJOURNAL ON ABJ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABL_LETZTERUSER" "~"
if joinKuerzel = BMUABL
searchResult := searchResult "LEFT JOIN BUERO.ABL_AVBUDGETDETAIL ON ABL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WICWIC
searchResult := searchResult "LEFT JOIN BIL.WIC_WIPCHECKLIST ON WIC_WICHECKLISTID = WIC_PARENT_CHECKLISTID" "~"
if joinKuerzel = WICWIC
searchResult := searchResult "LEFT JOIN BIL.WIC_WIPCHECKLIST ON WIC_PARENT_CHECKLISTID = WIC_WICHECKLISTID" "~"
if joinKuerzel = WILWII
searchResult := searchResult "LEFT JOIN BMDENTW.WII_WEITERE_INFO_ITEM ON WII_WIIID = WIL_WIIID" "~"
if joinKuerzel = WIIWIL
searchResult := searchResult "LEFT JOIN BMDENTW.WIL_WEITERE_INFO_LANG ON WIL_WIIID = WII_WIIID" "~"
if joinKuerzel = WIVWIP
searchResult := searchResult "LEFT JOIN BIL.WIP_WIPRUEFUNG ON WIP_WIPRUEFUNGSID = WIV_WIPRUEFUNGSID" "~"
if joinKuerzel = WIPWIV
searchResult := searchResult "LEFT JOIN BIL.WIV_WIVARIABLEN ON WIV_WIPRUEFUNGSID = WIP_WIPRUEFUNGSID" "~"
if joinKuerzel = WKRWKA
searchResult := searchResult "LEFT JOIN WWS.WKA_WARENAUSGKOSTENARTEN ON WKA_FIRMENNR = WKR_WKA_FIRMENNR AND WKA_WKALFDNR = WKR_WKALFDNR" "~"
if joinKuerzel = WKAWKR
searchResult := searchResult "LEFT JOIN WWS.WKR_WARENAUSGKOSTENRECH ON WKR_WKA_FIRMENNR = WKA_FIRMENNR AND WKR_WKALFDNR = WKA_WKALFDNR" "~"
if joinKuerzel = WEPWKR
searchResult := searchResult "LEFT JOIN WWS.WKR_WARENAUSGKOSTENRECH ON WKR_WKRID = WEP_WKRID" "~"
if joinKuerzel = WKRWEP
searchResult := searchResult "LEFT JOIN WWS.WEP_WWSERKONTROLLEPOS ON WEP_WKRID = WKR_WKRID" "~"
if joinKuerzel = WEUWKR
searchResult := searchResult "LEFT JOIN WWS.WKR_WARENAUSGKOSTENRECH ON WKR_WKRID = WEU_WKRID" "~"
if joinKuerzel = WKRWEU
searchResult := searchResult "LEFT JOIN WWS.WEU_WWSERKONTROLLEZU ON WEU_WKRID = WKR_WKRID" "~"
if joinKuerzel = ARLWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = ARL_FIRMENNR AND WKT_KONTENTABNR = ARL_KONTENTABNR" "~"
if joinKuerzel = WKTARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = WKT_FIRMENNR AND ARL_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = EFKWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = EFK_HAUPTFIRMENNR AND WKT_KONTENTABNR = EFK_BRUTTOABG_KONTENTABNR" "~"
if joinKuerzel = WKTEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_HAUPTFIRMENNR = WKT_FIRMENNR AND EFK_BRUTTOABG_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = ERZWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = ERZ_HAUPTFIRMENNR AND WKT_KONTENTABNR = ERZ_BRUTTOABG_KONTENTABNR" "~"
if joinKuerzel = WKTERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_HAUPTFIRMENNR = WKT_FIRMENNR AND ERZ_BRUTTOABG_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = RADWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = RAD_HAUPTFIRMENNR AND WKT_KONTENTABNR = RAD_KONTENTABNR" "~"
if joinKuerzel = WKTRAD
searchResult := searchResult "LEFT JOIN WWS.RAD_RABATTBETRAGDETAIL ON RAD_HAUPTFIRMENNR = WKT_FIRMENNR AND RAD_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = TRSWKT
searchResult := searchResult "LEFT JOIN WKT_WWSKONTENTAB.WKT_WWSKONTENTAB ON WKT_FIRMENNR = TRS_WKT_FIRMENNR AND WKT_KONTENTABNR = TRS_WKT_KONTENTABNR" "~"
if joinKuerzel = WKTTRS
searchResult := searchResult "LEFT JOIN BUERO.TRS_TEILSCHLUSSRECHSCHEMA ON TRS_WKT_FIRMENNR = WKT_FIRMENNR AND TRS_WKT_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = PSWWMD
searchResult := searchResult "LEFT JOIN BUERO.WMD_WOCHENMODELL ON WMD_FIRMENNR = PSW_WMD_FIRMENNR AND WMD_WOCHENMODELLNR = PSW_WOCHENMODELLNR" "~"
if joinKuerzel = WMDPSW
searchResult := searchResult "LEFT JOIN BUERO.PSW_PERSONENWERTE ON PSW_WMD_FIRMENNR = WMD_FIRMENNR AND PSW_WOCHENMODELLNR = WMD_WOCHENMODELLNR" "~"
if joinKuerzel = PVWWMD
searchResult := searchResult "LEFT JOIN BUERO.WMD_WOCHENMODELL ON WMD_FIRMENNR = PVW_WMD_FIRMENNR AND WMD_WOCHENMODELLNR = PVW_WOCHENMODELLNR" "~"
if joinKuerzel = WMDPVW
searchResult := searchResult "LEFT JOIN BUERO.PVW_PERSONVORERFASSWOCHSTD ON PVW_WMD_FIRMENNR = WMD_FIRMENNR AND PVW_WOCHENMODELLNR = WMD_WOCHENMODELLNR" "~"
if joinKuerzel = WRKWRK
searchResult := searchResult "LEFT JOIN WWS.WRK_WWSANKAUFSRNGKOPF ON WRK_FIRMENNR = WRK_FIRMENNR AND WRK_RECHNUNGLFDNR = WRK_URSRECHNUNGLFDNR" "~"
if joinKuerzel = WRKWRK
searchResult := searchResult "LEFT JOIN WWS.WRK_WWSANKAUFSRNGKOPF ON WRK_FIRMENNR = WRK_FIRMENNR AND WRK_URSRECHNUNGLFDNR = WRK_RECHNUNGLFDNR" "~"
if joinKuerzel = AGSWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = AGS_WTB_FIRMENNR AND WTB_WWSTEXTBNR = AGS_WTB_WWSTEXTBNR" "~"
if joinKuerzel = WTBAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_WTB_FIRMENNR = WTB_FIRMENNR AND AGS_WTB_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = AVEWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = AVE_BESCHR_FIRMENNR AND WTB_WWSTEXTBNR = AVE_BESCHR_WWSTEXTBNR" "~"
if joinKuerzel = WTBAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_BESCHR_FIRMENNR = WTB_FIRMENNR AND AVE_BESCHR_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = ERGWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = ERG_WTB_FIRMENNR AND WTB_WWSTEXTBNR = ERG_WWSTEXTBNR" "~"
if joinKuerzel = WTBERG
searchResult := searchResult "LEFT JOIN WWS.ERG_ERFASSUNGSGRUPPEN ON ERG_WTB_FIRMENNR = WTB_FIRMENNR AND ERG_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = LMSWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = LMS_WWSTEXTBNR_FIRMENNR AND WTB_WWSTEXTBNR = LMS_WWSTEXTBNR_STUFE1" "~"
if joinKuerzel = WTBLMS
searchResult := searchResult "LEFT JOIN WWS.LMS_LIEFERMAHNUNG ON LMS_WWSTEXTBNR_FIRMENNR = WTB_FIRMENNR AND LMS_WWSTEXTBNR_STUFE1 = WTB_WWSTEXTBNR" "~"
if joinKuerzel = PROWTB
searchResult := searchResult "LEFT JOIN WTB_WWSTEXTBAUSTEIN.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = PRO_WTB_FIRMENNR AND WTB_WWSTEXTBNR = PRO_WTB_WWSTEXTBNR" "~"
if joinKuerzel = WTBPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_WTB_FIRMENNR = WTB_FIRMENNR AND PRO_WTB_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = KAPWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = KAP_WTB_BESCH_FIRMENNR AND WTB_WWSTEXTBNR = KAP_WTB_BESCH_WWSTEXTBNR" "~"
if joinKuerzel = WTBKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_WTB_BESCH_FIRMENNR = WTB_FIRMENNR AND KAP_WTB_BESCH_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = WKGWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = WKG_FIRMENNR AND WTB_WWSTEXTBNR = WKG_SCHLUSSTEXT_WWSTEXTBNR" "~"
if joinKuerzel = WTBWKG
searchResult := searchResult "LEFT JOIN WWS.WKG_WWSKUNDENGRUPPE ON WKG_FIRMENNR = WTB_FIRMENNR AND WKG_SCHLUSSTEXT_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = BVOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BVO_LETZTERUSER" "~"
if joinKuerzel = BMUBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BWRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BWR_LETZTERUSER" "~"
if joinKuerzel = BMUBWR
searchResult := searchResult "LEFT JOIN BIL.BWR_BEWERTRESERVESUMMEN ON BWR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BWUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BWU_LETZTERUSER" "~"
if joinKuerzel = BMUBWU
searchResult := searchResult "LEFT JOIN BUERO.BWU_BEWERTUNGUSERFIELD ON BWU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BZTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BZT_LETZTERUSER" "~"
if joinKuerzel = BMUBZT
searchResult := searchResult "LEFT JOIN HRS.BZT_BGZUTARIFGRUPPE ON BZT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CAWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CAW_LETZTERUSER" "~"
if joinKuerzel = BMUCAW
searchResult := searchResult "LEFT JOIN BUERO.CAW_CRMABWESENHEIT ON CAW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CDCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CDC_LETZTERUSER" "~"
if joinKuerzel = BMUCDC
searchResult := searchResult "LEFT JOIN BMD.CDC_COMPASSDATACACHE ON CDC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CLWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CLW_LETZTERUSER" "~"
if joinKuerzel = BMUCLW
searchResult := searchResult "LEFT JOIN BUERO.CLW_CLAUSWAHLFELDWERT ON CLW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CMDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CMD_LETZTERUSER" "~"
if joinKuerzel = BMUCMD
searchResult := searchResult "LEFT JOIN BMDDEFAULT.CMD_CMDLINEPARAM ON CMD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CPRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CPR_LETZTERUSER" "~"
if joinKuerzel = BMUCPR
searchResult := searchResult "LEFT JOIN BUERO.CPR_CRMPROTOKOLL ON CPR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DAMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DAM_LETZTERUSER" "~"
if joinKuerzel = BMUDAM
searchResult := searchResult "LEFT JOIN DATANORM.DAM_DATANORMARTIKELMATCH ON DAM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DBDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DBD_LETZTERUSER" "~"
if joinKuerzel = BMUDBD
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DBD_KEYBREAKDOWN_PD ON DBD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DCZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DCZ_LETZTERUSER" "~"
if joinKuerzel = BMUDCZ
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DCZ_WAAUSDRUCK_PARAM_ZU ON DCZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DFFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DFF_LETZTERUSER" "~"
if joinKuerzel = BMUDFF
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFF_DFELDBESCHFORMAT ON DFF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DFLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DFL_LETZTERUSER" "~"
if joinKuerzel = BMUDFL
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFL_DFUNKBERECHT_LINK ON DFL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DFUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DFU_LETZTERUSER" "~"
if joinKuerzel = BMUDFU
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFU_DFUNKTION ON DFU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DIDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DID_LETZTERUSER" "~"
if joinKuerzel = BMUDID
searchResult := searchResult "LEFT JOIN DATANORM.DID_DIM_TEXT_DETAIL ON DID_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DKABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DKA_LETZTERUSER" "~"
if joinKuerzel = BMUDKA
searchResult := searchResult "LEFT JOIN BUERO.DKA_DOCSKATEGORIEZU ON DKA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DOHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DOH_LETZTERUSER" "~"
if joinKuerzel = BMUDOH
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DOH_OCRTEMPLATEHEADER ON DOH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DOKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DOK_LETZTERUSER" "~"
if joinKuerzel = BMUDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DRBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DRB_LETZTERUSER" "~"
if joinKuerzel = BMUDRB
searchResult := searchResult "LEFT JOIN BMD.DRB_DRUCKBAUSTEINE ON DRB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DRUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DRU_LETZTERUSER" "~"
if joinKuerzel = BMUDRU
searchResult := searchResult "LEFT JOIN BMD.DRU_DRUCKER ON DRU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DRVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DRV_LETZTERUSER" "~"
if joinKuerzel = BMUDRV
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DRV_DREPORTVORSCHLAG ON DRV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DTDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DTD_LETZTERUSER" "~"
if joinKuerzel = BMUDTD
searchResult := searchResult "LEFT JOIN DATANORM.DTD_TEXT_DETAIL ON DTD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DTHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DTH_LETZTERUSER" "~"
if joinKuerzel = BMUDTH
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DTH_DEFTREEHEAD ON DTH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DTMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DTM_LETZTERUSER" "~"
if joinKuerzel = BMUDTM
searchResult := searchResult "LEFT JOIN DATANORM.DTM_TEXT ON DTM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DTUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DTU_LETZTERUSER" "~"
if joinKuerzel = BMUDTU
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DTU_DTABUEBERS ON DTU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FIRZSK
searchResult := searchResult "LEFT JOIN ZSK_ZEITSCHEIBEKOPF.ZSK_ZEITSCHEIBEKOPF ON ZSK_ZSKID = FIR_ZSKID" "~"
if joinKuerzel = ZSKFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_ZSKID = ZSK_ZSKID" "~"
if joinKuerzel = ZVDZVM
searchResult := searchResult "LEFT JOIN BUERO.ZVM_ZEITPLANUNGVERSION ON ZVM_FIRMENNR = ZVD_FIRMENNR AND ZVM_DATUM_VON = ZVD_DATUM_VON AND ZVM_DATUM_BIS = ZVD_DATUM_BIS AND ZVM_PLG_FIRMENNR = ZVD_PLG_FIRMENNR AND ZVM_PLANGRLFDNR = ZVD_PLANGRLFDNR AND ZVM_ZVMVERSIONLFDNR = ZVD_ZVMVERSIONLFDNR" "~"
if joinKuerzel = ZVMZVD
searchResult := searchResult "LEFT JOIN BUERO.ZVD_ZEITPLANUNGVERSIONDAT ON ZVD_FIRMENNR = ZVM_FIRMENNR AND ZVD_DATUM_VON = ZVM_DATUM_VON AND ZVD_DATUM_BIS = ZVM_DATUM_BIS AND ZVD_PLG_FIRMENNR = ZVM_PLG_FIRMENNR AND ZVD_PLANGRLFDNR = ZVM_PLANGRLFDNR AND ZVD_ZVMVERSIONLFDNR = ZVM_ZVMVERSIONLFDNR" "~"
if joinKuerzel = LPOZWK
searchResult := searchResult "LEFT JOIN WWS.ZWK_ZUORDNUNGWVKOPF ON ZWK_BMDWVNR = LPO_BMDWVNR" "~"
if joinKuerzel = ZWKLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_BMDWVNR = ZWK_BMDWVNR" "~"
if joinKuerzel = AAMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AAM_LETZTERUSER" "~"
if joinKuerzel = BMUAAM
searchResult := searchResult "LEFT JOIN BIL.AAM_AUSDRUCKMASTER ON AAM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABS_LETZTERUSER" "~"
if joinKuerzel = BMUABS
searchResult := searchResult "LEFT JOIN HRS.ABS_ARBEITSSTAETTE ON ABS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABV_LETZTERUSER" "~"
if joinKuerzel = BMUABV
searchResult := searchResult "LEFT JOIN HRS.ABV_ABREVERGLEICH ON ABV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ACTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ACT_LETZTERUSER" "~"
if joinKuerzel = BMUACT
searchResult := searchResult "LEFT JOIN BIL.ACT_ANLAGEGUT_BERECH_TMP ON ACT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ACZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ACZ_LETZTERUSER" "~"
if joinKuerzel = BMUACZ
searchResult := searchResult "LEFT JOIN BIL.ACZ_ANLAGEGUTSACHKONTOZU ON ACZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ADBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ADB_LETZTERUSER" "~"
if joinKuerzel = BMUADB
searchResult := searchResult "LEFT JOIN WWS.ADB_ABGABENDETAILBON ON ADB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ADZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ADZ_LETZTERUSER" "~"
if joinKuerzel = BMUADZ
searchResult := searchResult "LEFT JOIN WWS.ADZ_ARTIKELDIENSTLNRZU ON ADZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AENBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AEN_LETZTERUSER" "~"
if joinKuerzel = BMUAEN
searchResult := searchResult "LEFT JOIN BUERO.AEN_AENDERUNGCRM ON AEN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AFIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AFI_LETZTERUSER" "~"
if joinKuerzel = BMUAFI
searchResult := searchResult "LEFT JOIN BMD.AFI_ABFRAGEFILTER ON AFI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AGRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AGR_LETZTERUSER" "~"
if joinKuerzel = BMUAGR
searchResult := searchResult "LEFT JOIN BUERO.AGR_ABRECHNUNGSGRUPPE ON AGR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AKABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AKA_LETZTERUSER" "~"
if joinKuerzel = BMUAKA
searchResult := searchResult "LEFT JOIN BUERO.AKA_AVKOSTENARTEN ON AKA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AKLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AKL_LETZTERUSER" "~"
if joinKuerzel = BMUAKL
searchResult := searchResult "LEFT JOIN WWS.AKL_ARTIKELKLASSIFIKATION ON AKL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AKUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AKU_LETZTERUSER" "~"
if joinKuerzel = BMUAKU
searchResult := searchResult "LEFT JOIN ZLE.AKU_ARTIKELKUNDEZULEA ON AKU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ALGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ALG_LETZTERUSER" "~"
if joinKuerzel = BMUALG
searchResult := searchResult "LEFT JOIN BIL.ALG_ANLAGENGRUPPE ON ALG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ANDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AND_LETZTERUSER" "~"
if joinKuerzel = BMUAND
searchResult := searchResult "LEFT JOIN BIL.AND_ANBUNUTZUNGSDAUER ON AND_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ANRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ANR_LETZTERUSER" "~"
if joinKuerzel = BMUANR
searchResult := searchResult "LEFT JOIN BUERO.ANR_ANREDE ON ANR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ATZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ATZ_LETZTERUSER" "~"
if joinKuerzel = BMUATZ
searchResult := searchResult "LEFT JOIN BMD.ATZ_ABFRAGETABELLENZU ON ATZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AUDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AUD_LETZTERUSER" "~"
if joinKuerzel = BMUAUD
searchResult := searchResult "LEFT JOIN BUERO.AUD_AUFGABEDOKUMENTZU ON AUD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AUWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AUW_LETZTERUSER" "~"
if joinKuerzel = BMUAUW
searchResult := searchResult "LEFT JOIN BUERO.AUW_AUSWAHL ON AUW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AVTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AVT_LETZTERUSER" "~"
if joinKuerzel = BMUAVT
searchResult := searchResult "LEFT JOIN BUERO.AVT_AVIMPORTTABELLE ON AVT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AWEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AWE_LETZTERUSER" "~"
if joinKuerzel = BMUAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AWRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AWR_LETZTERUSER" "~"
if joinKuerzel = BMUAWR
searchResult := searchResult "LEFT JOIN BUERO.AWR_AUSWERTUNGSKAT ON AWR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ACSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ACS_LETZTERUSER" "~"
if joinKuerzel = BMUACS
searchResult := searchResult "LEFT JOIN WWS.ACS_TEST ON ACS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AETBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AET_LETZTERUSER" "~"
if joinKuerzel = BMUAET
searchResult := searchResult "LEFT JOIN BIL.AET_ANLAGEGUT_ERFASS_TMP ON AET_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AI1BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AI1_LETZTERUSER" "~"
if joinKuerzel = BMUAI1
searchResult := searchResult "LEFT JOIN WWS.AI1_VA_ARTIKELIDENT ON AI1_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AIVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AIV_LETZTERUSER" "~"
if joinKuerzel = BMUAIV
searchResult := searchResult "LEFT JOIN WWS.AIV_INTERVALL_ARBEITSPLAN ON AIV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ALNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ALN_LETZTERUSER" "~"
if joinKuerzel = BMUALN
searchResult := searchResult "LEFT JOIN PRODUKTION.ALN_ALIBINUMMERN ON ALN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ANGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ANG_LETZTERUSER" "~"
if joinKuerzel = BMUANG
searchResult := searchResult "LEFT JOIN BMDINTERN.ANG_ANGEBOT ON ANG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ANSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ANS_LETZTERUSER" "~"
if joinKuerzel = BMUANS
searchResult := searchResult "LEFT JOIN BIL.ANS_ANLAGENSCHEMA ON ANS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ANZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ANZ_LETZTERUSER" "~"
if joinKuerzel = BMUANZ
searchResult := searchResult "LEFT JOIN BIL.ANZ_ANLAGEGUTKOSTZU ON ANZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = APCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = APC_LETZTERUSER" "~"
if joinKuerzel = BMUAPC
searchResult := searchResult "LEFT JOIN BIL.APC_ANBUPRODUKTIONSCODES ON APC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = APGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = APG_LETZTERUSER" "~"
if joinKuerzel = BMUAPG
searchResult := searchResult "LEFT JOIN WWS.APG_ARAPRODUKTGRUPPEN ON APG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = APZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = APZ_LETZTERUSER" "~"
if joinKuerzel = BMUAPZ
searchResult := searchResult "LEFT JOIN WWS.APZ_ARAPACKSTOFFTABZU ON APZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ARPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ARP_LETZTERUSER" "~"
if joinKuerzel = BMUARP
searchResult := searchResult "LEFT JOIN BUERO.ARP_ARBEITSZEITGESETZ_PAU ON ARP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ASCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ASC_LETZTERUSER" "~"
if joinKuerzel = BMUASC
searchResult := searchResult "LEFT JOIN ZLE.ASC_ARBEITSSCHEIN ON ASC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ASKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ASK_LETZTERUSER" "~"
if joinKuerzel = BMUASK
searchResult := searchResult "LEFT JOIN ZLE.ASK_SORTIERKOPF ON ASK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ATLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ATL_LETZTERUSER" "~"
if joinKuerzel = BMUATL
searchResult := searchResult "LEFT JOIN BMD.ATL_ATTRLOCK ON ATL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AUCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AUC_LETZTERUSER" "~"
if joinKuerzel = BMUAUC
searchResult := searchResult "LEFT JOIN BMD.AUC_AUSWERTUNGSCENTER ON AUC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AWKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AWK_LETZTERUSER" "~"
if joinKuerzel = BMUAWK
searchResult := searchResult "LEFT JOIN BIL.AWK_AUSWERTUNGKOPF ON AWK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AZBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AZB_LETZTERUSER" "~"
if joinKuerzel = BMUAZB
searchResult := searchResult "LEFT JOIN BUERO.AZB_ARBEITSZEITBEZ ON AZB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BAIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BAI_LETZTERUSER" "~"
if joinKuerzel = BMUBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BERBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BER_LETZTERUSER" "~"
if joinKuerzel = BMUBER
searchResult := searchResult "LEFT JOIN BUERO.BER_BERUF ON BER_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BFKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BFK_LETZTERUSER" "~"
if joinKuerzel = BMUBFK
searchResult := searchResult "LEFT JOIN WWS.BFK_BEFOERDKAT ON BFK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BGRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BGR_LETZTERUSER" "~"
if joinKuerzel = BMUBGR
searchResult := searchResult "LEFT JOIN BMD.BGR_BMDGRUPPENRECHTE ON BGR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BGZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BGZ_LETZTERUSER" "~"
if joinKuerzel = BMUBGZ
searchResult := searchResult "LEFT JOIN HRS.BGZ_BNZEILE ON BGZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BMPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BMP_LETZTERUSER" "~"
if joinKuerzel = BMUBMP
searchResult := searchResult "LEFT JOIN BMD.BMP_BMDMAILPROTOCOL ON BMP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BORBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BOR_LETZTERUSER" "~"
if joinKuerzel = BMUBOR
searchResult := searchResult "LEFT JOIN WWS.BOR_BONRABATT ON BOR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BPABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BPA_LETZTERUSER" "~"
if joinKuerzel = BMUBPA
searchResult := searchResult "LEFT JOIN FIBU.BPA_BUDGETPLANBILAUFTLG ON BPA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TLTSNR
searchResult := searchResult "LEFT JOIN SNR_SERIENNUMMERN.SNR_SERIENNUMMERN ON SNR_SERNRID = TLT_SERNRID" "~"
if joinKuerzel = SNRTLT
searchResult := searchResult "LEFT JOIN PRODUKTION.TLT_BT_LOGISTIK_TRANSPORT ON TLT_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = TRESNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_SERNRID = TRE_SERNRID" "~"
if joinKuerzel = SNRTRE
searchResult := searchResult "LEFT JOIN WWS.TRE_TRANSPORTEINHEIT ON TRE_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = TRPSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_SERNRID = TRP_SERNRID" "~"
if joinKuerzel = SNRTRP
searchResult := searchResult "LEFT JOIN WWS.TRP_TRANSPORTPOSITION ON TRP_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = KASSPK
searchResult := searchResult "LEFT JOIN SPK_STAPELKOPF.SPK_STAPELKOPF ON SPK_STAPELID = KAS_STAPELID" "~"
if joinKuerzel = SPKKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_STAPELID = SPK_STAPELID" "~"
if joinKuerzel = DKTSPL
searchResult := searchResult "LEFT JOIN SPL_STEMPEL.SPL_STEMPEL ON SPL_STEMPELLFDNR = DKT_STEMPELNR_POSTEINGANG" "~"
if joinKuerzel = SPLDKT
searchResult := searchResult "LEFT JOIN BUERO.DKT_DOCSKATEGORIE ON DKT_STEMPELNR_POSTEINGANG = SPL_STEMPELLFDNR" "~"
if joinKuerzel = BARSPQ
searchResult := searchResult "LEFT JOIN HRS.SPQ_SPESENARTEN_SEQ ON SPQ_SPESENARTID = BAR_SPQ_SPESENARTID" "~"
if joinKuerzel = SPQBAR
searchResult := searchResult "LEFT JOIN HRS.BAR_BARAUSLAGEN ON BAR_SPQ_SPESENARTID = SPQ_SPESENARTID" "~"
if joinKuerzel = SPPSPX
searchResult := searchResult "LEFT JOIN BMD.SPX_STAPELVARIABLE ON SPX_STAPELID = SPP_STAPELID AND SPX_STAPELVARIABLELFDNR = SPP_STAPELVARIABLELFDNR" "~"
if joinKuerzel = SPXSPP
searchResult := searchResult "LEFT JOIN BMD.SPP_STAPELPARAMETER ON SPP_STAPELID = SPX_STAPELID AND SPP_STAPELVARIABLELFDNR = SPX_STAPELVARIABLELFDNR" "~"
if joinKuerzel = ADWSRA
searchResult := searchResult "LEFT JOIN WWS.SRA_STEUERART ON SRA_FIRMENNR = ADW_HAUPTFIRMENNR AND SRA_STEUERARTNR = ADW_STEUERARTNR" "~"
if joinKuerzel = SRAADW
searchResult := searchResult "LEFT JOIN WWS.ADW_ABGABENDETAILWAP ON ADW_HAUPTFIRMENNR = SRA_FIRMENNR AND ADW_STEUERARTNR = SRA_STEUERARTNR" "~"
if joinKuerzel = EFKSRM
searchResult := searchResult "LEFT JOIN WWS.SRM_SAMMELRECHNUNGSMODELL ON SRM_SAMMELRECHID = EFK_SAMMELRECHID" "~"
if joinKuerzel = SRMEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_SAMMELRECHID = SRM_SAMMELRECHID" "~"
if joinKuerzel = EFKSRM
searchResult := searchResult "LEFT JOIN WWS.SRM_SAMMELRECHNUNGSMODELL ON SRM_SAMMELRECHID = EFK_ABRECHNUNGSMODELL_WHF" "~"
if joinKuerzel = SRMEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_ABRECHNUNGSMODELL_WHF = SRM_SAMMELRECHID" "~"
if joinKuerzel = ARLSRT
searchResult := searchResult "LEFT JOIN WWS.SRT_STEUERTAB ON SRT_FIRMENNR = ARL_FIRMENNR AND SRT_STEUERTABNR = ARL_STEUERTABNR" "~"
if joinKuerzel = SRTARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = SRT_FIRMENNR AND ARL_STEUERTABNR = SRT_STEUERTABNR" "~"
if joinKuerzel = SSPSSK
searchResult := searchResult "LEFT JOIN PRODUKTION.SSK_SAMMELSCHEIN_KOPF ON SSK_FIRMENNR = SSP_FIRMENNR AND SSK_SAMNR = SSP_SAMNR" "~"
if joinKuerzel = SSKSSP
searchResult := searchResult "LEFT JOIN PRODUKTION.SSP_SAMMELSCHEIN_POS ON SSP_FIRMENNR = SSK_FIRMENNR AND SSP_SAMNR = SSK_SAMNR" "~"
if joinKuerzel = BSDSTC
searchResult := searchResult "LEFT JOIN STC_STEUERCODE.STC_STEUERCODE ON STC_LANDNR = BSD_STEUERCODE_LANDNR AND STC_STEUERCODENR = BSD_STEUERCODENR" "~"
if joinKuerzel = STCBSD
searchResult := searchResult "LEFT JOIN WWS.BSD_BONUSERMDETAIL ON BSD_STEUERCODE_LANDNR = STC_LANDNR AND BSD_STEUERCODENR = STC_STEUERCODENR" "~"
if joinKuerzel = SKOSTC
searchResult := searchResult "LEFT JOIN FIBU.STC_STEUERCODE ON STC_LANDNR = SKO_LANDNR AND STC_STEUERCODENR = SKO_STEUERCODENR" "~"
if joinKuerzel = STCSKO
searchResult := searchResult "LEFT JOIN FIBU.SKO_STEUERKONTEN ON SKO_LANDNR = STC_LANDNR AND SKO_STEUERCODENR = STC_STEUERCODENR" "~"
if joinKuerzel = WEPSTC
searchResult := searchResult "LEFT JOIN STC_STEUERCODE.STC_STEUERCODE ON STC_LANDNR = WEP_STEUERCODE_LANDNR AND STC_STEUERCODENR = WEP_STEUERCODENR" "~"
if joinKuerzel = STCWEP
searchResult := searchResult "LEFT JOIN WWS.WEP_WWSERKONTROLLEPOS ON WEP_STEUERCODE_LANDNR = STC_LANDNR AND WEP_STEUERCODENR = STC_STEUERCODENR" "~"
if joinKuerzel = WGDSTC
searchResult := searchResult "LEFT JOIN FIBU.STC_STEUERCODE ON STC_LANDNR = WGD_LANDNR AND STC_STEUERCODENR = WGD_STEUERCODENR" "~"
if joinKuerzel = STCWGD
searchResult := searchResult "LEFT JOIN FIBU.WGD_WGBDETAIL ON WGD_LANDNR = STC_LANDNR AND WGD_STEUERCODENR = STC_STEUERCODENR" "~"
if joinKuerzel = ZVKSTC
searchResult := searchResult "LEFT JOIN FIBU.STC_STEUERCODE ON STC_LANDNR = ZVK_LANDNR AND STC_STEUERCODENR = ZVK_STEUERCODENR" "~"
if joinKuerzel = STCZVK
searchResult := searchResult "LEFT JOIN FIBU.ZVK_ZAHLUNGSVORSCHLAG ON ZVK_LANDNR = STC_LANDNR AND ZVK_STEUERCODENR = STC_STEUERCODENR" "~"
if joinKuerzel = SEBSTE
searchResult := searchResult "LEFT JOIN STE_STEUERERKLAERUNG.STE_STEUERERKLAERUNG ON STE_STEUER_ERKLNR = SEB_STEUER_ERKLNR" "~"
if joinKuerzel = STESEB
searchResult := searchResult "LEFT JOIN BIL.SEB_STEUERERKLBAUM ON SEB_STEUER_ERKLNR = STE_STEUER_ERKLNR" "~"
if joinKuerzel = STPSTK
searchResult := searchResult "LEFT JOIN ZLE.STK_STAFFELKOPF ON STK_STKID = STP_STKID" "~"
if joinKuerzel = STKSTP
searchResult := searchResult "LEFT JOIN ZLE.STP_STAFFELPOS ON STP_STKID = STK_STKID" "~"
if joinKuerzel = RNPSTV
searchResult := searchResult "LEFT JOIN STV_STATVERFAHREN.STV_STATVERFAHREN ON STV_FIRMENNR = RNP_STV_FIRMENNR AND STV_STVCODE = RNP_STVCODE" "~"
if joinKuerzel = STVRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_STV_FIRMENNR = STV_FIRMENNR AND RNP_STVCODE = STV_STVCODE" "~"
if joinKuerzel = PSJTGR
searchResult := searchResult "LEFT JOIN HRS.TGR_TARIFGRUPPE ON TGR_DBS_DATENBESTAND = PSJ_DBS_DATENBESTANDNR AND TGR_LOHNJAHR = PSJ_LOHNJAHR AND TGR_TARIFGRUPPENNR = PSJ_TGR_LEHRLINGNR" "~"
if joinKuerzel = TGRPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_DBS_DATENBESTANDNR = TGR_DBS_DATENBESTAND AND PSJ_LOHNJAHR = TGR_LOHNJAHR AND PSJ_TGR_LEHRLINGNR = TGR_TARIFGRUPPENNR" "~"
if joinKuerzel = TTGTGR
searchResult := searchResult "LEFT JOIN HRS.TGR_TARIFGRUPPE ON TGR_DBS_DATENBESTAND = TTG_DATENBESTAND AND TGR_LOHNJAHR = TTG_LOHNJAHR AND TGR_TARIFGRUPPENNR = TTG_TGR_NR" "~"
if joinKuerzel = TGRTTG
searchResult := searchResult "LEFT JOIN HRS.TTG_TARIFGR_ERGAENZUNG_ZU ON TTG_DATENBESTAND = TGR_DBS_DATENBESTAND AND TTG_LOHNJAHR = TGR_LOHNJAHR AND TTG_TGR_NR = TGR_TARIFGRUPPENNR" "~"
if joinKuerzel = PICWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = PIC_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = PIC_WAG_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = PIC_WAG_WARENUNTERGRNR" "~"
if joinKuerzel = WAGPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_WAG_FIRMENNR = WAG_FIRMENNR AND PIC_WAG_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND PIC_WAG_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = QMAWAG
searchResult := searchResult "LEFT JOIN WAG_WARENGRUPPE.WAG_WARENGRUPPE ON WAG_FIRMENNR = QMA_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = QMA_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = QMA_WARENUNTERGRNR" "~"
if joinKuerzel = WAGQMA
searchResult := searchResult "LEFT JOIN PRODUKTION.QMA_QM_ARTIKEL ON QMA_WAG_FIRMENNR = WAG_FIRMENNR AND QMA_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND QMA_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = EDZWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_WARENEINGKOPFID = EDZ_WAK_WARENEINGKOPFID" "~"
if joinKuerzel = WAKEDZ
searchResult := searchResult "LEFT JOIN WWS.EDZ_ERFASSDOKUMENTZU ON EDZ_WAK_WARENEINGKOPFID = WAK_WARENEINGKOPFID" "~"
if joinKuerzel = WABWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_WARENEINGKOPFID = WAB_WARENEINGKOPFID" "~"
if joinKuerzel = WAKWAB
searchResult := searchResult "LEFT JOIN WWS.WAB_WARENEINGANGBUCH ON WAB_WARENEINGKOPFID = WAK_WARENEINGKOPFID" "~"
if joinKuerzel = WARWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_WARENEINGKOPFID = WAR_WARENEINGKOPFID" "~"
if joinKuerzel = WAKWAR
searchResult := searchResult "LEFT JOIN WWS.WAR_WARENEINGANGRABATTE ON WAR_WARENEINGKOPFID = WAK_WARENEINGKOPFID" "~"
if joinKuerzel = WEUWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_WARENEINGKOPFID = WEU_WAK_WARENEINGKOPFID" "~"
if joinKuerzel = WAKWEU
searchResult := searchResult "LEFT JOIN WWS.WEU_WWSERKONTROLLEZU ON WEU_WAK_WARENEINGKOPFID = WAK_WARENEINGKOPFID" "~"
if joinKuerzel = DISWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_WARENEINGKOPFID = DIS_WAP_WARENEINGKOPFID AND WAP_WARENEINGPOSLFDNR = DIS_WAP_WARENEINGPOSLFDNR" "~"
if joinKuerzel = WAPDIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_WAP_WARENEINGKOPFID = WAP_WARENEINGKOPFID AND DIS_WAP_WARENEINGPOSLFDNR = WAP_WARENEINGPOSLFDNR" "~"
if joinKuerzel = KOPWAP
searchResult := searchResult "LEFT JOIN WAP_WARENEINGANGPOSITION.WAP_WARENEINGANGPOSITION ON WAP_WARENEINGKOPFID = KOP_WAP_WAKID AND WAP_WARENEINGPOSLFDNR = KOP_WAP_LFDNR" "~"
if joinKuerzel = WAPKOP
searchResult := searchResult "LEFT JOIN PRODUKTION.KOP_KOMMISSIONIERUNG_POS ON KOP_WAP_WAKID = WAP_WARENEINGKOPFID AND KOP_WAP_LFDNR = WAP_WARENEINGPOSLFDNR" "~"
if joinKuerzel = WARWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_WARENEINGKOPFID = WAR_WARENEINGKOPFID AND WAP_WARENEINGPOSLFDNR = WAR_WARENEINGPOSLFDNR" "~"
if joinKuerzel = WAPWAR
searchResult := searchResult "LEFT JOIN WWS.WAR_WARENEINGANGRABATTE ON WAR_WARENEINGKOPFID = WAP_WARENEINGKOPFID AND WAR_WARENEINGPOSLFDNR = WAP_WARENEINGPOSLFDNR" "~"
if joinKuerzel = WEDWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_WARENEINGKOPFID = WED_WARENEINGKOPFID AND WAP_WARENEINGPOSLFDNR = WED_WARENEINGPOSLFDNR" "~"
if joinKuerzel = WAPWED
searchResult := searchResult "LEFT JOIN WWS.WED_WARENEING_POSDETAIL ON WED_WARENEINGKOPFID = WAP_WARENEINGKOPFID AND WED_WARENEINGPOSLFDNR = WAP_WARENEINGPOSLFDNR" "~"
if joinKuerzel = WESWBG
searchResult := searchResult "LEFT JOIN WWS.WBG_WARENBESCHKOSTENGRP ON WBG_WBGID = WES_WBGID" "~"
if joinKuerzel = WBGWES
searchResult := searchResult "LEFT JOIN WWS.WES_WWSERKONTROLLEWBK ON WES_WBGID = WBG_WBGID" "~"
if joinKuerzel = AAKWBT
searchResult := searchResult "LEFT JOIN WWS.WBT_WWSBUDGETKOPF ON WBT_WWSBUDGETKOPFID = AAK_WWSBUDGETKOPFID" "~"
if joinKuerzel = WBTAAK
searchResult := searchResult "LEFT JOIN WWS.AAK_ARAKOPF ON AAK_WWSBUDGETKOPFID = WBT_WWSBUDGETKOPFID" "~"
if joinKuerzel = AAPWBW
searchResult := searchResult "LEFT JOIN WWS.WBW_WWSBUDGETWERTE ON WBW_WWSBUDGETWERTEID = AAP_WWSBUDGETWERTEID" "~"
if joinKuerzel = WBWAAP
searchResult := searchResult "LEFT JOIN WWS.AAP_ARAPOSITION ON AAP_WWSBUDGETWERTEID = WBW_WWSBUDGETWERTEID" "~"
if joinKuerzel = WCWWCP
searchResult := searchResult "LEFT JOIN BMD.WCP_WAAUSDRUCKPARAMETER ON WCP_WAPARAMNR = WCW_WAPARAMNR" "~"
if joinKuerzel = WCPWCW
searchResult := searchResult "LEFT JOIN BMD.WCW_WAAUSDRUCKPARAMWERT ON WCW_WAPARAMNR = WCP_WAPARAMNR" "~"
if joinKuerzel = WSZWEB
searchResult := searchResult "LEFT JOIN WWS.WEB_WARENEINSATZBUCH ON WEB_WEINSATZKOPFID = WSZ_WEINSATZKOPFID AND WEB_WEINSATZBUCHLFDNR = WSZ_WEINSATZBUCHLFDNR" "~"
if joinKuerzel = WEBWSZ
searchResult := searchResult "LEFT JOIN WWS.WSZ_WESEPS_ZUORDNUNG ON WSZ_WEINSATZKOPFID = WEB_WEINSATZKOPFID AND WSZ_WEINSATZBUCHLFDNR = WEB_WEINSATZBUCHLFDNR" "~"
if joinKuerzel = EPSWEI
searchResult := searchResult "LEFT JOIN WWS.WEI_WARENEINSATZKOPF ON WEI_WEINSATZKOPFID = EPS_WEINSATZKOPFID" "~"
if joinKuerzel = WEIEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_WEINSATZKOPFID = WEI_WEINSATZKOPFID" "~"
if joinKuerzel = ADWWEP
searchResult := searchResult "LEFT JOIN WWS.WEP_WWSERKONTROLLEPOS ON WEP_ERKONTRPOS_ID = ADW_ERKONTRPOS_ID" "~"
if joinKuerzel = WEPADW
searchResult := searchResult "LEFT JOIN WWS.ADW_ABGABENDETAILWAP ON ADW_ERKONTRPOS_ID = WEP_ERKONTRPOS_ID" "~"
if joinKuerzel = WABWEP
searchResult := searchResult "LEFT JOIN WWS.WEP_WWSERKONTROLLEPOS ON WEP_ERKONTRPOS_ID = WAB_ERKONTRPOS_ID" "~"
if joinKuerzel = WEPWAB
searchResult := searchResult "LEFT JOIN WWS.WAB_WARENEINGANGBUCH ON WAB_ERKONTRPOS_ID = WEP_ERKONTRPOS_ID" "~"
if joinKuerzel = WAOWEP
searchResult := searchResult "LEFT JOIN WWS.WEP_WWSERKONTROLLEPOS ON WEP_ERKONTRPOS_ID = WAO_ERKONTRPOS_ID" "~"
if joinKuerzel = WEPWAO
searchResult := searchResult "LEFT JOIN WWS.WAO_WARENEINGWBK ON WAO_ERKONTRPOS_ID = WEP_ERKONTRPOS_ID" "~"
if joinKuerzel = WEEWER
searchResult := searchResult "LEFT JOIN WWS.WER_WWSERKONTROLLEKOPF ON WER_ERKONTRKOPF_ID = WEE_ERKONTRKOPF_ID" "~"
if joinKuerzel = WERWEE
searchResult := searchResult "LEFT JOIN WWS.WEE_ERKONT_BESTELL_ZUORD ON WEE_ERKONTRKOPF_ID = WER_ERKONTRKOPF_ID" "~"
if joinKuerzel = WENWEI
searchResult := searchResult "LEFT JOIN WWS.WEI_WARENEINSATZKOPF ON WEI_WEINSATZKOPFID = WEN_WEINSATZKOPFID" "~"
if joinKuerzel = WEIWEN
searchResult := searchResult "LEFT JOIN WWS.WEN_WARENEINSATZRNP ON WEN_WEINSATZKOPFID = WEI_WEINSATZKOPFID" "~"
if joinKuerzel = WEOWEI
searchResult := searchResult "LEFT JOIN WWS.WEI_WARENEINSATZKOPF ON WEI_WEINSATZKOPFID = WEO_WEINSATZKOPFID" "~"
if joinKuerzel = WEIWEO
searchResult := searchResult "LEFT JOIN WWS.WEO_WARENEINSATZLPO ON WEO_WEINSATZKOPFID = WEI_WEINSATZKOPFID" "~"
if joinKuerzel = WEWWEI
searchResult := searchResult "LEFT JOIN WWS.WEI_WARENEINSATZKOPF ON WEI_WEINSATZKOPFID = WEW_WEINSATZKOPFID" "~"
if joinKuerzel = WEIWEW
searchResult := searchResult "LEFT JOIN WWS.WEW_WARENEINSATZWGZU ON WEW_WEINSATZKOPFID = WEI_WEINSATZKOPFID" "~"
if joinKuerzel = WGDWGB
searchResult := searchResult "LEFT JOIN FIBU.WGB_WARENEINGANGSBUCH ON WGB_FIRMENNR = WGD_FIRMENNR AND WGB_FIBUNR = WGD_FIBUNR AND WGB_WGBID = WGD_WGBID" "~"
if joinKuerzel = WGBWGD
searchResult := searchResult "LEFT JOIN FIBU.WGD_WGBDETAIL ON WGD_FIRMENNR = WGB_FIRMENNR AND WGD_FIBUNR = WGB_FIBUNR AND WGD_WGBID = WGB_WGBID" "~"
if joinKuerzel = SCMWII
searchResult := searchResult "LEFT JOIN BMDENTW.WII_WEITERE_INFO_ITEM ON WII_WIIID = SCM_WIIID" "~"
if joinKuerzel = WIISCM
searchResult := searchResult "LEFT JOIN BMDENTW.SCM_SCRMAIN ON SCM_WIIID = WII_WIIID" "~"
if joinKuerzel = WQIWII
searchResult := searchResult "LEFT JOIN WII_WEITERE_INFO_ITEM.WII_WEITERE_INFO_ITEM ON WII_WIIID = WQI_WIIID" "~"
if joinKuerzel = WIIWQI
searchResult := searchResult "LEFT JOIN BMDDEFAULT.WQI_WEITERE_INFO_ITEM ON WQI_WIIID = WII_WIIID" "~"
if joinKuerzel = WICWIP
searchResult := searchResult "LEFT JOIN BIL.WIP_WIPRUEFUNG ON WIP_WIPRUEFUNGSID = WIC_WIPRUEFUNGSID" "~"
if joinKuerzel = WIPWIC
searchResult := searchResult "LEFT JOIN BIL.WIC_WIPCHECKLIST ON WIC_WIPRUEFUNGSID = WIP_WIPRUEFUNGSID" "~"
if joinKuerzel = WIDWIP
searchResult := searchResult "LEFT JOIN BIL.WIP_WIPRUEFUNG ON WIP_WIPRUEFUNGSID = WID_WIPRUEFUNGSID" "~"
if joinKuerzel = WIPWID
searchResult := searchResult "LEFT JOIN BIL.WID_WIPDOKUMENTE ON WID_WIPRUEFUNGSID = WIP_WIPRUEFUNGSID" "~"
if joinKuerzel = ADEWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = ADE_HAUPTFIRMENNR AND WKT_KONTENTABNR = ADE_STEUERKONTENTABNR" "~"
if joinKuerzel = WKTADE
searchResult := searchResult "LEFT JOIN WWS.ADE_ABGABENDETAILERFPOS ON ADE_HAUPTFIRMENNR = WKT_FIRMENNR AND ADE_STEUERKONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = ERAWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = ERA_HAUPTFIRMENNR AND WKT_KONTENTABNR = ERA_KONTENTABNR" "~"
if joinKuerzel = WKTERA
searchResult := searchResult "LEFT JOIN WWS.ERA_ERFASSRABATT ON ERA_HAUPTFIRMENNR = WKT_FIRMENNR AND ERA_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = MITWKT
searchResult := searchResult "LEFT JOIN WKT_WWSKONTENTAB.WKT_WWSKONTENTAB ON WKT_FIRMENNR = MIT_WKT_FIRMENNR AND WKT_KONTENTABNR = MIT_WKT_KONTENTABNR" "~"
if joinKuerzel = WKTMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_WKT_FIRMENNR = WKT_FIRMENNR AND MIT_WKT_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = WAOWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = WAO_WKT_FIRMENNR AND WKT_KONTENTABNR = WAO_KONTENTABNR" "~"
if joinKuerzel = WKTWAO
searchResult := searchResult "LEFT JOIN WWS.WAO_WARENEINGWBK ON WAO_WKT_FIRMENNR = WKT_FIRMENNR AND WAO_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = WARWRK
searchResult := searchResult "LEFT JOIN WWS.WRK_WWSANKAUFSRNGKOPF ON WRK_FIRMENNR = WAR_WRKFIRMENNR AND WRK_RECHNUNGLFDNR = WAR_RECHNUNGLFDNR" "~"
if joinKuerzel = WRKWAR
searchResult := searchResult "LEFT JOIN WWS.WAR_WARENEINGANGRABATTE ON WAR_WRKFIRMENNR = WRK_FIRMENNR AND WAR_RECHNUNGLFDNR = WRK_RECHNUNGLFDNR" "~"
if joinKuerzel = WRKWRK
searchResult := searchResult "LEFT JOIN WWS.WRK_WWSANKAUFSRNGKOPF ON WRK_FIRMENNR = WRK_FIRMENNR AND WRK_RECHNUNGLFDNR = WRK_STORECHNUNGLFDNR" "~"
if joinKuerzel = WRKWRK
searchResult := searchResult "LEFT JOIN WWS.WRK_WWSANKAUFSRNGKOPF ON WRK_FIRMENNR = WRK_FIRMENNR AND WRK_STORECHNUNGLFDNR = WRK_RECHNUNGLFDNR" "~"
if joinKuerzel = BPKWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = BPK_ZAHLKO_WTB_FIRMENNR AND WTB_WWSTEXTBNR = BPK_ZAHLKO_WWSTEXTBNR" "~"
if joinKuerzel = WTBBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_ZAHLKO_WTB_FIRMENNR = WTB_FIRMENNR AND BPK_ZAHLKO_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = ERZWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = ERZ_HAUPTFIRMENNR AND WTB_WWSTEXTBNR = ERZ_KOPF_WWSTEXTBNR" "~"
if joinKuerzel = WTBERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_HAUPTFIRMENNR = WTB_FIRMENNR AND ERZ_KOPF_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = KARWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = KAR_WTB_FIRMENNR AND WTB_WWSTEXTBNR = KAR_WTB_WWSTEXTBNR" "~"
if joinKuerzel = WTBKAR
searchResult := searchResult "LEFT JOIN WWS.KAR_KUNDENARTIKEL ON KAR_WTB_FIRMENNR = WTB_FIRMENNR AND KAR_WTB_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = LATWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = LAT_ARL_FIRMENNR AND WTB_WWSTEXTBNR = LAT_WTB_WWSTEXTBNR" "~"
if joinKuerzel = WTBLAT
searchResult := searchResult "LEFT JOIN WWS.LAT_LIEFERANTENARTIKEL ON LAT_ARL_FIRMENNR = WTB_FIRMENNR AND LAT_WTB_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = ARLWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = ARL_FIRMENNR AND WTB_WWSTEXTBNR = ARL_BESCHR_WWSTEXTBNR" "~"
if joinKuerzel = WTBARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = WTB_FIRMENNR AND ARL_BESCHR_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = WKGWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = WKG_FIRMENNR AND WTB_WWSTEXTBNR = WKG_FUSS_RNG_WWSTEXTBNR" "~"
if joinKuerzel = WTBWKG
searchResult := searchResult "LEFT JOIN WWS.WKG_WWSKUNDENGRUPPE ON WKG_FIRMENNR = WTB_FIRMENNR AND WKG_FUSS_RNG_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = ZADZAM
searchResult := searchResult "LEFT JOIN DATANORM.ZAM_ZU_ABSCHLAG ON ZAM_ERST_NR = ZAD_ERST_NR AND ZAM_ART_NR = ZAD_ART_NR" "~"
if joinKuerzel = ZAMZAD
searchResult := searchResult "LEFT JOIN DATANORM.ZAD_ZU_ABSCHLAG_DETAIL ON ZAD_ERST_NR = ZAM_ERST_NR AND ZAD_ART_NR = ZAM_ART_NR" "~"
if joinKuerzel = AVEWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = AVE_ERFGRP_WT_FIRMENNR AND WTB_WWSTEXTBNR = AVE_ERFGRP_WWSTEXTBNR" "~"
if joinKuerzel = WTBAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ERFGRP_WT_FIRMENNR = WTB_FIRMENNR AND AVE_ERFGRP_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = ERZWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = ERZ_HAUPTFIRMENNR AND WTB_WWSTEXTBNR = ERZ_KOPF_LFS_WWSTEXTBNR" "~"
if joinKuerzel = WTBERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_HAUPTFIRMENNR = WTB_FIRMENNR AND ERZ_KOPF_LFS_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = LMSWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = LMS_WWSTEXTBNR_FIRMENNR AND WTB_WWSTEXTBNR = LMS_WWSTEXTBNR_SCH_STUFE2" "~"
if joinKuerzel = WTBLMS
searchResult := searchResult "LEFT JOIN WWS.LMS_LIEFERMAHNUNG ON LMS_WWSTEXTBNR_FIRMENNR = WTB_FIRMENNR AND LMS_WWSTEXTBNR_SCH_STUFE2 = WTB_WWSTEXTBNR" "~"
if joinKuerzel = LMSWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = LMS_WWSTEXTBNR_FIRMENNR AND WTB_WWSTEXTBNR = LMS_WWSTEXTBNR_SCH_STUFE3" "~"
if joinKuerzel = WTBLMS
searchResult := searchResult "LEFT JOIN WWS.LMS_LIEFERMAHNUNG ON LMS_WWSTEXTBNR_FIRMENNR = WTB_FIRMENNR AND LMS_WWSTEXTBNR_SCH_STUFE3 = WTB_WWSTEXTBNR" "~"
if joinKuerzel = ADDWWB
searchResult := searchResult "LEFT JOIN WWB_WWSBERICHTE.WWB_WWSBERICHTE ON WWB_WWSBERICHTNR = ADD_WWSBERICHTNR" "~"
if joinKuerzel = WWBADD
searchResult := searchResult "LEFT JOIN BUERO.ADD_AUSDRUCKMODELLDETAIL ON ADD_WWSBERICHTNR = WWB_WWSBERICHTNR" "~"
if joinKuerzel = ARLZBA
searchResult := searchResult "LEFT JOIN ZBA_ZEITBUCHUNGSART.ZBA_ZEITBUCHUNGSART ON ZBA_FIRMENNR = ARL_ZBA_FIRMENNR AND ZBA_ZBUCHARTNR = ARL_ZBUCHARTNR" "~"
if joinKuerzel = ZBAARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_ZBA_FIRMENNR = ZBA_FIRMENNR AND ARL_ZBUCHARTNR = ZBA_ZBUCHARTNR" "~"
if joinKuerzel = TMOZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_FIRMENNR = TMO_ZBA_FIRMENNR AND ZBA_ZBUCHARTNR = TMO_ZBUCHARTNR" "~"
if joinKuerzel = ZBATMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_ZBA_FIRMENNR = ZBA_FIRMENNR AND TMO_ZBUCHARTNR = ZBA_ZBUCHARTNR" "~"
if joinKuerzel = ZBUZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_FIRMENNR = ZBU_ASW_FIRMENNR AND ZBA_ZBUCHARTNR = ZBU_ZBUCHARTNR" "~"
if joinKuerzel = ZBAZBU
searchResult := searchResult "LEFT JOIN BUERO.ZBU_ZEITBUCHUNG ON ZBU_ASW_FIRMENNR = ZBA_FIRMENNR AND ZBU_ZBUCHARTNR = ZBA_ZBUCHARTNR" "~"
if joinKuerzel = TMOZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_FIRMENNR = TMO_FIRMENNR AND ZBA_ZBUCHARTNR = TMO_MAXIST_ZBUCHARTNR" "~"
if joinKuerzel = ZBATMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = ZBA_FIRMENNR AND TMO_MAXIST_ZBUCHARTNR = ZBA_ZBUCHARTNR" "~"
if joinKuerzel = ZBFZBG
searchResult := searchResult "LEFT JOIN BUERO.ZBG_ZEITBABERECHTGRUPPE ON ZBG_FIRMENNR = ZBF_ZBG_FIRMENNR AND ZBG_BAGRUPPNR = ZBF_BAGRUPPNR" "~"
if joinKuerzel = ZBGZBF
searchResult := searchResult "LEFT JOIN BUERO.ZBF_ZEITBAFUNKZUORD ON ZBF_ZBG_FIRMENNR = ZBG_FIRMENNR AND ZBF_BAGRUPPNR = ZBG_BAGRUPPNR" "~"
if joinKuerzel = ZBBZBK
searchResult := searchResult "LEFT JOIN BUERO.ZBK_ZEITBETRIEBSKALENDER ON ZBK_BETRIEBSKALID = ZBB_BETRIEBSKALID" "~"
if joinKuerzel = ZBKZBB
searchResult := searchResult "LEFT JOIN BUERO.ZBB_ZEITBETRKALBUCHART ON ZBB_BETRIEBSKALID = ZBK_BETRIEBSKALID" "~"
if joinKuerzel = BEWZBZ
searchResult := searchResult "LEFT JOIN BUERO.ZBZ_ZEITBEZUGSART ON ZBZ_FIRMENNR = BEW_ZBZ_FIRMENNR AND ZBZ_ZBEZUGSARTNR = BEW_ZBEZUGSARTNR_NEXTMON4" "~"
if joinKuerzel = ZBZBEW
searchResult := searchResult "LEFT JOIN BUERO.BEW_BEWERTUNGSMODELL ON BEW_ZBZ_FIRMENNR = ZBZ_FIRMENNR AND BEW_ZBEZUGSARTNR_NEXTMON4 = ZBZ_ZBEZUGSARTNR" "~"
if joinKuerzel = ZEPZEK
searchResult := searchResult "LEFT JOIN WWS.ZEK_ZEITEINHEITENKOPF ON ZEK_ZEITEINHEITENKOPFNR = ZEP_ZEITEINHEITENKOPFNR" "~"
if joinKuerzel = ZEKZEP
searchResult := searchResult "LEFT JOIN WWS.ZEP_ZEITEINHEITENPOS ON ZEP_ZEITEINHEITENKOPFNR = ZEK_ZEITEINHEITENKOPFNR" "~"
if joinKuerzel = YBUZES
searchResult := searchResult "LEFT JOIN FIBU.ZES_ZESSIONSTEXT ON ZES_FIRMENNR = YBU_FIRMENNR AND ZES_FIBUNR = YBU_FIBUNR AND ZES_ZESSKZ = YBU_ZES_ZESSKZ" "~"
if joinKuerzel = ZESYBU
searchResult := searchResult "LEFT JOIN FIBU.YBU_AUTOBUCHUNG ON YBU_FIRMENNR = ZES_FIRMENNR AND YBU_FIBUNR = ZES_FIBUNR AND YBU_ZES_ZESSKZ = ZES_ZESSKZ" "~"
if joinKuerzel = ZEDZID
searchResult := searchResult "LEFT JOIN BMD.ZID_ZEILENDEF ON ZID_ZEILENDEFKOPFID = ZED_ZEILENDEFKOPFID AND ZID_BANDID = ZED_BANDID" "~"
if joinKuerzel = ZIDZED
searchResult := searchResult "LEFT JOIN BMD.ZED_ZELLENDEF ON ZED_ZEILENDEFKOPFID = ZID_ZEILENDEFKOPFID AND ZED_BANDID = ZID_BANDID" "~"
if joinKuerzel = ZVKZKR
searchResult := searchResult "LEFT JOIN FIBU.ZKR_ZAHLUNGSKREIS ON ZKR_FIRMENNR = ZVK_FIRMENNR AND ZKR_FIBUNR = ZVK_FIBUNR AND ZKR_ZAHLUNGSKREISLFDNR = ZVK_ZKR_ZAHLUNGSKREISLFDNR" "~"
if joinKuerzel = ZKRZVK
searchResult := searchResult "LEFT JOIN FIBU.ZVK_ZAHLUNGSVORSCHLAG ON ZVK_FIRMENNR = ZKR_FIRMENNR AND ZVK_FIBUNR = ZKR_FIBUNR AND ZVK_ZKR_ZAHLUNGSKREISLFDNR = ZKR_ZAHLUNGSKREISLFDNR" "~"
if joinKuerzel = ZBMZKS
searchResult := searchResult "LEFT JOIN FIBU.ZKS_ZINSBERECHNUNGSKREIS ON ZKS_FIRMENNR = ZBM_FIRMENNR AND ZKS_FIBUNR = ZBM_FIBUNR AND ZKS_ZINSKREISLFDNR = ZBM_ZINSKREISLFDNR" "~"
if joinKuerzel = ZKSZBM
searchResult := searchResult "LEFT JOIN FIBU.ZBM_ZINSBERECHNUNG_SUMME ON ZBM_FIRMENNR = ZKS_FIRMENNR AND ZBM_FIBUNR = ZKS_FIBUNR AND ZBM_ZINSKREISLFDNR = ZKS_ZINSKREISLFDNR" "~"
if joinKuerzel = BOZZRT
searchResult := searchResult "LEFT JOIN ZRT_ZAHLUNGSART.ZRT_ZAHLUNGSART ON ZRT_ZAHLARTNR = BOZ_ZAHLARTNR" "~"
if joinKuerzel = ZRTBOZ
searchResult := searchResult "LEFT JOIN WWS.BOZ_BONZAHLUNG ON BOZ_ZAHLARTNR = ZRT_ZAHLARTNR" "~"
if joinKuerzel = ZWRZRT
searchResult := searchResult "LEFT JOIN BUERO.ZRT_ZAHLUNGSART ON ZRT_ZAHLARTNR = ZWR_ZAHLARTNR" "~"
if joinKuerzel = ZRTZWR
searchResult := searchResult "LEFT JOIN BUERO.ZWR_ZAHLART_WAEHRUNGRUND ON ZWR_ZAHLARTNR = ZRT_ZAHLARTNR" "~"
if joinKuerzel = TERZTZ
searchResult := searchResult "LEFT JOIN ZTZ_ZEITZONEN.ZTZ_ZEITZONEN ON ZTZ_ZEITZONENR = TER_BIS_ZEITZONENR" "~"
if joinKuerzel = ZTZTER
searchResult := searchResult "LEFT JOIN BUERO.TER_TERMIN ON TER_BIS_ZEITZONENR = ZTZ_ZEITZONENR" "~"
if joinKuerzel = DTZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DTZ_LETZTERUSER" "~"
if joinKuerzel = BMUDTZ
searchResult := searchResult "LEFT JOIN DATANORM.DTZ_DATANORM_TEXTZU ON DTZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EAK_LETZTERUSER" "~"
if joinKuerzel = BMUEAK
searchResult := searchResult "LEFT JOIN WWS.EAK_ERLOESABGRENZUNGSKOPF ON EAK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EARBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EAR_LETZTERUSER" "~"
if joinKuerzel = BMUEAR
searchResult := searchResult "LEFT JOIN WWS.EAR_EXTARTIKELWERTE ON EAR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EAZ_LETZTERUSER" "~"
if joinKuerzel = BMUEAZ
searchResult := searchResult "LEFT JOIN BUERO.EAZ_EVENTAUSBZU ON EAZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EB3BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EB3_LETZTERUSER" "~"
if joinKuerzel = BMUEB3
searchResult := searchResult "LEFT JOIN HRS.EB3_ELDADETAIL_BN_NEBEN ON EB3_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EBSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EBS_LETZTERUSER" "~"
if joinKuerzel = BMUEBS
searchResult := searchResult "LEFT JOIN BIL.EBS_EBILANZSTRUKTUR ON EBS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EDVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EDV_LETZTERUSER" "~"
if joinKuerzel = BMUEDV
searchResult := searchResult "LEFT JOIN WWS.EDV_ERFASSRABATTDETAILVOR ON EDV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EEFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EEF_LETZTERUSER" "~"
if joinKuerzel = BMUEEF
searchResult := searchResult "LEFT JOIN HRS.EEF_ELDADETAIL_EFZ ON EEF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EHTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EHT_LETZTERUSER" "~"
if joinKuerzel = BMUEHT
searchResult := searchResult "LEFT JOIN WWS.EHT_EINHEITENTAB ON EHT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ELIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ELI_LETZTERUSER" "~"
if joinKuerzel = BMUELI
searchResult := searchResult "LEFT JOIN HRS.ELI_ELDADETAIL_LZI ON ELI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EM2BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EM2_LETZTERUSER" "~"
if joinKuerzel = BMUEM2
searchResult := searchResult "LEFT JOIN HRS.EM2_ELDADETAIL_MBGM ON EM2_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ERMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ERM_LETZTERUSER" "~"
if joinKuerzel = BMUERM
searchResult := searchResult "LEFT JOIN FIBU.ERM_ERMITARBEITERZU ON ERM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ES2BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ES2_LETZTERUSER" "~"
if joinKuerzel = BMUES2
searchResult := searchResult "LEFT JOIN HRS.ES2_ELDADETAIL_SM2 ON ES2_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ESEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ESE_LETZTERUSER" "~"
if joinKuerzel = BMUESE
searchResult := searchResult "LEFT JOIN HRS.ESE_ELDADETAIL_SE ON ESE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EVIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EVI_LETZTERUSER" "~"
if joinKuerzel = BMUEVI
searchResult := searchResult "LEFT JOIN BUERO.EVI_EVIDENZ ON EVI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EVNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EVN_LETZTERUSER" "~"
if joinKuerzel = BMUEVN
searchResult := searchResult "LEFT JOIN BUERO.EVN_TEILNEHMER ON EVN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EZTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EZT_LETZTERUSER" "~"
if joinKuerzel = BMUEZT
searchResult := searchResult "LEFT JOIN HRS.EZT_ELDAZENTRALTABELLE ON EZT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FBFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FBF_LETZTERUSER" "~"
if joinKuerzel = BMUFBF
searchResult := searchResult "LEFT JOIN BMD.FBF_FELDBESCHFORMAT ON FBF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FDKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FDK_LETZTERUSER" "~"
if joinKuerzel = BMUFDK
searchResult := searchResult "LEFT JOIN BMD.FDK_FELDWERTEKOPF ON FDK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FESBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FES_LETZTERUSER" "~"
if joinKuerzel = BMUFES
searchResult := searchResult "LEFT JOIN BUERO.FES_FRISTERLEDIGTSTATUS ON FES_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FKZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FKZ_LETZTERUSER" "~"
if joinKuerzel = BMUFKZ
searchResult := searchResult "LEFT JOIN FIBU.FKZ_FREIESKZ ON FKZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FPZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FPZ_LETZTERUSER" "~"
if joinKuerzel = BMUFPZ
searchResult := searchResult "LEFT JOIN HRS.FPZ_FIRMENPENSIONSKASSEZU ON FPZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FRTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FRT_LETZTERUSER" "~"
if joinKuerzel = BMUFRT
searchResult := searchResult "LEFT JOIN ZLE.FRT_FAKTURATYP ON FRT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FSOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FSO_LETZTERUSER" "~"
if joinKuerzel = BMUFSO
searchResult := searchResult "LEFT JOIN FIBU.FSO_SORTIERPOS ON FSO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FSZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FSZ_LETZTERUSER" "~"
if joinKuerzel = BMUFSZ
searchResult := searchResult "LEFT JOIN BUERO.FSZ_FREIEANTWORTSETZU ON FSZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FTPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FTP_LETZTERUSER" "~"
if joinKuerzel = BMUFTP
searchResult := searchResult "LEFT JOIN WWS.FTP_FARBENTABPOS ON FTP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TLTTLK
searchResult := searchResult "LEFT JOIN PRODUKTION.TLK_BT_LOGISTIK_KOPF ON TLK_TLKID = TLT_TLKID" "~"
if joinKuerzel = TLKTLT
searchResult := searchResult "LEFT JOIN PRODUKTION.TLT_BT_LOGISTIK_TRANSPORT ON TLT_TLKID = TLK_TLKID" "~"
if joinKuerzel = TLATLP
searchResult := searchResult "LEFT JOIN PRODUKTION.TLP_BT_LOGISTIK_POSITION ON TLP_TLPID = TLA_TLPID" "~"
if joinKuerzel = TLPTLA
searchResult := searchResult "LEFT JOIN PRODUKTION.TLA_BT_LOGISTIK_ANGEFORDERT ON TLA_TLPID = TLP_TLPID" "~"
if joinKuerzel = AZDTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = AZD_FIRMENNR AND TMO_TAGESMODELLNR = AZD_TAGESMODELLNR" "~"
if joinKuerzel = TMOAZD
searchResult := searchResult "LEFT JOIN BUERO.AZD_ARBEITSZEITMODELLDET ON AZD_FIRMENNR = TMO_FIRMENNR AND AZD_TAGESMODELLNR = TMO_TAGESMODELLNR" "~"
if joinKuerzel = PKSTMO
searchResult := searchResult "LEFT JOIN TMO_TAGESMODELL.TMO_TAGESMODELL ON TMO_FIRMENNR = PKS_TMO_FIRMENNR AND TMO_TAGESMODELLNR = PKS_TMO_TAGESMODELLNR" "~"
if joinKuerzel = TMOPKS
searchResult := searchResult "LEFT JOIN PRODUKTION.PKS_SOLL_KAPAZITAET ON PKS_TMO_FIRMENNR = TMO_FIRMENNR AND PKS_TMO_TAGESMODELLNR = TMO_TAGESMODELLNR" "~"
if joinKuerzel = ZRBTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = ZRB_TMO_FIRMENNR AND TMO_TAGESMODELLNR = ZRB_TAGESMODELLNR" "~"
if joinKuerzel = TMOZRB
searchResult := searchResult "LEFT JOIN BUERO.ZRB_ZEITRUNDUNGBEDINGUNG ON ZRB_TMO_FIRMENNR = TMO_FIRMENNR AND ZRB_TAGESMODELLNR = TMO_TAGESMODELLNR" "~"
if joinKuerzel = WMDTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = WMD_FIRMENNR AND TMO_TAGESMODELLNR = WMD_TAGESMODELLNR2" "~"
if joinKuerzel = TMOWMD
searchResult := searchResult "LEFT JOIN BUERO.WMD_WOCHENMODELL ON WMD_FIRMENNR = TMO_FIRMENNR AND WMD_TAGESMODELLNR2 = TMO_TAGESMODELLNR" "~"
if joinKuerzel = TMOTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = TMO_FIRMENNR AND TMO_TAGESMODELLNR = TMO_WEIHNACHT_TAGESMODELLNR" "~"
if joinKuerzel = TMOTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = TMO_FIRMENNR AND TMO_WEIHNACHT_TAGESMODELLNR = TMO_TAGESMODELLNR" "~"
if joinKuerzel = TMOTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = TMO_FIRMENNR AND TMO_TAGESMODELLNR = TMO_MINDB_TAGESMODELLNR" "~"
if joinKuerzel = TMOTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = TMO_FIRMENNR AND TMO_MINDB_TAGESMODELLNR = TMO_TAGESMODELLNR" "~"
if joinKuerzel = WMDTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = WMD_FIRMENNR AND TMO_TAGESMODELLNR = WMD_TAGESMODELLNR6" "~"
if joinKuerzel = TMOWMD
searchResult := searchResult "LEFT JOIN BUERO.WMD_WOCHENMODELL ON WMD_FIRMENNR = TMO_FIRMENNR AND WMD_TAGESMODELLNR6 = TMO_TAGESMODELLNR" "~"
if joinKuerzel = ERPTOU
searchResult := searchResult "LEFT JOIN WWS.TOU_TOUR ON TOU_FIRMENNR = ERP_TOU_FIRMENNR AND TOU_TOURNR = ERP_TOU_TOURNR" "~"
if joinKuerzel = TOUERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_TOU_FIRMENNR = TOU_FIRMENNR AND ERP_TOU_TOURNR = TOU_TOURNR" "~"
if joinKuerzel = TGPTPT
searchResult := searchResult "LEFT JOIN HRS.TPT_TARIFPROZENT ON TPT_DATENBESTAND = TGP_DATENBESTAND AND TPT_LOHNJAHR = TGP_LOHNJAHR AND TPT_TARIFZWEIG = TGP_TPT_TARIFZWEIG AND TPT_PROZENTNR = TGP_TPT_PROZENTNR AND TPT_ABMONAT = TGP_TPT_ABMONAT" "~"
if joinKuerzel = TPTTGP
searchResult := searchResult "LEFT JOIN HRS.TGP_TARIFGR_TARIFPROZ_ZU ON TGP_DATENBESTAND = TPT_DATENBESTAND AND TGP_LOHNJAHR = TPT_LOHNJAHR AND TGP_TPT_TARIFZWEIG = TPT_TARIFZWEIG AND TGP_TPT_PROZENTNR = TPT_PROZENTNR AND TGP_TPT_ABMONAT = TPT_ABMONAT" "~"
if joinKuerzel = TRJTRE
searchResult := searchResult "LEFT JOIN WWS.TRE_TRANSPORTEINHEIT ON TRE_TRANSPORTEINHEITID = TRJ_TRANSPORTEINHEITID" "~"
if joinKuerzel = TRETRJ
searchResult := searchResult "LEFT JOIN WWS.TRJ_TRANSPORTEINH_JOURNAL ON TRJ_TRANSPORTEINHEITID = TRE_TRANSPORTEINHEITID" "~"
if joinKuerzel = AVETRS
searchResult := searchResult "LEFT JOIN TRS_TEILSCHLUSSRECHSCHEMA.TRS_TEILSCHLUSSRECHSCHEMA ON TRS_TRSID = AVE_TRSID" "~"
if joinKuerzel = TRSAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_TRSID = TRS_TRSID" "~"
if joinKuerzel = PROTRS
searchResult := searchResult "LEFT JOIN BUERO.TRS_TEILSCHLUSSRECHSCHEMA ON TRS_TRSID = PRO_TRSID" "~"
if joinKuerzel = TRSPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_TRSID = TRS_TRSID" "~"
if joinKuerzel = AMZTRT
searchResult := searchResult "LEFT JOIN TRT_TRANSPORTTYP.TRT_TRANSPORTTYP ON TRT_FIRMENNR = AMZ_FIRMENNR AND TRT_TRANSPORTTYPID = AMZ_TRANSPORTTYPID" "~"
if joinKuerzel = TRTAMZ
searchResult := searchResult "LEFT JOIN BUERO.AMZ_AUSDRUCKMODELLZU ON AMZ_FIRMENNR = TRT_FIRMENNR AND AMZ_TRANSPORTTYPID = TRT_TRANSPORTTYPID" "~"
if joinKuerzel = KASTSK
searchResult := searchResult "LEFT JOIN WWS.TSK_TOUCHSCREENKOPF ON TSK_TOUCHSCREENID = KAS_TSK_ID_BOZ_UN" "~"
if joinKuerzel = TSKKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_TSK_ID_BOZ_UN = TSK_TOUCHSCREENID" "~"
if joinKuerzel = UBEUAN
searchResult := searchResult "LEFT JOIN BMDINTERN.UAN_UPDATEANFORDERUNGSTXT ON UAN_ANFORDERUNGSTEXTNR = UBE_ANFORDERUNGSTEXTNR" "~"
if joinKuerzel = UANUBE
searchResult := searchResult "LEFT JOIN BMDINTERN.UBE_UPDATEBESTELLUNG ON UBE_ANFORDERUNGSTEXTNR = UAN_ANFORDERUNGSTEXTNR" "~"
if joinKuerzel = AWSUMS
searchResult := searchResult "LEFT JOIN UMS_UMSATZZEILE.UMS_UMSATZZEILE ON UMS_UMSATZZEILENNR = AWS_UMS_UMSATZZEILENNR" "~"
if joinKuerzel = UMSAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_UMS_UMSATZZEILENNR = UMS_UMSATZZEILENNR" "~"
if joinKuerzel = UVWUVF
searchResult := searchResult "LEFT JOIN FIBU.UVF_UVAFELDZU ON UVF_STEUER_ERKLNR = UVW_STEUER_ERKLNR AND UVF_UVAMODELLLFDNR = UVW_UVAMODELLLFDNR AND UVF_FELD_LFDNR = UVW_FELD_LFDNR" "~"
if joinKuerzel = UVFUVW
searchResult := searchResult "LEFT JOIN FIBU.UVW_UVAWERT ON UVW_STEUER_ERKLNR = UVF_STEUER_ERKLNR AND UVW_UVAMODELLLFDNR = UVF_UVAMODELLLFDNR AND UVW_FELD_LFDNR = UVF_FELD_LFDNR" "~"
if joinKuerzel = AVDVAK
searchResult := searchResult "LEFT JOIN BUERO.VAK_VERANSTALTUNGSKOPF ON VAK_FIRMENNR = AVD_VAK_FIRMENNR AND VAK_VSTAMMNR = AVD_VAK_VSTAMMNR AND VAK_VERANSTALTUNGSNR = AVD_VAK_VERANSTALTUNGSNR" "~"
if joinKuerzel = VAKAVD
searchResult := searchResult "LEFT JOIN BUERO.AVD_AUSBVERWDOKUMENTZU ON AVD_VAK_FIRMENNR = VAK_FIRMENNR AND AVD_VAK_VSTAMMNR = VAK_VSTAMMNR AND AVD_VAK_VERANSTALTUNGSNR = VAK_VERANSTALTUNGSNR" "~"
if joinKuerzel = BRMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BRM_LETZTERUSER" "~"
if joinKuerzel = BMUBRM
searchResult := searchResult "LEFT JOIN BUERO.BRM_BUEROMAKROS ON BRM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BSIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BSI_LETZTERUSER" "~"
if joinKuerzel = BMUBSI
searchResult := searchResult "LEFT JOIN WWSINDIV.BSI_BONUSSTATISTIK ON BSI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BSUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BSU_LETZTERUSER" "~"
if joinKuerzel = BMUBSU
searchResult := searchResult "LEFT JOIN FIBU.BSU_BANKEINZUGSSUMME ON BSU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BTD_LETZTERUSER" "~"
if joinKuerzel = BMUBTD
searchResult := searchResult "LEFT JOIN BUERO.BTD_BTERMSATZARTDETAIL ON BTD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BUABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BUA_LETZTERUSER" "~"
if joinKuerzel = BMUBUA
searchResult := searchResult "LEFT JOIN FIBU.BUA_BUCHUNGSART ON BUA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BUSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BUS_LETZTERUSER" "~"
if joinKuerzel = BMUBUS
searchResult := searchResult "LEFT JOIN FIBU.BUS_BUCHUNGSSYMBOL ON BUS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BVZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BVZ_LETZTERUSER" "~"
if joinKuerzel = BMUBVZ
searchResult := searchResult "LEFT JOIN FIBU.BVZ_BUCHUNGSVAR_ARTZU ON BVZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BWVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BWV_LETZTERUSER" "~"
if joinKuerzel = BMUBWV
searchResult := searchResult "LEFT JOIN BUERO.BWV_BEWERTUNGVERSION ON BWV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CCABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CCA_LETZTERUSER" "~"
if joinKuerzel = BMUCCA
searchResult := searchResult "LEFT JOIN BIL.CCA_CBCADDINFO ON CCA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CCEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CCE_LETZTERUSER" "~"
if joinKuerzel = BMUCCE
searchResult := searchResult "LEFT JOIN BIL.CCE_CBCENTITY ON CCE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CILBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CIL_LETZTERUSER" "~"
if joinKuerzel = BMUCIL
searchResult := searchResult "LEFT JOIN BMDINTERN.CIL_CLIENTSINFOLOGIN ON CIL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CLABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CLA_LETZTERUSER" "~"
if joinKuerzel = BMUCLA
searchResult := searchResult "LEFT JOIN BUERO.CLA_CLAUSWAHLFELD ON CLA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CLVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CLV_LETZTERUSER" "~"
if joinKuerzel = BMUCLV
searchResult := searchResult "LEFT JOIN BUERO.CLV_CHECKLISTVORLAGE ON CLV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CSZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CSZ_LETZTERUSER" "~"
if joinKuerzel = BMUCSZ
searchResult := searchResult "LEFT JOIN BMD.CSZ_CAI_SESSIONZU ON CSZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DBNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DBN_LETZTERUSER" "~"
if joinKuerzel = BMUDBN
searchResult := searchResult "LEFT JOIN BMD.DBN_DBCLONE ON DBN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DCABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DCA_LETZTERUSER" "~"
if joinKuerzel = BMUDCA
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DCA_WAAUSDRUCKE ON DCA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DCDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DCD_LETZTERUSER" "~"
if joinKuerzel = BMUDCD
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DCD_WAPARAMDEF ON DCD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DFBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DFB_LETZTERUSER" "~"
if joinKuerzel = BMUDFB
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFB_DFELDBESCHKOPF ON DFB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DIIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DII_LETZTERUSER" "~"
if joinKuerzel = BMUDII
searchResult := searchResult "LEFT JOIN BMD.DII_DIAGRAMMINSTANZ ON DII_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DKTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DKT_LETZTERUSER" "~"
if joinKuerzel = BMUDKT
searchResult := searchResult "LEFT JOIN BUERO.DKT_DOCSKATEGORIE ON DKT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DOGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DOG_LETZTERUSER" "~"
if joinKuerzel = BMUDOG
searchResult := searchResult "LEFT JOIN BMD.DOG_DATAOWNERGRUPPE ON DOG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DPKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DPK_LETZTERUSER" "~"
if joinKuerzel = BMUDPK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DPK_PERSONENKEYS_PD ON DPK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DRFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DRF_LETZTERUSER" "~"
if joinKuerzel = BMUDRF
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DRF_DREPORTFORMATVORLAGE ON DRF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DRTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DRT_LETZTERUSER" "~"
if joinKuerzel = BMUDRT
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DRT_DTABREPLGTABELLEZU ON DRT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DSPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DSP_LETZTERUSER" "~"
if joinKuerzel = BMUDSP
searchResult := searchResult "LEFT JOIN BUERO.DSP_DSGVO_PERSONALIEN ON DSP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DSQBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DSQ_LETZTERUSER" "~"
if joinKuerzel = BMUDSQ
searchResult := searchResult "LEFT JOIN HRS.DSQ_DIVSTAMM_SEQ ON DSQ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AZGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AZG_LETZTERUSER" "~"
if joinKuerzel = BMUAZG
searchResult := searchResult "LEFT JOIN BUERO.AZG_ABZUG ON AZG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BARBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BAR_LETZTERUSER" "~"
if joinKuerzel = BMUBAR
searchResult := searchResult "LEFT JOIN HRS.BAR_BARAUSLAGEN ON BAR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BAUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BAU_LETZTERUSER" "~"
if joinKuerzel = BMUBAU
searchResult := searchResult "LEFT JOIN BUERO.BAU_BAUM ON BAU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BAZ_LETZTERUSER" "~"
if joinKuerzel = BMUBAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BCGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BCG_LETZTERUSER" "~"
if joinKuerzel = BMUBCG
searchResult := searchResult "LEFT JOIN BUERO.BCG_BMDCOMUSERGRUPPENZU ON BCG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BESBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BES_LETZTERUSER" "~"
if joinKuerzel = BMUBES
searchResult := searchResult "LEFT JOIN BUERO.BES_BEWERBUNGSTATUS ON BES_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BFTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BFT_LETZTERUSER" "~"
if joinKuerzel = BMUBFT
searchResult := searchResult "LEFT JOIN BUERO.BFT_BERUFSTITEL ON BFT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BGPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BGP_LETZTERUSER" "~"
if joinKuerzel = BMUBGP
searchResult := searchResult "LEFT JOIN HRS.BGP_BEITRAGSGRUPPE ON BGP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BGSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BGS_LETZTERUSER" "~"
if joinKuerzel = BMUBGS
searchResult := searchResult "LEFT JOIN HRS.BGS_BEITRAGSGRUPPE_SEQ ON BGS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BKJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BKJ_LETZTERUSER" "~"
if joinKuerzel = BMUBKJ
searchResult := searchResult "LEFT JOIN WWS.BKJ_BANKOMATJOURNAL ON BKJ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BKMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BKM_LETZTERUSER" "~"
if joinKuerzel = BMUBKM
searchResult := searchResult "LEFT JOIN BUERO.BKM_ZEITBETRKALMAZUORD ON BKM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BLFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BLF_LETZTERUSER" "~"
if joinKuerzel = BMUBLF
searchResult := searchResult "LEFT JOIN BUERO.BLF_BLOCKLOCHFUELLER ON BLF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BMGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BMG_LETZTERUSER" "~"
if joinKuerzel = BMUBMG
searchResult := searchResult "LEFT JOIN BMD.BMG_BMDGRUPPE ON BMG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BMIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BMI_LETZTERUSER" "~"
if joinKuerzel = BMUBMI
searchResult := searchResult "LEFT JOIN HRS.BMI_BUDGETMITARBEITER ON BMI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BMKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BMK_LETZTERUSER" "~"
if joinKuerzel = BMUBMK
searchResult := searchResult "LEFT JOIN BMD.BMK_BMDEMAILKATEGORIE ON BMK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BMRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BMR_LETZTERUSER" "~"
if joinKuerzel = BMUBMR
searchResult := searchResult "LEFT JOIN BMD.BMR_BMDROLLE ON BMR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BOIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BOI_LETZTERUSER" "~"
if joinKuerzel = BMUBOI
searchResult := searchResult "LEFT JOIN WWSINDIV.BOI_BONUSTAB_IFA ON BOI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BRCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BRC_LETZTERUSER" "~"
if joinKuerzel = BMUBRC
searchResult := searchResult "LEFT JOIN BUERO.BRC_BEREICH ON BRC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BRTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BRT_LETZTERUSER" "~"
if joinKuerzel = BMUBRT
searchResult := searchResult "LEFT JOIN BUERO.BRT_BERICHT ON BRT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BTA_LETZTERUSER" "~"
if joinKuerzel = BMUBTA
searchResult := searchResult "LEFT JOIN FIBU.BTA_BUDGETTILGPLAN_AEND ON BTA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BTF_LETZTERUSER" "~"
if joinKuerzel = BMUBTF
searchResult := searchResult "LEFT JOIN BMD.BTF_BMDTABFELDBESCH ON BTF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BUEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BUE_LETZTERUSER" "~"
if joinKuerzel = BMUBUE
searchResult := searchResult "LEFT JOIN HRS.BUE_BUAK_URLAUBSEINREICHG ON BUE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BUPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BUP_LETZTERUSER" "~"
if joinKuerzel = BMUBUP
searchResult := searchResult "LEFT JOIN FIBU.BUP_BUERFIMPORTPARAMETER ON BUP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BVRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BVR_LETZTERUSER" "~"
if joinKuerzel = BMUBVR
searchResult := searchResult "LEFT JOIN FIBU.BVR_BANKRVREGELN ON BVR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BWFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BWF_LETZTERUSER" "~"
if joinKuerzel = BMUBWF
searchResult := searchResult "LEFT JOIN BUERO.BWF_BEWERTUNGFORMEL ON BWF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BWIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BWI_LETZTERUSER" "~"
if joinKuerzel = BMUBWI
searchResult := searchResult "LEFT JOIN BUERO.BWI_BEWERBUNGSINFO ON BWI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTSZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_FIRMENNR = BTS_ZBA_FIRMENNR AND ZBA_ZBUCHARTNR = BTS_ZBA_ZBUCHARTNR" "~"
if joinKuerzel = ZBABTS
searchResult := searchResult "LEFT JOIN BUERO.BTS_BTERMSATZART ON BTS_ZBA_FIRMENNR = ZBA_FIRMENNR AND BTS_ZBA_ZBUCHARTNR = ZBA_ZBUCHARTNR" "~"
if joinKuerzel = KINZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_FIRMENNR = KIN_ZBA_FIRMENNR AND ZBA_ZBUCHARTNR = KIN_ZBUCHARTNR" "~"
if joinKuerzel = ZBAKIN
searchResult := searchResult "LEFT JOIN BUERO.KIN_ZEITKOREFELDINFOS ON KIN_ZBA_FIRMENNR = ZBA_FIRMENNR AND KIN_ZBUCHARTNR = ZBA_ZBUCHARTNR" "~"
if joinKuerzel = ZVDZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_FIRMENNR = ZVD_ZBA_FIRMENNR AND ZBA_ZBUCHARTNR = ZVD_ZBUCHARTNR" "~"
if joinKuerzel = ZBAZVD
searchResult := searchResult "LEFT JOIN BUERO.ZVD_ZEITPLANUNGVERSIONDAT ON ZVD_ZBA_FIRMENNR = ZBA_FIRMENNR AND ZVD_ZBUCHARTNR = ZBA_ZBUCHARTNR" "~"
if joinKuerzel = PMDZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_FIRMENNR = PMD_FIRMENNR AND ZBA_ZBUCHARTNR = PMD_BEZAHLT_ZBUCHARTNR" "~"
if joinKuerzel = ZBAPMD
searchResult := searchResult "LEFT JOIN BUERO.PMD_PAUSENMODELL ON PMD_FIRMENNR = ZBA_FIRMENNR AND PMD_BEZAHLT_ZBUCHARTNR = ZBA_ZBUCHARTNR" "~"
if joinKuerzel = BWMZBZ
searchResult := searchResult "LEFT JOIN BUERO.ZBZ_ZEITBEZUGSART ON ZBZ_FIRMENNR = BWM_ZBZ_EINFIRMENNR AND ZBZ_ZBEZUGSARTNR = BWM_ZBEZUGSARTNREIN" "~"
if joinKuerzel = ZBZBWM
searchResult := searchResult "LEFT JOIN BUERO.BWM_BEWERTUNGMONAT ON BWM_ZBZ_EINFIRMENNR = ZBZ_FIRMENNR AND BWM_ZBEZUGSARTNREIN = ZBZ_ZBEZUGSARTNR" "~"
if joinKuerzel = ZUBZBZ
searchResult := searchResult "LEFT JOIN BUERO.ZBZ_ZEITBEZUGSART ON ZBZ_FIRMENNR = ZUB_ZBZ_FIRMENNR AND ZBZ_ZBEZUGSARTNR = ZUB_ZBEZUGSARTNR" "~"
if joinKuerzel = ZBZZUB
searchResult := searchResult "LEFT JOIN BUERO.ZUB_ZEITUMBUCHBEWERTMDZU ON ZUB_ZBZ_FIRMENNR = ZBZ_FIRMENNR AND ZUB_ZBEZUGSARTNR = ZBZ_ZBEZUGSARTNR" "~"
if joinKuerzel = BWMZBZ
searchResult := searchResult "LEFT JOIN BUERO.ZBZ_ZEITBEZUGSART ON ZBZ_FIRMENNR = BWM_ZBZ_AUSFIRMENNR AND ZBZ_ZBEZUGSARTNR = BWM_ZBEZUGSARTNRAUS" "~"
if joinKuerzel = ZBZBWM
searchResult := searchResult "LEFT JOIN BUERO.BWM_BEWERTUNGMONAT ON BWM_ZBZ_AUSFIRMENNR = ZBZ_FIRMENNR AND BWM_ZBEZUGSARTNRAUS = ZBZ_ZBEZUGSARTNR" "~"
if joinKuerzel = HSPZDK
searchResult := searchResult "LEFT JOIN ZDK_ZEILENDEFKOPF.ZDK_ZEILENDEFKOPF ON ZDK_ZEILENDEFKOPFID = HSP_ZDK_ZEILENDEFKOPFID" "~"
if joinKuerzel = ZDKHSP
searchResult := searchResult "LEFT JOIN HRS.HSP_SPALTENKOPF ON HSP_ZDK_ZEILENDEFKOPFID = ZDK_ZEILENDEFKOPFID" "~"
if joinKuerzel = KSPZDK
searchResult := searchResult "LEFT JOIN ZDK_ZEILENDEFKOPF.ZDK_ZEILENDEFKOPF ON ZDK_ZEILENDEFKOPFID = KSP_ZEILENLAYOUT" "~"
if joinKuerzel = ZDKKSP
searchResult := searchResult "LEFT JOIN KORE.KSP_SPALTENKOPF ON KSP_ZEILENLAYOUT = ZDK_ZEILENDEFKOPFID" "~"
if joinKuerzel = ZDEZIB
searchResult := searchResult "LEFT JOIN FIBU.ZIB_KONTENZINSENBERECHNG ON ZIB_FIRMENNR = ZDE_FIRMENNR AND ZIB_FIBUNR = ZDE_FIBUNR AND ZIB_ZIKLFDNR = ZDE_ZIKLFDNR AND ZIB_ZIBID = ZDE_ZIBID" "~"
if joinKuerzel = ZIBZDE
searchResult := searchResult "LEFT JOIN FIBU.ZDE_ZIB_DETAIL ON ZDE_FIRMENNR = ZIB_FIRMENNR AND ZDE_FIBUNR = ZIB_FIBUNR AND ZDE_ZIKLFDNR = ZIB_ZIKLFDNR AND ZDE_ZIBID = ZIB_ZIBID" "~"
if joinKuerzel = BAIZRT
searchResult := searchResult "LEFT JOIN BUERO.ZRT_ZAHLUNGSART ON ZRT_ZAHLARTNR = BAI_ZRT_ZAHLARTNR1" "~"
if joinKuerzel = ZRTBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_ZRT_ZAHLARTNR1 = ZRT_ZAHLARTNR" "~"
if joinKuerzel = PALZRT
searchResult := searchResult "LEFT JOIN ZRT_ZAHLUNGSART.ZRT_ZAHLUNGSART ON ZRT_ZAHLARTNR = PAL_ZAHLARTNR" "~"
if joinKuerzel = ZRTPAL
searchResult := searchResult "LEFT JOIN ZLE.PAL_PAUSCHALE ON PAL_ZAHLARTNR = ZRT_ZAHLARTNR" "~"
if joinKuerzel = PERZRT
searchResult := searchResult "LEFT JOIN BUERO.ZRT_ZAHLUNGSART ON ZRT_ZAHLARTNR = PER_ZAHLARTNR" "~"
if joinKuerzel = ZRTPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_ZAHLARTNR = ZRT_ZAHLARTNR" "~"
if joinKuerzel = ZSDZSK
searchResult := searchResult "LEFT JOIN ZLE.ZSK_ZEITSCHEIBEKOPF ON ZSK_ZSKID = ZSD_ZSKID" "~"
if joinKuerzel = ZSKZSD
searchResult := searchResult "LEFT JOIN ZLE.ZSD_ZEITSCHEIBEDETAIL ON ZSD_ZSKID = ZSK_ZSKID" "~"
if joinKuerzel = FIRZTZ
searchResult := searchResult "LEFT JOIN ZTZ_ZEITZONEN.ZTZ_ZEITZONEN ON ZTZ_ZEITZONENR = FIR_ZEITZONENR" "~"
if joinKuerzel = ZTZFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_ZEITZONENR = ZTZ_ZEITZONENR" "~"
if joinKuerzel = TERZTZ
searchResult := searchResult "LEFT JOIN ZTZ_ZEITZONEN.ZTZ_ZEITZONEN ON ZTZ_ZEITZONENR = TER_VON_ZEITZONENR" "~"
if joinKuerzel = ZTZTER
searchResult := searchResult "LEFT JOIN BUERO.TER_TERMIN ON TER_VON_ZEITZONENR = ZTZ_ZEITZONENR" "~"
if joinKuerzel = ZBUZTZ
searchResult := searchResult "LEFT JOIN ZTZ_ZEITZONEN.ZTZ_ZEITZONEN ON ZTZ_ZEITZONENR = ZBU_ZEITZONENR" "~"
if joinKuerzel = ZTZZBU
searchResult := searchResult "LEFT JOIN BUERO.ZBU_ZEITBUCHUNG ON ZBU_ZEITZONENR = ZTZ_ZEITZONENR" "~"
if joinKuerzel = ZVZZVB
searchResult := searchResult "LEFT JOIN BUERO.ZVB_TBBEZEICHNUNGSZWECK ON ZVB_LANDNR = ZVZ_LANDNR AND ZVB_TBFILEFORMAT = ZVZ_TBFILEFORMAT" "~"
if joinKuerzel = ZVBZVZ
searchResult := searchResult "LEFT JOIN BUERO.ZVZ_TBVERWENDUNGSZWECK ON ZVZ_LANDNR = ZVB_LANDNR AND ZVZ_TBFILEFORMAT = ZVB_TBFILEFORMAT" "~"
if joinKuerzel = BAIZVT
searchResult := searchResult "LEFT JOIN BUERO.ZVT_TBDATEIEN ON ZVT_ZVTID = BAI_TBFEU_ZVTID" "~"
if joinKuerzel = ZVTBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_TBFEU_ZVTID = ZVT_ZVTID" "~"
if joinKuerzel = BPPZWK
searchResult := searchResult "LEFT JOIN WWS.ZWK_ZUORDNUNGWVKOPF ON ZWK_BMDWVNR = BPP_BMDWVNR" "~"
if joinKuerzel = ZWKBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_BMDWVNR = ZWK_BMDWVNR" "~"
if joinKuerzel = RNPZWK
searchResult := searchResult "LEFT JOIN WWS.ZWK_ZUORDNUNGWVKOPF ON ZWK_BMDWVNR = RNP_BMDWVNR" "~"
if joinKuerzel = ZWKRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_BMDWVNR = ZWK_BMDWVNR" "~"
if joinKuerzel = WAGZWK
searchResult := searchResult "LEFT JOIN WWS.ZWK_ZUORDNUNGWVKOPF ON ZWK_BMDWVNR = WAG_BMDWVNR" "~"
if joinKuerzel = ZWKWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_BMDWVNR = ZWK_BMDWVNR" "~"
if joinKuerzel = AKRVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_FIRMENNR = AKR_VAS_FIRMENNR AND VAS_VSTAMMNR = AKR_VAS_VSTAMMNR" "~"
if joinKuerzel = VASAKR
searchResult := searchResult "LEFT JOIN BUERO.AKR_AVKOSTENARTENZU ON AKR_VAS_FIRMENNR = VAS_FIRMENNR AND AKR_VAS_VSTAMMNR = VAS_VSTAMMNR" "~"
if joinKuerzel = KVEVEB
searchResult := searchResult "LEFT JOIN FIBU.VEB_VORERFASSBUCHUNG ON VEB_FIRMENNR = KVE_FIRMENNR AND VEB_FIBUNR = KVE_FIBUNR AND VEB_VORERFASSID = KVE_VORERFASSID" "~"
if joinKuerzel = VEBKVE
searchResult := searchResult "LEFT JOIN FIBU.KVE_KOREVORERFASSUNG ON KVE_FIRMENNR = VEB_FIRMENNR AND KVE_FIBUNR = VEB_FIBUNR AND KVE_VORERFASSID = VEB_VORERFASSID" "~"
if joinKuerzel = EFKVED
searchResult := searchResult "LEFT JOIN WWS.VED_VIEW_AUFKOPF_DOKINFO ON VED_FIRMENNR = EFK_FIRMENNR AND VED_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND VED_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = VEDEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = VED_FIRMENNR AND EFK_ERFASSUNGSKZ = VED_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = VED_ERFASSUNGSNR" "~"
if joinKuerzel = LOPVFA
searchResult := searchResult "LEFT JOIN PRODUKTION.VFA_VERFAHRENSANWEISUNG_KOPF ON VFA_VERFAID = LOP_VERFAID" "~"
if joinKuerzel = VFALOP
searchResult := searchResult "LEFT JOIN PRODUKTION.LOP_LOGIKTABELLE_POS ON LOP_VERFAID = VFA_VERFAID" "~"
if joinKuerzel = PCKVFA
searchResult := searchResult "LEFT JOIN PRODUKTION.VFA_VERFAHRENSANWEISUNG_KOPF ON VFA_VERFAID = PCK_VERFAID" "~"
if joinKuerzel = VFAPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_VERFAID = VFA_VERFAID" "~"
if joinKuerzel = VFDVFA
searchResult := searchResult "LEFT JOIN PRODUKTION.VFA_VERFAHRENSANWEISUNG_KOPF ON VFA_VERFAID = VFD_VERFAID" "~"
if joinKuerzel = VFAVFD
searchResult := searchResult "LEFT JOIN PRODUKTION.VFD_VERFAHRENSANWEISUNG_POS ON VFD_VERFAID = VFA_VERFAID" "~"
if joinKuerzel = BPKVKA
searchResult := searchResult "LEFT JOIN KORE.VKA_VIEW_KOSTAMM_ABT ON VKA_FIRMENNR = BPK_FIRMENNR AND VKA_KOSTENSTAMMNR = BPK_KST_ABT_KOSTENSTAMMNR" "~"
if joinKuerzel = VKABPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_FIRMENNR = VKA_FIRMENNR AND BPK_KST_ABT_KOSTENSTAMMNR = VKA_KOSTENSTAMMNR" "~"
if joinKuerzel = ARLVKE
searchResult := searchResult "LEFT JOIN WWS.VKE_VIEW_KUKSUM_ARTIKEL ON VKE_ARL_FIRMENNR = ARL_FIRMENNR AND VKE_ARTIKELNR = ARL_ARTIKELNR AND VKE_FIRMENNR = ARL_FIRMENNR" "~"
if joinKuerzel = VKEARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = VKE_ARL_FIRMENNR AND ARL_ARTIKELNR = VKE_ARTIKELNR AND ARL_FIRMENNR = VKE_FIRMENNR" "~"
if joinKuerzel = RNGVKN
searchResult := searchResult "LEFT JOIN WWS.VKN_VA_AUSGKOSTRNGSUM ON VKN_FIRMENNR = RNG_FIRMENNR AND VKN_RECHNUNGLFDNR = RNG_RECHNUNGLFDNR" "~"
if joinKuerzel = VKNRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = VKN_FIRMENNR AND RNG_RECHNUNGLFDNR = VKN_RECHNUNGLFDNR" "~"
if joinKuerzel = ARLVLV
searchResult := searchResult "LEFT JOIN WWS.VLV_VA_ARLLETZTERVERK ON VLV_FIRMENNR = ARL_FIRMENNR AND VLV_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = VLVARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = VLV_FIRMENNR AND ARL_ARTIKELNR = VLV_ARTIKELNR" "~"
if joinKuerzel = DRFRPF
searchResult := searchResult "LEFT JOIN RPF_REPORTFORMATVORLAGE.RPF_REPORTFORMATVORLAGE ON RPF_KATALOGNR = DRF_KATALOGNR AND RPF_VORLAGENNR = DRF_VORLAGENNR" "~"
if joinKuerzel = RPFDRF
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DRF_DREPORTFORMATVORLAGE ON DRF_KATALOGNR = RPF_KATALOGNR AND DRF_VORLAGENNR = RPF_VORLAGENNR" "~"
if joinKuerzel = EROVPD
searchResult := searchResult "LEFT JOIN WWS.VPD_VPI_ERHOEHUNGDETAIL ON VPD_VPEID = ERO_VPEID AND VPD_VPDLFDNR = ERO_VPDLFDNR" "~"
if joinKuerzel = VPDERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_VPEID = VPD_VPEID AND ERO_VPDLFDNR = VPD_VPDLFDNR" "~"
if joinKuerzel = WEPVWI
searchResult := searchResult "LEFT JOIN WWS.VWI_VA_ERPOSWBK ON VWI_ERKONTRPOS_ID = WEP_ERKONTRPOS_ID" "~"
if joinKuerzel = VWIWEP
searchResult := searchResult "LEFT JOIN WWS.WEP_WWSERKONTROLLEPOS ON WEP_ERKONTRPOS_ID = VWI_ERKONTRPOS_ID" "~"
if joinKuerzel = WAKVWS
searchResult := searchResult "LEFT JOIN WWS.VWS_VA_WEINGBUCHSUMWAK ON VWS_WARENEINGKOPFID = WAK_WARENEINGKOPFID" "~"
if joinKuerzel = VWSWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_WARENEINGKOPFID = VWS_WARENEINGKOPFID" "~"
if joinKuerzel = PWKVZP
searchResult := searchResult "LEFT JOIN WWS.VZP_VA_PWEINZKUNDLIEFZU ON VZP_PWKID = PWK_PWKID" "~"
if joinKuerzel = VZPPWK
searchResult := searchResult "LEFT JOIN WWS.PWK_PREISRABATTWARTKOPF ON PWK_PWKID = VZP_PWKID" "~"
if joinKuerzel = LFSVZW
searchResult := searchResult "LEFT JOIN VZW_VERKEHRSZWEIG.VZW_VERKEHRSZWEIG ON VZW_FIRMENNR = LFS_VZW_FIRMENNR AND VZW_VZWCODE = LFS_VZWCODE" "~"
if joinKuerzel = VZWLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_VZW_FIRMENNR = VZW_FIRMENNR AND LFS_VZWCODE = VZW_VZWCODE" "~"
if joinKuerzel = RNGVZW
searchResult := searchResult "LEFT JOIN VZW_VERKEHRSZWEIG.VZW_VERKEHRSZWEIG ON VZW_FIRMENNR = RNG_VZW_FIRMENNR AND VZW_VZWCODE = RNG_VZWCODE" "~"
if joinKuerzel = VZWRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_VZW_FIRMENNR = VZW_FIRMENNR AND RNG_VZWCODE = VZW_VZWCODE" "~"
if joinKuerzel = WAKVZW
searchResult := searchResult "LEFT JOIN VZW_VERKEHRSZWEIG.VZW_VERKEHRSZWEIG ON VZW_FIRMENNR = WAK_VZW_FIRMENNR AND VZW_VZWCODE = WAK_VZWCODE" "~"
if joinKuerzel = VZWWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_VZW_FIRMENNR = VZW_FIRMENNR AND WAK_VZWCODE = VZW_VZWCODE" "~"
if joinKuerzel = BRVWAB
searchResult := searchResult "LEFT JOIN WAB_WARENEINGANGBUCH.WAB_WARENEINGANGBUCH ON WAB_WARENEINGBUCHID = BRV_WGBID" "~"
if joinKuerzel = WABBRV
searchResult := searchResult "LEFT JOIN FIBU.BRV_BANKRUECKVERBUCHUNG ON BRV_WGBID = WAB_WARENEINGBUCHID" "~"
if joinKuerzel = FZUWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = FZU_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEFZU
searchResult := searchResult "LEFT JOIN FIBU.FZU_FIBUFIBUZU ON FZU_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = GSKWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = GSK_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEGSK
searchResult := searchResult "LEFT JOIN WWS.GSK_GUTSCHEINKOPF ON GSK_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = WEUWER
searchResult := searchResult "LEFT JOIN WWS.WER_WWSERKONTROLLEKOPF ON WER_ERKONTRKOPF_ID = WEU_ERKONTRKOPF_ID" "~"
if joinKuerzel = WERWEU
searchResult := searchResult "LEFT JOIN WWS.WEU_WWSERKONTROLLEZU ON WEU_ERKONTRKOPF_ID = WER_ERKONTRKOPF_ID" "~"
if joinKuerzel = ARLWFG
searchResult := searchResult "LEFT JOIN WWS.WFG_FOLGEARTIKELGRUPPE ON WFG_WFGID = ARL_WFGID" "~"
if joinKuerzel = WFGARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_WFGID = WFG_WFGID" "~"
if joinKuerzel = WFTWFI
searchResult := searchResult "LEFT JOIN WEB.WFI_WEBFIRMA ON WFI_FIRMENNR = WFT_FIRMENNR" "~"
if joinKuerzel = WFIWFT
searchResult := searchResult "LEFT JOIN WEB.WFT_WEBFARBENTAB ON WFT_FIRMENNR = WFI_FIRMENNR" "~"
if joinKuerzel = ADBWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = ADB_STEUERFIRMENNR AND WKT_KONTENTABNR = ADB_STEUERKONTENTABNR" "~"
if joinKuerzel = WKTADB
searchResult := searchResult "LEFT JOIN WWS.ADB_ABGABENDETAILBON ON ADB_STEUERFIRMENNR = WKT_FIRMENNR AND ADB_STEUERKONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = ERDWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = ERD_HAUPTFIRMENNR AND WKT_KONTENTABNR = ERD_KONTENTABNR" "~"
if joinKuerzel = WKTERD
searchResult := searchResult "LEFT JOIN WWS.ERD_ERFASSRABATTDETAIL ON ERD_HAUPTFIRMENNR = WKT_FIRMENNR AND ERD_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = HNPWKT
searchResult := searchResult "LEFT JOIN WKT_WWSKONTENTAB.WKT_WWSKONTENTAB ON WKT_FIRMENNR = HNP_WKT_FIRMENNR AND WKT_KONTENTABNR = HNP_WKT_KONTENTABNR" "~"
if joinKuerzel = WKTHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_WKT_FIRMENNR = WKT_FIRMENNR AND HNP_WKT_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = KASWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = KAS_WKT_FIRMENNR AND WKT_KONTENTABNR = KAS_WKT_KONTENTABNR" "~"
if joinKuerzel = WKTKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_WKT_FIRMENNR = WKT_FIRMENNR AND KAS_WKT_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = WARWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = WAR_HAUPTFIRMENNR AND WKT_KONTENTABNR = WAR_KONTENTABNR" "~"
if joinKuerzel = WKTWAR
searchResult := searchResult "LEFT JOIN WWS.WAR_WARENEINGANGRABATTE ON WAR_HAUPTFIRMENNR = WKT_FIRMENNR AND WAR_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = WBGWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = WBG_WKT_FIRMENNR AND WKT_KONTENTABNR = WBG_KONTENTABNR" "~"
if joinKuerzel = WKTWBG
searchResult := searchResult "LEFT JOIN WWS.WBG_WARENBESCHKOSTENGRP ON WBG_WKT_FIRMENNR = WKT_FIRMENNR AND WBG_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = WEAWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = WEA_HAUPTFIRMENNR AND WKT_KONTENTABNR = WEA_KONTENTABNR" "~"
if joinKuerzel = WKTWEA
searchResult := searchResult "LEFT JOIN WWS.WEA_WWSERKONTROLLERABATTE ON WEA_HAUPTFIRMENNR = WKT_FIRMENNR AND WEA_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = AKZWKU
searchResult := searchResult "LEFT JOIN WKU_WWSKUNDE.WKU_WWSKUNDE ON WKU_FIRMENNR = AKZ_KUN_FIRMENNR AND WKU_KLID = AKZ_KUN_KLID" "~"
if joinKuerzel = WKUAKZ
searchResult := searchResult "LEFT JOIN WWS.AKZ_ARTIKELKUNDENZU ON AKZ_KUN_FIRMENNR = WKU_FIRMENNR AND AKZ_KUN_KLID = WKU_KLID" "~"
if joinKuerzel = WQLWQI
searchResult := searchResult "LEFT JOIN BMDDEFAULT.WQI_WEITERE_INFO_ITEM ON WQI_WIIID = WQL_WIIID" "~"
if joinKuerzel = WQIWQL
searchResult := searchResult "LEFT JOIN BMDDEFAULT.WQL_WEITERE_INFO_LANG ON WQL_WIIID = WQI_WIIID" "~"
if joinKuerzel = ADEWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = ADE_HAUPTFIRMENNR AND WTB_WWSTEXTBNR = ADE_WWSTEXTBNR_BEIBETRAG0" "~"
if joinKuerzel = WTBADE
searchResult := searchResult "LEFT JOIN WWS.ADE_ABGABENDETAILERFPOS ON ADE_HAUPTFIRMENNR = WTB_FIRMENNR AND ADE_WWSTEXTBNR_BEIBETRAG0 = WTB_WWSTEXTBNR" "~"
if joinKuerzel = EFKWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = EFK_ZAHLKO_WTB_FIRMENNR AND WTB_WWSTEXTBNR = EFK_ZAHLKO_WWSTEXTBNR" "~"
if joinKuerzel = WTBEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_ZAHLKO_WTB_FIRMENNR = WTB_FIRMENNR AND EFK_ZAHLKO_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = RNGWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = RNG_ZAHLKO_WTB_FIRMENNR AND WTB_WWSTEXTBNR = RNG_ZAHLKO_WWSTEXTBNR" "~"
if joinKuerzel = WTBRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_ZAHLKO_WTB_FIRMENNR = WTB_FIRMENNR AND RNG_ZAHLKO_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = ARBWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = ARB_KURZBESCHR_FIRMENNR AND WTB_WWSTEXTBNR = ARB_KURZBESCHR_WWSTEXTBNR" "~"
if joinKuerzel = WTBARB
searchResult := searchResult "LEFT JOIN WWS.ARB_ARTIKELBEREICHSWERTE ON ARB_KURZBESCHR_FIRMENNR = WTB_FIRMENNR AND ARB_KURZBESCHR_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = BOPWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = BOP_WTB_BESCH_FIRMENNR AND WTB_WWSTEXTBNR = BOP_WTB_BESCH_WWSTEXTBNR" "~"
if joinKuerzel = WTBBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_WTB_BESCH_FIRMENNR = WTB_FIRMENNR AND BOP_WTB_BESCH_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = EFKWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = EFK_ZAHLKOR_WTB_FIRMENNR AND WTB_WWSTEXTBNR = EFK_ZAHLKOR_WWSTEXTBNR" "~"
if joinKuerzel = WTBEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_ZAHLKOR_WTB_FIRMENNR = WTB_FIRMENNR AND EFK_ZAHLKOR_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = KASWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = KAS_WTB_FUSSTEXT_FIRMENNR AND WTB_WWSTEXTBNR = KAS_WTB_FUSSTEXTNR" "~"
if joinKuerzel = WTBKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_WTB_FUSSTEXT_FIRMENNR = WTB_FIRMENNR AND KAS_WTB_FUSSTEXTNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = ERZWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = ERZ_EMAILTEXT_WTB_FIRMENNR AND WTB_WWSTEXTBNR = ERZ_EMAILTEXT_WWSTEXTBNR" "~"
if joinKuerzel = WTBERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_EMAILTEXT_WTB_FIRMENNR = WTB_FIRMENNR AND ERZ_EMAILTEXT_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = DSVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DSV_LETZTERUSER" "~"
if joinKuerzel = BMUDSV
searchResult := searchResult "LEFT JOIN BUERO.DSV_DSGVO_VERZEICHNIS ON DSV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DUFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DUF_LETZTERUSER" "~"
if joinKuerzel = BMUDUF
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DUF_DUSERLINKFIELD ON DUF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DUZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DUZ_LETZTERUSER" "~"
if joinKuerzel = BMUDUZ
searchResult := searchResult "LEFT JOIN DATANORM.DUZ_DATANORM_WUGZU ON DUZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DWUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DWU_LETZTERUSER" "~"
if joinKuerzel = BMUDWU
searchResult := searchResult "LEFT JOIN DATANORM.DWU_WAREN_UNTER_GRUPPE ON DWU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DWZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DWZ_LETZTERUSER" "~"
if joinKuerzel = BMUDWZ
searchResult := searchResult "LEFT JOIN BUERO.DWZ_DOKUMENTWORKFLOWZU ON DWZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EFKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EFK_LETZTERUSER" "~"
if joinKuerzel = BMUEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ELVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ELV_LETZTERUSER" "~"
if joinKuerzel = BMUELV
searchResult := searchResult "LEFT JOIN WWS.ELV_ERFASSPOSLIEFERVOR ON ELV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EMTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EMT_LETZTERUSER" "~"
if joinKuerzel = BMUEMT
searchResult := searchResult "LEFT JOIN BUERO.EMT_EMAILART ON EMT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EPFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EPF_LETZTERUSER" "~"
if joinKuerzel = BMUEPF
searchResult := searchResult "LEFT JOIN WWS.EPF_EPSTKL_REF ON EPF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ERABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ERA_LETZTERUSER" "~"
if joinKuerzel = BMUERA
searchResult := searchResult "LEFT JOIN WWS.ERA_ERFASSRABATT ON ERA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ERGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ERG_LETZTERUSER" "~"
if joinKuerzel = BMUERG
searchResult := searchResult "LEFT JOIN WWS.ERG_ERFASSUNGSGRUPPEN ON ERG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ERPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ERP_LETZTERUSER" "~"
if joinKuerzel = BMUERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ESTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EST_LETZTERUSER" "~"
if joinKuerzel = BMUEST
searchResult := searchResult "LEFT JOIN BMD.EST_EDISCHEMATYP ON EST_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ESZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ESZ_LETZTERUSER" "~"
if joinKuerzel = BMUESZ
searchResult := searchResult "LEFT JOIN WWS.ESZ_ERZ_SRA_ZU ON ESZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FAPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FAP_LETZTERUSER" "~"
if joinKuerzel = BMUFAP
searchResult := searchResult "LEFT JOIN BUERO.FAP_FAPERSZU ON FAP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FBABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FBA_LETZTERUSER" "~"
if joinKuerzel = BMUFBA
searchResult := searchResult "LEFT JOIN BIL.FBA_XMLJABFBANHANG ON FBA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FBNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FBN_LETZTERUSER" "~"
if joinKuerzel = BMUFBN
searchResult := searchResult "LEFT JOIN BUERO.FBN_FRAGEBOGEN ON FBN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FEHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FEH_LETZTERUSER" "~"
if joinKuerzel = BMUFEH
searchResult := searchResult "LEFT JOIN WWS.FEH_FERTEP_HISTORIE ON FEH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FFABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FFA_LETZTERUSER" "~"
if joinKuerzel = BMUFFA
searchResult := searchResult "LEFT JOIN BUERO.FFA_FIRMENFINANZAMTZU ON FFA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FGMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FGM_LETZTERUSER" "~"
if joinKuerzel = BMUFGM
searchResult := searchResult "LEFT JOIN BUERO.FGM_FIRMENGEMEINDEZU ON FGM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FOXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FOX_LETZTERUSER" "~"
if joinKuerzel = BMUFOX
searchResult := searchResult "LEFT JOIN BMD.FOX_FORMELTEXT ON FOX_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FPRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FPR_LETZTERUSER" "~"
if joinKuerzel = BMUFPR
searchResult := searchResult "LEFT JOIN FIBU.FPR_FIBUPROZENTSATZ ON FPR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FRABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FRA_LETZTERUSER" "~"
if joinKuerzel = BMUFRA
searchResult := searchResult "LEFT JOIN BUERO.FRA_FRISTART ON FRA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FRIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FRI_LETZTERUSER" "~"
if joinKuerzel = BMUFRI
searchResult := searchResult "LEFT JOIN BUERO.FRI_FRISTVERLAENGERUNGEN ON FRI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FSTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FST_LETZTERUSER" "~"
if joinKuerzel = BMUFST
searchResult := searchResult "LEFT JOIN BUERO.FST_FRIST ON FST_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FZPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FZP_LETZTERUSER" "~"
if joinKuerzel = BMUFZP
searchResult := searchResult "LEFT JOIN FIBU.FZP_FINANZPLAN ON FZP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FURBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FUR_LETZTERUSER" "~"
if joinKuerzel = BMUFUR
searchResult := searchResult "LEFT JOIN BMD.FUR_FUNKRECHTE ON FUR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FWFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FWF_LETZTERUSER" "~"
if joinKuerzel = BMUFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GAZ_LETZTERUSER" "~"
if joinKuerzel = BMUGAZ
searchResult := searchResult "LEFT JOIN BUERO.GAZ_GESELLANGEHOERIGERZU ON GAZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GNHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GNH_LETZTERUSER" "~"
if joinKuerzel = BMUGNH
searchResult := searchResult "LEFT JOIN BIL.GNH_GAU_HEADER ON GNH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GOIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GOI_LETZTERUSER" "~"
if joinKuerzel = BMUGOI
searchResult := searchResult "LEFT JOIN BUERO.GOI_GRUPPENORDNERINSTZU ON GOI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GRDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GRD_LETZTERUSER" "~"
if joinKuerzel = BMUGRD
searchResult := searchResult "LEFT JOIN DATANORM.GRD_GRAFIK_DETAIL ON GRD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GSPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GSP_LETZTERUSER" "~"
if joinKuerzel = BMUGSP
searchResult := searchResult "LEFT JOIN WWS.GSP_GUTSCHEINPOSITION ON GSP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GVMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GVM_LETZTERUSER" "~"
if joinKuerzel = BMUGVM
searchResult := searchResult "LEFT JOIN BUERO.GVM_GHOSTVOLLMACHT ON GVM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GWQBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GWQ_LETZTERUSER" "~"
if joinKuerzel = BMUGWQ
searchResult := searchResult "LEFT JOIN HRS.GWQ_GWBANKZU_SEQ ON GWQ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HFVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HFV_LETZTERUSER" "~"
if joinKuerzel = BMUHFV
searchResult := searchResult "LEFT JOIN BMD.HFV_HILFEFAVORITEN ON HFV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HSKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HSK_LETZTERUSER" "~"
if joinKuerzel = BMUHSK
searchResult := searchResult "LEFT JOIN HRS.HSK_SORTIERKOPF ON HSK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IAK_LETZTERUSER" "~"
if joinKuerzel = BMUIAK
searchResult := searchResult "LEFT JOIN WWS.IAK_INVABWTKOPF ON IAK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IGZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IGZ_LETZTERUSER" "~"
if joinKuerzel = BMUIGZ
searchResult := searchResult "LEFT JOIN BMD.IGZ_INTERVALLGRUPPEZU ON IGZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IPRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IPR_LETZTERUSER" "~"
if joinKuerzel = BMUIPR
searchResult := searchResult "LEFT JOIN WWS.IPR_IMPORTREFERENZ_55 ON IPR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IZJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IZJ_LETZTERUSER" "~"
if joinKuerzel = BMUIZJ
searchResult := searchResult "LEFT JOIN BUERO.IZJ_ZAEHLERJOURNAL ON IZJ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KAS_LETZTERUSER" "~"
if joinKuerzel = BMUKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KBZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KBZ_LETZTERUSER" "~"
if joinKuerzel = BMUKBZ
searchResult := searchResult "LEFT JOIN KORE.KBZ_KOSTENBAUMZU ON KBZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KFFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KFF_LETZTERUSER" "~"
if joinKuerzel = BMUKFF
searchResult := searchResult "LEFT JOIN BUERO.KFF_KORETYPFIND_FELDERZU ON KFF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KKKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KKK_LETZTERUSER" "~"
if joinKuerzel = BMUKKK
searchResult := searchResult "LEFT JOIN WWS.KKK_KREDITKARTENKONTENZU ON KKK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KMIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KMI_LETZTERUSER" "~"
if joinKuerzel = BMUKMI
searchResult := searchResult "LEFT JOIN KORE.KMI_ISTMENGEN ON KMI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KOFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KOF_LETZTERUSER" "~"
if joinKuerzel = BMUKOF
searchResult := searchResult "LEFT JOIN PRODUKTION.KOF_KOKFILTER ON KOF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KSMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KSM_LETZTERUSER" "~"
if joinKuerzel = BMUKSM
searchResult := searchResult "LEFT JOIN WWS.KSM_KASSENSUMMEN ON KSM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KTGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KTG_LETZTERUSER" "~"
if joinKuerzel = BMUKTG
searchResult := searchResult "LEFT JOIN FIBU.KTG_KONTENGRENZEN ON KTG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KUKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KUK_LETZTERUSER" "~"
if joinKuerzel = BMUKUK
searchResult := searchResult "LEFT JOIN WWS.KUK_KUNDENKARTEI ON KUK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KUZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KUZ_LETZTERUSER" "~"
if joinKuerzel = BMUKUZ
searchResult := searchResult "LEFT JOIN BUERO.KUZ_KUNDENZAHLUNGEN ON KUZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KVEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KVE_LETZTERUSER" "~"
if joinKuerzel = BMUKVE
searchResult := searchResult "LEFT JOIN FIBU.KVE_KOREVORERFASSUNG ON KVE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BMLZVT
searchResult := searchResult "LEFT JOIN ZVT_TBDATEIEN.ZVT_TBDATEIEN ON ZVT_ZVTID = BML_ZVT_ZVTID" "~"
if joinKuerzel = ZVTBML
searchResult := searchResult "LEFT JOIN FIBU.BML_BANKRVMODEL ON BML_ZVT_ZVTID = ZVT_ZVTID" "~"
if joinKuerzel = BAIZVT
searchResult := searchResult "LEFT JOIN BUERO.ZVT_TBDATEIEN ON ZVT_ZVTID = BAI_TBFEINZUGEU_ZVTID" "~"
if joinKuerzel = ZVTBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_TBFEINZUGEU_ZVTID = ZVT_ZVTID" "~"
if joinKuerzel = ARLZWK
searchResult := searchResult "LEFT JOIN WWS.ZWK_ZUORDNUNGWVKOPF ON ZWK_BMDWVNR = ARL_BMDWVNR" "~"
if joinKuerzel = ZWKARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_BMDWVNR = ZWK_BMDWVNR" "~"
if joinKuerzel = ERPZWK
searchResult := searchResult "LEFT JOIN WWS.ZWK_ZUORDNUNGWVKOPF ON ZWK_BMDWVNR = ERP_BMDWVNR" "~"
if joinKuerzel = ZWKERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_BMDWVNR = ZWK_BMDWVNR" "~"
if joinKuerzel = ZXFZXD
searchResult := searchResult "LEFT JOIN BUERO.ZXD_ZEITAENDERUNG ON ZXD_ZANTRAGID = ZXF_ZANTRAGID AND ZXD_ZAENDERUNGNR = ZXF_ZAENDERUNGNR" "~"
if joinKuerzel = ZXDZXF
searchResult := searchResult "LEFT JOIN BUERO.ZXF_ZEITAENDERUNGFELD ON ZXF_ZANTRAGID = ZXD_ZANTRAGID AND ZXF_ZAENDERUNGNR = ZXD_ZAENDERUNGNR" "~"
if joinKuerzel = AAPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AAP_LETZTERUSER" "~"
if joinKuerzel = BMUAAP
searchResult := searchResult "LEFT JOIN WWS.AAP_ARAPOSITION ON AAP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABO_LETZTERUSER" "~"
if joinKuerzel = BMUABO
searchResult := searchResult "LEFT JOIN BIL.ABO_ANBUBUCHUNGKONTEN ON ABO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABU_LETZTERUSER" "~"
if joinKuerzel = BMUABU
searchResult := searchResult "LEFT JOIN BIL.ABU_ANLAGENBUCHHALTUNG ON ABU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ACFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ACF_LETZTERUSER" "~"
if joinKuerzel = BMUACF
searchResult := searchResult "LEFT JOIN FIBU.ACF_TESTFALL ON ACF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ADABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ADA_LETZTERUSER" "~"
if joinKuerzel = BMUADA
searchResult := searchResult "LEFT JOIN BUERO.ADA_ADRESSART ON ADA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AEZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AEZ_LETZTERUSER" "~"
if joinKuerzel = BMUAEZ
searchResult := searchResult "LEFT JOIN BUERO.AEZ_AUFGABENTICKETEMAILZU ON AEZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AGSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AGS_LETZTERUSER" "~"
if joinKuerzel = BMUAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AKRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AKR_LETZTERUSER" "~"
if joinKuerzel = BMUAKR
searchResult := searchResult "LEFT JOIN BUERO.AKR_AVKOSTENARTENZU ON AKR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ALZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ALZ_LETZTERUSER" "~"
if joinKuerzel = BMUALZ
searchResult := searchResult "LEFT JOIN WWS.ALZ_ALLERGENDATEN ON ALZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AMOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AMO_LETZTERUSER" "~"
if joinKuerzel = BMUAMO
searchResult := searchResult "LEFT JOIN KORE.AMO_AUSWERTUNGMODELL ON AMO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = APLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = APL_LETZTERUSER" "~"
if joinKuerzel = BMUAPL
searchResult := searchResult "LEFT JOIN WWS.APL_ARBEITSPLANLINIEZU ON APL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = APPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = APP_LETZTERUSER" "~"
if joinKuerzel = BMUAPP
searchResult := searchResult "LEFT JOIN BMDINTERN.APP_ASPPWD ON APP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ARGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ARG_LETZTERUSER" "~"
if joinKuerzel = BMUARG
searchResult := searchResult "LEFT JOIN BUERO.ARG_ARBEITSRUHEZEITGESETZ ON ARG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ASHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ASH_LETZTERUSER" "~"
if joinKuerzel = BMUASH
searchResult := searchResult "LEFT JOIN BUERO.ASH_AUSWEISHIST ON ASH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AUEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AUE_LETZTERUSER" "~"
if joinKuerzel = BMUAUE
searchResult := searchResult "LEFT JOIN ZLE.AUE_AUSWERTTYPEINST ON AUE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AUTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AUT_LETZTERUSER" "~"
if joinKuerzel = BMUAUT
searchResult := searchResult "LEFT JOIN ZLE.AUT_AUSWERTTYP ON AUT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AVDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AVD_LETZTERUSER" "~"
if joinKuerzel = BMUAVD
searchResult := searchResult "LEFT JOIN BUERO.AVD_AUSBVERWDOKUMENTZU ON AVD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AVIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AVI_LETZTERUSER" "~"
if joinKuerzel = BMUAVI
searchResult := searchResult "LEFT JOIN HRS.AVI_AVISOMELDUNG ON AVI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AVRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AVR_LETZTERUSER" "~"
if joinKuerzel = BMUAVR
searchResult := searchResult "LEFT JOIN BUERO.AVR_AVRUEZAHLUNGSTABELLE ON AVR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AVWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AVW_LETZTERUSER" "~"
if joinKuerzel = BMUAVW
searchResult := searchResult "LEFT JOIN BUERO.AVW_AVRUEZAHLUNGSWERTE ON AVW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AVZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AVZ_LETZTERUSER" "~"
if joinKuerzel = BMUAVZ
searchResult := searchResult "LEFT JOIN FIBU.AVZ_ANBUFIBZU ON AVZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BWXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BWX_LETZTERUSER" "~"
if joinKuerzel = BMUBWX
searchResult := searchResult "LEFT JOIN BMDWORK.BWX_BMDWORK ON BWX_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CKLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CKL_LETZTERUSER" "~"
if joinKuerzel = BMUCKL
searchResult := searchResult "LEFT JOIN BUERO.CKL_CHECKLISTE ON CKL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CTZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CTZ_LETZTERUSER" "~"
if joinKuerzel = BMUCTZ
searchResult := searchResult "LEFT JOIN BMDDEFAULT.CTZ_CACHETABLEZU ON CTZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DAPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DAP_LETZTERUSER" "~"
if joinKuerzel = BMUDAP
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DAP_WAAUSDRUCKPARAMETER ON DAP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DCEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DCE_LETZTERUSER" "~"
if joinKuerzel = BMUDCE
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DCE_WABAUMELEMENTE ON DCE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DCVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DCV_LETZTERUSER" "~"
if joinKuerzel = BMUDCV
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DCV_WAPARAMDEFVALUES ON DCV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DIMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DIM_LETZTERUSER" "~"
if joinKuerzel = BMUDIM
searchResult := searchResult "LEFT JOIN DATANORM.DIM_DIM_TEXT ON DIM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DKZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DKZ_LETZTERUSER" "~"
if joinKuerzel = BMUDKZ
searchResult := searchResult "LEFT JOIN BUERO.DKZ_DOKUMENTZU ON DKZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DNABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DNA_LETZTERUSER" "~"
if joinKuerzel = BMUDNA
searchResult := searchResult "LEFT JOIN DATANORM.DNA_ARTIKEL ON DNA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DNIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DNI_LETZTERUSER" "~"
if joinKuerzel = BMUDNI
searchResult := searchResult "LEFT JOIN DATANORM.DNI_DATEI_INFO ON DNI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DODBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DOD_LETZTERUSER" "~"
if joinKuerzel = BMUDOD
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DOD_OCRTEMPLATEDETAIL ON DOD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DOVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DOV_LETZTERUSER" "~"
if joinKuerzel = BMUDOV
searchResult := searchResult "LEFT JOIN BMD.DOV_DOGVORSCHLAG ON DOV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DPMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DPM_LETZTERUSER" "~"
if joinKuerzel = BMUDPM
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DPM_DFUNKTIONSPARAM ON DPM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DRGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DRG_LETZTERUSER" "~"
if joinKuerzel = BMUDRG
searchResult := searchResult "LEFT JOIN HRS.DRG_DIENSTREISEGEN ON DRG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DSSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DSS_LETZTERUSER" "~"
if joinKuerzel = BMUDSS
searchResult := searchResult "LEFT JOIN BUERO.DSS_DSGVO_STANDARD ON DSS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DVWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DVW_LETZTERUSER" "~"
if joinKuerzel = BMUDVW
searchResult := searchResult "LEFT JOIN BUERO.DVW_DOKUMENTENVERWEIS ON DVW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EAVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EAV_LETZTERUSER" "~"
if joinKuerzel = BMUEAV
searchResult := searchResult "LEFT JOIN HRS.EAV_ELDADETAIL_AV ON EAV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EFPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EFP_LETZTERUSER" "~"
if joinKuerzel = BMUEFP
searchResult := searchResult "LEFT JOIN HRS.EFP_ENTGELTFORTZAHLPROZ ON EFP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EGMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EGM_LETZTERUSER" "~"
if joinKuerzel = BMUEGM
searchResult := searchResult "LEFT JOIN HRS.EGM_ELDADETAIL_GM ON EGM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EHPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EHP_LETZTERUSER" "~"
if joinKuerzel = BMUEHP
searchResult := searchResult "LEFT JOIN WWS.EHP_EINHEITENTABPOS ON EHP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EM4BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EM4_LETZTERUSER" "~"
if joinKuerzel = BMUEM4
searchResult := searchResult "LEFT JOIN HRS.EM4_ELDADETAIL_VB ON EM4_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EMABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EMA_LETZTERUSER" "~"
if joinKuerzel = BMUEMA
searchResult := searchResult "LEFT JOIN BUERO.EMA_EMAIL ON EMA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EPRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EPR_LETZTERUSER" "~"
if joinKuerzel = BMUEPR
searchResult := searchResult "LEFT JOIN WWS.EPR_ERFASSPROVISION ON EPR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ERNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ERN_LETZTERUSER" "~"
if joinKuerzel = BMUERN
searchResult := searchResult "LEFT JOIN WWS.ERN_EXTARLKEYNAMES ON ERN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ERTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ERT_LETZTERUSER" "~"
if joinKuerzel = BMUERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ETLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ETL_LETZTERUSER" "~"
if joinKuerzel = BMUETL
searchResult := searchResult "LEFT JOIN BUERO.ETL_ENTGANGENE_TELEFONATE ON ETL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PERADR
searchResult := searchResult "LEFT JOIN BUERO.ADR_ADRESSE ON ADR_ADRESSLFDNR = PER_ADRESSLFDNR" "~"
if joinKuerzel = ADRPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = AARBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AAR_LETZTERUSER" "~"
if joinKuerzel = BMUAAR
searchResult := searchResult "LEFT JOIN WWS.AAR_ALTERNATIVARTIKEL ON AAR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABN_LETZTERUSER" "~"
if joinKuerzel = BMUABN
searchResult := searchResult "LEFT JOIN HRS.ABN_ABWESENHEIT_TAG ON ABN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABQBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABQ_LETZTERUSER" "~"
if joinKuerzel = BMUABQ
searchResult := searchResult "LEFT JOIN HRS.ABQ_ARBEITSSTAETTE_SEQ ON ABQ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABR_LETZTERUSER" "~"
if joinKuerzel = BMUABR
searchResult := searchResult "LEFT JOIN BIL.ABR_ANBUBEWRES ON ABR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ACEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ACE_LETZTERUSER" "~"
if joinKuerzel = BMUACE
searchResult := searchResult "LEFT JOIN FIBU.ACE_EXTTESTFALL ON ACE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ADDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ADD_LETZTERUSER" "~"
if joinKuerzel = BMUADD
searchResult := searchResult "LEFT JOIN BUERO.ADD_AUSDRUCKMODELLDETAIL ON ADD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ADEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ADE_LETZTERUSER" "~"
if joinKuerzel = BMUADE
searchResult := searchResult "LEFT JOIN WWS.ADE_ABGABENDETAILERFPOS ON ADE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ADPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ADP_LETZTERUSER" "~"
if joinKuerzel = BMUADP
searchResult := searchResult "LEFT JOIN BIL.ADP_AUSDRUCKGESTALTUNG ON ADP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ADWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ADW_LETZTERUSER" "~"
if joinKuerzel = BMUADW
searchResult := searchResult "LEFT JOIN WWS.ADW_ABGABENDETAILWAP ON ADW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AFKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AFK_LETZTERUSER" "~"
if joinKuerzel = BMUAFK
searchResult := searchResult "LEFT JOIN ZLE.AFK_EXTFILTERKOPF ON AFK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AGBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AGB_LETZTERUSER" "~"
if joinKuerzel = BMUAGB
searchResult := searchResult "LEFT JOIN WWS.AGB_ARBEITSGANGBAZZU ON AGB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AGLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AGL_LETZTERUSER" "~"
if joinKuerzel = BMUAGL
searchResult := searchResult "LEFT JOIN PRODUKTION.AGL_ARBEITSGANGZU ON AGL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AGPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AGP_LETZTERUSER" "~"
if joinKuerzel = BMUAGP
searchResult := searchResult "LEFT JOIN WWS.AGP_ARTIKELGRUPPENPOS ON AGP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AINBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AIN_LETZTERUSER" "~"
if joinKuerzel = BMUAIN
searchResult := searchResult "LEFT JOIN BUERO.AIN_AUSSCHREIBUNGINSERTZU ON AIN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AKNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AKN_LETZTERUSER" "~"
if joinKuerzel = BMUAKN
searchResult := searchResult "LEFT JOIN BIL.AKN_ANBU_KONSOL_AUSSCHLUSS ON AKN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AKVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AKV_LETZTERUSER" "~"
if joinKuerzel = BMUAKV
searchResult := searchResult "LEFT JOIN FIBU.AKV_AUTOKONTONR_VERGABE ON AKV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ALIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ALI_LETZTERUSER" "~"
if joinKuerzel = BMUALI
searchResult := searchResult "LEFT JOIN BMDDEFAULT.ALI_ATTRGRUPPELIZENZZU ON ALI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ALOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ALO_LETZTERUSER" "~"
if joinKuerzel = BMUALO
searchResult := searchResult "LEFT JOIN HRS.ALO_ARTIKELLOHNARTZU ON ALO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AMZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AMZ_LETZTERUSER" "~"
if joinKuerzel = BMUAMZ
searchResult := searchResult "LEFT JOIN BUERO.AMZ_AUSDRUCKMODELLZU ON AMZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = APSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = APS_LETZTERUSER" "~"
if joinKuerzel = BMUAPS
searchResult := searchResult "LEFT JOIN WWS.APS_ARBEITSPLAN_STAMM ON APS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AQBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AQB_LETZTERUSER" "~"
if joinKuerzel = BMUAQB
searchResult := searchResult "LEFT JOIN WWS.AQB_ARAQUOTENBLATT ON AQB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AROBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ARO_LETZTERUSER" "~"
if joinKuerzel = BMUARO
searchResult := searchResult "LEFT JOIN WWS.ARO_APS_KOSTEN ON ARO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ASMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ASM_LETZTERUSER" "~"
if joinKuerzel = BMUASM
searchResult := searchResult "LEFT JOIN BUERO.ASM_AUSBILDUNGSSTAMMMERKM ON ASM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AVBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AVB_LETZTERUSER" "~"
if joinKuerzel = BMUAVB
searchResult := searchResult "LEFT JOIN BUERO.AVB_AVBUDGET ON AVB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AZUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AZU_LETZTERUSER" "~"
if joinKuerzel = BMUAZU
searchResult := searchResult "LEFT JOIN WWS.AZU_STATISTIKARTIKELZU ON AZU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WKGWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = WKG_FIRMENNR AND WTB_WWSTEXTBNR = WKG_KOPF_RNG_WWSTEXTBNR" "~"
if joinKuerzel = WTBWKG
searchResult := searchResult "LEFT JOIN WWS.WKG_WWSKUNDENGRUPPE ON WKG_FIRMENNR = WTB_FIRMENNR AND WKG_KOPF_RNG_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = WADWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = WAD_HAUPTFIRMENNR AND WTB_WWSTEXTBNR = WAD_FUSS_RNG_WWSTEXTBNR" "~"
if joinKuerzel = WTBWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_HAUPTFIRMENNR = WTB_FIRMENNR AND WAD_FUSS_RNG_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = ERZWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = ERZ_HAUPTFIRMENNR AND WTB_WWSTEXTBNR = ERZ_FUSS_LFS_WWSTEXTBNR" "~"
if joinKuerzel = WTBERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_HAUPTFIRMENNR = WTB_FIRMENNR AND ERZ_FUSS_LFS_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = WKGWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = WKG_FIRMENNR AND WTB_WWSTEXTBNR = WKG_FUSS_LFS_WWSTEXTBNR" "~"
if joinKuerzel = WTBWKG
searchResult := searchResult "LEFT JOIN WWS.WKG_WWSKUNDENGRUPPE ON WKG_FIRMENNR = WTB_FIRMENNR AND WKG_FUSS_LFS_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = WGRWWB
searchResult := searchResult "LEFT JOIN WWS.WWB_WWSBERICHTE ON WWB_WWSBERICHTNR = WGR_WWSBERICHTNR" "~"
if joinKuerzel = WWBWGR
searchResult := searchResult "LEFT JOIN WWS.WGR_WWSBERICHTGRUPPIERUNG ON WGR_WWSBERICHTNR = WWB_WWSBERICHTNR" "~"
if joinKuerzel = WWBWWK
searchResult := searchResult "LEFT JOIN WWS.WWK_WWSBERICHTKOPF ON WWK_WWSBERICHTKOPFNR = WWB_BERICHTKOPFNR" "~"
if joinKuerzel = WWKWWB
searchResult := searchResult "LEFT JOIN WWS.WWB_WWSBERICHTE ON WWB_BERICHTKOPFNR = WWK_WWSBERICHTKOPFNR" "~"
if joinKuerzel = YKEYBU
searchResult := searchResult "LEFT JOIN FIBU.YBU_AUTOBUCHUNG ON YBU_YBMODELLID = YKE_YBMODELLID AND YBU_AUTOBUCHUNGID = YKE_AUTOBUCHUNGID" "~"
if joinKuerzel = YBUYKE
searchResult := searchResult "LEFT JOIN FIBU.YKE_AUTOBUCHKOREERF ON YKE_YBMODELLID = YBU_YBMODELLID AND YKE_AUTOBUCHUNGID = YBU_AUTOBUCHUNGID" "~"
if joinKuerzel = ZAWZAT
searchResult := searchResult "LEFT JOIN BUERO.ZAT_ZEITAUSWERTTYP ON ZAT_ZEITAUSWERTTYPNR = ZAW_ZEITAUSWERTTYPNR" "~"
if joinKuerzel = ZATZAW
searchResult := searchResult "LEFT JOIN BUERO.ZAW_ZEITAUSWERTUNG ON ZAW_ZEITAUSWERTTYPNR = ZAT_ZEITAUSWERTTYPNR" "~"
if joinKuerzel = ZPLZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_FIRMENNR = ZPL_ZBA_FIRMENNR AND ZBA_ZBUCHARTNR = ZPL_ZBUCHARTNR" "~"
if joinKuerzel = ZBAZPL
searchResult := searchResult "LEFT JOIN BUERO.ZPL_ZEITPLANUNG ON ZPL_ZBA_FIRMENNR = ZBA_FIRMENNR AND ZPL_ZBUCHARTNR = ZBA_ZBUCHARTNR" "~"
if joinKuerzel = WAGZBE
searchResult := searchResult "LEFT JOIN ZBE_ZUSTAENDIGKEITBEREICH.ZBE_ZUSTAENDIGKEITBEREICH ON ZBE_ZUSTBEREICHLFDNR = WAG_ZUSTBEREICHLFDNR" "~"
if joinKuerzel = ZBEWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_ZUSTBEREICHLFDNR = ZBE_ZUSTBEREICHLFDNR" "~"
if joinKuerzel = BKMZBK
searchResult := searchResult "LEFT JOIN BUERO.ZBK_ZEITBETRIEBSKALENDER ON ZBK_BETRIEBSKALID = BKM_BETRIEBSKALID" "~"
if joinKuerzel = ZBKBKM
searchResult := searchResult "LEFT JOIN BUERO.BKM_ZEITBETRKALMAZUORD ON BKM_BETRIEBSKALID = ZBK_BETRIEBSKALID" "~"
if joinKuerzel = BEWZBZ
searchResult := searchResult "LEFT JOIN BUERO.ZBZ_ZEITBEZUGSART ON ZBZ_FIRMENNR = BEW_ZBZ_FIRMENNR AND ZBZ_ZBEZUGSARTNR = BEW_ZBEZUGSARTNR_NEXTMON3" "~"
if joinKuerzel = ZBZBEW
searchResult := searchResult "LEFT JOIN BUERO.BEW_BEWERTUNGSMODELL ON BEW_ZBZ_FIRMENNR = ZBZ_FIRMENNR AND BEW_ZBEZUGSARTNR_NEXTMON3 = ZBZ_ZBEZUGSARTNR" "~"
if joinKuerzel = BUCZES
searchResult := searchResult "LEFT JOIN FIBU.ZES_ZESSIONSTEXT ON ZES_FIRMENNR = BUC_FIRMENNR AND ZES_FIBUNR = BUC_FIBUNR AND ZES_ZESSKZ = BUC_ZES_ZESSKZ" "~"
if joinKuerzel = ZESBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_FIRMENNR = ZES_FIRMENNR AND BUC_FIBUNR = ZES_FIBUNR AND BUC_ZES_ZESSKZ = ZES_ZESSKZ" "~"
if joinKuerzel = RNGZES
searchResult := searchResult "LEFT JOIN ZES_ZESSIONSTEXT.ZES_ZESSIONSTEXT ON ZES_FIRMENNR = RNG_FIBU_FIRMENNR AND ZES_FIBUNR = RNG_FIBUNR AND ZES_ZESSKZ = RNG_ZESSKZ" "~"
if joinKuerzel = ZESRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIBU_FIRMENNR = ZES_FIRMENNR AND RNG_FIBUNR = ZES_FIBUNR AND RNG_ZESSKZ = ZES_ZESSKZ" "~"
if joinKuerzel = ZIWZIT
searchResult := searchResult "LEFT JOIN BUERO.ZIT_ZUSATZINFOTYP ON ZIT_FIRMENNR = ZIW_ZIT_FIRMENNR AND ZIT_TYPNR = ZIW_ZIT_TYPNR" "~"
if joinKuerzel = ZITZIW
searchResult := searchResult "LEFT JOIN BUERO.ZIW_ZUSATZINFOWERT ON ZIW_ZIT_FIRMENNR = ZIT_FIRMENNR AND ZIW_ZIT_TYPNR = ZIT_TYPNR" "~"
if joinKuerzel = BTMZPK
searchResult := searchResult "LEFT JOIN BUERO.ZPK_ZEITPROFILKOPF ON ZPK_FIRMENNR = BTM_ZEITPROFIL_FIRMENNR AND ZPK_ZEITPROFILNR = BTM_ZEITPROFILNR" "~"
if joinKuerzel = ZPKBTM
searchResult := searchResult "LEFT JOIN BUERO.BTM_BTERMINAL ON BTM_ZEITPROFIL_FIRMENNR = ZPK_FIRMENNR AND BTM_ZEITPROFILNR = ZPK_ZEITPROFILNR" "~"
if joinKuerzel = BPKZRT
searchResult := searchResult "LEFT JOIN ZRT_ZAHLUNGSART.ZRT_ZAHLUNGSART ON ZRT_ZAHLARTNR = BPK_ZAHLARTNR" "~"
if joinKuerzel = ZRTBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_ZAHLARTNR = ZRT_ZAHLARTNR" "~"
if joinKuerzel = RAZZRT
searchResult := searchResult "LEFT JOIN BUERO.ZRT_ZAHLUNGSART ON ZRT_ZAHLARTNR = RAZ_ANZAHLUNG_ZAHLARTNR" "~"
if joinKuerzel = ZRTRAZ
searchResult := searchResult "LEFT JOIN BUERO.RAZ_RATENZAHLUNG ON RAZ_ANZAHLUNG_ZAHLARTNR = ZRT_ZAHLARTNR" "~"
if joinKuerzel = BAIZRT
searchResult := searchResult "LEFT JOIN BUERO.ZRT_ZAHLUNGSART ON ZRT_ZAHLARTNR = BAI_ZRT_ZAHLARTNR3" "~"
if joinKuerzel = ZRTBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_ZRT_ZAHLARTNR3 = ZRT_ZAHLARTNR" "~"
if joinKuerzel = BRGZSK
searchResult := searchResult "LEFT JOIN ZSK_ZEITSCHEIBEKOPF.ZSK_ZEITSCHEIBEKOPF ON ZSK_ZSKID = BRG_ZSKID" "~"
if joinKuerzel = ZSKBRG
searchResult := searchResult "LEFT JOIN BUERO.BRG_BERUFSGRUPPEN ON BRG_ZSKID = ZSK_ZSKID" "~"
if joinKuerzel = ASWZTZ
searchResult := searchResult "LEFT JOIN ZTZ_ZEITZONEN.ZTZ_ZEITZONEN ON ZTZ_ZEITZONENR = ASW_ZEITZONENR" "~"
if joinKuerzel = ZTZASW
searchResult := searchResult "LEFT JOIN BUERO.ASW_AUSWEIS ON ASW_ZEITZONENR = ZTZ_ZEITZONENR" "~"
if joinKuerzel = GBTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GBT_LETZTERUSER" "~"
if joinKuerzel = BMUGBT
searchResult := searchResult "LEFT JOIN BUERO.GBT_GEBURTSTAG ON GBT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GEKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GEK_LETZTERUSER" "~"
if joinKuerzel = BMUGEK
searchResult := searchResult "LEFT JOIN WWS.GEK_GEFAHRENGUTKOPF ON GEK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GFZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GFZ_LETZTERUSER" "~"
if joinKuerzel = BMUGFZ
searchResult := searchResult "LEFT JOIN BIL.GFZ_XMLJABGESFIRZU ON GFZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GMGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GMG_LETZTERUSER" "~"
if joinKuerzel = BMUGMG
searchResult := searchResult "LEFT JOIN BIL.GMG_GRUPPENMITGLIEDER ON GMG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GOEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GOE_LETZTERUSER" "~"
if joinKuerzel = BMUGOE
searchResult := searchResult "LEFT JOIN BUERO.GOE_GRUPPENORDNEREVENTZU ON GOE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GOHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GOH_LETZTERUSER" "~"
if joinKuerzel = BMUGOH
searchResult := searchResult "LEFT JOIN ZLE.GOH_GRUPPENORDNER_HNPOS ON GOH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GWUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GWU_LETZTERUSER" "~"
if joinKuerzel = BMUGWU
searchResult := searchResult "LEFT JOIN HRS.GWU_GWUNTERGRUPPE ON GWU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GWZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GWZ_LETZTERUSER" "~"
if joinKuerzel = BMUGWZ
searchResult := searchResult "LEFT JOIN HRS.GWZ_GWBANKZU ON GWZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HBTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HBT_LETZTERUSER" "~"
if joinKuerzel = BMUHBT
searchResult := searchResult "LEFT JOIN WWS.HBT_HAUPTBESTANDTEIL ON HBT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HINBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HIN_LETZTERUSER" "~"
if joinKuerzel = BMUHIN
searchResult := searchResult "LEFT JOIN HRS.HIN_HINWEIS ON HIN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HKPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HKP_LETZTERUSER" "~"
if joinKuerzel = BMUHKP
searchResult := searchResult "LEFT JOIN BMDENTW.HKP_HILFEKAPITEL ON HKP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HLBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HLB_LETZTERUSER" "~"
if joinKuerzel = BMUHLB
searchResult := searchResult "LEFT JOIN BMDENTW.HLB_HILFEBAUSTEIN ON HLB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HNPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HNP_LETZTERUSER" "~"
if joinKuerzel = BMUHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HSABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HSA_LETZTERUSER" "~"
if joinKuerzel = BMUHSA
searchResult := searchResult "LEFT JOIN HRS.HSA_SORTIERART ON HSA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IDPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IDP_LETZTERUSER" "~"
if joinKuerzel = BMUIDP
searchResult := searchResult "LEFT JOIN PRODUKTION.IDP_INFO_DELAY_BREAKDOWN ON IDP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IMZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IMZ_LETZTERUSER" "~"
if joinKuerzel = BMUIMZ
searchResult := searchResult "LEFT JOIN BUERO.IMZ_INSTANDHMASSNZU ON IMZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = INPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = INP_LETZTERUSER" "~"
if joinKuerzel = BMUINP
searchResult := searchResult "LEFT JOIN BMD.INP_INTPARAM ON INP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = INSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = INS_LETZTERUSER" "~"
if joinKuerzel = BMUINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ITBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ITB_LETZTERUSER" "~"
if joinKuerzel = BMUITB
searchResult := searchResult "LEFT JOIN BUERO.ITB_INFOTABBEWERTUNG ON ITB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ITMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ITM_LETZTERUSER" "~"
if joinKuerzel = BMUITM
searchResult := searchResult "LEFT JOIN BUERO.ITM_INTERNMAIL ON ITM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KARBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KAR_LETZTERUSER" "~"
if joinKuerzel = BMUKAR
searchResult := searchResult "LEFT JOIN WWS.KAR_KUNDENARTIKEL ON KAR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KBABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KBA_LETZTERUSER" "~"
if joinKuerzel = BMUKBA
searchResult := searchResult "LEFT JOIN KORE.KBA_KOREBAUM ON KBA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KDIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KDI_LETZTERUSER" "~"
if joinKuerzel = BMUKDI
searchResult := searchResult "LEFT JOIN KORE.KDI_ISTWERTE ON KDI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KEEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KEE_LETZTERUSER" "~"
if joinKuerzel = BMUKEE
searchResult := searchResult "LEFT JOIN KORE.KEE_ERFASSEINGABE ON KEE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KGLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KGL_LETZTERUSER" "~"
if joinKuerzel = BMUKGL
searchResult := searchResult "LEFT JOIN KORE.KGL_KOREGLIEDERUNG ON KGL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KGVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KGV_LETZTERUSER" "~"
if joinKuerzel = BMUKGV
searchResult := searchResult "LEFT JOIN FIBU.KGV_KONTENGUVVERBUCHUNG ON KGV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KFGWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = KFG_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEKFG
searchResult := searchResult "LEFT JOIN WWS.KFG_KTOFINDBETRGRENZEN ON KFG_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = PETWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = PET_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEPET
searchResult := searchResult "LEFT JOIN WWS.PET_PREISTAB ON PET_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = PICWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = PIC_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = PIDWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = PID_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEPID
searchResult := searchResult "LEFT JOIN WWS.PID_PICPREISDETAIL ON PID_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = WADWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = WAD_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = WKLWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = WKL_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEWKL
searchResult := searchResult "LEFT JOIN WWS.WKL_WARENAUSGKOSTENLIEF ON WKL_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = ZLGWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = ZLG_WAEHRUNGSCODE_FW" "~"
if joinKuerzel = WAEZLG
searchResult := searchResult "LEFT JOIN ZLE.ZLG_ZAHLUNGEN ON ZLG_WAEHRUNGSCODE_FW = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = HNPWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = HNP_FREMDWAEHRUNGSCODE" "~"
if joinKuerzel = WAEHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_FREMDWAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = PIWWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = PIW_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = PIW_WAG_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = PIW_WAG_WARENUNTERGRNR" "~"
if joinKuerzel = WAGPIW
searchResult := searchResult "LEFT JOIN WWS.PIW_PICWARENGRUPPE ON PIW_WAG_FIRMENNR = WAG_FIRMENNR AND PIW_WAG_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND PIW_WAG_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = PRPWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = PRP_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = PRP_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = PRP_WARENUNTERGRNR" "~"
if joinKuerzel = WAGPRP
searchResult := searchResult "LEFT JOIN WWS.PRP_PROVISIONTABPOS ON PRP_WAG_FIRMENNR = WAG_FIRMENNR AND PRP_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND PRP_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = PWKWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = PWK_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = PWK_WAG_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = PWK_WAG_WARENUNTERGRNR" "~"
if joinKuerzel = WAGPWK
searchResult := searchResult "LEFT JOIN WWS.PWK_PREISRABATTWARTKOPF ON PWK_WAG_FIRMENNR = WAG_FIRMENNR AND PWK_WAG_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND PWK_WAG_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = REWWAG
searchResult := searchResult "LEFT JOIN WAG_WARENGRUPPE.WAG_WARENGRUPPE ON WAG_FIRMENNR = REW_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = REW_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = REW_WARENUNTERGRNR" "~"
if joinKuerzel = WAGREW
searchResult := searchResult "LEFT JOIN ZLE.REW_REGELWERK ON REW_WAG_FIRMENNR = WAG_FIRMENNR AND REW_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND REW_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = AAPWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_WARENEINGKOPFID = AAP_WARENEINGKOPFID" "~"
if joinKuerzel = WAKAAP
searchResult := searchResult "LEFT JOIN WWS.AAP_ARAPOSITION ON AAP_WARENEINGKOPFID = WAK_WARENEINGKOPFID" "~"
if joinKuerzel = BZBWAK
searchResult := searchResult "LEFT JOIN WAK_WARENEINGANGKOPF.WAK_WARENEINGANGKOPF ON WAK_WARENEINGKOPFID = BZB_WAK_WARENEINGKOPFID" "~"
if joinKuerzel = WAKBZB
searchResult := searchResult "LEFT JOIN PRODUKTION.BZB_BAZZEITBUCHUNG ON BZB_WAK_WARENEINGKOPFID = WAK_WARENEINGKOPFID" "~"
if joinKuerzel = LVEWAK
searchResult := searchResult "LEFT JOIN WAK_WARENEINGANGKOPF.WAK_WARENEINGANGKOPF ON WAK_WARENEINGKOPFID = LVE_WAK_WARENEINGKOPFID" "~"
if joinKuerzel = WAKLVE
searchResult := searchResult "LEFT JOIN PRODUKTION.LVE_LEISTUNGSVORERFASSUNG ON LVE_WAK_WARENEINGKOPFID = WAK_WARENEINGKOPFID" "~"
if joinKuerzel = WAKWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_WARENEINGKOPFID = WAK_STORNOWAK" "~"
if joinKuerzel = WAKWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_STORNOWAK = WAK_WARENEINGKOPFID" "~"
if joinKuerzel = WAPWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_WARENEINGKOPFID = WAP_WARENEINGKOPFID" "~"
if joinKuerzel = WAKWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_WARENEINGKOPFID = WAK_WARENEINGKOPFID" "~"
if joinKuerzel = WAZWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_WARENEINGKOPFID = WAZ_WAP_WARENEINGKOPFID AND WAP_WARENEINGPOSLFDNR = WAZ_WAP_WARENEINGPOSLFDNR" "~"
if joinKuerzel = WAPWAZ
searchResult := searchResult "LEFT JOIN WWS.WAZ_WARENZUORDNUNG ON WAZ_WAP_WARENEINGKOPFID = WAP_WARENEINGKOPFID AND WAZ_WAP_WARENEINGPOSLFDNR = WAP_WARENEINGPOSLFDNR" "~"
if joinKuerzel = WARWAR
searchResult := searchResult "LEFT JOIN WWS.WAR_WARENEINGANGRABATTE ON WAR_WARENEINGRABATTID = WAR_SET_WARID" "~"
if joinKuerzel = WARWAR
searchResult := searchResult "LEFT JOIN WWS.WAR_WARENEINGANGRABATTE ON WAR_SET_WARID = WAR_WARENEINGRABATTID" "~"
if joinKuerzel = WCZWCP
searchResult := searchResult "LEFT JOIN BMD.WCP_WAAUSDRUCKPARAMETER ON WCP_WAPARAMNR = WCZ_WAPARAMNR" "~"
if joinKuerzel = WCPWCZ
searchResult := searchResult "LEFT JOIN BMD.WCZ_WAAUSDRUCK_PARAM_ZU ON WCZ_WAPARAMNR = WCP_WAPARAMNR" "~"
if joinKuerzel = WRZWEB
searchResult := searchResult "LEFT JOIN WWS.WEB_WARENEINSATZBUCH ON WEB_WEINSATZKOPFID = WRZ_WEINSATZKOPFID AND WEB_WEINSATZBUCHLFDNR = WRZ_WEINSATZBUCHLFDNR" "~"
if joinKuerzel = WEBWRZ
searchResult := searchResult "LEFT JOIN WWS.WRZ_WESRNP_ZUORDNUNG ON WRZ_WEINSATZKOPFID = WEB_WEINSATZKOPFID AND WRZ_WEINSATZBUCHLFDNR = WEB_WEINSATZBUCHLFDNR" "~"
if joinKuerzel = LAPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAP_LETZTERUSER" "~"
if joinKuerzel = BMULAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LATBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAT_LETZTERUSER" "~"
if joinKuerzel = BMULAT
searchResult := searchResult "LEFT JOIN WWS.LAT_LIEFERANTENARTIKEL ON LAT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LEDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LED_LETZTERUSER" "~"
if joinKuerzel = BMULED
searchResult := searchResult "LEFT JOIN BUERO.LED_LEAD ON LED_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LFIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LFI_LETZTERUSER" "~"
if joinKuerzel = BMULFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LFKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LFK_LETZTERUSER" "~"
if joinKuerzel = BMULFK
searchResult := searchResult "LEFT JOIN ZLE.LFK_LEISTFIBKOREZU ON LFK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LKMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LKM_LETZTERUSER" "~"
if joinKuerzel = BMULKM
searchResult := searchResult "LEFT JOIN WWS.LKM_LAGERKONTOMONATSWERTE ON LKM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LKPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LKP_LETZTERUSER" "~"
if joinKuerzel = BMULKP
searchResult := searchResult "LEFT JOIN BMDINTERN.LKP_LIZENZKUNDENVKPZU ON LKP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LKTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LKT_LETZTERUSER" "~"
if joinKuerzel = BMULKT
searchResult := searchResult "LEFT JOIN HRS.LKT_LOHNKONTO ON LKT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LPMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LPM_LETZTERUSER" "~"
if joinKuerzel = BMULPM
searchResult := searchResult "LEFT JOIN WWS.LPM_LAGERPLATZMONATSWERTE ON LPM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LSDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LSD_LETZTERUSER" "~"
if joinKuerzel = BMULSD
searchResult := searchResult "LEFT JOIN HRS.LSD_LOHNSTAPEL_DETAIL ON LSD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LSGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LSG_LETZTERUSER" "~"
if joinKuerzel = BMULSG
searchResult := searchResult "LEFT JOIN WWS.LSG_LIEFERSCHEINGRUPPEN ON LSG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LXDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LXD_LETZTERUSER" "~"
if joinKuerzel = BMULXD
searchResult := searchResult "LEFT JOIN BUERO.LXD_LOHNEXPPROTDETAIL ON LXD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MDDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MDD_LETZTERUSER" "~"
if joinKuerzel = BMUMDD
searchResult := searchResult "LEFT JOIN BUERO.MDD_MEDIENDOKUMENTZU ON MDD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MDEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MDE_LETZTERUSER" "~"
if joinKuerzel = BMUMDE
searchResult := searchResult "LEFT JOIN WWS.MDE_MOBILEDATENERFASSUNG ON MDE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MLSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MLS_LETZTERUSER" "~"
if joinKuerzel = BMUMLS
searchResult := searchResult "LEFT JOIN ZLE.MLS_MITARBLEISTTAUSCHSATZ ON MLS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MWTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MWT_LETZTERUSER" "~"
if joinKuerzel = BMUMWT
searchResult := searchResult "LEFT JOIN BUERO.MWT_MEDIENWERTE ON MWT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NUPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NUP_LETZTERUSER" "~"
if joinKuerzel = BMUNUP
searchResult := searchResult "LEFT JOIN BMD.NUP_NTCSUPDATE ON NUP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NVSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NVS_LETZTERUSER" "~"
if joinKuerzel = BMUNVS
searchResult := searchResult "LEFT JOIN BIL.NVS_NACHVERSTEU_PAR11A ON NVS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ODTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ODT_LETZTERUSER" "~"
if joinKuerzel = BMUODT
searchResult := searchResult "LEFT JOIN BMD.ODT_ONLINEDATEITRANSFER ON ODT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OFKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OFK_LETZTERUSER" "~"
if joinKuerzel = BMUOFK
searchResult := searchResult "LEFT JOIN LOHN.OFK_OF_FKREIS ON OFK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OFMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OFM_LETZTERUSER" "~"
if joinKuerzel = BMUOFM
searchResult := searchResult "LEFT JOIN LOHN.OFM_OF_FAMILIE ON OFM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OGWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OGW_LETZTERUSER" "~"
if joinKuerzel = BMUOGW
searchResult := searchResult "LEFT JOIN LOHN.OGW_OF_GEWERKSCHAFT ON OGW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OPKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OPK_LETZTERUSER" "~"
if joinKuerzel = BMUOPK
searchResult := searchResult "LEFT JOIN LOHN.OPK_OF_PROZKO ON OPK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OVEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OVE_LETZTERUSER" "~"
if joinKuerzel = BMUOVE
searchResult := searchResult "LEFT JOIN LOHN.OVE_OF_VERANTWORTLICHER ON OVE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PAIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAI_LETZTERUSER" "~"
if joinKuerzel = BMUPAI
searchResult := searchResult "LEFT JOIN BMDINTERN.PAI_PRODUKTANFINTERESSENT ON PAI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PBSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PBS_LETZTERUSER" "~"
if joinKuerzel = BMUPBS
searchResult := searchResult "LEFT JOIN PRODUKTION.PBS_BEFEHL ON PBS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EVMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EVM_LETZTERUSER" "~"
if joinKuerzel = BMUEVM
searchResult := searchResult "LEFT JOIN HRS.EVM_ELDADETAIL_VM ON EVM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EVSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EVS_LETZTERUSER" "~"
if joinKuerzel = BMUEVS
searchResult := searchResult "LEFT JOIN BUERO.EVS_EVENTLNSTATUS ON EVS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FAD_LETZTERUSER" "~"
if joinKuerzel = BMUFAD
searchResult := searchResult "LEFT JOIN FIBU.FAD_AUSDRUCKDETAIL ON FAD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FAK_LETZTERUSER" "~"
if joinKuerzel = BMUFAK
searchResult := searchResult "LEFT JOIN HRS.FAK_FAKTORTABELLE ON FAK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FAMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FAM_LETZTERUSER" "~"
if joinKuerzel = BMUFAM
searchResult := searchResult "LEFT JOIN FIBU.FAM_AUSDRUCKMASTER ON FAM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FAWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FAW_LETZTERUSER" "~"
if joinKuerzel = BMUFAW
searchResult := searchResult "LEFT JOIN HRS.FAW_FAKTORWERTE ON FAW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FGABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FGA_LETZTERUSER" "~"
if joinKuerzel = BMUFGA
searchResult := searchResult "LEFT JOIN BUERO.FGA_FRAGEBOGENAUSWAHL ON FGA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FGTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FGT_LETZTERUSER" "~"
if joinKuerzel = BMUFGT
searchResult := searchResult "LEFT JOIN WWS.FGT_FREMDGUTSCHEINTYP ON FGT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FIRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FIR_LETZTERUSER" "~"
if joinKuerzel = BMUFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FIWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FIW_LETZTERUSER" "~"
if joinKuerzel = BMUFIW
searchResult := searchResult "LEFT JOIN BUERO.FIW_FIRMENWERTE ON FIW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FKVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FKV_LETZTERUSER" "~"
if joinKuerzel = BMUFKV
searchResult := searchResult "LEFT JOIN FIBU.FKV_KONTENVERTEILUNG ON FKV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FPOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FPO_LETZTERUSER" "~"
if joinKuerzel = BMUFPO
searchResult := searchResult "LEFT JOIN BUERO.FPO_FAVPROJEKTZU ON FPO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FRDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FRD_LETZTERUSER" "~"
if joinKuerzel = BMUFRD
searchResult := searchResult "LEFT JOIN BUERO.FRD_FRISTDOKUMENTZU ON FRD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FTBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FTB_LETZTERUSER" "~"
if joinKuerzel = BMUFTB
searchResult := searchResult "LEFT JOIN BUERO.FTB_FAHRTENBUCH ON FTB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FTGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FTG_LETZTERUSER" "~"
if joinKuerzel = BMUFTG
searchResult := searchResult "LEFT JOIN BUERO.FTG_FEIERTAG ON FTG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GEGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GEG_LETZTERUSER" "~"
if joinKuerzel = BMUGEG
searchResult := searchResult "LEFT JOIN ZLE.GEG_GEGENSTANDSWERTE ON GEG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GLTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GLT_LETZTERUSER" "~"
if joinKuerzel = BMUGLT
searchResult := searchResult "LEFT JOIN BIL.GLT_GLIEDERUNGSTEXT ON GLT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GLVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GLV_LETZTERUSER" "~"
if joinKuerzel = BMUGLV
searchResult := searchResult "LEFT JOIN BIL.GLV_GLIEDERUNGSVERWEIS ON GLV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GNDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GND_LETZTERUSER" "~"
if joinKuerzel = BMUGND
searchResult := searchResult "LEFT JOIN BIL.GND_GAU_DATA ON GND_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HRSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HRS_LETZTERUSER" "~"
if joinKuerzel = BMUHRS
searchResult := searchResult "LEFT JOIN HRS.HRS_DUMMYTABLE ON HRS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IABBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IAB_LETZTERUSER" "~"
if joinKuerzel = BMUIAB
searchResult := searchResult "LEFT JOIN BIL.IAB_INVESTITIONSABZUG ON IAB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IAPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IAP_LETZTERUSER" "~"
if joinKuerzel = BMUIAP
searchResult := searchResult "LEFT JOIN WWS.IAP_INVABWTPOS ON IAP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IEPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IEP_LETZTERUSER" "~"
if joinKuerzel = BMUIEP
searchResult := searchResult "LEFT JOIN BMD.IEP_IMPEXP_PROTOKOLL ON IEP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IFPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IFP_LETZTERUSER" "~"
if joinKuerzel = BMUIFP
searchResult := searchResult "LEFT JOIN BUERO.IFP_INFOTABPOS ON IFP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IMNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IMN_LETZTERUSER" "~"
if joinKuerzel = BMUIMN
searchResult := searchResult "LEFT JOIN BUERO.IMN_INSTANDHMASSN ON IMN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IMOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IMO_LETZTERUSER" "~"
if joinKuerzel = BMUIMO
searchResult := searchResult "LEFT JOIN BUERO.IMO_IMMOBILIE ON IMO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IMSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IMS_LETZTERUSER" "~"
if joinKuerzel = BMUIMS
searchResult := searchResult "LEFT JOIN HRS.IMS_IMPORTSCHNITTSTELLEN ON IMS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AZNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AZN_LETZTERUSER" "~"
if joinKuerzel = BMUAZN
searchResult := searchResult "LEFT JOIN BUERO.AZN_AZNACHRICHTEN ON AZN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AZRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AZR_LETZTERUSER" "~"
if joinKuerzel = BMUAZR
searchResult := searchResult "LEFT JOIN BUERO.AZR_ABRECHNUNGSZEITRAUM ON AZR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BALBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BAL_LETZTERUSER" "~"
if joinKuerzel = BMUBAL
searchResult := searchResult "LEFT JOIN PRODUKTION.BAL_ARBEITSPLATZLAGERZU ON BAL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BDZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BDZ_LETZTERUSER" "~"
if joinKuerzel = BMUBDZ
searchResult := searchResult "LEFT JOIN BUERO.BDZ_BEWERBUNGDOKUMENTZU ON BDZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BIPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BIP_LETZTERUSER" "~"
if joinKuerzel = BMUBIP
searchResult := searchResult "LEFT JOIN FIBU.BIP_BUDGETINVESTPLAN ON BIP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BLDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BLD_LETZTERUSER" "~"
if joinKuerzel = BMUBLD
searchResult := searchResult "LEFT JOIN BUERO.BLD_BUNDESLAND ON BLD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BMUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BMU_LETZTERUSER" "~"
if joinKuerzel = BMUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BOABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BOA_LETZTERUSER" "~"
if joinKuerzel = BMUBOA
searchResult := searchResult "LEFT JOIN WWS.BOA_BONUSAUSSCHLUSS ON BOA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BPZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BPZ_LETZTERUSER" "~"
if joinKuerzel = BMUBPZ
searchResult := searchResult "LEFT JOIN WWS.BPZ_BESTELLPOSZUORDNUNG ON BPZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BRGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BRG_LETZTERUSER" "~"
if joinKuerzel = BMUBRG
searchResult := searchResult "LEFT JOIN BUERO.BRG_BERUFSGRUPPEN ON BRG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BRHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BRH_LETZTERUSER" "~"
if joinKuerzel = BMUBRH
searchResult := searchResult "LEFT JOIN FIBU.BRH_BANKRVKOPF ON BRH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BRPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BRP_LETZTERUSER" "~"
if joinKuerzel = BMUBRP
searchResult := searchResult "LEFT JOIN HRS.BRP_BRUTTOERFPROTOKOLL ON BRP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BRSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BRS_LETZTERUSER" "~"
if joinKuerzel = BMUBRS
searchResult := searchResult "LEFT JOIN FIBU.BRS_BANKRUECKAUFTEILSATZ ON BRS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BRUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BRU_LETZTERUSER" "~"
if joinKuerzel = BMUBRU
searchResult := searchResult "LEFT JOIN HRS.BRU_BETRIEBSRATSUMLAGE ON BRU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BSKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BSK_LETZTERUSER" "~"
if joinKuerzel = BMUBSK
searchResult := searchResult "LEFT JOIN WWS.BSK_BONUSERMKOPF ON BSK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BSZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BSZ_LETZTERUSER" "~"
if joinKuerzel = BMUBSZ
searchResult := searchResult "LEFT JOIN BUERO.BSZ_BESPRECHUNGZU ON BSZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BTE_LETZTERUSER" "~"
if joinKuerzel = BMUBTE
searchResult := searchResult "LEFT JOIN BMDENTW.BTE_BMDTABFELDBESCHEXT ON BTE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BTW_LETZTERUSER" "~"
if joinKuerzel = BMUBTW
searchResult := searchResult "LEFT JOIN BIL.BTW_BESCHEIDWERT ON BTW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BUDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BUD_LETZTERUSER" "~"
if joinKuerzel = BMUBUD
searchResult := searchResult "LEFT JOIN FIBU.BUD_BUDGET ON BUD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BUMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BUM_LETZTERUSER" "~"
if joinKuerzel = BMUBUM
searchResult := searchResult "LEFT JOIN BIL.BUM_BILUMBUCHUNG ON BUM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BUZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BUZ_LETZTERUSER" "~"
if joinKuerzel = BMUBUZ
searchResult := searchResult "LEFT JOIN BMD.BUZ_BMDUSERGRUPPENZU ON BUZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BVLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BVL_LETZTERUSER" "~"
if joinKuerzel = BMUBVL
searchResult := searchResult "LEFT JOIN WWS.BVL_LIEFERANTENZUORDNUNG ON BVL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BWBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BWB_LETZTERUSER" "~"
if joinKuerzel = BMUBWB
searchResult := searchResult "LEFT JOIN BUERO.BWB_BEWERBUNG ON BWB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BWEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BWE_LETZTERUSER" "~"
if joinKuerzel = BMUBWE
searchResult := searchResult "LEFT JOIN FIBU.BWE_BUDGETWARENEINKPLAN ON BWE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BWTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BWT_LETZTERUSER" "~"
if joinKuerzel = BMUBWT
searchResult := searchResult "LEFT JOIN BUERO.BWT_BENUTZERWERT ON BWT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BZSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BZS_LETZTERUSER" "~"
if joinKuerzel = BMUBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KLIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KLI_LETZTERUSER" "~"
if joinKuerzel = BMUKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KOKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KOK_LETZTERUSER" "~"
if joinKuerzel = BMUKOK
searchResult := searchResult "LEFT JOIN PRODUKTION.KOK_KOMMISSIONIERUNG_KOPF ON KOK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KOZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KOZ_LETZTERUSER" "~"
if joinKuerzel = BMUKOZ
searchResult := searchResult "LEFT JOIN PRODUKTION.KOZ_KOMMISSIONIERUNG_ZU ON KOZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KPMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KPM_LETZTERUSER" "~"
if joinKuerzel = BMUKPM
searchResult := searchResult "LEFT JOIN KORE.KPM_PLANMODELL ON KPM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAD_LETZTERUSER" "~"
if joinKuerzel = BMULAD
searchResult := searchResult "LEFT JOIN WWS.LAD_LAGERARTIKELDATEN ON LAD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LAWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAW_LETZTERUSER" "~"
if joinKuerzel = BMULAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LBKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LBK_LETZTERUSER" "~"
if joinKuerzel = BMULBK
searchResult := searchResult "LEFT JOIN WWS.LBK_LAGERBUCHKENNZ ON LBK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LGBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LGB_LETZTERUSER" "~"
if joinKuerzel = BMULGB
searchResult := searchResult "LEFT JOIN HRS.LGB_LGVBERECHNUNGEN ON LGB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LHPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LHP_LETZTERUSER" "~"
if joinKuerzel = BMULHP
searchResult := searchResult "LEFT JOIN BMDINTERN.LHP_LIZENZHAUPTPAKETZU ON LHP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LPEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LPE_LETZTERUSER" "~"
if joinKuerzel = BMULPE
searchResult := searchResult "LEFT JOIN HRS.LPE_PERSON ON LPE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MAGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MAG_LETZTERUSER" "~"
if joinKuerzel = BMUMAG
searchResult := searchResult "LEFT JOIN BUERO.MAG_MITARBGESPR ON MAG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MAHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MAH_LETZTERUSER" "~"
if joinKuerzel = BMUMAH
searchResult := searchResult "LEFT JOIN FIBU.MAH_MAHNUNG ON MAH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MEIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MEI_LETZTERUSER" "~"
if joinKuerzel = BMUMEI
searchResult := searchResult "LEFT JOIN BUERO.MEI_MEDIENINFO ON MEI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MGZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MGZ_LETZTERUSER" "~"
if joinKuerzel = BMUMGZ
searchResult := searchResult "LEFT JOIN BUERO.MGZ_MERKMALSGRUPPEZU ON MGZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MISBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MIS_LETZTERUSER" "~"
if joinKuerzel = BMUMIS
searchResult := searchResult "LEFT JOIN BUERO.MIS_MITARBSTELLEZU ON MIS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MIZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MIZ_LETZTERUSER" "~"
if joinKuerzel = BMUMIZ
searchResult := searchResult "LEFT JOIN BUERO.MIZ_MEDIENINFOZU ON MIZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MLZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MLZ_LETZTERUSER" "~"
if joinKuerzel = BMUMLZ
searchResult := searchResult "LEFT JOIN BUERO.MLZ_VORSKASSELEITZAHLZU ON MLZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MOKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MOK_LETZTERUSER" "~"
if joinKuerzel = BMUMOK
searchResult := searchResult "LEFT JOIN WWS.MOK_MODELLKOPF ON MOK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MWKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MWK_LETZTERUSER" "~"
if joinKuerzel = BMUMWK
searchResult := searchResult "LEFT JOIN BIL.MWK_MWRKOPF ON MWK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NFZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NFZ_LETZTERUSER" "~"
if joinKuerzel = BMUNFZ
searchResult := searchResult "LEFT JOIN BIL.NFZ_ANBUFIBUZU ON NFZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NUIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NUI_LETZTERUSER" "~"
if joinKuerzel = BMUNUI
searchResult := searchResult "LEFT JOIN BMD.NUI_NTCSUPDINSTALLATION ON NUI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ONVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ONV_LETZTERUSER" "~"
if joinKuerzel = BMUONV
searchResult := searchResult "LEFT JOIN HRS.ONV_ONLINEBEW_VORDIENST ON ONV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OPEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OPE_LETZTERUSER" "~"
if joinKuerzel = BMUOPE
searchResult := searchResult "LEFT JOIN LOHN.OPE_OF_PERSON ON OPE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OSZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OSZ_LETZTERUSER" "~"
if joinKuerzel = BMUOSZ
searchResult := searchResult "LEFT JOIN LOHN.OSZ_OF_SWBKZ ON OSZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OTEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OTE_LETZTERUSER" "~"
if joinKuerzel = BMUOTE
searchResult := searchResult "LEFT JOIN LOHN.OTE_OF_TEXTE ON OTE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OTHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OTH_LETZTERUSER" "~"
if joinKuerzel = BMUOTH
searchResult := searchResult "LEFT JOIN BMD.OTH_OCRTEMPLATEHEADER ON OTH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BCTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BCT_LETZTERUSER" "~"
if joinKuerzel = BMUBCT
searchResult := searchResult "LEFT JOIN BUERO.BCT_BMDCOMTEMPLATES ON BCT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BEKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BEK_LETZTERUSER" "~"
if joinKuerzel = BMUBEK
searchResult := searchResult "LEFT JOIN BUERO.BEK_BERECHTKATEGORIE ON BEK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BETBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BET_LETZTERUSER" "~"
if joinKuerzel = BMUBET
searchResult := searchResult "LEFT JOIN BUERO.BET_BETRIEBSMITTEL ON BET_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BFOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BFO_LETZTERUSER" "~"
if joinKuerzel = BMUBFO
searchResult := searchResult "LEFT JOIN FIBU.BFO_BANKFIBUOPZU ON BFO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BIMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BIM_LETZTERUSER" "~"
if joinKuerzel = BMUBIM
searchResult := searchResult "LEFT JOIN FIBU.BIM_BUERFIMPORTMODELL ON BIM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BKZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BKZ_LETZTERUSER" "~"
if joinKuerzel = BMUBKZ
searchResult := searchResult "LEFT JOIN BIL.BKZ_BILANZKENNZAHL ON BKZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BLBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BLB_LETZTERUSER" "~"
if joinKuerzel = BMUBLB
searchResult := searchResult "LEFT JOIN BMD.BLB_BLOB ON BLB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BLSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BLS_LETZTERUSER" "~"
if joinKuerzel = BMUBLS
searchResult := searchResult "LEFT JOIN BMD.BLS_BLOB_STAMM ON BLS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BLZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BLZ_LETZTERUSER" "~"
if joinKuerzel = BMUBLZ
searchResult := searchResult "LEFT JOIN BMDENTW.BLZ_PACKAGESZU ON BLZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BMHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BMH_LETZTERUSER" "~"
if joinKuerzel = BMUBMH
searchResult := searchResult "LEFT JOIN BMD.BMH_BMDUSER_VALUEHISTORY ON BMH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BOPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BOP_LETZTERUSER" "~"
if joinKuerzel = BMUBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BPDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BPD_LETZTERUSER" "~"
if joinKuerzel = BMUBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BPKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BPK_LETZTERUSER" "~"
if joinKuerzel = BMUBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BPVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BPV_LETZTERUSER" "~"
if joinKuerzel = BMUBPV
searchResult := searchResult "LEFT JOIN BIL.BPV_ANBU_BERECHPARAMS ON BPV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BRBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BRB_LETZTERUSER" "~"
if joinKuerzel = BMUBRB
searchResult := searchResult "LEFT JOIN BUERO.BRB_BUEROBERECHT ON BRB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BTG_LETZTERUSER" "~"
if joinKuerzel = BMUBTG
searchResult := searchResult "LEFT JOIN BUERO.BTG_BEWERTUNGTAG ON BTG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BTH_LETZTERUSER" "~"
if joinKuerzel = BMUBTH
searchResult := searchResult "LEFT JOIN BUERO.BTH_BTERMHERSTELLER ON BTH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BTM_LETZTERUSER" "~"
if joinKuerzel = BMUBTM
searchResult := searchResult "LEFT JOIN BUERO.BTM_BTERMINAL ON BTM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BTS_LETZTERUSER" "~"
if joinKuerzel = BMUBTS
searchResult := searchResult "LEFT JOIN BUERO.BTS_BTERMSATZART ON BTS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BURBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BUR_LETZTERUSER" "~"
if joinKuerzel = BMUBUR
searchResult := searchResult "LEFT JOIN BUERO.BUR_BMDCOMUPLOADTREE ON BUR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BVGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BVG_LETZTERUSER" "~"
if joinKuerzel = BMUBVG
searchResult := searchResult "LEFT JOIN BIL.BVG_BRANCHENVERGLEICH ON BVG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BWMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BWM_LETZTERUSER" "~"
if joinKuerzel = BMUBWM
searchResult := searchResult "LEFT JOIN BUERO.BWM_BEWERTUNGMONAT ON BWM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CAIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CAI_LETZTERUSER" "~"
if joinKuerzel = BMUCAI
searchResult := searchResult "LEFT JOIN BMD.CAI_CACHEINVALIDIERUNG ON CAI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CLDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CLD_LETZTERUSER" "~"
if joinKuerzel = BMUCLD
searchResult := searchResult "LEFT JOIN BMD.CLD_CLOADACCOUNT ON CLD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CWFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CWF_LETZTERUSER" "~"
if joinKuerzel = BMUCWF
searchResult := searchResult "LEFT JOIN BUERO.CWF_CRMWORKFLOW ON CWF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DBKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DBK_LETZTERUSER" "~"
if joinKuerzel = BMUDBK
searchResult := searchResult "LEFT JOIN BMD.DBK_DRUCKBAUSTEINKOPF ON DBK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZTVZVM
searchResult := searchResult "LEFT JOIN BUERO.ZVM_ZEITPLANUNGVERSION ON ZVM_FIRMENNR = ZTV_FIRMENNR AND ZVM_DATUM_VON = ZTV_DATUM_VON AND ZVM_DATUM_BIS = ZTV_DATUM_BIS AND ZVM_PLG_FIRMENNR = ZTV_PLG_FIRMENNR AND ZVM_PLANGRLFDNR = ZTV_PLANGRLFDNR AND ZVM_ZVMVERSIONLFDNR = ZTV_ZVMVERSIONLFDNR" "~"
if joinKuerzel = ZVMZTV
searchResult := searchResult "LEFT JOIN BUERO.ZTV_ZEITPLANTEXTVERSDAT ON ZTV_FIRMENNR = ZVM_FIRMENNR AND ZTV_DATUM_VON = ZVM_DATUM_VON AND ZTV_DATUM_BIS = ZVM_DATUM_BIS AND ZTV_PLG_FIRMENNR = ZVM_PLG_FIRMENNR AND ZTV_PLANGRLFDNR = ZVM_PLANGRLFDNR AND ZTV_ZVMVERSIONLFDNR = ZVM_ZVMVERSIONLFDNR" "~"
if joinKuerzel = AUGPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_PERSONENID = AUG_PERSONENID AND PER_FIRMENNR = AUG_PER_FIRMENNR" "~"
if joinKuerzel = PERAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_PERSONENID = PER_PERSONENID AND AUG_PER_FIRMENNR = PER_FIRMENNR" "~"
if joinKuerzel = DIPPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = DIP_FIRMENNR AND PER_PERSONENID = DIP_EXTPERSID" "~"
if joinKuerzel = PERDIP
searchResult := searchResult "LEFT JOIN BUERO.DIP_DIVERSE_PERSON ON DIP_FIRMENNR = PER_FIRMENNR AND DIP_EXTPERSID = PER_PERSONENID" "~"
if joinKuerzel = AAABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AAA_LETZTERUSER" "~"
if joinKuerzel = BMUAAA
searchResult := searchResult "LEFT JOIN BUERO.AAA_AUSDRUCKABLAUFART ON AAA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AAS_LETZTERUSER" "~"
if joinKuerzel = BMUAAS
searchResult := searchResult "LEFT JOIN BUERO.AAS_ANTWORTAUSWAHLSET ON AAS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABF_LETZTERUSER" "~"
if joinKuerzel = BMUABF
searchResult := searchResult "LEFT JOIN BMD.ABF_ABFRAGE ON ABF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABK_LETZTERUSER" "~"
if joinKuerzel = BMUABK
searchResult := searchResult "LEFT JOIN BUERO.ABK_ARCHIVBAUMKOPF ON ABK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AFBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AFB_LETZTERUSER" "~"
if joinKuerzel = BMUAFB
searchResult := searchResult "LEFT JOIN FIBU.AFB_ANBUFIB ON AFB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AFPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AFP_LETZTERUSER" "~"
if joinKuerzel = BMUAFP
searchResult := searchResult "LEFT JOIN ZLE.AFP_EXTFILTERPOS ON AFP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AFZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AFZ_LETZTERUSER" "~"
if joinKuerzel = BMUAFZ
searchResult := searchResult "LEFT JOIN BMD.AFZ_ABFRAGEFELDERZU ON AFZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AGABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AGA_LETZTERUSER" "~"
if joinKuerzel = BMUAGA
searchResult := searchResult "LEFT JOIN BUERO.AGA_ABGABENART ON AGA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AGMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AGM_LETZTERUSER" "~"
if joinKuerzel = BMUAGM
searchResult := searchResult "LEFT JOIN PRODUKTION.AGM_AG_PERSON_ZU ON AGM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AGUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AGU_LETZTERUSER" "~"
if joinKuerzel = BMUAGU
searchResult := searchResult "LEFT JOIN BIL.AGU_ANLAGEGUT ON AGU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AIZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AIZ_LETZTERUSER" "~"
if joinKuerzel = BMUAIZ
searchResult := searchResult "LEFT JOIN PRODUKTION.AIZ_AGPER_INFOZU ON AIZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AKKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AKK_LETZTERUSER" "~"
if joinKuerzel = BMUAKK
searchResult := searchResult "LEFT JOIN BUERO.AKK_AKTIONSTATUSKOPF ON AKK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AKOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AKO_LETZTERUSER" "~"
if joinKuerzel = BMUAKO
searchResult := searchResult "LEFT JOIN BMD.AKO_AGGREGATKOPF ON AKO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ALEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ALE_LETZTERUSER" "~"
if joinKuerzel = BMUALE
searchResult := searchResult "LEFT JOIN BUERO.ALE_ADRESSENLISTE ON ALE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AMUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AMU_LETZTERUSER" "~"
if joinKuerzel = BMUAMU
searchResult := searchResult "LEFT JOIN BIL.AMU_ANLAGEGUT_MDE_ZU ON AMU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = APIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = API_LETZTERUSER" "~"
if joinKuerzel = BMUAPI
searchResult := searchResult "LEFT JOIN BMDINTERN.API_ASPINFO ON API_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = APKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = APK_LETZTERUSER" "~"
if joinKuerzel = BMUAPK
searchResult := searchResult "LEFT JOIN BMD.APK_AKTIONSPROTOKOLL ON APK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ARMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ARM_LETZTERUSER" "~"
if joinKuerzel = BMUARM
searchResult := searchResult "LEFT JOIN BUERO.ARM_ARBEITSZEITGESETZ_MAX ON ARM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ARZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ARZ_LETZTERUSER" "~"
if joinKuerzel = BMUARZ
searchResult := searchResult "LEFT JOIN BIL.ARZ_ANLAGEGUTGRUPPEZU ON ARZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ASZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ASZ_LETZTERUSER" "~"
if joinKuerzel = BMUASZ
searchResult := searchResult "LEFT JOIN BUERO.ASZ_AUSSCHRFILIALEZU ON ASZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AULBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AUL_LETZTERUSER" "~"
if joinKuerzel = BMUAUL
searchResult := searchResult "LEFT JOIN WWS.AUL_ARTIKELUMSTELLLISTE ON AUL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEPWEG
searchResult := searchResult "LEFT JOIN WWS.WEG_WWSERKONTROLLEGRP ON WEG_WEGID = WEP_WEGID" "~"
if joinKuerzel = WEGWEP
searchResult := searchResult "LEFT JOIN WWS.WEP_WWSERKONTROLLEPOS ON WEP_WEGID = WEG_WEGID" "~"
if joinKuerzel = LPOWEI
searchResult := searchResult "LEFT JOIN WWS.WEI_WARENEINSATZKOPF ON WEI_WEINSATZKOPFID = LPO_WEINSATZKOPFID" "~"
if joinKuerzel = WEILPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_WEINSATZKOPFID = WEI_WEINSATZKOPFID" "~"
if joinKuerzel = ALNWEK
searchResult := searchResult "LEFT JOIN PRODUKTION.WEK_WERKZEUG ON WEK_FIRMENNR = ALN_WEK_FIRMENNR AND WEK_WERKZEUGID = ALN_WERKZEUGID" "~"
if joinKuerzel = WEKALN
searchResult := searchResult "LEFT JOIN PRODUKTION.ALN_ALIBINUMMERN ON ALN_WEK_FIRMENNR = WEK_FIRMENNR AND ALN_WERKZEUGID = WEK_WERKZEUGID" "~"
if joinKuerzel = WEAWER
searchResult := searchResult "LEFT JOIN WWS.WER_WWSERKONTROLLEKOPF ON WER_ERKONTRKOPF_ID = WEA_ERKONTRKOPF_ID" "~"
if joinKuerzel = WERWEA
searchResult := searchResult "LEFT JOIN WWS.WEA_WWSERKONTROLLERABATTE ON WEA_ERKONTRKOPF_ID = WER_ERKONTRKOPF_ID" "~"
if joinKuerzel = WEGWER
searchResult := searchResult "LEFT JOIN WWS.WER_WWSERKONTROLLEKOPF ON WER_ERKONTRKOPF_ID = WEG_ERKONTRKOPF_ID" "~"
if joinKuerzel = WERWEG
searchResult := searchResult "LEFT JOIN WWS.WEG_WWSERKONTROLLEGRP ON WEG_ERKONTRKOPF_ID = WER_ERKONTRKOPF_ID" "~"
if joinKuerzel = WKLWER
searchResult := searchResult "LEFT JOIN WWS.WER_WWSERKONTROLLEKOPF ON WER_ERKONTRKOPF_ID = WKL_ERKONTRKOPF_ID" "~"
if joinKuerzel = WERWKL
searchResult := searchResult "LEFT JOIN WWS.WKL_WARENAUSGKOSTENLIEF ON WKL_ERKONTRKOPF_ID = WER_ERKONTRKOPF_ID" "~"
if joinKuerzel = WFPWFA
searchResult := searchResult "LEFT JOIN WEB.WFA_WEBFARBEN ON WFA_FIRMENNR = WFP_FIRMENNR AND WFA_WTYP = WFP_WTYP AND WFA_WFARBENNR = WFP_WFARBENNR" "~"
if joinKuerzel = WFAWFP
searchResult := searchResult "LEFT JOIN WEB.WFP_WEBFARBENPOS ON WFP_FIRMENNR = WFA_FIRMENNR AND WFP_WTYP = WFA_WTYP AND WFP_WFARBENNR = WFA_WFARBENNR" "~"
if joinKuerzel = WFEWFH
searchResult := searchResult "LEFT JOIN WWS.WFH_FERTEP_HISTORIE ON WFH_WFHID = WFE_WFHID" "~"
if joinKuerzel = WFHWFE
searchResult := searchResult "LEFT JOIN WWS.WFE_FERTEP_AUFTDETAIL ON WFE_WFHID = WFH_WFHID" "~"
if joinKuerzel = WGIWGR
searchResult := searchResult "LEFT JOIN WWS.WGR_WWSBERICHTGRUPPIERUNG ON WGR_WWSBERICHTNR = WGI_WWSBERICHTNR AND WGR_BERICHTGRUPPNR = WGI_BERICHTGRUPPNR" "~"
if joinKuerzel = WGRWGI
searchResult := searchResult "LEFT JOIN WWS.WGI_WWSBERICHTGRUPPINFO ON WGI_WWSBERICHTNR = WGR_WWSBERICHTNR AND WGI_BERICHTGRUPPNR = WGR_BERICHTGRUPPNR" "~"
if joinKuerzel = WICWIC
searchResult := searchResult "LEFT JOIN BIL.WIC_WIPCHECKLIST ON WIC_WICHECKLISTID = WIC_ALT_PRIOR_CHECKLISTID" "~"
if joinKuerzel = WICWIC
searchResult := searchResult "LEFT JOIN BIL.WIC_WIPCHECKLIST ON WIC_ALT_PRIOR_CHECKLISTID = WIC_WICHECKLISTID" "~"
if joinKuerzel = UPKWII
searchResult := searchResult "LEFT JOIN WII_WEITERE_INFO_ITEM.WII_WEITERE_INFO_ITEM ON WII_WIIID = UPK_WIIID" "~"
if joinKuerzel = WIIUPK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.UPK_UPH_KUNDE ON UPK_WIIID = WII_WIIID" "~"
if joinKuerzel = WKEWKA
searchResult := searchResult "LEFT JOIN WWS.WKA_WARENAUSGKOSTENARTEN ON WKA_FIRMENNR = WKE_WKA_FIRMENNR AND WKA_WKALFDNR = WKE_WKALFDNR" "~"
if joinKuerzel = WKAWKE
searchResult := searchResult "LEFT JOIN WWS.WKE_WARENAUSGKOSTENERF ON WKE_WKA_FIRMENNR = WKA_FIRMENNR AND WKE_WKALFDNR = WKA_WKALFDNR" "~"
if joinKuerzel = WKLWKA
searchResult := searchResult "LEFT JOIN WWS.WKA_WARENAUSGKOSTENARTEN ON WKA_FIRMENNR = WKL_WKA_FIRMENNR AND WKA_WKALFDNR = WKL_WKALFDNR" "~"
if joinKuerzel = WKAWKL
searchResult := searchResult "LEFT JOIN WWS.WKL_WARENAUSGKOSTENLIEF ON WKL_WKA_FIRMENNR = WKA_FIRMENNR AND WKL_WKALFDNR = WKA_WKALFDNR" "~"
if joinKuerzel = ERPWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = ERP_ARL_FIRMENNR AND WKT_KONTENTABNR = ERP_KONTENTABNR" "~"
if joinKuerzel = WKTERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_ARL_FIRMENNR = WKT_FIRMENNR AND ERP_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = RNGWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = RNG_HAUPTFIRMENNR AND WKT_KONTENTABNR = RNG_BRUTTOABG_KONTENTABNR" "~"
if joinKuerzel = WKTRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_HAUPTFIRMENNR = WKT_FIRMENNR AND RNG_BRUTTOABG_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = WAPWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = WAP_ARL_FIRMENNR AND WKT_KONTENTABNR = WAP_FR_KONTENTABNR" "~"
if joinKuerzel = WKTWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_ARL_FIRMENNR = WKT_FIRMENNR AND WAP_FR_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = WASWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = WAS_WKT_FIRMENNR AND WKT_KONTENTABNR = WAS_KONTENTABNR" "~"
if joinKuerzel = WKTWAS
searchResult := searchResult "LEFT JOIN WWS.WAS_WARENEINSATZARL ON WAS_WKT_FIRMENNR = WKT_FIRMENNR AND WAS_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = WQCWQI
searchResult := searchResult "LEFT JOIN WQI_WEITERE_INFO_ITEM.WQI_WEITERE_INFO_ITEM ON WQI_WIIID = WQC_WIIID" "~"
if joinKuerzel = WQIWQC
searchResult := searchResult "LEFT JOIN BMD.WQC_WQI_CUSTOMER ON WQC_WIIID = WQI_WIIID" "~"
if joinKuerzel = WSSWSS
searchResult := searchResult "LEFT JOIN WWS.WSS_WWSSTATISTIK ON WSS_WSSID = WSS_SET_WSSID" "~"
if joinKuerzel = WSSWSS
searchResult := searchResult "LEFT JOIN WWS.WSS_WWSSTATISTIK ON WSS_SET_WSSID = WSS_WSSID" "~"
if joinKuerzel = ATAWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = ATA_FIRMENNR AND WTB_WWSTEXTBNR = ATA_WWSTEXTBNR" "~"
if joinKuerzel = WTBATA
searchResult := searchResult "LEFT JOIN WWS.ATA_ARTIKELTEXTARCHIV ON ATA_FIRMENNR = WTB_FIRMENNR AND ATA_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = LSGWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = LSG_FIRMENNR AND WTB_WWSTEXTBNR = LSG_WWSTEXTBNR" "~"
if joinKuerzel = WTBLSG
searchResult := searchResult "LEFT JOIN WWS.LSG_LIEFERSCHEINGRUPPEN ON LSG_FIRMENNR = WTB_FIRMENNR AND LSG_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = RATWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = RAT_HAUPTFIRMENNR AND WTB_WWSTEXTBNR = RAT_BONUS_WWSTEXTBNR" "~"
if joinKuerzel = WTBRAT
searchResult := searchResult "LEFT JOIN WWS.RAT_RABATTTAB ON RAT_HAUPTFIRMENNR = WTB_FIRMENNR AND RAT_BONUS_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = INRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = INR_LETZTERUSER" "~"
if joinKuerzel = BMUINR
searchResult := searchResult "LEFT JOIN BUERO.INR_INSERATE ON INR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = INZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = INZ_LETZTERUSER" "~"
if joinKuerzel = BMUINZ
searchResult := searchResult "LEFT JOIN BUERO.INZ_INSOLVENZPERSONZU ON INZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = JABBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = JAB_LETZTERUSER" "~"
if joinKuerzel = BMUJAB
searchResult := searchResult "LEFT JOIN BIL.JAB_JABINFO ON JAB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KAK_LETZTERUSER" "~"
if joinKuerzel = BMUKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KBEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KBE_LETZTERUSER" "~"
if joinKuerzel = BMUKBE
searchResult := searchResult "LEFT JOIN WWS.KBE_KASSEBUCHEINZELNW ON KBE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KOWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KOW_LETZTERUSER" "~"
if joinKuerzel = BMUKOW
searchResult := searchResult "LEFT JOIN HRS.KOW_KOLLEKTIVVERTRAGWERTE ON KOW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KPABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KPA_LETZTERUSER" "~"
if joinKuerzel = BMUKPA
searchResult := searchResult "LEFT JOIN WWS.KPA_KOSTENPERIODENAUFTLG ON KPA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KPKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KPK_LETZTERUSER" "~"
if joinKuerzel = BMUKPK
searchResult := searchResult "LEFT JOIN KORE.KPK_PLANKENNZAHLEN ON KPK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KSKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KSK_LETZTERUSER" "~"
if joinKuerzel = BMUKSK
searchResult := searchResult "LEFT JOIN KORE.KSK_KORESCHLUESSELKOPF ON KSK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KSZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KSZ_LETZTERUSER" "~"
if joinKuerzel = BMUKSZ
searchResult := searchResult "LEFT JOIN HRS.KSZ_BBKALKSZ ON KSZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KUGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KUG_LETZTERUSER" "~"
if joinKuerzel = BMUKUG
searchResult := searchResult "LEFT JOIN BUERO.KUG_KUNDENGRUPPE ON KUG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KVSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KVS_LETZTERUSER" "~"
if joinKuerzel = BMUKVS
searchResult := searchResult "LEFT JOIN HRS.KVS_KSERVICEZU ON KVS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KVTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KVT_LETZTERUSER" "~"
if joinKuerzel = BMUKVT
searchResult := searchResult "LEFT JOIN HRS.KVT_KVTAB ON KVT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KZPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KZP_LETZTERUSER" "~"
if joinKuerzel = BMUKZP
searchResult := searchResult "LEFT JOIN FIBU.KZP_ZAHLUNGSPLAN ON KZP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KZUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KZU_LETZTERUSER" "~"
if joinKuerzel = BMUKZU
searchResult := searchResult "LEFT JOIN KORE.KZU_KOREKOREZU ON KZU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LARBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAR_LETZTERUSER" "~"
if joinKuerzel = BMULAR
searchResult := searchResult "LEFT JOIN HRS.LAR_LOHNARTENREGEL ON LAR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LAYBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAY_LETZTERUSER" "~"
if joinKuerzel = BMULAY
searchResult := searchResult "LEFT JOIN BMD.LAY_LAYOUT ON LAY_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LDDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LDD_LETZTERUSER" "~"
if joinKuerzel = BMULDD
searchResult := searchResult "LEFT JOIN BMD.LDD_LIZDATEIDOWNLOAD ON LDD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LINBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LIN_LETZTERUSER" "~"
if joinKuerzel = BMULIN
searchResult := searchResult "LEFT JOIN PRODUKTION.LIN_PROD_LINIE ON LIN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LIZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LIZ_LETZTERUSER" "~"
if joinKuerzel = BMULIZ
searchResult := searchResult "LEFT JOIN BMD.LIZ_LIZENZ ON LIZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LSEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LSE_LETZTERUSER" "~"
if joinKuerzel = BMULSE
searchResult := searchResult "LEFT JOIN HRS.LSE_LOHNSCHABLONE ON LSE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LUABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LUA_LETZTERUSER" "~"
if joinKuerzel = BMULUA
searchResult := searchResult "LEFT JOIN PRODUKTION.LUA_LAGER_UMS_ANGEFORDERT ON LUA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MDABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MDA_LETZTERUSER" "~"
if joinKuerzel = BMUMDA
searchResult := searchResult "LEFT JOIN WWS.MDA_MDE_ARTEN ON MDA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MHTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MHT_LETZTERUSER" "~"
if joinKuerzel = BMUMHT
searchResult := searchResult "LEFT JOIN BUERO.MHT_MITARBHISTORIE ON MHT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MIVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MIV_LETZTERUSER" "~"
if joinKuerzel = BMUMIV
searchResult := searchResult "LEFT JOIN HRS.MIV_MITARBVORSCHLAG ON MIV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MLXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MLX_LETZTERUSER" "~"
if joinKuerzel = BMUMLX
searchResult := searchResult "LEFT JOIN BMDENTW.MLX_MEMLEAKS ON MLX_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MMZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MMZ_LETZTERUSER" "~"
if joinKuerzel = BMUMMZ
searchResult := searchResult "LEFT JOIN BUERO.MMZ_MAGBETRIEBSMITTELZU ON MMZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PFIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PFI_LETZTERUSER" "~"
if joinKuerzel = BMUPFI
searchResult := searchResult "LEFT JOIN BMD.PFI_PANELFILTER ON PFI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PFZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PFZ_LETZTERUSER" "~"
if joinKuerzel = BMUPFZ
searchResult := searchResult "LEFT JOIN BUERO.PFZ_PERSONFIRMENZU ON PFZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PGIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PGI_LETZTERUSER" "~"
if joinKuerzel = BMUPGI
searchResult := searchResult "LEFT JOIN WWS.PGI_PICARTIKELGRPITEM ON PGI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PGSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PGS_LETZTERUSER" "~"
if joinKuerzel = BMUPGS
searchResult := searchResult "LEFT JOIN BIL.PGS_PERSKORST_GUTSTUNDEN ON PGS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PID_LETZTERUSER" "~"
if joinKuerzel = BMUPID
searchResult := searchResult "LEFT JOIN WWS.PID_PICPREISDETAIL ON PID_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIX_LETZTERUSER" "~"
if joinKuerzel = BMUPIX
searchResult := searchResult "LEFT JOIN BIL.PIX_PERSRSTIMP_FELDSPERRE ON PIX_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKO_LETZTERUSER" "~"
if joinKuerzel = BMUPKO
searchResult := searchResult "LEFT JOIN BMD.PKO_PROTOKOLL ON PKO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKW_LETZTERUSER" "~"
if joinKuerzel = BMUPKW
searchResult := searchResult "LEFT JOIN BIL.PKW_PERSKORST_PARAMWERT ON PKW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PLDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PLD_LETZTERUSER" "~"
if joinKuerzel = BMUPLD
searchResult := searchResult "LEFT JOIN WWS.PLD_PICALLERGENDATEN ON PLD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PMPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PMP_LETZTERUSER" "~"
if joinKuerzel = BMUPMP
searchResult := searchResult "LEFT JOIN HRS.PMP_PERSSTAMMPROTOKOLL ON PMP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PMXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PMX_LETZTERUSER" "~"
if joinKuerzel = BMUPMX
searchResult := searchResult "LEFT JOIN KORE.PMX_PLANMENGENEXT ON PMX_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PPFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PPF_LETZTERUSER" "~"
if joinKuerzel = BMUPPF
searchResult := searchResult "LEFT JOIN BUERO.PPF_PERSONENPRUEFUNG ON PPF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PRKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PRK_LETZTERUSER" "~"
if joinKuerzel = BMUPRK
searchResult := searchResult "LEFT JOIN BUERO.PRK_PROJEKTKREIS ON PRK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PSKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PSK_LETZTERUSER" "~"
if joinKuerzel = BMUPSK
searchResult := searchResult "LEFT JOIN BIL.PSK_PERSRST_STERBET_KOPF ON PSK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PSMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PSM_LETZTERUSER" "~"
if joinKuerzel = BMUPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PSWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PSW_LETZTERUSER" "~"
if joinKuerzel = BMUPSW
searchResult := searchResult "LEFT JOIN BUERO.PSW_PERSONENWERTE ON PSW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PUIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PUI_LETZTERUSER" "~"
if joinKuerzel = BMUPUI
searchResult := searchResult "LEFT JOIN WWS.PUI_PICSTKL ON PUI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PVGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PVG_LETZTERUSER" "~"
if joinKuerzel = BMUPVG
searchResult := searchResult "LEFT JOIN BUERO.PVG_PERSONVORERFASSANGEH ON PVG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PVWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PVW_LETZTERUSER" "~"
if joinKuerzel = BMUPVW
searchResult := searchResult "LEFT JOIN BUERO.PVW_PERSONVORERFASSWOCHSTD ON PVW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PVYBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PVY_LETZTERUSER" "~"
if joinKuerzel = BMUPVY
searchResult := searchResult "LEFT JOIN BMD.PVY_PACKAGEVISIBILITY ON PVY_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PWPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PWP_LETZTERUSER" "~"
if joinKuerzel = BMUPWP
searchResult := searchResult "LEFT JOIN WWS.PWP_PREISWARTUNGPOS ON PWP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = QAWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = QAW_LETZTERUSER" "~"
if joinKuerzel = BMUQAW
searchResult := searchResult "LEFT JOIN BUERO.QAW_QUOTENAUSWERTUNG ON QAW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = QLFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = QLF_LETZTERUSER" "~"
if joinKuerzel = BMUQLF
searchResult := searchResult "LEFT JOIN BMD.QLF_LINKFIELD ON QLF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = QMABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = QMA_LETZTERUSER" "~"
if joinKuerzel = BMUQMA
searchResult := searchResult "LEFT JOIN PRODUKTION.QMA_QM_ARTIKEL ON QMA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = QUFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = QUF_LETZTERUSER" "~"
if joinKuerzel = BMUQUF
searchResult := searchResult "LEFT JOIN BUERO.QUF_QUOTENFRIST ON QUF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = QUOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = QUO_LETZTERUSER" "~"
if joinKuerzel = BMUQUO
searchResult := searchResult "LEFT JOIN BUERO.QUO_QUOTE ON QUO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CAD_LETZTERUSER" "~"
if joinKuerzel = BMUCAD
searchResult := searchResult "LEFT JOIN BMDDEFAULT.CAD_CACHEDEFINITION ON CAD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CPZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CPZ_LETZTERUSER" "~"
if joinKuerzel = BMUCPZ
searchResult := searchResult "LEFT JOIN BMDDEFAULT.CPZ_CMDLINEPARAMZU ON CPZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CSSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CSS_LETZTERUSER" "~"
if joinKuerzel = BMUCSS
searchResult := searchResult "LEFT JOIN BMDDEFAULT.CSS_CASCSTYLESHEET ON CSS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DAS_LETZTERUSER" "~"
if joinKuerzel = BMUDAS
searchResult := searchResult "LEFT JOIN BUERO.DAS_DOCSARCHIVSTAMM ON DAS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DBGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DBG_LETZTERUSER" "~"
if joinKuerzel = BMUDBG
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DBG_DBMDGRUPPE ON DBG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DBIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DBI_LETZTERUSER" "~"
if joinKuerzel = BMUDBI
searchResult := searchResult "LEFT JOIN BMD.DBI_DBCLONEITEM ON DBI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DBZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DBZ_LETZTERUSER" "~"
if joinKuerzel = BMUDBZ
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DBZ_DBMDUSERGRUPPENZU ON DBZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DIABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DIA_LETZTERUSER" "~"
if joinKuerzel = BMUDIA
searchResult := searchResult "LEFT JOIN BMD.DIA_DIAGRAMM ON DIA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DLFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DLF_LETZTERUSER" "~"
if joinKuerzel = BMUDLF
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DLF_LINKFIELD ON DLF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DLKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DLK_LETZTERUSER" "~"
if joinKuerzel = BMUDLK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DLK_LINK ON DLK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DLNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DLN_LETZTERUSER" "~"
if joinKuerzel = BMUDLN
searchResult := searchResult "LEFT JOIN BUERO.DLN_DIENSTLEISTUNGSNUMMER ON DLN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DNZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DNZ_LETZTERUSER" "~"
if joinKuerzel = BMUDNZ
searchResult := searchResult "LEFT JOIN HRS.DNZ_DIVNICHTLEISTUNGSZEIT ON DNZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DOFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DOF_LETZTERUSER" "~"
if joinKuerzel = BMUDOF
searchResult := searchResult "LEFT JOIN FIBU.DOF_FIBUDOKUMENT ON DOF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DPDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DPD_LETZTERUSER" "~"
if joinKuerzel = BMUDPD
searchResult := searchResult "LEFT JOIN BMD.DPD_DOCUMENTPOOLDETAIL ON DPD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DPFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DPF_LETZTERUSER" "~"
if joinKuerzel = BMUDPF
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DPF_DFUNKTIONSPARAMZU ON DPF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DPZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DPZ_LETZTERUSER" "~"
if joinKuerzel = BMUDPZ
searchResult := searchResult "LEFT JOIN BUERO.DPZ_DOCSPERSZU ON DPZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DRSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DRS_LETZTERUSER" "~"
if joinKuerzel = BMUDRS
searchResult := searchResult "LEFT JOIN BMD.DRS_DRUCKEREINSTELLUNG ON DRS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DVEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DVE_LETZTERUSER" "~"
if joinKuerzel = BMUDVE
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DVE_VERSION ON DVE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DVPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DVP_LETZTERUSER" "~"
if joinKuerzel = BMUDVP
searchResult := searchResult "LEFT JOIN BMD.DVP_DEVICEPROTOCOL ON DVP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DWABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DWA_LETZTERUSER" "~"
if joinKuerzel = BMUDWA
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DWA_WEBBROWSERADDON ON DWA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EAD_LETZTERUSER" "~"
if joinKuerzel = BMUEAD
searchResult := searchResult "LEFT JOIN HRS.EAD_ELDADETAIL_AD ON EAD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EAFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EAF_LETZTERUSER" "~"
if joinKuerzel = BMUEAF
searchResult := searchResult "LEFT JOIN HRS.EAF_ELDADETAIL_AFB ON EAF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EB2BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EB2_LETZTERUSER" "~"
if joinKuerzel = BMUEB2
searchResult := searchResult "LEFT JOIN HRS.EB2_ELDADETAIL_BN_BEGR ON EB2_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EBKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EBK_LETZTERUSER" "~"
if joinKuerzel = BMUEBK
searchResult := searchResult "LEFT JOIN FIBU.EBK_EBKORREKTURKONTEN ON EBK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EDKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EDK_LETZTERUSER" "~"
if joinKuerzel = BMUEDK
searchResult := searchResult "LEFT JOIN HRS.EDK_ERWDIVKOERPER ON EDK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ELZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ELZ_LETZTERUSER" "~"
if joinKuerzel = BMUELZ
searchResult := searchResult "LEFT JOIN HRS.ELZ_ELDADETAIL_LZM ON ELZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OTIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OTI_LETZTERUSER" "~"
if joinKuerzel = BMUOTI
searchResult := searchResult "LEFT JOIN LOHN.OTI_OF_TITEL ON OTI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OTRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OTR_LETZTERUSER" "~"
if joinKuerzel = BMUOTR
searchResult := searchResult "LEFT JOIN LOHN.OTR_OF_TRANSFER ON OTR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OUTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OUT_LETZTERUSER" "~"
if joinKuerzel = BMUOUT
searchResult := searchResult "LEFT JOIN LOHN.OUT_OF_ULTIMO ON OUT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OUWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OUW_LETZTERUSER" "~"
if joinKuerzel = BMUOUW
searchResult := searchResult "LEFT JOIN BMD.OUW_OUTLOOKBARWINDOW ON OUW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PAGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAG_LETZTERUSER" "~"
if joinKuerzel = BMUPAG
searchResult := searchResult "LEFT JOIN BMDDEFAULT.PAG_PARAMGRUPPE ON PAG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PDGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PDG_LETZTERUSER" "~"
if joinKuerzel = BMUPDG
searchResult := searchResult "LEFT JOIN WWS.PDG_PICDYNAMICGROUP ON PDG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PGABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PGA_LETZTERUSER" "~"
if joinKuerzel = BMUPGA
searchResult := searchResult "LEFT JOIN WWS.PGA_PICARTIKELGRPZU ON PGA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PGKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PGK_LETZTERUSER" "~"
if joinKuerzel = BMUPGK
searchResult := searchResult "LEFT JOIN BUERO.PGK_PERSONGKKZU ON PGK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PGLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PGL_LETZTERUSER" "~"
if joinKuerzel = BMUPGL
searchResult := searchResult "LEFT JOIN HRS.PGL_PERKZGLIEDERUNG ON PGL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIK_LETZTERUSER" "~"
if joinKuerzel = BMUPIK
searchResult := searchResult "LEFT JOIN WWS.PIK_PICKATEGORIEZU ON PIK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIP_LETZTERUSER" "~"
if joinKuerzel = BMUPIP
searchResult := searchResult "LEFT JOIN BUERO.PIP_PERSINFOPROTOKOLL ON PIP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIR_LETZTERUSER" "~"
if joinKuerzel = BMUPIR
searchResult := searchResult "LEFT JOIN WWS.PIR_PICRABATTDETAIL ON PIR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKH_LETZTERUSER" "~"
if joinKuerzel = BMUPKH
searchResult := searchResult "LEFT JOIN BIL.PKH_PERSKORSTHEADER ON PKH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKK_LETZTERUSER" "~"
if joinKuerzel = BMUPKK
searchResult := searchResult "LEFT JOIN BIL.PKK_PERSKORST_PARAMKOPF ON PKK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKR_LETZTERUSER" "~"
if joinKuerzel = BMUPKR
searchResult := searchResult "LEFT JOIN BIL.PKR_PERSKOSTEN_RST ON PKR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PLOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PLO_LETZTERUSER" "~"
if joinKuerzel = BMUPLO
searchResult := searchResult "LEFT JOIN PRODUKTION.PLO_PPS_LOS ON PLO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = POABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = POA_LETZTERUSER" "~"
if joinKuerzel = BMUPOA
searchResult := searchResult "LEFT JOIN BUERO.POA_POSTART ON POA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PRNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PRN_LETZTERUSER" "~"
if joinKuerzel = BMUPRN
searchResult := searchResult "LEFT JOIN WWS.PRN_EXTPIAKEYNAMES ON PRN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PRPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PRP_LETZTERUSER" "~"
if joinKuerzel = BMUPRP
searchResult := searchResult "LEFT JOIN WWS.PRP_PROVISIONTABPOS ON PRP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PSGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PSG_LETZTERUSER" "~"
if joinKuerzel = BMUPSG
searchResult := searchResult "LEFT JOIN WWS.PSG_PREISGRUPPE ON PSG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PTPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PTP_LETZTERUSER" "~"
if joinKuerzel = BMUPTP
searchResult := searchResult "LEFT JOIN WWS.PTP_PREISTABPOS ON PTP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PUSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PUS_LETZTERUSER" "~"
if joinKuerzel = BMUPUS
searchResult := searchResult "LEFT JOIN PRODUKTION.PUS_PPSUSERSTATI ON PUS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PVABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PVA_LETZTERUSER" "~"
if joinKuerzel = BMUPVA
searchResult := searchResult "LEFT JOIN BUERO.PVA_PERSONVORERFASSAKTION ON PVA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PWFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PWF_LETZTERUSER" "~"
if joinKuerzel = BMUPWF
searchResult := searchResult "LEFT JOIN WWS.PWF_PWKFIRMENZU ON PWF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PWKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PWK_LETZTERUSER" "~"
if joinKuerzel = BMUPWK
searchResult := searchResult "LEFT JOIN WWS.PWK_PREISRABATTWARTKOPF ON PWK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PWOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PWO_LETZTERUSER" "~"
if joinKuerzel = BMUPWO
searchResult := searchResult "LEFT JOIN BUERO.PWO_PASSWORT ON PWO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AVKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AVK_LETZTERUSER" "~"
if joinKuerzel = BMUAVK
searchResult := searchResult "LEFT JOIN BUERO.AVK_AVKONTINGENTE ON AVK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AWDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AWD_LETZTERUSER" "~"
if joinKuerzel = BMUAWD
searchResult := searchResult "LEFT JOIN BUERO.AWD_ANTWORTDETAIL ON AWD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AZVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AZV_LETZTERUSER" "~"
if joinKuerzel = BMUAZV
searchResult := searchResult "LEFT JOIN BUERO.AZV_ARBEITSZEITVERSION ON AZV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BAK_LETZTERUSER" "~"
if joinKuerzel = BMUBAK
searchResult := searchResult "LEFT JOIN BUERO.BAK_BANK ON BAK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BCIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BCI_LETZTERUSER" "~"
if joinKuerzel = BMUBCI
searchResult := searchResult "LEFT JOIN BMDENTW.BCI_BMDCONSTINTERN ON BCI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BEGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BEG_LETZTERUSER" "~"
if joinKuerzel = BMUBEG
searchResult := searchResult "LEFT JOIN BMD.BEG_BERECHTIGUNG ON BEG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BFIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BFI_LETZTERUSER" "~"
if joinKuerzel = BMUBFI
searchResult := searchResult "LEFT JOIN HRS.BFI_BUDGETFIRMA ON BFI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BFZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BFZ_LETZTERUSER" "~"
if joinKuerzel = BMUBFZ
searchResult := searchResult "LEFT JOIN BIL.BFZ_XMLJABBETFIRZU ON BFZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BGDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BGD_LETZTERUSER" "~"
if joinKuerzel = BMUBGD
searchResult := searchResult "LEFT JOIN HRS.BGD_BUDGETGLDNGBAUMDATA ON BGD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BINBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BIN_LETZTERUSER" "~"
if joinKuerzel = BMUBIN
searchResult := searchResult "LEFT JOIN BUERO.BIN_BEWERBERINSERATZU ON BIN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BIZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BIZ_LETZTERUSER" "~"
if joinKuerzel = BMUBIZ
searchResult := searchResult "LEFT JOIN FIBU.BIZ_BUDGETINVESTZAHLUNG ON BIZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BKTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BKT_LETZTERUSER" "~"
if joinKuerzel = BMUBKT
searchResult := searchResult "LEFT JOIN BUERO.BKT_AUSBILDUNGSKOSTEN_TMP ON BKT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BKUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BKU_LETZTERUSER" "~"
if joinKuerzel = BMUBKU
searchResult := searchResult "LEFT JOIN BUERO.BKU_BEFRAGUNGKRITERIUMZU ON BKU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BMABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BMA_LETZTERUSER" "~"
if joinKuerzel = BMUBMA
searchResult := searchResult "LEFT JOIN BMD.BMA_BMDMAILACCOUNT ON BMA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BMMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BMM_LETZTERUSER" "~"
if joinKuerzel = BMUBMM
searchResult := searchResult "LEFT JOIN HRS.BMM_BUAK_MONMELDUNG ON BMM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BMOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BMO_LETZTERUSER" "~"
if joinKuerzel = BMUBMO
searchResult := searchResult "LEFT JOIN FIBU.BMO_BUDGETMODELL ON BMO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BRVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BRV_LETZTERUSER" "~"
if joinKuerzel = BMUBRV
searchResult := searchResult "LEFT JOIN FIBU.BRV_BANKRUECKVERBUCHUNG ON BRV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BSGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BSG_LETZTERUSER" "~"
if joinKuerzel = BMUBSG
searchResult := searchResult "LEFT JOIN BUERO.BSG_BESPRECHUNG ON BSG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BSNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BSN_LETZTERUSER" "~"
if joinKuerzel = BMUBSN
searchResult := searchResult "LEFT JOIN FIBU.BSN_BILANZSTEUERKONTEN ON BSN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BUVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BUV_LETZTERUSER" "~"
if joinKuerzel = BMUBUV
searchResult := searchResult "LEFT JOIN FIBU.BUV_BUCHUNGSVARIANTE ON BUV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BZUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BZU_LETZTERUSER" "~"
if joinKuerzel = BMUBZU
searchResult := searchResult "LEFT JOIN FIBU.BZU_BUARTKONSZU ON BZU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BZVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BZV_LETZTERUSER" "~"
if joinKuerzel = BMUBZV
searchResult := searchResult "LEFT JOIN HRS.BZV_BUAK_ZUSCHLAGSVERRECHN ON BZV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CHVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CHV_LETZTERUSER" "~"
if joinKuerzel = BMUCHV
searchResult := searchResult "LEFT JOIN PRODUKTION.CHV_CHARGENVERFOLGUNG ON CHV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CMZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CMZ_LETZTERUSER" "~"
if joinKuerzel = BMUCMZ
searchResult := searchResult "LEFT JOIN BMD.CMZ_CMDZEILEN ON CMZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DACBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DAC_LETZTERUSER" "~"
if joinKuerzel = BMUDAC
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DAC_DATTRIBUTECLONE ON DAC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DAGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DAG_LETZTERUSER" "~"
if joinKuerzel = BMUDAG
searchResult := searchResult "LEFT JOIN BUERO.DAG_DOKANLAGEGUTZU ON DAG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MSTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MST_LETZTERUSER" "~"
if joinKuerzel = BMUMST
searchResult := searchResult "LEFT JOIN BUERO.MST_MITARBSTELLVERTRETER ON MST_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MVABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MVA_LETZTERUSER" "~"
if joinKuerzel = BMUMVA
searchResult := searchResult "LEFT JOIN FIBU.MVA_MAHNVZINSENAB ON MVA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MWRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MWR_LETZTERUSER" "~"
if joinKuerzel = BMUMWR
searchResult := searchResult "LEFT JOIN BIL.MWR_MWRDATEN ON MWR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NTCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NTC_LETZTERUSER" "~"
if joinKuerzel = BMUNTC
searchResult := searchResult "LEFT JOIN BMDENTW.NTC_NTCSARTIKEL ON NTC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OAZ_LETZTERUSER" "~"
if joinKuerzel = BMUOAZ
searchResult := searchResult "LEFT JOIN LOHN.OAZ_OF_ARBEITSZEITMODELL ON OAZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OBABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OBA_LETZTERUSER" "~"
if joinKuerzel = BMUOBA
searchResult := searchResult "LEFT JOIN BUERO.OBA_ONLBEWAUSSCHREIBUNGZU ON OBA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OBEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OBE_LETZTERUSER" "~"
if joinKuerzel = BMUOBE
searchResult := searchResult "LEFT JOIN LOHN.OBE_OF_BERUF ON OBE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OFIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OFI_LETZTERUSER" "~"
if joinKuerzel = BMUOFI
searchResult := searchResult "LEFT JOIN LOHN.OFI_OF_FIXWERTE ON OFI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OKSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OKS_LETZTERUSER" "~"
if joinKuerzel = BMUOKS
searchResult := searchResult "LEFT JOIN LOHN.OKS_OF_KOSTENSTELLE ON OKS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OUZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OUZ_LETZTERUSER" "~"
if joinKuerzel = BMUOUZ
searchResult := searchResult "LEFT JOIN BUERO.OUZ_ONLINEKONFUMFRAGEZU ON OUZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAD_LETZTERUSER" "~"
if joinKuerzel = BMUPAD
searchResult := searchResult "LEFT JOIN BMDDEFAULT.PAD_PARAMETERDEF ON PAD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PBABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PBA_LETZTERUSER" "~"
if joinKuerzel = BMUPBA
searchResult := searchResult "LEFT JOIN PRODUKTION.PBA_PLAN_BAS_ALT ON PBA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PBDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PBD_LETZTERUSER" "~"
if joinKuerzel = BMUPBD
searchResult := searchResult "LEFT JOIN BIL.PBD_PERSKORST_BERECHDET ON PBD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PCWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PCW_LETZTERUSER" "~"
if joinKuerzel = BMUPCW
searchResult := searchResult "LEFT JOIN PRODUKTION.PCW_PPSKAPAWERT ON PCW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PCXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PCX_LETZTERUSER" "~"
if joinKuerzel = BMUPCX
searchResult := searchResult "LEFT JOIN PRODUKTION.PCX_PPS_KAPA_EXT ON PCX_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PFABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PFA_LETZTERUSER" "~"
if joinKuerzel = BMUPFA
searchResult := searchResult "LEFT JOIN BUERO.PFA_PERSONFINANZAMTZU ON PFA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PFLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PFL_LETZTERUSER" "~"
if joinKuerzel = BMUPFL
searchResult := searchResult "LEFT JOIN BMDINTERN.PFL_PFLICHTENHEFT ON PFL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PFRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PFR_LETZTERUSER" "~"
if joinKuerzel = BMUPFR
searchResult := searchResult "LEFT JOIN WWS.PFR_PICFARBE ON PFR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PGEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PGE_LETZTERUSER" "~"
if joinKuerzel = BMUPGE
searchResult := searchResult "LEFT JOIN BUERO.PGE_PERSONGESELLZU ON PGE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PGOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PGO_LETZTERUSER" "~"
if joinKuerzel = BMUPGO
searchResult := searchResult "LEFT JOIN WWS.PGO_PICGROESSE ON PGO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PGRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PGR_LETZTERUSER" "~"
if joinKuerzel = BMUPGR
searchResult := searchResult "LEFT JOIN BUERO.PGR_PERSONGERICHTZU ON PGR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PICBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIC_LETZTERUSER" "~"
if joinKuerzel = BMUPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PJTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PJT_LETZTERUSER" "~"
if joinKuerzel = BMUPJT
searchResult := searchResult "LEFT JOIN HRS.PJT_PERSSTAMM_TMP ON PJT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKX_LETZTERUSER" "~"
if joinKuerzel = BMUPKX
searchResult := searchResult "LEFT JOIN FIBU.PKX_BUDGETPERSKOSTPARAMS ON PKX_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PMABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PMA_LETZTERUSER" "~"
if joinKuerzel = BMUPMA
searchResult := searchResult "LEFT JOIN BUERO.PMA_PROJEKTMITARBZU ON PMA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PUPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PUP_LETZTERUSER" "~"
if joinKuerzel = BMUPUP
searchResult := searchResult "LEFT JOIN BUERO.PUP_PERSONENUPDATE ON PUP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WADWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = WAD_HAUPTFIRMENNR AND WTB_WWSTEXTBNR = WAD_KOPFTEXT_WWSTEXTBNR" "~"
if joinKuerzel = WTBWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_HAUPTFIRMENNR = WTB_FIRMENNR AND WAD_KOPFTEXT_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = KODWTB
searchResult := searchResult "LEFT JOIN WTB_WWSTEXTBAUSTEIN.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = KOD_RECH_WTB_FIRMENNR AND WTB_WWSTEXTBNR = KOD_RECH_WWSTEXTBNR" "~"
if joinKuerzel = WTBKOD
searchResult := searchResult "LEFT JOIN BUERO.KOD_KONDITIONEN ON KOD_RECH_WTB_FIRMENNR = WTB_FIRMENNR AND KOD_RECH_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = LMSWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = LMS_WWSTEXTBNR_FIRMENNR AND WTB_WWSTEXTBNR = LMS_WWSTEXTBNR_SCH_STUFE1" "~"
if joinKuerzel = WTBLMS
searchResult := searchResult "LEFT JOIN WWS.LMS_LIEFERMAHNUNG ON LMS_WWSTEXTBNR_FIRMENNR = WTB_FIRMENNR AND LMS_WWSTEXTBNR_SCH_STUFE1 = WTB_WWSTEXTBNR" "~"
if joinKuerzel = WADWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = WAD_HAUPTFIRMENNR AND WTB_WWSTEXTBNR = WAD_KOPF_LFS_WWSTEXTBNR" "~"
if joinKuerzel = WTBWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_HAUPTFIRMENNR = WTB_FIRMENNR AND WAD_KOPF_LFS_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = AKZWVT
searchResult := searchResult "LEFT JOIN WWS.WVT_WARTUNGSVERTRAG ON WVT_WVLFDNR = AKZ_WVLFDNR" "~"
if joinKuerzel = WVTAKZ
searchResult := searchResult "LEFT JOIN WWS.AKZ_ARTIKELKUNDENZU ON AKZ_WVLFDNR = WVT_WVLFDNR" "~"
if joinKuerzel = KATZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_FIRMENNR = KAT_ZBA_FIRMENNR AND ZBA_ZBUCHARTNR = KAT_ZBUCHARTNR" "~"
if joinKuerzel = ZBAKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_ZBA_FIRMENNR = ZBA_FIRMENNR AND KAT_ZBUCHARTNR = ZBA_ZBUCHARTNR" "~"
if joinKuerzel = ZBAZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_FIRMENNR = ZBA_BESTAET_FIRMENNR AND ZBA_ZBUCHARTNR = ZBA_BESTAET_ZBUCHARTNR" "~"
if joinKuerzel = ZBAZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_BESTAET_FIRMENNR = ZBA_FIRMENNR AND ZBA_BESTAET_ZBUCHARTNR = ZBA_ZBUCHARTNR" "~"
if joinKuerzel = ZBBZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_FIRMENNR = ZBB_ZBA_FIRMENNR AND ZBA_ZBUCHARTNR = ZBB_ZBA_ZBUCHARTNR" "~"
if joinKuerzel = ZBAZBB
searchResult := searchResult "LEFT JOIN BUERO.ZBB_ZEITBETRKALBUCHART ON ZBB_ZBA_FIRMENNR = ZBA_FIRMENNR AND ZBB_ZBA_ZBUCHARTNR = ZBA_ZBUCHARTNR" "~"
if joinKuerzel = FRAZBE
searchResult := searchResult "LEFT JOIN BUERO.ZBE_ZUSTAENDIGKEITBEREICH ON ZBE_ZUSTBEREICHLFDNR = FRA_ZUSTBEREICHLFDNR" "~"
if joinKuerzel = ZBEFRA
searchResult := searchResult "LEFT JOIN BUERO.FRA_FRISTART ON FRA_ZUSTBEREICHLFDNR = ZBE_ZUSTBEREICHLFDNR" "~"
if joinKuerzel = PRSZBE
searchResult := searchResult "LEFT JOIN ZBE_ZUSTAENDIGKEITBEREICH.ZBE_ZUSTAENDIGKEITBEREICH ON ZBE_ZUSTBEREICHLFDNR = PRS_ZUS_ZUSTAENDIGKEITNR" "~"
if joinKuerzel = ZBEPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_ZUS_ZUSTAENDIGKEITNR = ZBE_ZUSTBEREICHLFDNR" "~"
if joinKuerzel = BTGZBZ
searchResult := searchResult "LEFT JOIN BUERO.ZBZ_ZEITBEZUGSART ON ZBZ_FIRMENNR = BTG_ZBZ_FIRMENNR AND ZBZ_ZBEZUGSARTNR = BTG_ZBEZUGSARTNR" "~"
if joinKuerzel = ZBZBTG
searchResult := searchResult "LEFT JOIN BUERO.BTG_BEWERTUNGTAG ON BTG_ZBZ_FIRMENNR = ZBZ_FIRMENNR AND BTG_ZBEZUGSARTNR = ZBZ_ZBEZUGSARTNR" "~"
if joinKuerzel = ZZUZKS
searchResult := searchResult "LEFT JOIN FIBU.ZKS_ZINSBERECHNUNGSKREIS ON ZKS_FIRMENNR = ZZU_FIRMENNR AND ZKS_FIBUNR = ZZU_FIBUNR AND ZKS_ZINSKREISLFDNR = ZZU_ZINSKREISLFDNR" "~"
if joinKuerzel = ZKSZZU
searchResult := searchResult "LEFT JOIN FIBU.ZZU_ZINSBERECHNUNG_ZLG_ZU ON ZZU_FIRMENNR = ZKS_FIRMENNR AND ZZU_FIBUNR = ZKS_FIBUNR AND ZZU_ZINSKREISLFDNR = ZKS_ZINSKREISLFDNR" "~"
if joinKuerzel = KOSZLT
searchResult := searchResult "LEFT JOIN ZLE.ZLT_ZEILENTYP ON ZLT_FIRMENNR = KOS_ZLT_FIRMENNR AND ZLT_ZEILENTYPNR = KOS_ZEILENTYPNR" "~"
if joinKuerzel = ZLTKOS
searchResult := searchResult "LEFT JOIN ZLE.KOS_KOSTENFAKTOR ON KOS_ZLT_FIRMENNR = ZLT_FIRMENNR AND KOS_ZEILENTYPNR = ZLT_ZEILENTYPNR" "~"
if joinKuerzel = ZMDZMB
searchResult := searchResult "LEFT JOIN BUERO.ZMB_ZEITMINDESTBESETZUNG ON ZMB_ZMBID = ZMD_ZMBID" "~"
if joinKuerzel = ZMBZMD
searchResult := searchResult "LEFT JOIN BUERO.ZMD_ZEITMINDESTBESETZDET ON ZMD_ZMBID = ZMB_ZMBID" "~"
if joinKuerzel = RWZZRB
searchResult := searchResult "LEFT JOIN BUERO.ZRB_ZEITRUNDUNGBEDINGUNG ON ZRB_FIRMENNR = RWZ_ZRB_FIRMENNR AND ZRB_ZRUNDBEDINGUNGNR = RWZ_ZRUNDBEDINGUNGNR" "~"
if joinKuerzel = ZRBRWZ
searchResult := searchResult "LEFT JOIN BUERO.RWZ_ZEITRUNDWOCHENMDZUORD ON RWZ_ZRB_FIRMENNR = ZRB_FIRMENNR AND RWZ_ZRUNDBEDINGUNGNR = ZRB_ZRUNDBEDINGUNGNR" "~"
if joinKuerzel = AVEZRT
searchResult := searchResult "LEFT JOIN ZRT_ZAHLUNGSART.ZRT_ZAHLUNGSART ON ZRT_ZAHLARTNR = AVE_ZRT_ZAHLARTNR" "~"
if joinKuerzel = ZRTAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ZRT_ZAHLARTNR = ZRT_ZAHLARTNR" "~"
if joinKuerzel = KSDZRT
searchResult := searchResult "LEFT JOIN ZRT_ZAHLUNGSART.ZRT_ZAHLUNGSART ON ZRT_ZAHLARTNR = KSD_ZAHLARTNR" "~"
if joinKuerzel = ZRTKSD
searchResult := searchResult "LEFT JOIN WWS.KSD_KASSENSUMMENDETAIL ON KSD_ZAHLARTNR = ZRT_ZAHLARTNR" "~"
if joinKuerzel = RAZZRT
searchResult := searchResult "LEFT JOIN BUERO.ZRT_ZAHLUNGSART ON ZRT_ZAHLARTNR = RAZ_RATENZAHLUNG_ZAHLARTNR" "~"
if joinKuerzel = ZRTRAZ
searchResult := searchResult "LEFT JOIN BUERO.RAZ_RATENZAHLUNG ON RAZ_RATENZAHLUNG_ZAHLARTNR = ZRT_ZAHLARTNR" "~"
if joinKuerzel = ZTUZTZ
searchResult := searchResult "LEFT JOIN BMD.ZTZ_ZEITZONEN ON ZTZ_ZEITZONENR = ZTU_ZEITZONENR" "~"
if joinKuerzel = ZTZZTU
searchResult := searchResult "LEFT JOIN BMD.ZTU_ZEITUMSTELLUNG ON ZTU_ZEITZONENR = ZTZ_ZEITZONENR" "~"
if joinKuerzel = BPPVKG
searchResult := searchResult "LEFT JOIN KORE.VKG_VIEW_KOSTAMM_GBR ON VKG_FIRMENNR = BPP_FIRMENNR AND VKG_KOSTENSTAMMNR = BPP_GBR_KOSTENSTAMMNR" "~"
if joinKuerzel = VKGBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_FIRMENNR = VKG_FIRMENNR AND BPP_GBR_KOSTENSTAMMNR = VKG_KOSTENSTAMMNR" "~"
if joinKuerzel = DELBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DEL_LETZTERUSER" "~"
if joinKuerzel = BMUDEL
searchResult := searchResult "LEFT JOIN BUERO.DEL_DELETE ON DEL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DEZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DEZ_LETZTERUSER" "~"
if joinKuerzel = BMUDEZ
searchResult := searchResult "LEFT JOIN DATANORM.DEZ_DATANORM_ERSTELLERZU ON DEZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DFKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DFK_LETZTERUSER" "~"
if joinKuerzel = BMUDFK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFK_DBMDFORMATKATALOG ON DFK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DGWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DGW_LETZTERUSER" "~"
if joinKuerzel = BMUDGW
searchResult := searchResult "LEFT JOIN HRS.DGW_DBGESETZWERTE ON DGW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DIHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DIH_LETZTERUSER" "~"
if joinKuerzel = BMUDIH
searchResult := searchResult "LEFT JOIN BMD.DIH_DIAGRAMMINSTANZKOPF ON DIH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DISBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DIS_LETZTERUSER" "~"
if joinKuerzel = BMUDIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DNLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DNL_LETZTERUSER" "~"
if joinKuerzel = BMUDNL
searchResult := searchResult "LEFT JOIN HRS.DNL_DNLISTE ON DNL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DNVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DNV_LETZTERUSER" "~"
if joinKuerzel = BMUDNV
searchResult := searchResult "LEFT JOIN HRS.DNV_DNVORSCHLAG ON DNV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DPOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DPO_LETZTERUSER" "~"
if joinKuerzel = BMUDPO
searchResult := searchResult "LEFT JOIN BMD.DPO_DOCUMENTPOOL ON DPO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DSMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DSM_LETZTERUSER" "~"
if joinKuerzel = BMUDSM
searchResult := searchResult "LEFT JOIN DATANORM.DSM_SET ON DSM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DVLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DVL_LETZTERUSER" "~"
if joinKuerzel = BMUDVL
searchResult := searchResult "LEFT JOIN BUERO.DVL_DOKUMENTVERLAUF ON DVL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DWHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DWH_LETZTERUSER" "~"
if joinKuerzel = BMUDWH
searchResult := searchResult "LEFT JOIN DATANORM.DWH_WAREN_HAUPT_GRUPPE ON DWH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EANBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EAN_LETZTERUSER" "~"
if joinKuerzel = BMUEAN
searchResult := searchResult "LEFT JOIN WWS.EAN_EANNUMMERN ON EAN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EBPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EBP_LETZTERUSER" "~"
if joinKuerzel = BMUEBP
searchResult := searchResult "LEFT JOIN FIBU.EBP_BESTELLPOSITION ON EBP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EEMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EEM_LETZTERUSER" "~"
if joinKuerzel = BMUEEM
searchResult := searchResult "LEFT JOIN WWS.EEM_EDIEINHEITENMAPPING ON EEM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EKLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EKL_LETZTERUSER" "~"
if joinKuerzel = BMUEKL
searchResult := searchResult "LEFT JOIN WWS.EKL_ERFASSKOPFLFS ON EKL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EKOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EKO_LETZTERUSER" "~"
if joinKuerzel = BMUEKO
searchResult := searchResult "LEFT JOIN HRS.EKO_ELDAKONTROLLE ON EKO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EKRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EKR_LETZTERUSER" "~"
if joinKuerzel = BMUEKR
searchResult := searchResult "LEFT JOIN WWS.EKR_ERFASSKOPFRECHNUNG ON EKR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EM3BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EM3_LETZTERUSER" "~"
if joinKuerzel = BMUEM3
searchResult := searchResult "LEFT JOIN HRS.EM3_ELDADETAIL_TG ON EM3_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EMBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EMB_LETZTERUSER" "~"
if joinKuerzel = BMUEMB
searchResult := searchResult "LEFT JOIN HRS.EMB_ELDADETAIL_MBGM_P ON EMB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ENTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ENT_LETZTERUSER" "~"
if joinKuerzel = BMUENT
searchResult := searchResult "LEFT JOIN BMD.ENT_EDINACHRICHTTYP ON ENT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ERBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ERB_LETZTERUSER" "~"
if joinKuerzel = BMUERB
searchResult := searchResult "LEFT JOIN FIBU.ERB_BUCHUNG ON ERB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ERDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ERD_LETZTERUSER" "~"
if joinKuerzel = BMUERD
searchResult := searchResult "LEFT JOIN WWS.ERD_ERFASSRABATTDETAIL ON ERD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ERXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ERX_LETZTERUSER" "~"
if joinKuerzel = BMUERX
searchResult := searchResult "LEFT JOIN HRS.ERX_ERFASSUNGSZUSATZ ON ERX_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EXPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EXP_LETZTERUSER" "~"
if joinKuerzel = BMUEXP
searchResult := searchResult "LEFT JOIN BUERO.EXP_EXTERNPERSONKEY ON EXP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FAVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FAV_LETZTERUSER" "~"
if joinKuerzel = BMUFAV
searchResult := searchResult "LEFT JOIN BUERO.FAV_FAVORITEN ON FAV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = QUSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = QUS_LETZTERUSER" "~"
if joinKuerzel = BMUQUS
searchResult := searchResult "LEFT JOIN BUERO.QUS_QUOTENSUMME ON QUS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RAPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RAP_LETZTERUSER" "~"
if joinKuerzel = BMURAP
searchResult := searchResult "LEFT JOIN WWS.RAP_RABATTTABPOS ON RAP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = REZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = REZ_LETZTERUSER" "~"
if joinKuerzel = BMUREZ
searchResult := searchResult "LEFT JOIN ZLE.REZ_REGELWERKZU ON REZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RGTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RGT_LETZTERUSER" "~"
if joinKuerzel = BMURGT
searchResult := searchResult "LEFT JOIN BMD.RGT_TABREPLGTABELLEZU ON RGT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RKTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RKT_LETZTERUSER" "~"
if joinKuerzel = BMURKT
searchResult := searchResult "LEFT JOIN BMD.RKT_REPLIKATIONSTRANSFER ON RKT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RMOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RMO_LETZTERUSER" "~"
if joinKuerzel = BMURMO
searchResult := searchResult "LEFT JOIN BUERO.RMO_RUNDUNGSMODELL ON RMO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RSBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RSB_LETZTERUSER" "~"
if joinKuerzel = BMURSB
searchResult := searchResult "LEFT JOIN BUERO.RSB_RESSOURCENBEDARF ON RSB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SA3BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SA3_LETZTERUSER" "~"
if joinKuerzel = BMUSA3
searchResult := searchResult "LEFT JOIN ONVIS.SA3_SAURER_BDE ON SA3_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SBCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SBC_LETZTERUSER" "~"
if joinKuerzel = BMUSBC
searchResult := searchResult "LEFT JOIN BUERO.SBC_SPEZBEWEGUNGSCODE ON SBC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCC_LETZTERUSER" "~"
if joinKuerzel = BMUSCC
searchResult := searchResult "LEFT JOIN BMDENTW.SCC_SCRREVISIONFILE ON SCC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCG_LETZTERUSER" "~"
if joinKuerzel = BMUSCG
searchResult := searchResult "LEFT JOIN PRODUKTION.SCG_SCORECARDKRITZU ON SCG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SFFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SFF_LETZTERUSER" "~"
if joinKuerzel = BMUSFF
searchResult := searchResult "LEFT JOIN BUERO.SFF_SKILLFINDERFILTER ON SFF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SIRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SIR_LETZTERUSER" "~"
if joinKuerzel = BMUSIR
searchResult := searchResult "LEFT JOIN HRS.SIR_STANDARDIMPORTREFERENZ ON SIR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SKOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SKO_LETZTERUSER" "~"
if joinKuerzel = BMUSKO
searchResult := searchResult "LEFT JOIN FIBU.SKO_STEUERKONTEN ON SKO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SLABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SLA_LETZTERUSER" "~"
if joinKuerzel = BMUSLA
searchResult := searchResult "LEFT JOIN WWS.SLA_STKLISTVARAUSWAHL ON SLA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SNIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SNI_LETZTERUSER" "~"
if joinKuerzel = BMUSNI
searchResult := searchResult "LEFT JOIN HRS.SNI_SCHNITTE ON SNI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPL_LETZTERUSER" "~"
if joinKuerzel = BMUSPL
searchResult := searchResult "LEFT JOIN BMD.SPL_STEMPEL ON SPL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPT_LETZTERUSER" "~"
if joinKuerzel = BMUSPT
searchResult := searchResult "LEFT JOIN BMD.SPT_STAPELSCHRITT ON SPT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SSZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SSZ_LETZTERUSER" "~"
if joinKuerzel = BMUSSZ
searchResult := searchResult "LEFT JOIN ZLE.SSZ_SORTIERKOPFSMIZU ON SSZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STV_LETZTERUSER" "~"
if joinKuerzel = BMUSTV
searchResult := searchResult "LEFT JOIN BUERO.STV_STATVERFAHREN ON STV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SWABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SWA_LETZTERUSER" "~"
if joinKuerzel = BMUSWA
searchResult := searchResult "LEFT JOIN HRS.SWA_SW_ANSPRUCH ON SWA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TCNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TCN_LETZTERUSER" "~"
if joinKuerzel = BMUTCN
searchResult := searchResult "LEFT JOIN BMD.TCN_TCVERSIONEN ON TCN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TDKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TDK_LETZTERUSER" "~"
if joinKuerzel = BMUTDK
searchResult := searchResult "LEFT JOIN WWS.TDK_WWSDATENKONTR_TMP ON TDK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = THVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = THV_LETZTERUSER" "~"
if joinKuerzel = BMUTHV
searchResult := searchResult "LEFT JOIN HRS.THV_TARIFGR_HAEUFIGVERW ON THV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TKTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TKT_LETZTERUSER" "~"
if joinKuerzel = BMUTKT
searchResult := searchResult "LEFT JOIN BMDENTW.TKT_TICKETBEREICHZU ON TKT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TKZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TKZ_LETZTERUSER" "~"
if joinKuerzel = BMUTKZ
searchResult := searchResult "LEFT JOIN BMDENTW.TKZ_TICKETKONTAKTZU ON TKZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ETDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ETD_LETZTERUSER" "~"
if joinKuerzel = BMUETD
searchResult := searchResult "LEFT JOIN BMDDEFAULT.ETD_ETIKETTENFORMAT ON ETD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ETFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ETF_LETZTERUSER" "~"
if joinKuerzel = BMUETF
searchResult := searchResult "LEFT JOIN BMD.ETF_ETIKETTENFORMAT ON ETF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ETKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ETK_LETZTERUSER" "~"
if joinKuerzel = BMUETK
searchResult := searchResult "LEFT JOIN WWS.ETK_ETIKETTENKLASSE ON ETK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EVGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EVG_LETZTERUSER" "~"
if joinKuerzel = BMUEVG
searchResult := searchResult "LEFT JOIN BUERO.EVG_EVENTTAGE ON EVG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EVKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EVK_LETZTERUSER" "~"
if joinKuerzel = BMUEVK
searchResult := searchResult "LEFT JOIN BUERO.EVK_EVENTKATEGORIEZU ON EVK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EVQBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EVQ_LETZTERUSER" "~"
if joinKuerzel = BMUEVQ
searchResult := searchResult "LEFT JOIN HRS.EVQ_ELDADETAIL_VSNR ON EVQ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EVRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EVR_LETZTERUSER" "~"
if joinKuerzel = BMUEVR
searchResult := searchResult "LEFT JOIN HRS.EVR_ELDADETAIL_VMR ON EVR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EVZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EVZ_LETZTERUSER" "~"
if joinKuerzel = BMUEVZ
searchResult := searchResult "LEFT JOIN BUERO.EVZ_TLNTAGZU ON EVZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FARBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FAR_LETZTERUSER" "~"
if joinKuerzel = BMUFAR
searchResult := searchResult "LEFT JOIN BMD.FAR_FARBCODE ON FAR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FATBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FAT_LETZTERUSER" "~"
if joinKuerzel = BMUFAT
searchResult := searchResult "LEFT JOIN WWS.FAT_FARBENTAB ON FAT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FBRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FBR_LETZTERUSER" "~"
if joinKuerzel = BMUFBR
searchResult := searchResult "LEFT JOIN BUERO.FBR_FIRMENBRANCHEZU ON FBR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FGBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FGB_LETZTERUSER" "~"
if joinKuerzel = BMUFGB
searchResult := searchResult "LEFT JOIN BUERO.FGB_FRAGEBOGEN ON FGB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FGKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FGK_LETZTERUSER" "~"
if joinKuerzel = BMUFGK
searchResult := searchResult "LEFT JOIN BUERO.FGK_FIRMENGKKZU ON FGK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FIFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FIF_LETZTERUSER" "~"
if joinKuerzel = BMUFIF
searchResult := searchResult "LEFT JOIN FIBU.FIF_FILIALEFIBU ON FIF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FINBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FIN_LETZTERUSER" "~"
if joinKuerzel = BMUFIN
searchResult := searchResult "LEFT JOIN BUERO.FIN_FINANZAMT ON FIN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FRZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FRZ_LETZTERUSER" "~"
if joinKuerzel = BMUFRZ
searchResult := searchResult "LEFT JOIN BUERO.FRZ_FRAGEBOGENREGELZU ON FRZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FSUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FSU_LETZTERUSER" "~"
if joinKuerzel = BMUFSU
searchResult := searchResult "LEFT JOIN FIBU.FSU_SORTSUMMENPOS ON FSU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FZUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FZU_LETZTERUSER" "~"
if joinKuerzel = BMUFZU
searchResult := searchResult "LEFT JOIN FIBU.FZU_FIBUFIBUZU ON FZU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GAD_LETZTERUSER" "~"
if joinKuerzel = BMUGAD
searchResult := searchResult "LEFT JOIN BUERO.GAD_GHOSTADRESSE ON GAD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GEABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GEA_LETZTERUSER" "~"
if joinKuerzel = BMUGEA
searchResult := searchResult "LEFT JOIN BUERO.GEA_GESCHAEFTSART ON GEA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GLUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GLU_LETZTERUSER" "~"
if joinKuerzel = BMUGLU
searchResult := searchResult "LEFT JOIN WWS.GLU_GELDLADENZUORDNUNG ON GLU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GPHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GPH_LETZTERUSER" "~"
if joinKuerzel = BMUGPH
searchResult := searchResult "LEFT JOIN BUERO.GPH_GOPHILFSTABELLE ON GPH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GRMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GRM_LETZTERUSER" "~"
if joinKuerzel = BMUGRM
searchResult := searchResult "LEFT JOIN DATANORM.GRM_GRAFIK ON GRM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GSABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GSA_LETZTERUSER" "~"
if joinKuerzel = BMUGSA
searchResult := searchResult "LEFT JOIN WWS.GSA_GUTSCHEINAKTION ON GSA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GSMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GSM_LETZTERUSER" "~"
if joinKuerzel = BMUGSM
searchResult := searchResult "LEFT JOIN PRODUKTION.GSM_GUTSCHLECHTMENGE ON GSM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GTZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GTZ_LETZTERUSER" "~"
if joinKuerzel = BMUGTZ
searchResult := searchResult "LEFT JOIN BIL.GTZ_GLIEDERUNGSTEXTZEILE ON GTZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PWTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PWT_LETZTERUSER" "~"
if joinKuerzel = BMUPWT
searchResult := searchResult "LEFT JOIN BIL.PWT_PERSRST_ALTERSV_WERT ON PWT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PWXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PWX_LETZTERUSER" "~"
if joinKuerzel = BMUPWX
searchResult := searchResult "LEFT JOIN KORE.PWX_PLANWERTEEXT ON PWX_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PZUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PZU_LETZTERUSER" "~"
if joinKuerzel = BMUPZU
searchResult := searchResult "LEFT JOIN WWS.PZU_PREISRABATTZUGRIFF ON PZU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = REABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = REA_LETZTERUSER" "~"
if joinKuerzel = BMUREA
searchResult := searchResult "LEFT JOIN HRS.REA_REISEAUFTEILUNG ON REA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = REWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = REW_LETZTERUSER" "~"
if joinKuerzel = BMUREW
searchResult := searchResult "LEFT JOIN ZLE.REW_REGELWERK ON REW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RFMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RFM_LETZTERUSER" "~"
if joinKuerzel = BMURFM
searchResult := searchResult "LEFT JOIN BUERO.RFM_RECHTSFORM ON RFM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RKEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RKE_LETZTERUSER" "~"
if joinKuerzel = BMURKE
searchResult := searchResult "LEFT JOIN BMD.RKE_TABREPLEINTRAG ON RKE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RNGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RNG_LETZTERUSER" "~"
if joinKuerzel = BMURNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RVKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RVK_LETZTERUSER" "~"
if joinKuerzel = BMURVK
searchResult := searchResult "LEFT JOIN ZLE.RVK_RECHVORLAGEKOPF ON RVK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RWZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RWZ_LETZTERUSER" "~"
if joinKuerzel = BMURWZ
searchResult := searchResult "LEFT JOIN BUERO.RWZ_ZEITRUNDWOCHENMDZUORD ON RWZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SA4BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SA4_LETZTERUSER" "~"
if joinKuerzel = BMUSA4
searchResult := searchResult "LEFT JOIN ONVIS.SA4_SAURER_BDE ON SA4_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCK_LETZTERUSER" "~"
if joinKuerzel = BMUSCK
searchResult := searchResult "LEFT JOIN PRODUKTION.SCK_SCKENNZAHL ON SCK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCL_LETZTERUSER" "~"
if joinKuerzel = BMUSCL
searchResult := searchResult "LEFT JOIN PRODUKTION.SCL_SCKENNZAHLDET ON SCL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCM_LETZTERUSER" "~"
if joinKuerzel = BMUSCM
searchResult := searchResult "LEFT JOIN BMDENTW.SCM_SCRMAIN ON SCM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCP_LETZTERUSER" "~"
if joinKuerzel = BMUSCP
searchResult := searchResult "LEFT JOIN BMDENTW.SCP_SCRPROT ON SCP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCT_LETZTERUSER" "~"
if joinKuerzel = BMUSCT
searchResult := searchResult "LEFT JOIN BMDENTW.SCT_SCRINTRAY ON SCT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCX_LETZTERUSER" "~"
if joinKuerzel = BMUSCX
searchResult := searchResult "LEFT JOIN KORE.SCX_SUMMENCHECKEXPORT ON SCX_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SLBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SLB_LETZTERUSER" "~"
if joinKuerzel = BMUSLB
searchResult := searchResult "LEFT JOIN BIL.SLB_SALDENBESTAETIGUNG ON SLB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SLZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SLZ_LETZTERUSER" "~"
if joinKuerzel = BMUSLZ
searchResult := searchResult "LEFT JOIN BIL.SLZ_STEUERLOHNZETTEL ON SLZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPG_LETZTERUSER" "~"
if joinKuerzel = BMUSPG
searchResult := searchResult "LEFT JOIN BMDENTW.SPG_PGMS ON SPG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPP_LETZTERUSER" "~"
if joinKuerzel = BMUSPP
searchResult := searchResult "LEFT JOIN BMD.SPP_STAPELPARAMETER ON SPP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPV_LETZTERUSER" "~"
if joinKuerzel = BMUSPV
searchResult := searchResult "LEFT JOIN WWS.SPV_PROVISIONSTAFFEL ON SPV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SREBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SRE_LETZTERUSER" "~"
if joinKuerzel = BMUSRE
searchResult := searchResult "LEFT JOIN BMD.SRE_STANDARDREPORT ON SRE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SRWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SRW_LETZTERUSER" "~"
if joinKuerzel = BMUSRW
searchResult := searchResult "LEFT JOIN WWS.SRW_STEUERARTWERT ON SRW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SSCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SSC_LETZTERUSER" "~"
if joinKuerzel = BMUSSC
searchResult := searchResult "LEFT JOIN FIBU.SSC_STEUERSCHLUESSEL ON SSC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STE_LETZTERUSER" "~"
if joinKuerzel = BMUSTE
searchResult := searchResult "LEFT JOIN BUERO.STE_STEUERERKLAERUNG ON STE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DAZ_LETZTERUSER" "~"
if joinKuerzel = BMUDAZ
searchResult := searchResult "LEFT JOIN DATANORM.DAZ_DATANORM_ARTIKELZU ON DAZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DBCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DBC_LETZTERUSER" "~"
if joinKuerzel = BMUDBC
searchResult := searchResult "LEFT JOIN BMD.DBC_DBCHANGE ON DBC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DBLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DBL_LETZTERUSER" "~"
if joinKuerzel = BMUDBL
searchResult := searchResult "LEFT JOIN BUERO.DBL_DBSLOHNDATEI ON DBL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DBPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DBP_LETZTERUSER" "~"
if joinKuerzel = BMUDBP
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DBP_DBERECHTPAKET ON DBP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DBXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DBX_LETZTERUSER" "~"
if joinKuerzel = BMUDBX
searchResult := searchResult "LEFT JOIN BUERO.DBX_FINONDATABOX ON DBX_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DCCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DCC_LETZTERUSER" "~"
if joinKuerzel = BMUDCC
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DCC_CHECK_COLUMNS ON DCC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DEIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DEI_LETZTERUSER" "~"
if joinKuerzel = BMUDEI
searchResult := searchResult "LEFT JOIN HRS.DEI_DBEINSTELLUNGEN ON DEI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DFEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DFE_LETZTERUSER" "~"
if joinKuerzel = BMUDFE
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFE_DFELDBESCHPOS ON DFE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DFGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DFG_LETZTERUSER" "~"
if joinKuerzel = BMUDFG
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFG_DFELDBESCHGRUPPE ON DFG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DFPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DFP_LETZTERUSER" "~"
if joinKuerzel = BMUDFP
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFP_DFUNKTIONPAKETZU ON DFP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DIKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DIK_LETZTERUSER" "~"
if joinKuerzel = BMUDIK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DIK_INSTITUTIONENKEYS_PD ON DIK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DIXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DIX_LETZTERUSER" "~"
if joinKuerzel = BMUDIX
searchResult := searchResult "LEFT JOIN BMD.DIX_DIAGRAMMAKTION ON DIX_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DKWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DKW_LETZTERUSER" "~"
if joinKuerzel = BMUDKW
searchResult := searchResult "LEFT JOIN BUERO.DKW_DOKKATWORKFLOWZU ON DKW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DOZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DOZ_LETZTERUSER" "~"
if joinKuerzel = BMUDOZ
searchResult := searchResult "LEFT JOIN BMD.DOZ_DATAOWNERZU ON DOZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DPABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DPA_LETZTERUSER" "~"
if joinKuerzel = BMUDPA
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DPA_DPAKET ON DPA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DPRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DPR_LETZTERUSER" "~"
if joinKuerzel = BMUDPR
searchResult := searchResult "LEFT JOIN BUERO.DPR_DOKUMENTPROJEKTZU ON DPR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DSDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DSD_LETZTERUSER" "~"
if joinKuerzel = BMUDSD
searchResult := searchResult "LEFT JOIN DATANORM.DSD_SET_DETAIL ON DSD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DSTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DST_LETZTERUSER" "~"
if joinKuerzel = BMUDST
searchResult := searchResult "LEFT JOIN HRS.DST_DIVSTAMM ON DST_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DTABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DTA_LETZTERUSER" "~"
if joinKuerzel = BMUDTA
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DTA_DBMDTABELLE ON DTA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DZABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DZA_LETZTERUSER" "~"
if joinKuerzel = BMUDZA
searchResult := searchResult "LEFT JOIN HRS.DZA_ANRECHNUNG ON DZA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EAEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EAE_LETZTERUSER" "~"
if joinKuerzel = BMUEAE
searchResult := searchResult "LEFT JOIN HRS.EAE_ELDADETAIL_AEK ON EAE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EAWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EAW_LETZTERUSER" "~"
if joinKuerzel = BMUEAW
searchResult := searchResult "LEFT JOIN HRS.EAW_ELDADETAIL_AEW ON EAW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EBGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EBG_LETZTERUSER" "~"
if joinKuerzel = BMUEBG
searchResult := searchResult "LEFT JOIN HRS.EBG_ELDADETAIL_BGN ON EBG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EBNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EBN_LETZTERUSER" "~"
if joinKuerzel = BMUEBN
searchResult := searchResult "LEFT JOIN HRS.EBN_ELDADETAIL_BN ON EBN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ERSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ERS_LETZTERUSER" "~"
if joinKuerzel = BMUERS
searchResult := searchResult "LEFT JOIN FIBU.ERS_KOST ON ERS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ERZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ERZ_LETZTERUSER" "~"
if joinKuerzel = BMUERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PVKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PVK_LETZTERUSER" "~"
if joinKuerzel = BMUPVK
searchResult := searchResult "LEFT JOIN WWS.PVK_PROVISIONTAB ON PVK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PVZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PVZ_LETZTERUSER" "~"
if joinKuerzel = BMUPVZ
searchResult := searchResult "LEFT JOIN WWS.PVZ_PROVISIONTABZU ON PVZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PWABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PWA_LETZTERUSER" "~"
if joinKuerzel = BMUPWA
searchResult := searchResult "LEFT JOIN WWS.PWA_PWKARTIKELZU ON PWA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PZGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PZG_LETZTERUSER" "~"
if joinKuerzel = BMUPZG
searchResult := searchResult "LEFT JOIN WWS.PZG_PICGROESSENZU ON PZG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PZKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PZK_LETZTERUSER" "~"
if joinKuerzel = BMUPZK
searchResult := searchResult "LEFT JOIN WWSINDIV.PZK_PROZKTOZU ON PZK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = QDABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = QDA_LETZTERUSER" "~"
if joinKuerzel = BMUQDA
searchResult := searchResult "LEFT JOIN PRODUKTION.QDA_QM_DATA ON QDA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = QLKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = QLK_LETZTERUSER" "~"
if joinKuerzel = BMUQLK
searchResult := searchResult "LEFT JOIN BMD.QLK_LINK ON QLK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = QMKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = QMK_LETZTERUSER" "~"
if joinKuerzel = BMUQMK
searchResult := searchResult "LEFT JOIN PRODUKTION.QMK_QM_KOPF ON QMK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RATBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RAT_LETZTERUSER" "~"
if joinKuerzel = BMURAT
searchResult := searchResult "LEFT JOIN WWS.RAT_RABATTTAB ON RAT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RCLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RCL_LETZTERUSER" "~"
if joinKuerzel = BMURCL
searchResult := searchResult "LEFT JOIN PRODUKTION.RCL_REPEATING_CHECKLIST ON RCL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = REOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = REO_LETZTERUSER" "~"
if joinKuerzel = BMUREO
searchResult := searchResult "LEFT JOIN BUERO.REO_RELEVANTEOBJEKTE ON REO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = REPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = REP_LETZTERUSER" "~"
if joinKuerzel = BMUREP
searchResult := searchResult "LEFT JOIN BUERO.REP_RESSOURCENPAKET ON REP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RERBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RER_LETZTERUSER" "~"
if joinKuerzel = BMURER
searchResult := searchResult "LEFT JOIN WWS.RER_RESERVIERUNGSREFERENZ ON RER_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RGSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RGS_LETZTERUSER" "~"
if joinKuerzel = BMURGS
searchResult := searchResult "LEFT JOIN BMD.RGS_TABREPLGSTANDORTZU ON RGS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RMDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RMD_LETZTERUSER" "~"
if joinKuerzel = BMURMD
searchResult := searchResult "LEFT JOIN BUERO.RMD_RUNDUNGSMODELLDET ON RMD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RPVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RPV_LETZTERUSER" "~"
if joinKuerzel = BMURPV
searchResult := searchResult "LEFT JOIN BMD.RPV_REPORTVORSCHLAG ON RPV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RUKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RUK_LETZTERUSER" "~"
if joinKuerzel = BMURUK
searchResult := searchResult "LEFT JOIN WWS.RUK_RUNDUNGKOPF ON RUK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RZSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RZS_LETZTERUSER" "~"
if joinKuerzel = BMURZS
searchResult := searchResult "LEFT JOIN BUERO.RZS_RUECKZAHLUNGSSTAMM ON RZS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SA1BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SA1_LETZTERUSER" "~"
if joinKuerzel = BMUSA1
searchResult := searchResult "LEFT JOIN ONVIS.SA1_SAURER_BDE ON SA1_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SABBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SAB_LETZTERUSER" "~"
if joinKuerzel = BMUSAB
searchResult := searchResult "LEFT JOIN BUERO.SAB_STELLENAUSBILDUNG ON SAB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SAD_LETZTERUSER" "~"
if joinKuerzel = BMUSAD
searchResult := searchResult "LEFT JOIN FIBU.SAD_SALDEN ON SAD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SAK_LETZTERUSER" "~"
if joinKuerzel = BMUSAK
searchResult := searchResult "LEFT JOIN FIBU.SAK_SACHKONTO ON SAK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SBSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SBS_LETZTERUSER" "~"
if joinKuerzel = BMUSBS
searchResult := searchResult "LEFT JOIN BUERO.SBS_STELLENBESCHREIBUNG ON SBS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCF_LETZTERUSER" "~"
if joinKuerzel = BMUSCF
searchResult := searchResult "LEFT JOIN BMDENTW.SCF_SCRFILE ON SCF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SERBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SER_LETZTERUSER" "~"
if joinKuerzel = BMUSER
searchResult := searchResult "LEFT JOIN BMD.SER_SQLEXECREQUEST ON SER_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SESBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SES_LETZTERUSER" "~"
if joinKuerzel = BMUSES
searchResult := searchResult "LEFT JOIN BIL.SES_STEUERERKLSETTINGS ON SES_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KLIVKH
searchResult := searchResult "LEFT JOIN BUERO.VKH_VIEW_KUNDENHAUPTGRUPPE ON VKH_FIRMENNR = KLI_KUG_FIRMENNR AND VKH_KUNDENHAUPTGRUPPENNR = KLI_KUNDENHAUPTGRUPPENNR" "~"
if joinKuerzel = VKHKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_KUG_FIRMENNR = VKH_FIRMENNR AND KLI_KUNDENHAUPTGRUPPENNR = VKH_KUNDENHAUPTGRUPPENNR" "~"
if joinKuerzel = LEBEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = LEB_ERP_FIRMENNR AND EFK_ERFASSUNGSKZ = LEB_ERP_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = LEB_ERP_ERFASSUNGSNR" "~"
if joinKuerzel = EFKLEB
searchResult := searchResult "LEFT JOIN BUERO.LEB_LEBENSLAUF ON LEB_ERP_FIRMENNR = EFK_FIRMENNR AND LEB_ERP_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND LEB_ERP_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = AACBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AAC_LETZTERUSER" "~"
if joinKuerzel = BMUAAC
searchResult := searchResult "LEFT JOIN BIL.AAC_ANBU_ATTRSEL_CHECK ON AAC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABW_LETZTERUSER" "~"
if joinKuerzel = BMUABW
searchResult := searchResult "LEFT JOIN BUERO.ABW_BEWERBERAUFGABEN ON ABW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ADKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ADK_LETZTERUSER" "~"
if joinKuerzel = BMUADK
searchResult := searchResult "LEFT JOIN BUERO.ADK_ADRESSENLISTEKOPF ON ADK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ADLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ADL_LETZTERUSER" "~"
if joinKuerzel = BMUADL
searchResult := searchResult "LEFT JOIN BUERO.ADL_ADLCODE ON ADL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AFTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AFT_LETZTERUSER" "~"
if joinKuerzel = BMUAFT
searchResult := searchResult "LEFT JOIN ZLE.AFT_AUFBEREITUNG ON AFT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AGTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AGT_LETZTERUSER" "~"
if joinKuerzel = BMUAGT
searchResult := searchResult "LEFT JOIN BIL.AGT_ANLAGEGUT_TMP ON AGT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AITBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AIT_LETZTERUSER" "~"
if joinKuerzel = BMUAIT
searchResult := searchResult "LEFT JOIN BIL.AIT_ANBU_IDS_TMP ON AIT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AMDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AMD_LETZTERUSER" "~"
if joinKuerzel = BMUAMD
searchResult := searchResult "LEFT JOIN KORE.AMD_MODELLDETAIL ON AMD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ANBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ANB_LETZTERUSER" "~"
if joinKuerzel = BMUANB
searchResult := searchResult "LEFT JOIN BIL.ANB_ANBUBUCHBELEGZU ON ANB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ANHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ANH_LETZTERUSER" "~"
if joinKuerzel = BMUANH
searchResult := searchResult "LEFT JOIN BUERO.ANH_ANGEHOERIGER ON ANH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ANTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ANT_LETZTERUSER" "~"
if joinKuerzel = BMUANT
searchResult := searchResult "LEFT JOIN BUERO.ANT_ANTWORT ON ANT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ARLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ARL_LETZTERUSER" "~"
if joinKuerzel = BMUARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ARSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ARS_LETZTERUSER" "~"
if joinKuerzel = BMUARS
searchResult := searchResult "LEFT JOIN WWS.ARS_ARTIKELSTATISTIK ON ARS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ARUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ARU_LETZTERUSER" "~"
if joinKuerzel = BMUARU
searchResult := searchResult "LEFT JOIN ZLE.ARU_ARTIKELUMLAGE ON ARU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ASFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ASF_LETZTERUSER" "~"
if joinKuerzel = BMUASF
searchResult := searchResult "LEFT JOIN BMD.ASF_AUTOSTARTFUNKTION ON ASF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ASPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ASP_LETZTERUSER" "~"
if joinKuerzel = BMUASP
searchResult := searchResult "LEFT JOIN BIL.ASP_SPALTENKOPF ON ASP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ASWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ASW_LETZTERUSER" "~"
if joinKuerzel = BMUASW
searchResult := searchResult "LEFT JOIN BUERO.ASW_AUSWEIS ON ASW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ATABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ATA_LETZTERUSER" "~"
if joinKuerzel = BMUATA
searchResult := searchResult "LEFT JOIN WWS.ATA_ARTIKELTEXTARCHIV ON ATA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AUZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AUZ_LETZTERUSER" "~"
if joinKuerzel = BMUAUZ
searchResult := searchResult "LEFT JOIN BIL.AUZ_AUSWERTUNGZUSATZ ON AUZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AWSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AWS_LETZTERUSER" "~"
if joinKuerzel = BMUAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AXZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AXZ_LETZTERUSER" "~"
if joinKuerzel = BMUAXZ
searchResult := searchResult "LEFT JOIN BMDENTW.AXZ_ATTREXTZU ON AXZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AZDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AZD_LETZTERUSER" "~"
if joinKuerzel = BMUAZD
searchResult := searchResult "LEFT JOIN BUERO.AZD_ARBEITSZEITMODELLDET ON AZD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AZMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AZM_LETZTERUSER" "~"
if joinKuerzel = BMUAZM
searchResult := searchResult "LEFT JOIN BUERO.AZM_ARBEITSZEITMODELL ON AZM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BATBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BAT_LETZTERUSER" "~"
if joinKuerzel = BMUBAT
searchResult := searchResult "LEFT JOIN BMD.BAT_BMDATTACHMENT ON BAT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BBKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BBK_LETZTERUSER" "~"
if joinKuerzel = BMUBBK
searchResult := searchResult "LEFT JOIN HRS.BBK_BUAK_BETRIEBSKZ ON BBK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BEMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BEM_LETZTERUSER" "~"
if joinKuerzel = BMUBEM
searchResult := searchResult "LEFT JOIN BMD.BEM_BMDEMAIL ON BEM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BEWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BEW_LETZTERUSER" "~"
if joinKuerzel = BMUBEW
searchResult := searchResult "LEFT JOIN BUERO.BEW_BEWERTUNGSMODELL ON BEW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BEZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BEZ_LETZTERUSER" "~"
if joinKuerzel = BMUBEZ
searchResult := searchResult "LEFT JOIN FIBU.BEZ_BANKEINZUG ON BEZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BGUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BGU_LETZTERUSER" "~"
if joinKuerzel = BMUBGU
searchResult := searchResult "LEFT JOIN HRS.BGU_BEITRAGSGRUPPEIMPZU ON BGU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BKEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BKE_LETZTERUSER" "~"
if joinKuerzel = BMUBKE
searchResult := searchResult "LEFT JOIN FIBU.BKE_BANKRVKOREERF ON BKE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BKRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BKR_LETZTERUSER" "~"
if joinKuerzel = BMUBKR
searchResult := searchResult "LEFT JOIN FIBU.BKR_BANKEINZUGSKREIS ON BKR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BKSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BKS_LETZTERUSER" "~"
if joinKuerzel = BMUBKS
searchResult := searchResult "LEFT JOIN FIBU.BKS_BUCHUNGSKONTROLLSUMME ON BKS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BLCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BLC_LETZTERUSER" "~"
if joinKuerzel = BMUBLC
searchResult := searchResult "LEFT JOIN BMD.BLC_BMDLOCK ON BLC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BLTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BLT_LETZTERUSER" "~"
if joinKuerzel = BMUBLT
searchResult := searchResult "LEFT JOIN BIL.BLT_BILANZLANGTEXT ON BLT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BONBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BON_LETZTERUSER" "~"
if joinKuerzel = BMUBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BOZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BOZ_LETZTERUSER" "~"
if joinKuerzel = BMUBOZ
searchResult := searchResult "LEFT JOIN WWS.BOZ_BONZAHLUNG ON BOZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BPBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BPB_LETZTERUSER" "~"
if joinKuerzel = BMUBPB
searchResult := searchResult "LEFT JOIN WWS.BPB_LIEFBESTAET ON BPB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BPTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BPT_LETZTERUSER" "~"
if joinKuerzel = BMUBPT
searchResult := searchResult "LEFT JOIN BUERO.BPT_BESPRECHPROTOKOLL ON BPT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BRABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BRA_LETZTERUSER" "~"
if joinKuerzel = BMUBRA
searchResult := searchResult "LEFT JOIN BUERO.BRA_BRANCHE ON BRA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BREBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BRE_LETZTERUSER" "~"
if joinKuerzel = BMUBRE
searchResult := searchResult "LEFT JOIN HRS.BRE_BRUTTOERFASSUNG ON BRE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BRZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BRZ_LETZTERUSER" "~"
if joinKuerzel = BMUBRZ
searchResult := searchResult "LEFT JOIN BUERO.BRZ_BUEROBERECHTZU ON BRZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BSDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BSD_LETZTERUSER" "~"
if joinKuerzel = BMUBSD
searchResult := searchResult "LEFT JOIN WWS.BSD_BONUSERMDETAIL ON BSD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BSPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BSP_LETZTERUSER" "~"
if joinKuerzel = BMUBSP
searchResult := searchResult "LEFT JOIN WWS.BSP_BONUSERMPOS ON BSP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BSTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BST_LETZTERUSER" "~"
if joinKuerzel = BMUBST
searchResult := searchResult "LEFT JOIN BUERO.BST_SONDERTAGE ON BST_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BTP_LETZTERUSER" "~"
if joinKuerzel = BMUBTP
searchResult := searchResult "LEFT JOIN FIBU.BTP_BUDGETTILGUNGSPLAN ON BTP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BTR_LETZTERUSER" "~"
if joinKuerzel = BMUBTR
searchResult := searchResult "LEFT JOIN BUERO.BTR_BETREFF ON BTR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BTT_LETZTERUSER" "~"
if joinKuerzel = BMUBTT
searchResult := searchResult "LEFT JOIN HRS.BTT_BRUTTOERF_TMP ON BTT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BTZ_LETZTERUSER" "~"
if joinKuerzel = BMUBTZ
searchResult := searchResult "LEFT JOIN BUERO.BTZ_BTERMINALZUORD ON BTZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BWHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BWH_LETZTERUSER" "~"
if joinKuerzel = BMUBWH
searchResult := searchResult "LEFT JOIN BUERO.BWH_BEWERBERHISTORIE ON BWH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BZBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BZB_LETZTERUSER" "~"
if joinKuerzel = BMUBZB
searchResult := searchResult "LEFT JOIN PRODUKTION.BZB_BAZZEITBUCHUNG ON BZB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FDBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FDB_LETZTERUSER" "~"
if joinKuerzel = BMUFDB
searchResult := searchResult "LEFT JOIN FIBU.FDB_FIBUDASHBOARD ON FDB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FFOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FFO_LETZTERUSER" "~"
if joinKuerzel = BMUFFO
searchResult := searchResult "LEFT JOIN BMD.FFO_FUNKFIRMENRECHTE_OPT ON FFO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FFRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FFR_LETZTERUSER" "~"
if joinKuerzel = BMUFFR
searchResult := searchResult "LEFT JOIN BMD.FFR_FUNKFIRMENRECHTE ON FFR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FFZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FFZ_LETZTERUSER" "~"
if joinKuerzel = BMUFFZ
searchResult := searchResult "LEFT JOIN BUERO.FFZ_FRAGEBOGENFRAGEZU ON FFZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FGEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FGE_LETZTERUSER" "~"
if joinKuerzel = BMUFGE
searchResult := searchResult "LEFT JOIN BUERO.FGE_FRAGE ON FGE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FRXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FRX_LETZTERUSER" "~"
if joinKuerzel = BMUFRX
searchResult := searchResult "LEFT JOIN BMDENTW.FRX_FRAMES ON FRX_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FVWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FVW_LETZTERUSER" "~"
if joinKuerzel = BMUFVW
searchResult := searchResult "LEFT JOIN BMD.FVW_FELDVORGABEWERTE ON FVW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FWKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FWK_LETZTERUSER" "~"
if joinKuerzel = BMUFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FZEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FZE_LETZTERUSER" "~"
if joinKuerzel = BMUFZE
searchResult := searchResult "LEFT JOIN FIBU.FZE_ZAHLSCHLUESSELEINGABE ON FZE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GAUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GAU_LETZTERUSER" "~"
if joinKuerzel = BMUGAU
searchResult := searchResult "LEFT JOIN BIL.GAU_GEWINNAUFTEILUNG ON GAU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GAWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GAW_LETZTERUSER" "~"
if joinKuerzel = BMUGAW
searchResult := searchResult "LEFT JOIN ZLE.GAW_GEBUEHRARTIKELWERT ON GAW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GEZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GEZ_LETZTERUSER" "~"
if joinKuerzel = BMUGEZ
searchResult := searchResult "LEFT JOIN ZLE.GEZ_GEBUEHRENZUSATZ ON GEZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GIDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GID_LETZTERUSER" "~"
if joinKuerzel = BMUGID
searchResult := searchResult "LEFT JOIN BUERO.GID_GELDIDENTIFIKATION ON GID_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GRTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GRT_LETZTERUSER" "~"
if joinKuerzel = BMUGRT
searchResult := searchResult "LEFT JOIN WWS.GRT_GROESSENTAB ON GRT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GSTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GST_LETZTERUSER" "~"
if joinKuerzel = BMUGST
searchResult := searchResult "LEFT JOIN BUERO.GST_GEBUEHRENSTUFE ON GST_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GUDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GUD_LETZTERUSER" "~"
if joinKuerzel = BMUGUD
searchResult := searchResult "LEFT JOIN WWSINDIV.GUD_GUTHABEN_IFA_DETAIL ON GUD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GWIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GWI_LETZTERUSER" "~"
if joinKuerzel = BMUGWI
searchResult := searchResult "LEFT JOIN BUERO.GWI_GELDWIEREG ON GWI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HAD_LETZTERUSER" "~"
if joinKuerzel = BMUHAD
searchResult := searchResult "LEFT JOIN HRS.HAD_AUSDRUCKDETAIL ON HAD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HAZ_LETZTERUSER" "~"
if joinKuerzel = BMUHAZ
searchResult := searchResult "LEFT JOIN WWS.HAZ_HAUPTLIEFARTIKELZU ON HAZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HISBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HIS_LETZTERUSER" "~"
if joinKuerzel = BMUHIS
searchResult := searchResult "LEFT JOIN HRS.HIS_HISTORIE ON HIS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HPTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HPT_LETZTERUSER" "~"
if joinKuerzel = BMUHPT
searchResult := searchResult "LEFT JOIN ZLE.HPT_HONORARPOSTEXTE ON HPT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HTKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HTK_LETZTERUSER" "~"
if joinKuerzel = BMUHTK
searchResult := searchResult "LEFT JOIN BUERO.HTK_HILFSTAB_KONTAKTKONTR ON HTK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IFRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IFR_LETZTERUSER" "~"
if joinKuerzel = BMUIFR
searchResult := searchResult "LEFT JOIN HRS.IFR_IMPORTFELDREFERENZ ON IFR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = INLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = INL_LETZTERUSER" "~"
if joinKuerzel = BMUINL
searchResult := searchResult "LEFT JOIN ZLE.INL_INTERNLEISTUNG ON INL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IPMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IPM_LETZTERUSER" "~"
if joinKuerzel = BMUIPM
searchResult := searchResult "LEFT JOIN HRS.IPM_INDIVPROGM ON IPM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IPUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IPU_LETZTERUSER" "~"
if joinKuerzel = BMUIPU
searchResult := searchResult "LEFT JOIN HRS.IPU_IMPORTPUFFER ON IPU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TLTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TLT_LETZTERUSER" "~"
if joinKuerzel = BMUTLT
searchResult := searchResult "LEFT JOIN PRODUKTION.TLT_BT_LOGISTIK_TRANSPORT ON TLT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TMLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TML_LETZTERUSER" "~"
if joinKuerzel = BMUTML
searchResult := searchResult "LEFT JOIN HRS.TML_TERMINISIERTELOHNART ON TML_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TMSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TMS_LETZTERUSER" "~"
if joinKuerzel = BMUTMS
searchResult := searchResult "LEFT JOIN BUERO.TMS_TMSICHERUNG ON TMS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TPEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TPE_LETZTERUSER" "~"
if joinKuerzel = BMUTPE
searchResult := searchResult "LEFT JOIN BUERO.TPE_TEMPPERSON_TMP ON TPE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TPOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TPO_LETZTERUSER" "~"
if joinKuerzel = BMUTPO
searchResult := searchResult "LEFT JOIN BUERO.TPO_TERMINPOSTZU ON TPO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TPRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TPR_LETZTERUSER" "~"
if joinKuerzel = BMUTPR
searchResult := searchResult "LEFT JOIN BUERO.TPR_TELEFONPROTOKOLL ON TPR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TSKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TSK_LETZTERUSER" "~"
if joinKuerzel = BMUTSK
searchResult := searchResult "LEFT JOIN WWS.TSK_TOUCHSCREENKOPF ON TSK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UAZ_LETZTERUSER" "~"
if joinKuerzel = BMUUAZ
searchResult := searchResult "LEFT JOIN BMDENTW.UAZ_USERLINKAUSLIEFERNZU ON UAZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UESBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UES_LETZTERUSER" "~"
if joinKuerzel = BMUUES
searchResult := searchResult "LEFT JOIN BMD.UES_UEBERSETZUNG ON UES_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UEVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UEV_LETZTERUSER" "~"
if joinKuerzel = BMUUEV
searchResult := searchResult "LEFT JOIN BIL.UEV_STEUERUEBERGVERLUSTE ON UEV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UPLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UPL_LETZTERUSER" "~"
if joinKuerzel = BMUUPL
searchResult := searchResult "LEFT JOIN BMDENTW.UPL_UPDATEHINWEIS_NLS ON UPL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VATBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VAT_LETZTERUSER" "~"
if joinKuerzel = BMUVAT
searchResult := searchResult "LEFT JOIN BUERO.VAT_TEILNAHME ON VAT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VLTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VLT_LETZTERUSER" "~"
if joinKuerzel = BMUVLT
searchResult := searchResult "LEFT JOIN WWS.VLT_VA_LIEFERSCHEIN ON VLT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VPGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VPG_LETZTERUSER" "~"
if joinKuerzel = BMUVPG
searchResult := searchResult "LEFT JOIN WWS.VPG_VPERMITTLUNG_EFKZUORD ON VPG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VRLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VRL_LETZTERUSER" "~"
if joinKuerzel = BMUVRL
searchResult := searchResult "LEFT JOIN WWS.VRL_VA_PWLIEFARTIKEL ON VRL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VSRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VSR_LETZTERUSER" "~"
if joinKuerzel = BMUVSR
searchResult := searchResult "LEFT JOIN FIBU.VSR_VSTERSTATTUNG ON VSR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VSTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VST_LETZTERUSER" "~"
if joinKuerzel = BMUVST
searchResult := searchResult "LEFT JOIN WWS.VST_VERKAUFSSTATISTIK ON VST_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WAEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAE_LETZTERUSER" "~"
if joinKuerzel = BMUWAE
searchResult := searchResult "LEFT JOIN BMD.WAE_WAEHRUNG ON WAE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WCPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WCP_LETZTERUSER" "~"
if joinKuerzel = BMUWCP
searchResult := searchResult "LEFT JOIN BMD.WCP_WAAUSDRUCKPARAMETER ON WCP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEB_LETZTERUSER" "~"
if joinKuerzel = BMUWEB
searchResult := searchResult "LEFT JOIN WWS.WEB_WARENEINSATZBUCH ON WEB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WETBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WET_LETZTERUSER" "~"
if joinKuerzel = BMUWET
searchResult := searchResult "LEFT JOIN WWS.WET_WARENEINSATZARTZU ON WET_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WFIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WFI_LETZTERUSER" "~"
if joinKuerzel = BMUWFI
searchResult := searchResult "LEFT JOIN WEB.WFI_WEBFIRMA ON WFI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WFNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WFN_LETZTERUSER" "~"
if joinKuerzel = BMUWFN
searchResult := searchResult "LEFT JOIN BUERO.WFN_WORKFLOWACTION ON WFN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WKEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WKE_LETZTERUSER" "~"
if joinKuerzel = BMUWKE
searchResult := searchResult "LEFT JOIN WWS.WKE_WARENAUSGKOSTENERF ON WKE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WKPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WKP_LETZTERUSER" "~"
if joinKuerzel = BMUWKP
searchResult := searchResult "LEFT JOIN WWS.WKP_WWSKONTENTABPOS ON WKP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WKZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WKZ_LETZTERUSER" "~"
if joinKuerzel = BMUWKZ
searchResult := searchResult "LEFT JOIN WWS.WKZ_WFKKUNDELIEFERANTZU ON WKZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GUTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GUT_LETZTERUSER" "~"
if joinKuerzel = BMUGUT
searchResult := searchResult "LEFT JOIN WWSINDIV.GUT_GUTHABEN_IFA ON GUT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GVTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GVT_LETZTERUSER" "~"
if joinKuerzel = BMUGVT
searchResult := searchResult "LEFT JOIN HRS.GVT_GEMVERTEILUNG ON GVT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HFTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HFT_LETZTERUSER" "~"
if joinKuerzel = BMUHFT
searchResult := searchResult "LEFT JOIN BUERO.HFT_HILFSTABELLE ON HFT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HLLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HLL_LETZTERUSER" "~"
if joinKuerzel = BMUHLL
searchResult := searchResult "LEFT JOIN BMDENTW.HLL_HILFEDOKUMENT ON HLL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HSOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HSO_LETZTERUSER" "~"
if joinKuerzel = BMUHSO
searchResult := searchResult "LEFT JOIN HRS.HSO_SORTIERPOS ON HSO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HVGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HVG_LETZTERUSER" "~"
if joinKuerzel = BMUHVG
searchResult := searchResult "LEFT JOIN ZLE.HVG_HONVORGRUPPE ON HVG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IAMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IAM_LETZTERUSER" "~"
if joinKuerzel = BMUIAM
searchResult := searchResult "LEFT JOIN BUERO.IAM_INSTANZAKTIONMITARB ON IAM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IARBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IAR_LETZTERUSER" "~"
if joinKuerzel = BMUIAR
searchResult := searchResult "LEFT JOIN BMDINTERN.IAR_IDEEART ON IAR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IDBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IDB_LETZTERUSER" "~"
if joinKuerzel = BMUIDB
searchResult := searchResult "LEFT JOIN BMDINTERN.IDB_IDEEBEMERKUNG ON IDB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IDSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IDS_LETZTERUSER" "~"
if joinKuerzel = BMUIDS
searchResult := searchResult "LEFT JOIN BMDINTERN.IDS_IDEESTATUS ON IDS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IFABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IFA_LETZTERUSER" "~"
if joinKuerzel = BMUIFA
searchResult := searchResult "LEFT JOIN WWSINDIV.IFA_FAKTURA ON IFA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ISZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ISZ_LETZTERUSER" "~"
if joinKuerzel = BMUISZ
searchResult := searchResult "LEFT JOIN BUERO.ISZ_INFOTABSTELLEZU ON ISZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IZSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IZS_LETZTERUSER" "~"
if joinKuerzel = BMUIZS
searchResult := searchResult "LEFT JOIN BIL.IZS_INVZUSUMMEN ON IZS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = JAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = JAK_LETZTERUSER" "~"
if joinKuerzel = BMUJAK
searchResult := searchResult "LEFT JOIN BIL.JAK_JABKOPF ON JAK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KGSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KGS_LETZTERUSER" "~"
if joinKuerzel = BMUKGS
searchResult := searchResult "LEFT JOIN WWS.KGS_KATZU_ARBEITSGANG ON KGS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KKSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KKS_LETZTERUSER" "~"
if joinKuerzel = BMUKKS
searchResult := searchResult "LEFT JOIN BUERO.KKS_KONSOLIDIERUNGSKREIS ON KKS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KKZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KKZ_LETZTERUSER" "~"
if joinKuerzel = BMUKKZ
searchResult := searchResult "LEFT JOIN BUERO.KKZ_KREDITKARTENKUNDENZU ON KKZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KMABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KMA_LETZTERUSER" "~"
if joinKuerzel = BMUKMA
searchResult := searchResult "LEFT JOIN WWS.KMA_KUNDENMITARBBEZUG ON KMA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KOMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KOM_LETZTERUSER" "~"
if joinKuerzel = BMUKOM
searchResult := searchResult "LEFT JOIN PRODUKTION.KOM_KOMBI ON KOM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KPPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KPP_LETZTERUSER" "~"
if joinKuerzel = BMUKPP
searchResult := searchResult "LEFT JOIN BUERO.KPP_KAMPAGNENPHASE ON KPP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KSEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KSE_LETZTERUSER" "~"
if joinKuerzel = BMUKSE
searchResult := searchResult "LEFT JOIN KORE.KSE_KORESCHLUESSELEINGABE ON KSE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KSUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KSU_LETZTERUSER" "~"
if joinKuerzel = BMUKSU
searchResult := searchResult "LEFT JOIN WWS.KSU_KASSENSUMMENKOPF ON KSU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KTABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KTA_LETZTERUSER" "~"
if joinKuerzel = BMUKTA
searchResult := searchResult "LEFT JOIN WWS.KTA_KOSTENTYPENAUFTLG ON KTA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KTKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KTK_LETZTERUSER" "~"
if joinKuerzel = BMUKTK
searchResult := searchResult "LEFT JOIN FIBU.KTK_KONTENKREIS ON KTK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KVGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KVG_LETZTERUSER" "~"
if joinKuerzel = BMUKVG
searchResult := searchResult "LEFT JOIN KORE.KVG_KORESTAMMVERTEILUNG ON KVG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KZEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KZE_LETZTERUSER" "~"
if joinKuerzel = BMUKZE
searchResult := searchResult "LEFT JOIN KORE.KZE_ZUSCHLAGEINGABE ON KZE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ESMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ESM_LETZTERUSER" "~"
if joinKuerzel = BMUESM
searchResult := searchResult "LEFT JOIN HRS.ESM_ELDADETAIL_SM ON ESM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ETZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ETZ_LETZTERUSER" "~"
if joinKuerzel = BMUETZ
searchResult := searchResult "LEFT JOIN BUERO.ETZ_EVENTTERMINZU ON ETZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EVLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EVL_LETZTERUSER" "~"
if joinKuerzel = BMUEVL
searchResult := searchResult "LEFT JOIN BUERO.EVL_EVENTLIEFERANTZU ON EVL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EVTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EVT_LETZTERUSER" "~"
if joinKuerzel = BMUEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EWLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EWL_LETZTERUSER" "~"
if joinKuerzel = BMUEWL
searchResult := searchResult "LEFT JOIN BUERO.EWL_EVENTWARTELISTE ON EWL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FBKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FBK_LETZTERUSER" "~"
if joinKuerzel = BMUFBK
searchResult := searchResult "LEFT JOIN BMD.FBK_FELDBESCHKOPF ON FBK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FDOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FDO_LETZTERUSER" "~"
if joinKuerzel = BMUFDO
searchResult := searchResult "LEFT JOIN FIBU.FDO_FIBUDOKUMENTZU ON FDO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FEABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FEA_LETZTERUSER" "~"
if joinKuerzel = BMUFEA
searchResult := searchResult "LEFT JOIN WWS.FEA_FERTEP_AUFTEILUNG ON FEA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FORBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FOR_LETZTERUSER" "~"
if joinKuerzel = BMUFOR
searchResult := searchResult "LEFT JOIN BMD.FOR_FORMEL ON FOR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FOTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FOT_LETZTERUSER" "~"
if joinKuerzel = BMUFOT
searchResult := searchResult "LEFT JOIN BUERO.FOT_FOTO ON FOT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FPKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FPK_LETZTERUSER" "~"
if joinKuerzel = BMUFPK
searchResult := searchResult "LEFT JOIN WWSINDIV.FPK_PROZENTSATZKONTEN ON FPK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FWEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FWE_LETZTERUSER" "~"
if joinKuerzel = BMUFWE
searchResult := searchResult "LEFT JOIN HRS.FWE_FIXWERTE ON FWE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GBKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GBK_LETZTERUSER" "~"
if joinKuerzel = BMUGBK
searchResult := searchResult "LEFT JOIN HRS.GBK_GEBIETEKV ON GBK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GKOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GKO_LETZTERUSER" "~"
if joinKuerzel = BMUGKO
searchResult := searchResult "LEFT JOIN FIBU.GKO_GESELLSCHAFTERKONTEN ON GKO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GLZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GLZ_LETZTERUSER" "~"
if joinKuerzel = BMUGLZ
searchResult := searchResult "LEFT JOIN BIL.GLZ_GLIEDERUNGSZAHL ON GLZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GSKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GSK_LETZTERUSER" "~"
if joinKuerzel = BMUGSK
searchResult := searchResult "LEFT JOIN WWS.GSK_GUTSCHEINKOPF ON GSK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GTPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GTP_LETZTERUSER" "~"
if joinKuerzel = BMUGTP
searchResult := searchResult "LEFT JOIN WWS.GTP_GROESSENTABPOS ON GTP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GWBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GWB_LETZTERUSER" "~"
if joinKuerzel = BMUGWB
searchResult := searchResult "LEFT JOIN HRS.GWB_GWBERECHNUNG ON GWB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GWKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GWK_LETZTERUSER" "~"
if joinKuerzel = BMUGWK
searchResult := searchResult "LEFT JOIN BUERO.GWK_GEWERKSCHAFT ON GWK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GWPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GWP_LETZTERUSER" "~"
if joinKuerzel = BMUGWP
searchResult := searchResult "LEFT JOIN BMD.GWP_GROUPWISEPOSTOFFICE ON GWP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GZGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GZG_LETZTERUSER" "~"
if joinKuerzel = BMUGZG
searchResult := searchResult "LEFT JOIN WWS.GZG_GELDZAEHLUNG ON GZG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GZUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GZU_LETZTERUSER" "~"
if joinKuerzel = BMUGZU
searchResult := searchResult "LEFT JOIN WWS.GZU_GEFAHRENGUTZU ON GZU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HKTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HKT_LETZTERUSER" "~"
if joinKuerzel = BMUHKT
searchResult := searchResult "LEFT JOIN BMDENTW.HKT_HILFEKAPITELTBZU ON HKT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HSPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HSP_LETZTERUSER" "~"
if joinKuerzel = BMUHSP
searchResult := searchResult "LEFT JOIN HRS.HSP_SPALTENKOPF ON HSP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IFTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IFT_LETZTERUSER" "~"
if joinKuerzel = BMUIFT
searchResult := searchResult "LEFT JOIN BUERO.IFT_INFOTAB ON IFT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IFZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IFZ_LETZTERUSER" "~"
if joinKuerzel = BMUIFZ
searchResult := searchResult "LEFT JOIN HRS.IFZ_IMPORTFELDREFERENZZU ON IFZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IMDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IMD_LETZTERUSER" "~"
if joinKuerzel = BMUIMD
searchResult := searchResult "LEFT JOIN BUERO.IMD_INSERATMEDIUMDETAIL ON IMD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STK_LETZTERUSER" "~"
if joinKuerzel = BMUSTK
searchResult := searchResult "LEFT JOIN ZLE.STK_STAFFELKOPF ON STK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STS_LETZTERUSER" "~"
if joinKuerzel = BMUSTS
searchResult := searchResult "LEFT JOIN BIL.STS_STEUERSTAMMDAT ON STS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SZUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SZU_LETZTERUSER" "~"
if joinKuerzel = BMUSZU
searchResult := searchResult "LEFT JOIN HRS.SZU_SCHNITTLAZU ON SZU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TCTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TCT_LETZTERUSER" "~"
if joinKuerzel = BMUTCT
searchResult := searchResult "LEFT JOIN BMD.TCT_TCTESTCASE ON TCT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TCZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TCZ_LETZTERUSER" "~"
if joinKuerzel = BMUTCZ
searchResult := searchResult "LEFT JOIN BMD.TCZ_TCVERSIONCASEALZU ON TCZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TEDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TED_LETZTERUSER" "~"
if joinKuerzel = BMUTED
searchResult := searchResult "LEFT JOIN BUERO.TED_TERMINDOKUMENTZU ON TED_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TFPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TFP_LETZTERUSER" "~"
if joinKuerzel = BMUTFP
searchResult := searchResult "LEFT JOIN FIBU.TFP_FIBUPERSON_TMP ON TFP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TLEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TLE_LETZTERUSER" "~"
if joinKuerzel = BMUTLE
searchResult := searchResult "LEFT JOIN PRODUKTION.TLE_BT_LOGISTIK_EMPFANGEN ON TLE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TMDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TMD_LETZTERUSER" "~"
if joinKuerzel = BMUTMD
searchResult := searchResult "LEFT JOIN HRS.TMD_TERMINSTAMMDATEN ON TMD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TPJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TPJ_LETZTERUSER" "~"
if joinKuerzel = BMUTPJ
searchResult := searchResult "LEFT JOIN BUERO.TPJ_TEMPPROJEKT_TMP ON TPJ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TPZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TPZ_LETZTERUSER" "~"
if joinKuerzel = BMUTPZ
searchResult := searchResult "LEFT JOIN ZLE.TPZ_TAETIGKEITPAUSCHALEZU ON TPZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TRPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TRP_LETZTERUSER" "~"
if joinKuerzel = BMUTRP
searchResult := searchResult "LEFT JOIN WWS.TRP_TRANSPORTPOSITION ON TRP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TRZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TRZ_LETZTERUSER" "~"
if joinKuerzel = BMUTRZ
searchResult := searchResult "LEFT JOIN BUERO.TRZ_TERMINRESSOURCENZU ON TRZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TTGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TTG_LETZTERUSER" "~"
if joinKuerzel = BMUTTG
searchResult := searchResult "LEFT JOIN HRS.TTG_TARIFGR_ERGAENZUNG_ZU ON TTG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TZMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TZM_LETZTERUSER" "~"
if joinKuerzel = BMUTZM
searchResult := searchResult "LEFT JOIN BUERO.TZM_TERMINALZUORDMASTER ON TZM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UMABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UMA_LETZTERUSER" "~"
if joinKuerzel = BMUUMA
searchResult := searchResult "LEFT JOIN BMD.UMA_USERMATCHCODE ON UMA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UPKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UPK_LETZTERUSER" "~"
if joinKuerzel = BMUUPK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.UPK_UPH_KUNDE ON UPK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UZLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UZL_LETZTERUSER" "~"
if joinKuerzel = BMUUZL
searchResult := searchResult "LEFT JOIN BUERO.UZL_UNTERZIEL ON UZL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VAHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VAH_LETZTERUSER" "~"
if joinKuerzel = BMUVAH
searchResult := searchResult "LEFT JOIN BUERO.VAH_VERANSTABHAENGIG ON VAH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VAZ_LETZTERUSER" "~"
if joinKuerzel = BMUVAZ
searchResult := searchResult "LEFT JOIN WWS.VAZ_VERTABRZAHLUNGEN ON VAZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VCZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VCZ_LETZTERUSER" "~"
if joinKuerzel = BMUVCZ
searchResult := searchResult "LEFT JOIN BIL.VCZ_VIEW_ANLAGEGUTSACHKONTOZU ON VCZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VETBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VET_LETZTERUSER" "~"
if joinKuerzel = BMUVET
searchResult := searchResult "LEFT JOIN BUERO.VET_VERANSTALTUNGSTAG ON VET_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VEUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VEU_LETZTERUSER" "~"
if joinKuerzel = BMUVEU
searchResult := searchResult "LEFT JOIN FIBU.VEU_VORERFASSPROZAUFTEIL ON VEU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VJLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VJL_LETZTERUSER" "~"
if joinKuerzel = BMUVJL
searchResult := searchResult "LEFT JOIN WWS.VJL_VA_LAGERJOURNAL ON VJL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VOPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VOP_LETZTERUSER" "~"
if joinKuerzel = BMUVOP
searchResult := searchResult "LEFT JOIN PRODUKTION.VOP_VOB_PERSON_ZU ON VOP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VRSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VRS_LETZTERUSER" "~"
if joinKuerzel = BMUVRS
searchResult := searchResult "LEFT JOIN WWS.VRS_VERTABRRECHPOSSTORNO ON VRS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SFRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SFR_LETZTERUSER" "~"
if joinKuerzel = BMUSFR
searchResult := searchResult "LEFT JOIN BUERO.SFR_SKILLFINDERREGEL ON SFR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SFTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SFT_LETZTERUSER" "~"
if joinKuerzel = BMUSFT
searchResult := searchResult "LEFT JOIN BUERO.SFT_SKILLFINDER_TMP ON SFT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SLKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SLK_LETZTERUSER" "~"
if joinKuerzel = BMUSLK
searchResult := searchResult "LEFT JOIN WWS.SLK_STUECKLISTENKOPF ON SLK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SMIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SMI_LETZTERUSER" "~"
if joinKuerzel = BMUSMI
searchResult := searchResult "LEFT JOIN ZLE.SMI_SMARTINTERFACE ON SMI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SNRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SNR_LETZTERUSER" "~"
if joinKuerzel = BMUSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SOZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SOZ_LETZTERUSER" "~"
if joinKuerzel = BMUSOZ
searchResult := searchResult "LEFT JOIN BUERO.SOZ_SOZIAL ON SOZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPW_LETZTERUSER" "~"
if joinKuerzel = BMUSPW
searchResult := searchResult "LEFT JOIN BIL.SPW_SPIEGELWERTE ON SPW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SRTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SRT_LETZTERUSER" "~"
if joinKuerzel = BMUSRT
searchResult := searchResult "LEFT JOIN WWS.SRT_STEUERTAB ON SRT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SSUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SSU_LETZTERUSER" "~"
if joinKuerzel = BMUSSU
searchResult := searchResult "LEFT JOIN BIL.SSU_STEUERSACHKONTOZU ON SSU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STA_LETZTERUSER" "~"
if joinKuerzel = BMUSTA
searchResult := searchResult "LEFT JOIN BUERO.STA_STELLENAUSBILDUNGEN ON STA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STB_LETZTERUSER" "~"
if joinKuerzel = BMUSTB
searchResult := searchResult "LEFT JOIN PRODUKTION.STB_STAKE_HEAD ON STB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STM_LETZTERUSER" "~"
if joinKuerzel = BMUSTM
searchResult := searchResult "LEFT JOIN BUERO.STM_STELLENMERKMALE ON STM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STO_LETZTERUSER" "~"
if joinKuerzel = BMUSTO
searchResult := searchResult "LEFT JOIN PRODUKTION.STO_STAKE_TYPORDER ON STO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SVKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SVK_LETZTERUSER" "~"
if joinKuerzel = BMUSVK
searchResult := searchResult "LEFT JOIN FIBU.SVK_SKONTOVERBUCHKZ ON SVK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SYSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SYS_LETZTERUSER" "~"
if joinKuerzel = BMUSYS
searchResult := searchResult "LEFT JOIN BMD.SYS_BMDSYSTEM ON SYS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TAPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TAP_LETZTERUSER" "~"
if joinKuerzel = BMUTAP
searchResult := searchResult "LEFT JOIN ZLE.TAP_TAGESABPROTOKOLL ON TAP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TBGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TBG_LETZTERUSER" "~"
if joinKuerzel = BMUTBG
searchResult := searchResult "LEFT JOIN HRS.TBG_ELDADETAIL_BGN_TMP ON TBG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TBUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TBU_LETZTERUSER" "~"
if joinKuerzel = BMUTBU
searchResult := searchResult "LEFT JOIN FIBU.TBU_BUCHUNG_TMP ON TBU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TERBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TER_LETZTERUSER" "~"
if joinKuerzel = BMUTER
searchResult := searchResult "LEFT JOIN BUERO.TER_TERMIN ON TER_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TEZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TEZ_LETZTERUSER" "~"
if joinKuerzel = BMUTEZ
searchResult := searchResult "LEFT JOIN WWS.TEZ_ERFASSKOPFZU_TMP ON TEZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TLABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TLA_LETZTERUSER" "~"
if joinKuerzel = BMUTLA
searchResult := searchResult "LEFT JOIN PRODUKTION.TLA_BT_LOGISTIK_ANGEFORDERT ON TLA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TLUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TLU_LETZTERUSER" "~"
if joinKuerzel = BMUTLU
searchResult := searchResult "LEFT JOIN PRODUKTION.TLU_BT_LOGISTIK_ZU ON TLU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TOUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TOU_LETZTERUSER" "~"
if joinKuerzel = BMUTOU
searchResult := searchResult "LEFT JOIN WWS.TOU_TOUR ON TOU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TOZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TOZ_LETZTERUSER" "~"
if joinKuerzel = BMUTOZ
searchResult := searchResult "LEFT JOIN WWS.TOZ_TOURZUORDNUNG ON TOZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TPDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TPD_LETZTERUSER" "~"
if joinKuerzel = BMUTPD
searchResult := searchResult "LEFT JOIN BUERO.TPD_TELPROTDOKUMENTZU ON TPD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TSSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TSS_LETZTERUSER" "~"
if joinKuerzel = BMUTSS
searchResult := searchResult "LEFT JOIN ZLE.TSS_TAGESABSOLLSTUNDEN ON TSS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ITVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ITV_LETZTERUSER" "~"
if joinKuerzel = BMUITV
searchResult := searchResult "LEFT JOIN BMD.ITV_INTERVALL ON ITV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IVPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IVP_LETZTERUSER" "~"
if joinKuerzel = BMUIVP
searchResult := searchResult "LEFT JOIN WWS.IVP_INVENTURPOSITION ON IVP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KAEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KAE_LETZTERUSER" "~"
if joinKuerzel = BMUKAE
searchResult := searchResult "LEFT JOIN WWS.KAE_KASSENEINAUSGABEN ON KAE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KAMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KAM_LETZTERUSER" "~"
if joinKuerzel = BMUKAM
searchResult := searchResult "LEFT JOIN KORE.KAM_AUSDRUCKMASTER ON KAM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KAUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KAU_LETZTERUSER" "~"
if joinKuerzel = BMUKAU
searchResult := searchResult "LEFT JOIN BIL.KAU_KONTOAUFGLIEDERUNG ON KAU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KDZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KDZ_LETZTERUSER" "~"
if joinKuerzel = BMUKDZ
searchResult := searchResult "LEFT JOIN BUERO.KDZ_KOREDOKUMENTZU ON KDZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KFZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KFZ_LETZTERUSER" "~"
if joinKuerzel = BMUKFZ
searchResult := searchResult "LEFT JOIN KORE.KFZ_KOREFIBUZU ON KFZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KIZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KIZ_LETZTERUSER" "~"
if joinKuerzel = BMUKIZ
searchResult := searchResult "LEFT JOIN BUERO.KIZ_KUND_INFOTABPOSZU ON KIZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KKVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KKV_LETZTERUSER" "~"
if joinKuerzel = BMUKKV
searchResult := searchResult "LEFT JOIN KORE.KKV_KSTKONTOVARIATOR ON KKV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KMPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KMP_LETZTERUSER" "~"
if joinKuerzel = BMUKMP
searchResult := searchResult "LEFT JOIN KORE.KMP_PLANMENGEN ON KMP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KOCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KOC_LETZTERUSER" "~"
if joinKuerzel = BMUKOC
searchResult := searchResult "LEFT JOIN PRODUKTION.KOC_KOMBI_CHARGE ON KOC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KOSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KOS_LETZTERUSER" "~"
if joinKuerzel = BMUKOS
searchResult := searchResult "LEFT JOIN ZLE.KOS_KOSTENFAKTOR ON KOS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KRABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KRA_LETZTERUSER" "~"
if joinKuerzel = BMUKRA
searchResult := searchResult "LEFT JOIN WWS.KRA_KASSENRABATT ON KRA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KRIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KRI_LETZTERUSER" "~"
if joinKuerzel = BMUKRI
searchResult := searchResult "LEFT JOIN BUERO.KRI_KRITERIUM ON KRI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KSVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KSV_LETZTERUSER" "~"
if joinKuerzel = BMUKSV
searchResult := searchResult "LEFT JOIN HRS.KSV_KOSTENVERTEILUNG ON KSV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KTOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KTO_LETZTERUSER" "~"
if joinKuerzel = BMUKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KZZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KZZ_LETZTERUSER" "~"
if joinKuerzel = BMUKZZ
searchResult := searchResult "LEFT JOIN KORE.KZZ_KSTKENNZAHLZU ON KZZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAK_LETZTERUSER" "~"
if joinKuerzel = BMULAK
searchResult := searchResult "LEFT JOIN HRS.LAK_LOHNARTENKOPPELUNG ON LAK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LANBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAN_LETZTERUSER" "~"
if joinKuerzel = BMULAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LEBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LEB_LETZTERUSER" "~"
if joinKuerzel = BMULEB
searchResult := searchResult "LEFT JOIN BUERO.LEB_LEBENSLAUF ON LEB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LEHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LEH_LETZTERUSER" "~"
if joinKuerzel = BMULEH
searchResult := searchResult "LEFT JOIN HRS.LEH_LOHNEINSTELLUNG_HIST ON LEH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LEIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LEI_LETZTERUSER" "~"
if joinKuerzel = BMULEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LFZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LFZ_LETZTERUSER" "~"
if joinKuerzel = BMULFZ
searchResult := searchResult "LEFT JOIN BMDDEFAULT.LFZ_DLIZG_NTC_ZU ON LFZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LGZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LGZ_LETZTERUSER" "~"
if joinKuerzel = BMULGZ
searchResult := searchResult "LEFT JOIN HRS.LGZ_LOHNARTGRUPPEZU ON LGZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LITBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LIT_LETZTERUSER" "~"
if joinKuerzel = BMULIT
searchResult := searchResult "LEFT JOIN HRS.LIT_LOHNINSTITUTION ON LIT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LKVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LKV_LETZTERUSER" "~"
if joinKuerzel = BMULKV
searchResult := searchResult "LEFT JOIN HRS.LKV_LOHNKOSTENVERTEILUNG ON LKV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DALBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DAL_LETZTERUSER" "~"
if joinKuerzel = BMUDAL
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DAL_DATTRLOCK ON DAL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DBEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DBE_LETZTERUSER" "~"
if joinKuerzel = BMUDBE
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DBE_DBERECHTIGUNG ON DBE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DCFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DCF_LETZTERUSER" "~"
if joinKuerzel = BMUDCF
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DCF_WABAUMELEMAUSDRUCKZU ON DCF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DDCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DDC_LETZTERUSER" "~"
if joinKuerzel = BMUDDC
searchResult := searchResult "LEFT JOIN BUERO.DDC_DELETEDDOCS ON DDC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DFRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DFR_LETZTERUSER" "~"
if joinKuerzel = BMUDFR
searchResult := searchResult "LEFT JOIN WWS.DFR_DOKUMENTFREIGABE ON DFR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DHZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DHZ_LETZTERUSER" "~"
if joinKuerzel = BMUDHZ
searchResult := searchResult "LEFT JOIN DATANORM.DHZ_DATANORM_WHGZU ON DHZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DIPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DIP_LETZTERUSER" "~"
if joinKuerzel = BMUDIP
searchResult := searchResult "LEFT JOIN BUERO.DIP_DIVERSE_PERSON ON DIP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DIZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DIZ_LETZTERUSER" "~"
if joinKuerzel = BMUDIZ
searchResult := searchResult "LEFT JOIN BUERO.DIZ_DOCSINSTZU ON DIZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DLGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DLG_LETZTERUSER" "~"
if joinKuerzel = BMUDLG
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DLG_DLIZENZGRUPPE ON DLG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DOUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DOU_LETZTERUSER" "~"
if joinKuerzel = BMUDOU
searchResult := searchResult "LEFT JOIN BMD.DOU_DATAOWNERUSERZU_TMP ON DOU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DREBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DRE_LETZTERUSER" "~"
if joinKuerzel = BMUDRE
searchResult := searchResult "LEFT JOIN HRS.DRE_ERWDRUCKEREINST ON DRE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DRKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DRK_LETZTERUSER" "~"
if joinKuerzel = BMUDRK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DRK_DTABREPLGRUPPE ON DRK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DSKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DSK_LETZTERUSER" "~"
if joinKuerzel = BMUDSK
searchResult := searchResult "LEFT JOIN BUERO.DSK_DSGVO_DATENKATEGORIE ON DSK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DTPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DTP_LETZTERUSER" "~"
if joinKuerzel = BMUDTP
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DTP_DEFTREEPOS ON DTP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DVHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DVH_LETZTERUSER" "~"
if joinKuerzel = BMUDVH
searchResult := searchResult "LEFT JOIN BUERO.DVH_DIENSTVERHAELTNIS ON DVH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DVKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DVK_LETZTERUSER" "~"
if joinKuerzel = BMUDVK
searchResult := searchResult "LEFT JOIN BUERO.DVK_DSGVO_VERZEICHNISKATZU ON DVK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DZUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DZU_LETZTERUSER" "~"
if joinKuerzel = BMUDZU
searchResult := searchResult "LEFT JOIN HRS.DZU_DOKZU_HRS ON DZU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EAOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EAO_LETZTERUSER" "~"
if joinKuerzel = BMUEAO
searchResult := searchResult "LEFT JOIN BMD.EAO_EXTARCHIVOP ON EAO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EAPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EAP_LETZTERUSER" "~"
if joinKuerzel = BMUEAP
searchResult := searchResult "LEFT JOIN WWS.EAP_ERLOESABGRENZUNGSPOS ON EAP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EDABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EDA_LETZTERUSER" "~"
if joinKuerzel = BMUEDA
searchResult := searchResult "LEFT JOIN HRS.EDA_ELDADETAIL_ADR ON EDA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EDZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EDZ_LETZTERUSER" "~"
if joinKuerzel = BMUEDZ
searchResult := searchResult "LEFT JOIN WWS.EDZ_ERFASSDOKUMENTZU ON EDZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EEPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EEP_LETZTERUSER" "~"
if joinKuerzel = BMUEEP
searchResult := searchResult "LEFT JOIN WWS.EEP_EDIEINHEITENPOS ON EEP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EFHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EFH_LETZTERUSER" "~"
if joinKuerzel = BMUEFH
searchResult := searchResult "LEFT JOIN HRS.EFH_ELDADETAIL_FH ON EFH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EGZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EGZ_LETZTERUSER" "~"
if joinKuerzel = BMUEGZ
searchResult := searchResult "LEFT JOIN HRS.EGZ_ERGAENZUNG ON EGZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EKJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EKJ_LETZTERUSER" "~"
if joinKuerzel = BMUEKJ
searchResult := searchResult "LEFT JOIN PRODUKTION.EKJ_ERP_KOREJOURNAL ON EKJ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EM5BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EM5_LETZTERUSER" "~"
if joinKuerzel = BMUEM5
searchResult := searchResult "LEFT JOIN HRS.EM5_ELDADETAIL_VP ON EM5_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WLZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WLZ_LETZTERUSER" "~"
if joinKuerzel = BMUWLZ
searchResult := searchResult "LEFT JOIN WWS.WLZ_WESLAJ_ZUORDNUNG ON WLZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WMDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WMD_LETZTERUSER" "~"
if joinKuerzel = BMUWMD
searchResult := searchResult "LEFT JOIN BUERO.WMD_WOCHENMODELL ON WMD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WMZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WMZ_LETZTERUSER" "~"
if joinKuerzel = BMUWMZ
searchResult := searchResult "LEFT JOIN BUERO.WMZ_WFAMITARBZU ON WMZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WQIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WQI_LETZTERUSER" "~"
if joinKuerzel = BMUWQI
searchResult := searchResult "LEFT JOIN BMDDEFAULT.WQI_WEITERE_INFO_ITEM ON WQI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WSNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WSN_LETZTERUSER" "~"
if joinKuerzel = BMUWSN
searchResult := searchResult "LEFT JOIN WWS.WSN_WWSSEQUENZNUMMERN ON WSN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WSSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WSS_LETZTERUSER" "~"
if joinKuerzel = BMUWSS
searchResult := searchResult "LEFT JOIN WWS.WSS_WWSSTATISTIK ON WSS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WWBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WWB_LETZTERUSER" "~"
if joinKuerzel = BMUWWB
searchResult := searchResult "LEFT JOIN WWS.WWB_WWSBERICHTE ON WWB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WWKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WWK_LETZTERUSER" "~"
if joinKuerzel = BMUWWK
searchResult := searchResult "LEFT JOIN WWS.WWK_WWSBERICHTKOPF ON WWK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = XBRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = XBR_LETZTERUSER" "~"
if joinKuerzel = BMUXBR
searchResult := searchResult "LEFT JOIN BIL.XBR_XBRLDATA ON XBR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = XCNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = XCN_LETZTERUSER" "~"
if joinKuerzel = BMUXCN
searchResult := searchResult "LEFT JOIN BIL.XCN_EXTCHECKLIST_NOTES ON XCN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = XSEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = XSE_LETZTERUSER" "~"
if joinKuerzel = BMUXSE
searchResult := searchResult "LEFT JOIN BUERO.XSE_XML_STEUERERKL ON XSE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = YBUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = YBU_LETZTERUSER" "~"
if joinKuerzel = BMUYBU
searchResult := searchResult "LEFT JOIN FIBU.YBU_AUTOBUCHUNG ON YBU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = YKEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = YKE_LETZTERUSER" "~"
if joinKuerzel = BMUYKE
searchResult := searchResult "LEFT JOIN FIBU.YKE_AUTOBUCHKOREERF ON YKE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZBEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZBE_LETZTERUSER" "~"
if joinKuerzel = BMUZBE
searchResult := searchResult "LEFT JOIN BUERO.ZBE_ZUSTAENDIGKEITBEREICH ON ZBE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZBUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZBU_LETZTERUSER" "~"
if joinKuerzel = BMUZBU
searchResult := searchResult "LEFT JOIN BUERO.ZBU_ZEITBUCHUNG ON ZBU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZESBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZES_LETZTERUSER" "~"
if joinKuerzel = BMUZES
searchResult := searchResult "LEFT JOIN FIBU.ZES_ZESSIONSTEXT ON ZES_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZKSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZKS_LETZTERUSER" "~"
if joinKuerzel = BMUZKS
searchResult := searchResult "LEFT JOIN FIBU.ZKS_ZINSBERECHNUNGSKREIS ON ZKS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZOEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZOE_LETZTERUSER" "~"
if joinKuerzel = BMUZOE
searchResult := searchResult "LEFT JOIN FIBU.ZOE_ZOLLBELEGERFASSUNG ON ZOE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZPTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZPT_LETZTERUSER" "~"
if joinKuerzel = BMUZPT
searchResult := searchResult "LEFT JOIN BUERO.ZPT_ZEITPLANTEXT ON ZPT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZSKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZSK_LETZTERUSER" "~"
if joinKuerzel = BMUZSK
searchResult := searchResult "LEFT JOIN ZLE.ZSK_ZEITSCHEIBEKOPF ON ZSK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZSWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZSW_LETZTERUSER" "~"
if joinKuerzel = BMUZSW
searchResult := searchResult "LEFT JOIN BUERO.ZSW_ZEITSUPERWE ON ZSW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZUZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZUZ_LETZTERUSER" "~"
if joinKuerzel = BMUZUZ
searchResult := searchResult "LEFT JOIN BUERO.ZUZ_ZEITUMBUCHZUORD ON ZUZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZZTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZZT_LETZTERUSER" "~"
if joinKuerzel = BMUZZT
searchResult := searchResult "LEFT JOIN BUERO.ZZT_ZEITZUORDNUNG ON ZZT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LAJKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_KOSTENSTAMMNR = LAJ_GBR_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_GBR_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = WAGVKG
searchResult := searchResult "LEFT JOIN KORE.VKG_VIEW_KOSTAMM_GBR ON VKG_FIRMENNR = WAG_FIRMENNR AND VKG_KOSTENSTAMMNR = WAG_GBR_KOSTENSTAMMNR" "~"
if joinKuerzel = VKGWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = VKG_FIRMENNR AND WAG_GBR_KOSTENSTAMMNR = VKG_KOSTENSTAMMNR" "~"
if joinKuerzel = ARLVKS
searchResult := searchResult "LEFT JOIN KORE.VKS_VIEW_KOSTAMM_KST ON VKS_FIRMENNR = ARL_FIRMENNR AND VKS_KOSTENSTAMMNR = ARL_KOSTENST_KOSTENSTAMMNR" "~"
if joinKuerzel = VKSARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = VKS_FIRMENNR AND ARL_KOSTENST_KOSTENSTAMMNR = VKS_KOSTENSTAMMNR" "~"
if joinKuerzel = LAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAZ_LETZTERUSER" "~"
if joinKuerzel = BMULAZ
searchResult := searchResult "LEFT JOIN WWS.LAZ_LIEFARTIKEL_ZUSATZ ON LAZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LBZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LBZ_LETZTERUSER" "~"
if joinKuerzel = BMULBZ
searchResult := searchResult "LEFT JOIN BIL.LBZ_LANGFRISTERST_BELEGZU ON LBZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LLDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LLD_LETZTERUSER" "~"
if joinKuerzel = BMULLD
searchResult := searchResult "LEFT JOIN ZLE.LLD_LEALOHNDATEN ON LLD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LMWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LMW_LETZTERUSER" "~"
if joinKuerzel = BMULMW
searchResult := searchResult "LEFT JOIN WWS.LMW_LAGERMONATSWERTE ON LMW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LOABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LOA_LETZTERUSER" "~"
if joinKuerzel = BMULOA
searchResult := searchResult "LEFT JOIN HRS.LOA_LOHNART ON LOA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LUEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LUE_LETZTERUSER" "~"
if joinKuerzel = BMULUE
searchResult := searchResult "LEFT JOIN PRODUKTION.LUE_LAGER_UMS_EMPFANGEN ON LUE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LUKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LUK_LETZTERUSER" "~"
if joinKuerzel = BMULUK
searchResult := searchResult "LEFT JOIN PRODUKTION.LUK_LAGER_UMS_KOPF ON LUK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LUPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LUP_LETZTERUSER" "~"
if joinKuerzel = BMULUP
searchResult := searchResult "LEFT JOIN PRODUKTION.LUP_LAGER_UMS_POS ON LUP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LVPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LVP_LETZTERUSER" "~"
if joinKuerzel = BMULVP
searchResult := searchResult "LEFT JOIN HRS.LVP_LVPERSSTAMM ON LVP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LVTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LVT_LETZTERUSER" "~"
if joinKuerzel = BMULVT
searchResult := searchResult "LEFT JOIN HRS.LVT_LOHNKOSTENVERTEILG_TMP ON LVT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LWPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LWP_LETZTERUSER" "~"
if joinKuerzel = BMULWP
searchResult := searchResult "LEFT JOIN FIBU.LWP_LANDWIRT_PRODUKTE ON LWP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LZUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LZU_LETZTERUSER" "~"
if joinKuerzel = BMULZU
searchResult := searchResult "LEFT JOIN HRS.LZU_LOHNZUSTAND ON LZU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MAK_LETZTERUSER" "~"
if joinKuerzel = BMUMAK
searchResult := searchResult "LEFT JOIN BMD.MAK_MAKROFUNKTIONSZU ON MAK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MBZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MBZ_LETZTERUSER" "~"
if joinKuerzel = BMUMBZ
searchResult := searchResult "LEFT JOIN BUERO.MBZ_MEDIENBETRIEBSZEIT ON MBZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MGBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MGB_LETZTERUSER" "~"
if joinKuerzel = BMUMGB
searchResult := searchResult "LEFT JOIN FIBU.MGB_MAHNGEBUEHR ON MGB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MIKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MIK_LETZTERUSER" "~"
if joinKuerzel = BMUMIK
searchResult := searchResult "LEFT JOIN BUERO.MIK_MITARBEITERKARRIERE ON MIK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MSGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MSG_LETZTERUSER" "~"
if joinKuerzel = BMUMSG
searchResult := searchResult "LEFT JOIN BMDENTW.MSG_MESSAGE ON MSG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MZZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MZZ_LETZTERUSER" "~"
if joinKuerzel = BMUMZZ
searchResult := searchResult "LEFT JOIN BUERO.MZZ_MITARBZIELZU ON MZZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NOGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NOG_LETZTERUSER" "~"
if joinKuerzel = BMUNOG
searchResult := searchResult "LEFT JOIN BUERO.NOG_NOTIZGRUPPE ON NOG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NOTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NOT_LETZTERUSER" "~"
if joinKuerzel = BMUNOT
searchResult := searchResult "LEFT JOIN BUERO.NOT_NOTIZ ON NOT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NUDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NUD_LETZTERUSER" "~"
if joinKuerzel = BMUNUD
searchResult := searchResult "LEFT JOIN BMD.NUD_NTCSUPDATEONLINEDATEI ON NUD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NUKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NUK_LETZTERUSER" "~"
if joinKuerzel = BMUNUK
searchResult := searchResult "LEFT JOIN BMD.NUK_NTCSUPDPRODUKT ON NUK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NUOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NUO_LETZTERUSER" "~"
if joinKuerzel = BMUNUO
searchResult := searchResult "LEFT JOIN BMD.NUO_NTCSUPDPRIORITAET ON NUO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OBBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OBB_LETZTERUSER" "~"
if joinKuerzel = BMUOBB
searchResult := searchResult "LEFT JOIN BUERO.OBB_ONLBEWBEWERBUNG ON OBB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OBZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OBZ_LETZTERUSER" "~"
if joinKuerzel = BMUOBZ
searchResult := searchResult "LEFT JOIN LOHN.OBZ_OF_BANKLEITZAHL ON OBZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OFRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OFR_LETZTERUSER" "~"
if joinKuerzel = BMUOFR
searchResult := searchResult "LEFT JOIN BUERO.OFR_FRAGEBOGENREGEL ON OFR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IOBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IOB_LETZTERUSER" "~"
if joinKuerzel = BMUIOB
searchResult := searchResult "LEFT JOIN BUERO.IOB_INSTANDHOBJEKT ON IOB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IOGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IOG_LETZTERUSER" "~"
if joinKuerzel = BMUIOG
searchResult := searchResult "LEFT JOIN BUERO.IOG_INSTANDHOBJEKTGRUPPE ON IOG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IPDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IPD_LETZTERUSER" "~"
if joinKuerzel = BMUIPD
searchResult := searchResult "LEFT JOIN HRS.IPD_INDIVPROGD ON IPD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IVGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IVG_LETZTERUSER" "~"
if joinKuerzel = BMUIVG
searchResult := searchResult "LEFT JOIN BMD.IVG_INTERVALLGRUPPE ON IVG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IVKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IVK_LETZTERUSER" "~"
if joinKuerzel = BMUIVK
searchResult := searchResult "LEFT JOIN WWS.IVK_INVENTURKOPF ON IVK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KAD_LETZTERUSER" "~"
if joinKuerzel = BMUKAD
searchResult := searchResult "LEFT JOIN KORE.KAD_AUSDRUCKDETAIL ON KAD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KAIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KAI_LETZTERUSER" "~"
if joinKuerzel = BMUKAI
searchResult := searchResult "LEFT JOIN WWS.KAI_KNDARTIKELINFO ON KAI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KAPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KAP_LETZTERUSER" "~"
if joinKuerzel = BMUKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KATBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KAT_LETZTERUSER" "~"
if joinKuerzel = BMUKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KFIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KFI_LETZTERUSER" "~"
if joinKuerzel = BMUKFI
searchResult := searchResult "LEFT JOIN BUERO.KFI_KORETYPENFINDUNG ON KFI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KKABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KKA_LETZTERUSER" "~"
if joinKuerzel = BMUKKA
searchResult := searchResult "LEFT JOIN BUERO.KKA_KREDITKARTENART ON KKA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KMDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KMD_LETZTERUSER" "~"
if joinKuerzel = BMUKMD
searchResult := searchResult "LEFT JOIN HRS.KMD_KOMMSTDETAIL ON KMD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KMZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KMZ_LETZTERUSER" "~"
if joinKuerzel = BMUKMZ
searchResult := searchResult "LEFT JOIN HRS.KMZ_KOMMSTZENTRALE ON KMZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KONBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KON_LETZTERUSER" "~"
if joinKuerzel = BMUKON
searchResult := searchResult "LEFT JOIN BUERO.KON_KONTAKTPERSON ON KON_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KPOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KPO_LETZTERUSER" "~"
if joinKuerzel = BMUKPO
searchResult := searchResult "LEFT JOIN WWS.KPO_KASSENPROVISION ON KPO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KRDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KRD_LETZTERUSER" "~"
if joinKuerzel = BMUKRD
searchResult := searchResult "LEFT JOIN HRS.KRD_KONTENREFERENZDETAIL ON KRD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KSDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KSD_LETZTERUSER" "~"
if joinKuerzel = BMUKSD
searchResult := searchResult "LEFT JOIN WWS.KSD_KASSENSUMMENDETAIL ON KSD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KSLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KSL_LETZTERUSER" "~"
if joinKuerzel = BMUKSL
searchResult := searchResult "LEFT JOIN BUERO.KSL_KONTAKTSYNCEINST ON KSL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KZKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KZK_LETZTERUSER" "~"
if joinKuerzel = BMUKZK
searchResult := searchResult "LEFT JOIN KORE.KZK_ZUSCHLAGKOPF ON KZK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LAGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAG_LETZTERUSER" "~"
if joinKuerzel = BMULAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LBDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LBD_LETZTERUSER" "~"
if joinKuerzel = BMULBD
searchResult := searchResult "LEFT JOIN WWS.LBD_LIEFBEWDATEN ON LBD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LENBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LEN_LETZTERUSER" "~"
if joinKuerzel = BMULEN
searchResult := searchResult "LEFT JOIN BUERO.LEN_LOHNEINSTELLUNGEN ON LEN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LISBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LIS_LETZTERUSER" "~"
if joinKuerzel = BMULIS
searchResult := searchResult "LEFT JOIN HRS.LIS_LOHNIMPORTSPERRE ON LIS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LKOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LKO_LETZTERUSER" "~"
if joinKuerzel = BMULKO
searchResult := searchResult "LEFT JOIN WWS.LKO_LAGERKONTO ON LKO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LMBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LMB_LETZTERUSER" "~"
if joinKuerzel = BMULMB
searchResult := searchResult "LEFT JOIN WWS.LMB_LAGERMINMAXBESTAND ON LMB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LNGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LNG_LETZTERUSER" "~"
if joinKuerzel = BMULNG
searchResult := searchResult "LEFT JOIN BMDENTW.LNG_LANGUAGE ON LNG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VSZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VSZ_LETZTERUSER" "~"
if joinKuerzel = BMUVSZ
searchResult := searchResult "LEFT JOIN BUERO.VSZ_AVSTAMMVORAUSSETZZU ON VSZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VTMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VTM_LETZTERUSER" "~"
if joinKuerzel = BMUVTM
searchResult := searchResult "LEFT JOIN BUERO.VTM_VA_TAGESMODELL ON VTM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VTXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VTX_LETZTERUSER" "~"
if joinKuerzel = BMUVTX
searchResult := searchResult "LEFT JOIN BUERO.VTX_VA_UESLANGTEXT ON VTX_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VZABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VZA_LETZTERUSER" "~"
if joinKuerzel = BMUVZA
searchResult := searchResult "LEFT JOIN HRS.VZA_VORZEITART ON VZA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAD_LETZTERUSER" "~"
if joinKuerzel = BMUWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WCEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WCE_LETZTERUSER" "~"
if joinKuerzel = BMUWCE
searchResult := searchResult "LEFT JOIN BMD.WCE_WABAUMELEMENTE ON WCE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WED_LETZTERUSER" "~"
if joinKuerzel = BMUWED
searchResult := searchResult "LEFT JOIN WWS.WED_WARENEING_POSDETAIL ON WED_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WFABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WFA_LETZTERUSER" "~"
if joinKuerzel = BMUWFA
searchResult := searchResult "LEFT JOIN WEB.WFA_WEBFARBEN ON WFA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WFFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WFF_LETZTERUSER" "~"
if joinKuerzel = BMUWFF
searchResult := searchResult "LEFT JOIN BUERO.WFF_WORKFLOWFUNC ON WFF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WIDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WID_LETZTERUSER" "~"
if joinKuerzel = BMUWID
searchResult := searchResult "LEFT JOIN BIL.WID_WIPDOKUMENTE ON WID_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WIZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WIZ_LETZTERUSER" "~"
if joinKuerzel = BMUWIZ
searchResult := searchResult "LEFT JOIN WWS.WIZ_WEINSATZKOPFZU ON WIZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WTBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WTB_LETZTERUSER" "~"
if joinKuerzel = BMUWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZPDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZPD_LETZTERUSER" "~"
if joinKuerzel = BMUZPD
searchResult := searchResult "LEFT JOIN BUERO.ZPD_ZEITPROFILDETAIL ON ZPD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZPKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZPK_LETZTERUSER" "~"
if joinKuerzel = BMUZPK
searchResult := searchResult "LEFT JOIN BUERO.ZPK_ZEITPROFILKOPF ON ZPK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZSTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZST_LETZTERUSER" "~"
if joinKuerzel = BMUZST
searchResult := searchResult "LEFT JOIN BUERO.ZST_ZEITSALDO_TMP ON ZST_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZTSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZTS_LETZTERUSER" "~"
if joinKuerzel = BMUZTS
searchResult := searchResult "LEFT JOIN BUERO.ZTS_ZEITTAGESSUMMEN ON ZTS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZUWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZUW_LETZTERUSER" "~"
if joinKuerzel = BMUZUW
searchResult := searchResult "LEFT JOIN BUERO.ZUW_ZEITUMSATZWERTE ON ZUW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZVZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZVZ_LETZTERUSER" "~"
if joinKuerzel = BMUZVZ
searchResult := searchResult "LEFT JOIN BUERO.ZVZ_TBVERWENDUNGSZWECK ON ZVZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PLCV2A
searchResult := searchResult "LEFT JOIN PRODUKTION.V2A_VA_VPP_ARL ON V2A_PCHARGENID = PLC_PCHARGENID" "~"
if joinKuerzel = V2APLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_PCHARGENID = V2A_PCHARGENID" "~"
if joinKuerzel = SNRLKO
searchResult := searchResult "LEFT JOIN WWS.LKO_LAGERKONTO ON LKO_SERNRID = SNR_SERNRID AND LKO_FIRMENNR = SNR_FIRMENNR" "~"
if joinKuerzel = LKOSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_SERNRID = LKO_SERNRID AND SNR_FIRMENNR = LKO_FIRMENNR" "~"
if joinKuerzel = IVPLKO
searchResult := searchResult "LEFT JOIN WWS.LKO_LAGERKONTO ON " "~"
if joinKuerzel = LKOIVP
searchResult := searchResult "LEFT JOIN WWS.IVP_INVENTURPOSITION ON " "~"
if joinKuerzel = ARLVAV
searchResult := searchResult "LEFT JOIN WWS.VAV_VIEW_ARTIKEL_VKEXKL ON VAV_FIRMENNR = ARL_FIRMENNR AND VAV_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = VAVARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = VAV_FIRMENNR AND ARL_ARTIKELNR = VAV_ARTIKELNR" "~"
if joinKuerzel = ARLVKI
searchResult := searchResult "LEFT JOIN KORE.VKI_VIEW_KOSTAMM_DIM ON VKI_FIRMENNR = ARL_FIRMENNR AND VKI_KOSTENSTAMMNR = ARL_DIM_KOSTENSTAMMNR" "~"
if joinKuerzel = VKIARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = VKI_FIRMENNR AND ARL_DIM_KOSTENSTAMMNR = VKI_KOSTENSTAMMNR" "~"
if joinKuerzel = ZWPLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = ZWP_LANDNR" "~"
if joinKuerzel = LANZWP
searchResult := searchResult "LEFT JOIN WWS.ZWP_ZUORDNUNGWVPOS ON ZWP_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = ARLVDA
searchResult := searchResult "LEFT JOIN WWS.VDA_VIEW_DISPOLISTEARTIKEL ON VDA_FIRMENNR = ARL_FIRMENNR AND VDA_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = VDAARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = VDA_FIRMENNR AND ARL_ARTIKELNR = VDA_ARTIKELNR" "~"
if joinKuerzel = EMIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EMI_LETZTERUSER" "~"
if joinKuerzel = BMUEMI
searchResult := searchResult "LEFT JOIN HRS.EMI_ELDADETAIL_MA ON EMI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EPSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EPS_LETZTERUSER" "~"
if joinKuerzel = BMUEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EPVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EPV_LETZTERUSER" "~"
if joinKuerzel = BMUEPV
searchResult := searchResult "LEFT JOIN WWS.EPV_ERFASSPOSRECHVOR ON EPV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ERWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ERW_LETZTERUSER" "~"
if joinKuerzel = BMUERW
searchResult := searchResult "LEFT JOIN FIBU.ERW_WORKFLOW ON ERW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EVVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EVV_LETZTERUSER" "~"
if joinKuerzel = BMUEVV
searchResult := searchResult "LEFT JOIN BUERO.EVV_EVENTVORTRAGZU ON EVV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FAGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FAG_LETZTERUSER" "~"
if joinKuerzel = BMUFAG
searchResult := searchResult "LEFT JOIN HRS.FAG_FINANZAMTGHOST ON FAG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FDHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FDH_LETZTERUSER" "~"
if joinKuerzel = BMUFDH
searchResult := searchResult "LEFT JOIN FIBU.FDH_FIBUDASHBOARDKOPF ON FDH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FDPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FDP_LETZTERUSER" "~"
if joinKuerzel = BMUFDP
searchResult := searchResult "LEFT JOIN BMD.FDP_FELDWERTEPOS ON FDP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FKEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FKE_LETZTERUSER" "~"
if joinKuerzel = BMUFKE
searchResult := searchResult "LEFT JOIN FIBU.FKE_FIBUKOREERFASSUNG ON FKE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FKTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FKT_LETZTERUSER" "~"
if joinKuerzel = BMUFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FRBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FRB_LETZTERUSER" "~"
if joinKuerzel = BMUFRB
searchResult := searchResult "LEFT JOIN WWS.FRB_FARBE ON FRB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FSPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FSP_LETZTERUSER" "~"
if joinKuerzel = BMUFSP
searchResult := searchResult "LEFT JOIN FIBU.FSP_SPALTENKOPF ON FSP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GAS_LETZTERUSER" "~"
if joinKuerzel = BMUGAS
searchResult := searchResult "LEFT JOIN BMD.GAS_GLOBAL_ACTIONSTACK ON GAS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GE2BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GE2_LETZTERUSER" "~"
if joinKuerzel = BMUGE2
searchResult := searchResult "LEFT JOIN BIL.GE2_GEB2RECHTSGESCHAEFT ON GE2_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GEFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GEF_LETZTERUSER" "~"
if joinKuerzel = BMUGEF
searchResult := searchResult "LEFT JOIN WWS.GEF_GEFAHRENGUTSTAMM ON GEF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GEPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GEP_LETZTERUSER" "~"
if joinKuerzel = BMUGEP
searchResult := searchResult "LEFT JOIN WWS.GEP_GEFAHRENGUTPOS ON GEP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GERBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GER_LETZTERUSER" "~"
if joinKuerzel = BMUGER
searchResult := searchResult "LEFT JOIN BUERO.GER_GERICHT ON GER_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GKVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GKV_LETZTERUSER" "~"
if joinKuerzel = BMUGKV
searchResult := searchResult "LEFT JOIN HRS.GKV_GEBSTUFEKV ON GKV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GLSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GLS_LETZTERUSER" "~"
if joinKuerzel = BMUGLS
searchResult := searchResult "LEFT JOIN BIL.GLS_GLIEDERUNGSSCHEMA ON GLS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GPOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GPO_LETZTERUSER" "~"
if joinKuerzel = BMUGPO
searchResult := searchResult "LEFT JOIN BUERO.GPO_GRUPPENORDNER ON GPO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GROBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GRO_LETZTERUSER" "~"
if joinKuerzel = BMUGRO
searchResult := searchResult "LEFT JOIN WWS.GRO_GROESSE ON GRO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GRSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GRS_LETZTERUSER" "~"
if joinKuerzel = BMUGRS
searchResult := searchResult "LEFT JOIN BUERO.GRS_GELDRISIKO ON GRS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GSFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GSF_LETZTERUSER" "~"
if joinKuerzel = BMUGSF
searchResult := searchResult "LEFT JOIN WWS.GSF_GUTSCHEINFILIALDATEN ON GSF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GZJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GZJ_LETZTERUSER" "~"
if joinKuerzel = BMUGZJ
searchResult := searchResult "LEFT JOIN HRS.GZJ_GLAEUBIGERZU_JAHR ON GZJ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GZKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GZK_LETZTERUSER" "~"
if joinKuerzel = BMUGZK
searchResult := searchResult "LEFT JOIN WWS.GZK_GELDZAEHLKOPF ON GZK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GZMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GZM_LETZTERUSER" "~"
if joinKuerzel = BMUGZM
searchResult := searchResult "LEFT JOIN HRS.GZM_GLAEUBIGERZU_MONAT ON GZM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LMABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LMA_LETZTERUSER" "~"
if joinKuerzel = BMULMA
searchResult := searchResult "LEFT JOIN WWS.LMA_LAGERMONATSABSCHLUSS ON LMA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LOPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LOP_LETZTERUSER" "~"
if joinKuerzel = BMULOP
searchResult := searchResult "LEFT JOIN PRODUKTION.LOP_LOGIKTABELLE_POS ON LOP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LPOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LPO_LETZTERUSER" "~"
if joinKuerzel = BMULPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LPRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LPR_LETZTERUSER" "~"
if joinKuerzel = BMULPR
searchResult := searchResult "LEFT JOIN ZLE.LPR_LEAPERSON ON LPR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LVEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LVE_LETZTERUSER" "~"
if joinKuerzel = BMULVE
searchResult := searchResult "LEFT JOIN PRODUKTION.LVE_LEISTUNGSVORERFASSUNG ON LVE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LVZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LVZ_LETZTERUSER" "~"
if joinKuerzel = BMULVZ
searchResult := searchResult "LEFT JOIN BMDINTERN.LVZ_LIZENZVKPZU ON LVZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LZZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LZZ_LETZTERUSER" "~"
if joinKuerzel = BMULZZ
searchResult := searchResult "LEFT JOIN BMD.LZZ_AUTOLIZENZZU ON LZZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MAD_LETZTERUSER" "~"
if joinKuerzel = BMUMAD
searchResult := searchResult "LEFT JOIN HRS.MAD_MASSENDATENERF ON MAD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MAS_LETZTERUSER" "~"
if joinKuerzel = BMUMAS
searchResult := searchResult "LEFT JOIN FIBU.MAS_MAHN_STAMM ON MAS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MEDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MED_LETZTERUSER" "~"
if joinKuerzel = BMUMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MFLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MFL_LETZTERUSER" "~"
if joinKuerzel = BMUMFL
searchResult := searchResult "LEFT JOIN BMDINTERN.MFL_MFLIZENZEN ON MFL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MLTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MLT_LETZTERUSER" "~"
if joinKuerzel = BMUMLT
searchResult := searchResult "LEFT JOIN ZLE.MLT_MITARBLEISTTAUSCH ON MLT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MVKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MVK_LETZTERUSER" "~"
if joinKuerzel = BMUMVK
searchResult := searchResult "LEFT JOIN BUERO.MVK_MITARBVORSORGEKASSE ON MVK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MVPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MVP_LETZTERUSER" "~"
if joinKuerzel = BMUMVP
searchResult := searchResult "LEFT JOIN BUERO.MVP_MITARBEITERVORLAGEPOS ON MVP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NLZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NLZ_LETZTERUSER" "~"
if joinKuerzel = BMUNLZ
searchResult := searchResult "LEFT JOIN HRS.NLZ_NICHTLEISTUNGSZEITEN ON NLZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OFABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OFA_LETZTERUSER" "~"
if joinKuerzel = BMUOFA
searchResult := searchResult "LEFT JOIN LOHN.OFA_OF_FINANZAMT ON OFA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OPLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OPL_LETZTERUSER" "~"
if joinKuerzel = BMUOPL
searchResult := searchResult "LEFT JOIN LOHN.OPL_OF_PLZ ON OPL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ORLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ORL_LETZTERUSER" "~"
if joinKuerzel = BMUORL
searchResult := searchResult "LEFT JOIN LOHN.ORL_OF_REISELEISTUNG_TMP ON ORL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OTABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OTA_LETZTERUSER" "~"
if joinKuerzel = BMUOTA
searchResult := searchResult "LEFT JOIN LOHN.OTA_OF_TAETIGKEIT ON OTA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PABBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAB_LETZTERUSER" "~"
if joinKuerzel = BMUPAB
searchResult := searchResult "LEFT JOIN WWS.PAB_PROVARTBAUM ON PAB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAK_LETZTERUSER" "~"
if joinKuerzel = BMUPAK
searchResult := searchResult "LEFT JOIN BIL.PAK_PERSRST_ALTERSV_KOPF ON PAK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PAUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAU_LETZTERUSER" "~"
if joinKuerzel = BMUPAU
searchResult := searchResult "LEFT JOIN BMDDEFAULT.PAU_PARAMUNTERGRUPPE ON PAU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAZ_LETZTERUSER" "~"
if joinKuerzel = BMUPAZ
searchResult := searchResult "LEFT JOIN BMDDEFAULT.PAZ_PARAMGRUPPEPAKETZU ON PAZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PBCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PBC_LETZTERUSER" "~"
if joinKuerzel = BMUPBC
searchResult := searchResult "LEFT JOIN PRODUKTION.PBC_CODEPAGE ON PBC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PETBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PET_LETZTERUSER" "~"
if joinKuerzel = BMUPET
searchResult := searchResult "LEFT JOIN WWS.PET_PREISTAB ON PET_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PEZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PEZ_LETZTERUSER" "~"
if joinKuerzel = BMUPEZ
searchResult := searchResult "LEFT JOIN BUERO.PEZ_PERSONENZU ON PEZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ULEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ULE_LETZTERUSER" "~"
if joinKuerzel = BMUULE
searchResult := searchResult "LEFT JOIN BMDINTERN.ULE_UPDATELEBENSLAUF ON ULE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UPNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UPN_LETZTERUSER" "~"
if joinKuerzel = BMUUPN
searchResult := searchResult "LEFT JOIN BMDDEFAULT.UPN_UPH_KUNDE_NLS ON UPN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UVWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UVW_LETZTERUSER" "~"
if joinKuerzel = BMUUVW
searchResult := searchResult "LEFT JOIN FIBU.UVW_UVAWERT ON UVW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VMRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VMR_LETZTERUSER" "~"
if joinKuerzel = BMUVMR
searchResult := searchResult "LEFT JOIN WWS.VMR_VERTABRMAXRABATTE ON VMR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VSWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VSW_LETZTERUSER" "~"
if joinKuerzel = BMUVSW
searchResult := searchResult "LEFT JOIN WWS.VSW_VERTRETERSTATISTIK ON VSW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VTSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VTS_LETZTERUSER" "~"
if joinKuerzel = BMUVTS
searchResult := searchResult "LEFT JOIN WWS.VTS_VERTRETERSTUFE ON VTS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VWABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VWA_LETZTERUSER" "~"
if joinKuerzel = BMUVWA
searchResult := searchResult "LEFT JOIN BUERO.VWA_VORSCHLAGSWERTEARCHIV ON VWA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAS_LETZTERUSER" "~"
if joinKuerzel = BMUWAS
searchResult := searchResult "LEFT JOIN WWS.WAS_WARENEINSATZARL ON WAS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WATBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAT_LETZTERUSER" "~"
if joinKuerzel = BMUWAT
searchResult := searchResult "LEFT JOIN BUERO.WAT_WEBANMELDUNG ON WAT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WBSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WBS_LETZTERUSER" "~"
if joinKuerzel = BMUWBS
searchResult := searchResult "LEFT JOIN WWS.WBS_WWSBERICHT_SNAPSHOT ON WBS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WBWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WBW_LETZTERUSER" "~"
if joinKuerzel = BMUWBW
searchResult := searchResult "LEFT JOIN WWS.WBW_WWSBUDGETWERTE ON WBW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WBXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WBX_LETZTERUSER" "~"
if joinKuerzel = BMUWBX
searchResult := searchResult "LEFT JOIN WEB.WBX_WEBTEXTE ON WBX_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WCFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WCF_LETZTERUSER" "~"
if joinKuerzel = BMUWCF
searchResult := searchResult "LEFT JOIN BMD.WCF_WABAUMELEMAUSDRUCKZU ON WCF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WDIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WDI_LETZTERUSER" "~"
if joinKuerzel = BMUWDI
searchResult := searchResult "LEFT JOIN BUERO.WDI_WFDIAGRAMMINSTANZZU ON WDI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WENBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEN_LETZTERUSER" "~"
if joinKuerzel = BMUWEN
searchResult := searchResult "LEFT JOIN WWS.WEN_WARENEINSATZRNP ON WEN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEU_LETZTERUSER" "~"
if joinKuerzel = BMUWEU
searchResult := searchResult "LEFT JOIN WWS.WEU_WWSERKONTROLLEZU ON WEU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEW_LETZTERUSER" "~"
if joinKuerzel = BMUWEW
searchResult := searchResult "LEFT JOIN WWS.WEW_WARENEINSATZWGZU ON WEW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WFEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WFE_LETZTERUSER" "~"
if joinKuerzel = BMUWFE
searchResult := searchResult "LEFT JOIN WWS.WFE_FERTEP_AUFTDETAIL ON WFE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WPZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WPZ_LETZTERUSER" "~"
if joinKuerzel = BMUWPZ
searchResult := searchResult "LEFT JOIN WWS.WPZ_WESLPO_ZUORDNUNG ON WPZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WQUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WQU_LETZTERUSER" "~"
if joinKuerzel = BMUWQU
searchResult := searchResult "LEFT JOIN BMD.WQU_WQI_BMU_ZU ON WQU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WRZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WRZ_LETZTERUSER" "~"
if joinKuerzel = BMUWRZ
searchResult := searchResult "LEFT JOIN WWS.WRZ_WESRNP_ZUORDNUNG ON WRZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WSKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WSK_LETZTERUSER" "~"
if joinKuerzel = BMUWSK
searchResult := searchResult "LEFT JOIN WWS.WSK_WWSKORESTATISTIK ON WSK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WVTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WVT_LETZTERUSER" "~"
if joinKuerzel = BMUWVT
searchResult := searchResult "LEFT JOIN WWS.WVT_WARTUNGSVERTRAG ON WVT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WWMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WWM_LETZTERUSER" "~"
if joinKuerzel = BMUWWM
searchResult := searchResult "LEFT JOIN WWS.WWM_WWSMAKROZUORD ON WWM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZAD_LETZTERUSER" "~"
if joinKuerzel = BMUZAD
searchResult := searchResult "LEFT JOIN DATANORM.ZAD_ZU_ABSCHLAG_DETAIL ON ZAD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZBBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZBB_LETZTERUSER" "~"
if joinKuerzel = BMUZBB
searchResult := searchResult "LEFT JOIN BUERO.ZBB_ZEITBETRKALBUCHART ON ZBB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WAGVKT
searchResult := searchResult "LEFT JOIN KORE.VKT_VIEW_KOSTAMM_KTR ON VKT_FIRMENNR = WAG_FIRMENNR AND VKT_KOSTENSTAMMNR = WAG_KOSTENTR_KOSTENSTAMMNR" "~"
if joinKuerzel = VKTWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = VKT_FIRMENNR AND WAG_KOSTENTR_KOSTENSTAMMNR = VKT_KOSTENSTAMMNR" "~"
if joinKuerzel = PLCREH
searchResult := searchResult "LEFT JOIN PRODUKTION.REH_REIHE ON REH_FIRMENNR = PLC_REH_FIRMENNR AND REH_REIHE = PLC_REIHE" "~"
if joinKuerzel = REHPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_REH_FIRMENNR = REH_FIRMENNR AND PLC_REIHE = REH_REIHE" "~"
if joinKuerzel = OGKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OGK_LETZTERUSER" "~"
if joinKuerzel = BMUOGK
searchResult := searchResult "LEFT JOIN LOHN.OGK_OF_GKK ON OGK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OKTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OKT_LETZTERUSER" "~"
if joinKuerzel = BMUOKT
searchResult := searchResult "LEFT JOIN LOHN.OKT_OF_KOSTENTRAEGER ON OKT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ORTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ORT_LETZTERUSER" "~"
if joinKuerzel = BMUORT
searchResult := searchResult "LEFT JOIN BUERO.ORT_ORT ON ORT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OSLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OSL_LETZTERUSER" "~"
if joinKuerzel = BMUOSL
searchResult := searchResult "LEFT JOIN DATANORM.OSL_SET_ON_LBSL ON OSL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OUNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OUN_LETZTERUSER" "~"
if joinKuerzel = BMUOUN
searchResult := searchResult "LEFT JOIN BMD.OUN_OUTLOOKBARTABNODES ON OUN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OVGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OVG_LETZTERUSER" "~"
if joinKuerzel = BMUOVG
searchResult := searchResult "LEFT JOIN LOHN.OVG_OF_VERWENDUNGSGRUPPE ON OVG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PAVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAV_LETZTERUSER" "~"
if joinKuerzel = BMUPAV
searchResult := searchResult "LEFT JOIN WWS.PAV_PICALTERNATIVARTIKEL ON PAV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PGZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PGZ_LETZTERUSER" "~"
if joinKuerzel = BMUPGZ
searchResult := searchResult "LEFT JOIN BUERO.PGZ_PLANGRUPPETMOZU ON PGZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIE_LETZTERUSER" "~"
if joinKuerzel = BMUPIE
searchResult := searchResult "LEFT JOIN WWS.PIE_PICEINHEIT ON PIE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIH_LETZTERUSER" "~"
if joinKuerzel = BMUPIH
searchResult := searchResult "LEFT JOIN WWS.PIH_PICSPRACHEZU ON PIH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIW_LETZTERUSER" "~"
if joinKuerzel = BMUPIW
searchResult := searchResult "LEFT JOIN WWS.PIW_PICWARENGRUPPE ON PIW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKB_LETZTERUSER" "~"
if joinKuerzel = BMUPKB
searchResult := searchResult "LEFT JOIN BIL.PKB_PERSKORSTBUCHBELEGZU ON PKB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKS_LETZTERUSER" "~"
if joinKuerzel = BMUPKS
searchResult := searchResult "LEFT JOIN PRODUKTION.PKS_SOLL_KAPAZITAET ON PKS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKZ_LETZTERUSER" "~"
if joinKuerzel = BMUPKZ
searchResult := searchResult "LEFT JOIN BUERO.PKZ_PERSONKONTAKTZU ON PKZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PLCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PLC_LETZTERUSER" "~"
if joinKuerzel = BMUPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PSABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PSA_LETZTERUSER" "~"
if joinKuerzel = BMUPSA
searchResult := searchResult "LEFT JOIN HRS.PSA_PERSONALSTAMM_ABRSTAT ON PSA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PSOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PSO_LETZTERUSER" "~"
if joinKuerzel = BMUPSO
searchResult := searchResult "LEFT JOIN BUERO.PSO_PERSONSOZIALZU ON PSO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PTRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PTR_LETZTERUSER" "~"
if joinKuerzel = BMUPTR
searchResult := searchResult "LEFT JOIN PRODUKTION.PTR_TRANSAKTION ON PTR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PTXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PTX_LETZTERUSER" "~"
if joinKuerzel = BMUPTX
searchResult := searchResult "LEFT JOIN BMD.PTX_PERFORMANCETESTTAB ON PTX_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PWBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PWB_LETZTERUSER" "~"
if joinKuerzel = BMUPWB
searchResult := searchResult "LEFT JOIN WWS.PWB_PWKRABATTTABZU ON PWB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PWZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PWZ_LETZTERUSER" "~"
if joinKuerzel = BMUPWZ
searchResult := searchResult "LEFT JOIN WWS.PWZ_PWKKUNDELIEFERANTZU ON PWZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = QAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = QAZ_LETZTERUSER" "~"
if joinKuerzel = BMUQAZ
searchResult := searchResult "LEFT JOIN BUERO.QAZ_QUOTENAUSWERTUNGZU ON QAZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = QUVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = QUV_LETZTERUSER" "~"
if joinKuerzel = BMUQUV
searchResult := searchResult "LEFT JOIN BUERO.QUV_QUOTENVERLAENGERUNG ON QUV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = REDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RED_LETZTERUSER" "~"
if joinKuerzel = BMURED
searchResult := searchResult "LEFT JOIN HRS.RED_REISEDETAIL ON RED_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = REGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = REG_LETZTERUSER" "~"
if joinKuerzel = BMUREG
searchResult := searchResult "LEFT JOIN BUERO.REG_REGION ON REG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = REMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = REM_LETZTERUSER" "~"
if joinKuerzel = BMUREM
searchResult := searchResult "LEFT JOIN BUERO.REM_REMINDER ON REM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SEBSEB
searchResult := searchResult "LEFT JOIN BIL.SEB_STEUERERKLBAUM ON SEB_SEBID = SEB_PARENT_SEBID" "~"
if joinKuerzel = SEBSEB
searchResult := searchResult "LEFT JOIN BIL.SEB_STEUERERKLBAUM ON SEB_PARENT_SEBID = SEB_SEBID" "~"
if joinKuerzel = LNSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LNS_LETZTERUSER" "~"
if joinKuerzel = BMULNS
searchResult := searchResult "LEFT JOIN HRS.LNS_LOHNSCHABLONENDETAILS ON LNS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LOKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LOK_LETZTERUSER" "~"
if joinKuerzel = BMULOK
searchResult := searchResult "LEFT JOIN PRODUKTION.LOK_LOGIKTABELLE_KOPF ON LOK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LPFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LPF_LETZTERUSER" "~"
if joinKuerzel = BMULPF
searchResult := searchResult "LEFT JOIN BMDENTW.LPF_LIZENZPAKETFUNKZU ON LPF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LRSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LRS_LETZTERUSER" "~"
if joinKuerzel = BMULRS
searchResult := searchResult "LEFT JOIN BIL.LRS_LANGFRISTIGE_RST ON LRS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LTSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LTS_LETZTERUSER" "~"
if joinKuerzel = BMULTS
searchResult := searchResult "LEFT JOIN ZLE.LTS_LEISTTAUSCHSATZ ON LTS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LXMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LXM_LETZTERUSER" "~"
if joinKuerzel = BMULXM
searchResult := searchResult "LEFT JOIN BUERO.LXM_LOHNEXPPROTOKOLL ON LXM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LZSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LZS_LETZTERUSER" "~"
if joinKuerzel = BMULZS
searchResult := searchResult "LEFT JOIN HRS.LZS_LOHNZETTEL_SV ON LZS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MAVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MAV_LETZTERUSER" "~"
if joinKuerzel = BMUMAV
searchResult := searchResult "LEFT JOIN FIBU.MAV_MAHNVZINSEN ON MAV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MKRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MKR_LETZTERUSER" "~"
if joinKuerzel = BMUMKR
searchResult := searchResult "LEFT JOIN FIBU.MKR_MAHNKREIS ON MKR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MOPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MOP_LETZTERUSER" "~"
if joinKuerzel = BMUMOP
searchResult := searchResult "LEFT JOIN WWS.MOP_MODELLPOSVAR ON MOP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MVTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MVT_LETZTERUSER" "~"
if joinKuerzel = BMUMVT
searchResult := searchResult "LEFT JOIN BUERO.MVT_MEDIENVERLEIHTERMINZU ON MVT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NSCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NSC_LETZTERUSER" "~"
if joinKuerzel = BMUNSC
searchResult := searchResult "LEFT JOIN BMD.NSC_NETSCANNER ON NSC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NUTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NUT_LETZTERUSER" "~"
if joinKuerzel = BMUNUT
searchResult := searchResult "LEFT JOIN BMD.NUT_NTCSUPDTRANSFER ON NUT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OBDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OBD_LETZTERUSER" "~"
if joinKuerzel = BMUOBD
searchResult := searchResult "LEFT JOIN BUERO.OBD_ONLBEWDOKUMENTE ON OBD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OBGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OBG_LETZTERUSER" "~"
if joinKuerzel = BMUOBG
searchResult := searchResult "LEFT JOIN LOHN.OBG_OF_BEITRAGSGRUPPE ON OBG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OBTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OBT_LETZTERUSER" "~"
if joinKuerzel = BMUOBT
searchResult := searchResult "LEFT JOIN LOHN.OBT_OF_BLOBTEXT ON OBT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ODABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ODA_LETZTERUSER" "~"
if joinKuerzel = BMUODA
searchResult := searchResult "LEFT JOIN BMD.ODA_ONLINEDATEI ON ODA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ONMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ONM_LETZTERUSER" "~"
if joinKuerzel = BMUONM
searchResult := searchResult "LEFT JOIN HRS.ONM_ONLINEBEW_MERKMALE ON ONM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OUEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OUE_LETZTERUSER" "~"
if joinKuerzel = BMUOUE
searchResult := searchResult "LEFT JOIN BMD.OUE_OUTLOOKBARELEM ON OUE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PAEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAE_LETZTERUSER" "~"
if joinKuerzel = BMUPAE
searchResult := searchResult "LEFT JOIN BMDINTERN.PAE_PRODUKTANFRAGE ON PAE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PALBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAL_LETZTERUSER" "~"
if joinKuerzel = BMUPAL
searchResult := searchResult "LEFT JOIN ZLE.PAL_PAUSCHALE ON PAL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAS_LETZTERUSER" "~"
if joinKuerzel = BMUPAS
searchResult := searchResult "LEFT JOIN WWS.PAS_PACKSTOFFPREIS ON PAS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PAWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAW_LETZTERUSER" "~"
if joinKuerzel = BMUPAW
searchResult := searchResult "LEFT JOIN BMD.PAW_PARAMWERT ON PAW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PDSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PDS_LETZTERUSER" "~"
if joinKuerzel = BMUPDS
searchResult := searchResult "LEFT JOIN BIL.PDS_PERSKORST_DNSTAMM ON PDS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PEKKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = PEK_FIRMENNR AND KTO_FIBUNR = PEK_FIBUNR AND KTO_KONTONR = PEK_KONTONR" "~"
if joinKuerzel = PEKKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = PEK_FIRMENNR AND KTO_FIBUNR = PEK_FIBUNR AND KTO_KONTONR = PEK_GEGENVERR_KONTONR" "~"
if joinKuerzel = PEKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PEK_LETZTERUSER" "~"
if joinKuerzel = BMUPEK
searchResult := searchResult "LEFT JOIN FIBU.PEK_PERSONENKONTO ON PEK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PERBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PER_LETZTERUSER" "~"
if joinKuerzel = BMUPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HAMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HAM_LETZTERUSER" "~"
if joinKuerzel = BMUHAM
searchResult := searchResult "LEFT JOIN HRS.HAM_AUSDRUCKMASTER ON HAM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HLDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HLD_LETZTERUSER" "~"
if joinKuerzel = BMUHLD
searchResult := searchResult "LEFT JOIN BMDENTW.HLD_HILFEDOKUMENT ON HLD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HLZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HLZ_LETZTERUSER" "~"
if joinKuerzel = BMUHLZ
searchResult := searchResult "LEFT JOIN BMDENTW.HLZ_HILFEDOKBAUSTEINZU ON HLZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HTBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HTB_LETZTERUSER" "~"
if joinKuerzel = BMUHTB
searchResult := searchResult "LEFT JOIN BMDENTW.HTB_HILFETEXTBAUSTEIN ON HTB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HVBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HVB_LETZTERUSER" "~"
if joinKuerzel = BMUHVB
searchResult := searchResult "LEFT JOIN HRS.HVB_VERRECHNUNGSBASIS ON HVB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HZLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HZL_LETZTERUSER" "~"
if joinKuerzel = BMUHZL
searchResult := searchResult "LEFT JOIN BUERO.HZL_HAUPTZIEL ON HZL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IDEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IDE_LETZTERUSER" "~"
if joinKuerzel = BMUIDE
searchResult := searchResult "LEFT JOIN BMDINTERN.IDE_IDEE ON IDE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IMMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IMM_LETZTERUSER" "~"
if joinKuerzel = BMUIMM
searchResult := searchResult "LEFT JOIN BUERO.IMM_INSERATMEDIUMMASTER ON IMM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IPZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IPZ_LETZTERUSER" "~"
if joinKuerzel = BMUIPZ
searchResult := searchResult "LEFT JOIN BUERO.IPZ_INTERNPROJEKTZU ON IPZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IWVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IWV_LETZTERUSER" "~"
if joinKuerzel = BMUIWV
searchResult := searchResult "LEFT JOIN BUERO.IWV_INTRASTATWV ON IWV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KAJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KAJ_LETZTERUSER" "~"
if joinKuerzel = BMUKAJ
searchResult := searchResult "LEFT JOIN WWS.KAJ_KASSENJOURNAL ON KAJ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KAZ_LETZTERUSER" "~"
if joinKuerzel = BMUKAZ
searchResult := searchResult "LEFT JOIN BUERO.KAZ_KATEGORIEZU ON KAZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KFGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KFG_LETZTERUSER" "~"
if joinKuerzel = BMUKFG
searchResult := searchResult "LEFT JOIN WWS.KFG_KTOFINDBETRGRENZEN ON KFG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KGBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KGB_LETZTERUSER" "~"
if joinKuerzel = BMUKGB
searchResult := searchResult "LEFT JOIN PRODUKTION.KGB_KATZU_PPSKAPA ON KGB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KIMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KIM_LETZTERUSER" "~"
if joinKuerzel = BMUKIM
searchResult := searchResult "LEFT JOIN FIBU.KIM_KONTENCSVIMPORTMODELL ON KIM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KINBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KIN_LETZTERUSER" "~"
if joinKuerzel = BMUKIN
searchResult := searchResult "LEFT JOIN BUERO.KIN_ZEITKOREFELDINFOS ON KIN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KMGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KMG_LETZTERUSER" "~"
if joinKuerzel = BMUKMG
searchResult := searchResult "LEFT JOIN KORE.KMG_KOREMENGE ON KMG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KODBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KOD_LETZTERUSER" "~"
if joinKuerzel = BMUKOD
searchResult := searchResult "LEFT JOIN BUERO.KOD_KONDITIONEN ON KOD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KOPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KOP_LETZTERUSER" "~"
if joinKuerzel = BMUKOP
searchResult := searchResult "LEFT JOIN PRODUKTION.KOP_KOMMISSIONIERUNG_POS ON KOP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KRMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KRM_LETZTERUSER" "~"
if joinKuerzel = BMUKRM
searchResult := searchResult "LEFT JOIN HRS.KRM_KONTENREFERENZTAB ON KRM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KSBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KSB_LETZTERUSER" "~"
if joinKuerzel = BMUKSB
searchResult := searchResult "LEFT JOIN HRS.KSB_KRANKENSTANDSBESCH ON KSB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KSCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KSC_LETZTERUSER" "~"
if joinKuerzel = BMUKSC
searchResult := searchResult "LEFT JOIN KORE.KSC_KORESCHLUESSEL ON KSC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KSPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KSP_LETZTERUSER" "~"
if joinKuerzel = BMUKSP
searchResult := searchResult "LEFT JOIN KORE.KSP_SPALTENKOPF ON KSP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KSTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KST_LETZTERUSER" "~"
if joinKuerzel = BMUKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KUDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KUD_LETZTERUSER" "~"
if joinKuerzel = BMUKUD
searchResult := searchResult "LEFT JOIN BMD.KUD_KURSDATEI ON KUD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KURBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KUR_LETZTERUSER" "~"
if joinKuerzel = BMUKUR
searchResult := searchResult "LEFT JOIN KORE.KUR_UMLAGEREIHENFOLGE ON KUR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PFFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PFF_LETZTERUSER" "~"
if joinKuerzel = BMUPFF
searchResult := searchResult "LEFT JOIN BUERO.PFF_PAGEFGBFRAGEZU ON PFF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PGMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PGM_LETZTERUSER" "~"
if joinKuerzel = BMUPGM
searchResult := searchResult "LEFT JOIN BUERO.PGM_PERSONGEMEINDEZU ON PGM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PGPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PGP_LETZTERUSER" "~"
if joinKuerzel = BMUPGP
searchResult := searchResult "LEFT JOIN WWS.PGP_PICARTIKELGRP ON PGP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIB_LETZTERUSER" "~"
if joinKuerzel = BMUPIB
searchResult := searchResult "LEFT JOIN WWS.PIB_PICRABATTSTAFFEL ON PIB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIF_LETZTERUSER" "~"
if joinKuerzel = BMUPIF
searchResult := searchResult "LEFT JOIN WWS.PIF_PICKUNDENZU ON PIF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIJ_LETZTERUSER" "~"
if joinKuerzel = BMUPIJ
searchResult := searchResult "LEFT JOIN WWS.PIJ_PICKUNDENGRZU ON PIJ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PILBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIL_LETZTERUSER" "~"
if joinKuerzel = BMUPIL
searchResult := searchResult "LEFT JOIN WWS.PIL_PICKUNDE ON PIL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIM_LETZTERUSER" "~"
if joinKuerzel = BMUPIM
searchResult := searchResult "LEFT JOIN BUERO.PIM_PERSONIMPORT ON PIM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PINBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIN_LETZTERUSER" "~"
if joinKuerzel = BMUPIN
searchResult := searchResult "LEFT JOIN WWS.PIN_PICANBINDEARTZU ON PIN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIZ_LETZTERUSER" "~"
if joinKuerzel = BMUPIZ
searchResult := searchResult "LEFT JOIN WWS.PIZ_PICARTIKELZU ON PIZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKL_LETZTERUSER" "~"
if joinKuerzel = BMUPKL
searchResult := searchResult "LEFT JOIN BUERO.PKL_PROJEKTPERSZU ON PKL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PLPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PLP_LETZTERUSER" "~"
if joinKuerzel = BMUPLP
searchResult := searchResult "LEFT JOIN PRODUKTION.PLP_PLANUNGSPERIODE ON PLP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PLSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PLS_LETZTERUSER" "~"
if joinKuerzel = BMUPLS
searchResult := searchResult "LEFT JOIN WWS.PLS_PICALLERGEN ON PLS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PMDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PMD_LETZTERUSER" "~"
if joinKuerzel = BMUPMD
searchResult := searchResult "LEFT JOIN BUERO.PMD_PAUSENMODELL ON PMD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PMZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PMZ_LETZTERUSER" "~"
if joinKuerzel = BMUPMZ
searchResult := searchResult "LEFT JOIN BUERO.PMZ_PAUSENTAGESMODELLZU ON PMZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PPPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PPP_LETZTERUSER" "~"
if joinKuerzel = BMUPPP
searchResult := searchResult "LEFT JOIN PRODUKTION.PPP_KAPA_POOL ON PPP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PPUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PPU_LETZTERUSER" "~"
if joinKuerzel = BMUPPU
searchResult := searchResult "LEFT JOIN PRODUKTION.PPU_KAPA_POOL_ZU ON PPU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PREBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PRE_LETZTERUSER" "~"
if joinKuerzel = BMUPRE
searchResult := searchResult "LEFT JOIN WWS.PRE_EXTPARTIKELWERTE ON PRE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PROBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PRO_LETZTERUSER" "~"
if joinKuerzel = BMUPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PRSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PRS_LETZTERUSER" "~"
if joinKuerzel = BMUPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PRWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PRW_LETZTERUSER" "~"
if joinKuerzel = BMUPRW
searchResult := searchResult "LEFT JOIN BUERO.PRW_PROJEKTWERT ON PRW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PSEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PSE_LETZTERUSER" "~"
if joinKuerzel = BMUPSE
searchResult := searchResult "LEFT JOIN BIL.PSE_PERSRST_STERBET_WERT ON PSE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PSRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PSR_LETZTERUSER" "~"
if joinKuerzel = BMUPSR
searchResult := searchResult "LEFT JOIN HRS.PSR_PROGRAMMSPERRE ON PSR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PSTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PST_LETZTERUSER" "~"
if joinKuerzel = BMUPST
searchResult := searchResult "LEFT JOIN PRODUKTION.PST_STAPEL ON PST_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PSZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PSZ_LETZTERUSER" "~"
if joinKuerzel = BMUPSZ
searchResult := searchResult "LEFT JOIN BUERO.PSZ_PERSONENKZ ON PSZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PUCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PUC_LETZTERUSER" "~"
if joinKuerzel = BMUPUC
searchResult := searchResult "LEFT JOIN PRODUKTION.PUC_PPSUSERSTATICOMBI ON PUC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZBFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZBF_LETZTERUSER" "~"
if joinKuerzel = BMUZBF
searchResult := searchResult "LEFT JOIN BUERO.ZBF_ZEITBAFUNKZUORD ON ZBF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZBKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZBK_LETZTERUSER" "~"
if joinKuerzel = BMUZBK
searchResult := searchResult "LEFT JOIN BUERO.ZBK_ZEITBETRIEBSKALENDER ON ZBK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZBSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZBS_LETZTERUSER" "~"
if joinKuerzel = BMUZBS
searchResult := searchResult "LEFT JOIN FIBU.ZBS_ZINSBERECHNUNG_STAMM ON ZBS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZIABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZIA_LETZTERUSER" "~"
if joinKuerzel = BMUZIA
searchResult := searchResult "LEFT JOIN FIBU.ZIA_KONTENZINSAB ON ZIA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZIBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZIB_LETZTERUSER" "~"
if joinKuerzel = BMUZIB
searchResult := searchResult "LEFT JOIN FIBU.ZIB_KONTENZINSENBERECHNG ON ZIB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZLBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZLB_LETZTERUSER" "~"
if joinKuerzel = BMUZLB
searchResult := searchResult "LEFT JOIN BUERO.ZLB_ZBEZARTLOHNBLOCKSORT ON ZLB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZLTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZLT_LETZTERUSER" "~"
if joinKuerzel = BMUZLT
searchResult := searchResult "LEFT JOIN ZLE.ZLT_ZEILENTYP ON ZLT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZLZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZLZ_LETZTERUSER" "~"
if joinKuerzel = BMUZLZ
searchResult := searchResult "LEFT JOIN BUERO.ZLZ_ZIELZUORDNUNG ON ZLZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZMABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZMA_LETZTERUSER" "~"
if joinKuerzel = BMUZMA
searchResult := searchResult "LEFT JOIN BUERO.ZMA_ZEITBABERECHTMAZUORD ON ZMA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZMBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZMB_LETZTERUSER" "~"
if joinKuerzel = BMUZMB
searchResult := searchResult "LEFT JOIN BUERO.ZMB_ZEITMINDESTBESETZUNG ON ZMB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZRBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZRB_LETZTERUSER" "~"
if joinKuerzel = BMUZRB
searchResult := searchResult "LEFT JOIN BUERO.ZRB_ZEITRUNDUNGBEDINGUNG ON ZRB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZRTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZRT_LETZTERUSER" "~"
if joinKuerzel = BMUZRT
searchResult := searchResult "LEFT JOIN BUERO.ZRT_ZAHLUNGSART ON ZRT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZTZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZTZ_LETZTERUSER" "~"
if joinKuerzel = BMUZTZ
searchResult := searchResult "LEFT JOIN BMD.ZTZ_ZEITZONEN ON ZTZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZVDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZVD_LETZTERUSER" "~"
if joinKuerzel = BMUZVD
searchResult := searchResult "LEFT JOIN BUERO.ZVD_ZEITPLANUNGVERSIONDAT ON ZVD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PCKGEI
searchResult := searchResult "LEFT JOIN PRODUKTION.GEI_VA_PCK_SCM_INFO ON GEI_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = GEIPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_PPSKAPAID = GEI_PPSKAPAID" "~"
if joinKuerzel = PCKZEV
searchResult := searchResult "LEFT JOIN PRODUKTION.ZEV_VA_LEI_ZEILEN ON ZEV_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = ZEVPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_PPSKAPAID = ZEV_PPSKAPAID" "~"
if joinKuerzel = IAPARS
searchResult := searchResult "LEFT JOIN WWS.ARS_ARTIKELSTATISTIK ON ARS_FIRMENNR = IAP_FIRMENNR AND ARS_STATISTIKLFDNR = IAP_STATISTIKLFDNR" "~"
if joinKuerzel = ARSIAP
searchResult := searchResult "LEFT JOIN WWS.IAP_INVABWTPOS ON IAP_FIRMENNR = ARS_FIRMENNR AND IAP_STATISTIKLFDNR = ARS_STATISTIKLFDNR" "~"
if joinKuerzel = ARLVAW
searchResult := searchResult "LEFT JOIN WWS.VAW_VIEW_ARTIKELSTATSUM ON VAW_HAUPT_FIRMENNR = ARL_FIRMENNR AND VAW_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = VAWARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = VAW_HAUPT_FIRMENNR AND ARL_ARTIKELNR = VAW_ARTIKELNR" "~"
if joinKuerzel = LAJKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_KOSTENSTAMMNR = LAJ_ABT_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_ABT_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = ARLVAE
searchResult := searchResult "LEFT JOIN WWS.VAE_VIEW_ARTIKEL_EK ON VAE_FIRMENNR = ARL_FIRMENNR AND VAE_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = VAEARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = VAE_FIRMENNR AND ARL_ARTIKELNR = VAE_ARTIKELNR" "~"
if joinKuerzel = WAGVKI
searchResult := searchResult "LEFT JOIN KORE.VKI_VIEW_KOSTAMM_DIM ON VKI_FIRMENNR = WAG_FIRMENNR AND VKI_KOSTENSTAMMNR = WAG_DIM_KOSTENSTAMMNR" "~"
if joinKuerzel = VKIWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = VKI_FIRMENNR AND WAG_DIM_KOSTENSTAMMNR = VKI_KOSTENSTAMMNR" "~"
if joinKuerzel = RKABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RKA_LETZTERUSER" "~"
if joinKuerzel = BMURKA
searchResult := searchResult "LEFT JOIN BMD.RKA_REPLIKATIONSAENDERUNG ON RKA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RSIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RSI_LETZTERUSER" "~"
if joinKuerzel = BMURSI
searchResult := searchResult "LEFT JOIN WWS.RSI_REGKASIGNATUREH ON RSI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RVCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RVC_LETZTERUSER" "~"
if joinKuerzel = BMURVC
searchResult := searchResult "LEFT JOIN FIBU.RVC_RVCODE ON RVC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RZIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RZI_LETZTERUSER" "~"
if joinKuerzel = BMURZI
searchResult := searchResult "LEFT JOIN HRS.RZI_REISEZIELE ON RZI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RZUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RZU_LETZTERUSER" "~"
if joinKuerzel = BMURZU
searchResult := searchResult "LEFT JOIN BUERO.RZU_RESSOURCENPAKETZU ON RZU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RZZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RZZ_LETZTERUSER" "~"
if joinKuerzel = BMURZZ
searchResult := searchResult "LEFT JOIN BUERO.RZZ_AZGARGZEITEN ON RZZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SATBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SAT_LETZTERUSER" "~"
if joinKuerzel = BMUSAT
searchResult := searchResult "LEFT JOIN BUERO.SAT_SETANTWORT ON SAT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SBKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SBK_LETZTERUSER" "~"
if joinKuerzel = BMUSBK
searchResult := searchResult "LEFT JOIN WWS.SBK_SAISONBESCHAFFKOPF ON SBK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SBPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SBP_LETZTERUSER" "~"
if joinKuerzel = BMUSBP
searchResult := searchResult "LEFT JOIN WWS.SBP_SAISONBESCHAFFPOS ON SBP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCE_LETZTERUSER" "~"
if joinKuerzel = BMUSCE
searchResult := searchResult "LEFT JOIN PRODUKTION.SCE_SCORECARD ON SCE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SFEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SFE_LETZTERUSER" "~"
if joinKuerzel = BMUSFE
searchResult := searchResult "LEFT JOIN BUERO.SFE_SKILLFINDERELEMENT ON SFE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SFKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SFK_LETZTERUSER" "~"
if joinKuerzel = BMUSFK
searchResult := searchResult "LEFT JOIN BUERO.SFK_SYNCFELDKOPF ON SFK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SKEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SKE_LETZTERUSER" "~"
if joinKuerzel = BMUSKE
searchResult := searchResult "LEFT JOIN BIL.SKE_STEUERKUNDENZUERW ON SKE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SKZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SKZ_LETZTERUSER" "~"
if joinKuerzel = BMUSKZ
searchResult := searchResult "LEFT JOIN BUERO.SKZ_STEUERKUNDENZU ON SKZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SLRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SLR_LETZTERUSER" "~"
if joinKuerzel = BMUSLR
searchResult := searchResult "LEFT JOIN BUERO.SLR_SKILLREGELELEMENT ON SLR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SMABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SMA_LETZTERUSER" "~"
if joinKuerzel = BMUSMA
searchResult := searchResult "LEFT JOIN HRS.SMA_SCHNITTMA ON SMA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPK_LETZTERUSER" "~"
if joinKuerzel = BMUSPK
searchResult := searchResult "LEFT JOIN BMD.SPK_STAPELKOPF ON SPK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPX_LETZTERUSER" "~"
if joinKuerzel = BMUSPX
searchResult := searchResult "LEFT JOIN BMD.SPX_STAPELVARIABLE ON SPX_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STP_LETZTERUSER" "~"
if joinKuerzel = BMUSTP
searchResult := searchResult "LEFT JOIN ZLE.STP_STAFFELPOS ON STP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TAEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TAE_LETZTERUSER" "~"
if joinKuerzel = BMUTAE
searchResult := searchResult "LEFT JOIN WWS.TAE_AUFTSCHNELLERF_TMP ON TAE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TAZ_LETZTERUSER" "~"
if joinKuerzel = BMUTAZ
searchResult := searchResult "LEFT JOIN BMDENTW.TAZ_TICKETAUFGABEZU ON TAZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TCABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TCA_LETZTERUSER" "~"
if joinKuerzel = BMUTCA
searchResult := searchResult "LEFT JOIN BMD.TCA_TCACTIONLIST ON TCA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TMOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TMO_LETZTERUSER" "~"
if joinKuerzel = BMUTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TRXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TRX_LETZTERUSER" "~"
if joinKuerzel = BMUTRX
searchResult := searchResult "LEFT JOIN WWS.TRX_TEILSCHLUSSRECHSCHEMA ON TRX_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TSWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TSW_LETZTERUSER" "~"
if joinKuerzel = BMUTSW
searchResult := searchResult "LEFT JOIN WWS.TSW_STATISTIKWERTE_TMP ON TSW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TTBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TTB_LETZTERUSER" "~"
if joinKuerzel = BMUTTB
searchResult := searchResult "LEFT JOIN BIL.TTB_TANTIEMENBERECHNUNG ON TTB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PFDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PFD_LETZTERUSER" "~"
if joinKuerzel = BMUPFD
searchResult := searchResult "LEFT JOIN HRS.PFD_PFAENDUNG_DETAIL ON PFD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PFEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PFE_LETZTERUSER" "~"
if joinKuerzel = BMUPFE
searchResult := searchResult "LEFT JOIN BMD.PFE_PANELFILTERELEM ON PFE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PFTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PFT_LETZTERUSER" "~"
if joinKuerzel = BMUPFT
searchResult := searchResult "LEFT JOIN HRS.PFT_PFAENDUNG_TILGUNG ON PFT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIA_LETZTERUSER" "~"
if joinKuerzel = BMUPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIO_LETZTERUSER" "~"
if joinKuerzel = BMUPIO
searchResult := searchResult "LEFT JOIN WWS.PIO_PICARTIKELDOKZU ON PIO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PITBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIT_LETZTERUSER" "~"
if joinKuerzel = BMUPIT
searchResult := searchResult "LEFT JOIN BMD.PIT_POSTIT ON PIT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKM_LETZTERUSER" "~"
if joinKuerzel = BMUPKM
searchResult := searchResult "LEFT JOIN PRODUKTION.PKM_KM_MASTER ON PKM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKP_LETZTERUSER" "~"
if joinKuerzel = BMUPKP
searchResult := searchResult "LEFT JOIN FIBU.PKP_BUDGETPERSKOSTPLAN ON PKP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKU_LETZTERUSER" "~"
if joinKuerzel = BMUPKU
searchResult := searchResult "LEFT JOIN BUERO.PKU_PROJEKTZU ON PKU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKYBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKY_LETZTERUSER" "~"
if joinKuerzel = BMUPKY
searchResult := searchResult "LEFT JOIN BIL.PKY_PKH_PKK_ZU ON PKY_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PNZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PNZ_LETZTERUSER" "~"
if joinKuerzel = BMUPNZ
searchResult := searchResult "LEFT JOIN BUERO.PNZ_PERSONENZUSATZ ON PNZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PPABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PPA_LETZTERUSER" "~"
if joinKuerzel = BMUPPA
searchResult := searchResult "LEFT JOIN PRODUKTION.PPA_PSPPARAM ON PPA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PRDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PRD_LETZTERUSER" "~"
if joinKuerzel = BMUPRD
searchResult := searchResult "LEFT JOIN ZLE.PRD_PERIODE ON PRD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PRTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PRT_LETZTERUSER" "~"
if joinKuerzel = BMUPRT
searchResult := searchResult "LEFT JOIN BUERO.PRT_PROJEKTTAETIGKEIT ON PRT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PSJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PSJ_LETZTERUSER" "~"
if joinKuerzel = BMUPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PTSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PTS_LETZTERUSER" "~"
if joinKuerzel = BMUPTS
searchResult := searchResult "LEFT JOIN WWS.PTS_PREISTABPOSSTAFFEL ON PTS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PTTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PTT_LETZTERUSER" "~"
if joinKuerzel = BMUPTT
searchResult := searchResult "LEFT JOIN BMD.PTT_PERFORMANCETESTTAB_TMP ON PTT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PTZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PTZ_LETZTERUSER" "~"
if joinKuerzel = BMUPTZ
searchResult := searchResult "LEFT JOIN WWS.PTZ_PREISTABZU ON PTZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PWRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PWR_LETZTERUSER" "~"
if joinKuerzel = BMUPWR
searchResult := searchResult "LEFT JOIN WWS.PWR_RABATTWARTUNGPOS ON PWR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RAD_LETZTERUSER" "~"
if joinKuerzel = BMURAD
searchResult := searchResult "LEFT JOIN WWS.RAD_RABATTBETRAGDETAIL ON RAD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RAS_LETZTERUSER" "~"
if joinKuerzel = BMURAS
searchResult := searchResult "LEFT JOIN WWS.RAS_RABATTTABPOSSTAFFEL ON RAS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RDDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RDD_LETZTERUSER" "~"
if joinKuerzel = BMURDD
searchResult := searchResult "LEFT JOIN BIL.RDD_RSTDRUCKDETAIL ON RDD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = REIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = REI_LETZTERUSER" "~"
if joinKuerzel = BMUREI
searchResult := searchResult "LEFT JOIN HRS.REI_REISESTAMM ON REI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RETBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RET_LETZTERUSER" "~"
if joinKuerzel = BMURET
searchResult := searchResult "LEFT JOIN BIL.RET_BEWRES_ERFASS_TMP ON RET_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RGMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RGM_LETZTERUSER" "~"
if joinKuerzel = BMURGM
searchResult := searchResult "LEFT JOIN DATANORM.RGM_RABATT_GRUPPE ON RGM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RKGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RKG_LETZTERUSER" "~"
if joinKuerzel = BMURKG
searchResult := searchResult "LEFT JOIN BMD.RKG_TABREPLGRUPPE ON RKG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KUSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KUS_LETZTERUSER" "~"
if joinKuerzel = BMUKUS
searchResult := searchResult "LEFT JOIN BMD.KUS_KURS ON KUS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LAABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAA_LETZTERUSER" "~"
if joinKuerzel = BMULAA
searchResult := searchResult "LEFT JOIN WWS.LAA_LAGERART ON LAA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LABBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAB_LETZTERUSER" "~"
if joinKuerzel = BMULAB
searchResult := searchResult "LEFT JOIN BIL.LAB_LATENTESTEUERN_BELEGZU ON LAB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LAJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAJ_LETZTERUSER" "~"
if joinKuerzel = BMULAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LAQBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAQ_LETZTERUSER" "~"
if joinKuerzel = BMULAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAS_LETZTERUSER" "~"
if joinKuerzel = BMULAS
searchResult := searchResult "LEFT JOIN BIL.LAS_LATENTE_STEUERN ON LAS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LGDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LGD_LETZTERUSER" "~"
if joinKuerzel = BMULGD
searchResult := searchResult "LEFT JOIN DATANORM.LGD_LEISTUNG_DETAIL ON LGD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LGFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LGF_LETZTERUSER" "~"
if joinKuerzel = BMULGF
searchResult := searchResult "LEFT JOIN HRS.LGF_LGVMAKROS ON LGF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LIBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LIB_LETZTERUSER" "~"
if joinKuerzel = BMULIB
searchResult := searchResult "LEFT JOIN WWS.LIB_LIEFERBEDINGUNG ON LIB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LOZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LOZ_LETZTERUSER" "~"
if joinKuerzel = BMULOZ
searchResult := searchResult "LEFT JOIN HRS.LOZ_LOHNERFASSUNGZLE ON LOZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LRKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LRK_LETZTERUSER" "~"
if joinKuerzel = BMULRK
searchResult := searchResult "LEFT JOIN BIL.LRK_LANGFRISTIGE_RSTKONTEN ON LRK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LSKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LSK_LETZTERUSER" "~"
if joinKuerzel = BMULSK
searchResult := searchResult "LEFT JOIN BIL.LSK_LATENTESTEUERN_KONTEN ON LSK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LTXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LTX_LETZTERUSER" "~"
if joinKuerzel = BMULTX
searchResult := searchResult "LEFT JOIN BUERO.LTX_LANGTEXT ON LTX_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LVKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LVK_LETZTERUSER" "~"
if joinKuerzel = BMULVK
searchResult := searchResult "LEFT JOIN BMDINTERN.LVK_LIZENZVKP ON LVK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LZFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LZF_LETZTERUSER" "~"
if joinKuerzel = BMULZF
searchResult := searchResult "LEFT JOIN HRS.LZF_LOHNZETTEL_FIN ON LZF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MITBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MIT_LETZTERUSER" "~"
if joinKuerzel = BMUMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MKOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MKO_LETZTERUSER" "~"
if joinKuerzel = BMUMKO
searchResult := searchResult "LEFT JOIN BIL.MKO_MINDESTKOEST ON MKO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NEGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NEG_LETZTERUSER" "~"
if joinKuerzel = BMUNEG
searchResult := searchResult "LEFT JOIN BIL.NEG_NICHTENTGEWINNE ON NEG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NGZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NGZ_LETZTERUSER" "~"
if joinKuerzel = BMUNGZ
searchResult := searchResult "LEFT JOIN BUERO.NGZ_NOTIZGRUPPEZU ON NGZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OAK_LETZTERUSER" "~"
if joinKuerzel = BMUOAK
searchResult := searchResult "LEFT JOIN LOHN.OAK_OF_AKREIS ON OAK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OAUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OAU_LETZTERUSER" "~"
if joinKuerzel = BMUOAU
searchResult := searchResult "LEFT JOIN LOHN.OAU_OF_AUFTRAG ON OAU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OBPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OBP_LETZTERUSER" "~"
if joinKuerzel = BMUOBP
searchResult := searchResult "LEFT JOIN BUERO.OBP_ONLBEWPERSSTAMM ON OBP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OBRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OBR_LETZTERUSER" "~"
if joinKuerzel = BMUOBR
searchResult := searchResult "LEFT JOIN LOHN.OBR_OF_BETRIEBSRAT ON OBR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ONSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ONS_LETZTERUSER" "~"
if joinKuerzel = BMUONS
searchResult := searchResult "LEFT JOIN HRS.ONS_ONLINEBEW_STAMMDATEN ON ONS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ONZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ONZ_LETZTERUSER" "~"
if joinKuerzel = BMUONZ
searchResult := searchResult "LEFT JOIN HRS.ONZ_ONLINEBEW_STELLE_ZU ON ONZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OROBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ORO_LETZTERUSER" "~"
if joinKuerzel = BMUORO
searchResult := searchResult "LEFT JOIN LOHN.ORO_OF_REGORT ON ORO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PZEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PZE_LETZTERUSER" "~"
if joinKuerzel = BMUPZE
searchResult := searchResult "LEFT JOIN HRS.PZE_PERKZGLGZEILE ON PZE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RAEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RAE_LETZTERUSER" "~"
if joinKuerzel = BMURAE
searchResult := searchResult "LEFT JOIN FIBU.RAE_RATE ON RAE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RAGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RAG_LETZTERUSER" "~"
if joinKuerzel = BMURAG
searchResult := searchResult "LEFT JOIN BUERO.RAG_RUHEARBEITSZEITGESETZ ON RAG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RDMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RDM_LETZTERUSER" "~"
if joinKuerzel = BMURDM
searchResult := searchResult "LEFT JOIN BIL.RDM_RSTDRUCKMASTER ON RDM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RDRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RDR_LETZTERUSER" "~"
if joinKuerzel = BMURDR
searchResult := searchResult "LEFT JOIN ZLE.RDR_RECHDRUCKSESSION ON RDR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RELBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = REL_LETZTERUSER" "~"
if joinKuerzel = BMUREL
searchResult := searchResult "LEFT JOIN BUERO.REL_RELIGION ON REL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RGZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RGZ_LETZTERUSER" "~"
if joinKuerzel = BMURGZ
searchResult := searchResult "LEFT JOIN BMD.RGZ_ROLLEGRUPPEZU ON RGZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RIABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RIA_LETZTERUSER" "~"
if joinKuerzel = BMURIA
searchResult := searchResult "LEFT JOIN PRODUKTION.RIA_RISIKOANALYSE ON RIA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RIHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RIH_LETZTERUSER" "~"
if joinKuerzel = BMURIH
searchResult := searchResult "LEFT JOIN PRODUKTION.RIH_RISIKOANALYSE_HEAD ON RIH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RPTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RPT_LETZTERUSER" "~"
if joinKuerzel = BMURPT
searchResult := searchResult "LEFT JOIN BMD.RPT_REPORT ON RPT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RSEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RSE_LETZTERUSER" "~"
if joinKuerzel = BMURSE
searchResult := searchResult "LEFT JOIN WWS.RSE_RESERVIERUNGSSYSEXT ON RSE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RVDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RVD_LETZTERUSER" "~"
if joinKuerzel = BMURVD
searchResult := searchResult "LEFT JOIN ZLE.RVD_RECHVORLAGEDETAIL ON RVD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RZGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RZG_LETZTERUSER" "~"
if joinKuerzel = BMURZG
searchResult := searchResult "LEFT JOIN BUERO.RZG_RUHEZEITGESETZ ON RZG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SA2BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SA2_LETZTERUSER" "~"
if joinKuerzel = BMUSA2
searchResult := searchResult "LEFT JOIN ONVIS.SA2_SAURER_BDE ON SA2_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SAWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SAW_LETZTERUSER" "~"
if joinKuerzel = BMUSAW
searchResult := searchResult "LEFT JOIN WWSINDIV.SAW_STATISTIKWERTE ON SAW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SAZ_LETZTERUSER" "~"
if joinKuerzel = BMUSAZ
searchResult := searchResult "LEFT JOIN BUERO.SAZ_SACHKONTOARCHIVZU ON SAZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SBGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SBG_LETZTERUSER" "~"
if joinKuerzel = BMUSBG
searchResult := searchResult "LEFT JOIN BMD.SBG_STDBRIEFGRUPPE ON SBG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCH_LETZTERUSER" "~"
if joinKuerzel = BMUSCH
searchResult := searchResult "LEFT JOIN BMD.SCH_SCHEDULE ON SCH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCI_LETZTERUSER" "~"
if joinKuerzel = BMUSCI
searchResult := searchResult "LEFT JOIN PRODUKTION.SCI_SCINSTANZDET ON SCI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCN_LETZTERUSER" "~"
if joinKuerzel = BMUSCN
searchResult := searchResult "LEFT JOIN BMDENTW.SCN_SCRNOTIZ ON SCN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCQBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCQ_LETZTERUSER" "~"
if joinKuerzel = BMUSCQ
searchResult := searchResult "LEFT JOIN PRODUKTION.SCQ_SCKZGRP ON SCQ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCR_LETZTERUSER" "~"
if joinKuerzel = BMUSCR
searchResult := searchResult "LEFT JOIN BMDENTW.SCR_SCRREGELWERK ON SCR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCV_LETZTERUSER" "~"
if joinKuerzel = BMUSCV
searchResult := searchResult "LEFT JOIN BMDENTW.SCV_SCRREVISION ON SCV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SEBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SEB_LETZTERUSER" "~"
if joinKuerzel = BMUSEB
searchResult := searchResult "LEFT JOIN BIL.SEB_STEUERERKLBAUM ON SEB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SIDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SID_LETZTERUSER" "~"
if joinKuerzel = BMUSID
searchResult := searchResult "LEFT JOIN HRS.SID_HRSSEQ ON SID_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SN2BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SN2_LETZTERUSER" "~"
if joinKuerzel = BMUSN2
searchResult := searchResult "LEFT JOIN WWS.SN2_VA_SNR_SERIENNUMMERN ON SN2_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UEBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UEB_LETZTERUSER" "~"
if joinKuerzel = BMUUEB
searchResult := searchResult "LEFT JOIN BUERO.UEB_UEBERWEISUNGSART ON UEB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UPHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UPH_LETZTERUSER" "~"
if joinKuerzel = BMUUPH
searchResult := searchResult "LEFT JOIN BMDENTW.UPH_UPDATEHINWEIS ON UPH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UTUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UTU_LETZTERUSER" "~"
if joinKuerzel = BMUUTU
searchResult := searchResult "LEFT JOIN BMD.UTU_UTABUEBERS ON UTU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VEPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VEP_LETZTERUSER" "~"
if joinKuerzel = BMUVEP
searchResult := searchResult "LEFT JOIN WWS.VEP_VERTRETERABRECHPOS ON VEP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VFDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VFD_LETZTERUSER" "~"
if joinKuerzel = BMUVFD
searchResult := searchResult "LEFT JOIN PRODUKTION.VFD_VERFAHRENSANWEISUNG_POS ON VFD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VMKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VMK_LETZTERUSER" "~"
if joinKuerzel = BMUVMK
searchResult := searchResult "LEFT JOIN WWS.VMK_VERSMGR_KOPF ON VMK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VPBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VPB_LETZTERUSER" "~"
if joinKuerzel = BMUVPB
searchResult := searchResult "LEFT JOIN WWS.VPB_VPERMITTLUNG_ERKZUORD ON VPB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VPFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VPF_LETZTERUSER" "~"
if joinKuerzel = BMUVPF
searchResult := searchResult "LEFT JOIN BMD.VPF_VPFAD ON VPF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VRIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VRI_LETZTERUSER" "~"
if joinKuerzel = BMUVRI
searchResult := searchResult "LEFT JOIN BUERO.VRI_VERSIONINFO ON VRI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VTZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VTZ_LETZTERUSER" "~"
if joinKuerzel = BMUVTZ
searchResult := searchResult "LEFT JOIN BUERO.VTZ_VERANSTALTUNGTERMINZU ON VTZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WAABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAA_LETZTERUSER" "~"
if joinKuerzel = BMUWAA
searchResult := searchResult "LEFT JOIN BIL.WAA_WIPAKTENART ON WAA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WAFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAF_LETZTERUSER" "~"
if joinKuerzel = BMUWAF
searchResult := searchResult "LEFT JOIN WWS.WAF_WWSADRZUFIRMENBEZ ON WAF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WBDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WBD_LETZTERUSER" "~"
if joinKuerzel = BMUWBD
searchResult := searchResult "LEFT JOIN WWS.WBD_WARENBESCHKOSTENDET ON WBD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WCDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WCD_LETZTERUSER" "~"
if joinKuerzel = BMUWCD
searchResult := searchResult "LEFT JOIN BMD.WCD_WAPARAMDEF ON WCD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WCWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WCW_LETZTERUSER" "~"
if joinKuerzel = BMUWCW
searchResult := searchResult "LEFT JOIN BMD.WCW_WAAUSDRUCKPARAMWERT ON WCW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEI_LETZTERUSER" "~"
if joinKuerzel = BMUWEI
searchResult := searchResult "LEFT JOIN WWS.WEI_WARENEINSATZKOPF ON WEI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WELBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEL_LETZTERUSER" "~"
if joinKuerzel = BMUWEL
searchResult := searchResult "LEFT JOIN WWS.WEL_WARENEINSATZLAGERZU ON WEL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEO_LETZTERUSER" "~"
if joinKuerzel = BMUWEO
searchResult := searchResult "LEFT JOIN WWS.WEO_WARENEINSATZLPO ON WEO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEP_LETZTERUSER" "~"
if joinKuerzel = BMUWEP
searchResult := searchResult "LEFT JOIN WWS.WEP_WWSERKONTROLLEPOS ON WEP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WFDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WFD_LETZTERUSER" "~"
if joinKuerzel = BMUWFD
searchResult := searchResult "LEFT JOIN WWS.WFD_FOLGEARTIKELDETAIL ON WFD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WFGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WFG_LETZTERUSER" "~"
if joinKuerzel = BMUWFG
searchResult := searchResult "LEFT JOIN WWS.WFG_FOLGEARTIKELGRUPPE ON WFG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WFHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WFH_LETZTERUSER" "~"
if joinKuerzel = BMUWFH
searchResult := searchResult "LEFT JOIN WWS.WFH_FERTEP_HISTORIE ON WFH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WGBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WGB_LETZTERUSER" "~"
if joinKuerzel = BMUWGB
searchResult := searchResult "LEFT JOIN FIBU.WGB_WARENEINGANGSBUCH ON WGB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WIBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WIB_LETZTERUSER" "~"
if joinKuerzel = BMUWIB
searchResult := searchResult "LEFT JOIN BIL.WIB_WIBERICHTSART ON WIB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WIIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WII_LETZTERUSER" "~"
if joinKuerzel = BMUWII
searchResult := searchResult "LEFT JOIN BMDENTW.WII_WEITERE_INFO_ITEM ON WII_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WILBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WIL_LETZTERUSER" "~"
if joinKuerzel = BMUWIL
searchResult := searchResult "LEFT JOIN BMDENTW.WIL_WEITERE_INFO_LANG ON WIL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RKSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RKS_LETZTERUSER" "~"
if joinKuerzel = BMURKS
searchResult := searchResult "LEFT JOIN BMD.RKS_REPLIKATIONSSTANDORT ON RKS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RNPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RNP_LETZTERUSER" "~"
if joinKuerzel = BMURNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RNRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RNR_LETZTERUSER" "~"
if joinKuerzel = BMURNR
searchResult := searchResult "LEFT JOIN WWS.RNR_RECHNUNGSRABATT ON RNR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RSKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RSK_LETZTERUSER" "~"
if joinKuerzel = BMURSK
searchResult := searchResult "LEFT JOIN WWS.RSK_REGISTRIERKASSE ON RSK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RSPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RSP_LETZTERUSER" "~"
if joinKuerzel = BMURSP
searchResult := searchResult "LEFT JOIN BIL.RSP_RSTSPALTENKOPF ON RSP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RTZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RTZ_LETZTERUSER" "~"
if joinKuerzel = BMURTZ
searchResult := searchResult "LEFT JOIN ZLE.RTZ_RECHTEXTZU ON RTZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SARBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SAR_LETZTERUSER" "~"
if joinKuerzel = BMUSAR
searchResult := searchResult "LEFT JOIN HRS.SAR_SCHWERARBEIT ON SAR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SBFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SBF_LETZTERUSER" "~"
if joinKuerzel = BMUSBF
searchResult := searchResult "LEFT JOIN BMD.SBF_STDBRIEF ON SBF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCB_LETZTERUSER" "~"
if joinKuerzel = BMUSCB
searchResult := searchResult "LEFT JOIN BMDENTW.SCB_SCRPERSZU ON SCB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCD_LETZTERUSER" "~"
if joinKuerzel = BMUSCD
searchResult := searchResult "LEFT JOIN BMDENTW.SCD_SCRDEPSCR ON SCD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCYBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCY_LETZTERUSER" "~"
if joinKuerzel = BMUSCY
searchResult := searchResult "LEFT JOIN BMDENTW.SCY_SCRDEPENDENCY ON SCY_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SEKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SEK_LETZTERUSER" "~"
if joinKuerzel = BMUSEK
searchResult := searchResult "LEFT JOIN BUERO.SEK_SEKRETARIAT ON SEK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPE_LETZTERUSER" "~"
if joinKuerzel = BMUSPE
searchResult := searchResult "LEFT JOIN HRS.SPE_SPESENARTEN ON SPE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPN_LETZTERUSER" "~"
if joinKuerzel = BMUSPN
searchResult := searchResult "LEFT JOIN FIBU.SPN_SPENDE ON SPN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPQBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPQ_LETZTERUSER" "~"
if joinKuerzel = BMUSPQ
searchResult := searchResult "LEFT JOIN HRS.SPQ_SPESENARTEN_SEQ ON SPQ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPR_LETZTERUSER" "~"
if joinKuerzel = BMUSPR
searchResult := searchResult "LEFT JOIN BUERO.SPR_SPRACHE ON SPR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPZ_LETZTERUSER" "~"
if joinKuerzel = BMUSPZ
searchResult := searchResult "LEFT JOIN BMD.SPZ_SESSIONPAKETZU ON SPZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SRBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SRB_LETZTERUSER" "~"
if joinKuerzel = BMUSRB
searchResult := searchResult "LEFT JOIN BIL.SRB_STEUERBUCHUNGEN ON SRB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STC_LETZTERUSER" "~"
if joinKuerzel = BMUSTC
searchResult := searchResult "LEFT JOIN FIBU.STC_STEUERCODE ON STC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SUKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SUK_LETZTERUSER" "~"
if joinKuerzel = BMUSUK
searchResult := searchResult "LEFT JOIN HRS.SUK_SUM_KOE ON SUK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SWSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SWS_LETZTERUSER" "~"
if joinKuerzel = BMUSWS
searchResult := searchResult "LEFT JOIN HRS.SWS_SW_STUNDEN ON SWS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SYNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SYN_LETZTERUSER" "~"
if joinKuerzel = BMUSYN
searchResult := searchResult "LEFT JOIN BMD.SYN_SYNCDATEN ON SYN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SZPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SZP_LETZTERUSER" "~"
if joinKuerzel = BMUSZP
searchResult := searchResult "LEFT JOIN BUERO.SZP_SRVRECHZENTRUMPROT ON SZP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SZZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SZZ_LETZTERUSER" "~"
if joinKuerzel = BMUSZZ
searchResult := searchResult "LEFT JOIN PRODUKTION.SZZ_STAKE_ZU ON SZZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TBNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TBN_LETZTERUSER" "~"
if joinKuerzel = BMUTBN
searchResult := searchResult "LEFT JOIN BUERO.TBN_TERMINBENACHRICHTIGUNG ON TBN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TBRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TBR_LETZTERUSER" "~"
if joinKuerzel = BMUTBR
searchResult := searchResult "LEFT JOIN HRS.TBR_BESCHAEFTIGTENGRUPPE ON TBR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TBTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TBT_LETZTERUSER" "~"
if joinKuerzel = BMUTBT
searchResult := searchResult "LEFT JOIN BMDENTW.TBT_TICKETKATEGORIEZU ON TBT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OTDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OTD_LETZTERUSER" "~"
if joinKuerzel = BMUOTD
searchResult := searchResult "LEFT JOIN BMD.OTD_OCRTEMPLATEDETAIL ON OTD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OUBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OUB_LETZTERUSER" "~"
if joinKuerzel = BMUOUB
searchResult := searchResult "LEFT JOIN BMD.OUB_OUTLOOKBAR ON OUB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OUFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OUF_LETZTERUSER" "~"
if joinKuerzel = BMUOUF
searchResult := searchResult "LEFT JOIN BUERO.OUF_ONLINEUMFRAGE ON OUF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PARBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAR_LETZTERUSER" "~"
if joinKuerzel = BMUPAR
searchResult := searchResult "LEFT JOIN BMDDEFAULT.PAR_PARAM ON PAR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PATBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAT_LETZTERUSER" "~"
if joinKuerzel = BMUPAT
searchResult := searchResult "LEFT JOIN WWS.PAT_PACKSTOFFTAB ON PAT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PBOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PBO_LETZTERUSER" "~"
if joinKuerzel = BMUPBO
searchResult := searchResult "LEFT JOIN BIL.PBO_PERSKORST_KONTEN ON PBO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PBRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PBR_LETZTERUSER" "~"
if joinKuerzel = BMUPBR
searchResult := searchResult "LEFT JOIN BUERO.PBR_PERSONBRANCHEZU ON PBR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PCKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PCK_LETZTERUSER" "~"
if joinKuerzel = BMUPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PCPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PCP_LETZTERUSER" "~"
if joinKuerzel = BMUPCP
searchResult := searchResult "LEFT JOIN PRODUKTION.PCP_PPS_PRODUKT ON PCP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PCZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PCZ_LETZTERUSER" "~"
if joinKuerzel = BMUPCZ
searchResult := searchResult "LEFT JOIN BUERO.PCZ_PERSONKZCOMPASSZU ON PCZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PDWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PDW_LETZTERUSER" "~"
if joinKuerzel = BMUPDW
searchResult := searchResult "LEFT JOIN BMDDEFAULT.PDW_PARAMWERT ON PDW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PDZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PDZ_LETZTERUSER" "~"
if joinKuerzel = BMUPDZ
searchResult := searchResult "LEFT JOIN BUERO.PDZ_PROJEKTWERTDOKZU ON PDZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PII_LETZTERUSER" "~"
if joinKuerzel = BMUPII
searchResult := searchResult "LEFT JOIN WWS.PII_PICKUNDENGRUPPE ON PII_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKD_LETZTERUSER" "~"
if joinKuerzel = BMUPKD
searchResult := searchResult "LEFT JOIN PRODUKTION.PKD_KM_DETAIL ON PKD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PLGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PLG_LETZTERUSER" "~"
if joinKuerzel = BMUPLG
searchResult := searchResult "LEFT JOIN BUERO.PLG_PLANGRUPPE ON PLG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PLIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PLI_LETZTERUSER" "~"
if joinKuerzel = BMUPLI
searchResult := searchResult "LEFT JOIN BUERO.PLI_PIPELINE ON PLI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PLZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PLZ_LETZTERUSER" "~"
if joinKuerzel = BMUPLZ
searchResult := searchResult "LEFT JOIN BUERO.PLZ_PLANGRUPPEZU ON PLZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PNKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PNK_LETZTERUSER" "~"
if joinKuerzel = BMUPNK
searchResult := searchResult "LEFT JOIN BUERO.PNK_PERSONNUMMERNKREIS ON PNK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PSYBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PSY_LETZTERUSER" "~"
if joinKuerzel = BMUPSY
searchResult := searchResult "LEFT JOIN WWS.PSY_PICSYNCFELDZU ON PSY_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PVFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PVF_LETZTERUSER" "~"
if joinKuerzel = BMUPVF
searchResult := searchResult "LEFT JOIN BUERO.PVF_PERSONENVORERFASSUNG ON PVF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PZFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PZF_LETZTERUSER" "~"
if joinKuerzel = BMUPZF
searchResult := searchResult "LEFT JOIN WWS.PZF_PICFARBENZU ON PZF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = QSTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = QST_LETZTERUSER" "~"
if joinKuerzel = BMUQST
searchResult := searchResult "LEFT JOIN PRODUKTION.QST_QM_STAMM ON QST_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = REFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = REF_LETZTERUSER" "~"
if joinKuerzel = BMUREF
searchResult := searchResult "LEFT JOIN BUERO.REF_REFERENZ ON REF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = REHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = REH_LETZTERUSER" "~"
if joinKuerzel = BMUREH
searchResult := searchResult "LEFT JOIN PRODUKTION.REH_REIHE ON REH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RLKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RLK_LETZTERUSER" "~"
if joinKuerzel = BMURLK
searchResult := searchResult "LEFT JOIN WWS.RLK_RECHNUNGSPLANKOPF ON RLK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RLPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RLP_LETZTERUSER" "~"
if joinKuerzel = BMURLP
searchResult := searchResult "LEFT JOIN WWS.RLP_RECHNUNGSPLANPOS ON RLP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SRABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SRA_LETZTERUSER" "~"
if joinKuerzel = BMUSRA
searchResult := searchResult "LEFT JOIN WWS.SRA_STEUERART ON SRA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SSKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SSK_LETZTERUSER" "~"
if joinKuerzel = BMUSSK
searchResult := searchResult "LEFT JOIN PRODUKTION.SSK_SAMMELSCHEIN_KOPF ON SSK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = T18BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = T18_LETZTERUSER" "~"
if joinKuerzel = BMUT18
searchResult := searchResult "LEFT JOIN HRS.T18_ELDADETAIL_E18_TMP ON T18_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TAD_LETZTERUSER" "~"
if joinKuerzel = BMUTAD
searchResult := searchResult "LEFT JOIN HRS.TAD_ELDADETAIL_AD_TMP ON TAD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TAGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TAG_LETZTERUSER" "~"
if joinKuerzel = BMUTAG
searchResult := searchResult "LEFT JOIN ZLE.TAG_TAGESABSCHLUSS ON TAG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TAK_LETZTERUSER" "~"
if joinKuerzel = BMUTAK
searchResult := searchResult "LEFT JOIN WWS.TAK_ARTIKEL_KAUFAUSW_TMP ON TAK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TARBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TAR_LETZTERUSER" "~"
if joinKuerzel = BMUTAR
searchResult := searchResult "LEFT JOIN BUERO.TAR_TERMINARTIKELZU ON TAR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TAVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TAV_LETZTERUSER" "~"
if joinKuerzel = BMUTAV
searchResult := searchResult "LEFT JOIN BUERO.TAV_VERTEILUNG ON TAV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TBABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TBA_LETZTERUSER" "~"
if joinKuerzel = BMUTBA
searchResult := searchResult "LEFT JOIN BUERO.TBA_TAGESMDBUCHARTAUFB ON TBA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TCKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TCK_LETZTERUSER" "~"
if joinKuerzel = BMUTCK
searchResult := searchResult "LEFT JOIN BMD.TCK_TCKONFIGURATIONEN ON TCK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TEABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TEA_LETZTERUSER" "~"
if joinKuerzel = BMUTEA
searchResult := searchResult "LEFT JOIN BUERO.TEA_TELEFONART ON TEA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TEHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TEH_LETZTERUSER" "~"
if joinKuerzel = BMUTEH
searchResult := searchResult "LEFT JOIN BUERO.TEH_TELEKOMHARDWARE ON TEH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TFKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TFK_LETZTERUSER" "~"
if joinKuerzel = BMUTFK
searchResult := searchResult "LEFT JOIN BMD.TFK_FELDBESCHKOPF_TMP ON TFK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TGPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TGP_LETZTERUSER" "~"
if joinKuerzel = BMUTGP
searchResult := searchResult "LEFT JOIN HRS.TGP_TARIFGR_TARIFPROZ_ZU ON TGP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TIPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TIP_LETZTERUSER" "~"
if joinKuerzel = BMUTIP
searchResult := searchResult "LEFT JOIN BUERO.TIP_TERMINALIMPPROTOKOLL ON TIP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TMZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TMZ_LETZTERUSER" "~"
if joinKuerzel = BMUTMZ
searchResult := searchResult "LEFT JOIN BUERO.TMZ_TAGESMDPRUEFZUORD ON TMZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TREBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TRE_LETZTERUSER" "~"
if joinKuerzel = BMUTRE
searchResult := searchResult "LEFT JOIN WWS.TRE_TRANSPORTEINHEIT ON TRE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TSABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TSA_LETZTERUSER" "~"
if joinKuerzel = BMUTSA
searchResult := searchResult "LEFT JOIN FIBU.TSA_TEILSCHLUSSRECHABLAUF ON TSA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VAK_LETZTERUSER" "~"
if joinKuerzel = BMUVAK
searchResult := searchResult "LEFT JOIN BUERO.VAK_VERANSTALTUNGSKOPF ON VAK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VEBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VEB_LETZTERUSER" "~"
if joinKuerzel = BMUVEB
searchResult := searchResult "LEFT JOIN FIBU.VEB_VORERFASSBUCHUNG ON VEB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VFRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VFR_LETZTERUSER" "~"
if joinKuerzel = BMUVFR
searchResult := searchResult "LEFT JOIN WWS.VFR_VERLEIHFREISTELLUNG ON VFR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VLJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VLJ_LETZTERUSER" "~"
if joinKuerzel = BMUVLJ
searchResult := searchResult "LEFT JOIN WWS.VLJ_VERLEIHJOURNAL ON VLJ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VPHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VPH_LETZTERUSER" "~"
if joinKuerzel = BMUVPH
searchResult := searchResult "LEFT JOIN WWS.VPH_VPERMITTLUNG_FARZUORD ON VPH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VPKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VPK_LETZTERUSER" "~"
if joinKuerzel = BMUVPK
searchResult := searchResult "LEFT JOIN WWS.VPK_VPERMITTLUNG_KUGZUORD ON VPK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VPUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VPU_LETZTERUSER" "~"
if joinKuerzel = BMUVPU
searchResult := searchResult "LEFT JOIN BMD.VPU_VPFADUSER ON VPU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VPWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VPW_LETZTERUSER" "~"
if joinKuerzel = BMUVPW
searchResult := searchResult "LEFT JOIN BUERO.VPW_VPIWERTE ON VPW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WKGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WKG_LETZTERUSER" "~"
if joinKuerzel = BMUWKG
searchResult := searchResult "LEFT JOIN WWS.WKG_WWSKUNDENGRUPPE ON WKG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WKRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WKR_LETZTERUSER" "~"
if joinKuerzel = BMUWKR
searchResult := searchResult "LEFT JOIN WWS.WKR_WARENAUSGKOSTENRECH ON WKR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = XFBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = XFB_LETZTERUSER" "~"
if joinKuerzel = BMUXFB
searchResult := searchResult "LEFT JOIN BMDENTW.XFB_GELOESCHTE_FB_DATEN ON XFB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = YBMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = YBM_LETZTERUSER" "~"
if joinKuerzel = BMUYBM
searchResult := searchResult "LEFT JOIN FIBU.YBM_AUTOBUCHMODELLE ON YBM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZAS_LETZTERUSER" "~"
if joinKuerzel = BMUZAS
searchResult := searchResult "LEFT JOIN BUERO.ZAS_ZAHLSCHEIN ON ZAS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZATBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZAT_LETZTERUSER" "~"
if joinKuerzel = BMUZAT
searchResult := searchResult "LEFT JOIN BUERO.ZAT_ZEITAUSWERTTYP ON ZAT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZBABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZBA_LETZTERUSER" "~"
if joinKuerzel = BMUZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZBZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZBZ_LETZTERUSER" "~"
if joinKuerzel = BMUZBZ
searchResult := searchResult "LEFT JOIN BUERO.ZBZ_ZEITBEZUGSART ON ZBZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZDKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZDK_LETZTERUSER" "~"
if joinKuerzel = BMUZDK
searchResult := searchResult "LEFT JOIN BMD.ZDK_ZEILENDEFKOPF ON ZDK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZEKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZEK_LETZTERUSER" "~"
if joinKuerzel = BMUZEK
searchResult := searchResult "LEFT JOIN WWS.ZEK_ZEITEINHEITENKOPF ON ZEK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZIDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZID_LETZTERUSER" "~"
if joinKuerzel = BMUZID
searchResult := searchResult "LEFT JOIN BMD.ZID_ZEILENDEF ON ZID_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZIKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZIK_LETZTERUSER" "~"
if joinKuerzel = BMUZIK
searchResult := searchResult "LEFT JOIN FIBU.ZIK_KONTENZINSKREIS ON ZIK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZIWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZIW_LETZTERUSER" "~"
if joinKuerzel = BMUZIW
searchResult := searchResult "LEFT JOIN BUERO.ZIW_ZUSATZINFOWERT ON ZIW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZKRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZKR_LETZTERUSER" "~"
if joinKuerzel = BMUZKR
searchResult := searchResult "LEFT JOIN FIBU.ZKR_ZAHLUNGSKREIS ON ZKR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZMSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZMS_LETZTERUSER" "~"
if joinKuerzel = BMUZMS
searchResult := searchResult "LEFT JOIN BUERO.ZMS_ZEITMONATSSUMMEN ON ZMS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZSGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZSG_LETZTERUSER" "~"
if joinKuerzel = BMUZSG
searchResult := searchResult "LEFT JOIN BUERO.ZSG_ZEITSALDENGRENZWERT ON ZSG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZTUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZTU_LETZTERUSER" "~"
if joinKuerzel = BMUZTU
searchResult := searchResult "LEFT JOIN BMD.ZTU_ZEITUMSTELLUNG ON ZTU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZWPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZWP_LETZTERUSER" "~"
if joinKuerzel = BMUZWP
searchResult := searchResult "LEFT JOIN WWS.ZWP_ZUORDNUNGWVPOS ON ZWP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZXABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZXA_LETZTERUSER" "~"
if joinKuerzel = BMUZXA
searchResult := searchResult "LEFT JOIN BUERO.ZXA_ZEITAENDERUNGSANTRAG ON ZXA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PCKV2P
searchResult := searchResult "LEFT JOIN PRODUKTION.V2P_VA_PLC_PPS_CHARGE ON V2P_PCHARGENID = PCK_PCHARGENID" "~"
if joinKuerzel = V2PPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_PCHARGENID = V2P_PCHARGENID" "~"
if joinKuerzel = LAJPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_PCHARGENID = LAJ_PCHARGENID" "~"
if joinKuerzel = PLCLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_PCHARGENID = PLC_PCHARGENID" "~"
if joinKuerzel = INTRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_RECHNUNGSPOSID = INT_EXTID" "~"
if joinKuerzel = RNPINT
searchResult := searchResult "LEFT JOIN BUERO.INT_INTRASTAT ON INT_EXTID = RNP_RECHNUNGSPOSID" "~"
if joinKuerzel = ARLVEG
searchResult := searchResult "LEFT JOIN WWS.VEG_VIEW_ENHTAB_GRUNDPREISENH ON VEG_FIRMENNR = ARL_FIRMENNR AND VEG_EINHEITENTABNR = ARL_EINHEITENTABNR" "~"
if joinKuerzel = VEGARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = VEG_FIRMENNR AND ARL_EINHEITENTABNR = VEG_EINHEITENTABNR" "~"
if joinKuerzel = ARLVKA
searchResult := searchResult "LEFT JOIN KORE.VKA_VIEW_KOSTAMM_ABT ON VKA_FIRMENNR = ARL_FIRMENNR AND VKA_KOSTENSTAMMNR = ARL_ABT_KOSTENSTAMMNR" "~"
if joinKuerzel = VKAARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = VKA_FIRMENNR AND ARL_ABT_KOSTENSTAMMNR = VKA_KOSTENSTAMMNR" "~"
if joinKuerzel = WAGVKA
searchResult := searchResult "LEFT JOIN KORE.VKA_VIEW_KOSTAMM_ABT ON VKA_FIRMENNR = WAG_FIRMENNR AND VKA_KOSTENSTAMMNR = WAG_ABT_KOSTENSTAMMNR" "~"
if joinKuerzel = VKAWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = VKA_FIRMENNR AND WAG_ABT_KOSTENSTAMMNR = VKA_KOSTENSTAMMNR" "~"
if joinKuerzel = TBZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TBZ_LETZTERUSER" "~"
if joinKuerzel = BMUTBZ
searchResult := searchResult "LEFT JOIN BUERO.TBZ_TERMINALBUCHARTZUORD ON TBZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TCDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TCD_LETZTERUSER" "~"
if joinKuerzel = BMUTCD
searchResult := searchResult "LEFT JOIN BMD.TCD_TCKONFDATEN ON TCD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TFTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TFT_LETZTERUSER" "~"
if joinKuerzel = BMUTFT
searchResult := searchResult "LEFT JOIN WWS.TFT_TOURFEIERTAG ON TFT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TITBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TIT_LETZTERUSER" "~"
if joinKuerzel = BMUTIT
searchResult := searchResult "LEFT JOIN BUERO.TIT_TITEL ON TIT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TIZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TIZ_LETZTERUSER" "~"
if joinKuerzel = BMUTIZ
searchResult := searchResult "LEFT JOIN BMDENTW.TIZ_TICKETPEPAUFGABEZU ON TIZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TLNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TLN_LETZTERUSER" "~"
if joinKuerzel = BMUTLN
searchResult := searchResult "LEFT JOIN BUERO.TLN_TEILNEHMER ON TLN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TLZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TLZ_LETZTERUSER" "~"
if joinKuerzel = BMUTLZ
searchResult := searchResult "LEFT JOIN HRS.TLZ_ELDADETAIL_ELZ_TMP ON TLZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TOOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TOO_LETZTERUSER" "~"
if joinKuerzel = BMUTOO
searchResult := searchResult "LEFT JOIN WWS.TOO_TOUROBJEKT ON TOO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TRSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TRS_LETZTERUSER" "~"
if joinKuerzel = BMUTRS
searchResult := searchResult "LEFT JOIN BUERO.TRS_TEILSCHLUSSRECHSCHEMA ON TRS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TSDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TSD_LETZTERUSER" "~"
if joinKuerzel = BMUTSD
searchResult := searchResult "LEFT JOIN WWS.TSD_TOUCHSCREENDETAIL ON TSD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UDPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UDP_LETZTERUSER" "~"
if joinKuerzel = BMUUDP
searchResult := searchResult "LEFT JOIN KORE.UDP_UMLAGEPLANWERTE ON UDP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ULKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ULK_LETZTERUSER" "~"
if joinKuerzel = BMUULK
searchResult := searchResult "LEFT JOIN BMD.ULK_USERLINK ON ULK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UMFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UMF_LETZTERUSER" "~"
if joinKuerzel = BMUUMF
searchResult := searchResult "LEFT JOIN BUERO.UMF_UMFRAGE ON UMF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UMPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UMP_LETZTERUSER" "~"
if joinKuerzel = BMUUMP
searchResult := searchResult "LEFT JOIN KORE.UMP_UMLAGEPLANMENGEN ON UMP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VAS_LETZTERUSER" "~"
if joinKuerzel = BMUVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VCPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VCP_LETZTERUSER" "~"
if joinKuerzel = BMUVCP
searchResult := searchResult "LEFT JOIN PRODUKTION.VCP_VA_CHG_PLC_CHARGEN ON VCP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VDZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VDZ_LETZTERUSER" "~"
if joinKuerzel = BMUVDZ
searchResult := searchResult "LEFT JOIN BUERO.VDZ_VSTAMMDOKZU ON VDZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VEVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VEV_LETZTERUSER" "~"
if joinKuerzel = BMUVEV
searchResult := searchResult "LEFT JOIN BMDENTW.VEV_NTCSVERSION ON VEV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VFABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VFA_LETZTERUSER" "~"
if joinKuerzel = BMUVFA
searchResult := searchResult "LEFT JOIN PRODUKTION.VFA_VERFAHRENSANWEISUNG_KOPF ON VFA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VKPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VKP_LETZTERUSER" "~"
if joinKuerzel = BMUVKP
searchResult := searchResult "LEFT JOIN FIBU.VKP_VSTKPROJEKT ON VKP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VPDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VPD_LETZTERUSER" "~"
if joinKuerzel = BMUVPD
searchResult := searchResult "LEFT JOIN WWS.VPD_VPI_ERHOEHUNGDETAIL ON VPD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VPNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VPN_LETZTERUSER" "~"
if joinKuerzel = BMUVPN
searchResult := searchResult "LEFT JOIN WWS.VPN_VERSMGR_POS ON VPN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VPZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VPZ_LETZTERUSER" "~"
if joinKuerzel = BMUVPZ
searchResult := searchResult "LEFT JOIN BUERO.VPZ_VORGANGPHASEZU ON VPZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VZWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VZW_LETZTERUSER" "~"
if joinKuerzel = BMUVZW
searchResult := searchResult "LEFT JOIN BUERO.VZW_VERKEHRSZWEIG ON VZW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WAMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAM_LETZTERUSER" "~"
if joinKuerzel = BMUWAM
searchResult := searchResult "LEFT JOIN BUERO.WAM_WEBANMELDUNG ON WAM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WAPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAP_LETZTERUSER" "~"
if joinKuerzel = BMUWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VVVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VVV_LETZTERUSER" "~"
if joinKuerzel = BMUVVV
searchResult := searchResult "LEFT JOIN BIL.VVV_STEUERVERLUSTVV ON VVV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VWVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VWV_LETZTERUSER" "~"
if joinKuerzel = BMUVWV
searchResult := searchResult "LEFT JOIN FIBU.VWV_VSTERST_WERTEVORRAT ON VWV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WBGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WBG_LETZTERUSER" "~"
if joinKuerzel = BMUWBG
searchResult := searchResult "LEFT JOIN WWS.WBG_WARENBESCHKOSTENGRP ON WBG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WCABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WCA_LETZTERUSER" "~"
if joinKuerzel = BMUWCA
searchResult := searchResult "LEFT JOIN BMD.WCA_WAAUSDRUCKE ON WCA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WCZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WCZ_LETZTERUSER" "~"
if joinKuerzel = BMUWCZ
searchResult := searchResult "LEFT JOIN BMD.WCZ_WAAUSDRUCK_PARAM_ZU ON WCZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEE_LETZTERUSER" "~"
if joinKuerzel = BMUWEE
searchResult := searchResult "LEFT JOIN WWS.WEE_ERKONT_BESTELL_ZUORD ON WEE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEJ_LETZTERUSER" "~"
if joinKuerzel = BMUWEJ
searchResult := searchResult "LEFT JOIN WWS.WEJ_WARENEINSATZLAJ ON WEJ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEK_LETZTERUSER" "~"
if joinKuerzel = BMUWEK
searchResult := searchResult "LEFT JOIN PRODUKTION.WEK_WERKZEUG ON WEK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WFKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WFK_LETZTERUSER" "~"
if joinKuerzel = BMUWFK
searchResult := searchResult "LEFT JOIN WWS.WFK_FOLGEARTIKELKAT ON WFK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WFTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WFT_LETZTERUSER" "~"
if joinKuerzel = BMUWFT
searchResult := searchResult "LEFT JOIN WEB.WFT_WEBFARBENTAB ON WFT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WGIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WGI_LETZTERUSER" "~"
if joinKuerzel = BMUWGI
searchResult := searchResult "LEFT JOIN WWS.WGI_WWSBERICHTGRUPPINFO ON WGI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WGZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WGZ_LETZTERUSER" "~"
if joinKuerzel = BMUWGZ
searchResult := searchResult "LEFT JOIN WWS.WGZ_WFK_WKG_ZU ON WGZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WIFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WIF_LETZTERUSER" "~"
if joinKuerzel = BMUWIF
searchResult := searchResult "LEFT JOIN BMDENTW.WIF_WII_DFU_ZU ON WIF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WIVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WIV_LETZTERUSER" "~"
if joinKuerzel = BMUWIV
searchResult := searchResult "LEFT JOIN BIL.WIV_WIVARIABLEN ON WIV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WKABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WKA_LETZTERUSER" "~"
if joinKuerzel = BMUWKA
searchResult := searchResult "LEFT JOIN WWS.WKA_WARENAUSGKOSTENARTEN ON WKA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WPTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WPT_LETZTERUSER" "~"
if joinKuerzel = BMUWPT
searchResult := searchResult "LEFT JOIN WWS.WPT_WWSPERSON_TMP ON WPT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = XSPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = XSP_LETZTERUSER" "~"
if joinKuerzel = BMUXSP
searchResult := searchResult "LEFT JOIN BUERO.XSP_XML_STEUERERKL_PAKET ON XSP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZABBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZAB_LETZTERUSER" "~"
if joinKuerzel = BMUZAB
searchResult := searchResult "LEFT JOIN BUERO.ZAB_AUSWEISBUCHARTZU ON ZAB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZDEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZDE_LETZTERUSER" "~"
if joinKuerzel = BMUZDE
searchResult := searchResult "LEFT JOIN FIBU.ZDE_ZIB_DETAIL ON ZDE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZITBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZIT_LETZTERUSER" "~"
if joinKuerzel = BMUZIT
searchResult := searchResult "LEFT JOIN BUERO.ZIT_ZUSATZINFOTYP ON ZIT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZTVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZTV_LETZTERUSER" "~"
if joinKuerzel = BMUZTV
searchResult := searchResult "LEFT JOIN BUERO.ZTV_ZEITPLANTEXTVERSDAT ON ZTV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZUBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZUB_LETZTERUSER" "~"
if joinKuerzel = BMUZUB
searchResult := searchResult "LEFT JOIN BUERO.ZUB_ZEITUMBUCHBEWERTMDZU ON ZUB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZUSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZUS_LETZTERUSER" "~"
if joinKuerzel = BMUZUS
searchResult := searchResult "LEFT JOIN BUERO.ZUS_ZUSTAENDIGKEIT ON ZUS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZVBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZVB_LETZTERUSER" "~"
if joinKuerzel = BMUZVB
searchResult := searchResult "LEFT JOIN BUERO.ZVB_TBBEZEICHNUNGSZWECK ON ZVB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZVMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZVM_LETZTERUSER" "~"
if joinKuerzel = BMUZVM
searchResult := searchResult "LEFT JOIN BUERO.ZVM_ZEITPLANUNGVERSION ON ZVM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZVTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZVT_LETZTERUSER" "~"
if joinKuerzel = BMUZVT
searchResult := searchResult "LEFT JOIN BUERO.ZVT_TBDATEIEN ON ZVT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ARLVLD
searchResult := searchResult "LEFT JOIN WWS.VLD_VIEW_ARTIKELLAGERDISPO ON VLD_FIRMENNR = ARL_FIRMENNR AND VLD_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = VLDARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = VLD_FIRMENNR AND ARL_ARTIKELNR = VLD_ARTIKELNR" "~"
if joinKuerzel = PLCV3A
searchResult := searchResult "LEFT JOIN PRODUKTION.V3A_VA_V2P_ARL ON V3A_PCHARGENID = PLC_PCHARGENID" "~"
if joinKuerzel = V3APLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_PCHARGENID = V3A_PCHARGENID" "~"
if joinKuerzel = SCMWII
searchResult := searchResult "LEFT JOIN BMDENTW.WII_WEITERE_INFO_ITEM ON WII_WIIID = SCM_WIIID" "~"
if joinKuerzel = WIISCM
searchResult := searchResult "LEFT JOIN BMDENTW.SCM_SCRMAIN ON SCM_WIIID = WII_WIIID" "~"
if joinKuerzel = RNABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RNA_LETZTERUSER" "~"
if joinKuerzel = BMURNA
searchResult := searchResult "LEFT JOIN WWS.RNA_RECHNUNGANZAHLUNG ON RNA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RPKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RPK_LETZTERUSER" "~"
if joinKuerzel = BMURPK
searchResult := searchResult "LEFT JOIN WWS.RPK_RECHNUNGSPOS_KUM_TMP ON RPK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RPMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RPM_LETZTERUSER" "~"
if joinKuerzel = BMURPM
searchResult := searchResult "LEFT JOIN PRODUKTION.RPM_RETRO_PRODUKT_MATERIAL ON RPM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RPZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RPZ_LETZTERUSER" "~"
if joinKuerzel = BMURPZ
searchResult := searchResult "LEFT JOIN ZLE.RPZ_RECHPOSZU ON RPZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RTIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RTI_LETZTERUSER" "~"
if joinKuerzel = BMURTI
searchResult := searchResult "LEFT JOIN BMD.RTI_RTFINDEX ON RTI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RUPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RUP_LETZTERUSER" "~"
if joinKuerzel = BMURUP
searchResult := searchResult "LEFT JOIN WWS.RUP_RUNDUNGPOSITION ON RUP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RZWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RZW_LETZTERUSER" "~"
if joinKuerzel = BMURZW
searchResult := searchResult "LEFT JOIN HRS.RZW_REISEZWECK ON RZW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SAGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SAG_LETZTERUSER" "~"
if joinKuerzel = BMUSAG
searchResult := searchResult "LEFT JOIN WWS.SAG_STATISTIKARTIKELGRP ON SAG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SAPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SAP_LETZTERUSER" "~"
if joinKuerzel = BMUSAP
searchResult := searchResult "LEFT JOIN BUERO.SAP_SAMMELPROJEKT ON SAP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SBABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SBA_LETZTERUSER" "~"
if joinKuerzel = BMUSBA
searchResult := searchResult "LEFT JOIN HRS.SBA_SCHNITTBASIS ON SBA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCJ_LETZTERUSER" "~"
if joinKuerzel = BMUSCJ
searchResult := searchResult "LEFT JOIN BMDENTW.SCJ_SCRPROJZU ON SCJ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SDTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SDT_LETZTERUSER" "~"
if joinKuerzel = BMUSDT
searchResult := searchResult "LEFT JOIN BMDENTW.SDT_SCRDOCTEXTZU ON SDT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SKLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SKL_LETZTERUSER" "~"
if joinKuerzel = BMUSKL
searchResult := searchResult "LEFT JOIN WWS.SKL_STUECKELUNGSLISTE ON SKL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SLPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SLP_LETZTERUSER" "~"
if joinKuerzel = BMUSLP
searchResult := searchResult "LEFT JOIN WWS.SLP_STUECKLISTENPOSITION ON SLP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SMZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SMZ_LETZTERUSER" "~"
if joinKuerzel = BMUSMZ
searchResult := searchResult "LEFT JOIN ZLE.SMZ_SMARTINTERFACEZU ON SMZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SN1BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SN1_LETZTERUSER" "~"
if joinKuerzel = BMUSN1
searchResult := searchResult "LEFT JOIN WWS.SN1_VA_SNR_SERIENNUMMERN ON SN1_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SNVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SNV_LETZTERUSER" "~"
if joinKuerzel = BMUSNV
searchResult := searchResult "LEFT JOIN HRS.SNV_SCHNITTVORSCHLAG ON SNV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPC_LETZTERUSER" "~"
if joinKuerzel = BMUSPC
searchResult := searchResult "LEFT JOIN BMD.SPC_SPELLCHECKERDICT ON SPC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SRMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SRM_LETZTERUSER" "~"
if joinKuerzel = BMUSRM
searchResult := searchResult "LEFT JOIN WWS.SRM_SAMMELRECHNUNGSMODELL ON SRM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SRPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SRP_LETZTERUSER" "~"
if joinKuerzel = BMUSRP
searchResult := searchResult "LEFT JOIN WWS.SRP_STEUERTABPOS ON SRP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STG_LETZTERUSER" "~"
if joinKuerzel = BMUSTG
searchResult := searchResult "LEFT JOIN FIBU.STG_STEUERGRUPPE ON STG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STL_LETZTERUSER" "~"
if joinKuerzel = BMUSTL
searchResult := searchResult "LEFT JOIN BUERO.STL_STELLE ON STL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STZ_LETZTERUSER" "~"
if joinKuerzel = BMUSTZ
searchResult := searchResult "LEFT JOIN WWS.STZ_STEUERTEXTZUORDNUNG ON STZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SYDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SYD_LETZTERUSER" "~"
if joinKuerzel = BMUSYD
searchResult := searchResult "LEFT JOIN BMD.SYD_SYSTEMDRUCKER ON SYD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SYKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SYK_LETZTERUSER" "~"
if joinKuerzel = BMUSYK
searchResult := searchResult "LEFT JOIN BMD.SYK_BMDSYSTEMKOMPONENTE ON SYK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TRJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TRJ_LETZTERUSER" "~"
if joinKuerzel = BMUTRJ
searchResult := searchResult "LEFT JOIN WWS.TRJ_TRANSPORTEINH_JOURNAL ON TRJ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WARBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAR_LETZTERUSER" "~"
if joinKuerzel = BMUWAR
searchResult := searchResult "LEFT JOIN WWS.WAR_WARENEINGANGRABATTE ON WAR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WAUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAU_LETZTERUSER" "~"
if joinKuerzel = BMUWAU
searchResult := searchResult "LEFT JOIN WWS.WAU_WWSAUFGABENAKTION ON WAU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WBABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WBA_LETZTERUSER" "~"
if joinKuerzel = BMUWBA
searchResult := searchResult "LEFT JOIN WEB.WBA_WEBARTIKEL ON WBA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WBOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WBO_LETZTERUSER" "~"
if joinKuerzel = BMUWBO
searchResult := searchResult "LEFT JOIN WEB.WBO_WEBOUTLOOKBAR ON WBO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WCUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WCU_LETZTERUSER" "~"
if joinKuerzel = BMUWCU
searchResult := searchResult "LEFT JOIN BMD.WCU_WAAUSDRUCKUSERZU ON WCU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WCVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WCV_LETZTERUSER" "~"
if joinKuerzel = BMUWCV
searchResult := searchResult "LEFT JOIN BMD.WCV_WAPARAMDEFVALUES ON WCV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WDRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WDR_LETZTERUSER" "~"
if joinKuerzel = BMUWDR
searchResult := searchResult "LEFT JOIN WWS.WDR_WWSDATENREIHENFOLGE ON WDR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEM_LETZTERUSER" "~"
if joinKuerzel = BMUWEM
searchResult := searchResult "LEFT JOIN WWS.WEM_WARENEINSATZBUCHKZZU ON WEM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WFZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WFZ_LETZTERUSER" "~"
if joinKuerzel = BMUWFZ
searchResult := searchResult "LEFT JOIN BUERO.WFZ_WORKFLOWZU ON WFZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WGRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WGR_LETZTERUSER" "~"
if joinKuerzel = BMUWGR
searchResult := searchResult "LEFT JOIN WWS.WGR_WWSBERICHTGRUPPIERUNG ON WGR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WOHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WOH_LETZTERUSER" "~"
if joinKuerzel = BMUWOH
searchResult := searchResult "LEFT JOIN BUERO.WOH_WOHNUNG ON WOH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WPRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WPR_LETZTERUSER" "~"
if joinKuerzel = BMUWPR
searchResult := searchResult "LEFT JOIN WEB.WPR_WEBPROTOKOLL ON WPR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WRPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WRP_LETZTERUSER" "~"
if joinKuerzel = BMUWRP
searchResult := searchResult "LEFT JOIN WWS.WRP_WWSANKAUFSRNGPOS ON WRP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = YASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = YAS_LETZTERUSER" "~"
if joinKuerzel = BMUYAS
searchResult := searchResult "LEFT JOIN FIBU.YAS_AUTOBUCHAUFTEILSATZ ON YAS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZBGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZBG_LETZTERUSER" "~"
if joinKuerzel = BMUZBG
searchResult := searchResult "LEFT JOIN BUERO.ZBG_ZEITBABERECHTGRUPPE ON ZBG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZBMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZBM_LETZTERUSER" "~"
if joinKuerzel = BMUZBM
searchResult := searchResult "LEFT JOIN FIBU.ZBM_ZINSBERECHNUNG_SUMME ON ZBM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZBRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZBR_LETZTERUSER" "~"
if joinKuerzel = BMUZBR
searchResult := searchResult "LEFT JOIN FIBU.ZBR_ZINSBERECHNUNG ON ZBR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZCLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZCL_LETZTERUSER" "~"
if joinKuerzel = BMUZCL
searchResult := searchResult "LEFT JOIN BUERO.ZCL_ZEITCHANGELOG ON ZCL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZKKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZKK_LETZTERUSER" "~"
if joinKuerzel = BMUZKK
searchResult := searchResult "LEFT JOIN KORE.ZKK_KSTKONTOZU ON ZKK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZLSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZLS_LETZTERUSER" "~"
if joinKuerzel = BMUZLS
searchResult := searchResult "LEFT JOIN ZLE.ZLS_ZEILAB_SICHERUNG ON ZLS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZMDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZMD_LETZTERUSER" "~"
if joinKuerzel = BMUZMD
searchResult := searchResult "LEFT JOIN BUERO.ZMD_ZEITMINDESTBESETZDET ON ZMD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZNGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZNG_LETZTERUSER" "~"
if joinKuerzel = BMUZNG
searchResult := searchResult "LEFT JOIN BUERO.ZNG_ZEITNACHRICHTGESENDET ON ZNG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZSUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZSU_LETZTERUSER" "~"
if joinKuerzel = BMUZSU
searchResult := searchResult "LEFT JOIN FIBU.ZSU_ZAHLSUMME ON ZSU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZVKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZVK_LETZTERUSER" "~"
if joinKuerzel = BMUZVK
searchResult := searchResult "LEFT JOIN FIBU.ZVK_ZAHLUNGSVORSCHLAG ON ZVK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PLCELD
searchResult := searchResult "LEFT JOIN PRODUKTION.ELD_VA_ELDATUM ON ELD_PCHARGENID = PLC_PCHARGENID" "~"
if joinKuerzel = ELDPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_PCHARGENID = ELD_PCHARGENID" "~"
if joinKuerzel = PROPKL
searchResult := searchResult "LEFT JOIN BUERO.PKL_PROJEKTPERSZU ON PKL_PRO_FIRMENNR = PRO_FIRMENNR AND PKL_PROJEKTNR = PRO_PROJEKTNR AND PKL_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = PKLPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = PKL_PRO_FIRMENNR AND PRO_PROJEKTNR = PKL_PROJEKTNR AND PRO_PROJEKTTYP = PKL_PROJEKTTYP" "~"
if joinKuerzel = SIPPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_PCHARGENID = SIP_PCHARGENID" "~"
if joinKuerzel = PLCSIP
searchResult := searchResult "LEFT JOIN WWS.SIP_VA_SNRIOB_TO_PLC_VIA_EPF ON SIP_PCHARGENID = PLC_PCHARGENID" "~"
if joinKuerzel = LAJKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_KOSTENSTAMMNR = LAJ_KOTR_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_KOTR_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = ARLVAN
searchResult := searchResult "LEFT JOIN WWS.VAN_VIEW_ARTIKEL_EP ON VAN_FIRMENNR = ARL_FIRMENNR AND VAN_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = VANARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = VAN_FIRMENNR AND ARL_ARTIKELNR = VAN_ARTIKELNR" "~"
if joinKuerzel = WAGVKS
searchResult := searchResult "LEFT JOIN KORE.VKS_VIEW_KOSTAMM_KST ON VKS_FIRMENNR = WAG_FIRMENNR AND VKS_KOSTENSTAMMNR = WAG_KOSTENST_KOSTENSTAMMNR" "~"
if joinKuerzel = VKSWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = VKS_FIRMENNR AND WAG_KOSTENST_KOSTENSTAMMNR = VKS_KOSTENSTAMMNR" "~"
if joinKuerzel = TWPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TWP_LETZTERUSER" "~"
if joinKuerzel = BMUTWP
searchResult := searchResult "LEFT JOIN WWS.TWP_WARENEINGANGPOS_TMP ON TWP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TWZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TWZ_LETZTERUSER" "~"
if joinKuerzel = BMUTWZ
searchResult := searchResult "LEFT JOIN WWS.TWZ_WEINSKOPFZU_TMP ON TWZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UANBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UAN_LETZTERUSER" "~"
if joinKuerzel = BMUUAN
searchResult := searchResult "LEFT JOIN BMDINTERN.UAN_UPDATEANFORDERUNGSTXT ON UAN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UDIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UDI_LETZTERUSER" "~"
if joinKuerzel = BMUUDI
searchResult := searchResult "LEFT JOIN KORE.UDI_UMLAGEISTWERTE ON UDI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UMIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UMI_LETZTERUSER" "~"
if joinKuerzel = BMUUMI
searchResult := searchResult "LEFT JOIN KORE.UMI_UMLAGEISTMENGE ON UMI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = USPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = USP_LETZTERUSER" "~"
if joinKuerzel = BMUUSP
searchResult := searchResult "LEFT JOIN BMDINTERN.USP_UPDATESPERRE ON USP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UTHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UTH_LETZTERUSER" "~"
if joinKuerzel = BMUUTH
searchResult := searchResult "LEFT JOIN BMD.UTH_USERTREEHEAD ON UTH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UTPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UTP_LETZTERUSER" "~"
if joinKuerzel = BMUUTP
searchResult := searchResult "LEFT JOIN BMD.UTP_USERTREEPOS ON UTP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UVABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UVA_LETZTERUSER" "~"
if joinKuerzel = BMUUVA
searchResult := searchResult "LEFT JOIN FIBU.UVA_UVAMODELL ON UVA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UVFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UVF_LETZTERUSER" "~"
if joinKuerzel = BMUUVF
searchResult := searchResult "LEFT JOIN FIBU.UVF_UVAFELDZU ON UVF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VBVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VBV_LETZTERUSER" "~"
if joinKuerzel = BMUVBV
searchResult := searchResult "LEFT JOIN FIBU.VBV_VORERFASSBUCH ON VBV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VEKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VEK_LETZTERUSER" "~"
if joinKuerzel = BMUVEK
searchResult := searchResult "LEFT JOIN WWS.VEK_VERTRETERABRECHKOPF ON VEK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VERBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VER_LETZTERUSER" "~"
if joinKuerzel = BMUVER
searchResult := searchResult "LEFT JOIN BUERO.VER_VERSANDART ON VER_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VJDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VJD_LETZTERUSER" "~"
if joinKuerzel = BMUVJD
searchResult := searchResult "LEFT JOIN WWS.VJD_VERLEIHJOURNALDETAIL ON VJD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VPIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VPI_LETZTERUSER" "~"
if joinKuerzel = BMUVPI
searchResult := searchResult "LEFT JOIN BUERO.VPI_VPINDEX ON VPI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VRGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VRG_LETZTERUSER" "~"
if joinKuerzel = BMUVRG
searchResult := searchResult "LEFT JOIN BIL.VRG_VERLUSTANRECHNUNG ON VRG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VSKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VSK_LETZTERUSER" "~"
if joinKuerzel = BMUVSK
searchResult := searchResult "LEFT JOIN FIBU.VSK_VSTPAKET ON VSK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VWZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VWZ_LETZTERUSER" "~"
if joinKuerzel = BMUVWZ
searchResult := searchResult "LEFT JOIN BUERO.VWZ_VORGANGWORKFLOWZU ON VWZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VZEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VZE_LETZTERUSER" "~"
if joinKuerzel = BMUVZE
searchResult := searchResult "LEFT JOIN BUERO.VZE_VORDIENSTZEITEN ON VZE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WABBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAB_LETZTERUSER" "~"
if joinKuerzel = BMUWAB
searchResult := searchResult "LEFT JOIN WWS.WAB_WARENEINGANGBUCH ON WAB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAK_LETZTERUSER" "~"
if joinKuerzel = BMUWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAZ_LETZTERUSER" "~"
if joinKuerzel = BMUWAZ
searchResult := searchResult "LEFT JOIN WWS.WAZ_WARENZUORDNUNG ON WAZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WBFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WBF_LETZTERUSER" "~"
if joinKuerzel = BMUWBF
searchResult := searchResult "LEFT JOIN WWS.WBF_WWSBERICHTFILTER ON WBF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WDABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WDA_LETZTERUSER" "~"
if joinKuerzel = BMUWDA
searchResult := searchResult "LEFT JOIN BUERO.WDA_WFDAGRAMMZU ON WDA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEG_LETZTERUSER" "~"
if joinKuerzel = BMUWEG
searchResult := searchResult "LEFT JOIN WWS.WEG_WWSERKONTROLLEGRP ON WEG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WESBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WES_LETZTERUSER" "~"
if joinKuerzel = BMUWES
searchResult := searchResult "LEFT JOIN WWS.WES_WWSERKONTROLLEWBK ON WES_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = INTRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_RECHNUNGSPOSID = INT_EXTID" "~"
if joinKuerzel = RNPINT
searchResult := searchResult "LEFT JOIN BUERO.INT_INTRASTAT ON INT_EXTID = RNP_RECHNUNGSPOSID" "~"
if joinKuerzel = LAJKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_KOSTENSTAMMNR = LAJ_DIM_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_DIM_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = LFSVKM
searchResult := searchResult "LEFT JOIN WWS.VKM_VA_AUSGKOSTLFSSUM ON VKM_FIRMENNR = LFS_FIRMENNR AND VKM_LIEFERSCHEINLFDNR = LFS_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = VKMLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_FIRMENNR = VKM_FIRMENNR AND LFS_LIEFERSCHEINLFDNR = VKM_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = IOBSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_IOBID = IOB_IOBID" "~"
if joinKuerzel = SNRIOB
searchResult := searchResult "LEFT JOIN BUERO.IOB_INSTANDHOBJEKT ON IOB_IOBID = SNR_IOBID" "~"
if joinKuerzel = V2PEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = V2P_WWSERF_FIRMENNR AND EFK_ERFASSUNGSKZ = V2P_WWSERF_KZ AND EFK_ERFASSUNGSNR = V2P_WWSERF_NR" "~"
if joinKuerzel = EFKV2P
searchResult := searchResult "LEFT JOIN PRODUKTION.V2P_VA_PLC_PPS_CHARGE ON V2P_WWSERF_FIRMENNR = EFK_FIRMENNR AND V2P_WWSERF_KZ = EFK_ERFASSUNGSKZ AND V2P_WWSERF_NR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = WFPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WFP_LETZTERUSER" "~"
if joinKuerzel = BMUWFP
searchResult := searchResult "LEFT JOIN WEB.WFP_WEBFARBENPOS ON WFP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WGDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WGD_LETZTERUSER" "~"
if joinKuerzel = BMUWGD
searchResult := searchResult "LEFT JOIN FIBU.WGD_WGBDETAIL ON WGD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WICBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WIC_LETZTERUSER" "~"
if joinKuerzel = BMUWIC
searchResult := searchResult "LEFT JOIN BIL.WIC_WIPCHECKLIST ON WIC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WIPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WIP_LETZTERUSER" "~"
if joinKuerzel = BMUWIP
searchResult := searchResult "LEFT JOIN BIL.WIP_WIPRUEFUNG ON WIP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WKLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WKL_LETZTERUSER" "~"
if joinKuerzel = BMUWKL
searchResult := searchResult "LEFT JOIN WWS.WKL_WARENAUSGKOSTENLIEF ON WKL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WKTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WKT_LETZTERUSER" "~"
if joinKuerzel = BMUWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WQCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WQC_LETZTERUSER" "~"
if joinKuerzel = BMUWQC
searchResult := searchResult "LEFT JOIN BMD.WQC_WQI_CUSTOMER ON WQC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WQLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WQL_LETZTERUSER" "~"
if joinKuerzel = BMUWQL
searchResult := searchResult "LEFT JOIN BMDDEFAULT.WQL_WEITERE_INFO_LANG ON WQL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WRKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WRK_LETZTERUSER" "~"
if joinKuerzel = BMUWRK
searchResult := searchResult "LEFT JOIN WWS.WRK_WWSANKAUFSRNGKOPF ON WRK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WTEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WTE_LETZTERUSER" "~"
if joinKuerzel = BMUWTE
searchResult := searchResult "LEFT JOIN BUERO.WTE_WEBTEILNEHMER ON WTE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZAK_LETZTERUSER" "~"
if joinKuerzel = BMUZAK
searchResult := searchResult "LEFT JOIN BUERO.ZAK_ZEITAUSWERTMAKRO ON ZAK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZBOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZBO_LETZTERUSER" "~"
if joinKuerzel = BMUZBO
searchResult := searchResult "LEFT JOIN BUERO.ZBO_ZEITBABERECHTZUORD ON ZBO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZBPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZBP_LETZTERUSER" "~"
if joinKuerzel = BMUZBP
searchResult := searchResult "LEFT JOIN BUERO.ZBP_ZEITBUCHPROTOKOLL ON ZBP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZFIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZFI_LETZTERUSER" "~"
if joinKuerzel = BMUZFI
searchResult := searchResult "LEFT JOIN BUERO.ZFI_ZEITFEHLERIGNORE ON ZFI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZINBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZIN_LETZTERUSER" "~"
if joinKuerzel = BMUZIN
searchResult := searchResult "LEFT JOIN FIBU.ZIN_ZAHLUNGSINFORMATION ON ZIN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZNSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZNS_LETZTERUSER" "~"
if joinKuerzel = BMUZNS
searchResult := searchResult "LEFT JOIN BUERO.ZNS_ZEITNACHRICHTSYSTEM ON ZNS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZSABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZSA_LETZTERUSER" "~"
if joinKuerzel = BMUZSA
searchResult := searchResult "LEFT JOIN BUERO.ZSA_ZEITSALDEN ON ZSA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZSDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZSD_LETZTERUSER" "~"
if joinKuerzel = BMUZSD
searchResult := searchResult "LEFT JOIN ZLE.ZSD_ZEITSCHEIBEDETAIL ON ZSD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZTGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZTG_LETZTERUSER" "~"
if joinKuerzel = BMUZTG
searchResult := searchResult "LEFT JOIN HRS.ZTG_ZAHLTAG ON ZTG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZUHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZUH_LETZTERUSER" "~"
if joinKuerzel = BMUZUH
searchResult := searchResult "LEFT JOIN BUERO.ZUH_ZEITUMBUCHHIST ON ZUH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZUUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZUU_LETZTERUSER" "~"
if joinKuerzel = BMUZUU
searchResult := searchResult "LEFT JOIN BUERO.ZUU_ZEITBEZARTUMBUCHUSER ON ZUU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZWRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZWR_LETZTERUSER" "~"
if joinKuerzel = BMUZWR
searchResult := searchResult "LEFT JOIN BUERO.ZWR_ZAHLART_WAEHRUNGRUND ON ZWR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZXDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZXD_LETZTERUSER" "~"
if joinKuerzel = BMUZXD
searchResult := searchResult "LEFT JOIN BUERO.ZXD_ZEITAENDERUNG ON ZXD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SNRSIP
searchResult := searchResult "LEFT JOIN WWS.SIP_VA_SNRIOB_TO_PLC_VIA_EPF ON SIP_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = SIPSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_SERNRID = SIP_SERNRID" "~"
if joinKuerzel = LAJKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_KOSTENSTAMMNR = LAJ_KOST_KOSTENSTAMMNR" "~"
if joinKuerzel = KSTLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_KOST_KOSTENSTAMMNR = KST_KOSTENSTAMMNR" "~"
if joinKuerzel = ARLVKG
searchResult := searchResult "LEFT JOIN KORE.VKG_VIEW_KOSTAMM_GBR ON VKG_FIRMENNR = ARL_FIRMENNR AND VKG_KOSTENSTAMMNR = ARL_GBR_KOSTENSTAMMNR" "~"
if joinKuerzel = VKGARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = VKG_FIRMENNR AND ARL_GBR_KOSTENSTAMMNR = VKG_KOSTENSTAMMNR" "~"
if joinKuerzel = ARLVAL
searchResult := searchResult "LEFT JOIN WWS.VAL_VIEW_ARTIKELLAGERWERTE ON VAL_ARL_FIRMENNR = ARL_FIRMENNR AND VAL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = VALARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = VAL_ARL_FIRMENNR AND ARL_ARTIKELNR = VAL_ARTIKELNR" "~"
if joinKuerzel = LATVLD
searchResult := searchResult "LEFT JOIN WWS.VLD_VIEW_ARTIKELLAGERDISPO ON VLD_FIRMENNR = LAT_ARL_FIRMENNR AND VLD_ARTIKELNR = LAT_ARL_ARTIKELNR" "~"
if joinKuerzel = VLDLAT
searchResult := searchResult "LEFT JOIN WWS.LAT_LIEFERANTENARTIKEL ON LAT_ARL_FIRMENNR = VLD_FIRMENNR AND LAT_ARL_ARTIKELNR = VLD_ARTIKELNR" "~"
if joinKuerzel = VAAARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = VAA_FIRMENNR AND ARL_ARTIKELNR = VAA_ARTIKELNR" "~"
if joinKuerzel = ARLVAA
searchResult := searchResult "LEFT JOIN WWS.VAA_VIEW_LAGSUM_ARLLAG ON VAA_FIRMENNR = ARL_FIRMENNR AND VAA_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = DIGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DIG_ERSTELLUSER" "~"
if joinKuerzel = BMUDIG
searchResult := searchResult "LEFT JOIN BMD.DIG_DIAGRAMMGRUPPE ON DIG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DIXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DIX_ERSTELLUSER" "~"
if joinKuerzel = BMUDIX
searchResult := searchResult "LEFT JOIN BMD.DIX_DIAGRAMMAKTION ON DIX_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DKABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DKA_ERSTELLUSER" "~"
if joinKuerzel = BMUDKA
searchResult := searchResult "LEFT JOIN BUERO.DKA_DOCSKATEGORIEZU ON DKA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DLKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DLK_ERSTELLUSER" "~"
if joinKuerzel = BMUDLK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DLK_LINK ON DLK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DLNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DLN_ERSTELLUSER" "~"
if joinKuerzel = BMUDLN
searchResult := searchResult "LEFT JOIN BUERO.DLN_DIENSTLEISTUNGSNUMMER ON DLN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DOVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DOV_ERSTELLUSER" "~"
if joinKuerzel = BMUDOV
searchResult := searchResult "LEFT JOIN BMD.DOV_DOGVORSCHLAG ON DOV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DPZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DPZ_ERSTELLUSER" "~"
if joinKuerzel = BMUDPZ
searchResult := searchResult "LEFT JOIN BUERO.DPZ_DOCSPERSZU ON DPZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DRSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DRS_ERSTELLUSER" "~"
if joinKuerzel = BMUDRS
searchResult := searchResult "LEFT JOIN BMD.DRS_DRUCKEREINSTELLUNG ON DRS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DRVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DRV_ERSTELLUSER" "~"
if joinKuerzel = BMUDRV
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DRV_DREPORTVORSCHLAG ON DRV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DSKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DSK_ERSTELLUSER" "~"
if joinKuerzel = BMUDSK
searchResult := searchResult "LEFT JOIN BUERO.DSK_DSGVO_DATENKATEGORIE ON DSK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DSMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DSM_ERSTELLUSER" "~"
if joinKuerzel = BMUDSM
searchResult := searchResult "LEFT JOIN DATANORM.DSM_SET ON DSM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DSTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DST_ERSTELLUSER" "~"
if joinKuerzel = BMUDST
searchResult := searchResult "LEFT JOIN HRS.DST_DIVSTAMM ON DST_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DSVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DSV_ERSTELLUSER" "~"
if joinKuerzel = BMUDSV
searchResult := searchResult "LEFT JOIN BUERO.DSV_DSGVO_VERZEICHNIS ON DSV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DVEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DVE_ERSTELLUSER" "~"
if joinKuerzel = BMUDVE
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DVE_VERSION ON DVE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DVHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DVH_ERSTELLUSER" "~"
if joinKuerzel = BMUDVH
searchResult := searchResult "LEFT JOIN BUERO.DVH_DIENSTVERHAELTNIS ON DVH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DVPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DVP_ERSTELLUSER" "~"
if joinKuerzel = BMUDVP
searchResult := searchResult "LEFT JOIN BMD.DVP_DEVICEPROTOCOL ON DVP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DWABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DWA_ERSTELLUSER" "~"
if joinKuerzel = BMUDWA
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DWA_WEBBROWSERADDON ON DWA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EB2BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EB2_ERSTELLUSER" "~"
if joinKuerzel = BMUEB2
searchResult := searchResult "LEFT JOIN HRS.EB2_ELDADETAIL_BN_BEGR ON EB2_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EB3BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EB3_ERSTELLUSER" "~"
if joinKuerzel = BMUEB3
searchResult := searchResult "LEFT JOIN HRS.EB3_ELDADETAIL_BN_NEBEN ON EB3_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EBKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EBK_ERSTELLUSER" "~"
if joinKuerzel = BMUEBK
searchResult := searchResult "LEFT JOIN FIBU.EBK_EBKORREKTURKONTEN ON EBK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EBSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EBS_ERSTELLUSER" "~"
if joinKuerzel = BMUEBS
searchResult := searchResult "LEFT JOIN BIL.EBS_EBILANZSTRUKTUR ON EBS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EDVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EDV_ERSTELLUSER" "~"
if joinKuerzel = BMUEDV
searchResult := searchResult "LEFT JOIN WWS.EDV_ERFASSRABATTDETAILVOR ON EDV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EDZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EDZ_ERSTELLUSER" "~"
if joinKuerzel = BMUEDZ
searchResult := searchResult "LEFT JOIN WWS.EDZ_ERFASSDOKUMENTZU ON EDZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EFWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EFW_ERSTELLUSER" "~"
if joinKuerzel = BMUEFW
searchResult := searchResult "LEFT JOIN HRS.EFW_ENTGELTFORTZ_WERTE ON EFW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EINBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EIN_ERSTELLUSER" "~"
if joinKuerzel = BMUEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EM3BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EM3_ERSTELLUSER" "~"
if joinKuerzel = BMUEM3
searchResult := searchResult "LEFT JOIN HRS.EM3_ELDADETAIL_TG ON EM3_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ENTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ENT_ERSTELLUSER" "~"
if joinKuerzel = BMUENT
searchResult := searchResult "LEFT JOIN BMD.ENT_EDINACHRICHTTYP ON ENT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EPRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EPR_ERSTELLUSER" "~"
if joinKuerzel = BMUEPR
searchResult := searchResult "LEFT JOIN WWS.EPR_ERFASSPROVISION ON EPR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EPVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EPV_ERSTELLUSER" "~"
if joinKuerzel = BMUEPV
searchResult := searchResult "LEFT JOIN WWS.EPV_ERFASSPOSRECHVOR ON EPV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ERSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ERS_ERSTELLUSER" "~"
if joinKuerzel = BMUERS
searchResult := searchResult "LEFT JOIN FIBU.ERS_KOST ON ERS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ESZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ESZ_ERSTELLUSER" "~"
if joinKuerzel = BMUESZ
searchResult := searchResult "LEFT JOIN WWS.ESZ_ERZ_SRA_ZU ON ESZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FAGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FAG_ERSTELLUSER" "~"
if joinKuerzel = BMUFAG
searchResult := searchResult "LEFT JOIN HRS.FAG_FINANZAMTGHOST ON FAG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FAWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FAW_ERSTELLUSER" "~"
if joinKuerzel = BMUFAW
searchResult := searchResult "LEFT JOIN HRS.FAW_FAKTORWERTE ON FAW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FBFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FBF_ERSTELLUSER" "~"
if joinKuerzel = BMUFBF
searchResult := searchResult "LEFT JOIN BMD.FBF_FELDBESCHFORMAT ON FBF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FDBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FDB_ERSTELLUSER" "~"
if joinKuerzel = BMUFDB
searchResult := searchResult "LEFT JOIN FIBU.FDB_FIBUDASHBOARD ON FDB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FEABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FEA_ERSTELLUSER" "~"
if joinKuerzel = BMUFEA
searchResult := searchResult "LEFT JOIN WWS.FEA_FERTEP_AUFTEILUNG ON FEA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FFABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FFA_ERSTELLUSER" "~"
if joinKuerzel = BMUFFA
searchResult := searchResult "LEFT JOIN BUERO.FFA_FIRMENFINANZAMTZU ON FFA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FKEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FKE_ERSTELLUSER" "~"
if joinKuerzel = BMUFKE
searchResult := searchResult "LEFT JOIN FIBU.FKE_FIBUKOREERFASSUNG ON FKE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FKVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FKV_ERSTELLUSER" "~"
if joinKuerzel = BMUFKV
searchResult := searchResult "LEFT JOIN FIBU.FKV_KONTENVERTEILUNG ON FKV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FRZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FRZ_ERSTELLUSER" "~"
if joinKuerzel = BMUFRZ
searchResult := searchResult "LEFT JOIN BUERO.FRZ_FRAGEBOGENREGELZU ON FRZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FSTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FST_ERSTELLUSER" "~"
if joinKuerzel = BMUFST
searchResult := searchResult "LEFT JOIN BUERO.FST_FRIST ON FST_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FSUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FSU_ERSTELLUSER" "~"
if joinKuerzel = BMUFSU
searchResult := searchResult "LEFT JOIN FIBU.FSU_SORTSUMMENPOS ON FSU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FTBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FTB_ERSTELLUSER" "~"
if joinKuerzel = BMUFTB
searchResult := searchResult "LEFT JOIN BUERO.FTB_FAHRTENBUCH ON FTB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FURBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FUR_ERSTELLUSER" "~"
if joinKuerzel = BMUFUR
searchResult := searchResult "LEFT JOIN BMD.FUR_FUNKRECHTE ON FUR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FWKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FWK_ERSTELLUSER" "~"
if joinKuerzel = BMUFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GE2BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GE2_ERSTELLUSER" "~"
if joinKuerzel = BMUGE2
searchResult := searchResult "LEFT JOIN BIL.GE2_GEB2RECHTSGESCHAEFT ON GE2_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GEMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GEM_ERSTELLUSER" "~"
if joinKuerzel = BMUGEM
searchResult := searchResult "LEFT JOIN BUERO.GEM_GEMEINDE ON GEM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GEZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GEZ_ERSTELLUSER" "~"
if joinKuerzel = BMUGEZ
searchResult := searchResult "LEFT JOIN ZLE.GEZ_GEBUEHRENZUSATZ ON GEZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GOLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GOL_ERSTELLUSER" "~"
if joinKuerzel = BMUGOL
searchResult := searchResult "LEFT JOIN ZLE.GOL_GRUPPENORDNER_LEISTUNG ON GOL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GPHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GPH_ERSTELLUSER" "~"
if joinKuerzel = BMUGPH
searchResult := searchResult "LEFT JOIN BUERO.GPH_GOPHILFSTABELLE ON GPH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GRDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GRD_ERSTELLUSER" "~"
if joinKuerzel = BMUGRD
searchResult := searchResult "LEFT JOIN DATANORM.GRD_GRAFIK_DETAIL ON GRD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GSMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GSM_ERSTELLUSER" "~"
if joinKuerzel = BMUGSM
searchResult := searchResult "LEFT JOIN PRODUKTION.GSM_GUTSCHLECHTMENGE ON GSM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GSVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GSV_ERSTELLUSER" "~"
if joinKuerzel = BMUGSV
searchResult := searchResult "LEFT JOIN BIL.GSV_GSVGBERECH ON GSV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GSWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GSW_ERSTELLUSER" "~"
if joinKuerzel = BMUGSW
searchResult := searchResult "LEFT JOIN BMDDEFAULT.GSW_GESETZWERT ON GSW_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GTZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GTZ_ERSTELLUSER" "~"
if joinKuerzel = BMUGTZ
searchResult := searchResult "LEFT JOIN BIL.GTZ_GLIEDERUNGSTEXTZEILE ON GTZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GUDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GUD_ERSTELLUSER" "~"
if joinKuerzel = BMUGUD
searchResult := searchResult "LEFT JOIN WWSINDIV.GUD_GUTHABEN_IFA_DETAIL ON GUD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GUTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GUT_ERSTELLUSER" "~"
if joinKuerzel = BMUGUT
searchResult := searchResult "LEFT JOIN WWSINDIV.GUT_GUTHABEN_IFA ON GUT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GWKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GWK_ERSTELLUSER" "~"
if joinKuerzel = BMUGWK
searchResult := searchResult "LEFT JOIN BUERO.GWK_GEWERKSCHAFT ON GWK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HBTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HBT_ERSTELLUSER" "~"
if joinKuerzel = BMUHBT
searchResult := searchResult "LEFT JOIN WWS.HBT_HAUPTBESTANDTEIL ON HBT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HEBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HEB_ERSTELLUSER" "~"
if joinKuerzel = BMUHEB
searchResult := searchResult "LEFT JOIN BIL.HEB_HEBESATZ ON HEB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HNPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HNP_ERSTELLUSER" "~"
if joinKuerzel = BMUHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HVGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HVG_ERSTELLUSER" "~"
if joinKuerzel = BMUHVG
searchResult := searchResult "LEFT JOIN ZLE.HVG_HONVORGRUPPE ON HVG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IABBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IAB_ERSTELLUSER" "~"
if joinKuerzel = BMUIAB
searchResult := searchResult "LEFT JOIN BIL.IAB_INVESTITIONSABZUG ON IAB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IFPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IFP_ERSTELLUSER" "~"
if joinKuerzel = BMUIFP
searchResult := searchResult "LEFT JOIN BUERO.IFP_INFOTABPOS ON IFP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IGZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IGZ_ERSTELLUSER" "~"
if joinKuerzel = BMUIGZ
searchResult := searchResult "LEFT JOIN BMD.IGZ_INTERVALLGRUPPEZU ON IGZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = INSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = INS_ERSTELLUSER" "~"
if joinKuerzel = BMUINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IOBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IOB_ERSTELLUSER" "~"
if joinKuerzel = BMUIOB
searchResult := searchResult "LEFT JOIN BUERO.IOB_INSTANDHOBJEKT ON IOB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IPUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IPU_ERSTELLUSER" "~"
if joinKuerzel = BMUIPU
searchResult := searchResult "LEFT JOIN HRS.IPU_IMPORTPUFFER ON IPU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IZSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IZS_ERSTELLUSER" "~"
if joinKuerzel = BMUIZS
searchResult := searchResult "LEFT JOIN BIL.IZS_INVZUSUMMEN ON IZS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KAD_ERSTELLUSER" "~"
if joinKuerzel = BMUKAD
searchResult := searchResult "LEFT JOIN KORE.KAD_AUSDRUCKDETAIL ON KAD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KAMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KAM_ERSTELLUSER" "~"
if joinKuerzel = BMUKAM
searchResult := searchResult "LEFT JOIN KORE.KAM_AUSDRUCKMASTER ON KAM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KBABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KBA_ERSTELLUSER" "~"
if joinKuerzel = BMUKBA
searchResult := searchResult "LEFT JOIN KORE.KBA_KOREBAUM ON KBA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KDIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KDI_ERSTELLUSER" "~"
if joinKuerzel = BMUKDI
searchResult := searchResult "LEFT JOIN KORE.KDI_ISTWERTE ON KDI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KEPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KEP_ERSTELLUSER" "~"
if joinKuerzel = BMUKEP
searchResult := searchResult "LEFT JOIN WWS.KEP_KASSENERFASSUNGSPROT ON KEP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KFZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KFZ_ERSTELLUSER" "~"
if joinKuerzel = BMUKFZ
searchResult := searchResult "LEFT JOIN KORE.KFZ_KOREFIBUZU ON KFZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KINBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KIN_ERSTELLUSER" "~"
if joinKuerzel = BMUKIN
searchResult := searchResult "LEFT JOIN BUERO.KIN_ZEITKOREFELDINFOS ON KIN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KMABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KMA_ERSTELLUSER" "~"
if joinKuerzel = BMUKMA
searchResult := searchResult "LEFT JOIN WWS.KMA_KUNDENMITARBBEZUG ON KMA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KMIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KMI_ERSTELLUSER" "~"
if joinKuerzel = BMUKMI
searchResult := searchResult "LEFT JOIN KORE.KMI_ISTMENGEN ON KMI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KMZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KMZ_ERSTELLUSER" "~"
if joinKuerzel = BMUKMZ
searchResult := searchResult "LEFT JOIN HRS.KMZ_KOMMSTZENTRALE ON KMZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KOKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KOK_ERSTELLUSER" "~"
if joinKuerzel = BMUKOK
searchResult := searchResult "LEFT JOIN PRODUKTION.KOK_KOMMISSIONIERUNG_KOPF ON KOK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KONBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KON_ERSTELLUSER" "~"
if joinKuerzel = BMUKON
searchResult := searchResult "LEFT JOIN BUERO.KON_KONTAKTPERSON ON KON_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KOSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KOS_ERSTELLUSER" "~"
if joinKuerzel = BMUKOS
searchResult := searchResult "LEFT JOIN ZLE.KOS_KOSTENFAKTOR ON KOS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KSPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KSP_ERSTELLUSER" "~"
if joinKuerzel = BMUKSP
searchResult := searchResult "LEFT JOIN KORE.KSP_SPALTENKOPF ON KSP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KTOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KTO_ERSTELLUSER" "~"
if joinKuerzel = BMUKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KUZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KUZ_ERSTELLUSER" "~"
if joinKuerzel = BMUKUZ
searchResult := searchResult "LEFT JOIN BUERO.KUZ_KUNDENZAHLUNGEN ON KUZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KVTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KVT_ERSTELLUSER" "~"
if joinKuerzel = BMUKVT
searchResult := searchResult "LEFT JOIN HRS.KVT_KVTAB ON KVT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KZZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KZZ_ERSTELLUSER" "~"
if joinKuerzel = BMUKZZ
searchResult := searchResult "LEFT JOIN KORE.KZZ_KSTKENNZAHLZU ON KZZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LARBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LAR_ERSTELLUSER" "~"
if joinKuerzel = BMULAR
searchResult := searchResult "LEFT JOIN HRS.LAR_LOHNARTENREGEL ON LAR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LEPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LEP_ERSTELLUSER" "~"
if joinKuerzel = BMULEP
searchResult := searchResult "LEFT JOIN ZLE.LEP_LEISTPROTOKOLL ON LEP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LFSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LFS_ERSTELLUSER" "~"
if joinKuerzel = BMULFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LGNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LGN_ERSTELLUSER" "~"
if joinKuerzel = BMULGN
searchResult := searchResult "LEFT JOIN BIL.LGN_LAENDERGRUPPEN ON LGN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LGRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LGR_ERSTELLUSER" "~"
if joinKuerzel = BMULGR
searchResult := searchResult "LEFT JOIN HRS.LGR_LOHNARTGRUPPE ON LGR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LKOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LKO_ERSTELLUSER" "~"
if joinKuerzel = BMULKO
searchResult := searchResult "LEFT JOIN WWS.LKO_LAGERKONTO ON LKO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LKTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LKT_ERSTELLUSER" "~"
if joinKuerzel = BMULKT
searchResult := searchResult "LEFT JOIN HRS.LKT_LOHNKONTO ON LKT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LSPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LSP_ERSTELLUSER" "~"
if joinKuerzel = BMULSP
searchResult := searchResult "LEFT JOIN HRS.LSP_LOHNSTAPEL ON LSP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LVKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LVK_ERSTELLUSER" "~"
if joinKuerzel = BMULVK
searchResult := searchResult "LEFT JOIN BMDINTERN.LVK_LIZENZVKP ON LVK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LZFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LZF_ERSTELLUSER" "~"
if joinKuerzel = BMULZF
searchResult := searchResult "LEFT JOIN HRS.LZF_LOHNZETTEL_FIN ON LZF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MBZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MBZ_ERSTELLUSER" "~"
if joinKuerzel = BMUMBZ
searchResult := searchResult "LEFT JOIN BUERO.MBZ_MEDIENBETRIEBSZEIT ON MBZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MEIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MEI_ERSTELLUSER" "~"
if joinKuerzel = BMUMEI
searchResult := searchResult "LEFT JOIN BUERO.MEI_MEDIENINFO ON MEI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MITBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MIT_ERSTELLUSER" "~"
if joinKuerzel = BMUMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MLXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MLX_ERSTELLUSER" "~"
if joinKuerzel = BMUMLX
searchResult := searchResult "LEFT JOIN BMDENTW.MLX_MEMLEAKS ON MLX_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MVTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MVT_ERSTELLUSER" "~"
if joinKuerzel = BMUMVT
searchResult := searchResult "LEFT JOIN BUERO.MVT_MEDIENVERLEIHTERMINZU ON MVT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MZZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MZZ_ERSTELLUSER" "~"
if joinKuerzel = BMUMZZ
searchResult := searchResult "LEFT JOIN BUERO.MZZ_MITARBZIELZU ON MZZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NUBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NUB_ERSTELLUSER" "~"
if joinKuerzel = BMUNUB
searchResult := searchResult "LEFT JOIN BMD.NUB_NTCSUPDBEZIEHER ON NUB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NUTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NUT_ERSTELLUSER" "~"
if joinKuerzel = BMUNUT
searchResult := searchResult "LEFT JOIN BMD.NUT_NTCSUPDTRANSFER ON NUT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OAZ_ERSTELLUSER" "~"
if joinKuerzel = BMUOAZ
searchResult := searchResult "LEFT JOIN LOHN.OAZ_OF_ARBEITSZEITMODELL ON OAZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OBZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OBZ_ERSTELLUSER" "~"
if joinKuerzel = BMUOBZ
searchResult := searchResult "LEFT JOIN LOHN.OBZ_OF_BANKLEITZAHL ON OBZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OKLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OKL_ERSTELLUSER" "~"
if joinKuerzel = BMUOKL
searchResult := searchResult "LEFT JOIN LOHN.OKL_OF_KLIENT ON OKL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ONVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ONV_ERSTELLUSER" "~"
if joinKuerzel = BMUONV
searchResult := searchResult "LEFT JOIN HRS.ONV_ONLINEBEW_VORDIENST ON ONV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OPDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OPD_ERSTELLUSER" "~"
if joinKuerzel = BMUOPD
searchResult := searchResult "LEFT JOIN LOHN.OPD_OF_PIPEDOGS ON OPD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OPLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OPL_ERSTELLUSER" "~"
if joinKuerzel = BMUOPL
searchResult := searchResult "LEFT JOIN LOHN.OPL_OF_PLZ ON OPL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OSLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OSL_ERSTELLUSER" "~"
if joinKuerzel = BMUOSL
searchResult := searchResult "LEFT JOIN DATANORM.OSL_SET_ON_LBSL ON OSL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OUBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OUB_ERSTELLUSER" "~"
if joinKuerzel = BMUOUB
searchResult := searchResult "LEFT JOIN BMD.OUB_OUTLOOKBAR ON OUB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OUEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OUE_ERSTELLUSER" "~"
if joinKuerzel = BMUOUE
searchResult := searchResult "LEFT JOIN BMD.OUE_OUTLOOKBARELEM ON OUE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OUTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OUT_ERSTELLUSER" "~"
if joinKuerzel = BMUOUT
searchResult := searchResult "LEFT JOIN LOHN.OUT_OF_ULTIMO ON OUT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PACBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAC_ERSTELLUSER" "~"
if joinKuerzel = BMUPAC
searchResult := searchResult "LEFT JOIN WWS.PAC_PACKSTOFF ON PAC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PAGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAG_ERSTELLUSER" "~"
if joinKuerzel = BMUPAG
searchResult := searchResult "LEFT JOIN BMDDEFAULT.PAG_PARAMGRUPPE ON PAG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PBCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PBC_ERSTELLUSER" "~"
if joinKuerzel = BMUPBC
searchResult := searchResult "LEFT JOIN PRODUKTION.PBC_CODEPAGE ON PBC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PBRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PBR_ERSTELLUSER" "~"
if joinKuerzel = BMUPBR
searchResult := searchResult "LEFT JOIN BUERO.PBR_PERSONBRANCHEZU ON PBR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PEABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PEA_ERSTELLUSER" "~"
if joinKuerzel = BMUPEA
searchResult := searchResult "LEFT JOIN WWS.PEA_PICEANNUMMERN ON PEA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PETBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PET_ERSTELLUSER" "~"
if joinKuerzel = BMUPET
searchResult := searchResult "LEFT JOIN WWS.PET_PREISTAB ON PET_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PFDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PFD_ERSTELLUSER" "~"
if joinKuerzel = BMUPFD
searchResult := searchResult "LEFT JOIN HRS.PFD_PFAENDUNG_DETAIL ON PFD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PFIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PFI_ERSTELLUSER" "~"
if joinKuerzel = BMUPFI
searchResult := searchResult "LEFT JOIN BMD.PFI_PANELFILTER ON PFI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PGEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PGE_ERSTELLUSER" "~"
if joinKuerzel = BMUPGE
searchResult := searchResult "LEFT JOIN BUERO.PGE_PERSONGESELLZU ON PGE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PGKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PGK_ERSTELLUSER" "~"
if joinKuerzel = BMUPGK
searchResult := searchResult "LEFT JOIN BUERO.PGK_PERSONGKKZU ON PGK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PGLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PGL_ERSTELLUSER" "~"
if joinKuerzel = BMUPGL
searchResult := searchResult "LEFT JOIN HRS.PGL_PERKZGLIEDERUNG ON PGL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIF_ERSTELLUSER" "~"
if joinKuerzel = BMUPIF
searchResult := searchResult "LEFT JOIN WWS.PIF_PICKUNDENZU ON PIF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PINBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIN_ERSTELLUSER" "~"
if joinKuerzel = BMUPIN
searchResult := searchResult "LEFT JOIN WWS.PIN_PICANBINDEARTZU ON PIN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PJOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PJO_ERSTELLUSER" "~"
if joinKuerzel = BMUPJO
searchResult := searchResult "LEFT JOIN WWS.PJO_PREISJOURNAL ON PJO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PJTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PJT_ERSTELLUSER" "~"
if joinKuerzel = BMUPJT
searchResult := searchResult "LEFT JOIN HRS.PJT_PERSSTAMM_TMP ON PJT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKO_ERSTELLUSER" "~"
if joinKuerzel = BMUPKO
searchResult := searchResult "LEFT JOIN BMD.PKO_PROTOKOLL ON PKO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKP_ERSTELLUSER" "~"
if joinKuerzel = BMUPKP
searchResult := searchResult "LEFT JOIN FIBU.PKP_BUDGETPERSKOSTPLAN ON PKP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKU_ERSTELLUSER" "~"
if joinKuerzel = BMUPKU
searchResult := searchResult "LEFT JOIN BUERO.PKU_PROJEKTZU ON PKU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PODBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = POD_ERSTELLUSER" "~"
if joinKuerzel = BMUPOD
searchResult := searchResult "LEFT JOIN BUERO.POD_POSTDOKUMENTZU ON POD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PPUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PPU_ERSTELLUSER" "~"
if joinKuerzel = BMUPPU
searchResult := searchResult "LEFT JOIN PRODUKTION.PPU_KAPA_POOL_ZU ON PPU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PREBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PRE_ERSTELLUSER" "~"
if joinKuerzel = BMUPRE
searchResult := searchResult "LEFT JOIN WWS.PRE_EXTPARTIKELWERTE ON PRE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PSKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PSK_ERSTELLUSER" "~"
if joinKuerzel = BMUPSK
searchResult := searchResult "LEFT JOIN BIL.PSK_PERSRST_STERBET_KOPF ON PSK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PTTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PTT_ERSTELLUSER" "~"
if joinKuerzel = BMUPTT
searchResult := searchResult "LEFT JOIN BMD.PTT_PERFORMANCETESTTAB_TMP ON PTT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PUSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PUS_ERSTELLUSER" "~"
if joinKuerzel = BMUPUS
searchResult := searchResult "LEFT JOIN PRODUKTION.PUS_PPSUSERSTATI ON PUS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = R2ABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = R2A_ERSTELLUSER" "~"
if joinKuerzel = BMUR2A
searchResult := searchResult "LEFT JOIN PRODUKTION.R2A_RPM_ARK_ZU ON R2A_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RAGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RAG_ERSTELLUSER" "~"
if joinKuerzel = BMURAG
searchResult := searchResult "LEFT JOIN BUERO.RAG_RUHEARBEITSZEITGESETZ ON RAG_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = REPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = REP_ERSTELLUSER" "~"
if joinKuerzel = BMUREP
searchResult := searchResult "LEFT JOIN BUERO.REP_RESSOURCENPAKET ON REP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RGDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RGD_ERSTELLUSER" "~"
if joinKuerzel = BMURGD
searchResult := searchResult "LEFT JOIN DATANORM.RGD_RABATT_GRUPPE_DETAIL ON RGD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RGSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RGS_ERSTELLUSER" "~"
if joinKuerzel = BMURGS
searchResult := searchResult "LEFT JOIN BMD.RGS_TABREPLGSTANDORTZU ON RGS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RGZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RGZ_ERSTELLUSER" "~"
if joinKuerzel = BMURGZ
searchResult := searchResult "LEFT JOIN BMD.RGZ_ROLLEGRUPPEZU ON RGZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RKTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RKT_ERSTELLUSER" "~"
if joinKuerzel = BMURKT
searchResult := searchResult "LEFT JOIN BMD.RKT_REPLIKATIONSTRANSFER ON RKT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RPRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RPR_ERSTELLUSER" "~"
if joinKuerzel = BMURPR
searchResult := searchResult "LEFT JOIN WWS.RPR_RECHNUNGSPROVISION ON RPR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RUZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RUZ_ERSTELLUSER" "~"
if joinKuerzel = BMURUZ
searchResult := searchResult "LEFT JOIN BMD.RUZ_ROLLEUSERZU ON RUZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RZZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RZZ_ERSTELLUSER" "~"
if joinKuerzel = BMURZZ
searchResult := searchResult "LEFT JOIN BUERO.RZZ_AZGARGZEITEN ON RZZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SA1BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SA1_ERSTELLUSER" "~"
if joinKuerzel = BMUSA1
searchResult := searchResult "LEFT JOIN ONVIS.SA1_SAURER_BDE ON SA1_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SA3BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SA3_ERSTELLUSER" "~"
if joinKuerzel = BMUSA3
searchResult := searchResult "LEFT JOIN ONVIS.SA3_SAURER_BDE ON SA3_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SATBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SAT_ERSTELLUSER" "~"
if joinKuerzel = BMUSAT
searchResult := searchResult "LEFT JOIN BUERO.SAT_SETANTWORT ON SAT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SBCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SBC_ERSTELLUSER" "~"
if joinKuerzel = BMUSBC
searchResult := searchResult "LEFT JOIN BUERO.SBC_SPEZBEWEGUNGSCODE ON SBC_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCF_ERSTELLUSER" "~"
if joinKuerzel = BMUSCF
searchResult := searchResult "LEFT JOIN BMDENTW.SCF_SCRFILE ON SCF_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCM_ERSTELLUSER" "~"
if joinKuerzel = BMUSCM
searchResult := searchResult "LEFT JOIN BMDENTW.SCM_SCRMAIN ON SCM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SLKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SLK_ERSTELLUSER" "~"
if joinKuerzel = BMUSLK
searchResult := searchResult "LEFT JOIN WWS.SLK_STUECKLISTENKOPF ON SLK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SMABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SMA_ERSTELLUSER" "~"
if joinKuerzel = BMUSMA
searchResult := searchResult "LEFT JOIN HRS.SMA_SCHNITTMA ON SMA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SMIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SMI_ERSTELLUSER" "~"
if joinKuerzel = BMUSMI
searchResult := searchResult "LEFT JOIN ZLE.SMI_SMARTINTERFACE ON SMI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SNIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SNI_ERSTELLUSER" "~"
if joinKuerzel = BMUSNI
searchResult := searchResult "LEFT JOIN HRS.SNI_SCHNITTE ON SNI_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SNRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SNR_ERSTELLUSER" "~"
if joinKuerzel = BMUSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPP_ERSTELLUSER" "~"
if joinKuerzel = BMUSPP
searchResult := searchResult "LEFT JOIN BMD.SPP_STAPELPARAMETER ON SPP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPT_ERSTELLUSER" "~"
if joinKuerzel = BMUSPT
searchResult := searchResult "LEFT JOIN BMD.SPT_STAPELSCHRITT ON SPT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPX_ERSTELLUSER" "~"
if joinKuerzel = BMUSPX
searchResult := searchResult "LEFT JOIN BMD.SPX_STAPELVARIABLE ON SPX_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SRBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SRB_ERSTELLUSER" "~"
if joinKuerzel = BMUSRB
searchResult := searchResult "LEFT JOIN BIL.SRB_STEUERBUCHUNGEN ON SRB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SVKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SVK_ERSTELLUSER" "~"
if joinKuerzel = BMUSVK
searchResult := searchResult "LEFT JOIN FIBU.SVK_SKONTOVERBUCHKZ ON SVK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SWABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SWA_ERSTELLUSER" "~"
if joinKuerzel = BMUSWA
searchResult := searchResult "LEFT JOIN HRS.SWA_SW_ANSPRUCH ON SWA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SZPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SZP_ERSTELLUSER" "~"
if joinKuerzel = BMUSZP
searchResult := searchResult "LEFT JOIN BUERO.SZP_SRVRECHZENTRUMPROT ON SZP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TCDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TCD_ERSTELLUSER" "~"
if joinKuerzel = BMUTCD
searchResult := searchResult "LEFT JOIN BMD.TCD_TCKONFDATEN ON TCD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TCTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TCT_ERSTELLUSER" "~"
if joinKuerzel = BMUTCT
searchResult := searchResult "LEFT JOIN BMD.TCT_TCTESTCASE ON TCT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TERBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TER_ERSTELLUSER" "~"
if joinKuerzel = BMUTER
searchResult := searchResult "LEFT JOIN BUERO.TER_TERMIN ON TER_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TGPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TGP_ERSTELLUSER" "~"
if joinKuerzel = BMUTGP
searchResult := searchResult "LEFT JOIN HRS.TGP_TARIFGR_TARIFPROZ_ZU ON TGP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TMOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TMO_ERSTELLUSER" "~"
if joinKuerzel = BMUTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TOUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TOU_ERSTELLUSER" "~"
if joinKuerzel = BMUTOU
searchResult := searchResult "LEFT JOIN WWS.TOU_TOUR ON TOU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TPOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TPO_ERSTELLUSER" "~"
if joinKuerzel = BMUTPO
searchResult := searchResult "LEFT JOIN BUERO.TPO_TERMINPOSTZU ON TPO_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UAZ_ERSTELLUSER" "~"
if joinKuerzel = BMUUAZ
searchResult := searchResult "LEFT JOIN BMDENTW.UAZ_USERLINKAUSLIEFERNZU ON UAZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UBEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UBE_ERSTELLUSER" "~"
if joinKuerzel = BMUUBE
searchResult := searchResult "LEFT JOIN BMDINTERN.UBE_UPDATEBESTELLUNG ON UBE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UEBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UEB_ERSTELLUSER" "~"
if joinKuerzel = BMUUEB
searchResult := searchResult "LEFT JOIN BUERO.UEB_UEBERWEISUNGSART ON UEB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UPLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UPL_ERSTELLUSER" "~"
if joinKuerzel = BMUUPL
searchResult := searchResult "LEFT JOIN BMDENTW.UPL_UPDATEHINWEIS_NLS ON UPL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UVABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UVA_ERSTELLUSER" "~"
if joinKuerzel = BMUUVA
searchResult := searchResult "LEFT JOIN FIBU.UVA_UVAMODELL ON UVA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VBVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VBV_ERSTELLUSER" "~"
if joinKuerzel = BMUVBV
searchResult := searchResult "LEFT JOIN FIBU.VBV_VORERFASSBUCH ON VBV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VEBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VEB_ERSTELLUSER" "~"
if joinKuerzel = BMUVEB
searchResult := searchResult "LEFT JOIN FIBU.VEB_VORERFASSBUCHUNG ON VEB_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VEKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VEK_ERSTELLUSER" "~"
if joinKuerzel = BMUVEK
searchResult := searchResult "LEFT JOIN WWS.VEK_VERTRETERABRECHKOPF ON VEK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VEPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VEP_ERSTELLUSER" "~"
if joinKuerzel = BMUVEP
searchResult := searchResult "LEFT JOIN WWS.VEP_VERTRETERABRECHPOS ON VEP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VEVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VEV_ERSTELLUSER" "~"
if joinKuerzel = BMUVEV
searchResult := searchResult "LEFT JOIN BMDENTW.VEV_NTCSVERSION ON VEV_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VHEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VHE_ERSTELLUSER" "~"
if joinKuerzel = BMUVHE
searchResult := searchResult "LEFT JOIN WWS.VHE_VA_HAUPTEANNUMMERN ON VHE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VPHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VPH_ERSTELLUSER" "~"
if joinKuerzel = BMUVPH
searchResult := searchResult "LEFT JOIN WWS.VPH_VPERMITTLUNG_FARZUORD ON VPH_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VPNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VPN_ERSTELLUSER" "~"
if joinKuerzel = BMUVPN
searchResult := searchResult "LEFT JOIN WWS.VPN_VERSMGR_POS ON VPN_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VPZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VPZ_ERSTELLUSER" "~"
if joinKuerzel = BMUVPZ
searchResult := searchResult "LEFT JOIN BUERO.VPZ_VORGANGPHASEZU ON VPZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VTSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VTS_ERSTELLUSER" "~"
if joinKuerzel = BMUVTS
searchResult := searchResult "LEFT JOIN WWS.VTS_VERTRETERSTUFE ON VTS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WADBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAD_ERSTELLUSER" "~"
if joinKuerzel = BMUWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WAUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAU_ERSTELLUSER" "~"
if joinKuerzel = BMUWAU
searchResult := searchResult "LEFT JOIN WWS.WAU_WWSAUFGABENAKTION ON WAU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WBTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WBT_ERSTELLUSER" "~"
if joinKuerzel = BMUWBT
searchResult := searchResult "LEFT JOIN WWS.WBT_WWSBUDGETKOPF ON WBT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WCEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WCE_ERSTELLUSER" "~"
if joinKuerzel = BMUWCE
searchResult := searchResult "LEFT JOIN BMD.WCE_WABAUMELEMENTE ON WCE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WCUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WCU_ERSTELLUSER" "~"
if joinKuerzel = BMUWCU
searchResult := searchResult "LEFT JOIN BMD.WCU_WAAUSDRUCKUSERZU ON WCU_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEA_ERSTELLUSER" "~"
if joinKuerzel = BMUWEA
searchResult := searchResult "LEFT JOIN WWS.WEA_WWSERKONTROLLERABATTE ON WEA_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEM_ERSTELLUSER" "~"
if joinKuerzel = BMUWEM
searchResult := searchResult "LEFT JOIN WWS.WEM_WARENEINSATZBUCHKZZU ON WEM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEP_ERSTELLUSER" "~"
if joinKuerzel = BMUWEP
searchResult := searchResult "LEFT JOIN WWS.WEP_WWSERKONTROLLEPOS ON WEP_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WFTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WFT_ERSTELLUSER" "~"
if joinKuerzel = BMUWFT
searchResult := searchResult "LEFT JOIN WEB.WFT_WEBFARBENTAB ON WFT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WILBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WIL_ERSTELLUSER" "~"
if joinKuerzel = BMUWIL
searchResult := searchResult "LEFT JOIN BMDENTW.WIL_WEITERE_INFO_LANG ON WIL_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WPRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WPR_ERSTELLUSER" "~"
if joinKuerzel = BMUWPR
searchResult := searchResult "LEFT JOIN WEB.WPR_WEBPROTOKOLL ON WPR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WRZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WRZ_ERSTELLUSER" "~"
if joinKuerzel = BMUWRZ
searchResult := searchResult "LEFT JOIN WWS.WRZ_WESRNP_ZUORDNUNG ON WRZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WSSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WSS_ERSTELLUSER" "~"
if joinKuerzel = BMUWSS
searchResult := searchResult "LEFT JOIN WWS.WSS_WWSSTATISTIK ON WSS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WSZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WSZ_ERSTELLUSER" "~"
if joinKuerzel = BMUWSZ
searchResult := searchResult "LEFT JOIN WWS.WSZ_WESEPS_ZUORDNUNG ON WSZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WUZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WUZ_ERSTELLUSER" "~"
if joinKuerzel = BMUWUZ
searchResult := searchResult "LEFT JOIN BUERO.WUZ_WUNSCHARBEITSZEIT ON WUZ_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WWMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WWM_ERSTELLUSER" "~"
if joinKuerzel = BMUWWM
searchResult := searchResult "LEFT JOIN WWS.WWM_WWSMAKROZUORD ON WWM_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = XBRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = XBR_ERSTELLUSER" "~"
if joinKuerzel = BMUXBR
searchResult := searchResult "LEFT JOIN BIL.XBR_XBRLDATA ON XBR_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = XSEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = XSE_ERSTELLUSER" "~"
if joinKuerzel = BMUXSE
searchResult := searchResult "LEFT JOIN BUERO.XSE_XML_STEUERERKL ON XSE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZBEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZBE_ERSTELLUSER" "~"
if joinKuerzel = BMUZBE
searchResult := searchResult "LEFT JOIN BUERO.ZBE_ZUSTAENDIGKEITBEREICH ON ZBE_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZIKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZIK_ERSTELLUSER" "~"
if joinKuerzel = BMUZIK
searchResult := searchResult "LEFT JOIN FIBU.ZIK_KONTENZINSKREIS ON ZIK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZKSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZKS_ERSTELLUSER" "~"
if joinKuerzel = BMUZKS
searchResult := searchResult "LEFT JOIN FIBU.ZKS_ZINSBERECHNUNGSKREIS ON ZKS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZLTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZLT_ERSTELLUSER" "~"
if joinKuerzel = BMUZLT
searchResult := searchResult "LEFT JOIN ZLE.ZLT_ZEILENTYP ON ZLT_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZMSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZMS_ERSTELLUSER" "~"
if joinKuerzel = BMUZMS
searchResult := searchResult "LEFT JOIN BUERO.ZMS_ZEITMONATSSUMMEN ON ZMS_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZWKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZWK_ERSTELLUSER" "~"
if joinKuerzel = BMUZWK
searchResult := searchResult "LEFT JOIN WWS.ZWK_ZUORDNUNGWVKOPF ON ZWK_ERSTELLUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPPDRS
searchResult := searchResult "LEFT JOIN BMD.DRS_DRUCKEREINSTELLUNG ON DRS_DREINSTELLUNGSID = SPP_VORSCHLAGSWERT" "~"
if joinKuerzel = DRSSPP
searchResult := searchResult "LEFT JOIN BMD.SPP_STAPELPARAMETER ON SPP_VORSCHLAGSWERT = DRS_DREINSTELLUNGSID" "~"
if joinKuerzel = ABSABQ
searchResult := searchResult "LEFT JOIN HRS.ABQ_ARBEITSSTAETTE_SEQ ON ABQ_ABSID = ABS_ABSID" "~"
if joinKuerzel = ABQABS
searchResult := searchResult "LEFT JOIN HRS.ABS_ARBEITSSTAETTE ON ABS_ABSID = ABQ_ABSID" "~"
if joinKuerzel = LZSABS
searchResult := searchResult "LEFT JOIN HRS.ABS_ARBEITSSTAETTE ON ABS_ABSID = LZS_ABSID AND ABS_LOHNJAHR = LZS_BZJJ AND ABS_FIRMENNR = LZS_DVFIRMENNR" "~"
if joinKuerzel = ABSLZS
searchResult := searchResult "LEFT JOIN HRS.LZS_LOHNZETTEL_SV ON LZS_ABSID = ABS_ABSID AND LZS_BZJJ = ABS_LOHNJAHR AND LZS_DVFIRMENNR = ABS_FIRMENNR" "~"
if joinKuerzel = PROADK
searchResult := searchResult "LEFT JOIN BUERO.ADK_ADRESSENLISTEKOPF ON ADK_FIRMENNR = PRO_ADK_FIRMENNR AND ADK_ADRLISTENNR = PRO_ADK_ADRLISTENNR" "~"
if joinKuerzel = ADKPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_ADK_FIRMENNR = ADK_FIRMENNR AND PRO_ADK_ADRLISTENNR = ADK_ADRLISTENNR" "~"
if joinKuerzel = ADDADO
searchResult := searchResult "LEFT JOIN BUERO.ADO_AUSDRUCKMODELL ON ADO_FIRMENNR = ADD_FIRMENNR AND ADO_AUSDRUCKMODELLNR = ADD_AUSDRUCKMODELLNR" "~"
if joinKuerzel = ADOADD
searchResult := searchResult "LEFT JOIN BUERO.ADD_AUSDRUCKMODELLDETAIL ON ADD_FIRMENNR = ADO_FIRMENNR AND ADD_AUSDRUCKMODELLNR = ADO_AUSDRUCKMODELLNR" "~"
if joinKuerzel = LFSADO
searchResult := searchResult "LEFT JOIN ADO_AUSDRUCKMODELL.ADO_AUSDRUCKMODELL ON ADO_FIRMENNR = LFS_AUSDRUCKMODELLFIRMENNR AND ADO_AUSDRUCKMODELLNR = LFS_AUSDRUCKMODELLNR" "~"
if joinKuerzel = ADOLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_AUSDRUCKMODELLFIRMENNR = ADO_FIRMENNR AND LFS_AUSDRUCKMODELLNR = ADO_AUSDRUCKMODELLNR" "~"
if joinKuerzel = WAKADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = WAK_RECH_ADRESSLFDNR" "~"
if joinKuerzel = ADRWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_RECH_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = WRKADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = WRK_RECHLEG_ADRESSLFDNR" "~"
if joinKuerzel = ADRWRK
searchResult := searchResult "LEFT JOIN WWS.WRK_WWSANKAUFSRNGKOPF ON WRK_RECHLEG_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = ZBMADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = ZBM_ADRESSLFDNR" "~"
if joinKuerzel = ADRZBM
searchResult := searchResult "LEFT JOIN FIBU.ZBM_ZINSBERECHNUNG_SUMME ON ZBM_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = BONADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = BON_LIEFEREMPF_ADRESSLFDNR" "~"
if joinKuerzel = ADRBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_LIEFEREMPF_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = BPKADR
searchResult := searchResult "LEFT JOIN ADR_ADRESSE.ADR_ADRESSE ON ADR_ADRESSLFDNR = BPK_RECHLEG_ADRESSLFDNR" "~"
if joinKuerzel = ADRBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_RECHLEG_ADRESSLFDNR = ADR_ADRESSLFDNR" "~"
if joinKuerzel = AFBAFB
searchResult := searchResult "LEFT JOIN FIBU.AFB_ANBUFIB ON AFB_ANBUFIBID = AFB_PARENT_ANBUFIBID" "~"
if joinKuerzel = AFBAFB
searchResult := searchResult "LEFT JOIN FIBU.AFB_ANBUFIB ON AFB_PARENT_ANBUFIBID = AFB_ANBUFIBID" "~"
if joinKuerzel = AFPAFK
searchResult := searchResult "LEFT JOIN ZLE.AFK_EXTFILTERKOPF ON AFK_FIRMENNR = AFP_FIRMENNR AND AFK_FILTERKOPFNR = AFP_FILTERKOPFNR" "~"
if joinKuerzel = AFKAFP
searchResult := searchResult "LEFT JOIN ZLE.AFP_EXTFILTERPOS ON AFP_FIRMENNR = AFK_FIRMENNR AND AFP_FILTERKOPFNR = AFK_FILTERKOPFNR" "~"
if joinKuerzel = RSBAGN
searchResult := searchResult "LEFT JOIN AGN_AG_PERSON_ZU.AGN_AG_PERSON_ZU ON AGN_AG_ID = RSB_AGS_AGSTAMMID AND AGN_AGPERSONZULFDNR = RSB_AGN_AGPERSONZULFDNR" "~"
if joinKuerzel = AGNRSB
searchResult := searchResult "LEFT JOIN BUERO.RSB_RESSOURCENBEDARF ON RSB_AGS_AGSTAMMID = AGN_AG_ID AND RSB_AGN_AGPERSONZULFDNR = AGN_AGPERSONZULFDNR" "~"
if joinKuerzel = ZBKAGR
searchResult := searchResult "LEFT JOIN BUERO.AGR_ABRECHNUNGSGRUPPE ON AGR_FIRMENNR = ZBK_AGR_FIRMENNR AND AGR_ABRECHNUNGSGRPNR = ZBK_AGR_ABRECHGRPNR" "~"
if joinKuerzel = AGRZBK
searchResult := searchResult "LEFT JOIN BUERO.ZBK_ZEITBETRIEBSKALENDER ON ZBK_AGR_FIRMENNR = AGR_FIRMENNR AND ZBK_AGR_ABRECHGRPNR = AGR_ABRECHNUNGSGRPNR" "~"
if joinKuerzel = ABRAGU
searchResult := searchResult "LEFT JOIN BIL.AGU_ANLAGEGUT ON AGU_ANLAGEGUTID = ABR_ANLAGEGUTID" "~"
if joinKuerzel = AGUABR
searchResult := searchResult "LEFT JOIN BIL.ABR_ANBUBEWRES ON ABR_ANLAGEGUTID = AGU_ANLAGEGUTID" "~"
if joinKuerzel = DISAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = DIS_AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDDIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_AID_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = VLJAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = VLJ_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDVLJ
searchResult := searchResult "LEFT JOIN WWS.VLJ_VERLEIHJOURNAL ON VLJ_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = VSWAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = VSW_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDVSW
searchResult := searchResult "LEFT JOIN WWS.VSW_VERTRETERSTATISTIK ON VSW_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = WSSAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_ARTIKELIDLFDNR = WSS_ARTIKELIDLFDNR" "~"
if joinKuerzel = AIDWSS
searchResult := searchResult "LEFT JOIN WWS.WSS_WWSSTATISTIK ON WSS_ARTIKELIDLFDNR = AID_ARTIKELIDLFDNR" "~"
if joinKuerzel = AVZAJO
searchResult := searchResult "LEFT JOIN AJO_ANBUJOURNAL.AJO_ANBUJOURNAL ON AJO_ANBUJOURNALID = AVZ_ANBUJOURNALID AND AJO_ANLAGEGUTID = AVZ_ANLAGEGUTID" "~"
if joinKuerzel = AJOAVZ
searchResult := searchResult "LEFT JOIN FIBU.AVZ_ANBUFIBZU ON AVZ_ANBUJOURNALID = AJO_ANBUJOURNALID AND AVZ_ANLAGEGUTID = AJO_ANLAGEGUTID" "~"
if joinKuerzel = AUGAKS
searchResult := searchResult "LEFT JOIN BUERO.AKS_AKTIONSTATUS ON AKS_AKKID = AUG_AKKID AND AKS_AKSNR = AUG_AKSNR" "~"
if joinKuerzel = AKSAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_AKKID = AKS_AKKID AND AUG_AKSNR = AKS_AKSNR" "~"
if joinKuerzel = DVHAMG
searchResult := searchResult "LEFT JOIN BUERO.AMG_GKKABMELDEGRUND ON AMG_ABMELDEGRUNDNR = DVH_ABMELDEGRUNDNR" "~"
if joinKuerzel = AMGDVH
searchResult := searchResult "LEFT JOIN BUERO.DVH_DIENSTVERHAELTNIS ON DVH_ABMELDEGRUNDNR = AMG_ABMELDEGRUNDNR" "~"
if joinKuerzel = ADRANR
searchResult := searchResult "LEFT JOIN BUERO.ANR_ANREDE ON ANR_ANREDENR = ADR_EHEPAAR_PERS_ANREDENR" "~"
if joinKuerzel = ANRADR
searchResult := searchResult "LEFT JOIN BUERO.ADR_ADRESSE ON ADR_EHEPAAR_PERS_ANREDENR = ANR_ANREDENR" "~"
if joinKuerzel = FGEANT
searchResult := searchResult "LEFT JOIN BUERO.ANT_ANTWORT ON ANT_ANTWORTID = FGE_ANTWORTID" "~"
if joinKuerzel = ANTFGE
searchResult := searchResult "LEFT JOIN BUERO.FGE_FRAGE ON FGE_ANTWORTID = ANT_ANTWORTID" "~"
if joinKuerzel = EFKAPA
searchResult := searchResult "LEFT JOIN APA_ARBEITSPLAN.APA_ARBEITSPLAN ON APA_AP_ID = EFK_AP_ID" "~"
if joinKuerzel = APAEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_AP_ID = APA_AP_ID" "~"
if joinKuerzel = PCKAPA
searchResult := searchResult "LEFT JOIN PRODUKTION.APA_ARBEITSPLAN ON APA_AP_ID = PCK_AP_ID" "~"
if joinKuerzel = APAPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_AP_ID = APA_AP_ID" "~"
if joinKuerzel = WAFAPG
searchResult := searchResult "LEFT JOIN WWS.APG_ARAPRODUKTGRUPPEN ON APG_FIRMENNR = WAF_APG_FIRMENNR AND APG_PRODUKTGRPNR = WAF_APG_PRODUKTGRPNR" "~"
if joinKuerzel = APGWAF
searchResult := searchResult "LEFT JOIN WWS.WAF_WWSADRZUFIRMENBEZ ON WAF_APG_FIRMENNR = APG_FIRMENNR AND WAF_APG_PRODUKTGRPNR = APG_PRODUKTGRPNR" "~"
if joinKuerzel = APLAPS
searchResult := searchResult "LEFT JOIN WWS.APS_ARBEITSPLAN_STAMM ON APS_APSTAMM_ID = APL_APSTAMM_ID" "~"
if joinKuerzel = APSAPL
searchResult := searchResult "LEFT JOIN WWS.APL_ARBEITSPLANLINIEZU ON APL_APSTAMM_ID = APS_APSTAMM_ID" "~"
if joinKuerzel = ARMARG
searchResult := searchResult "LEFT JOIN BUERO.ARG_ARBEITSRUHEZEITGESETZ ON ARG_FIRMENNR = ARM_FIRMENNR AND ARG_ARGNR = ARM_ARGNR" "~"
if joinKuerzel = ARGARM
searchResult := searchResult "LEFT JOIN BUERO.ARM_ARBEITSZEITGESETZ_MAX ON ARM_FIRMENNR = ARG_FIRMENNR AND ARM_ARGNR = ARG_ARGNR" "~"
if joinKuerzel = MITARG
searchResult := searchResult "LEFT JOIN BUERO.ARG_ARBEITSRUHEZEITGESETZ ON ARG_FIRMENNR = MIT_ARG_FIRMENNR AND ARG_ARGNR = MIT_ARGNR" "~"
if joinKuerzel = ARGMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_ARG_FIRMENNR = ARG_FIRMENNR AND MIT_ARGNR = ARG_ARGNR" "~"
if joinKuerzel = AWSARK
searchResult := searchResult "LEFT JOIN ARK_ARTIKELKLASSE.ARK_ARTIKELKLASSE ON ARK_FIRMENNR = AWS_ARK_FIRMENNR AND ARK_ARTIKELKLASSE = AWS_ARK_ARTIKELKLASSE" "~"
if joinKuerzel = ARKAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_ARK_FIRMENNR = ARK_FIRMENNR AND AWS_ARK_ARTIKELKLASSE = ARK_ARTIKELKLASSE" "~"
if joinKuerzel = R2AARK
searchResult := searchResult "LEFT JOIN ARK_ARTIKELKLASSE.ARK_ARTIKELKLASSE ON ARK_FIRMENNR = R2A_ARK_FIRMENNR AND ARK_ARTIKELKLASSE = R2A_ARTIKELKLASSE" "~"
if joinKuerzel = ARKR2A
searchResult := searchResult "LEFT JOIN PRODUKTION.R2A_RPM_ARK_ZU ON R2A_ARK_FIRMENNR = ARK_FIRMENNR AND R2A_ARTIKELKLASSE = ARK_ARTIKELKLASSE" "~"
if joinKuerzel = ACSARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = ACS_ARL_FIRMENNR AND ARL_ARTIKELNR = ACS_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLACS
searchResult := searchResult "LEFT JOIN WWS.ACS_TEST ON ACS_ARL_FIRMENNR = ARL_FIRMENNR AND ACS_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = ALZARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = ALZ_ARL_FIRMENNR AND ARL_ARTIKELNR = ALZ_ARTIKELNR" "~"
if joinKuerzel = ARLALZ
searchResult := searchResult "LEFT JOIN WWS.ALZ_ALLERGENDATEN ON ALZ_ARL_FIRMENNR = ARL_FIRMENNR AND ALZ_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = BAZARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = BAZ_ARL_FIRMENNR AND ARL_ARTIKELNR = BAZ_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLBAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_ARL_FIRMENNR = ARL_FIRMENNR AND BAZ_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = BETARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = BET_WWS_FIRMENNR AND ARL_ARTIKELNR = BET_WWS_ARTIKELNR" "~"
if joinKuerzel = ARLBET
searchResult := searchResult "LEFT JOIN BUERO.BET_BETRIEBSMITTEL ON BET_WWS_FIRMENNR = ARL_FIRMENNR AND BET_WWS_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = EHPARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = EHP_FIRMENNR AND ARL_ARTIKELNR = EHP_FOLGE_ARTIKELNR" "~"
if joinKuerzel = ARLEHP
searchResult := searchResult "LEFT JOIN WWS.EHP_EINHEITENTABPOS ON EHP_FIRMENNR = ARL_FIRMENNR AND EHP_FOLGE_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = KOPARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = KOP_ARL_FIRMENNR AND ARL_ARTIKELNR = KOP_ARTIKELNR" "~"
if joinKuerzel = ARLKOP
searchResult := searchResult "LEFT JOIN PRODUKTION.KOP_KOMMISSIONIERUNG_POS ON KOP_ARL_FIRMENNR = ARL_FIRMENNR AND KOP_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = KUKARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = KUK_ARL_FIRMENNR AND ARL_ARTIKELNR = KUK_ARTIKELNR" "~"
if joinKuerzel = ARLKUK
searchResult := searchResult "LEFT JOIN WWS.KUK_KUNDENKARTEI ON KUK_ARL_FIRMENNR = ARL_FIRMENNR AND KUK_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = LHPARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = LHP_HP_ARL_FIRMENNR AND ARL_ARTIKELNR = LHP_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLLHP
searchResult := searchResult "LEFT JOIN BMDINTERN.LHP_LIZENZHAUPTPAKETZU ON LHP_HP_ARL_FIRMENNR = ARL_FIRMENNR AND LHP_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = PACARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = PAC_FIRMENNR AND ARL_ARTIKELNR = PAC_ARA_ARTIKELNR" "~"
if joinKuerzel = ARLPAC
searchResult := searchResult "LEFT JOIN WWS.PAC_PACKSTOFF ON PAC_FIRMENNR = ARL_FIRMENNR AND PAC_ARA_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = PJOARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = PJO_ARL_FIRMENNR AND ARL_ARTIKELNR = PJO_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLPJO
searchResult := searchResult "LEFT JOIN WWS.PJO_PREISJOURNAL ON PJO_ARL_FIRMENNR = ARL_FIRMENNR AND PJO_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = PTPARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = PTP_ARL_FIRMENNR AND ARL_ARTIKELNR = PTP_ARTIKELNR" "~"
if joinKuerzel = ARLPTP
searchResult := searchResult "LEFT JOIN WWS.PTP_PREISTABPOS ON PTP_ARL_FIRMENNR = ARL_FIRMENNR AND PTP_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = TLPARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = TLP_ARL_FIRMENNR AND ARL_ARTIKELNR = TLP_ARTIKELNR" "~"
if joinKuerzel = ARLTLP
searchResult := searchResult "LEFT JOIN PRODUKTION.TLP_BT_LOGISTIK_POSITION ON TLP_ARL_FIRMENNR = ARL_FIRMENNR AND TLP_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = TLTARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = TLT_ARL_FIRMENNR AND ARL_ARTIKELNR = TLT_ARTIKELNR" "~"
if joinKuerzel = ARLTLT
searchResult := searchResult "LEFT JOIN PRODUKTION.TLT_BT_LOGISTIK_TRANSPORT ON TLT_ARL_FIRMENNR = ARL_FIRMENNR AND TLT_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = TRTARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = TRT_ARL_FIRMENNR AND ARL_ARTIKELNR = TRT_ARTIKELNR" "~"
if joinKuerzel = ARLTRT
searchResult := searchResult "LEFT JOIN WWS.TRT_TRANSPORTTYP ON TRT_ARL_FIRMENNR = ARL_FIRMENNR AND TRT_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = VSWARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = VSW_HAUPTFIRMENNR AND ARL_ARTIKELNR = VSW_ARTIKELNR" "~"
if joinKuerzel = ARLVSW
searchResult := searchResult "LEFT JOIN WWS.VSW_VERTRETERSTATISTIK ON VSW_HAUPTFIRMENNR = ARL_FIRMENNR AND VSW_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = WETARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = WET_ARL_FIRMENNR AND ARL_ARTIKELNR = WET_ARTIKELNR" "~"
if joinKuerzel = ARLWET
searchResult := searchResult "LEFT JOIN WWS.WET_WARENEINSATZARTZU ON WET_ARL_FIRMENNR = ARL_FIRMENNR AND WET_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = WOLARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = WOL_ARL_FIRMENNR AND ARL_ARTIKELNR = WOL_ARL_ARTIKELNR" "~"
if joinKuerzel = ARLWOL
searchResult := searchResult "LEFT JOIN ZLE.WOL_WOCHENLEISTUNG ON WOL_ARL_FIRMENNR = ARL_FIRMENNR AND WOL_ARL_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = WRPARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = WRP_HAUPTFIRMENNR AND ARL_ARTIKELNR = WRP_ARTIKELNR" "~"
if joinKuerzel = ARLWRP
searchResult := searchResult "LEFT JOIN WWS.WRP_WWSANKAUFSRNGPOS ON WRP_HAUPTFIRMENNR = ARL_FIRMENNR AND WRP_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = WSSARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = WSS_ARL_FIRMENNR AND ARL_ARTIKELNR = WSS_ARTIKELNR" "~"
if joinKuerzel = ARLWSS
searchResult := searchResult "LEFT JOIN WWS.WSS_WWSSTATISTIK ON WSS_ARL_FIRMENNR = ARL_FIRMENNR AND WSS_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = ARLARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = ARL_FIRMENNR AND ARL_ARTIKELNR = ARL_VERLEIH_TM_ARTIKELNR" "~"
if joinKuerzel = ARLARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = ARL_FIRMENNR AND ARL_VERLEIH_TM_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = ARUARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = ARU_TAT_FIRMENNR AND ARL_ARTIKELNR = ARU_TAT_ARTIKELNR" "~"
if joinKuerzel = ARLARU
searchResult := searchResult "LEFT JOIN ZLE.ARU_ARTIKELUMLAGE ON ARU_TAT_FIRMENNR = ARL_FIRMENNR AND ARU_TAT_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = MEDARL
searchResult := searchResult "LEFT JOIN ARL_ARTIKEL.ARL_ARTIKEL ON ARL_FIRMENNR = MED_ARL_ENTSPR_FIRMENNR AND ARL_ARTIKELNR = MED_ARL_ENTSPR_ARTIKELNR" "~"
if joinKuerzel = ARLMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_ARL_ENTSPR_FIRMENNR = ARL_FIRMENNR AND MED_ARL_ENTSPR_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = PICARS
searchResult := searchResult "LEFT JOIN WWS.ARS_ARTIKELSTATISTIK ON ARS_FIRMENNR = PIC_ARS_FIRMENNR AND ARS_STATISTIKLFDNR = PIC_ARS_STATISTIKLFDNR" "~"
if joinKuerzel = ARSPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_ARS_FIRMENNR = ARS_FIRMENNR AND PIC_ARS_STATISTIKLFDNR = ARS_STATISTIKLFDNR" "~"
if joinKuerzel = PWKARS
searchResult := searchResult "LEFT JOIN WWS.ARS_ARTIKELSTATISTIK ON ARS_FIRMENNR = PWK_ARS_FIRMENNR AND ARS_STATISTIKLFDNR = PWK_ARS_STATISTIKLFDNR" "~"
if joinKuerzel = ARSPWK
searchResult := searchResult "LEFT JOIN WWS.PWK_PREISRABATTWARTKOPF ON PWK_ARS_FIRMENNR = ARS_FIRMENNR AND PWK_ARS_STATISTIKLFDNR = ARS_STATISTIKLFDNR" "~"
if joinKuerzel = BPKASJ
searchResult := searchResult "LEFT JOIN ASJ_ASYNCHRONEJOBS.ASJ_ASYNCHRONEJOBS ON ASJ_JOB_ID = BPK_ASYNC_JOBID" "~"
if joinKuerzel = ASJBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_ASYNC_JOBID = ASJ_JOB_ID" "~"
if joinKuerzel = SSZASK
searchResult := searchResult "LEFT JOIN ZLE.ASK_SORTIERKOPF ON ASK_FIRMENNR = SSZ_ASK_FIRMENNR AND ASK_SORTKOPFNR = SSZ_ASK_SORTKOPFNR" "~"
if joinKuerzel = ASKSSZ
searchResult := searchResult "LEFT JOIN ZLE.SSZ_SORTIERKOPFSMIZU ON SSZ_ASK_FIRMENNR = ASK_FIRMENNR AND SSZ_ASK_SORTKOPFNR = ASK_SORTKOPFNR" "~"
if joinKuerzel = ASZASR
searchResult := searchResult "LEFT JOIN BUERO.ASR_AUSSCHREIBUNG ON ASR_FIRMENNR = ASZ_ASR_FIRMENNR AND ASR_AUSSCHREIBUNGSNR = ASZ_ASR_AUSSCHREIBUNGSNR" "~"
if joinKuerzel = ASRASZ
searchResult := searchResult "LEFT JOIN BUERO.ASZ_AUSSCHRFILIALEZU ON ASZ_ASR_FIRMENNR = ASR_FIRMENNR AND ASZ_ASR_AUSSCHREIBUNGSNR = ASR_AUSSCHREIBUNGSNR" "~"
if joinKuerzel = AWEATE
searchResult := searchResult "LEFT JOIN ATE_ABTEILUNG.ATE_ABTEILUNG ON ATE_FIRMENNR = AWE_ATE_MIT_FIRMENNR AND ATE_ABTEILUNGSNR = AWE_ATE_MIT_ABTEILUNGSNR" "~"
if joinKuerzel = ATEAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_ATE_MIT_FIRMENNR = ATE_FIRMENNR AND AWE_ATE_MIT_ABTEILUNGSNR = ATE_ABTEILUNGSNR" "~"
if joinKuerzel = AWSATE
searchResult := searchResult "LEFT JOIN ATE_ABTEILUNG.ATE_ABTEILUNG ON ATE_FIRMENNR = AWS_ATE_KLI_FIRMENNR AND ATE_ABTEILUNGSNR = AWS_ATE_KLI_ABTEILUNGSNR" "~"
if joinKuerzel = ATEAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_ATE_KLI_FIRMENNR = ATE_FIRMENNR AND AWS_ATE_KLI_ABTEILUNGSNR = ATE_ABTEILUNGSNR" "~"
if joinKuerzel = KASATE
searchResult := searchResult "LEFT JOIN ATE_ABTEILUNG.ATE_ABTEILUNG ON ATE_FIRMENNR = KAS_ATE_FIRMENNR AND ATE_ABTEILUNGSNR = KAS_ATE_ABTEILUNGSNR" "~"
if joinKuerzel = ATEKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_ATE_FIRMENNR = ATE_FIRMENNR AND KAS_ATE_ABTEILUNGSNR = ATE_ABTEILUNGSNR" "~"
if joinKuerzel = LEIATE
searchResult := searchResult "LEFT JOIN ATE_ABTEILUNG.ATE_ABTEILUNG ON ATE_FIRMENNR = LEI_ATE_PER_FIRMENNR AND ATE_ABTEILUNGSNR = LEI_ATE_PER_ABTEILUNGSNR" "~"
if joinKuerzel = ATELEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_ATE_PER_FIRMENNR = ATE_FIRMENNR AND LEI_ATE_PER_ABTEILUNGSNR = ATE_ABTEILUNGSNR" "~"
if joinKuerzel = UMFATE
searchResult := searchResult "LEFT JOIN BUERO.ATE_ABTEILUNG ON ATE_FIRMENNR = UMF_ABT_FIRMENNR AND ATE_ABTEILUNGSNR = UMF_ABT_ABTEILUNGSNR" "~"
if joinKuerzel = ATEUMF
searchResult := searchResult "LEFT JOIN BUERO.UMF_UMFRAGE ON UMF_ABT_FIRMENNR = ATE_FIRMENNR AND UMF_ABT_ABTEILUNGSNR = ATE_ABTEILUNGSNR" "~"
if joinKuerzel = ZBUATE
searchResult := searchResult "LEFT JOIN BUERO.ATE_ABTEILUNG ON ATE_FIRMENNR = ZBU_ATE_FIRMENNR AND ATE_ABTEILUNGSNR = ZBU_ATE_ABTEILUNGSNR" "~"
if joinKuerzel = ATEZBU
searchResult := searchResult "LEFT JOIN BUERO.ZBU_ZEITBUCHUNG ON ZBU_ATE_FIRMENNR = ATE_FIRMENNR AND ZBU_ATE_ABTEILUNGSNR = ATE_ABTEILUNGSNR" "~"
if joinKuerzel = KJOAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_AUFGABENNR = KJO_AUFGABENNR" "~"
if joinKuerzel = AUGKJO
searchResult := searchResult "LEFT JOIN BUERO.KJO_KONTAKTJOURNAL ON KJO_AUFGABENNR = AUG_AUFGABENNR" "~"
if joinKuerzel = AWSAVS
searchResult := searchResult "LEFT JOIN AVS_AVSTATUS.AVS_AVSTATUS ON AVS_FIRMENNR = AWS_AVS_FIRMENNR AND AVS_STATUSTYP = AWS_AVS_STATUSTYP AND AVS_STATUSNR = AWS_AVS_STATUSNR" "~"
if joinKuerzel = AVSAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_AVS_FIRMENNR = AVS_FIRMENNR AND AWS_AVS_STATUSTYP = AVS_STATUSTYP AND AWS_AVS_STATUSNR = AVS_STATUSNR" "~"
if joinKuerzel = MABAVS
searchResult := searchResult "LEFT JOIN BUERO.AVS_AVSTATUS ON AVS_FIRMENNR = MAB_AVS_FIRMENNR AND AVS_STATUSTYP = MAB_AVS_TYP AND AVS_STATUSNR = MAB_AVS_NR" "~"
if joinKuerzel = AVSMAB
searchResult := searchResult "LEFT JOIN BUERO.MAB_MITARBAUSBILDUNG ON MAB_AVS_FIRMENNR = AVS_FIRMENNR AND MAB_AVS_TYP = AVS_STATUSTYP AND MAB_AVS_NR = AVS_STATUSNR" "~"
if joinKuerzel = AWZAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_FIRMENNR = AWZ_AWE_FIRMENNR_SUB AND AWE_AUSWERTTYPNR = AWZ_AWE_AUSWERTTYPNR_SUB AND AWE_AUSWERTNR = AWZ_AWE_AUSWERTNR_SUB" "~"
if joinKuerzel = AWEAWZ
searchResult := searchResult "LEFT JOIN ZLE.AWZ_AUSWERTUNGZU ON AWZ_AWE_FIRMENNR_SUB = AWE_FIRMENNR AND AWZ_AWE_AUSWERTTYPNR_SUB = AWE_AUSWERTTYPNR AND AWZ_AWE_AUSWERTNR_SUB = AWE_AUSWERTNR" "~"
if joinKuerzel = AWEAWR
searchResult := searchResult "LEFT JOIN AWR_AUSWERTUNGSKAT.AWR_AUSWERTUNGSKAT ON AWR_AWRKATID = AWE_AWR_AWRKATID" "~"
if joinKuerzel = AWRAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_AWR_AWRKATID = AWR_AWRKATID" "~"
if joinKuerzel = ERBBAI
searchResult := searchResult "LEFT JOIN BAI_BANKINSTITUTIONSZU.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = ERB_BAI_BANKINSTLFDNR" "~"
if joinKuerzel = BAIERB
searchResult := searchResult "LEFT JOIN FIBU.ERB_BUCHUNG ON ERB_BAI_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = YBUBAI
searchResult := searchResult "LEFT JOIN BAI_BANKINSTITUTIONSZU.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = YBU_FIRMA_BAI_LFDNR" "~"
if joinKuerzel = BAIYBU
searchResult := searchResult "LEFT JOIN FIBU.YBU_AUTOBUCHUNG ON YBU_FIRMA_BAI_LFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = FIWBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_BANKINSTLFDNR = FIW_GER_BANKINSTLFDNR" "~"
if joinKuerzel = BAIFIW
searchResult := searchResult "LEFT JOIN BUERO.FIW_FIRMENWERTE ON FIW_GER_BANKINSTLFDNR = BAI_BANKINSTLFDNR" "~"
if joinKuerzel = PKDBAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_FIRMENNR = PKD_BAZ_FIRMENNR AND BAZ_BAZ = PKD_BAZ_BAZ" "~"
if joinKuerzel = BAZPKD
searchResult := searchResult "LEFT JOIN PRODUKTION.PKD_KM_DETAIL ON PKD_BAZ_FIRMENNR = BAZ_FIRMENNR AND PKD_BAZ_BAZ = BAZ_BAZ" "~"
if joinKuerzel = PRZBAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_FIRMENNR = PRZ_BAZ_FIRMENNR AND BAZ_BAZ = PRZ_BAZ" "~"
if joinKuerzel = BAZPRZ
searchResult := searchResult "LEFT JOIN PRODUKTION.PRZ_PROZESSBAZZU ON PRZ_BAZ_FIRMENNR = BAZ_FIRMENNR AND PRZ_BAZ = BAZ_BAZ" "~"
if joinKuerzel = FIRBEK
searchResult := searchResult "LEFT JOIN BUERO.BEK_BERECHTKATEGORIE ON BEK_BERECHTKATEGORIENR = FIR_BERECHTKATEGORIENR" "~"
if joinKuerzel = BEKFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_BERECHTKATEGORIENR = BEK_BERECHTKATEGORIENR" "~"
if joinKuerzel = DBEBEG
searchResult := searchResult "LEFT JOIN BEG_BERECHTIGUNG.BEG_BERECHTIGUNG ON BEG_BERECHTIGUNGSNR = DBE_BERECHTIGUNGSNR" "~"
if joinKuerzel = BEGDBE
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DBE_DBERECHTIGUNG ON DBE_BERECHTIGUNGSNR = BEG_BERECHTIGUNGSNR" "~"
if joinKuerzel = BUPBIM
searchResult := searchResult "LEFT JOIN FIBU.BIM_BUERFIMPORTMODELL ON BIM_BIMODELLNR = BUP_BIMODELLNR" "~"
if joinKuerzel = BIMBUP
searchResult := searchResult "LEFT JOIN FIBU.BUP_BUERFIMPORTPARAMETER ON BUP_BIMODELLNR = BIM_BIMODELLNR" "~"
if joinKuerzel = BIZBIP
searchResult := searchResult "LEFT JOIN FIBU.BIP_BUDGETINVESTPLAN ON BIP_FIRMENNR = BIZ_FIRMENNR AND BIP_FIBUNR = BIZ_FIBUNR AND BIP_BUDGETMODELLNR = BIZ_BUDGETMODELLNR AND BIP_BUDGETJAHR = BIZ_BUDGETJAHR AND BIP_INVESTITIONLFDNR = BIZ_INVESTITIONLFDNR AND BIP_BUART = BIZ_BUART" "~"
if joinKuerzel = BIPBIZ
searchResult := searchResult "LEFT JOIN FIBU.BIZ_BUDGETINVESTZAHLUNG ON BIZ_FIRMENNR = BIP_FIRMENNR AND BIZ_FIBUNR = BIP_FIBUNR AND BIZ_BUDGETMODELLNR = BIP_BUDGETMODELLNR AND BIZ_BUDGETJAHR = BIP_BUDGETJAHR AND BIZ_INVESTITIONLFDNR = BIP_INVESTITIONLFDNR AND BIZ_BUART = BIP_BUART" "~"
if joinKuerzel = BEZBKR
searchResult := searchResult "LEFT JOIN FIBU.BKR_BANKEINZUGSKREIS ON BKR_FIRMENNR = BEZ_FIRMENNR AND BKR_FIBUNR = BEZ_FIBUNR AND BKR_EINZUGSKREISLFDNR = BEZ_EINZUGSKREISLFDNR" "~"
if joinKuerzel = BKRBEZ
searchResult := searchResult "LEFT JOIN FIBU.BEZ_BANKEINZUG ON BEZ_FIRMENNR = BKR_FIRMENNR AND BEZ_FIBUNR = BKR_FIBUNR AND BEZ_EINZUGSKREISLFDNR = BKR_EINZUGSKREISLFDNR" "~"
if joinKuerzel = FGBBKU
searchResult := searchResult "LEFT JOIN BUERO.BKU_BEFRAGUNGKRITERIUMZU ON BKU_KRITERIENNR = FGB_KRITERIENNR AND BKU_BEFRAGUNGSNR = FGB_BEFRAGUNGSNR" "~"
if joinKuerzel = BKUFGB
searchResult := searchResult "LEFT JOIN BUERO.FGB_FRAGEBOGEN ON FGB_KRITERIENNR = BKU_KRITERIENNR AND FGB_BEFRAGUNGSNR = BKU_BEFRAGUNGSNR" "~"
if joinKuerzel = KZZBKZ
searchResult := searchResult "LEFT JOIN BKZ_BILANZKENNZAHL.BKZ_BILANZKENNZAHL ON BKZ_FIRMENNR = KZZ_BKZ_FIRMENNR AND BKZ_ISTEIGENE = KZZ_BKZ_ISTEIGENE AND BKZ_KZNR = KZZ_BKZ_KZNR AND BKZ_KZPAKETNR = KZZ_BKZ_KZPAKETNR" "~"
if joinKuerzel = BKZKZZ
searchResult := searchResult "LEFT JOIN KORE.KZZ_KSTKENNZAHLZU ON KZZ_BKZ_FIRMENNR = BKZ_FIRMENNR AND KZZ_BKZ_ISTEIGENE = BKZ_ISTEIGENE AND KZZ_BKZ_KZNR = BKZ_KZNR AND KZZ_BKZ_KZPAKETNR = BKZ_KZPAKETNR" "~"
if joinKuerzel = BLBBLS
searchResult := searchResult "LEFT JOIN BMD.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = BLB_BLOBLFDNR" "~"
if joinKuerzel = BLSBLB
searchResult := searchResult "LEFT JOIN BMD.BLB_BLOB ON BLB_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = BATBLS
searchResult := searchResult "LEFT JOIN BMD.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = BAT_BLOBLFDNR" "~"
if joinKuerzel = BLSBAT
searchResult := searchResult "LEFT JOIN BMD.BAT_BMDATTACHMENT ON BAT_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = BURBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = BUR_DATEI_BLOBLFDNR" "~"
if joinKuerzel = BLSBUR
searchResult := searchResult "LEFT JOIN BUERO.BUR_BMDCOMUPLOADTREE ON BUR_DATEI_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = CAWBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = CAW_HTML_BLOBLFDNR" "~"
if joinKuerzel = BLSCAW
searchResult := searchResult "LEFT JOIN BUERO.CAW_CRMABWESENHEIT ON CAW_HTML_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = CDCBLS
searchResult := searchResult "LEFT JOIN BMD.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = CDC_XMLDATA_BLOBLFDNR" "~"
if joinKuerzel = BLSCDC
searchResult := searchResult "LEFT JOIN BMD.CDC_COMPASSDATACACHE ON CDC_XMLDATA_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = HTBBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = HTB_BLOBLFDNR" "~"
if joinKuerzel = BLSHTB
searchResult := searchResult "LEFT JOIN BMDENTW.HTB_HILFETEXTBAUSTEIN ON HTB_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = KAPBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = KAP_BLS_BLOBNR_PREISINFO" "~"
if joinKuerzel = BLSKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_BLS_BLOBNR_PREISINFO = BLS_BLOBLFDNR" "~"
if joinKuerzel = KGBBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = KGB_BLOBLFDNR" "~"
if joinKuerzel = BLSKGB
searchResult := searchResult "LEFT JOIN PRODUKTION.KGB_KATZU_PPSKAPA ON KGB_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = KSLBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = KSL_MITARB_BLOBLFDNR" "~"
if joinKuerzel = BLSKSL
searchResult := searchResult "LEFT JOIN BUERO.KSL_KONTAKTSYNCEINST ON KSL_MITARB_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = PGLBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = PGL_GLOBFILTERBLOBLFDNR" "~"
if joinKuerzel = BLSPGL
searchResult := searchResult "LEFT JOIN HRS.PGL_PERKZGLIEDERUNG ON PGL_GLOBFILTERBLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = PRSBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = PRS_CHECKLISTE_BLOBLFDNR" "~"
if joinKuerzel = BLSPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_CHECKLISTE_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = VOBBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = VOB_RTFTEXT_BLOBLFDNR" "~"
if joinKuerzel = BLSVOB
searchResult := searchResult "LEFT JOIN PRODUKTION.VOB_VORGANGSBENACHR ON VOB_RTFTEXT_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = TERBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = TER_BLOBLFDNR" "~"
if joinKuerzel = BLSTER
searchResult := searchResult "LEFT JOIN BUERO.TER_TERMIN ON TER_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = OUFBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = OUF_BILD_LT" "~"
if joinKuerzel = BLSOUF
searchResult := searchResult "LEFT JOIN BUERO.OUF_ONLINEUMFRAGE ON OUF_BILD_LT = BLS_BLOBLFDNR" "~"
if joinKuerzel = FIRBLS
searchResult := searchResult "LEFT JOIN BLS_BLOB_STAMM.BLS_BLOB_STAMM ON BLS_BLOBLFDNR = FIR_BRIEFFLG_BLOBLFDNR" "~"
if joinKuerzel = BLSFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_BRIEFFLG_BLOBLFDNR = BLS_BLOBLFDNR" "~"
if joinKuerzel = BEMBMA
searchResult := searchResult "LEFT JOIN BMD.BMA_BMDMAILACCOUNT ON BMA_ACCOUNTLFDNR = BEM_ACCOUNTLFDNR" "~"
if joinKuerzel = BMABEM
searchResult := searchResult "LEFT JOIN BMD.BEM_BMDEMAIL ON BEM_ACCOUNTLFDNR = BMA_ACCOUNTLFDNR" "~"
if joinKuerzel = ATLBMG
searchResult := searchResult "LEFT JOIN BMD.BMG_BMDGRUPPE ON BMG_BMDGRUPPENID = ATL_BMDGRUPPENID" "~"
if joinKuerzel = BMGATL
searchResult := searchResult "LEFT JOIN BMD.ATL_ATTRLOCK ON ATL_BMDGRUPPENID = BMG_BMDGRUPPENID" "~"
if joinKuerzel = BEXBMO
searchResult := searchResult "LEFT JOIN FIBU.BMO_BUDGETMODELL ON BMO_FIRMENNR = BEX_FIRMENNR AND BMO_FIBUNR = BEX_FIBUNR AND BMO_BUDGETMODELLNR = BEX_BUDGETMODELLNR" "~"
if joinKuerzel = BMOBEX
searchResult := searchResult "LEFT JOIN FIBU.BEX_BUDGETEXT ON BEX_FIRMENNR = BMO_FIRMENNR AND BEX_FIBUNR = BMO_FIBUNR AND BEX_BUDGETMODELLNR = BMO_BUDGETMODELLNR" "~"
if joinKuerzel = FZEBMO
searchResult := searchResult "LEFT JOIN FIBU.BMO_BUDGETMODELL ON BMO_FIRMENNR = FZE_FIRMENNR AND BMO_FIBUNR = FZE_FIBUNR AND BMO_BUDGETMODELLNR = FZE_BUDGETMODELLNR" "~"
if joinKuerzel = BMOFZE
searchResult := searchResult "LEFT JOIN FIBU.FZE_ZAHLSCHLUESSELEINGABE ON FZE_FIRMENNR = BMO_FIRMENNR AND FZE_FIBUNR = BMO_FIBUNR AND FZE_BUDGETMODELLNR = BMO_BUDGETMODELLNR" "~"
if joinKuerzel = INSBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = INS_BMDUSERID" "~"
if joinKuerzel = BMUINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = LENBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = LEN_BMDUSERID" "~"
if joinKuerzel = BMULEN
searchResult := searchResult "LEFT JOIN BUERO.LEN_LOHNEINSTELLUNGEN ON LEN_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = NOGBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = NOG_BMDUSERID" "~"
if joinKuerzel = BMUNOG
searchResult := searchResult "LEFT JOIN BUERO.NOG_NOTIZGRUPPE ON NOG_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = PIMBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = PIM_IMPORTUSERID" "~"
if joinKuerzel = BMUPIM
searchResult := searchResult "LEFT JOIN BUERO.PIM_PERSONIMPORT ON PIM_IMPORTUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = PPABMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = PPA_BMDUSERID" "~"
if joinKuerzel = BMUPPA
searchResult := searchResult "LEFT JOIN PRODUKTION.PPA_PSPPARAM ON PPA_BMDUSERID = BMU_BMDUSERID" "~"
if joinKuerzel = PVABMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = PVA_ERLEDIGT_VON" "~"
if joinKuerzel = BMUPVA
searchResult := searchResult "LEFT JOIN BUERO.PVA_PERSONVORERFASSAKTION ON PVA_ERLEDIGT_VON = BMU_BMDUSERID" "~"
if joinKuerzel = WAKBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = WAK_STORNOUSER" "~"
if joinKuerzel = BMUWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_STORNOUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEPBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = WEP_PREISSP_USERID" "~"
if joinKuerzel = BMUWEP
searchResult := searchResult "LEFT JOIN WWS.WEP_WWSERKONTROLLEPOS ON WEP_PREISSP_USERID = BMU_BMDUSERID" "~"
if joinKuerzel = WRKBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = WRK_VERSPEICHERUNGSUSER" "~"
if joinKuerzel = BMUWRK
searchResult := searchResult "LEFT JOIN WWS.WRK_WWSANKAUFSRNGKOPF ON WRK_VERSPEICHERUNGSUSER = BMU_BMDUSERID" "~"
if joinKuerzel = XSPBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = XSP_EINGEREICHT_VON" "~"
if joinKuerzel = BMUXSP
searchResult := searchResult "LEFT JOIN BUERO.XSP_XML_STEUERERKL_PAKET ON XSP_EINGEREICHT_VON = BMU_BMDUSERID" "~"
if joinKuerzel = PLCBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = PLC_MARK_USER" "~"
if joinKuerzel = BMUPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_MARK_USER = BMU_BMDUSERID" "~"
if joinKuerzel = FWFBMU
searchResult := searchResult "LEFT JOIN BMU_BMDUSER.BMU_BMDUSER ON BMU_BMDUSERID = FWF_SPERREBISPERIODE_USER" "~"
if joinKuerzel = BMUFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_SPERREBISPERIODE_USER = BMU_BMDUSERID" "~"
if joinKuerzel = BONBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_BONKOPFID = BON_REFERENZ_BONKOPFID" "~"
if joinKuerzel = BONBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_REFERENZ_BONKOPFID = BON_BONKOPFID" "~"
if joinKuerzel = BOPBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_BONKOPFID = BOP_BONKOPFID" "~"
if joinKuerzel = BONBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_BONKOPFID = BON_BONKOPFID" "~"
if joinKuerzel = BOABOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_BONPOSID = BOA_BONPOSID" "~"
if joinKuerzel = BOPBOA
searchResult := searchResult "LEFT JOIN WWS.BOA_BONUSAUSSCHLUSS ON BOA_BONPOSID = BOP_BONPOSID" "~"
if joinKuerzel = BPDBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_FIRMENNR = BPD_RA_FIRMENNR AND BPD_ERFASSUNGSKZ = BPD_RA_ERFASSUNGSKZ AND BPD_BESTPRODERFASSUNGSNR = BPD_RA_BESTPRODERFASSUNGSNR AND BPD_BESTPOSLFDNR = BPD_RA_BESTPOSLFDNR AND BPD_ERFASSPOSDETAILLFDNR = BPD_RA_ERFASSPOSDETAILLFDNR" "~"
if joinKuerzel = BPDBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_RA_FIRMENNR = BPD_FIRMENNR AND BPD_RA_ERFASSUNGSKZ = BPD_ERFASSUNGSKZ AND BPD_RA_BESTPRODERFASSUNGSNR = BPD_BESTPRODERFASSUNGSNR AND BPD_RA_BESTPOSLFDNR = BPD_BESTPOSLFDNR AND BPD_RA_ERFASSPOSDETAILLFDNR = BPD_ERFASSPOSDETAILLFDNR" "~"
if joinKuerzel = BVOBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_BESTPOSDETAILID = BVO_BESTPOSDETAILID" "~"
if joinKuerzel = BPDBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_BESTPOSDETAILID = BPD_BESTPOSDETAILID" "~"
if joinKuerzel = DISBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_FIRMENNR = DIS_FIRMENNR AND BPD_ERFASSUNGSKZ = DIS_BPD_ERFASSUNGSKZ AND BPD_BESTPRODERFASSUNGSNR = DIS_BPD_BESTPRODERFASSNR AND BPD_BESTPOSLFDNR = DIS_BPD_BESTPOSLFDNR AND BPD_ERFASSPOSDETAILLFDNR = DIS_BPD_ERFASSPOSDETLFDNR" "~"
if joinKuerzel = BPDDIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_FIRMENNR = BPD_FIRMENNR AND DIS_BPD_ERFASSUNGSKZ = BPD_ERFASSUNGSKZ AND DIS_BPD_BESTPRODERFASSNR = BPD_BESTPRODERFASSUNGSNR AND DIS_BPD_BESTPOSLFDNR = BPD_BESTPOSLFDNR AND DIS_BPD_ERFASSPOSDETLFDNR = BPD_ERFASSPOSDETAILLFDNR" "~"
if joinKuerzel = BPDBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_FIRMENNR = BPD_FIRMENNR AND BPP_ERFASSUNGSKZ = BPD_ERFASSUNGSKZ AND BPP_BESTPRODERFASSUNGSNR = BPD_BESTPRODERFASSUNGSNR AND BPP_BESTPOSLFDNR = BPD_BESTPOSLFDNR" "~"
if joinKuerzel = BPPBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_FIRMENNR = BPP_FIRMENNR AND BPD_ERFASSUNGSKZ = BPP_ERFASSUNGSKZ AND BPD_BESTPRODERFASSUNGSNR = BPP_BESTPRODERFASSUNGSNR AND BPD_BESTPOSLFDNR = BPP_BESTPOSLFDNR" "~"
if joinKuerzel = BPPBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_FIRMENNR = BPP_FIRMENNR AND BPP_ERFASSUNGSKZ = BPP_ERFASSUNGSKZ AND BPP_BESTPRODERFASSUNGSNR = BPP_BESTPRODERFASSUNGSNR AND BPP_BESTPOSLFDNR = BPP_HAUPT_BESTPOSLFDNR" "~"
if joinKuerzel = BPPBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_FIRMENNR = BPP_FIRMENNR AND BPP_ERFASSUNGSKZ = BPP_ERFASSUNGSKZ AND BPP_BESTPRODERFASSUNGSNR = BPP_BESTPRODERFASSUNGSNR AND BPP_HAUPT_BESTPOSLFDNR = BPP_BESTPOSLFDNR" "~"
if joinKuerzel = AWSBRA
searchResult := searchResult "LEFT JOIN BRA_BRANCHE.BRA_BRANCHE ON BRA_LANDNR = AWS_BRA_LANDNR AND BRA_BRANCHENNR = AWS_BRA_BRANCHENNR" "~"
if joinKuerzel = BRAAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_BRA_LANDNR = BRA_LANDNR AND AWS_BRA_BRANCHENNR = BRA_BRANCHENNR" "~"
if joinKuerzel = MITBRG
searchResult := searchResult "LEFT JOIN BUERO.BRG_BERUFSGRUPPEN ON BRG_FIRMENNR = MIT_BRG_FIRMENNR AND BRG_BRGGRUPPENNR = MIT_BRGGRUPPENNR" "~"
if joinKuerzel = BRGMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_BRG_FIRMENNR = BRG_FIRMENNR AND MIT_BRGGRUPPENNR = BRG_BRGGRUPPENNR" "~"
if joinKuerzel = PTPBRG
searchResult := searchResult "LEFT JOIN BRG_BERUFSGRUPPEN.BRG_BERUFSGRUPPEN ON BRG_FIRMENNR = PTP_BRG_FIRMENNR AND BRG_BRGGRUPPENNR = PTP_BRG_BRGGRUPPENNR" "~"
if joinKuerzel = BRGPTP
searchResult := searchResult "LEFT JOIN WWS.PTP_PREISTABPOS ON PTP_BRG_FIRMENNR = BRG_FIRMENNR AND PTP_BRG_BRGGRUPPENNR = BRG_BRGGRUPPENNR" "~"
if joinKuerzel = WFNBRM
searchResult := searchResult "LEFT JOIN BUERO.BRM_BUEROMAKROS ON BRM_BUEROMAKROID = WFN_MITARB_BUEROMAKROID" "~"
if joinKuerzel = BRMWFN
searchResult := searchResult "LEFT JOIN BUERO.WFN_WORKFLOWACTION ON WFN_MITARB_BUEROMAKROID = BRM_BUEROMAKROID" "~"
if joinKuerzel = BGPBGS
searchResult := searchResult "LEFT JOIN HRS.BGS_BEITRAGSGRUPPE_SEQ ON BGS_BEITRAGSGRUPPEID = BGP_LEHRLING_PVAV_BGPID" "~"
if joinKuerzel = BGSBGP
searchResult := searchResult "LEFT JOIN HRS.BGP_BEITRAGSGRUPPE ON BGP_LEHRLING_PVAV_BGPID = BGS_BEITRAGSGRUPPEID" "~"
if joinKuerzel = BTZBTM
searchResult := searchResult "LEFT JOIN BUERO.BTM_BTERMINAL ON BTM_FIRMENNR = BTZ_BTM_FIRMENNR AND BTM_TERMINALNR = BTZ_BTM_TERMINALNR" "~"
if joinKuerzel = BTMBTZ
searchResult := searchResult "LEFT JOIN BUERO.BTZ_BTERMINALZUORD ON BTZ_BTM_FIRMENNR = BTM_FIRMENNR AND BTZ_BTM_TERMINALNR = BTM_TERMINALNR" "~"
if joinKuerzel = BIPBUA
searchResult := searchResult "LEFT JOIN FIBU.BUA_BUCHUNGSART ON BUA_FIRMENNR = BIP_FIRMENNR AND BUA_FIBUNR = BIP_FIBUNR AND BUA_BUART = BIP_BUART" "~"
if joinKuerzel = BUABIP
searchResult := searchResult "LEFT JOIN FIBU.BIP_BUDGETINVESTPLAN ON BIP_FIRMENNR = BUA_FIRMENNR AND BIP_FIBUNR = BUA_FIBUNR AND BIP_BUART = BUA_BUART" "~"
if joinKuerzel = BTPBUA
searchResult := searchResult "LEFT JOIN FIBU.BUA_BUCHUNGSART ON BUA_FIRMENNR = BTP_FIRMENNR AND BUA_FIBUNR = BTP_FIBUNR AND BUA_BUART = BTP_BUART" "~"
if joinKuerzel = BUABTP
searchResult := searchResult "LEFT JOIN FIBU.BTP_BUDGETTILGUNGSPLAN ON BTP_FIRMENNR = BUA_FIRMENNR AND BTP_FIBUNR = BUA_FIBUNR AND BTP_BUART = BUA_BUART" "~"
if joinKuerzel = BUABUA
searchResult := searchResult "LEFT JOIN FIBU.BUA_BUCHUNGSART ON BUA_FIRMENNR = BUA_FIRMENNR AND BUA_FIBUNR = BUA_FIBUNR AND BUA_BUART = BUA_STORNO_BUART" "~"
if joinKuerzel = BUABUA
searchResult := searchResult "LEFT JOIN FIBU.BUA_BUCHUNGSART ON BUA_FIRMENNR = BUA_FIRMENNR AND BUA_FIBUNR = BUA_FIBUNR AND BUA_STORNO_BUART = BUA_BUART" "~"
if joinKuerzel = FZPBUA
searchResult := searchResult "LEFT JOIN FIBU.BUA_BUCHUNGSART ON BUA_FIRMENNR = FZP_FIRMENNR AND BUA_FIBUNR = FZP_FIBUNR AND BUA_BUART = FZP_BUART" "~"
if joinKuerzel = BUAFZP
searchResult := searchResult "LEFT JOIN FIBU.FZP_FINANZPLAN ON FZP_FIRMENNR = BUA_FIRMENNR AND FZP_FIBUNR = BUA_FIBUNR AND FZP_BUART = BUA_BUART" "~"
if joinKuerzel = KDPBUA
searchResult := searchResult "LEFT JOIN BUA_BUCHUNGSART.BUA_BUCHUNGSART ON BUA_FIRMENNR = KDP_FIRMENNR AND BUA_FIBUNR = KDP_FIBUNR AND BUA_BUART = KDP_BUART" "~"
if joinKuerzel = BUAKDP
searchResult := searchResult "LEFT JOIN KORE.KDP_PLANWERTE ON KDP_FIRMENNR = BUA_FIRMENNR AND KDP_FIBUNR = BUA_FIBUNR AND KDP_BUART = BUA_BUART" "~"
if joinKuerzel = KEKBUA
searchResult := searchResult "LEFT JOIN BUA_BUCHUNGSART.BUA_BUCHUNGSART ON BUA_FIRMENNR = KEK_FWK_FIRMENNR AND BUA_FIBUNR = KEK_KOPF_KONTO_FIBUNR AND BUA_BUART = KEK_KOPF_BUART" "~"
if joinKuerzel = BUAKEK
searchResult := searchResult "LEFT JOIN KORE.KEK_ERFASSKOPF ON KEK_FWK_FIRMENNR = BUA_FIRMENNR AND KEK_KOPF_KONTO_FIBUNR = BUA_FIBUNR AND KEK_KOPF_BUART = BUA_BUART" "~"
if joinKuerzel = PWXBUA
searchResult := searchResult "LEFT JOIN BUA_BUCHUNGSART.BUA_BUCHUNGSART ON BUA_FIRMENNR = PWX_FIRMENNR AND BUA_FIBUNR = PWX_FIBUNR AND BUA_BUART = PWX_BUART" "~"
if joinKuerzel = BUAPWX
searchResult := searchResult "LEFT JOIN KORE.PWX_PLANWERTEEXT ON PWX_FIRMENNR = BUA_FIRMENNR AND PWX_FIBUNR = BUA_FIBUNR AND PWX_BUART = BUA_BUART" "~"
if joinKuerzel = BASBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_BUCHUNGID = BAS_BUCHUNGID AND BUC_FIRMENNR = BAS_FIRMENNR AND BUC_FIBUNR = BAS_FIBUNR" "~"
if joinKuerzel = BUCBAS
searchResult := searchResult "LEFT JOIN FIBU.BAS_BUCAUFTEILUNGSSATZ ON BAS_BUCHUNGID = BUC_BUCHUNGID AND BAS_FIRMENNR = BUC_FIRMENNR AND BAS_FIBUNR = BUC_FIBUNR" "~"
if joinKuerzel = FKEBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_BUCHUNGID = FKE_BUCHUNGID AND BUC_FIRMENNR = FKE_FIRMENNR AND BUC_FIBUNR = FKE_FIBUNR" "~"
if joinKuerzel = BUCFKE
searchResult := searchResult "LEFT JOIN FIBU.FKE_FIBUKOREERFASSUNG ON FKE_BUCHUNGID = BUC_BUCHUNGID AND FKE_FIRMENNR = BUC_FIRMENNR AND FKE_FIBUNR = BUC_FIBUNR" "~"
if joinKuerzel = ZVRBUC
searchResult := searchResult "LEFT JOIN BUC_BUCHUNG.BUC_BUCHUNG ON BUC_BUCHUNGID = ZVR_BID_BUCHUNGID3 AND BUC_FIRMENNR = ZVR_FWF_FIRMENNR2 AND BUC_FIBUNR = ZVR_BUC_FIBUNR" "~"
if joinKuerzel = BUCZVR
searchResult := searchResult "LEFT JOIN ZX_GELOESCHT.ZVR_RECHNVORSCHLAEGE ON ZVR_BID_BUCHUNGID3 = BUC_BUCHUNGID AND ZVR_FWF_FIRMENNR2 = BUC_FIRMENNR AND ZVR_BUC_FIBUNR = BUC_FIBUNR" "~"
if joinKuerzel = BPDBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_BESTELLVORSCHLAGID = BPD_BESTELLVORSCHLAGID" "~"
if joinKuerzel = BVOBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_BESTELLVORSCHLAGID = BVO_BESTELLVORSCHLAGID" "~"
if joinKuerzel = BWFBWV
searchResult := searchResult "LEFT JOIN BUERO.BWV_BEWERTUNGVERSION ON BWV_FIRMENNR = BWF_BWV_FIRMENNR AND BWV_BWVERSIONNR = BWF_BWVERSIONNR" "~"
if joinKuerzel = BWVBWF
searchResult := searchResult "LEFT JOIN BUERO.BWF_BEWERTUNGFORMEL ON BWF_BWV_FIRMENNR = BWV_FIRMENNR AND BWF_BWVERSIONNR = BWV_BWVERSIONNR" "~"
if joinKuerzel = ABDBZS
searchResult := searchResult "LEFT JOIN HRS.BZS_BNZEILE_SEQ ON BZS_BNZEILEID = ABD_BNZEILEID" "~"
if joinKuerzel = BZSABD
searchResult := searchResult "LEFT JOIN HRS.ABD_ABRECHNUNGSDETAIL ON ABD_BNZEILEID = BZS_BNZEILEID" "~"
if joinKuerzel = CABCAW
searchResult := searchResult "LEFT JOIN BUERO.CAW_CRMABWESENHEIT ON CAW_CAWID = CAB_CAWID" "~"
if joinKuerzel = CAWCAB
searchResult := searchResult "LEFT JOIN BUERO.CAB_CRMABWBERECHTZU ON CAB_CAWID = CAW_CAWID" "~"
if joinKuerzel = CCCCCE
searchResult := searchResult "LEFT JOIN BIL.CCE_CBCENTITY ON CCE_CCEID = CCC_CCEID" "~"
if joinKuerzel = CCECCC
searchResult := searchResult "LEFT JOIN BIL.CCC_CBCCONSTENTITY ON CCC_CCEID = CCE_CCEID" "~"
if joinKuerzel = ACSCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_CHARGEIDNR = ACS_CHG_ID" "~"
if joinKuerzel = CHGACS
searchResult := searchResult "LEFT JOIN WWS.ACS_TEST ON ACS_CHG_ID = CHG_CHARGEIDNR" "~"
if joinKuerzel = AVLCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_CHARGEIDNR = AVL_CHG_CHARGEIDNR" "~"
if joinKuerzel = CHGAVL
searchResult := searchResult "LEFT JOIN WWS.AVL_AUFTRAGSVORERFLIEFDAT ON AVL_CHG_CHARGEIDNR = CHG_CHARGEIDNR" "~"
if joinKuerzel = DISCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_CHARGEIDNR = DIS_CHARGEIDNR" "~"
if joinKuerzel = CHGDIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_CHARGEIDNR = CHG_CHARGEIDNR" "~"
if joinKuerzel = EPSCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_CHARGEIDNR = EPS_CHARGEIDNR" "~"
if joinKuerzel = CHGEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_CHARGEIDNR = CHG_CHARGEIDNR" "~"
if joinKuerzel = ASRCWF
searchResult := searchResult "LEFT JOIN BUERO.CWF_CRMWORKFLOW ON CWF_CWFID = ASR_CWF_CWFID" "~"
if joinKuerzel = CWFASR
searchResult := searchResult "LEFT JOIN BUERO.ASR_AUSSCHREIBUNG ON ASR_CWF_CWFID = CWF_CWFID" "~"
if joinKuerzel = KATCWF
searchResult := searchResult "LEFT JOIN BUERO.CWF_CRMWORKFLOW ON CWF_CWFID = KAT_CWFID" "~"
if joinKuerzel = CWFKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_CWFID = CWF_CWFID" "~"
if joinKuerzel = SBSCWF
searchResult := searchResult "LEFT JOIN BUERO.CWF_CRMWORKFLOW ON CWF_CWFID = SBS_CWF_CWFID" "~"
if joinKuerzel = CWFSBS
searchResult := searchResult "LEFT JOIN BUERO.SBS_STELLENBESCHREIBUNG ON SBS_CWF_CWFID = CWF_CWFID" "~"
if joinKuerzel = VASCWF
searchResult := searchResult "LEFT JOIN BUERO.CWF_CRMWORKFLOW ON CWF_CWFID = VAS_CWF_CWFID" "~"
if joinKuerzel = CWFVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_CWF_CWFID = CWF_CWFID" "~"
if joinKuerzel = SBGDBP
searchResult := searchResult "LEFT JOIN DBP_DBERECHTPAKET.DBP_DBERECHTPAKET ON DBP_BERECHTPAKETNR = SBG_BERECHTPAKETNR" "~"
if joinKuerzel = DBPSBG
searchResult := searchResult "LEFT JOIN BMD.SBG_STDBRIEFGRUPPE ON SBG_BERECHTPAKETNR = DBP_BERECHTPAKETNR" "~"
if joinKuerzel = BGZDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = BGZ_DBS_DATENBESTANDNR" "~"
if joinKuerzel = DBSBGZ
searchResult := searchResult "LEFT JOIN HRS.BGZ_BNZEILE ON BGZ_DBS_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = DBLDBS
searchResult := searchResult "LEFT JOIN BUERO.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = DBL_DBS_DATENBESTANDNR" "~"
if joinKuerzel = DBSDBL
searchResult := searchResult "LEFT JOIN BUERO.DBL_DBSLOHNDATEI ON DBL_DBS_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = DDSDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = DDS_DBSNR" "~"
if joinKuerzel = DBSDDS
searchResult := searchResult "LEFT JOIN HRS.DDS_DBSDIVSTAMM ON DDS_DBSNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = EFZDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = EFZ_DBSNR" "~"
if joinKuerzel = DBSEFZ
searchResult := searchResult "LEFT JOIN HRS.EFZ_ENTGELTFORTZAHLUNG ON EFZ_DBSNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = EKODBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = EKO_DATENBESTANDNR" "~"
if joinKuerzel = DBSEKO
searchResult := searchResult "LEFT JOIN HRS.EKO_ELDAKONTROLLE ON EKO_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = FAKDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = FAK_DATENBESTANDNR" "~"
if joinKuerzel = DBSFAK
searchResult := searchResult "LEFT JOIN HRS.FAK_FAKTORTABELLE ON FAK_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = LAWDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = LAW_DBSNR" "~"
if joinKuerzel = DBSLAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_DBSNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = MBMDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = MBM_DATENBESTANDNR" "~"
if joinKuerzel = DBSMBM
searchResult := searchResult "LEFT JOIN HRS.MBM_MBGM ON MBM_DATENBESTANDNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = TGRDBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = TGR_DBS_DATENBESTAND" "~"
if joinKuerzel = DBSTGR
searchResult := searchResult "LEFT JOIN HRS.TGR_TARIFGRUPPE ON TGR_DBS_DATENBESTAND = DBS_DATENBESTANDNR" "~"
if joinKuerzel = VZADBS
searchResult := searchResult "LEFT JOIN DBS_DATENBESTAND.DBS_DATENBESTAND ON DBS_DATENBESTANDNR = VZA_DBSNR" "~"
if joinKuerzel = DBSVZA
searchResult := searchResult "LEFT JOIN HRS.VZA_VORZEITART ON VZA_DBSNR = DBS_DATENBESTANDNR" "~"
if joinKuerzel = ALIDBT
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DBT_DBMDTABFELDBESCH ON DBT_BMDCONSTNAME = ALI_BMDCONSTNAME" "~"
if joinKuerzel = DBTALI
searchResult := searchResult "LEFT JOIN BMDDEFAULT.ALI_ATTRGRUPPELIZENZZU ON ALI_BMDCONSTNAME = DBT_BMDCONSTNAME" "~"
if joinKuerzel = BTEDBT
searchResult := searchResult "LEFT JOIN DBT_DBMDTABFELDBESCH.DBT_DBMDTABFELDBESCH ON DBT_TABELLENNAME = BTE_TABELLENNAME AND DBT_FELDNAME = BTE_FELDNAME" "~"
if joinKuerzel = DBTBTE
searchResult := searchResult "LEFT JOIN BMDENTW.BTE_BMDTABFELDBESCHEXT ON BTE_TABELLENNAME = DBT_TABELLENNAME AND BTE_FELDNAME = DBT_FELDNAME" "~"
if joinKuerzel = DNVDBT
searchResult := searchResult "LEFT JOIN DBT_DBMDTABFELDBESCH.DBT_DBMDTABFELDBESCH ON DBT_TABELLENNAME = DNV_TABELLENNAME AND DBT_FELDNAME = DNV_FELDNAME" "~"
if joinKuerzel = DBTDNV
searchResult := searchResult "LEFT JOIN HRS.DNV_DNVORSCHLAG ON DNV_TABELLENNAME = DBT_TABELLENNAME AND DNV_FELDNAME = DBT_FELDNAME" "~"
if joinKuerzel = UVFDBT
searchResult := searchResult "LEFT JOIN DBT_DBMDTABFELDBESCH.DBT_DBMDTABFELDBESCH ON DBT_BMDCONSTNAME = UVF_BMDCONSTNAME" "~"
if joinKuerzel = DBTUVF
searchResult := searchResult "LEFT JOIN FIBU.UVF_UVAFELDZU ON UVF_BMDCONSTNAME = DBT_BMDCONSTNAME" "~"
if joinKuerzel = FFODFU
searchResult := searchResult "LEFT JOIN DFU_DFUNKTION.DFU_DFUNKTION ON DFU_FUNKTIONSNR = FFO_FUNKTIONSNR" "~"
if joinKuerzel = DFUFFO
searchResult := searchResult "LEFT JOIN BMD.FFO_FUNKFIRMENRECHTE_OPT ON FFO_FUNKTIONSNR = DFU_FUNKTIONSNR" "~"
if joinKuerzel = PARDFU
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFU_DFUNKTION ON DFU_FUNKTIONSNR = PAR_SUCHFUNKTIONSNR" "~"
if joinKuerzel = DFUPAR
searchResult := searchResult "LEFT JOIN BMDDEFAULT.PAR_PARAM ON PAR_SUCHFUNKTIONSNR = DFU_FUNKTIONSNR" "~"
if joinKuerzel = UPKDFU
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFU_DFUNKTION ON DFU_FUNKTIONSNR = UPK_FUNKTIONSNR" "~"
if joinKuerzel = DFUUPK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.UPK_UPH_KUNDE ON UPK_FUNKTIONSNR = DFU_FUNKTIONSNR" "~"
if joinKuerzel = WDADIA
searchResult := searchResult "LEFT JOIN DIA_DIAGRAMM.DIA_DIAGRAMM ON DIA_DIAGRAMMNR = WDA_DIA_DIAGRAMMNR_SUB" "~"
if joinKuerzel = DIAWDA
searchResult := searchResult "LEFT JOIN BUERO.WDA_WFDAGRAMMZU ON WDA_DIA_DIAGRAMMNR_SUB = DIA_DIAGRAMMNR" "~"
if joinKuerzel = WDIDIH
searchResult := searchResult "LEFT JOIN DIH_DIAGRAMMINSTANZKOPF.DIH_DIAGRAMMINSTANZKOPF ON DIH_DIAGRAMMNR = WDI_DIH_DIAGRAMMNR_SUB AND DIH_INSTANZNR = WDI_DIH_INSTANZNR_SUB" "~"
if joinKuerzel = DIHWDI
searchResult := searchResult "LEFT JOIN BUERO.WDI_WFDIAGRAMMINSTANZZU ON WDI_DIH_DIAGRAMMNR_SUB = DIH_DIAGRAMMNR AND WDI_DIH_INSTANZNR_SUB = DIH_INSTANZNR" "~"
if joinKuerzel = TREDIP
searchResult := searchResult "LEFT JOIN DIP_DIVERSE.DIP_DIVERSE ON DIP_FIRMENNR = TRE_FRAECHTER_FIRMENNR AND DIP_EXTPERSID = TRE_FRAECHTER_EXTPERSID" "~"
if joinKuerzel = DIPTRE
searchResult := searchResult "LEFT JOIN WWS.TRE_TRANSPORTEINHEIT ON TRE_FRAECHTER_FIRMENNR = DIP_FIRMENNR AND TRE_FRAECHTER_EXTPERSID = DIP_EXTPERSID" "~"
if joinKuerzel = VWZDIX
searchResult := searchResult "LEFT JOIN DIX_DIAGRAMMAKTION.DIX_DIAGRAMMAKTION ON DIX_DIAGRAMMNR = VWZ_DIAGRAMMNR AND DIX_AKTIONSNR = VWZ_AKTIONSNR" "~"
if joinKuerzel = DIXVWZ
searchResult := searchResult "LEFT JOIN BUERO.VWZ_VORGANGWORKFLOWZU ON VWZ_DIAGRAMMNR = DIX_DIAGRAMMNR AND VWZ_AKTIONSNR = DIX_AKTIONSNR" "~"
if joinKuerzel = YBUDLN
searchResult := searchResult "LEFT JOIN DLN_DIENSTLEISTUNGSNUMMER.DLN_DIENSTLEISTUNGSNUMMER ON DLN_LANDNR = YBU_LAN_LANDNR AND DLN_DLCODE = YBU_ADL_DLCODE" "~"
if joinKuerzel = DLNYBU
searchResult := searchResult "LEFT JOIN FIBU.YBU_AUTOBUCHUNG ON YBU_LAN_LANDNR = DLN_LANDNR AND YBU_ADL_DLCODE = DLN_DLCODE" "~"
if joinKuerzel = DEZDNE
searchResult := searchResult "LEFT JOIN DATANORM.DNE_ERSTELLER ON DNE_ERST_NR = DEZ_ERST_NR" "~"
if joinKuerzel = DNEDEZ
searchResult := searchResult "LEFT JOIN DATANORM.DEZ_DATANORM_ERSTELLERZU ON DEZ_ERST_NR = DNE_ERST_NR" "~"
if joinKuerzel = DNIDNE
searchResult := searchResult "LEFT JOIN DATANORM.DNE_ERSTELLER ON DNE_ERST_NR = DNI_ERST_NR" "~"
if joinKuerzel = DNEDNI
searchResult := searchResult "LEFT JOIN DATANORM.DNI_DATEI_INFO ON DNI_ERST_NR = DNE_ERST_NR" "~"
if joinKuerzel = DOZDOG
searchResult := searchResult "LEFT JOIN BMD.DOG_DATAOWNERGRUPPE ON DOG_DATAOWNERGRUPPENNR = DOZ_DATAOWNERGRUPPENNR" "~"
if joinKuerzel = DOGDOZ
searchResult := searchResult "LEFT JOIN BMD.DOZ_DATAOWNERZU ON DOZ_DATAOWNERGRUPPENNR = DOG_DATAOWNERGRUPPENNR" "~"
if joinKuerzel = DWZDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_ARCHIVNR = DWZ_ARCHIVNR AND DOK_DOKUMENTENNR = DWZ_DOKUMENTENNR" "~"
if joinKuerzel = DOKDWZ
searchResult := searchResult "LEFT JOIN BUERO.DWZ_DOKUMENTWORKFLOWZU ON DWZ_ARCHIVNR = DOK_ARCHIVNR AND DWZ_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = EDZDOK
searchResult := searchResult "LEFT JOIN DOK_DOKUMENT.DOK_DOKUMENT ON DOK_ARCHIVNR = EDZ_DOK_ARCHIVNR AND DOK_DOKUMENTENNR = EDZ_DOK_DOKUMENTENNR" "~"
if joinKuerzel = DOKEDZ
searchResult := searchResult "LEFT JOIN WWS.EDZ_ERFASSDOKUMENTZU ON EDZ_DOK_ARCHIVNR = DOK_ARCHIVNR AND EDZ_DOK_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = KJODOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_ARCHIVNR = KJO_ARCHIVNR AND DOK_DOKUMENTENNR = KJO_DOKUMENTENNR" "~"
if joinKuerzel = DOKKJO
searchResult := searchResult "LEFT JOIN BUERO.KJO_KONTAKTJOURNAL ON KJO_ARCHIVNR = DOK_ARCHIVNR AND KJO_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = LFSDOK
searchResult := searchResult "LEFT JOIN DOK_DOKUMENT.DOK_DOKUMENT ON DOK_ARCHIVNR = LFS_DOK_ARCHIVNR AND DOK_DOKUMENTENNR = LFS_DOK_DOKUMENTENNR" "~"
if joinKuerzel = DOKLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_DOK_ARCHIVNR = DOK_ARCHIVNR AND LFS_DOK_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = PWXDOK
searchResult := searchResult "LEFT JOIN DOK_DOKUMENT.DOK_DOKUMENT ON DOK_ARCHIVNR = PWX_EXCEL_ARCHIVNR AND DOK_DOKUMENTENNR = PWX_EXCEL_DOKUMENTENNR" "~"
if joinKuerzel = DOKPWX
searchResult := searchResult "LEFT JOIN KORE.PWX_PLANWERTEEXT ON PWX_EXCEL_ARCHIVNR = DOK_ARCHIVNR AND PWX_EXCEL_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = AKKDPA
searchResult := searchResult "LEFT JOIN DPA_DPAKET.DPA_DPAKET ON DPA_PAKETNR = AKK_PAKETNR" "~"
if joinKuerzel = DPAAKK
searchResult := searchResult "LEFT JOIN BUERO.AKK_AKTIONSTATUSKOPF ON AKK_PAKETNR = DPA_PAKETNR" "~"
if joinKuerzel = DKPDPO
searchResult := searchResult "LEFT JOIN DPO_DOCUMENTPOOL.DPO_DOCUMENTPOOL ON DPO_DPOID = DKP_DPOID" "~"
if joinKuerzel = DPODKP
searchResult := searchResult "LEFT JOIN FIBU.DKP_DOKUMENTENPOOL ON DKP_DPOID = DPO_DPOID" "~"
if joinKuerzel = JABDRB
searchResult := searchResult "LEFT JOIN DRB.DRB ON FIRMENNR = JAB_FUSS_FIRMENNR AND BSTKZ = JAB_FUSS_BSTKZ AND DR_FIRMENUNTERNR = JAB_FUSS_DR_FIRMENUNTERNR AND BSTNR = JAB_FUSS_BSTNR" "~"
if joinKuerzel = DRBJAB
searchResult := searchResult "LEFT JOIN BIL.JAB_JABINFO ON JAB_FUSS_FIRMENNR = FIRMENNR AND JAB_FUSS_BSTKZ = BSTKZ AND JAB_FUSS_DR_FIRMENUNTERNR = DR_FIRMENUNTERNR AND JAB_FUSS_BSTNR = BSTNR" "~"
if joinKuerzel = DREDRS
searchResult := searchResult "LEFT JOIN DRS_DRUCKEREINSTELLUNG.DRS_DRUCKEREINSTELLUNG ON DRS_DREINSTELLUNGSID = DRE_DREINSTELLUNGSID" "~"
if joinKuerzel = DRSDRE
searchResult := searchResult "LEFT JOIN HRS.DRE_ERWDRUCKEREINST ON DRE_DREINSTELLUNGSID = DRS_DREINSTELLUNGSID" "~"
if joinKuerzel = BAIDRS
searchResult := searchResult "LEFT JOIN DRS_DRUCKEREINSTELLUNG.DRS_DRUCKEREINSTELLUNG ON DRS_DREINSTELLUNGSID = BAI_SA_DREINSTELLUNGSID2" "~"
if joinKuerzel = DRSBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_SA_DREINSTELLUNGSID2 = DRS_DREINSTELLUNGSID" "~"
if joinKuerzel = BAIDRS
searchResult := searchResult "LEFT JOIN DRS_DRUCKEREINSTELLUNG.DRS_DRUCKEREINSTELLUNG ON DRS_DREINSTELLUNGSID = BAI_WL_DREINSTELLUNGSID" "~"
if joinKuerzel = DRSBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_WL_DREINSTELLUNGSID = DRS_DREINSTELLUNGSID" "~"
if joinKuerzel = KASDRS
searchResult := searchResult "LEFT JOIN DRS_DRUCKEREINSTELLUNG.DRS_DRUCKEREINSTELLUNG ON DRS_DREINSTELLUNGSID = KAS_ZWBON_BELEG_DRSID" "~"
if joinKuerzel = DRSKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_ZWBON_BELEG_DRSID = DRS_DREINSTELLUNGSID" "~"
if joinKuerzel = PSMDST
searchResult := searchResult "LEFT JOIN HRS.DST_DIVSTAMM ON DST_DSTID = PSM_DSTID_FIBU AND DST_LOHNJAHR = PSM_PSMJAHR AND DST_FIRMENNR = PSM_DVFIRMENNR" "~"
if joinKuerzel = DSTPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_DSTID_FIBU = DST_DSTID AND PSM_PSMJAHR = DST_LOHNJAHR AND PSM_DVFIRMENNR = DST_FIRMENNR" "~"
if joinKuerzel = QLKDTA
searchResult := searchResult "LEFT JOIN DTA_DBMDTABELLE.DTA_DBMDTABELLE ON DTA_TABELLENNAME = QLK_NACH_TABELLENNAME" "~"
if joinKuerzel = DTAQLK
searchResult := searchResult "LEFT JOIN BMD.QLK_LINK ON QLK_NACH_TABELLENNAME = DTA_TABELLENNAME" "~"
if joinKuerzel = LZUDVH
searchResult := searchResult "LEFT JOIN DVH_DIENSTVERHAELTNIS.DVH_DIENSTVERHAELTNIS ON DVH_MIT_FIRMENNR = LZU_FIRMENNR AND DVH_MITARBEITERID = LZU_MITARBEITERID AND DVH_DIENSTVLFDNR = LZU_DIENSTVLFDNR" "~"
if joinKuerzel = DVHLZU
searchResult := searchResult "LEFT JOIN HRS.LZU_LOHNZUSTAND ON LZU_FIRMENNR = DVH_MIT_FIRMENNR AND LZU_MITARBEITERID = DVH_MITARBEITERID AND LZU_DIENSTVLFDNR = DVH_DIENSTVLFDNR" "~"
if joinKuerzel = SBMDVH
searchResult := searchResult "LEFT JOIN BUERO.DVH_DIENSTVERHAELTNIS ON DVH_MIT_FIRMENNR = SBM_DVH_FIRMENNR AND DVH_MITARBEITERID = SBM_DVH_MITARBEITERID AND DVH_DIENSTVLFDNR = SBM_DIENSTVLFDNR" "~"
if joinKuerzel = DVHSBM
searchResult := searchResult "LEFT JOIN BUERO.SBM_SBSPERSONALMITARBZU ON SBM_DVH_FIRMENNR = DVH_MIT_FIRMENNR AND SBM_DVH_MITARBEITERID = DVH_MITARBEITERID AND SBM_DIENSTVLFDNR = DVH_DIENSTVLFDNR" "~"
if joinKuerzel = WEPEBP
searchResult := searchResult "LEFT JOIN EBP_BESTELLPOSITION.EBP_BESTELLPOSITION ON EBP_FIRMENNR = WEP_EBP_FIRMENNR AND EBP_FIBUNR = WEP_EBP_FIBUNR AND EBP_ERFASSNR = WEP_EBP_ERFASSNR AND EBP_EBPNR = WEP_EBP_EBPNR" "~"
if joinKuerzel = EBPWEP
searchResult := searchResult "LEFT JOIN WWS.WEP_WWSERKONTROLLEPOS ON WEP_EBP_FIRMENNR = EBP_FIRMENNR AND WEP_EBP_FIBUNR = EBP_FIBUNR AND WEP_EBP_ERFASSNR = EBP_ERFASSNR AND WEP_EBP_EBPNR = EBP_EBPNR" "~"
if joinKuerzel = AWSEFK
searchResult := searchResult "LEFT JOIN EFK_ERFASSKOPFWWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = AWS_EFK_FIRMENNR AND EFK_ERFASSUNGSKZ = AWS_EFK_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = AWS_EFK_ERFASSUNGSNR" "~"
if joinKuerzel = EFKAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_EFK_FIRMENNR = EFK_FIRMENNR AND AWS_EFK_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND AWS_EFK_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = BONEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = BON_EFK_FIRMENNR AND EFK_ERFASSUNGSKZ = BON_EFK_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = BON_EFK_ERFASSUNGSNR" "~"
if joinKuerzel = EFKBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_EFK_FIRMENNR = EFK_FIRMENNR AND BON_EFK_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND BON_EFK_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = DISEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = DIS_FIRMENNR AND EFK_ERFASSUNGSKZ = DIS_ERO_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = DIS_ERO_ERFASSUNGSNR" "~"
if joinKuerzel = EFKDIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_FIRMENNR = EFK_FIRMENNR AND DIS_ERO_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND DIS_ERO_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = TBAEFK
searchResult := searchResult "LEFT JOIN EFK_ERFASSKOPFWWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = TBA_EFK_FIRMENNR AND EFK_ERFASSUNGSKZ = TBA_EFK_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = TBA_EFK_ERFASSUNGSNR" "~"
if joinKuerzel = EFKTBA
searchResult := searchResult "LEFT JOIN BUERO.TBA_TAGESMDBUCHARTAUFB ON TBA_EFK_FIRMENNR = EFK_FIRMENNR AND TBA_EFK_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND TBA_EFK_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = ZBUEFK
searchResult := searchResult "LEFT JOIN EFK_ERFASSKOPFWWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = ZBU_EFK_FIRMENNR AND EFK_ERFASSUNGSKZ = ZBU_EFK_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = ZBU_EFK_ERFASSUNGSNR" "~"
if joinKuerzel = EFKZBU
searchResult := searchResult "LEFT JOIN BUERO.ZBU_ZEITBUCHUNG ON ZBU_EFK_FIRMENNR = EFK_FIRMENNR AND ZBU_EFK_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND ZBU_EFK_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = ERPEGS
searchResult := searchResult "LEFT JOIN EGS_ERFASSUNGSGRUND.EGS_ERFASSUNGSGRUND ON EGS_GRUND_ID = ERP_ABLEHNUNGS_GRUND_ID" "~"
if joinKuerzel = EGSERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_ABLEHNUNGS_GRUND_ID = EGS_GRUND_ID" "~"
if joinKuerzel = EHPEHP
searchResult := searchResult "LEFT JOIN WWS.EHP_EINHEITENTABPOS ON EHP_FIRMENNR = EHP_FIRMENNR AND EHP_EINHEITENTABNR = EHP_EINHEITENTABNR AND EHP_EINHEITKBEZ = EHP_UNTEREINHEITKBEZ" "~"
if joinKuerzel = EHPEHP
searchResult := searchResult "LEFT JOIN WWS.EHP_EINHEITENTABPOS ON EHP_FIRMENNR = EHP_FIRMENNR AND EHP_EINHEITENTABNR = EHP_EINHEITENTABNR AND EHP_UNTEREINHEITKBEZ = EHP_EINHEITKBEZ" "~"
if joinKuerzel = WFGEHT
searchResult := searchResult "LEFT JOIN WWS.EHT_EINHEITENTAB ON EHT_FIRMENNR = WFG_EHT_FIRMENNR AND EHT_EINHEITENTABNR = WFG_EHT_EINHEITENTABNR" "~"
if joinKuerzel = EHTWFG
searchResult := searchResult "LEFT JOIN WWS.WFG_FOLGEARTIKELGRUPPE ON WFG_EHT_FIRMENNR = EHT_FIRMENNR AND WFG_EHT_EINHEITENTABNR = EHT_EINHEITENTABNR" "~"
if joinKuerzel = BSDEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = BSD_EINHEITKBEZ" "~"
if joinKuerzel = EINBSD
searchResult := searchResult "LEFT JOIN WWS.BSD_BONUSERMDETAIL ON BSD_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = EHPEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = EHP_EINHEITKBEZ" "~"
if joinKuerzel = EINEHP
searchResult := searchResult "LEFT JOIN WWS.EHP_EINHEITENTABPOS ON EHP_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = KAREIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = KAR_VERPACKUNGSEINHEITKBEZ" "~"
if joinKuerzel = EINKAR
searchResult := searchResult "LEFT JOIN WWS.KAR_KUNDENARTIKEL ON KAR_VERPACKUNGSEINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = KOPEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = KOP_EINHEITKBEZ" "~"
if joinKuerzel = EINKOP
searchResult := searchResult "LEFT JOIN PRODUKTION.KOP_KOMMISSIONIERUNG_POS ON KOP_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PRZEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = PRZ_EINHEIT" "~"
if joinKuerzel = EINPRZ
searchResult := searchResult "LEFT JOIN PRODUKTION.PRZ_PROZESSBAZZU ON PRZ_EINHEIT = EIN_EINHEITKBEZ" "~"
if joinKuerzel = QMKEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = QMK_QTY_EINH" "~"
if joinKuerzel = EINQMK
searchResult := searchResult "LEFT JOIN PRODUKTION.QMK_QM_KOPF ON QMK_QTY_EINH = EIN_EINHEITKBEZ" "~"
if joinKuerzel = TLAEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = TLA_QTY_EINHEIT" "~"
if joinKuerzel = EINTLA
searchResult := searchResult "LEFT JOIN PRODUKTION.TLA_BT_LOGISTIK_ANGEFORDERT ON TLA_QTY_EINHEIT = EIN_EINHEITKBEZ" "~"
if joinKuerzel = TRTEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = TRT_EINHEITKBEZ" "~"
if joinKuerzel = EINTRT
searchResult := searchResult "LEFT JOIN WWS.TRT_TRANSPORTTYP ON TRT_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = WFDEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = WFD_EINHEITKBEZ" "~"
if joinKuerzel = EINWFD
searchResult := searchResult "LEFT JOIN WWS.WFD_FOLGEARTIKELDETAIL ON WFD_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = AVEEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = AVE_VKPR_EINHEITKBEZ" "~"
if joinKuerzel = EINAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_VKPR_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = BVOEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = BVO_EINHEITKBEZ_GE" "~"
if joinKuerzel = EINBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_EINHEITKBEZ_GE = EIN_EINHEITKBEZ" "~"
if joinKuerzel = BOPEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = BOP_VKP_EINHEITKBEZ" "~"
if joinKuerzel = EINBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_VKP_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = EPSEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = EPS_LAENGENEINHEITKBEZ" "~"
if joinKuerzel = EINEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_LAENGENEINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = ERPEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = ERP_EINHEITKBEZ_LBH" "~"
if joinKuerzel = EINERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_EINHEITKBEZ_LBH = EIN_EINHEITKBEZ" "~"
if joinKuerzel = KAPEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = KAP_VKP_EINHEITKBEZ" "~"
if joinKuerzel = EINKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_VKP_EINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = LINEIN
searchResult := searchResult "LEFT JOIN EIN_EINHEIT.EIN_EINHEIT ON EIN_EINHEITKBEZ = LIN_PQTY_M_UNIT" "~"
if joinKuerzel = EINLIN
searchResult := searchResult "LEFT JOIN PRODUKTION.LIN_PROD_LINIE ON LIN_PQTY_M_UNIT = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PIAEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = PIA_EINKAUFSEINHEITKBEZ" "~"
if joinKuerzel = EINPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_EINKAUFSEINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = PIAEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_EINHEITKBEZ = PIA_VERPACKUNGSEINHEITKBEZ" "~"
if joinKuerzel = EINPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_VERPACKUNGSEINHEITKBEZ = EIN_EINHEITKBEZ" "~"
if joinKuerzel = WKPEKZ
searchResult := searchResult "LEFT JOIN WWS.EKZ_ERLOESAUFWANDKZ ON EKZ_FIRMENNR = WKP_FIRMENNR AND EKZ_ERLOESAUFWANDKZ = WKP_ERLOESAUFWANDKZ" "~"
if joinKuerzel = EKZWKP
searchResult := searchResult "LEFT JOIN WWS.WKP_WWSKONTENTABPOS ON WKP_FIRMENNR = EKZ_FIRMENNR AND WKP_ERLOESAUFWANDKZ = EKZ_ERLOESAUFWANDKZ" "~"
if joinKuerzel = ELVELV
searchResult := searchResult "LEFT JOIN WWS.ELV_ERFASSPOSLIEFERVOR ON ELV_FIRMENNR = ELV_FIRMENNR AND ELV_ERFASSUNGSKZ = ELV_ERFASSUNGSKZ AND ELV_ERFASSUNGSNR = ELV_ERFASSUNGSNR AND ELV_ERFASSPOSLFDNR = ELV_H_ERFASSPOSLFDNR AND ELV_ERFASSPOSORIGLFDNR = ELV_H_ERFASSPOSORIGLFDNR AND ELV_ERFASSPOSLVLFDNR = ELV_H_ERFASSPOSLVLFDNR" "~"
if joinKuerzel = ELVELV
searchResult := searchResult "LEFT JOIN WWS.ELV_ERFASSPOSLIEFERVOR ON ELV_FIRMENNR = ELV_FIRMENNR AND ELV_ERFASSUNGSKZ = ELV_ERFASSUNGSKZ AND ELV_ERFASSUNGSNR = ELV_ERFASSUNGSNR AND ELV_H_ERFASSPOSLFDNR = ELV_ERFASSPOSLFDNR AND ELV_H_ERFASSPOSORIGLFDNR = ELV_ERFASSPOSORIGLFDNR AND ELV_H_ERFASSPOSLVLFDNR = ELV_ERFASSPOSLVLFDNR" "~"
if joinKuerzel = EMAEMT
searchResult := searchResult "LEFT JOIN BUERO.EMT_EMAILART ON EMT_EMAILARTNR = EMA_EMAILARTNR" "~"
if joinKuerzel = EMTEMA
searchResult := searchResult "LEFT JOIN BUERO.EMA_EMAIL ON EMA_EMAILARTNR = EMT_EMAILARTNR" "~"
if joinKuerzel = ADDENT
searchResult := searchResult "LEFT JOIN ENT_EDINACHRICHTTYP.ENT_EDINACHRICHTTYP ON ENT_ENTID = ADD_ENTID" "~"
if joinKuerzel = ENTADD
searchResult := searchResult "LEFT JOIN BUERO.ADD_AUSDRUCKMODELLDETAIL ON ADD_ENTID = ENT_ENTID" "~"
if joinKuerzel = BPDEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_EPSTKLISTID = BPD_EPSTKLISTID" "~"
if joinKuerzel = EPSBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_EPSTKLISTID = EPS_EPSTKLISTID" "~"
if joinKuerzel = BPZEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_EPSTKLISTID = BPZ_EPSTKLISTID" "~"
if joinKuerzel = EPSBPZ
searchResult := searchResult "LEFT JOIN WWS.BPZ_BESTELLPOSZUORDNUNG ON BPZ_EPSTKLISTID = EPS_EPSTKLISTID" "~"
if joinKuerzel = KAPEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_EPSTKLISTID = KAP_EPS_EPSTKLISTID" "~"
if joinKuerzel = EPSKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_EPS_EPSTKLISTID = EPS_EPSTKLISTID" "~"
if joinKuerzel = WSZEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_EPSTKLISTID = WSZ_EPSTKLISTID" "~"
if joinKuerzel = EPSWSZ
searchResult := searchResult "LEFT JOIN WWS.WSZ_WESEPS_ZUORDNUNG ON WSZ_EPSTKLISTID = EPS_EPSTKLISTID" "~"
if joinKuerzel = EPSEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_EPSTKLISTID = EPS_RETOUR_URSPRUNGID" "~"
if joinKuerzel = EPSEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_RETOUR_URSPRUNGID = EPS_EPSTKLISTID" "~"
if joinKuerzel = ERMERB
searchResult := searchResult "LEFT JOIN FIBU.ERB_BUCHUNG ON ERB_FIRMENNR = ERM_ERB_FIRMENNR AND ERB_FIBUNR = ERM_ERB_FIBUNR AND ERB_ERFASSNR = ERM_ERB_ERFASSNR" "~"
if joinKuerzel = ERBERM
searchResult := searchResult "LEFT JOIN FIBU.ERM_ERMITARBEITERZU ON ERM_ERB_FIRMENNR = ERB_FIRMENNR AND ERM_ERB_FIBUNR = ERB_FIBUNR AND ERM_ERB_ERFASSNR = ERB_ERFASSNR" "~"
if joinKuerzel = EQIERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_FIRMENNR = EQI_FIRMENNR AND ERO_ERFASSUNGSKZ = EQI_ERFASSUNGSKZ AND ERO_ERFASSUNGSNR = EQI_ERFASSUNGSNR AND ERO_ERFASSPOSLFDNR = EQI_ERFASSPOSLFDNR AND ERO_ERFASSPOSORIGLFDNR = EQI_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = EROEQI
searchResult := searchResult "LEFT JOIN WWS.EQI_ERFASSPOSQUELLINFO ON EQI_FIRMENNR = ERO_FIRMENNR AND EQI_ERFASSUNGSKZ = ERO_ERFASSUNGSKZ AND EQI_ERFASSUNGSNR = ERO_ERFASSUNGSNR AND EQI_ERFASSPOSLFDNR = ERO_ERFASSPOSLFDNR AND EQI_ERFASSPOSORIGLFDNR = ERO_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = RNRERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_FIRMENNR = RNR_FIRMENNR AND ERO_ERFASSUNGSKZ = RNR_ERFASSUNGSKZ AND ERO_ERFASSUNGSNR = RNR_ERFASSUNGSNR AND ERO_ERFASSPOSLFDNR = RNR_ERFASSPOSLFDNR AND ERO_ERFASSPOSORIGLFDNR = RNR_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = ERORNR
searchResult := searchResult "LEFT JOIN WWS.RNR_RECHNUNGSRABATT ON RNR_FIRMENNR = ERO_FIRMENNR AND RNR_ERFASSUNGSKZ = ERO_ERFASSUNGSKZ AND RNR_ERFASSUNGSNR = ERO_ERFASSUNGSNR AND RNR_ERFASSPOSLFDNR = ERO_ERFASSPOSLFDNR AND RNR_ERFASSPOSORIGLFDNR = ERO_ERFASSPOSORIGLFDNR" "~"
if joinKuerzel = WAPERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = WAP_ERT_FIRMENNR AND ERT_ERFASSTEXTNR = WAP_KURZBESCH_ERFASSTEXTNR" "~"
if joinKuerzel = ERTWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_ERT_FIRMENNR = ERT_FIRMENNR AND WAP_KURZBESCH_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = AVEERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = AVE_ERFGRP_ET_FIRMENNR AND ERT_ERFASSTEXTNR = AVE_ERFGRP_ERFASSTEXTNR4" "~"
if joinKuerzel = ERTAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ERFGRP_ET_FIRMENNR = ERT_FIRMENNR AND AVE_ERFGRP_ERFASSTEXTNR4 = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = BPKERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = BPK_FIRMENNR AND ERT_ERFASSTEXTNR = BPK_BESTELL_ERFASSTEXTNR" "~"
if joinKuerzel = ERTBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_FIRMENNR = ERT_FIRMENNR AND BPK_BESTELL_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = BPPERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = BPP_FIRMENNR AND ERT_ERFASSTEXTNR = BPP_BESCH_ERFASSTEXTNR" "~"
if joinKuerzel = ERTBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_FIRMENNR = ERT_FIRMENNR AND BPP_BESCH_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = BPKERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = BPK_FIRMENNR AND ERT_ERFASSTEXTNR = BPK_EMAIL_ERFASSTEXTNR" "~"
if joinKuerzel = ERTBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_FIRMENNR = ERT_FIRMENNR AND BPK_EMAIL_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = EFKERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = EFK_FIRMENNR AND ERT_ERFASSTEXTNR = EFK_BESTELL_ERFASSTEXTNR" "~"
if joinKuerzel = ERTEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = ERT_FIRMENNR AND EFK_BESTELL_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = EFKERT
searchResult := searchResult "LEFT JOIN WWS.ERT_ERFASSTEXTE ON ERT_FIRMENNR = EFK_FIRMENNR AND ERT_ERFASSTEXTNR = EFK_EMAIL_ERFASSTEXTNR" "~"
if joinKuerzel = ERTEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = ERT_FIRMENNR AND EFK_EMAIL_ERFASSTEXTNR = ERT_ERFASSTEXTNR" "~"
if joinKuerzel = PEKERW
searchResult := searchResult "LEFT JOIN FIBU.ERW_WORKFLOW ON ERW_FIRMENNR = PEK_ERW_FIRMENNR AND ERW_WORKFLOWNR = PEK_ERW_WORKFLOWNR" "~"
if joinKuerzel = ERWPEK
searchResult := searchResult "LEFT JOIN FIBU.PEK_PERSONENKONTO ON PEK_ERW_FIRMENNR = ERW_FIRMENNR AND PEK_ERW_WORKFLOWNR = ERW_WORKFLOWNR" "~"
if joinKuerzel = EAKERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_FIRMENNR = EAK_FIRMENNR AND ERZ_ERFASSUNGSKZ = EAK_ERFASSUNGSKZ" "~"
if joinKuerzel = ERZEAK
searchResult := searchResult "LEFT JOIN WWS.EAK_ERLOESABGRENZUNGSKOPF ON EAK_FIRMENNR = ERZ_FIRMENNR AND EAK_ERFASSUNGSKZ = ERZ_ERFASSUNGSKZ" "~"
if joinKuerzel = ERZERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_FIRMENNR = ERZ_VKEK_ERFKZ_FIRMENNR AND ERZ_ERFASSUNGSKZ = ERZ_VKEK_ERFKZ" "~"
if joinKuerzel = ERZERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_VKEK_ERFKZ_FIRMENNR = ERZ_FIRMENNR AND ERZ_VKEK_ERFKZ = ERZ_ERFASSUNGSKZ" "~"
if joinKuerzel = WKPERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_FIRMENNR = WKP_ERZ_FIRMENNR AND ERZ_ERFASSUNGSKZ = WKP_ERFASSUNGSKZ" "~"
if joinKuerzel = ERZWKP
searchResult := searchResult "LEFT JOIN WWS.WKP_WWSKONTENTABPOS ON WKP_ERZ_FIRMENNR = ERZ_FIRMENNR AND WKP_ERFASSUNGSKZ = ERZ_ERFASSUNGSKZ" "~"
if joinKuerzel = WZZERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_FIRMENNR = WZZ_ERZ_FIRMENNR AND ERZ_ERFASSUNGSKZ = WZZ_ERZ_ERFASSUNGSKZ" "~"
if joinKuerzel = ERZWZZ
searchResult := searchResult "LEFT JOIN WWS.WZZ_WARENEINSATZERZZU ON WZZ_ERZ_FIRMENNR = ERZ_FIRMENNR AND WZZ_ERZ_ERFASSUNGSKZ = ERZ_ERFASSUNGSKZ" "~"
if joinKuerzel = ERZERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_FIRMENNR = ERZ_PROD_ERFKZ_FIRMENNR AND ERZ_ERFASSUNGSKZ = ERZ_PROD_ERFKZ" "~"
if joinKuerzel = ERZERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_PROD_ERFKZ_FIRMENNR = ERZ_FIRMENNR AND ERZ_PROD_ERFKZ = ERZ_ERFASSUNGSKZ" "~"
if joinKuerzel = TEREVG
searchResult := searchResult "LEFT JOIN BUERO.EVG_EVENTTAGE ON EVG_EVGTAGID = TER_EVGTAGID" "~"
if joinKuerzel = EVGTER
searchResult := searchResult "LEFT JOIN BUERO.TER_TERMIN ON TER_EVGTAGID = EVG_EVGTAGID" "~"
if joinKuerzel = EADEZT
searchResult := searchResult "LEFT JOIN HRS.EZT_ELDAZENTRALTABELLE ON EZT_EZTID = EAD_EZTID" "~"
if joinKuerzel = EZTEAD
searchResult := searchResult "LEFT JOIN HRS.EAD_ELDADETAIL_AD ON EAD_EZTID = EZT_EZTID" "~"
if joinKuerzel = EAWEZT
searchResult := searchResult "LEFT JOIN HRS.EZT_ELDAZENTRALTABELLE ON EZT_EZTID = EAW_EZTID" "~"
if joinKuerzel = EZTEAW
searchResult := searchResult "LEFT JOIN HRS.EAW_ELDADETAIL_AEW ON EAW_EZTID = EZT_EZTID" "~"
if joinKuerzel = EBVEZT
searchResult := searchResult "LEFT JOIN HRS.EZT_ELDAZENTRALTABELLE ON EZT_EZTID = EBV_EZTID" "~"
if joinKuerzel = EZTEBV
searchResult := searchResult "LEFT JOIN HRS.EBV_ELDADETAIL_BV ON EBV_EZTID = EZT_EZTID" "~"
if joinKuerzel = ELIEZT
searchResult := searchResult "LEFT JOIN HRS.EZT_ELDAZENTRALTABELLE ON EZT_EZTID = ELI_EZTID" "~"
if joinKuerzel = EZTELI
searchResult := searchResult "LEFT JOIN HRS.ELI_ELDADETAIL_LZI ON ELI_EZTID = EZT_EZTID" "~"
if joinKuerzel = PKWFAK
searchResult := searchResult "LEFT JOIN FAK_FAKTORTABELLE.FAK_FAKTORTABELLE ON FAK_FAKID = PKW_FAKID" "~"
if joinKuerzel = FAKPKW
searchResult := searchResult "LEFT JOIN BIL.PKW_PERSKORST_PARAMWERT ON PKW_FAKID = FAK_FAKID" "~"
if joinKuerzel = TTBFAK
searchResult := searchResult "LEFT JOIN FAK_FAKTORTABELLE.FAK_FAKTORTABELLE ON FAK_FAKID = TTB_STAFFEL_ID" "~"
if joinKuerzel = FAKTTB
searchResult := searchResult "LEFT JOIN BIL.TTB_TANTIEMENBERECHNUNG ON TTB_STAFFEL_ID = FAK_FAKID" "~"
if joinKuerzel = FSKFAM
searchResult := searchResult "LEFT JOIN FIBU.FAM_AUSDRUCKMASTER ON FAM_FIBUAUSDRMASTERNR = FSK_USEDFORMASTER" "~"
if joinKuerzel = FAMFSK
searchResult := searchResult "LEFT JOIN FIBU.FSK_SORTIERKOPF ON FSK_USEDFORMASTER = FAM_FIBUAUSDRMASTERNR" "~"
if joinKuerzel = DNAFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = DNA_FARBCODETABNR AND FAR_FARBCODENR = DNA_FARBCODENR" "~"
if joinKuerzel = FARDNA
searchResult := searchResult "LEFT JOIN DATANORM.DNA_ARTIKEL ON DNA_FARBCODETABNR = FAR_FARBCODETABNR AND DNA_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = EANFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = EAN_FARBCODETABNR AND FAR_FARBCODENR = EAN_FARBCODENR" "~"
if joinKuerzel = FAREAN
searchResult := searchResult "LEFT JOIN WWS.EAN_EANNUMMERN ON EAN_FARBCODETABNR = FAR_FARBCODETABNR AND EAN_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = EHPFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = EHP_FARBCODETABNR AND FAR_FARBCODENR = EHP_FARBCODENR" "~"
if joinKuerzel = FAREHP
searchResult := searchResult "LEFT JOIN WWS.EHP_EINHEITENTABPOS ON EHP_FARBCODETABNR = FAR_FARBCODETABNR AND EHP_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = FTPFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = FTP_FARBCODETABNR AND FAR_FARBCODENR = FTP_FARBCODENR" "~"
if joinKuerzel = FARFTP
searchResult := searchResult "LEFT JOIN WWS.FTP_FARBENTABPOS ON FTP_FARBCODETABNR = FAR_FARBCODETABNR AND FTP_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = GRTFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = GRT_FARBCODETABNR AND FAR_FARBCODENR = GRT_FARBCODENR" "~"
if joinKuerzel = FARGRT
searchResult := searchResult "LEFT JOIN WWS.GRT_GROESSENTAB ON GRT_FARBCODETABNR = FAR_FARBCODETABNR AND GRT_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = PTSFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = PTS_FARBCODETABNR AND FAR_FARBCODENR = PTS_FARBCODENR" "~"
if joinKuerzel = FARPTS
searchResult := searchResult "LEFT JOIN WWS.PTS_PREISTABPOSSTAFFEL ON PTS_FARBCODETABNR = FAR_FARBCODETABNR AND PTS_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = RASFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = RAS_FARBCODETABNR AND FAR_FARBCODENR = RAS_FARBCODENR" "~"
if joinKuerzel = FARRAS
searchResult := searchResult "LEFT JOIN WWS.RAS_RABATTTABPOSSTAFFEL ON RAS_FARBCODETABNR = FAR_FARBCODETABNR AND RAS_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = TBEFAR
searchResult := searchResult "LEFT JOIN BMD.FAR_FARBCODE ON FAR_FARBCODETABNR = TBE_FARBCODETABNR AND FAR_FARBCODENR = TBE_FARBCODENR" "~"
if joinKuerzel = FARTBE
searchResult := searchResult "LEFT JOIN BMD.TBE_TEMPBERECHTIGUNG ON TBE_FARBCODETABNR = FAR_FARBCODETABNR AND TBE_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = TCZFAR
searchResult := searchResult "LEFT JOIN BMD.FAR_FARBCODE ON FAR_FARBCODETABNR = TCZ_FARBCODETABNR AND FAR_FARBCODENR = TCZ_FARBCODENR" "~"
if joinKuerzel = FARTCZ
searchResult := searchResult "LEFT JOIN BMD.TCZ_TCVERSIONCASEALZU ON TCZ_FARBCODETABNR = FAR_FARBCODETABNR AND TCZ_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = WANFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = WAN_FARBCODETABNR AND FAR_FARBCODENR = WAN_FARBCODENR" "~"
if joinKuerzel = FARWAN
searchResult := searchResult "LEFT JOIN ONVIS.WAN_WARENNUMMERN ON WAN_FARBCODETABNR = FAR_FARBCODETABNR AND WAN_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = WKTFAR
searchResult := searchResult "LEFT JOIN FAR_FARBCODE.FAR_FARBCODE ON FAR_FARBCODETABNR = WKT_FARBCODETABNR AND FAR_FARBCODENR = WKT_FARBCODENR" "~"
if joinKuerzel = FARWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FARBCODETABNR = FAR_FARBCODETABNR AND WKT_FARBCODENR = FAR_FARBCODENR" "~"
if joinKuerzel = ARLFEA
searchResult := searchResult "LEFT JOIN WWS.FEA_FERTEP_AUFTEILUNG ON FEA_FEAID = ARL_FEAID" "~"
if joinKuerzel = FEAARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FEAID = FEA_FEAID" "~"
if joinKuerzel = PSMFFA
searchResult := searchResult "LEFT JOIN FFA_FIRMENFINANZAMTZU.FFA_FIRMENFINANZAMTZU ON FFA_FIRMENNR = PSM_DVFIRMENNR AND FFA_FIRMAGUELTIGJAHR = PSM_PSMJAHR AND FFA_FFINANZAMTLFDNR = PSM_FINANZAMTLFDNR" "~"
if joinKuerzel = FFAPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_DVFIRMENNR = FFA_FIRMENNR AND PSM_PSMJAHR = FFA_FIRMAGUELTIGJAHR AND PSM_FINANZAMTLFDNR = FFA_FFINANZAMTLFDNR" "~"
if joinKuerzel = ABDFGK
searchResult := searchResult "LEFT JOIN FGK_FIRMENGKKZU.FGK_FIRMENGKKZU ON FGK_FIRMENNR = ABD_DVFIRMENNR AND FGK_FIRMAGUELTIGJAHR = ABD_JAHR AND FGK_GKKLFDNR = ABD_GKKLFDNR" "~"
if joinKuerzel = FGKABD
searchResult := searchResult "LEFT JOIN HRS.ABD_ABRECHNUNGSDETAIL ON ABD_DVFIRMENNR = FGK_FIRMENNR AND ABD_JAHR = FGK_FIRMAGUELTIGJAHR AND ABD_GKKLFDNR = FGK_GKKLFDNR" "~"
if joinKuerzel = ABDFGM
searchResult := searchResult "LEFT JOIN FGM_FIRMENGEMEINDEZU.FGM_FIRMENGEMEINDEZU ON FGM_FIRMENNR = ABD_DVFIRMENNR AND FGM_FIRMAGUELTIGJAHR = ABD_JAHR AND FGM_FGEMEINDELFDNR = ABD_FGEMEINDELFDNR" "~"
if joinKuerzel = FGMABD
searchResult := searchResult "LEFT JOIN HRS.ABD_ABRECHNUNGSDETAIL ON ABD_DVFIRMENNR = FGM_FIRMENNR AND ABD_JAHR = FGM_FIRMAGUELTIGJAHR AND ABD_FGEMEINDELFDNR = FGM_FGEMEINDELFDNR" "~"
if joinKuerzel = BREFGM
searchResult := searchResult "LEFT JOIN FGM_FIRMENGEMEINDEZU.FGM_FIRMENGEMEINDEZU ON FGM_FIRMENNR = BRE_FIRMENNR AND FGM_FIRMAGUELTIGJAHR = BRE_LOHNJAHR AND FGM_FGEMEINDELFDNR = BRE_FGEMEINDELFDNR" "~"
if joinKuerzel = FGMBRE
searchResult := searchResult "LEFT JOIN HRS.BRE_BRUTTOERFASSUNG ON BRE_FIRMENNR = FGM_FIRMENNR AND BRE_LOHNJAHR = FGM_FIRMAGUELTIGJAHR AND BRE_FGEMEINDELFDNR = FGM_FGEMEINDELFDNR" "~"
if joinKuerzel = BTTFGM
searchResult := searchResult "LEFT JOIN FGM_FIRMENGEMEINDEZU.FGM_FIRMENGEMEINDEZU ON FGM_FIRMENNR = BTT_FIRMENNR AND FGM_FIRMAGUELTIGJAHR = BTT_LFI_LOHNJAHR AND FGM_FGEMEINDELFDNR = BTT_FGEMEINDELFDNR" "~"
if joinKuerzel = FGMBTT
searchResult := searchResult "LEFT JOIN HRS.BTT_BRUTTOERF_TMP ON BTT_FIRMENNR = FGM_FIRMENNR AND BTT_LFI_LOHNJAHR = FGM_FIRMAGUELTIGJAHR AND BTT_FGEMEINDELFDNR = FGM_FGEMEINDELFDNR" "~"
if joinKuerzel = KMDFGM
searchResult := searchResult "LEFT JOIN FGM_FIRMENGEMEINDEZU.FGM_FIRMENGEMEINDEZU ON FGM_FIRMENNR = KMD_FIRMENNR AND FGM_FIRMAGUELTIGJAHR = KMD_JAHR AND FGM_FGEMEINDELFDNR = KMD_GD_LFDNR" "~"
if joinKuerzel = FGMKMD
searchResult := searchResult "LEFT JOIN HRS.KMD_KOMMSTDETAIL ON KMD_FIRMENNR = FGM_FIRMENNR AND KMD_JAHR = FGM_FIRMAGUELTIGJAHR AND KMD_GD_LFDNR = FGM_FGEMEINDELFDNR" "~"
if joinKuerzel = SUDFGM
searchResult := searchResult "LEFT JOIN FGM_FIRMENGEMEINDEZU.FGM_FIRMENGEMEINDEZU ON FGM_FIRMENNR = SUD_FIRMENNR AND FGM_FIRMAGUELTIGJAHR = SUD_JAHR AND FGM_FGEMEINDELFDNR = SUD_GEMEINDELFDNR" "~"
if joinKuerzel = FGMSUD
searchResult := searchResult "LEFT JOIN HRS.SUD_SUM_KOE_DIFF ON SUD_FIRMENNR = FGM_FIRMENNR AND SUD_JAHR = FGM_FIRMAGUELTIGJAHR AND SUD_GEMEINDELFDNR = FGM_FGEMEINDELFDNR" "~"
if joinKuerzel = SUKFGM
searchResult := searchResult "LEFT JOIN FGM_FIRMENGEMEINDEZU.FGM_FIRMENGEMEINDEZU ON FGM_FIRMENNR = SUK_FIRMENNR AND FGM_FIRMAGUELTIGJAHR = SUK_JAHR AND FGM_FGEMEINDELFDNR = SUK_GEMEINDELFDNR" "~"
if joinKuerzel = FGMSUK
searchResult := searchResult "LEFT JOIN HRS.SUK_SUM_KOE ON SUK_FIRMENNR = FGM_FIRMENNR AND SUK_JAHR = FGM_FIRMAGUELTIGJAHR AND SUK_GEMEINDELFDNR = FGM_FGEMEINDELFDNR" "~"
if joinKuerzel = KEKFIF
searchResult := searchResult "LEFT JOIN FIF_FILIALEFIBU.FIF_FILIALEFIBU ON FIF_FIRMENNR = KEK_KOPF_FIBU_FIRMENNR AND FIF_FIBUNR = KEK_KOPF_FIBUNR AND FIF_FILIALNR = KEK_KOPF_FILIALNR" "~"
if joinKuerzel = FIFKEK
searchResult := searchResult "LEFT JOIN KORE.KEK_ERFASSKOPF ON KEK_KOPF_FIBU_FIRMENNR = FIF_FIRMENNR AND KEK_KOPF_FIBUNR = FIF_FIBUNR AND KEK_KOPF_FILIALNR = FIF_FILIALNR" "~"
if joinKuerzel = SKOFIF
searchResult := searchResult "LEFT JOIN FIBU.FIF_FILIALEFIBU ON FIF_FIRMENNR = SKO_FIRMENNR AND FIF_FIBUNR = SKO_FIBUNR AND FIF_FILIALNR = SKO_FILIALNR" "~"
if joinKuerzel = FIFSKO
searchResult := searchResult "LEFT JOIN FIBU.SKO_STEUERKONTEN ON SKO_FIRMENNR = FIF_FIRMENNR AND SKO_FIBUNR = FIF_FIBUNR AND SKO_FILIALNR = FIF_FILIALNR" "~"
if joinKuerzel = YBUFIF
searchResult := searchResult "LEFT JOIN FIBU.FIF_FILIALEFIBU ON FIF_FIRMENNR = YBU_FIRMENNR AND FIF_FIBUNR = YBU_FIBUNR AND FIF_FILIALNR = YBU_FIF_FILIALNR" "~"
if joinKuerzel = FIFYBU
searchResult := searchResult "LEFT JOIN FIBU.YBU_AUTOBUCHUNG ON YBU_FIRMENNR = FIF_FIRMENNR AND YBU_FIBUNR = FIF_FIBUNR AND YBU_FIF_FILIALNR = FIF_FILIALNR" "~"
if joinKuerzel = QUSFIN
searchResult := searchResult "LEFT JOIN BUERO.FIN_FINANZAMT ON FIN_LANDNR = QUS_LANDNR AND FIN_FINANZAMTNR = QUS_FINANZAMTNR" "~"
if joinKuerzel = FINQUS
searchResult := searchResult "LEFT JOIN BUERO.QUS_QUOTENSUMME ON QUS_LANDNR = FIN_LANDNR AND QUS_FINANZAMTNR = FIN_FINANZAMTNR" "~"
if joinKuerzel = ZKSFIP
searchResult := searchResult "LEFT JOIN FIP_FILTERPANEL.FIP_FILTERPANEL ON FIP_FILTERID = ZKS_FIP_FILTERID" "~"
if joinKuerzel = FIPZKS
searchResult := searchResult "LEFT JOIN FIBU.ZKS_ZINSBERECHNUNGSKREIS ON ZKS_FIP_FILTERID = FIP_FILTERID" "~"
if joinKuerzel = ABKFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = ABK_FIRMENNR" "~"
if joinKuerzel = FIRABK
searchResult := searchResult "LEFT JOIN BUERO.ABK_ARCHIVBAUMKOPF ON ABK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ADCFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ADC_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRADC
searchResult := searchResult "LEFT JOIN WWS.ADC_ABGABENDETAILRECH ON ADC_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ALEFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = ALE_ABSENDER_FIRMENNR" "~"
if joinKuerzel = FIRALE
searchResult := searchResult "LEFT JOIN BUERO.ALE_ADRESSENLISTE ON ALE_ABSENDER_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ANHFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = ANH_FIRMENNR" "~"
if joinKuerzel = FIRANH
searchResult := searchResult "LEFT JOIN BUERO.ANH_ANGEHOERIGER ON ANH_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ASBFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ASB_FIRMENNR" "~"
if joinKuerzel = FIRASB
searchResult := searchResult "LEFT JOIN ZLE.ASB_AUSWERT_SUB ON ASB_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AUEFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = AUE_FIRMENNR" "~"
if joinKuerzel = FIRAUE
searchResult := searchResult "LEFT JOIN ZLE.AUE_AUSWERTTYPEINST ON AUE_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = AVKFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = AVK_FIRMENNR" "~"
if joinKuerzel = FIRAVK
searchResult := searchResult "LEFT JOIN BUERO.AVK_AVKONTINGENTE ON AVK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BESFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = BES_FIRMENNR" "~"
if joinKuerzel = FIRBES
searchResult := searchResult "LEFT JOIN BUERO.BES_BEWERBUNGSTATUS ON BES_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BPKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = BPK_FILIALNR" "~"
if joinKuerzel = FIRBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_FILIALNR = FIR_FIRMENNR" "~"
if joinKuerzel = BRGFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = BRG_FIRMENNR" "~"
if joinKuerzel = FIRBRG
searchResult := searchResult "LEFT JOIN BUERO.BRG_BERUFSGRUPPEN ON BRG_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BRMFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = BRM_FIRMENNR" "~"
if joinKuerzel = FIRBRM
searchResult := searchResult "LEFT JOIN BUERO.BRM_BUEROMAKROS ON BRM_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BTHFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = BTH_FIRMENNR" "~"
if joinKuerzel = FIRBTH
searchResult := searchResult "LEFT JOIN BUERO.BTH_BTERMHERSTELLER ON BTH_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BWVFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = BWV_FIRMENNR" "~"
if joinKuerzel = FIRBWV
searchResult := searchResult "LEFT JOIN BUERO.BWV_BEWERTUNGVERSION ON BWV_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = DBKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = DBK_FIRMENNR" "~"
if joinKuerzel = FIRDBK
searchResult := searchResult "LEFT JOIN BMD.DBK_DRUCKBAUSTEINKOPF ON DBK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = DFBFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = DFB_FIRMENNR" "~"
if joinKuerzel = FIRDFB
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFB_DFELDBESCHKOPF ON DFB_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = DIPFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = DIP_FIRMENNR" "~"
if joinKuerzel = FIRDIP
searchResult := searchResult "LEFT JOIN BUERO.DIP_DIVERSE_PERSON ON DIP_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = E18FIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = E18_DVFIRMENNR" "~"
if joinKuerzel = FIRE18
searchResult := searchResult "LEFT JOIN HRS.E18_ELDADETAIL_E18 ON E18_DVFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = EBGFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = EBG_DVFIRMENNR" "~"
if joinKuerzel = FIREBG
searchResult := searchResult "LEFT JOIN HRS.EBG_ELDADETAIL_BGN ON EBG_DVFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = EDVFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = EDV_FIRMENNR" "~"
if joinKuerzel = FIREDV
searchResult := searchResult "LEFT JOIN WWS.EDV_ERFASSRABATTDETAILVOR ON EDV_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = EFHFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = EFH_DVFIRMENNR" "~"
if joinKuerzel = FIREFH
searchResult := searchResult "LEFT JOIN HRS.EFH_ELDADETAIL_FH ON EFH_DVFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = EKKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = EKK_FIRMENNR" "~"
if joinKuerzel = FIREKK
searchResult := searchResult "LEFT JOIN WWS.EKK_EXTERNE_KUNDENKARTEI ON EKK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ERZFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ERZ_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FGEFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = FGE_FIRMENNR" "~"
if joinKuerzel = FIRFGE
searchResult := searchResult "LEFT JOIN BUERO.FGE_FRAGE ON FGE_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FIRFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = FIR_STB_FIRMENNR" "~"
if joinKuerzel = FIRFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_STB_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = HSOFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = HSO_FIRMENNR" "~"
if joinKuerzel = FIRHSO
searchResult := searchResult "LEFT JOIN HRS.HSO_SORTIERPOS ON HSO_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = IMOFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = IMO_FIRMENNR" "~"
if joinKuerzel = FIRIMO
searchResult := searchResult "LEFT JOIN BUERO.IMO_IMMOBILIE ON IMO_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = IOBFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = IOB_FIRMENNR" "~"
if joinKuerzel = FIRIOB
searchResult := searchResult "LEFT JOIN BUERO.IOB_INSTANDHOBJEKT ON IOB_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = IPZFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = IPZ_FIRMENNR" "~"
if joinKuerzel = FIRIPZ
searchResult := searchResult "LEFT JOIN BUERO.IPZ_INTERNPROJEKTZU ON IPZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = KEKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = KEK_EXTFIRMENNR" "~"
if joinKuerzel = FIRKEK
searchResult := searchResult "LEFT JOIN KORE.KEK_ERFASSKOPF ON KEK_EXTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = LMSFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LMS_FIRMENNR" "~"
if joinKuerzel = FIRLMS
searchResult := searchResult "LEFT JOIN WWS.LMS_LIEFERMAHNUNG ON LMS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = LVEFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LVE_ARL_FIRMENNR" "~"
if joinKuerzel = FIRLVE
searchResult := searchResult "LEFT JOIN PRODUKTION.LVE_LEISTUNGSVORERFASSUNG ON LVE_ARL_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = MIKFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = MIK_FIRMENNR" "~"
if joinKuerzel = FIRMIK
searchResult := searchResult "LEFT JOIN BUERO.MIK_MITARBEITERKARRIERE ON MIK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = MLSFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = MLS_FIRMENNR" "~"
if joinKuerzel = FIRMLS
searchResult := searchResult "LEFT JOIN ZLE.MLS_MITARBLEISTTAUSCHSATZ ON MLS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = OUFFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = OUF_FIRMENNR" "~"
if joinKuerzel = FIROUF
searchResult := searchResult "LEFT JOIN BUERO.OUF_ONLINEUMFRAGE ON OUF_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PRSFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PRS_FIRMENNR" "~"
if joinKuerzel = FIRPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PTRFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PTR_FIRMENNR" "~"
if joinKuerzel = FIRPTR
searchResult := searchResult "LEFT JOIN PRODUKTION.PTR_TRANSAKTION ON PTR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PVAFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = PVA_FIRMENNR" "~"
if joinKuerzel = FIRPVA
searchResult := searchResult "LEFT JOIN BUERO.PVA_PERSONVORERFASSAKTION ON PVA_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PVZFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PVZ_FIRMENNR" "~"
if joinKuerzel = FIRPVZ
searchResult := searchResult "LEFT JOIN WWS.PVZ_PROVISIONTABZU ON PVZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RNGFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = RNG_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = RNPFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = RNP_FILIALNR" "~"
if joinKuerzel = FIRRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FILIALNR = FIR_FIRMENNR" "~"
if joinKuerzel = RVKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = RVK_FIRMENNR" "~"
if joinKuerzel = FIRRVK
searchResult := searchResult "LEFT JOIN ZLE.RVK_RECHVORLAGEKOPF ON RVK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SAWFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = SAW_FIRMENNR" "~"
if joinKuerzel = FIRSAW
searchResult := searchResult "LEFT JOIN WWSINDIV.SAW_STATISTIKWERTE ON SAW_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SBVFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = SBV_FIRMENNR" "~"
if joinKuerzel = FIRSBV
searchResult := searchResult "LEFT JOIN BUERO.SBV_STELLENBESCHVORLAGE ON SBV_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SCKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = SCK_FIR_FIRMENNR" "~"
if joinKuerzel = FIRSCK
searchResult := searchResult "LEFT JOIN PRODUKTION.SCK_SCKENNZAHL ON SCK_FIR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SFFFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = SFF_FIRMENNR" "~"
if joinKuerzel = FIRSFF
searchResult := searchResult "LEFT JOIN BUERO.SFF_SKILLFINDERFILTER ON SFF_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SPKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = SPK_FIRMENNR" "~"
if joinKuerzel = FIRSPK
searchResult := searchResult "LEFT JOIN BMD.SPK_STAPELKOPF ON SPK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = TRJFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = TRJ_FIRMENNR" "~"
if joinKuerzel = FIRTRJ
searchResult := searchResult "LEFT JOIN WWS.TRJ_TRANSPORTEINH_JOURNAL ON TRJ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = UIDFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = UID_FIRMENNR" "~"
if joinKuerzel = FIRUID
searchResult := searchResult "LEFT JOIN BUERO.UID_UIDNUMMER ON UID_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = WDRFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = WDR_FIRMENNR" "~"
if joinKuerzel = FIRWDR
searchResult := searchResult "LEFT JOIN WWS.WDR_WWSDATENREIHENFOLGE ON WDR_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ZAKFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = ZAK_FIRMENNR" "~"
if joinKuerzel = FIRZAK
searchResult := searchResult "LEFT JOIN BUERO.ZAK_ZEITAUSWERTMAKRO ON ZAK_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ZBGFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = ZBG_FIRMENNR" "~"
if joinKuerzel = FIRZBG
searchResult := searchResult "LEFT JOIN BUERO.ZBG_ZEITBABERECHTGRUPPE ON ZBG_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ZITFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = ZIT_FIRMENNR" "~"
if joinKuerzel = FIRZIT
searchResult := searchResult "LEFT JOIN BUERO.ZIT_ZUSATZINFOTYP ON ZIT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ZVZFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = ZVZ_FIRMENNR" "~"
if joinKuerzel = FIRZVZ
searchResult := searchResult "LEFT JOIN BUERO.ZVZ_TBVERWENDUNGSZWECK ON ZVZ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = BSPFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = BSP_BONUSGRUPPEN_FIRMENNR" "~"
if joinKuerzel = FIRBSP
searchResult := searchResult "LEFT JOIN WWS.BSP_BONUSERMPOS ON BSP_BONUSGRUPPEN_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = FIRFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = FIR_FIL_GEHOERT_FIRMENNR" "~"
if joinKuerzel = FIRFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIL_GEHOERT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = KUKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = KUK_VERRECH_FIRMENNR" "~"
if joinKuerzel = FIRKUK
searchResult := searchResult "LEFT JOIN WWS.KUK_KUNDENKARTEI ON KUK_VERRECH_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = LEIFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LEI_MITARB_FIRMENNR" "~"
if joinKuerzel = FIRLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_MITARB_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = QAWFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = QAW_QUOTEN_FIRMENNR" "~"
if joinKuerzel = FIRQAW
searchResult := searchResult "LEFT JOIN BUERO.QAW_QUOTENAUSWERTUNG ON QAW_QUOTEN_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = QUOFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = QUO_QUOTEN_FIRMENNR" "~"
if joinKuerzel = FIRQUO
searchResult := searchResult "LEFT JOIN BUERO.QUO_QUOTE ON QUO_QUOTEN_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = VSWFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = VSW_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRVSW
searchResult := searchResult "LEFT JOIN WWS.VSW_VERTRETERSTATISTIK ON VSW_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = WAKFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = WAK_FILIALNR" "~"
if joinKuerzel = FIRWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_FILIALNR = FIR_FIRMENNR" "~"
if joinKuerzel = EANFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = EAN_FIRMENNR" "~"
if joinKuerzel = FIREAN
searchResult := searchResult "LEFT JOIN WWS.EAN_EANNUMMERN ON EAN_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = EROFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = ERO_HAUPTFIRMENNR" "~"
if joinKuerzel = FIRERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_HAUPTFIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = LAJFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = LAJ_FIRMENNR" "~"
if joinKuerzel = FIRLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PACFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = PAC_FIRMENNR" "~"
if joinKuerzel = FIRPAC
searchResult := searchResult "LEFT JOIN WWS.PAC_PACKSTOFF ON PAC_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = SAIFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = SAI_FIRMENNR" "~"
if joinKuerzel = FIRSAI
searchResult := searchResult "LEFT JOIN WWS.SAI_SAISON ON SAI_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = WADFIR
searchResult := searchResult "LEFT JOIN FIR_FIRMA.FIR_FIRMA ON FIR_FIRMENNR = WAD_FWW_FIRMENNR" "~"
if joinKuerzel = FIRWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_FWW_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = KLIFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = KLI_WP_FIRMENNR" "~"
if joinKuerzel = FIRKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_WP_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = PERFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = PER_HAUPT_FIRMENNR" "~"
if joinKuerzel = FIRPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_HAUPT_FIRMENNR = FIR_FIRMENNR" "~"
if joinKuerzel = ERPVKA
searchResult := searchResult "LEFT JOIN KORE.VKA_VIEW_KOSTAMM_ABT ON VKA_FIRMENNR = ERP_FIRMENNR AND VKA_KOSTENSTAMMNR = ERP_ABT_KOSTENSTAMMNR" "~"
if joinKuerzel = VKAERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_FIRMENNR = VKA_FIRMENNR AND ERP_ABT_KOSTENSTAMMNR = VKA_KOSTENSTAMMNR" "~"
if joinKuerzel = FOTFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = FOT_FKT_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = FOT_FIRMENKATEGORIEKZ AND FKT_FIRMENKATEGORIELFDNR = FOT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = FKTFOT
searchResult := searchResult "LEFT JOIN BUERO.FOT_FOTO ON FOT_FKT_FIRMENNR = FKT_FIRMENNR AND FOT_FIRMENKATEGORIEKZ = FKT_FIRMENKATEGORIEKZ AND FOT_FIRMENKATEGORIELFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = HNPFKT
searchResult := searchResult "LEFT JOIN FKT_FIRMENKATEGORIE.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = HNP_FKT_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = HNP_FIRMENKATEGORIEKZ AND FKT_FIRMENKATEGORIELFDNR = HNP_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = FKTHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_FKT_FIRMENNR = FKT_FIRMENNR AND HNP_FIRMENKATEGORIEKZ = FKT_FIRMENKATEGORIEKZ AND HNP_FIRMENKATEGORIELFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = MGZFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = MGZ_FKT_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = MGZ_FKT_FIRMENKATKZ AND FKT_FIRMENKATEGORIELFDNR = MGZ_FKT_FIRMENKATLFDNR" "~"
if joinKuerzel = FKTMGZ
searchResult := searchResult "LEFT JOIN BUERO.MGZ_MERKMALSGRUPPEZU ON MGZ_FKT_FIRMENNR = FKT_FIRMENNR AND MGZ_FKT_FIRMENKATKZ = FKT_FIRMENKATEGORIEKZ AND MGZ_FKT_FIRMENKATLFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = EVTFKT
searchResult := searchResult "LEFT JOIN BUERO.FKT_FIRMENKATEGORIE ON FKT_FIRMENNR = EVT_PAKET_FIRMENNR AND FKT_FIRMENKATEGORIEKZ = EVT_PAKET_KATEGORIEKZ AND FKT_FIRMENKATEGORIELFDNR = EVT_PAKET_KATEGORIELFDNR" "~"
if joinKuerzel = FKTEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_PAKET_FIRMENNR = FKT_FIRMENNR AND EVT_PAKET_KATEGORIEKZ = FKT_FIRMENKATEGORIEKZ AND EVT_PAKET_KATEGORIELFDNR = FKT_FIRMENKATEGORIELFDNR" "~"
if joinKuerzel = BLGFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = BLG_FORMELNR" "~"
if joinKuerzel = FORBLG
searchResult := searchResult "LEFT JOIN BUERO.BLG_BEZARTLISTGEN ON BLG_FORMELNR = FOR_FORMELNR" "~"
if joinKuerzel = FOXFOR
searchResult := searchResult "LEFT JOIN BMD.FOR_FORMEL ON FOR_FORMELNR = FOX_FORMELNR" "~"
if joinKuerzel = FORFOX
searchResult := searchResult "LEFT JOIN BMD.FOX_FORMELTEXT ON FOX_FORMELNR = FOR_FORMELNR" "~"
if joinKuerzel = LGFFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = LGF_MAKROID" "~"
if joinKuerzel = FORLGF
searchResult := searchResult "LEFT JOIN HRS.LGF_LGVMAKROS ON LGF_MAKROID = FOR_FORMELNR" "~"
if joinKuerzel = OUEFOR
searchResult := searchResult "LEFT JOIN BMD.FOR_FORMEL ON FOR_FORMELNR = OUE_FORMELNR" "~"
if joinKuerzel = FOROUE
searchResult := searchResult "LEFT JOIN BMD.OUE_OUTLOOKBARELEM ON OUE_FORMELNR = FOR_FORMELNR" "~"
if joinKuerzel = SCAFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = SCA_MAKRO_BEWERT" "~"
if joinKuerzel = FORSCA
searchResult := searchResult "LEFT JOIN PRODUKTION.SCA_SCKRITERIUM ON SCA_MAKRO_BEWERT = FOR_FORMELNR" "~"
if joinKuerzel = SPLFOR
searchResult := searchResult "LEFT JOIN BMD.FOR_FORMEL ON FOR_FORMELNR = SPL_FORMELNR" "~"
if joinKuerzel = FORSPL
searchResult := searchResult "LEFT JOIN BMD.SPL_STEMPEL ON SPL_FORMELNR = FOR_FORMELNR" "~"
if joinKuerzel = VWAFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = VWA_MAKROID" "~"
if joinKuerzel = FORVWA
searchResult := searchResult "LEFT JOIN BUERO.VWA_VORSCHLAGSWERTEARCHIV ON VWA_MAKROID = FOR_FORMELNR" "~"
if joinKuerzel = ZVTFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = ZVT_FORMELNR_KOPF" "~"
if joinKuerzel = FORZVT
searchResult := searchResult "LEFT JOIN BUERO.ZVT_TBDATEIEN ON ZVT_FORMELNR_KOPF = FOR_FORMELNR" "~"
if joinKuerzel = DFZFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = DFZ_MACRO_F4_ID" "~"
if joinKuerzel = FORDFZ
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFZ_DFELDGRUPPENZU ON DFZ_MACRO_F4_ID = FOR_FORMELNR" "~"
if joinKuerzel = IMSFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = IMS_MACRO_BEFORE_WRITE" "~"
if joinKuerzel = FORIMS
searchResult := searchResult "LEFT JOIN HRS.IMS_IMPORTSCHNITTSTELLEN ON IMS_MACRO_BEFORE_WRITE = FOR_FORMELNR" "~"
if joinKuerzel = ZNSFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = ZNS_BEDINGUNG_FORMELNR" "~"
if joinKuerzel = FORZNS
searchResult := searchResult "LEFT JOIN BUERO.ZNS_ZEITNACHRICHTSYSTEM ON ZNS_BEDINGUNG_FORMELNR = FOR_FORMELNR" "~"
if joinKuerzel = ZVTFOR
searchResult := searchResult "LEFT JOIN FOR_FORMEL.FOR_FORMEL ON FOR_FORMELNR = ZVT_FORMELNR_PROT_ENDE" "~"
if joinKuerzel = FORZVT
searchResult := searchResult "LEFT JOIN BUERO.ZVT_TBDATEIEN ON ZVT_FORMELNR_PROT_ENDE = FOR_FORMELNR" "~"
if joinKuerzel = TRTFOT
searchResult := searchResult "LEFT JOIN FOT_FOTO.FOT_FOTO ON FOT_FIRMENNR = TRT_FOT_FIRMENNR AND FOT_FOTONR = TRT_FOT_FOTONR_GESCHL" "~"
if joinKuerzel = FOTTRT
searchResult := searchResult "LEFT JOIN WWS.TRT_TRANSPORTTYP ON TRT_FOT_FIRMENNR = FOT_FIRMENNR AND TRT_FOT_FOTONR_GESCHL = FOT_FOTONR" "~"
if joinKuerzel = TRTFOT
searchResult := searchResult "LEFT JOIN FOT_FOTO.FOT_FOTO ON FOT_FIRMENNR = TRT_FOT_FIRMENNR AND FOT_FOTONR = TRT_FOT_FOTONR_OFFEN" "~"
if joinKuerzel = FOTTRT
searchResult := searchResult "LEFT JOIN WWS.TRT_TRANSPORTTYP ON TRT_FOT_FIRMENNR = FOT_FIRMENNR AND TRT_FOT_FOTONR_OFFEN = FOT_FOTONR" "~"
if joinKuerzel = ARBFRB
searchResult := searchResult "LEFT JOIN WWS.FRB_FARBE ON FRB_FIRMENNR = ARB_VORG_FRB_FIRMENNR AND FRB_FARBENNR = ARB_VORG_FARBENNR" "~"
if joinKuerzel = FRBARB
searchResult := searchResult "LEFT JOIN WWS.ARB_ARTIKELBEREICHSWERTE ON ARB_VORG_FRB_FIRMENNR = FRB_FIRMENNR AND ARB_VORG_FARBENNR = FRB_FARBENNR" "~"
if joinKuerzel = HNPFRT
searchResult := searchResult "LEFT JOIN ZLE.FRT_FAKTURATYP ON FRT_FIRMENNR = HNP_FRT_FIRMENNR AND FRT_FAKTURATYPNR = HNP_FRT_FAKTURATYPNR" "~"
if joinKuerzel = FRTHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_FRT_FIRMENNR = FRT_FIRMENNR AND HNP_FRT_FAKTURATYPNR = FRT_FAKTURATYPNR" "~"
if joinKuerzel = FRIFST
searchResult := searchResult "LEFT JOIN BUERO.FST_FRIST ON FST_FRISTLFDNR = FRI_FRISTLFDNR" "~"
if joinKuerzel = FSTFRI
searchResult := searchResult "LEFT JOIN BUERO.FRI_FRISTVERLAENGERUNGEN ON FRI_FRISTLFDNR = FST_FRISTLFDNR" "~"
if joinKuerzel = ANBFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = ANB_FIBUFIRMENNR AND FWF_FIBUNR = ANB_FIBUNR" "~"
if joinKuerzel = FWFANB
searchResult := searchResult "LEFT JOIN BIL.ANB_ANBUBUCHBELEGZU ON ANB_FIBUFIRMENNR = FWF_FIRMENNR AND ANB_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = BIMFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = BIM_FIRMENNR AND FWF_FIBUNR = BIM_FIBUNR" "~"
if joinKuerzel = FWFBIM
searchResult := searchResult "LEFT JOIN FIBU.BIM_BUERFIMPORTMODELL ON BIM_FIRMENNR = FWF_FIRMENNR AND BIM_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = EBKFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = EBK_FIRMENNR AND FWF_FIBUNR = EBK_FIBUNR" "~"
if joinKuerzel = FWFEBK
searchResult := searchResult "LEFT JOIN FIBU.EBK_EBKORREKTURKONTEN ON EBK_FIRMENNR = FWF_FIRMENNR AND EBK_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = FDHFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = FDH_FIRMENNR AND FWF_FIBUNR = FDH_FIBUNR" "~"
if joinKuerzel = FWFFDH
searchResult := searchResult "LEFT JOIN FIBU.FDH_FIBUDASHBOARDKOPF ON FDH_FIRMENNR = FWF_FIRMENNR AND FDH_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = FPRFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = FPR_FIRMENNR AND FWF_FIBUNR = FPR_FIBUNR" "~"
if joinKuerzel = FWFFPR
searchResult := searchResult "LEFT JOIN FIBU.FPR_FIBUPROZENTSATZ ON FPR_FIRMENNR = FWF_FIRMENNR AND FPR_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = FWKFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = FWK_FIRMENNR AND FWF_FIBUNR = FWK_FWF_FIBUNR" "~"
if joinKuerzel = FWFFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = FWF_FIRMENNR AND FWK_FWF_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = FZKFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = FZK_FIRMENNR AND FWF_FIBUNR = FZK_FIBUNR" "~"
if joinKuerzel = FWFFZK
searchResult := searchResult "LEFT JOIN FIBU.FZK_ZAHLSCHLUESSELKOPF ON FZK_FIRMENNR = FWF_FIRMENNR AND FZK_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = KFZFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = KFZ_FWF_FIRMENNR AND FWF_FIBUNR = KFZ_FWF_FIBUNR" "~"
if joinKuerzel = FWFKFZ
searchResult := searchResult "LEFT JOIN KORE.KFZ_KOREFIBUZU ON KFZ_FWF_FIRMENNR = FWF_FIRMENNR AND KFZ_FWF_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = KSKFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = KSK_GEWICHTUNG_FIRMENNR AND FWF_FIBUNR = KSK_GEWICHTUNG_FIBUNR" "~"
if joinKuerzel = FWFKSK
searchResult := searchResult "LEFT JOIN KORE.KSK_KORESCHLUESSELKOPF ON KSK_GEWICHTUNG_FIRMENNR = FWF_FIRMENNR AND KSK_GEWICHTUNG_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = MKRFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = MKR_FIRMENNR AND FWF_FIBUNR = MKR_FIBUNR" "~"
if joinKuerzel = FWFMKR
searchResult := searchResult "LEFT JOIN FIBU.MKR_MAHNKREIS ON MKR_FIRMENNR = FWF_FIRMENNR AND MKR_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = PROFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = PRO_FWF_FIRMENNR AND FWF_FIBUNR = PRO_FIBUNR" "~"
if joinKuerzel = FWFPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FWF_FIRMENNR = FWF_FIRMENNR AND PRO_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = SKOFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = SKO_FIRMENNR AND FWF_FIBUNR = SKO_FIBUNR" "~"
if joinKuerzel = FWFSKO
searchResult := searchResult "LEFT JOIN FIBU.SKO_STEUERKONTEN ON SKO_FIRMENNR = FWF_FIRMENNR AND SKO_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = SRBFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = SRB_FIBUFIRMENNR AND FWF_FIBUNR = SRB_FIBUNR" "~"
if joinKuerzel = FWFSRB
searchResult := searchResult "LEFT JOIN BIL.SRB_STEUERBUCHUNGEN ON SRB_FIBUFIRMENNR = FWF_FIRMENNR AND SRB_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = XBRFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = XBR_FIRMENNR AND FWF_FIBUNR = XBR_FIBUNR" "~"
if joinKuerzel = FWFXBR
searchResult := searchResult "LEFT JOIN BIL.XBR_XBRLDATA ON XBR_FIRMENNR = FWF_FIRMENNR AND XBR_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = YBMFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = YBM_FIRMENNR AND FWF_FIBUNR = YBM_FIBUNR" "~"
if joinKuerzel = FWFYBM
searchResult := searchResult "LEFT JOIN FIBU.YBM_AUTOBUCHMODELLE ON YBM_FIRMENNR = FWF_FIRMENNR AND YBM_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = YBUFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = YBU_FIRMENNR AND FWF_FIBUNR = YBU_FIBUNR" "~"
if joinKuerzel = FWFYBU
searchResult := searchResult "LEFT JOIN FIBU.YBU_AUTOBUCHUNG ON YBU_FIRMENNR = FWF_FIRMENNR AND YBU_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = ZVRFWF
searchResult := searchResult "LEFT JOIN FWF_FIRMENWERTEFIBU.FWF_FIRMENWERTEFIBU ON FWF_FIRMENNR = ZVR_FIRMENNR AND FWF_FIBUNR = ZVR_FIBUNR" "~"
if joinKuerzel = FWFZVR
searchResult := searchResult "LEFT JOIN ZX_GELOESCHT.ZVR_RECHNVORSCHLAEGE ON ZVR_FIRMENNR = FWF_FIRMENNR AND ZVR_FIBUNR = FWF_FIBUNR" "~"
if joinKuerzel = AVBFWK
searchResult := searchResult "LEFT JOIN FWK_FIRMENWERTEKORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = AVB_FIRMENNR AND FWK_KORENR = AVB_KORENR" "~"
if joinKuerzel = FWKAVB
searchResult := searchResult "LEFT JOIN BUERO.AVB_AVBUDGET ON AVB_FIRMENNR = FWK_FIRMENNR AND AVB_KORENR = FWK_KORENR" "~"
if joinKuerzel = KMGFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = KMG_FIRMENNR AND FWK_KORENR = KMG_KORENR" "~"
if joinKuerzel = FWKKMG
searchResult := searchResult "LEFT JOIN KORE.KMG_KOREMENGE ON KMG_FIRMENNR = FWK_FIRMENNR AND KMG_KORENR = FWK_KORENR" "~"
if joinKuerzel = KMPFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = KMP_FIRMENNR AND FWK_KORENR = KMP_KORENR" "~"
if joinKuerzel = FWKKMP
searchResult := searchResult "LEFT JOIN KORE.KMP_PLANMENGEN ON KMP_FIRMENNR = FWK_FIRMENNR AND KMP_KORENR = FWK_KORENR" "~"
if joinKuerzel = KZUFWK
searchResult := searchResult "LEFT JOIN KORE.FWK_FIRMENWERTEKORE ON FWK_FIRMENNR = KZU_FIRMENNR AND FWK_KORENR = KZU_KORENR" "~"
if joinKuerzel = FWKKZU
searchResult := searchResult "LEFT JOIN KORE.KZU_KOREKOREZU ON KZU_FIRMENNR = FWK_FIRMENNR AND KZU_KORENR = FWK_KORENR" "~"
if joinKuerzel = GZUGEF
searchResult := searchResult "LEFT JOIN WWS.GEF_GEFAHRENGUTSTAMM ON GEF_GEFGUTID = GZU_GEFGUTID" "~"
if joinKuerzel = GEFGZU
searchResult := searchResult "LEFT JOIN WWS.GZU_GEFAHRENGUTZU ON GZU_GEFGUTID = GEF_GEFGUTID" "~"
if joinKuerzel = FIWGEM
searchResult := searchResult "LEFT JOIN BUERO.GEM_GEMEINDE ON GEM_LANDNR = FIW_GEM_LANDNR AND GEM_GEMEINDENR = FIW_GEM_GEMEINDENR" "~"
if joinKuerzel = GEMFIW
searchResult := searchResult "LEFT JOIN BUERO.FIW_FIRMENWERTE ON FIW_GEM_LANDNR = GEM_LANDNR AND FIW_GEM_GEMEINDENR = GEM_GEMEINDENR" "~"
if joinKuerzel = GNDGNH
searchResult := searchResult "LEFT JOIN BIL.GNH_GAU_HEADER ON GNH_GNHID = GND_GNHID" "~"
if joinKuerzel = GNHGND
searchResult := searchResult "LEFT JOIN BIL.GND_GAU_DATA ON GND_GNHID = GNH_GNHID" "~"
if joinKuerzel = DBGBMG
searchResult := searchResult "LEFT JOIN BMG_BMDGRUPPE.BMG_BMDGRUPPE ON BMG_BMDGRUPPENID = DBG_BMDGRUPPENID" "~"
if joinKuerzel = BMGDBG
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DBG_DBMDGRUPPE ON DBG_BMDGRUPPENID = BMG_BMDGRUPPENID" "~"
if joinKuerzel = AIDGRO
searchResult := searchResult "LEFT JOIN WWS.GRO_GROESSE ON GRO_FIRMENNR = AID_GRO_FIRMENNR AND GRO_GROESSENKBEZ = AID_ARTIKELIDGRKBEZ" "~"
if joinKuerzel = GROAID
searchResult := searchResult "LEFT JOIN WWS.AID_ARTIKELIDENTIFIKATION ON AID_GRO_FIRMENNR = GRO_FIRMENNR AND AID_ARTIKELIDGRKBEZ = GRO_GROESSENKBEZ" "~"
if joinKuerzel = EANGRO
searchResult := searchResult "LEFT JOIN WWS.GRO_GROESSE ON GRO_FIRMENNR = EAN_GRO_FIRMENNR AND GRO_GROESSENKBEZ = EAN_GROESSENKBEZ" "~"
if joinKuerzel = GROEAN
searchResult := searchResult "LEFT JOIN WWS.EAN_EANNUMMERN ON EAN_GRO_FIRMENNR = GRO_FIRMENNR AND EAN_GROESSENKBEZ = GRO_GROESSENKBEZ" "~"
if joinKuerzel = LMBGRO
searchResult := searchResult "LEFT JOIN WWS.GRO_GROESSE ON GRO_FIRMENNR = LMB_ARL_FIRMENNR AND GRO_GROESSENKBEZ = LMB_GROESSENKBEZ" "~"
if joinKuerzel = GROLMB
searchResult := searchResult "LEFT JOIN WWS.LMB_LAGERMINMAXBESTAND ON LMB_ARL_FIRMENNR = GRO_FIRMENNR AND LMB_GROESSENKBEZ = GRO_GROESSENKBEZ" "~"
if joinKuerzel = PTPGRO
searchResult := searchResult "LEFT JOIN WWS.GRO_GROESSE ON GRO_FIRMENNR = PTP_GRO_FIRMENNR AND GRO_GROESSENKBEZ = PTP_GROESSENKBEZ" "~"
if joinKuerzel = GROPTP
searchResult := searchResult "LEFT JOIN WWS.PTP_PREISTABPOS ON PTP_GRO_FIRMENNR = GRO_FIRMENNR AND PTP_GROESSENKBEZ = GRO_GROESSENKBEZ" "~"
if joinKuerzel = PFTGZJ
searchResult := searchResult "LEFT JOIN HRS.GZJ_GLAEUBIGERZU_JAHR ON GZJ_DVFIRMENNR = PFT_DVFIRMENNR AND GZJ_FIRMENNR = PFT_FIRMENNR AND GZJ_MITARBEITERID = PFT_MITARBEITERID AND GZJ_GZJNR = PFT_GZJNR" "~"
if joinKuerzel = GZJPFT
searchResult := searchResult "LEFT JOIN HRS.PFT_PFAENDUNG_TILGUNG ON PFT_DVFIRMENNR = GZJ_DVFIRMENNR AND PFT_FIRMENNR = GZJ_FIRMENNR AND PFT_MITARBEITERID = GZJ_MITARBEITERID AND PFT_GZJNR = GZJ_GZJNR" "~"
if joinKuerzel = DBTHLB
searchResult := searchResult "LEFT JOIN HLB_HILFEBAUSTEIN.HLB_HILFEBAUSTEIN ON HLB_HLBID = DBT_HLBID" "~"
if joinKuerzel = HLBDBT
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DBT_DBMDTABFELDBESCH ON DBT_HLBID = HLB_HLBID" "~"
if joinKuerzel = HLZHLB
searchResult := searchResult "LEFT JOIN BMDENTW.HLB_HILFEBAUSTEIN ON HLB_HLBID = HLZ_HLBID" "~"
if joinKuerzel = HLBHLZ
searchResult := searchResult "LEFT JOIN BMDENTW.HLZ_HILFEDOKBAUSTEINZU ON HLZ_HLBID = HLB_HLBID" "~"
if joinKuerzel = PAUHLB
searchResult := searchResult "LEFT JOIN HLB_HILFEBAUSTEIN.HLB_HILFEBAUSTEIN ON HLB_HLBID = PAU_HLBID" "~"
if joinKuerzel = HLBPAU
searchResult := searchResult "LEFT JOIN BMDDEFAULT.PAU_PARAMUNTERGRUPPE ON PAU_HLBID = HLB_HLBID" "~"
if joinKuerzel = DFUHLD
searchResult := searchResult "LEFT JOIN HLD_HILFEDOKUMENT.HLD_HILFEDOKUMENT ON HLD_HLDID = DFU_HLDID_MOBILE" "~"
if joinKuerzel = HLDDFU
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFU_DFUNKTION ON DFU_HLDID_MOBILE = HLD_HLDID" "~"
if joinKuerzel = RNGHNP
searchResult := searchResult "LEFT JOIN HNP_HONORARPOS.HNP_HONORARPOS ON HNP_HONORARPOSNR = RNG_HONORARPOSNR" "~"
if joinKuerzel = HNPRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_HONORARPOSNR = HNP_HONORARPOSNR" "~"
if joinKuerzel = HSOHSA
searchResult := searchResult "LEFT JOIN HRS.HSA_SORTIERART ON HSA_SORTARTLFDNR = HSO_SORTARTLFDNR" "~"
if joinKuerzel = HSAHSO
searchResult := searchResult "LEFT JOIN HRS.HSO_SORTIERPOS ON HSO_SORTARTLFDNR = HSA_SORTARTLFDNR" "~"
if joinKuerzel = MGZIFT
searchResult := searchResult "LEFT JOIN BUERO.IFT_INFOTAB ON IFT_INFOTABID = MGZ_IFT_INFOTABID" "~"
if joinKuerzel = IFTMGZ
searchResult := searchResult "LEFT JOIN BUERO.MGZ_MERKMALSGRUPPEZU ON MGZ_IFT_INFOTABID = IFT_INFOTABID" "~"
if joinKuerzel = LEBIMN
searchResult := searchResult "LEFT JOIN BUERO.IMN_INSTANDHMASSN ON IMN_IMNID = LEB_IMNID" "~"
if joinKuerzel = IMNLEB
searchResult := searchResult "LEFT JOIN BUERO.LEB_LEBENSLAUF ON LEB_IMNID = IMN_IMNID" "~"
if joinKuerzel = DBXINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_IART = DBX_INS_ART AND INS_LANDNR = DBX_INS_LANDNR AND INS_INR = DBX_INS_NR" "~"
if joinKuerzel = INSDBX
searchResult := searchResult "LEFT JOIN BUERO.DBX_FINONDATABOX ON DBX_INS_ART = INS_IART AND DBX_INS_LANDNR = INS_LANDNR AND DBX_INS_NR = INS_INR" "~"
if joinKuerzel = DIZINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_IART = DIZ_IART AND INS_LANDNR = DIZ_LANDNR AND INS_INR = DIZ_INR" "~"
if joinKuerzel = INSDIZ
searchResult := searchResult "LEFT JOIN BUERO.DIZ_DOCSINSTZU ON DIZ_IART = INS_IART AND DIZ_LANDNR = INS_LANDNR AND DIZ_INR = INS_INR" "~"
if joinKuerzel = KUZINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_IART = KUZ_IART AND INS_LANDNR = KUZ_LANDNR AND INS_INR = KUZ_INR" "~"
if joinKuerzel = INSKUZ
searchResult := searchResult "LEFT JOIN BUERO.KUZ_KUNDENZAHLUNGEN ON KUZ_IART = INS_IART AND KUZ_LANDNR = INS_LANDNR AND KUZ_INR = INS_INR" "~"
if joinKuerzel = SNRIOB
searchResult := searchResult "LEFT JOIN IOB_INSTANDHOBJEKT.IOB_INSTANDHOBJEKT ON IOB_IOBID = SNR_IOBID" "~"
if joinKuerzel = IOBSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_IOBID = IOB_IOBID" "~"
if joinKuerzel = AVEIOB
searchResult := searchResult "LEFT JOIN IOB_INSTANDHOBJEKT.IOB_INSTANDHOBJEKT ON IOB_IOBID = AVE_EFKESD_IOBID" "~"
if joinKuerzel = IOBAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_EFKESD_IOBID = IOB_IOBID" "~"
if joinKuerzel = ONMITB
searchResult := searchResult "LEFT JOIN ITB_INFOTABBEWERTUNG.ITB_INFOTABBEWERTUNG ON ITB_INFOTABLFDNR = ONM_INFOTABID AND ITB_BEWERTUNGSNR = ONM_BEWERTUNGSNR" "~"
if joinKuerzel = ITBONM
searchResult := searchResult "LEFT JOIN HRS.ONM_ONLINEBEW_MERKMALE ON ONM_INFOTABID = ITB_INFOTABLFDNR AND ONM_BEWERTUNGSNR = ITB_BEWERTUNGSNR" "~"
if joinKuerzel = IGZIVG
searchResult := searchResult "LEFT JOIN BMD.IVG_INTERVALLGRUPPE ON IVG_INTERVALLGRPID = IGZ_INTERVALLGRPID" "~"
if joinKuerzel = IVGIGZ
searchResult := searchResult "LEFT JOIN BMD.IGZ_INTERVALLGRUPPEZU ON IGZ_INTERVALLGRPID = IVG_INTERVALLGRPID" "~"
if joinKuerzel = ZWPIWV
searchResult := searchResult "LEFT JOIN IWV_INTRASTATWV.IWV_INTRASTATWV ON IWV_LANDNR = ZWP_LANDNR AND IWV_INTRASTATWVNR = ZWP_INTRASTATWVNR AND IWV_INTRASTATABDATUM = ZWP_INTRASTATABDATUM" "~"
if joinKuerzel = IWVZWP
searchResult := searchResult "LEFT JOIN WWS.ZWP_ZUORDNUNGWVPOS ON ZWP_LANDNR = IWV_LANDNR AND ZWP_INTRASTATWVNR = IWV_INTRASTATWVNR AND ZWP_INTRASTATABDATUM = IWV_INTRASTATABDATUM" "~"
if joinKuerzel = KRAKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_KASSENPOSID = KRA_KASSENPOSID" "~"
if joinKuerzel = KAPKRA
searchResult := searchResult "LEFT JOIN WWS.KRA_KASSENRABATT ON KRA_KASSENPOSID = KAP_KASSENPOSID" "~"
if joinKuerzel = KAPKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_KASSENPOSID = KAP_FOLGE_KASSENPOSID" "~"
if joinKuerzel = KAPKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_FOLGE_KASSENPOSID = KAP_KASSENPOSID" "~"
if joinKuerzel = AIVKAT
searchResult := searchResult "LEFT JOIN KAT_KATEGORIE.KAT_KATEGORIE ON KAT_KATEGORIEKZ = AIV_PRO_KATEGORIEKZ AND KAT_KATEGORIELFDNR = AIV_PRO_KATEGORIELFDNR" "~"
if joinKuerzel = KATAIV
searchResult := searchResult "LEFT JOIN WWS.AIV_INTERVALL_ARBEITSPLAN ON AIV_PRO_KATEGORIEKZ = KAT_KATEGORIEKZ AND AIV_PRO_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = AKZKAT
searchResult := searchResult "LEFT JOIN KAT_KATEGORIE.KAT_KATEGORIE ON KAT_KATEGORIEKZ = AKZ_KATEGORIEKZ AND KAT_KATEGORIELFDNR = AKZ_KATEGORIELFDNR" "~"
if joinKuerzel = KATAKZ
searchResult := searchResult "LEFT JOIN WWS.AKZ_ARTIKELKUNDENZU ON AKZ_KATEGORIEKZ = KAT_KATEGORIEKZ AND AKZ_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = POSKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_KATEGORIEKZ = POS_POSTART_KATEGORIEKZ AND KAT_KATEGORIELFDNR = POS_POSTART_KATEGORIELFDNR" "~"
if joinKuerzel = KATPOS
searchResult := searchResult "LEFT JOIN BUERO.POS_POST ON POS_POSTART_KATEGORIEKZ = KAT_KATEGORIEKZ AND POS_POSTART_KATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = KAZKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_KATEGORIEKZ = KAZ_UKATEGORIEKZ AND KAT_KATEGORIELFDNR = KAZ_UKATEGORIELFDNR" "~"
if joinKuerzel = KATKAZ
searchResult := searchResult "LEFT JOIN BUERO.KAZ_KATEGORIEZU ON KAZ_UKATEGORIEKZ = KAT_KATEGORIEKZ AND KAZ_UKATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = STTKAT
searchResult := searchResult "LEFT JOIN KAT_KATEGORIE.KAT_KATEGORIE ON KAT_KATEGORIEKZ = STT_KAT_UKATEGORIEKZ AND KAT_KATEGORIELFDNR = STT_KAT_UKATEGORIELFDNR" "~"
if joinKuerzel = KATSTT
searchResult := searchResult "LEFT JOIN PRODUKTION.STT_STAKE_TYP ON STT_KAT_UKATEGORIEKZ = KAT_KATEGORIEKZ AND STT_KAT_UKATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = WFNKAT
searchResult := searchResult "LEFT JOIN BUERO.KAT_KATEGORIE ON KAT_KATEGORIEKZ = WFN_UNTERKATEGORIEKZ AND KAT_KATEGORIELFDNR = WFN_UNTERKATEGORIELFDNR" "~"
if joinKuerzel = KATWFN
searchResult := searchResult "LEFT JOIN BUERO.WFN_WORKFLOWACTION ON WFN_UNTERKATEGORIEKZ = KAT_KATEGORIEKZ AND WFN_UNTERKATEGORIELFDNR = KAT_KATEGORIELFDNR" "~"
if joinKuerzel = WIIKAZ
searchResult := searchResult "LEFT JOIN KAZ_KATEGORIEZU.KAZ_KATEGORIEZU ON KAZ_HKATEGORIEKZ = WII_PAKETKZ AND KAZ_HKATEGORIELFDNR = WII_PAKETLFDNR AND KAZ_UKATEGORIEKZ = WII_MODULKZ AND KAZ_UKATEGORIELFDNR = WII_MODULLFDNR" "~"
if joinKuerzel = KAZWII
searchResult := searchResult "LEFT JOIN BMDENTW.WII_WEITERE_INFO_ITEM ON WII_PAKETKZ = KAZ_HKATEGORIEKZ AND WII_PAKETLFDNR = KAZ_HKATEGORIELFDNR AND WII_MODULKZ = KAZ_UKATEGORIEKZ AND WII_MODULLFDNR = KAZ_UKATEGORIELFDNR" "~"
if joinKuerzel = AMDKBA
searchResult := searchResult "LEFT JOIN KORE.KBA_KOREBAUM ON KBA_FIRMENNR = AMD_FIRMENNR AND KBA_KORENR = AMD_KORENR AND KBA_KOREBAUMNR = AMD_KOREBAUMNR" "~"
if joinKuerzel = KBAAMD
searchResult := searchResult "LEFT JOIN KORE.AMD_MODELLDETAIL ON AMD_FIRMENNR = KBA_FIRMENNR AND AMD_KORENR = KBA_KORENR AND AMD_KOREBAUMNR = KBA_KOREBAUMNR" "~"
if joinKuerzel = KZEKBA
searchResult := searchResult "LEFT JOIN KORE.KBA_KOREBAUM ON KBA_FIRMENNR = KZE_FWK_FIRMENNR AND KBA_KORENR = KZE_KORENR AND KBA_KOREBAUMNR = KZE_KOREBAUMNR" "~"
if joinKuerzel = KBAKZE
searchResult := searchResult "LEFT JOIN KORE.KZE_ZUSCHLAGEINGABE ON KZE_FWK_FIRMENNR = KBA_FIRMENNR AND KZE_KORENR = KBA_KORENR AND KZE_KOREBAUMNR = KBA_KOREBAUMNR" "~"
if joinKuerzel = BURKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = BUR_KLI_FIRMENNR AND KLI_KLID = BUR_KLI_KLID" "~"
if joinKuerzel = KLIBUR
searchResult := searchResult "LEFT JOIN BUERO.BUR_BMDCOMUPLOADTREE ON BUR_KLI_FIRMENNR = KLI_FIRMENNR AND BUR_KLI_KLID = KLI_KLID" "~"
if joinKuerzel = EVNKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = EVN_RECHNUNG_FIRMENNR AND KLI_KLID = EVN_RECHNUNG_KLID" "~"
if joinKuerzel = KLIEVN
searchResult := searchResult "LEFT JOIN BUERO.EVN_TEILNEHMER ON EVN_RECHNUNG_FIRMENNR = KLI_FIRMENNR AND EVN_RECHNUNG_KLID = KLI_KLID" "~"
if joinKuerzel = LFSKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = LFS_LIEFEREMPF_FIRMENNR AND KLI_KLID = LFS_LIEFEREMPF_KLID" "~"
if joinKuerzel = KLILFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_LIEFEREMPF_FIRMENNR = KLI_FIRMENNR AND LFS_LIEFEREMPF_KLID = KLI_KLID" "~"
if joinKuerzel = LTKKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = LTK_KLI_FIRMENNR AND KLI_KLID = LTK_KLID" "~"
if joinKuerzel = KLILTK
searchResult := searchResult "LEFT JOIN ZLE.LTK_LEISTTAUSCHKOPF ON LTK_KLI_FIRMENNR = KLI_FIRMENNR AND LTK_KLID = KLI_KLID" "~"
if joinKuerzel = PEKKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = PEK_KLID_FIRMENNR AND KLI_KLID = PEK_KLID" "~"
if joinKuerzel = KLIPEK
searchResult := searchResult "LEFT JOIN FIBU.PEK_PERSONENKONTO ON PEK_KLID_FIRMENNR = KLI_FIRMENNR AND PEK_KLID = KLI_KLID" "~"
if joinKuerzel = PVZKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = PVZ_KLI_FIRMENNR AND KLI_KLID = PVZ_KLID" "~"
if joinKuerzel = KLIPVZ
searchResult := searchResult "LEFT JOIN WWS.PVZ_PROVISIONTABZU ON PVZ_KLI_FIRMENNR = KLI_FIRMENNR AND PVZ_KLID = KLI_KLID" "~"
if joinKuerzel = REDKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = RED_KLI_FIRMENNR AND KLI_KLID = RED_KLI_KLID" "~"
if joinKuerzel = KLIRED
searchResult := searchResult "LEFT JOIN HRS.RED_REISEDETAIL ON RED_KLI_FIRMENNR = KLI_FIRMENNR AND RED_KLI_KLID = KLI_KLID" "~"
if joinKuerzel = REIKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = REI_KLI_FIRMENNR AND KLI_KLID = REI_KLI_KLID" "~"
if joinKuerzel = KLIREI
searchResult := searchResult "LEFT JOIN HRS.REI_REISESTAMM ON REI_KLI_FIRMENNR = KLI_FIRMENNR AND REI_KLI_KLID = KLI_KLID" "~"
if joinKuerzel = VAKKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = VAK_KLI_FIRMENNR AND KLI_KLID = VAK_KLI_KLID" "~"
if joinKuerzel = KLIVAK
searchResult := searchResult "LEFT JOIN BUERO.VAK_VERANSTALTUNGSKOPF ON VAK_KLI_FIRMENNR = KLI_FIRMENNR AND VAK_KLI_KLID = KLI_KLID" "~"
if joinKuerzel = VATKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = VAT_RECHEMPF_FIRMENNR AND KLI_KLID = VAT_RECHEMPF_KLID" "~"
if joinKuerzel = KLIVAT
searchResult := searchResult "LEFT JOIN BUERO.VAT_TEILNAHME ON VAT_RECHEMPF_FIRMENNR = KLI_FIRMENNR AND VAT_RECHEMPF_KLID = KLI_KLID" "~"
if joinKuerzel = WAKKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = WAK_LIEFERANT_FIRMENNR AND KLI_KLID = WAK_LIEFERANT_KLID" "~"
if joinKuerzel = KLIWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_LIEFERANT_FIRMENNR = KLI_FIRMENNR AND WAK_LIEFERANT_KLID = KLI_KLID" "~"
if joinKuerzel = PCKKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = PCK_AUFTRAGNEHMER_FIRMENNR AND KLI_KLID = PCK_AUFTRAGNEHMER_KLID" "~"
if joinKuerzel = KLIPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_AUFTRAGNEHMER_FIRMENNR = KLI_FIRMENNR AND PCK_AUFTRAGNEHMER_KLID = KLI_KLID" "~"
if joinKuerzel = BPKKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = BPK_RECHNUNGEMPF_FIRMENNR AND KLI_KLID = BPK_RECHNUNGEMPF_KLID" "~"
if joinKuerzel = KLIBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_RECHNUNGEMPF_FIRMENNR = KLI_FIRMENNR AND BPK_RECHNUNGEMPF_KLID = KLI_KLID" "~"
if joinKuerzel = EFKKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = EFK_RECHNUNGEMPF_FIRMENNR AND KLI_KLID = EFK_RECHNUNGEMPF_KLID" "~"
if joinKuerzel = KLIEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_RECHNUNGEMPF_FIRMENNR = KLI_FIRMENNR AND EFK_RECHNUNGEMPF_KLID = KLI_KLID" "~"
if joinKuerzel = MDEKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = MDE_LIEFEREMPF_FIRMENNR AND KLI_KLID = MDE_LIEFEREMPF_KLID" "~"
if joinKuerzel = KLIMDE
searchResult := searchResult "LEFT JOIN WWS.MDE_MOBILEDATENERFASSUNG ON MDE_LIEFEREMPF_FIRMENNR = KLI_FIRMENNR AND MDE_LIEFEREMPF_KLID = KLI_KLID" "~"
if joinKuerzel = VJSKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = VJS_LIEFEREMPF_FIRMENNR AND KLI_KLID = VJS_LIEFEREMPF_KLID" "~"
if joinKuerzel = KLIVJS
searchResult := searchResult "LEFT JOIN WWS.VJS_VA_VERLEIHJOURNALSUM ON VJS_LIEFEREMPF_FIRMENNR = KLI_FIRMENNR AND VJS_LIEFEREMPF_KLID = KLI_KLID" "~"
if joinKuerzel = WADKLI
searchResult := searchResult "LEFT JOIN KLI_KUNDE_LIEFERANT.KLI_KUNDE_LIEFERANT ON KLI_FIRMENNR = WAD_BONUSKUNDE_FIRMENNR AND KLI_KLID = WAD_BONUSKUNDE_KLID" "~"
if joinKuerzel = KLIWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_BONUSKUNDE_FIRMENNR = KLI_FIRMENNR AND WAD_BONUSKUNDE_KLID = KLI_KLID" "~"
if joinKuerzel = KPRKMG
searchResult := searchResult "LEFT JOIN KORE.KMG_KOREMENGE ON KMG_FIRMENNR = KPR_FIRMENNR AND KMG_KORENR = KPR_KORENR AND KMG_KOREMENGENNR = KPR_KOREMENGENNR" "~"
if joinKuerzel = KMGKPR
searchResult := searchResult "LEFT JOIN KORE.KPR_PLANPREISE ON KPR_FIRMENNR = KMG_FIRMENNR AND KPR_KORENR = KMG_KORENR AND KPR_KOREMENGENNR = KMG_KOREMENGENNR" "~"
if joinKuerzel = PMXKMG
searchResult := searchResult "LEFT JOIN KORE.KMG_KOREMENGE ON KMG_FIRMENNR = PMX_FIRMENNR AND KMG_KORENR = PMX_KORENR AND KMG_KOREMENGENNR = PMX_KOREMENGENNR" "~"
if joinKuerzel = KMGPMX
searchResult := searchResult "LEFT JOIN KORE.PMX_PLANMENGENEXT ON PMX_FIRMENNR = KMG_FIRMENNR AND PMX_KORENR = KMG_KORENR AND PMX_KOREMENGENNR = KMG_KOREMENGENNR" "~"
if joinKuerzel = UMPKMG
searchResult := searchResult "LEFT JOIN KORE.KMG_KOREMENGE ON KMG_FIRMENNR = UMP_FIRMENNR AND KMG_KORENR = UMP_KORENR AND KMG_KOREMENGENNR = UMP_KOREMENGENNR" "~"
if joinKuerzel = KMGUMP
searchResult := searchResult "LEFT JOIN KORE.UMP_UMLAGEPLANMENGEN ON UMP_FIRMENNR = KMG_FIRMENNR AND UMP_KORENR = KMG_KORENR AND UMP_KOREMENGENNR = KMG_KOREMENGENNR" "~"
if joinKuerzel = ERZKOD
searchResult := searchResult "LEFT JOIN KOD_KONDITIONEN.KOD_KONDITIONEN ON KOD_KONDNR = ERZ_ZAHLKONDNR" "~"
if joinKuerzel = KODERZ
searchResult := searchResult "LEFT JOIN WWS.ERZ_ERFASSKENNZ ON ERZ_ZAHLKONDNR = KOD_KONDNR" "~"
if joinKuerzel = PEKKOD
searchResult := searchResult "LEFT JOIN KOD_KONDITIONEN.KOD_KONDITIONEN ON KOD_KONDNR = PEK_KOD_KUN_KONDNR" "~"
if joinKuerzel = KODPEK
searchResult := searchResult "LEFT JOIN FIBU.PEK_PERSONENKONTO ON PEK_KOD_KUN_KONDNR = KOD_KONDNR" "~"
if joinKuerzel = WAPKOK
searchResult := searchResult "LEFT JOIN KOK_KOMMISSIONIERUNG_KOPF.KOK_KOMMISSIONIERUNG_KOPF ON KOK_FIRMENNR = WAP_KOK_FIRMENNR AND KOK_KOMID = WAP_KOK_KOMID" "~"
if joinKuerzel = KOKWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_KOK_FIRMENNR = KOK_FIRMENNR AND WAP_KOK_KOMID = KOK_KOMID" "~"
if joinKuerzel = ELVKOP
searchResult := searchResult "LEFT JOIN KOP_KOMMISSIONIERUNG_POS.KOP_KOMMISSIONIERUNG_POS ON KOP_FIRMENNR = ELV_FIRMENNR AND KOP_KOMID = ELV_KOP_KOMID AND KOP_KOPLFDNR = ELV_KOP_KOPLFDNR" "~"
if joinKuerzel = KOPELV
searchResult := searchResult "LEFT JOIN WWS.ELV_ERFASSPOSLIEFERVOR ON ELV_FIRMENNR = KOP_FIRMENNR AND ELV_KOP_KOMID = KOP_KOMID AND ELV_KOP_KOPLFDNR = KOP_KOPLFDNR" "~"
if joinKuerzel = KMPKPK
searchResult := searchResult "LEFT JOIN KORE.KPK_PLANKENNZAHLEN ON KPK_PLANKZID = KMP_ALG_ABHAENGIG_ID" "~"
if joinKuerzel = KPKKMP
searchResult := searchResult "LEFT JOIN KORE.KMP_PLANMENGEN ON KMP_ALG_ABHAENGIG_ID = KPK_PLANKZID" "~"
if joinKuerzel = BKUKRI
searchResult := searchResult "LEFT JOIN BUERO.KRI_KRITERIUM ON KRI_KRITERIENNR = BKU_KRITERIENNR" "~"
if joinKuerzel = KRIBKU
searchResult := searchResult "LEFT JOIN BUERO.BKU_BEFRAGUNGKRITERIUMZU ON BKU_KRITERIENNR = KRI_KRITERIENNR" "~"
if joinKuerzel = LAWKRM
searchResult := searchResult "LEFT JOIN HRS.KRM_KONTENREFERENZTAB ON KRM_DATENBESTANDNR = LAW_DBSNR AND KRM_KONTOREFNR = LAW_KONTOREFNR" "~"
if joinKuerzel = KRMLAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_DBSNR = KRM_DATENBESTANDNR AND LAW_KONTOREFNR = KRM_KONTOREFNR" "~"
if joinKuerzel = ERSKSK
searchResult := searchResult "LEFT JOIN KSK_KORESCHLUESSELKOPF.KSK_KORESCHLUESSELKOPF ON KSK_FIRMENNR = ERS_KORE_FIRMENNR AND KSK_KORENR = ERS_KORENR AND KSK_KENNZ_UEB_UML = ERS_KENNZ_UEB_UML AND KSK_SCHLUESSEL = ERS_SCHLUESSEL AND KSK_SCHLUESSELART_ISTPLAN = ERS_SCHLUESSELART_ISTPLAN AND KSK_SCHLUESSELART_GRENZ = ERS_SCHLUESSELART_GRENZ AND KSK_SCHLUESSELMODELLNR = ERS_SCHLUESSELMODELLNR" "~"
if joinKuerzel = KSKERS
searchResult := searchResult "LEFT JOIN FIBU.ERS_KOST ON ERS_KORE_FIRMENNR = KSK_FIRMENNR AND ERS_KORENR = KSK_KORENR AND ERS_KENNZ_UEB_UML = KSK_KENNZ_UEB_UML AND ERS_SCHLUESSEL = KSK_SCHLUESSEL AND ERS_SCHLUESSELART_ISTPLAN = KSK_SCHLUESSELART_ISTPLAN AND ERS_SCHLUESSELART_GRENZ = KSK_SCHLUESSELART_GRENZ AND ERS_SCHLUESSELMODELLNR = KSK_SCHLUESSELMODELLNR" "~"
if joinKuerzel = KSCKSK
searchResult := searchResult "LEFT JOIN KORE.KSK_KORESCHLUESSELKOPF ON KSK_FIRMENNR = KSC_FIRMENNR AND KSK_KORENR = KSC_KORENR AND KSK_KENNZ_UEB_UML = KSC_KENNZ_UEB_UML AND KSK_SCHLUESSEL = KSC_SCHLUESSEL AND KSK_SCHLUESSELART_ISTPLAN = KSC_SCHLUESSELART_ISTPLAN AND KSK_SCHLUESSELART_GRENZ = KSC_SCHLUESSELART_GRENZ AND KSK_SCHLUESSELMODELLNR = KSC_SCHLUESSELMODELLNR" "~"
if joinKuerzel = KSKKSC
searchResult := searchResult "LEFT JOIN KORE.KSC_KORESCHLUESSEL ON KSC_FIRMENNR = KSK_FIRMENNR AND KSC_KORENR = KSK_KORENR AND KSC_KENNZ_UEB_UML = KSK_KENNZ_UEB_UML AND KSC_SCHLUESSEL = KSK_SCHLUESSEL AND KSC_SCHLUESSELART_ISTPLAN = KSK_SCHLUESSELART_ISTPLAN AND KSC_SCHLUESSELART_GRENZ = KSK_SCHLUESSELART_GRENZ AND KSC_SCHLUESSELMODELLNR = KSK_SCHLUESSELMODELLNR" "~"
if joinKuerzel = KKVKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = KKV_FIRMENNR AND KST_KORENR = KKV_KORENR AND KST_KOSTENTYP = KKV_KOSTENTYP AND KST_KOSTENSTAMMNR = KKV_KOSTENSTAMMNR AND KST_KNOTENREFNR = KKV_KNOTENREFNR" "~"
if joinKuerzel = KSTKKV
searchResult := searchResult "LEFT JOIN KORE.KKV_KSTKONTOVARIATOR ON KKV_FIRMENNR = KST_FIRMENNR AND KKV_KORENR = KST_KORENR AND KKV_KOSTENTYP = KST_KOSTENTYP AND KKV_KOSTENSTAMMNR = KST_KOSTENSTAMMNR AND KKV_KNOTENREFNR = KST_KNOTENREFNR" "~"
if joinKuerzel = KSEKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = KSE_FIRMENNR AND KST_KORENR = KSE_KORENR AND KST_KOSTENTYP = KSE_KOTR_KOSTENTYP AND KST_KOSTENSTAMMNR = KSE_KOTR_KOSTENSTAMMNR AND KST_KNOTENREFNR = KSE_KOTR_KNOTENREFNR" "~"
if joinKuerzel = KSTKSE
searchResult := searchResult "LEFT JOIN KORE.KSE_KORESCHLUESSELEINGABE ON KSE_FIRMENNR = KST_FIRMENNR AND KSE_KORENR = KST_KORENR AND KSE_KOTR_KOSTENTYP = KST_KOSTENTYP AND KSE_KOTR_KOSTENSTAMMNR = KST_KOSTENSTAMMNR AND KSE_KOTR_KNOTENREFNR = KST_KNOTENREFNR" "~"
if joinKuerzel = KSEKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_FIRMENNR = KSE_FIRMENNR AND KST_KORENR = KSE_KORENR AND KST_KOSTENTYP = KSE_ABT_KOSTENTYP AND KST_KOSTENSTAMMNR = KSE_ABT_KOSTENSTAMMNR AND KST_KNOTENREFNR = KSE_ABT_KNOTENREFNR" "~"
if joinKuerzel = KSTKSE
searchResult := searchResult "LEFT JOIN KORE.KSE_KORESCHLUESSELEINGABE ON KSE_FIRMENNR = KST_FIRMENNR AND KSE_KORENR = KST_KORENR AND KSE_ABT_KOSTENTYP = KST_KOSTENTYP AND KSE_ABT_KOSTENSTAMMNR = KST_KOSTENSTAMMNR AND KSE_ABT_KNOTENREFNR = KST_KNOTENREFNR" "~"
if joinKuerzel = BUMKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = BUM_FIRMENNR AND KTO_FIBUNR = BUM_FIBUNR AND KTO_KONTONR = BUM_KONTONR_SOLL" "~"
if joinKuerzel = KTOBUM
searchResult := searchResult "LEFT JOIN BIL.BUM_BILUMBUCHUNG ON BUM_FIRMENNR = KTO_FIRMENNR AND BUM_FIBUNR = KTO_FIBUNR AND BUM_KONTONR_SOLL = KTO_KONTONR" "~"
if joinKuerzel = ERBKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = ERB_FIRMENNR AND KTO_FIBUNR = ERB_FIBUNR AND KTO_KONTONR = ERB_LIEFKONTO" "~"
if joinKuerzel = KTOERB
searchResult := searchResult "LEFT JOIN FIBU.ERB_BUCHUNG ON ERB_FIRMENNR = KTO_FIRMENNR AND ERB_FIBUNR = KTO_FIBUNR AND ERB_LIEFKONTO = KTO_KONTONR" "~"
if joinKuerzel = ERKKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = ERK_FIRMENNR AND KTO_FIBUNR = ERK_FIBUNR AND KTO_KONTONR = ERK_ERFASSKONTONR" "~"
if joinKuerzel = KTOERK
searchResult := searchResult "LEFT JOIN FIBU.ERK_KONTO ON ERK_FIRMENNR = KTO_FIRMENNR AND ERK_FIBUNR = KTO_FIBUNR AND ERK_ERFASSKONTONR = KTO_KONTONR" "~"
if joinKuerzel = EXTKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = EXT_FIRMENNR AND KTO_FIBUNR = EXT_FIBUNR AND KTO_KONTONR = EXT_KONTONR" "~"
if joinKuerzel = KTOEXT
searchResult := searchResult "LEFT JOIN FIBU.EXT_EXTJOURNAL ON EXT_FIRMENNR = KTO_FIRMENNR AND EXT_FIBUNR = KTO_FIBUNR AND EXT_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = GLZKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = GLZ_FIRMENNR AND KTO_FIBUNR = GLZ_FIBUNR AND KTO_KONTONR = GLZ_KONTONR" "~"
if joinKuerzel = KTOGLZ
searchResult := searchResult "LEFT JOIN BIL.GLZ_GLIEDERUNGSZAHL ON GLZ_FIRMENNR = KTO_FIRMENNR AND GLZ_FIBUNR = KTO_FIBUNR AND GLZ_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = KMIKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = KMI_FIRMENNR AND KTO_FIBUNR = KMI_FIBUNR AND KTO_KONTONR = KMI_KONTONR" "~"
if joinKuerzel = KTOKMI
searchResult := searchResult "LEFT JOIN KORE.KMI_ISTMENGEN ON KMI_FIRMENNR = KTO_FIRMENNR AND KMI_FIBUNR = KTO_FIBUNR AND KMI_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = KURKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = KUR_FIRMENNR AND KTO_FIBUNR = KUR_FIBUNR AND KTO_KONTONR = KUR_KONTONR" "~"
if joinKuerzel = KTOKUR
searchResult := searchResult "LEFT JOIN KORE.KUR_UMLAGEREIHENFOLGE ON KUR_FIRMENNR = KTO_FIRMENNR AND KUR_FIBUNR = KTO_FIBUNR AND KUR_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = RNGKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = RNG_FIBU_FIRMENNR AND KTO_FIBUNR = RNG_FIBUNR AND KTO_KONTONR = RNG_BRUTTOABG_KONTONR" "~"
if joinKuerzel = KTORNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIBU_FIRMENNR = KTO_FIRMENNR AND RNG_FIBUNR = KTO_FIBUNR AND RNG_BRUTTOABG_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = SPWKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = SPW_FIRMENNR AND KTO_FIBUNR = SPW_FIBUNR AND KTO_KONTONR = SPW_KONTONR" "~"
if joinKuerzel = KTOSPW
searchResult := searchResult "LEFT JOIN BIL.SPW_SPIEGELWERTE ON SPW_FIRMENNR = KTO_FIRMENNR AND SPW_FIBUNR = KTO_FIBUNR AND SPW_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = TDKKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = TDK_FIRMENNR AND KTO_FIBUNR = TDK_FIBUNR AND KTO_KONTONR = TDK_KONTONR" "~"
if joinKuerzel = KTOTDK
searchResult := searchResult "LEFT JOIN WWS.TDK_WWSDATENKONTR_TMP ON TDK_FIRMENNR = KTO_FIRMENNR AND TDK_FIBUNR = KTO_FIBUNR AND TDK_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = VKPKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = VKP_FIRMENNR AND KTO_FIBUNR = VKP_FIBUNR AND KTO_KONTONR = VKP_KUERZUNG_KTO_KLASSE2" "~"
if joinKuerzel = KTOVKP
searchResult := searchResult "LEFT JOIN FIBU.VKP_VSTKPROJEKT ON VKP_FIRMENNR = KTO_FIRMENNR AND VKP_FIBUNR = KTO_FIBUNR AND VKP_KUERZUNG_KTO_KLASSE2 = KTO_KONTONR" "~"
if joinKuerzel = VKPKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = VKP_FIRMENNR AND KTO_FIBUNR = VKP_FIBUNR AND KTO_KONTONR = VKP_KUERZUNG_KTO_KLASSE7" "~"
if joinKuerzel = KTOVKP
searchResult := searchResult "LEFT JOIN FIBU.VKP_VSTKPROJEKT ON VKP_FIRMENNR = KTO_FIRMENNR AND VKP_FIBUNR = KTO_FIBUNR AND VKP_KUERZUNG_KTO_KLASSE7 = KTO_KONTONR" "~"
if joinKuerzel = VKPKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = VKP_FIRMENNR AND KTO_FIBUNR = VKP_FIBUNR AND KTO_KONTONR = VKP_KUERZUNG_KTO_KLASSE8" "~"
if joinKuerzel = KTOVKP
searchResult := searchResult "LEFT JOIN FIBU.VKP_VSTKPROJEKT ON VKP_FIRMENNR = KTO_FIRMENNR AND VKP_FIBUNR = KTO_FIBUNR AND VKP_KUERZUNG_KTO_KLASSE8 = KTO_KONTONR" "~"
if joinKuerzel = VSRKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = VSR_FIRMENNR AND KTO_FIBUNR = VSR_FIBUNR AND KTO_KONTONR = VSR_KONTONR" "~"
if joinKuerzel = KTOVSR
searchResult := searchResult "LEFT JOIN FIBU.VSR_VSTERSTATTUNG ON VSR_FIRMENNR = KTO_FIRMENNR AND VSR_FIBUNR = KTO_FIBUNR AND VSR_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = WEBKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = WEB_FWF_FIRMENNR AND KTO_FIBUNR = WEB_FIBUNR AND KTO_KONTONR = WEB_AUFWANDSKONTO" "~"
if joinKuerzel = KTOWEB
searchResult := searchResult "LEFT JOIN WWS.WEB_WARENEINSATZBUCH ON WEB_FWF_FIRMENNR = KTO_FIRMENNR AND WEB_FIBUNR = KTO_FIBUNR AND WEB_AUFWANDSKONTO = KTO_KONTONR" "~"
if joinKuerzel = ZIKKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = ZIK_FIRMENNR AND KTO_FIBUNR = ZIK_FIBUNR AND KTO_KONTONR = ZIK_KONTONR" "~"
if joinKuerzel = KTOZIK
searchResult := searchResult "LEFT JOIN FIBU.ZIK_KONTENZINSKREIS ON ZIK_FIRMENNR = KTO_FIRMENNR AND ZIK_FIBUNR = KTO_FIBUNR AND ZIK_KONTONR = KTO_KONTONR" "~"
if joinKuerzel = BEZKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = BEZ_FIRMENNR AND KTO_FIBUNR = BEZ_FIBUNR AND KTO_KONTONR = BEZ_GEGENVERRKONTO" "~"
if joinKuerzel = KTOBEZ
searchResult := searchResult "LEFT JOIN FIBU.BEZ_BANKEINZUG ON BEZ_FIRMENNR = KTO_FIRMENNR AND BEZ_FIBUNR = KTO_FIBUNR AND BEZ_GEGENVERRKONTO = KTO_KONTONR" "~"
if joinKuerzel = SRBKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = SRB_FIBUFIRMENNR AND KTO_FIBUNR = SRB_FIBUNR AND KTO_KONTONR = SRB_GEGENKONTONR" "~"
if joinKuerzel = KTOSRB
searchResult := searchResult "LEFT JOIN BIL.SRB_STEUERBUCHUNGEN ON SRB_FIBUFIRMENNR = KTO_FIRMENNR AND SRB_FIBUNR = KTO_FIBUNR AND SRB_GEGENKONTONR = KTO_KONTONR" "~"
if joinKuerzel = UDPKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = UDP_FIRMENNR AND KTO_FIBUNR = UDP_FIBUNR AND KTO_KONTONR = UDP_ORIGINALKONTONR" "~"
if joinKuerzel = KTOUDP
searchResult := searchResult "LEFT JOIN KORE.UDP_UMLAGEPLANWERTE ON UDP_FIRMENNR = KTO_FIRMENNR AND UDP_FIBUNR = KTO_FIBUNR AND UDP_ORIGINALKONTONR = KTO_KONTONR" "~"
if joinKuerzel = UMIKTO
searchResult := searchResult "LEFT JOIN KTO_KONTO.KTO_KONTO ON KTO_FIRMENNR = UMI_FIRMENNR AND KTO_FIBUNR = UMI_FIBUNR AND KTO_KONTONR = UMI_ORIGINALKONTONR" "~"
if joinKuerzel = KTOUMI
searchResult := searchResult "LEFT JOIN KORE.UMI_UMLAGEISTMENGE ON UMI_FIRMENNR = KTO_FIRMENNR AND UMI_FIBUNR = KTO_FIBUNR AND UMI_ORIGINALKONTONR = KTO_KONTONR" "~"
if joinKuerzel = VEBKTO
searchResult := searchResult "LEFT JOIN FIBU.KTO_KONTO ON KTO_FIRMENNR = VEB_FIRMENNR AND KTO_FIBUNR = VEB_FIBUNR AND KTO_KONTONR = VEB_GEGENKONTONR" "~"
if joinKuerzel = KTOVEB
searchResult := searchResult "LEFT JOIN FIBU.VEB_VORERFASSBUCHUNG ON VEB_FIRMENNR = KTO_FIRMENNR AND VEB_FIBUNR = KTO_FIBUNR AND VEB_GEGENKONTONR = KTO_KONTONR" "~"
if joinKuerzel = KUSKUD
searchResult := searchResult "LEFT JOIN BMD.KUD_KURSDATEI ON KUD_KURSDATEINR = KUS_KURSDATEINR" "~"
if joinKuerzel = KUDKUS
searchResult := searchResult "LEFT JOIN BMD.KUS_KURS ON KUS_KURSDATEINR = KUD_KURSDATEINR" "~"
if joinKuerzel = VPKKUG
searchResult := searchResult "LEFT JOIN KUG_KUNDENGRUPPE.KUG_KUNDENGRUPPE ON KUG_FIRMENNR = VPK_FIRMENNR AND KUG_KUNDENHAUPTGRUPPENNR = VPK_KUNDENHAUPTGRUPPENNR AND KUG_KUNDENUNTERGRUPPENNR = VPK_KUNDENUNTERGRUPPENNR" "~"
if joinKuerzel = KUGVPK
searchResult := searchResult "LEFT JOIN WWS.VPK_VPERMITTLUNG_KUGZUORD ON VPK_FIRMENNR = KUG_FIRMENNR AND VPK_KUNDENHAUPTGRUPPENNR = KUG_KUNDENHAUPTGRUPPENNR AND VPK_KUNDENUNTERGRUPPENNR = KUG_KUNDENUNTERGRUPPENNR" "~"
if joinKuerzel = UDIKUV
searchResult := searchResult "LEFT JOIN KORE.KUV_UMLAGEVARIANTE ON KUV_FIRMENNR = UDI_FIRMENNR AND KUV_KORENR = UDI_KORENR AND KUV_VARIANTE = UDI_VARIANTE" "~"
if joinKuerzel = KUVUDI
searchResult := searchResult "LEFT JOIN KORE.UDI_UMLAGEISTWERTE ON UDI_FIRMENNR = KUV_FIRMENNR AND UDI_KORENR = KUV_KORENR AND UDI_VARIANTE = KUV_VARIANTE" "~"
if joinKuerzel = UMPKUV
searchResult := searchResult "LEFT JOIN KORE.KUV_UMLAGEVARIANTE ON KUV_FIRMENNR = UMP_FIRMENNR AND KUV_KORENR = UMP_KORENR AND KUV_VARIANTE = UMP_VARIANTE" "~"
if joinKuerzel = KUVUMP
searchResult := searchResult "LEFT JOIN KORE.UMP_UMLAGEPLANMENGEN ON UMP_FIRMENNR = KUV_FIRMENNR AND UMP_KORENR = KUV_KORENR AND UMP_VARIANTE = KUV_VARIANTE" "~"
if joinKuerzel = KASKZA
searchResult := searchResult "LEFT JOIN WWS.KZA_KASSENZAHLUNGSARTENZU ON KZA_KZAID = KAS_RETOURGELD_KZAID" "~"
if joinKuerzel = KZAKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_RETOURGELD_KZAID = KZA_KZAID" "~"
if joinKuerzel = BAZLAG
searchResult := searchResult "LEFT JOIN LAG_LAGER.LAG_LAGER ON LAG_FIRMENNR = BAZ_LAG_FIRMENNR AND LAG_LAGERNR = BAZ_LAGERNR" "~"
if joinKuerzel = LAGBAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_LAG_FIRMENNR = LAG_FIRMENNR AND BAZ_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = IVPLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = IVP_FIRMENNR AND LAG_LAGERNR = IVP_LAGERNR" "~"
if joinKuerzel = LAGIVP
searchResult := searchResult "LEFT JOIN WWS.IVP_INVENTURPOSITION ON IVP_FIRMENNR = LAG_FIRMENNR AND IVP_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = KOPLAG
searchResult := searchResult "LEFT JOIN LAG_LAGER.LAG_LAGER ON LAG_FIRMENNR = KOP_LAG_FIRMENNR AND LAG_LAGERNR = KOP_LAGERNR" "~"
if joinKuerzel = LAGKOP
searchResult := searchResult "LEFT JOIN PRODUKTION.KOP_KOMMISSIONIERUNG_POS ON KOP_LAG_FIRMENNR = LAG_FIRMENNR AND KOP_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = LGWLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = LGW_FIRMENNR AND LAG_LAGERNR = LGW_LAGERNR" "~"
if joinKuerzel = LAGLGW
searchResult := searchResult "LEFT JOIN WWS.LGW_LAGERWERTE ON LGW_FIRMENNR = LAG_FIRMENNR AND LGW_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = LMWLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = LMW_FIRMENNR AND LAG_LAGERNR = LMW_LAGERNR" "~"
if joinKuerzel = LAGLMW
searchResult := searchResult "LEFT JOIN WWS.LMW_LAGERMONATSWERTE ON LMW_FIRMENNR = LAG_FIRMENNR AND LMW_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = LUALAG
searchResult := searchResult "LEFT JOIN LAG_LAGER.LAG_LAGER ON LAG_FIRMENNR = LUA_LAG_FIRMENNR AND LAG_LAGERNR = LUA_LAGERNR" "~"
if joinKuerzel = LAGLUA
searchResult := searchResult "LEFT JOIN PRODUKTION.LUA_LAGER_UMS_ANGEFORDERT ON LUA_LAG_FIRMENNR = LAG_FIRMENNR AND LUA_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = LUKLAG
searchResult := searchResult "LEFT JOIN LAG_LAGER.LAG_LAGER ON LAG_FIRMENNR = LUK_SRC_LAG_FIRMENNR AND LAG_LAGERNR = LUK_SRC_LAGERNR" "~"
if joinKuerzel = LAGLUK
searchResult := searchResult "LEFT JOIN PRODUKTION.LUK_LAGER_UMS_KOPF ON LUK_SRC_LAG_FIRMENNR = LAG_FIRMENNR AND LUK_SRC_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = REALAG
searchResult := searchResult "LEFT JOIN LAG_LAGER.LAG_LAGER ON LAG_FIRMENNR = REA_FIRMENNR AND LAG_LAGERNR = REA_LAGERNR" "~"
if joinKuerzel = LAGREA
searchResult := searchResult "LEFT JOIN WWS_NEU.REA_REGAL ON REA_FIRMENNR = LAG_FIRMENNR AND REA_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = TLPLAG
searchResult := searchResult "LEFT JOIN LAG_LAGER.LAG_LAGER ON LAG_FIRMENNR = TLP_SRC_LAG_FIRMENNR AND LAG_LAGERNR = TLP_SRC_LAGERNR" "~"
if joinKuerzel = LAGTLP
searchResult := searchResult "LEFT JOIN PRODUKTION.TLP_BT_LOGISTIK_POSITION ON TLP_SRC_LAG_FIRMENNR = LAG_FIRMENNR AND TLP_SRC_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = WAKLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = WAK_LAG_FIRMENNR AND LAG_LAGERNR = WAK_LAGERNR" "~"
if joinKuerzel = LAGWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_LAG_FIRMENNR = LAG_FIRMENNR AND WAK_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = WENLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = WEN_LAG_FIRMENNR AND LAG_LAGERNR = WEN_LAGERNR" "~"
if joinKuerzel = LAGWEN
searchResult := searchResult "LEFT JOIN WWS.WEN_WARENEINSATZRNP ON WEN_LAG_FIRMENNR = LAG_FIRMENNR AND WEN_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = KOPLAG
searchResult := searchResult "LEFT JOIN LAG_LAGER.LAG_LAGER ON LAG_FIRMENNR = KOP_ZIEL_LAG_FIRMENNR AND LAG_LAGERNR = KOP_ZIEL_LAGERNR" "~"
if joinKuerzel = LAGKOP
searchResult := searchResult "LEFT JOIN PRODUKTION.KOP_KOMMISSIONIERUNG_POS ON KOP_ZIEL_LAG_FIRMENNR = LAG_FIRMENNR AND KOP_ZIEL_LAGERNR = LAG_LAGERNR" "~"
if joinKuerzel = ARLLAG
searchResult := searchResult "LEFT JOIN WWS.LAG_LAGER ON LAG_FIRMENNR = ARL_VORG_LAG_FIRMENNR_EK AND LAG_LAGERNR = ARL_VORG_LAGERNR_EK" "~"
if joinKuerzel = LAGARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_VORG_LAG_FIRMENNR_EK = LAG_FIRMENNR AND ARL_VORG_LAGERNR_EK = LAG_LAGERNR" "~"
if joinKuerzel = WEJLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_FIRMENNR = WEJ_LAJ_FIRMENNR AND LAJ_LAGERJOURNALLFDNR = WEJ_LAGERJOURNALLFDNR" "~"
if joinKuerzel = LAJWEJ
searchResult := searchResult "LEFT JOIN WWS.WEJ_WARENEINSATZLAJ ON WEJ_LAJ_FIRMENNR = LAJ_FIRMENNR AND WEJ_LAGERJOURNALLFDNR = LAJ_LAGERJOURNALLFDNR" "~"
if joinKuerzel = WLZLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_FIRMENNR = WLZ_LAJ_FIRMENNR AND LAJ_LAGERJOURNALLFDNR = WLZ_LAJ_LAGERJOURNALLFDNR" "~"
if joinKuerzel = LAJWLZ
searchResult := searchResult "LEFT JOIN WWS.WLZ_WESLAJ_ZUORDNUNG ON WLZ_LAJ_FIRMENNR = LAJ_FIRMENNR AND WLZ_LAJ_LAGERJOURNALLFDNR = LAJ_LAGERJOURNALLFDNR" "~"
if joinKuerzel = LAJLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_FIRMENNR = LAJ_FIRMENNR AND LAJ_LAGERJOURNALLFDNR = LAJ_STORNO_GS_ORIG_LFDNR" "~"
if joinKuerzel = LAJLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_FIRMENNR = LAJ_FIRMENNR AND LAJ_STORNO_GS_ORIG_LFDNR = LAJ_LAGERJOURNALLFDNR" "~"
if joinKuerzel = ADWLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = ADW_LANDNR" "~"
if joinKuerzel = LANADW
searchResult := searchResult "LEFT JOIN WWS.ADW_ABGABENDETAILWAP ON ADW_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = DBSLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = DBS_LANDNR" "~"
if joinKuerzel = LANDBS
searchResult := searchResult "LEFT JOIN BUERO.DBS_DATENBESTAND ON DBS_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = EQILAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = EQI_URSPR_LANDNR" "~"
if joinKuerzel = LANEQI
searchResult := searchResult "LEFT JOIN WWS.EQI_ERFASSPOSQUELLINFO ON EQI_URSPR_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = ERKLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = ERK_LANDNR" "~"
if joinKuerzel = LANERK
searchResult := searchResult "LEFT JOIN FIBU.ERK_KONTO ON ERK_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = FINLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = FIN_LANDNR" "~"
if joinKuerzel = LANFIN
searchResult := searchResult "LEFT JOIN BUERO.FIN_FINANZAMT ON FIN_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = GEKLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = GEK_LANDNR" "~"
if joinKuerzel = LANGEK
searchResult := searchResult "LEFT JOIN WWS.GEK_GEFAHRENGUTKOPF ON GEK_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = JAKLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = JAK_LANDNR" "~"
if joinKuerzel = LANJAK
searchResult := searchResult "LEFT JOIN BIL.JAK_JABKOPF ON JAK_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = KRDLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = KRD_LANDNR" "~"
if joinKuerzel = LANKRD
searchResult := searchResult "LEFT JOIN HRS.KRD_KONTENREFERENZDETAIL ON KRD_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = PERLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = PER_LANDNR" "~"
if joinKuerzel = LANPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = REGLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = REG_LANDNR" "~"
if joinKuerzel = LANREG
searchResult := searchResult "LEFT JOIN BUERO.REG_REGION ON REG_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = FIRLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = FIR_RECHTSVERSION_LANDNR" "~"
if joinKuerzel = LANFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_RECHTSVERSION_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = WKPLAN
searchResult := searchResult "LEFT JOIN LAN_LAND.LAN_LAND ON LAN_LANDNR = WKP_BEST_LANDNR" "~"
if joinKuerzel = LANWKP
searchResult := searchResult "LEFT JOIN WWS.WKP_WWSKONTENTABPOS ON WKP_BEST_LANDNR = LAN_LANDNR" "~"
if joinKuerzel = ZASLAN
searchResult := searchResult "LEFT JOIN BUERO.LAN_LAND ON LAN_LANDNR = ZAS_AUFTRAGGEBERLANDNR" "~"
if joinKuerzel = LANZAS
searchResult := searchResult "LEFT JOIN BUERO.ZAS_ZAHLSCHEIN ON ZAS_AUFTRAGGEBERLANDNR = LAN_LANDNR" "~"
if joinKuerzel = AVELAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = AVE_LAG_FIRMENNR AND LAP_LAGERNR = AVE_LAG_LAGERNR AND LAP_LAGERPLATZ = AVE_LAP_LAGERPLATZ" "~"
if joinKuerzel = LAPAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_LAG_FIRMENNR = LAP_FIRMENNR AND AVE_LAG_LAGERNR = LAP_LAGERNR AND AVE_LAP_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = BPDLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = BPD_LAG_FIRMENNR AND LAP_LAGERNR = BPD_LAG_LAGERNR AND LAP_LAGERPLATZ = BPD_LAGERPLATZ" "~"
if joinKuerzel = LAPBPD
searchResult := searchResult "LEFT JOIN WWS.BPD_BESTPOSDETAIL ON BPD_LAG_FIRMENNR = LAP_FIRMENNR AND BPD_LAG_LAGERNR = LAP_LAGERNR AND BPD_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = IAPLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = IAP_FIRMENNR AND LAP_LAGERNR = IAP_LAGERNR AND LAP_LAGERPLATZ = IAP_LAGERPLATZ" "~"
if joinKuerzel = LAPIAP
searchResult := searchResult "LEFT JOIN WWS.IAP_INVABWTPOS ON IAP_FIRMENNR = LAP_FIRMENNR AND IAP_LAGERNR = LAP_LAGERNR AND IAP_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = IVKLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = IVK_FIRMENNR AND LAP_LAGERNR = IVK_LAGERNR AND LAP_LAGERPLATZ = IVK_LAGERPLATZ" "~"
if joinKuerzel = LAPIVK
searchResult := searchResult "LEFT JOIN WWS.IVK_INVENTURKOPF ON IVK_FIRMENNR = LAP_FIRMENNR AND IVK_LAGERNR = LAP_LAGERNR AND IVK_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = LAJLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = LAJ_ARTIKELFIRMENNR AND LAP_LAGERNR = LAJ_LAGERNR AND LAP_LAGERPLATZ = LAJ_LAGERPLATZ" "~"
if joinKuerzel = LAPLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_ARTIKELFIRMENNR = LAP_FIRMENNR AND LAJ_LAGERNR = LAP_LAGERNR AND LAJ_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = TRJLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = TRJ_FIRMENNR AND LAP_LAGERNR = TRJ_LAGERNR AND LAP_LAGERPLATZ = TRJ_LAGERPLATZ" "~"
if joinKuerzel = LAPTRJ
searchResult := searchResult "LEFT JOIN WWS.TRJ_TRANSPORTEINH_JOURNAL ON TRJ_FIRMENNR = LAP_FIRMENNR AND TRJ_LAGERNR = LAP_LAGERNR AND TRJ_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = WAKLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = WAK_LAG_FIRMENNR AND LAP_LAGERNR = WAK_LAGERNR AND LAP_LAGERPLATZ = WAK_LAGERPLATZ" "~"
if joinKuerzel = LAPWAK
searchResult := searchResult "LEFT JOIN WWS.WAK_WARENEINGANGKOPF ON WAK_LAG_FIRMENNR = LAP_FIRMENNR AND WAK_LAGERNR = LAP_LAGERNR AND WAK_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = LPOLAP
searchResult := searchResult "LEFT JOIN WWS.LAP_LAGERPLATZ ON LAP_FIRMENNR = LPO_ZIELLAGER_FIRMENNR AND LAP_LAGERNR = LPO_ZIELLAGER_LAGERNR AND LAP_LAGERPLATZ = LPO_ZIELLAGER_LAGERPLATZ" "~"
if joinKuerzel = LAPLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_ZIELLAGER_FIRMENNR = LAP_FIRMENNR AND LPO_ZIELLAGER_LAGERNR = LAP_LAGERNR AND LPO_ZIELLAGER_LAGERPLATZ = LAP_LAGERPLATZ" "~"
if joinKuerzel = ALOLAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = ALO_LOHNARTID" "~"
if joinKuerzel = LAQALO
searchResult := searchResult "LEFT JOIN HRS.ALO_ARTIKELLOHNARTZU ON ALO_LOHNARTID = LAQ_LOHNARTID" "~"
if joinKuerzel = IPULAQ
searchResult := searchResult "LEFT JOIN HRS.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = IPU_LAQ_LOHNARTID" "~"
if joinKuerzel = LAQIPU
searchResult := searchResult "LEFT JOIN HRS.IPU_IMPORTPUFFER ON IPU_LAQ_LOHNARTID = LAQ_LOHNARTID" "~"
if joinKuerzel = RAPLAQ
searchResult := searchResult "LEFT JOIN LAQ_LOHNART_SEQ.LAQ_LOHNART_SEQ ON LAQ_LOHNARTID = RAP_LOHNARTID" "~"
if joinKuerzel = LAQRAP
searchResult := searchResult "LEFT JOIN WWS.RAP_RABATTTABPOS ON RAP_LOHNARTID = LAQ_LOHNARTID" "~"
if joinKuerzel = VLALAT
searchResult := searchResult "LEFT JOIN WWS.LAT_LIEFERANTENARTIKEL ON LAT_LIEFERANTENARTIKELID = VLA_LIEFERANTENARTIKELID" "~"
if joinKuerzel = LATVLA
searchResult := searchResult "LEFT JOIN WWS.VLA_VIEW_HAUPTLIEFERARTIKEL ON VLA_LIEFERANTENARTIKELID = LAT_LIEFERANTENARTIKELID" "~"
if joinKuerzel = BBKLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = BBK_FIRMENNR AND LFI_LOHNJAHR = BBK_LOHNJAHR" "~"
if joinKuerzel = LFIBBK
searchResult := searchResult "LEFT JOIN HRS.BBK_BUAK_BETRIEBSKZ ON BBK_FIRMENNR = LFI_FIRMENNR AND BBK_LOHNJAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = EAFLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = EAF_FIRMENNR AND LFI_LOHNJAHR = EAF_JAHR" "~"
if joinKuerzel = LFIEAF
searchResult := searchResult "LEFT JOIN HRS.EAF_ELDADETAIL_AFB ON EAF_FIRMENNR = LFI_FIRMENNR AND EAF_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = EDALFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = EDA_FIRMENNR AND LFI_LOHNJAHR = EDA_JAHR" "~"
if joinKuerzel = LFIEDA
searchResult := searchResult "LEFT JOIN HRS.EDA_ELDADETAIL_ADR ON EDA_FIRMENNR = LFI_FIRMENNR AND EDA_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = GZMLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = GZM_DVFIRMENNR AND LFI_LOHNJAHR = GZM_JAHR" "~"
if joinKuerzel = LFIGZM
searchResult := searchResult "LEFT JOIN HRS.GZM_GLAEUBIGERZU_MONAT ON GZM_DVFIRMENNR = LFI_FIRMENNR AND GZM_JAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = PSJLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = PSJ_FIRMENNR AND LFI_LOHNJAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = LFIPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = LFI_FIRMENNR AND PSJ_LOHNJAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = SPELFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = SPE_FIRMENNR AND LFI_LOHNJAHR = SPE_LOHNJAHR" "~"
if joinKuerzel = LFISPE
searchResult := searchResult "LEFT JOIN HRS.SPE_SPESENARTEN ON SPE_FIRMENNR = LFI_FIRMENNR AND SPE_LOHNJAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = SZULFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_FIRMENNR = SZU_FIRMENNR AND LFI_LOHNJAHR = SZU_LOHNJAHR" "~"
if joinKuerzel = LFISZU
searchResult := searchResult "LEFT JOIN HRS.SZU_SCHNITTLAZU ON SZU_FIRMENNR = LFI_FIRMENNR AND SZU_LOHNJAHR = LFI_LOHNJAHR" "~"
if joinKuerzel = VMZLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_FIRMENNR = VMZ_LFS_FIRMENNR AND LFS_LIEFERSCHEINLFDNR = VMZ_LFS_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = LFSVMZ
searchResult := searchResult "LEFT JOIN WWS.VMZ_VERSMGR_LFS_ZUORD ON VMZ_LFS_FIRMENNR = LFS_FIRMENNR AND VMZ_LFS_LIEFERSCHEINLFDNR = LFS_LIEFERSCHEINLFDNR" "~"
if joinKuerzel = PZELGF
searchResult := searchResult "LEFT JOIN HRS.LGF_LGVMAKROS ON LGF_LGVMAKROID = PZE_LGF_LGVMAKROID" "~"
if joinKuerzel = LGFPZE
searchResult := searchResult "LEFT JOIN HRS.PZE_PERKZGLGZEILE ON PZE_LGF_LGVMAKROID = LGF_LGVMAKROID" "~"
if joinKuerzel = SZULGR
searchResult := searchResult "LEFT JOIN HRS.LGR_LOHNARTGRUPPE ON LGR_LGRID = SZU_LGRID AND LGR_JAHR = SZU_LOHNJAHR AND LGR_DBSNR = SZU_DBSNR" "~"
if joinKuerzel = LGRSZU
searchResult := searchResult "LEFT JOIN HRS.SZU_SCHNITTLAZU ON SZU_LGRID = LGR_LGRID AND SZU_LOHNJAHR = LGR_JAHR AND SZU_DBSNR = LGR_DBSNR" "~"
if joinKuerzel = AWSLIN
searchResult := searchResult "LEFT JOIN LIN_PROD_LINIE.LIN_PROD_LINIE ON LIN_FIRMENNR = AWS_LIN_FIRMENNR AND LIN_LINIE = AWS_LIN_LINIE" "~"
if joinKuerzel = LINAWS
searchResult := searchResult "LEFT JOIN ZLE.AWS_AUSWERT_AUSWAHL ON AWS_LIN_FIRMENNR = LIN_FIRMENNR AND AWS_LIN_LINIE = LIN_LINIE" "~"
if joinKuerzel = LAWLSE
searchResult := searchResult "LEFT JOIN HRS.LSE_LOHNSCHABLONE ON LSE_DBSNR = LAW_DBSNR AND LSE_JAHR = LAW_JAHR AND LSE_LSENR = LAW_LSENR" "~"
if joinKuerzel = LSELAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_DBSNR = LSE_DBSNR AND LAW_JAHR = LSE_JAHR AND LAW_LSENR = LSE_LSENR" "~"
if joinKuerzel = AMZLSG
searchResult := searchResult "LEFT JOIN LSG_LIEFERSCHEINGRUPPEN.LSG_LIEFERSCHEINGRUPPEN ON LSG_FIRMENNR = AMZ_FIRMENNR AND LSG_LFSHAUPTGRUPPE = AMZ_LFSHAUPTGRUPPE AND LSG_LFSUNTERGRUPPE = AMZ_LFSUNTERGRUPPE" "~"
if joinKuerzel = LSGAMZ
searchResult := searchResult "LEFT JOIN BUERO.AMZ_AUSDRUCKMODELLZU ON AMZ_FIRMENNR = LSG_FIRMENNR AND AMZ_LFSHAUPTGRUPPE = LSG_LFSHAUPTGRUPPE AND AMZ_LFSUNTERGRUPPE = LSG_LFSUNTERGRUPPE" "~"
if joinKuerzel = ARLLTX
searchResult := searchResult "LEFT JOIN BUERO.LTX_LANGTEXT ON LTX_LANGTXTLFDNR = ARL_PRODUKTLANGTEXT" "~"
if joinKuerzel = LTXARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_PRODUKTLANGTEXT = LTX_LANGTXTLFDNR" "~"
if joinKuerzel = TERLTX
searchResult := searchResult "LEFT JOIN BUERO.LTX_LANGTEXT ON LTX_LANGTXTLFDNR = TER_LANGTXTLFDNR" "~"
if joinKuerzel = LTXTER
searchResult := searchResult "LEFT JOIN BUERO.TER_TERMIN ON TER_LANGTXTLFDNR = LTX_LANGTXTLFDNR" "~"
if joinKuerzel = POZMAB
searchResult := searchResult "LEFT JOIN BUERO.MAB_MITARBAUSBILDUNG ON MAB_MITARBAUSBID = POZ_MAB_MITARBAUSBID" "~"
if joinKuerzel = MABPOZ
searchResult := searchResult "LEFT JOIN BUERO.POZ_PISWORKFLOWZU ON POZ_MAB_MITARBAUSBID = MAB_MITARBAUSBID" "~"
if joinKuerzel = MZZMAG
searchResult := searchResult "LEFT JOIN BUERO.MAG_MITARBGESPR ON MAG_MAGID = MZZ_MAGID" "~"
if joinKuerzel = MAGMZZ
searchResult := searchResult "LEFT JOIN BUERO.MZZ_MITARBZIELZU ON MZZ_MAGID = MAG_MAGID" "~"
if joinKuerzel = TERMAG
searchResult := searchResult "LEFT JOIN BUERO.MAG_MITARBGESPR ON MAG_MAGID = TER_MAG_MAGID" "~"
if joinKuerzel = MAGTER
searchResult := searchResult "LEFT JOIN BUERO.TER_TERMIN ON TER_MAG_MAGID = MAG_MAGID" "~"
if joinKuerzel = AMZMDA
searchResult := searchResult "LEFT JOIN MDA_MDE_ARTEN.MDA_MDE_ARTEN ON MDA_MDEARTID = AMZ_MDEARTID" "~"
if joinKuerzel = MDAAMZ
searchResult := searchResult "LEFT JOIN BUERO.AMZ_AUSDRUCKMODELLZU ON AMZ_MDEARTID = MDA_MDEARTID" "~"
if joinKuerzel = LEIMED
searchResult := searchResult "LEFT JOIN MED_MEDIEN.MED_MEDIEN ON MED_MEDIENID = LEI_MED_MEDIENID" "~"
if joinKuerzel = MEDLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_MED_MEDIENID = MED_MEDIENID" "~"
if joinKuerzel = BKMMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = BKM_MIT_FIRMENNR AND MIT_MITARBEITERID = BKM_MITARBEITERID" "~"
if joinKuerzel = MITBKM
searchResult := searchResult "LEFT JOIN BUERO.BKM_ZEITBETRKALMAZUORD ON BKM_MIT_FIRMENNR = MIT_FIRMENNR AND BKM_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = DSVMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = DSV_MIT_FIRMENNR AND MIT_MITARBEITERID = DSV_MITARBEITERID" "~"
if joinKuerzel = MITDSV
searchResult := searchResult "LEFT JOIN BUERO.DSV_DSGVO_VERZEICHNIS ON DSV_MIT_FIRMENNR = MIT_FIRMENNR AND DSV_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = EKOMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = EKO_AKTIONSART_FIRMENNR AND MIT_MITARBEITERID = EKO_AKTIONSART_MITID" "~"
if joinKuerzel = MITEKO
searchResult := searchResult "LEFT JOIN HRS.EKO_ELDAKONTROLLE ON EKO_AKTIONSART_FIRMENNR = MIT_FIRMENNR AND EKO_AKTIONSART_MITID = MIT_MITARBEITERID" "~"
if joinKuerzel = ELVMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = ELV_KOMMISS_FIRMENNR AND MIT_MITARBEITERID = ELV_KOMMISS_MITARBEITERID" "~"
if joinKuerzel = MITELV
searchResult := searchResult "LEFT JOIN WWS.ELV_ERFASSPOSLIEFERVOR ON ELV_KOMMISS_FIRMENNR = MIT_FIRMENNR AND ELV_KOMMISS_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = FSTMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = FST_MIT_FIRMENNR AND MIT_MITARBEITERID = FST_MITARBEITERID" "~"
if joinKuerzel = MITFST
searchResult := searchResult "LEFT JOIN BUERO.FST_FRIST ON FST_MIT_FIRMENNR = MIT_FIRMENNR AND FST_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = HNPMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = HNP_GF_FIRMENNR AND MIT_MITARBEITERID = HNP_GF_MITARBEITERID" "~"
if joinKuerzel = MITHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_GF_FIRMENNR = MIT_FIRMENNR AND HNP_GF_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = IMNMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = IMN_MIT_FIRMENNR AND MIT_MITARBEITERID = IMN_MITARBEITERID" "~"
if joinKuerzel = MITIMN
searchResult := searchResult "LEFT JOIN BUERO.IMN_INSTANDHMASSN ON IMN_MIT_FIRMENNR = MIT_FIRMENNR AND IMN_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = LZSMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = LZS_MIT_FIRMENNR AND MIT_MITARBEITERID = LZS_MITARBEITERID" "~"
if joinKuerzel = MITLZS
searchResult := searchResult "LEFT JOIN HRS.LZS_LOHNZETTEL_SV ON LZS_MIT_FIRMENNR = MIT_FIRMENNR AND LZS_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = PANMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = PAN_VERT_FIRMENNR AND MIT_MITARBEITERID = PAN_VERT_PERSONENID" "~"
if joinKuerzel = MITPAN
searchResult := searchResult "LEFT JOIN WWS.PAN_PROVARTBAUMKNOTEN ON PAN_VERT_FIRMENNR = MIT_FIRMENNR AND PAN_VERT_PERSONENID = MIT_MITARBEITERID" "~"
if joinKuerzel = PRWMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = PRW_MIT_FIRMENNR AND MIT_MITARBEITERID = PRW_MITARBEITERID" "~"
if joinKuerzel = MITPRW
searchResult := searchResult "LEFT JOIN BUERO.PRW_PROJEKTWERT ON PRW_MIT_FIRMENNR = MIT_FIRMENNR AND PRW_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = PSWMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = PSW_MIT_AL_FIRMENNR AND MIT_MITARBEITERID = PSW_MIT_AL_MITARBEITERID" "~"
if joinKuerzel = MITPSW
searchResult := searchResult "LEFT JOIN BUERO.PSW_PERSONENWERTE ON PSW_MIT_AL_FIRMENNR = MIT_FIRMENNR AND PSW_MIT_AL_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = PTPMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = PTP_MIT_FIRMENNR AND MIT_MITARBEITERID = PTP_MITARBEITERID" "~"
if joinKuerzel = MITPTP
searchResult := searchResult "LEFT JOIN WWS.PTP_PREISTABPOS ON PTP_MIT_FIRMENNR = MIT_FIRMENNR AND PTP_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = SSKMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = SSK_MIT_FIRMENNR AND MIT_MITARBEITERID = SSK_MITARBEITERID" "~"
if joinKuerzel = MITSSK
searchResult := searchResult "LEFT JOIN PRODUKTION.SSK_SAMMELSCHEIN_KOPF ON SSK_MIT_FIRMENNR = MIT_FIRMENNR AND SSK_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = TERMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = TER_MIT_FIRMENNR AND MIT_MITARBEITERID = TER_MITARBEITERID" "~"
if joinKuerzel = MITTER
searchResult := searchResult "LEFT JOIN BUERO.TER_TERMIN ON TER_MIT_FIRMENNR = MIT_FIRMENNR AND TER_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = VEPMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = VEP_VERTR_FIRMENNR AND MIT_MITARBEITERID = VEP_VERTR_MITARBEITERID" "~"
if joinKuerzel = MITVEP
searchResult := searchResult "LEFT JOIN WWS.VEP_VERTRETERABRECHPOS ON VEP_VERTR_FIRMENNR = MIT_FIRMENNR AND VEP_VERTR_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = ZPTMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = ZPT_MIT_FIRMENNR AND MIT_MITARBEITERID = ZPT_MITARBEITERID" "~"
if joinKuerzel = MITZPT
searchResult := searchResult "LEFT JOIN BUERO.ZPT_ZEITPLANTEXT ON ZPT_MIT_FIRMENNR = MIT_FIRMENNR AND ZPT_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = ZTVMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = ZTV_MIT_FIRMENNR AND MIT_MITARBEITERID = ZTV_MITARBEITERID" "~"
if joinKuerzel = MITZTV
searchResult := searchResult "LEFT JOIN BUERO.ZTV_ZEITPLANTEXTVERSDAT ON ZTV_MIT_FIRMENNR = MIT_FIRMENNR AND ZTV_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = EFKMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = EFK_V2_FIRMENNR AND MIT_MITARBEITERID = EFK_V2_MITARBEITERID" "~"
if joinKuerzel = MITEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_V2_FIRMENNR = MIT_FIRMENNR AND EFK_V2_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = MSTMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_FIRMENNR = MST_STV_FIRMENNR AND MIT_MITARBEITERID = MST_STV_MITARBEITERID" "~"
if joinKuerzel = MITMST
searchResult := searchResult "LEFT JOIN BUERO.MST_MITARBSTELLVERTRETER ON MST_STV_FIRMENNR = MIT_FIRMENNR AND MST_STV_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = RNGMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = RNG_V2_FIRMENNR AND MIT_MITARBEITERID = RNG_V2_MITARBEITERID" "~"
if joinKuerzel = MITRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_V2_FIRMENNR = MIT_FIRMENNR AND RNG_V2_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = BONMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = BON_V4_FIRMENNR AND MIT_MITARBEITERID = BON_V4_MITARBEITERID" "~"
if joinKuerzel = MITBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_V4_FIRMENNR = MIT_FIRMENNR AND BON_V4_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = BPKMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = BPK_SACHB3_FIRMENNR AND MIT_MITARBEITERID = BPK_SACHB3_MITARBEITERID" "~"
if joinKuerzel = MITBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_SACHB3_FIRMENNR = MIT_FIRMENNR AND BPK_SACHB3_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = BONMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = BON_VERK_FIRMENNR AND MIT_MITARBEITERID = BON_VERK_MITARBEITERID" "~"
if joinKuerzel = MITBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_VERK_FIRMENNR = MIT_FIRMENNR AND BON_VERK_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = BONMIT
searchResult := searchResult "LEFT JOIN MIT_MITARBEITER.MIT_MITARBEITER ON MIT_FIRMENNR = BON_KASSIER_FIRMENNR AND MIT_MITARBEITERID = BON_KASSIER_MITARBEITERID" "~"
if joinKuerzel = MITBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_KASSIER_FIRMENNR = MIT_FIRMENNR AND BON_KASSIER_MITARBEITERID = MIT_MITARBEITERID" "~"
if joinKuerzel = LFIMLZ
searchResult := searchResult "LEFT JOIN MLZ_VORSKASSELEITZAHLZU.MLZ_VORSKASSELEITZAHLZU ON MLZ_LANDNR = LFI_MLZ_LANDNR AND MLZ_KASSENR = LFI_MLZ_KASSENR AND MLZ_LEITZAHL = LFI_MLZ_LEITZAHL" "~"
if joinKuerzel = MLZLFI
searchResult := searchResult "LEFT JOIN HRS.LFI_LOHNFIRMA ON LFI_MLZ_LANDNR = MLZ_LANDNR AND LFI_MLZ_KASSENR = MLZ_KASSENR AND LFI_MLZ_LEITZAHL = MLZ_LEITZAHL" "~"
if joinKuerzel = ARLMOK
searchResult := searchResult "LEFT JOIN WWS.MOK_MODELLKOPF ON MOK_FIRMENNR = ARL_FIRMENNR AND MOK_MODELLKOPFGUID = ARL_MODELLKOPFGUID" "~"
if joinKuerzel = MOKARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = MOK_FIRMENNR AND ARL_MODELLKOPFGUID = MOK_MODELLKOPFGUID" "~"
if joinKuerzel = NUPNUK
searchResult := searchResult "LEFT JOIN BMD.NUK_NTCSUPDPRODUKT ON NUK_NTCSUPDPRODID = NUP_NTCSUPDPRODID" "~"
if joinKuerzel = NUKNUP
searchResult := searchResult "LEFT JOIN BMD.NUP_NTCSUPDATE ON NUP_NTCSUPDPRODID = NUK_NTCSUPDPRODID" "~"
if joinKuerzel = NUPNUO
searchResult := searchResult "LEFT JOIN BMD.NUO_NTCSUPDPRIORITAET ON NUO_NTCSUPDPRIOID = NUP_NTCSUPDPRIOID" "~"
if joinKuerzel = NUONUP
searchResult := searchResult "LEFT JOIN BMD.NUP_NTCSUPDATE ON NUP_NTCSUPDPRIOID = NUO_NTCSUPDPRIOID" "~"
if joinKuerzel = NUTNUP
searchResult := searchResult "LEFT JOIN BMD.NUP_NTCSUPDATE ON NUP_NTCSUPDATEID = NUT_NTCSUPDATEID" "~"
if joinKuerzel = NUPNUT
searchResult := searchResult "LEFT JOIN BMD.NUT_NTCSUPDTRANSFER ON NUT_NTCSUPDATEID = NUP_NTCSUPDATEID" "~"
if joinKuerzel = OPEOAK
searchResult := searchResult "LEFT JOIN LOHN.OAK_OF_AKREIS ON OAK_KLIENTENNR = OPE_KLIENTENNR AND OAK_AKNR = OPE_AKNR" "~"
if joinKuerzel = OAKOPE
searchResult := searchResult "LEFT JOIN LOHN.OPE_OF_PERSON ON OPE_KLIENTENNR = OAK_KLIENTENNR AND OPE_AKNR = OAK_AKNR" "~"
if joinKuerzel = OASOAZ
searchResult := searchResult "LEFT JOIN LOHN.OAZ_OF_ARBEITSZEITMODELL ON OAZ_KLIENTENNR = OAS_KLIENTENNR AND OAZ_OFAZNR = OAS_OFAZNR" "~"
if joinKuerzel = OAZOAS
searchResult := searchResult "LEFT JOIN LOHN.OAS_OF_ARBEITSZEITSTUNDEN ON OAS_KLIENTENNR = OAZ_KLIENTENNR AND OAS_OFAZNR = OAZ_OFAZNR" "~"
if joinKuerzel = OBAOBB
searchResult := searchResult "LEFT JOIN BUERO.OBB_ONLBEWBEWERBUNG ON OBB_BEWERBUNGSID = OBA_OBB_BEWERBUNGSID" "~"
if joinKuerzel = OBBOBA
searchResult := searchResult "LEFT JOIN BUERO.OBA_ONLBEWAUSSCHREIBUNGZU ON OBA_OBB_BEWERBUNGSID = OBB_BEWERBUNGSID" "~"
if joinKuerzel = OPEOFK
searchResult := searchResult "LEFT JOIN LOHN.OFK_OF_FKREIS ON OFK_KLIENTENNR = OPE_KLIENTENNR AND OFK_FKNR = OPE_FKNR" "~"
if joinKuerzel = OFKOPE
searchResult := searchResult "LEFT JOIN LOHN.OPE_OF_PERSON ON OPE_KLIENTENNR = OFK_KLIENTENNR AND OPE_FKNR = OFK_FKNR" "~"
if joinKuerzel = FIWORT
searchResult := searchResult "LEFT JOIN BUERO.ORT_ORT ON ORT_LANDNR = FIW_FIRMSITZ_LANDNR AND ORT_PLZ = FIW_FIRMSITZ_ORT_PLZ AND ORT_PLZUNTERNR = FIW_FIRMSITZ_PLZUNTERNR" "~"
if joinKuerzel = ORTFIW
searchResult := searchResult "LEFT JOIN BUERO.FIW_FIRMENWERTE ON FIW_FIRMSITZ_LANDNR = ORT_LANDNR AND FIW_FIRMSITZ_ORT_PLZ = ORT_PLZ AND FIW_FIRMSITZ_PLZUNTERNR = ORT_PLZUNTERNR" "~"
if joinKuerzel = RZIORT
searchResult := searchResult "LEFT JOIN ORT_ORT.ORT_ORT ON ORT_LANDNR = RZI_ORT_LANDNR AND ORT_PLZ = RZI_ORT_PLZ AND ORT_PLZUNTERNR = RZI_ORT_PLZUNTERNR" "~"
if joinKuerzel = ORTRZI
searchResult := searchResult "LEFT JOIN HRS.RZI_REISEZIELE ON RZI_ORT_LANDNR = ORT_LANDNR AND RZI_ORT_PLZ = ORT_PLZ AND RZI_ORT_PLZUNTERNR = ORT_PLZUNTERNR" "~"
if joinKuerzel = OPEOST
searchResult := searchResult "LEFT JOIN LOHN.OST_OF_STAAT ON OST_STAATENNR = OPE_STAATENNR" "~"
if joinKuerzel = OSTOPE
searchResult := searchResult "LEFT JOIN LOHN.OPE_OF_PERSON ON OPE_STAATENNR = OST_STAATENNR" "~"
if joinKuerzel = OLEOTA
searchResult := searchResult "LEFT JOIN LOHN.OTA_OF_TAETIGKEIT ON OTA_KLIENTENNR = OLE_KLIENTENNR AND OTA_TAETIGKEITENNR = OLE_OTA_TAETIGKEITENNR" "~"
if joinKuerzel = OTAOLE
searchResult := searchResult "LEFT JOIN LOHN.OLE_OF_LEISTUNG ON OLE_KLIENTENNR = OTA_KLIENTENNR AND OLE_OTA_TAETIGKEITENNR = OTA_TAETIGKEITENNR" "~"
if joinKuerzel = OUZOUF
searchResult := searchResult "LEFT JOIN BUERO.OUF_ONLINEUMFRAGE ON OUF_OUFID = OUZ_OUFID" "~"
if joinKuerzel = OUFOUZ
searchResult := searchResult "LEFT JOIN BUERO.OUZ_ONLINEKONFUMFRAGEZU ON OUZ_OUFID = OUF_OUFID" "~"
if joinKuerzel = OKTOVE
searchResult := searchResult "LEFT JOIN LOHN.OVE_OF_VERANTWORTLICHER ON OVE_KLIENTENNR = OKT_KLIENTENNR AND OVE_VERANTWNR = OKT_VERANTWNR" "~"
if joinKuerzel = OVEOKT
searchResult := searchResult "LEFT JOIN LOHN.OKT_OF_KOSTENTRAEGER ON OKT_KLIENTENNR = OVE_KLIENTENNR AND OKT_VERANTWNR = OVE_VERANTWNR" "~"
if joinKuerzel = PAPPAT
searchResult := searchResult "LEFT JOIN WWS.PAT_PACKSTOFFTAB ON PAT_FIRMENNR = PAP_FIRMENNR AND PAT_PACKSTOFFTABNR = PAP_PACKSTOFFTABNR" "~"
if joinKuerzel = PATPAP
searchResult := searchResult "LEFT JOIN WWS.PAP_PACKSTOFFTABPOS ON PAP_FIRMENNR = PAT_FIRMENNR AND PAP_PACKSTOFFTABNR = PAT_PACKSTOFFTABNR" "~"
if joinKuerzel = WAGPAT
searchResult := searchResult "LEFT JOIN WWS.PAT_PACKSTOFFTAB ON PAT_FIRMENNR = WAG_FIRMENNR AND PAT_PACKSTOFFTABNR = WAG_PACKSTOFFTABNR" "~"
if joinKuerzel = PATWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = PAT_FIRMENNR AND WAG_PACKSTOFFTABNR = PAT_PACKSTOFFTABNR" "~"
if joinKuerzel = PBMPBS
searchResult := searchResult "LEFT JOIN PRODUKTION.PBS_BEFEHL ON PBS_PBSID = PBM_PBSID" "~"
if joinKuerzel = PBSPBM
searchResult := searchResult "LEFT JOIN PRODUKTION.PBM_BEFEHL ON PBM_PBSID = PBS_PBSID" "~"
if joinKuerzel = EPSPCK
searchResult := searchResult "LEFT JOIN PCK_PPS_KAPA.PCK_PPS_KAPA ON PCK_PPSKAPAID = EPS_PPSKAPAID" "~"
if joinKuerzel = PCKEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = GSMPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_PPSKAPAID = GSM_PPSKAPAID" "~"
if joinKuerzel = PCKGSM
searchResult := searchResult "LEFT JOIN PRODUKTION.GSM_GUTSCHLECHTMENGE ON GSM_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = PCXPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_PPSKAPAID = PCX_PPSKAPAID" "~"
if joinKuerzel = PCKPCX
searchResult := searchResult "LEFT JOIN PRODUKTION.PCX_PPS_KAPA_EXT ON PCX_PPSKAPAID = PCK_PPSKAPAID" "~"
if joinKuerzel = UPHPCK
searchResult := searchResult "LEFT JOIN PCK_PPS_KAPA.PCK_PPS_KAPA ON PCK_PPSKAPAID = UPH_WIIID" "~"
if joinKuerzel = PCKUPH
searchResult := searchResult "LEFT JOIN BMDENTW.UPH_UPDATEHINWEIS ON UPH_WIIID = PCK_PPSKAPAID" "~"
if joinKuerzel = AGLPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_PPSKAPAID = AGL_AG_PARENT_ID" "~"
if joinKuerzel = PCKAGL
searchResult := searchResult "LEFT JOIN PRODUKTION.AGL_ARBEITSGANGZU ON AGL_AG_PARENT_ID = PCK_PPSKAPAID" "~"
if joinKuerzel = APAPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_PPSKAPAID = APA_AG_START_ID" "~"
if joinKuerzel = PCKAPA
searchResult := searchResult "LEFT JOIN PRODUKTION.APA_ARBEITSPLAN ON APA_AG_START_ID = PCK_PPSKAPAID" "~"
if joinKuerzel = GSMPCP
searchResult := searchResult "LEFT JOIN PRODUKTION.PCP_PPS_PRODUKT ON PCP_PCHARGENID = GSM_PCHARGENID AND PCP_PPSPROD_LFDNR = GSM_PPSPROD_LFDNR" "~"
if joinKuerzel = PCPGSM
searchResult := searchResult "LEFT JOIN PRODUKTION.GSM_GUTSCHLECHTMENGE ON GSM_PCHARGENID = PCP_PCHARGENID AND GSM_PPSPROD_LFDNR = PCP_PPSPROD_LFDNR" "~"
if joinKuerzel = PDWPAW
searchResult := searchResult "LEFT JOIN PAW_PARAMWERT.PAW_PARAMWERT ON PAW_PARAMGRUPPE = PDW_PARAMGRUPPE AND PAW_PARAMUNTERGRUPPE = PDW_PARAMUNTERGRUPPE AND PAW_PARAMWERTKEYEXT = PDW_PARAMWERTKEYEXT AND PAW_PARAMNR = PDW_PARAMNR AND PAW_PARAMWERTLFDNR = PDW_PARAMWERTLFDNR" "~"
if joinKuerzel = PAWPDW
searchResult := searchResult "LEFT JOIN BMDDEFAULT.PDW_PARAMWERT ON PDW_PARAMGRUPPE = PAW_PARAMGRUPPE AND PDW_PARAMUNTERGRUPPE = PAW_PARAMUNTERGRUPPE AND PDW_PARAMWERTKEYEXT = PAW_PARAMWERTKEYEXT AND PDW_PARAMNR = PAW_PARAMNR AND PDW_PARAMWERTLFDNR = PAW_PARAMWERTLFDNR" "~"
if joinKuerzel = AGSPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = AGS_RECHEMPF_FIRMENNR AND PER_PERSONENID = AGS_RECHEMPF_PERSONENID" "~"
if joinKuerzel = PERAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_RECHEMPF_FIRMENNR = PER_FIRMENNR AND AGS_RECHEMPF_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = ALEPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = ALE_PER_FIRMENNR AND PER_PERSONENID = ALE_PER_PERSONENID" "~"
if joinKuerzel = PERALE
searchResult := searchResult "LEFT JOIN BUERO.ALE_ADRESSENLISTE ON ALE_PER_FIRMENNR = PER_FIRMENNR AND ALE_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = APSPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = APS_VERANTW_FIRMENNR AND PER_PERSONENID = APS_VERANTW_PERSONENID" "~"
if joinKuerzel = PERAPS
searchResult := searchResult "LEFT JOIN WWS.APS_ARBEITSPLAN_STAMM ON APS_VERANTW_FIRMENNR = PER_FIRMENNR AND APS_VERANTW_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = AUBPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = AUB_FIRMENNR AND PER_PERSONENID = AUB_PERSONENID" "~"
if joinKuerzel = PERAUB
searchResult := searchResult "LEFT JOIN BIL.AUB_KIND ON AUB_FIRMENNR = PER_FIRMENNR AND AUB_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = AVEPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = AVE_KONTAKT_FIRMENNR AND PER_PERSONENID = AVE_KONTAKT_PERSONENID" "~"
if joinKuerzel = PERAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_KONTAKT_FIRMENNR = PER_FIRMENNR AND AVE_KONTAKT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = BINPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = BIN_PER_FIRMENNR AND PER_PERSONENID = BIN_PER_PERSONENID" "~"
if joinKuerzel = PERBIN
searchResult := searchResult "LEFT JOIN BUERO.BIN_BEWERBERINSERATZU ON BIN_PER_FIRMENNR = PER_FIRMENNR AND BIN_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = BUCPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = BUC_PER_FIRMENNR AND PER_PERSONENID = BUC_PER_PERSONENID" "~"
if joinKuerzel = PERBUC
searchResult := searchResult "LEFT JOIN FIBU.BUC_BUCHUNG ON BUC_PER_FIRMENNR = PER_FIRMENNR AND BUC_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = ERPPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = ERP_LEISTENDERUNTERN_FIRNR AND PER_PERSONENID = ERP_LEISTENDERUNTERN_PERID" "~"
if joinKuerzel = PERERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_LEISTENDERUNTERN_FIRNR = PER_FIRMENNR AND ERP_LEISTENDERUNTERN_PERID = PER_PERSONENID" "~"
if joinKuerzel = EVAPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = EVA_FIRMENNR AND PER_PERSONENID = EVA_PERSONENID" "~"
if joinKuerzel = PEREVA
searchResult := searchResult "LEFT JOIN BUERO.EVA_EVENTTEAM ON EVA_FIRMENNR = PER_FIRMENNR AND EVA_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = EVVPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = EVV_FIRMENNR AND PER_PERSONENID = EVV_PERSONENID" "~"
if joinKuerzel = PEREVV
searchResult := searchResult "LEFT JOIN BUERO.EVV_EVENTVORTRAGZU ON EVV_FIRMENNR = PER_FIRMENNR AND EVV_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = GSVPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = GSV_PER_FIRMENNR AND PER_PERSONENID = GSV_PERSONENID" "~"
if joinKuerzel = PERGSV
searchResult := searchResult "LEFT JOIN BIL.GSV_GSVGBERECH ON GSV_PER_FIRMENNR = PER_FIRMENNR AND GSV_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = IVPPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = IVP_PER_FIRMENNR AND PER_PERSONENID = IVP_PERSONENID" "~"
if joinKuerzel = PERIVP
searchResult := searchResult "LEFT JOIN WWS.IVP_INVENTURPOSITION ON IVP_PER_FIRMENNR = PER_FIRMENNR AND IVP_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = KOSPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = KOS_PER_FIRMENNR AND PER_PERSONENID = KOS_PER_PERSONENID" "~"
if joinKuerzel = PERKOS
searchResult := searchResult "LEFT JOIN ZLE.KOS_KOSTENFAKTOR ON KOS_PER_FIRMENNR = PER_FIRMENNR AND KOS_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = MRVPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = MRV_PER_FIRMENNR AND PER_PERSONENID = MRV_PERSONENID" "~"
if joinKuerzel = PERMRV
searchResult := searchResult "LEFT JOIN ZLE.MRV_MONATSRECHNUNGVORLAGE ON MRV_PER_FIRMENNR = PER_FIRMENNR AND MRV_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PCKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = PCK_LEISTUNG_FIRMENNR AND PER_PERSONENID = PCK_LEISTUNG_PERSONENID" "~"
if joinKuerzel = PERPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_LEISTUNG_FIRMENNR = PER_FIRMENNR AND PCK_LEISTUNG_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PEZPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PEZ_FIRMENNR AND PER_PERSONENID = PEZ_PERSONENID" "~"
if joinKuerzel = PERPEZ
searchResult := searchResult "LEFT JOIN BUERO.PEZ_PERSONENZU ON PEZ_FIRMENNR = PER_FIRMENNR AND PEZ_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PFZPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PFZ_PER_FIRMENNR AND PER_PERSONENID = PFZ_PERSONENID" "~"
if joinKuerzel = PERPFZ
searchResult := searchResult "LEFT JOIN BUERO.PFZ_PERSONFIRMENZU ON PFZ_PER_FIRMENNR = PER_FIRMENNR AND PFZ_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PKLPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PKL_FIRMENNR AND PER_PERSONENID = PKL_PERSONENID" "~"
if joinKuerzel = PERPKL
searchResult := searchResult "LEFT JOIN BUERO.PKL_PROJEKTPERSZU ON PKL_FIRMENNR = PER_FIRMENNR AND PKL_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PNZPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PNZ_FIRMENNR AND PER_PERSONENID = PNZ_PERSONENID" "~"
if joinKuerzel = PERPNZ
searchResult := searchResult "LEFT JOIN BUERO.PNZ_PERSONENZUSATZ ON PNZ_FIRMENNR = PER_FIRMENNR AND PNZ_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PPUPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = PPU_PER_FIRMENNR AND PER_PERSONENID = PPU_PER_PERSONENID" "~"
if joinKuerzel = PERPPU
searchResult := searchResult "LEFT JOIN PRODUKTION.PPU_KAPA_POOL_ZU ON PPU_PER_FIRMENNR = PER_FIRMENNR AND PPU_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = QAZPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = QAZ_PER_FIRMENNR AND PER_PERSONENID = QAZ_PERSONENID" "~"
if joinKuerzel = PERQAZ
searchResult := searchResult "LEFT JOIN BUERO.QAZ_QUOTENAUSWERTUNGZU ON QAZ_PER_FIRMENNR = PER_FIRMENNR AND QAZ_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = RNGPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = RNG_RECHEMPF_KONT_FIRMENNR AND PER_PERSONENID = RNG_RECHEMPF_KONTAKTPERSID" "~"
if joinKuerzel = PERRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_RECHEMPF_KONT_FIRMENNR = PER_FIRMENNR AND RNG_RECHEMPF_KONTAKTPERSID = PER_PERSONENID" "~"
if joinKuerzel = RVKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = RVK_PER_FIRMENNR AND PER_PERSONENID = RVK_PERSONENID" "~"
if joinKuerzel = PERRVK
searchResult := searchResult "LEFT JOIN ZLE.RVK_RECHVORLAGEKOPF ON RVK_PER_FIRMENNR = PER_FIRMENNR AND RVK_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = SRBPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = SRB_PER_FIRMENNR AND PER_PERSONENID = SRB_PERSONENID" "~"
if joinKuerzel = PERSRB
searchResult := searchResult "LEFT JOIN BIL.SRB_STEUERBUCHUNGEN ON SRB_PER_FIRMENNR = PER_FIRMENNR AND SRB_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = TELPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = TEL_FIRMENNR AND PER_PERSONENID = TEL_PERSONENID" "~"
if joinKuerzel = PERTEL
searchResult := searchResult "LEFT JOIN BUERO.TEL_TELEFON ON TEL_FIRMENNR = PER_FIRMENNR AND TEL_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = TFPPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = TFP_FIRMENNR AND PER_PERSONENID = TFP_PERSONENID" "~"
if joinKuerzel = PERTFP
searchResult := searchResult "LEFT JOIN FIBU.TFP_FIBUPERSON_TMP ON TFP_FIRMENNR = PER_FIRMENNR AND TFP_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = TRJPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = TRJ_UMB_PER_FIRMENNR AND PER_PERSONENID = TRJ_UMB_PER_PERSONENID" "~"
if joinKuerzel = PERTRJ
searchResult := searchResult "LEFT JOIN WWS.TRJ_TRANSPORTEINH_JOURNAL ON TRJ_UMB_PER_FIRMENNR = PER_FIRMENNR AND TRJ_UMB_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = UEBPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = UEB_PER_FIRMENNR AND PER_PERSONENID = UEB_PERSONENID" "~"
if joinKuerzel = PERUEB
searchResult := searchResult "LEFT JOIN BUERO.UEB_UEBERWEISUNGSART ON UEB_PER_FIRMENNR = PER_FIRMENNR AND UEB_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = UMFPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = UMF_ANSPRECH_PER_FIRMENNR AND PER_PERSONENID = UMF_ANSPRECH_PERSONENID" "~"
if joinKuerzel = PERUMF
searchResult := searchResult "LEFT JOIN BUERO.UMF_UMFRAGE ON UMF_ANSPRECH_PER_FIRMENNR = PER_FIRMENNR AND UMF_ANSPRECH_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = VJSPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = VJS_AUFTRAGGEBER_FIRMENNR AND PER_PERSONENID = VJS_AUFTRAGGEBER_KLID" "~"
if joinKuerzel = PERVJS
searchResult := searchResult "LEFT JOIN WWS.VJS_VA_VERLEIHJOURNALSUM ON VJS_AUFTRAGGEBER_FIRMENNR = PER_FIRMENNR AND VJS_AUFTRAGGEBER_KLID = PER_PERSONENID" "~"
if joinKuerzel = VSWPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = VSW_VERTRETER_FIRMENNR AND PER_PERSONENID = VSW_VERTRETER_PERSONENID" "~"
if joinKuerzel = PERVSW
searchResult := searchResult "LEFT JOIN WWS.VSW_VERTRETERSTATISTIK ON VSW_VERTRETER_FIRMENNR = PER_FIRMENNR AND VSW_VERTRETER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = VVZPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = VVZ_PER_FIRMENNR AND PER_PERSONENID = VVZ_PER_PERSONENID" "~"
if joinKuerzel = PERVVZ
searchResult := searchResult "LEFT JOIN BUERO.VVZ_VTAGVORTRAGENDERZU ON VVZ_PER_FIRMENNR = PER_FIRMENNR AND VVZ_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = WATPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = WAT_TN_FIRMENNR AND PER_PERSONENID = WAT_TN_PERSONENID" "~"
if joinKuerzel = PERWAT
searchResult := searchResult "LEFT JOIN BUERO.WAT_WEBANMELDUNG ON WAT_TN_FIRMENNR = PER_FIRMENNR AND WAT_TN_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = XSPPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = XSP_PER_FIRMENNR AND PER_PERSONENID = XSP_PER_PERSONENID" "~"
if joinKuerzel = PERXSP
searchResult := searchResult "LEFT JOIN BUERO.XSP_XML_STEUERERKL_PAKET ON XSP_PER_FIRMENNR = PER_FIRMENNR AND XSP_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = DOKDKZ
searchResult := searchResult "LEFT JOIN BUERO.DKZ_DOKUMENTZU ON DKZ_ARCHIVNR = DOK_ARCHIVNR AND DKZ_DOKUMENTENNR = DOK_DOKUMENTENNR" "~"
if joinKuerzel = DOKPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = DOK_ENTLEHNER_FIRMENNR AND PER_PERSONENID = DOK_ENTLEHNER_PERSONENID" "~"
if joinKuerzel = PERDOK
searchResult := searchResult "LEFT JOIN BUERO.DOK_DOKUMENT ON DOK_ENTLEHNER_FIRMENNR = PER_FIRMENNR AND DOK_ENTLEHNER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = EFKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = EFK_LIEFEMPF_KONT_FIRMENNR AND PER_PERSONENID = EFK_LIEFEMPF_KONTAKTPERSID" "~"
if joinKuerzel = PEREFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_LIEFEMPF_KONT_FIRMENNR = PER_FIRMENNR AND EFK_LIEFEMPF_KONTAKTPERSID = PER_PERSONENID" "~"
if joinKuerzel = KAKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = KAK_LIEFEREMPF_FIRMENNR AND PER_PERSONENID = KAK_LIEFEREMPF_KONPERSID" "~"
if joinKuerzel = PERKAK
searchResult := searchResult "LEFT JOIN WWS.KAK_KASSENKOPF ON KAK_LIEFEREMPF_FIRMENNR = PER_FIRMENNR AND KAK_LIEFEREMPF_KONPERSID = PER_PERSONENID" "~"
if joinKuerzel = MITPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = MIT_LIEF_FIRMENNR AND PER_PERSONENID = MIT_LIEF_PERSONENID" "~"
if joinKuerzel = PERMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_LIEF_FIRMENNR = PER_FIRMENNR AND MIT_LIEF_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = PERPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PER_GES_ENTSPR_FIRMENNR AND PER_PERSONENID = PER_GES_ENTSPR_PERSONENID" "~"
if joinKuerzel = PERPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_GES_ENTSPR_FIRMENNR = PER_FIRMENNR AND PER_GES_ENTSPR_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = TOUPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = TOU_SPEDITION_FIRMENNR AND PER_PERSONENID = TOU_SPEDITION_EXTPERSID" "~"
if joinKuerzel = PERTOU
searchResult := searchResult "LEFT JOIN WWS.TOU_TOUR ON TOU_SPEDITION_FIRMENNR = PER_FIRMENNR AND TOU_SPEDITION_EXTPERSID = PER_PERSONENID" "~"
if joinKuerzel = VAKPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = VAK_ANBIETER_FIRMENNR AND PER_PERSONENID = VAK_ANBIETER_PERSONENID" "~"
if joinKuerzel = PERVAK
searchResult := searchResult "LEFT JOIN BUERO.VAK_VERANSTALTUNGSKOPF ON VAK_ANBIETER_FIRMENNR = PER_FIRMENNR AND VAK_ANBIETER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = VETPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = VET_VORT_FIRMENNR AND PER_PERSONENID = VET_VORT_PERSONENID" "~"
if joinKuerzel = PERVET
searchResult := searchResult "LEFT JOIN BUERO.VET_VERANSTALTUNGSTAG ON VET_VORT_FIRMENNR = PER_FIRMENNR AND VET_VORT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = AAEPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = AAE_AUFTRAGGEBER_FIRMENNR AND PER_PERSONENID = AAE_AUFTRAGGEBER_KLID" "~"
if joinKuerzel = PERAAE
searchResult := searchResult "LEFT JOIN WWS.AAE_ANGEBOTSAUSW_ERFQ_TMP ON AAE_AUFTRAGGEBER_FIRMENNR = PER_FIRMENNR AND AAE_AUFTRAGGEBER_KLID = PER_PERSONENID" "~"
if joinKuerzel = ALEPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = ALE_GES_PER_FIRMENNR AND PER_PERSONENID = ALE_GES_PER_PERSONENID" "~"
if joinKuerzel = PERALE
searchResult := searchResult "LEFT JOIN BUERO.ALE_ADRESSENLISTE ON ALE_GES_PER_FIRMENNR = PER_FIRMENNR AND ALE_GES_PER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = HNPPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = HNP_OFFZUGEHOER_FIRMENNR AND PER_PERSONENID = HNP_OFFZUGEHOER_PERSONENID" "~"
if joinKuerzel = PERHNP
searchResult := searchResult "LEFT JOIN ZLE.HNP_HONORARPOS ON HNP_OFFZUGEHOER_FIRMENNR = PER_FIRMENNR AND HNP_OFFZUGEHOER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = KSTPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = KST_ADR_PERSON_FIRMENNR AND PER_PERSONENID = KST_ADR_PERSON_ID" "~"
if joinKuerzel = PERKST
searchResult := searchResult "LEFT JOIN KORE.KST_KORESTAMM ON KST_ADR_PERSON_FIRMENNR = PER_FIRMENNR AND KST_ADR_PERSON_ID = PER_PERSONENID" "~"
if joinKuerzel = LEDPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = LED_ENT_FIRMENNR AND PER_PERSONENID = LED_ENT_PERSONENID" "~"
if joinKuerzel = PERLED
searchResult := searchResult "LEFT JOIN BUERO.LED_LEAD ON LED_ENT_FIRMENNR = PER_FIRMENNR AND LED_ENT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = MABPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = MAB_GENEHMIGER_FIRMENNR AND PER_PERSONENID = MAB_GENEHMIGER_PERSONENID" "~"
if joinKuerzel = PERMAB
searchResult := searchResult "LEFT JOIN BUERO.MAB_MITARBAUSBILDUNG ON MAB_GENEHMIGER_FIRMENNR = PER_FIRMENNR AND MAB_GENEHMIGER_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = MITPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = MIT_GUTSCHRIFT_FIRMENNR AND PER_PERSONENID = MIT_GUTSCHRIFT_PERSONENID" "~"
if joinKuerzel = PERMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_GUTSCHRIFT_FIRMENNR = PER_FIRMENNR AND MIT_GUTSCHRIFT_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = RIAPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = RIA_PER_FIRMENNR_UMSETZUNG AND PER_PERSONENID = RIA_PER_ID_UMSETZUNG" "~"
if joinKuerzel = PERRIA
searchResult := searchResult "LEFT JOIN PRODUKTION.RIA_RISIKOANALYSE ON RIA_PER_FIRMENNR_UMSETZUNG = PER_FIRMENNR AND RIA_PER_ID_UMSETZUNG = PER_PERSONENID" "~"
if joinKuerzel = VJSPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = VJS_LIEFEREMPF_FIRMENNR AND PER_PERSONENID = VJS_LIEFEREMPF_KLID" "~"
if joinKuerzel = PERVJS
searchResult := searchResult "LEFT JOIN WWS.VJS_VA_VERLEIHJOURNALSUM ON VJS_LIEFEREMPF_FIRMENNR = PER_FIRMENNR AND VJS_LIEFEREMPF_KLID = PER_PERSONENID" "~"
if joinKuerzel = FSTPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = FST_VERANTW_FIRMENNR AND PER_PERSONENID = FST_VERANTW_PERSONENID" "~"
if joinKuerzel = PERFST
searchResult := searchResult "LEFT JOIN BUERO.FST_FRIST ON FST_VERANTW_FIRMENNR = PER_FIRMENNR AND FST_VERANTW_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = BPKPER
searchResult := searchResult "LEFT JOIN PER_PERSON.PER_PERSON ON PER_FIRMENNR = BPK_RECHEMPF_KONT_FIRMENNR AND PER_PERSONENID = BPK_RECHEMPF_KONTAKTPERSID" "~"
if joinKuerzel = PERBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_RECHEMPF_KONT_FIRMENNR = PER_FIRMENNR AND BPK_RECHEMPF_KONTAKTPERSID = PER_PERSONENID" "~"
if joinKuerzel = PERPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = PER_FOLGEVERT4_FIRMENNR AND PER_PERSONENID = PER_FOLGEVERT4_PERSONENID" "~"
if joinKuerzel = PERPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FOLGEVERT4_FIRMENNR = PER_FIRMENNR AND PER_FOLGEVERT4_PERSONENID = PER_PERSONENID" "~"
if joinKuerzel = WKUPET
searchResult := searchResult "LEFT JOIN PET_PREISTAB.PET_PREISTAB ON PET_FIRMENNR = WKU_PET_FIRMENNR AND PET_PREISTABNR = WKU_PREISTABNR" "~"
if joinKuerzel = PETWKU
searchResult := searchResult "LEFT JOIN WWS_NEU.WKU_WWSKUNDE ON WKU_PET_FIRMENNR = PET_FIRMENNR AND WKU_PREISTABNR = PET_PREISTABNR" "~"
if joinKuerzel = PCKPFA
searchResult := searchResult "LEFT JOIN PFA_PERSONFINANZAMTZU.PFA_PERSONFINANZAMTZU ON PFA_FIRMENNR = PCK_AUFTRAGGEBER_FIRMENNR AND PFA_PERSONENID = PCK_AUFTRAGGEBER_PERID AND PFA_PFINANZAMTLFDNR = PCK_PFINANZAMTLFDNR" "~"
if joinKuerzel = PFAPCK
searchResult := searchResult "LEFT JOIN PRODUKTION.PCK_PPS_KAPA ON PCK_AUFTRAGGEBER_FIRMENNR = PFA_FIRMENNR AND PCK_AUFTRAGGEBER_PERID = PFA_PERSONENID AND PCK_PFINANZAMTLFDNR = PFA_PFINANZAMTLFDNR" "~"
if joinKuerzel = PZFPFR
searchResult := searchResult "LEFT JOIN WWS.PFR_PICFARBE ON PFR_PICID = PZF_PICID AND PFR_PICFARBENNR = PZF_PICFARBENNR" "~"
if joinKuerzel = PFRPZF
searchResult := searchResult "LEFT JOIN WWS.PZF_PICFARBENZU ON PZF_PICID = PFR_PICID AND PZF_PICFARBENNR = PFR_PICFARBENNR" "~"
if joinKuerzel = BFZPGE
searchResult := searchResult "LEFT JOIN PGE_PERSONGESELLZU.PGE_PERSONGESELLZU ON PGE_KUN_FIRMENNR = BFZ_PGE_GES_FIRMENNR AND PGE_KUN_PERSONENID = BFZ_PGE_KUN_PERSONENID AND PGE_GES_FIRMENNR = BFZ_PGE_KUN_FIRMENNR AND PGE_GES_PERSONENID = BFZ_PGE_GES_PERSONENID" "~"
if joinKuerzel = PGEBFZ
searchResult := searchResult "LEFT JOIN BIL.BFZ_XMLJABBETFIRZU ON BFZ_PGE_GES_FIRMENNR = PGE_KUN_FIRMENNR AND BFZ_PGE_KUN_PERSONENID = PGE_KUN_PERSONENID AND BFZ_PGE_KUN_FIRMENNR = PGE_GES_FIRMENNR AND BFZ_PGE_GES_PERSONENID = PGE_GES_PERSONENID" "~"
if joinKuerzel = PGWPGE
searchResult := searchResult "LEFT JOIN BUERO.PGE_PERSONGESELLZU ON PGE_KUN_FIRMENNR = PGW_KUN_FIRMENNR AND PGE_KUN_PERSONENID = PGW_KUN_PERSONENID AND PGE_GES_FIRMENNR = PGW_GES_FIRMENNR AND PGE_GES_PERSONENID = PGW_GES_PERSONENID AND PGE_GES_PERSONUNTERNR = PGW_GES_PERSONUNTERNR" "~"
if joinKuerzel = PGEPGW
searchResult := searchResult "LEFT JOIN BUERO.PGW_PERSONGESELLWERTE ON PGW_KUN_FIRMENNR = PGE_KUN_FIRMENNR AND PGW_KUN_PERSONENID = PGE_KUN_PERSONENID AND PGW_GES_FIRMENNR = PGE_GES_FIRMENNR AND PGW_GES_PERSONENID = PGE_GES_PERSONENID AND PGW_GES_PERSONUNTERNR = PGE_GES_PERSONUNTERNR" "~"
if joinKuerzel = PEAPGO
searchResult := searchResult "LEFT JOIN WWS.PGO_PICGROESSE ON PGO_PICID = PEA_PICID AND PGO_PICGROESSENKBEZ = PEA_PICGROESSENKBEZ" "~"
if joinKuerzel = PGOPEA
searchResult := searchResult "LEFT JOIN WWS.PEA_PICEANNUMMERN ON PEA_PICID = PGO_PICID AND PEA_PICGROESSENKBEZ = PGO_PICGROESSENKBEZ" "~"
if joinKuerzel = PZGPGO
searchResult := searchResult "LEFT JOIN WWS.PGO_PICGROESSE ON PGO_PICID = PZG_PICID AND PGO_PICGROESSENKBEZ = PZG_PICGROESSENKBEZ" "~"
if joinKuerzel = PGOPZG
searchResult := searchResult "LEFT JOIN WWS.PZG_PICGROESSENZU ON PZG_PICID = PGO_PICID AND PZG_PICGROESSENKBEZ = PGO_PICGROESSENKBEZ" "~"
if joinKuerzel = PLDPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_PICID = PLD_PICID AND PIA_PICARTIKELNR = PLD_PICARTIKELNR" "~"
if joinKuerzel = PIAPLD
searchResult := searchResult "LEFT JOIN WWS.PLD_PICALLERGENDATEN ON PLD_PICID = PIA_PICID AND PLD_PICARTIKELNR = PIA_PICARTIKELNR" "~"
if joinKuerzel = PREPIA
searchResult := searchResult "LEFT JOIN WWS.PIA_PICARTIKEL ON PIA_PICID = PRE_PIA_PICID AND PIA_PICARTIKELNR = PRE_PICARTIKELNR" "~"
if joinKuerzel = PIAPRE
searchResult := searchResult "LEFT JOIN WWS.PRE_EXTPARTIKELWERTE ON PRE_PIA_PICID = PIA_PICID AND PRE_PICARTIKELNR = PIA_PICARTIKELNR" "~"
if joinKuerzel = AAZPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = AAZ_POSTITNR" "~"
if joinKuerzel = PITAAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.AAZ_AUFARBEITARTIKELZU ON AAZ_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = ABRPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = ABR_POSTITNR" "~"
if joinKuerzel = PITABR
searchResult := searchResult "LEFT JOIN BIL.ABR_ANBUBEWRES ON ABR_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = AFBPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = AFB_POSTITNR" "~"
if joinKuerzel = PITAFB
searchResult := searchResult "LEFT JOIN FIBU.AFB_ANBUFIB ON AFB_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = AGUPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = AGU_POSTITNR" "~"
if joinKuerzel = PITAGU
searchResult := searchResult "LEFT JOIN BIL.AGU_ANLAGEGUT ON AGU_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = AUTPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = AUT_POSTITNR" "~"
if joinKuerzel = PITAUT
searchResult := searchResult "LEFT JOIN ZLE.AUT_AUSWERTTYP ON AUT_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = BAZPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = BAZ_POSTITNR" "~"
if joinKuerzel = PITBAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = BSTPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = BST_POSTITNR" "~"
if joinKuerzel = PITBST
searchResult := searchResult "LEFT JOIN BUERO.BST_SONDERTAGE ON BST_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = CAWPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = CAW_POSTITNR" "~"
if joinKuerzel = PITCAW
searchResult := searchResult "LEFT JOIN BUERO.CAW_CRMABWESENHEIT ON CAW_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = DAMPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = DAM_POSTITNR" "~"
if joinKuerzel = PITDAM
searchResult := searchResult "LEFT JOIN DATANORM.DAM_DATANORMARTIKELMATCH ON DAM_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = DNAPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = DNA_POSTITNR" "~"
if joinKuerzel = PITDNA
searchResult := searchResult "LEFT JOIN DATANORM.DNA_ARTIKEL ON DNA_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = DSVPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = DSV_POSTITNR" "~"
if joinKuerzel = PITDSV
searchResult := searchResult "LEFT JOIN BUERO.DSV_DSGVO_VERZEICHNIS ON DSV_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = EKRPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = EKR_POSTITNR" "~"
if joinKuerzel = PITEKR
searchResult := searchResult "LEFT JOIN WWS.EKR_ERFASSKOPFRECHNUNG ON EKR_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = EVTPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = EVT_POSTITNR" "~"
if joinKuerzel = PITEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = FWFPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = FWF_POSTITNR" "~"
if joinKuerzel = PITFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = GIDPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = GID_POSTITNR" "~"
if joinKuerzel = PITGID
searchResult := searchResult "LEFT JOIN BUERO.GID_GELDIDENTIFIKATION ON GID_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = GSPPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = GSP_POSTITNR" "~"
if joinKuerzel = PITGSP
searchResult := searchResult "LEFT JOIN WWS.GSP_GUTSCHEINPOSITION ON GSP_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = GWIPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = GWI_POSTITNR" "~"
if joinKuerzel = PITGWI
searchResult := searchResult "LEFT JOIN BUERO.GWI_GELDWIEREG ON GWI_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = IVKPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = IVK_POSTITNR" "~"
if joinKuerzel = PITIVK
searchResult := searchResult "LEFT JOIN WWS.IVK_INVENTURKOPF ON IVK_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = LIBPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = LIB_POSTITNR" "~"
if joinKuerzel = PITLIB
searchResult := searchResult "LEFT JOIN WWS.LIB_LIEFERBEDINGUNG ON LIB_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = LPOPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = LPO_POSTITNR" "~"
if joinKuerzel = PITLPO
searchResult := searchResult "LEFT JOIN WWS.LPO_LIEFERSCHEINPOS ON LPO_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = LXMPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = LXM_POSTITNR" "~"
if joinKuerzel = PITLXM
searchResult := searchResult "LEFT JOIN BUERO.LXM_LOHNEXPPROTOKOLL ON LXM_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PKDPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PKD_POSTITNR" "~"
if joinKuerzel = PITPKD
searchResult := searchResult "LEFT JOIN PRODUKTION.PKD_KM_DETAIL ON PKD_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PRSPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = PRS_POSTITNR" "~"
if joinKuerzel = PITPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = RASPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = RAS_POSTITNR" "~"
if joinKuerzel = PITRAS
searchResult := searchResult "LEFT JOIN WWS.RAS_RABATTTABPOSSTAFFEL ON RAS_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = RNGPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = RNG_POSTITNR" "~"
if joinKuerzel = PITRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = SMIPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = SMI_POSTITNR" "~"
if joinKuerzel = PITSMI
searchResult := searchResult "LEFT JOIN ZLE.SMI_SMARTINTERFACE ON SMI_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = TLAPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = TLA_POSTITNR" "~"
if joinKuerzel = PITTLA
searchResult := searchResult "LEFT JOIN PRODUKTION.TLA_BT_LOGISTIK_ANGEFORDERT ON TLA_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = TLEPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = TLE_POSTITNR" "~"
if joinKuerzel = PITTLE
searchResult := searchResult "LEFT JOIN PRODUKTION.TLE_BT_LOGISTIK_EMPFANGEN ON TLE_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = VEBPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = VEB_POSTITNR" "~"
if joinKuerzel = PITVEB
searchResult := searchResult "LEFT JOIN FIBU.VEB_VORERFASSBUCHUNG ON VEB_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = WMDPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = WMD_POSTITNR" "~"
if joinKuerzel = PITWMD
searchResult := searchResult "LEFT JOIN BUERO.WMD_WOCHENMODELL ON WMD_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = AVEPIT
searchResult := searchResult "LEFT JOIN PIT_POSTIT.PIT_POSTIT ON PIT_POSTITNR = AVE_KOPF_POSTITNR" "~"
if joinKuerzel = PITAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_KOPF_POSTITNR = PIT_POSTITNR" "~"
if joinKuerzel = PKWPKK
searchResult := searchResult "LEFT JOIN BIL.PKK_PERSKORST_PARAMKOPF ON PKK_PKKSEQID = PKW_PKKSEQID" "~"
if joinKuerzel = PKKPKW
searchResult := searchResult "LEFT JOIN BIL.PKW_PERSKORST_PARAMWERT ON PKW_PKKSEQID = PKK_PKKSEQID" "~"
if joinKuerzel = PLCPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_PCHARGENID = PLC_PCHARGENID_SOURCE" "~"
if joinKuerzel = PLCPLC
searchResult := searchResult "LEFT JOIN PRODUKTION.PLC_PPS_CHARGE ON PLC_PCHARGENID_SOURCE = PLC_PCHARGENID" "~"
if joinKuerzel = APAPLG
searchResult := searchResult "LEFT JOIN PLG_PLANGRUPPE.PLG_PLANGRUPPE ON PLG_FIRMENNR = APA_PLG_FIRMENNR AND PLG_PLANGRLFDNR = APA_PLG_PLANGRLFDNR" "~"
if joinKuerzel = PLGAPA
searchResult := searchResult "LEFT JOIN PRODUKTION.APA_ARBEITSPLAN ON APA_PLG_FIRMENNR = PLG_FIRMENNR AND APA_PLG_PLANGRLFDNR = PLG_PLANGRLFDNR" "~"
if joinKuerzel = KASPLG
searchResult := searchResult "LEFT JOIN PLG_PLANGRUPPE.PLG_PLANGRUPPE ON PLG_FIRMENNR = KAS_PLG_FIRMENNR AND PLG_PLANGRLFDNR = KAS_PLG_PLANGRLFDNR" "~"
if joinKuerzel = PLGKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_PLG_FIRMENNR = PLG_FIRMENNR AND KAS_PLG_PLANGRLFDNR = PLG_PLANGRLFDNR" "~"
if joinKuerzel = PLZPLG
searchResult := searchResult "LEFT JOIN BUERO.PLG_PLANGRUPPE ON PLG_FIRMENNR = PLZ_PLG_FIRMENNR AND PLG_PLANGRLFDNR = PLZ_PLG_PLANGRLFDNR" "~"
if joinKuerzel = PLGPLZ
searchResult := searchResult "LEFT JOIN BUERO.PLZ_PLANGRUPPEZU ON PLZ_PLG_FIRMENNR = PLG_FIRMENNR AND PLZ_PLG_PLANGRLFDNR = PLG_PLANGRLFDNR" "~"
if joinKuerzel = ZBUPLG
searchResult := searchResult "LEFT JOIN BUERO.PLG_PLANGRUPPE ON PLG_FIRMENNR = ZBU_PLG_FIRMENNR AND PLG_PLANGRLFDNR = ZBU_PLANGRLFDNR" "~"
if joinKuerzel = PLGZBU
searchResult := searchResult "LEFT JOIN BUERO.ZBU_ZEITBUCHUNG ON ZBU_PLG_FIRMENNR = PLG_FIRMENNR AND ZBU_PLANGRLFDNR = PLG_PLANGRLFDNR" "~"
if joinKuerzel = LEDPLI
searchResult := searchResult "LEFT JOIN BUERO.PLI_PIPELINE ON PLI_PLIID = LED_PLIID" "~"
if joinKuerzel = PLILED
searchResult := searchResult "LEFT JOIN BUERO.LED_LEAD ON LED_PLIID = PLI_PLIID" "~"
if joinKuerzel = FSTPOS
searchResult := searchResult "LEFT JOIN BUERO.POS_POST ON POS_POSTNR = FST_POSTNR AND POS_POSTKZ = FST_POSTKZ" "~"
if joinKuerzel = POSFST
searchResult := searchResult "LEFT JOIN BUERO.FST_FRIST ON FST_POSTNR = POS_POSTNR AND FST_POSTKZ = POS_POSTKZ" "~"
if joinKuerzel = AVEPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = AVE_PRO_FIR_FIRMENNR AND PRO_PROJEKTNR = AVE_PRO_PROJEKTNR AND PRO_PROJEKTTYP = AVE_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_PRO_FIR_FIRMENNR = PRO_FIRMENNR AND AVE_PRO_PROJEKTNR = PRO_PROJEKTNR AND AVE_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = BDLPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = BDL_PRO_FIRMENNR AND PRO_PROJEKTNR = BDL_PRO_PROJEKTNR AND PRO_PROJEKTTYP = BDL_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROBDL
searchResult := searchResult "LEFT JOIN BUERO.BDL_BUERODELETE ON BDL_PRO_FIRMENNR = PRO_FIRMENNR AND BDL_PRO_PROJEKTNR = PRO_PROJEKTNR AND BDL_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = CPRPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = CPR_PRO_FIRMENNR AND PRO_PROJEKTNR = CPR_PROJEKTNR AND PRO_PROJEKTTYP = CPR_PROJEKTTYP" "~"
if joinKuerzel = PROCPR
searchResult := searchResult "LEFT JOIN BUERO.CPR_CRMPROTOKOLL ON CPR_PRO_FIRMENNR = PRO_FIRMENNR AND CPR_PROJEKTNR = PRO_PROJEKTNR AND CPR_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = EVTPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = EVT_PRO_FIRMENNR AND PRO_PROJEKTNR = EVT_PROJEKTNR AND PRO_PROJEKTTYP = EVT_PROJEKTTYP" "~"
if joinKuerzel = PROEVT
searchResult := searchResult "LEFT JOIN BUERO.EVT_EVENT ON EVT_PRO_FIRMENNR = PRO_FIRMENNR AND EVT_PROJEKTNR = PRO_PROJEKTNR AND EVT_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = IPZPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = IPZ_EXTERN_FIRMENNR AND PRO_PROJEKTNR = IPZ_EXTERN_PROJEKTNR AND PRO_PROJEKTTYP = IPZ_EXTERN_PROJEKTTYP" "~"
if joinKuerzel = PROIPZ
searchResult := searchResult "LEFT JOIN BUERO.IPZ_INTERNPROJEKTZU ON IPZ_EXTERN_FIRMENNR = PRO_FIRMENNR AND IPZ_EXTERN_PROJEKTNR = PRO_PROJEKTNR AND IPZ_EXTERN_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = KOSPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = KOS_PRO_FIRMENNR AND PRO_PROJEKTNR = KOS_PRO_PROJEKTNR AND PRO_PROJEKTTYP = KOS_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROKOS
searchResult := searchResult "LEFT JOIN ZLE.KOS_KOSTENFAKTOR ON KOS_PRO_FIRMENNR = PRO_FIRMENNR AND KOS_PRO_PROJEKTNR = PRO_PROJEKTNR AND KOS_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = MEDPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = MED_PRO_FIRMENNR AND PRO_PROJEKTNR = MED_PRO_PROJEKTNR AND PRO_PROJEKTTYP = MED_PRO_PROJEKTTYP" "~"
if joinKuerzel = PROMED
searchResult := searchResult "LEFT JOIN BUERO.MED_MEDIEN ON MED_PRO_FIRMENNR = PRO_FIRMENNR AND MED_PRO_PROJEKTNR = PRO_PROJEKTNR AND MED_PRO_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = PKUPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_PROJEKTID = PKU_HAUPTPROJEKTID" "~"
if joinKuerzel = PROPKU
searchResult := searchResult "LEFT JOIN BUERO.PKU_PROJEKTZU ON PKU_HAUPTPROJEKTID = PRO_PROJEKTID" "~"
if joinKuerzel = PROPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = PRO_BASIS_FIRMENNR AND PRO_PROJEKTNR = PRO_BASIS_PROJEKTNR AND PRO_PROJEKTTYP = PRO_BASIS_PROJEKTTYP" "~"
if joinKuerzel = PROPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_BASIS_FIRMENNR = PRO_FIRMENNR AND PRO_BASIS_PROJEKTNR = PRO_PROJEKTNR AND PRO_BASIS_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = SCJPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = SCJ_FIRMENNR AND PRO_PROJEKTNR = SCJ_PROJEKTNR AND PRO_PROJEKTTYP = SCJ_PROJEKTTYP" "~"
if joinKuerzel = PROSCJ
searchResult := searchResult "LEFT JOIN BMDENTW.SCJ_SCRPROJZU ON SCJ_FIRMENNR = PRO_FIRMENNR AND SCJ_PROJEKTNR = PRO_PROJEKTNR AND SCJ_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = TERPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = TER_PRO_FIRMENNR AND PRO_PROJEKTNR = TER_PROJEKTNR AND PRO_PROJEKTTYP = TER_PROJEKTTYP" "~"
if joinKuerzel = PROTER
searchResult := searchResult "LEFT JOIN BUERO.TER_TERMIN ON TER_PRO_FIRMENNR = PRO_FIRMENNR AND TER_PROJEKTNR = PRO_PROJEKTNR AND TER_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = WRKPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = WRK_PRO_FIRMENNR AND PRO_PROJEKTNR = WRK_PROJEKTNR AND PRO_PROJEKTTYP = WRK_PROJEKTTYP" "~"
if joinKuerzel = PROWRK
searchResult := searchResult "LEFT JOIN WWS.WRK_WWSANKAUFSRNGKOPF ON WRK_PRO_FIRMENNR = PRO_FIRMENNR AND WRK_PROJEKTNR = PRO_PROJEKTNR AND WRK_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = LEIPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = LEI_ORG_PRO_FIRMENNR AND PRO_PROJEKTNR = LEI_ORG_PROJEKTNR AND PRO_PROJEKTTYP = LEI_ORG_PROJEKTTYP" "~"
if joinKuerzel = PROLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_ORG_PRO_FIRMENNR = PRO_FIRMENNR AND LEI_ORG_PROJEKTNR = PRO_PROJEKTNR AND LEI_ORG_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = PROPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FIRMENNR = PRO_FOLGEVON_FIRMENNR AND PRO_PROJEKTNR = PRO_FOLGEVON_PROJEKTNR AND PRO_PROJEKTTYP = PRO_FOLGEVON_PROJEKTTYP" "~"
if joinKuerzel = PROPRO
searchResult := searchResult "LEFT JOIN BUERO.PRO_PROJEKT ON PRO_FOLGEVON_FIRMENNR = PRO_FIRMENNR AND PRO_FOLGEVON_PROJEKTNR = PRO_PROJEKTNR AND PRO_FOLGEVON_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = WIPPRO
searchResult := searchResult "LEFT JOIN PRO_PROJEKT.PRO_PROJEKT ON PRO_FIRMENNR = WIP_CHECKL_FIRMENNR AND PRO_PROJEKTNR = WIP_CHECKL_PROJEKTNR AND PRO_PROJEKTTYP = WIP_CHECKL_PROJEKTTYP" "~"
if joinKuerzel = PROWIP
searchResult := searchResult "LEFT JOIN BIL.WIP_WIPRUEFUNG ON WIP_CHECKL_FIRMENNR = PRO_FIRMENNR AND WIP_CHECKL_PROJEKTNR = PRO_PROJEKTNR AND WIP_CHECKL_PROJEKTTYP = PRO_PROJEKTTYP" "~"
if joinKuerzel = KPOPRP
searchResult := searchResult "LEFT JOIN WWS.PRP_PROVISIONTABPOS ON PRP_PROVTABPOSID = KPO_PROVTABPOSID" "~"
if joinKuerzel = PRPKPO
searchResult := searchResult "LEFT JOIN WWS.KPO_KASSENPROVISION ON KPO_PROVTABPOSID = PRP_PROVTABPOSID" "~"
if joinKuerzel = AGSPRS
searchResult := searchResult "LEFT JOIN PRS_PROZESS.PRS_PROZESS ON PRS_FIRMENNR = AGS_PRS_FIRMENNR AND PRS_PROZESSID = AGS_PROZESSID" "~"
if joinKuerzel = PRSAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_PRS_FIRMENNR = PRS_FIRMENNR AND AGS_PROZESSID = PRS_PROZESSID" "~"
if joinKuerzel = IDPPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_FIRMENNR = IDP_PRS_FIRMENNR AND PRS_PROZESSID = IDP_PROZESSID" "~"
if joinKuerzel = PRSIDP
searchResult := searchResult "LEFT JOIN PRODUKTION.IDP_INFO_DELAY_BREAKDOWN ON IDP_PRS_FIRMENNR = PRS_FIRMENNR AND IDP_PROZESSID = PRS_PROZESSID" "~"
if joinKuerzel = PRSPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_FIRMENNR = PRS_REFPRS_FIRMENNR AND PRS_PROZESSID = PRS_REF_PROZESSID" "~"
if joinKuerzel = PRSPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_REFPRS_FIRMENNR = PRS_FIRMENNR AND PRS_REF_PROZESSID = PRS_PROZESSID" "~"
if joinKuerzel = SLPPRS
searchResult := searchResult "LEFT JOIN PRS_PROZESS.PRS_PROZESS ON PRS_FIRMENNR = SLP_PRS_FIRMENNR AND PRS_PROZESSID = SLP_PRS_PROZESSID" "~"
if joinKuerzel = PRSSLP
searchResult := searchResult "LEFT JOIN WWS.SLP_STUECKLISTENPOSITION ON SLP_PRS_FIRMENNR = PRS_FIRMENNR AND SLP_PRS_PROZESSID = PRS_PROZESSID" "~"
if joinKuerzel = PRZPRS
searchResult := searchResult "LEFT JOIN PRODUKTION.PRS_PROZESS ON PRS_FIRMENNR = PRZ_FIRMENNR AND PRS_PROZESSID = PRZ_PROZESSID_ALTERNATIV_4" "~"
if joinKuerzel = PRSPRZ
searchResult := searchResult "LEFT JOIN PRODUKTION.PRZ_PROZESSBAZZU ON PRZ_FIRMENNR = PRS_FIRMENNR AND PRZ_PROZESSID_ALTERNATIV_4 = PRS_PROZESSID" "~"
if joinKuerzel = PRTPRT
searchResult := searchResult "LEFT JOIN BUERO.PRT_PROJEKTTAETIGKEIT ON PRT_PTNR = PRT_HAUPT_PTNR" "~"
if joinKuerzel = PRTPRT
searchResult := searchResult "LEFT JOIN BUERO.PRT_PROJEKTTAETIGKEIT ON PRT_HAUPT_PTNR = PRT_PTNR" "~"
if joinKuerzel = KAPPSG
searchResult := searchResult "LEFT JOIN WWS.PSG_PREISGRUPPE ON PSG_FIRMENNR = KAP_ARL_FIRMENNR AND PSG_PREISHAUPTGRNR = KAP_PSG_PREISHAUPTGRNR AND PSG_PREISUNTERGRNR = KAP_PSG_PREISUNTERGRNR" "~"
if joinKuerzel = PSGKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_ARL_FIRMENNR = PSG_FIRMENNR AND KAP_PSG_PREISHAUPTGRNR = PSG_PREISHAUPTGRNR AND KAP_PSG_PREISUNTERGRNR = PSG_PREISUNTERGRNR" "~"
if joinKuerzel = RNRPSG
searchResult := searchResult "LEFT JOIN WWS.PSG_PREISGRUPPE ON PSG_FIRMENNR = RNR_HAUPTFIRMENNR AND PSG_PREISHAUPTGRNR = RNR_PREISHAUPTGRNR AND PSG_PREISUNTERGRNR = RNR_PREISUNTERGRNR" "~"
if joinKuerzel = PSGRNR
searchResult := searchResult "LEFT JOIN WWS.RNR_RECHNUNGSRABATT ON RNR_HAUPTFIRMENNR = PSG_FIRMENNR AND RNR_PREISHAUPTGRNR = PSG_PREISHAUPTGRNR AND RNR_PREISUNTERGRNR = PSG_PREISUNTERGRNR" "~"
if joinKuerzel = BREPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = BRE_FIRMENNR AND PSJ_MITARBEITERID = BRE_MITARBEITERID AND PSJ_DIENSTVLFDNR = BRE_DIENSTVLFDNR AND PSJ_LOHNJAHR = BRE_LOHNJAHR" "~"
if joinKuerzel = PSJBRE
searchResult := searchResult "LEFT JOIN HRS.BRE_BRUTTOERFASSUNG ON BRE_FIRMENNR = PSJ_FIRMENNR AND BRE_MITARBEITERID = PSJ_MITARBEITERID AND BRE_DIENSTVLFDNR = PSJ_DIENSTVLFDNR AND BRE_LOHNJAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = IPUPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = IPU_PSJ_FIRMENNR AND PSJ_MITARBEITERID = IPU_PSJ_MITARBEITERID AND PSJ_DIENSTVLFDNR = IPU_PSJ_DIENSTVLFDNR AND PSJ_LOHNJAHR = IPU_PSJ_LOHNJAHR" "~"
if joinKuerzel = PSJIPU
searchResult := searchResult "LEFT JOIN HRS.IPU_IMPORTPUFFER ON IPU_PSJ_FIRMENNR = PSJ_FIRMENNR AND IPU_PSJ_MITARBEITERID = PSJ_MITARBEITERID AND IPU_PSJ_DIENSTVLFDNR = PSJ_DIENSTVLFDNR AND IPU_PSJ_LOHNJAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = REIPSJ
searchResult := searchResult "LEFT JOIN HRS.PSJ_PERSONALSTAMM_JAHR ON PSJ_FIRMENNR = REI_PSJ_FIRMENNR AND PSJ_MITARBEITERID = REI_PSJ_MITARBEITERID AND PSJ_DIENSTVLFDNR = REI_PSJ_DIENSTVLFDNR AND PSJ_LOHNJAHR = REI_PSJ_LOHNJAHR" "~"
if joinKuerzel = PSJREI
searchResult := searchResult "LEFT JOIN HRS.REI_REISESTAMM ON REI_PSJ_FIRMENNR = PSJ_FIRMENNR AND REI_PSJ_MITARBEITERID = PSJ_MITARBEITERID AND REI_PSJ_DIENSTVLFDNR = PSJ_DIENSTVLFDNR AND REI_PSJ_LOHNJAHR = PSJ_LOHNJAHR" "~"
if joinKuerzel = PSEPSK
searchResult := searchResult "LEFT JOIN BIL.PSK_PERSRST_STERBET_KOPF ON PSK_PSKLFDNR = PSE_PSKLFDNR" "~"
if joinKuerzel = PSKPSE
searchResult := searchResult "LEFT JOIN BIL.PSE_PERSRST_STERBET_WERT ON PSE_PSKLFDNR = PSK_PSKLFDNR" "~"
if joinKuerzel = ABGPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_FIRMENNR = ABG_FIRMENNR AND PSM_MITARBEITERID = ABG_MITARBEITERID AND PSM_DIENSTVLFDNR = ABG_DIENSTVLFDNR AND PSM_PSMJAHR = ABG_JAHR AND PSM_PSMMONAT = ABG_MONAT AND PSM_PSMISTORG = ABG_PSMISTORG" "~"
if joinKuerzel = PSMABG
searchResult := searchResult "LEFT JOIN HRS.ABG_ABRECHNUNG ON ABG_FIRMENNR = PSM_FIRMENNR AND ABG_MITARBEITERID = PSM_MITARBEITERID AND ABG_DIENSTVLFDNR = PSM_DIENSTVLFDNR AND ABG_JAHR = PSM_PSMJAHR AND ABG_MONAT = PSM_PSMMONAT AND ABG_PSMISTORG = PSM_PSMISTORG" "~"
if joinKuerzel = KSZPSM
searchResult := searchResult "LEFT JOIN HRS.PSM_PERSONALSTAMM_MONAT ON PSM_FIRMENNR = KSZ_FIRMENNR AND PSM_MITARBEITERID = KSZ_MITARBEITERID AND PSM_DIENSTVLFDNR = KSZ_DVNR AND PSM_PSMJAHR = KSZ_JAHR AND PSM_PSMMONAT = KSZ_LOHNMONAT AND PSM_PSMISTORG = KSZ_PSMISTORG" "~"
if joinKuerzel = PSMKSZ
searchResult := searchResult "LEFT JOIN HRS.KSZ_BBKALKSZ ON KSZ_FIRMENNR = PSM_FIRMENNR AND KSZ_MITARBEITERID = PSM_MITARBEITERID AND KSZ_DVNR = PSM_DIENSTVLFDNR AND KSZ_JAHR = PSM_PSMJAHR AND KSZ_LOHNMONAT = PSM_PSMMONAT AND KSZ_PSMISTORG = PSM_PSMISTORG" "~"
if joinKuerzel = PSSPST
searchResult := searchResult "LEFT JOIN PRODUKTION.PST_STAPEL ON PST_FIRMENNR = PSS_FIRMENNR AND PST_NR = PSS_NR" "~"
if joinKuerzel = PSTPSS
searchResult := searchResult "LEFT JOIN PRODUKTION.PSS_STAPEL_SCHRITT ON PSS_FIRMENNR = PST_FIRMENNR AND PSS_NR = PST_NR" "~"
if joinKuerzel = EVAPSZ
searchResult := searchResult "LEFT JOIN BUERO.PSZ_PERSONENKZ ON PSZ_PERSONENART = EVA_PERSONENART AND PSZ_PERSONKZNR = EVA_PERSONKZNR" "~"
if joinKuerzel = PSZEVA
searchResult := searchResult "LEFT JOIN BUERO.EVA_EVENTTEAM ON EVA_PERSONENART = PSZ_PERSONENART AND EVA_PERSONKZNR = PSZ_PERSONKZNR" "~"
if joinKuerzel = PTSPTP
searchResult := searchResult "LEFT JOIN WWS.PTP_PREISTABPOS ON PTP_FIRMENNR = PTS_FIRMENNR AND PTP_PREISTABNR = PTS_PREISTABNR AND PTP_PREISTABPOSNR = PTS_PREISTABPOSNR" "~"
if joinKuerzel = PTPPTS
searchResult := searchResult "LEFT JOIN WWS.PTS_PREISTABPOSSTAFFEL ON PTS_FIRMENNR = PTP_FIRMENNR AND PTS_PREISTABNR = PTP_PREISTABNR AND PTS_PREISTABPOSNR = PTP_PREISTABPOSNR" "~"
if joinKuerzel = VOBPUC
searchResult := searchResult "LEFT JOIN PRODUKTION.PUC_PPSUSERSTATICOMBI ON PUC_PUCID = VOB_PUC_PUCID" "~"
if joinKuerzel = PUCVOB
searchResult := searchResult "LEFT JOIN PRODUKTION.VOB_VORGANGSBENACHR ON VOB_PUC_PUCID = PUC_PUCID" "~"
if joinKuerzel = PVZPVK
searchResult := searchResult "LEFT JOIN WWS.PVK_PROVISIONTAB ON PVK_PROVTABID = PVZ_PROVTABID" "~"
if joinKuerzel = PVKPVZ
searchResult := searchResult "LEFT JOIN WWS.PVZ_PROVISIONTABZU ON PVZ_PROVTABID = PVK_PROVTABID" "~"
if joinKuerzel = PWBPWK
searchResult := searchResult "LEFT JOIN WWS.PWK_PREISRABATTWARTKOPF ON PWK_PWKID = PWB_PWKID" "~"
if joinKuerzel = PWKPWB
searchResult := searchResult "LEFT JOIN WWS.PWB_PWKRABATTTABZU ON PWB_PWKID = PWK_PWKID" "~"
if joinKuerzel = PWFPWK
searchResult := searchResult "LEFT JOIN WWS.PWK_PREISRABATTWARTKOPF ON PWK_PWKID = PWF_PWKID" "~"
if joinKuerzel = PWKPWF
searchResult := searchResult "LEFT JOIN WWS.PWF_PWKFIRMENZU ON PWF_PWKID = PWK_PWKID" "~"
if joinKuerzel = QAZQAW
searchResult := searchResult "LEFT JOIN BUERO.QAW_QUOTENAUSWERTUNG ON QAW_QAUSWERTID = QAZ_QAUSWERTID" "~"
if joinKuerzel = QAWQAZ
searchResult := searchResult "LEFT JOIN BUERO.QAZ_QUOTENAUSWERTUNGZU ON QAZ_QAUSWERTID = QAW_QAUSWERTID" "~"
if joinKuerzel = CHGQMK
searchResult := searchResult "LEFT JOIN QMK_QM_KOPF.QMK_QM_KOPF ON QMK_QM_KOPF_ID = CHG_QM_KOPF_ID" "~"
if joinKuerzel = QMKCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_QM_KOPF_ID = QMK_QM_KOPF_ID" "~"
if joinKuerzel = QDAQST
searchResult := searchResult "LEFT JOIN PRODUKTION.QST_QM_STAMM ON QST_FIRMENNR = QDA_QST_FIRMENNR AND QST_QM_NR = QDA_QST_QM_NR" "~"
if joinKuerzel = QSTQDA
searchResult := searchResult "LEFT JOIN PRODUKTION.QDA_QM_DATA ON QDA_QST_FIRMENNR = QST_FIRMENNR AND QDA_QST_QM_NR = QST_QM_NR" "~"
if joinKuerzel = RASRAP
searchResult := searchResult "LEFT JOIN WWS.RAP_RABATTTABPOS ON RAP_FIRMENNR = RAS_FIRMENNR AND RAP_RABTABNR = RAS_RABTABNR AND RAP_RABTABPOSNR = RAS_RABTABPOSNR" "~"
if joinKuerzel = RAPRAS
searchResult := searchResult "LEFT JOIN WWS.RAS_RABATTTABPOSSTAFFEL ON RAS_FIRMENNR = RAP_FIRMENNR AND RAS_RABTABNR = RAP_RABTABNR AND RAS_RABTABPOSNR = RAP_RABTABPOSNR" "~"
if joinKuerzel = BSKRAT
searchResult := searchResult "LEFT JOIN WWS.RAT_RABATTTAB ON RAT_FIRMENNR = BSK_BONUSTABELLEN_FIRMENNR AND RAT_RABTABNR = BSK_BONUSTABELLENNR" "~"
if joinKuerzel = RATBSK
searchResult := searchResult "LEFT JOIN WWS.BSK_BONUSERMKOPF ON BSK_BONUSTABELLEN_FIRMENNR = RAT_FIRMENNR AND BSK_BONUSTABELLENNR = RAT_RABTABNR" "~"
if joinKuerzel = AVERAZ
searchResult := searchResult "LEFT JOIN RAZ_RATENZAHLUNG.RAZ_RATENZAHLUNG ON RAZ_RATENMODELLID = AVE_RATENMODELLID" "~"
if joinKuerzel = RAZAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_RATENMODELLID = RAZ_RATENMODELLID" "~"
if joinKuerzel = RDDRDM
searchResult := searchResult "LEFT JOIN BIL.RDM_RSTDRUCKMASTER ON RDM_RDMNR = RDD_RDMNR" "~"
if joinKuerzel = RDMRDD
searchResult := searchResult "LEFT JOIN BIL.RDD_RSTDRUCKDETAIL ON RDD_RDMNR = RDM_RDMNR" "~"
if joinKuerzel = REDREG
searchResult := searchResult "LEFT JOIN REG_REGION.REG_REGION ON REG_LANDNR = RED_REG_LANDNR AND REG_REGIONNR = RED_REG_REGIONNR" "~"
if joinKuerzel = REGRED
searchResult := searchResult "LEFT JOIN HRS.RED_REISEDETAIL ON RED_REG_LANDNR = REG_LANDNR AND RED_REG_REGIONNR = REG_REGIONNR" "~"
if joinKuerzel = RZIREI
searchResult := searchResult "LEFT JOIN HRS.REI_REISESTAMM ON REI_REISENUMMER = RZI_REISENUMMER" "~"
if joinKuerzel = REIRZI
searchResult := searchResult "LEFT JOIN HRS.RZI_REISEZIELE ON RZI_REISENUMMER = REI_REISENUMMER" "~"
if joinKuerzel = PERREL
searchResult := searchResult "LEFT JOIN BUERO.REL_RELIGION ON REL_RELIGIONSNR = PER_RELIGIONSNR" "~"
if joinKuerzel = RELPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_RELIGIONSNR = REL_RELIGIONSNR" "~"
if joinKuerzel = PKMREL
searchResult := searchResult "LEFT JOIN REL_RELIGION.REL_RELIGION ON REL_RELIGIONSNR = PKM_REL_RELIGIONSNR" "~"
if joinKuerzel = RELPKM
searchResult := searchResult "LEFT JOIN PRODUKTION.PKM_KM_MASTER ON PKM_REL_RELIGIONSNR = REL_RELIGIONSNR" "~"
if joinKuerzel = MEVREP
searchResult := searchResult "LEFT JOIN BUERO.REP_RESSOURCENPAKET ON REP_RESPAKETID = MEV_RESPAKETID" "~"
if joinKuerzel = REPMEV
searchResult := searchResult "LEFT JOIN BUERO.MEV_MEDIENVERLEIH ON MEV_RESPAKETID = REP_RESPAKETID" "~"
if joinKuerzel = RSBREP
searchResult := searchResult "LEFT JOIN BUERO.REP_RESSOURCENPAKET ON REP_RESPAKETID = RSB_RESPAKETID" "~"
if joinKuerzel = REPRSB
searchResult := searchResult "LEFT JOIN BUERO.RSB_RESSOURCENBEDARF ON RSB_RESPAKETID = REP_RESPAKETID" "~"
if joinKuerzel = BOPRER
searchResult := searchResult "LEFT JOIN WWS.RER_RESERVIERUNGSREFERENZ ON RER_RESERVIERUNGS_ID_BMD = BOP_RESERVIERUNGS_ID_BMD" "~"
if joinKuerzel = RERBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_RESERVIERUNGS_ID_BMD = RER_RESERVIERUNGS_ID_BMD" "~"
if joinKuerzel = KGVRFM
searchResult := searchResult "LEFT JOIN RFM_RECHTSFORM.RFM_RECHTSFORM ON RFM_LANDNR = KGV_LANDNR AND RFM_RECHTSFORMNR = KGV_RECHTSFORMNR" "~"
if joinKuerzel = RFMKGV
searchResult := searchResult "LEFT JOIN FIBU.KGV_KONTENGUVVERBUCHUNG ON KGV_LANDNR = RFM_LANDNR AND KGV_RECHTSFORMNR = RFM_RECHTSFORMNR" "~"
if joinKuerzel = RIARIH
searchResult := searchResult "LEFT JOIN PRODUKTION.RIH_RISIKOANALYSE_HEAD ON RIH_RIHID = RIA_RIHID" "~"
if joinKuerzel = RIHRIA
searchResult := searchResult "LEFT JOIN PRODUKTION.RIA_RISIKOANALYSE ON RIA_RIHID = RIH_RIHID" "~"
if joinKuerzel = KSMRKS
searchResult := searchResult "LEFT JOIN RKS_REPLIKATIONSSTANDORT.RKS_REPLIKATIONSSTANDORT ON RKS_REPLSTANDONR = KSM_ERSTELLREPLSTANDONR" "~"
if joinKuerzel = RKSKSM
searchResult := searchResult "LEFT JOIN WWS.KSM_KASSENSUMMEN ON KSM_ERSTELLREPLSTANDONR = RKS_REPLSTANDONR" "~"
if joinKuerzel = PNKRKS
searchResult := searchResult "LEFT JOIN RKS_REPLIKATIONSSTANDORT.RKS_REPLIKATIONSSTANDORT ON RKS_REPLSTANDONR = PNK_REPLSTANDONR" "~"
if joinKuerzel = RKSPNK
searchResult := searchResult "LEFT JOIN BUERO.PNK_PERSONNUMMERNKREIS ON PNK_REPLSTANDONR = RKS_REPLSTANDONR" "~"
if joinKuerzel = BONRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = BON_RNG_FIRMENNR AND RNG_RECHNUNGLFDNR = BON_RNG_RECHNUNGLFDNR" "~"
if joinKuerzel = RNGBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_RNG_FIRMENNR = RNG_FIRMENNR AND BON_RNG_RECHNUNGLFDNR = RNG_RECHNUNGLFDNR" "~"
if joinKuerzel = RPRRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = RPR_FIRMENNR AND RNG_RECHNUNGLFDNR = RPR_RECHNUNGLFDNR" "~"
if joinKuerzel = RNGRPR
searchResult := searchResult "LEFT JOIN WWS.RPR_RECHNUNGSPROVISION ON RPR_FIRMENNR = RNG_FIRMENNR AND RPR_RECHNUNGLFDNR = RNG_RECHNUNGLFDNR" "~"
if joinKuerzel = VEPRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_FIRMENNR = VEP_RNG_FIRMENNR AND RNG_RECHNUNGLFDNR = VEP_RNG_RECHNUNGLFDNR" "~"
if joinKuerzel = RNGVEP
searchResult := searchResult "LEFT JOIN WWS.VEP_VERTRETERABRECHPOS ON VEP_RNG_FIRMENNR = RNG_FIRMENNR AND VEP_RNG_RECHNUNGLFDNR = RNG_RECHNUNGLFDNR" "~"
if joinKuerzel = LAJRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = LAJ_BUCHUNGS_FIRMENNR AND RNP_RECHNUNGLFDNR = LAJ_RECHNUNGLFDNR AND RNP_RECHNUNGSPOSLFDNR = LAJ_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = RNPLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_BUCHUNGS_FIRMENNR = RNP_FIRMENNR AND LAJ_RECHNUNGLFDNR = RNP_RECHNUNGLFDNR AND LAJ_RECHNUNGSPOSLFDNR = RNP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = EPSRPM
searchResult := searchResult "LEFT JOIN RPM_RETRO_PRODUKT_MATERIAL.RPM_RETRO_PRODUKT_MATERIAL ON RPM_RPMID = EPS_RPMID" "~"
if joinKuerzel = RPMEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_RPMID = RPM_RPMID" "~"
if joinKuerzel = FSORPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = FSO_REPORTNR_HTMLVORLAGE" "~"
if joinKuerzel = RPTFSO
searchResult := searchResult "LEFT JOIN FIBU.FSO_SORTIERPOS ON FSO_REPORTNR_HTMLVORLAGE = RPT_REPORTNR" "~"
if joinKuerzel = SBSRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = SBS_REPORTNR" "~"
if joinKuerzel = RPTSBS
searchResult := searchResult "LEFT JOIN BUERO.SBS_STELLENBESCHREIBUNG ON SBS_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = WFNRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = WFN_RPT_REPORTNR" "~"
if joinKuerzel = RPTWFN
searchResult := searchResult "LEFT JOIN BUERO.WFN_WORKFLOWACTION ON WFN_RPT_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = DRVRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = DRV_REP2_REPORTNR" "~"
if joinKuerzel = RPTDRV
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DRV_DREPORTVORSCHLAG ON DRV_REP2_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = KASRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = KAS_BELEGDRUCK_REPORTNR" "~"
if joinKuerzel = RPTKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_BELEGDRUCK_REPORTNR = RPT_REPORTNR" "~"
if joinKuerzel = ZAWRPT
searchResult := searchResult "LEFT JOIN RPT_REPORT.RPT_REPORT ON RPT_REPORTNR = ZAW_REPORTNR_PLGTXT" "~"
if joinKuerzel = RPTZAW
searchResult := searchResult "LEFT JOIN BUERO.ZAW_ZEITAUSWERTUNG ON ZAW_REPORTNR_PLGTXT = RPT_REPORTNR" "~"
if joinKuerzel = MEVRSB
searchResult := searchResult "LEFT JOIN BUERO.RSB_RESSOURCENBEDARF ON RSB_RSBID = MEV_RSBID" "~"
if joinKuerzel = RSBMEV
searchResult := searchResult "LEFT JOIN BUERO.MEV_MEDIENVERLEIH ON MEV_RSBID = RSB_RSBID" "~"
if joinKuerzel = RSDRSD
searchResult := searchResult "LEFT JOIN WWS.RSD_REGKASPROTOKOLL ON RSD_REGKASPROTOKOLLID = RSD_VORIGERBELEGID" "~"
if joinKuerzel = RSDRSD
searchResult := searchResult "LEFT JOIN WWS.RSD_REGKASPROTOKOLL ON RSD_VORIGERBELEGID = RSD_REGKASPROTOKOLLID" "~"
if joinKuerzel = KASRSI
searchResult := searchResult "LEFT JOIN WWS.RSI_REGKASIGNATUREH ON RSI_FIRMENNR = KAS_RSI_FIRMENNR AND RSI_SIGNATUREHLFDNR = KAS_RSI_SIGNATUREHLFDNR" "~"
if joinKuerzel = RSIKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_RSI_FIRMENNR = RSI_FIRMENNR AND KAS_RSI_SIGNATUREHLFDNR = RSI_SIGNATUREHLFDNR" "~"
if joinKuerzel = RTZRVD
searchResult := searchResult "LEFT JOIN ZLE.RVD_RECHVORLAGEDETAIL ON RVD_RVDID = RTZ_RVDID" "~"
if joinKuerzel = RVDRTZ
searchResult := searchResult "LEFT JOIN ZLE.RTZ_RECHTEXTZU ON RTZ_RVDID = RVD_RVDID" "~"
if joinKuerzel = SABSAB
searchResult := searchResult "LEFT JOIN BUERO.SAB_STELLENAUSBILDUNG ON SAB_STELLAUSBID = SAB_ABHAENGIG_VON" "~"
if joinKuerzel = SABSAB
searchResult := searchResult "LEFT JOIN BUERO.SAB_STELLENAUSBILDUNG ON SAB_ABHAENGIG_VON = SAB_STELLAUSBID" "~"
if joinKuerzel = ABOSAK
searchResult := searchResult "LEFT JOIN SAK_SACHKONTO.SAK_SACHKONTO ON SAK_FIRMENNR = ABO_SAK_FIRMENNR AND SAK_FIBUNR = ABO_SAK_FIBUNR AND SAK_KONTONR = ABO_SAK_KONTONR" "~"
if joinKuerzel = SAKABO
searchResult := searchResult "LEFT JOIN BIL.ABO_ANBUBUCHUNGKONTEN ON ABO_SAK_FIRMENNR = SAK_FIRMENNR AND ABO_SAK_FIBUNR = SAK_FIBUNR AND ABO_SAK_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = BTPSAK
searchResult := searchResult "LEFT JOIN FIBU.SAK_SACHKONTO ON SAK_FIRMENNR = BTP_FIRMENNR AND SAK_FIBUNR = BTP_FIBUNR AND SAK_KONTONR = BTP_KONTONR" "~"
if joinKuerzel = SAKBTP
searchResult := searchResult "LEFT JOIN FIBU.BTP_BUDGETTILGUNGSPLAN ON BTP_FIRMENNR = SAK_FIRMENNR AND BTP_FIBUNR = SAK_FIBUNR AND BTP_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = BWESAK
searchResult := searchResult "LEFT JOIN FIBU.SAK_SACHKONTO ON SAK_FIRMENNR = BWE_FIRMENNR AND SAK_FIBUNR = BWE_FIBUNR AND SAK_KONTONR = BWE_WEK_KONTONR" "~"
if joinKuerzel = SAKBWE
searchResult := searchResult "LEFT JOIN FIBU.BWE_BUDGETWARENEINKPLAN ON BWE_FIRMENNR = SAK_FIRMENNR AND BWE_FIBUNR = SAK_FIBUNR AND BWE_WEK_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = SAZSAK
searchResult := searchResult "LEFT JOIN SAK_SACHKONTO.SAK_SACHKONTO ON SAK_FIRMENNR = SAZ_SAK_FIRMENNR AND SAK_FIBUNR = SAZ_SAK_FIBUNR AND SAK_KONTONR = SAZ_SAK_KONTONR" "~"
if joinKuerzel = SAKSAZ
searchResult := searchResult "LEFT JOIN BUERO.SAZ_SACHKONTOARCHIVZU ON SAZ_SAK_FIRMENNR = SAK_FIRMENNR AND SAZ_SAK_FIBUNR = SAK_FIBUNR AND SAZ_SAK_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = BPASAK
searchResult := searchResult "LEFT JOIN FIBU.SAK_SACHKONTO ON SAK_FIRMENNR = BPA_FIRMENNR AND SAK_FIBUNR = BPA_FIBUNR AND SAK_KONTONR = BPA_PLANBIL_KONTONR" "~"
if joinKuerzel = SAKBPA
searchResult := searchResult "LEFT JOIN FIBU.BPA_BUDGETPLANBILAUFTLG ON BPA_FIRMENNR = SAK_FIRMENNR AND BPA_FIBUNR = SAK_FIBUNR AND BPA_PLANBIL_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = BTPSAK
searchResult := searchResult "LEFT JOIN FIBU.SAK_SACHKONTO ON SAK_FIRMENNR = BTP_FIRMENNR AND SAK_FIBUNR = BTP_FIBUNR AND SAK_KONTONR = BTP_ZINSEN_KONTONR" "~"
if joinKuerzel = SAKBTP
searchResult := searchResult "LEFT JOIN FIBU.BTP_BUDGETTILGUNGSPLAN ON BTP_FIRMENNR = SAK_FIRMENNR AND BTP_FIBUNR = SAK_FIBUNR AND BTP_ZINSEN_KONTONR = SAK_KONTONR" "~"
if joinKuerzel = AFTSAP
searchResult := searchResult "LEFT JOIN SAP_SAMMELPROJEKT.SAP_SAMMELPROJEKT ON SAP_FIRMENNR = AFT_SAP_FIRMENNR AND SAP_SAMMELPROJEKTNR = AFT_SAMMELPROJEKTNR" "~"
if joinKuerzel = SAPAFT
searchResult := searchResult "LEFT JOIN ZLE.AFT_AUFBEREITUNG ON AFT_SAP_FIRMENNR = SAP_FIRMENNR AND AFT_SAMMELPROJEKTNR = SAP_SAMMELPROJEKTNR" "~"
if joinKuerzel = LFSSBC
searchResult := searchResult "LEFT JOIN SBC_SPEZBEWEGUNGSCODE.SBC_SPEZBEWEGUNGSCODE ON SBC_FIRMENNR = LFS_SBC_FIRMENNR AND SBC_SBCCODE = LFS_SBC_SBCCODE" "~"
if joinKuerzel = SBCLFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_SBC_FIRMENNR = SBC_FIRMENNR AND LFS_SBC_SBCCODE = SBC_SBCCODE" "~"
if joinKuerzel = SBFSBG
searchResult := searchResult "LEFT JOIN BMD.SBG_STDBRIEFGRUPPE ON SBG_BRIEFGRUPPENNR = SBF_BRIEFGRUPPENNR" "~"
if joinKuerzel = SBGSBF
searchResult := searchResult "LEFT JOIN BMD.SBF_STDBRIEF ON SBF_BRIEFGRUPPENNR = SBG_BRIEFGRUPPENNR" "~"
if joinKuerzel = SCOSCE
searchResult := searchResult "LEFT JOIN PRODUKTION.SCE_SCORECARD ON SCE_SCEID = SCO_SCEID" "~"
if joinKuerzel = SCESCO
searchResult := searchResult "LEFT JOIN PRODUKTION.SCO_SCINSTANZ ON SCO_SCEID = SCE_SCEID" "~"
if joinKuerzel = RIHSCK
searchResult := searchResult "LEFT JOIN PRODUKTION.SCK_SCKENNZAHL ON SCK_SCKID = RIH_SCKID_DETECTION" "~"
if joinKuerzel = SCKRIH
searchResult := searchResult "LEFT JOIN PRODUKTION.RIH_RISIKOANALYSE_HEAD ON RIH_SCKID_DETECTION = SCK_SCKID" "~"
if joinKuerzel = SCISCL
searchResult := searchResult "LEFT JOIN PRODUKTION.SCL_SCKENNZAHLDET ON SCL_SCLID = SCI_SCLID" "~"
if joinKuerzel = SCLSCI
searchResult := searchResult "LEFT JOIN PRODUKTION.SCI_SCINSTANZDET ON SCI_SCLID = SCL_SCLID" "~"
if joinKuerzel = TIZSCM
searchResult := searchResult "LEFT JOIN BMDENTW.SCM_SCRMAIN ON SCM_SCRNR = TIZ_SCRNR" "~"
if joinKuerzel = SCMTIZ
searchResult := searchResult "LEFT JOIN BMDENTW.TIZ_TICKETPEPAUFGABEZU ON TIZ_SCRNR = SCM_SCRNR" "~"
if joinKuerzel = TKTSCM
searchResult := searchResult "LEFT JOIN BMDENTW.SCM_SCRMAIN ON SCM_SCRNR = TKT_SCRNR" "~"
if joinKuerzel = SCMTKT
searchResult := searchResult "LEFT JOIN BMDENTW.TKT_TICKETBEREICHZU ON TKT_SCRNR = SCM_SCRNR" "~"
if joinKuerzel = DVHSEK
searchResult := searchResult "LEFT JOIN BUERO.SEK_SEKRETARIAT ON SEK_FIRMENNR = DVH_SEK_FIRMENNR AND SEK_SEKRETARIATSNR = DVH_SEKRETARIATSNR" "~"
if joinKuerzel = SEKDVH
searchResult := searchResult "LEFT JOIN BUERO.DVH_DIENSTVERHAELTNIS ON DVH_SEK_FIRMENNR = SEK_FIRMENNR AND DVH_SEKRETARIATSNR = SEK_SEKRETARIATSNR" "~"
if joinKuerzel = SLRSFR
searchResult := searchResult "LEFT JOIN BUERO.SFR_SKILLFINDERREGEL ON SFR_SKILLREGELID = SLR_SKILLREGELID" "~"
if joinKuerzel = SFRSLR
searchResult := searchResult "LEFT JOIN BUERO.SLR_SKILLREGELELEMENT ON SLR_SKILLREGELID = SFR_SKILLREGELID" "~"
if joinKuerzel = SFFSFR
searchResult := searchResult "LEFT JOIN BUERO.SFR_SKILLFINDERREGEL ON SFR_SKILLREGELID = SFF_SFR_SKILLREGELID_IFT" "~"
if joinKuerzel = SFRSFF
searchResult := searchResult "LEFT JOIN BUERO.SFF_SKILLFINDERFILTER ON SFF_SFR_SKILLREGELID_IFT = SFR_SKILLREGELID" "~"
if joinKuerzel = PICSFZ
searchResult := searchResult "LEFT JOIN SFZ_SYNCFELDZUORDNUNG.SFZ_SYNCFELDZUORDNUNG ON SFZ_SYNCFELDZUID = PIC_SYNCFELDZUID_PREIS_EP" "~"
if joinKuerzel = SFZPIC
searchResult := searchResult "LEFT JOIN WWS.PIC_PREISIMPORTCAT ON PIC_SYNCFELDZUID_PREIS_EP = SFZ_SYNCFELDZUID" "~"
if joinKuerzel = FTGSID
searchResult := searchResult "LEFT JOIN SID_HRSSEQ.SID_HRSSEQ ON SID_HRSSEQID = FTG_HRSSEQID" "~"
if joinKuerzel = SIDFTG
searchResult := searchResult "LEFT JOIN BUERO.FTG_FEIERTAG ON FTG_HRSSEQID = SID_HRSSEQID" "~"
if joinKuerzel = KOWSID
searchResult := searchResult "LEFT JOIN HRS.SID_HRSSEQ ON SID_HRSSEQID = KOW_HRSSEQID" "~"
if joinKuerzel = SIDKOW
searchResult := searchResult "LEFT JOIN HRS.KOW_KOLLEKTIVVERTRAGWERTE ON KOW_HRSSEQID = SID_HRSSEQID" "~"
if joinKuerzel = REISID
searchResult := searchResult "LEFT JOIN HRS.SID_HRSSEQ ON SID_HRSSEQID = REI_SID_KVID" "~"
if joinKuerzel = SIDREI
searchResult := searchResult "LEFT JOIN HRS.REI_REISESTAMM ON REI_SID_KVID = SID_HRSSEQID" "~"
if joinKuerzel = SZUSID
searchResult := searchResult "LEFT JOIN HRS.SID_HRSSEQ ON SID_HRSSEQID = SZU_KVID" "~"
if joinKuerzel = SIDSZU
searchResult := searchResult "LEFT JOIN HRS.SZU_SCHNITTLAZU ON SZU_KVID = SID_HRSSEQID" "~"
if joinKuerzel = BVOSLK
searchResult := searchResult "LEFT JOIN WWS.SLK_STUECKLISTENKOPF ON SLK_STKLISTKOPFID = BVO_SLK_STKLISTKOPFID" "~"
if joinKuerzel = SLKBVO
searchResult := searchResult "LEFT JOIN WWS.BVO_BESTELLVORSCHLAG ON BVO_SLK_STKLISTKOPFID = SLK_STKLISTKOPFID" "~"
if joinKuerzel = EPSSLK
searchResult := searchResult "LEFT JOIN WWS.SLK_STUECKLISTENKOPF ON SLK_STKLISTKOPFID = EPS_STKLISTKOPFID" "~"
if joinKuerzel = SLKEPS
searchResult := searchResult "LEFT JOIN WWS.EPS_ERFASSPOS_STUECKLISTE ON EPS_STKLISTKOPFID = SLK_STKLISTKOPFID" "~"
if joinKuerzel = SLPSLK
searchResult := searchResult "LEFT JOIN WWS.SLK_STUECKLISTENKOPF ON SLK_STKLISTKOPFID = SLP_STKLISTPOSID" "~"
if joinKuerzel = SLKSLP
searchResult := searchResult "LEFT JOIN WWS.SLP_STUECKLISTENPOSITION ON SLP_STKLISTPOSID = SLK_STKLISTKOPFID" "~"
if joinKuerzel = SBASMA
searchResult := searchResult "LEFT JOIN HRS.SMA_SCHNITTMA ON SMA_FIRMENNR = SBA_FIRMENNR AND SMA_MITARBEITERID = SBA_MITARBEITERID AND SMA_DIENSTVLFDNR = SBA_DIENSTVLFDNR AND SMA_LOHNJAHR = SBA_LOHNJAHR AND SMA_DBSNR = SBA_DBSNR AND SMA_SCHNITTNR = SBA_SCHNITTNR" "~"
if joinKuerzel = SMASBA
searchResult := searchResult "LEFT JOIN HRS.SBA_SCHNITTBASIS ON SBA_FIRMENNR = SMA_FIRMENNR AND SBA_MITARBEITERID = SMA_MITARBEITERID AND SBA_DIENSTVLFDNR = SMA_DIENSTVLFDNR AND SBA_LOHNJAHR = SMA_LOHNJAHR AND SBA_DBSNR = SMA_DBSNR AND SBA_SCHNITTNR = SMA_SCHNITTNR" "~"
if joinKuerzel = AWZSMI
searchResult := searchResult "LEFT JOIN ZLE.SMI_SMARTINTERFACE ON SMI_FIRMENNR = AWZ_SMI_FIRMENNR AND SMI_SMARTINTERFACEID = AWZ_SMI_SMARTINTERFACEID" "~"
if joinKuerzel = SMIAWZ
searchResult := searchResult "LEFT JOIN ZLE.AWZ_AUSWERTUNGZU ON AWZ_SMI_FIRMENNR = SMI_FIRMENNR AND AWZ_SMI_SMARTINTERFACEID = SMI_SMARTINTERFACEID" "~"
if joinKuerzel = LAWSNI
searchResult := searchResult "LEFT JOIN HRS.SNI_SCHNITTE ON SNI_DBSNR = LAW_DBSNR AND SNI_SCHNITTNR = LAW_SCHNITTNR_3" "~"
if joinKuerzel = SNILAW
searchResult := searchResult "LEFT JOIN HRS.LAW_LOHNART ON LAW_DBSNR = SNI_DBSNR AND LAW_SCHNITTNR_3 = SNI_SCHNITTNR" "~"
if joinKuerzel = AVESNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_SERNRID = AVE_SNR_SERNRID" "~"
if joinKuerzel = SNRAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_SNR_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = BAZSNR
searchResult := searchResult "LEFT JOIN SNR_SERIENNUMMERN.SNR_SERIENNUMMERN ON SNR_SERNRID = BAZ_SERNRID" "~"
if joinKuerzel = SNRBAZ
searchResult := searchResult "LEFT JOIN PRODUKTION.BAZ_BAZ ON BAZ_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = BOPSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_SERNRID = BOP_SNR_SERNRID" "~"
if joinKuerzel = SNRBOP
searchResult := searchResult "LEFT JOIN WWS.BOP_BONPOSITIONEN ON BOP_SNR_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = IVPSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_SERNRID = IVP_SERNRID" "~"
if joinKuerzel = SNRIVP
searchResult := searchResult "LEFT JOIN WWS.IVP_INVENTURPOSITION ON IVP_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = TLASNR
searchResult := searchResult "LEFT JOIN SNR_SERIENNUMMERN.SNR_SERIENNUMMERN ON SNR_SERNRID = TLA_SERNRID" "~"
if joinKuerzel = SNRTLA
searchResult := searchResult "LEFT JOIN PRODUKTION.TLA_BT_LOGISTIK_ANGEFORDERT ON TLA_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = TRJSNR
searchResult := searchResult "LEFT JOIN WWS.SNR_SERIENNUMMERN ON SNR_SERNRID = TRJ_SERNRID" "~"
if joinKuerzel = SNRTRJ
searchResult := searchResult "LEFT JOIN WWS.TRJ_TRANSPORTEINH_JOURNAL ON TRJ_SERNRID = SNR_SERNRID" "~"
if joinKuerzel = EKOSPK
searchResult := searchResult "LEFT JOIN SPK_STAPELKOPF.SPK_STAPELKOPF ON SPK_STAPELID = EKO_AKTIONSART_STAPELID" "~"
if joinKuerzel = SPKEKO
searchResult := searchResult "LEFT JOIN HRS.EKO_ELDAKONTROLLE ON EKO_AKTIONSART_STAPELID = SPK_STAPELID" "~"
if joinKuerzel = SAPSPR
searchResult := searchResult "LEFT JOIN BUERO.SPR_SPRACHE ON SPR_SPRACHNR = SAP_SPRACHNR" "~"
if joinKuerzel = SPRSAP
searchResult := searchResult "LEFT JOIN BUERO.SAP_SAMMELPROJEKT ON SAP_SPRACHNR = SPR_SPRACHNR" "~"
if joinKuerzel = ADBSRA
searchResult := searchResult "LEFT JOIN WWS.SRA_STEUERART ON SRA_FIRMENNR = ADB_HAUPTFIRMENNR AND SRA_STEUERARTNR = ADB_STEUERARTNR" "~"
if joinKuerzel = SRAADB
searchResult := searchResult "LEFT JOIN WWS.ADB_ABGABENDETAILBON ON ADB_HAUPTFIRMENNR = SRA_FIRMENNR AND ADB_STEUERARTNR = SRA_STEUERARTNR" "~"
if joinKuerzel = AVESRM
searchResult := searchResult "LEFT JOIN WWS.SRM_SAMMELRECHNUNGSMODELL ON SRM_SAMMELRECHID = AVE_ABRECHNUNGSMODELL_WHF" "~"
if joinKuerzel = SRMAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_ABRECHNUNGSMODELL_WHF = SRM_SAMMELRECHID" "~"
if joinKuerzel = VEBSSC
searchResult := searchResult "LEFT JOIN FIBU.SSC_STEUERSCHLUESSEL ON SSC_FIRMENNR = VEB_FIRMENNR AND SSC_FIBUNR = VEB_FIBUNR AND SSC_LANDNR = VEB_LANDNR AND SSC_STEUERSCHLUESSELNR = VEB_STEUERSCHLUESSELNR" "~"
if joinKuerzel = SSCVEB
searchResult := searchResult "LEFT JOIN FIBU.VEB_VORERFASSBUCHUNG ON VEB_FIRMENNR = SSC_FIRMENNR AND VEB_FIBUNR = SSC_FIBUNR AND VEB_LANDNR = SSC_LANDNR AND VEB_STEUERSCHLUESSELNR = SSC_STEUERSCHLUESSELNR" "~"
if joinKuerzel = EASSTC
searchResult := searchResult "LEFT JOIN FIBU.STC_STEUERCODE ON STC_LANDNR = EAS_LANDNR AND STC_STEUERCODENR = EAS_STEUERCODENR" "~"
if joinKuerzel = STCEAS
searchResult := searchResult "LEFT JOIN FIBU.EAS_ERKONTOAUFTEILSATZ ON EAS_LANDNR = STC_LANDNR AND EAS_STEUERCODENR = STC_STEUERCODENR" "~"
if joinKuerzel = MAVSTC
searchResult := searchResult "LEFT JOIN FIBU.STC_STEUERCODE ON STC_LANDNR = MAV_LANDNR AND STC_STEUERCODENR = MAV_STEUERCODENR" "~"
if joinKuerzel = STCMAV
searchResult := searchResult "LEFT JOIN FIBU.MAV_MAHNVZINSEN ON MAV_LANDNR = STC_LANDNR AND MAV_STEUERCODENR = STC_STEUERCODENR" "~"
if joinKuerzel = SSCSTC
searchResult := searchResult "LEFT JOIN FIBU.STC_STEUERCODE ON STC_LANDNR = SSC_LANDNR AND STC_STEUERCODENR = SSC_STC_STEUERCODENR" "~"
if joinKuerzel = STCSSC
searchResult := searchResult "LEFT JOIN FIBU.SSC_STEUERSCHLUESSEL ON SSC_LANDNR = STC_LANDNR AND SSC_STC_STEUERCODENR = STC_STEUERCODENR" "~"
if joinKuerzel = VEBSTC
searchResult := searchResult "LEFT JOIN FIBU.STC_STEUERCODE ON STC_LANDNR = VEB_LANDNR AND STC_STEUERCODENR = VEB_STC_STEUERCODENR" "~"
if joinKuerzel = STCVEB
searchResult := searchResult "LEFT JOIN FIBU.VEB_VORERFASSBUCHUNG ON VEB_LANDNR = STC_LANDNR AND VEB_STC_STEUERCODENR = STC_STEUERCODENR" "~"
if joinKuerzel = WAOSTC
searchResult := searchResult "LEFT JOIN STC_STEUERCODE.STC_STEUERCODE ON STC_LANDNR = WAO_STEUERCODE_LANDNR AND STC_STEUERCODENR = WAO_STEUERCODENR" "~"
if joinKuerzel = STCWAO
searchResult := searchResult "LEFT JOIN WWS.WAO_WARENEINGWBK ON WAO_STEUERCODE_LANDNR = STC_LANDNR AND WAO_STEUERCODENR = STC_STEUERCODENR" "~"
if joinKuerzel = ASOSTK
searchResult := searchResult "LEFT JOIN ZLE.STK_STAFFELKOPF ON STK_STKID = ASO_STK_STKID" "~"
if joinKuerzel = STKASO
searchResult := searchResult "LEFT JOIN ZLE.ASO_SORTIERPOS ON ASO_STK_STKID = STK_STKID" "~"
if joinKuerzel = GEASTV
searchResult := searchResult "LEFT JOIN BUERO.STV_STATVERFAHREN ON STV_FIRMENNR = GEA_STV_FIRMENNR AND STV_STVCODE = GEA_STV_STVCODE" "~"
if joinKuerzel = STVGEA
searchResult := searchResult "LEFT JOIN BUERO.GEA_GESCHAEFTSART ON GEA_STV_FIRMENNR = STV_FIRMENNR AND GEA_STV_STVCODE = STV_STVCODE" "~"
if joinKuerzel = SAKSVK
searchResult := searchResult "LEFT JOIN FIBU.SVK_SKONTOVERBUCHKZ ON SVK_FIRMENNR = SAK_FIRMENNR AND SVK_FIBUNR = SAK_FIBUNR AND SVK_SKONTOVERBUCHKZ = SAK_SVK_SKONTOVERBUCHKZ" "~"
if joinKuerzel = SVKSAK
searchResult := searchResult "LEFT JOIN FIBU.SAK_SACHKONTO ON SAK_FIRMENNR = SVK_FIRMENNR AND SAK_FIBUNR = SVK_FIBUNR AND SAK_SVK_SKONTOVERBUCHKZ = SVK_SKONTOVERBUCHKZ" "~"
if joinKuerzel = DRVSYD
searchResult := searchResult "LEFT JOIN SYD_SYSTEMDRUCKER.SYD_SYSTEMDRUCKER ON SYD_DRUCKERNAME = DRV_DRUCKERNAME" "~"
if joinKuerzel = SYDDRV
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DRV_DREPORTVORSCHLAG ON DRV_DRUCKERNAME = SYD_DRUCKERNAME" "~"
if joinKuerzel = SYKSYS
searchResult := searchResult "LEFT JOIN BMD.SYS_BMDSYSTEM ON SYS_SYSID = SYK_SYSID" "~"
if joinKuerzel = SYSSYK
searchResult := searchResult "LEFT JOIN BMD.SYK_BMDSYSTEMKOMPONENTE ON SYK_SYSID = SYS_SYSID" "~"
if joinKuerzel = TCDTCK
searchResult := searchResult "LEFT JOIN BMD.TCK_TCKONFIGURATIONEN ON TCK_TCKNAME = TCD_TCKNAME" "~"
if joinKuerzel = TCKTCD
searchResult := searchResult "LEFT JOIN BMD.TCD_TCKONFDATEN ON TCD_TCKNAME = TCK_TCKNAME" "~"
if joinKuerzel = TELTEH
searchResult := searchResult "LEFT JOIN BUERO.TEH_TELEKOMHARDWARE ON TEH_TELKOMNR = TEL_TELKOMNR" "~"
if joinKuerzel = TEHTEL
searchResult := searchResult "LEFT JOIN BUERO.TEL_TELEFON ON TEL_TELKOMNR = TEH_TELKOMNR" "~"
if joinKuerzel = DISTLA
searchResult := searchResult "LEFT JOIN TLA_BT_LOGISTIK_ANGEFORDER.TLA_BT_LOGISTIK_ANGEFORDER ON TLA_TLAID = DIS_TLAID" "~"
if joinKuerzel = TLADIS
searchResult := searchResult "LEFT JOIN WWS.DIS_DISPOLISTE ON DIS_TLAID = TLA_TLAID" "~"
if joinKuerzel = MDETLP
searchResult := searchResult "LEFT JOIN TLP_BT_LOGISTIK_POSITION.TLP_BT_LOGISTIK_POSITION ON TLP_TLPID = MDE_TLPID" "~"
if joinKuerzel = TLPMDE
searchResult := searchResult "LEFT JOIN WWS.MDE_MOBILEDATENERFASSUNG ON MDE_TLPID = TLP_TLPID" "~"
if joinKuerzel = BZBTMO
searchResult := searchResult "LEFT JOIN TMO_TAGESMODELL.TMO_TAGESMODELL ON TMO_FIRMENNR = BZB_TMO_FIRMENNR AND TMO_TAGESMODELLNR = BZB_TAGESMODELLNR" "~"
if joinKuerzel = TMOBZB
searchResult := searchResult "LEFT JOIN PRODUKTION.BZB_BAZZEITBUCHUNG ON BZB_TMO_FIRMENNR = TMO_FIRMENNR AND BZB_TAGESMODELLNR = TMO_TAGESMODELLNR" "~"
if joinKuerzel = PMZTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = PMZ_FIRMENNR AND TMO_TAGESMODELLNR = PMZ_TAGESMODELLNR" "~"
if joinKuerzel = TMOPMZ
searchResult := searchResult "LEFT JOIN BUERO.PMZ_PAUSENTAGESMODELLZU ON PMZ_FIRMENNR = TMO_FIRMENNR AND PMZ_TAGESMODELLNR = TMO_TAGESMODELLNR" "~"
if joinKuerzel = TMOTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = TMO_FIRMENNR AND TMO_TAGESMODELLNR = TMO_SONNTAG_TAGESMODELLNR" "~"
if joinKuerzel = TMOTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = TMO_FIRMENNR AND TMO_SONNTAG_TAGESMODELLNR = TMO_TAGESMODELLNR" "~"
if joinKuerzel = TMZTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = TMZ_TMO_FIRMENNR AND TMO_TAGESMODELLNR = TMZ_MASTER_TAGESMDNR" "~"
if joinKuerzel = TMOTMZ
searchResult := searchResult "LEFT JOIN BUERO.TMZ_TAGESMDPRUEFZUORD ON TMZ_TMO_FIRMENNR = TMO_FIRMENNR AND TMZ_MASTER_TAGESMDNR = TMO_TAGESMODELLNR" "~"
if joinKuerzel = WUZTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = WUZ_TMO_FIRMENNR AND TMO_TAGESMODELLNR = WUZ_TAGESMODELLNR" "~"
if joinKuerzel = TMOWUZ
searchResult := searchResult "LEFT JOIN BUERO.WUZ_WUNSCHARBEITSZEIT ON WUZ_TMO_FIRMENNR = TMO_FIRMENNR AND WUZ_TAGESMODELLNR = TMO_TAGESMODELLNR" "~"
if joinKuerzel = ZVDTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = ZVD_TMO_FIRMENNR AND TMO_TAGESMODELLNR = ZVD_TAGESMODELLNR" "~"
if joinKuerzel = TMOZVD
searchResult := searchResult "LEFT JOIN BUERO.ZVD_ZEITPLANUNGVERSIONDAT ON ZVD_TMO_FIRMENNR = TMO_FIRMENNR AND ZVD_TAGESMODELLNR = TMO_TAGESMODELLNR" "~"
if joinKuerzel = WMDTMO
searchResult := searchResult "LEFT JOIN BUERO.TMO_TAGESMODELL ON TMO_FIRMENNR = WMD_FIRMENNR AND TMO_TAGESMODELLNR = WMD_TAGESMODELLNR3" "~"
if joinKuerzel = TMOWMD
searchResult := searchResult "LEFT JOIN BUERO.WMD_WOCHENMODELL ON WMD_FIRMENNR = TMO_FIRMENNR AND WMD_TAGESMODELLNR3 = TMO_TAGESMODELLNR" "~"
if joinKuerzel = TFTTOU
searchResult := searchResult "LEFT JOIN WWS.TOU_TOUR ON TOU_FIRMENNR = TFT_TOU_VON_FIRMENNR AND TOU_TOURNR = TFT_TOU_VON_TOURNR" "~"
if joinKuerzel = TOUTFT
searchResult := searchResult "LEFT JOIN WWS.TFT_TOURFEIERTAG ON TFT_TOU_VON_FIRMENNR = TOU_FIRMENNR AND TFT_TOU_VON_TOURNR = TOU_TOURNR" "~"
if joinKuerzel = TOUTOU
searchResult := searchResult "LEFT JOIN WWS.TOU_TOUR ON TOU_FIRMENNR = TOU_UEBERG_FIRMENNR AND TOU_TOURNR = TOU_UEBERG_TOURNR" "~"
if joinKuerzel = TOUTOU
searchResult := searchResult "LEFT JOIN WWS.TOU_TOUR ON TOU_UEBERG_FIRMENNR = TOU_FIRMENNR AND TOU_UEBERG_TOURNR = TOU_TOURNR" "~"
if joinKuerzel = TKZTPR
searchResult := searchResult "LEFT JOIN TPR_TELEFONPROTOKOLL.TPR_TELEFONPROTOKOLL ON TPR_FIRMENNR = TKZ_TPR_FIRMENNR AND TPR_TELPROTLFDNR = TKZ_TPR_TELPROTLFDNR" "~"
if joinKuerzel = TPRTKZ
searchResult := searchResult "LEFT JOIN BMDENTW.TKZ_TICKETKONTAKTZU ON TKZ_TPR_FIRMENNR = TPR_FIRMENNR AND TKZ_TPR_TELPROTLFDNR = TPR_TELPROTLFDNR" "~"
if joinKuerzel = TRPTRE
searchResult := searchResult "LEFT JOIN WWS.TRE_TRANSPORTEINHEIT ON TRE_TRANSPORTEINHEITID = TRP_TRANSPORTEINHEITID" "~"
if joinKuerzel = TRETRP
searchResult := searchResult "LEFT JOIN WWS.TRP_TRANSPORTPOSITION ON TRP_TRANSPORTEINHEITID = TRE_TRANSPORTEINHEITID" "~"
if joinKuerzel = AZGTSA
searchResult := searchResult "LEFT JOIN TSA_TEILSCHLUSSRECHABLAUF.TSA_TEILSCHLUSSRECHABLAUF ON TSA_FIRMENNR = AZG_TSA_FIRMENNR AND TSA_ABLAUFNR = AZG_TSA_ABLAUFNR" "~"
if joinKuerzel = TSAAZG
searchResult := searchResult "LEFT JOIN BUERO.AZG_ABZUG ON AZG_TSA_FIRMENNR = TSA_FIRMENNR AND AZG_TSA_ABLAUFNR = TSA_ABLAUFNR" "~"
if joinKuerzel = BEZTSA
searchResult := searchResult "LEFT JOIN FIBU.TSA_TEILSCHLUSSRECHABLAUF ON TSA_FIRMENNR = BEZ_FIRMENNR AND TSA_ABLAUFNR = BEZ_ABLAUFNR" "~"
if joinKuerzel = TSABEZ
searchResult := searchResult "LEFT JOIN FIBU.BEZ_BANKEINZUG ON BEZ_FIRMENNR = TSA_FIRMENNR AND BEZ_ABLAUFNR = TSA_ABLAUFNR" "~"
if joinKuerzel = ZVKTSA
searchResult := searchResult "LEFT JOIN FIBU.TSA_TEILSCHLUSSRECHABLAUF ON TSA_FIRMENNR = ZVK_FIRMENNR AND TSA_ABLAUFNR = ZVK_ABLAUFNR" "~"
if joinKuerzel = TSAZVK
searchResult := searchResult "LEFT JOIN FIBU.ZVK_ZAHLUNGSVORSCHLAG ON ZVK_FIRMENNR = TSA_FIRMENNR AND ZVK_ABLAUFNR = TSA_ABLAUFNR" "~"
if joinKuerzel = KASTSK
searchResult := searchResult "LEFT JOIN WWS.TSK_TOUCHSCREENKOPF ON TSK_TOUCHSCREENID = KAS_TSK_ID_MI" "~"
if joinKuerzel = TSKKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_TSK_ID_MI = TSK_TOUCHSCREENID" "~"
if joinKuerzel = KASTSK
searchResult := searchResult "LEFT JOIN WWS.TSK_TOUCHSCREENKOPF ON TSK_TOUCHSCREENID = KAS_TSK_ID_KRAP_UN" "~"
if joinKuerzel = TSKKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_TSK_ID_KRAP_UN = TSK_TOUCHSCREENID" "~"
if joinKuerzel = KASTSK
searchResult := searchResult "LEFT JOIN WWS.TSK_TOUCHSCREENKOPF ON TSK_TOUCHSCREENID = KAS_TSK_ID_KRAS_UN" "~"
if joinKuerzel = TSKKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_TSK_ID_KRAS_UN = TSK_TOUCHSCREENID" "~"
if joinKuerzel = KASTSK
searchResult := searchResult "LEFT JOIN WWS.TSK_TOUCHSCREENKOPF ON TSK_TOUCHSCREENID = KAS_TSK_ID_KAK_RE" "~"
if joinKuerzel = TSKKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_TSK_ID_KAK_RE = TSK_TOUCHSCREENID" "~"
if joinKuerzel = KASTSK
searchResult := searchResult "LEFT JOIN WWS.TSK_TOUCHSCREENKOPF ON TSK_TOUCHSCREENID = KAS_TSK_ID_KAP_RE" "~"
if joinKuerzel = TSKKAS
searchResult := searchResult "LEFT JOIN WWS.KAS_KASSA ON KAS_TSK_ID_KAP_RE = TSK_TOUCHSCREENID" "~"
if joinKuerzel = ULFULK
searchResult := searchResult "LEFT JOIN BMD.ULK_USERLINK ON ULK_LINKGUID = ULF_LINKGUID" "~"
if joinKuerzel = ULKULF
searchResult := searchResult "LEFT JOIN BMD.ULF_USERLINKFIELD ON ULF_LINKGUID = ULK_LINKGUID" "~"
if joinKuerzel = SAPUMS
searchResult := searchResult "LEFT JOIN UMS_UMSATZZEILE.UMS_UMSATZZEILE ON UMS_UMSATZZEILENNR = SAP_UMSATZZEILENNR" "~"
if joinKuerzel = UMSSAP
searchResult := searchResult "LEFT JOIN BUERO.SAP_SAMMELPROJEKT ON SAP_UMSATZZEILENNR = UMS_UMSATZZEILENNR" "~"
if joinKuerzel = UPKUPH
searchResult := searchResult "LEFT JOIN UPH_UPDATEHINWEIS.UPH_UPDATEHINWEIS ON UPH_SCRNR = UPK_SCRNR" "~"
if joinKuerzel = UPHUPK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.UPK_UPH_KUNDE ON UPK_SCRNR = UPH_SCRNR" "~"
if joinKuerzel = VAKVAS
searchResult := searchResult "LEFT JOIN BUERO.VAS_VERANSTALTUNGSTAMM ON VAS_FIRMENNR = VAK_FIRMENNR AND VAS_VSTAMMNR = VAK_VSTAMMNR" "~"
if joinKuerzel = VASVAK
searchResult := searchResult "LEFT JOIN BUERO.VAK_VERANSTALTUNGSKOPF ON VAK_FIRMENNR = VAS_FIRMENNR AND VAK_VSTAMMNR = VAS_VSTAMMNR" "~"
if joinKuerzel = BPKVBD
searchResult := searchResult "LEFT JOIN WWS.VBD_VIEW_BESKOPF_DOKINFO ON VBD_FIRMENNR = BPK_FIRMENNR AND VBD_ERFASSUNGSKZ = BPK_ERFASSUNGSKZ AND VBD_BESTPRODERFASSUNGSNR = BPK_BESTPRODERFASSUNGSNR" "~"
if joinKuerzel = VBDBPK
searchResult := searchResult "LEFT JOIN WWS.BPK_BESTPRODKOPF ON BPK_FIRMENNR = VBD_FIRMENNR AND BPK_ERFASSUNGSKZ = VBD_ERFASSUNGSKZ AND BPK_BESTPRODERFASSUNGSNR = VBD_BESTPRODERFASSUNGSNR" "~"
if joinKuerzel = VRSVEK
searchResult := searchResult "LEFT JOIN WWS.VEK_VERTRETERABRECHKOPF ON VEK_VERTABRKOPFID = VRS_VEK_VERTABRKOPFID" "~"
if joinKuerzel = VEKVRS
searchResult := searchResult "LEFT JOIN WWS.VRS_VERTABRRECHPOSSTORNO ON VRS_VEK_VERTABRKOPFID = VEK_VERTABRKOPFID" "~"
if joinKuerzel = DRSVER
searchResult := searchResult "LEFT JOIN VER_VERSANDART.VER_VERSANDART ON VER_VERSANDARTNR = DRS_ARCHIV_VERSANDARTNR" "~"
if joinKuerzel = VERDRS
searchResult := searchResult "LEFT JOIN BMD.DRS_DRUCKEREINSTELLUNG ON DRS_ARCHIV_VERSANDARTNR = VER_VERSANDARTNR" "~"
if joinKuerzel = AKRVET
searchResult := searchResult "LEFT JOIN BUERO.VET_VERANSTALTUNGSTAG ON VET_FIRMENNR = AKR_VET_FIRMENNR AND VET_VSTAMMNR = AKR_VET_VSTAMMNR AND VET_VERANSTALTUNGSNR = AKR_VET_VERANSTALTUNGSNR AND VET_VERANSTALTUNGSTAGNR = AKR_VET_VERANSTALTUNGSTAGNR" "~"
if joinKuerzel = VETAKR
searchResult := searchResult "LEFT JOIN BUERO.AKR_AVKOSTENARTENZU ON AKR_VET_FIRMENNR = VET_FIRMENNR AND AKR_VET_VSTAMMNR = VET_VSTAMMNR AND AKR_VET_VERANSTALTUNGSNR = VET_VERANSTALTUNGSNR AND AKR_VET_VERANSTALTUNGSTAGNR = VET_VERANSTALTUNGSTAGNR" "~"
if joinKuerzel = MEVVET
searchResult := searchResult "LEFT JOIN BUERO.VET_VERANSTALTUNGSTAG ON VET_FIRMENNR = MEV_VET_FIRMENNR AND VET_VSTAMMNR = MEV_VET_VSTAMMNR AND VET_VERANSTALTUNGSNR = MEV_VET_VERANSTALTUNGSNR AND VET_VERANSTALTUNGSTAGNR = MEV_VET_VERANSTALTUNGSTAGNR" "~"
if joinKuerzel = VETMEV
searchResult := searchResult "LEFT JOIN BUERO.MEV_MEDIENVERLEIH ON MEV_VET_FIRMENNR = VET_FIRMENNR AND MEV_VET_VSTAMMNR = VET_VSTAMMNR AND MEV_VET_VERANSTALTUNGSNR = VET_VERANSTALTUNGSNR AND MEV_VET_VERANSTALTUNGSTAGNR = VET_VERANSTALTUNGSTAGNR" "~"
if joinKuerzel = AGSVFA
searchResult := searchResult "LEFT JOIN VFA_VERFAHRENSANWEISUNG_K.VFA_VERFAHRENSANWEISUNG_K ON VFA_VERFAID = AGS_VERFAID" "~"
if joinKuerzel = VFAAGS
searchResult := searchResult "LEFT JOIN WWS.AGS_ARBEITSGANG ON AGS_VERFAID = VFA_VERFAID" "~"
if joinKuerzel = RNPVKG
searchResult := searchResult "LEFT JOIN KORE.VKG_VIEW_KOSTAMM_GBR ON VKG_FIRMENNR = RNP_FIRMENNR AND VKG_KOSTENSTAMMNR = RNP_GBR_KOSTENSTAMMNR" "~"
if joinKuerzel = VKGRNP
searchResult := searchResult "LEFT JOIN WWS.RNP_RECHNUNGSPOS ON RNP_FIRMENNR = VKG_FIRMENNR AND RNP_GBR_KOSTENSTAMMNR = VKG_KOSTENSTAMMNR" "~"
if joinKuerzel = ERPVKS
searchResult := searchResult "LEFT JOIN KORE.VKS_VIEW_KOSTAMM_KST ON VKS_FIRMENNR = ERP_FIRMENNR AND VKS_KOSTENSTAMMNR = ERP_KOSTENST_KOSTENSTAMMNR" "~"
if joinKuerzel = VKSERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_FIRMENNR = VKS_FIRMENNR AND ERP_KOSTENST_KOSTENSTAMMNR = VKS_KOSTENSTAMMNR" "~"
if joinKuerzel = BPPVKT
searchResult := searchResult "LEFT JOIN KORE.VKT_VIEW_KOSTAMM_KTR ON VKT_FIRMENNR = BPP_FIRMENNR AND VKT_KOSTENSTAMMNR = BPP_KOSTENTR_KOSTENSTAMMNR" "~"
if joinKuerzel = VKTBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_FIRMENNR = VKT_FIRMENNR AND BPP_KOSTENTR_KOSTENSTAMMNR = VKT_KOSTENSTAMMNR" "~"
if joinKuerzel = ERPVKT
searchResult := searchResult "LEFT JOIN KORE.VKT_VIEW_KOSTAMM_KTR ON VKT_FIRMENNR = ERP_FIRMENNR AND VKT_KOSTENSTAMMNR = ERP_KOSTENTR_KOSTENSTAMMNR" "~"
if joinKuerzel = VKTERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_FIRMENNR = VKT_FIRMENNR AND ERP_KOSTENTR_KOSTENSTAMMNR = VKT_KOSTENSTAMMNR" "~"
if joinKuerzel = VJDVLJ
searchResult := searchResult "LEFT JOIN WWS.VLJ_VERLEIHJOURNAL ON VLJ_JOURNALID = VJD_LIEFERUNG_VLJID" "~"
if joinKuerzel = VLJVJD
searchResult := searchResult "LEFT JOIN WWS.VJD_VERLEIHJOURNALDETAIL ON VJD_LIEFERUNG_VLJID = VLJ_JOURNALID" "~"
if joinKuerzel = AWEVOB
searchResult := searchResult "LEFT JOIN VOB_VORGANGSBENACHR.VOB_VORGANGSBENACHR ON VOB_VOBID = AWE_PEP_BL_VOB_VOBID1" "~"
if joinKuerzel = VOBAWE
searchResult := searchResult "LEFT JOIN ZLE.AWE_AUSWERTUNG ON AWE_PEP_BL_VOB_VOBID1 = VOB_VOBID" "~"
if joinKuerzel = VPDVPE
searchResult := searchResult "LEFT JOIN WWS.VPE_VPI_ERHOEHUNG ON VPE_VPEID = VPD_VPEID" "~"
if joinKuerzel = VPEVPD
searchResult := searchResult "LEFT JOIN WWS.VPD_VPI_ERHOEHUNGDETAIL ON VPD_VPEID = VPE_VPEID" "~"
if joinKuerzel = DASVPF
searchResult := searchResult "LEFT JOIN VPF_VPFAD.VPF_VPFAD ON VPF_VPFADNR = DAS_VPFADNR" "~"
if joinKuerzel = VPFDAS
searchResult := searchResult "LEFT JOIN BUERO.DAS_DOCSARCHIVSTAMM ON DAS_VPFADNR = VPF_VPFADNR" "~"
if joinKuerzel = VPUVPF
searchResult := searchResult "LEFT JOIN BMD.VPF_VPFAD ON VPF_VPFADNR = VPU_VPFADNR" "~"
if joinKuerzel = VPFVPU
searchResult := searchResult "LEFT JOIN BMD.VPU_VPFADUSER ON VPU_VPFADNR = VPF_VPFADNR" "~"
if joinKuerzel = PRPVTS
searchResult := searchResult "LEFT JOIN WWS.VTS_VERTRETERSTUFE ON VTS_FIRMENNR = PRP_VTS_FIRMENNR AND VTS_STUFE = PRP_VTS_STUFE" "~"
if joinKuerzel = VTSPRP
searchResult := searchResult "LEFT JOIN WWS.PRP_PROVISIONTABPOS ON PRP_VTS_FIRMENNR = VTS_FIRMENNR AND PRP_VTS_STUFE = VTS_STUFE" "~"
if joinKuerzel = ARLVTX
searchResult := searchResult "LEFT JOIN BUERO.VTX_VA_UESLANGTEXT ON VTX_LANGTXTLFDNR = ARL_PRODUKTLANGTEXT" "~"
if joinKuerzel = VTXARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_PRODUKTLANGTEXT = VTX_LANGTXTLFDNR" "~"
if joinKuerzel = EFKVUA
searchResult := searchResult "LEFT JOIN WWS.VUA_VA_AUFANG_VERGLEICH ON VUA_AUF_FIRMENNR = EFK_FIRMENNR AND VUA_AUF_ERFASSUNGSKZ = EFK_ERFASSUNGSKZ AND VUA_AUF_ERFASSUNGSNR = EFK_ERFASSUNGSNR" "~"
if joinKuerzel = VUAEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = VUA_AUF_FIRMENNR AND EFK_ERFASSUNGSKZ = VUA_AUF_ERFASSUNGSKZ AND EFK_ERFASSUNGSNR = VUA_AUF_ERFASSUNGSNR" "~"
if joinKuerzel = WAGVWH
searchResult := searchResult "LEFT JOIN WWS.VWH_VIEW_WARENHAUPTGRUPPE ON VWH_FIRMENNR = WAG_FIRMENNR AND VWH_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR" "~"
if joinKuerzel = VWHWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = VWH_FIRMENNR AND WAG_WARENHAUPTGRNR = VWH_WARENHAUPTGRNR" "~"
if joinKuerzel = EFKVZW
searchResult := searchResult "LEFT JOIN VZW_VERKEHRSZWEIG.VZW_VERKEHRSZWEIG ON VZW_FIRMENNR = EFK_VZW_FIRMENNR AND VZW_VZWCODE = EFK_VZWCODE" "~"
if joinKuerzel = VZWEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_VZW_FIRMENNR = VZW_FIRMENNR AND EFK_VZWCODE = VZW_VZWCODE" "~"
if joinKuerzel = ADCWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = ADC_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEADC
searchResult := searchResult "LEFT JOIN WWS.ADC_ABGABENDETAILRECH ON ADC_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = ADEWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = ADE_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEADE
searchResult := searchResult "LEFT JOIN WWS.ADE_ABGABENDETAILERFPOS ON ADE_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = ADWWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = ADW_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEADW
searchResult := searchResult "LEFT JOIN WWS.ADW_ABGABENDETAILWAP ON ADW_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = ERBWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = ERB_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEERB
searchResult := searchResult "LEFT JOIN FIBU.ERB_BUCHUNG ON ERB_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = GSPWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = GSP_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEGSP
searchResult := searchResult "LEFT JOIN WWS.GSP_GUTSCHEINPOSITION ON GSP_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = MGBWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = MGB_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEMGB
searchResult := searchResult "LEFT JOIN FIBU.MGB_MAHNGEBUEHR ON MGB_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = SCKWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = SCK_WAE_WAEHRUNG" "~"
if joinKuerzel = WAESCK
searchResult := searchResult "LEFT JOIN PRODUKTION.SCK_SCKENNZAHL ON SCK_WAE_WAEHRUNG = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = WAOWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = WAO_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEWAO
searchResult := searchResult "LEFT JOIN WWS.WAO_WARENEINGWBK ON WAO_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = WAPWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = WAP_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = ZVKWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = ZVK_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEZVK
searchResult := searchResult "LEFT JOIN FIBU.ZVK_ZAHLUNGSVORSCHLAG ON ZVK_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = WKLWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = WKL_WAEHRUNGSCODE_ERW" "~"
if joinKuerzel = WAEWKL
searchResult := searchResult "LEFT JOIN WWS.WKL_WARENAUSGKOSTENLIEF ON WKL_WAEHRUNGSCODE_ERW = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = WKRWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = WKR_WAEHRUNGSCODE_ERW" "~"
if joinKuerzel = WAEWKR
searchResult := searchResult "LEFT JOIN WWS.WKR_WARENAUSGKOSTENRECH ON WKR_WAEHRUNGSCODE_ERW = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = FIRWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = FIR_ZWEIT_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_ZWEIT_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = FWFWAE
searchResult := searchResult "LEFT JOIN WAE_WAEHRUNG.WAE_WAEHRUNG ON WAE_WAEHRUNGSCODE = FWF_BH_ZWEIT_WAEHRUNGSCODE" "~"
if joinKuerzel = WAEFWF
searchResult := searchResult "LEFT JOIN FIBU.FWF_FIRMENWERTEFIBU ON FWF_BH_ZWEIT_WAEHRUNGSCODE = WAE_WAEHRUNGSCODE" "~"
if joinKuerzel = KMAWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = KMA_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = KMA_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = KMA_WARENUNTERGRNR" "~"
if joinKuerzel = WAGKMA
searchResult := searchResult "LEFT JOIN WWS.KMA_KUNDENMITARBBEZUG ON KMA_WAG_FIRMENNR = WAG_FIRMENNR AND KMA_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND KMA_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = LEIWAG
searchResult := searchResult "LEFT JOIN WAG_WARENGRUPPE.WAG_WARENGRUPPE ON WAG_FIRMENNR = LEI_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = LEI_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = LEI_WARENUNTERGRNR" "~"
if joinKuerzel = WAGLEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_WAG_FIRMENNR = WAG_FIRMENNR AND LEI_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND LEI_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = SLPWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = SLP_WAG_FIRMENNR AND WAG_WARENHAUPTGRNR = SLP_WAG_WARENHAUPTGRNR AND WAG_WARENUNTERGRNR = SLP_WAG_WARENUNTERGRNR" "~"
if joinKuerzel = WAGSLP
searchResult := searchResult "LEFT JOIN WWS.SLP_STUECKLISTENPOSITION ON SLP_WAG_FIRMENNR = WAG_FIRMENNR AND SLP_WAG_WARENHAUPTGRNR = WAG_WARENHAUPTGRNR AND SLP_WAG_WARENUNTERGRNR = WAG_WARENUNTERGRNR" "~"
if joinKuerzel = PCPWAK
searchResult := searchResult "LEFT JOIN WAK_WARENEINGANGKOPF.WAK_WARENEINGANGKOPF ON WAK_WARENEINGKOPFID = PCP_WARENEINGKOPFID" "~"
if joinKuerzel = WAKPCP
searchResult := searchResult "LEFT JOIN PRODUKTION.PCP_PPS_PRODUKT ON PCP_WARENEINGKOPFID = WAK_WARENEINGKOPFID" "~"
if joinKuerzel = ADWWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_WARENEINGKOPFID = ADW_WARENEINGKOPFID AND WAP_WARENEINGPOSLFDNR = ADW_WARENEINGPOSLFDNR" "~"
if joinKuerzel = WAPADW
searchResult := searchResult "LEFT JOIN WWS.ADW_ABGABENDETAILWAP ON ADW_WARENEINGKOPFID = WAP_WARENEINGKOPFID AND ADW_WARENEINGPOSLFDNR = WAP_WARENEINGPOSLFDNR" "~"
if joinKuerzel = WBWWBT
searchResult := searchResult "LEFT JOIN WWS.WBT_WWSBUDGETKOPF ON WBT_WWSBUDGETKOPFID = WBW_WWSBUDGETKOPFID" "~"
if joinKuerzel = WBTWBW
searchResult := searchResult "LEFT JOIN WWS.WBW_WWSBUDGETWERTE ON WBW_WWSBUDGETKOPFID = WBT_WWSBUDGETKOPFID" "~"
if joinKuerzel = WCVWCD
searchResult := searchResult "LEFT JOIN BMD.WCD_WAPARAMDEF ON WCD_WAPARAMDEFNR = WCV_WAPARAMDEFNR" "~"
if joinKuerzel = WCDWCV
searchResult := searchResult "LEFT JOIN BMD.WCV_WAPARAMDEFVALUES ON WCV_WAPARAMDEFNR = WCD_WAPARAMDEFNR" "~"
if joinKuerzel = WASWEI
searchResult := searchResult "LEFT JOIN WWS.WEI_WARENEINSATZKOPF ON WEI_WEINSATZKOPFID = WAS_WEINSATZKOPFID" "~"
if joinKuerzel = WEIWAS
searchResult := searchResult "LEFT JOIN WWS.WAS_WARENEINSATZARL ON WAS_WEINSATZKOPFID = WEI_WEINSATZKOPFID" "~"
if joinKuerzel = WEJWEI
searchResult := searchResult "LEFT JOIN WWS.WEI_WARENEINSATZKOPF ON WEI_WEINSATZKOPFID = WEJ_WEINSATZKOPFID" "~"
if joinKuerzel = WEIWEJ
searchResult := searchResult "LEFT JOIN WWS.WEJ_WARENEINSATZLAJ ON WEJ_WEINSATZKOPFID = WEI_WEINSATZKOPFID" "~"
if joinKuerzel = WFHWEI
searchResult := searchResult "LEFT JOIN WWS.WEI_WARENEINSATZKOPF ON WEI_WEINSATZKOPFID = WFH_WEINSATZKOPFID" "~"
if joinKuerzel = WEIWFH
searchResult := searchResult "LEFT JOIN WWS.WFH_FERTEP_HISTORIE ON WFH_WEINSATZKOPFID = WEI_WEINSATZKOPFID" "~"
if joinKuerzel = WEAWEP
searchResult := searchResult "LEFT JOIN WWS.WEP_WWSERKONTROLLEPOS ON WEP_ERKONTRPOS_ID = WEA_ERKONTRPOS_ID" "~"
if joinKuerzel = WEPWEA
searchResult := searchResult "LEFT JOIN WWS.WEA_WWSERKONTROLLERABATTE ON WEA_ERKONTRPOS_ID = WEP_ERKONTRPOS_ID" "~"
if joinKuerzel = WEPWER
searchResult := searchResult "LEFT JOIN WWS.WER_WWSERKONTROLLEKOPF ON WER_ERKONTRKOPF_ID = WEP_ERKONTRKOPF_ID" "~"
if joinKuerzel = WERWEP
searchResult := searchResult "LEFT JOIN WWS.WEP_WWSERKONTROLLEPOS ON WEP_ERKONTRKOPF_ID = WER_ERKONTRKOPF_ID" "~"
if joinKuerzel = WFDWFK
searchResult := searchResult "LEFT JOIN WWS.WFK_FOLGEARTIKELKAT ON WFK_WFKID = WFD_WFKID" "~"
if joinKuerzel = WFKWFD
searchResult := searchResult "LEFT JOIN WWS.WFD_FOLGEARTIKELDETAIL ON WFD_WFKID = WFK_WFKID" "~"
if joinKuerzel = WMZWFN
searchResult := searchResult "LEFT JOIN BUERO.WFN_WORKFLOWACTION ON WFN_WFNID = WMZ_WFNID" "~"
if joinKuerzel = WFNWMZ
searchResult := searchResult "LEFT JOIN BUERO.WMZ_WFAMITARBZU ON WMZ_WFNID = WFN_WFNID" "~"
if joinKuerzel = WIVWIK
searchResult := searchResult "LEFT JOIN BIL.WIK_WIVARIABLENKATEGORIE ON WIK_WIVARKATID = WIV_WIVARKATID" "~"
if joinKuerzel = WIKWIV
searchResult := searchResult "LEFT JOIN BIL.WIV_WIVARIABLEN ON WIV_WIVARKATID = WIK_WIVARKATID" "~"
if joinKuerzel = WEPWKA
searchResult := searchResult "LEFT JOIN WWS.WKA_WARENAUSGKOSTENARTEN ON WKA_FIRMENNR = WEP_WKA_FIRMENNR AND WKA_WKALFDNR = WEP_WKALFDNR" "~"
if joinKuerzel = WKAWEP
searchResult := searchResult "LEFT JOIN WWS.WEP_WWSERKONTROLLEPOS ON WEP_WKA_FIRMENNR = WKA_FIRMENNR AND WEP_WKALFDNR = WKA_WKALFDNR" "~"
if joinKuerzel = WEPWKL
searchResult := searchResult "LEFT JOIN WWS.WKL_WARENAUSGKOSTENLIEF ON WKL_WKLID = WEP_WKLID" "~"
if joinKuerzel = WKLWEP
searchResult := searchResult "LEFT JOIN WWS.WEP_WWSERKONTROLLEPOS ON WEP_WKLID = WKL_WKLID" "~"
if joinKuerzel = WEUWKL
searchResult := searchResult "LEFT JOIN WWS.WKL_WARENAUSGKOSTENLIEF ON WKL_WKLID = WEU_WKLID" "~"
if joinKuerzel = WKLWEU
searchResult := searchResult "LEFT JOIN WWS.WEU_WWSERKONTROLLEZU ON WEU_WKLID = WKL_WKLID" "~"
if joinKuerzel = AVEWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = AVE_WKT_FIRMENNR AND WKT_KONTENTABNR = AVE_KONTENTABNR" "~"
if joinKuerzel = WKTAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_WKT_FIRMENNR = WKT_FIRMENNR AND AVE_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = KZAWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = KZA_WKT_FIRMENNR AND WKT_KONTENTABNR = KZA_WKT_KONTENTABNR" "~"
if joinKuerzel = WKTKZA
searchResult := searchResult "LEFT JOIN WWS.KZA_KASSENZAHLUNGSARTENZU ON KZA_WKT_FIRMENNR = WKT_FIRMENNR AND KZA_WKT_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = RATWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = RAT_HAUPTFIRMENNR AND WKT_KONTENTABNR = RAT_KONTENTABNR" "~"
if joinKuerzel = WKTRAT
searchResult := searchResult "LEFT JOIN WWS.RAT_RABATTTAB ON RAT_HAUPTFIRMENNR = WKT_FIRMENNR AND RAT_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = RNRWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = RNR_HAUPTFIRMENNR AND WKT_KONTENTABNR = RNR_KONTENTABNR" "~"
if joinKuerzel = WKTRNR
searchResult := searchResult "LEFT JOIN WWS.RNR_RECHNUNGSRABATT ON RNR_HAUPTFIRMENNR = WKT_FIRMENNR AND RNR_KONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = SRPWKT
searchResult := searchResult "LEFT JOIN WWS.WKT_WWSKONTENTAB ON WKT_FIRMENNR = SRP_FIRMENNR AND WKT_KONTENTABNR = SRP_STEUERKONTENTABNR" "~"
if joinKuerzel = WKTSRP
searchResult := searchResult "LEFT JOIN WWS.SRP_STEUERTABPOS ON SRP_FIRMENNR = WKT_FIRMENNR AND SRP_STEUERKONTENTABNR = WKT_KONTENTABNR" "~"
if joinKuerzel = MITWMD
searchResult := searchResult "LEFT JOIN BUERO.WMD_WOCHENMODELL ON WMD_FIRMENNR = MIT_WMD_FIRMENNR AND WMD_WOCHENMODELLNR = MIT_WOCHENMODELLNR" "~"
if joinKuerzel = WMDMIT
searchResult := searchResult "LEFT JOIN BUERO.MIT_MITARBEITER ON MIT_WMD_FIRMENNR = WMD_FIRMENNR AND MIT_WOCHENMODELLNR = WMD_WOCHENMODELLNR" "~"
if joinKuerzel = PKDWMD
searchResult := searchResult "LEFT JOIN WMD_WOCHENMODELL.WMD_WOCHENMODELL ON WMD_FIRMENNR = PKD_WMD_FIRMENNR AND WMD_WOCHENMODELLNR = PKD_WMD_WOCHENMODELLNR" "~"
if joinKuerzel = WMDPKD
searchResult := searchResult "LEFT JOIN PRODUKTION.PKD_KM_DETAIL ON PKD_WMD_FIRMENNR = WMD_FIRMENNR AND PKD_WMD_WOCHENMODELLNR = WMD_WOCHENMODELLNR" "~"
if joinKuerzel = WMDWMD
searchResult := searchResult "LEFT JOIN BUERO.WMD_WOCHENMODELL ON WMD_FIRMENNR = WMD_VZ_FIRMENNR AND WMD_WOCHENMODELLNR = WMD_VZ_WOCHENMODELLNR" "~"
if joinKuerzel = WMDWMD
searchResult := searchResult "LEFT JOIN BUERO.WMD_WOCHENMODELL ON WMD_VZ_FIRMENNR = WMD_FIRMENNR AND WMD_VZ_WOCHENMODELLNR = WMD_WOCHENMODELLNR" "~"
if joinKuerzel = LAJWRP
searchResult := searchResult "LEFT JOIN WWS.WRP_WWSANKAUFSRNGPOS ON WRP_FIRMENNR = LAJ_BUCHUNGS_FIRMENNR AND WRP_RECHNUNGLFDNR = LAJ_WRP_RECHNUNGLFDNR AND WRP_RECHNUNGSPOSLFDNR = LAJ_WRP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = WRPLAJ
searchResult := searchResult "LEFT JOIN WWS.LAJ_LAGERJOURNAL ON LAJ_BUCHUNGS_FIRMENNR = WRP_FIRMENNR AND LAJ_WRP_RECHNUNGLFDNR = WRP_RECHNUNGLFDNR AND LAJ_WRP_RECHNUNGSPOSLFDNR = WRP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = WRPWRP
searchResult := searchResult "LEFT JOIN WWS.WRP_WWSANKAUFSRNGPOS ON WRP_FIRMENNR = WRP_FIRMENNR AND WRP_RECHNUNGLFDNR = WRP_RECHNUNGLFDNR AND WRP_RECHNUNGSPOSLFDNR = WRP_H_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = WRPWRP
searchResult := searchResult "LEFT JOIN WWS.WRP_WWSANKAUFSRNGPOS ON WRP_FIRMENNR = WRP_FIRMENNR AND WRP_RECHNUNGLFDNR = WRP_RECHNUNGLFDNR AND WRP_H_RECHNUNGSPOSLFDNR = WRP_RECHNUNGSPOSLFDNR" "~"
if joinKuerzel = WSKWSK
searchResult := searchResult "LEFT JOIN WWS.WSK_WWSKORESTATISTIK ON WSK_WSKID = WSK_SET_WSKID" "~"
if joinKuerzel = WSKWSK
searchResult := searchResult "LEFT JOIN WWS.WSK_WWSKORESTATISTIK ON WSK_SET_WSKID = WSK_WSKID" "~"
if joinKuerzel = ADBWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = ADB_HAUPTFIRMENNR AND WTB_WWSTEXTBNR = ADB_WWSTEXTBNR_BEIBETRAG0" "~"
if joinKuerzel = WTBADB
searchResult := searchResult "LEFT JOIN WWS.ADB_ABGABENDETAILBON ON ADB_HAUPTFIRMENNR = WTB_FIRMENNR AND ADB_WWSTEXTBNR_BEIBETRAG0 = WTB_WWSTEXTBNR" "~"
if joinKuerzel = ADCWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = ADC_HAUPTFIRMENNR AND WTB_WWSTEXTBNR = ADC_WWSTEXTBNR_BEIBETRAG0" "~"
if joinKuerzel = WTBADC
searchResult := searchResult "LEFT JOIN WWS.ADC_ABGABENDETAILRECH ON ADC_HAUPTFIRMENNR = WTB_FIRMENNR AND ADC_WWSTEXTBNR_BEIBETRAG0 = WTB_WWSTEXTBNR" "~"
if joinKuerzel = EQIWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = EQI_WTB_FIRMENNR AND WTB_WWSTEXTBNR = EQI_BESCH_WWSTEXTBNR" "~"
if joinKuerzel = WTBEQI
searchResult := searchResult "LEFT JOIN WWS.EQI_ERFASSPOSQUELLINFO ON EQI_WTB_FIRMENNR = WTB_FIRMENNR AND EQI_BESCH_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = ERPWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = ERP_ARL_FIRMENNR AND WTB_WWSTEXTBNR = ERP_KURZBEZ_WWSTEXTBNR" "~"
if joinKuerzel = WTBERP
searchResult := searchResult "LEFT JOIN WWS.ERP_ERFASSPOSITIONEN ON ERP_ARL_FIRMENNR = WTB_FIRMENNR AND ERP_KURZBEZ_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = KAPWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = KAP_WTB_KBEZ_FIRMENNR AND WTB_WWSTEXTBNR = KAP_WTB_KBEZ_WWSTEXTBNR" "~"
if joinKuerzel = WTBKAP
searchResult := searchResult "LEFT JOIN WWS.KAP_KASSENPOSITIONEN ON KAP_WTB_KBEZ_FIRMENNR = WTB_FIRMENNR AND KAP_WTB_KBEZ_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = PKAWTB
searchResult := searchResult "LEFT JOIN WTB_WWSTEXTBAUSTEIN.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = PKA_WTB_FIRMENNR AND WTB_WWSTEXTBNR = PKA_WTB_WWSTEXTBNR" "~"
if joinKuerzel = WTBPKA
searchResult := searchResult "LEFT JOIN BUERO.PKA_PROJEKTART ON PKA_WTB_FIRMENNR = WTB_FIRMENNR AND PKA_WTB_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = WAGWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = WAG_FIRMENNR AND WTB_WWSTEXTBNR = WAG_BESCHR_WWSTEXTBNR" "~"
if joinKuerzel = WTBWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_FIRMENNR = WTB_FIRMENNR AND WAG_BESCHR_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = WADWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = WAD_EMAILTEXT_WTB_FIRMENNR AND WTB_WWSTEXTBNR = WAD_EMAILTEXT_WWSTEXTBNR" "~"
if joinKuerzel = WTBWAD
searchResult := searchResult "LEFT JOIN WWS.WAD_WWSADRESSZUSATZ ON WAD_EMAILTEXT_WTB_FIRMENNR = WTB_FIRMENNR AND WAD_EMAILTEXT_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = WKGWTB
searchResult := searchResult "LEFT JOIN WWS.WTB_WWSTEXTBAUSTEIN ON WTB_FIRMENNR = WKG_EMAILTEXT_WTB_FIRMENNR AND WTB_WWSTEXTBNR = WKG_EMAILTEXT_WWSTEXTBNR" "~"
if joinKuerzel = WTBWKG
searchResult := searchResult "LEFT JOIN WWS.WKG_WWSKUNDENGRUPPE ON WKG_EMAILTEXT_WTB_FIRMENNR = WTB_FIRMENNR AND WKG_EMAILTEXT_WWSTEXTBNR = WTB_WWSTEXTBNR" "~"
if joinKuerzel = WBFWWB
searchResult := searchResult "LEFT JOIN WWS.WWB_WWSBERICHTE ON WWB_WWSBERICHTNR = WBF_WWSBERICHTNR" "~"
if joinKuerzel = WWBWBF
searchResult := searchResult "LEFT JOIN WWS.WBF_WWSBERICHTFILTER ON WBF_WWSBERICHTNR = WWB_WWSBERICHTNR" "~"
if joinKuerzel = YBUYBM
searchResult := searchResult "LEFT JOIN FIBU.YBM_AUTOBUCHMODELLE ON YBM_YBMODELLID = YBU_YBMODELLID" "~"
if joinKuerzel = YBMYBU
searchResult := searchResult "LEFT JOIN FIBU.YBU_AUTOBUCHUNG ON YBU_YBMODELLID = YBM_YBMODELLID" "~"
if joinKuerzel = BRBZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_FIRMENNR = BRB_FIRMENNR AND ZBA_ZBUCHARTNR = BRB_ZBUCHARTNR" "~"
if joinKuerzel = ZBABRB
searchResult := searchResult "LEFT JOIN BUERO.BRB_BUEROBERECHT ON BRB_FIRMENNR = ZBA_FIRMENNR AND BRB_ZBUCHARTNR = ZBA_ZBUCHARTNR" "~"
if joinKuerzel = LEIZBA
searchResult := searchResult "LEFT JOIN ZBA_ZEITBUCHUNGSART.ZBA_ZEITBUCHUNGSART ON ZBA_FIRMENNR = LEI_ZBA_FIRMENNR AND ZBA_ZBUCHARTNR = LEI_ZBA_ZBUCHARTNR" "~"
if joinKuerzel = ZBALEI
searchResult := searchResult "LEFT JOIN ZLE.LEI_LEISTUNG ON LEI_ZBA_FIRMENNR = ZBA_FIRMENNR AND LEI_ZBA_ZBUCHARTNR = ZBA_ZBUCHARTNR" "~"
if joinKuerzel = ZBOZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_FIRMENNR = ZBO_ZBA_FIRMENNR AND ZBA_ZBUCHARTNR = ZBO_ZBUCHARTNR" "~"
if joinKuerzel = ZBAZBO
searchResult := searchResult "LEFT JOIN BUERO.ZBO_ZEITBABERECHTZUORD ON ZBO_ZBA_FIRMENNR = ZBA_FIRMENNR AND ZBO_ZBUCHARTNR = ZBA_ZBUCHARTNR" "~"
if joinKuerzel = ZBAZBZ
searchResult := searchResult "LEFT JOIN BUERO.ZBZ_ZEITBEZUGSART ON ZBZ_FIRMENNR = ZBA_ZBZ_FIRMENNR AND ZBZ_ZBEZUGSARTNR = ZBA_ZBEZUGSARTNR" "~"
if joinKuerzel = ZBZZBA
searchResult := searchResult "LEFT JOIN BUERO.ZBA_ZEITBUCHUNGSART ON ZBA_ZBZ_FIRMENNR = ZBZ_FIRMENNR AND ZBA_ZBEZUGSARTNR = ZBZ_ZBEZUGSARTNR" "~"
if joinKuerzel = ZBRZKS
searchResult := searchResult "LEFT JOIN FIBU.ZKS_ZINSBERECHNUNGSKREIS ON ZKS_FIRMENNR = ZBR_FIRMENNR AND ZKS_FIBUNR = ZBR_FIBUNR AND ZKS_ZINSKREISLFDNR = ZBR_ZINSKREISLFDNR" "~"
if joinKuerzel = ZKSZBR
searchResult := searchResult "LEFT JOIN FIBU.ZBR_ZINSBERECHNUNG ON ZBR_FIRMENNR = ZKS_FIRMENNR AND ZBR_FIBUNR = ZKS_FIBUNR AND ZBR_ZINSKREISLFDNR = ZKS_ZINSKREISLFDNR" "~"
if joinKuerzel = BONZRT
searchResult := searchResult "LEFT JOIN ZRT_ZAHLUNGSART.ZRT_ZAHLUNGSART ON ZRT_ZAHLARTNR = BON_ZRT_ZAHLARTNR" "~"
if joinKuerzel = ZRTBON
searchResult := searchResult "LEFT JOIN WWS.BON_BONKOPF ON BON_ZRT_ZAHLARTNR = ZRT_ZAHLARTNR" "~"
if joinKuerzel = RNGZRT
searchResult := searchResult "LEFT JOIN ZRT_ZAHLUNGSART.ZRT_ZAHLUNGSART ON ZRT_ZAHLARTNR = RNG_ZAHLARTNR" "~"
if joinKuerzel = ZRTRNG
searchResult := searchResult "LEFT JOIN WWS.RNG_RECHNUNG ON RNG_ZAHLARTNR = ZRT_ZAHLARTNR" "~"
if joinKuerzel = BAIZRT
searchResult := searchResult "LEFT JOIN BUERO.ZRT_ZAHLUNGSART ON ZRT_ZAHLARTNR = BAI_ZRT_ZAHLARTNR2" "~"
if joinKuerzel = ZRTBAI
searchResult := searchResult "LEFT JOIN BUERO.BAI_BANKINSTITUTIONSZU ON BAI_ZRT_ZAHLARTNR2 = ZRT_ZAHLARTNR" "~"
if joinKuerzel = TSRZTZ
searchResult := searchResult "LEFT JOIN ZTZ_ZEITZONEN.ZTZ_ZEITZONEN ON ZTZ_ZEITZONENR = TSR_BIS_ZEITZONENR" "~"
if joinKuerzel = ZTZTSR
searchResult := searchResult "LEFT JOIN BUERO.TSR_SERIENTERMIN ON TSR_BIS_ZEITZONENR = ZTZ_ZEITZONENR" "~"
if joinKuerzel = BMLZVZ
searchResult := searchResult "LEFT JOIN ZVZ_TBVERWENDUNGSZWECK.ZVZ_TBVERWENDUNGSZWECK ON ZVZ_ZVZID = BML_ZVZ_ZVZID" "~"
if joinKuerzel = ZVZBML
searchResult := searchResult "LEFT JOIN FIBU.BML_BANKRVMODEL ON BML_ZVZ_ZVZID = ZVZ_ZVZID" "~"
if joinKuerzel = FIRINS
searchResult := searchResult "LEFT JOIN BUERO.INS_INSTITUTION ON INS_INR = FIR_FIRMENNR" "~"
if joinKuerzel = INSFIR
searchResult := searchResult "LEFT JOIN BUERO.FIR_FIRMA ON FIR_FIRMENNR = INS_INR" "~"
if joinKuerzel = INTWAP
searchResult := searchResult "LEFT JOIN WWS.WAP_WARENEINGANGPOSITION ON WAP_WARENEINGPOSID = INT_EXTID" "~"
if joinKuerzel = WAPINT
searchResult := searchResult "LEFT JOIN BUERO.INT_INTRASTAT ON INT_EXTID = WAP_WARENEINGPOSID" "~"
if joinKuerzel = EFKVKG
searchResult := searchResult "LEFT JOIN KORE.VKG_VIEW_KOSTAMM_GBR ON VKG_FIRMENNR = EFK_FIRMENNR AND VKG_KOSTENSTAMMNR = EFK_GESCHBER_KOSTENSTAMMNR" "~"
if joinKuerzel = VKGEFK
searchResult := searchResult "LEFT JOIN WWS.EFK_ERFASSKOPFWWS ON EFK_FIRMENNR = VKG_FIRMENNR AND EFK_GESCHBER_KOSTENSTAMMNR = VKG_KOSTENSTAMMNR" "~"
if joinKuerzel = VATKLI
searchResult := searchResult "LEFT JOIN BUERO.KLI_KUNDE_LIEFERANT ON KLI_KLID = VAT_TN_PER_PERSONENID AND KLI_FIRMENNR = VAT_TN_PER_FIRMENNR" "~"
if joinKuerzel = KLIVAT
searchResult := searchResult "LEFT JOIN BUERO.VAT_TEILNAHME ON VAT_TN_PER_PERSONENID = KLI_KLID AND VAT_TN_PER_FIRMENNR = KLI_FIRMENNR" "~"
if joinKuerzel = AALBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AAL_LETZTERUSER" "~"
if joinKuerzel = BMUAAL
searchResult := searchResult "LEFT JOIN BIL.AAL_AUSDRUCKLAENDER ON AAL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ABDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ABD_LETZTERUSER" "~"
if joinKuerzel = BMUABD
searchResult := searchResult "LEFT JOIN HRS.ABD_ABRECHNUNGSDETAIL ON ABD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ACABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ACA_LETZTERUSER" "~"
if joinKuerzel = BMUACA
searchResult := searchResult "LEFT JOIN FIBU.ACA_TESTFALL_AUSWERTUNG ON ACA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ADCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ADC_LETZTERUSER" "~"
if joinKuerzel = BMUADC
searchResult := searchResult "LEFT JOIN WWS.ADC_ABGABENDETAILRECH ON ADC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ADOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ADO_LETZTERUSER" "~"
if joinKuerzel = BMUADO
searchResult := searchResult "LEFT JOIN BUERO.ADO_AUSDRUCKMODELL ON ADO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AEEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AEE_LETZTERUSER" "~"
if joinKuerzel = BMUAEE
searchResult := searchResult "LEFT JOIN BUERO.AEE_AUSDRUCKEMAILEMPF ON AEE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AGNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AGN_LETZTERUSER" "~"
if joinKuerzel = BMUAGN
searchResult := searchResult "LEFT JOIN WWS.AGN_AG_PERSON_ZU ON AGN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AJTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AJT_LETZTERUSER" "~"
if joinKuerzel = BMUAJT
searchResult := searchResult "LEFT JOIN BIL.AJT_ANBUJOURNAL_TMP ON AJT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AKSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AKS_LETZTERUSER" "~"
if joinKuerzel = BMUAKS
searchResult := searchResult "LEFT JOIN BUERO.AKS_AKTIONSTATUS ON AKS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AKTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AKT_LETZTERUSER" "~"
if joinKuerzel = BMUAKT
searchResult := searchResult "LEFT JOIN WWS.AKT_AKTIONSSTAMM ON AKT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ALSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ALS_LETZTERUSER" "~"
if joinKuerzel = BMUALS
searchResult := searchResult "LEFT JOIN WWS.ALS_ALLERGEN ON ALS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ALTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ALT_LETZTERUSER" "~"
if joinKuerzel = BMUALT
searchResult := searchResult "LEFT JOIN ZLE.ALT_ARCHIVLEISTUNGEN ON ALT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AMGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AMG_LETZTERUSER" "~"
if joinKuerzel = BMUAMG
searchResult := searchResult "LEFT JOIN BUERO.AMG_GKKABMELDEGRUND ON AMG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AMPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AMP_LETZTERUSER" "~"
if joinKuerzel = BMUAMP
searchResult := searchResult "LEFT JOIN BUERO.AMP_AUTOMAILARCHIVPARAM ON AMP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AOZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AOZ_LETZTERUSER" "~"
if joinKuerzel = BMUAOZ
searchResult := searchResult "LEFT JOIN WWS.AOZ_ARTIKELDOKUMENTZU ON AOZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = APABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = APA_LETZTERUSER" "~"
if joinKuerzel = BMUAPA
searchResult := searchResult "LEFT JOIN PRODUKTION.APA_ARBEITSPLAN ON APA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = APRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = APR_LETZTERUSER" "~"
if joinKuerzel = BMUAPR
searchResult := searchResult "LEFT JOIN BUERO.APR_ARCHIVPROTOKOLL ON APR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ARBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ARB_LETZTERUSER" "~"
if joinKuerzel = BMUARB
searchResult := searchResult "LEFT JOIN WWS.ARB_ARTIKELBEREICHSWERTE ON ARB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ARTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ART_LETZTERUSER" "~"
if joinKuerzel = BMUART
searchResult := searchResult "LEFT JOIN BIL.ART_ANBUBEWRES_TMP ON ART_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ASTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AST_LETZTERUSER" "~"
if joinKuerzel = BMUAST
searchResult := searchResult "LEFT JOIN WWS.AST_ARTIKELSTATISTIK ON AST_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ASUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ASU_LETZTERUSER" "~"
if joinKuerzel = BMUASU
searchResult := searchResult "LEFT JOIN BIL.ASU_ANLAGENSUMMEN ON ASU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ATFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ATF_LETZTERUSER" "~"
if joinKuerzel = BMUATF
searchResult := searchResult "LEFT JOIN BUERO.ATF_ABTEILUNGFIRMENZU ON ATF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AUGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AUG_LETZTERUSER" "~"
if joinKuerzel = BMUAUG
searchResult := searchResult "LEFT JOIN BUERO.AUG_AUFGABE ON AUG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AUSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AUS_LETZTERUSER" "~"
if joinKuerzel = BMUAUS
searchResult := searchResult "LEFT JOIN BIL.AUS_AUSWERTUNG ON AUS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AVEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AVE_LETZTERUSER" "~"
if joinKuerzel = BMUAVE
searchResult := searchResult "LEFT JOIN WWS.AVE_AUFTRAGSVORERFASSUNG ON AVE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AVGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AVG_LETZTERUSER" "~"
if joinKuerzel = BMUAVG
searchResult := searchResult "LEFT JOIN WWS.AVG_AUFTRAGSVORERFGRUPP ON AVG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AVSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AVS_LETZTERUSER" "~"
if joinKuerzel = BMUAVS
searchResult := searchResult "LEFT JOIN BUERO.AVS_AVSTATUS ON AVS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AWCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AWC_LETZTERUSER" "~"
if joinKuerzel = BMUAWC
searchResult := searchResult "LEFT JOIN ZLE.AWC_SPALTENAUSWAHL ON AWC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AXTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AXT_LETZTERUSER" "~"
if joinKuerzel = BMUAXT
searchResult := searchResult "LEFT JOIN BMDENTW.AXT_ATTREXT ON AXT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AZKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AZK_LETZTERUSER" "~"
if joinKuerzel = BMUAZK
searchResult := searchResult "LEFT JOIN BIL.AZK_ANBUZUSCHUSSKENNZEICH ON AZK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = AZZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = AZZ_LETZTERUSER" "~"
if joinKuerzel = BMUAZZ
searchResult := searchResult "LEFT JOIN FIBU.AZZ_AUSDRUCKZUSATZ ON AZZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BAPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BAP_LETZTERUSER" "~"
if joinKuerzel = BMUBAP
searchResult := searchResult "LEFT JOIN FIBU.BAP_BUDGETARTIKELPLAN ON BAP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BCUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BCU_LETZTERUSER" "~"
if joinKuerzel = BMUBCU
searchResult := searchResult "LEFT JOIN BUERO.BCU_BMDCOMUSER ON BCU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BDLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BDL_LETZTERUSER" "~"
if joinKuerzel = BMUBDL
searchResult := searchResult "LEFT JOIN BUERO.BDL_BUERODELETE ON BDL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BLGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BLG_LETZTERUSER" "~"
if joinKuerzel = BMUBLG
searchResult := searchResult "LEFT JOIN BUERO.BLG_BEZARTLISTGEN ON BLG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BMFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BMF_LETZTERUSER" "~"
if joinKuerzel = BMUBMF
searchResult := searchResult "LEFT JOIN BMD.BMF_BMDFORMATKATALOG ON BMF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BPLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BPL_LETZTERUSER" "~"
if joinKuerzel = BMUBPL
searchResult := searchResult "LEFT JOIN BMDENTW.BPL_PACKAGES ON BPL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BPOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BPO_LETZTERUSER" "~"
if joinKuerzel = BMUBPO
searchResult := searchResult "LEFT JOIN BUERO.BPO_BESPRECHOBJEKTE ON BPO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BPPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BPP_LETZTERUSER" "~"
if joinKuerzel = BMUBPP
searchResult := searchResult "LEFT JOIN WWS.BPP_BESTPOSITION ON BPP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BRKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BRK_LETZTERUSER" "~"
if joinKuerzel = BMUBRK
searchResult := searchResult "LEFT JOIN HRS.BRK_BEARBEITERKZ ON BRK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BSEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BSE_LETZTERUSER" "~"
if joinKuerzel = BMUBSE
searchResult := searchResult "LEFT JOIN BMD.BSE_BMDSESSION ON BSE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BTKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BTK_LETZTERUSER" "~"
if joinKuerzel = BMUBTK
searchResult := searchResult "LEFT JOIN FIBU.BTK_BUCHUNGSTEXTKUERZEL ON BTK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BWKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BWK_LETZTERUSER" "~"
if joinKuerzel = BMUBWK
searchResult := searchResult "LEFT JOIN BUERO.BWK_BENUTZERWERTKATEGORIE ON BWK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = BWPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = BWP_LETZTERUSER" "~"
if joinKuerzel = BMUBWP
searchResult := searchResult "LEFT JOIN BUERO.BWP_BENUTZERWERTEPARAM ON BWP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CABBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CAB_LETZTERUSER" "~"
if joinKuerzel = BMUCAB
searchResult := searchResult "LEFT JOIN BUERO.CAB_CRMABWBERECHTZU ON CAB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CBRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CBR_LETZTERUSER" "~"
if joinKuerzel = BMUCBR
searchResult := searchResult "LEFT JOIN BUERO.CBR_CRMABWBENACHREGEL ON CBR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CCCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CCC_LETZTERUSER" "~"
if joinKuerzel = BMUCCC
searchResult := searchResult "LEFT JOIN BIL.CCC_CBCCONSTENTITY ON CCC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = CHGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = CHG_LETZTERUSER" "~"
if joinKuerzel = BMUCHG
searchResult := searchResult "LEFT JOIN WWS.CHG_CHARGE ON CHG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DAK_LETZTERUSER" "~"
if joinKuerzel = BMUDAK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DAK_DATTRIBUTKATEGORIE ON DAK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DBSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DBS_LETZTERUSER" "~"
if joinKuerzel = BMUDBS
searchResult := searchResult "LEFT JOIN BUERO.DBS_DATENBESTAND ON DBS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DBTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DBT_LETZTERUSER" "~"
if joinKuerzel = BMUDBT
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DBT_DBMDTABFELDBESCH ON DBT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DCPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DCP_LETZTERUSER" "~"
if joinKuerzel = BMUDCP
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DCP_CONSTRAINTS_PD ON DCP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DDSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DDS_LETZTERUSER" "~"
if joinKuerzel = BMUDDS
searchResult := searchResult "LEFT JOIN HRS.DDS_DBSDIVSTAMM ON DDS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DFZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DFZ_LETZTERUSER" "~"
if joinKuerzel = BMUDFZ
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DFZ_DFELDGRUPPENZU ON DFZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DIGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DIG_LETZTERUSER" "~"
if joinKuerzel = BMUDIG
searchResult := searchResult "LEFT JOIN BMD.DIG_DIAGRAMMGRUPPE ON DIG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DKPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DKP_LETZTERUSER" "~"
if joinKuerzel = BMUDKP
searchResult := searchResult "LEFT JOIN FIBU.DKP_DOKUMENTENPOOL ON DKP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DKVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DKV_LETZTERUSER" "~"
if joinKuerzel = BMUDKV
searchResult := searchResult "LEFT JOIN BUERO.DKV_DOKUMENTVERSION ON DKV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DNEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DNE_LETZTERUSER" "~"
if joinKuerzel = BMUDNE
searchResult := searchResult "LEFT JOIN DATANORM.DNE_ERSTELLER ON DNE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DQUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DQU_LETZTERUSER" "~"
if joinKuerzel = BMUDQU
searchResult := searchResult "LEFT JOIN BMD.DQU_DATENQUELLE ON DQU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DRZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DRZ_LETZTERUSER" "~"
if joinKuerzel = BMUDRZ
searchResult := searchResult "LEFT JOIN BMD.DRZ_DRUCKERBENUTZERWERT ON DRZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = DUKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = DUK_LETZTERUSER" "~"
if joinKuerzel = BMUDUK
searchResult := searchResult "LEFT JOIN BMDDEFAULT.DUK_DUSERLINK ON DUK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = E18BMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = E18_LETZTERUSER" "~"
if joinKuerzel = BMUE18
searchResult := searchResult "LEFT JOIN HRS.E18_ELDADETAIL_E18 ON E18_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EAS_LETZTERUSER" "~"
if joinKuerzel = BMUEAS
searchResult := searchResult "LEFT JOIN FIBU.EAS_ERKONTOAUFTEILSATZ ON EAS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EBVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EBV_LETZTERUSER" "~"
if joinKuerzel = BMUEBV
searchResult := searchResult "LEFT JOIN HRS.EBV_ELDADETAIL_BV ON EBV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EDNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EDN_LETZTERUSER" "~"
if joinKuerzel = BMUEDN
searchResult := searchResult "LEFT JOIN BMD.EDN_EDINACHRICHT ON EDN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EEZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EEZ_LETZTERUSER" "~"
if joinKuerzel = BMUEEZ
searchResult := searchResult "LEFT JOIN WWS.EEZ_EDIEINHEITENZU ON EEZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EFABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EFA_LETZTERUSER" "~"
if joinKuerzel = BMUEFA
searchResult := searchResult "LEFT JOIN WWS.EFA_ERFASSUNGSART ON EFA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EFWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EFW_LETZTERUSER" "~"
if joinKuerzel = BMUEFW
searchResult := searchResult "LEFT JOIN HRS.EFW_ENTGELTFORTZ_WERTE ON EFW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EFZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EFZ_LETZTERUSER" "~"
if joinKuerzel = BMUEFZ
searchResult := searchResult "LEFT JOIN HRS.EFZ_ENTGELTFORTZAHLUNG ON EFZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EGSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EGS_LETZTERUSER" "~"
if joinKuerzel = BMUEGS
searchResult := searchResult "LEFT JOIN BUERO.EGS_ERFASSUNGSGRUND ON EGS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EINBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EIN_LETZTERUSER" "~"
if joinKuerzel = BMUEIN
searchResult := searchResult "LEFT JOIN WWS.EIN_EINHEIT ON EIN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EKKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EKK_LETZTERUSER" "~"
if joinKuerzel = BMUEKK
searchResult := searchResult "LEFT JOIN WWS.EKK_EXTERNE_KUNDENKARTEI ON EKK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EKZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EKZ_LETZTERUSER" "~"
if joinKuerzel = BMUEKZ
searchResult := searchResult "LEFT JOIN WWS.EKZ_ERLOESAUFWANDKZ ON EKZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EQIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EQI_LETZTERUSER" "~"
if joinKuerzel = BMUEQI
searchResult := searchResult "LEFT JOIN WWS.EQI_ERFASSPOSQUELLINFO ON EQI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ERKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ERK_LETZTERUSER" "~"
if joinKuerzel = BMUERK
searchResult := searchResult "LEFT JOIN FIBU.ERK_KONTO ON ERK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EROBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ERO_LETZTERUSER" "~"
if joinKuerzel = BMUERO
searchResult := searchResult "LEFT JOIN WWS.ERO_ERFASSPOSORIG ON ERO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ESDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ESD_LETZTERUSER" "~"
if joinKuerzel = BMUESD
searchResult := searchResult "LEFT JOIN WWS.ESD_ERFASSUNGSERVICEDATEN ON ESD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EVABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EVA_LETZTERUSER" "~"
if joinKuerzel = BMUEVA
searchResult := searchResult "LEFT JOIN BUERO.EVA_EVENTTEAM ON EVA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = EXTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = EXT_LETZTERUSER" "~"
if joinKuerzel = BMUEXT
searchResult := searchResult "LEFT JOIN FIBU.EXT_EXTJOURNAL ON EXT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FAZ_LETZTERUSER" "~"
if joinKuerzel = BMUFAZ
searchResult := searchResult "LEFT JOIN BUERO.FAZ_FRAGEANTWORTZU ON FAZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FBPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FBP_LETZTERUSER" "~"
if joinKuerzel = BMUFBP
searchResult := searchResult "LEFT JOIN BMD.FBP_FELDBESCHPOS ON FBP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FEDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FED_LETZTERUSER" "~"
if joinKuerzel = BMUFED
searchResult := searchResult "LEFT JOIN WWS.FED_FERTEP_AUFTDETAIL ON FED_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FEZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FEZ_LETZTERUSER" "~"
if joinKuerzel = BMUFEZ
searchResult := searchResult "LEFT JOIN BMD.FEZ_FELDGRUPPENZU ON FEZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FFBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FFB_LETZTERUSER" "~"
if joinKuerzel = BMUFFB
searchResult := searchResult "LEFT JOIN BMD.FFB_FUNKFELDBESCHRECHTE ON FFB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FGRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FGR_LETZTERUSER" "~"
if joinKuerzel = BMUFGR
searchResult := searchResult "LEFT JOIN BUERO.FGR_FIRMENGERICHTZU ON FGR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FIGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FIG_LETZTERUSER" "~"
if joinKuerzel = BMUFIG
searchResult := searchResult "LEFT JOIN BUERO.FIG_FIRMENGRUPPENZU ON FIG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FIPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FIP_LETZTERUSER" "~"
if joinKuerzel = BMUFIP
searchResult := searchResult "LEFT JOIN BMD.FIP_FILTERPANEL ON FIP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FIXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FIX_LETZTERUSER" "~"
if joinKuerzel = BMUFIX
searchResult := searchResult "LEFT JOIN FIBU.FIX_FIXKONTEN ON FIX_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FIZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FIZ_LETZTERUSER" "~"
if joinKuerzel = BMUFIZ
searchResult := searchResult "LEFT JOIN FIBU.FIZ_FILIALEKONSZU ON FIZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FKKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FKK_LETZTERUSER" "~"
if joinKuerzel = BMUFKK
searchResult := searchResult "LEFT JOIN ZLE.FKK_FAKTURAKZKUNDENZU ON FKK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FMJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FMJ_LETZTERUSER" "~"
if joinKuerzel = BMUFMJ
searchResult := searchResult "LEFT JOIN WWS.FMJ_FISKALMODULJOURNAL ON FMJ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FNSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FNS_LETZTERUSER" "~"
if joinKuerzel = BMUFNS
searchResult := searchResult "LEFT JOIN BIL.FNS_FINANZINSTRUMENTE ON FNS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FSKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FSK_LETZTERUSER" "~"
if joinKuerzel = BMUFSK
searchResult := searchResult "LEFT JOIN FIBU.FSK_SORTIERKOPF ON FSK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = FZKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = FZK_LETZTERUSER" "~"
if joinKuerzel = BMUFZK
searchResult := searchResult "LEFT JOIN FIBU.FZK_ZAHLSCHLUESSELKOPF ON FZK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GEBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GEB_LETZTERUSER" "~"
if joinKuerzel = BMUGEB
searchResult := searchResult "LEFT JOIN ZLE.GEB_GEBUEHRENTABELLEN ON GEB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GEMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GEM_LETZTERUSER" "~"
if joinKuerzel = BMUGEM
searchResult := searchResult "LEFT JOIN BUERO.GEM_GEMEINDE ON GEM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GKKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GKK_LETZTERUSER" "~"
if joinKuerzel = BMUGKK
searchResult := searchResult "LEFT JOIN BUERO.GKK_KRANKENKASSA ON GKK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GKLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GKL_LETZTERUSER" "~"
if joinKuerzel = BMUGKL
searchResult := searchResult "LEFT JOIN BIL.GKL_GROESSENKLASSEN ON GKL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GOLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GOL_LETZTERUSER" "~"
if joinKuerzel = BMUGOL
searchResult := searchResult "LEFT JOIN ZLE.GOL_GRUPPENORDNER_LEISTUNG ON GOL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GOPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GOP_LETZTERUSER" "~"
if joinKuerzel = BMUGOP
searchResult := searchResult "LEFT JOIN BUERO.GOP_GRUPPENORDNERPERSZU ON GOP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GSVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GSV_LETZTERUSER" "~"
if joinKuerzel = BMUGSV
searchResult := searchResult "LEFT JOIN BIL.GSV_GSVGBERECH ON GSV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = GSWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = GSW_LETZTERUSER" "~"
if joinKuerzel = BMUGSW
searchResult := searchResult "LEFT JOIN BMDDEFAULT.GSW_GESETZWERT ON GSW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HAKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HAK_LETZTERUSER" "~"
if joinKuerzel = BMUHAK
searchResult := searchResult "LEFT JOIN HRS.HAK_AKONTO ON HAK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HEBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HEB_LETZTERUSER" "~"
if joinKuerzel = BMUHEB
searchResult := searchResult "LEFT JOIN BIL.HEB_HEBESATZ ON HEB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HLFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HLF_LETZTERUSER" "~"
if joinKuerzel = BMUHLF
searchResult := searchResult "LEFT JOIN BMDENTW.HLF_HILFEFAVORITEN ON HLF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = HVPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = HVP_LETZTERUSER" "~"
if joinKuerzel = BMUHVP
searchResult := searchResult "LEFT JOIN HRS.HVP_VERRECHUNGSPOSITION ON HVP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ICDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ICD_LETZTERUSER" "~"
if joinKuerzel = BMUICD
searchResult := searchResult "LEFT JOIN BUERO.ICD_ICCODES ON ICD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IFCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IFC_LETZTERUSER" "~"
if joinKuerzel = BMUIFC
searchResult := searchResult "LEFT JOIN BMDENTW.IFC_INTERNERFARBCODE ON IFC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IKZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IKZ_LETZTERUSER" "~"
if joinKuerzel = BMUIKZ
searchResult := searchResult "LEFT JOIN BUERO.IKZ_INSTITUTIONKONTAKTZU ON IKZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = INTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = INT_LETZTERUSER" "~"
if joinKuerzel = BMUINT
searchResult := searchResult "LEFT JOIN BUERO.INT_INTRASTAT ON INT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = IVZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = IVZ_LETZTERUSER" "~"
if joinKuerzel = BMUIVZ
searchResult := searchResult "LEFT JOIN BUERO.IVZ_INSOLVENZ ON IVZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KDPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KDP_LETZTERUSER" "~"
if joinKuerzel = BMUKDP
searchResult := searchResult "LEFT JOIN KORE.KDP_PLANWERTE ON KDP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KEKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KEK_LETZTERUSER" "~"
if joinKuerzel = BMUKEK
searchResult := searchResult "LEFT JOIN KORE.KEK_ERFASSKOPF ON KEK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KEPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KEP_LETZTERUSER" "~"
if joinKuerzel = BMUKEP
searchResult := searchResult "LEFT JOIN WWS.KEP_KASSENERFASSUNGSPROT ON KEP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KGDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KGD_LETZTERUSER" "~"
if joinKuerzel = BMUKGD
searchResult := searchResult "LEFT JOIN WWS.KGD_KASSENGELDLADE ON KGD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KJOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KJO_LETZTERUSER" "~"
if joinKuerzel = BMUKJO
searchResult := searchResult "LEFT JOIN BUERO.KJO_KONTAKTJOURNAL ON KJO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KPRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KPR_LETZTERUSER" "~"
if joinKuerzel = BMUKPR
searchResult := searchResult "LEFT JOIN KORE.KPR_PLANPREISE ON KPR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KSWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KSW_LETZTERUSER" "~"
if joinKuerzel = BMUKSW
searchResult := searchResult "LEFT JOIN KORE.KSW_KORESTAMMWERTE ON KSW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KUVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KUV_LETZTERUSER" "~"
if joinKuerzel = BMUKUV
searchResult := searchResult "LEFT JOIN KORE.KUV_UMLAGEVARIANTE ON KUV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KVNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KVN_LETZTERUSER" "~"
if joinKuerzel = BMUKVN
searchResult := searchResult "LEFT JOIN HRS.KVN_KVNAME ON KVN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KVXBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KVX_LETZTERUSER" "~"
if joinKuerzel = BMUKVX
searchResult := searchResult "LEFT JOIN HRS.KVX_KVTEXT ON KVX_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = KZABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = KZA_LETZTERUSER" "~"
if joinKuerzel = BMUKZA
searchResult := searchResult "LEFT JOIN WWS.KZA_KASSENZAHLUNGSARTENZU ON KZA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LDOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LDO_LETZTERUSER" "~"
if joinKuerzel = BMULDO
searchResult := searchResult "LEFT JOIN BUERO.LDO_LEBENSLAUFDOKUMENTZU ON LDO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LFSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LFS_LETZTERUSER" "~"
if joinKuerzel = BMULFS
searchResult := searchResult "LEFT JOIN WWS.LFS_LIEFERSCHEIN ON LFS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LGMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LGM_LETZTERUSER" "~"
if joinKuerzel = BMULGM
searchResult := searchResult "LEFT JOIN DATANORM.LGM_LEISTUNG ON LGM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LGNBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LGN_LETZTERUSER" "~"
if joinKuerzel = BMULGN
searchResult := searchResult "LEFT JOIN BIL.LGN_LAENDERGRUPPEN ON LGN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LGRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LGR_LETZTERUSER" "~"
if joinKuerzel = BMULGR
searchResult := searchResult "LEFT JOIN HRS.LGR_LOHNARTGRUPPE ON LGR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LMSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LMS_LETZTERUSER" "~"
if joinKuerzel = BMULMS
searchResult := searchResult "LEFT JOIN WWS.LMS_LIEFERMAHNUNG ON LMS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LPZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LPZ_LETZTERUSER" "~"
if joinKuerzel = BMULPZ
searchResult := searchResult "LEFT JOIN BUERO.LPZ_LIZENZKEYPERSONZU ON LPZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LSPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LSP_LETZTERUSER" "~"
if joinKuerzel = BMULSP
searchResult := searchResult "LEFT JOIN HRS.LSP_LOHNSTAPEL ON LSP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LTKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LTK_LETZTERUSER" "~"
if joinKuerzel = BMULTK
searchResult := searchResult "LEFT JOIN ZLE.LTK_LEISTTAUSCHKOPF ON LTK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = LZTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = LZT_LETZTERUSER" "~"
if joinKuerzel = BMULZT
searchResult := searchResult "LEFT JOIN BMD.LZT_LIZENZTEST ON LZT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MABBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MAB_LETZTERUSER" "~"
if joinKuerzel = BMUMAB
searchResult := searchResult "LEFT JOIN BUERO.MAB_MITARBAUSBILDUNG ON MAB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MBMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MBM_LETZTERUSER" "~"
if joinKuerzel = BMUMBM
searchResult := searchResult "LEFT JOIN HRS.MBM_MBGM ON MBM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MBOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MBO_LETZTERUSER" "~"
if joinKuerzel = BMUMBO
searchResult := searchResult "LEFT JOIN BMD.MBO_MAILBOX ON MBO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MEVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MEV_LETZTERUSER" "~"
if joinKuerzel = BMUMEV
searchResult := searchResult "LEFT JOIN BUERO.MEV_MEDIENVERLEIH ON MEV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MPDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MPD_LETZTERUSER" "~"
if joinKuerzel = BMUMPD
searchResult := searchResult "LEFT JOIN WWS.MPD_MODELLPOSVARDETAIL ON MPD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MRDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MRD_LETZTERUSER" "~"
if joinKuerzel = BMUMRD
searchResult := searchResult "LEFT JOIN ZLE.MRD_MONATSRECHNUNGDETAIL ON MRD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MRVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MRV_LETZTERUSER" "~"
if joinKuerzel = BMUMRV
searchResult := searchResult "LEFT JOIN ZLE.MRV_MONATSRECHNUNGVORLAGE ON MRV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MSUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MSU_LETZTERUSER" "~"
if joinKuerzel = BMUMSU
searchResult := searchResult "LEFT JOIN FIBU.MSU_MAHNSUMME ON MSU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MUZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MUZ_LETZTERUSER" "~"
if joinKuerzel = BMUMUZ
searchResult := searchResult "LEFT JOIN BUERO.MUZ_MITARBUSERZU ON MUZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = MVLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = MVL_LETZTERUSER" "~"
if joinKuerzel = BMUMVL
searchResult := searchResult "LEFT JOIN BUERO.MVL_MITARBEITERVORLAGE ON MVL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NEBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NEB_LETZTERUSER" "~"
if joinKuerzel = BMUNEB
searchResult := searchResult "LEFT JOIN BIL.NEB_NEBENWERTE ON NEB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NORBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NOR_LETZTERUSER" "~"
if joinKuerzel = BMUNOR
searchResult := searchResult "LEFT JOIN WWS.NOR_NORM ON NOR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NUBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NUB_LETZTERUSER" "~"
if joinKuerzel = BMUNUB
searchResult := searchResult "LEFT JOIN BMD.NUB_NTCSUPDBEZIEHER ON NUB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = NUYBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = NUY_LETZTERUSER" "~"
if joinKuerzel = BMUNUY
searchResult := searchResult "LEFT JOIN BMD.NUY_NTCSUPDTYP ON NUY_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OAS_LETZTERUSER" "~"
if joinKuerzel = BMUOAS
searchResult := searchResult "LEFT JOIN LOHN.OAS_OF_ARBEITSZEITSTUNDEN ON OAS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OBLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OBL_LETZTERUSER" "~"
if joinKuerzel = BMUOBL
searchResult := searchResult "LEFT JOIN BUERO.OBL_ONLBEWLEBENSLAUF ON OBL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OBQBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OBQ_LETZTERUSER" "~"
if joinKuerzel = BMUOBQ
searchResult := searchResult "LEFT JOIN BUERO.OBQ_ONLBEWQUALIFIKATION ON OBQ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OCTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OCT_LETZTERUSER" "~"
if joinKuerzel = BMUOCT
searchResult := searchResult "LEFT JOIN BMD.OCT_OCR_SUCHTEXT ON OCT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OGEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OGE_LETZTERUSER" "~"
if joinKuerzel = BMUOGE
searchResult := searchResult "LEFT JOIN LOHN.OGE_OF_GEMEINDE ON OGE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OKLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OKL_LETZTERUSER" "~"
if joinKuerzel = BMUOKL
searchResult := searchResult "LEFT JOIN LOHN.OKL_OF_KLIENT ON OKL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OKVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OKV_LETZTERUSER" "~"
if joinKuerzel = BMUOKV
searchResult := searchResult "LEFT JOIN LOHN.OKV_OF_KOLLEKTIV ON OKV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OKZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OKZ_LETZTERUSER" "~"
if joinKuerzel = BMUOKZ
searchResult := searchResult "LEFT JOIN LOHN.OKZ_OF_KFZVERWALTUNG ON OKZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OLABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OLA_LETZTERUSER" "~"
if joinKuerzel = BMUOLA
searchResult := searchResult "LEFT JOIN LOHN.OLA_OF_LOHNART ON OLA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OLEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OLE_LETZTERUSER" "~"
if joinKuerzel = BMUOLE
searchResult := searchResult "LEFT JOIN LOHN.OLE_OF_LEISTUNG ON OLE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OPDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OPD_LETZTERUSER" "~"
if joinKuerzel = BMUOPD
searchResult := searchResult "LEFT JOIN LOHN.OPD_OF_PIPEDOGS ON OPD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ORSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ORS_LETZTERUSER" "~"
if joinKuerzel = BMUORS
searchResult := searchResult "LEFT JOIN LOHN.ORS_OF_REISESTAMM ON ORS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OSTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OST_LETZTERUSER" "~"
if joinKuerzel = BMUOST
searchResult := searchResult "LEFT JOIN LOHN.OST_OF_STAAT ON OST_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = OUPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = OUP_LETZTERUSER" "~"
if joinKuerzel = BMUOUP
searchResult := searchResult "LEFT JOIN BUERO.OUP_ONLUMFRAGEPAGE ON OUP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PAABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAA_LETZTERUSER" "~"
if joinKuerzel = BMUPAA
searchResult := searchResult "LEFT JOIN BMDDEFAULT.PAA_PARAMAUSPRAEGUNG ON PAA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PACBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAC_LETZTERUSER" "~"
if joinKuerzel = BMUPAC
searchResult := searchResult "LEFT JOIN WWS.PAC_PACKSTOFF ON PAC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PANBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAN_LETZTERUSER" "~"
if joinKuerzel = BMUPAN
searchResult := searchResult "LEFT JOIN WWS.PAN_PROVARTBAUMKNOTEN ON PAN_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PAPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PAP_LETZTERUSER" "~"
if joinKuerzel = BMUPAP
searchResult := searchResult "LEFT JOIN WWS.PAP_PACKSTOFFTABPOS ON PAP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PBMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PBM_LETZTERUSER" "~"
if joinKuerzel = BMUPBM
searchResult := searchResult "LEFT JOIN PRODUKTION.PBM_BEFEHL ON PBM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PBWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PBW_LETZTERUSER" "~"
if joinKuerzel = BMUPBW
searchResult := searchResult "LEFT JOIN PRODUKTION.PBW_PLAN_WERT ON PBW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PCLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PCL_LETZTERUSER" "~"
if joinKuerzel = BMUPCL
searchResult := searchResult "LEFT JOIN BUERO.PCL_PERSONCLOUDZU ON PCL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PEABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PEA_LETZTERUSER" "~"
if joinKuerzel = BMUPEA
searchResult := searchResult "LEFT JOIN WWS.PEA_PICEANNUMMERN ON PEA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PGWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PGW_LETZTERUSER" "~"
if joinKuerzel = BMUPGW
searchResult := searchResult "LEFT JOIN BUERO.PGW_PERSONGESELLWERTE ON PGW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PIGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIG_LETZTERUSER" "~"
if joinKuerzel = BMUPIG
searchResult := searchResult "LEFT JOIN WWS.PIG_PICPREISGRUPPE ON PIG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PISBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PIS_LETZTERUSER" "~"
if joinKuerzel = BMUPIS
searchResult := searchResult "LEFT JOIN WWS.PIS_PICPREISSTAFFEL ON PIS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PJOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PJO_LETZTERUSER" "~"
if joinKuerzel = BMUPJO
searchResult := searchResult "LEFT JOIN WWS.PJO_PREISJOURNAL ON PJO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKA_LETZTERUSER" "~"
if joinKuerzel = BMUPKA
searchResult := searchResult "LEFT JOIN BUERO.PKA_PROJEKTART ON PKA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKCBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKC_LETZTERUSER" "~"
if joinKuerzel = BMUPKC
searchResult := searchResult "LEFT JOIN HRS.PKC_PENSKASSA_CALC ON PKC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PKGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PKG_LETZTERUSER" "~"
if joinKuerzel = BMUPKG
searchResult := searchResult "LEFT JOIN BMDDEFAULT.PKG_PROTOKOLLGRUPPE ON PKG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PLTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PLT_LETZTERUSER" "~"
if joinKuerzel = BMUPLT
searchResult := searchResult "LEFT JOIN BUERO.PLT_PIPELINESTUFE ON PLT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PODBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = POD_LETZTERUSER" "~"
if joinKuerzel = BMUPOD
searchResult := searchResult "LEFT JOIN BUERO.POD_POSTDOKUMENTZU ON POD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = POSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = POS_LETZTERUSER" "~"
if joinKuerzel = BMUPOS
searchResult := searchResult "LEFT JOIN BUERO.POS_POST ON POS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = POZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = POZ_LETZTERUSER" "~"
if joinKuerzel = BMUPOZ
searchResult := searchResult "LEFT JOIN BUERO.POZ_PISWORKFLOWZU ON POZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PPZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PPZ_LETZTERUSER" "~"
if joinKuerzel = BMUPPZ
searchResult := searchResult "LEFT JOIN BUERO.PPZ_PLANGRUPPEPROZESSZU ON PPZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PRZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PRZ_LETZTERUSER" "~"
if joinKuerzel = BMUPRZ
searchResult := searchResult "LEFT JOIN PRODUKTION.PRZ_PROZESSBAZZU ON PRZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PSPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PSP_LETZTERUSER" "~"
if joinKuerzel = BMUPSP
searchResult := searchResult "LEFT JOIN PRODUKTION.PSP_STAPEL_SCHRITT_PARAM ON PSP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PSSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PSS_LETZTERUSER" "~"
if joinKuerzel = BMUPSS
searchResult := searchResult "LEFT JOIN PRODUKTION.PSS_STAPEL_SCHRITT ON PSS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = PWEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = PWE_LETZTERUSER" "~"
if joinKuerzel = BMUPWE
searchResult := searchResult "LEFT JOIN WWS.PWE_PWKPREISTABZU ON PWE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = R2ABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = R2A_LETZTERUSER" "~"
if joinKuerzel = BMUR2A
searchResult := searchResult "LEFT JOIN PRODUKTION.R2A_RPM_ARK_ZU ON R2A_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RAZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RAZ_LETZTERUSER" "~"
if joinKuerzel = BMURAZ
searchResult := searchResult "LEFT JOIN BUERO.RAZ_RATENZAHLUNG ON RAZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RECBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = REC_LETZTERUSER" "~"
if joinKuerzel = BMUREC
searchResult := searchResult "LEFT JOIN BMD.REC_RECORDINTERVALL ON REC_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RGDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RGD_LETZTERUSER" "~"
if joinKuerzel = BMURGD
searchResult := searchResult "LEFT JOIN DATANORM.RGD_RABATT_GRUPPE_DETAIL ON RGD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RPFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RPF_LETZTERUSER" "~"
if joinKuerzel = BMURPF
searchResult := searchResult "LEFT JOIN BMD.RPF_REPORTFORMATVORLAGE ON RPF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RPRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RPR_LETZTERUSER" "~"
if joinKuerzel = BMURPR
searchResult := searchResult "LEFT JOIN WWS.RPR_RECHNUNGSPROVISION ON RPR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RRDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RRD_LETZTERUSER" "~"
if joinKuerzel = BMURRD
searchResult := searchResult "LEFT JOIN WWS.RRD_RECHNUNGSRABATTDETAIL ON RRD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RSDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RSD_LETZTERUSER" "~"
if joinKuerzel = BMURSD
searchResult := searchResult "LEFT JOIN WWS.RSD_REGKASPROTOKOLL ON RSD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RSOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RSO_LETZTERUSER" "~"
if joinKuerzel = BMURSO
searchResult := searchResult "LEFT JOIN HRS.RSO_REPORTSORTIERUNG ON RSO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = RUZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = RUZ_LETZTERUSER" "~"
if joinKuerzel = BMURUZ
searchResult := searchResult "LEFT JOIN BMD.RUZ_ROLLEUSERZU ON RUZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SAIBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SAI_LETZTERUSER" "~"
if joinKuerzel = BMUSAI
searchResult := searchResult "LEFT JOIN WWS.SAI_SAISON ON SAI_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SALBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SAL_LETZTERUSER" "~"
if joinKuerzel = BMUSAL
searchResult := searchResult "LEFT JOIN FIBU.SAL_SALDEN ON SAL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SBBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SBB_LETZTERUSER" "~"
if joinKuerzel = BMUSBB
searchResult := searchResult "LEFT JOIN HRS.SBB_SOLLBESETZUNG ON SBB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SBMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SBM_LETZTERUSER" "~"
if joinKuerzel = BMUSBM
searchResult := searchResult "LEFT JOIN BUERO.SBM_SBSPERSONALMITARBZU ON SBM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SBVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SBV_LETZTERUSER" "~"
if joinKuerzel = BMUSBV
searchResult := searchResult "LEFT JOIN BUERO.SBV_STELLENBESCHVORLAGE ON SBV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCA_LETZTERUSER" "~"
if joinKuerzel = BMUSCA
searchResult := searchResult "LEFT JOIN PRODUKTION.SCA_SCKRITERIUM ON SCA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SCOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SCO_LETZTERUSER" "~"
if joinKuerzel = BMUSCO
searchResult := searchResult "LEFT JOIN PRODUKTION.SCO_SCINSTANZ ON SCO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SEQBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SEQ_LETZTERUSER" "~"
if joinKuerzel = BMUSEQ
searchResult := searchResult "LEFT JOIN BMD.SEQ_SEQUENZ ON SEQ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SFZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SFZ_LETZTERUSER" "~"
if joinKuerzel = BMUSFZ
searchResult := searchResult "LEFT JOIN BUERO.SFZ_SYNCFELDZUORDNUNG ON SFZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPF_LETZTERUSER" "~"
if joinKuerzel = BMUSPF
searchResult := searchResult "LEFT JOIN BMDENTW.SPF_SCRPROJFILEZU ON SPF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPS_LETZTERUSER" "~"
if joinKuerzel = BMUSPS
searchResult := searchResult "LEFT JOIN FIBU.SPS_SPENDENSUMME ON SPS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SPUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SPU_LETZTERUSER" "~"
if joinKuerzel = BMUSPU
searchResult := searchResult "LEFT JOIN BMDENTW.SPU_SCRPGMZU ON SPU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SSPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SSP_LETZTERUSER" "~"
if joinKuerzel = BMUSSP
searchResult := searchResult "LEFT JOIN PRODUKTION.SSP_SAMMELSCHEIN_POS ON SSP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STF_LETZTERUSER" "~"
if joinKuerzel = BMUSTF
searchResult := searchResult "LEFT JOIN FIBU.STF_FIBUSTEUERCODEZU ON STF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STHBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STH_LETZTERUSER" "~"
if joinKuerzel = BMUSTH
searchResult := searchResult "LEFT JOIN PRODUKTION.STH_STAKE ON STH_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STT_LETZTERUSER" "~"
if joinKuerzel = BMUSTT
searchResult := searchResult "LEFT JOIN PRODUKTION.STT_STAKE_TYP ON STT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = STWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = STW_LETZTERUSER" "~"
if joinKuerzel = BMUSTW
searchResult := searchResult "LEFT JOIN BIL.STW_STEUERWERT ON STW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = SUDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = SUD_LETZTERUSER" "~"
if joinKuerzel = BMUSUD
searchResult := searchResult "LEFT JOIN HRS.SUD_SUM_KOE_DIFF ON SUD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TALBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TAL_LETZTERUSER" "~"
if joinKuerzel = BMUTAL
searchResult := searchResult "LEFT JOIN WWS.TAL_ARTIKELZUORDNUNG_TMP ON TAL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TASBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TAS_LETZTERUSER" "~"
if joinKuerzel = BMUTAS
searchResult := searchResult "LEFT JOIN BMD.TAS_ACTIONSTACK_TMP ON TAS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TBEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TBE_LETZTERUSER" "~"
if joinKuerzel = BMUTBE
searchResult := searchResult "LEFT JOIN BMD.TBE_TEMPBERECHTIGUNG ON TBE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TCVBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TCV_LETZTERUSER" "~"
if joinKuerzel = BMUTCV
searchResult := searchResult "LEFT JOIN BMD.TCV_TCVERSIONCASE ON TCV_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TELBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TEL_LETZTERUSER" "~"
if joinKuerzel = BMUTEL
searchResult := searchResult "LEFT JOIN BUERO.TEL_TELEFON ON TEL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TGRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TGR_LETZTERUSER" "~"
if joinKuerzel = BMUTGR
searchResult := searchResult "LEFT JOIN HRS.TGR_TARIFGRUPPE ON TGR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TLJBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TLJ_LETZTERUSER" "~"
if joinKuerzel = BMUTLJ
searchResult := searchResult "LEFT JOIN WWS.TLJ_LAGERJOURNAL_TMP ON TLJ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TLKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TLK_LETZTERUSER" "~"
if joinKuerzel = BMUTLK
searchResult := searchResult "LEFT JOIN PRODUKTION.TLK_BT_LOGISTIK_KOPF ON TLK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TLPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TLP_LETZTERUSER" "~"
if joinKuerzel = BMUTLP
searchResult := searchResult "LEFT JOIN PRODUKTION.TLP_BT_LOGISTIK_POSITION ON TLP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TPTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TPT_LETZTERUSER" "~"
if joinKuerzel = BMUTPT
searchResult := searchResult "LEFT JOIN HRS.TPT_TARIFPROZENT ON TPT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TRTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TRT_LETZTERUSER" "~"
if joinKuerzel = BMUTRT
searchResult := searchResult "LEFT JOIN WWS.TRT_TRANSPORTTYP ON TRT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TSRBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TSR_LETZTERUSER" "~"
if joinKuerzel = BMUTSR
searchResult := searchResult "LEFT JOIN BUERO.TSR_SERIENTERMIN ON TSR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = TWLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = TWL_LETZTERUSER" "~"
if joinKuerzel = BMUTWL
searchResult := searchResult "LEFT JOIN WWS.TWL_WEINSATZLAJ_TMP ON TWL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UATBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UAT_LETZTERUSER" "~"
if joinKuerzel = BMUUAT
searchResult := searchResult "LEFT JOIN BUERO.UAT_UMFRAGEANTWTEILNZU ON UAT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UBEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UBE_LETZTERUSER" "~"
if joinKuerzel = BMUUBE
searchResult := searchResult "LEFT JOIN BMDINTERN.UBE_UPDATEBESTELLUNG ON UBE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UDBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UDB_LETZTERUSER" "~"
if joinKuerzel = BMUUDB
searchResult := searchResult "LEFT JOIN BIL.UDB_ANGUNTERBILANZ ON UDB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UIDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UID_LETZTERUSER" "~"
if joinKuerzel = BMUUID
searchResult := searchResult "LEFT JOIN BUERO.UID_UIDNUMMER ON UID_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ULFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ULF_LETZTERUSER" "~"
if joinKuerzel = BMUULF
searchResult := searchResult "LEFT JOIN BMD.ULF_USERLINKFIELD ON ULF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UMSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UMS_LETZTERUSER" "~"
if joinKuerzel = BMUUMS
searchResult := searchResult "LEFT JOIN WWS.UMS_UMSATZZEILE ON UMS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = UVSBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = UVS_LETZTERUSER" "~"
if joinKuerzel = BMUUVS
searchResult := searchResult "LEFT JOIN FIBU.UVS_MCUSTEUERCODEZU ON UVS_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VHEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VHE_LETZTERUSER" "~"
if joinKuerzel = BMUVHE
searchResult := searchResult "LEFT JOIN WWS.VHE_VA_HAUPTEANNUMMERN ON VHE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VKDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VKD_LETZTERUSER" "~"
if joinKuerzel = BMUVKD
searchResult := searchResult "LEFT JOIN BUERO.VKD_VKOPFDOKZU ON VKD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VMZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VMZ_LETZTERUSER" "~"
if joinKuerzel = BMUVMZ
searchResult := searchResult "LEFT JOIN WWS.VMZ_VERSMGR_LFS_ZUORD ON VMZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VOBBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VOB_LETZTERUSER" "~"
if joinKuerzel = BMUVOB
searchResult := searchResult "LEFT JOIN PRODUKTION.VOB_VORGANGSBENACHR ON VOB_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VPEBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VPE_LETZTERUSER" "~"
if joinKuerzel = BMUVPE
searchResult := searchResult "LEFT JOIN WWS.VPE_VPI_ERHOEHUNG ON VPE_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VVZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VVZ_LETZTERUSER" "~"
if joinKuerzel = BMUVVZ
searchResult := searchResult "LEFT JOIN BUERO.VVZ_VTAGVORTRAGENDERZU ON VVZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = VZZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = VZZ_LETZTERUSER" "~"
if joinKuerzel = BMUVZZ
searchResult := searchResult "LEFT JOIN HRS.VZZ_VORZEITZU ON VZZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WAGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAG_LETZTERUSER" "~"
if joinKuerzel = BMUWAG
searchResult := searchResult "LEFT JOIN WWS.WAG_WARENGRUPPE ON WAG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WAOBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WAO_LETZTERUSER" "~"
if joinKuerzel = BMUWAO
searchResult := searchResult "LEFT JOIN WWS.WAO_WARENEINGWBK ON WAO_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WBKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WBK_LETZTERUSER" "~"
if joinKuerzel = BMUWBK
searchResult := searchResult "LEFT JOIN WEB.WBK_WEBWARENKORBKOPF ON WBK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WBPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WBP_LETZTERUSER" "~"
if joinKuerzel = BMUWBP
searchResult := searchResult "LEFT JOIN WEB.WBP_WEBWARENKORBPOS ON WBP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WBTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WBT_LETZTERUSER" "~"
if joinKuerzel = BMUWBT
searchResult := searchResult "LEFT JOIN WWS.WBT_WWSBUDGETKOPF ON WBT_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WBUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WBU_LETZTERUSER" "~"
if joinKuerzel = BMUWBU
searchResult := searchResult "LEFT JOIN WEB.WBU_WEBKUNDE ON WBU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEA_LETZTERUSER" "~"
if joinKuerzel = BMUWEA
searchResult := searchResult "LEFT JOIN WWS.WEA_WWSERKONTROLLERABATTE ON WEA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WERBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WER_LETZTERUSER" "~"
if joinKuerzel = BMUWER
searchResult := searchResult "LEFT JOIN WWS.WER_WWSERKONTROLLEKOPF ON WER_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WEZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WEZ_LETZTERUSER" "~"
if joinKuerzel = BMUWEZ
searchResult := searchResult "LEFT JOIN PRODUKTION.WEZ_WERKZEUGBAZZU ON WEZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WIKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WIK_LETZTERUSER" "~"
if joinKuerzel = BMUWIK
searchResult := searchResult "LEFT JOIN BIL.WIK_WIVARIABLENKATEGORIE ON WIK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WOLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WOL_LETZTERUSER" "~"
if joinKuerzel = BMUWOL
searchResult := searchResult "LEFT JOIN ZLE.WOL_WOCHENLEISTUNG ON WOL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WPDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WPD_LETZTERUSER" "~"
if joinKuerzel = BMUWPD
searchResult := searchResult "LEFT JOIN BIL.WPD_WPDIFFERENZEN ON WPD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WQFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WQF_LETZTERUSER" "~"
if joinKuerzel = BMUWQF
searchResult := searchResult "LEFT JOIN BMDDEFAULT.WQF_WQI_DFU_ZU ON WQF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WSTBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WST_LETZTERUSER" "~"
if joinKuerzel = BMUWST
searchResult := searchResult "LEFT JOIN WWS.WST_WWSSTATISTIK_TMP ON WST_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WSZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WSZ_LETZTERUSER" "~"
if joinKuerzel = BMUWSZ
searchResult := searchResult "LEFT JOIN WWS.WSZ_WESEPS_ZUORDNUNG ON WSZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WURBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WUR_LETZTERUSER" "~"
if joinKuerzel = BMUWUR
searchResult := searchResult "LEFT JOIN WWS.WUR_WWSURL ON WUR_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WUZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WUZ_LETZTERUSER" "~"
if joinKuerzel = BMUWUZ
searchResult := searchResult "LEFT JOIN BUERO.WUZ_WUNSCHARBEITSZEIT ON WUZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WVDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WVD_LETZTERUSER" "~"
if joinKuerzel = BMUWVD
searchResult := searchResult "LEFT JOIN WWS.WVD_WEINSATZVERLEIHDAT ON WVD_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = WZZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = WZZ_LETZTERUSER" "~"
if joinKuerzel = BMUWZZ
searchResult := searchResult "LEFT JOIN WWS.WZZ_WARENEINSATZERZZU ON WZZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZAMBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZAM_LETZTERUSER" "~"
if joinKuerzel = BMUZAM
searchResult := searchResult "LEFT JOIN DATANORM.ZAM_ZU_ABSCHLAG ON ZAM_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZAWBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZAW_LETZTERUSER" "~"
if joinKuerzel = BMUZAW
searchResult := searchResult "LEFT JOIN BUERO.ZAW_ZEITAUSWERTUNG ON ZAW_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZEDBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZED_LETZTERUSER" "~"
if joinKuerzel = BMUZED
searchResult := searchResult "LEFT JOIN BMD.ZED_ZELLENDEF ON ZED_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZEPBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZEP_LETZTERUSER" "~"
if joinKuerzel = BMUZEP
searchResult := searchResult "LEFT JOIN WWS.ZEP_ZEITEINHEITENPOS ON ZEP_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZIZBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZIZ_LETZTERUSER" "~"
if joinKuerzel = BMUZIZ
searchResult := searchResult "LEFT JOIN BUERO.ZIZ_ZUSATZINFOZUORD ON ZIZ_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZLABMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZLA_LETZTERUSER" "~"
if joinKuerzel = BMUZLA
searchResult := searchResult "LEFT JOIN ZLE.ZLA_ZEILAB ON ZLA_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZLGBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZLG_LETZTERUSER" "~"
if joinKuerzel = BMUZLG
searchResult := searchResult "LEFT JOIN ZLE.ZLG_ZAHLUNGEN ON ZLG_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZPLBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZPL_LETZTERUSER" "~"
if joinKuerzel = BMUZPL
searchResult := searchResult "LEFT JOIN BUERO.ZPL_ZEITPLANUNG ON ZPL_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZWKBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZWK_LETZTERUSER" "~"
if joinKuerzel = BMUZWK
searchResult := searchResult "LEFT JOIN WWS.ZWK_ZUORDNUNGWVKOPF ON ZWK_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZXFBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZXF_LETZTERUSER" "~"
if joinKuerzel = BMUZXF
searchResult := searchResult "LEFT JOIN BUERO.ZXF_ZEITAENDERUNGFELD ON ZXF_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ZZUBMU
searchResult := searchResult "LEFT JOIN BMD.BMU_BMDUSER ON BMU_BMDUSERID = ZZU_LETZTERUSER" "~"
if joinKuerzel = BMUZZU
searchResult := searchResult "LEFT JOIN FIBU.ZZU_ZINSBERECHNUNG_ZLG_ZU ON ZZU_LETZTERUSER = BMU_BMDUSERID" "~"
if joinKuerzel = ARLVAI
searchResult := searchResult "LEFT JOIN WWS.VAI_VIEW_ARTIKEL_VKINKL ON VAI_FIRMENNR = ARL_FIRMENNR AND VAI_ARTIKELNR = ARL_ARTIKELNR" "~"
if joinKuerzel = VAIARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = VAI_FIRMENNR AND ARL_ARTIKELNR = VAI_ARTIKELNR" "~"
if joinKuerzel = EHTVEG
searchResult := searchResult "LEFT JOIN WWS.VEG_VIEW_ENHTAB_GRUNDPREISENH ON VEG_FIRMENNR = EHT_FIRMENNR AND VEG_EINHEITENTABNR = EHT_EINHEITENTABNR" "~"
if joinKuerzel = VEGEHT
searchResult := searchResult "LEFT JOIN WWS.EHT_EINHEITENTAB ON EHT_FIRMENNR = VEG_FIRMENNR AND EHT_EINHEITENTABNR = VEG_EINHEITENTABNR" "~"
if joinKuerzel = ARLVKT
searchResult := searchResult "LEFT JOIN KORE.VKT_VIEW_KOSTAMM_KTR ON VKT_FIRMENNR = ARL_FIRMENNR AND VKT_KOSTENSTAMMNR = ARL_KOSTENTR_KOSTENSTAMMNR" "~"
if joinKuerzel = VKTARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = VKT_FIRMENNR AND ARL_KOSTENTR_KOSTENSTAMMNR = VKT_KOSTENSTAMMNR" "~"
if joinKuerzel = NSCDOG
searchResult := searchResult "LEFT JOIN BMD.DOG_DATAOWNERGRUPPE ON DOG_DATAOWNERGRUPPENNR = NSC_SHOW_DOG" "~"
if joinKuerzel = DOGNSC
searchResult := searchResult "LEFT JOIN BMD.NSC_NETSCANNER ON NSC_SHOW_DOG = DOG_DATAOWNERGRUPPENNR" "~"
if joinKuerzel = VAAARL
searchResult := searchResult "LEFT JOIN WWS.ARL_ARTIKEL ON ARL_FIRMENNR = VAA_FIRMENNR AND ARL_ARTIKELNR = VAA_ARTIKELNR" "~"
if joinKuerzel = ARLVAA
searchResult := searchResult "LEFT JOIN WWS.VAA_VIEW_LAGSUM_ARLLAG ON VAA_FIRMENNR = ARL_FIRMENNR AND VAA_ARTIKELNR = ARL_ARTIKELNR" "~"



;==========================================================================
;Hier hinzufügen falls normal nach Alphabet sortiert im Fenster!!!!!!!!!!!!
;==========================================================================

if joinKuerzel = MITPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = MIT_MIT1_FIRMENNR AND PER_PERSONENID = MIT_MIT1_MITARBEITERID" "~"
if joinKuerzel = MITPER
searchResult := searchResult "LEFT JOIN BUERO.PER_PERSON ON PER_FIRMENNR = MIT_MIT2_FIRMENNR AND PER_PERSONENID = MIT_MIT2_MITARBEITERID" "~"


;DKZ
if joinKuerzel = V2PEFK
searchResult := searchResult "LEFT JOIN BUERO.DKZ_DOKUMENTZU ON EFK_FIRMENNR = V2P_WWSERF_FIRMENNR AND EFK_ERFASSUNGSKZ = V2P_WWSERF_KZ AND EFK_ERFASSUNGSNR = V2P_WWSERF_NR" "~"

;Views BUERO
if joinKuerzel = DOKDMD
searchResult := searchResult "LEFT JOIN BUERO.DMD_V_DOKMETADATA ON DMD_ARCHIVNR = DOK_ARCHIVNR AND DOK_DOKUMENTENNR = DMD_DOKUMENTENNR" "~"
if joinKuerzel = DKZDMD
searchResult := searchResult "LEFT JOIN BUERO.DMD_V_DOKMETADATA ON DMD_ARCHIVNR = DKZ_ARCHIVNR AND DOK_DOKUMENTENNR = DKZ_DOKUMENTENNR" "~"

;Views WWS
;if joinKuerzel = ARLVAV
;searchResult := searchResult "LEFT JOIN WWS.VAV_VIEW_ARTIKEL_VKEXKL ON VAV_FIRMENNR = ARL_FIRMENNR AND VAV_ARTIKELNR = ARL_ARTIKELNR" "~"
;if joinKuerzel = ARLVJS
;searchResult := searchResult "LEFT JOIN WWS.VJS_VA_VERLEIHJOURNALSUM ON VJS_HAUPTFIRMENNR = ARL_FIRMENNR AND VJS_ARTIKELNR = VJS_ARL_ARTIKELNR" "~"
;if joinKuerzel = ARLVAV
;searchResult := searchResult "LEFT JOIN WWS.VJL_VA_LAGERJOURNAL ON VJL_ARTIKELFIRMENNR = ARL_FIRMENNR AND VJL_ARTIKELNR = ARL_ARTIKELNR" "~"
;if joinKuerzel = ARLVJL
;==========================================================================
;ENDE 
;==========================================================================



FoundPos := RegExMatch(searchResult, "(?<=JOIN )(.*?)(?= )" , Tabelle)
WithoutSchema := SubStr(Tabelle,  InStr(Tabelle, ".")+1)
WrongTable := WithoutSchema "." WithoutSchema
TableKuerzel := SubStr(WithoutSchema,  1,3)
searchResult := StrReplace(searchResult, WrongTable, search(TableKuerzel), lTest)

return searchResult
}
